## dspy-demo

> DSPy demo project with 30+ tutorials for learning DSPy 3.1.0 — a framework for building AI applications through "programming rather than prompting." Uses Python 3.10-3.14, UV package manager, and multiple LLM providers (OpenAI, Anthropic, Google, Groq).

# CLAUDE.md

## Project Overview

DSPy demo project with 30+ tutorials for learning DSPy 3.1.0 — a framework for building AI applications through "programming rather than prompting." Uses Python 3.10-3.14, UV package manager, and multiple LLM providers (OpenAI, Anthropic, Google, Groq).

## Common Commands

```bash
# Dependencies
uv sync                              # Install (creates .venv)
uv sync --dev                        # Install with dev deps
uv lock --upgrade && uv sync         # Update all deps

# Running
uv run jupyter lab                   # Interactive notebooks
uv run python scripts/01_fundamentals/getting_started.py  # Run a script

# Dev tools
uv run pytest                        # Tests
uv run black .                       # Format
uv run isort .                       # Sort imports
uv run mypy .                        # Type check
uv run pre-commit run --all-files    # All pre-commit hooks
```

## Architecture

### Dual-Format Design

Every tutorial exists as both a Jupyter notebook (`notebooks/`) and a Python script (`scripts/`). Both are organized into matching subdirectories:

| Directory | Content |
|---|---|
| `01_fundamentals/` | Core DSPy concepts |
| `02_core_modules/` | Built-in modules (Predict, ChainOfThought, Reasoning) |
| `03_building_programs/` | Building AI programs (RAG, agents) |
| `04_optimization/` | Optimization techniques |
| `05_advanced/` | Multimodal, adapters, usage tracking |
| `06_deployment/` | Production deployment patterns |
| `07_real_world/` | Real-world examples |

**Note:** Old directory names (`01_basics/`, `02_building/`, etc.) still exist for backward compatibility. New work should use the structure above.

### Key Directories

- **`utils/`** — Centralized LM setup (`setup_default_lm()`, `configure_dspy()`), color-coded output helpers (`print_step()`, `print_result()`, `print_error()`), dataset loaders, adapter/usage-tracking helpers. All scripts import from here. See source for full API.
- **`data/`** — Sample JSON datasets. Note: only `qa_dataset.json` is loaded from disk by `utils/datasets.py`; other dataset functions return hardcoded examples.
- **`models/`** — Minimal pre-trained model configs (only `basic_qa/` and `configs/` exist).
- **`docs/LEARNING_PATH.md`** — Structured curriculum for learning DSPy progressively.

### Environment

API keys and config go in `.env` (copy from `.env.example`). The `utils` package auto-loads it via `python-dotenv`. See `.env.example` for all supported variables.

### Standard Script Structure

All scripts follow this pattern: set `sys.path`, load `.env`, call `setup_default_lm()`, configure DSPy, then implement. See any script in `scripts/01_fundamentals/` for a concrete example.

## Development Guidelines

- Create parallel implementations in both `notebooks/` and `scripts/` under the appropriate category
- Use `utils` functions for LM setup and output formatting — don't configure providers directly
- Import sample data from `utils/datasets.py` when possible
- Follow existing naming: lowercase with underscores
- Code style: Black (line length 88), isort, type hints preferred
- Test both notebook and script versions when modifying shared utilities

## DSPy 3.x Features

DSPy 3.1.0 is backward compatible with 2.x — no migration needed. New features (with reference scripts):

- **Reasoning** (`dspy.Reasoning`) — reasoning-capable model support → `scripts/02_core_modules/05_reasoning.py`
- **Multimodal** (`dspy.Image`, `dspy.Audio`) — image/audio in signatures → `scripts/05_advanced/01_multimodal.py`
- **Adapters** (`ChatAdapter`, `JSONAdapter`, `XMLAdapter`) — prompt/output formatting → `scripts/05_advanced/02_adapters.py`
- **Advanced Optimizers** (GEPA, SIMBA, MIPROv2) → `scripts/04_optimization/03_gepa.py`
- **Usage Tracking** — token/cost monitoring via `configure_dspy(track_usage=True)` → `scripts/05_advanced/04_usage_tracking.py`

<!-- REPOWISE:START — Do not edit below this line. Auto-generated by Repowise. -->
## Codebase Intelligence — dspy-demo (Repowise)

This project is indexed by [Repowise](https://repowise.dev) — a codebase intelligence layer.
Last indexed: 2026-04-04. Confidence: 100%.
### Architecture
This repository is a structured **learning and experimentation platform for DSPy** — a framework for programming (rather than prompting) language models. It is organized as a progressive tutorial and example collection, guiding developers from foundational DSPy concepts through increasingly advanced use cases such as question answering, text classification, entity extraction, and financial data analysis. The project is not a production application but rather a **developer education resource and sandbox**, providing runnable scripts, reusable utility modules, sample datasets, and pre-configured model definitions. It is designed to be worked through sequentially, with numbered script directories reflecting a recommended learning path.
### Key Modules
| Module | Purpose | Owner |
|--------|---------|-------|
| `utils` | The utils module is a shared utility library that serves as the foundational sup | — |
| `scripts` | The scripts module is a comprehensive, structured collection of Python example s | — |
### Tech Stack
**Languages:** Python
**Frameworks:** FastAPI, PyTorch, Pydantic

### Hotspots (High Churn)
| File | Churn | 90d Commits | Owner |
|------|-------|-------------|-------|
| `utils/__init__.py` | 98.5th %ile | 1 | Ahmad Zaib |
| `scripts/07_real_world/yahoo_finance_analysis.py` | 97.0th %ile | 1 | Ahmad Zaib |
| `scripts/07_real_world/email_extraction.py` | 95.5th %ile | 1 | Ahmad Zaib |
| `scripts/07_real_world/ai_text_game.py` | 94.0th %ile | 1 | Ahmad Zaib |
| `scripts/06_deployment/streaming.py` | 92.5th %ile | 1 | Ahmad Zaib |

### Repowise MCP Tools

This project has a Repowise MCP server configured. Use these tools to understand the codebase before reading raw source files — they provide richer context including documentation, ownership, history, and architectural decisions.

**Recommended workflow:**

1. Starting a new task? Call `get_overview()` first to understand the architecture.
2. Before reading/modifying files, call `get_context(targets=["path/to/file.py"])` — faster and richer than reading source directly.
3. Before making changes, call `get_risk(targets=["path/to/file.py"])` to understand impact and hotspot status.
4. Don't know where something lives? Call `search_codebase(query="authentication flow")`.
5. Need to understand why code is structured a certain way? Call `get_why(query="why JWT over sessions")`.
6. Need to understand how two modules connect? Call `get_dependency_path(source="src/auth", target="src/db")`.
7. Before cleanup tasks, call `get_dead_code()` to find confirmed unused code.
8. For documentation or diagrams, call `get_architecture_diagram(scope="src/auth")`.

| Tool | When to use |
|------|------------|
| `get_overview()` | First call on unfamiliar codebase or new task |
| `get_context(targets=[...])` | Before reading or modifying specific files/symbols |
| `get_risk(targets=[...])` | Before changing files — assess blast radius |
| `get_why(query="...")` | Before architectural changes — find decisions |
| `search_codebase(query="...")` | Don't know where something lives |
| `get_dependency_path(source=..., target=...)` | Understand connections between modules |
| `get_dead_code()` | Before cleanup or refactoring |
| `get_architecture_diagram(scope=...)` | Need a visual of the structure |

### Codebase Conventions
**Commands:**
- Test: `pytest`
- Typecheck: `mypy .`

<!-- REPOWISE:END -->

---
> Source: [ten24bytes/dspy-demo](https://github.com/ten24bytes/dspy-demo) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-08-16 -->
