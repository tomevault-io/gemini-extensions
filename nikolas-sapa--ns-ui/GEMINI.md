## ns-ui

> Reference for an agent **editing ns-ui itself**. Two other documents may be the one you want:

# AGENTS.md — working inside this repo

Reference for an agent **editing ns-ui itself**. Two other documents may be the one you want:

- Consuming a component in some other project → `https://design.helpmarq.com/llms.txt`, or the
  README.
- Setup, the add-a-component walkthrough, the token rule, and how to open a PR → `CONTRIBUTING.md`.
  That is the shared ground for humans and agents, and it is shorter.

What follows is the detail `CONTRIBUTING.md` deliberately leaves out: the `autoplay`, `useWhen` and
`gate` descriptor schemas, how prop signatures are extracted for `llms.txt`, and the constraints in
the preview driver that are easy to regress. Read the section you need, not the whole file.

## Structure

Each component is a self-contained folder: `registry/{core,loud}/<name>/` with:

- `component.tsx` — the actual component. `"use client"`, zero or minimal deps, tokens not hex.
- `demo.tsx` — the preview render used by `/preview/<name>` and the screenshot verifier.
- `meta.json` — the single source of truth: `name`, `title`, `description`, `collection`, `tags`,
  `instruction` (a rich paragraph describing behavior — read by `scripts/build-llms.ts`), `dependencies`,
  the optional site-only `autoplay` descriptor (see below) that makes the landing-page card
  demonstrate itself, and the optional site-only `useWhen` override (see below) for llms.txt's
  "use when" line.
- `screenshots/` — generated by `npm run verify`, not hand-authored.

`core` = Geist-dark restraint (169 components). `loud` = deliberately flashy showcase (28). Collection
is decided at creation time and drives folder placement, not a runtime flag.

## meta.json is the source of truth — never hand-edit generated files

`registry.json` (root) and `public/r/<name>.json` are generated from `meta.json` sidecars by
`npm run registry:build` (`scripts/build-registry.ts`). `public/llms.txt` and `public/llms-full.txt`
are generated the same way by `scripts/build-llms.ts` (chained into the same `registry:build` script).

Never hand-edit `registry.json`, anything under `public/r/`, or `public/llms*.txt` — edits will be
silently overwritten on the next build. If something in the generated output is wrong, fix
`meta.json` (or the component source, for prop signatures) and rebuild.

`public/r/` is gitignored — it's regenerated at build time (locally and on Vercel). `public/llms.txt`
/ `public/llms-full.txt` are NOT gitignored; they're small enough to commit, and get regenerated on
every build anyway so a stale commit self-heals on the next deploy.

## Autoplay — how a card demonstrates itself

Landing-page cards are iframes onto `/preview/<name>?embed=1&autoplay=1`, scaled down and
pointer-events:none. Most of the registry only wakes on input, so without help three quarters of
the grid showed a still frame (measured: 28 components at exactly 0.00% pixel change at rest).

A component declares **what kind of input demonstrates it** under an optional `autoplay` key in its
`meta.json`, and one shared driver (`app/preview/[name]/autoplay-driver.tsx`) synthesises that
input. There are no per-component animations — if a component needs something the five modes below
can't express, extend the driver, don't special-case the component.

**Where it runs:** only when *both* `embed=1` and `autoplay=1` are present. `/preview/<name>` with
no params is the honest reference — fully interactive, zero synthetic input — and must stay that
way. No descriptor ⇒ nothing runs (still frame, never a crash). `prefers-reduced-motion: reduce`
⇒ nothing runs.

**Rebuild after editing:** the descriptors are collected into `lib/autoplay.generated.json` by
`scripts/build-autoplay.ts`. Editing `meta.json` alone changes nothing on screen — run
`npm run autoplay:build` (or `npm run registry:build`, which chains it). A malformed descriptor
fails that script loudly rather than shipping.

Autoplay is **site-only** metadata: `build-registry.ts` and `build-llms.ts` read named meta fields,
so the key never reaches `registry.json`, `public/r/*.json` or `llms*.txt`. Keep it that way — it
describes the card, not the component's API, and a consumer must never see it as a prop.

### Descriptor schema

```jsonc
"autoplay": {
  "mode": "pointer-path" | "scroll" | "press" | "drag" | "type" | "none",  // required
  "period": 4000,   // ms for one full cycle including its rest beat. default 4000, min 400
  "delay": 600,     // ms after mount before the first cycle. default 600
  "target": "button" // CSS selector, queried inside the demo root. see per-mode meaning below
}
```

`target` never throws: matching nothing (or omitting it) falls back to the demo root. Coordinates
are normalized to the resolved box, `[0,0]` = its top-left. Absent key, or `"mode": "none"`, means
"genuinely ambient, needs nothing" — no driver mounts.

| mode | extra keys | what it does |
|---|---|---|
| `pointer-path` | `path`: `"sweep"` \| `"orbit"` \| `"figure8"` (default `sweep`)<br>`inset`: −0.45…0.45 (default 0.12) | Moves a synthetic pointer over the target box on a loop, with the full `pointerenter`/`over`/`move`/`out`/`leave` sequence (plus `mouse*` mirrors) and correct `clientX/Y`. `sweep` = left→right→left at the box's vertical centre; `orbit` = one ellipse per cycle; `figure8` = a lissajous. 86% of the period moves, the rest is a pointer-out beat so "settles back on leave" is visible. Wakes proximity, hover, magnetic and cursor-reactive components — the largest group. |
| `scroll` | `scroller`: CSS selector (default: the embed document)<br>`range`: `[from, to]` fractions (default `[0, 1]`) | Eases the scroll position from `from`→`to`→`from` across one period. Contained to the iframe; the host page never moves. |
| `press` | `hold`: ms held down (default 900) | `pointerenter` → `pointerdown` on `target` (default `button`) → hold → `pointerup` + `click`, then rests for `period − hold`. `target` is re-queried each cycle, so a demo that remounts its control after confirming still works. Set `hold` *above* the component's own threshold (hold-to-confirm's `holdMs` is 1200, so it uses 1500). |
| `drag` | `from`, `to`: normalized `[x, y]` (defaults `[0.2,0.5]` → `[0.8,0.5]`)<br>`inset` (default 0) | `pointerdown` at `from`, eased move to `to` and back, `pointerup`. For sliders, split/compare handles, draggable stages. |
| `type` | `text`: string to type (**required**)<br>`cps`: characters/sec (default 3; use 2 when the per-key motion is the point)<br>`hold`: ms the full string rests before it clears and loops (default 1600)<br>`target`: input selector (default `input, textarea, [contenteditable]`) | Types `text` into the target, one real key sequence per character — cancelable `keydown`, then (only if the component did **not** `preventDefault` it) `beforeinput` + native-setter value write + `input`, then `keyup`. That fork drives both keyboard-first controls that own `keydown` (cipher-reel-otp) and ordinary controlled inputs. When `target` matches **several** elements (a per-box OTP), character k goes to match k — the demo is `inert`, so the component's own focus-advance is a no-op and every key would otherwise pile into box 1. Wakes anything that only animates on keystroke. Note: a demo that self-completes on the typed value (cipher-reel-otp verifies on `481632` and disables) types in once and rests, replaying when the card remounts, rather than looping. |
| `none` | — | Explicitly ambient. Same effect as omitting the key; use it to record that the question was asked. |

For `pointer-path` and `drag`, the box is the **union of every element matching `target`** — the
demo root is usually `min-h-screen`, so its centre is nowhere near the thing worth showing.
`"target": "button"` over magnetic-dock's nine items unions to exactly the dock row. Negative
`inset` overshoots the box so a sweep enters and leaves it. For `press`, only the first match is used.

### Driver constraints (don't regress these)

- **`inert` stays on in autoplay mode.** It is what stops a demo's mount-time `focus()` from
  scrolling the *host* page (~1000px, fixed once already). `inert` removes the subtree from hit
  testing — `document.elementFromPoint()` over an inert demo returns `<body>` (measured) — so the
  driver does its own hit test and dispatches straight to the element. Programmatic `dispatchEvent`
  is unaffected by `inert`. Never "just drop inert" to make events land.
- One rAF loop per frame, pointer work throttled to ~30Hz, paused on `visibilitychange`. Offscreen
  cards are already torn down by the showcase's mount manager (`MOUNT_CAP = 12`), which unmounts
  the iframe entirely — that, not an IntersectionObserver inside the frame, is what bounds cost.
  An IO inside the iframe cannot see host scroll and would always report "visible".
- `Element.prototype.{set,release}PointerCapture` is wrapped in a try/catch for the driver's
  lifetime and restored on cleanup: a synthetic `pointerId` matches no live pointer, and a throw
  inside a component's `pointerdown` handler would abort it mid-way.

## `useWhen` — disambiguating llms.txt when components share a role

`build-llms.ts`'s "use when" line is normally *derived* from `tags` (see `deriveUseWhen` /
`FUNCTIONAL_TAGS` in that script): the first functional tag names the UI role, the rest become a
parenthetical of flavor tags. That's fine for a singleton, but when two or more components share a
role (e.g. three canvas/webgl heroes), tag-derivation gives every one of them the same generic
line and a flavor-tag dump that reads as a spec sheet, not a decision — an agent asked for "a hero
where the background reacts to the cursor" can't tell them apart.

Where that happens, set an optional `useWhen` string in the clustered components' `meta.json`.
`deriveUseWhen` prefers it over tag-derivation when present. Write it by hand from that
component's `instruction` field (already the richest description of its real behavior) — name the
concrete distinguishing behavior and the situation it suits, in roughly one sentence, and say when
a sibling is the better pick instead of just describing yourself in isolation. Only add it once a
component turns out to share a role with something else; don't pre-emptively hand-write one for a
component with no ambiguity to resolve — tag-derivation is fine there and won't rot.

`useWhen` is site-only, like `autoplay`: `build-registry.ts` never reads it, so it can't leak into
`registry.json` or `public/r/*.json` — a consumer of the component must never see it as a prop.

## The token/theme rule (hard constraint, not a suggestion)

Colors come from CSS custom properties already in scope — `--background --foreground --muted
--border --accent` — never hardcoded hex, in component markup OR in canvas/SVG draw code. This
broke the light theme across the whole registry once already. If a component derives ink for a
`<canvas>`, read it via `getComputedStyle` at mount/theme-change, don't bake in a color literal.
Both light and dark themes must render correctly — verify both, not just whichever your terminal
defaults to.

## Build & verify gate

```bash
npm run registry:build   # meta.json -> registry.json, public/r/*.json, public/llms*.txt
npm run typecheck        # tsc --noEmit, must be clean
npm run verify           # requires dev server running; screenshots every component x theme x state,
                          # hard-fails on console errors, blank renders, or malformed meta.json
```

A component (or a change to one) isn't done until `npm run verify` passes and the idle/default-state
screenshot has actually been looked at — that's the state the owner judges first and hardest.

### What the gate actually checks

Per component × theme: console errors, blank render, hover differs from default, keyboard focus
differs from an unfocused baseline, dark and light not byte-identical. Plus, once per component:
a DOM/ARIA audit derived from the rendered markup — every exposed, non-disabled interactive control
must have an accessible name; `role=switch|checkbox|radio` must carry `aria-checked`; a visible
dialog must have an accessible name; and if the component renders any control at all, Tab must
reach something. Display-only components (skeletons, charts, canvas heroes) render no controls and
are skipped by that last rule rather than fudged past it. `aria-hidden` subtrees are exempt — a
visually hidden proxy `<input type=file>` behind a real button is correct, not a finding.

Per-element tabbability is deliberately *not* asserted: roving-tabindex chips and spinner buttons
whose `<input>` owns the keyboard are legitimate and would false-fail.

### `gate` — verifying the open/expanded/armed state

By default the gate only ever sees the **resting** state. That is how a popover clipped invisible by
an ancestor's `overflow-hidden` shipped green. A component with a characteristic non-resting state
declares how to reach it under an optional `gate` key in its `meta.json`:

```jsonc
"gate": {
  "openBy": "button[aria-haspopup=menu]",  // CSS selector: click this (first match)
  "expect": "[role=menuitem]"              // CSS selector: this must then be visible AND hittable
}
```

The gate clicks `openBy`, waits, then requires `expect` to be genuinely on screen: non-zero box, not
`visibility:hidden`/transparent, and `document.elementFromPoint()` at its centre resolving to it or
something inside it. A non-zero box alone proves nothing — that is exactly what an ancestor's
`overflow:hidden` leaves behind. It also writes a `<theme>-open.png` screenshot, so the open state
is finally something the owner can look at.

**Point `expect` at the thing that must be seen, not at a wrapper.** drape-menu's `[role=menu]`
container measures 0×0 (its items are absolutely positioned), so `expect` there is `[role=menuitem]`.
Both keys are required if the key is present; the key is optional, and a component without one is
verified exactly as before.

## Prop-signature extraction (`scripts/build-llms.ts`)

`build-llms.ts` scans each `component.tsx` for its exported prop shape using bracket-balanced text
scanning (no AST/parser dependency, matching `build-registry.ts`'s plain-node style). It recognizes:
named `export interface FooProps` / `export type FooProps`, inline destructured object types on
`export function Foo({ ... }: { ... })`, and `forwardRef<Handle, Props>(...)`. If you introduce a
prop-shape pattern the scanner doesn't recognize, it will print a loud `WARNING` naming the
component and emit `props: (unresolved — read the source)` in the output rather than silently
dropping the component's props — if you see that warning, either simplify the prop declaration to
one of the recognized shapes or extend the scanner, don't ignore it.

## Generated files and parallel work

These files are **generated and not committed**. Never hand-edit them; never add them
to a commit:

| File | Generated by |
|---|---|
| `registry/index.tsx` | `scripts/build-index.ts` — from the folder structure |
| `registry.json` | `scripts/build-registry.ts` — from `meta.json` sidecars |
| `public/r/*.json` | `shadcn build` |
| `public/llms.txt`, `public/llms-full.txt` | `scripts/build-llms.ts` |
| `lib/autoplay.generated.json`, `lib/card-frame.generated.json` | `scripts/build-autoplay.ts` |

All of them come from one command: `npm run registry:build`. It runs automatically
before `dev`, `typecheck`, `verify` and `build`, so a fresh clone needs no extra step.

**Why they aren't committed:** every branch that adds a component regenerates all of
them, so committing them means guaranteed merge conflicts in files nobody hand-wrote.
`registry/index.tsx` was the worst offender — it was hand-maintained with one line per
component, so two branches adding components always collided, and an early breeding
round hit a real concurrent-write race there.

**Adding a component** is therefore just: create `registry/<collection>/<name>/` with
`component.tsx`, `demo.tsx`, `meta.json`, then run `npm run registry:build`.
Registration is automatic — there is no longer a central file to edit, and no reason
for two parallel branches to touch the same file.

**Working in parallel:** use a git worktree on its own branch, and pick an unused port
(`npx next start -p 34xx`). Always verify against a **production** build, never a dev
server: with several agents hitting one dev server, Turbopack serves corrupted chunks
on 15-50% of loads, the page never hydrates, and components render at their unhydrated
JSX defaults — which looks exactly like a catastrophic component bug. Measured 0/12
failures against a production build versus 15-50% on dev.

---
> Source: [nikolas-sapa/ns-ui](https://github.com/nikolas-sapa/ns-ui) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-07-29 -->
