## voice-studio

> This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Voice Studio by MSR — a fully-offline, local web UI for **multiple open-source TTS engines** (VibeVoice-1.5B, Kokoro-82M, Kitten TTS Mini, Chatterbox Multilingual V3, OmniVoice, VoxCPM2, Qwen3-TTS CustomVoice) plus Whisper speech-to-text. FastAPI backend serves the models; React + Vite frontend is a multi-segment podcast editor. Only one engine is loaded at a time to keep memory low.

## Commands

Primary entry point (cross-platform, from repo root):
```bash
python studio.py setup            # one-time: venv, deps, PyTorch/CUDA auto-detect, system-dep checks, model picker
python studio.py start            # run backend + frontend together (auto dev/prod)
python studio.py start --dev      # force dev (uvicorn :8880 + Vite :5173, hot reload)
python studio.py start --prod     # force prod (single server :8880 serving UI + API)
python studio.py models           # re-open the interactive model picker
```
`studio.py` is stdlib-only and bootstraps `backend/venv`; it forwards `start` flags (`--device`, `--port`, …) to `backend.cli`. The raw commands below are the underlying primitives.

Backend (run from repo root; package is `backend`):
```bash
python -m backend.cli --engine vibevoice --device cuda   # start server :8880; --device cpu|mps|auto
python -m backend.cli --help                             # all flags (--engine, --model, --kokoro-lang, --chatterbox-lang, --models-dir, --port)
cd backend && python -m pytest tests/                    # smoke tests (stubbed model, no weights needed, ~seconds)
cd backend && python -m pytest tests/test_smoke.py::<name>   # single test
```

Frontend (from `frontend/`):
```bash
npm run dev        # Vite :5173, proxies /api/* → :8880 (no CORS setup needed)
npm run typecheck  # tsc -b --noEmit
npm run build      # tsc -b && vite build
```

Two terminals: one backend, one frontend. First backend boot downloads model weights to `backend/models/` (VibeVoice ~5.4 GB). On Windows, install a CUDA PyTorch wheel *before* `pip install -r backend/requirements.txt` or CUDA silently falls back to CPU.

## Architecture

The backend is organized around a pluggable **engine abstraction**. Understanding it requires reading across `core/engines/`, `core/engine_manager.py`, and `services/synthesize.py`:

- **`core/engines/__init__.py`** — the `Engine` ABC. Each engine (`vibevoice_engine.py`, `kokoro_engine.py`, `chatterbox_engine.py`) is a self-contained model+processor implementing `load/unload/is_loaded/synthesize/...`. Engines are constructed at startup but loaded **lazily** on first use. `EngineSynthRequest`/`EngineResult` are the engine-agnostic I/O dataclasses; engines ignore fields they don't understand (e.g. Kokoro ignores `reference_audio`; only Chatterbox uses `cfg_weight`/`exaggeration`/`language_id`).
- **`core/engine_manager.py`** — `EngineManager` owns the engine registry and tracks the single active engine. Switching engines unloads the previous one. The active choice is persisted to `backend/.last_engine` and restored on restart. **Register new engines here** (the dict order drives the UI selector).
- **Some engines run out-of-process in isolated venvs.** Four engines can't share the main venv because of hard dependency pins: `chatterbox-tts` pins `transformers==5.2.0`, **OmniVoice** pins `transformers>=5.3.0` + `torch 2.8`, **VoxCPM** needs `torch>=2.5` / CUDA>=12 plus a heavy dependency tail (funasr, modelscope, datasets, gradio), and **Qwen3-TTS CustomVoice** pins `transformers==4.57.3` (via `qwen-tts`) — all incompatible with VibeVoice's `transformers==4.51.3` (and with each other). So `core/engines/chatterbox_engine.py`, `core/engines/omnivoice_engine.py`, `core/engines/voxcpm_engine.py`, and `core/engines/qwen_engine.py` are **proxies**: each keeps the normal `Engine` interface but drives a worker (`backend/chatterbox_worker.py` / `backend/omnivoice_worker.py` / `backend/voxcpm_worker.py` / `backend/qwen_worker.py`) running in its own venv (`backend/venv-chatterbox` / `backend/venv-omnivoice` / `backend/venv-voxcpm` / `backend/venv-qwen`, built on demand by `studio.py`). They talk newline-delimited JSON over stdin/stdout (stderr drained on a thread to avoid pipe deadlock; the worker fd-redirects its own stdout so model-load noise can't corrupt the protocol); audio passes as temp WAV paths. Each engine's deps live in `requirements-chatterbox.txt` / `requirements-omnivoice.txt` / `requirements-voxcpm.txt` / `requirements-qwen.txt`, never the main `requirements.txt`; `studio.py` installs the package first, then force-reinstalls a CUDA-matched torch wheel (Chatterbox: cu124/cu118 for torch 2.6; OmniVoice: cu128/cu126 for torch 2.8, via `envdetect.detect_omnivoice_cuda_tag`; VoxCPM: cu126/cu128 via `envdetect.detect_voxcpm_cuda_tag`; Qwen: cu126/cu128 via `envdetect.detect_qwen_cuda_tag`). VoxCPM is **Python 3.10–3.12 only** (torchcodec/funasr); `studio.py`'s `_ensure_voxcpm_env` guards this. Qwen has no Python-version guard (`qwen-tts` allows 3.9–3.13). **Venvs are provisioned with `uv`** (installed into `backend/venv` via pip on first use) rather than `python -m venv`+`pip`: a generic builder (`_ensure_engine_env_uv`) runs `uv venv --python <sys.executable>` + `uv pip install`, hardlinking packages from a shared repo-local cache (`backend/.uv-cache`, forced onto the venvs' volume so hardlinks work). Identical builds are then stored once — the `2.11.0+cu128` torch shared by OmniVoice/VoxCPM/Qwen, and (after CUDA-tag alignment, see below) the `2.6.0` torch shared by the main venv + Chatterbox — cutting total venv disk ~27.5 GB → ~13 GB. If uv can't be installed, each engine falls back to the retained pip path (`_ensure_*_env_pip`). A `.{engine}-ready` marker (written last) gates `installed()` so an interrupted install never looks complete. The **install flow is generic over installable engines**: `app.state.engine_installers` is a registry of `EngineEnvInstaller(subcommand)` (`backend/services/chatterbox_install.py`; `ChatterboxInstaller` is a thin alias), `POST/GET /api/engines/{name}/install` runs `studio.py install-{name}` and streams the live log, and the frontend `InstallEngineDialog` (parametrized by engine) drives it. Each engine's `info()` carries an `installed` flag (isolated engines = their venv marker exists). OmniVoice and VoxCPM expose **per-speaker voice modes** via two capability flags on `EngineInfo` — `supports_voice_modes` (mode toggle) and `supports_style_clone` (style works alongside a reference clip). OmniVoice has three modes — clone (reference clip), design (a free-text `instruct` attribute prompt, from the fixed `DESIGN_CHIPS` vocabulary), and auto (no prompt) — with `supports_style_clone=false` (design prompt only without a reference); VoxCPM sets both flags true and has five — auto / design (inline `(style)` prefix, no reference) / clone / controllable clone (clone + inline style) / ultimate clone (reference + `prompt_text` transcript) — using free-text design/style (no `DESIGN_CHIPS`). The worker composes the inline `(style)` prefix; CFG maps to `cfg_value`, and an inference-timesteps "Quality" control (Fast/Balanced/High = 5/10/25) rides the existing `inference_steps` field. The mode + optional `instruct` ride **per-speaker** through the synth request (`SynthSpeaker.voice_mode`/`instruct` → `EngineSynthRequest` → the proxy's mode-dispatching `_build_synth_msg`); design/auto carry no reference voice, so `SynthService` skips voice resolution for them and folds mode/`instruct` into the per-segment + join cache keys so modes/prompts never collide. The toggle is capability-gated in both `SpeakerRoster` (Podcast mode) and `TtsEditor` (Text-to-Voice mode); the per-speaker mode is **derived** in the UI (`omnivoiceMode ?? (voice ? "clone" : "auto")`, see the shared `frontend/src/lib/voiceModes.ts`). **Qwen3-TTS CustomVoice** is different — it's a built-in-voice engine (9 premium voices, Kokoro-style; no cloning, no Clone/Design/Auto modes) with a third capability flag `supports_style_prompt`: an **always-available** free-text style field (no mode toggle, no voice gating) that rides the same per-speaker `voiceDesign` → `instruct` field, gated in both `SpeakerRoster` and `TtsEditor`. It exposes 10 languages + Auto via `language_id`, and a Qwen-only **Advanced generation** panel in `ControlPanel` (temperature / top_p / top_k / repetition_penalty / seed) whose params thread through `SynthRequestBody` → `SynthService` (both single- and multi-speaker `EngineSynthRequest` paths) → `qwen_engine._build_synth_msg` → the worker's `generate_custom_voice`. Those params fold into the per-segment + join cache keys (backend `qwen_gen`/`|qg=`; frontend `qwenGenSig`/`CachedAudio.genSig`) so changing any one re-synthesizes. Qwen's CFG slider is a no-op (`engineHints.ts` `QWEN_HINTS` points users to the Advanced panel).
- **Model weights download with progress (in-process engines).** Each engine's `info()` also carries a `downloaded` flag — `True` by default, but VibeVoice/Kokoro override `Engine.downloaded()` to probe the local HF cache via `core/model_cache.py::model_downloaded` (a network-free `snapshot_download(local_files_only=True)`). When the UI sees `downloaded:false` it offers a **Download** button instead of **Switch**: `POST /api/engines/{name}/download` drives `backend/services/model_download.py::ModelDownloader` (single-flight daemon thread running `snapshot_download` with a custom `tqdm` subclass that aggregates byte deltas; total size pre-fetched via `HfApi().model_info`), and the `DownloadModelDialog` polls `GET /api/engines/{name}/download` for a live `{percent, speed_bps, eta_sec, log, …}` snapshot. `vibevoice`/`kokoro`/`kitten`/`omnivoice`/`voxcpm`/`qwen` are downloadable here (the endpoints 400 otherwise). **Kitten TTS Mini** (`core/engines/kitten_engine.py`, `KittenML/kitten-tts-mini-0.8`) is an ~80M **ONNX** StyleTTS2 model that runs **in-process in the main venv** like Kokoro — its only non-shared dep is `onnxruntime` (it reuses Kokoro's misaki/espeak/spacy stack), so no isolated worker. Built-in-voice engine (8 English voices, no cloning); the model exposes raw ids `expr-voice-{2..5}-{m,f}` but ships friendly aliases in its `config.json` (`Bella`/`Jasper`/…) which the engine surfaces as the display name while keeping the raw id for the model call. `generate()` **silently degrades** on an unknown voice rather than raising, so `synthesize()` validates against the known set first. In `MODEL_CATALOG` + `DOWNLOADABLE` + `DELETABLE`. OmniVoice runs in an isolated worker but its weights live in the same shared HF cache, so the main-process downloader pre-fetches the `k2-fsa/OmniVoice` repo with a progress bar (the ~11 MB Qwen tokenizer slice still streams on first worker load). VoxCPM is the same — its `openbmb/VoxCPM2` model (2B, 48 kHz, 30 languages) lives in the shared HF cache (added to `MODEL_CATALOG` + `DOWNLOADABLE`), so it pre-fetches in-process despite running in its own worker. Qwen is the same — its `Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice` model (1.7B, ~24 kHz) lives in the shared HF cache (also in `MODEL_CATALOG` + `DOWNLOADABLE` + `DELETABLE` + `UNINSTALLABLE`), pre-fetched in-process. Chatterbox keeps its separate venv-install flow and lazy in-worker weight download.
- **`services/synthesize.py`** — `SynthService` is everything engine-*agnostic*: input validation, normalizing free text into canonical `Speaker N:` scripts, per-segment disk cache lookup/write, and **thread serialization** (a single `threading.Lock` + single-worker `ThreadPoolExecutor` so concurrent requests don't fight over the GPU). Multi-speaker scripts are split per-line and synthesized as separate calls, then PCM-concatenated with silence gaps. Cache keys fold engine name + cfg/exaggeration/language knobs in, so cross-engine or cross-knob results never collide. Voices carry an optional `reference_transcript` (persisted in `voices.json`, editable in the voice meta dialog) that drives VoxCPM ultimate cloning; `SynthService` resolves it into `EngineSynthRequest.reference_text` and folds it — plus the quality timesteps — into the per-segment cache key. The `ModelDownloader` thread is independent of this lock/executor, so weight downloads never block synthesis.
- **Speech-to-text (ASR) is a separate abstraction, not an `Engine`.** `core/asr/__init__.py` defines the `AsrEngine` ABC (`load/unload/is_loaded/downloaded/languages/transcribe`) with `AsrRequest`/`AsrResult`/`AsrSegment`. It is deliberately **not** a `core.engines.Engine`: that ABC is text→audio, so folding ASR in would force every speech engine to grow methods it can't implement, and would surface Whisper in the TTS engine selector. `core/asr/whisper_engine.py` runs **`openai/whisper-large-v3-turbo` in-process in the main venv** (transformers 4.51.3 ships `WhisperForConditionalGeneration`) — no worker, no isolated venv. 809 M params, 16 kHz mono input, ~1.56 GB VRAM in fp16, ~13–16× realtime, 99 languages. Whisper's attention window is 30 s, so long-form audio rides the transformers ASR `pipeline` (`chunk_length_s=30, stride_length_s=5`), which returns `chunks` with `(start, end)` spans; `_chunks_to_segments` drops the trailing chunk whose `end` is `None`. **Two version landmines:** `generate(..., return_language=True)` is *not* a valid kwarg on 4.51.3 (raises `ValueError`) — use `model.detect_language()`; and `languages()` reads transformers' static `LANGUAGES` map rather than the loaded model's `generation_config.lang_to_id`, so `/api/asr/status` can populate the UI picker **before** the lazy first load. `services/transcribe.py::AsrService` owns everything engine-agnostic — extension/size/duration validation, decode-to-16 kHz-mono via librosa, and an `AsrCache` (`services/asr_cache.py`, one JSON per hash) keyed on **audio content hash + language + timestamps flag**. `api/asr.py` exposes `POST /api/asr/transcribe` (takes **either** an uploaded `file` **or** a `cache_hash` naming audio already in `SynthCache` — exactly one, else 422) and `GET /api/asr/status`. `POST /api/voices/{voice_id}/transcribe` (in `api/voices.py`) transcribes a stored reference voice and **returns the text without persisting it** — the user reviews it in `VoiceMetaDialog`'s "Transcribe" button and saves via `POST /api/voices/{id}/meta`. This auto-fills VoxCPM's `reference_transcript`. **Weight downloads reuse the engine route**: `api/engines.py`'s `/{name}/download` validates against `DOWNLOADABLE`, *not* the engine registry, so adding `whisper` to `MODEL_CATALOG` + `DOWNLOADABLE` makes `/api/engines/whisper/download` work with zero new code. Whisper is constructed at startup but **never registered in `EngineManager`** (a test asserts it stays out of `/api/engines`). It *is* in `DELETABLE`, so its weights are reclaimable — `ModelDeleter` takes an `asr_service` and resolves the live model via `_live_engine()` (ASR service first, then the TTS registry), because it must be unloaded before `rmtree` or Windows refuses to remove open files. `GET /api/asr/status` also returns `name`/`display_name`/`description`/`license`/`model_url`, which `EngineSelector` renders as a **Speech-to-text card with no Switch button** — the popup doubles as the model manager, and without the card there'd be no way to delete Whisper's 1.6 GB.
- **Voice-to-voice dubbing reuses ASR + Synth — no new model.** `services/dub.py::DubService` re-voices a transcript's segments in one chosen voice: it synthesizes each (user-edited) segment through `SynthService` (`SynthRequest`/`Speaker`), then `_reconstruct_timeline` lays the takes back on the original timeline — leading silence = the first segment's start, inter-segment silence = the original pause (`start_i − end_{i−1}`, clamped ≥ 0), and each take at its **natural length** (never time-stretched). Per-segment synthesis holds only for **clone** (and engines without voice modes): the fixed reference voice keeps every take consistent. In **design/auto** modes (OmniVoice/VoxCPM, `voice_mode` design/auto) there's no reference to anchor consistency, so a per-segment synth would invent a different voice per sentence and drift — DubService instead synthesizes the **whole transcript in one call** (whitespace-collapsed so `SynthService` sees a single utterance), keeping the leading offset but not the inter-segment pauses. It also carries `voice_mode`/`instruct` per `Speaker` (SynthService does the per-engine enforcement) and folds both into the cache key; the target voice is required only for clone. All segments share one engine ⇒ one sample rate, so silence is generated at that rate. PCM↔WAV go through the extracted `services/audio_pcm.py` (`pcm16_from_wav`/`pcm16_to_wav`, moved out of `api/download.py` so a service never imports from an `api` module). Results cache in their own `<cache_dir>/dub/` via `services/dub_cache.py::DubCache` — a small, reliable WAV+JSON-sidecar store, **not** `JoinCache` (which stores under `downloads/` but delegates reads to `SynthCache.get`, whose meta-file check looks in the cache **root**, so a freshly-put join entry never re-hits within a process). `api/dub.py` exposes `POST /api/dub` (schema `DubRequestBody` in `api/schemas.py`, dep `get_dub_service` in `api/deps.py`) returning `audio/wav` with `X-Sample-Rate`/`X-Audio-Duration-Sec`/`X-Cache`/`X-Cache-Hash`; `app.py` wires `DubService(synth=synth_service, cache=DubCache(...))`. v1 scope is same-language, audio→audio, natural/loose timing, single voice, whole-track — cross-language (translation), video, diarization, background separation, and strict time-stretch are deliberate v2 hooks.
- **Machine translation is a third abstraction (text→text), like ASR.** `core/translate/__init__.py` defines the `Translator` ABC (`load/unload/is_loaded/translate/downloaded/languages/needs_source_lang`) with `TranslateRequest`/`TranslateResult`. One **in-process** implementation in the main venv (plain `transformers` seq2seq, no isolated env): `core/translate/m2m100_translator.py` (`M2M100ForConditionalGeneration`, MIT, ~100 langs, `needs_source_lang=True`, sets `tokenizer.src_lang` + `forced_bos_token_id`) whose **identity is parameterized** so the **418M** (`facebook/m2m100_418M`, key `m2m100`) and **1.2B** (`facebook/m2m100_1.2B`, key `m2m100_large`) checkpoints share the class. (MADLAD-400 3B was evaluated and dropped: its transformers download is ~12 GB — too heavy for a "medium" tier; Tencent's Hy-MT2 is Apache-2.0 and better quality but needs `transformers>=5.6` + `trust_remote_code`, i.e. an isolated worker, deferred. NLLB/SeamlessM4T are out on CC-BY-NC.) `core/translate/lang_codes.py` holds the static `M2M100_LANGS` list (so the picker populates before download) and `normalize_source` (Whisper→M2M code aliases). `services/translate.py::TranslateService` is engine-agnostic: registry + single **active** model (switch unloads previous, persisted to `backend/.last_translator`), batches segments, runs under the shared `GpuGate`, and caches via `services/translate_cache.py::TranslateCache` (per-hash JSON, folds text+source+target+model). `api/translate.py` exposes `GET /api/translate/status`, `POST /api/translate/activate`, and `POST /api/translate` (segments or texts). Both models are in `MODEL_CATALOG` + `DOWNLOADABLE` + `DELETABLE`, so `/api/engines/{name}/download` works with zero new code; `ModelDeleter` gained a `translate_service` so their weights unload before `rmtree`. Translators are constructed at startup but **never registered in `EngineManager`** (a test asserts they stay out of `/api/engines`), exactly like Whisper. **Dub integration:** `DubService` takes a `translate=` and `dub()` gains `source_language`/`target_language`/`translator`; when a target language is set and differs from the source it translates before synthesis (per-segment for clone, whole-transcript for design/auto) and folds all three into the cache key. **Transcribe integration** is frontend-only (a "Translate to…" control calling `POST /api/translate`). `hf_xet` is a required dep — these repos are Xet-backed and the weights fetch fails without it. **Download plumbing gained two things for this:** catalog entries carry an optional `ignore` glob list (M2M ships a duplicate `rust_model.ot`/tf/flax/gguf; skipping them halves the 1.2B download 9.9→5 GB), threaded through `ModelDownloader` + the CLI + `_repo_total_bytes`; and the downloader now runs `snapshot_download` in a **killable subprocess** so `POST /api/engines/{name}/download/cancel` can abort mid-file (a blocking daemon thread couldn't be interrupted) — `DownloadCancelled` → state `cancelled`. Frontend: a **Translation card** in `ControlPanel` (`useTranslateStatus` hook), a target-language picker in `DubEditor`, the Transcribe control, and a **Cancel button** in `DownloadModelDialog`; the download/delete dialogs check `translateStatus.models` (generic) to refresh translate status (like Whisper). A **third translator** — `core/translate/argos_translator.py` (`ArgosTranslator`, key `argos`) — is the open-source **Argos Translate** engine (the one LibreTranslate wraps), run **in-process** in the main venv via the `argostranslate` lib (no isolated worker; verified conflict-free). It's an alternative to M2M (M2M stays default): 50 languages (`core/translate/argos_langs.py::ARGOS_LANGS`, incl. Urdu) with **per-language packs installed on demand** — `translate()` resolves the needed packs via an English pivot (`required_packages(source,target)` → `{source→en, en→target}`) and installs any missing ones through `argostranslate.package` (`update_package_index`/`get_available_packages`/`AvailablePackage.install()`), into `settings.argos_packages_dir` (`backend/models/argos`, set via the `ARGOS_PACKAGES_DIR` env before argostranslate's settings import). It's **not** in `MODEL_CATALOG`/`DOWNLOADABLE` (no HF repo) and carries a new `Translator.on_demand()` capability (True) surfaced in `/api/translate/status`, which `TranslationCard.tsx` renders as "offline · installs on demand" instead of a Download button.
- **`core/gpu_gate.py`** — `GpuGate` is the single lock + single-worker executor that **every** GPU consumer submits through, so a transcription can never run concurrently with a synthesis. `app.py` builds one and injects it into both `SynthService` and `AsrService`; `SynthService` constructs its own when none is passed, so standalone callers still work. Streaming synthesis needs the gate held *across generator yields*, which a submit+result call can't express, so `GpuGate` also exposes `exclusive()` as a context manager.
- **`app.py`** — FastAPI factory. Wires the singletons (`VoiceRegistry`, `SynthCache`, `JoinCache`, `EngineManager`, `SynthService`) onto `app.state`, merges each engine's built-in voices into the registry, eager-loads the active engine in `lifespan`, and maps `BackendError` → HTTP status. **Critical ordering quirk:** `app.py` and `cli.py` configure the HuggingFace cache dir (`core/hf_paths.py`) *before* importing anything that pulls in transformers/kokoro/huggingface_hub. Keep heavy imports below that call.
- **`api/`** — thin routers (`health`, `engines`, `voices`, `synthesize`, `download`, `cache`, `stream`, `system`). All Pydantic models live in `api/schemas.py`; FastAPI dependencies in `api/deps.py`. `/stream` (WebSocket) is mostly a stub — streaming only exists for engines whose `supports_streaming()` is True. `GET /api/system/stats` (`api/system.py`) is a **stateless** hardware snapshot for the UI status bar — CPU/RAM/disk via `psutil`, cache bytes via `SynthCache.total_size()`, and **VRAM via NVML** (`nvidia-ml-py`, the library `nvidia-smi` itself queries), falling back to `torch.cuda.mem_get_info()` and finally to `null` when there's no CUDA. **Do not use `mem_get_info` as the primary source:** on Windows/WDDM the OS virtualizes VRAM and the CUDA runtime can't see other processes' allocations — measured 6862 MiB used per the driver against 1.18 GiB per `mem_get_info` on the same idle machine. The NVML handle is initialized lazily and cached (`_NVML_STATE`) since the endpoint is polled every 2 s. Each metric is isolated so one failure never 500s the response. It's read on every 2s poll (no background thread), independent of the synth lock.
- **`config.py`** — `pydantic-settings`, sourced from env + `backend/.env` + CLI overrides (CLI flags in `cli.py` map to `Settings` field overrides). Holds per-engine defaults.

- **`studio.py` + `tools/envdetect.py` (repo root)** — the cross-platform launcher. `studio.py` is **stdlib-only** (runs before the venv exists) and orchestrates setup/start/models; `tools/envdetect.py` does CUDA detection → PyTorch wheel-index mapping. `studio.py optimize-venvs` (`--include-main` to also rebuild the base venv) rebuilds existing engine venvs via uv to reclaim disk on an already-installed machine (it removes + rebuilds each; reports the volume's real free-space delta, since logical sizes double-count hardlinks). Chatterbox's CUDA tag is **aligned to the main venv's actual torch build** (`main_venv_torch_tag` reads the `torch-*.dist-info` tag → passed as `preferred_tag` to `_chatterbox_torch_tag`) so its identical `2.6.0` build dedupes with the main venv. Model pre-download lives in `backend/scripts/download_models.py` (catalog + `snapshot_download`). Its `MODEL_CATALOG` is the **single source of truth** for the picker: `studio.py` is stdlib-only and can't import backend code, so `_load_catalog()` shells out to `python -m backend.scripts.download_models --list` (JSON on the last stdout line) and falls back to a short hardcoded list only if that fails. **Never re-introduce a hand-mirrored catalog in `studio.py`** — the old copy silently rotted and never offered OmniVoice/VoxCPM/Qwen/Whisper. Prod mode is enabled by `_mount_frontend()` in `app.py`, which serves `frontend/dist` at `/` when present. Pure helpers for all of this are unit-tested in `backend/tests/test_setup_helpers.py`.

- **Versioning + auto-update.** The repo-root `VERSION` file is the single source of truth (`backend/core/version.py::get_version()`), surfaced via `/api/health` + `/api/config`. `services/update_check.py::UpdateChecker` compares it to the latest GitHub release (`GET /repos/msrbuilds/voice-studio/releases/latest`, cached, failure-tolerant; refreshed on startup via `refresh_async`). Applying an update reuses the engine-install pattern: `services/update_run.py::UpdateRunner` (a clone of `EngineEnvInstaller`) runs `studio.py update --tag <tag>` — which guards for a clean git checkout on the right remote, then `git checkout <tag>` + `pip install -r` + `npm run build` — and streams its log through `POST/GET /api/update`. The frontend `useUpdate` hook + ControlPanel "About" section (also shows the builder credit + MSR social links) + `UpdateDialog` drive it; non-git/dirty installs get a notify-only path (link to the release page). The checker treats a GitHub **404 on `/releases/latest`** (repo has no published releases yet) as a clean "nothing to update to" snapshot (`error=None`), not a failure. **Release process** (automated by `.github/workflows/release.yml`): bump `VERSION`, sync `backend/pyproject.toml` (**not** repo root) + `frontend/package.json` — run `npm version X.Y.Z --no-git-tag-version` in `frontend/` so `package-lock.json` moves too, or the lock silently drifts — commit to `main`, then `git tag vX.Y.Z && git push origin vX.Y.Z`. The workflow verifies the tag matches `VERSION`, then publishes a GitHub Release with auto-generated notes. (`docs/` is gitignored — internal working notes stay local, so the release flow never depends on a committed notes file.) First release: **v0.2.0**.

Frontend: `frontend/src/App.tsx` is the orchestrator (segments, speakers, playback, export). State lives in `lib/store.ts` (`useReducer`). `lib/api.ts` has typed `/api/*` wrappers; engine/voice/config state come from the `hooks/` (`useEngine`, `useVoices`, `useConfig`). The CFG slider is engine-aware — it maps to VibeVoice's `cfg_scale` or Chatterbox's `cfg_weight` depending on the active engine (`lib/engineHints.ts`).

The UI is a **three-column layout** (ElevenLabs-style): `VoiceLibrary` (left, voices only) · adaptive middle (`HardwareStatusBar` + `MiddleToolbar` + content + podcast-only `InlinePlayer`) · `ControlPanel` (right — engine selector, CFG/exaggeration sliders, synthesis-cache panel, theme, device info; persistent + collapsible to a strip via `localStorage` `vs.controlPanel.open`). The **`HardwareStatusBar`** sits at the top of the middle column above the toolbar: a chip row (CPU/RAM/VRAM/Disk/Cache, mini bars, amber ≥75% / red ≥90%) whose chips **divide the width equally** (each chip is `flex-1`, and its mini bar is itself `flex-1` so leftover space is shared rather than pooling at the right; the bar-less Cache chip uses a spacer to match) fed by the `useSystemStats` hook polling `GET /api/system/stats` every 2s. It's collapsible to a thin strip (state in `localStorage` `vs.statusBar.open`); collapsing **pauses polling**. The VRAM chip hides when the backend reports `vram: null` (no CUDA). The middle column adapts to a **user-chosen project mode** (`useProjectMode` hook → `vs.mode`/`vs.tts` in localStorage), NOT the engine — both modes work on every engine since `SynthService` splits multi-speaker scripts per line. **Podcast** mode = `SpeakerRoster` + segment cards; **Text-to-Voice** mode = `TtsEditor` (single textarea + char/word/duration counts via `lib/textStats.ts`, flat active-voice picked in the library). A **third `transcribe` mode** = `TranscribeEditor` (drop an audio file → editable transcript → Copy / "Send to Text-to-Voice" / download `.srt`/`.vtt`) with a left `TranscribeControls` panel (language picker = Auto-detect + Whisper's 99 languages, timestamps toggle, model status). It's fed by the `useAsrStatus` hook (`GET /api/asr/status`) and gates behind a **Download Whisper (1.6 GB)** button when `downloaded` is false. **Careful:** Whisper is not in the engine registry, so `DownloadModelDialog`'s `onDone` must *not* call `setActiveEngine("whisper")` — `App.tsx` special-cases it to just refresh the ASR status. A **fourth `dub` mode** = `DubEditor` (drop an audio clip → transcribe → editable per-segment text → **Generate Dub** → plays + downloads the re-voiced WAV via `api.dub` → `POST /api/dub`); the target voice is picked in the left `VoiceLibrary` (same `selectedVoiceId`/`onSelectVoice` wiring as TTS, backed by `pm.dub.voiceId`), so unlike Transcribe it shows the library, not `TranscribeControls`. It reuses the same Whisper download gate; its dubbed WAV is held in the component (not the project cache), so the toolbar's Subtitles export is hidden in dub mode too. `lib/subtitles.ts` holds pure `segmentsToSrt`/`segmentsToVtt`/`formatTimestamp` (unit-tested); `formatTimestamp` rounds **total** milliseconds once before decomposing, because splitting seconds off first and flooring the remainder loses to binary float (`3.9 - 3 === 0.8999999999999995` → `,899`). A `ModeChooser` shows on first run; a `ModeToggle` switches later (each mode keeps its own buffer: `vs.tts`, `vs.transcribe`, `vs.dub`). A stored `vs.mode` of `"music"` (from the removed mode) no longer parses, so those users fall back to the `ModeChooser` and re-pick. Transcribe and Dub modes show no `SampleMenu` (their input is an audio file, not a script). **Subtitles for generated audio**: `ImportExportMenu` gains a "Subtitles (.srt)" item (hidden in Transcribe mode, disabled until audio exists) — `App.handleExportSubtitles` takes the rendered WAV (the joined podcast via `downloadPodcast`, or the cached single TTS take), uploads it to `/api/asr/transcribe` with `timestamps=true`, and formats the result with `segmentsToSrt`. `buildPodcastPayload` is shared by the WAV and subtitle exports so the per-segment request list is built once. **Language dropdown** (`LanguageSelect`) is engine-driven from backend `EngineInfo.languages` (`Engine.languages()`): Chatterbox = 23 codes as a synth `language_id` param; Kokoro = its 4 voice-group languages used to **filter** the voice list; VibeVoice/OmniVoice = none. `Samples` (`SampleMenu`) is mode-aware — `PODCAST_SAMPLES` vs `TTS_SAMPLES` in `lib/samples.ts`, including native-script اردو / हिन्दी content. **RTL text surfaces** (the TTS textarea, podcast segment cards, the transcript box, and the voice-meta `reference_transcript` field) set `dir` + alignment from `lib/textStats.ts::textDirection`/`isRtlText`, which weighs RTL-vs-Latin characters across the whole string. Prefer it over the native `dir="auto"`, which decides from the *first strong character* — an Urdu sentence opening with an English word would lay out left-to-right.

**Music generation was removed entirely** (PRs #19 and #21). ACE-Step went first (memory-hungry, garbled output), then MusicGen-small replaced it and was itself removed — its audio was unusable. There is no `music` mode, no `MusicEditor`/`MusicControls`, no `/api/music/*` route, no `MusicRequest`/`synthesize_music`, and no `Engine.supports_music()` capability. Re-adding music means rebuilding that seam from scratch; don't assume any of it survives in the tree. (The per-engine HF `IGNORE_PATTERNS` plumbing went with it — it only ever existed to skip MusicGen's duplicate `*.bin` weights.)

## Gotchas

- The README is detailed but predates the multi-engine refactor — its "Project layout" lists `core/config.py`/`core/model.py` paths that have moved. Trust the actual tree (`config.py` at `backend/` root, engines under `core/engines/`).
- `backend/voices/`, `backend/uploads/`, `backend/cache/`, and `backend/models/` are gitignored runtime dirs. Built-in voices are scanned at startup — adding a file to `backend/voices/` requires a restart.
- Concurrent synthesize requests serialize by design. Don't add parallelism inside `SynthService` without a queue upstream.

---
> Source: [msrbuilds/voice-studio](https://github.com/msrbuilds/voice-studio) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-08-12 -->
