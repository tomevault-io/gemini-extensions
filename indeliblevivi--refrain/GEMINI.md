## refrain

> Read `SPEC.md`, `docs/PRODUCT.md`, `docs/ARCHITECTURE.md`, and `docs/ROADMAP.md` before changing programme order or public contracts.

# Refrain agent contract

Read `SPEC.md`, `docs/PRODUCT.md`, `docs/ARCHITECTURE.md`, and `docs/ROADMAP.md` before changing programme order or public contracts.

## Authority and boundaries

- Project naming is `Refrain` / an `air` / `hum`; **Refrain hums an air.** is shared across English and Chinese. `packages/renderer/src/ui-copy.ts` owns interface prose; language is local presentation state, never musical identity. Preserve authored text and exact names; do not invent a Chinese product name or a second localized renderer.
- `air@1-experimental` is the canonical music source for the current development line. AIR@0 remains an exact historical reader and explicit migration input, not a second active grammar. Compiled events, MIDI, WAV, visual layouts, and browser state are projections.
- The current host agent chooses musical expression. Compiler code may normalize notation, expand explicit transforms, enforce ranges and safety, and emit warnings; it must not silently rewrite music for taste.
- Keep the core stateless. Do not add user accounts, OAuth, hosted relationship memory, an official work library, or a hidden composition model.
- Refrain remains self-hosted rather than a Refrain-operated backend. The canonical private OpenAI path is one loopback HTTP origin plus an operator-owned outbound Secure MCP Tunnel; do not expose the origin publicly, copy another product's OAuth issuer, or fork one deployment per host.
- Relationship context remains host-side and may inform the authored air and optional caption. `hum` must not accept, echo, hash, or store private conversation context.
- Production Canvas aesthetics come from the Faye/Selen-approved visual-research line. The active private G candidate is the selective Selen v21 transplant in `packages/renderer`; preserve that source authority and do not redraw the composers from screenshots or restore the retired generic density-lane UI. The soundbench remains disposable functional engineering UI.
- Tier 1 URL and Tier 2 MCP Canvas must import the exact same renderer package and preserve one visual-language contract. A host adapter may wrap it but must not fork product UI.
- Audio never auto-plays in chat/browser surfaces. A user gesture starts playback. Device-authorized autonomous playback is a later, separate integration boundary.
- Sound assets require asset-level provenance. A package license does not determine the license of bundled samples.
- Project-original functional materials use SUL-1.0; authored content and example music use CC BY-NC-SA 4.0. `LICENSING.md` owns the path-level scope; preserve all third-party terms and never call this OSI open source. Changing the selected licenses or their scope requires owner confirmation.

## Canonical paths

- `packages/identity`: the one synchronous browser-safe, cross-runtime SHA-256 primitive for deterministic text identities. Keep domain serializers in their owning packages; do not add a second hash implementation or Node-only crypto to browser-shared code.
- `packages/air-schema`: current AIR@1 source types/parser/diagnostics, exact AIR@0 historical parsing and explicit migration, closed rational/conductor/groove/phrase/technique syntax, and carried vocabulary identity. Version dispatch must remain format-exact; do not coerce an invalid object into another generation.
- `packages/compiler`: versioned expansion, conductor/meter schedule, normalized motif material, phrase/segment projections, explicit technique/tuplet/groove lowering, structural validation, and listening lint. AIR@1 compilation consumes the exact immutable vocabulary carried by its source; installed pack state must never mutate vocabulary implicitly.
- `packages/soundpack`: stable model-facing identity in `instrument-vocabulary.json`; lightweight routing in `sound-catalog.json`; exact implementation/provenance in immutable content-addressed candidate shards under `candidates/sha256`; direct candidate digest pins in `sound-profiles.json`; build-time SFZ and closed versioned synth-family compilers; and strict extension authorities. `vnext.ts` owns the vocabulary-closure / sparse-profile / scene@1 / recipe / palette@1 / binding@1 contracts plus exact ExtensionPack@0 and ExtensionPack@1 `authoring-vocabulary` modules; `pack-manager.ts` owns explicit-root installed/hydrated/shelf/archive-pin state; `proof-packs.ts` assembles the acoustic and synthetic proofs without duplicating candidate authority. `factory.ts` remains shard lookup and exact candidate/profile/palette closure. Do not restore a monolithic implementation manifest, permissive dual reader, legacy profile resolver, or fetch-all default. Historical integrity and current-runtime resolution remain separate for both binding generations. Never move engine defaults back into identity records, conflate validation layers, or create a second gain/ceiling authority.
- `packages/audio-engine`: historical bindings continue through `performance-plan@2-experimental`; historical complete-piece bindings use `performance-plan@3-experimental`; Binding@1 uses `performance-plan@4-experimental`. Plans execute inside one sealed `refrain-execution-bundle@0-experimental` with local compiled authority, typed execution index/checkpoints, and separate first-use `PreparationPlan`. The package owns derived `ResolvedRenderProfile`, candidate/region/candidate-scoped-RR/loop/release resolution, exact required-asset closure, shared byte-count/SHA-256 verification, atomic local projection, bounded browser/worker/Node execution, RenderScene@1 bus DSP, deterministic non-mutating `ListeningReport@0`, native versus explicit audition-matched amplitude policy, and exact RenderReceipt@2/@3. Every sound-starting complete-piece operation must ensure its adapter is ready, rebase active prefetch, and start atomically at its target frame; never implement first-play seek as audible `play()` followed by `seek()`. Adapters execute rather than re-resolve plans. Do not duplicate compiled events inside new plan shapes or silently upgrade a legacy binding. Authored `RenderProfile` input is retired.
- `packages/renderer`: the shared Tier 1/Tier 2 source-to-view implementation, bounded `refrain-structure@1-experimental` whole-form visual facts plus explicit detail windows, `refrain-selection@0-experimental`, exact `refrain-presentation-ref@0-experimental` bytes across inline/session/file/MCP delivery, AIR@1 Receipt@1 relation/embodiment authority, current `refrain-artifact@3-experimental`, and strict synchronous historical `@0`/`@1`/`@2` helpers. `artifact-document.ts` keeps the complete portable save/handoff authority separate from view-only binding selection in browser and MCP; never reconstruct an imported archive from its playback projection. `AirRenderer.tsx` owns React/audio/export/exact-selection effects; `selen-v21-model.ts` is the typed real-data adapter; `SelenV21Canvas.tsx` is the lifecycle-only bridge; and `selen-v21-runtime.ts` is the one Selen product runtime with four distinct composers. Do not create a second React/SVG renderer or let theme geometry leak into the shared semantic model. Whole-piece DOM must remain bounded rather than event-shaped; dense motif treatment may suppress whole-form marks but must retain exact anchors and canonical selection. Versioned identity authorities own canonical source/receipt/evidence serializers; AIR@1 receipt, verification, and embodiment serializers reconstruct the closed field order before hashing, so JSON object insertion order is transport state while array order remains identity-bearing. Shape-only checks remain distinct from integrity. Artifact parsing binds canonical source to its same-generation receipt before validating the exact receipt/binding/projection provenance graph; missing runtime sound must disable exact execution without invalidating canonical continuity or silently rebinding.
- `packages/mcp-server`: strict model-visible authoring schemas, compact truthful success envelopes, stateless `hum`, receipts, stdio/HTTP protocol surfaces, and MCP App wrapper. Stable same-document schema IDs may deduplicate repeated definitions, but they must not loosen input validation; every published local reference must resolve inside its own schema document. AIR@1 model-visible success carries one exact Artifact@3 as the sole source/receipt/binding continuation authority; the compact output envelope does not truncate the returned artifact, and `from.parentArtifact` accepts that complete object unchanged and derives its bound authorities. Do not restore parallel top-level AIR@1 source/receipt/binding copies or require the model to reassemble them. Its production resource surface contains exactly one explicit-version `ui://` App document with self-contained JS/CSS because OpenAI Secure MCP Tunnel transports MCP JSON-RPC but not iframe asset GETs. Keep runtime release identity out of resource discovery: it belongs to `release.json`, OCI labels, `/healthz`, and private tool-result metadata. Do not restore build-derived query pointers, a second release resource, loopback imports, or published sound assets as Canvas repairs. The MCP Canvas is a direct-nodes zero-asset build and must visibly default an omitted new-root AIR@1 binding to exact `f-synthetic-beat@0`; current continuation inherits the exact parent default/sole carried binding and never fills an unbound/ambiguous parent with a root default; an explicit binding always wins, and an explicit sampled binding remains exact but unavailable in that Canvas. Ordinary browser surfaces retain sampled playback. Expanded events remain local and must not enter model-facing structured results.
- `bin/refrain.mjs`: the installable source-tree CLI dispatcher. The formal `mcp stdio` route must load the self-contained production Canvas from the MCP package root regardless of caller working directory; `npm run build:mcp-host` is its explicit prerequisite. It must remain a thin shell over canonical TypeScript entrypoints, preserve the caller's working directory for file arguments, provide local no-auth/no-network `doctor`, and never grow a second compiler, renderer, exporter, pack manager, MCP contract, or hidden composition path.
- `scripts/package-mcp-runtime.mjs`, `deploy/`, and `docs/runbooks/self-host-mcp.md`: the clean-revision runtime-only bundle, per-file release manifest and identity-bearing health/resource contract, loopback container/service contract, optional OpenAI tunnel wrapper, and deployment/acceptance authority. Packaging must refuse dirty source and build from a detached clean worktree. Immutable sound bytes may be reused only through the repo-external content-addressed store owned by `scripts/sound-content-store-root.ts`; every selected object remains subject to exact byte-count/SHA-256 verification and real-time acquisition progress before it is projected into the clean worktree. Do not restore a disposable worktree-local cache or import compiled output from a developer checkout. The image must carry every tracked runtime-bundle root represented by `release.json.files`; production dependencies remain the pinned-lockfile install projection. `deploy/openai-tunnel/refrain-tunnel-preflight` must derive the one loopback MCP target from the same `%d/profile.yaml` credential passed to `tunnel-client`, accept only 2xx or the recognized protocol statuses 400/405/406/415 plus the same-origin exact release identity, and never restore a hard-coded port, 404 acceptance, or a parallel target environment variable. Keep secrets and target-specific tunnel IDs outside Git; one immutable image ID or repository digest is the activation and rollback boundary.
- `apps/presentation/src/first-air.ts` and `App.tsx`: the first-listen root verifies the exact two Artifact@3 works in `examples/demo` through canonical presentation authority and mounts the shared Canvas. `npm run try` is the local newcomer entry; `build:try` prepares only those works’ union of exact samples using `scripts/prepare-demo.ts` and publishes a relative-path `.demo-assets` projection with provenance and notices. It must not add a composer, upload endpoint, or second renderer. Supplied broken presentation links remain errors; file imports preserve exact bindings. Run `npm run test:try` for this entry or its static delivery changes.
- `apps/presentation`: the canonical implementations behind `refrain draft`, `refrain inspect`, local `refrain hum`, `refrain produce`, `refrain open`, `refrain export`, `refrain bindings`, and `refrain packs`, plus the retained npm-script compatibility entries. It owns the CLI-opened inline/expiring-session/file delivery shell; strict versioned AIR/artifact/source/MIDI/native-WAV/profile/scene/palette/binding/render-receipt/asset-closure/provenance exporter; binding discovery; and operator-rooted pack lifecycle. AIR@1 uses Presentation@2, Artifact@3, and export manifest@4; do not silently downgrade it to an AIR@0 receipt or artifact. Local `authoring.ts` delegates silent draft vocabulary and artifact sealing to existing factories/`humV1`; `inspect-air.ts` owns bounded CLI facts, never music or receipt authority. `production.ts` compiles exact-source/base-bound editable settings into new Scene@1/Binding@1 objects while retaining the entire original artifact graph; master materialization uses canonical plan resolution and emits no derived overrides. Browser, offline and production inspection share `audio-engine/src/scene-routing.ts`; do not duplicate route matching. Opening prepares only the selected AIR/binding asset closure in a preview-owned temporary projection and chooses an available loopback port with same-origin session delivery; an empty `assetBaseUrl` is the explicit same-origin sample plane, while `undefined` means no sample plane. Do not restore a fixed-profile `preopen`, accumulating copy path, or Binding@1-to-@0 downgrade. Complete-piece WAV uses bounded two-pass Node block rendering; `--matched-preview` is a distinct listening transform, never the ordinary export default.
- `apps/soundbench`: functional audition and QA harness, not production Canvas.
- `fixtures`: valid, invalid, and negative-listening evidence.
- `docs/TESTING.md`: the concise test-topology and local MCP host command contract. Keep source, Chromium host behavior, clean package candidate, activated runtime, named-host behavior, and owner acceptance as separate evidence levels.

## Verification

Run `npm run check`, `npm run cli:smoke`, `npm run audio:smoke`, `npm run mcp:smoke`, `npm run selection:smoke`, and—after a build—`npm run mcp:http-smoke` for shared contract or CLI changes. Sound-candidate or amplitude/receipt changes additionally require `npm run soundpack:catalog`, explicit selective acquisition with `refrain fetch --candidate <id>` or the retained npm-script selector, `npm run listening:packets -- --out=tmp/g3b-listening-packets`, and `npm run listening:verify`; packet generation is mechanical evidence, never listening acceptance, while the strict verifier must fail if a Faye-accepted candidate/fallback digest, receipt identity, or asset/candidate closure no longer reproduces. Run `npm run export:repro-smoke` for exporter, offline audio, plan, serializer, or receipt changes. Confirm that historical audition-matched decisions reproduce separately from native-gain export. Identity-primitive or serializer changes require fixed-ID and Node/browser-primitive parity tests for `canonicalAirJson`, verification-core JSON, and `receiptIdentityJson`. Inspect candidate required-asset closure and exact digest-bound prior decisions after any candidate-shard or profile hash change. MCP schema/portability changes require `tools/list` size/reference assertions and fresh-process continuation evidence. Parser/compiler changes require focused fixture tests. Audio/UI changes require real browser playback checks in addition to build success.

MCP App resource, host bridge, capability-aware export, selection return, or renderer lifecycle changes additionally require `npm run test:mcp-host`. That local Chromium lane proves only the exact self-contained App and declared observable host profiles; it never proves the Secure MCP Tunnel, deployed revision, undocumented ChatGPT behavior, workspace policy, Faye/Selen acceptance, or owner acceptance.

Runtime packaging, release identity, container, systemd, or tunnel changes additionally require the process-level tunnel preflight regression plus a clean committed-tree `npm run mcp:package -- --out=<new ignored path>` proof; inspect `release.json`, the packaged preflight helper/service pair, materialized OCI labels, exact `/healthz`, the private tool-result release metadata, and the one-resource App discovery contract. This is candidate evidence only and never authorizes installation, image activation, tunnel restart, or live deployment.

AIR@1 source/receipt/artifact changes additionally require the five-fixture `fixtures:air-v1` corpus, AIR@0 migration tests, strict any-version dispatch failures, mixed-meter MIDI/export integration, missing-extension-sound behavior, and an exact Artifact@3 desktop/mobile user-gesture playback check. Corpus diversity is implementation evidence, not independent model authorship, genre quality, or listening acceptance.

Extension-pack, sparse-profile, scene@1, Binding@1, plan@4, receipt@3, artifact@2, or local-manager changes additionally require `npm run packs -- proofs`, pack lifecycle/shared-byte tests, the vnext presentation export integration test, repeated offline DSP equality, and real browser playback of an exact Artifact@2. Pack proof success is engineering evidence, never listening or release acceptance.

F candidate/palette changes additionally require `npm run f:palette-packets -- --out=tmp/f-palette-packets --render-stress`; its native, matched, and stress renders are mechanical evidence only, and `pending-faye` must not be promoted by a passing command.

Complete-piece scale, plan, renderer, transport, delivery, or bounded-render changes additionally require `npm run tranche:d:evidence -- --render-all` against both checked-in complete-piece fixtures and real-browser checks of explicit-play startup, pause/resume/stop/restart, seek/section jump, looped-sustain reconstruction, desktop/mobile overflow, keyboard controls, no pre-gesture asset fetch, and console cleanliness. Evidence under `tmp/` and browser screenshots are local QA artifacts, not tracked product assets.

Keep `.github/workflows/ci.yml` path-unfiltered so the `core-contracts`, its focused `mcp-host-browser` dependency, and `clean-production` checks always appear on pull requests and `main`. Sound content cache is only a performance projection: cache `apps/soundbench/public/soundpacks`, key it from the catalog plus immutable candidate shards, and still run an explicit profile-targeted `soundpack:fetch` to re-verify byte count and SHA-256. The separate sound-evidence workflow may be path-filtered because it is not a required check. Keep workflow permissions read-only and third-party actions pinned to reviewed full commit SHAs.

Update `docs/current-state.md` when implementation, candidate, runtime-tested, or live status changes. Do not describe a candidate soundpack, renderer, MCP host, or export path as accepted before its corresponding evidence exists.

## Reader-facing documentation

`README.md` and `README.zh-CN.md` are coequal reader entrances. `docs/GETTING-STARTED.md` owns the shortest source-install/listen/save path; `docs/MCP.md` owns local MCP connection and host boundaries; `plugins/refrain/README.md` distinguishes the Skill-only candidate from installation and MCP connection. `docs/diagrams/architecture.mmd` and its rendered SVG share the evidence map in that directory. Keep private operational inventory and historical deployment notes outside Git; `docs/current-state.md` contains portable candidate/acceptance facts only. Public source visibility and broad product-release acceptance remain separate.

## Static demo deployment

`.github/workflows/pages.yml` builds and tests `dist-try` for GitHub Pages. Only its deployment job receives `pages: write` and `id-token: write`; all build and existing CI jobs remain read-only. The static demo is separate from MCP runtime deployment and must never publish the general presentation public directory or assets outside the two-work closure. README hero screenshots are intentionally absent; theme galleries must be labeled as static screenshots, with actual interaction linked to the demo.

---
> Source: [IndelibleVivi/refrain](https://github.com/IndelibleVivi/refrain) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-09-16 -->
