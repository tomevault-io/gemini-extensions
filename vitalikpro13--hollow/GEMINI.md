## hollow

> Hollow is a fully distributed, encrypted Discord alternative. No central servers. Members collectively host the server. See `HOLLOW_PLAN.md` for the full architecture, phase history, and current TODO checklist.

# HOLLOW — Project Instructions for Claude Code

## What Is This
Hollow is a fully distributed, encrypted Discord alternative. No central servers. Members collectively host the server. See `HOLLOW_PLAN.md` for the full architecture, phase history, and current TODO checklist.

## Tech Stack
- **UI:** Flutter (Dart) — all platforms (Windows, macOS, Linux, Android, iOS, Web)
- **Backend:** Rust via `flutter_rust_bridge` v2.11.1 FFI
- **Networking:** WSS relay (signaling + text/CRDT/MLS) + WebRTC data channels (files/shards P2P) + WebRTC media (voice/video P2P). libp2p fully removed.
- **E2EE:** vodozemac (Olm/Double Ratchet) for DMs, OpenMLS 0.8 for servers, SFrame (AES-128-GCM) for voice/video/screen share
- **Local DB:** SQLCipher (encrypted SQLite)
- **Identity:** Ed25519 keypairs via BIP-39 mnemonic (ed25519-dalek, NativeKeypair)
- **Org ID:** com.anonlisten
- **Project name:** hollow

## Project Structure
```
HOLLOW/
├── lib/                  # Dart/Flutter code (UI, app logic, state management)
│   ├── main.dart         # Entry point (ProviderScope + RustLib.init + window_manager init)
│   └── src/
│       ├── core/         # Models, Riverpod providers, service wrappers
│       ├── theme/        # Hollow design system (colors, spacing, typography, ThemeExtension)
│       └── ui/
│           ├── shell/    # Layout: hollow_shell, server_strip, channel_sidebar, member_panel, user_bar, mobile_nav, window_title_bar
│           ├── chat/     # ChatPane, MessageBubble, ChannelChatPane, ChannelMessageBubble
│           ├── settings/ # ServerSettingsPanel, OverviewTab, ChannelsTab, MembersTab, DangerZoneTab
│           ├── sidebar/  # PeerCard, EmptyPeerList
│           ├── components/ # HollowPressable, HollowButton, HollowTextField, HollowDialog, HollowTooltip, HollowToast, HollowToggle, HollowAvatar, HollowCard, StatusDot
│           ├── dialogs/  # InviteDialog, MnemonicDialog, CreateServerDialog, CreateChannelDialog, TwitchJoinDialog
│           └── animations/ # HollowCurves, HollowDurations, FadeSlideTransition, ScaleFadeTransition, SelectionShimmer, AmbientBackground, StartupRevealScope, RevealWidgets
├── rust/hollow_core/      # Rust library crate (networking, crypto, storage)
│   └── src/
│       ├── api/          # FFI layer (flutter_rust_bridge scans these)
│       ├── node/         # Networking modules (modularized from swarm.rs monolith)
│       │   ├── swarm.rs         # Event loop dispatcher + handle_incoming_request (~6.2k lines, envelope dispatch fully extracted)
│       │   ├── types.rs         # NetworkEvent, NodeCommand, HavenMessage, MessageEnvelope, helper structs
│       │   ├── crypto_handler.rs # Signing, Olm/MLS encryption, key exchange, coordinator election
│       │   ├── sync_handler.rs  # CRDT ops, server/channel CRUD, member management, sync
│       │   ├── message_ops.rs   # Send/edit/delete messages, emoji reactions (DMs + channels)
│       │   ├── social.rs        # Friends, profiles, typing indicators
│       │   ├── vault_ops.rs     # Vault shard storage, upload/download, recovery pool
│       │   ├── file_handler.rs  # File send/receive, stream handling, WebRTC transfers
│       │   ├── voice_handler.rs # Voice channels, 1:1 calls, WebRTC signaling
│       │   ├── gossip_relay.rs  # Gossip broadcast, peer exchange, timer handlers
│       │   ├── gossip.rs        # GossipOverlay, PeerScore, neighbor selection
│       │   ├── ws_client.rs     # WebSocket relay client
│       │   ├── ws_stream_transfer.rs # Binary stream reassembly
│       │   ├── signaling.rs     # Bootstrap peer discovery
│       │   ├── file_transfer.rs # File chunking utilities
│       │   ├── recovery_pool.rs # Recovery pool state management
│       │   ├── twitch.rs         # Twitch OAuth (Device Code Grant), follow/sub checks, proof validation
│       │   ├── image_convert.rs # WebP conversion
│       │   └── link_preview.rs  # URL link preview fetching
│       ├── crypto/       # Olm encryption + MLS + persistence
│       ├── identity/     # Ed25519 keypair management (native_identity.rs, keys.rs)
│       └── storage/      # SQLCipher message store
├── relay/                # Signaling HTTP + WS room router (Rust, legacy — superseded by relay-uws)
├── relay-uws/            # Production relay (uWebSockets C++, native TLS, deployed on OVH VPS)
├── packages/flutter_webrtc/ # Forked flutter_webrtc 1.4.1 (WASAPI loopback, native screen recording, macOS ScreenCaptureKit screen share)
├── rust_builder/         # flutter_rust_bridge build system (cargokit)
├── vendor/ffmpeg/        # Bundled native binaries (gitignored, see fetch_ffmpeg.ps1)
├── legal/                # Privacy Policy, Terms of Use, version manifest (manifest.json)
├── HOLLOW_PLAN.md         # Full architecture & design document (authoritative for all phase details)
└── CLAUDE.md             # This file
```

## Build & Run Commands
```bash
# Run on current platform (debug)
flutter run -d windows

# Build release
flutter build windows

# Run widget tests (no device needed, ~1s)
flutter test test/

# Check Rust code
cd rust/hollow_core && cargo check
cd rust/hollow_core && cargo clippy

# Multi-node integration harness — the PRIMARY way to verify distributed-logic changes
cargo test --lib test_harness -- --nocapture   # drive N real nodes through an in-process MockRelay
cargo test --lib                               # full Rust suite (harness tests included)

# Regenerate FFI bindings after Rust API changes (run from project root)
flutter_rust_bridge_codegen generate --rust-input "crate::api" --rust-root "rust/hollow_core" --dart-output "lib/src/rust"

# Deploy relay server updates to VPS (uWebSockets C++ relay)
scp relay-uws/src/*.cpp relay-uws/src/*.h relay-uws/CMakeLists.txt ubuntu@141.227.186.209:/home/ubuntu/relay-uws/src/
ssh ubuntu@141.227.186.209 "cd /home/ubuntu/relay-uws/build && cmake .. -DCMAKE_BUILD_TYPE=Release && make -j2 && sudo setcap cap_net_bind_service=+ep hollow-relay && sudo systemctl restart hollow-relay"

# Windows release: build + code-sign all binaries + Inno Setup installer + sign installer + portable zip (one command, prompts for Certum card PIN)
pwsh scripts\build_release.ps1                 # full pipeline (flutter build → sign → installer → sign → zip)
pwsh scripts\build_release.ps1 -SkipBuild      # repackage an existing build without rebuilding
pwsh scripts\sign_release.ps1                  # just code-sign every .exe/.dll in build\windows\x64\runner\Release
```

**Windows code signing (Certum):** Cert lives on a `cryptoCertum3.7` USB card in minidriver mode. signtool needs the cert bound to the CNG Key Storage Provider — the scripts self-heal this via `certutil -repairstore`. Output (signed installer + zip) lands in `installer\Output\` (gitignored). See `reference_certum_signing_procedure` + `project_windows_installer_pipeline` memories. Vitalik tests the install/uninstall GUI flow himself.

## Hollow Design System (Phase 2.75)
All UI uses custom Hollow widgets — no Material defaults.

- **HollowPressable:** Press: opacity 0.85 + scale 0.98 (spring). Hover: color transition 150ms + shadow lift. `subtle` mode for list items.
- **HollowButton:** 4 variants: `.filled()`, `.ghost()`, `.outline()`, `.danger()`. Props: `onPressed`, `child`, `icon`, `expand`, `compact`.
- **HollowTextField:** `OutlineInputBorder`, animated border (→accent on focus, →error on error), focus glow. Optional `prefixIcon`, `borderRadius`, `isDense`, `showCounter`.
- **HollowDialog:** `showHollowDialog()` — scale 0.95→1.0 + fade, full-screen glassmorphism blur (0→12 sigma).
- **HollowTooltip:** Overlay-based, 400ms delay, fade+slide entrance.
- **HollowToast:** Slide-up + fade, auto-dismiss. Three types: success/error/info. One visible at a time.
- **HollowToggle:** Spring physics thumb, color crossfade track.
- **StatusDot:** Optional `pulse` for breathing glow (3s cycle).

## Key Architecture Notes
- **Multi-node test harness = PRIMARY testing for distributed logic.** `rust/hollow_core/src/node/test_harness.rs` (`#[cfg(test)]`) spins up N real `spawn_node` event loops in ONE process, each with its own temp SQLCipher DB + keypairs, wired through an in-process `MockRelay` (no sockets/TLS/network). Drive real multi-device scenarios (DMs, servers, offline/online, revoke) and assert on each node's REAL state via two-layer **inspectors** (UI-layer = master-collapsed, reads through the same resolver/CRDT accessors the Dart providers use → green inspector == green UI; raw-layer = device-keyed truth, catches the divergence between them). **Always rely on the harness to verify ring-1 (distributed-logic core) and ring-2 control/signaling changes before declaring them done** — it catches the multi-device state/event bugs that were previously only found by manual 3-device log-copying. Coverage line (what it can/can't verify) in `reports/HARNESS_COVERAGE_MAP.md`; design + build ladder in `reports/MULTINODE_TEST_HARNESS_HANDOFF.md`; wiki `rust_test_harness`. Production seam: `run_event_loop` takes `db_path`/`db_passphrase` as params (was process-global `data_dir()`); `spawn_node_mock` is the cfg(test) twin that skips the real WS/signaling. NOT covered: WebRTC media plane, Flutter pixels, FFI, native push/keychain, real relay C++ — those stay Vitalik's manual/Tier-2 pass.
- **Node module structure:** `swarm.rs` is the event loop dispatcher; domain logic lives in focused modules (`crypto_handler`, `sync_handler`, `message_ops`, `social`, `vault_ops`, `file_handler`, `voice_handler`, `gossip_relay`). Types/enums are in `types.rs`. Each module exports `pub(crate) async fn handle_*()` functions called from swarm.rs match arms. Functions take individual state variables as parameters (no SwarmContext struct — deferred due to borrow checker constraints with crypto helpers).
- **Persistence actors:** `CrdtStore` (`node/crdt_store.rs`) and `CryptoStore` (`crypto/store.rs`) own long-lived SQLCipher connections in `spawn_blocking` threads. Fire-and-forget via mpsc channels. CrdtStore uses batch-drain (blocking_recv + try_recv loop) to coalesce multiple CRDT ops into one DB write per server. All sync_handler save sites use CrdtStore. MLS state persistence uses CryptoStore. Never open `MessageStore::open()` in sync handlers.
- **Peer state tracking in swarm.rs:** `ws_room_peers` (room → peer set), `synced_peers` (HashSet<String>). WS PeerJoined triggers key exchange + sync. 30s keepalive ping in ws_client.rs.
- **Relay domain (self-hosting):** Configurable via `relayDomainProvider` (Dart) + `set_relay_url()` FFI (Rust). Persisted in SQLCipher. Default: `relay.anonlisten.com`. All WS/signaling/STUN/TURN URLs derive from this domain.
- **Event streaming:** Rust→Dart via `StreamSink` (flutter_rust_bridge). `watch_network_events()` in `api/network.rs`, `EventStreamNotifier` in `event_provider.dart`.
- **Profile/avatar loading:** Profiles load WITHOUT blobs at startup (`getAllProfilesLight()`). Avatar bytes lazy-load on-demand via `AvatarNotifier` in `avatar_provider.dart` (same pattern as `ServerAvatarNotifier`). Banner bytes via `bannerProvider` (FutureProvider.family). `HollowAvatar` is a `ConsumerWidget` that auto-fetches from avatar cache — don't pass `imageBytes:` from profile data. `ProfileUpdated` event invalidates both caches.
- **Navigation shell:** Two layout modes (persisted via `layoutModeProvider`): Dock mode (default, FriendsBar + BottomBar) and Classic mode (Discord-like 4-panel). Mobile: bottom nav with single-panel views. Vitalik uses Dock mode — put new UI in `bottom_bar.dart`.
- **Window chrome:** `window_manager` ^0.5.1, `setAsFrameless()`, custom 32px title bar.
- **Theme:** `HollowTheme.dark()`/`.light()` + `darkWithHue()`/`lightWithHue()`. `themeModeProvider` persisted to SQLCipher.
- **Icons:** `lucide_icons_flutter: ^3.1.14`. All `LucideIcons.*` (camelCase). Uses `alertTriangle`/`alertCircle`. No `cloudCheck` — uses `cloud`. Brand icons use vendored `lib/src/core/brand_icons.dart` (`BrandIcons.*`, `BrandIconColors.*`) with `assets/fonts/SimpleIcons.ttf`. Also `atlas_icons: ^0.0.12` (`Atlas.*`, snake_case constants, e.g. `Atlas.adult_18` for NSFW — note the constant is `adult_18`, not `at_adult_18`).
- **Identity at-rest protection:** Two opt-in modes (Settings > Security): password (Argon2id + AES-256-GCM, blocks app launch) or OS keychain (Windows Credential Manager + DPAPI fallback / macOS Keychain, silent unlock). `unlock_identity()` must be called before any identity/DB operation. Recovery: 24-word mnemonic. See `project_identity_protection_v2.md` for byte format and implementation details.
- **License key system:** Relay loads `keys.json` (enabled toggle + key list), validates during WS auth, hot-reloads every 30s with active connection revocation. App checks `/relay-status` on startup, shows key dialog if required, caches key in SQLCipher via `set_license_key()` FFI.
- **Temporary nicknames:** Ephemeral relay-scoped nicknames for friend requests. RAM only (`nickname_to_peer`/`peer_to_nickname` maps in `RelayState`), released on disconnect. Relay handles `claim_nickname`/`release_nickname`/`resolve_nickname` text messages. Rust does two-step resolution (resolve → normal friend request). UI auto-detects peer ID (`12D3KooW` prefix) vs nickname in a unified input. Provider: `temporaryNicknameProvider`, resets on `RelayDisconnected`.
- **Window title bar:** `WindowTitleBar` lives in `MaterialApp.builder` (above Navigator), NOT inside `HollowShell`. Navigator child wrapped in `ClipRect` to prevent `BackdropFilter` blur bleed. Never move the title bar back inside the shell.
- **Logging:** `hollow_log!` macro → stderr + `hollow_debug.log` (release-safe, 10MB rotation). `hollow_crash.log` captures Flutter/platform errors (5MB rotation).
- **Relay:** uWebSockets C++ relay (`relay-uws/`) on OVH VPS, native OpenSSL TLS, port 443. Domain: `relay.anonlisten.com`. **No backpressure soft limit, no binary rate limit** — removed because they silently dropped messages. `maxPayloadLength`=64MB — NEVER lower (silently kills connections). `maxBackpressure`=64MB (hard). DoS protection: Ed25519 auth + license key revocation + per-IP limits. Topic routing: `0x07` frame with channel_id, `0x03` universal broadcast. See `feedback_relay_rules.md` for details.
- **Storage layout:** data root is `dirs::data_dir()/hollow` (`keys.rs::data_dir`, overridable via `set_data_dir()` / `HOLLOW_DATA_DIR`) — Windows `%APPDATA%\hollow`, macOS `~/Library/Application Support/hollow`, Linux `$XDG_DATA_HOME/hollow` (default `~/.local/share/hollow`); mobile uses the sandboxed app documents container (iOS migrated into the App Group). Subfolders under that root: `files/` (full-replication), `vault/` (erasure-coded shards), `vault_cache/` (LRU-evicted, 1GB cap). Plus `identity.key`/`identity.device` + `messages.db`. (NOT `~/.hollow` — that was the old/incorrect path.)

## Coding Conventions
- Dart: follow `flutter_lints` / `analysis_options.yaml`. Rust: follow `cargo clippy`.
- File naming: snake_case for Dart and Rust files.
- No Electron, no Node.js, no web frameworks — Flutter only for UI.
- **NEVER pass `WidgetRef ref` as a constructor parameter** to child widgets. Always use `ConsumerWidget` or `ConsumerStatefulWidget` instead. Passing `ref` causes cascade rebuilds.
- Use `AnimatedOpacity` (GPU-composited) for per-item opacity. Never use the `Opacity` widget.
- **CRITICAL — Backward-compatible DB schema:** ALWAYS add `#[serde(default)]` to ANY new field added to a persisted Rust struct (e.g., `ServerState`, any struct stored as JSON in SQLCipher). Without it, old data lacking the new field fails to deserialize and silently disappears (servers vanish, data lost).
- **CRITICAL — flutter_webrtc native: use `sourceId` for ALL input device selection (audio AND video).** Correct pattern: `{'optional': [{'sourceId': deviceId}], 'width': ..., 'height': ...}`. `{'deviceId': ...}` is silently ignored.
- **CRITICAL — server switching must batch provider writes atomically.** `channelListProvider`, `channelLayoutProvider`, `selectedServerProvider`, and `selectedChannelProvider` must all update in a single synchronous block. See `server_strip.dart:_selectServer` for the canonical pattern.
- **CRITICAL — TURN ICE config: split URIs into separate entries.** flutter_webrtc's native `CreateIceServers` has a single `uri` field per `IceServer` struct. Always map each TURN URI to its own entry.
- **CRITICAL — MLS epoch staleness after reconnection.** Sync requests must use plaintext `HavenMessage`. CRDT broadcast must fall back to plaintext on MLS failure. See `feedback_mls_patterns.md` for all 8 MLS rules.
- **CRITICAL — WS send failures must trigger reconnection.** `send_command()` in `ws_client.rs` returns `bool`. If false (TCP write failed), the main loop must break, push the failed command to `pending_commands`, and reconnect. Never silently discard send errors.
- **CRITICAL — mobile selection providers cleared in `.then()`, not `dispose()`.** See `feedback_mobile_ui_patterns.md` for full pattern.
- **CRITICAL — in-memory message lists must dedup by `message_id`.** `chat_provider`/`channel_chat_provider` `_addMessage`/`receiveMessage` skip when a non-null `message_id` already exists — the same message arrives via both `loadHistory` (DB, possibly written by the FCM push-fetch node) AND a live network event. Naive append shows it twice even when the DB is correct. See `feedback_ui_dedup_by_message_id.md`.
- **CRITICAL — multi-device device-vs-master routing (Phase 6).** A person has several DEVICE peer_ids that all resolve to ONE MASTER identity. The relay reports DEVICE ids (rooms, `peersProvider`, `connStatus`, typing events); friend/DM/profile/presence UI keys on the MASTER (`friend.peerId`). Two iron rules: **(1) per-person UI/lookups must collapse device→master** (Rust `resolver::resolve`/`same_identity`; Dart `deviceLinkProvider.identityOf`/`onlineIdentitiesProvider`/`identityIsOnline`) — a direct `map[friend.master]` silently never matches a multi-device friend (broke presence dots, Home column, chat-header Encrypted, typing display). **(2) SENDS must target a DEVICE, never the bare master** — the master authenticates as NO socket, so anything addressed to it is silently dropped (broke DM messages, DM typing). Fan out via `resolver::devices_for(master)` UNIONed with live DM-room peers resolving to that master. **`dm_room_code` is PURE — never resolve inside it** (room must be `f(my_master, friend_identity)` so two friends always agree; resolving there diverged the room per-side → keying errors even for plain friends). Sibling self-echo DMs carry `is_own`/`convo` so they render OUTGOING under the right thread. **DM peer-fallback (Step 5 second half, LIVE 2026-06-18):** a multi-device requester sets `DmSyncRequest{both_directions:true}` so a FRIEND re-serves your OWN messages sent from another (offline) device (`get_dm_messages_for_sibling`, gated on `!devices_for(local_master).is_empty()`) — otherwise stranded. **`DmSyncItem.mine` is RESPONDER-RELATIVE:** a cross-IDENTITY direction field is sender-relative, so the receiver re-orients it (`is_mine = if same_identity(sender) { mine } else { !mine }`) for BOTH insert AND sig context; a cross-DEVICE (same-identity sibling) field is NOT inverted. Forgetting this rendered backfilled DMs on the wrong side + flooded sig-verify failures. **Servers/MLS ARE multi-device as of Step 6 (LIVE 2026-06-18)** — both of a person's devices hold their own MLS leaf and member panels DO collapse devices→master now. **CORRECTION (2026-06-23): a FRESH single-device install is NOT byte-for-byte pre-multi-device — `device_peer_id != master_peer_id` ALWAYS (since `ad7b49e`; `keys.rs::generate_new_identity` mints a random device key, deliberate per `device_key.rs`). So every fresh friend RUNS the device↔master path; safety comes from the resolver+fixes being correct, not device==master.** This masked two real fresh-install bugs (both FIXED + live-verified 2026-06-23): **(a) one-directional DM** — the friend-request inbox-leave races the Olm handshake and drops the accepter's KeyBundle (requester in no shared room yet) → one side never builds a session, the other defers forever; healed via a **DM-room co-presence re-key** in the PeerJoined+RoomMembers handlers (`!olm.has_session(peer) && room == dm_room_code(local_master, resolve(peer))` → fresh KeyRequest, bypassing the freshness gate) + **accepted-friend added to the reconciliation-sweep gate**. **(b) remove→re-add while online auto-accepted with no consent** — the `FriendRemove` receive handler didn't clear `pending_friend_accepts`/`pending_friend_requests` (now threaded in + cleared), and the RoomMembers friend req/removal/accept drains were gated behind `is_new` (skipped for an already-synced peer) → MOVED out of the `is_new` guard. See `feedback_friend_handshake_olm_inbox_leave_race.md`, `feedback_friend_readd_online_auto_accept.md`, `project_multidevice_step6_mls_leaves.md`, `project_multidevice_migration_state.md`, `reports/MULTI_DEVICE_IMPLEMENTATION_TRACKER.md`.
- **CRITICAL — multi-device SERVERS/MLS (Step 6).** MLS leaf credential = the bare `device_peer_id` (each device its own distinct leaf; `MlsManager::new(&device_peer_id)`). CRDT `ServerState.members` is MASTER-keyed (joiner resolved device→master at `MemberAdded`; `canonicalize_members` folds legacy device-keyed entries on load/sync). **THE INVARIANT:** `state.members` master-keyed, `mls.group_members()` device-keyed → every MLS↔CRDT compare bridges through `resolver`/`same_identity`; use `ServerState::is_member()` not `members.contains_key()`. A `crdt/mod.rs` resolver-hook chokepoint makes `get_role`/`has_permission`/`is_banned`/`is_member` collapse device→master internally (one fix for ~24 lookup sites). Every targeted member SEND fans master→online devices via `crypto_handler::send_raw_to_identity` (the bare master is in no room → dropped). **A LINKED SIBLING MUST regenerate its OWN MLS signer/credential** — the snapshot import copies the source's `mls_identity` table; reusing it = `DuplicateSignatureKey`/`CannotDecryptOwnMessage` (`import_pending_link` clears it; startup net regenerates iff `cred != device && (cred != master || has_sibling)` — NEVER re-key a sole owner). Post-join MLS bootstrap sends the KeyPackage to the SyncResponse responder (`peer_str`) directly, NOT via resolver-based election (resolver is cold on a fresh join). Channel messages need `subscribeChannels` on EVERY channel-open (relay topic-routes `SendToRoomTopic`; subscribed socket only gets its topic set) — `ref.listen(selectedChannelProvider)` in hollow_shell + `mobile_shell._openChannel`. See `project_multidevice_step6_mls_leaves.md`, `feedback_mls_sibling_identity_collision.md`.
- **CRITICAL — multi-device SERVER SYNC (Step 9D, 2026-06-19).** Server lifecycle ops must converge BOTH the actor's OWN sibling devices AND offline members. (1) **Sibling fan-out:** plaintext-only handlers (kick/ban/role/leave + pin/unpin/layout/pledge/nickname/twitch) call `fan_to_own_siblings(ws_cmd_tx, ws_room_peers, local_peer_str, local_device_id, data)` — fans to our online siblings EXCLUDING the acting device (it already applied; re-feeding it its own MLS commit fails `process_commit` → self-drops the group). MLS-first ops (channel CRUD, rename, settings, unban, labels, visibility) already converge siblings via `SendToRoom`. Add `local_device_id` to any new such handler. (2) **Server DELETE is a replicable `CrdtPayload::ServerDeleted` tombstone, NOT a one-shot** — apply sets `ServerState.deleted` (`#[serde(default)]`) + drains members but KEEPS the shell+op_log so the owner serves it to reconnecting peers; offline members reconcile on reconnect via grow-only SyncResponse; hidden from `get_joined_servers`/harness `servers()`. Owner-author MUST be validated at EVERY ingest (CrdtOpBroadcast `allowed` match + a filter before merge in BOTH SyncResponse and MLS SyncResp, checked against the receiver's own role map). Plain-kick self-eviction = "self no longer in members after merge". (3) **Server CREATE auto-onboards online siblings** via `HavenMessage::SiblingServerAnnounce` → the sibling runs a SAME-IDENTITY join fast-path (skips ban/Twitch/private/owner-verify gates) → gets snapshot + MLS leaf. (4) **Keystone MLS leaf:** the keystone device (`device==master`) regenerates a device-credentialed leaf when `has_sibling` (`stale_keystone` in swarm.rs startup; NEVER re-key a sole device), and re-joins owned groups via the **sibling-re-adds-sibling** fast path in the MlsKeyPackage handler (election excludes our own identity); old+new leaf SHARE credential → remove-then-add. (5) **OFFLINE-then-reconnect server sync (2026-06-19 follow-up):** `SiblingServerAnnounce` is a one-shot live msg, so a sibling offline at create/join never learned the server. RE-ANNOUNCE every non-deleted server when a sibling appears in our `inbox:{master}` — on BOTH the `PeerJoined` (already-online side) AND `RoomMembers` (reconnecting side) handlers. The receive handler ALWAYS runs the inline join flow, **even for an already-held server** (deleted the `contains_key` early-return) — the join completes with 0 new ops when pending and fires `NetworkEvent::ServerJoined` → Dart `onServerCreated` (UNCONDITIONAL list insert). NEVER rely on `ServerUpdated`→`onServerUpdated` to surface a new server — that DB-read path proved unreliable; `ServerJoined` is the only event that refreshes the list reliably. (6) **Manual sync escape hatch:** Security→Your Devices refresh-icon on each other-device row → FFI `request_state_sync(source)` → `SiblingStateSyncRequest` → the SOURCE re-announces all servers + re-shares friends (servers+friends only). (7) **Your-Devices list vanished on restart = Dart staleness:** `myDevicesProvider` reads `deviceLinkProvider` (Rust-resolver mirror), which was only `refresh()`ed at event-stream start (races node readiness, no retry) + on `DeviceListUpdated`. Data was always in the DB. Fix: refresh in `_DevicesSection`/`_DevicesSectionMobile` `initState` (no live listener keeps the mirror warm while Settings is closed) + retry the `event_provider` startup warm-up (400ms/1.2s/3s). See `feedback_server_lifecycle_sibling_sync.md`, `feedback_sibling_server_reannounce_and_device_list_ui.md`, `feedback_manual_state_sync_button.md`, `feedback_keystone_mls_leaf_regen.md`, `project_multidevice_9c_9d_polish.md`.
- **CRITICAL — multi-device PUSH (Step 9A, LIVE 2026-06-20 — closes the epic).** The relay keys the push token + offline buffer by the **DEVICE** id a socket authed with, and replays a buffered frame ONLY to a socket presenting that exact device id. So: (1) **The fetch node auths as its DEVICE, not master** (`start_fetch_node` + NSE `fetch_and_decrypt` build the WS-auth triple from `id.device_keypair`; **DB passphrase stays MASTER-derived** — a device-derived one opens an empty DB). `run_fetch` gained `local_master`; DM room = `dm_room_code(local_master, resolve(sender))` (master-paired; `dm_room_code` pure); callers `warm_from_links`+`seed_self` first; the DM is stored under `resolve(sender)` (master) so it lands in the one shared thread. (2) **The DM/channel sender targets offline-but-real devices**: `collect_target_devices` (+ file_handler, + channel 0x09 fan-out) adds `devices_for(master)` ∩ `olm.has_session` ∩ not-in-room (the ghost-safe predicate — a real offline phone has a session, a never-contacted ghost doesn't) so a fully-quit phone is buffered+pushed; own siblings stay push-suppressed; channel fan-out resolves each offline MASTER member→devices (mention flag per master). (3) **Dart per-person dedup**: push handler + `get_push_profile` resolve `sender`→master; iOS hints-cache (`push_hints_cache.dart`) keys each friend under master + ALL device ids (`getDeviceLinks()`) so the NSE's resolver-less `map[sender]` hits for a device-id sender. Harness: `dm_buffers_for_offline_real_sibling_device`. **KNOWN PARKED GAP:** iOS DM push TAP opens a device-keyed empty thread (tap arrives via FCM `data['sender']`=device id; the `_openChatFromPush` device→master resolve via `identityFor` didn't fix it — links cold at cold-start tap; Android fine). Push delivery/content unaffected. See `project_multidevice_9a_push.md`, `wiki/push_notifications.md`.
- **CRITICAL — multi-device REVOCATION + ghost fan-out (Steps 7/8, LIVE 2026-06-18).** Device list now carries a master-signed `revoked: Vec<String>` tombstone array (signing payload gained `:{sorted_revoked_csv}`; max-version-wins — higher version adds AND un-revokes, replay can't shrink). **Revocation is manual-only** (`NodeCommand::RevokeDevice`/FFI `revoke_device`; any device-you-control revokes another — all hold the master key, no "promotion"). The tombstone MUST be sent **to the revoked device FIRST** (`handle_revoke_device`) so its ingest fires `NetworkEvent::SelfRevoked` → `event_provider._selfNuke()` (`stash_pending_wipe` FIRST, then best-effort `overlayState:` toast — NEVER plain `HollowToast.show(ctx)`, it throws from a navigator-key context and aborts the wipe) → data-dir wipe + relaunch to Welcome. `ingest_device_list` returns `IngestOutcome { our_devices_grew, newly_revoked }`; `swarm::enforce_device_revocations` drops the Olm session (+ erases the persisted pickle via `CryptoStore::delete_session`) and, coordinator-only, removes the single MLS leaf via `pending_mls_removals`. **GHOST FAN-OUT (the load-bearing fix):** the DM/file fan-out (`message_ops::collect_target_devices`) targets `devices_for(master)` filtered to ids **CURRENTLY IN A ROOM** — a dead ghost (re-link cycles mint new ids; union-merge never prunes) keeps a STALE session so `has_session` is NOT enough; room-presence is the liveness test. Single-device offline FCM push preserved via `fallback_self`. **Dart:** DM unread + notifications resolve `fromPeer`→master (`identityOf`) or the nav pill sticks counting a dead device. **Phantom-chat guard:** `resolver::mark_revoked`/`is_revoked`; DM+typing receive paths drop messages from a revoked-but-alive device (after `forget` its id resolves-to-self → would spawn a standalone "unknown peer" chat). **Step 8 Devices panel** (Settings → Security → Your Devices, desktop `user_settings_dialog` + mobile `mobile_settings_tab`): lists my devices (shortened id, online, local editable label via `device_labels` table, "This device", "Remove device" → revoke), offline-unlabeled ghosts behind "Show all (N offline)". See `feedback_ghost_device_fanout.md`, `feedback_toast_from_nonwidget_overlaystate.md`.
- **CRITICAL — device linking / snapshot sync (Step 4, LIVE).** A new device pulls the full identity+DB from an online one via a 6-char code (no QR). **The transfer REUSES the `.hollow` backup pipeline end-to-end, NOT a parallel path:** sender `export_backup_bytes(code,…)` (the link CODE is the passphrase) → stream as `StreamKind::LinkSnapshot` → receiver `stash_pending_link(blob, code)` writes `pending_link.hollow`+`.code` and RESTARTS → next launch `_bootstrap` (FIRST, before `hasIdentity()`) `has_pending_link()`→`import_pending_link()` (deletes throwaway identity, then `import_backup_bytes` — identical to a manual restore). **NEVER import in-place while the node runs:** the original `import_snapshot_bytes` fought the live SQLCipher connection + WAL + a protection-mismatched throwaway `identity.device` → "Loading… forever". `import_backup` works only because it runs pre-node-start. The empty device uses a THROWAWAY identity just to get a relay socket; the snapshot overwrites it (the relay needs no new "guest" mode). 6-char code = temp-nickname RAM-map clone on the relay (`link:{code}` rendezvous room, 5-min TTL, one-shot). Sender progress is an indeterminate spinner (fire-and-forget push), receiver has the real byte bar. See `feedback_link_import_identity_device.md`, `project_multidevice_step4_link_design.md`.
- **CRITICAL — push notifications (FCM Tier 1/2).** Relay buffers offline DMs in RAM (`offline_buffer`, 100 text + 1 image/peer, 24h TTL) and replays on DM-room join — Tier 2 fetch can't get the triggering message otherwise (relay is a dumb pipe). `fetch.rs` parses binary `0x06` frames (not text). **Offline images:** sender inlines AES-encrypted bytes into the FileHeader → `0x08 SendDirectImage` frame (must target `dm_room_code()` directly — offline peers are in no room) → relay buffers under the image cap → fetch decrypts, writes the file, AND inserts the `messages` row itself (companion text DM is dropped to offline peers). The notification shows a lightweight line — `📷 Image` (captionless) or `📷 <caption>` — NOT a BigPicture preview (removed: decode+downscale on the notif thread was too slow; image still syncs to DB). **Captioned offline image:** the caption is sent EXACTLY ONCE via `crypto_handler::send_encrypted_text_to_peer` (0x04, text cap) AFTER the FileHeader — never via `send_encrypted_message` for an offline image (it encrypts-then-discards, burning an Olm ratchet slot the receiver never sees → decrypt gap). **Signatures:** the row sig rides on the DirectMessage envelope; for offline images the FileHeader must carry `sig`/`pk` (captionless: only sig carrier, signed over `[file:<id>]`), and `promote_file_sentinel_to_caption` UPDATEs text+sig+pk together (else "Unsigned"). **Notification UX (Android done):** post ONE populated banner after the fetch (no generic flash + swap); per-peer card (`sender.hashCode`) + InboxStyle last-3 + Android N-count; `groupKey:'hollow_dm_group'` + silent summary (`GroupAlertBehavior.children`) bundles multiple peers; `dismissPeerNotification()` clears the summary when no children remain. Status-bar icon MUST be a monochrome white silhouette (`@drawable/ic_stat_hollow`) — `@mipmap/ic_launcher` renders as a white blob (Android keeps only alpha). Launcher icon via `flutter_launcher_icons` from `assets/hollow_icon_android.png` (square full-bleed render of `hollow_main_logo.svg`; solid `#0D0F14` adaptive bg). See `feedback_fcm_image_invisible_bubble.md`, `project_push_notification_implementation.md`. Background isolates: NEVER trust `RustLib.init()` to be idempotent (reused isolate throws "twice" → treat as ready) and NEVER call `load_or_create_identity()` (use `load_existing_identity()` — generating yields wrong peer_id/DB passphrase). `log_path()` honors `set_data_dir()` so mobile `hollow_debug.log` writes. See `project_push_notification_implementation.md`, `feedback_rustlib_init_not_idempotent.md`. Firebase config files (`google-services.json`, `GoogleService-Info.plist`) are gitignored — public repo, contributors bring their own project. **iOS specifics:** classic non-UIScene AppDelegate (`register(with: self)`) + firebase pinned below the iOS-SDK-v12 break (`firebase_core ^3.15.2`, `firebase_messaging ^15.2.10`) to keep iOS 13 — UIScene would force iOS 15 for no gain. iOS push is a VISIBLE ALERT (`apns-priority:10`, `apns-push-type:alert`, generic `body:'New message'`, `mutable-content:1`, `content-available:1`) — NOT a pure silent push (Apple throttles those). Rich banner (name+avatar) via a **Notification Service Extension** (`ios/NotificationService/`) that reads a shared **App Group** (`group.com.anonlisten.hollow`) "push hints" cache written by `lib/src/core/services/push_hints_cache.dart` (`PushHintsCache.scheduleWrite`, hooked into `friendsProvider.loadAll()`). Writer path & NSE reader path MUST match (`push_hints/hints.json`, no `hollow/` prefix). **iOS Tier B — decrypted message TEXT in the banner (force-killed) IS LIVE** (the Dart `content-available` bg handler does NOT run when iOS apps are force-killed — only the NSE does). The NSE itself fetches the ciphertext from OUR relay (fetch-peer mode, E2EE) and decrypts on-device via a Rust C-ABI `hollow_push_fetch_and_decrypt` (`rust/hollow_core/src/push_enrich.rs`, OUTSIDE `crate::api` so frb doesn't scan it; wraps `node::fetch::run_fetch` with its own `current_thread` tokio runtime). **PRIVACY: push stays empty (`{wake, sender}` only) — NEVER put ciphertext in the push (leaks size/timing metadata to Apple/Google).** The data dir is MIGRATED into the App Group container (`<AppGroup>/hollow_data`) so app + NSE share one SQLCipher DB (`lib/src/core/services/ios_data_dir_migration.dart`, runs in `main.dart` before RustLib opens the DB). **CRITICAL — that shared DB MUST use `journal_mode=TRUNCATE` on iOS, NOT WAL** — WAL's persistent `-shm` lock in a shared container makes RunningBoard kill the suspended app with `EXC_CRASH 0xdead10cc` (see `feedback_ios_appgroup_sqlite_wal_crash.md`); `busy_timeout=4000` lets app+NSE coexist. App-active heartbeat (`<AppGroup>/push_diag/app_active.txt`, touched on resume / cleared on pause) lets the NSE skip its fetch when the app is alive (live node already has the message). NSE shows last-3 messages + "N new" subtitle; multi-peer grouping is native via `threadIdentifier=sender`; `apns-collapse-id` (FNV hash, matched in sidecar+Dart) replaces the peer's banner. NSE links `hollow_core` via a Podfile `target 'NotificationService'` block (ONLY that pod, not `flutter_install_all_ios_pods`) + bridging header `NotificationService/HollowPushBridge.h`. NSE footprint ~3MB (vs 24MB cap). Diagnostics: Security-tab "Export Push Diagnostics" button reads `<AppGroup>/push_diag/nse_metrics.log`. **Vitalik builds iOS via Xcode Archive himself — NEVER run `flutter build ios`** (`feedback_no_ios_build_command.md`). See `project_ios_push_tier_b_disposable_nse.md`, `feedback_app_group_path_match.md`, `feedback_ios_xcode26_toolchain.md` (objectVersion 70→54 Podfile patch, NSE build-phase cycle fix).
- **CRITICAL — CHANNEL push notifications (server channels).** Sender fans out one **0x09 frame** per OFFLINE member (`[0x09][room\0][target\0][channel\0][flags:1][payload]`, payload = the SAME MLS-group/public wire bytes the room broadcast carried — `send_mls_broadcast_topic` returns them) — the relay never learns membership, the SENDER picks targets from its CRDT and computes the per-target mention flag (@everyone/@name/@nickname/reply-author). Relay buffers (separate 30/peer channel cap) + fires a push **filtered by `set_push_prefs`** (RAM registry, `peer→server→{level, channels{}}`, re-sent on reconnect like the token; filtering MUST be relay-side — iOS alert pushes can't be suppressed after delivery) with debounce (non-mention 120s/server + max 3 while offline, reset on full-app room rejoin; mention 10s; 5s/peer floor). Sidecar sends `type:channel_wake` (+server/channel/mention as strings); iOS collapse-id = `hash("server:channel")` → one replaceable banner per channel. Fetch node (`run_fetch` + `server_room` param) joins the SERVER room and decrypts via `MlsManager::from_persisted` — stale-epoch decrypt failure = graceful content-free fallback, app self-heals via channel sync. Dart `_handleChannelWake` re-checks the LOCAL level via `get_push_channel_meta` (names + effective level from SQLCipher), groups the replay burst per channel, posts "Server • #channel / Name: text" under `hollow_channel_group`; tap payload `channel:<sid>:<cid>` → `registerOpenChannelHandler`. NSE: `hollow_push_fetch_and_decrypt` gained a 6th `server_room` param ("" = DM). Prefs sync is MOBILE-ONLY (`notification_provider._syncPushPrefsToRelay`) — a desktop sync would overwrite the phone's filters. Text messages only (no channel files/edits) in v1. See `project_channel_push_notifications.md`.
- **CRITICAL — LOCAL notifications (in-app + native, distinct from FCM push).** `system_notification_provider.notifyDm/notifyChannel` route by app state. **Desktop:** native OS toast on `isHidden || !isFocused`; in-app card (`NotificationOverlay`) whenever the window is VISIBLE (`!isHidden`). `event_provider`'s `isViewingDm/isViewingChannel` gate (which suppresses the chat you're actively reading) ANDs in `appActive` = desktop `windowFocusedProvider` (driven by main.dart `onWindowFocus`/`onWindowBlur`) / mobile `!appLifecycleProvider.isBackground` — WITHOUT the mobile lifecycle term, backgrounding from INSIDE a chat suppressed its OS notif. **Windows native toast** uses `flutter_local_notifications_windows` DIRECTLY (the unified `flutter_local_notifications` facade does NOT route Windows): rich (avatar + name + ONE line + inline Reply); FRESH toast id per message so a new one STACKS (replacing wipes a mid-typed reply); brand icon via `WindowsInitializationSettings.iconPath`; Reply args encode `reply:<peerId>`, text in `response.data['replyText']`. macOS/Linux = plain `local_notifier`. **Mobile:** backgrounded-but-connected → real OS banner via `push.showLocalDmNotification/showLocalChannelNotification` (reuse the FCM UI); foreground → the ONLY in-app banner is `MobileInChatBanner` (in `MobileChatRoute`, for OTHER convos, 3 msgs + 5s countdown ring, `Material(transparency)` wrap to avoid the yellow underline; the old top-tabs `MobileNotificationBanner` was REMOVED). Channel @mention for NEVER-opened channels is intentionally NOT delivered live (would need subscribe-to-all = receive+decrypt all server traffic); killed-app mobile gets it via the relay channel-push. Mobile @mention autocomplete added in `mobile_chat_route.dart`. See `project_local_notifications_desktop_mobile.md`.
- **CRITICAL — flutter_webrtc Windows: never use `replaceTrack` reuse for mid-call media.** ALWAYS use `pc.addTrack(track, stream)` / `pc.removeTrack(sender)`. See `voice_channel_service.dart`.
- **CRITICAL — mic gain / call loudness uses `Helper.setCaptureGain()`, NOT `setVolume()`.** `setVolume` on a LOCAL capture track is a no-op for outgoing audio (libwebrtc only honors it on REMOTE tracks). Outgoing loudness is a native **capture-post-processor** (makeup gain + soft limiter to -3 dBFS, runs AFTER WebRTC's AGC/NS/EC) added to the fork on all platforms: desktop `flutter_capture_gain_processor.{h,cc}` (impl `RTCAudioProcessing::CustomProcessing`, attached in `flutter_webrtc_base.cc`), Android `CaptureGainProcessor.java` (on `audioProcessingController.capturePostProcessing`), darwin `CaptureGainProcessor.{h,m}` (`ExternalAudioProcessingDelegate` on `capturePostProcessingAdapter`; ios/macos `Classes/` have symlink forwarders, mode 120000). One method-channel `setCaptureGain`. APM float PCM is int16-scaled (~±32768, NOT ±1.0) — ceiling = `0.7079*32768`. Slider 34%–200% (floor≠0 so it can't mute), default 100%, live mid-call. We CANNOT retune AGC (prebuilt libwebrtc exposes no target-level knob). See `project_voice_loudness_normalization`.
- **CRITICAL — SFrame key index must be set on every new cryptor.** Call `setKeyIndexForPeer` after enabling. Use `rotateKey` (not `setSharedKey`) in `setSframeKey`.
- **CRITICAL — Share WebRTC reconnection: receiver-initiates, sender-catches.** See `feedback_webrtc_patterns.md`.
- **CRITICAL — call/VC signal types are WHITELISTED in Rust, not passed through.** `handle_call_send_signal` / `handle_voice_channel_send_signal` (voice_handler.rs) map each signalType to a dedicated `HavenMessage`/envelope variant; unknown types are SILENTLY dropped. A new signal type needs 3 Rust touches: types.rs variant (`#[serde(default)]` fields), the send match arm, the incoming dispatch arm in swarm.rs. No codegen needed. See `feedback_mobile_call_audio_route.md`.
- **CRITICAL — renegotiation glare (one-way video).** Never drop an inbound `sdp_offer`: queue while busy + retry on failure (call_provider `_queueRenegOffer`). Camera auto-enable on video calls is STAGGERED (polite peer 300ms / other 1500ms, lower-peer-id-polite like invite glare). Any new renegotiation trigger must consider both sides acting simultaneously. See `feedback_renegotiation_glare.md`.
- **CRITICAL — DM/VC camera codecs are VP8-ONLY (`_constrainCameraCodecs` / `_preferVp8ForVideoTrackOnPc`).** Never widen the camera sender's codec list: H.265/AV1 payload types kill the iOS answerer outright, and even H264/VP9 entries make iOS's FIRST call of a session fail applying its own answer while VideoToolbox is cold ("Failed to set local video description recv parameters" → first call black, later calls fine). VP8/libvpx is what negotiation picks anyway. A failed inbound renegotiation ROLLS BACK to stable (voice_service) — without it the PC wedges in have-remote-offer and `createOffer` errors forever. Route inbound `sdp_offer` by CALL IDENTITY (`_service.hasActiveCall`), never by `status == active` (active lags ICE/DTLS by seconds; misrouted offers fall into initial-call setup and are silently consumed). See `project_ios_camera_black_screen_debug.md`.
- **CRITICAL — dialogs are keyboard-aware globally.** `showHollowDialog` pads every dialog by `viewInsets` + strips them (mirrors Flutter's Dialog) — NEVER add viewInsets padding inside a dialog builder (double-pad). `HollowDialog` scrolls content via Flexible and goes full-width under 600px. Mobile-only input flows prefer bottom sheets (`isScrollControlled` + manual viewInsets padding + SafeArea). See `feedback_dialog_keyboard_insets.md`.
- **App Lock (mobile):** PIN = numeric secret through the same Rust Argon2id flow; biometric = secret in flutter_secure_storage released after `local_auth` (3.x: named params, NO AuthenticationOptions). Lock-type marker lives in secure storage (readable BEFORE identity unlock). MainActivity MUST extend `FlutterFragmentActivity`. Speaker/deafen/badges + proximity/wakelock rules: `feedback_mobile_call_audio_route.md`, `project_app_lock_pin_biometric.md`.
- **Push tap → chat:** 3 entry points all required (`onMessageOpenedApp`, `getInitialMessage`, local-notif payload + launch details); handlers registered by MobileShell, cold-start taps buffered. Per-peer `plugin.show()` MUST set `payload: sender`. See `feedback_push_tap_navigation.md`.
- **CRITICAL — always `await` WebRTC resource disposal.** `RTCVideoRenderer.dispose()`, `RTCPeerConnection.close()`/`.dispose()`, and `MediaStream.dispose()` are async — unawaited calls leak ~200 MB per session.
- **CRITICAL — sender side needs `FileCompleted` emit too.** Any new field added to `FileHeader`/`StoredFile` will be missing from sender's UI unless the send path also emits `FileCompleted`.
- **CRITICAL — `flutter_rust_bridge` codegen:** run from project root with explicit args. Codegen errors if you `cd` into `rust/hollow_core/` first.
- **CRITICAL — message signing must use `message_signing_payload()` + timestamp parity.** ALL signing sites must use the canonical payload. Dart timestamps MUST be hydrated from Rust's signed value (not `DateTime.now()`).
- **CRITICAL — HollowAvatar: never pass `imageBytes:` from profileProvider.** `profileProvider` loads light profiles (no blobs). HollowAvatar auto-fetches from `avatarProvider`. Only pass `imageBytes:` for non-profile data (archive exports, explicit overrides).
- **CRITICAL — sender-side link previews (privacy).** Receivers MUST NEVER make HTTP requests to previewed URLs.
- **CRITICAL — sync batch receivers must check `message_id` existence before INSERT.** Edited messages bypass text-based UNIQUE dedup. See `feedback_sync_patterns.md`.
- **CRITICAL — `WsEvent::Disconnected` must clear ALL sync-gating state.** `synced_peers`, `key_request_in_flight`, `mls_bootstrap_requested`, `pending_messages` — all cleared alongside `ws_room_peers`.
- **CRITICAL — never use raw `OverlayEntry` inside `SelectionArea`.** Use `showDialog` with `barrierColor: Colors.transparent` instead.
- **CRITICAL — `HollowToast` from non-widget code (providers/notifiers) must pass `overlayState:`.** `Overlay.of(hollowNavigatorKey.currentContext)` throws "No Overlay widget found" — the Navigator's own context has no Overlay *ancestor* (the Overlay is its child). `HollowToast.show` has an optional `overlayState` param that inserts directly: `final o = hollowNavigatorKey.currentState?.overlay; if (o != null) HollowToast.show(o.context, msg, overlayState: o);`. Wrap in post-frame + try/catch, and run any critical state teardown (e.g. `_cleanup()`) BEFORE the toast so a toast failure can't abort it. See `feedback_toast_from_nonwidget_overlaystate.md`.
- **CRITICAL — Share-backed large files (>34 MB):** `FileHeader.share_ref` bypasses size checks in 3 places. Skip `PendingFileStream` when `share_ref.is_some()`. STUN-only. >34 MB files (DM + channel) now prompt a **confirmation dialog** (`confirmLargeFileShare`, `large_file_share_dialog.dart`) — "Send as Share / Cancel" — instead of silently auto-converting (channels) or rejecting (DMs); DM Share enabled (it was already transport-complete). See `feedback_share_backed_files.md`, `project_storage_manager.md`.
- **CRITICAL — sender stream temps (`.stream_send_*.tmp`) must be deleted after WS-relay sends.** A file send AES-encrypts to a ciphertext temp in `files/` to feed the stream; it only lives long enough to stream to ONLINE members (offline members re-fetch a fresh copy on demand). Delete it after the awaited WS stream UNLESS `pending_webrtc_sends.contains_key(file_id)` (a WebRTC send owns it, cleaned on `WebRtcTransferComplete`). Gossip (6+ image) keeps it (async Dart event). Boot-time sweep in swarm.rs mops orphans. Without this, WS-relay sends leaked a duplicate (doubled server disk). The stored file itself is PLAINTEXT (at-rest file enc is a separate epic, HOLLOW_PLAN 1949). See `feedback_stream_send_temp_cleanup.md`.
- **Storage Manager:** Settings → "Files & Storage" (merged category) has a dashboard (total + segmented usage bar + per-conversation breakdown w/ trash + "⋯" cleanup menu) in shared `lib/src/ui/settings/storage_section.dart` (`storage_provider.dart` + `api/storage.rs` FFIs). Files/vault-cache caps are ENFORCED via `enforce_storage_caps` on `FileCompleted` (were no-op sliders before). Clearing bytes keeps signed FileHeader rows → re-downloadable cards. See `project_storage_manager.md`.
- **MLS coordinator model:** `is_mls_coordinator()` — deterministic election (lowest online peer_id). Recovery targets coordinator, NOT owner. All MLS correctness rules (persist on encrypt, decrypt-fail sync, recovery, coordinator exclusion, rejoin cleanup) in `feedback_mls_patterns.md`.
- **CRITICAL — `get_missing_file_ids()` checks disk, not just DB.** Files may exist without valid `completed_at` — reads directory into HashSet to prevent redundant transfers.
- **Public channels:** Per-channel `is_public: bool` in `ChannelInfo` CRDT. Public channels skip MLS — plaintext `HavenMessage::PublicChannelMessage` via `SendToRoom`, still Ed25519-signed. Send handlers in `message_ops.rs` branch on `server.is_channel_public()`. Toggle via globe icon (MANAGE_CHANNELS permission). Broadcast channels = public + "Admin+" posting mode. Public Channel Browser in `bottom_bar.dart`. See `project_public_channels.md` for guest sync, sender profiles, and browser implementation details.
- **CRITICAL — channel-chat device→master collapse + sync self-heal (multi-device, LIVE 2026-06-24).** A channel message is SIGNED by + attributed to the sender's MASTER (`message_ops` channel send signs with `local_peer` = master). The MLS receive path resolves `sender_master = resolver::resolve(sender_peer_id)`; the PUBLIC handlers (`HavenMessage::PublicChannelMessage` + Edit/Delete/Add/RemoveReaction twins in swarm.rs, + `fetch.rs::try_process_channel_msg` BOTH branches) MUST `resolve(peer_str)` too — else the row is stored under the DEVICE id (raw `12D3KooW…` bubble) AND the sig fails verify (signed-with-master, verified-against-device). Wire-only, no FFI/codegen. **Dart display:** every channel display site resolves `senderId→master` via `deviceLinkProvider.identityOf` (cold/single-device → no-op): `channel_message_bubble.dart` (profile/nickname/name/avatar/colour/tap), `channel_chat_pane.dart` + `mobile_chat_route.dart` (reply banner, reply-preview author, search, pinned, **message-proof `senderPeerId`** — the proof rebuilds the master-id payload). File-request stays RAW (`handle_request_file` resolves master→one device internally). The Dart fix only RE-RENDERS. **Self-heal for rows stored device-keyed BEFORE the fix:** channel sync is `INSERT OR IGNORE` + an `already_exists` skip, so a later VERIFIED copy can't overwrite a wedged corrupt row. `MessageStore::repair_channel_message_sender` UPDATEs sender/sig/pk when a synced item's sig VERIFIES (`crypto_handler::verify_message_signature`, which binds PeerId↔pubkey → unforgeable) and the stored sender differs — wired into BOTH channel-sync receive paths (swarm.rs plaintext + `sync_handler::handle_envelope_channel_sync_batch`, which now verifies inline). NEVER fake a `ChannelMessageEdited` event to refresh (marks "(edited)", doesn't move the in-memory senderId); the corrected sender renders on the next `loadHistory`. See `feedback_channel_display_device_master_collapse.md`, `feedback_channel_sync_self_heal_corrupt_sender.md`.
- **CRITICAL — channels_tab auto-save must guard against empty channelListProvider.** Check `channels.isNotEmpty` before auto-saving derived layout state.
- **CRITICAL — CRDT property changes need optimistic UI updates.** Update `channelListProvider` BEFORE the FFI call — `CrdtStore` is fire-and-forget, DB may be stale when `ServerUpdated` fires.
- **Roles & permissions:** 4 power roles (Owner/Admin/Moderator/Member) + cosmetic labels. `role_permissions` overrides `default_permissions()`. Tier-gated editing. See `project_roles_design.md`.
- **CRITICAL — per-channel MLS subgroups (Option B, TEXT + VOICE both live).** A restricted channel (`visibility != Everyone && !is_public`, i.e. `ServerState::channel_uses_subgroup`) is MLS-encrypted under its OWN group keyed `crate::crypto::subgroup_id(server, channel)` = `"{server}#{channel}"` (`split_group_key` is the inverse), NOT the server-wide group — so visibility is cryptographically enforced (non-qualifying members never get a decryptable copy). `MlsManager.groups` is opaque-string-keyed; the MLS `HavenMessage` variants carry `#[serde(default)] channel_id: Option<String>` (`None`=server group, backward compatible). Membership = `crypto_handler::reconcile_subgroups_for_server` (coordinator-gated, idempotent; run on role/visibility change + received CRDT ops): removals queued directly, additions PULL the joiner's KeyPackage. THE SUBTLETY: subgroup coordinator now PREFERS the OWNER when online (owner always `can_see_channel` → globally-agreed single coordinator; avoids split-brain where two qualifying masters each create divergent groups with the same id), else lowest online master who STILL qualifies AND already holds a leaf. Persistence reload (swarm.rs startup + api/network.rs push-fetch) must enumerate subgroup ids. **PHASE 2 (VOICE) LIVE 2026-06-22:** a restricted voice channel derives its SFrame key from the SUBGROUP `export_secret`, not the server group. `NetworkEvent::MlsEpochChanged` gained `channel_id: Option<String>` in BOTH `node::types` and `crate::api::network` (THIS is an FFI change — codegen required, unlike the HavenMessage variants). All MlsEpochChanged emit sites pass channel_id. `voice_handler::handle_voice_channel_join` derives SFrame from `subgroup_id` when restricted, REJECTS the join if `!can_see_channel`, and `request_subgroup_bootstrap` if it qualifies but lacks the subgroup (never falls back to the server key). Welcome handler emits MlsEpochChanged. Dart `voiceChannelProvider` caches SFrame keyed by `(serverId, channelId)`; a restricted channel uses ONLY its subgroup key. **VC auto-leave on lost visibility:** `auto_leave_invisible_voice_channels` runs on BOTH the plaintext AND the MLS `CrdtOp` apply paths (MLS usually wins the race + its path had no reconcile/auto-leave) + Dart `_evictVoiceIfInvisible` calls `leaveChannel()` + `onLocalLeft` runs full WebRTC/audio teardown when server-forced. **VC participants key on the ROUTABLE WS sender (`peer_str`), NEVER the MLS leaf credential (`sender_peer_id`)** — for a multi-device sender they differ; the leaf isn't a live socket → a phantom 2nd participant + unreachable SDP/ICE. See `project_per_channel_mls_subgroups.md`, `feedback_owner_coordinator_mls_recovery.md`.
- **CRITICAL — server-group MLS recovery: owner-coordinator + plaintext CRDT op broadcast.** Server-group adds/recovery PREFER the OWNER as coordinator (`crypto_handler::elect_server_coordinator` / `server_bootstrap_target`) — the owner always holds the group, so it's the single authoritative committer (linear epochs); a non-owner committer can add a member then fan-out-MISS another leaf (e.g. the owner), which diverges permanently with no retry (the 3+-distinct-member deadlock). CRDT-mutating handlers in `sync_handler` ALWAYS also broadcast the op as PLAINTEXT `CrdtOpBroadcast` (idempotent — op_log dedups, receiver re-validates author+permission), not just on MLS-send failure: an MLS op silently drops on a receiver at a skewed epoch with no recovery, permanently losing server metadata (a new channel it never learns exists). The relay sees signed server metadata — accepted trade-off. MLS `WrongEpoch` decrypt-fail also fires a server-group `SyncRequest` (op-log delta, 1s dedup) + recovery/Welcome sends one. See `feedback_owner_coordinator_mls_recovery.md`.
- **CRITICAL — channel visibility/posting UI reactivity (desktop + mobile).** Visibility/posting CRDT changes propagate correctly; the UI must REACT. `_refreshServerState` (event_provider.dart) reloads `channelListProvider` AND invalidates `serverChannelsProvider(id)` on a ramp (0/120/400/1000ms — defeats the fire-and-forget CrdtStore DB-write race; a single delay was unreliable). Eviction listeners on `visibleChannelsProvider`: desktop `hollow_shell` selects the next visible channel else home; mobile `mobile_chat_route` pops to Chats. MOBILE GOTCHA: `channelListProvider` exists only for the SELECTED server, but the Chats tab has none — so `MobileChatRoute.initState` must `loadForServer` on open and the Chats-tab `_ChannelList` must role-filter (like `visibleChannelsProvider`) + react to `serverChannelsProvider`/`myRole`/`myPermissions`. NEVER `ref.invalidate` in `initState` (ProviderScope not wired yet → throws; defer to a post-frame callback). See `feedback_channel_visibility_posting_ui_reactivity.md`.
- **CRITICAL — new CrdtPayload variants must emit `ServerUpdated` in both match blocks.** Don't fall into `_ =>` wildcard (emits `SyncCompleted`, no provider invalidation). Explicitly list in both `handle_envelope_crdt_op` and `handle_incoming_request`.
- **CRITICAL — received CRDT ops must be persisted via `insert_crdt_op` at EVERY apply site.** `op_log` is `#[serde(skip_serializing)]` — the state JSON alone loses history on restart; a member then serves near-empty op logs to joiners. All three sync-merge sites (plaintext `SyncResponse`, Olm `SyncResp`, MLS `handle_envelope_sync_resp`) insert all incoming ops (INSERT OR IGNORE, idempotent). Joins additionally send `HavenMessage::ServerStateSnapshot` BEFORE the op log (op logs compact at 1000 ops and can't be trusted for reconstruction); the joiner adopts it only while its join is pending. Self-leave `MemberRemoved { peer_id == op.author }` is always allowed; permission checks validate `op.author`, never the transport sender. See `feedback_crdt_sync_persistence.md`.
- **CRITICAL — `RoomMembers` is the authoritative presence snapshot.** The relay only broadcasts `PeerLeft` for rooms a connection is CURRENTLY in — entries from a peer's earlier connection go stale and pin them "online" forever. The `RoomMembers` handler diffs old vs new sets and emits `PeerDisconnected` for vanished peers; `PeerLeft` refreshes (re-joins) any room that still lists the leaving peer. Any cache keyed on room membership must tolerate missed `PeerLeft` events. See `feedback_ws_presence_stale_rooms.md`.
- **CRITICAL — list rows with per-item loaded state need `ValueKey(item id)`.** The mobile chats tab mixes DM/server rows in a reordering ListView; without keys Flutter re-parents row State across DIFFERENT conversations (a joined server showed another server's channel list while every logged ID looked correct). Also implement `didUpdateWidget` reload when the identity prop changes. See `feedback_listview_state_reuse_keys.md`.
- **HollowTooltip: always use `_dismiss()` pattern** — immediate overlay removal, no reverse animation.
- **`scrollable_positioned_list: ^0.3.8`** — sentinel pattern with `itemCount: messages.length + 1`. Do not remove this package.
- **`showHollowDialog` overlays need a `Material` ancestor** for `Text` widgets, otherwise yellow debug underline.
- **Android cross-compilation:** SQLCipher needs prebuilt OpenSSL 1.1.1w per-arch (vendored). Target-prefixed env vars must be **system env vars** (Cargo `[env]` doesn't reach cargokit). See `feedback_android_platform.md`.
- **CRITICAL — Android TLS: Rust crates must use `webpki-roots`, never `native-roots`.** Switching to `native-roots` silently breaks all WSS connections on Android.
- **Android/iOS data directory:** `hollowDataDir` from `hollow_data_dir.dart`. Mobile uses `getApplicationDocumentsDirectory()/hollow`. Rust: `set_data_dir()` FFI before `start_node()`.
- **Mobile app lifecycle:** `WidgetsBindingObserver` on Android/iOS — resume: WiFi lock + rejoin WS rooms, pause: release WiFi lock.
- **Mobile UI architecture:** `lib/src/ui/mobile/`. `MobileShell` (4-tab) replaces desktop below 600px breakpoint. Chat views push onto root navigator. All mobile code gated behind platform check — desktop unaffected. See `feedback_mobile_ui_patterns.md` for all mobile-specific rules (selection providers, pill layering, bottom sheets, background image, etc.).
- **CRITICAL — mobile floating pills must NOT go in `app.dart` builder.** Pills in MobileShell + MobileChatRoute stacks. Only `IncomingCallOverlay` in builder. See `feedback_mobile_ui_patterns.md`.
- **Widget tests:** `test/helpers/test_app.dart` — `pumpHollowMobile()` mocks FFI providers. Tests run in ~1s without device.
- **Feature matrix:** `reports/FEATURE_MATRIX.md` — mobile port punch list.
- **Forked `flutter_webrtc` at `packages/flutter_webrtc/`** — pubspec `path:` reference. Adds WASAPI loopback, native screen recording, native screen share. When iterating on fork's native C++, delete `build/windows/x64/plugins/flutter_webrtc/` before rebuilding, and **always build `--release` if testing from the Release folder**.
- **Screen recording/share/audio:** Native capture (no ffmpeg) on Windows (Graphics Capture API) and macOS (ScreenCaptureKit). Screen share audio uses out-of-process data channel on Windows (ADM interference workaround), Process Tap on macOS. See `project_screen_capture.md` for full architecture.
- **CRITICAL — Linux window close: minimize to taskbar, not tray.** Never add tray logic for Linux (AppIndicator broken). Second close while minimized = quit.
- **CRITICAL — Linux `record` package needs `parecord`.** Not on PipeWire systems. Mic test crashes; voice calls work fine (libwebrtc). Needs WebRTC path for Linux mic testing.
- **Flatpak packaging:** `flatpak/` directory. `build-flatpak.sh`. Requires `--socket=x11` (not `fallback-x11`). Data dir: `~/.var/app/com.anonlisten.Hollow/`.
- **CRITICAL — Windows annotation mode: use `window_manager` maximize/unmaximize.** Never raw Win32 or `setFullScreen`. See `feedback_annotation_window_management.md` for enter/exit sequence.

## Semantic Memory Search (hollow-memory MCP)
- **Tool:** `memory_search(query, limit=5)` — semantic vector search across all memory files, HOLLOW_PLAN.md, WHITEPAPER.md, CLAUDE.md. Use it proactively when you need to recall decisions, patterns, or context by meaning rather than exact filename.
- **When to use:** Fuzzy recall ("what was that thing about..."), cross-referencing decisions, finding relevant memories before making architectural choices, or when you're unsure which memory file contains the answer.
- **Reindex:** Run `memory_reindex()` after modifying memory files, HOLLOW_PLAN.md, or CLAUDE.md (automatic during `/compush`).
- **Save liberally:** Discovery is by meaning now, not by scanning an index. Save granular patterns, decision rationale, subtle bug causes, non-obvious code behaviors — anything useful to recall later. The threshold is "would finding this by meaning help a future session?" not "is this important enough for the index?"
- **Location:** `tools/hollow-memory/` — local ONNX embeddings, sqlite-vec, zero API costs.
- **Wiki:** `tools/hollow-memory/wiki/` contains ~40 machine-optimized markdown files covering every UI panel, data flow, background system, provider, and Rust module. Each is chunked by `## ` heading and indexed. Search queries like "voice channel WebRTC flow" or "CRDT sync handler" return precise wiki results with file paths and function references. Update relevant wiki files during `/compush` when features change.

## Rules
- Never commit secrets, keys, or credentials.
- Rust handles: networking, crypto, CRDTs, storage engine. Dart handles: UI, app logic, state management.
- All crypto operations must use constant-time implementations.
- Ask before making architectural decisions not covered in HOLLOW_PLAN.md.
- **HOLLOW_PLAN.md is the authoritative source** for all phase details, feature checklists, and completion status. Don't duplicate that information here or in memory files.
- **VPS deployment:** Ask user — never store credentials.
- **Local dev commands:** Can run `cargo check/test/clippy`, `flutter_rust_bridge_codegen generate`, `flutter analyze` freely.
- **Building/running the app:** User runs `flutter run -d windows` themselves for testing on their two laptops.
- **VPS SSH:** `ssh ubuntu@141.227.186.209` — key-only, no passphrase. Can be used freely for config checks, log inspection, deployments.

---
> Source: [VitalikPro13/HOLLOW](https://github.com/VitalikPro13/HOLLOW) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-06-24 -->
