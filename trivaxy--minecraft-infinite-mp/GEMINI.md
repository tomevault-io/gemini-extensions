## minecraft-infinite-mp

> **This landed as a feature during the bug-fixing phase below, at the user's explicit call**,

# Working notes for agents

## The command system was replaced with Brigadier, and every command deleted

**This landed as a feature during the bug-fixing phase below, at the user's explicit call**,
and it is the one thing on this page that is not a bug fix. Read it before touching anything
that runs or tests a command.

The old framework was an unfinished leftover: one abstract `Command`, a static `Command[256]`
with a static cursor, `holder.command.split(" ")`, and arity dispatch (`args.length == 4/3/2`)
that made `/tp Alice 1 2` parse as coordinates. Tab completion read a second, hand-written
`suggestions()` table that the parser never consulted, so the two disagreed. Non-`CommandException`
throwables — commonly an out-of-bounds `args[n]` — were logged at WARNING and the sender was
told nothing at all.

It now runs on `com.mojang:brigadier:1.3.10`. See DEVELOPING.md for the shape and for how a
command is registered. What matters here:

- **The dispatcher ships nearly empty.** All 64 commands were deleted, not translated — the
  user's call, on the grounds that the set was bloat. `CoreCommands`/`ClientCommands`/`ServerCommands`
  are the three registration hooks. Commands come back one at a time, and **`/give` is the first
  one back** (`commands/core/GiveCommand.java`, op-only, both sides). It takes the two forms the
  user asked for — `/give <item id>` and `/give <player> <item id>` — and no count argument, so
  it is *not* a drop-in for the twelve dead smoke suites below, which all called `/give <id> <count>`.
  It hands over one full stack. Read the DEVELOPING.md note on sibling argument branches before
  adding a second command shaped like it: registration order decides which form wins, and a
  branch that throws loses its error message to whichever sibling parsed.
- **Twelve smoke suites are dead as a result**, and this is known and accepted. The rig drove
  the server with `setblock` (27 uses), `teleport` (15), `weather` (8), `give` (8), `clearinv`
  (8), `gamerule` (6), `effect` (5), `time`/`summon`/`count` (3 each) and `kill`. `:server:smokeTest`
  cannot pass until this is dealt with. **The agreed follow-up is to give `Bot` direct
  server-side helpers** (`setBlock`, `giveItem`, …) so the rig stops depending on the command
  surface at all, rather than to bring those twelve commands back to serve it.
- **`/stop` is back** (`net.minecraft.server.commands.StopCommand`, op-only, dedicated server), initiating a graceful server shutdown.
- **`/kick <player>` is back** (`net.minecraft.server.commands.KickCommand`, op-only, dedicated server).
  It lives in `ServerCommands` rather than `CoreCommands` because `ConfigManagerClient.kickPlayer`
  is an empty method — registered in core it would be a command that silently does nothing in
  singleplayer. It answers the sender *before* it kicks, so kicking yourself still confirms.
  Covered by `KickSmokeTest` (needs `kickbot` in `run/server/ops.txt`).
- **`/ban <player> [reason]` and `/unban <player>` are back** (`BanCommand`, `UnbanCommand`,
  op-only, dedicated server). **Nothing new was written to make them work** — `BanList`,
  `BanEntry`, `banned-players.txt` and the login refusal in `ServerConfigurationManager.login`
  were all still there, unreferenced by any command since the purge; these two are a front end
  onto `ConfigManager.banPlayer`/`pardonPlayer`. `/ban` takes a name, not an online player, so
  an absent player can be banned; if they *are* online it kicks them with the same wording the
  login refusal uses. The reason is a greedy trailing string and is optional; omitted, `BanEntry`
  supplies its own default ("Banned by an operator."). `BanEntry` also carries a `banEndDate`
  that nothing sets — **temporary bans are already modelled and already expire** (`removeExpiredBans`
  runs on every `isBanned`), so a `/tempban` would need a date parser and nothing else.
  `/unban` reads `getBannedPlayers().isBanned` (a `ServerConfigurationManager` cast —
  `ConfigManager` does not expose it) so a typo comes back as "That player is not banned."
  rather than silently doing nothing. Covered by `BanSmokeTest` (needs `banbot` in
  `run/server/ops.txt`); it always pardons what it banned, and proves the pardon by logging
  back in. **Together, /kick /ban /unban moved `HelpSmokeTest`'s hardcoded op command list from
  three entries to six.**
- **`/tp <x> <y> <z>` is back** (`commands/core/TpCommand.java`, op-only, both sides — the
  client implements `teleportPlayerTo` for real, so unlike `/kick` it belongs in `CoreCommands`).
  **Nothing new was written for the coordinate parser either**: `Vec3Argument`, `WorldCoordinate`
  and `WorldCoordinates` already parsed `10 20 30`, `~ ~5 ~` and `~-2.5 ~ ~1.25`, and had no
  caller outside `CommandFrameworkSmokeTest`'s scratch tree. It teleports the sender, and the
  sender of a command block's line is the nearest player within 16 blocks — so `/tp ~ ~ ~` in a
  command block yanks that player onto the block (`~` is the block, the mover is the player),
  and with nobody in range it is "That command has to be run by a player." That source carries a
  null `ConfigManager` (nothing in `:core` can hand it one), so `/tp` moves the entity directly
  there rather than through `teleportPlayerTo` — right in singleplayer, but on a dedicated server
  the client is never told and the next movement packet rubber-bands the player back, so it does
  nothing. None of this is exercised: the command block path has no coverage at all.
  One thing did change under it: `CommandSourceStack.of` now stores `posY - yOffset`, so `~`
  resolves against the feet — see the DEVELOPING.md note. That is a no-op on the dedicated
  server (`EntityPlayerMP` zeroes `yOffset`) and therefore **not something `TpSmokeTest` can
  catch**; it is singleplayer, where `LocalPlayer` keeps 1.62, that `/tp ~ ~ ~` would have
  walked up. Covered by `TpSmokeTest` (needs `tpbot` in `run/server/ops.txt`), which never
  calls `sink()` — a bot that holds still is a bot the server counts as floating, and 80 ticks
  of that is a kick, so it spends the counter with a short `settle()` and re-anchors between
  phases. The op list in `HelpSmokeTest` is now **seven**.
- **`/op <player>` and `/deop <player>` are back** (`OpCommand`, `DeopCommand`, op-only,
  dedicated server — `ConfigManagerClient.opPlayer`/`deopPlayer` are empty, so `CoreCommands`
  would make them silent no-ops in singleplayer). **Nothing new was written here either**:
  `ServerConfigurationManager.opPlayer`/`deopPlayer` already maintained the `ops` set, rewrote
  `ops.txt` and broadcast an `OpPacket`; `ConfigManager` already exposed `isOp`, so unlike
  `/unban` neither of these needs a cast. Both take a name rather than an online player, so an
  absent one can be opped, and both refuse a no-op ("That player is already an operator." /
  "That player is not an operator.") rather than doing nothing to a typo.
  What makes the grant live is `NetServerHandler.createCommandSource` asking `isOp` on **every**
  command, so a player opped mid-session is opped for their next line without relogging — that,
  in both directions, is what `OpSmokeTest` actually checks (needs `opbot` in `run/server/ops.txt`
  and needs `sidekick` to stay out of it; it always deops what it opped). Note that `saveOps`
  rewrites the file from a `HashSet`, so **every /op or /deop reshuffles the order of ops.txt**
  and drops any comment or blank line in it. The op list in `HelpSmokeTest` is now **nine**.
- **`/gamemode <mode> [player]` is back** (`commands/core/GamemodeCommand.java`, op-only, both
  sides). `GamemodeArgument` takes a name, an initial or `0..3`. The command is four lines; what it
  needed underneath is the section below. The op list in `HelpSmokeTest` is now **ten**.
- **`/score add|remove|set <player> <amount>` is back** (`commands/core/ScoreCommand.java`,
  op-only, both sides). **Nothing new was written to carry the value to the client**: the diff in
  `EntityPlayerMP.onUpdateEntity` (`score` against `lastScore`, sent as a `ScorePacket`) and the
  `ScorePacket` handler on the client were both already there, so writing `Player.score` is the
  whole command — in singleplayer it runs on the player itself. The catch that costs a debugging
  session is that `onUpdateEntity` is driven by `NetServerHandler`'s **movement** handling, not
  by the tick loop: a player whose movement packets the server is ignoring never learns their
  score changed. `remove` floors at zero rather than going negative, and the amount takes a new
  `NonNegativeIntArgument`, so `-5` comes back as "Amount must be 0 or greater" rather than
  parsing as an `add` in disguise. All three subcommands require an explicit player; there is no
  self form. Covered by
  `ScoreSmokeTest` (needs `scorebot` in `run/server/ops.txt` and needs `sidekick` to stay out of
  it). The op list in `HelpSmokeTest` is now **eleven**.
- **Command blocks stayed.** The block, the tile entity, its NBT and the edit screen are all
  untouched; `CommandBlock.onTick` runs its line through `Commands.active()` with a source
  anchored at the block, so `~` means the block rather than whoever is standing nearby. What
  went was the `CommandsManagerCore` registry it carried (a fourth copy of every core command)
  and its ~150-line stub `ConfigManager`. The old `catch (Exception) {}` around the whole
  dispatch is gone too, so a command block now reports its errors to the nearest player unless
  `suppressOutput` is set. With nothing registered it has nothing to run yet.
- **There is no tab completion**, and it is not a thing to re-add casually — see DEVELOPING.md.
  The old one read the deleted `Command[256]` and a hand-written `suggestions()` table and went
  with them. `ChatScreen`'s Tab key now completes player names only.
- `PROTOCOL_VERSION` was **not** bumped. Nothing on the wire changed: commands still ride
  `ChatPacket`.
- Deleted with them: the whole `/worldedit` suite and `game.world.edit` (the selection was
  global statics, never per-player, so it was multiplayer-broken anyway), its `exporter`
  package, `CommandUseTimer` + the unregistered `CommandDevItems`, and 247 of the 253 keys in
  `commands.lang`. `DebugStick` was the WorldEdit wand and is now an inert position readout —
  **whether that item stays, changes or goes has not been decided.**
- `ConfigManager` moved out of `net.minecraft.commands` to `net.minecraft.game.player`. It is
  the server's player registry, not a command type; commands were only ever a caller.

Two things worth knowing because they were found rather than designed:

- **The stdin thread is running before `worlds` is populated.** Building the command source at
  queue time NPEs on a command typed during startup; it is built at drain time instead, which
  is why `addCommand` takes a `Supplier<CommandSourceStack>` rather than the source itself.
  Found by piping a command into the *shipped server jar*, not by any test.
- **Brigadier does not filter completions by `requires()`** — the trap waiting for whoever
  wires completion up later. Vanilla only appears to because it sends each client a
  pre-filtered command tree.

### Verifying command work

```bash
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.CommandFrameworkSmokeTest
                                            # the framework alone; no server needed
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.CommandWireSmokeTest
                                            # needs a running server; no op needed
```

`CommandFrameworkSmokeTest` still registers its own scratch tree, because `/give` alone is not
enough to exercise the framework. It covers dispatch, `requires()` gating, the error cursor, and relative coordinates.
`CommandWireSmokeTest` is the half it cannot reach: that a real `ChatPacket` beginning with `/`
still arrives at the dispatcher and gets an answer back.

```bash
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.GiveSmokeTest
                                            # needs a running server and "givebot" in ops.txt
```

`GiveSmokeTest` covers `/give` itself: both forms landing in a slot, and the three ways it can
be told no.

```bash
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.TpSmokeTest
                                            # needs a running server and "tpbot" in ops.txt
```

`TpSmokeTest` covers `/tp` and the `~` reader on the wire: absolute coordinates landing exactly,
offsets up, down and fractional, a bare `~ ~ ~` moving nothing, and the two ways a position can
be malformed.

```bash
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.OpSmokeTest
                                            # needs a running server, "opbot" in ops.txt,
                                            # and "sidekick" not in it
```

`OpSmokeTest` covers `/op` and `/deop`: both no-op refusals, the confirmation lines, and — the
half worth having — a second bot gaining and then losing op mid-session, proved by what it is
allowed to run rather than by the chat.

```bash
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.GamemodeSmokeTest
                                            # needs a running server and "modebot" in ops.txt
```

`GamemodeSmokeTest` covers `/gamemode` and the server behaviour behind it; see the gamemode section
for what it pins down and how it was measured red.

```bash
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.ScoreSmokeTest
                                            # needs a running server, "scorebot" in ops.txt,
                                            # and "sidekick" not in it
```

`ScoreSmokeTest` covers `/score` on the wire: the permission gate, the arithmetic including the
floor at zero, a named player's score reaching that player's own client, the sender's three
confirmation lines, and the four ways it can be told no. Two things about it are worth knowing
before editing it. It never calls `settle()`, because that helper sinks 0.2 before each send:
once the login corrective teleport clears `hasMoved`, the bot never re-echoes `lastPos` within
the 0.1 re-arm tolerance and the server silently ignores every packet it sends for the rest of
the run — no ticks, so no score sync, while chat replies keep arriving because `handleChat` is
ungated. And every expected value is relative to a score the test pins itself, because a set to
the score the player already has sends nothing (`score == lastScore`) and would make the packet
checks coin flips. All seven suites pass at HEAD.

**Neither covers the command block** — that path needs a real `World` with a redstone signal
and has not been exercised at all, by a bot or by hand.

## Gamemode is server-authoritative now, and the client controller is transport-only

Gamemode used to be a client-only idea. Nothing carried it on the wire, nothing on the server
acted on it, and `Minecraft.clientSetGamemode` was the only writer.

- **One source of truth**: `player.properties.gamemode`, saved as the NBT byte `Gamemode` in the
  player .dat and read by ~40 places in `:core` that run on both sides. `mc.gamemode` no longer
  answers "what mode am I"; it answers "do my interactions go over the wire", and there are two of
  them — `GameMode` (local) and `MultiplayerMode` (networked). `SurvivalMode`, `CreativeMode`,
  `AdventureMode` and `SpectatorMode` are **gone**; their behaviour moved into `GameMode`, branching
  on the mode. That collapse is the fix for the real bug: `MultiplayerMode extends SurvivalMode`, so
  all fourteen `instanceof CreativeMode` / `SpectatorMode` tests outside the package read as
  survival over the wire and creative, adventure and spectator did nothing in multiplayer. They are
  `properties` checks now, and `PlayerProperties` has `isCreative()` / `isAdventure()` /
  `isSpectator()` beside the raw int the old comparisons still use.
- **`GamemodePacket` (id 73, server→client)** carries it: at login from `NetLoginHandler.doLogin`
  after the NBT is read, from `recreatePlayerEntity` (respawn and dimension change are both that one
  method, and `Player.clone` already carries `properties` across), and on every `/gamemode`.
  Self-only — nothing consumes another player's mode; making spectators invisible to others would
  turn it into a broadcast.
- **`ConfigManager.setGamemode`** is the seam, exactly like `teleportPlayerTo`: the server sets and
  sends the packet, the client sets locally. That is why `/gamemode` can live in `CoreCommands` and
  still work in singleplayer.
- **The server enforces it now**, which it never did: no floating kick for creative or spectator
  (`NetServerHandler`), no rubber-band out of blocks for spectator, an instant break for creative in
  `ItemInWorldManager.useBlock`, and no drops and no tool damage for a creative break in `hitBlock`.
- **`CreativeSlotPacket` (id 107, client→server)** is how the creative inventory reaches the server:
  the client says "this stack is now in this slot", and the server applies it **only if it agrees
  the sender is in creative**, otherwise sending the real contents back. That gamemode check is the
  whole security of it — it is a client asking the server to conjure an item. What decides *when*
  to send one is a diff, not a click handler: `CreativeScreen` snapshots `player.container` and
  sends what changed on each update, the server's own detectAndSendChanges in reverse. Every route
  into that screen — mouse, shift, the drop key, the number keys, the bin — writes through the
  player container, and hooking them one at a time is how B-49 happened. Pick-block is the one
  creative write that happens with no screen open at all, so `GameMode.swapItem` sends its own
  (B-49b); anything else added later that moves items client-side in creative needs the same. `MultiplayerMode.windowClick`
  sends nothing at all when the open container is the creative one: it exists only on the client, so
  its slot numbers are meaningless on window 0 (B-20 records the same trap for the number keys).
- `ModeSwitcher` sends `/gamemode` in multiplayer rather than switching locally, so the server
  refuses a non-op; `NetworkWorld` no longer inherits the singleplayer `WorldSettings` statics.
  `DeathScreen`'s permadeath spectator switch is singleplayer-only — driving it server-side in
  multiplayer is **not done**.

`GamemodeSmokeTest` covers the server half (needs `modebot` in `run/server/ops.txt`): the packet at
login and on each form of the command, the mode surviving a relog, the floating kick exempting
creative and still catching survival, the instant break, no drops for it, and the creative slot
request being accepted in creative and refused in survival. **Measured red/green** — reverting the
`hitBlock` guard turns the drops check red, reverting the `useBlock` branch turns the instant-break
check red, and reverting the flight gate turns the creative-floating check red. The drops check only
means anything because the bot is holding a pickaxe: what stone drops is `canHarvestBlock`, so
bare-handed it is green against any build.

**The client half has no automated coverage and needs a human** — the mode collapse touches block
breaking, reach, flight, the HUD and the inventory screens. The client starts clean; nobody has yet
played creative, adventure or spectator in either world since.

## Current goal: bug-fixing phase

Multiplayer works end to end — login, chat, chunk streaming, movement, and all five dimensions
running server-side. What's left before new features is a backlog of real bugs found by playing.

**We are not starting Phase 4 (dimension transfer) until the board below is clear.** Layering
dimension transfer on top of unverified movement and sync would make transfer bugs nearly
impossible to diagnose.

### How a session works

**One bug per session.** Pick the top open bug, fix it, verify it, mark it done, stop. Don't
opportunistically fix three things — the point is that each fix gets a real verification pass
instead of a plausible-looking diff.

A session is done when:

1. The bug has a **named root cause**, not a symptom description. "The counter is only reset in
   the branch where it is never read" — not "players get kicked".
2. The fix is **verified against something that fails without it.** A log line from real play, a
   measurement, or a smoke test that goes red when reverted. Bot what is trivially bottable
   yourself; ask the user to play what is not — see "Bot it or play it" below.
3. `./gradlew build` is clean and `:server:smokeTest` passes.
4. The board below is updated with what was found, including anything ruled out.

Ruling a suspicion out is a real result. Record it.

---

## Bug board

Ids are unique across both tables and never reused — take the next free number, and a letter
suffix (`B-16b`) for a bug found while fixing another or regressed out of its fix.

Entries written before the `:core` merge are left as they were, so they name classes and paths
that have since moved. `Packet7UseEntity` → `UsePacket`, `Packet15Place` → `PlacePacket`,
`Packet16BlockItemSwitch` → `ItemSwitchPacket`, `Packet27Position` → `PositionPacket`,
`Packet31RelEntityMove` → `MovePacket`, `Packet32EntityLook` → `LookPacket`,
`Packet33RelEntityMoveLook` → `MoveLookPacket`, `Packet102WindowClick` → `ClickPacket`;
`game.item.crafting` → `game.item.recipe`; and anything described as living in "both trees" or
"the shared code" is now a single file under `core/`. See DEVELOPING.md.

### Open

| # | Bug | Where | Status |
|---|---|---|---|
| B-13 | **Sprint is cancelled while crawling.** `LocalPlayer.handleSprinting` force-clears sprint whenever `crawling` is true (an anti-crawl-sprint guard), but the crawl key can be pressed *during* a sprint with no headroom check (`canCrawlHere` only gates un-crawling) — so a sprint into a 1-block gap is legal, yet the sprint flag dies the same tick. Reported while verifying B-12 as "beginning a sprint doesn't break me out", which may actually be this rather than the rubber-band. Decide: should sprint-crawl be allowed (remove the guard), or should starting crawl break sprint cleanly first? | `client/player/LocalPlayer.java:185-189` | From reading during B-12; **unconfirmed** |
| B-01 | **Spawn point can land in open ocean.** `findSpawnPoint` random-walks ±64 blocks looking for solid ground and gives up after 1000 attempts, then uses wherever the walk stopped — no suitability check, no warning. Observed spawning ~1000 blocks from origin in water, which is about what 1000 random steps gets you. | `game/world/dimension/Dimension.java:100` | Diagnosed by reading; **confirmed during B-05** — a fresh login at (1011.5, 129.62, 287.5) fell 19.7 blocks before anything stopped it and then read as supported to a check that only water, lava or a mount can satisfy. Open water ~20 blocks deep, ~1000 blocks out, as predicted. The spawn Y ends in .62, so check for a second stance/feet swap while you are in there |
| B-02 | **Container contents may not sync when several blocks change in one chunk in one tick.** The tile-entity loop decodes packed coordinates out of `numBlocksToUpdate` (the *count*) where it should read `blocksToUpdate[i2]`. Repro: place ~6 chests in one chunk within a second, put items in, have the other player open each one. | `server/world/ServerChunkDistance.java:290-292` | Predicted; **the defect is confirmed by inspection** — the loop decodes all three axes out of `numBlocksToUpdate`, so every iteration resolves the same wrong coordinate. The player-visible symptom is still unobserved |
| B-03 | **Chunks may stream in slowly.** One chunk per movement packet, gated on fewer than 4 packets pending. Symptom would be terrain crawling in while running or flying. | `server/player/EntityPlayerMP.java:223` | Predicted; **premise confirmed during B-04 live verification** — a stationary client receives `PrepChunkPacket`s but zero chunk *data*; streaming only advances on movement packets (`HeightSyncSmokeTest` live mode has to pump position packets) |
| B-08 | **`gauntletUriel`'s 3×3 break picks its plane from a block id.** `ItemInWorldManager.hitBlock` calls `onDestroyBlock(i4, …)` passing the *block id* into `Tool.onBlockDestroyed`'s `side` parameter, which selects which of the three planes the gauntlet clears. The client's `MultiplayerMode.removeBlock` has the same swap; `SurvivalMode` (singleplayer) passes the real `side`. | `server/player/ItemInWorldManager.java:107`, `client/network/player/MultiplayerMode.java:40` | Found during B-07 by reading. **Unconfirmed in play** — needs a gauntlet |
| B-09 | **`Block.dropItem` reads metadata out of a block that is already air.** Its RUBY (insta-smelt) and ADAMANTINE branches re-read `world.getBlockMetadata(x, y, z)` instead of using the `meta` they were passed, and the server drops *after* clearing the block, so they see 0. A ruby tool smelting anything with a meaningful metadata looks up the wrong smelting result. Singleplayer drops before clearing, so it is multiplayer-only. | `game/block/Block.java:300-310`, called from `server/player/ItemInWorldManager.java:121` | Found during B-07 by reading; predates it and was left untouched. **Unconfirmed in play** — needs a ruby tool |
| B-24 | **Five of the six explosion flavours never reach the client at all.** `WorldServer` overrides exactly one explosion method — the base `createExplosion`, the one that sends `ExplosionPacket`. `createMagicExplosion`, `createFireExplosion`, `createFrostExplosion`, `createSmallExplosion` and `createWindExplosion` are inherited from `World` unchanged, so they send nothing, and the particles and sound they emit through `addParticles(true)` are handed to `WorldManager.spawnParticle`/`playSound`, both empty stubs — the B-19 failure mode. Splash potions (`ThrownPotion`), `Windball`, `Breeze`, `CreeperWind` (radius 100), `CreeperMagic` and `CreeperFrost` therefore knock players around and damage them with no particle, no sound and no feedback of any kind. Compounds with B-23: a magic explosion spawns `LingeringCloud`s, which are untracked, so what it leaves behind is invisible as well. | `server/world/WorldServer.java:106`, `game/world/World.java:1422-1464` | Found by reading; **unconfirmed in play** |
| B-27 | **A crawl or sneak toggle is broadcast to every player on the server, in every dimension, including the one who sent it.** `handlePlayerState` ends in `configManager.sendPacketToAll(packet)` where every other player-visible event goes through that dimension's `EntityTracker`. A client ignores an id it doesn't know and its own, so nothing renders wrong — it is O(players) packets per toggle crossing dimension boundaries, and it is what made B-22 look like a duplicate broadcast. | `server/network/NetServerHandler.java:115` | Found by reading; **bandwidth and tidiness, no known visible symptom** |
| B-28 | **`LoadedChunk.distModifier` is never reset.** `markBlockNeedsUpdate` ORs in `1 << (y >> 4)` and `tick()` clears `numBlocksToUpdate` but not the mask, so it only ever grows. It is read on the 64-blocks-in-one-tick path (`new UpdateChunkPacket(chunk, false, distModifier)`), which therefore resends every section that chunk has *ever* touched instead of the ones that changed this tick. The payload is still correct — this is wasted bandwidth, not corruption. | `server/world/ServerChunkDistance.java:251, 275-320` | Found by reading; **bandwidth only** |
| B-36 | **Bots at the spawn point get kicked with "Flying is not enabled on this server" partway through a run.** `TwoClientTest` and `QuickStackSmokeTest` both fail this way at HEAD; every assertion after the kick then fails for the trivial reason that nobody is listening, which is why `QuickStackSmokeTest`'s count moves between runs (10 and 13 seen) without a second defect behind it. Almost certainly downstream of B-01 — the spawn is open ocean, a sinking bot is airborne over water for a long time, and B-00a's `playerInAirTime` fix was about the counter never resetting, not about water. Whether a **human** treading water at spawn also gets kicked is the question worth answering first; if so this is a real gameplay bug and not a rig problem | `server/network/NetServerHandler.java` (the floating check), `game/world/dimension/Dimension.java:100` (B-01) | Observed 2026-08-15 while verifying B-25. **Not caused by B-25** — confirmed by stashing that change out and re-running both tests against a clean tree, which fail identically. Nobody has looked at the kick site yet |
| B-38c | **A player whose saved position is far from the world spawn logs in and is never ticked by the world.** `World.tickEntity` runs `entity.onTick()` only inside `if (shouldTick && entity.addedToChunk)`, itself inside `if (!shouldTick \|\| chunksExist(x-32, 0, z-32, x+32, height, z+32))` — all 25 chunks around the player. A player who fails that guard silently loses **everything** `EntityPlayerMP.onTick` drives: the container sync (so `/give` reports success on the console and no `SlotPacket` ever reaches the client), `itemInWorldManager.beginBlockRemoval` (so digs never finish), and the equipment broadcast. Nothing is logged, the player can still walk, chat and run commands, and a later `/teleport` does **not** clear it. Very likely the same guard behind the asymmetric entity tracking seen alongside it: after a long teleport, the teleported player tracks the stationary one but not the reverse, so `sendPacketToTrackedPlayers` from the teleported player reaches nobody | `game/world/World.java:1225-1241` (the guard), `server/player/EntityPlayerMP.java:148` (what is lost) | **Reproducible A/B, found while verifying B-38.** With `run/server/world/players/sleepbot.dat` holding a position ~900 blocks from spawn: zero `SlotPacket`s in a 10s window after a `/give` the console confirms, across a server restart. Delete the `.dat` so the bot logs in at the world spawn: the `SlotPacket` arrives, *and keeps arriving after it teleports to the same far position*. One run re-wedges it, which is why `SleepSmokeTest.finish()` now parks its bots back where they logged in. **The guard is the suspect, not the confirmed cause** — nobody has instrumented `chunksExist` to watch it return false. Whether a human logging out far from spawn hits this is the question worth answering first; if so it is severe and quiet |
| B-38b | **A bed a player logged out in stays "occupied" forever, and nobody can use it again.** `Bed.blockActivated` refuses an occupied bed before it reaches `goToSleep`, and the flag is only ever cleared by `Player.wakeUpPlayer` against that player's own `bedPosition`. On a reconnect `Player.readFromNBT:1163` rebuilds `bedPosition` from where the player was standing — and `NetServerHandler.handlePlayerMovement`'s sleeping branch has been snapping the sleeper back to `lastPos*`, the spot it walked to the bed from, every movement packet since it got in. So the wake-up clears the flag on a block that is not a bed and the real bed keeps it. Vanilla does not have this because `sleepInBedAt` ends in `setPlayerLocation`, which moves `lastPos*` onto the bed; `EntityPlayerMP.goToSleep` sends the `SleepPacket` and nothing else. The one-line-looking fix (add the `teleportTo` to `goToSleep`) also clears `hasMoved`, which stops `sleepTimer` until the client echoes — so it needs the wake path checked, not just the line added | `server/player/EntityPlayerMP.java:337` (no `teleportTo`), `server/network/NetServerHandler.java:183-188`, `game/entity/player/Player.java:1163` | **Confirmed live during B-38** — a bed left occupied by an aborted run refused every later attempt until it was dug out and rebuilt, which is what `SleepSmokeTest.buildBed` now does. Left alone deliberately: B-38 was already five load-bearing changes. **B-42 added the `teleportTo` this row asks for**, for its own reason, and checked the wake path with it: the sleeper's saved position is now the bed, so `readFromNBT` should rebuild `bedPosition` onto the real bed and the wake should clear the right block's flag. **Nobody has logged out asleep and back in to watch it** — that is all this row needs now |
| B-42b | **The desync rubber-band switches a player off and logs nothing.** `handlePlayerMovement` ends in `if (z22 && (z23 \|\| !z24) && !isPlayerSleeping()) { this.teleportTo(lastPos*); return; }`. `z23` — "moved wrongly" — logs three lines; `!z24`, the player *ending inside a block*, logs nothing at all, and both take the same branch. That `teleportTo` clears `hasMoved`, and `handlePlayerMovement`, `handlePlace` and everything they gate then ignore the player until an exact echo comes back. A real client always echoes, so the live blast radius is small; the diagnostic cost is not. It cost an hour of B-42: digs and places were dropped in silence and the failure surfaced as `no bed at 877,150,768 after placing one there - found block id 0`, which names the wrong block and no reason. Worth a log line either way, and worth asking whether `!z24` should rubber-band at all — the block a sleeper's box pokes into is exactly this case | `server/network/NetServerHandler.java:255-260` | Found while fixing B-42; **the mechanism is confirmed, the player-visible cost is not**. `Bot` now adopts every teleport, so the rig no longer trips on it |
| B-42c | **`EntityTrackerEntry` decides "did it move" from the absolute position, not the delta.** `boolean z11 = Math.abs(i2) >= 8 \|\| Math.abs(i3) >= 8 \|\| Math.abs(i4) >= 8` reads the encoded position (`floor(posX * 32)`), where vanilla reads the deltas `i7/i8/i9` it computed one line earlier. Anything more than a quarter of a block from the origin therefore counts as having moved, forever: every tracked entity gets a `MovePacket` — routinely with all-zero deltas — every `totalUpdates` ticks, and `encodedPos*` is rewritten each time. The payload is correct, so this is bandwidth, not corruption, and it is the same shape as B-28 | `server/entity/EntityTrackerEntry.java:139` | Found by reading during B-42; **bandwidth only, unmeasured** |
| B-29 | **`handleBlockItemSwitch` accepts hotbar index 9.** The guard is `id >= 0 && id <= 9` while `PlayerInventory.getCurrentItem()` returns `null` for `currentSlot >= 9`, so a client that sends 9 leaves the server believing the player holds nothing while the client draws a held item — every place/attack/use resolves against empty hands. The stock client only ever sends 0-8 (`MultiplayerMode.syncCurrentPlayItem`), so this is unreachable in normal play. | `server/network/NetServerHandler.java:516` | Found by reading; **not reachable from the stock client** — an input-validation gap, recorded rather than fixed |

B-24 to B-29 came out of one read-only sweep of the merged tree; none of them has been seen on the wire
or in play. **B-24 is the one worth doing next** — a whole system that works in singleplayer and
produces nothing at all on a client. B-23, B-25 and B-26, the other three of that set, are fixed below.

B-01 affects singleplayer too — it is not a multiplayer regression.

B-02 note: the packed block-update coordinates changed in B-04 — they are now `int`s laid out
`x << 20 | z << 16 | y` (decode: `x = c >> 20 & 15`, `z = c >> 16 & 15`, `y = c & 0xFFFF`). The
loop's actual bug — decoding out of `numBlocksToUpdate` (the count) instead of
`blocksToUpdate[i2]` — was left untouched and now uses the new layout.

### Fixed

| B-58 | **On a dedicated server a crawling player could not attack or interact with anything, and every other player's eye sat at ankle height on the client.** `handleUseEntity` gates every click on `playerEntity.isVisible(target)`, whose raytrace started at feet+1.62 — 0.72 above the top of a crawler's own 0.9m box, i.e. inside the ceiling block of the 1-block gap crawling exists for. `rayTraceBlocks` tests the starting block before it steps, and `Block.rayTrace` returns the exit face when the origin is inside the box, so the ray reported an obstruction immediately. The same origin fed `Mob.isVisible` (mobs lost sight of a crawler), `Arrow`/`Projectile` spawn (an arrow shot while crawling is born in the ceiling) and `isInMaterial` (the drowning probe). Found by reading, from the question "does `getEyeHeight` account for crawling?" | **`getEyeHeight()` had no single meaning.** It returned an offset from `posY`, but `posY`'s anchor is per-class (`yOffset`), so every override hardcoded a constant tuned to its own anchor: `Entity` `0.0F` and `Mob` `height * 0.85F` measure from the feet; `Player` `0.12F` measures from an eye-anchored `posY`, so it was never an eye height at all but vanilla's small fudge above one; `EntityPlayerMP` pinned `1.62F` because its `posY` is the feet, which is right standing and wrong in every other pose; and `RemotePlayer` — feet-anchored like the server's copy but with no override — inherited the player's `0.12F`, putting the eye of every other player 1.5 blocks low for `HookRenderer`'s grapple origin and `MobRenderer`'s nameplate lift. The nameplates survived by accident: `renderLabel` short-circuits its visibility raytrace with `|| entity instanceof Player` | The name now means one thing — **height above `boundingBox.minY`** — and nothing reads `posY + getEyeHeight()` any more. `Entity.getEyeY()` is the absolute eye for logic; `Entity.getEyeOffset()` (`getEyeHeight() - yOffset`) is the eye relative to `posY`, for render code that has already interpolated it. `Player` holds the one pose table (1.62 standing, 1.42 sneaking, 0.56 crawling — the same numbers `setCrawling`/`setSneaking` write to `yOffset`), clamped to `height - 0.1F` so the 0.2m box of death and sleep cannot put the eye outside the body; `EntityPlayerMP`'s eye pin and the `isInsideBlock` clamp that existed only to compensate for it are both deleted, and `RemotePlayer` is correct without an override. 30 call sites converted. Mobs are numerically unchanged, since `yOffset` 0 makes the old and new expressions identical. Two sign errors that were invisible while the term was 0.12 came out with it: `LookAt` subtracted the eye height instead of adding it, and `Stalker.teleportToEntity` had `- target.posY + target.getEyeHeight()` where vanilla subtracts both; both now read `getEyeY()`. `MobRenderer.renderLabel` no longer applies the `eye * 1.5` lift itself — `renderLabels` does, so it stays a mob-shaped offset, and `PlayerRenderer`'s two label constants absorbed the 0.18 it used to contribute, leaving nameplates where they were | `core/entity/Entity.java:690-700`, `entity/player/Player.java:1350`, `server/player/EntityPlayerMP.java:144-162`, `client/render/game/GameRenderer.java`, `client/render/entity/{MobRenderer,PlayerRenderer,projectile/HookRenderer}.java` | **Compile-verified and read-verified; the failing case is not bot-reachable.** Reproducing it needs a solid block above a crawling player and the rig has had no way to place one since the command purge — this is exactly the `Bot.setBlock` helper this file already lists as the agreed follow-up, and `CrawlSmokeTest` should grow a phase once it exists. What was measured: `:core`/`:client`/`:server` compile clean, `FootstepSmokeTest` all-pass (it exercises `RemotePlayer` sneak/ground handling), and the two failures in the sweep — `TwoClientTest`'s float kick and `CrawlSmokeTest` phase 2's duplicate broadcast — reproduce identically on the reverted tree, so neither belongs to this change |

| B-60d | **A sleeping player floated 0.2 blocks above the bed to everyone else, and their own first-person sleep camera sat 0.2 high with them.** Reported from play with a screenshot; the first-person half was not reported, because only the sleeper can see it | The same B-60 root cause in its second shape. `putToBed` passed `setPosition(x, y + 0.9375, z)` — an **eye-anchored** Y that landed correctly only because `yOffset = 0.2` dropped the box back down. B-60 deleted the write and left the literal, so the box, the model (`PlayerRenderer` draws at `posY - yOffset`) and the camera all rose by exactly the offset that used to cancel it. **Grepping for code that reads `yOffset` cannot find this class** — the tell is a magic number sitting in the same method as the write you removed | `0.9375 -> 0.7375`, which restores the box to `bedY + 0.7375` (0.175 above the mattress, where it always was) and takes the sleep camera back to `bedY + 1.0575` with it. `wakeUpPlayer`'s no-free-spot branch went `+0.1 -> +0.3` for the same reason: it was lifting the box relative to a `posY` that had moved. The stale `EntityPlayerMP` comment about `teleportTo` adding the sleeper's `yOffset` back on is rewritten | Not bot-reachable — a bot cannot get into a bed without `/setblock` and `/give <id> <count>`, both dead since the command purge, which is why `SleepSmokeTest` stops where it does. Verified arithmetically against the pre-refactor values: model at **64.7375** both ways, sleep camera at **65.0575** both ways, wake box at **65.0375** both ways, for a bed at y=64. **The sweep this prompted also cleared riding**, which had been sitting in the unverified column since B-60: `Player.getYOffset()` going from `yOffset - 0.5` to a flat `-0.5` cancels the anchor change exactly, so a rider's box, model and camera all land where they did before — `RemotePlayer.mountEntity`'s `yOffset = 1.62` flip was pure compensation, as B-60 assumed. Every `setPosition`/`teleportTo` in `Player` has now been re-derived; the only other shift found was the Phantom spawned on a cross-dimension wake, 0.2 lower and not worth a constant. **One knock-on of B-60c is worth knowing**: the death camera now sinks over ~5 ticks instead of dropping in one, because `renderEye` settles under gravity toward the 0.1 the clamp gives a 0.2m box |

| B-60c | **Sneaking and crawling snapped the camera instead of easing it.** Reported from play. Same root cause as B-60b, which is the point of the entry | **`posY` was not holding the eye's value, it was holding the eye inside a pipeline**, and the eye inherited four things from living there that nobody had written down: the renderer's `prevY -> posY` interpolation smeared *every* eye change over at least a tick; `posY` is the origin the view matrix rotates about (B-60b); the box's own gravity gave the crawl its fall; `colHeight` eased it up steps. B-60 moved the value out and left all four behind. The old transitions were never hand-written tweens — crawling *displaced the box* (feet up 1.06, and the `+1.3F` on standing up was 1.06 plus 0.24 of deliberate overshoot) and gravity settled it, with the eye riding `posY` down. Sneak never moved the box at all; it was a one-tick step that only ever looked smooth *because* the renderer interpolated it | A render-only `renderEye` on `Player`, settling toward `getEyeHeight()` under the same `-0.08 / *0.98` integration the falling box used, snapping on the way up the way the old code did, and interpolated per frame through a new `Entity.getEyeOffset(tickDelta)`. Logic keeps the crisp `getEyeHeight()`, so client and server still agree on reach and raytraces — the tween is confined to the viewpoint. `setCrawling(false)` seeds it 0.24 high to reproduce the overshoot. **Found by the same analysis rather than by a third report**: `Mob.getPosition` (the crosshair and entity-pick origin), both camera collision probes and the grapple line were all still reading the crisp eye, which would have drifted the crosshair off the camera for the five ticks a crawl takes; all four now take the interpolated form. The nameplate lift stays crisp — it is a per-entity decoration, not the viewpoint | Not bot-reachable. Verified by replicating the shipped integration against a reconstruction of the pre-refactor dynamics: crawl on, crawl off, sneak on and sneak off all match **to 0.0000 across every tick**. Server sweep re-run unchanged (Drop/Crawl/Footstep/Score), confirming the change is client-local. DEVELOPING.md now carries the two-form eye table and the rule that produced it: **when you lift a quantity out of a variable other machinery already processes, value-equality proves nothing — enumerate what that machinery was doing for it first** |

| B-60b | **The camera pivoted around the player's feet instead of their eyes.** Looking down swung the viewpoint forward and into the floor, looking up swung it backward. Reported from play, immediately after B-60 | B-60 made `updateCamera`'s eye lift real (it had been a no-op `0.0F` for players, because `getEyeHeight()` and `yOffset` were kept equal), and it was issued at the **top** of the method. **GL post-multiplies: the last transform issued is the first one applied to a vertex.** Issued first, `glTranslatef(0, -getEyeOffset(), 0)` was applied *after* the pitch/yaw rotations at the bottom of the method — in view space — so the 1.62 lift tilted with the camera instead of standing straight up in the world. The rotations then pivoted about the feet. It was invisible while the translate was zero, which is exactly why moving it never looked like a decision | The translate is now the last statement in `updateCamera`, so it applies on world axes and the rotations pivot about the eye. Nothing else moved: the third-person pull-back and the low-ceiling nudge are issued before the rotations *because* they are meant to be view-space | Not bot-reachable — no smoke test can see through the camera. Verified by reconstructing the two matrix orders and solving each for the world point that maps to the view origin: the broken order walks the eye from (0, 1.62, 0) at pitch 0 to (0, 0.00, 1.62) at pitch -90 — on the floor, 1.62 ahead, which is the report — and to (0, 1.15, -1.15) at +45, the backward half of it; the fixed order holds (0, 1.62, 0) at every pitch. The server sweep was re-run and is unchanged (Drop, Score, Footstep, Tp all-pass), which only confirms the fix is client-local. **Still needs a human for the sleep camera and riding** |

| B-60 | **`posY` meant two different places depending on which `Player` subclass you had, and every consumer paid for it.** Not reported from play — raised while reviewing B-59 as "why not have `yOffset` and `posY` mean the same thing across SP and MP?", which is the right question | The split was never SP vs MP: `EntityPlayerMP` and `RemotePlayer` both zeroed `yOffset` and were anchored at the **feet**, while the `Player` base kept 1.62 and anchored `LocalPlayer` at the **eye**. One outlier out of three, and the compensations were everywhere — `Bobber`, `Eye` and `BoatItem` hand-rolling `posY + 1.62 - yOffset`, `Piston` doing it with 1.82, `Bucket` reading `+ yOffset - yOffset`, `CommandSourceStack`/`World.playMobSound`/`EntityPlayerMP.teleportTo` subtracting the anchor back out, `RemotePlayer.mountEntity` flipping `yOffset` to 1.62 while riding so `getYOffset()` arithmetic would land, and `resetHeight()` existing in three classes purely to undo the base class's writes. B-58 routed *around* this with `getEyeY()`/`getEyeOffset()` rather than removing it; B-59 was one call site it missed | **The feet, everywhere.** `Player.yOffset` is 0 and nothing writes it again: the pose table lives only in `getEyeHeight()`, `setCrawling` and `setSneaking` resize the box instead of moving the anchor (taking the `+1.3F` fudge with them), and the death and sleep camera drops now fall out of `getEyeHeight()`'s `height - 0.1F` clamp against the 0.2m box. `getYOffset()` is a flat `-0.5`. **Deleted**: `resetHeight()` and its two calls, `EntityPlayerMP`'s and `RemotePlayer`'s `setCrawling` and `resetHeight` overrides, `RemotePlayer.mountEntity`, both `yOffset = 0.0F` constructor writes and `PlayerController`'s sneak writes. **The camera is the load-bearing half**: `updateCamera` dropped its `if (!(player instanceof Player))` guard and now translates by `-getEyeOffset()` for every camera entity, which is what re-lifts the eye off a feet-anchored `posY` — that guard was a no-op before only because `getEyeHeight()` and `yOffset` were kept equal. Everything reading an interpolated `posY` as a render origin composes correctly through it and needed no change; the sites that read `posY` as *a world height* became `getEyeY()` (fog brightness, the inside-a-block overlay, thunder, void fog, rain, held-item light, sound distance). `Mob.getPosition(tick)` — the block-selection and entity-pick raytrace origin, and nothing else — gained `getEyeOffset()`, which also stops mobs raytracing from their feet. On the wire nothing changed: `y` was already `boundingBox.minY` and `stance` is now `getEyeY()` instead of a raw `posY`, with the server's own teleport sending `d3 + getEyeHeight()` instead of `d3 + 1.62` | `DropSmokeTest` grew crawling and sneaking phases, so the eye table is now read back off the wire in all three poses (1.62/1.42/0.56, each less the 0.3 the drop sits below the eye) — and the feet check in each phase is the half that pins "a pose moves the eye, not the player". **Server sweep all-pass**: Drop, Login, HeightSync, Footstep, Tp, Sprint, Score, Gamemode, Give, CommandWire, Help. `ScoreSmokeTest` and `TpSmokeTest` are the ones worth naming — the first only reports at all if the position echo still re-arms `hasMoved`, the second pins `~` after `CommandSourceStack` stopped subtracting the anchor. Pre-existing failures reproduce unchanged and belong to the dead-command suites or B-27: Crawl phase 2, Sleep, Projectile, BlockBreak, EntityTracking, Death, QuickStack. **The client half is compile- and read-verified only** — no bot can look through the player's eyes, so **camera height, the sleep camera and riding still need a human in-game pass**. The sleep camera is the one deliberate behaviour change: its hand-tuned `yOffset - 1.62 + 1.0 + 0.3` became a flat `-0.22F` on top of the unconditional eye lift, reproducing the old height (feet + 0.32) rather than the 0.1 the clamp alone would give |

| B-59 | **A thrown item has no arc in multiplayer.** Singleplayer pops it up in front of you and it lands a few blocks ahead; on a dedicated server it appears at your feet, already touching the floor. Reported from play | `Player.dropPlayerItem` spawned the item at **`posY - 0.3`**, and `posY`'s anchor is per-class: the client player carries `yOffset` 1.62 so `posY` is the eyes and the item is born at chest height, while `EntityPlayerMP` zeroes `yOffset` so `posY` is the feet and the item is born 0.3 blocks *under the floor*, inside a block, with nowhere to arc to. The velocity was never the problem — `motionY` is `-sin(pitch) * 0.3 + 0.1` on both sides and is sent (items are tracked with motion updates on), it just had no room. This is the one call site B-58 did not convert: it reads `posY` bare rather than `posY + getEyeHeight()`, so grepping for the term that moved missed it | The spawn is `getEyeY() - 0.3`, B-58's absolute eye. In singleplayer this is a **no-op by construction** — standing, sneaking and crawling all set `yOffset` to the same number `getEyeHeight()` returns, so `getEyeY()` and `posY` are equal. The death path is unaffected too: `inventory.clearContents(true)` runs before `onDeath` drops `yOffset` to 0.1 | `DropSmokeTest` (needs `dropbot` in `run/server/ops.txt`), which measures the spawn against the ground position the server accepted. **Measured red then green**: -0.30 blocks above the feet on the reverted tree, +1.32 with the fix. Note it never `sink()`s — `land()` does its own sinking and hands back the position the server agreed to, and a bot that keeps sinking afterwards drifts away from it (an early draft read 8.69) |

| B-57 | **Arrows put into a quiver disappear after a relog** — the quiver's slots are empty the moment you rejoin, and the arrows drop out of the item's own tooltip preview as soon as anything in the inventory changes. Reported from play | Backpack and quiver contents ride in the equipped accessory item's NBT, and the container's copy of them is filled in only when the item is **equipped** (`AccessorySlot.putStack` → `PlayerContainer.loadContents`). A login equips nothing: `PlayerInventory.loadContents` deserializes the accessory straight into the slot array, so `quiverContents` came back empty while the item's tag still held the arrows. `LocalPlayer.readFromNBT` has always called `loadContents` for exactly this reason — `EntityPlayerMP.readFromNBT` never did, so **the bug is multiplayer-only**. `saveContents` then rewrites the tag from the container on *any* backpack/quiver slot change (`ArrowSlot.onChanged`, `BackpackSlot.onChanged`, `getStackInSlot`), so the first inventory change wrote the empty container over the arrows — which is also why the tooltip preview went on showing them right up until it | `EntityPlayerMP.readFromNBT` loads the container from the accessory the way the client already does, and `writeToNBT` flushes it back before the inventory is serialized. The flush is the half that covers the paths mutating `quiverContents` without a slot change — `consumeQuiverItem`, i.e. firing a bow, is the one that matters. The backpack/quiver guard sits in one `holdsContainerContents` helper and matches the client's, so `quiverGold` stays out of it deliberately: the golden quiver's arrows are infinite, `ContainerScreen` never draws slots for it, and `AccessorySlot.putStack` never loaded it either | `QuiverSmokeTest` (new; `quiverbot` in `run/server/ops.txt`), 11 checks green on a live server. **Watched go red**: with the fix reverted, exactly two fail — "the arrows are in the quiver after the rejoin" and "an inventory change does not empty the quiver", both naming quiver slot 69 empty — and everything else stays green, including the same run's stocking phase, so the write path is proven not to be the problem. Singleplayer was never affected and is untouched |
| B-56 | **Using a jukebox dropped every player within 64 blocks. Once dropped, the disc could not be taken back out, a broken jukebox grew back where it stood, and both it and the disc only turned up after a relogin.** Reported from play | `MusicPacket.writePacketData` wrote `stop` *before* the track name; `readPacketData` reads it *after*. Only the server ever builds one — `WorldManager.playRecord` → `sendPacketToAllCoord(..., 64.0, ...)` — so every client in earshot read the boolean out of the string's length prefix, got `""` for the track and a stop it was never sent, and left the name's bytes in the stream. The next packet id was whatever those bytes said (98, in every run here), `Packet.readPacket` threw, and `NetworkManager.onNetworkError` shut the connection down as "Internal exception". Both jukebox paths send one: `Record.useOnBlock` on the way in, `Jukebox.ejectRecord` on the way out — and eject sends it *first*, before it clears the record, resets the metadata and spawns the drop | The write order now matches the read order. One line, and the whole fix | **The other three symptoms were the same bug and needed nothing.** They are what a client looks like when the server stops being able to talk to it and carries on regardless: the eject *did* happen, the break *did* drop both items, and a relogin was simply the first time anyone was told. `JukeboxSmokeTest` (new; `jukebot` in `run/server/ops.txt`, `jukewatch` needs none) covers the lot — 14 checks, all green with the fix, six red without it, including a bystander bot that does nothing but stand there and is dropped anyway. Note the survival checks read on past the MusicPacket deliberately: the mangled packet itself parses, and it is the *next* read that dies, so a check that stops at the packet passes against the broken build. Tile-entity persistence was suspected and is **sound** — a record survives a relogin and a full `/stop` and restart, proven by the test's `load`/`eject` halves. A killed server is what loses it, and loses the whole block with it |
| B-55 | **The client resolved A records only, so a bare domain pointing at a tunnel or a non-default port could not be joined.** Reported by the fork this batch came from, not from play here | `ClientHandler` and `PingHandler` both did `new Socket(InetAddress.getByName(host), port)`, and both call sites defaulted a missing port to 25565. Nothing ever asked for `_minecraft._tcp.<host>` | New `client/network/ServerAddress`: `resolve(host)` queries the SRV record through the JRE's own DNS provider (`com.sun.jndi.dns.DnsContextFactory`, 3s timeout, 1 retry) and falls back to the plain host on **any** failure — the whole lookup is wrapped in `catch (Throwable)`. It is skipped for IPv4/IPv6 literals and single-label hosts so `localhost` stays instant. `of(host, port)` is the explicit-port path: SRV is consulted **only** when the address carried no `:port`, which is vanilla's rule. `ConnectingScreen` gained a host-only constructor that resolves **on `ThreadConnectToServer`**, not in `joinServer` — a 3s DNS stall on the render thread would freeze the client. `PingHandler`'s lookup was already on the ping thread | Probed live against real DNS: `mc.mineplex.com` → `java.mineplex.com:25565`, byte-identical to `dig +short SRV _minecraft._tcp.mc.mineplex.com` including the trailing-dot trim; `mc.hypixel.net` (no SRV) falls back unchanged in 102ms; `localhost`/`127.0.0.1` short-circuit in 0ms; `example.invalid` falls back in 1ms. **No live non-default-port SRV was found to test against** — the port field is proven positionally, by the target field beside it parsing correctly |
| B-54 | **Scrolling the hotbar or standing on the sleep screen could crash the client with an NPE when the world went away underneath it.** Same class as B-53's fix, which this pair was missed by | `Minecraft.runTick` dereferences `this.player` in two places that are not inside the `world != null && player != null` block: the SleepScreen dismissal (`!this.player.isPlayerSleeping()`) and the mouse-wheel branch (`this.player.inventory.changeCurrentItem`), which is gated only on `currentScreen == null`. Being disconnected while asleep is the reachable one | Null guards on both. The SleepScreen branch now reads `player == null \|\| !isPlayerSleeping()`, so a null player *dismisses* the screen rather than crashing — which routes into `setScreen(null)`, and **that path had the same latent deref** (`newScreen == null && this.player.health <= 0`), so it is guarded too. The redundant `currentScreen != null` beside the `instanceof` went with it | Read, not played — the client has no rig. The three guards are the whole change |
| B-53 | **The deflate output buffer for chunk packets was sized at the *uncompressed* length, so incompressible chunk data was silently truncated on the wire** | `UpdateChunkPacket`'s constructor did `this.chunkData = new byte[i1]` where `i1` is the byte count it just packed, then `deflate()` into it. Deflate *expands* incompressible input by roughly 0.03% + 5 bytes, so the tail is dropped and `tempLength` records the short count. The read side has caught this since the "Chunk data size mismatch" check went in — but catching it means the client throws, not that the chunk arrives | Output is now sized to zlib's `deflateBound` (`i1 + (i1 >> 12) + (i1 >> 14) + (i1 >> 25) + 32`) with a doubling grow loop behind `!deflater.finished()`, so truncation is unrepresentable rather than merely unlikely. Over-allocating is free: `writePacketData` writes `chunkData, 0, tempLength` and `getPacketSize` reads `tempLength`, so nothing consumes `chunkData.length` | **Red/green in isolation** on a 98560-byte payload (8 sections + biomes): random incompressible input under the old code deflates to 98560 and inflates back to only 98523 → **CORRUPT**; under the fix it deflates to 98601 — 41 bytes *larger* than the input, which is the bug in one number — and round-trips exactly. Realistic chunk data compresses to 1298 bytes **byte-identical under both**, so this is not a wire change. Live: `TwoClientTest` 6/7 (B-36's kick, baseline), both clients received chunk data; `ContainerOpenSmokeTest` all pass |
| B-52 | **One stale entry in the chunk drop queue stopped singleplayer unloading chunks — permanently.** Reported by the fork this batch came from as "chunks never released on unload"; not observed here | `ChunkProvider.unloadOldChunks` did `if (chunk == null) { return false; }` on a queued hash with no chunk behind it, **without dequeuing it**. Every later call pulls the same hash and returns at the same line, so the drop queue only grows and nothing is ever saved or freed again. The reachable trigger is the shared `emptyChunk`, which is built once as `new EmptyChunk(world, 0, 0)` and handed out for every out-of-border column while still being `put` into `chunkMap` under the *real* coords — so the sweep below it calls `dropChunk(chunk.xPosition, chunk.zPosition)` = `dropChunk(0, 0)` for any of them, queueing a hash that need not be in `chunkMap` at all. `:server` is unaffected: `WorldServer` uses `ChunkProviderServer`, which has its own loop | The stale hash is removed and the loop continues, and the sweep skips `emptyChunk` outright so the bogus `(0,0)` drop is never queued — the second half also stops the real spawn chunk being dropped out from under a player standing in it. `ChunkProviderServer`'s own loop has an unguarded `chunk3.onChunkUnload()` on a `get` that can return null; left alone, not part of this batch | **Not covered by a bot and not played** — the rig drives a dedicated server, which uses the other provider, so this is unreachable from `:server:smokeTest` by construction. Verified by reading; needs a singleplayer session with a world border set to confirm |
| B-51 | **Every menu screen leaked a full-viewport GL texture, on open and again on every window resize** | `GuiScreen.setResolution` runs `glGenTextures` unconditionally and only `GuiScreen.onClose` freed the result. **Ten screens override `onClose()` and none of them call `super`** — BiomeToggle, WorldOptions, WorldRenaming, WorldSelect, Sleep, DirectConnect, ServerSelect, Connecting, Chat, AddServer — so each open of any of them abandons a `displayWidth × displayHeight` RGB texture. Worse, `Minecraft` calls `setResolution` again on **every resize** (`:638`), which leaked once per resize even on the screens that *do* chain to super | Not fixed by editing ten screens, which is the same trap one screen number eleven away: the delete moved out of `onClose` into `public final releaseViewportTexture()`, guarded by a `0` sentinel (GL never returns 0 from `glGenTextures`, so it doubles as "none"). `setResolution` releases before it allocates, and `Minecraft.setScreen` calls it right after `onClose()` — unconditionally, so an override that skips `super` can no longer skip the free. `onClose`'s body is now empty and the ten screens are untouched | Bytecode-checked (`javap`): `glDeleteTextures` appears **only** inside `releaseViewportTexture`; `setResolution` calls it at offset 49, before `glGenTextures` at 60; `setScreen` calls `onClose` then `releaseViewportTexture` with no branch between them. **Not played** — needs a GL context, and no rig has one; the leak was never measured live, here or on the fork that reported it |
| B-50b | **Blast furnaces opened as ordinary furnaces in multiplayer**, counting the wrong stat. Found while fixing B-50 | `BlastFurnace extends Furnace` and inherits `displayGUIFurnace`, whose server override hardcoded type `2`. `ClientHandler` has had a **case 6** building a `BlastFurnaceEntity` the whole time and nothing ever sent a 6, so it was dead code | The type is now `instanceof BlastFurnaceEntity ? BLAST_FURNACE : FURNACE`, which is the whole fix | Two cases in `ContainerOpenSmokeTest`, plain furnace beside it as the control. **Watched go red** with the ternary reverted: `expected inventoryType 6 and got 2` |
| B-50 | **Ten blocks with a custom GUI did nothing at all when right-clicked in multiplayer** — anvil, carver, grindstone, smoker, hellforge, crafter, beacon, vacuum, dispenser and obsidian chest. No screen, no error, no log line. Reported from play | **`EntityPlayerMP` overrode 6 of the 21 `displayGUI*` methods and `Player`'s bodies for the rest were `{}`.** The click reached the server, `blockActivated` called a method that did nothing, and no `ContainerOpenPacket` was ever sent. `ContainerOpenPacket` is constructed in exactly six places in the tree, all in `EntityPlayerMP`, so nothing else could open a window. Never a regression - these were never implemented. Chest, workbench, furnace, barrel, bookshelf and generator worked the whole time, which is why it read as "some blocks" rather than "containers are broken" | **No protocol change was needed**: `ContainerOpenPacket` already carried `windowId, type, title, slots, x, y, z, state`, and every one of the ten client screens builds from those. The six ids grew to seventeen `public static final int` constants on the packet, so both sides read the table from one place. The five-line window dance in `EntityPlayerMP` (window id, packet, container, listener) is now one `openWindow` helper and the sixteen overrides are a call each. `ClientHandler.handleContainerOpen` assigns `windowId` once after the switch instead of in every case - case 6 had no `break` and was safe only by being last - and a `default:` now warns rather than leaving a window open server-side with no screen on the client. **The silent stub is the part that mattered**: `Player`'s bodies for the sixteen the server must implement now log which one is missing, so a seventeenth GUI cannot ship dead the same way. `abstract` was not an option - `RemotePlayer` extends `Player` and would need sixteen no-op bodies. The four that are deliberately client-only (sign, command block, book, knowledge book) stay `{}`, which now reads as intent rather than omission | `ContainerOpenSmokeTest` (new), **52 checks green on a live server, and watched go red**: with the ten overrides deleted, chest still passes and all ten fail with no `ContainerOpenPacket`, and the new tripwire names all ten in the server log. The slot-count check is the one guarding B-06's failure mode - client and server each build their own container and clicks resolve by index, so the count in the test is the *client* container's, counted by hand. `TwoClientTest` 6/7, identical to baseline including B-36's kick |
| B-49b | **Middle-click pick-block in multiplayer creative put the block in the hotbar, and it vanished the moment you touched it in the inventory.** Found by play, immediately after B-49 | Same class of bug, different path. Pick-block is `GameMode.swapItem` → `PlayerInventory.swapItem`, which writes a new stack straight into `mainInventory[currentSlot]` when the item is not already on the hotbar. No container, no screen, so neither the B-49 diff nor any click handler saw it — the server simply never learned the item existed, and the first real window click over that slot resynced it away | `GameMode.swapItem` sends a `CreativeSlotPacket` for the slot it just filled, found through `container.getSlotInInventory(inventory, currentSlot)` rather than a hardcoded hotbar offset. `setCreativeSlot` is a no-op on the local `GameMode`, so singleplayer is untouched. The survival branch (`swapFromInventory`) only *selects* an existing slot and needs nothing | **Not covered by a bot** — GUI-only, same as B-49. Re-test: middle-click a block you do not already carry, one you do, and with a full hotbar |
| B-49 | **In multiplayer creative, any item taken out of the creative inventory vanished instantly** — and so did an item picked up from your own inventory with the Inventory tab open. Reported from play, straight after creative became reachable in multiplayer at all | `CreativeScreen.mouseClicked` handles the click and then falls through to `super.mouseClicked`, which ends in `mc.gamemode.windowClick(this.container.windowId, …)`. The creative container is the client's alone, so that sent a `ClickPacket` for **window 0** carrying *creative* slot numbers; the server applied it to the real `PlayerContainer` and answered `updateHeldItem()`, which is the server's cursor — empty. The client's cursor was cleared a tick after the pickup, so the item appeared to evaporate in hand. Exactly the hazard B-20 named for the number-key path, still live on the mouse path because until now no multiplayer client could reach creative | `MultiplayerMode.windowClick` sends nothing when `player.openContainer` is a `CreativeContainer`; the local click still runs, so singleplayer is untouched. What the server learns instead is the per-update diff of `player.container` described in the gamemode section, which covers the routes a per-handler hook would keep missing | **Not covered by a bot** — the click paths are GUI-only and the client has no rig. Found by play; the reporter should re-test taking items from each tab, moving them around the inventory, the number keys and the bin |
| B-47 | **Switching gamemode while connected to a server silently cut the client off from it.** `Minecraft.clientSetGamemode` replaced `mc.gamemode` with a *singleplayer* mode object, throwing away `MultiplayerMode` and every packet-sending override on it — dig, place, use, window click, close, item switch. The client kept playing against its own copy of the world and the server never heard another interaction from it. Reachable two ways while online: the mode-switcher key, and `DeathScreen` on a permadeath death | The controller is transport-only now and is never swapped for a mode change: `setGamemode` writes `properties.gamemode` and leaves `mc.gamemode` alone, and the four singleplayer mode classes are gone (see the gamemode section). `ModeSwitcher` sends `/gamemode` when online; the permadeath switch is singleplayer-only | Not covered by a bot — the client has no rig. Found by reading, while wiring `/gamemode`; the code path is plain enough that the fix is the argument |
| B-48 | **A multiplayer client took its gamemode from a singleplayer static.** `NetworkWorld` builds a `WorldInfo`, whose constructor copies `WorldSettings.gamemode` and `WorldSettings.cheats` — the values the world-creation screen last wrote. `Minecraft.changeWorld` then pushed that into `player.properties`, so whoever last made a creative singleplayer world joined a server *believing* they were creative, with cheats on and the mode-switcher key unlocked. The core checks that read `properties.gamemode` run client-side too, so their view of damage, item use and durability diverged from the server's | `NetworkWorld` sets survival and cheats-off explicitly, and the server's `GamemodePacket` at login is the answer from then on | Same as B-47 — read, not played. `GamemodeSmokeTest` proves the server half sends the right mode at login, which is what corrects the client |
| B-44 | **Server-side changes to anything outside the hotbar never reached the client.** Armor durability from mob hits, offhand stack changes, and main-inventory writes stayed invisible until a relog or a rejected click forced a full resync. Found while investigating "server-side inventory mutations do not sync" | `ClientHandler.handleSetSlot` routed `SlotPacket(windowId=0, ...)` to the player container **only for slots 36–44** (hotbar); slots 0–35 (crafting grid, armor, accessories, main inventory) and 45+ (offhand, backpack, quiver) were silently discarded — while the server's per-tick `updateCraftResults` diff sends exactly those packets for every container type indiscriminately. The full-resync path (`ContainerContentsPacket`) covered all slots, which is why a rejected click healed the view and why the bug read as "syncs when I touch my inventory". Fixed by deleting the hotbar carve-out: window 0 now applies every slot; the pickup-animation flavour keeps the 36–44 guard, since that is cosmetic rather than routing | **Played and confirmed** — armor durability from a zombie hit appears immediately and survives clicks. Client bytecode checked (`javap`): `putStackInSlot` sits outside the bipush 36/45 animation guard. `TwoClientTest` 6/7 with only B-36's known flying kick, identical to baseline. The investigation also ruled the reverse theories out by reading: mutations themselves carry no dirty flag (`PlayerInventory.addItemToInventory`/`setSlotContents` write the array, both `onChanged()` overrides are empty), so the per-tick diff is the *only* sync mechanism and this client-side drop was its single point of failure |
| B-45 | **Fall damage appeared to cost armor durability on the client, then the boots snapped back to full on the next inventory interaction.** Zombie hits behaved correctly, which made it read as a fall-specific armor bug | Not an armor bug at all — a **client-side damage re-derivation**. `NetworkPlayer.setHealth` latched once and then routed every health packet through `LocalPlayer.setHealth`, which re-ran `applyDamageMultiplier(DamageSource.generic, delta)` to fake the hurt feedback. `generic` is *blockable*, so the prediction damaged armor for every source — including `fall`, `drown`, `wither` and the other `bypassArmor()` sources the server deliberately exempts — while also double-applying armor reduction to health, adding `generic`'s 0.3 exhaustion per hit, and being able to trigger the rosary revival fantasy on lethal receipts. Zombie hits happened to match the server's arithmetic (stuck); bypassed sources didn't (snap-back). The damage *source* never travels on the wire, so no delta-based reconstruction can know it. Fixed by making `NetworkPlayer.setHealth` accept the value verbatim; the `beingDamaged` latch is gone. The hurt flash and sound are unaffected — they arrive separately as `EntityStatePacket(2)`, which `Mob.damageEntity` broadcasts to the player themselves | **Diagnosed by instrumentation, confirmed by play.** A temporary trace on `Player.damageArmor` (the tree's only armor-durability write choke point — every armor `itemDmg` change funnels through it) printed `LocalPlayer.setHealth:622` as the caller on a fall, client-side only, once per health packet; a server-free probe invoked the real `fall(7.0)` on a real `Player` with real chain boots twice and showed durability untouched (health 20->16), proving the shared fall chain innocent. After the fix, replays show no trace and no durability movement on falls; zombie-hit durability still drops and **stays**. Trace and probe removed after verification. The related bow symptom turned out to be B-46. **Not fixed, deliberately: syncing the damage source** — rejected as an unsound prediction enabler (`ItemStack.damageItem` rolls Unbreaking against divergent client/server RNG, so faithful prediction is impossible even with the source) and singleplayer has no source-specific feedback to match |
| B-46 | **Firing a bow never depleted the arrow stack visibly, and bow durability never visibly dropped, in multiplayer.** The arrow entity flew and behaved normally | The **`isChangingQuantityOnly` mute swallowing unpredicted deltas.** `NetServerHandler.handlePlace` ran `openContainer.updateCraftResults()` with the flag set, which makes `EntityPlayerMP.setStack` a no-op — refreshing the `craftResult` cache to the new arrow count *without sending it*. The mute exists to skip echoing changes the client already predicted, and vanilla gets away with it because client and server run identical click logic; here `Bow.use` is client-side-gated by the `!world.isOnline` conversion, so the client predicts **nothing**, and the swallowed diff never fires again on any later tick. The arrow flight looked fine because entity spawns ride a separate channel. The same mute sat in `handleWindowClick`'s accept path, swallowing every unpredicated click side-effect (quiver contents, accessory effects). Fixed by deleting the mechanism: the flag, both mute sites, and both guards in `setStack`/`updateHeldItem` are gone — accepted clicks and places now echo the server's truth, which is idempotent where prediction was correct and self-healing where it wasn't. The stale `copyStack` dance around the mute went with it | `TwoClientTest` 6/7, identical to baseline including B-36's kick. `QuickStackSmokeTest` fails, **identically with the fix stashed out** — it drives `/give`, which does not exist in this tree yet, so its result is the rig's dead-command problem, not the change's. **Played and confirmed** — arrows deplete the moment they are fired, hotbar or main inventory; bow durability drops; shift-clicking and dragging show no flicker from the now-unmuted echoes |
| B-41b | **Placing blocks or using items on blocks played duplicate sounds for the acting player in multiplayer.** Reported as "when a client plays a block, they hear two break sounds and potentially two placement sounds... one sound plays locally and immediately, then another packet arrives to the player breaking the block causing it to play again shortly afterwards" | Regression out of B-41: B-41 added `SoundPacket` and implemented `WorldManager.playBlockSound` / `playSound` to broadcast server audio to nearby clients, supporting player attribution (`sendPacketToPlayerCoord(Player player, ...)`) to exclude the player who predicted the sound locally. However, block placement (`BlockItem.useOnBlock`, `BlockItem.placeSlab`, `BlockItem.use`, `BedItem.useOnBlock`, `DoorItem.useOnBlock`, `FlagItem.useOnBlock`, `PlaceableItem.useOnBlock`, `Seeds.useOnBlock`, `FoodItem.useOnBlock`), tools and item interactions (`Axe`, `Hoe`, `Shovel`, `Pickaxe`, `Sword`, `FireStriker`, `Bucket`, `Fertilizer`, `ThrowableItem`, `Key`, `Screwdriver`, `Barrel`, and staff weapons) called `world.playBlockSound` / `world.playSoundEffect` without passing `Player player` (which was already available in their parameters). As a result, `WorldManager` treated them as un-attributed global sounds and broadcast `SoundPacket` to the acting player, who had already predicted and played the sound locally on the client. Fixed by passing `player` into all block placement, tool use, and item interaction sound triggers in `core/`. | `SoundSmokeTest` (Phase 4), **measured red/green against a live server.** Phase 4 tests block placement sound attribution: `actor` places a stone block via `PlacePacket` -> `watcher` receives `SoundPacket("step.stone")` categorized as `TYPE_BLOCK`, while `actor` receives zero duplicate `SoundPacket`s from the server. All 14 checks in `SoundSmokeTest` pass against the fixed build. `./gradlew build` clean |
| B-43 | **The wither status effect does not deal damage to players or mobs.** Reported as "the wither status effect does not seem to do anything to players when in multiplayer" | `Effect.performEffect` only handled regeneration, poison, hunger, heal, and harm — `EffectList.wither` was missing entirely, so the tick loop in `Mob.tickEffects` -> `EffectData.onUpdate` -> `performEffect` never applied damage. In addition, `Effect.isReady` did not handle wither's `40 >> amplifier` interval, `DamageSource.wither` was undefined, and no death message existed in `gui.lang`. Fixed by: (1) adding wither handling in `Effect.performEffect` (`living.damageEntity(DamageSource.wither, 1)`), (2) adding interval calculation in `Effect.isReady` (`40 >> amplifier`), (3) defining `DamageSource.wither = new DamageSource("wither").bypassArmor()`, and (4) adding `death.wither=%s withered away.` in `gui.lang` | `WitherSmokeTest` (new), **measured red/green against a live server.** Phase 1 tests receiving `EntityEffectPacket` and confirms ticking damage reduces player health over the wire. Phase 2 confirms `Mob.heal` anti-healing guard blocks health gain while withered. Phase 3 confirms wither damage kills the player and broadcasts the `"%s withered away."` death message. Red against pre-fix build: health remained 20 (failed ticking damage) and no death broadcast was generated. Green with fix: all 5 checks pass |
| B-42d | **The nametag of a player in a bed renders about a block and a third below the body.** Reported while play-testing B-42, which is what made it visible: before that fix the sleeper was drawn at the spot it walked from, so the tag was wrong in the same way but nowhere near a bed to notice | `PlayerRenderer.renderLabels` passes `y - 1.5` for a sleeper and `y + 2.15` for everyone else, where `y` is `entity.posY` straight out of `EntityRenderManager.renderEntity`. Vanilla's `-1.5` is written against a player anchored at the **eye line** (`yOffset` 1.62, so `posY` is the eyes); a `RemotePlayer` here is anchored at the **feet** (`yOffset = 0` — the same MP convention `EntityPlayerMP` keeps and B-42 had to work around), so the offset lands 1.62 too low. The standing branch had already been translated for that anchor — `2.15` plus `renderLabel`'s own `getEyeHeight() + getEyeHeight() / 2` (0.18 for a `Player`) puts it 0.53 above a 1.8 head, which is where vanilla's puts it too — and the sleeping branch was missed, leaving the tag at `posY - 1.32`: under the mattress, inside the floor. Now `y + 0.6`, so `posY + 0.78`, which clears the lying model by that same 0.53: `rotate` lays the model flat about Z, so its vertical half-extent is the model's *depth*, about 0.25 | **Played and confirmed** — the tag sits above the sleeping body. That is the only verification there is or can be: no bot renders a label and the constant lives in GL code that needs a context, so the derivation above is the whole argument, with the standing branch as its control — the same expression with the anchor already accounted for. Client-side only, so it needs a client relaunch and no server restart |
| B-42 | **Other players see someone in a bed lying on the floor beside it.** Reported with a screenshot: the sleeping model is flat on the ground a couple of blocks from the bed, while the sleeper's own client is on the bed and the night still passes | `Player.putToBed` moves the entity onto the bed on every copy, but on the server that position survives about 100ms. `NetServerHandler`'s `lastPos*` still holds the spot the player walked to the bed from, and `handlePlayerMovement`'s sleeping branch runs `teleportTo(lastPosX, lastPosY, lastPosZ)` on **every** movement packet — ten a second — so the authoritative position snaps straight back off the bed. `EntityTrackerEntry` encodes *that* position, and the onlooker's `ClientHandler.handleEntityMovement` drives `Entity.serverX/Y/Z` from it — which `putToBed` never touches. So the `SleepPacket` puts the model on the bed for one tick and the next `MovePacket` drags it back to where the sleeper stood when it clicked. Vanilla does not have this because `sleepInBedAt` ends in `setPlayerLocation`; `EntityPlayerMP.goToSleep` only broadcast the packet. Fixed by adding that call — `playerNetServerHandler.teleportTo(posX, posY - yOffset, posZ, yaw, pitch)` — which moves `lastPos*` onto the bed and makes the snap a no-op. **The `- yOffset` is load-bearing and cost a run to find.** `NetServerHandler.teleportTo` hands the value to `Entity.teleportTo`, which adds `yOffset` back on, and a sleeper carries 0.2 (`putToBed`) while an MP player is otherwise anchored at 0 (feet, see `resetHeight`). Passing `posY` raised the 0.2-tall sleeping box to 150.9375–151.1375, straddling the block boundary, and `EntityPlayerMP.isInsideBlock`'s probe — capped at `maxY - 0.1` — then reads the block *above* the bed: **both bots suffocated in bed 11 seconds after lying down**, under the stone roof the test builds, and the two-sleeper clock check failed because they were dead rather than asleep. Feet-relative the box is 150.7375–150.9375, wholly inside the bed's own block, and all three copies of the position then agree exactly | `SleepSmokeTest` gained two checks, **both measured red against a live server with the one line reverted**. The onlooker's is the reported symptom in numbers: `Bot` now tracks other entities the way a client does (`positionOf`, fed by `PlayerSpawnPacket`, `TeleportPacket` and `EntityMovementPacket`), and red is `dreambot has sleepbot at 875.0,150.1875,768.0` against a bed at `877.5,_,769.5` — the standing spot, 2.06 blocks away, which is the screenshot. Green is on the bed. The sleeper's own half is red as `sleepbot's last accepted position is 875.0,150.0,768.0`. **The suite is green end to end for the first time** — 29 checks, including the two the B-38 row records as red: those were B-38c's wedged saves plus a rig hazard, not a sleeping defect (`world/players/{sleepbot,dreambot}.dat` deleted, and `Bot.read` now adopts *every* teleport rather than only the first — see B-42b). `BlockBreakSmokeTest` green; `TwoClientTest` still fails only on B-36's flying kick, identically with the rig change taken back out. **Not played** — no bot renders a model, so this is verified as coordinates on the wire, not as a picture |
| B-41 | **Interacting with doors, trapdoors, fence gates, chests, and other interactables in multiplayer played no sound for other clients.** Interacting with a door/trapdoor/gate only played audio locally on the player who clicked it (predicted client-side in `blockActivated`); all other connected clients heard absolute silence. Server-driven sound events (redstone-powered doors, chest opening/closing) played no sound for anyone at all | In singleplayer, `World.playBlockSound` / `playSoundEffect` invokes `worldListener.playBlockSound(...)` on `WorldManager`, which directly routes to `SoundManager` in the client JVM. In multiplayer: (1) `WorldManager` on the dedicated server had empty stubs for `playBlockSound`, `playSound`, `playMobSound`, and `playAmbienceSound`, so every server-side sound trigger was silently swallowed. (2) No generic sound packet existed on the wire to transport named sound effects, 3D coordinates, volume, pitch, or sound categories from server to clients. (3) Direct player interactions on blocks like doors predicted audio locally on the clicking client (`world.playBlockSound(...)` without entity attribution), which would cause echoing/double-audio if the server broadcast the resulting sound blindly to all players including the originator. (4) `ChestContainer` never called `chestContents.open()` on construction or `inventory.close()` on `onClose()`, leaving `ChestEntity.players` stuck at 0 so chest open/close sounds and lid animations never fired in multiplayer. Fixed by: (1) Adding `SoundPacket` (packet ID 62, `SERVER_TO_CLIENT`) serializing `soundName`, `(x, y, z)`, `volume`, `pitch`, and `soundType` (`TYPE_GENERIC`, `TYPE_BLOCK`, `TYPE_MOB`). (2) Adding player-attributing overloads `WorldListener.playBlockSound(Player, ...)` and `WorldListener.playSound(Player, ...)` (defaulting to non-attributed calls) and matching methods in `World.java`. (3) Implementing `WorldManager.playBlockSound`, `playSound`, and `playMobSound` using `configManager.sendPacketToPlayerCoord(...)` which sends to all players in radius while excluding the optional attributed player (preventing double-audio). (4) Implementing `ClientHandler.handleSound` on the client to invoke `mc.sndManager.playSound(soundName, x, y, z, volume, pitch)`. (5) Passing the activating `Player` in `Door`, `Trapdoor`, `FenceGate`, `Cauldron`, and `LockedChest` `blockActivated` calls. (6) Adding `open()` and `onClose()` in `ChestContainer`. (7) Adding `getMaxMeta()` to `Door` and `Battery` so operator `/setblock` commands work with block metadata. `PROTOCOL_VERSION` bumped to 20260817 | `SoundSmokeTest` (new), **measured red/green against a live server.** Phase 0 tests `SoundPacket` write/read serialization and clientbound decoder recognition. Phase 1 tests direct wooden door open/close: `doorbot` right-clicks door -> `watchbot` receives `SoundPacket("random.door_open")` (and `door_close` on close) categorized as `TYPE_BLOCK` near target coordinates, while `doorbot` is excluded from the server broadcast to prevent double audio. Phase 2 tests redstone door toggle: powering door with adjacent `Battery` broadcasts `SoundPacket` to **both** `doorbot` and `watchbot`. Phase 3 tests chest container: `doorbot` right-clicks chest -> `watchbot` receives `SoundPacket("random.chest_open")`. All 14 checks green against the fixed build; revert of packet 62 flow mapping fails phase 0 decoder; revert of `WorldManager.playBlockSound` fails watcher receipt with timeout |
| B-38 | **Beds set a respawn point but never passed the night, and one player getting into bed dropped every other player on the server.** Both halves of the report, and they compound: the kick happens the instant the first player lies down, so the survivor is always alone, which is the case the clock fix below is about | **Five defects, and each of the first three alone keeps the night from passing.** (1) **The kick.** `SleepPacket` is id 17, mapped `CLIENT_TO_SERVER` while only the server ever sends it — `EntityPlayerMP.goToSleep` broadcasts it to tracked players and no client code has ever sent one. `Packet.readPacket(dis, false)` refuses an id that is not in `clientboundAllowedIds`, so every client watching the sleeper throws `IOException` out of `NetworkManager` and is disconnected. The sleeper survives, because `sendPacketToTrackedPlayers` excludes the entity itself. Now `SERVER_TO_CLIENT` (vanilla has it `BOTH`; nothing here sends it upward, so the tighter direction is also the honest one). (2) **The clock.** `EntityPlayerMP.preventSleeping()` overrode the base predicate — "is this player holding a dream catcher" — with `world.isDaytime()`. `Player.wakeUpPlayer` picks the dream-dimension branch on `!preventSleeping()`, so at night, which is the only time anyone sleeps, the server *always* took it — and `Player.switchDimension` is an **empty method**; only `LocalPlayer`/`NetworkPlayer` override it. The player woke, the spawn was set (which is why the report says respawn points work), and `setTime` was never reached. The paired `awaken()` override, also `isDaytime()`, added a second wake path that fires the same branch; both overrides are decompile leftovers and both are gone. The server now wakes the way singleplayer does, out of `World.playersSleeping()`/`triggerSleep()`. (3) **Once per sleeper.** The advance lived in `Player.wakeUpPlayer`, called from `triggerSleep`'s loop, so N sleepers moved the clock N × dayLength/2 — **two players move a whole `dayLength` and land on the same time of night**, which is the reported symptom again with (1) and (2) fixed. Moved to `World.triggerSleep`, applied once, and skipped when every sleeper had a dream catcher and left for dimension 2 instead. (4) **Cancelling out of bed skipped the night.** `handleEntityAction` state 3 called `wakeUpPlayer(false, …)` — the same non-reset path sleeping-through uses. Harmless only because (2) was swallowing it; with (2) fixed, pressing Esc in a bed would advance the clock. Now `true`, which is vanilla. (5) **A player who logged out asleep could never log back in.** `Player.readFromNBT` wakes them, `EntityPlayerMP.wakeUpPlayer` ends in `playerNetServerHandler.teleportTo(…)`, and during login there is no handler yet — `NullPointerException` out of `NetLoginHandler.doLogin`, "Internal server error", every time, forever. Guarded, as vanilla does. Not a scope decision: (1) and (2) put bots to sleep and the kill left the file that way. (6) **Found by playing the first five, and the one the player actually sees: getting out of a bed charged the *client* a rest cooldown the server never charged.** `EntityPlayerMP.wakeUpPlayer` broadcasts `AnimationPacket(this, 3)` through `sendPacketToTrackedPlayersAndTrackedEntity` — the waking player is in that broadcast, and `ClientHandler.getEntityByID` resolves its own id to `mc.player` — and `handleAnimation` case 3 runs `wakeUpPlayer(false, ...)`, the *slept-through* variant, which sets `timeWithoutRest = dayLength/2`. Cancel out of a bed once and the client is locked out of every bed for 18000 of its own ticks while the server, which charged nothing, keeps putting it to sleep. The client's `goToSleep` then returns `NOT_POSSIBLE_NOW` and never opens `SleepScreen`, so the player keeps right-clicking a bed the server has already put them in — and reads **"This bed is occupied!"** about their own body, from `Bed.blockActivated` on the server, while the night passes around them. Reported exactly that way. Fixed by putting the *kind* of wake on the wire: `AnimationPacket` state 3 stays "slept through", new state 6 is "got out early", and `EntityPlayerMP.wakeUpPlayer` picks between them from the `resetTimer` argument it was already given. The client maps 6 to `wakeUpPlayer(true, ...)`. **Not fixed, and the reason this needs watching:** `timeWithoutRest` is per-copy and still not synced — the two ends now only agree because the same event drives both. The client decrements it on its own tick and the server on the movement packets it receives, so a laggy client's copy runs *ahead*, which fails permissive (the client asks and the server refuses, a stuck sleep screen) rather than the 15-minute lockout above. `PROTOCOL_VERSION` → 20260816 for the widened `bedY`, which went over the wire as a **signed byte** for a world 300+ blocks tall — a bed at y=150 arrives as -106, fails `wakeUpPlayer`'s `abs(posY - y) <= 2`, and the remote player stands upright in the bed instead of lying in it | `SleepSmokeTest` (new), **every check measured red against its own defect, one revert at a time, against a live server.** Phase 0 needs no server at all: it round-trips a `SleepPacket` through `Packet.readPacket(…, false)`, the client's own decoder. Red with the flow map reverted: `readPacket threw: … !clientboundAllowedIds.contains(17)` — and the live phase reports `dreambot died reading the packet: java.io.IOException`, the report's kick verbatim. Red with `bedY` back to a byte: `sent 875,150,769 got 875,-106,769`. Phase 1 sleeps one bot and records the jump (18020 ticks, dayLength 36000 plus the 20 ticks between `UpdateTimePacket`s); red with `preventSleeping()` restored: no jump in 45s. Phase 2 puts a second bot in a second bed, watches **each bot receive the other's** `SleepPacket` and stay connected, and asserts the jump is the same 18020 — red with the advance back in `wakeUpPlayer`: **36020, a whole day, the same time of night it was.** (5) was red for real before it was fixed and is green after, in the server log rather than the test: `NullPointerException at EntityPlayerMP.wakeUpPlayer(363)` from `readPlayerData` on login. `BlockBreakSmokeTest` still green; `:server:smokeTest` still fails only on B-36's flying kick, identically to HEAD. (6) has its own pair: red is `the server sent animate 3, which the client turns into wakeUpPlayer(false, ...)`, green is animate 6 for the cancel and 3 for the sleep-through, asserted separately. A **server-free** phase pins the consequence rather than the packet, on a real `NetworkWorld` and a real bed: after `wakeUpPlayer(true, ...)` the player can get straight back into bed, after `wakeUpPlayer(false, ...)` it cannot. **Two checks are red at the end of this session and neither is about sleeping** — the two-sleeper wire phase fails because `sleepbot` does not track `dreambot` after `dreambot`'s long teleport, so no `SleepPacket` is broadcast; the test now reads the bed's own occupied bit and reports that `dreambot` **is** in the bed, which is what rules the sleeping code out. Both passed earlier in the same session, before the bots' saved player files were deleted to clear B-38c, and the behaviour they cover (one clock advance for N sleepers) was measured red/green in the previous session and is untouched. See B-38c. Needs **both** `sleepbot` and `dreambot` opped, and the server to itself — the night only passes when every player in the Overworld is asleep. **Not yet played** — nobody has watched a second player's model lie down, and the sleep fade and `SleepScreen` are untouched and unexercised |
| B-40 | **Nothing the server said to a single player ever arrived.** Reported as "right clicking the bed does nothing, and I don't get a message... there may be a valid reason from serverside to not let me in the bed but I'm not getting it on client" — which is exactly right, and it is not a bed bug | `Player.addChatMessage` is an **empty stub**. `LocalPlayer` overrides it to write to the chat overlay; **`EntityPlayerMP` never did**, so every message the server addressed to one player went nowhere in multiplayer. **17 call sites across the game**, not just beds: all four bed refusals, `Altar` and `RespawnAnchor` setting a shrine, `Teleporter` occupied, `Painting` refusing to hang, `MagicMirror`, `Eye` reporting no stronghold, `Guardian`, `CommandBlockEntity`'s output, and the blood-moon / blue-moon / sun announcements out of `World.tick`. It was invisible for as long as it was because the *client* used to run `Bed.blockActivated` too and print its own copy — B-39 removed that second voice, and the silence underneath it became audible. Fixed by overriding it in `EntityPlayerMP` to send a `ChatPacket`, null-guarded on the handler the way B-38's `wakeUpPlayer` is, since `World.tick` can reach a player mid-login. Then, at the user's call, the two refusals that had **no branch at all**: `Bed.blockActivated`'s if-chain covered `SET_POSITION`, `NOT_POSSIBLE_NOW` and `NOT_POSSIBLE_HERE` and fell off the end for `TOO_FAR_AWAY` and `OTHER_PROBLEM`. It is now a `switch` with a `default`, so a status nobody has thought of yet still says something rather than nothing — the whole failure mode of this session in one line. Two new keys in `tile.lang`: `tooFar` and `cannot`. `TOO_FAR_AWAY` is genuinely reachable in play — `handlePlace` allows 8 blocks of reach and `goToSleep` wants the player inside a 3x2x3 box, and that gap was silent | `SleepSmokeTest` gained "a bed the server refuses says why, in chat": sleep through the night, then go straight back to the same bed with the rest timer still running. **Measured red/green** — red is `nothing came back`, green prints the server's own words, **`You are not tired enough to sleep right now.`**, which is the message the user should have had all along. That is also the answer to why their bed did nothing: they had slept minutes earlier and `timeWithoutRest` runs half a day. A failed `getIntoBed` now reports the refusal it was given, so the next reader is not left guessing which of `goToSleep`'s four gates shut. The out-of-reach branch has its own pair — the bot stands at `BED_X - 4`, inside the server's reach and outside `goToSleep`'s box — red `got nothing`, green the real string. **The other 16 call sites are fixed by the same one override and none of them has been exercised**; the fix is structural, the verification is one path |
| B-39 | **Sleeping is now decided by the server alone.** Not a bug report — a redesign, at the user's call, after B-38's seventh symptom: right-clicking a bed put the camera on the floor at the foot of it, looking sideways, and "This bed is occupied" came back again. That is a *standing* camera — `wakeUpPlayer` repositions to `Bed.nearestFreePos`, and `GameRenderer.updateCamera:248`'s sleep branch stops applying, so the walking yaw survives. The client had entered the bed and been pulled straight back out of it while the server kept it asleep | **The fault was structural and B-38 only ever moved it around.** Both ends ran `Bed.blockActivated` → `goToSleep` independently and re-derived the verdict from state that was never synced — the rest cooldown (B-38's sixth defect), block light, bed metadata, position. Four of B-38's seven symptoms are that one fault, so the seventh was fixed by removing the second decider rather than by patching it. Now: `Bed.blockActivated` returns immediately when `world.isOnline` — the click is already travelling as a `PlacePacket` and the client makes no decision at all. The server answers `SleepPacket` through `sendPacketToTrackedPlayersAndTrackedEntity` instead of `sendPacketToTrackedPlayers`, which had **excluded the sleeper** — the one client that had to be told, and the reason it was left guessing. `ClientHandler.handleSleep` applies it with the new `Player.putToBed`, the state change lifted out of `goToSleep` with none of the checks, so there is nothing left to disagree with. `Player.onTick`'s `!isInBed()` self-wake and `SleepScreen`'s local `wakeUpPlayer` are both gated to singleplayer; cancelling is now a request that the server answers with the B-38 wake animation. Refusals were already server chat, so the client's copies just go away, and `timeWithoutRest` stops needing a sync because the client never reads it. **Singleplayer is untouched by construction** — with no server it is the `!isOnline` side, and `goToSleep` still calls `putToBed` itself. One incidental fix: `setSize(0.2F, 0.2F)` and `yOffset = 0.2F` used to run before the distance check, so a too-far click left the player shrunk | `SleepSmokeTest` gained "the server tells the sleeping player itself, not just the onlookers". **Measured red/green**: reverted to `sendPacketToTrackedPlayers`, `no SleepPacket addressed to sleepbot`; restored, it arrives naming the right bed. The rest of the suite is unchanged and still green, which is the point — the server side of sleeping did not move. **The client half is unplayed and untestable from a bot**: no bot renders a camera, opens a `SleepScreen`, or runs `Bed.blockActivated` with `isOnline` true. It needs a play test |
| B-37 | **Item durability on the client ran exactly one point ahead of the server.** Mining showed the bar drop, then snap back a point; a tool "broke" client-side, was restored by the next slot sync with one use left, and broke for real on the following swing | `MultiplayerMode.removeBlock` damaged the held item itself — `onDestroyBlock`, then the `stackSize == 0` destroy — and then called `super.removeBlock`, which is `SurvivalMode.removeBlock`, which does the identical block again. Two applications per break on the client against `ItemInWorldManager.hitBlock`'s one on the server. It reads as a *constant* one-point lead rather than a widening one because every `SlotPacket` overwrites the whole stack: the client's base is always the server's value, so each break went d → d+2 and was pulled back to d+1. That also explains the endgame — the client passes `maxDamage` while the server is one short, so the resync hands back a tool with a single use left. With a mismatched tool `Tool.onBlockDestroyed` charges 2, so the lead there was two points, not one. Fixed by deleting the `MultiplayerMode.removeBlock` override outright; the inherited `SurvivalMode` one is a superset of it (it also computes `canHarvest` and drops). Two incidental consequences, both toward correct: the override passed the block **id** where `Item.onBlockDestroyed` wants `side`, which only `gauntletUriel` reads, and its pre-damage ran before `SurvivalMode` computed `canHarvest`. **Left alone, separate bug:** `ItemInWorldManager.hitBlock` passes the block id as `side` too, and has no `side` to pass — so the server and client still disagree about the gauntlet's break plane | `client/network/player/MultiplayerMode.java` (override removed). **Measured red/green, statically:** `javap -c` over the compiled break path counts `ItemStack.onDestroyBlock` invocations reachable from `removeBlock` — 2 before the fix (1 in `MultiplayerMode`, 1 in `SurvivalMode`), 1 after, against the server's 1. `BlockBreakSmokeTest` green either way (server-side, untouched). `./gradlew build` clean. `TwoClientTest` still fails on B-30/B-36's flying kick, unrelated and pre-existing. **Not yet played** — nobody has watched a bar stay put across a mine |
| B-25 | **Weather never reached multiplayer clients.** The server ran a real weather cycle out of `level.dat` — dousing fires, hydrating farmland, forming snow and ice, flipping `FleeSun`/`RestrictSun` mob AI — and every client sat at `rainingStrength = 0` for the life of the session. No rain, no storm darkening. The block changes it causes *do* arrive through the normal update path, so snow and ice appear under a clear sky | Two independent holes, and **each one alone pins the strength at 0**, so both had to be closed. (1) Nothing on the wire carried the state at all: the raining/thundering flags live in `WorldInfo` off `level.dat` and no packet had ever referenced them. (2) `updateWeather()` is reachable only from `World.tick()`, and `NetworkWorld.tick()` deliberately does not call `super.tick()` — so even a client told it was raining would never ramp. Fixed with a new `WeatherPacket` (id 71, server→client, one byte: bit 0 raining, bit 1 thundering), and by splitting `World.updateWeather()` in two. The timer half — rainTime/thunderTime countdown and the flag flips — **stays server-only on purpose**; only the interpolation tail moved into `World.updateWeatherStrength()`, which `NetworkWorld.tick()` now calls. That split is what makes the flags-only wire format work: the client ramps its own 0.01/tick from the flag rather than being fed a float every tick, so a weather change costs **one byte per player**, not 8 bytes × 20 tps. Server sends it from `MinecraftServer.doTick` when `WorldServer.weatherChanged()` sees a flip (per-dimension, seeded from `level.dat` at construction so a load does not fire a spurious broadcast) and from `ServerConfigurationManager.updateWorldTimeForPlayers`, which is already the login and respawn hook — a client walking into an already-raining world learns it at join. `PROTOCOL_VERSION` → 20260815. **Not fixed, deliberately: the lightning flash.** `World.lightningStrikeTime` is set by `LightningBolt`, which B-23 put on the wire as an entity — the flash follows from the bolt existing client-side, not from this packet, and belongs to that fix | `WeatherSmokeTest` (new), **measured red/green against a live server, both halves separately.** Green: 8 of 8. Red with the wire half reverted: no `WeatherPacket` at login at all. Red with only `NetworkWorld.tick()`'s ramp call removed — packets arrive fine, and the strength reads **`0.0` after 100 ticks of raining**, which is the reported symptom exactly. The client half ticks a real `NetworkWorld` and asserts the 0.01/tick rate, the clamp at full rain, and the ramp back down; the wire half awaits the login packet *before* settling (a `settle()` reads straight past it — the first draft failed on that, not on the game) and then flips the server with `/weather rain` and `/weather clear`. Needs `weatherbot` opped. **Not yet played** — nobody has watched rain fall on a second client, and the renderers (`WeatherRenderer`, `SkyRenderer`'s darkening, `FogRenderer`) are untouched and untested by this |
| B-35 | **Footstep sounds did not play in multiplayer for remote players or mobs.** Remote walking players and mobs were completely silent and produced no footstep particles on sand/snow | Minecraft does not send footstep packets over the wire; footsteps are calculated and played locally on the client based on movement. In singleplayer, `Entity.moveEntity` updates `posX`/`posZ` and accumulates `this.distance += sqrt(dx^2 + dz^2) * 0.6` when on the ground. In multiplayer on the client: (1) `RemotePlayer` has `noClip = true`, never calls `moveEntity()`, and interpolates coordinates directly in `mobTick()` via `setPosition()` with `onGround` uninitialized (false), calculating delta in `onTick()` only for limb swing (`limbYaw`/`limbSwing`) and never updating `this.distance` or calling `playStepSound()`. (2) `Mob` in multiplayer interpolates coordinates via `setPosition()` in `mobTick()`, then calls `moveEntityWithHeading()` -> `moveEntity()`, but `Entity.moveEntity` line 517 is guarded by `!this.world.isOnline || this instanceof Player || !(this instanceof Mob)` so `moveEntity` never modifies `posX`/`posZ` on the client (to prevent client physics fighting server packets). Thus `oldX` and `oldY` were 0, `this.distance` never advanced, and `playStepSound()` was never called. Fixed by extracting `Entity.updateFootstep(dx, dz)`, calling it in `Mob.moveEntityWithHeading` on the client with the packet movement deltas, and in `RemotePlayer.onTick` with the tick displacement and an active bounding-box ground support check (`getBoundingBoxes(this, boundingBox.getOffsetBoundingBox(0.0, -0.1, 0.0))`). World-mutating `onEntityWalking` blocks (`Farmland`, `LiquidBlock`) are guarded with `!world.isOnline` | `FootstepSmokeTest` (new), **measured red/green.** Asserts that: (1) a `RemotePlayer` moving across solid ground detects `onGround = true`, accumulates `distance > 2.0`, and advances `nextStep` at standard walking cadence, (2) sneaking, mid-air (unsupported), and flying silence footsteps, (3) a `Mob` (`Cow`) in multiplayer accumulates distance and advances `nextStep` from packet interpolation, and (4) singleplayer mob movement does not double-accumulate distance. Red on reverted build: 4 checks fail with `got distance=0.0` (RemotePlayer unsupported and silent, Cow silent). Green with fix: all 9 checks pass |
| B-34 | **F3 biome always reads "Valley Hills" in multiplayer, and the sky color never changes.** Two independent holes, both of which had to be closed. (1) A world-type leak: `ClientHandler.handleLogin` built the `NetworkWorld` from only seed+dimension, and `World(SaveHandler, name, seed, Dimension)` never re-read the level settings — `Dimension.init` ran over a fresh `WorldInfo` whose `worldType` was whatever the world-select screen last left in `WorldSettings`, and the DEFAULT/fallback branch built a `SingleProvider(BiomeList.valleyHills)` for the whole session. (2) **The one the first fix missed, and which kept the symptom alive after it**: `WorldInfo(long seed, String name, WorldSettings settings)` — the ctor the client uses — never defaults its `boolean[] biomes` to true (the no-arg and NBT paths do it via `Arrays.fill(this.biomes, true)` in `WorldInfo()`; the seed/name/settings ctor does not call `this()`). A boolean[] left un-filled is all-false, `World` then points the static `BiomeList.enabledBiomes` at it, `BiomesLayer` checks `enabledBiomes[chosen.id]` on every pick, fails every one, and emits `biomeDefault.id` = 255 everywhere. So the client's biome *generator* was now correct (passed the wire `worldType`) but every answer it computed was still `255` → `World.getBiomeAt` / `Chunk.getBiomeAt` → `BiomeList.biomeDefault` → valleyHills. The biome → sky link is direct: `Dimension.getSkyColor` averages `world.getBiomeAt(...).getSkyColor()` over a 16x16 column, and one constant biome means one constant sky. | `core/network/packet/misc/LoginPacket.java` + `server/network/NetLoginHandler.java` + `client/network/ClientHandler.java` + `client/network/world/NetworkWorld.java` carry `worldType`/`singleBiomeId` over the wire and re-run `Dimension.init` on the client. `core/game/world/WorldInfo.java` gained `setType`/`setSingleBiome` **and** the real second fix: `biomes` now defaults through an instance initializer that all ctors run, so `WorldInfo(seed, name, null)` also produces an all-enabled map. `PROTOCOL_VERSION` → 20260814. Not fixed: the four non-overworld dimensions' types are not on the wire (one shared level.dat); the same leak will reappear there if any of them ever derives its provider from type | **Measured red/green, then playtested.** Red: a probe on a live login reported `enabledBiomes[4](valleyHills)=false` and `enabledBiomes[6](jungle)=false` in the bot JVM and a 289/289 all-valleyHills biome map against the server's own provider; pre-fix `b`'s constructor design guarantees the user sees the same. Green with the field-initializer fix: live `BiomeSmokeTest` reports 0/289 mismatches and 2/289 valleyHills across the region, and `enabledBiomes[4]=[6]=true`. Server started for both probe+test and stopped afterwards (port 25565 free). **Then played** — the user walked between biomes in multiplayer: F3 now reports the real biome at each spot and the sky tint changes across boundaries |
| B-32 | **A punch played no arm swing on any other client.** Your own swing animated normally; nobody else's ever did | The wire was never the problem — `NetworkPlayer.swingItem` sends, `handleAnimation` applies, `EntityPlayerMP.swingItem` broadcasts, `ClientHandler.handleAnimation` calls `swingItem()` on the right entity. What was missing is the per-tick *advance*. `Player.swingItem` only latches `swingProgress = -1, swinging = true`; the field the renderer reads (`Mob.swing`, via `getSwing` → `BipedalModel`) is written by `Player.swingArms()`, and its sole caller was `Player.mobTick()` — **a method `RemotePlayer` replaces outright**, so a remote player latched `swinging = true` forever with `swing` pinned at 0. Fixed by moving the `swingArms()` call to `Player.onTick()`, which all three player types reach. **The obvious fix — adding `super.mobTick()` to `RemotePlayer` — would have been wrong**, and reading `Player.mobTick` top to bottom is what ruled it out: that override is a deliberate replacement (it re-implements the camYaw/camPitch block verbatim), and `Player.mobTick` also heals, ticks the inventory, applies necklace effects, and in the topaz-armour branch mutates `motionY` and spawns 20 particles a tick — client-side ghost effects of exactly the B-14/B-15/B-23 family. `Player.onTick` is the right home because it is already where per-tick *animation* bookkeeping lives: the cape's `capeX/shiftX` interpolation sits there, which is why remote capes wave and remote arms did not. **The three player types have complementary tick topologies and no single method all three reach naively** — `LocalPlayer` overrides `mobTick` (with super) and not `onTick`; `RemotePlayer` overrides `onTick` (with super) and replaces `mobTick`; `EntityPlayerMP` replaces `onTick` and overrides `mobTick` (with super), reaching `Player.onTick` only through `onUpdateEntity`. That last route is the one that makes the fix safe server-side, and it was checked before trusting it | `SwingSmokeTest` (new), **measured red/green against a live server.** It holds the mechanism, not the symptom — no bot can look at an arm. `EntityPlayerMP.swingItem` broadcasts only `if (!this.swinging)` and nothing clears that flag except `swingArms()`, so counting broadcasts across four punches asks whether the advance is reachable. Green: 4 of 4. Red with `swingArms()` unreachable: **1 of 4** — the server latches after the first punch. **The first draft of this test was worthless and said so loudly**: with the bots merely `settle()`d at the ocean spawn it reported 1 of 4 *with the fix in*, and at HEAD too. A probe in `handlePlayerMovement` showed why — **80 consecutive movement packets with `hasMoved=false`**, so `onUpdateEntity` never ran and `swingArms` was called zero times. `settle()` alone does not arm `hasMoved` at the spawn: the fall draws a corrective teleport, `Bot.read` adopts only the *first* teleport it sees, and the echo can never match again. The rig now teleports onto placed stone the way `BlockBreakSmokeTest` does. This is the B-05 failure a third time — a bot can be sending packets and still be entirely ignored. **Then played** — two players, and the puncher's arm now swings on the other client, repeatedly rather than once |
| B-33 | **A remote player's cape killed the client outright.** Reported as "many of these exceptions in her logs" while the cape rendered fine, which reads as harmless log spam — **it is not.** There is no `try`/`catch` anywhere in the render path: `renderCape` → `WorldRenderer.renderEntities` → `GameRenderer.render` → `Minecraft.runLoop`, and the only handler is the one wrapping the whole `while (this.running)` loop in `Minecraft.run`, which calls `clearWorld()` and shows a crash report. Every one of those log entries was a crash, and the cape "rendering correctly" was a later session where the download won the race | `PlayerRenderer.renderCape:261` and `renderElytra:301` call `loadSkin(texture, null)` — a null fallback name, deliberately, because a downloaded cape has no local stand-in. `TextureLoader.downloadTexture` ended `return data != null && data.name >= 0 ? data.name : this.getTexture(name)`, and `getTexture` opens with `atlasPath.equals(...)`. Vanilla's equivalent is `... : (par2Str == null ? -1 : getTexture(par2Str))` — **the null guard was lost in the decompile**, and `bindTexture`'s existing `if (tex >= 0)` check is the tell that the -1 path was designed for. `data.name` stays -1 from `obtainData` until the download thread sets `image`, so any frame drawn in that window threw, and it never recovered at all if the download 404'd or timed out. It hit the *second* player to join hardest: they render the first player immediately while `SkinDownloader` is still doing two Mojang round trips before `obtainEntitySkin` even registers the cape. Fixed by restoring the guard | **Played, not measured — and it was accepted on that basis deliberately.** `TextureLoader` lives in `client/`, which the smoketest source set cannot see (it is in `server/`), and its constructor needs a GL context, so there is no probe to write. What *was* checked is the blast radius, by auditing every caller: only `renderCape:261` and `renderElytra:301` pass a null name, and they are exactly the two crash sites. `MobRenderer:81`, both `SeraphimRenderer` calls and `ItemRenderer:422/512` all pass a real texture name — `Player.texture` defaults to `/mob/player/char.png` and is never null — so `name == null` is false for them and their behaviour is byte-for-byte unchanged. That matters for the two `ItemRenderer` sites in particular, which pass the result to `GL11.glBindTexture` **directly** rather than through the `bindTexture(int)` guard, and would make an invalid GL call on -1. While the download is in flight the cape now draws with whatever texture was last bound (the player's skin) for a frame or two instead of crashing, which is vanilla's behaviour. **The play test is the whole verification here** — two players, one caped, walked up to on a fresh launch with the download still in flight, and no crash |
| B-31 | **Breaking a block with metadata crashed every other client in a 64-block radius.** The breaker saw nothing wrong; everyone watching died in their render loop with a `NullPointerException` at `WorldRenderer.worldEvent:1108`. Meta-0 blocks were harmless, which is what made it read as intermittent rather than as every break | `ItemInWorldManager.hitBlock` packed the 2001 step-sound effect's data as `id + meta * 256`, the layout from before block ids were widened past 255. `WorldRenderer.worldEvent` case 2001 decodes `id = data & 4095` and `meta = data >> 12 & 0xFF`, which is what **every other emitter in the tree writes** — 20-odd block classes, `Tool`, `Sheep`, `Silverfish`, `Antlion`, `Mob`, and the client's own `GameMode.java:69` (`metadata << 12 \| block.id`). Singleplayer went through `GameMode` and was correct; the server was the last caller on the old layout. Stained glass (83) with metadata 1 therefore goes out as 339, and `BlockList.blocks` is 4096 slots of which **3774 are null**, so `blocks[339]` is null and `block.getStepSound(...)` throws. The `index > 0` guard above it passes — the value is not 0, it is simply not a block. `hitBlock` had already read the metadata into `i5` one line above and then re-read it inline, so the fix is `i5 << 12 \| i4` | `BlockBreakSmokeTest` (new), **measured red/green against a live server**, and the red was measured against the *final* test. One bot digs, a second standing 3 blocks away reads the `BlockEventPacket`. Red on the reverted build: `data=339`, decoding to `id 339 (no block)` instead of `id 83 (ColoredGlass)`, metadata 0 instead of 1, and `BlockList.blocks[339]` null — the client's crash predicate copied verbatim off `WorldRenderer:1107-1108`. Green with the fix. **Phase 1 is green in both directions on purpose**: plain stone, metadata 0, where the two layouts agree — it is the case that always worked, and it doubles as the rig's own precondition, since a watcher that hears it is provably in range and a silent phase 2 can then only mean the dig failed. **Then played** — two players, one breaking metadata-carrying blocks, and the other no longer crashes |
| B-26 | **Every death on a server wiped the player's whole inventory onto the floor and never placed a grave** — `PlayerProperties.grave` defaults to **true**, so a multiplayer death was doing the opposite of what the world was configured for, while singleplayer buried you properly. `keepInventory`/baby mode was ignored the same way, the cursor stack was silently lost rather than dropped, and `lastDeathPos`, the death stats and the death-throw motion never happened | `EntityPlayerMP.onDeath` **replaced** `Player.onDeath` instead of extending it: three lines — `inventory.clearContents(true)`, halve the score, broadcast the message — with no `super` call, which put the grave branch, the baby-mode branch, `openContainer.onClose(this)` and every stat below them out of reach on a server. Vanilla's `EntityPlayerMP.onDeath` does not call super either, which is where the shape came from; vanilla's `EntityPlayer.onDeath` simply has none of the grave/baby-mode logic this one carries. Fixed by calling `super.onDeath(ds)` first and keeping the two MP-only lines after it. **`resetHeight()` follows the super call on purpose**: `Player.onDeath` ends by moving `yOffset` to the 0.1 a dying singleplayer body uses, and an MP player is anchored at its feet (B-12), so the movement handler would add that offset to every position the client sends while the death screen is up. That one is an invariant guard, not a measured symptom — the resulting discrepancy is inside the movement handler's ±0.5 tolerance and the entity is discarded at respawn anyway | `DeathSmokeTest` (new), **measured red/green against a live server**, and the red was measured against the *final* test, not an earlier draft. Red on the reverted build: no grave block on the wire, and `/count Item` goes from 52 to **57** across the death — the five stacks hitting the floor, exactly. Green with the fix. Two checks stay green in both directions on purpose: the death message must still broadcast (the only thing the old override did that had to survive), and baby mode must *suppress* the grave rather than fill one. **Then played**, which is the half no bot could reach: a grave has no `blockActivated`, so it is emptied by **breaking** it (`PlayerGrave.onBlockRemoval` → `clearInventory`, and `dropID` returns 0 so the block itself yields nothing). User died in multiplayer, broke the grave, and the items dropped |
| B-26b | **Nothing survived a respawn** — score, every game rule set with `/gamerule`, bed and portal spawns, the ender chest, and in baby mode the inventory the death had deliberately kept. Found while fixing B-26: with `onDeath` corrected, keepInventory *still* lost everything, because the loss is one layer further on | `ServerConfigurationManager.recreatePlayerEntity` builds a fresh `EntityPlayerMP` and copies exactly two fields onto it — `entityId` and `playerNetServerHandler`. **`Player.clone(Player, boolean)`, the method that exists to carry the rest, had zero callers in the entire tree**; vanilla calls it from its respawn path. Fixed by calling `clone(old, false)` there. `clone` could not be called as written: its baby-mode branch ended `this.container = player.container`, which leaves `openContainer` pointing at a *different* container on the same window id (the B-06 desync) and keeps `PlayerContainer.thePlayer` — a `final` field — pointing at an entity that has left the world. It now builds a fresh `PlayerContainer` over the adopted inventory; the two lines above it that copied `quiverContents`/`backpackContents` into the container it was about to throw away are gone, because those contents ride in the accessory item's own NBT and come back through `loadContents`. **The `keepStats == true` branch has the identical aliasing and was left alone — it is still uncalled**, and dimension transfer (Phase 4) will be its first caller | Same run as B-26. Its own red: the respawned player holds **0 of the 5** stacks with keepinventory on. One check guards the seam between the two fixes — the grave branch copies stacks in and never clears the inventory it copied from, so what empties the player is the respawn handing them a new one; if `clone` ever carried the inventory over unconditionally, a grave death would mint a second copy of everything, and that check goes red |
| B-23 | **`LightningBolt`, `LingeringCloud` and `VoidCrystal` never reached any client** — lightning struck, set fires and damaged everything within 3 blocks with nothing to see or hear; a magic explosion's cloud applied effects for ~40s invisibly; a placed end crystal simply was not there | **Two independent holes, both of which had to be closed.** (1) `EntityTracker.trackEntity` is an if/else chain over types and had no branch for any of the three, so no `EntityTrackerEntry` was created and no spawn packet was ever sent — the B-14 failure, three more times. `EntityTrackerEntry.getSpawnPacket` had no case for them either (it throws), and `EntitySpawnPacket.EntityType` had no id to send. (2) **Lightning had a second hole behind the first**: `World.addWeatherEffect` adds to `weatherEffects` and never touches `spawnEntity`, so the storm path (`World.tickBlocks`) and `Trident` never reached the tracker even once a branch existed. Fixed with `LIGHTNING(100)`/`CLOUD(110)`/`CRYSTAL(120)` on the wire, the three tracker branches, the three `getSpawnPacket` cases, a `WorldServer.addWeatherEffect` override that hands the entity to `obtainEntitySkin`, and the matching `releaseEntitySkin` in `World.tickEntities`' weather loop — without that last one a tracker entry for every bolt ever struck leaks and is ticked forever. `PROTOCOL_VERSION` bumped to 20260813. **The client half needed care, because making these exist client-side hands a client three entities that mutate the world.** `LightningBolt` set fire blocks in its constructor *and* its tick and ran `onStruckByLightning` on everything nearby; `VoidCrystal` set fire under itself and dealt beam damage; `LingeringCloud` resolved its own death off a locally-rolled `maxTime`, which is the B-15 kill-it-yourself failure. All are now `!world.isOnline`-guarded. Deliberately left on the client: the thunder and explode sounds and `lightningStrikeTime` (the flash) — that is the whole point — and the crystal's beam *target* selection, which is a local nearest-monster scan, so the beam renders without syncing an entity id. `LingeringCloud`'s particle colour needed a wire representation and got the spawn packet's `metadata` short: the potion id, or `-1` for an untinted cloud. Client-side lightning goes into `weatherEffects`, not `entities` — `WorldRenderer` only draws bolts from that list | `EntityTrackingSmokeTest` (new), **measured red/green against a live server.** Green: all three spawn packets arrive with the right metadata. Red: with the fix stashed the suite will not even *compile* (the three `EntityType` constants do not exist) — restoring only the enum and reverting the rest gives the honest failure, all three checks reporting no packet at all. Triggers are real, not `/summon` for lightning: the crystal and cloud come from `/summon`, the bolt from a summoned `Spear` (`Trident`) dropped on a summoned `Cow` while it rains, which is `addWeatherEffect` and lands in under a second. A background storm strike also works and was seen on the wire twice, but it is one in 100000 per loaded chunk per tick and only over a thunder-capable biome — measured at roughly one a minute, far too rare to assert on. **Accepted on the wire evidence alone, deliberately** — the user did not have the items and took the fix without a play test rather than leave it sitting. Nothing here has been seen on a screen and the three renderers (`LightningBoltRenderer`, `CrystalRenderer`, the cloud's particles) are untested; see "Still needs a human" for what to look at if anyone is ever in a position to |
| B-17 | Eight entity type ids were registered twice in `EntityManager`, so eight mobs were unreachable by id and eight others answered to a number that was not only theirs: `Monkey`/`Vulture` 120, `Meerkat`/`Bat` 121, `Seal`/`Fae` 122, `Crab`/`Penguin` 123, `Turtle`/`Fish` 124, `Frog`/`Dolphin` 125, `Alligator`/`Shark` 126, `Bird`/`Jellyfish` 127 | `addMapping` writes two maps that disagree under a duplicate: `IdToClass.put(id, class)` **overwrites**, so the last registration wins the number, while `classToId.put(class, id)` keeps **both** classes pointing at it. So the server asked `classToId` what a Monkey is (120) and the client asked `IdToClass` what 120 is (Vulture) — the id round trip was not a round trip. Hit `MobPacket`/`EntitySpawnPacket` on the wire and `createEntityFromId` anywhere (e.g. `SpawnArrow`'s random 100–199 roll, which could never produce those eight). Save files were never affected: entity NBT is keyed on the **name** (`stringToConstructor`), which had no duplicates. Fixed by moving the eight later-registered classes to 130–137, continuing after `Ostrich` 129 — the block they were appended to. `addMapping` now throws at class-init on a duplicate id **or** name, so this cannot come back quietly | `EntityIdSmokeTest` (new, needs no server): name → class → id → class for all 124 registered types. **Measured red/green** — restore the eight ids and remove the guard and exactly the eight losing classes come back wrong (`Monkey: sent as id 120, rebuilt as Vulture`, and 7 more); restore the ids and keep the guard and `EntityManager` refuses to initialize (`Error: Entity id 120 is already registered to Monkey`). Green run: all 124 round trip. Not yet seen in play — a client-side look at a summoned Monkey is still worth doing |
| B-00a | Every player kicked ~4s after they started moving ("floating too long") | `playerInAirTime` was reset only inside the `allow-flight=true` branch — the branch where it is never read — so with the default it was a monotonic counter. It also tested `d15`, which the line above forces to `0.0` unconditionally, so the hovering question was never actually asked. | Real play log (kick at exactly 80 ticks). The `MovementSmokeTest` half of this was worthless and the fix was wrong anyway — see B-05 |
| B-05 | Still kicked ~4s after joining, "Flying is not enabled on this server", while walking on solid ground | B-00a's replacement support test asked `playerEntity.onGround`, which comes from the `moveEntity` call two lines above it, and `moveEntity` only sets it when a **downward** move was clipped: `onGround = requestedDy != clippedDy && requestedDy < 0`. A client walking on flat ground reports the same Y every tick, so the delta re-applied server-side is dy=0, nothing is clipped, and `onGround` is false for a player standing on the floor. Every movement packet counted as hovering — 80 of them, four seconds, kicked. Fixed by asking the world instead: any non-air block in the player's box extended 0.55 down is support. | Real play — five minutes of walking and building, no kick, where before it was four seconds flat. Negative test: with the fix stashed, a bot on a stone platform is kicked mid-walk |
| B-00b | Being kicked crashed the client instead of showing the disconnect screen | `NetworkWorld.tick()` drains packets, so the kick lands inside `Minecraft.runTick`'s `world.tick()` call; `ClientHandler` responds with `changeWorld(null)`, nulling `world` and `player` while `runTick` is still on the stack. | Guarded; covers every disconnect path (kick, lost connection, server stop) |
| B-00c | Every server-initiated teleport placed the player 1.62 blocks too high, including on login | `PlayerTeleportPacket(x, y, stance, z)` was called as `(x, y + 1.62, y, z)` — y and stance swapped. | Rests on real play (login puts you where you logged out). The `MovementSmokeTest` "0 rubber-bands" originally cited here proved nothing — the server was ignoring that bot |
| B-00d | `save-on` disabled saving and `save-off` enabled it; every later `save-all` silently did nothing | `setSaving(true)` is what *suspends* saving (`canSave()` returns `!levelSaving`). The two commands had the booleans the wrong way round. | Console: `save-off` → `save-all` writes nothing; `save-on` → `save-all` writes all five worlds |
| B-04 | Multiplayer could not carry blocks above Y=255: sections 16–19 of the 20-section world never synced, and dig/place above Y=255 corrupted | The wire format was 256-high in four places: `UpdateChunkPacket` sent its section masks as 16-bit shorts (`& 0xFFFF` truncation) and `ServerChunkDistance` clamped section bits ≥ 16 out of the update mask; `UpdateSectionPacket` packed coordinates into a short with only 8 bits of Y; `DigPacket`/`Packet15Place`/`BlockEventPacket` sent Y as one byte. The client chunk-size check also assumed 14336 bytes per section (an upper nibble array for *every* section) where the writer only sends one for sections with block ids > 255. | Masks widened to 32-bit ints and Y fields to shorts in both trees; exact-size check computed from both masks. `HeightSyncSmokeTest` round-trips blocks at Y=5/255/300/315 through real packet framing (goes red with the 16-bit mask restored — negative test) and its live mode `/setblock`s at Y=300 against a running server, receiving the block back as `UpdateSectionPacket y=300 id=1`. `PROTOCOL_VERSION` bumped to 20260810 so a stale client jar disconnects at login instead of misparsing |
| B-04b | `Chunk.setChunkData` diverged between the trees (`checkParity` red before this session touched anything) | The client copy had bounds-guarded `arraycopy`s (tolerating short payloads); the server tree still had the raw copies. | Client version mirrored into the server tree; `checkParity` clean |
| B-06 | In the knowledge book GUI, clicking an item snapped it straight back to its slot, and shift-click moved it into the book slot while leaving a duplicate behind | The book has **no server-side container**. `LocalPlayer.displayGUIRecipe` opens `RecipeScreen`, and `ContainerScreen.initGui` swapped the client's 37-slot `LoreContainer` into `openContainer` — while `EntityPlayerMP.displayGUIRecipe` is an empty inherited stub, so the server still had its 70-slot `PlayerContainer`. Both sit on **window 0**, so `handleWindowClick`'s id check passed and every click was resolved against a different layout (book slot 28 is the server's inventory id 24, not hotbar 0). The server computed a different stack, `areItemStacksEqual` failed, and it replied `TransactionPacket(accepted=false)` plus a full `ContainerContentsPacket` — that resync *is* the snap-back. Shift-click is the same event: the item lands in the client-only book slot, the server restores the inventory slot, and the client draws both. | `KnowledgeBookSmokeTest` (new) reproduces it on the wire: `/give` lands in player-container slot 40, the index the book would have sent (28) is **rejected with a resync**, the translated index 40 is **accepted**. Fix keeps `openContainer` pointing at the real `PlayerContainer` and translates each click through `getSlotInInventory`; the book slot now only ever holds a *copy*, so no item leaves the inventory. Play test still wanted — see below |
| B-06b | Opening the book while holding an item on the cursor dropped it on the ground server-side while the client silently cleared the cursor | Switching screens runs `ContainerScreen.onClose` → a close for window 0, and the server's `Container.onClose` drops the cursor stack. `LocalPlayer.displayGUIRecipe` papered over the client half with `dropItems = false`. | Found while fixing B-06. The book keeps the player container open now, so a screen backed by that same container skips its close on the way out (`keepContainerOpen`) and the cursor stack is never touched. Opening the book from a *crafting table* still closes that window, which is correct |
| B-07 | Mining any block with any tool in multiplayer kicked the player with "Internal server error" — the block vanished, nothing dropped. Singleplayer was fine | `ItemInWorldManager.hitBlock` cleared the block **before** running the held item's `onDestroyBlock`, and `Tool.onBlockDestroyed` re-reads the world it was called about: `world.world.getBlock(x, y, z)`. Air is not a `Block` — `BlockList.blocks[0]` is `null`, never assigned — so the read came back null and `block.getTool()` threw. Both client game modes call `onDestroyBlock` while the block is still there and clear it last, which is why singleplayer never hit it. `paxel` and `gauntletUriel` are the only tools that survived, and only because they short-circuit the `getTool()` term. `NetworkListener` catches the NPE and kicks the player, so the process stays up — what dies is the session, once per block | Real play: the NPE is in the server log three separate times (`run/server/logs/server_2026-08-10.log:194`, `:285`, `:331`), all with the identical `Tool.onBlockDestroyed:63 ← hitBlock:107` frame. The premise was then measured rather than assumed — a probe against the built jar prints `blocks[0] = null`, `stone.hardness = 1.5` (so the `hardness > 0` branch is entered) and a stone pickaxe as a `Tool` that is neither paxel nor gauntlet. **Play test confirmed the fix** — mining with a tool no longer kicks |
| B-10 | **Multiplayer could not sprint at all** — sprint key and double-tap W both dead, stamina bar never moved | The server sent the join health packet as `EntityHealthPacket(this.health)` — a one-arg constructor that never sets the packet's `food`/`foodSaturation` fields, while `writePacketData` writes all three, so every joining client received **food=0** (`lastHealth` starts at `-99999999`, so the first tick after login always fires the packet). `ClientHandler.handleHealth` applies it verbatim, and from then on `LocalPlayer.handleSprinting` saw `getFoodStats().getHunger() < 6.0F` every tick and forced `setSprinting(false)` + `setCanSprint(false)` — closing both sprint triggers, which gate on `canSprint()` (itself falling back to the canSprint flag, or food ≥ 6). `isSprinting()` never went true, so stamina never drained. Ruled out first: the server re-broadcasting the player's own data-watcher flags (the entity tracker excludes the player from their own view). Fixed by making the packet carry the real food stats and having `EntityPlayerMP` resend it whenever health, food or saturation changes. | `SprintSmokeTest` (new) against a live server, red/green: the old build's join packet measured on the wire as `health=20 food=0 saturation=0.0` → FAIL; the fixed build sends `health=20 food=20 saturation=5.0` → PASS. **Play test confirmed** — sprint engages and the stamina bar drains in real play |
| B-11 | **Sprint state never reached the server** — no sprint knockback in PvP, no server-side sprint exhaustion in hunger worlds, other clients never learned the state | `NetServerHandler.handleEntityAction` branched only on state 3 (wake from bed); states 4/5 (sprint start/stop), which `NetworkPlayer.sendStatePacket` sends on every state change, were dropped, so the server's player entity never had the sprint flag. Setting it server-side is safe for movement validation: the server re-applies the client's exact deltas through raw `moveEntity`, and `landSpeed`/`jumpSpeed` — the only fields sprint touches — feed only input-driven `moveEntityWithHeading`, which never runs for players. Both sides also clear the flag locally on a landed hit (`attackEntityWithItem` runs on both), so state stays coherent across sprint hits. Fixed by handling states 4/5. | `SprintSmokeTest` phase 2 (two bots), red/green against a live server: with the old build the sprinter's metadata never gained flag 3 → FAIL; with the fix a nearby bot receives `EntityMetadataPacket` with the bit set on sprint start and cleared on stop → PASS. Knockback feel itself falls under the PvP baseline item |
| B-12 | **Crawl in multiplayer soft-locked the player** — crawl engaged and horizontal speed worked, but every movement packet raised a "Player moved wrongly!", and the server rubber-banded the client one stance up, from which it fell again, endlessly; re-pressing crawl or sprinting didn't break the loop | **Anchor mismatch.** An MP player is anchored at its bounding-box minY (feet): `EntityPlayerMP`'s constructor sets `yOffset = 0.0F` and `resetHeight()` restores 0. But `Player.setCrawling` is shared singleplayer code that assumes an *eye-line* anchor and moves `yOffset` to 0.56 for crawl. After a toggle the movement handler's `teleportTo(posY)` does `posY = y + yOffset` and adds the 0.56 twice, while the client reports its position feet-anchored — a persistent, drift-free +0.56 vertical desync (the log shows it exactly: `Got 131.000 / Expected 131.560`, every tick, x/z moving normally which is why crawl *movement* worked). Fixed by overriding `EntityPlayerMP.setCrawling` to keep the feet anchor — set the flag, shrink the box to 0.9m, leave `yOffset` at 0. Two earlier theories failed the same play test and were reverted: (a) `setCrawling(false)`'s unconditional `+1.3` stand-up hop was blamed (wrong — the 23:52 log showed feet at exactly ground level, not wedged), (b) a crawl-aware stance byte in the corrective teleport (untested when the real cause landed; the hardcoded `+1.62` was never read for offsets). Lesson recorded once here because it applies to the whole board: the 23:52 "play test" ran a jar built *after* it — check build timestamps against the test before trusting a failure. | Probe against the built jar (`Cow` stand-in, real `Entity` position math) printing the box sinking 0.56 into the floor on `setCrawling(true)` and re-centering at +0.56 after `teleportTo`. `CrawlSmokeTest` (two bots): crawl start/stop `PlayerStatePacket`s broadcast to a watcher; a refused toggle broadcasts nothing. **Play test confirmed the final fix** — crawl out of a hole, stand up, no `moved wrongly` |
| B-14 | **No projectile was visible in multiplayer** — a thrown rock damaged, dropped and impacted normally server-side, but nothing rendered on any client | `EntityTracker.trackEntity` is an if/else chain over entity types and simply has no `Projectile` branch, so no `EntityTrackerEntry` is ever created and no spawn packet is ever sent. It is the only call site that creates one (`WorldManager.onEntityAdded:28`), so the entity exists only server-side. Reported for rocks; it was every one of the 15 `Projectile` subclasses. `Bobber` is the odd one out and did render — it extends `Entity` directly, not `Projectile`, and has its own branch. A second defect sat behind it: `EntityTrackerEntry.getSpawnPacket` *did* have a `Projectile` case, but it sent `EntityType.SNOWBALL` for all of them and `ClientHandler.handleEntitySpawn` built a literal `Snowball`, so fixing the tracking alone would have rendered a rock as a snowball (`ProjectileRenderer` draws `itemAssigned`). Fixed with a shared `ProjectileType` registry carrying the subtype in the spawn packet's `metadata` short; `SNOWBALL` renamed to `PROJECTILE`. `Projectile.tick` now resolves impacts only where the world is authoritative — the client used to be free to run `onImpact`, which for a rock spawns a `DroppedItem` and for a snowball/sandball/fireball sets a block, all of which would have become live ghosts the moment the entity started existing client-side. `Hook` opts back in via `impactsOnClient`; it grapples the local player and already carried its own `isOnline` guards. `PROTOCOL_VERSION` bumped to 20260811 | **Play test confirmed the reported symptom is gone** — a thrown rock is visible in multiplayer and renders as a rock. A rock landing on the ground still drops a rock, and only one — the ghost-effect guard holds, and the drop is the server's. Accepted as tentative: only rocks were thrown, so the other 14 subtypes and the `Hook` client-impact carve-out are **untested in play**. `ProjectileSmokeTest` (new) exists but was **never run** — a bot `/give`s itself a rock, holds it, right-clicks, and expects `EntitySpawnPacket type=PROJECTILE metadata=ROCK` with a non-zero launch velocity; it would go red on the old build, where no spawn packet arrives at all. Nothing here has been measured on the wire |

| B-15 | **Hostile mobs were present server-side but invisible on the client** — a zombie's hurt sound with nothing to see, then damage from nowhere; a creeper detonating out of thin air at night. Some mobs did sync | **The client resolves mob despawn locally.** `World.tick` calls `entity.despawnTick()` unconditionally (`game/world/World.java:1163` — shared code, correct on the server), and `Mob.despawn` had no `world.isOnline` guard, so a client-side copy of a mob `setDead()`s itself: **instantly** when it is more than 128 blocks from the player (`distTotal > 16384`), or after `age > 600` at a 1/800 chance per tick when it is more than 32 blocks away. Once it is dead the client drops it from `world.entities` — never rendered, never ticked — but `NetworkWorld.entitiesById` still holds it, and **the server never re-sends a spawn packet** because the player is still in that entry's `trackedPlayers`. The ghost stays permanently invisible while the server's mob walks over and kills you, and `Packet31RelEntityMove` keeps dragging the dead copy along, which is why `EntityStatePacket` status 2 still plays the hurt sound (`Mob.updateState:1173`, resolved through `getEntityByID` → `entityInMemory`) at the real mob's position. `Mob.damageEntity` already opens with exactly this `world.isOnline` guard — despawn was the one client-side kill path that missed it. Hostiles are hit hardest because they spawn continuously in the 24–181 block band and walk in; animals are mostly already in place and close when their chunk arrives. Fixed by returning early from `Mob.despawn` when `world.isOnline` | Measured against the spawner's own geometry: `MobSpawner.performSpawning` draws from ±8 chunks around each player, so of the 73984 eligible spawn columns **30.5% are beyond 128 blocks and are killed client-side on the first tick**, 65.2% fall in the 32–128 band and die within a minute or two of lingering, and only **4.3%** are close enough to keep resetting `age` — which is the "some mobs do sync" the report mentions. The client/server copies of `World.tick` and `Mob.despawn` were diffed byte-for-byte first, so the server tree's copy is the client's. `checkParity` clean, `build` clean. **Play test pending** — see below |

| B-16 | **Crawling into a 1-block space took repeated suffocation damage in multiplayer** | Same anchor-mismatch family as B-12. `Mob.tick`'s suffocation check is `floor(posY + getEyeHeight())` → `world.isBlockFull`, and `EntityPlayerMP.getEyeHeight()` is pinned at 1.62 while the crawl bounding box drops to 0.9. With feet at y=10 the suffocation sample lands at 11.62 → block y=11, the crawl-space ceiling itself, so every tick under a block dealt `inWall` damage. Singleplayer is unaffected: `Player.getEyeHeight()` is 0.12 and `Player.setCrawling` raises `posY` itself when it moves `yOffset`, both keeping the sample inside the gap. Fixed by overriding `isInsideBlock()` in `EntityPlayerMP` to sample `boundingBox.maxY` — inside the crawl box while crawling, still at the head while standing. Server-only change; no `checkParity` change. **That sample was wrong on the boundary — see B-16b** | **Play test confirmed** — crawled into a 1-block tunnel and took no damage |

| B-18 | **A boat hit in multiplayer never wobbled.** The renderer's shake is driven entirely by three plain `Boat` fields — `hitTime`, `damageTaken`, `direction` (`BoatRenderer:23-30`) — and the only code that ever writes them is `Boat.damageEntity`, which is fenced behind `!world.isOnline`. Nothing put them on the wire: `Boat.init()` was empty, so the entity's `DataSyncer` carried no objects and `EntityTrackerEntry`'s metadata branch had nothing to send; the tracker's `beenAttacked` path (`EntityTrackerEntry:201`) only sends an `EntityVelocityPacket`. So a multiplayer client's copy sat at `hitTime = 0` forever and the renderer's `shake > 0` guard was never once true. `Boat.hurtAnim()` exists but is unreachable for boats — its only caller is `ClientHandler.handleAnimation` on `animate == 2`, which nothing sends for a vehicle and which would `ClassCastException` on the unconditional `(Player)entity` cast at the top of the method anyway. Fixed the vanilla way: the three fields become `dataSync` objects 17/18/19 with accessors, so the existing tracker metadata path syncs them for every viewer. `Raft.init()` was an empty override that did not call `super.init()` — left alone it would have registered nothing and NPE'd in `getObjectInt` on the first tick, so the pointless override is gone. **`Minecart` had the identical defect** — same three plain fields, same `!world.isOnline` fence, same empty `init()` — and got the identical fix in the same session at the user's direction; its three subclasses inherit `init()`, so nothing else needed touching | **Play test confirmed for boats** — a punched boat rocks in multiplayer. That result covers other viewers too without a second client: `MultiplayerMode.attackEntity` does run `attackEntityWithItem` locally, but `Boat.damageEntity` returns immediately when `world.isOnline`, so the wobble the attacker saw could only have arrived as tracker metadata — the same broadcast every other tracking player receives. Minecarts are the same code path but **unplayed**. Note the tracker syncs both vehicles every 5 ticks (`EntityTracker:52-55`) against a 10-tick wobble, so the animation can start up to a quarter second late and run short; that is vanilla's cadence, but if it reads as sluggish it is this |
| B-19 | **Boats left no water spray in multiplayer.** The wake emitter sat inside the `else` half of `Boat.onTick`'s `if (world.isOnline)` split — the branch that only the authoritative world runs. A multiplayer client takes the other branch, which interpolates position and returns, so it never reached the emitter; the server ran it and handed the particles to `WorldManager.spawnParticle`, which is an empty stub. Every boat wake in multiplayer was being drawn on the machine with no screen. Fixed by hoisting the emitter out of both branches into `spawnWake`, called unconditionally at the end of `onTick` and driven by the tick's actual position delta (`posX - prevX`) rather than `motionX` — the delta is what both sides have, and on the server it equals the post-clip motion, so singleplayer is unchanged. It cannot be gated on `isOnline`: singleplayer is the `!isOnline` side and would lose its spray | **Play test confirmed** — a boat under way in multiplayer throws spray again. Negative side of it is structural: reverted, the multiplayer branch has no path to `spawnParticle` at all |
| B-16b | **Suffocation damage whenever the player's head merely touched a block above, standing, in multiplayer** — a regression introduced by B-16's fix | Sampling `floor(boundingBox.maxY)` reads the block layer *above* the box whenever `maxY` is exactly an integer, and moving up into a ceiling makes it exactly an integer every time: `BoundingBox.calculateYOffset` clips an upward move to `dist = block.minY - aabb.maxY`, parking the box top precisely on the block face. So jumping in a 2-high space, or any push up into a ceiling, sampled the ceiling itself and dealt `inWall` every tick it stayed in contact. The client disagreed and showed no overlay — its own `isInsideBlock` samples `posY + 0.12` against a `yOffset` that is 1.62 standing / 0.56 crawling, always strictly inside the box — which is why this read as multiplayer-only like its parent. Fixed by keeping the eye-line probe but capping it under the box top: `min(posY + getEyeHeight(), boundingBox.maxY - 0.1)`. Standing that is 1.62 against a 1.7 cap, so vanilla standing behaviour is untouched; crawling it is 0.8, inside the 0.9 box; and it is never on the boundary | **Play test confirmed** — no damage under a ceiling, and crawling still takes none (B-16 holds) |
| B-20 | **Number-key quickstack desynced in multiplayer** — hovering a slot and pressing 1–9 swapped the item with that hotbar slot on the client only | `ContainerScreen.update`'s 1–9 loop mutated `player.inventory` and the slot directly and sent nothing. Singleplayer is the authoritative world so it looked right; in multiplayer the server never learned the swap and reverted it on the next resync. There was no protocol for it — `windowClick` only understands mouse buttons 0/1. Fixed by encoding the swap as a `Packet102WindowClick` with `isActive=true` and the hotbar index in `mouseClick` **offset by 2**, which cannot collide with a real click (nothing else in either tree sends `mouseClick >= 2`); `NetServerHandler.handleWindowClick` decodes it ahead of the normal path and answers with a full `updateCraftingInventory` refresh whether or not the swap took, so a rejected swap resyncs the client instead of stranding it. The swap itself is `Container.swapWithHotbarSlot` (parity file), and both worlds now route through it via `GameMode.containerSwapWithHotbar` — base impl does the local swap, `MultiplayerMode` overrides to send the packet, the same shape `windowClick` already uses. `CreativeScreen` stays client-local and is branched out *before* the gamemode call: its container exists only on the client, so its slot numbers are meaningless to the server's `PlayerContainer` on the same window 0 — the B-06 failure mode exactly. **The first cut of this fix opened an item-duplication hole; see the verification column** | `QuickStackSmokeTest` (new), **run green against a live server** — 21 checks: swap in, swap out, out-of-range hotbar index rejected, and a phase 4 that stages a real craft and takes the result. Note the test as first written could never have passed: it put the hotbar index in `mouseClick` **without** the +2 the client sends, so it fell through to the ordinary click path; caught in review, not by running it. **Play test confirmed the reported symptom is gone.** What the play test did *not* cover is the dupe: review caught that `swapWithHotbarSlot` skipped `canTakeStack` and never called `onPickup`, and its `validItem` guard was itself skipped whenever the hotbar slot was **empty** — so hovering a craft result with an empty hotbar slot handed over the item with the ingredients uneaten, and the anvil result without charging levels. Now gated on `canTakeStack` with `onPickup` fired after the swap, matching `windowClick`'s order, plus a `stackSize > slot.stackLimit()` check that stops 64 items entering a limit-1 armour or beacon slot. **Measured red/green:** phase 4 puts a log in the player container's 2×2 grid and number-keys the 4 planks out; with `canTakeStack`/`onPickup` reverted the server reports `grid slot 1 still holds 1x id 17` — a free craft — and that one check goes red while all 20 others stay green. Singleplayer inherits the same fix; it had the identical dupe, it just was not a desync so nobody looked. One deliberate behaviour change: the old code called `validItem(null)`, and armour/accessory/arrow slots return false for null, so a number key could never *empty* those slots — it can now. The bot needs `swapbot` in `run/server/ops.txt` |
| B-21 | **The server accepted a short or corrupt chunk payload silently.** Found while merging the trees, not by play | `UpdateChunkPacket.readPacketData` discarded the return of `inflater.inflate()`, so a payload that decompressed to fewer bytes than the section masks promised left the tail of `chunkData` as zeros and was processed as if it were real air. The client's copy of the same file had computed the expected length and thrown on a mismatch since B-04 — the two copies sat at `packet/world/` and `packet/`, and the old `checkParity` only compared files at the **same** relative path, so it never once compared them. Fixed by taking the client's length check. **The blind spot is the real finding**: any divergence that also moved or renamed a file was invisible to that task for its whole lifetime. It also hid a three-line drift across `lang/desc.lang`, `lang/gui.lang` and `lang/item.lang`, which were never checked at all | Both copies are now one file in `core/`, so the class of bug is gone rather than fixed — `:core` compiles once and both builds link the same bytecode. Full smoke suite re-run green against a live server afterwards (7/8; `CrawlSmokeTest` fails identically at HEAD, see B-22) |

### Open — found during the `:core` merge

| # | Bug | Where | Status |
|---|---|---|---|
| B-30 | **`TwoClientTest` and `QuickStackSmokeTest` fail at HEAD for an environment reason, not a code one.** Alice and swapbot are both kicked "for floating too long" a few seconds after login. Neither bot sinks, and both log in at a *saved* position out of `run/server/world/players/*.dat` rather than the spawn point — Alice at y=146.62, swapbot at 145.62, both just above the ocean surface, where the B-05 support test correctly finds nothing under them. Bob, saved two blocks lower at 144.62, is in the water and survives, which is why only some phases fail. The saved Y drifts upward a little each run as the bot is nudged by buoyancy and re-saved, so this got worse over time and will come back after any fix to the `.dat` files. **Measured A/B**: both tests fail identically with the B-23 fix stashed, so this is not a regression from it | `smoketest/TwoClientTest.java`, `smoketest/QuickStackSmokeTest.java`, saved player data in `run/server/world/players/` | **Open as a test defect**, same shape as B-22. The fix is in the harness, not the game: `Bot.sink()` these two the way `boltbot` and the newer tests do, or teleport them to solid ground first. Found while verifying B-23. **Still failing identically at the end of B-26** (`swapbot was kicked for floating too long`, Alice on `Flying is not enabled`), which is how that session confirmed the two were not its own doing. `DeathSmokeTest` now shows the tidier fix: `followTeleport` to land, then `/setblock` a floor under the feet — an armed bot standing on a real block is never kicked |
| B-22 | ~~A crawl state change reaches other players twice.~~ **Not a game bug — the test asserts something the server never did.** `refusedUncrawlCheck` sends four raw `PlayerStatePacket`s by hand (true, true, false, false) and then asserts the watcher sees exactly one start and one stop, i.e. that the *server* collapses the repeats. Nothing server-side ever did: `handlePlayerState` applies and broadcasts whatever arrives, once each. The dedupe the test is describing is client-side — `NetworkPlayer.sendStatePacket` only sends when the flag actually changed — and a bot writing packets directly bypasses it. `[true, true, false, false]` is the correct answer to what was sent, which is also why a build from `e93a8a7` produced byte-identical output: the premise was wrong in both trees | `server/src/smoketest/java/net/minecraft/smoketest/CrawlSmokeTest.java:85-99`, `client/network/player/NetworkPlayer.java:126-137` | **Open as a test defect**, not a protocol one. Fix is in the test: send the pair once and expect two packets, or keep the four and expect four. What the phase actually wanted to cover — a *refused* uncrawl broadcasting nothing — has no bot-reachable ground truth (its own comment says so) and still needs a play test. Found during the B-23 to B-29 read-only sweep |

---

## Authentication and distribution

`online-mode=true` works again. The old path called `session.minecraft.net`
(`joinserver.jsp` / `checkserver.jsp`), which Mojang decommissioned years ago, so every
online-mode login failed and the default was pinned to false. Both halves now speak the
modern endpoints: the client POSTs `sessionserver.mojang.com/session/minecraft/join`
(`ClientHandler.joinServer`) and the server confirms with `/hasJoined`
(`NetLoginHandler.hasJoined`). The access token never reaches the server — Mojang is the
middleman, which is the entire point of the three-way handshake.

Three things worth knowing before touching this:

- **No encryption handshake is involved, deliberately.** Mojang treats `serverId` as an
  opaque string and only checks that both sides sent the same one, so the existing random
  `Long.toHexString(rand.nextLong())` is enough. Vanilla 1.3+ derives that hash from a shared
  secret and the server's public key; skipping that means a hostile server could relay a
  join and impersonate the player elsewhere. Acceptable while we are the only server, and
  the reason not to advertise this as equivalent to vanilla's.
- **The token comes from the launcher, not from us.** Prism/MultiMC hand legacy instances
  their session as `token:<accessToken>:<uuid>`, which `Session` parses. There is no
  password login to fall back on — Yggdrasil was shut down in 2023 — so a client with no
  token cannot join an online-mode server at all. `Session.canAuthenticate()` gates that and
  produces a readable message instead of a mystery failure.
- **`online-mode` defaults to `true`**, so a server unpacked into a fresh directory is
  authenticated rather than wide open, and the first run writes the key into
  `server.properties` (nothing used to create that file at all). Dev needs the opposite:
  `Bot` throws the moment a handshake returns a real serverId, so an online-mode server makes
  the whole suite unrunnable. `:server:runServer` writes `online-mode=false` into
  `run/server/server.properties` when the key is absent, and never overrides an explicit one —
  if you set it to true by hand to play-test auth, the suite will fail until you set it back.

Identity is a bare username everywhere it matters (`ops.txt`, `white-list.txt`,
`world/players/<Name>.dat`), which is what makes offline mode dangerous on a reachable port
rather than merely unauthenticated: connecting as an opped name grants op, and connecting as
a name with a save file inherits that inventory. The whitelist is spoofable the same way and
is not a substitute.

**Distribution.** `:client:prismExport` builds a Prism/MultiMC instance export, the same
shape Infdev+ itself shipped as `Latest.zip`. The template lives in `dist/prism/`, extracted
from that original. The vanilla a1.0.4 jar is not in it and must not be added:
`patches/net.minecraft.json` points Prism at `launcher.mojang.com`, so the base game comes
from Mojang and the player supplies their own account. That is what keeps this a mod rather
than a redistribution of the game. `:server:serverJar` is the exception — it bundles the
whole game with no launcher in the loop, so it is for our own hosting, not for handing out.

## Verification

**Twelve of the suites below drove the server with commands and cannot run until `Bot` grows
its own server-side helpers** — see the Brigadier section at the top of this file:
`BlockBreakSmokeTest`, `DeathSmokeTest`, `EntityTrackingSmokeTest`, `HeightSyncSmokeTest`,
`KnowledgeBookSmokeTest`, `ProjectileSmokeTest`, `QuickStackSmokeTest`, `SleepSmokeTest`,
`SoundSmokeTest`, `SwingSmokeTest`, `WeatherSmokeTest`, `WitherSmokeTest`. Left listed rather
than deleted: the commands they reach for are the shopping list for that follow-up.

```bash
./gradlew build                             # must be clean before any session ends
./gradlew :server:runServer                 # needed by every test below except the first
./gradlew :server:smokeTest                 # two clients: login, chunks, chat relay
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.CommandFrameworkSmokeTest
                                            # the brigadier framework; no server needed
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.CommandWireSmokeTest
                                            # needs a running server; no op needed
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.LoginSmokeTest \
                            -PsmokeArgs="Alice 12"
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.HeightSyncSmokeTest
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.HeightSyncSmokeTest \
                            -PsmokeArgs="live"   # needs a running server + HeightBot in run/server/ops.txt
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.KnowledgeBookSmokeTest
                                             # needs a running server + bookbot in run/server/ops.txt
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.SprintSmokeTest
                                             # needs a running server; no op needed
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.AuthSmokeTest
                                             # needs online-mode=true + network; not part of a default run
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.CrawlSmokeTest
                                             # needs a running server; no op needed
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.ProjectileSmokeTest
                                             # needs a running server + rockbot in run/server/ops.txt
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.EntityTrackingSmokeTest
                                             # needs a running server + boltbot in run/server/ops.txt
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.DeathSmokeTest
                                             # needs a running server + gravebot in run/server/ops.txt
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.BlockBreakSmokeTest
                                             # needs a running server + breakbot in run/server/ops.txt
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.SwingSmokeTest
                                             # needs a running server + punchbot in run/server/ops.txt
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.BiomeSmokeTest
                                              # needs a running server; no op needed
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.FootstepSmokeTest
                                              # direct mechanics test; no server needed
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.ContainerOpenSmokeTest
                                              # needs a running server + guibot in run/server/ops.txt
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.SleepSmokeTest
                                              # needs a running server with sleepbot AND dreambot
                                              # in run/server/ops.txt, and no other player logged in
./gradlew :server:setPlayerDimension -PsmokePlayer=Alice -PsmokeDimension=1   # server stopped
```

The smoke tests speak the real wire format using the server's own packet classes, so they cannot
drift from the game. **Each bot runs in its own JVM deliberately** — see DEVELOPING.md.

**Start a server when you need one, and shut it down when you are done.** Running the live tests is
your job, not something to ask permission for — but the server must not outlive the turn that
started it. Kill it and confirm port 25565 is free before you report done. One left running holds
the port against the user's own launch, keeps writing to `run/server/world`, and survives the
session. If the user already has one up, use theirs and leave it alone.

### Probes and smoke tests

Two tiers. Most sessions only need the first.

**A probe** answers one question during diagnosis and is then thrown away. Drop a file with a
`main` into `server/src/smoketest/java/net/minecraft/smoketest/` and run it — `-PsmokeClass` takes
any main class, so nothing has to be registered anywhere:

```java
public class Probe {
    public static void main(String[] args) throws Exception {
        Bot bot = Bot.join(args, "probe");
        System.out.println(bot.await(EntityHealthPacket.class, 20000).food);
        bot.close();
    }
}
```

```bash
./gradlew :server:smokeTest -PsmokeClass=net.minecraft.smoketest.Probe
```

A probe that needs **no server at all** is the fastest signal there is: build a `World`, a `Chunk`
or an entity and read the answer off real game objects. That is what settled B-07 (`blocks[0] =
null`), B-12 (the box sinking 0.56 into the floor) and B-15 (the spawner's own geometry). Reach for
that before writing a bot.

**A smoke test** is a probe that earned a place in the suite: it demonstrated the bug on the wire
and you *watched it go red*. Promote it, delete the probe, add it to the list above. Never promote
a probe you have not seen fail — a check that has only ever been green proves nothing about the
game, only about itself.

`Bot` is the headless client — `join`, `read`, `await`, `awaitLast`, `awaitAll`, `drain`, `chat`,
`sendAction`, `sendState`, `settle`, `sink`, `followTeleport`, `positionOf`, `close`. `positionOf`
answers where this bot's client would *draw another entity*, accumulated from the same three packets
`ClientHandler` drives `Entity.serverX/Y/Z` from — it is how B-42 measured what an onlooker sees.
`Checks` is the tally —
`check`, `note`, `abort`, `done`. Three things they handle that cost real sessions to learn:

- **Every wait pumps every live bot.** The server ignores a player until its login teleport is
  echoed back exactly (`NetServerHandler.hasMoved`), so a bot that stops sending position packets
  is silently dropped and every assertion about it becomes meaningless — the `MovementSmokeTest`
  failure recorded in B-05. A watcher waiting on a mover keeps that mover alive with no
  interleaving by hand.
- **One bot per JVM if it decodes chunks.** `UpdateChunkPacket` and `UpdateSectionPacket` share a
  static scratch buffer. Bots trading small packets share a JVM safely; anything reading chunk data
  needs its own process (`TwoClientTest` spawns them).
- **A teleport switches the server off, and `followTeleport` switches it back on.** Every
  server-initiated teleport clears `hasMoved`, and `handlePlayerMovement` does *all* of its work
  inside one `if (hasMoved)` — movement, chunk streaming, the health sync. A bot that `/teleport`s
  and keeps sending its old position is not just ignored for movement: it stops receiving block
  updates for the chunk it is standing in, and its health never changes on the wire no matter what
  happens to it. `read()` now adopts every teleport as it arrives, the way a real client echoes one
  straight back, and `followTeleport` is how a test *waits* for a particular one. The catch is that
  an armed bot is also *eligible to be kicked*, so it needs to be standing on something — see below.

Three more things that cost this session real time, in the hope they cost the next one less:

- **A bot holding a position in mid-air is kicked as flying after 80 packets** — four seconds — and
  every assertion after that goes silently green because `read()` returns nothing. A *falling* bot
  is safe (`posY - d3 < -0.03125` resets the counter), and so is one in water, which is why
  `sink()` and `settle()` work at spawn. Standing still anywhere else needs a block under the feet:
  `DeathSmokeTest` `/setblock`s its own floor and teleports onto it at an integer Y.
- **Around the spawn, single block changes are invisible.** `ServerChunkDistance` sends one changed
  block as `UpdateBlockPacket`, 2–63 as `UpdateSectionPacket`, and 64+ as a whole-chunk resend — and
  the chunks near spawn churn hard enough to sit in that last case constantly (measured: 530
  `UpdateChunkPacket`s in six seconds). A test asserting on a specific block change needs a quiet
  chunk; the land at 875,150,768 is one, and a `/setblock` that comes back is the proof.
- **A bot that dies where the test builds leaves a block there.** `playerGrave` is block 282, it has
  no collision box, and nothing ever clears it — so the next run walks through it and cannot see it,
  but `BedItem` refuses to place into anything that is not air. B-42 lost a run to exactly that:
  two bots suffocated in bed, their graves landed in the *pillow* cells, and every later run failed
  as `no bed at 877,150,768 ... found block id 0`, naming the half that was fine. When a place stops
  working at a site that used to work, probe the block the item needs as well as the one you clicked.
- **`/setblock <x> <y> <z> 0` throws.** `BlockList.blocks[0]` is null (the B-07 finding), so air
  fails `checkBlockId` and the command errors — silently, from a bot's point of view. Reset a block
  by setting a *different real block*, not by clearing it. And a `setblock` that changes nothing
  sends nothing, which makes a re-run of the same test look like a missing packet.
- **The first `/teleport` into unexplored terrain stops the server dead for half a minute.** That
  region is generated synchronously on the main thread, and while it runs the server answers
  nothing at all — commands queue up unexecuted and every wait in the test expires in order, so
  the failure surfaces as whichever check happened to be waiting when the budget ran out rather
  than as a stall. Measured at 32 seconds for the land at 875,150,768 on a fresh world, against a
  10-second run once those chunks existed. Any teleport away from spawn wants a timeout in tens of
  seconds, not the 5–6 that is plenty everywhere else, and the first run of a new test against a
  new world is not evidence of anything until it has been run twice.
- **`settle()` does not arm `hasMoved`, and nothing warns you.** Anything measured through
  `onUpdateEntity` — the health sync, chunk streaming, and every per-tick effect on the player
  entity including `swingArms` — runs only inside `handlePlayerMovement`'s `if (hasMoved)`. A bot
  that just settles at the spawn used not to get there: the fall draws a corrective teleport, and
  `Bot.read` adopted only the **first** teleport it saw, so from then on the echo could never match
  (fixed in B-42 — it adopts every one now, which is what a real client does). B-32's
  first draft measured 80 consecutive movement packets with `hasMoved=false` and reported a bug
  that did not exist, with the fix already in. `followTeleport` after a real `/teleport` onto
  placed stone is what arms it — which means an op'd bot even when the test looks like it needs
  nothing. `CrawlSmokeTest` and `SprintSmokeTest` get away with `settle()` alone only because
  `handlePlayerState` and `handleEntityAction` are not gated on `hasMoved`.

Bots that `/give` themselves must be in `run/server/ops.txt`, with the server restarted after.

### Bot it or play it

**Write the bot when the question is trivially bottable. Ask for a play test when it is not.**

Trivially bottable — just do it, do not ask:

- a packet's contents, presence, absence or count on the wire
- the server's verdict on something a client sends: accepted, rejected, resynced, ignored
- slot arithmetic, ids, registries, save files, wire framing
- anything needing no server at all — a `World`, a `Chunk`, an entity, real game objects in a `main`

Needs a human — say so and stop:

- how something *feels*: knockback, animation timing, camera, sound, sluggishness
- anything needing real terrain under the player. The spawn is open ocean (B-01), so a bot cannot
  stand on land, dig a ceiling or crawl into a 1-block gap without op and a pile of setup
- the whole experience end to end — walk around for five minutes, report whether you get kicked

Ask in two or three lines: what to do, and what counts as pass or fail. Their answer is the
verification and it goes in the board's Verified column.

The cautionary tale is still worth knowing, because half of it is still live. `MovementSmokeTest`
(written for B-00a, deleted in B-05) reported "walked 15s clean" against a server that kicked every
real player within four seconds, for two reasons:

- **A bot can be moving and still be ignored.** `NetServerHandler.hasMoved` is cleared by every
  server-initiated teleport and re-armed only by an exact echo. **`Bot` handles this now** — it is
  why the harness exists.
- **It stood in the wrong place.** Open ocean, where `isInWater()` counts as support, so the check
  could never fire. **Nothing handles this for you.** A bot on the spawn point is in the sea.

The first hole is closed. The second is the one that decides which side of the line a bug falls on.

### Still needs a human

Baseline items B2 and B4–B16 (server list ping, block/chest/furnace sync, crafting, mobs, item
pickup, PvP, death and respawn, sleeping, rejoin persistence, console commands). See the plan's
Verification section. These have never been run.

B-14 left two things unplayed: a projectile other than a rock (snowball, musket, fireball, egg,
potion — the subtype id has only ever been exercised for `ROCK`), and the grappling hook (the one
entity still allowed to resolve impacts client-side).

B-40 is worth a sweep beyond beds now that the server can talk at all: hanging a painting where it
does not fit, an occupied teleporter, the magic mirror, an eye of ender with no stronghold in range,
and a blood or blue moon rising — all of those wrote a message that no multiplayer client has ever
received. Only the bed path has been exercised.

**B-39 is the one to play, and none of it has been seen.** Sleeping is now a round trip: the click
sends a packet and nothing happens until the server answers, so the first thing to check is that
getting into a bed still feels immediate rather than laggy. Then that the camera lands *on* the bed
facing along it, that the screen opens and closes with the sleep rather than beside it, that cancel
leaves the bed cleanly, and that a refused bed says why and leaves you standing normally. Singleplayer
needs a pass too — it takes a different branch through the same code and should be exactly as before.

B-38 has been played once, which is how defect (6) was found, and B-42 came out of a second pass.
**The other player's model does now lie on the bed** — played and confirmed, which is what B-42 was
for, and the same pass turned up B-42d's nametag. What is still unwatched: the fade and
`SleepScreen`, and two players asleep at once turning night into morning. The cancel path is the one to lean on — get into a bed and out again several times in a row,
then sleep for real; before the fix the second attempt onward was refused client-side with no screen
and the bed reported itself occupied.

B-38 made beds work on the wire and on the clock, and most of it has not been looked at. Worth a
play test with two players, at night, in a lit and roofed room — `goToSleep` refuses ("cursed") if
any of the eight blocks over and beside the pillow is open to the sky or dark: that the *other*
player's model actually lies down (the `SleepPacket` arrives and carries the right bed now, but no
bot can see a model), that the screen fades and `SleepScreen` behaves, that the cancel button
leaves the bed without passing the night, and that with two people asleep it becomes morning
rather than the same night over again. Also worth trying once with only one of the two in bed —
nothing should happen, and nobody should be kicked.

B-25 put rain on the wire but nobody has watched it fall. Worth a look, in multiplayer, all of it
off one `/weather rain`: that the rain actually draws (`WeatherRenderer`), that the sky darkens and
the fog closes in over the ~5 seconds the strength ramps rather than snapping (`SkyRenderer`,
`FogRenderer`), and that a client who joins *during* the storm arrives already wet instead of
ramping up from dry — that last one is the login packet, and it is the case the wire test covers
but no eye has. Thunder is on the wire too and is even less exercised: `/weather thunder` should
darken further, and the flash needs B-23's bolt to be visible, not this packet.

---
> Source: [Trivaxy/Minecraft-Infinite-MP](https://github.com/Trivaxy/Minecraft-Infinite-MP) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-08-27 -->
