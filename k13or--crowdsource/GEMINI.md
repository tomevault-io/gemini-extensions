## crowdsource

> Проект — народный русский перевод Guild Wars 2. Подробный порядок работы для

# Как здесь работать

Проект — народный русский перевод Guild Wars 2. Подробный порядок работы для
участников лежит в [README.md](README.md); здесь — то, что нужно держать в
голове с первой минуты.

## 1. Иерархия правок: батчи → bin → граф

**`dictionary.bin` руками не правим никогда.** Он собирается из батчей.

```
правим батчи (crowdsource/<категория>/<имя>.csv)
  -> python tools/dict_tool.py frombatches --batches-win --apply
  -> python tools/index.py build          # граф И реестр названий
```

Батч решает про строку три вещи, и все три обязан решать он, а не bin:

| что | откуда берётся | если не оттуда |
|---|---|---|
| ТЕКСТ | колонка `translate` | — |
| МЕСТО | [ROUTES.txt](ROUTES.txt), `tools/routes.py` | категорию не переназначить: правку некуда записать и нечем проверить в PR |
| СУЩЕСТВОВАНИЕ | строка есть в батче | запись из словаря не удалить никогда |

Место — это группа отключения, и до 2026-09-03 оно хранилось ТОЛЬКО в bin:
`frombatches` смотрела, где строка лежит сейчас, и клала правку туда же. Теперь
категорию даёт `ROUTES.txt` (хеш → категория, `tools/routes.py build|check|set`,
ручные решения — в `ROUTES_FIX.csv`), а `--prune` сносит из bin то, чего нет ни в
одном батче.

Пустая ячейка перевода — «ещё не переведено», а не «удалить»: такая строка
сохраняет то, что уже есть в bin. Удаляет только ОТСУТСТВИЕ строки в батчах.

Строку, которую держит слой `pn_*`, сборка кладёт в её категорию ПО-АНГЛИЙСКИ:
русскую форму подставляет слой, и только так работает выключатель имён. Это
правило сборки, а не разовый скрипт — перевод названия, вернувшийся в обычный
батч, гасится автоматически.

**Граф в сборке bin не участвует.** `dict_tool.py` не знает про `sync/index.db`
вовсе: bin собирается из батчей и предыдущего bin, а граф выводится ИЗ bin.
Обратной связи нет и быть не может — иначе кольцо. Реестр названий (`names.py`)
влияет на bin только через посредника: `names.py emit --write` выписывает
найденное в батчи `pn/*.csv`, и дальше они вливаются штатным путём.

`index.py build` с 2026-08-31 строит не только граф, но и реестр названий
(таблицы `name` и `route`) — отдельно `names.py scan` звать не нужно.

Причина в [README §«Порядок работы»](README.md#порядок-работы-батчи--источник-истины):
батчи в git, у них есть история и откат; bin весит больше сотни мегабайт, в git
не лежит и правится необратимо. Разъезд между ними не видит ни один линтер —
обе стороны сами по себе чисты.

Латиница имён внутри фраз возвращается `tools/namelatin.py` — но только там, где
в переводе стоит ДОСЛОВНАЯ форма из слоя. Косвенные падежи он не трогает: слой не
склоняет, «в Львиной Арке» стало бы «в Львиная Арка». Это работа очереди «имена в
косвенных падежах», её делают руками.

У каждой пишущей по bin команды есть парная по батчам (`canon` → `canonbatches`,
`typo` → `tools/typofix.py`, `combat.py apply` → `combat.py batches`,
`gw2s_import.py apply` → `gw2s_import.py batches`). Звать надо парную. Если
правка всё же легла в bin первой — довести её до батчей в тот же заход, иначе
следующая сборка её сотрёт.

Только в bin живут русские имена словарей (`dict_tool.py dictnames`). Слой имён
`pn_*` **исключением не является**: он заведён в батчи `crowdsource/pn/*.csv`, и
`frombatches` их вливает (`batch_files(with_pn=True)`). `pnadd`/`pnset` пишут мимо
этого пути — правкой слоя считать их нельзя, иначе запись останется вне git.

## 2. Обязательные гейты

| когда | что запустить | что должно получиться |
|---|---|---|
| перед сдачей батча | `python validate.py <файл>` | 0 ошибок |
| заливка переводов | `python tools/batch_tool.py fill <батч> <json>` | сам делает бэкап в `.batch_bak/` и сверяет плейсхолдеры и колонку english |
| после записи в bin | `python tools/charscan.py compare .dict_bak/<последний бэкап>` | по незатронутым классам **+0, регрессий 0** |
| после правки bin | дописать раздел в [DEFECTS.md](DEFECTS.md) | состояние и остаток по каждому классу |
| после сборки bin | `python tools/dict_tool.py orphans` | «вне батчей: обычных 0, слоя 0» |
| сдача батча | `batch_tool.py claim <батч> --who <ник>`, затем `python stats.py --mark-done` | ✅ в [CLAIMS.md](CLAIMS.md) |

`charscan compare` идёт минуты — это нормально, не прерывать.

## 3. Канон терминов — в этом порядке

1. [GLOSSARY.md](GLOSSARY.md);
2. **человеческий корпус** — батчи, у которых в CLAIMS.md в колонке «Кто взял»
   не написано «Автозаполнение». Машинные батчи каноном не являются;
3. вики;
4. своё решение — и сразу в GLOSSARY.md, чтобы стало прецедентом.

Для названий предметов: тип вещи берётся из `/v2/items` через
`tools/itemfacts.py` (Targe — щит, Greaves — сапоги), прецеденты людей —
через `tools/index.py proof`. Названия сетов брони — из человеческих батчей
гардероба `ui_003…ui_007`, а не из головы.

Всё, что игрок может пойти гуглить (персонажи, локации, предметы, облики,
умения, титулы, фракции), обязано лежать в слое `pn_*` — смысл слоя в
обратимости, а не в переводе.

Обратимость держится на ДВУХ свойствах названия, и оба проверяются реестром
`tools/names.py`: название заведено в `pn_*` (`names.py gap`) **и** общий словарь
не перевёл его внутри фразы (`names.py inline`). Одного первого мало: игрок гасит
слой, а в строке уже стоит кириллица. Раскладка классов по словарям — в
[NAMEMAP.md](NAMEMAP.md).

Второе свойство стережёт линтер **ошибкой**: «название из слоя пропало из
перевода» — оно есть в оригинале, а в переводе его нет, переведено или выброшено.
Класс заведён 2026-08-30, ловит 6,55% корпуса (30 544 строки, 934 файла из 996):
правило было записано давно, но не проверялось. Пока старое не расчищено, флаг
`--warn-names` опускает класс до предупреждения; переключатель — константа
`NAME_LOST_IS_ERROR` в `validate.py`.

**В слой идёт не всякое название:** строку, которая целиком является названием,
гасит своя категория, копия в `pn_*` заставила бы игрока щёлкать два выключателя.
Линтер это учитывает — считает только во фразе (три и более строчных слова вне
самого названия).

## 4. Прежде чем переводить руками

Ручной перевод — последний шаг, а не первый:

1. **подстановка из корпуса** — строка уже могла встретиться в другом батче;
   совпадения переносим как есть, это заодно держит единообразие;
2. **генерация однотипных семей по правилу** — названия предметов, статы,
   материалы и прочее собираются шаблоном из уже утверждённых кусков
   (на `items_024` так закрылось 479 строк из 500 без единого нового термина);
3. и только остаток — руками.

Порядок слов в названиях предметов задан человеческими батчами `items_013` и
`items_014`: прилагательное-сет + предмет + суффикс + стат-префикс, а материал
(«из тёмной стали») идёт хвостом после стат-префикса.

Если в колонке `english` лежит не английский, а немецкий, испанский или
французский текст (частая беда батчей `new_034` и дальше) — это не строка на
перевод. Хеш считается от испорченного english, такая запись в игре мертва:
её надо удалить, а не переводить.

## 5. Типографика

* многоточие пишем **как в оригинале**: `...` в оригинале — `...` в переводе;
* кавычки внешние «ёлочки», внутренние „лапки“;
* ё расставляем;
* где в английском `[s]` или `[pl:"…"]` — в переводе группа форм
  `[форма1|форма2|форма3]`, ровно три (1 / 2–4 / 5+), и основу к ней приклеивать
  нельзя («союзника[|а|ов]» проходит линтер и ломается в игре). Пустая форма по
  краям законна — `Ящик[|а|ов]`, `секунд[а|ы|]`, `раз[|а|]`; в середине это
  потерянное окончание. Обратно `[pl:"…"]` в перевод не пишем никогда;
* строки не на английском (французские хвосты клиента) не переводим, а удаляем —
  `tools/langfilter.py check` / `apply`.

## 6. Грабли, на которые уже наступали

* **CSV батча читаем только через `dict_tool.read_csv`.** Обычный
  `open(fp, encoding="utf-8-sig")` без `newline=""` меняет CR+LF на LF ВНУТРИ
  закавыченного поля: english перестаёт совпадать с оригиналом из игры, хеш
  считается от другой строки, и запись становится недостижимой. Так молча
  выпали 3 229 строк словаря и 818 строк живых батчей, а в bin осело 823
  мёртвых двойника.
* **`csv_to_bin.py` не разбирает аргументы.** Любой запуск без явной папки —
  включая `--help` — собирает bin из пустого места и затирает 121 МБ. Не звать
  вовсе: путь в bin один, через `frombatches`.
* **`index.py build` падает с PermissionError**, пока запущен `tools/graphserve.py`
  — он держит `sync/index.db`. Сначала остановить сервер.
* **Репозиторий `crowdsource` публичный.** Адрес сервиса выкачки строк намеренно
  не в коде: `GW2_SYNC_URL` или `sync/endpoint.txt` (в .gitignore). Не печатать,
  не коммитить, не цитировать.
* **Бэкапы bin** — `.dict_bak/`. Имя файла — время запуска, который его создал,
  содержимое — состояние ДО этого запуска.
* Категория словаря = группа отключения в игре. Названия и описания разведены
  (`item_names` / `item_descriptions` и так далее), новые словари не создаём.

## 7. Релиз

Ассет релиза один — сам `glyphCore/dictionary.bin` (архив не собирается,
инструкция по установке живёт в README). Порядок:

```
python tools/dict_tool.py orphans              # должно быть «обычных 0, слоя 0»
python tools/dict_tool.py frombatches --batches-win --prune --apply   # bin из батчей
python tools/index.py build                                   # граф
python make_release.py                    # проверка + готовая команда публикации
git push origin main
gh release create vГГГГ.ММ.ДД "<путь>/dictionary.bin" -t "Русификатор GW2 vГГГГ.ММ.ДД" -n "..."
```

`make_release.py` ничего не пишет и не публикует: он читает заголовок GCDCT2
(размер, категории, число пар), проверяет, что на месте слой `pn_*` и категория
«основной», гоняет по всему словарю линтер `validate.check_row`, считает sha256
ассета и печатает готовую команду `gh release create` с тегом-датой. Тег можно
задать аргументом (`python make_release.py v2026.08.17`), `--no-lint` пропускает
линт (он идёт минуты), `--lint-details` показывает примеры находок.

Битые строки релиз **не блокируют** — они и так уже в игре; make_release просто
показывает их число до публикации. Разбор — `tools/dict_tool.py broken`.

**`release.py` — не путать с `make_release.py`.** Это остаток прежнего порядка:
он вливает батчи через `merge_back.py` в `main_strings.csv` и `dict_*.csv` в
папке glyphCore, а этого CSV-слоя больше нет — вливание стало пустой операцией,
её заменил `frombatches`. Из полезного там осталась вторая половина: коммит в
локальный git-репозиторий `glyphCore/` (в индексе `dictionary.bin` и `config.ini`),
то есть ещё одна точка отката для bin помимо `.dict_bak/`.

## 8. Карта документов

| файл | о чём |
|---|---|
| [README.md](README.md) | полный порядок работы, как переводить, как сдавать |
| [GLOSSARY.md](GLOSSARY.md) | канон терминов |
| [CLAIMS.md](CLAIMS.md) | доска захвата батчей, кто что взял, что вычитано |
| [DEFECTS.md](DEFECTS.md) | реестр классов дефектов bin: состояние, чем чинили, остаток |
| [TERMS_OPEN.md](TERMS_OPEN.md) | споры о терминах, которые ждут решения владельца: числа по корпусу и слою, примеры |
| [NAMEMAP.md](NAMEMAP.md) | раскладка «класс названия → словарь `pn_*`»: чем управляет выключатель имён |
| [NAMECASES.md](NAMECASES.md) | падежные формы имён, собранные из корпуса: материал для очереди «имена в косвенных падежах» |
| [ROUTES.txt](ROUTES.txt) | какой строке в какой категории bin лежать; правки-исключения — в `ROUTES_FIX.csv` |
| `orphans/` | записи, попавшие в словарь мимо батчей (вливания чужих словарей, ранние переразметки). Заведены батчами `dict_tool.py orphans --export`, чтобы ими вообще можно было управлять; на перевод не раздаются |

---
> Source: [K13or/crowdsource](https://github.com/K13or/crowdsource) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-09-23 -->
