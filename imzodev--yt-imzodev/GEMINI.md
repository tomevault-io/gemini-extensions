## yt-imzodev

> When you need to install dependencies, run `bun install`


This project uses Bun as the JavaScript runtime. You can find more information about Bun at https://bun.sh

To install dependencies, run `bun install`
To run the project, run `bun run dev` (FIRST CHECK IF THERE IS ALREADY A PROCESS RUNNING)
To build the project, run `bun run build` (ONLY WHEN THE USER ASKS)
To preview the project, run `bun run preview` (ONLY WHEN THE USER ASKS)
To add a new dependency, run `bun add <package-name>`
To remove a dependency, run `bun remove <package-name>`
To update a dependency, run `bun update <package-name>`
To check for updates, run `bun check`
To update all dependencies, run `bun update`

---
> Converted and distributed by [TomeVault](https://tomevault.io/claim/imzodev) — claim your Tome and manage your conversions.
<!-- tomevault:4.0:gemini_md:2026-04-13 -->
