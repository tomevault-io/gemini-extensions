## dukkanci

> **قاعدة إلزامية لأي جلسة عمل (بشرية أو ذكاء اصطناعي):**

# CLAUDE.md — ذاكرة مشروع دكانجي

**قاعدة إلزامية لأي جلسة عمل (بشرية أو ذكاء اصطناعي):**
1. اقرأ هذا الملف كاملاً قبل البدء بأي مهمة — خصوصاً قسم "الأخطاء المتكررة المعروفة" وقسم "سجل الإصلاحات" في الأسفل، للتأكد أن ما تحاول حلّه لم يُحلّ سابقاً بطريقة معينة يجب اتّباعها أو تجنّب تكرارها.
2. لا تُغلق أي مهمة (إصلاح أو ميزة جديدة) قبل إضافة سطر جديد في "سجل الإصلاحات" أسفل هذا الملف. هذا الملف هو الذاكرة الوحيدة الدائمة للمشروع بين الجلسات المختلفة.

---

## 1. نظرة عامة سريعة

- **المنصة:** دكانجي — سوق طلبات محلي (مطاعم وسوبرماركت) في تركيا.
- **الواجهة الأمامية:** `index.html` + ملف واحد `app.js` (~12,884 سطراً، 684 دالة) + `styles.css`.
- **الخلفية:** Vercel serverless functions تحت `api/` + قاعدة بيانات وتخزين ملفات على Supabase.
- **التطبيقات الجوالة:** عبر Capacitor (`android/`, `ios/`).
- **Service Worker:** `sw.js` — تخزين مؤقت + إشعارات Push.

## 2. مصدر الحقيقة الوحيد للبيانات — مهم جداً

- بيانات المتاجر والمنتجات الفعلية تُقرأ **حصرياً من Supabase**. مجلد `migrations/` يوثّق كل تغيير على المخطط بالترتيب الزمني (اسم الملف).
- ملفات `*-data.js` في جذر المشروع (56 ملفاً، مثل `zaitoune-data.js`) هي **كود قديم ميت (dead code)** — تأكدنا أن لا شيء في `app.js` أو `server.js` يستدعيها حالياً. **لا تُعدَّل ولا تُعتمد كمصدر بيانات أبداً.**
- عند التعامل معها نهائياً: انقلها إلى مجلد `archive/` أو احذفها، ونظّف مصفوفة `APP_SHELL` في `sw.js` من كل إشارة إليها (لا تزال تحتوي حالياً على ~30 إشارة قديمة لها).
- أي خطأ متعلق بظهور/اختفاء متجر → تحقّق أولاً من سجلّه في Supabase، وليس من هذه الملفات القديمة.

## 3. قواعد العمل بـ Git (إلزامية)

1. لا تترك أي فرع (branch) أو نسخة عمل (`git worktree`) مفتوحة لأكثر من يوم عمل واحد. كل ميزة/إصلاح: فرع قصير العمر → commit → دمج في `main` فوراً → حذف الفرع محلياً وبعيداً.
2. قبل البدء بأي عمل جديد:
   ```
   git checkout main
   git pull
   git status   # يجب أن يكون نظيفاً؛ إن لم يكن، توقف واسأل قبل المتابعة
   ```
3. عند الانتهاء من أي مهمة:
   ```
   git worktree remove <path>
   git branch -d <branch-name>
   git push origin --delete <branch-name>
   ```
4. لا تدمج فرعاً عمره أكثر من أسبوعين دون مراجعة يدوية كاملة لمحتواه — قد يعيد كوداً تجاوزه الزمن ويُلغي إصلاحات لاحقة.
5. إذا ظهر خطأ "Unable to unlink .git/index.lock"، فهذا يعني وجود عملية git عالقة أو متزامنة؛ تحقق من ذلك قبل حذف القفل يدوياً.
6. **ملاحظة حالة معروفة (2026-07-10):** المشروع كان يعمل على فرع `supermarket-baseline-categories` (وليس `main`) مع 111 ملفاً معدَّلاً وغير محفوظ. راجع حالة هذا الفرع قبل بدء أي عمل جديد ولا تتراكم عليه المزيد من التعديلات غير المحفوظة.
7. **تنظيف دوري:** الجهاز يستخدم PowerShell (لا يوجد bash افتراضياً)، لذا شغّل `.\scripts\git-cleanup.ps1` من PowerShell في جذر المشروع كل أسبوع تقريباً لحذف نُسخ العمل والفروع المدموجة تلقائياً (يوجد أيضاً `scripts/git-cleanup.sh` لمن يفضّل Git Bash). راجع يدوياً القائمة التي يطبعها السكربت في النهاية قبل حذف أي فرع منها.

## 4. قائمة تحقق النشر الإلزامية (Deploy Checklist)

عند تعديل `app.js` أو `styles.css`، رقم الإصدار `?v=NNN` يجب تحديثه معاً في **3 مواضع**:

1. `index.html` → وسم `<script src="/app.js?v=NNN">` ووسم `<link href="/styles.css?v=NNN">`.
2. `sw.js` → الثابت `CACHE = "dukkanci-vNNN"`.
3. `sw.js` → كل إشارة لـ `app.js?v=` و `styles.css?v=` داخل مصفوفة `APP_SHELL`.

**قبل كل نشر، شغّل:**
```
npm run check:versions
```
هذا السكربت (`scripts/check-versions.js`) يفشل صراحةً إن كانت الأرقام غير متطابقة بين `index.html` و`sw.js`، بدل اكتشاف الخطأ بعد وصوله للمستخدمين.

> **حالة معروفة عند كتابة هذا الملف:** `index.html` على `app.js?v=264` / `styles.css?v=161`، بينما `sw.js` لا يزال على `app.js?v=176` / `styles.css?v=111`. يجب تصحيح هذا الانحراف في أقرب نشر قادم.

## 5. خريطة الملفات الأساسية

| المسار | الدور |
|---|---|
| `index.html` | الصفحة الرئيسية وكل وسوم `<script>`/`<link>` |
| `app.js` | كل منطق الواجهة (عربة، دفع، عرض متاجر، لوحة تاجر، تتبع...) — ملف ضخم، **ابحث عن الدالة بالاسم قبل إضافة أي منطق جديد** لتجنّب التكرار |
| `styles.css` | كل الأنماط |
| `sw.js` | Service Worker: تخزين مؤقت + إشعارات Push |
| `api/` | دوال Vercel serverless (طلبات، دفع، خرائط، حملات...) |
| `lib/` | أدوات مشتركة (بوابة AI، معالجة صور الكتالوج، Google indexing...) |
| `migrations/` | كل تغييرات مخطط Supabase، مرتبة بالتاريخ في اسم الملف |
| `scripts/` | سكربتات بناء/استخراج بيانات لمرة واحدة لكل متجر — **ليست** جزءاً من التشغيل الحي |
| `catalog/` | الكتالوج المشترك بين متاجر السوبرماركت |
| `*-data.js` (جذر المشروع) | **كود قديم ميت — لا تستخدمه** (راجع قسم 2) |

## 6. الأخطاء المتكررة المعروفة (راجعها قبل إعادة الاختراع)

- **تجمد زر إرسال الطلب / رسوم توصيل صفرية** للمتاجر بدون إحداثيات — أُصلح منطقياً في `app.js`، لكن ظهور الإصلاح فعلياً يتطلب تطبيق قسم 4 (رفع رقم الإصدار في الأماكن الثلاثة) وإلا يبدو "غير مطبَّق" رغم أن الكود صحيح.
- **ظهور/اختفاء متجر** بعد إضافته مباشرة إلى Supabase — تأكد أن منطق العرض في `app.js` يقرأ نفس الحقول التي يُدخلها Supabase، وليس من `*-data.js`.
- **فقدان طلبات صامت** — عولج جزئياً عبر مسار حفظ مستقل في `api/notify-order.js` بدل الاعتماد فقط على كتابة المتصفح المباشرة. أي تعديل مستقبلي على تدفق الطلب يجب أن يمر عبر هذا المسار أيضاً وليس فقط الكتابة المباشرة.
- **ثغرات API غير محمية** — تحقّق دائماً من التحقق من الهوية/الصلاحيات عند إضافة أي `endpoint` جديد داخل `api/`.
- **حذف متجر من Supabase لا يكفي وحده** — كل متاجر السوبرماركت/المطاعم القديمة مبنية كملف `<slug>-data.js` مُحمَّل عبر `<script>` مباشر في `index.html` ومُدرَج أيضاً في `sw.js` (مصفوفة `APP_SHELL`)، وليس مجرد كود ميت كما هو الحال مع الـ56 ملف `*-data.js` المذكورة في قسم 2. حذف صف المتجر من Supabase فقط (بدون حذف `<slug>-data.js` وإزالة أسطره الثلاثة من `app.js`: `stores.push(...)`, `products.push(...)`, `...XDeliverySettings`) يترك المتجر ظاهراً على الموقع فعلياً رغم اختفائه من قاعدة البيانات — هذا بالضبط ما حدث مع متجر حلاب 1881 (id 49) بعد إزالته من Supabase في 2026-07-10 دون تنظيف الحزمة الأمامية.
- **متجر له طلبات = صفّه في `stores` لا يُحذف أبداً (قيد قاعدة بيانات صلب، وليس خياراً)** — `orders_store_id_fkey` مُعرَّف `RESTRICT` وليس `CASCADE`، فأي محاولة `DELETE` على متجر باع ولو طلباً واحداً ترجع `23503: Key (id)=(N) is still referenced from table "orders"`. **النتيجة العملية:** «الحذف الكامل» لمثل هذا المتجر يعني حتماً: حذف منتجاته وبيانات دخوله وإشعاراته + تنظيف الحزمة الأمامية بالكامل + ترك الصفّ خاملاً بـ`approval_status='rejected'` — والصفّ سيعود دائماً إلى مصفوفة `stores` من Supabase في كل تحميل صفحة. لذلك **الاعتماد على تنظيف الحزمة الأمامية وحدها لإخفاء متجر مرفوض غير كافٍ**: `renderStorePage()`/`updateHead()` كانتا تعرضان أي متجر يجده `getStore(id)` بلا بوابة اعتماد، فبقي `/store/<id>` الرقمي يعرض صفحة متجر كاملة. أُضيفت بوابة `approvalStatus === "rejected"` في كلتيهما بتاريخ 2026-07-19 (انظر سطر يمن شيف في سجل الإصلاحات) — إن أضفت مساراً جديداً يقرأ `getStore()` ويعرض بيانات متجر للزبون، **مرِّره على نفس البوابة**. لاحظ أن `pending` مستثنى عمداً (الرابط المباشر هو وسيلة مراجعة المتجر قبل اعتماده). **المخرج الوحيد** من قيد الـFK هو حذف صفوف `orders` أولاً — وهو قرار تجاري لا تقني (يمحو المبيعات من التقارير والمحاسبة بلا رجعة)، فلا تتخذه من تلقاء نفسك: اسأل المستخدم صراحةً كما حدث مع متجر 20 في 2026-07-19.
- **تعديل حقل واحد (lat/lng/mapUrl/hours...) لمتجر "مدموج يدوياً" لا يكفي من Supabase وحده** — نفس فئة الخطأ أعلاه لكن على مستوى حقل واحد بدل المتجر كاملاً: بعض الحقول (`location`/`mapUrl` مؤكَّد، وربما غيرها) تُقرأ من `<slug>-data.js` الثابت حصرياً حتى لو كان للمتجر أيضاً صفّ في Supabase مُحدَّث بقيم صحيحة — لأن `stores.push(xStore)` في `app.js` يُلحق/يُقدَّم على نتيجة Supabase لهذه الحقول. قبل أي `UPDATE` على `stores` في Supabase لمتجر قديم: ابحث أولاً هل يملك ملف `<slug>-data.js` مُحمَّلاً في `index.html` (`grep` عن اسم المتجر أو رقم `id` فيه) — إن وُجد، عدِّل الحقل في الملف نفسه (+رفع `?v=` في `index.html` و`sw.js`)، وSupabase بعدها اختياري/مرجعي فقط لهذا الحقل. مثال: إحداثيات متجر 17 (`afgan-data.js`) صُحِّحت في Supabase أولاً في 2026-07-12 ولم يظهر أي أثر على الموقع حتى عُدِّل الملف الثابت مباشرة.
- **بكسل فيسبوك (Meta Pixel) متناثر على 3 معرّفات مختلفة وعبر Business Managers منفصلة** — البكسل الحقيقي النشط فعلياً على الموقع (`meta_pixel_id` في `integration_settings`) قد لا يكون مرئياً أصلاً لحساب الإعلانات الذي تُنشأ عليه الحملات؛ تحقّق دائماً عبر Meta Business Suite (Data Sources → Datasets & pixels → تبويب Connected assets) قبل افتراض أن التتبّع يعمل. كما أن أي Business Manager جديد (أقل من ~عدة أسابيع) **يمنعه فيسبوك نفسه** من مشاركة أصوله (بكسل/جمهور) مع Business آخر عبر "Assign partner" — رسالة الخطأ: "Businesses new to Facebook Products can share this asset... after several weeks". انظر سطر 2026-07-12 أدناه (قرقوشة فرفوشة) للتفاصيل والحل المؤقت المطبَّق.

## 7. سجل الإصلاحات (Fix Log)

**أضف سطراً جديداً في أعلى الجدول (تحت رأس الجدول مباشرة) مع كل مهمة تنتهي منها. لا تحذف السجل القديم أبداً — هذا هو أساس منع تكرار نفس الخطأ ونسيان الحلول السابقة.**

| التاريخ | المشكلة | السبب الجذري | الإصلاح | الملفات المعدّلة | تم تحديث الإصدار في المواضع الثلاثة؟ |
|---|---|---|---|---|---|
| 2026-07-28 | متابعة لميزة التعبئة التلقائية من الخريطة (السطر التالي أدناه، نفس اليوم): المستخدم طلب صراحةً تخطي أي معاينة على هاتفه («لا اريد المعاينة على هاتفي») والانتقال مباشرة لرفع نسخة تحمل هذه التحديثات إلى Google Play | ليس خطأ — نشر مباشر بطلب صريح من المستخدم، مع تحذير مُعلَن مسبقاً بلا تراجع: الميزة **لم تُختبَر حياً على أي جهاز/محاكي إطلاقاً**، فقط تحليل ساكن (`flutter analyze`) ومراجعة كود. طُرح هذا التحذير صراحةً على المستخدم قبل الإرسال للمراجعة تحديداً («هل تريد الإرسال الآن رغم ذلك؟») فأكَّد المستخدم («yes, submit it») | رُفع `pubspec.yaml` إلى `version: 1.0.3+4` (versionCode 4، يتبع مباشرة versionCode 3 المنشور صباح اليوم)، والتُزم كل تعديلات هذه الجلسة (ملف `geocoding_service.dart` الجديد + تحديثات `app_strings.dart`/`turkish_address.dart`/`address_form_screen.dart`) ودُفعت لـ`main` (commit `8e04e597`). بُنيت حزمة إصدار موقَّعة (`flutter build appbundle --release`، ~692 ثانية) بنفس مفاتيح التوقيع المُهيَّأة مسبقاً. رُفعت الحزمة لقناة Production عبر Play Console: نافذة رفع الملف الأصلية تجاوزت حد أداة `file_upload` الآلية (10MB، والملف 59.4MB) فاختار المستخدم الملف يدوياً بنفسه من منتقي ملفات النظام الحقيقي بعد أن حدَّدت له المسار الدقيق. اسم الإصدار تعبَّأ تلقائياً «4 (1.0.3)»، وأُضيفت ملاحظات إصدار عربية تصف الميزتين (تعبئة تلقائية من الخريطة + نصوص عربية أوضح). 🔴 **عائق بيئي عابر أثناء العملية:** ظهرت رسالة «Multiple Chrome browsers are connected... none has been selected» بعد اتصال متصفح Chrome ثانٍ بنفس الحساب أثناء الجلسة — حُلَّ عبر `switch_browser` (إرسال طلب اتصال لكل متصفحات Chrome المتصلة وانتظار اختيار المستخدم من داخل الإضافة نفسها)، بلا فقدان لحالة الجلسة (نفس تبويب Play Console استمر بعد إعادة الربط). **الحالة النهائية عند إغلاق المهمة:** الإصدار **4 (1.0.3)** — «Start full rollout» — أُرسل فعلاً لمراجعة غوغل على قناة Production («Changes in review»؛ الإصدار السابق 3 (1.0.2) كان قد استغرق أقل من ساعتين ليصبح «Available on Google Play» فعلياً — مراجعة أسرع من المتوسط المُعلَن رسمياً 7 أيام) | `dukkanci_customer_app/pubspec.yaml`, `dukkanci_customer_app/lib/core/services/geocoding_service.dart` (جديد)، `dukkanci_customer_app/lib/core/localization/app_strings.dart`, `dukkanci_customer_app/lib/core/utils/turkish_address.dart`, `dukkanci_customer_app/lib/features/addresses/presentation/address_form_screen.dart`, `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`؛ نظام إصدار Flutter الخاص به هو `pubspec.yaml`، مُحدَّث أعلاه). **مدفوع لـ`main`** (commit `8e04e597`) ومُرسَل فعلياً لمراجعة Google Play Production — بناءً على طلب المستخدم الصريح المتكرر («ارفع نسخة» ثم تأكيد «yes, submit it»)
| 2026-07-28 | متابعة لإصلاح الخريطة/الموقع أعلاه (نفس اليوم): بعد أن أكَّد المستخدم أن الخريطة تعمل حياً على جهازه، طلب تحسيناً شاملاً لنفس شاشة «عنوان جديد»: (١) عند تثبيت المؤشر على باب المنزل، تعبئة المحافظة/المنطقة/الحي تلقائياً وترك رقم البناء/الشقة/الطابق فقط للإدخال اليدوي، (٢) نصوص الشرح/التلميح الظاهرة بالتركية («İl seçin»، «Cadde / Sokak Adı»...) تصبح عربية بأمثلة حقيقية، (٣) قوائم اختيار المحافظة/المنطقة «أجمل» من الحالية | ليست علّة — طلب ميزة/تحسين. `TrLocationRepository` كانت تملك أصلاً دالة `matchByName()` معلَّقة بتعليق صريح «Best-effort match of a free-text name (e.g. from reverse-geocoding)» **لكن غير مستدعاة من أي مكان** — البنية التحتية للمطابقة جاهزة منذ بناء نظام العناوين التركي، لم يبقَ إلا تنفيذ التحديد العكسي (reverse geocoding) نفسه وربطه. لا يوجد أي مفتاح Google Geocoding مخصَّص للتطبيق (`AppConfig.mapsApiKey` المُعرَّف في الكود فارغ افتراضياً وغير موصول أصلاً بأي بناء فعلي — المفتاح الحقيقي المُحقَن في AndroidManifest يأتي مباشرة من Gradle، لا من Dart)، لكن نقطة `/api/maps-key` على الموقع (وثّقتها `google-cloud-maps` memory: «browser key بلا قيد referrer، بالتصميم») تصلح تماماً لاستدعاء REST مباشر بلا حاجة أي إعداد جديد — **مفتاح مقيَّد بـ«Android apps»** (كمفتاح خرائط أندرويد الحالي) **يرفض طلبات REST الخام** لأنه يعتمد التحقق عبر مكتبة Google Play Services لا عبر ترويسات HTTP عادية، فاستخدام مفتاح الويب هنا (لا مفتاح أندرويد) قرار متعمَّد لا صدفة | ملف جديد `lib/core/services/geocoding_service.dart`: `GeocodingService` بـ`Dio()` **مستقل عمداً عن `ApiClient` المشترك** (الذي يُرفق توكن الزبون تلقائياً بكل طلب — لا يجوز تسريبه لطلبات غير مصادَق عليها لـ`dukkanci.com.tr/api/maps-key` ولا لـ`maps.googleapis.com`) — يجلب المفتاح مرة واحدة (مخزَّن مؤقتاً)، ثم `reverseGeocode(lat,lng)` يستدعي REST Geocoding API مباشرة (`language=tr`) ويستخرج `administrative_area_level_1/2`, `neighborhood/sublocality`, `route` — **نفس بالضبط منطق `pickAddressComponent()`/`googleReverseFull()` في `app.js`، منقول حرفياً**. فشل أي خطوة (لا مفتاح، لا شبكة، لا نتائج) يُعيد `null` بصمت — مساعدة فقط، لا تحجب أبداً الاختيار اليدوي (نفس فلسفة `commitAddressPin()` بالموقع، §5: «لا تعتمد على الخريطة وحدها»). وُصلت في `address_form_screen.dart`: `_onCameraIdle()` (يُطلَق عند كل استقرار للكاميرا، أي كل مرة يترك فيها المستخدم السحب) يستدعي `_autoFillFromLocation()` جديدة — بترتيب متسلسل (محافظة→منطقة→حي، كل خطوة تنتظر مطابقة الخطوة السابقة أولاً كي لا تُجلَب بيانات منطقة/حي لمحافظة خاطئة) ومحروسة بـ`_geoToken` متزايد (نفس نمط `addr2CommitToken` بالموقع) يمنع استجابة قديمة بطيئة من الكتابة فوق نتيجة أحدث لو حرَّك الزبون الخريطة عدة مرات متتالية بسرعة. اسم الشارع يُعبَّأ **فقط إن كان الحقل فارغاً** (لا يطمس تعديلاً يدوياً سابقاً). أُضيف شريط حالة صغير تحت الخريطة (دائرة تحميل + «جارٍ تحديد العنوان من الخريطة…» ثم علامة صح خضراء + «تم تعبئة العنوان تلقائياً — راجعه وعدّله إذا لزم») — يختفي بصمت عند الفشل (لا رسالة خطأ لشيء تصميمه أصلاً «قد ينجح أو لا»). **عربنة الشرح:** في `app_strings.dart` — كل حقول `addr*Hint`/`addr*Label` (لا حقول `addr*Err*` التي تبقى تركية عمداً بنص §14 الحرفي الموثَّق في تعليق الكود نفسه، ولا القيم المُختارة/العنوان النهائي المُجمَّع في `turkish_address.dart` التي تبقى تركية رسمية لأنها ما يقرأه المندوب فعلياً) تحوَّلت لعربية بأمثلة حقيقية («مثلاً: اسطنبول» للمحافظة، «مثلاً: زيتون بورنو» للمنطقة، «مثلاً: كاياباشي» للحي — نفس المثال المُستخدَم أصلاً في migration نظام العناوين). أُضيف أيضاً `trRoadTypeArabicLabel` في `turkish_address.dart` (تعليق عربي مرافق مثل «جادة»/«شارع» بجانب كل خيار Cadde/Sokak في القائمة المنسدلة — **عرض فقط**، لا يمسّ `trRoadTypes`/`_roadTypeSuffix` نفسيهما فلا يتغيّر العنوان النهائي المُركَّب). **تجميل قائمة الاختيار:** أُعيد بناء `_LocationPickerSheet` بالكامل: مقبض سحب أعلى الورقة، عدّاد نتائج حي بجانب العنوان، حقل بحث مستدير بخلفية `cream` وتلميح عربي «ابحث بالاسم…» بدل «...» الفارغ، صفوف `_PickerRow` جديدة بأيقونة دبوس موقع + فواصل رفيعة (`ListView.separated` بدل `ListView` بسيطة)، حالة فارغة بأيقونة `search_off` ونص عربي «لا توجد نتائج مطابقة» بدل «Sonuç bulunamadı» التركية المكتوبة حرفياً في الودجة، وصفّ «سأكتبه بنفسي» مميَّز بلون العلامة وأيقونة قلم. **التحقّق:** `flutter analyze` أعاد **نفس الأربع ملاحظات المعلوماتية القديمة فقط** (صفر جديدة — رصد لاحقاً `unnecessary_underscores` واحد في `separatorBuilder: (_, __) => ...` وأُصلح بتسمية المعاملين بدل الشرطتين السفليتين، ثم أُعيد تشغيل `flutter analyze` فعلياً بعد الإصلاح وتأكَّد الرجوع لخط الأساس نفسه). ⚠️ **لم يُتحقَّق من التدفّق الحي (سحب الخريطة → تعبئة تلقائية فعلية) على أي جهاز/محاكي** — بيئة العمل قتلت عملية محاكي `dukkanci_test` مرتين متتاليتين فور اكتمال أي أمر Bash تالٍ رغم إطلاقها بنفس أسلوب `nohup … & disown` الأمامي الموثَّق سابقاً كحل ناجح (على الأرجح Windows Job Object يحصر كل عمليات الجلسة الفرعية تحت مظلة واحدة تُغلَق تلقائياً عند اكتمال أي استدعاء Bash لاحق، بصرف النظر عن `disown` — قيد بيئي أعمق من مجرد استدعاء خلفي واحد كما وُثِّق سابقاً)، وطلب المستخدم لاحقاً صراحةً تخطي المعاينة على هاتفه الحقيقي أيضاً والانتقال مباشرة لرفع نسخة الإنتاج | `dukkanci_customer_app/lib/core/services/geocoding_service.dart` (جديد)، `dukkanci_customer_app/lib/core/localization/app_strings.dart`, `dukkanci_customer_app/lib/core/utils/turkish_address.dart`, `dukkanci_customer_app/lib/features/addresses/presentation/address_form_screen.dart`, `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`). **⚠️ لم يُختبَر حياً على أي جهاز حقيقي أو محاكي — التحقّق الوحيد المتاح هو التحليل الساكن (`flutter analyze`) ومراجعة الكود**، بناءً على طلب المستخدم الصريح بتخطي المعاينة والانتقال مباشرة لرفع نسخة الإنتاج
| 2026-07-28 | متابعة لنشر إصدار 3 (1.0.2) على Production (السطر التالي أدناه، نفس اليوم): المستخدم حدَّث التطبيق من المتجر فعلاً وأبلغ أن **الخريطة لا تزال لا تظهر**، لكن طلب تفعيل الموقع أصبح يعمل بشكل صحيح | 🔴 **الإصلاح السابق (تسجيل بصمتَي SHA-1 لمفتاحَي Debug وUpload في Google Cloud Console) كان غير كافٍ لبناء Play Store الفعلي — سبب مختلف تماماً لم يظهر إلا على جهاز حقيقي يستخدم النسخة المنشورة فعلياً.** تحقّق مباشر من Play Console: الإصدار 3 (1.0.2) كان بالفعل **«Available on Google Play»** (نُشر تلقائياً بسرعة غير متوقَّعة، خلال دقائق من الإرسال للمراجعة)، فاستُبعد احتمال أن يكون المستخدم لا يزال على النسخة القديمة. **الاكتشاف الجذري:** Google Play يستخدم **«Play App Signing»** — عند تفعيله (مُفعَّل هنا فعلاً، ويظهر في «Test and release» كـ«Releases are signed by Google Play»)، لا يوزَّع على المستخدمين ملف الـAAB الموقَّع بمفتاح الرفع (`upload-keystore.jks`، بصمته `B1:14:28:...` المسجَّلة سابقاً) بل يُعاد توقيع الـAPK النهائي بمفتاح **مختلف تماماً** يديره غوغل نفسه («App signing key») — فالبصمتان المسجَّلتان سابقاً (Debug + Upload key) لم تكونا لتطابقا أبداً شهادة التوقيع الفعلية على جهاز أي مستخدم حقيقي، بصرف النظر عن صحة الإصلاح السابق تماماً لبيئة الاختبار المحلي (محاكي بحزمة debug). لم يكن هذا معروفاً مسبقاً لأن كل التحقّق السابق تم عبر بناء debug مباشر على المحاكي، لا عبر تنزيل فعلي من المتجر | استُخرجت شهادة توقيع Google Play الفعلية («App signing key») من Play Console (المسار الجديد بعد نقل الميزة: Protected with Play → قسم «Play Store protection» → «Protect app signing key» → «Manage Play app signing» → «Download certificates» — الأزرار المرئية «SHA-1 certificate fingerprint» في الواجهة لا تعرض القيمة نصياً [نسخ للحافظة فقط، بلا توست ظاهر]، فاستُخدم تنزيل ملف `certificates.zip` وحساب البصمات منه مباشرة عبر `openssl x509 -noout -fingerprint -sha1`). الملف يحوي 3 شهادات (`deployment_cert.der`, `hybrid_classical_cert.der`, `hybrid_pqc_cert.der` — أثر ميزة «Quantum-ready (beta)» الجديدة في Play Console: توقيع هجين كلاسيكي+ما بعد الكم)؛ نظراً لعدم وجود توثيق حاسم لأي الشهادتين الكلاسيكيتين (`deployment_cert` أو `hybrid_classical_cert`) هي التي يقرأها Android الفعلي عبر `PackageManager` عند استعلام Maps SDK، **سُجِّلت بصمتا SHA-1 لكلتيهما معاً** (لا ضرر من تسجيل بصمة إضافية، وتغطية الاحتمالين أرخص من التخمين الخاطئ): `9D:24:3C:18:2A:6C:6F:72:CD:E4:34:1D:02:71:3B:A6:E6:3F:69:BE` (deployment_cert) و`3E:CD:09:A0:8F:50:3F:7E:04:EE:79:79:1C:F9:0E:FE:77:E8:4D:C7` (hybrid_classical_cert) — أُضيفتا لنفس مفتاح «Android Production» في Google Cloud Console بجانب البصمتين الموجودتين مسبقاً (Debug + Upload)، فأصبح المفتاح الآن يحمل **4 إدخالات** لنفس الحزمة `com.dukkanci.app` تغطي كل مسارات التوقيع الأربعة المحتملة (تطوير محلي، رفع للمتجر، وتوقيع غوغل الفعلي بشقّيه). Google يذكر صراحة أن التفعيل قد يستغرق حتى 5 دقائق. ⚠️ **لم يُتحقَّق بعد من نجاح الإصلاح على الجهاز الحقيقي** — بانتظار أن يعيد المستخدم تجربة شاشة «عنوان جديد» بعد انتظار قصير؛ إن استمر الفراغ، الاحتمال التالي هو أن الشهادة الصحيحة غير هاتين الاثنتين إطلاقاً (مثلاً `hybrid_pqc_cert`، رغم استبعادها منطقياً لأن Android القديم/Maps SDK لا يفهم توقيع ما بعد الكم) | لا شيء في كود المستودع (نشاط Google Cloud Console بحت، خارج المستودع؛ الكود ثابت منذ `e92c37f0`) + `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل — لا تعديل كود، فقط تسجيل بصمات إضافية على مفتاح API موجود)
| 2026-07-28 | متابعة لإصلاحَي خريطة/طلب موقع «عنوان جديد» وتحقّقهما الحي (الأسطر التالية أدناه، نفس اليوم): المستخدم طلب رفع حزمة App Bundle موقَّعة فعلياً إلى Google Play Console على قناة **Production** | ليس خطأ برمجياً — تنفيذ نشر فعلي، لكنه اصطدم بعقبة تزامن حقيقية: أول محاولة رفع رُفضت من Play Console بـ«Version code 3 has already been used». فحص «Latest releases and bundles» كشف إصداراً آخر بنفس versionCode 3 (1.0.2) **كان بالفعل «In review» على قناة Production**، مرفوعاً في نفس اليوم (12:08 ظهراً) **بلا أي ملاحظات إصدار مكتوبة** — ليس من هذه الجلسة (كل محاولاتها فشلت قبل ذلك بالتوقيت)، وعلى الأرجح من جلسة Claude متزامنة أخرى تعمل على نفس المستودع (نمط تزامن موثَّق مراراً في هذا الملف، هنا امتدّ لأول مرة إلى لوحة Play Console نفسها لا فقط Git). بدل تجاوز الأمر بصمت (رفع versionCode 4 مثلاً وترك الإصدار المجهول قائماً)، طُرح الموقف على المستخدم صراحة عبر `AskUserQuestion` — قرار عالي الأثر (رفعة إنتاج عامة غامضة المصدر تخصّ كل مستخدمي المتجر) لا يجوز اتخاذه بالنيابة — فاختار المستخدم صراحة: **«تجاهل/احذف الإصدار 3 القائم، وارفع نسختي»** | نُفِّذ القرار عبر مسارين رسميين **قابلين للتراجع بالكامل** في Play Console (لا حذف قسري نهائي): (١) Publishing overview → قسم «Changes in review» → «Remove changes» (يُعيد الإصدار المجهول لحالة مسودة فقط، لا يحذفه). (٢) فتح الإصدار (أصبح مسودة الآن) → «Discard release» (يُزيله من قائمة الإصدارات، لكن حزمته تبقى في «artifact library» قابلة للاسترجاع). بعد تحرّر versionCode 3 من التعارض: أُنشئ إصدار Production جديد، واستُخدمت **نفس** حزمة `app-release.aab` الموقَّعة (59.4MB) التي كانت قد رُفعت مسبقاً بنجاح في محاولة سابقة فاشلة (ظلّت محفوظة في «artifact library» رغم فشل نشرها آنذاك) — اختيرت عبر «Add from library» بدل إعادة رفعها يدوياً من جديد عبر منتقي ملفات النظام. أُضيفت ملاحظات إصدار عربية («إصلاح مشكلة عدم ظهور خريطة عنوان التوصيل الجديد، وإضافة طلب تفعيل الموقع تلقائياً عند إضافة عنوان جديد»). قبل الخطوة الأخيرة غير القابلة للتراجع بسهولة (إرسال فعلي لمراجعة غوغل)، طُلب تأكيد صريح مستقل من المستخدم فأتى («yes, submit it»)، فنُفِّذ الإرسال فعلياً («Submit 1 change for review» → «Send changes for review»). **الحالة النهائية عند إغلاق المهمة:** الإصدار **3 (1.0.2)** — «Start full rollout» — أصبح ضمن قسم **«Changes in review»** على قناة Production (مراجعة غوغل عادةً ≤7 أيام، وقد تطول)؛ عند القبول سيُنشَر تلقائياً لكل مستخدمي المتجر الحقيقيين بلا أي بوابة نشر يدوية إضافية (لأن «Managed publishing» معطَّل على هذا التطبيق) | لا شيء في كود المستودع (نشاط لوحة Play Console بحت، خارج المستودع؛ الكود نفسه ثابت منذ الالتزام `e92c37f0` الموثَّق في السطر التالي أدناه) + `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل — عملية نشر لبناء مؤكَّد سابقاً، لا تعديل كود جديد في هذه الجولة)
| 2026-07-28 | متابعة للتحقّق الحي أعلاه (نفس اليوم): المستخدم طلب تسجيل بصمتي SHA-1 فعلياً في Google Cloud Console ثم إعادة البناء والتثبيت والتحقّق أن الخريطة تُعرَض | **مُغلَق بالكامل — الخريطة تعمل الآن حياً ومؤكَّدة.** بدل تسجيل البصمتين على مفتاح "web Production" المُستخدَم سابقاً (خطر: هذا المفتاح مُقيَّد أصلاً على الأرجح بـHTTP referrer للموقع، وإضافة قيد "Android apps" فوقه إما يفشل [Cloud Console يسمح بنوع تقييد تطبيق واحد فقط] أو يستبدل تقييد الويب فيكسر الموقع)، اكتُشف بفحص صفحة Credentials في Google Cloud Console (مشروع `dukkanci-331012`) أن مفتاحاً مخصَّصاً **موجوداً مسبقاً منذ 2021** بالاسم "Android Production" (`AIzaSyBEZJcm_BQAVMH3k21HXwGxycLIN3QE7U0`)، مقيَّداً بدقة بـ3 واجهات فقط (Geocoding API + **Maps SDK for Android** + Places API) — تماماً ما يحتاجه تطبيق الجوال — لكنه كان بلا أي قيد تطبيق («Application restrictions: None»، مع تحذير Google نفسه: "This key can currently be used with any application. Restrict where it can be used to improve security"). **الإصلاح المزدوج:** (١) في Google Cloud Console: فُعِّل قيد "Android apps" على مفتاح "Android Production" هذا تحديداً، وأُضيف إدخالان لـ`com.dukkanci.app` (بصمة Debug: `1A:35:6D:FA:74:81:0C:FC:46:E6:AA:43:C3:E7:6D:EA:2B:BF:B6:B4`، وبصمة Release: `B1:14:28:C7:0D:8C:A5:05:E4:B4:67:AA:01:49:0B:14:7B:F7:DE:05`) — يُغلق أيضاً ثغرة أمنية حقيقية كانت قائمة أصلاً (مفتاح فعّال بلا أي قيد استخدام منذ 5 سنوات). (٢) في `build.gradle.kts`: استُبدلت القيمة الاحتياطية من مفتاح "web Production" (`AIzaSyA0sLQ7F2ivkWnR3XRPwmJx6HiNkHvqT3g`، الذي أثبت `adb logcat` سابقاً أنه يرمي `Authorization failure` على أندرويد) إلى مفتاح "Android Production" المُصلَح أعلاه. **التحقّق الحي الكامل بعد إعادة بناء APK جديد (v1.0.2+3) وتثبيته على محاكي `dukkanci_test` وتشغيله فعلياً:** فُتحت شاشة «عنوان جديد» (نفس مسار التحقّق السابق: حساب حقيقي ← عناويني ← إضافة عنوان جديد) — **الخريطة عرضت بلاطات Google Maps حقيقية كاملة** (شوارع، مبانٍ مُسمّاة، مناطق خضراء، الدبوس الأحمر في المنتصف — موقع محاكي GPS الافتراضي في حرم Google الرئيسي)، بعد ظهور مؤشر تحميل دوّار لحظياً ثم اكتمال الرسم خلال ثوانٍ. `adb logcat` بعد التحديث لا يحتوي **أي** سطر `Authorization failure` جديد إطلاقاً (فقط رسائل تهيئة SDK الطبيعية: `Google Play services client version`، `maps renderer version`). **كلا جزأي المشكلة الأصلية مغلقان ومؤكَّدان بالاختبار الحي 100%** — لا حاجة لأي عمل إضافي على هذه المهمة | `dukkanci_customer_app/android/app/build.gradle.kts` (استبدال المفتاح الاحتياطي فقط) + قيد "Android apps" على مفتاح "Android Production" في Google Cloud Console (مشروع dukkanci-331012، خارج المستودع) + `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`)
| 2026-07-28 | المستخدم طلب رفع كل تعديلات الجلسات السابقة على تطبيق Flutter (المتراكمة بلا commit عبر عدة جلسات متزامنة) ليتمكن من بناء نسخة محدَّثة ورفعها على Google Play | لا خطأ — تجميع عمل متراكم. الملفات المعدَّلة تمثّل إعادة تصميم للصفحة الرئيسية وعناصرها المشتركة (بدأت جلسة "أسلوب Yemeksepeti" 2026-07-17، واستمرت بجلسات لاحقة غير موثَّقة سطراً بسطر): شريط ثقة جديد (`TrustBar`) أسفل الهيدر مباشرة، بلاطات تصنيفات مصوَّرة (`CategoryTiles` بدل `CategoryStrip`، بصورة أفضل متجر مفتوح في كل تصنيف بدل صورة عامة)، وصفّا منتجات أفقيان جديدان (`ProductRail`): «عروض اليوم» من منتجات بخصم حقيقي (`old_price>price`) و«منتجات مقترحة لك» من العلَم الحقيقي `featured` (لا بيانات "الأكثر طلباً" مُختلَقة، بصراحة نفس مبدأ الموقع). تحديثات مصاحبة في نظام التصميم (`app_colors`/`app_text_styles`/`app_theme`/`status_badge`) وبطاقة/صفّ المتجر لدعم هذه العناصر، وخط Tajawal الحقيقي كعائلة عرض (`AppDisplay`) بدل الافتراضي. `.gitignore` اكتسب قاعدة صريحة إضافية لمنع `key.properties`/`*.jks` من الالتزام مستقبلاً (تحقّق `git check-ignore`: كلاهما مستبعدان فعلاً، ولا وجود لهما في `git ls-files` — لا كشف أسرار). `build.gradle.kts` اكتسب إعداد توقيع إصدار حقيقي (`signingConfigs.release` من `android/key.properties`+`upload-keystore.jks` الموجودين محلياً، مع رجوع آمن لتوقيع debug إن غاب الملف) — ضروري لبناء أي رفعة جديدة موقَّعة لـGoogle Play. 🔴 **اكتشاف حرج قبل الالتزام:** `pubspec.yaml` كان لا يزال يحمل `version: 1.0.1+2` — نفس الإصدار (`versionCode 2`) المرفوع بالفعل على Google Play Console (سطر 2026-07-24 أعلاه، "Internal testing... Available") — رفع نسخة بنفس الرقم كان سيُرفَض من Play Console مباشرة (لا يقبل versionCode مكرَّراً أو أقل) | لا إصلاح كود جديد من هذه الجلسة — تجميع/توثيق/تحقّق فقط لعمل جلسات سابقة، مع تصحيح واحد ضروري: رفع `pubspec.yaml` إلى `version: 1.0.2+3` (`versionCode 3`) ليقبله Play Console. **التحقّق قبل الالتزام:** `flutter analyze` على كامل الشجرة المجمَّعة (كل الملفات أعلاه معاً) أعاد **نفس الأربع ملاحظات المعلوماتية القديمة فقط، صفر أخطاء جديدة** — تحقّق فعلي بعد تشغيله، وليس افتراضاً بالاستناد لتحقّق كل جلسة على حدة. `flutter build apk --debug --target-platform android-x64` نجح فعلياً وأنتج `app-debug.apk` حقيقياً (بُني ونُصِّب على محاكي `dukkanci_test` حياً في نفس الجلسة، وإن انهار المحاكي لاحقاً لأسباب بيئية بحتة قبل اختبار تفاعلي كامل — راجع سطري OTP/العنوان المهيكل أعلاه لتفاصيل ذلك). لم يُبنَ `app-release.aab` موقَّع في هذه الجلسة (يحتاج تشغيلاً منفصلاً؛ كل مستلزماته — المفتاح والإعداد ونسخة الإصدار — أصبحت جاهزة الآن) | `dukkanci_customer_app/pubspec.yaml`, `dukkanci_customer_app/.gitignore`, `dukkanci_customer_app/android/app/build.gradle.kts`, `dukkanci_customer_app/lib/app/providers.dart`, `dukkanci_customer_app/lib/core/theme/app_colors.dart`, `dukkanci_customer_app/lib/core/theme/app_text_styles.dart`, `dukkanci_customer_app/lib/core/theme/app_theme.dart`, `dukkanci_customer_app/lib/core/widgets/status_badge.dart`, `dukkanci_customer_app/lib/features/cart/presentation/cart_screen.dart`, `dukkanci_customer_app/lib/features/home/presentation/home_screen.dart`, `dukkanci_customer_app/lib/features/home/presentation/widgets/{category_strip,category_tiles,product_rail,promo_hero,store_card,store_rail,trust_bar}.dart`, `dukkanci_customer_app/lib/features/products/presentation/product_screen.dart`, `dukkanci_customer_app/lib/features/stores/data/store_repository.dart`, `dukkanci_customer_app/lib/features/stores/presentation/store_screen.dart`, `dukkanci_customer_app/assets/fonts/Tajawal-*.ttf` (جديد)، `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`؛ نظام إصدار Flutter الخاص به هو `pubspec.yaml`، مُحدَّث أعلاه). **مدفوع لـ`main`** بناءً على طلب المستخدم الصريح
| 2026-07-28 | متابعة لإصلاح خريطة عنوان جديد (السطر التالي أدناه، نفس اليوم): المستخدم طلب بناء APK جديد واختباره فعلياً على جهاز — تحقّق حي كامل (بناء + تثبيت + تشغيل على محاكي `dukkanci_test`) | **النتيجة النهائية: (١) إصلاح طلب الموقع مؤكَّد يعمل حياً. (٢) إصلاح الخريطة غير كافٍ — السبب الحقيقي مختلف عمّا افتُرض.** بعد تجاوز سلسلة طويلة من أعطال بيئية بحتة (Gradle daemon قُتل أثناء أول بناء بسبب تنافس موارد؛ محاولات إطلاق المحاكي عبر `run_in_background` كانت تُقتَل بمجرد اكتمال تلك المهمة الخلفية — الأرجح Windows Job Object يُصفّي كل العمليات الوريثة رغم `disown`، فأُعيد الإطلاق كعملية أمامية `nohup … & disown` بدل مهمة خلفية؛ قفل AVD عالق من الانهيار الأول احتاج حذف `hardware-qemu.ini.lock`/`multiinstance.lock` يدوياً؛ ثم **حمل المعالج المضيف بلغ 100% فعلياً** [تأكَّد مرتين عبر `wmic cpu get loadpercentage`] بسبب جلسات متزامنة أخرى تعمل على نفس الجهاز، منتجاً سلسلة ANR حقيقية لواجهة النظام والتطبيق معاً وتأخّراً شديداً في معالجة اللمسات حتى إن نقرة واحدة قد تُنفَّذ متأخرة بدقائق على شاشة مختلفة تماماً عمّا استُهدفت له [شوهد حرفياً: نقرة كانت تستهدف «إضافة عنوان جديد» شغَّلت متصفح Chrome بدلاً من ذلك] — عولج بإعادة كل خطوة بتحقّق منفصل بعد كل نقرة واحدة عبر `uiautomator dump` لإحداثيات دقيقة بدل التخمين من لقطة شاشة). **بعد الوصول فعلياً لشاشة «عنوان جديد» (حساب حقيقي 905533333362 ← عناويني ← إضافة عنوان جديد):** ظهر **مباشرة وتلقائياً** مربّع حوار نظام أندرويد الأصلي «Allow دكانجي to access this device's location؟» بخياراته الثلاثة القياسية — يؤكّد أن إصلاح `WidgetsBinding.instance.addPostFrameCallback(() => _useCurrentLocation())` يعمل حرفياً كما صُمِّم، بلا حاجة لأي تعديل إضافي. لكن بعد الموافقة («While using the app»)، **الخريطة بقيت رمادية فارغة تماماً** — نفس العطل الأصلي المُبلَّغ عنه بالضبط. فحص `adb logcat` كشف السبب الحقيقي القاطع (غير الغموض التوثيقي المذكور في السطر التالي): 🔴 **`E Google Maps Android API: Authorization failure.`** ثم تفصيلاً: `Ensure that the following Android Key exists: API Key: AIzaSyA0sLQ7F2ivkWnR3XRPwmJx6HiNkHvqT3g / Android Application: 1A:35:6D:FA:74:81:0C:FC:46:E6:AA:43:C3:E7:6D:EA:2B:BF:B6:B4;com.dukkanci.app` — أي أن المفتاح **موجود ومحقون بشكل صحيح تماماً في البيان** (تأكَّد ذلك مسبقاً في الإصلاح السابق) **لكنه غير مُصرَّح له باستخدام Maps SDK for Android لهذا التطبيق تحديداً** في Google Cloud Console — يحسم بشكل قاطع الشك الذي تُرك معلَّقاً في السطر التالي (هل المفتاح مقيَّد بـHTTP referrer أم لا: **نعم، هو كذلك فعلياً**، أو على الأقل لا يحمل تفويض "Android apps" لهذا التطبيق) | **لم يُعدَّل أي كود إضافي في هذه الجولة** — المشكلة المتبقية بنيوية/صلاحيات Google Cloud Console بحتة، خارج نطاق أي تعديل برمجي ممكن من هذه الجلسة (لا صلاحية وصول للوحة التحكم). **الخطوة المطلوبة من المستخدم حصراً:** في Google Cloud Console → APIs & Services → Credentials → المفتاح `AIzaSyA0sLQ7F2ivkWnR3XRPwmJx6HiNkHvqT3g` → إما (أ) إضافة قيد "Android apps" يشمل الحزمة `com.dukkanci.app` مع **كلا** البصمتين (Debug: `1A:35:6D:FA:74:81:0C:FC:46:E6:AA:43:C3:E7:6D:EA:2B:BF:B6:B4` — نفس بصمة `~/.android/debug.keystore` الافتراضية لكل مطوّري Flutter؛ Release: `B1:14:28:C7:0D:8C:A5:05:E4:B4:67:AA:01:49:0B:14:7B:F7:DE:05` — من `android/app/upload-keystore.jks`، استُخرجت هذه الجلسة عبر `keytool -list -v`)، أو (ب) — إن كان هذا المفتاح مُخصَّصاً عمداً لموقع الويب فقط بقيد HTTP referrer — إنشاء **مفتاح Android منفصل** بقيد "Android apps" بنفس البصمتين ومروره وقت البناء عبر `-PmapsApiKey=…`/`MAPS_API_KEY` (يطغى تلقائياً على القيمة الاحتياطية المضافة في الإصلاح السابق، بلا أي تعديل كود إضافي). **التحقّق الإيجابي المكتمل فعلياً بلا حاجة لأي شيء آخر:** إصلاح طلب الموقع (الجزء الثاني من الإصلاح السابق) **مغلق ومؤكَّد 100% بالاختبار الحي** — لا حاجة لإعادة اختباره مستقبلاً ما لم يتغيّر الكود مجدداً | لا شيء (تحقّق حي فقط؛ الكود من الإصلاح السابق كافٍ ولم يُعدَّل) + `CLAUDE.md` | لا ينطبق (تحقّق فقط، لا تعديل كود)
| 2026-07-28 | المستخدم أرسل لقطة شاشة من شاشة «عنوان جديد» في تطبيق Flutter (نموذج العنوان التركي، `AddressFormScreen`) تُظهر مربّع الخريطة فارغاً تماماً (رمادي، بلا أي بلاطات خريطة، فقط شعار Google وأيقونة الموقع)، وأبلغ أن الصفحة **لا تطلب من المستخدم تفعيل ميزة تحديد المواقع** إطلاقاً | خللان مستقلّان: (١) 🔴 **الخريطة فارغة = عرض تعطّل مفتاح خرائط جوجل الكلاسيكي على أندرويد.** `android/app/build.gradle.kts` كان يقرأ `mapsApiKey` من خاصية Gradle أو متغيّر بيئة `MAPS_API_KEY` عند البناء، **بقيمة احتياطية فارغة `""`** إن لم يُمرَّر أيّ منهما — ولا `android/local.properties` (الملف المحلي الفعلي على هذا الجهاز) ولا أي أمر بناء سابق كان يمرّر القيمة، فحُقن مفتاح فارغ في `AndroidManifest.xml` (`com.google.android.geo.API_KEY`)، وهذا تحديداً ما يُنتج قماشة خرائط جوجل الرمادية الفارغة مع شعار Google الظاهر دون أي بلاطة — نفس اللقطة المُرسَلة حرفياً. (٢) **لا طلب لإذن/تفعيل الموقع:** `_useCurrentLocation()` لم تكن تُستدعى إطلاقاً عند فتح الشاشة (فقط عند الضغط اليدوي على زر «GPS» العائم فوق الخريطة)، **و**حتى عند الضغط عليه كانت الدالة تُنهي التنفيذ بصمت (`return` عارٍ بلا أي رسالة) في كل من حالتي: خدمة الموقع (GPS) مُعطَّلة على الجهاز، أو رفض المستخدم الإذن نهائياً (`deniedForever`) — فلا يظهر أي مربّع حوار يطلب من المستخدم تفعيل شيء على الإطلاق، تماماً كما وصف | **(١) مفتاح الخريطة:** بدَّلت القيمة الاحتياطية الفارغة في `build.gradle.kts` بنفس المفتاح الحيّ الذي يخدمه الموقع فعلاً لأي زائر مجهول عبر `https://www.dukkanci.com.tr/api/maps-key` (ليس سرّاً — يُشحَن للعميل مباشرة هناك أيضاً)، مع الإبقاء على إمكانية تجاوزه عبر `-PmapsApiKey=`/`MAPS_API_KEY` كما كان. ⚠️ **تحذير موثَّق صراحة في `README.md` (قسم «إعداد يدوي مطلوب») يستحق نقله للمستخدم:** مفتاح خرائط الموقع قد يكون مُقيَّداً بـ HTTP referrer (لا يعمل مع Maps SDK for Android الذي يتطلّب تقييد "Android apps" بمعرّف الحزمة `com.dukkanci.app` وبصمتَي SHA-1)، بينما تعليق `api/maps-key.js` نفسه يذكر أنه "مفتاح متصفّح بلا تقييد referrer" — تناقض توثيقي غير محسوم من هذه الجلسة (لا صلاحية وصول لـ Google Cloud Console للتحقّق). **النتيجة العملية:** الإصلاح يرفع الاحتمال من "فشل مؤكَّد 100%" (مفتاح فارغ) إلى "يعمل غالباً" — إن بقيت الخريطة فارغة بعد هذا التغيير، الخطوة التالية إما تفعيل "Maps SDK for Android" ضمن قيود API لنفس المفتاح من Console، أو إنشاء مفتاح Android مخصَّص ومروره وقت البناء كما يوثّق `README.md` بالتفصيل بالفعل. **(٢) طلب الموقع:** أُضيف استدعاء تلقائي لـ`_useCurrentLocation()` عبر `WidgetsBinding.instance.addPostFrameCallback` في `initState()` **لصفحة عنوان جديد فقط** (لا لتعديل عنوان محفوظ مسبقاً — كي لا يُزاح موقعه المحفوظ فور فتح الشاشة). أُعيدت كتابة `_useCurrentLocation()` بالكامل: بدل `return` الصامت، إن كانت خدمة الموقع مُعطَّلة تظهر نافذة `AlertDialog` («خدمة الموقع غير مفعّلة») بزر «فتح الإعدادات» يستدعي `Geolocator.openLocationSettings()`؛ وإن رفض المستخدم الإذن نهائياً (`deniedForever`) تظهر نافذة مماثلة («إذن الموقع مرفوض») بزر يفتح `Geolocator.openAppSettings()`؛ رفض الإذن العادي (أول مرة فقط) يبقى صامتاً عمداً (نافذة النظام نفسها كافية، لا حاجة لإزعاج إضافي). أُضيفت 5 نصوص عربية جديدة في `app_strings.dart` لهذه الحوارات. **التحقّق:** `flutter analyze` أعاد **نفس الأربع ملاحظات المعلوماتية القديمة فقط** (صفر جديدة، بعد تصحيح مخالفتَي `curly_braces_in_flow_control_structures` اللتين ظهرتا أولاً من صياغتي الأصلية للحوارَين). ⚠️ لم يُشغَّل محاكي حي (لا محاكي مُهيَّأ في هذه الجلسة، ومفتاح الخرائط الحقيقي يحتاج جهازاً/محاكياً فعلياً معتمداً على Google Play للتحقّق البصري من ظهور بلاطات الخريطة فعلاً) | `dukkanci_customer_app/android/app/build.gradle.kts`, `dukkanci_customer_app/lib/features/addresses/presentation/address_form_screen.dart`, `dukkanci_customer_app/lib/core/localization/app_strings.dart`, `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-28 | المستخدم أرسل لقطة شاشة من الصفحة الرئيسية لتطبيق Flutter تُظهر ثلاث ملاحظات: (١) زر «تصفّح المتاجر» داخل بطاقة الترحيب («كل ما تحتاجه يوصلك») لا يستجيب عند الضغط، (٢) طلب استبدال أيقونة المتجر الشفافة الظاهرة في خلفية نفس البطاقة الحمراء بشعار دكانجي الحقيقي (أرفق ملف الشعار)، (٣) طلب إزالة بانر «ماركت عمر» من شرائط المتاجر في الهيرو الرئيسي واستبداله ببانر متجر «روا لتوزيع المياه» | (١) `_WelcomeSlide` في `promo_hero.dart` كانت `Container` عادياً — النص/الخلفية البيضاء لزر «تصفّح المتاجر» بلا أي `GestureDetector`/`PressScale` يلفّه، بعكس `_StorePromoSlide` المجاورة له في نفس الملف التي تستخدم `PressScale(onTap:...)` بشكل صحيح؛ زر بصري بلا أي مستمع نقر. (٢) نفس الشريحة كانت ترسم `Icon(Icons.storefront_rounded)` عام بدل الشعار الحقيقي الموجود أصلاً في حزمة الأصول (`assets/images/logo.png`، نفس الملف المستخدَم في شاشة البداية). (٣) هيرو الرئيسية (`_HomeBody` في `home_screen.dart`) يبني شرائح المتاجر من: 4 متاجر مدفوعة بترتيب ثابت (`_paidHeroIds`) ثم تعبئة تلقائية من متاجر العروض الحقيقية (`offers.take(5)`) — ماركت عمر (id 99) يملك خصماً حقيقياً على منتجاته فيدخل هذه التعبئة التلقائية بلا أي طريقة يدوية لاستبعاده أو لفرض متجر آخر مكانه؛ تحقّق مباشر من Supabase أن متجر «روا لتوزيع المياه» (id 115، معتمد ومفتوح وله صورة غلاف) هو المقصود بالاستبدال | (١) لُفّ زر «تصفّح المتاجر» بـ`PressScale` مع `onTap: () => context.push(AppRoutes.categoryPath('all'))` (نفس مسار «عرض الكل» المستخدَم أصلاً لعدّاد المتاجر الأكثر رواجاً بالرئيسية). (٢) استُبدلت الأيقونة العامة بـ`Image.asset('assets/images/logo.png')` داخل `Opacity(opacity:0.16)` بنفس موضع/حجم العلامة المائية السابقة تقريباً (تحقّق مسبق أن الملف يحمل قناة ألفا شفافة فعلياً عبر `sharp`، فلا خلفية بيضاء طارئة حول الشعار عند التراكب). (٣) أُضيف ثابتان جديدان في `_HomeBody` (`_heroExcludedStoreId=99`، `_heroReplacementStoreId=115`) وأُعيد بناء منطق تجميع `heroStores`: شريحة متجر 115 (إن كان معتمداً وله صورة) تُدرَج مباشرة بعد المتاجر الأربعة المدفوعة وقبل أي تعبئة تلقائية، بينما استُبعد متجر 99 صراحةً من كل من قائمة العروض وقائمة الاحتياط الشائعة (`popular`) ومن التعبئة التلقائية نفسها — فلا يظهر بانره في الهيرو حتى لو استمر خصمه الحقيقي فعّالاً مستقبلاً. **التحقّق:** `flutter analyze` أعاد **نفس الأربع ملاحظات المعلوماتية القديمة فقط** (صفر جديدة). تحقّق مباشر عبر Supabase REST (خارج موصل MCP المربوط بمشروع غير ذي صلة، بنفس أسلوب الجلسات السابقة) أن متجر 115 `approval_status='approved'`، `open=true`، وله `cover_image`/`logo_image` صالحين — يضمن أن الاستبدال سيظهر فعلياً لا شريحة فارغة صامتة. ⚠️ لم يُشغَّل محاكي حي (تعديل منطق فرز/تجميع صرف + ربط زر بمسار موجود مسبقاً، مُتحقَّق منه ثابتاً بلا حاجة لتفاعل بصري إضافي) | `dukkanci_customer_app/lib/features/home/presentation/widgets/promo_hero.dart`, `dukkanci_customer_app/lib/features/home/presentation/home_screen.dart`, `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`). **مدفوع لـ`main` (`git commit` + `push`) بناءً على طلب المستخدم الصريح**
| 2026-07-28 | المستخدم أرسل لقطة شاشة من شاشة تفاصيل طلب حقيقي (`DK-5186538605#`، متجر روا لتوزيع المياه، طلب جديد فعلاً) في تطبيق Flutter، وأبلغ أن الضغط على زر الرجوع (نظام أندرويد) عند هذه الشاشة **يُخرِج المستخدم من التطبيق بالكامل** بدل الرجوع لصفحة المتجر/طلباتي | 🔴 السبب في `checkout_screen.dart` لا في `order_detail_screen.dart` نفسها: بعد نجاح إرسال الطلب، `_submit()` كانت تستدعي `context.go(AppRoutes.orderDetailPath(orderId))` — و`go()` في go_router **يستبدل مكدّس التنقّل بالكامل** (وليس يُضيف فوقه كـ`push()`)، فتصبح شاشة تفاصيل الطلب هي **جذر** المكدّس الوحيد بلا أي شاشة تحتها. `AppBar` في `order_detail_screen.dart` (سطر 30) لا يُصرِّح `leading` صراحة، بل يعتمد على سلوك Flutter الافتراضي (يُظهر سهم الرجوع تلقائياً فقط إن كان `Navigator.canPop(context)==true`) — فمع مكدّس بعنصر واحد، لا يظهر أي سهم رجوع في الشريط العلوي (يطابق تماماً لقطة المستخدم: لا سهم رجوع ظاهر)، وزر الرجوع الفعلي لنظام أندرويد يجد مكدّس التنقّل الجذري فارغاً فيُغلق التطبيق بأكمله بدل التنقّل داخله — تحقّق مباشر عبر `grep` أن `context.go(...)` (لا `push`) هو الاستدعاء الوحيد المؤدي لهذه الشاشة بعد الدفع الفعلي (شاشة «طلباتي» تستخدم `context.push` بشكل صحيح أصلاً ولا تعاني هذه العلّة) | أُبقي `context.go(AppRoutes.orders)` **قبل** الانتقال (لا فائدة من إبقاء سلة/دفع في المكدّس بعد نجاح الطلب فعلاً)، ثم أُضيف `context.push(AppRoutes.orderDetailPath(orderId))` فوقه مباشرة — فيصبح مكدّس التنقّل: «طلباتي» (جذر) ← «تفاصيل الطلب» (فوقها). هذا يمنح `Navigator.canPop` قيمة `true` فيظهر سهم الرجوع تلقائياً في `AppBar` (بلا أي تعديل على `order_detail_screen.dart` نفسها — سلوك Flutter الافتراضي يكفي)، وزر رجوع أندرويد يُرجع المستخدم فعلياً لصفحة «طلباتي» بدل إغلاق التطبيق — يحقق حرفياً طلب المستخدم «العودة إلى صفحة طلباتي». **التحقّق:** `flutter analyze` أعاد نفس الست ملاحظات المعلوماتية القديمة فقط (صفر جديدة، تحقّق فعلي بتشغيله بعد التعديل)، و`flutter test` **الكامل** (62 اختباراً عبر كل ملفات المشروع) نجح بالكامل بلا أي فشل أو انحدار. ⚠️ لم يُشغَّل محاكي حي (تعديل سطر تنقّل واحد فوق مسار طلب مُتحقَّق منه حياً في جلسات سابقة، بلا أي تغيير في منطق الطلب/الشبكة) | `dukkanci_customer_app/lib/features/checkout/presentation/checkout_screen.dart`, `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-28 | متابعة لإصلاح تنبيه OTP أعلاه (نفس اليوم): المستخدم طلب أن يعتمد حقل «عنوان التوصيل» في شاشة «إتمام الطلب» بتطبيق Flutter مربعات منفصلة (المدينة، المنطقة، المحلة، الشارع، رقم البناء [يقبل رقماً وحرفاً]، الطابق، رقم الشقة، معلومات إضافية) بدل صندوقي نص حر | 🔴 اكتُشف أثناء الفحص أن **كل هذه الحقول المهيكلة كانت موجودة فعلاً** في نموذج «عناويني» الحقيقي (`address_form_screen.dart`/`SavedAddress`، مبني في جلسة نظام العناوين التركي 2026-07-22: بحث İl/İlçe/Mahalle بالكلمات المفتاحية + Cadde/Sokak + Dış Kapı No [رقم+حرف] + Kat + İç Kapı No + Adres Tarifi) — لكن شاشة الدفع نفسها لم تكن تستخدمه، بل تعرض فقط صندوقي نص حر مستقلَّين (`_addressController`/`_addressDetailsController`) يُملآن تلقائياً من عنوان محفوظ إن اختير، أو يُكتَبان يدوياً بلا أي بنية إن لم يوجد عنوان محفوظ أصلاً | حُذف الصندوقان الحرّان بالكامل من `checkout_screen.dart` — العنوان الآن **إلزامياً** عبر النموذج المهيكل نفسه المستخدَم في «عناويني»، بلا تكرار أي كود: إن لم يوجد عنوان محفوظ بعد، بطاقة «إضافة عنوان جديد» تفتح مباشرة `AddressFormScreen` (نفس الشاشة، عبر `AppRoutes.addressForm`)؛ إن وُجدت عناوين، بطاقة ملخّص العنوان المختار (تسمية+نص العنوان الكامل+مؤشر توفر الموقع) مع زرّين: قلم «تعديل» يفتح نفس النموذج مُعبَّأً مسبقاً بعنوان الزبون بالضبط لتصحيح حقل واحد بسرعة، و«تغيير العنوان» يفتح ورقة اختيار من العناوين المحفوظة **التي أُضيف إليها سطر جديد «إضافة عنوان جديد»** (يعيد عرض النموذج فارغاً) بدل الاقتصار على قائمة العناوين الموجودة فقط. **إصلاح تمكيني مطلوب لهذا الربط:** `AddressFormScreen._save()` كانت تُغلق نفسها بـ`context.pop()` بلا قيمة عائدة (مناسب فقط لمسار «عناويني» الذي لا ينتظر نتيجة) — عُدِّلت لـ`context.pop(address)` كي تستطيع شاشة الدفع (push مع انتظار `Future<SavedAddress?>`) استلام العنوان المحفوظ فوراً وتطبيقه دون الرجوع لقائمة عناوين منفصلة؛ لا يؤثر هذا على مسارات «عناويني» الحالية لأنها لا تنتظر قيمة العودة أصلاً. حُدِّث بناء الطلب (`_placeOrder`) وشرط التحقق في `_submit` ليعتمدا على وجود `_selectedAddress` (بدل نص فارغ في الصندوق الحر)، مع اشتقاق `addressText`/`addressDetails` المُرسَلين للخادم من `SavedAddress.formattedAddress`/`detailsLine` بدل الحقلين النصيين المحذوفين — نفس الحقول التي يعرضها تفصيل الطلب وتاجر واتساب دون أي تغيير في شكل البيانات المُرسَلة. حُذفت 3 نصوص واجهة صارت ميتة (`addressTextLabel`/`addressDetailsLabel`/`addressDetailsHint`/`useSavedAddress`) وأُضيف نص جديد واحد (`changeAddress`). **التحقّق:** `flutter analyze` أعاد نفس الملاحظات المعلوماتية القديمة فقط بلا أي خطأ جديد (تحقّق فعلي بتشغيله بعد التعديل، بالتزامن مع جلسة أخرى كانت تعدّل `address_form_screen.dart`/`app_strings.dart` بالتوازي — تحقّقت أن تعديلاتي الاثنين تعايشا بلا تعارض نصي حقيقي). ⚠️ لم يُشغَّل محاكي حي (تعديل بنية/تدفّق فوق نموذج عنوان مُتحقَّق منه حياً في جلسة سابقة، بلا أي تغيير في مخطط `SavedAddress`/الخادم) | `dukkanci_customer_app/lib/features/checkout/presentation/checkout_screen.dart`, `dukkanci_customer_app/lib/features/addresses/presentation/address_form_screen.dart`, `dukkanci_customer_app/lib/core/localization/app_strings.dart`, `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-28 | المستخدم أرسل لقطة شاشة من شاشة «إتمام الطلب» بتطبيق Flutter (متجر روا لتوزيع المياه) عند ظهور حقل «أدخل رمز التحقق» مع رسالة «الرمز غير صحيح»، ولاحظ أن الزبون لا يُخبَر مسبقاً بأنه سيصله رمز تأكيد عبر واتساب، ولا يملك أي طريقة ظاهرة لتصحيح رقم الهاتف إن كان خاطئاً قبل أن يعلق في هذه الشاشة | ليس خطأ منطق OTP نفسه (يعمل حياً بالفعل، انظر [[checkout-otp]])، بل فجوة تجربة استخدام حقيقية في `checkout_screen.dart`: عند إرسال الرمز لأول مرة، الكود كان ينتقل مباشرة من `_submitting=true` إلى إظهار حقل الرمز (`_awaitingOtp=true`) بلا أي إشعار للزبون أن رسالة أُرسلت فعلاً ولا إلى أي رقم؛ وحقل الهاتف نفسه يبقى في القسم الأعلى من نفس الشاشة بلا أي رابط يعيد المستخدم إليه أو يصفّر حالة الانتظار — فتعديل الرقم يدوياً بعد الإرسال كان سيُبقي `_awaitingOtp=true` ويحاول التحقق من رمز أُرسل لرقم مختلف، منتجاً بالضبط رسالة «الرمز غير صحيح» الظاهرة في اللقطة بلا أي مخرج واضح | أُضيفت نافذة تأكيد (`AlertDialog`) تظهر **قبل** أي إرسال فعلي لرمز التحقق لأول مرة، تعرض الرقم المُدخَل صراحةً وتوضح أن رمز تحقق سيُرسَل إليه عبر واتساب، بخيارين: «تعديل الرقم» (يُغلق النافذة ويُركِّز حقل الهاتف مع تحديد كامل نصه للتصحيح السريع، بلا إرسال) أو «إرسال الرمز» (يكمل الإرسال كالمعتاد). بعد الإرسال الفعلي، أُضيف نص داخل بطاقة إدخال الرمز نفسها يذكر الرقم الذي أُرسل إليه («أرسلنا رمز التحقق عبر واتساب إلى ...») + رابطا «تغيير رقم الهاتف؟» (يُصفِّر `_awaitingOtp` ويمسح الرمز ويُعيد التركيز لحقل الهاتف — يعالج بالضبط حالة اللقطة: زبون علق على رمز خاطئ لرقم خاطئ) و«إعادة إرسال الرمز» (يستدعي `sendOtp` من جديد لنفس الرقم مع نفس معالجة تعطّل واتساب الموجودة أصلاً في `_submit()`). لم يُمسّ منطق الإرسال/التحقق نفسه ولا مساره عبر `/api/notify-order` إطلاقاً — إضافة واجهة صرفة فوق نفس الاستدعاءات. **التحقّق:** `flutter analyze` أعاد **نفس الأربع ملاحظات المعلوماتية القديمة فقط** (إهلاكات `anonKey`/`Radio` معروفة، صفر جديدة) — تحقّق فعلي بتشغيله بعد التعديل لا افتراضاً. ⚠️ لم يُشغَّل محاكي حي في هذه الجولة (تعديل تركيب/تدفّق واجهة فوق مسارات OTP مُتحقَّق منها حياً في جلسات سابقة، بلا أي تغيير في العقد الشبكي أو نموذج البيانات) | `dukkanci_customer_app/lib/features/checkout/presentation/checkout_screen.dart`, `dukkanci_customer_app/lib/core/localization/app_strings.dart`, `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-27 | المستخدم طلب إعادة تسمية متجر «مياه غلاطة الطبيعية» (id 115) إلى «روا لتوزيع المياه» مع استبدال الشعار والغلاف بصورتين أرسلهما مباشرة في المحادثة (لوغو REWA أزرق + بانر تسويقي بخلفية جبلية) | ليس خطأ — إعادة تسمية علامة تجارية. 🔴 **قيد أدوات حقيقي حُلّ بطريقة غير معتادة:** لا توجد أداة مباشرة لحفظ صور مُرفَقة في الدردشة كملفات على القرص؛ البحث في مجلدات التنزيلات/الحافظة المؤقتة لم يُظهر أي ملف (الصور لا تُكتب تلقائياً كملفات مستقلة). **الحل:** الصورتان كانتا مخزَّنتين فعلياً كبيانات `base64` مضمَّنة داخل نص محضر الجلسة (`.jsonl`) في `~/.claude/projects/...` — استُخرجتا برمجياً (قراءة الملف، فلترة كتل `type:"image"` من رسائل المستخدم، فك ترميز `base64`) وحُفظتا كملفي `webp` في scratchpad الجلسة، ثم عُولجتا: الشعار (1536×1024، خلفية شفافة فعلياً لا بيضاء — تأكَّد ببكسل الزاوية `[0,0,0,0]`) قُصَّ لحدود المحتوى الفعلي (`sharp.trim`) وأُعيد توسيطه على قماشة بيضاء 400×400 (نفس معيار شعارات المتاجر الأخرى)؛ الغلاف (1942×809 أصلي، نسبة ٢.٤:١) ضُغط لعرض ١٢٨٠px JPEG q85 دون أي قصّ إضافي. **تحقّق قصّ `object-fit:cover` قبل الاعتماد** (نفس منهجية الجلسات السابقة): محاكاة قصّ نسبة الهيرو (٤:١) أظهرت بقاء الشعار/العنوان/المنتجات/شريط المزايا كاملة (فقط الشريط السفلي للتواصل يُقصّ جزئياً، متوقَّع وغير حرج)؛ محاكاة قصّ نسبة البطاقة (~١٫٤٦:١) أظهرت أيضاً بقاء كل العناصر الأساسية ظاهرة | (١) `galatawater-data.js`: `name` → «روا لتوزيع المياه»، `logo` (الحرف الاحتياطي) → «ر»، ووصف المتجر أُعيد صياغته ليطابق الاسم الجديد (نفس المحتوى الوظيفي، فقط استبدال اسم العلامة). (٢) الشعار والغلاف استُبدلا **في نفس المسارين القديمين** (`logo.png`, `cover.jpg`) فلا حاجة لتعديل أي مرجع مسار في أي ملف آخر. (٣) نفس تحديثي الاسم/الوصف طُبِّقا مباشرة على صف `stores` (id 115) في Supabase عبر REST بمفتاح service role (نفس قيد الجلسات السابقة: موصل Supabase MCP هنا مربوط بمشروع شخصي غير ذي صلة). لم يُلمس الـslug (`galata-natural-water-esenyurt` بقي كما هو لتفادي كسر أي رابط/QR سابق). **التحقّق (حي عبر preview محلي):** عنوان التبويب وعنوان الصفحة كلاهما «روا لتوزيع المياه» فوراً، الغلاف والشعار كلاهما HTTP 200 وحُمِّلا فعلياً بأبعادهما الصحيحة (1280×533 و400×400، `complete:true`)، `/category/mineral-water` يعرض بطاقة المتجر بالاسم الجديد، صفر أخطاء كونسول. `npm run check:versions` ناجح (لم يتغيّر app.js/styles.css فعلياً في هذه المهمة) | `galatawater-data.js`, `index.html`, `sw.js`, `assets/photos/galatawater/logo.png` (استبدال في مكانه), `assets/photos/galatawater/cover.jpg` (استبدال في مكانه), `CLAUDE.md` + عمودا `stores.name`/`stores.description`/`stores.logo` للمتجر id 115 في Supabase | نعم — `npm run check:versions` ناجح (app.js/styles.css بلا تغيير؛ رُفع فقط `galatawater-data.js?v=2→3` و`CACHE v280→281` لأن محتوى الملف المُجمَّع تغيّر).
**تحديث لاحق (نفس اليوم):** بطلب المستخدم الصريح، بُدِّل اسم المنتجين أيضاً («غلاطة» → «روا» في الاسم والوصف) في `galatawater-data.js` **و**صفَّي `products` (id 1950001/1950002) في Supabase، مع رفع `galatawater-data.js?v=3→4` و`CACHE v281→282`. تحقّق حي عبر preview محلي أن صفحة المتجر تعرض المنتجين بالاسم الجديد («جالون مياه روا الطبيعية 18-19 لتر»، «كرتون مياه روا المعدنية...») بلا أي أخطاء كونسول. **مدفوع لـ`main`** بناءً على طلب المستخدم الصريح.
**تحديث ثانٍ (نفس اليوم):** المستخدم طلب استبدال منتج الدمجانة (id 1950001، كان «جالون مياه روا الطبيعية 18-19 لتر» بسعر 170) بمنتج علامة تجارية مختلفة «مياه مايا داغ دمجانة 19 لتر» بصورة حقيقية أرسلها مباشرة في الدردشة، بسعر **150 ل.ت واصل الباب** ضمن المجمعات الثمانية المُسمّاة — نفس منهجية استخراج الصور المُرفَقة من محضر الجلسة `.jsonl` (لا أداة مباشرة لحفظها كملف) المُستخدمة سابقاً في نفس اليوم. الصورة (500×500، خلفية بيضاء نظيفة، بلا حاجة قصّ/تسطيح) استبدلت `p1.jpg` في مكانه. **لم يتطلب أي تعديل على إعدادات التوصيل** — المجمعات الثمانية مُعرَّفة أصلاً بـ`fee:0` (`namedZones`)، فتعيين سعر المنتج مباشرة إلى 150 يحقق «١٥٠ ل.ت واصل الباب» تلقائياً لتلك المناطق (السعر الإجمالي = سعر المنتج + توصيل صفر)، بينما خارجها يُضاف رسم المسافة العادي (٢٠ ل.ت/كم) فوق الـ150 كما هو المعمول به لبقية المنتج الآخر. حُدِّث الاسم/الوصف/السعر/الوحدة («دمجانة» بدل «جالون») في `galatawater-data.js` وصف `products` (id 1950001) في Supabase معاً، ورُفع `galatawater-data.js?v=4→5` و`CACHE v282→283`. **التحقّق (حي عبر preview محلي):** صفحة المتجر تعرض «مياه مايا داغ دمجانة 19 لتر / ١٥٠ ل.ت / دمجانة» بشكل صحيح، الصورة الجديدة تُخدَّم HTTP 200 فعلياً (`fetch` مباشر — العنصر نفسه كان لا يزال خارج نطاق التحميل الكسول `loading=lazy` وقت الفحص)، صفر أخطاء كونسول. `npm run check:versions` ناجح. **مدفوع لـ`main`** (commit `fb4258a4`) بناءً على طلب المستخدم الصريح — تحقّق حي بعد النشر أن `p1.jpg` والاسم/السعر الجديدان يُخدَّمان فعلاً على الموقع الحي.
**تحديث ثالث (نفس اليوم):** المستخدم طلب استبدال المنتج الثاني (id 1950002، كان «كرتون مياه روا المعدنية - أكواب 180 مل (60 كأس)» بسعر 180) بمنتج علامة «ابانت» — «مياه ابانت كاسات 180 مل عدد 60» بسعر **190 ل.ت**، بصورة كرتون حقيقية أرسلها مباشرة (نفس منهجية استخراج الصورة من محضر الجلسة). حُدِّث الاسم/الوصف/السعر في `galatawater-data.js` وصف `products` (id 1950002) في Supabase معاً، واستُبدلت `p2.jpg` في مكانها، ورُفع `galatawater-data.js?v=5→6` و`CACHE v283→284`. **التحقّق (حي عبر preview محلي):** صفحة المتجر تعرض المنتجين معاً («مياه مايا داغ دمجانة 19 لتر / ١٥٠ ل.ت» و«مياه ابانت كاسات 180 مل عدد 60 / ١٩٠ ل.ت»)، `p2.jpg` الجديدة تُخدَّم HTTP 200 فعلياً، صفر أخطاء كونسول. `npm run check:versions` ناجح | `galatawater-data.js`, `index.html`, `sw.js`, `assets/photos/galatawater/p1.jpg` (استبدال في مكانه — التحديث الثاني), `assets/photos/galatawater/p2.jpg` (استبدال في مكانه — التحديث الثالث), `CLAUDE.md` + صفَّا `products` id 1950001/1950002 في Supabase (كل تحديث على حدة) | نعم — `npm run check:versions` ناجح لكل تحديث. **لم يُنفَّذ `git commit`/push بعد للتحديث الثالث — بانتظار تأكيد المستخدم**.
**تحديث رابع (نفس اليوم):** المستخدم طلب تغيير رابط صفحة المتجر ليستخدم «rewa» بدل «غلاطة» (يعني الاسم الإنجليزي القديم `galata` في الـslug). 🔴 **اكتشاف معماري أثناء التنفيذ:** رابط المتجر الفعلي لا يُبنى من `store-slugs.js` وحده كما قد يُفترض — `storeParam()`/`resolveStoreSlug()` في `app.js`/`lib/store-slug.js` يعطيان أولوية قصوى لعمود **`stores.slug` في Supabase** (سطر: «admin-set DB slug > legacy static map (store-slugs.js) > auto-generated»)، وSupabase كان لا يزال يحمل القيمة القديمة `galata-natural-water-esenyurt` رغم أنها مطابقة نصياً لِما في `store-slugs.js` — تعديل الملف المُجمَّع وحده كان سيبقى بلا أي أثر على الرابط الفعلي المُولَّد (SPA/SSR/sitemap الثلاثة تقرأ نفس الأولوية). عُدِّل الاثنان معاً: `store-slugs.js` (`115: "rewa-water-esenyurt"`) **و**عمود `stores.slug` في Supabase مباشرة إلى نفس القيمة. رُفع `store-slugs.js?v=265→266` و`CACHE v284→285`. **تحذير صريح للمستخدم:** هذا يخالف قاعدة «الـslug لا يُعاد تسميته بعد النشر (يكسر الروابط الحيّة)» الموثَّقة في تعليق `store-slugs.js` نفسه — قُبل الاستثناء لأن المتجر عمره يومان فقط ورُفض `galata-natural-water-esenyurt` القديم عمداً (يعيد استخدام اسم العلامة الميتة)، بنفس سابقة توحيد slug متجر الدوماني (id 84) في 2026-07-12. **الرابط القديم أصبح 404 فعلياً** (تحقّق مباشر: `/store/galata-natural-water-esenyurt` يعيد عنوان الصفحة الرئيسية العام لا صفحة المتجر) — لا إعادة توجيه، لأن المتجر جديد جداً وبلا أي طلبات/تسويق مرتبط بالرابط القديم بعد. **التحقّق (حي عبر preview محلي):** `/store/rewa-water-esenyurt` يعرض صفحة المتجر الصحيحة، `/store/115` الرقمي لا يزال يعمل (شبكة أمان)، النقر الفعلي على بطاقة المتجر في `/category/mineral-water` يُنتج فعلياً `location.pathname === "/store/rewa-water-esenyurt"` (تحقّق `pushState` حي لا افتراض)، صفر أخطاء كونسول. `npm run check:versions` ناجح | `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + عمود `stores.slug` للمتجر id 115 في Supabase | نعم — `npm run check:versions` ناجح. **مدفوع لـ`main`** (commit `15e4d432`) بناءً على طلب المستخدم — تحقّق حي بعد النشر أن الرابط الجديد HTTP 200 والرابط القديم يعرض «الصفحة غير موجودة» + `<meta name="robots" content="noindex">` في استجابة الخادم الخام (حالة HTTP تبقى 200 حسب تصميم البنية SSR/SPA للموقع بالكامل — لا كود 404 حرفي لأي رابط متجر غير موجود، سلوك متوقَّع وليس خللاً).
**تحديث خامس (نفس اليوم):** المستخدم طلب تصنيفاً أدق للمنتجين بدل تصنيف «مياه معدنية» المشترك لكليهما (نفس تصنيف المتجر نفسه، فكان تكراراً عديم الفائدة داخل صفحة المتجر). حُدِّث `category` لكل منتج في `galatawater-data.js` وصفَّي `products` في Supabase: منتج الدمجانة → «دمجانات المياه»، منتج الكرتون → «أكواب المياه». رُفع `galatawater-data.js?v=6→7` و`CACHE v285→286`. **التحقّق (حي عبر preview محلي):** صفحة المتجر أصبحت تعرض شريط تصنيفات فرعي حقيقي («الكل ٢ / دمجانات المياه ١ / أكواب المياه ١») بدل تصنيف واحد مكرَّر، كل منتج يحمل بادئة تصنيفه الصحيحة في البطاقة، صفر أخطاء كونسول. `npm run check:versions` ناجح | `galatawater-data.js`, `index.html`, `sw.js`, `CLAUDE.md` + عمود `products.category` لصفَّي id 1950001/1950002 في Supabase | نعم — `npm run check:versions` ناجح. **مدفوع لـ`main`** (commit `62ec940b`) بناءً على طلب المستخدم — تحقّق حي بعد النشر أن التصنيفين الجديدين يظهران فعلاً.
**تحديث سادس (نفس اليوم):** المستخدم طلب ضبط ساعات العمل الحقيقية («من 9 صباحاً إلى 10 ليلاً كل الأيام») بدل النص العام السابق («التوصيل يومياً — تواصل عبر واتساب لتأكيد الطلب») الذي لم يكن قابلاً للتحليل الآلي أصلاً (بلا نطاق ساعات رقمي، فتُعامَل شارة «مفتوح/مغلق الآن» بحالة غير معروفة افتراضياً). كُتب النص بالصيغة القياسية المتَّبعة في المشروع لضمان تحليلها الصحيح بواسطة `storeHoursStatus()`/`parseHoursTimeRange()` في `app.js` (تحقّق مسبق من منطق التحليل قبل الكتابة، لا تخميناً): **«يومياً 9:00 ص – 10:00 م»**. حُدِّث الحقل في `galatawater-data.js` وعمود `stores.hours` في Supabase معاً، ورُفع `galatawater-data.js?v=7→8` و`CACHE v286→287`. **التحقّق (حي عبر preview محلي):** قسم «أوقات العمل» يعرض النص الجديد حرفياً، شارة «مفتوح ويستقبل الطلبات» أعلى الصفحة محسوبة بشكل صحيح من النطاق الجديد (9:00–22:00)، صفر أخطاء كونسول. `npm run check:versions` ناجح | `galatawater-data.js`, `index.html`, `sw.js`, `CLAUDE.md` + عمود `stores.hours` للمتجر id 115 في Supabase | نعم — `npm run check:versions` ناجح. **مدفوع لـ`main`** (commit `b9263363`) بناءً على طلب المستخدم — تحقّق حي بعد النشر أن النص الجديد يُخدَّم فعلاً.
**تحديث سابع (نفس اليوم):** المستخدم طلب **إضافة** منتج ثالث (لا استبدال هذه المرة) — «مضخة يدوية لسحب مياه الدمجانة» بسعر 150 ل.ت شامل التوصيل، بصورة حقيقية أرسلها مباشرة (نفس منهجية استخراج الصور من محضر الجلسة). صُنِّف تصنيفاً جديداً مستقلاً **«إكسسوارات المياه»** (متسقاً مع تعميق دقة التصنيفات المطلوب في التحديث الخامس) بدل إلحاقه بأحد التصنيفين الموجودين. أُضيف كعنصر ثالث في `galatawaterFullCatalog` (المعرّف يُحسَب تلقائياً `1950001+index` فأصبح **1950003** بلا تعديل يدوي إضافي)، مع تحقّق مسبق مباشر من Supabase أن هذا المعرّف غير مستخدَم (`ABORT` احترازي معتاد قبل أي `INSERT` على هذا المشروع). أُدرِج صف `products` جديد مطابق تماماً (لا `UPDATE` على صف قائم هذه المرة) + صورة `p3.jpg` جديدة في المجلد. رُفع `galatawater-data.js?v=8→9` و`CACHE v287→288`. **التحقّق (حي عبر preview محلي):** صفحة المتجر أصبحت تعرض «٣ من ٣ منتجاً» وشريط تصنيفات بثلاث فئات («الكل ٣ / دمجانات المياه ١ / أكواب المياه ١ / إكسسوارات المياه ١»)، صورة `p3.jpg` تُخدَّم HTTP 200 فعلياً، صفر أخطاء كونسول. `npm run check:versions` ناجح | `galatawater-data.js`, `index.html`, `sw.js`, `assets/photos/galatawater/p3.jpg` (جديد), `CLAUDE.md` + صف `products` جديد id 1950003 في Supabase | نعم — `npm run check:versions` ناجح. **مدفوع لـ`main`** (commit `e578e733`) بناءً على طلب المستخدم — تحقّق حي بعد النشر أن المنتج الثالث ظاهر فعلاً.
**تحديث ثامن (نفس اليوم):** المستخدم طلب إضافة دمجانة رابعة من علامة كتبها نصاً «ليلا» بسعر 160 ل.ت واصل الباب، بصورة حقيقية أرسلها مباشرة. 🔴 **تباين ظاهري بين النص المكتوب والصورة المرفقة، صحّحه المستخدم صراحة:** الملصق على الدمجانة في الصورة يقرأ **«Lita»** — فاقترحتُ ابتداءً «ليتا» (التحويل الصوتي الحرفي) بدل «ليلا» كما كتب المستخدم أول مرة، لكن المستخدم أكّد صراحة في رسالة تالية «مياه ليلا وليس لينا» — أي أن الاسم التجاري المقصود فعلاً هو **«ليلا»** (وليس تحويلي «ليتا» الذي بدا له كـ«لينا» بصرياً). اعتُمد **«ليلا»** نهائياً بقرار المستخدم الصريح بعد التنبيه، ونُقلت هذه التسمية لـ`galatawaterFullCatalog` وصفّ `products` معاً. أُضيف كعنصر رابع (معرّف تلقائي **1950004**، تحقّق مسبق من Supabase أنه غير مستخدَم)، بتصنيف «دمجانات المياه» (نفس تصنيف منتج مايا داغ — أول تصنيف يحوي أكثر من منتج واحد). رُفع `galatawater-data.js?v=9→10` و`CACHE v288→289` (لا حاجة رفع إضافي عند تصحيح الاسم لاحقاً لأن التغيير السابق لم يُرفع بعد). **التحقّق (حي عبر preview محلي):** صفحة المتجر تعرض «٤ من ٤ منتجاً» مع اسم «دمجانة مياه ليلا الطبيعية 19 لتر» الصحيح، تصنيف «دمجانات المياه» يحوي منتجين (مايا داغ + ليلا)، صورة `p4.jpg` تُخدَّم HTTP 200 فعلياً، صفر أخطاء كونسول. `npm run check:versions` ناجح | `galatawater-data.js`, `index.html`, `sw.js`, `assets/photos/galatawater/p4.jpg` (جديد), `CLAUDE.md` + صف `products` جديد id 1950004 في Supabase | نعم — `npm run check:versions` ناجح. **مدفوع لـ`main`** (commit `3faa5aa4`) بناءً على طلب المستخدم — تحقّق حي بعد النشر أن المنتج الرابع «ليلا» يظهر فعلاً.
**تحديث تاسع (نفس اليوم):** المستخدم أبلغ أن صورة «دمجانة ليلا» ظهرت بشكل غير صحيح على الموقع، وطلب تصغيرها وتصحيح عرضها. 🔴 **السبب الجذري:** `p4.jpg` الأصلية كانت 323×619 (نسبة 0.522، ضيقة وطويلة جداً بلا أي هامش أبيض — الصورة مقصوصة حتى حوافها تماماً)، بينما كل حاويات عرض صورة المنتج في الموقع مربّعة أو شبه مربّعة (`.product-card__image` نسبة 1.15، `.product-gallery > img`/`.product-page-media img` نسبة 1:1) وتستخدم `object-fit:cover`. حساب القصّ الفعلي أظهر أن حاوية البطاقة (1.15) كانت تقصّ ~27% من الأعلى و27% من الأسفل من هذه الصورة الضيقة تحديداً — يقطع الغطاء الأزرق أعلى الدمجانة والقاعدة أسفلها، بعكس صور المنتجات الأخرى الأقرب للمربّع (p1/p3 مربّعة أصلاً، p2 نسبة 0.667 تُقصّ ~21% فقط وتبقى مقبولة بصرياً). هذا يطابق تماماً شكوى المستخدم «تصغير الصورة» — الحل الصحيح تقنياً هو تصغير الدمجانة نفسها داخل إطار أكبر (هامش أبيض)، لا تصغير ملف الصورة. **الإصلاح:** أُعيد بناء `p4.jpg`: تصغير الدمجانة لارتفاع 580px (من أصل 619) ووضعها في منتصف قماشة بيضاء مربّعة 700×700 (هامش ≥45px فوق وتحت، محسوب مسبقاً بدقة رياضية ليتجاوز نسبة القصّ 1.15: `1-1/1.15=13%` من 700 = ~45.6px). **تحقّق قصّ فعلي قبل الاعتماد** (نفس منهجية كل الجلسات السابقة): محاكاة برمجية لقصّ 1.15 على الصورة الجديدة أظهرت الدمجانة كاملة من الغطاء حتى القاعدة بهامش أبيض متبقٍّ واضح أعلى وأسفل. لم يتغيّر مسار الصورة (`p4.jpg` نفسه استُبدل في مكانه) فلا حاجة لتعديل `galatawater-data.js`/Supabase (حقل `image` كما هو) ولا رفع رقم إصدار (الصورة غير مُدرَجة أصلاً في `APP_SHELL` بـ`sw.js`، ونفس سياسة «صور فقط = لا رقم إصدار» المتَّبعة في جلسات سابقة). **التحقّق (حي عبر preview محلي):** تأكيد مباشر من القرص (وليس فقط `fetch` من نفس التبويب الذي كان يحمل نسخة قديمة مخزَّنة مؤقتاً محلياً من فحوصات سابقة في نفس الجلسة) أن الملف الفعلي 700×700 بالمحتوى الصحيح، صفر أخطاء كونسول | `assets/photos/galatawater/p4.jpg` (استبدال في مكانه), `CLAUDE.md` | لا ينطبق (صور فقط) — **مدفوع لـ`main`** (commit `e1ad0e9f`) بناءً على طلب المستخدم — تحقّق حي بعد النشر أن الصورة المصحَّحة (700×700) تُخدَّم فعلياً.
**تحديث عاشر (نفس اليوم):** المستخدم طلب إضافة منتج خامس — «مياه كاردلين 200 مل عدد 72» بسعر 225 ل.ت واصل، بصورة صندوق حقيقية أرسلها مباشرة. صُنِّف تصنيفاً جديداً مستقلاً **«زجاجات المياه»** (بدل إلحاقه بـ«أكواب المياه» رغم تشابه الحجم — عبوات كاردلين زجاجات مغلقة بغطاء حقيقية لا أكواب مفتوحة، فالتمييز أدق تطبيقاً لنفس مبدأ التحديث الخامس). الصورة المصدر (360×369، شبه مربّعة تماماً) لم تحتج أي حشو/تصغير هذه المرة (خلافاً لصورة دمجانة ليلا) — تحقّق مسبق من نسبة القصّ 1.15 أظهر فقداناً طفيفاً جداً فقط، فاعتُمدت كما هي بعد تسطيح قناة الألفا (كانت مبهمة بالكامل أصلاً، بلا شفافية فعلية). أُضيف كعنصر خامس في `galatawaterFullCatalog` (معرّف تلقائي **1950005**، تحقّق مسبق من Supabase أنه غير مستخدَم). رُفع `galatawater-data.js?v=10→11` و`CACHE v289→290`. **التحقّق (حي عبر preview محلي):** صفحة المتجر أصبحت تعرض «٥ من ٥ منتجاً» وشريط تصنيفات بخمس فئات («الكل ٥ / دمجانات المياه ٢ / أكواب المياه ١ / إكسسوارات المياه ١ / زجاجات المياه ١»)، صورة `p5.jpg` تُخدَّم HTTP 200 فعلياً، صفر أخطاء كونسول. `npm run check:versions` ناجح | `galatawater-data.js`, `index.html`, `sw.js`, `assets/photos/galatawater/p5.jpg` (جديد), `CLAUDE.md` + صف `products` جديد id 1950005 في Supabase | نعم — `npm run check:versions` ناجح. **مدفوع لـ`main`** (commit `2fb29dc9`) بناءً على طلب المستخدم — تحقّق حي بعد النشر أن المنتج الخامس يظهر فعلاً.
**تحديث حادي عشر (نفس اليوم):** المستخدم طلب ميزتين جديدتين تخصّان الظهور العام لمتجر روا على كامل الموقع (لا تعديل بيانات متجر بعد الآن، بل منطق `app.js` نفسه): (١) عند وجود العميل ضمن 2 كم فعلياً من المتجر، يتصدّر المتجر صفحة «/stores» (عنوانها الفعلي «المتاجر القريبة منك»)؛ (٢) نتائج البحث عن «ماء / مياه / دمجانة ماء / ماء 19 لتر» إلخ تتصدّرها منتجات روا. 🔴 **اكتشافان حقيقيان أثناء التحقّق الحي كشفا أن الحل الأول (فرز بعد التطابق) لم يكن كافياً إطلاقاً:** (أ) `getMatchingProducts()` تتوقف فور جمع 60 نتيجة (`limit` الافتراضي) أثناء مسحها لمصفوفة `products` (~13,401 منتجاً، ومنتجات روا قرب نهايتها تماماً لأن `id` يبدأ بـ1950001) — فأي بحث شائع («مياه»، «ماء») يجمع 60 تطابقاً من متاجر أخرى **قبل وصول الفحص لمنتجات روا أصلاً**، فتُسقَط بصمت تماماً بصرف النظر عن أي فرز لاحق (تحقّق حي قاطع: صفر نتيجة روا لبحث «مياه» رغم تطابق اسم المتجر نفسه). **الإصلاح الجذري:** أعيدت كتابة الدالة بدلوين — دلو أولوية (`WATER_SEARCH_PRIORITY_STORE_IDS=[115]`) يُجمَّع بلا أي حد أقصى (لن يفلت من الفحص مهما كان موقعه في المصفوفة)، ودلو عادي يتوقف عند الحد الافتراضي — ثم يُدمَجان (الأولوية أولاً) ويُقصّان للحد النهائي. (ب) لتغطية «ماء» (بلا هاء، لا يطابق «مياه» نصياً — كلمتان مختلفتا الحروف فعلياً) و«دمجانة ماء»/«ماء 19 لتر»، أُضيفت مرادفات `product_synonyms` («ماء» + مرادف نوعي لكل من المنتجات الخمسة) عبر REST مباشرة (`status:'done'` للتفعيل الفوري بلا انتظار مراجعة إدارية). لكن التحقّق الحي بعد الإضافة أظهر أن الفهرس المحمَّل في المتصفح (`productSynonymIndex`) **افتقد الصفوف الخمسة الجديدة تحديداً** (3635 من أصل 3640 صفاً حقيقياً غير فارغ) — 🔴 **علة حقيقية ثانية مستقلة تماماً، غير خاصة بروا، تصيب أي صف جديد يُضاف لجدول `product_synonyms`:** `loadSynonymIndex()` في `app.js` كانت تُصفِّح الجدول بـ`.range(from, from+999)` **بلا أي `.order()`** — وPostgreSQL/PostgREST لا يضمنان ترتيب مسح ثابتاً بين استدعاءات `.range()` منفصلة بلا ترتيب صريح، فيمكن لصفحتين متتاليتين أن تتخطيا/تُكرِّرا صفوفاً فعلياً (خصوصاً مع كتابة متزامنة كإدراجي للصفوف الخمسة أثناء نفس الجلسة). **الإصلاح:** أُضيف `.order("product_id")` قبل `.range()` — إصلاح عام يضمن تحميل الفهرس كاملاً دائماً لأي منتج مستقبلي، وليس فقط لروا. (٢) **الأولوية الجغرافية:** آلية جديدة مستقلة عن `PAID_PRIORITY_STORE_IDS` الحالية (تلك دائمة وغير مشروطة لأربعة متاجر دافعة؛ هذه مشروطة بمسافة حقيقية) — `PROXIMITY_PRIORITY_RULES=[{storeId:115,maxKm:2}]` + `proximityPriorityRank()` (يستخدم `branchDistanceKm()` الموجودة أصلاً، تعمل لأي متجر له إحداثيات لا فقط مجموعات الفروع رغم اسمها) + `sortStoresByProximityPriority()`، مُطبَّقة في `getFilteredStores()` **فقط ضمن العرض الافتراضي «الأنسب لك»** (لا تتجاوز فرزاً صريحاً كـ«الأقرب»/«الأعلى تقييماً» يختاره الزائر بنفسه، ونفس فلسفة `PAID_PRIORITY_STORE_IDS`) **وتحت المتاجر الأربعة المدفوعة دائماً** (لا يغيّر التزام الدفع القائم). أُضيف أيضاً `sortStoresByWaterSearchPriority()` (بلا شرط مسافة) يُطبَّق فقط أثناء بحث فعلي (`state.search` غير فارغ) على قائمة بطاقات المتاجر المطابقة أسفل نتائج المنتجات في نفس الصفحة. **التحقّق (حي بالكامل عبر console المتصفح المحلي، محاكاة سيناريوهات متعددة):** بحث «مياه»/«ماء»/«دمجانة ماء»/«ماء 19 لتر» الأربعة جميعاً تُصدِّر منتجات روا الخمسة أولاً (بعد إصلاح العلّتين، لا قبله) ضمن نتائج حقيقية ≥60 من كل الموقع؛ بطاقات المتاجر أسفل النتائج تُصدِّر روا أيضاً؛ محاكاة موقع = إحداثيات المتجر نفسه (0 كم) → روا في المرتبة 5 (بعد الأربعة المدفوعة مباشرة) من أصل 80 متجراً؛ محاكاة موقع بعيد (>20 كم) → روا يعود لترتيبه الطبيعي (المرتبة 60) بلا أي أولوية مصطنعة؛ فرز صريح «الأقرب»/«الأعلى تقييماً» غير متأثرَين بقاعدتي الأولوية الجديدتين (تحقّق مباشر: تقييم صريح وضع روا في المرتبة 79 من 80 — لا كسر لاختيار الزائر)؛ بحث غير متعلق («بيتزا») أعاد صفر تسرّب لمنتجات روا؛ بلا موقع محدَّد أو بلا بحث نشط، ترتيب الأربعة المدفوعة يبقى كما هو تماماً (فحص انحدار صريح). صفر أخطاء كونسول عبر الصفحة الرئيسية و`/stores` وصفحة متجر روا. `node --check app.js` سليم و`npm run check:versions` ناجح (رُفع `app.js?v=361→362` و`CACHE v290→291` لأن `app.js` تغيّر فعلياً هذه المرة — أول تعديل على الملف المشترك خلال سلسلة تعديلات هذا المتجر اليوم كلها). ⚠️ **نطاق الأثر أوسع من متجر واحد:** إصلاح `loadSynonymIndex()` يعالج علّة صامتة كانت ستُسقط أي مرادف بحث جديد يُضاف لأي منتج على المنصة كلها، وقاعدتا الأولوية الجديدتان (الجغرافية وبحث الماء) تُطبَّقان على كامل صفحة `/stores` والبحث الرئيسي، لا فقط على صفحة متجر روا | `app.js`, `index.html`, `sw.js`, `CLAUDE.md` + 5 صفوف `product_synonyms` (id 1950001-1950005) في Supabase | نعم — `npm run check:versions` ناجح (app.js v361→362، CACHE v290→291؛ styles.css/store-slugs.js بلا تغيير). **مدفوع لـ`main`** (commit `6faddb6c`) بناءً على طلب المستخدم — تحقّق حي بعد النشر على `app.js?v=362` أن بحث «مياه» يُصدِّر منتجات روا فعلاً + جولة انحدار شاملة على `/` و`/stores` و`/offers` و`/dalil` و`/ask-dukkanci` و`/category/restaurants` ومتجر آخر مدفوع (باشا بيتزريا) بصفر أخطاء كونسول عبر كل الصفحات. **تحقّق إضافي لاحق (نفس اليوم):** بطلب المستخدم «تحقق أن روا يظهر أول في نتائج بحث دمجانة على /stores الحي» — تأكيد حي أن بحث «دمجانة» يعرض 3 منتجات، جميعها من روا حصراً ومتصدّرة («٣ منتج / نتائج البحث عن «دمجانة»»)؛ قسم بطاقات المتاجر أسفلها فارغ («٠ متجر») لأن كلمة «دمجانة» موجودة في أسماء المنتجات فقط لا في اسم/وصف المتجر نفسه — سلوك متوقَّع وليس خللاً، ونتائج المنتجات (الأهم والأولى في الصفحة) صحيحة تماماً.
**تحديث ثانٍ عشر (نفس اليوم):** المستخدم طلب تطبيق أولويتَي القرب/بحث الماء في تطبيق Flutter أيضاً بعد أن تحقّقتُ أنهما **غير موجودتين إطلاقاً هناك** (كود منفصل تماماً عن `app.js`؛ آلية «المتاجر المدفوعة» الوحيدة المنقولة `_paidHeroIds` مقصورة على شريط الهيرو في الرئيسية فقط، لا تُطبَّق على أي قائمة متاجر أو بحث). أُنشئ ملف مشترك جديد `lib/core/utils/store_priority.dart` (`proximityPriorityStoreId=115`, `proximityPriorityMaxKm=2.0`, `waterSearchPriorityStoreIds={115}`, `sortStoresByProximityPriority()`, `sortStoresByWaterSearchPriority()` — نفس المنطق العربي في `app.js` بالحرف، لا إعادة اختراع) ووُصل في موضعين: (١) `search_screen.dart`: `_filtered` (قائمة المتاجر الافتراضية + نتائج بحث المتاجر) — القرب يُطبَّق فقط في الفرع الافتراضي (لا يتجاوز فلتري «الأقرب»/«الأعلى تقييماً» الصريحين اللذين يختارهما الزائر يدوياً عبر رقائق الفلترة)، وبحث الماء يُطبَّق إضافياً فوقه فقط أثناء بحث فعلي؛ و`_productHits` (نتائج بحث المنتجات) — فرز مباشر بعد الدمج (اسم+مرادف). (٢) `category_screen.dart` (قائمة «كل المتاجر»، مكافئ `/stores` الفعلي في Flutter): القرب يُطبَّق **بعد** فرز مفتوح/تقييم الموجود أصلاً (فرز مستقر لا يكسر ترتيب الباقين) — بلا أي أثر عملي إلا على `categoryKey=='all'` (روا لا ينتمي لأي تصنيف آخر فيبقى غائباً عن قوائمها بصرف النظر). 🔴 **فرق معماري مهم عن الموقع لم يتطلّب أي إصلاح إضافي هذه المرة:** بحث المنتجات في Flutter خادمي (استعلام Supabase مباشر `ilike` على اسم/تصنيف المنتج فقط، بلا اسم المتجر) لا عميلي كـ`getMatchingProducts` في `app.js` — فلا وجود لعلة «التوقّف المبكر بعد 60 نتيجة يُسقط متاجر لاحقة بصمت» هنا أصلاً (طبيعة الاستعلام مختلفة تماماً)، وأسماء منتجات روا الخمسة تحوي «مياه» حرفياً في حقل `name` نفسه (لا حاجة الاعتماد على اسم المتجر كما في الموقع)؛ ومرادف «ماء» الذي أُضيف سابقاً اليوم لجدول `product_synonyms` (لتغطية بحث الموقع) **يخدم Flutter تلقائياً بلا أي عمل إضافي** لأن `searchProductsBySynonym()` في `store_repository.dart` يقرأ نفس الجدول. **التحقّق:** `flutter analyze` نظيف (نفس 4 ملاحظات معلوماتية قديمة فقط، صفر جديد). أُضيف اختبار وحدة جديد `test/store_priority_test.dart` (7 اختبارات: تصدّر ضمن 2كم، عدم التصدّر خارج 2كم، صفر أثر بلا موقع عميل، صفر أثر بلا إحداثيات للمتجر، لا تصدّر لأي متجر آخر مهما قرب، تصدّر بحث الماء، عدم اختراع متجر غير مطابق) — كلها نجحت. `flutter test` **الكامل** (53 اختباراً عبر كل ملفات المشروع، يشمل الاختبارات الجديدة) نجح بالكامل بلا أي فشل أو انحدار. ⚠️ **لم يُشغَّل محاكي حي** (لا تغيير في بنية الواجهة، فقط منطق فرز صرف مُتحقَّق منه بالكامل عبر اختبارات وحدة دقيقة تغطي نفس السيناريوهات المُختبَرة حياً على الموقع) — إفصاح صريح بدل الادّعاء | `dukkanci_customer_app/lib/core/utils/store_priority.dart` (جديد), `dukkanci_customer_app/lib/features/search/presentation/search_screen.dart`, `dukkanci_customer_app/lib/features/category/presentation/category_screen.dart`, `dukkanci_customer_app/test/store_priority_test.dart` (جديد), `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-26 | متابعة لإضافة متجر «مياه غلاطة الطبيعية» (السطر التالي أدناه): المستخدم طلب إلغاء «رسوم التوصيل الثابتة التي هي 150 ليرة» لهذا المتجر تحديداً، مؤكِّداً أن السعر للمجمعات السكنية الثمانية المُضافة يجب أن يبقى بلا أي تكلفة توصيل إضافية | 🔴 **لم يكن هناك أي رسم ثابت 150 مُعرَّف لهذا المتجر إطلاقاً** — الرسوم داخل المجمعات الثمانية (`namedZones`, `fee:0`) كانت سليمة تماماً من البداية وغير متأثرة، لكن اكتُشف بالبحث في `app.js` (`normalizeDeliveryFee()`, سطر 2028) أن **كل متجر** بوضع `mode:"distance"` يخضع لسياسة عامة صامتة: `Math.max(150, Math.ceil(rawFee/50)*50)` — أي حدّ أدنى **150 ل.ت** مفروض تلقائياً على أي عنوان خارج المجمعات المُسمّاة، مهما كانت المسافة قصيرة (حتى لو كان الرسم الفعلي 20-40 ل.ت فقط) — هذا ما رآه المستخدم فعلياً عند اختبار عنوان قريب لكن غير مطابق لأي من المجمعات الثمانية حرفياً في النص. **قيد إضافي مكتشَف أثناء الإصلاح:** رسوم/إعدادات التوصيل لكل متجر مُخزَّنة فقط في الملف المُجمَّع + مُظلَّلة بـ`localStorage` لكل متصفح (`loadDeliverySettings()`: القيمة المحفوظة محلياً **تطغى بالكامل** على القيمة المجمَّعة الجديدة) — أي متصفح فتح صفحة هذا المتجر قبل هذا الإصلاح (ومنهم على الأرجح متصفح المستخدم نفسه أثناء اختباره) يحمل نسخة مجمَّدة بلا الحقل الجديد، فيبقى الحد الأدنى ساري المفعول رغم رفع الملف — نفس فئة خطأ `khawali40`/`restaurantRate20` الموثَّقة سابقاً في هذا الملف | أُضيف حقل جديد اختياري `noFeeFloor:true` داخل `galatawaterDeliverySettings` (لا يؤثر على أي متجر آخر — الحقل غير معرَّف لهم فيبقون خاضعين للحد الأدنى 150 كما كان)، مع تعديل سطر واحد في `estimateDeliveryQuote()`: `fee: settings.noFeeFloor ? rawFee : normalizeDeliveryFee(rawFee)`. رسالة الشرح في بطاقة «حسبة التوصيل» («الحد الأدنى 150...») مبنية أصلاً على مقارنة `raw !== quote.fee` فتختفي تلقائياً بلا أي تعديل إضافي متى تساوى الاثنان. أُضيفت أيضاً مهاجرة جديدة `galatawaterNoFeeFloor` في `runDeliveryMigrations()` (نفس نمط `restaurantRate20`) تحذف أي نسخة `localStorage` مجمَّدة سابقة لمعرّف 115 كاملة، فتُجبَر كل المتصفحات (بما فيها متصفح المستخدم الذي اختبر المشكلة) على قراءة الإعدادات الجديدة فوراً بلا انتظار انتهاء أي صلاحية تخزين. **التحقّق (حي عبر preview محلي، بمحاكاة متصفح يحمل نسخة قديمة مجمَّدة فعلياً قبل رؤية الإصلاح):** بعد حقن `localStorage["dukkanci-delivery-settings"]["115"]` بنسخة قديمة يدوياً وإعادة التحميل، تأكَّد أن المهاجرة حذفت النسخة القديمة وأن `getDeliverySettings(115)` أعاد `noFeeFloor:true` مع كل المجمعات الثمانية سليمة؛ عنوان داخل «Hayat Park 4» أعاد `fee:0` (بلا تغيير، كما هو متوقَّع)؛ عنوان قريب غير مطابق لأي مجمّع (~2 كم ذهاباً وإياباً) أعاد `fee:41` (بدل 150 قبل الإصلاح)؛ عنوان أبعد (~13 كم) أعاد `fee:264` (الرقم الخام الدقيق، بلا تقريب لأعلى 50 أيضاً). **فحص انحدار على متجر آخر (56، باشا بيتزريا):** نفس حساب مسافة قصيرة أعاد `rawFee:36` لكن `fee:150` كما كان تماماً — يؤكّد أن الإصلاح مقصور على متجر 115 فقط بلا أي أثر على بقية المنصة. صفر أخطاء كونسول | `app.js`, `galatawater-data.js`, `index.html`, `sw.js`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v360→361، CACHE v279→280؛ store-slugs.js/category-slugs.js/styles.css بلا تغيير). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-26 | المستخدم طلب إضافة أول متجر مياه شرب على المنصة («مياه غلاطة الطبيعية - مياه ينابيع»، إسنيورت) **تحت تصنيف رئيسي جديد كلياً «المياه المعدنية»** لم يكن موجوداً من قبل، مع لوغو حقيقي من العلامة التجارية ومنتجين (جالون 18-19 لتر بـ170 ل.ت، وكرتون 60 كأس 180 مل بـ180 ل.ت) — كلا السعرين «شامل التوصيل» ضمن 8 مجمعات سكنية محدَّدة بالاسم، وخارجها تُحتسب المسافة الزائدة بـ20 ل.ت/كم | ميزة إضافة جديدة (لا خطأ سابق) — لكنها أول مرة يُضاف فيها **تصنيف رئيسي جديد** بدل متجر ضمن تصنيف قائم، فكشفت أن التصنيفات الفعلية الحيّة **لا تُقرأ من `HOME_CATEGORIES` في `app.js` إطلاقاً بل من صف `site_settings.categories.items` في Supabase** الذي يطغى عليها بالكامل (تحقّق مباشر: الصف يحمل 9 عناصر تخالف حتى ترتيب/صياغة المصفوفة الافتراضية في الكود) — تعديل `app.js` وحده كان سيبقى بلا أي أثر على الموقع الحي. 🔴 **اكتشاف تقني حاسم أثناء معالجة اللوغو المُرسَل (صورة PNG بخلفية رمادية داكنة متدرّجة تبدو "قذرة" بصرياً):** الخلفية الرمادية لم تكن بيانات بكسل فعلية بل قناة ألفا (شفافية) متغيّرة بشكل صحيح تماماً (بحث برمجي: زاوية الصورة `alpha=0` بينما قمة الجبل `alpha=254`) — أداة `Read` تعرض الشفافية كخلفية رمادية/متدرّجة للعرض فقط، وليست جزءاً من الصورة الفعلية؛ **قبل** اكتشاف هذا حاولتُ "تسطيح" الخلفية يدوياً عبر تدرّج شعاعي (radial gradient) بمعادلة `blend:"lighten"` في `sharp` فأنتج نتيجة معكوسة تماماً (مركز أسود، حواف بيضاء) لخلل غير محسوم في تفسير sharp لوضع الدمج هذا على هذا الإصدار — تُرك السبب الجذري لذلك اللغز دون حل لأن `sharp(src).flatten({background:{r:255,g:255,b:255}})` المباشر (الطريقة الصحيحة القياسية لتفريغ قناة ألفا فوق لون خلفية) أعاد فوراً بالضبط اللوغو الأبيض النظيف الذي أرسله المستخدم أصلاً — **درس عام: لا تحاول "تنظيف" خلفية PNG تبدو ملوَّنة/متدرّجة قبل التحقّق من قناة ألفا الفعلية للصورة؛ قد تكون شفافية سليمة تماماً يُسيء عرضها أداة المعاينة فقط.** 🔴 **قيد ألصق إشعار مستخدم متأخر:** رسالة "احسب التوصيل حسب المسافة فوق سعر محدد لـ8 مجمعات" وصلت بعد تنفيذ جزء من الحفظ — تبيَّن بالفحص أن محرك التسعير (`estimateDeliveryQuote`) يملك أصلاً ميزة `settings.namedZones` (مطابقة أطول كلمة مفتاحية في نص العنوان مقابل قائمة مناطق مُسمّاة، كل منطقة برسم توصيل خاص يتفوّق كلياً على حساب المسافة) لكنها **لم تُستخدم من قبل إلا لمتجر واحد فقط (id 50 صفا الشام)** بل **كانت واجهة اختيار "هل عنوانك في أحد هذه المجمعات؟" في نافذتي العنوان مقفلة حرفياً بشرط `storeId === 50`** في موضعين (أول تحقّق قبل تصميم الحل ظنّاً مبدئياً أنها ميزة عامة). | **(أ) التصنيف:** أُضيف عنصر جديد لكل من: `HOME_CATEGORIES` في `app.js` (للاتساق/الاحتياط الافتراضي) **و**— الأهم فعلياً — صف `site_settings.categories.items` في Supabase مباشرة (عبر PATCH بمفتاح service-role) + `category-slugs.js` (`"mineral-water": "المياه المعدنية"`) بصورة تصنيف جديدة `assets/photos/store-mineral-water.jpg` (اقتصاص واقعي بمساعدة `sharp` من صورة جالون حقيقية على خلفية سوداء أرسلها المستخدم). **(ب) اللوغو:** `sharp(src).flatten({background:"#fff"})` ثم اقتصاص محكم لصندوق المحتوى الفعلي (اكتشاف حدود تلقائي ببحث بكسلي غير أبيض) وتربيعه بحشوة بيضاء إلى 400×400 — درس تنفيذي إضافي: تسلسل `extract→extend→resize` في استدعاء واحد مُتَّصل أنتج أبعاداً خاطئة تماماً (400×910 بدل 400×400) بسبب خلل في تمرير البيانات الوصفية بين عمليات sharp المتسلسلة بلا تجسيد وسيط؛ حُلّ بتجسيد كل خطوة كملف فعلي على القرص قبل الخطوة التالية. **(ج) التوصيل:** أُضيف `namedZones` (8 مجمعات: Hayat Park 4, Sembol İstanbul Residence, Self İstanbul, İstanbul Prestij Park, HEP İstanbul, Babacan Premium, Almina Tower, Radius Residence) كل واحدة برسم `fee:0` داخل `galatawaterDeliverySettings`، مع `mode:"distance"` و`ratePerKm:20` كاحتياط لأي عنوان خارج هذه المجمعات (السعر المُعلَن "١٧٠/١٨٠ شامل التوصيل" يتحقّق فقط ضمنها؛ خارجها تُحسب الأجرة الكاملة ذهاباً وإياباً على المسافة، وليس فقط الجزء الزائد — تبسيط عملي واضح للمستخدم في الملخّص). **عُمِّمت ميزة منتقي المجمعات** (كانت حصراً لمتجر 50) بتغيير الشرطين الصلبين `cartStoreId === 50`/`storeId === 50` إلى فحص عام (أي متجر يملك `namedZones` مُعرَّفة يحصل على نفس الواجهة تلقائياً) في نافذتي العنوان — تغيير من سطرين لا يغيّر أي سلوك لأي متجر آخر (القيمة تبقى فارغة لهم فتختفي الواجهة كما كانت). **صور المتجر:** استُخرجت من 5 صور حقيقية أرسلها المستخدم (لوغو نظيف، صورة جالون 19 لتر بخلفية سوداء احترافية، صورة كرتون+أكواب ترويجية) — لا صور مولَّدة اصطناعياً جديدة، فقط معالجة/اقتصاص للأصول الحقيقية المُرسَلة. الموقع الجغرافي حُلّ من Plus Code («2M6P+H8H Esenyurt, İstanbul») عبر Geocoding API الحي (لا نتيجة على Google Places — نشاط توصيل منزلي صغير بلا صفحة أعمال، مثل حالات سابقة موثَّقة). المتجر رقم **115** (تحقّق `max(id)`=114 مرتين — أول الجلسة ومباشرة قبل الإدراج، صفر تصادم)، معرّفا المنتجين **1950001/1950002**. `approval_status='pending'` عمداً. وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `galatawaterDeliverySettings` spread) + `store-slugs.js` (`115: "galata-natural-water-esenyurt"`) + `<script src="/galatawater-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع لـSupabase عبر سكربت Node مباشر (service-role REST). **التحقّق (حي عبر preview محلي):** `/store/galata-natural-water-esenyurt` عرض العنوان والوصف والمنتجين بسعريهما الصحيحين وقسم «أسعار توصيل خاصة» بكل المجمعات الثمانية بقيمة «٠ ل.ت» لكل منها والاحتياط «حسب المسافة · ٢٠ ل.ت/كم»، الصفحة الرئيسية أظهرت بلاطة «المياه المعدنية» الجديدة في قسم التصنيفات بصورتها وتسميتها الصحيحتين، `/category/mineral-water` حمّل بعنوان صحيح («المياه المعدنية | دكانجي») وأظهر رسالة «لا توجد متاجر بعد» بشكل صحيح (سلوك `pending` المتوقَّع، مطابق لكل المتاجر الجديدة السابقة)، كل روابط الصور (لوغو/غلاف/منتجين/بلاطة التصنيف) أعادت HTTP 200 فعلياً، وصفر أخطاء كونسول عبر كل الصفحات المفحوصة. `npm run check:versions` ناجح | `galatawater-data.js` (جديد)، `assets/photos/galatawater/**` (جديد: logo.png, cover.jpg, p1.jpg, p2.jpg)، `assets/photos/store-mineral-water.jpg` (جديد)، `app.js` (تعميم `namedZones` + `HOME_CATEGORIES` + الأسلاك الثلاثة)، `store-slugs.js`, `category-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 115، صفَّا `products` 1950001/1950002، وتحديث `site_settings.categories` في Supabase | نعم — `npm run check:versions` ناجح (app.js v359→360، store-slugs.js v264→265، category-slugs.js v83→84، CACHE v278→279؛ styles.css بلا تغيير v194). **تحديث لاحق (نفس اليوم):** بطلب صريح من المستخدم «اعتمد المتجر»، حُدِّث `approval_status` إلى `'approved'` مباشرة عبر Supabase — تحقّق حي فوري أن `/category/mineral-water` صار يعرض «1 متجر في إسطنبول على دكانجي» ببطاقة المتجر ومنتجيه وشارة «مفتوح»/«متجر رسمي»، بلا أخطاء كونسول. **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-24 | المستخدم طلب إضافة سوبر ماركت «بلودان ماركت» (Bludan Market) من رابط `app.trybany.com/ar/bludan-market-1lk/` — أكبر كتالوج يُضاف حتى الآن بعد صفا الشام (**2,556 منتجاً فريداً** عبر 45 فئة) | ميزة إضافة جديدة (لا خطأ سابق) — أول مصدر من نوع **trybany.com Next.js App Router** (منصة مختلفة عن `trybany.com/api/v1` القديمة المستخدَمة سابقاً لمتجرَي Orange Turkey/ATA ET؛ هذا نطاق `app.trybany.com` منفصل). 🔴 **علة تسمية خطيرة كادت تُتلِف بيانات حية:** يوجد مسبقاً على المنصة سلسلة مطعم شاورما باسم «بلودان» (id 61/62، `bludan-kayasehir-restaurant`/`bludan-fatih-restaurant`) لا علاقة له بالسوبر ماركت الجديد سوى تشابه الاسم (بلودان اسم بلدة سورية شائع الاستخدام تجارياً) — أول محاولة تنزيل شعار/غلاف المتجر الجديد استخدمت مجلد `assets/photos/bludan/` نفسه فعلياً **overwrote** ملفي `cover.jpg`/`logo.png` الحقيقيين للمطعم القديم (`git status` أظهر `M` على كليهما) قبل اكتشاف التصادم؛ أُصلح فوراً بـ`git checkout --` لاستعادة الملفين الأصليين، ونُقلت كل أصول المتجر الجديد لمجلد مستقل `assets/photos/bludanmarket/` وslug مميَّز `bludan-market-fatih` (يخالف تماماً `bludan-fatih-restaurant` الموجود). **درس استخراج جديد لعائلة trybany.com الحديثة (App Router):** لا يوجد أي endpoint REST مباشر يُعيد قائمة كاملة بالمنتجات؛ الصفحة تُصيَّر بـReact Server Components، وبيانات المنتجات مضمَّنة كنص JSON مهرَّب (`\"key\":`) داخل استدعاءات `self.__next_f.push` — لكن هذا التمثيل **غير موثوق دائماً**: لفئة واحدة (شامبو وصابون) لم يظهر أي بلوك JSON منتجات إطلاقاً رغم ظهور المنتجات بصرياً على الصفحة (تحقّق: 0 نتيجة لنمط `\"products\":[` بينما `grep` لنص "شامبو" أظهر تطابقات). **الحل الأكثر مقاومة الذي اعتُمد نهائياً: تحليل HTML المُصيَّر مباشرة** بدل الاعتماد على JSON المضمَّن — كل بطاقة منتج تبدأ بـ`<div id="<slug>" class="px-0 ...">` (slug فريد يطابق رابط `/product/<slug>/`)، فاستُخدم هذا كنقطة تقسيم موثوقة لاستخراج الاسم (من `<a href=".../product/<slug>/">النص</a>`) والسعر (`current-price...>N<span`) وحالة عدم التوفر (`not-available">غير متاح حاليا`) ورابط الصورة عالي الدقة (إعادة بناء `https://assets.trybany.com/uploads/products/<id>/resized/<hash>-HD.jpg` من الأجزاء المُرمَّزة `%2F` في `srcSet`) — نجح هذا الأسلوب بلا استثناء واحد عبر كل الفئات الـ45 (2673 بطاقة خام، 2556 فريدة صالحة للنشر بعد استبعاد غير المتوفر/بلا صورة). **شجرة الفئات مكتشَفة بالتنقّل الحي:** الصفحة الرئيسية تعرض 12 قسماً رئيسياً فقط بأعداد إجمالية (801+255+...=2462، رقم مختلف قليلاً عن العدد الفعلي المُستخرَج 2556 — الفارق طبيعي، عدّاد الصفحة الرئيسية غير حيّ بدقة)؛ فتح كل قسم رئيسي عبر متصفح حي كشف أنه يُعيد توجيه (`router.replace` من طرف العميل، لا Location header) لأول قسم فرعي منه، وقراءة `selectedCategory.children` من ذلك القسم الفرعي أعطت قائمة كل الأشقاء دفعة واحدة (بلا حاجة تخمين أسماء الفئات الفرعية). **مطابقة Google Maps مؤكَّدة بدقة قاطعة:** حلّ رابط Google Maps المختصر (`maps.app.goo.gl/Bc7PcUms1WTXGLdTA`) أعطى إحداثيات دقيقة، ثم بحث Nearby بهذه الإحداثيات أعاد تطابقاً حرفياً كاملاً لرقم الهاتف (`+90 552 284 18 15` = نفس رقم واتساب المتجر بالضبط)، الاسم «BLUDAN market ماركت بلودان»، تقييم حقيقي 4.0 (119 تقييماً)، وساعات عمل يومية موحّدة (9:00 ص – 1:00 ص) من `opening_hours.weekday_text`. الشعار الحقيقي حُمِّل من ملف تعريف trybany نفسه؛ الغلاف — لأن trybany لم يوفّر بانراً تسويقياً حقيقياً (صورة عامة `generic-cover.png`) — اعتُمدت بدلاً منه صورة واجهة محل حقيقية من مساهمات Google Places (نفس نمط استخدام صورة حقيقية بديلة عن بانر غائب، سابقة موثَّقة مراراً). **قرار معماري: لا كتالوج مُجمَّع في الملف (`bludanmarketProductCatalog = []`)** — بنفس سياسة صفا الشام (1317 منتجاً) بالضبط: كتالوج بهذا الحجم (2556) كان سيجعل حزمة الواجهة الأمامية بطيئة التحميل جداً لو أُدرِج كنص JS حرفي؛ كل المنتجات تُقرأ حصراً من Supabase. **صور المنتجات استُضيفت محلياً في المستودع** (`assets/photos/bludanmarket/products/`، 2556 ملف JPEG ≤800px q82 mozjpeg، 147MB إجمالياً) بدل Supabase Storage — نفس النمط المتَّبع لكل إضافة متجر سابقة في هذا المشروع (لم يُستخدَم Storage إلا استثناءً واحداً سابقاً لصور شعار/غلاف محسَّنة، وليس كنمط عام لصور المنتجات كما ظننتُ أولاً قبل التحقّق من حجم `assets/photos/` الفعلي في المستودع الحي — 679MB قبل هذه الإضافة، يثبت أن الاستضافة المحلية هي المعتمَدة فعلياً رغم حجمها). **تحقّق سباق قبل الكتابة (نفس درس فستق حلب/الوكالة):** `max(stores.id)`/`max(products.id)`/توفر الـslug فُحصت مباشرة قبل التنفيذ (113/1930017، صفر تصادم) — المتجر رقم **114**، حزمة معرّفات المنتجات **1940001–1942556**. `approval_status='pending'` عمداً (طرف ثالث حقيقي غير مؤكَّد كشراكة دكانجي). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `bludanmarketDeliverySettings` spread) + `store-slugs.js` (`114: "bludan-market-fatih"`) + `<script src="/bludanmarket-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع لـSupabase عبر سكربت Node مباشر (service-role REST، معاملة للمتجر ثم 13 دفعة من 200 صف منتج). **التحقّق (حي عبر preview محلي، لا افتراض):** `/store/bludan-market-fatih` عرض العنوان الصحيح («دكانجي - بلودان ماركت»)، 2556 منتجاً محمَّلة فعلياً من Supabase (البيانات المحلية فارغة عمداً)، 45 زر تصنيف بعدّادات صحيحة مطابقة تماماً لحساب الاستخراج («الكل 2556»، «المعلبات 172»، «العناية الشخصية 199»...)، عيّنة صور (أول/وسط/آخر منتج: p1/p1000/p2556) + الشعار + الغلاف كلها HTTP 200 فعلياً، `/category/supermarket` **لا** يعرض المتجر إطلاقاً (سلوك `pending` صحيح، تحقّق مباشر `[data-id="114"]` غائب)، بينما الرابط المباشر يعمل، والصفحة الرئيسية بلا أي أثر وبصفر أخطاء كونسول عبر كل الفحوصات | `bludanmarket-data.js` (جديد)، `assets/photos/bludanmarket/**` (جديد، 2558 ملف: 2556 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 114 وصفوف `products` 1940001-1942556 في Supabase | نعم — `npm run check:versions` ناجح (app.js v358→359، store-slugs.js v263→264، CACHE v277→278؛ styles.css بلا تغيير v194). **مدفوع لـ`main`** (commit `d4c77e0d`، Vercel نجح، تحقّق حي: `/store/bludan-market-fatih` وكل عيّنات الصور HTTP 200 على الموقع الحي، ومطعم بلودان الشاورما القديم غير متأثر). **تحديث لاحق (نفس اليوم):** بطلب صريح من المستخدم «اعتمد المتجر»، حُدِّث `approval_status` إلى `'approved'` مباشرة عبر Supabase — تحقّق حي فوري أن `/category/supermarket` صار يعرض بطاقة المتجر فعلاً (`[data-id="114"]` موجودة بشارتَي «مفتوح»/«متجر رسمي»)
| 2026-07-24 | متابعة لإصلاح زر «دخول»: جلسة سابقة رفعت نسخة Flutter القديمة (بلا الإصلاح) لمسار Internal testing على Google Play Console (`versionCode 1`، غير مُراجَعة بعد)، فطلب المستخدم رفع النسخة المُصلَحة بدلها | لا يوجد أي نظام آلي لرفع الإصدارات إلى Play Console في هذا المشروع (لا fastlane، لا مفتاح Google Play Developer API، لا GitHub Actions) — الطريقة الوحيدة المتاحة يدوية عبر متصفح المستخدم الحقيقي (Claude in Chrome). 🔴 **قيد تقني اكتُشف أثناء التنفيذ:** أدوات المتصفح لا تسمح باختيار ملف محلي عشوائي من قرص المستخدم لحقل رفع ملف (`file_upload`) إلا إن كان الملف "مُشارَكاً مع الجلسة" صراحة — وأدوات سطح المكتب (`computer-use`) لا يمكنها التفاعل مع نوافذ اختيار الملف الأصلية لأن المتصفح دائماً في مستوى "قراءة فقط" لهذه الأدوات — فطُلب من المستخدم اختيار الملف يدوياً بنفسه من المسار المحدَّد (نجح فوراً). رقم الإصدار في `pubspec.yaml` كان لا يزال `1.0.0+1` (نفس رقم النسخة المرفوعة سابقاً بالضبط) — رُفع صراحة بموافقة المستخدم إلى `1.0.1+2` تفادياً لرفض Play Console رفع حزمة بنفس `versionCode` | (1) رفع `version:` في `dukkanci_customer_app/pubspec.yaml` إلى `1.0.1+2`. (2) بناء حزمة Android App Bundle موقَّعة فعلياً (`flutter build appbundle --release`، ~639 ثانية) باستخدام keystore الإصدار الحقيقي الموجود مسبقاً (`android/key.properties`/`upload-keystore.jks`، أُنشئا في جلسة سابقة 2026-07-23 ولم يُستخدما في رفع فعلي حتى الآن) → `app-release.aab` (59.3MB). (3) عبر Claude in Chrome (حساب `dukkanci.tr@gmail.com`، منظمة "Dukkanci"، تطبيق «دكانجي - Dukkanci»): Test and release → Internal testing → Create new release → المستخدم رفع الملف يدوياً → كتابة ملاحظات إصدار عربية (`<ar>إصلاح مشكلة عدم استجابة زر «دخول»...</ar>`) → Next → Save and publish → تأكيد. **تحقّق مباشر بعد النشر:** الإصدار الحيّ على Play Console أصبح **"2 (1.0.1) — Available to internal testers — Not reviewed"** (لم يُرسَل للمراجعة النهائية، كما طلب المستخدم صراحة أن يبقى الحال)، والإصدار القديم `1 (1.0.0)` انتقل لسجلّ "Release history" بلا حذف. 🔴 **اكتشاف جانبي غير متعلق بهذه المهمة رُصد أثناء محاولة أولى دخلت حساباً خاطئاً بالخطأ:** حساب Google آخر غير ذي صلة بالمشروع (منظمة "Ali karajeh") ظهر بتحذير "developer profile and all apps have been removed... Google couldn't verify your identity" — لا علاقة له بحساب دكانجي الفعلي، ذُكر للمستخدم فقط لتوضيح سبب التوقف المؤقت قبل تصحيح الرابط | `dukkanci_customer_app/pubspec.yaml`, `CLAUDE.md` + إصدار جديد على Google Play Console (Internal testing track، خارج المستودع) | لا ينطبق (رقم إصدار Flutter منفصل تماماً عن أرقام app.js/styles.css/CACHE الموصوفة في قسم 4 — ذلك النظام خاص بالموقع فقط)
| 2026-07-24 | متابعة لإعادة تصميم /about (السطر التالي أدناه): المستخدم طلب فحص «باقي الصفحات المشابهة» (الشروط والأحكام، الأسئلة الشائعة) وتحديثها بنفس الأسلوب | نفس فئة الخطأ بالضبط: `renderTermsPage()` كانت 5 أسطر عامة جداً عبر `renderStaticPage()` الخام (بلا هوية بصرية ولا تفاصيل حقيقية عن آلية العمل الفعلية: OTP واتساب، عدم وجود عمولة، طرق الدفع الثلاث بلا دفع إلكتروني...)، و`renderFaqPage()` كانت 4 أسئلة عامة فقط بتصميم `<details>` خام بلا أي هيكلة. كما تبيَّن أن `updateHead()` لا يضبط `title`/`desc` مخصَّصين لمساري `faq`/`terms` إطلاقاً (نفس علة `about` المُصلَحة في السطر التالي) رغم إدراج كليهما ضمن `indexableRoute` — فكانا يظهران بعنوان الرئيسية العام | أُعيدت كتابة الصفحتين بمحتوى حقيقي مبني على الميزات الفعلية الموثَّقة في هذا الملف ([[checkout-otp]], [[payment-methods]], [[delivery-settings-localstorage]], [[subscription-pricing]]): «الشروط» 10 بنود حقيقية (نطاق الخدمة كوسيط تقني، تأكيد الطلب بـOTP واتساب، الأسعار بلا عمولة، 3 طرق دفع بلا دفع إلكتروني، مسؤولية المتجر عن منتجاته، الإلغاء/التعديل عبر واتساب، الشكاوى، الحسابات، الاستخدام المقبول، تعديل الشروط) كبطاقات أيقونة+عنوان+فقرة. «الأسئلة الشائعة» أُعيدت هيكلتها لـ5 مجموعات (15 سؤالاً: الطلب والدفع، التوصيل والمناطق [يستخدم `DALIL_REGION_LIST.length` الحي كما في /about لا رقماً ثابتاً]، التقييمات والشكاوى، لأصحاب المتاجر، الحساب والخصوصية) كأكورديون مجمَّع، أول سؤال مفتوح افتراضياً. كلتا الصفحتين تشتركان في نطاق CSS مستقل جديد `.legal-*` (هيرو غامق متدرّج بنفس لغة `/about` البصرية، لكن بإعادة استخدام حركتَي `merchantFadeUp`/`merchantFloat` الموجودتين أصلاً بدل تكرارهما — لا تكرار كود، ولا تشارك مع `.about-*`/`.why-*` حفاظاً على استقلال كل صفحة كما جرت العادة في المشروع) + دالة مشتركة صغيرة `legalCtaBoxHTML()` لصندوق التواصل الختامي المتطابق في الصفحتين (بدل تكرار HTML). أُضيف أيضاً `title`/`desc` مخصَّصان لمساري `faq`/`terms` في `updateHead()`. كلا الصفحتين تربط لـ`/privacy.html` (سياسة الخصوصية الحقيقية الموجودة في المستودع). **التحقّق (حي عبر preview محلي):** `node --check app.js` سليم وتوازن أقواس `styles.css` محسوب برمجياً (عمق نهائي صفر)؛ /faq عرضت 5 مجموعات و15 سؤالاً بسؤال واحد مفتوح افتراضياً وإجابة المنطقة تعرض «13» الحية فعلاً، /terms عرضت 10 بطاقات بشبكة عمودين على 1280px وعمود واحد على 375px، رابط سياسة الخصوصية موجود وصحيح، النقر الفعلي على الأكورديون يعمل، التنقّل عبر روابط الفوتر (`/faq`، `/terms`) من الرئيسية عبر SPA بلا إعادة تحميل نجح لكلتا الصفحتين، صفر انزياح أفقي على 1280px و375px، وصفر أخطاء كونسول عبر كل الفحوصات | `app.js`, `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v357→358، styles.css v193→194، CACHE v276→277). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-24 | المستخدم طلب إعادة كتابة محتوى صفحة `/about` («من نحن») ليلائم طبيعة الموقع وخدماته، مع إعادة تصميمها بشكل لائق ومناسب | ليست خطأً — `renderAboutPage()` كانت 3 أسطر نصية عامة جداً («منصة متخصصة في ربط سكان إسطنبول...») عبر `renderStaticPage()` المشتركة (نفس القالب الخام المستخدَم لـ/contact و/terms: هيرو مضغوط + فقرات نص فقط، بلا أي هوية بصرية أو بيانات حقيقية عن المنصة). كما تبيَّن أثناء الفحص أن `updateHead()` لم يكن يضبط عنوان/وصف مخصَّصين لمسار `about` إطلاقاً رغم إدراجه ضمن `indexableRoute` — فكان يظهر بعنوان الرئيسية العام «دكانجي \| سوق الحي بين يديك» في الوسم `<title>`، إشارة SEO ضعيفة لصفحة مفهرسة | أُعيدت كتابة `renderAboutPage()` بالكامل بمحتوى حقيقي: هيرو غامق (تدرّج + فقاعات عائمة، بإعادة استخدام حركتَي `merchantFadeUp`/`merchantFloat` المعرَّفتين أصلاً لصفحات `/merchant` بدل تكرارهما) + 3 بطاقات إحصاء **حية** (`stores.filter(isStoreApproved).length`، `homeCategoriesOrdered().length`، `DALIL_REGION_LIST.length` — لا رقم مُختلَق، نفس مبدأ الصدق المتكرر في هذا المشروع ونفس نمط `merchantAuthAsideHTML()`)، قسم «قصتنا» (لماذا بُني دكانجي: مشكلة تشتّت رسائل واتساب مع كل محل على حدة قبل المنصة)، شبكة 4 بطاقات «مبادئنا» (أسعار بلا عمولة، توصيل واضح مسبقاً، تقييمات حقيقية، دعم مباشر عبر واتساب)، قسم «ماذا نقدّم» بإعادة استخدام مكوّن `categoryCard()`/`.category-grid` الموجودَين فعلاً (لا مكوّن جديد، ولا `shopTile()`/`.shop-tile` لأنها مكوّن مقصور فعلياً على شريط الجوال في الرئيسية بلا أي CSS لسطح المكتب — تحقّق أن كل قواعد `.shop-tile*` معرَّفة حصراً داخل `@media (max-width:640px)`)، بانر «لأصحاب المتاجر» بإعادة استخدام مكوّن `.merchant-cta` الموجود حرفياً (نفس السوق المستخدَم في `/category/<slug>`)، وقسم تواصل ختامي (واتساب + رابط `/contact`). أُضيف أيضاً `title`/`desc` مخصَّصان لمسار `about` في `updateHead()` بنفس نمط بقية المسارات. CSS جديد بالكامل تحت نطاق مستقل `.about-page`/`.about-*` (لا تداخل مع `.why-*` الخاصة بـ/why-dukkanci ولا مع أي صفحة أخرى) في نهاية `styles.css`. **التحقّق (حي عبر preview محلي، لا افتراض):** `node --check app.js` سليم وتوازن أقواس `styles.css` محسوب برمجياً (عمق نهائي صفر)؛ الصفحة الحية أظهرت العنوان الصحيح في التبويب («من نحن \| دكانجي»)، الأرقام الحية الثلاثة (+97 متجر، 8 تصنيفات، 13 منطقة) محسوبة فعلياً من نفس مصفوفات الموقع، هيرو غامق بتدرّج ونص أبيض صحيحين (`getComputedStyle`)، الشبكة ثنائية العمود على 1280px وعمود واحد على 375px بلا أي انزياح أفقي (`scrollWidth === innerWidth` على كلا العرضين)، النقر الفعلي على رابط الفوتر `/about` من الرئيسية أدّى تنقّلاً حياً عبر SPA (بلا إعادة تحميل) لعنوان الصفحة الصحيح، والنقر على بطاقة تصنيف («مطاعم») من داخل الصفحة نقل فعلياً لصفحة القسم الصحيحة، وروابط الإجراءات (`تصفّح المتاجر`→`/stores`، زرّا «انضم كتاجر»→`data-action=join-merchant`، «تواصل عبر واتساب»→`wa.me/905528000220` بنص مُرمَّز صحيح، «صفحة التواصل»→`/contact`) جميعها بقيم `href`/`data-action` صحيحة، وصفر أخطاء كونسول عبر كل الفحوصات. 🔴 **علة حقيقية اكتُشفت بالمعاينة الحية للمستخدم (لا بالمراجعة النظرية):** بانر «لأصحاب المتاجر» كان غائباً تماماً على عرض الموبايل — تبيَّن أن `<section class="section merchant-cta-section">` المُستنسَخ حرفياً من الرئيسية يصطدم بقاعدة CSS عامة غير مُقيَّدة بصفحة (`@media (max-width:640px){ .merchant-cta-section{display:none} }`) كُتبت أصلاً لإخفاء بانر الرئيسية التسويقي على الموبايل (تجربة «الشراء أولاً» الموثَّقة في سطر 2026-07-11)، فأخفت بصمت نفس البانر على /about أيضاً بمجرد إعادة استخدام اسم الصنف نفسه. أُصلح بتغيير صنف القسم الخارجي فقط إلى `about-merchant-section` (مستقل تماماً، بلا أي تأثير على إخفاء الرئيسية) مع إبقاء `<div class="merchant-cta">` الداخلي كما هو حرفياً — تحقّق حي بعد الإصلاح: `getBoundingClientRect().height` للقسم أصبحت 511px و`display:block` على عرض 617px (دون 640px)، والنص الداخلي يظهر بشكل صحيح | `app.js`, `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v355→357، styles.css v192→193، CACHE v274→276). **مدفوع لـ`main`** بناءً على طلب المستخدم الصريح
| 2026-07-24 | متابعة لإصلاح زر «دخول» في حسابي (السطر التالي أدناه): المستخدم طلب تشغيل محاكي فعلي وتجربة تسجيل الدخول الحقيقي بدل الاكتفاء بمراجعة الكود | لم يكن الإصلاح قد اختُبر حياً بعد. **قيد أمان حقيقي واجه التنفيذ:** إكمال تسجيل الدخول يتطلب رمز OTP حقيقي يُرسَل عبر واتساب من رقم دكانجي التجاري الفعلي لأي رقم هاتف يُدخَل — لا يجوز استخدام رقم عشوائي/مُختلَق (رسالة واتساب حقيقية لطرف ثالث غير موافق). طُرح هذا صراحةً على المستخدم (`AskUserQuestion`) فاختار استخدام رقمه الشخصي وزوّده مباشرة (`+905533333362`). 🔴 **عطل بيئي متكرر أثناء التنفيذ لا علاقة له بكود التطبيق:** محاكي `dukkanci_test` انهار 3 مرات متتالية (segfault حقيقي مؤكَّد بـ`exit code 139` في سجلّ العملية، ليس مجرد توقّف): أول مرتين بعد ضغط ذاكرة حاد على الجهاز (1.85GB متاح فقط من 15.84GB بسبب Gradle daemon خامل ~1.2GB + جلسات متزامنة أخرى)، ولم يُحلّ نهائياً إلا بالتبديل من تسريع GPU إلى الرسم البرمجي (`-gpu swiftshader_indirect`) — على الأرجح تعارض في محرك تسريع الرسوميات (WHPX) تحت الحمل الحالي للجهاز، فضلاً عن نوافذ ANR روتينية متكررة لـ«Pixel Launcher»/«System UI» (موثَّقة مسبقاً كعطل بيئي معروف، عولجت جميعها بالضغط على «Wait» ريثما تستقر). **إحداثيات اللمس المحسوبة من لقطات الشاشة يدوياً كانت غير موثوقة** بسبب بطء الرسم البرمجي (المحتوى يتحرك بين التقاط اللقطة وتنفيذ اللمسة، ما تسبَّب بالدخول سهواً لصفحات تصنيفات بدل التبويب المطلوب) — الحل: `adb shell uiautomator dump` لاستخراج `bounds` الدقيقة لكل عنصر (`content-desc`) واللمس بإحداثيات مركز العنصر الفعلية بدل التخمين البصري، ما أثبت أنه أدق بكثير. **رمز OTP الأول (`512302`) انتهت صلاحيته فعلياً** أثناء انتظار الرد من المستخدم (فارق زمني طويل بسبب أعطال المحاكي أعلاه) — رسالة الخطأ الصحيحة «انتهت صلاحية رمز التحقق» ظهرت بشكل سليم تماماً (تحقّق إضافي غير مخطَّط لمسار معالجة الأخطاء `Failure.fromCode('OTP_EXPIRED')`)، فطُلب رمز جديد (`847278`) عبر «إعادة إرسال الرمز» | لم يُعدَّل أي كود — هذه جولة تحقّق حي بحتة للإصلاح الموصوف في السطر التالي أدناه. **التحقّق الكامل من طرف إلى طرف على قاعدة الإنتاج الحقيقية (`www.dukkanci.com.tr`، بناء `flutter build apk --debug --dart-define=API_BASE_URL=https://www.dukkanci.com.tr`):** فتح تبويب «حسابي» → ضغط زر «دخول» **فتح فعلاً** نافذة تسجيل الدخول السفلية (تأكيد مباشر أن العطل الأصلي — زر بلا أي استجابة — انتهى تماماً) → إدخال رقم حقيقي → «إرسال رمز التحقق» أرسل فعلياً رسالة واتساب حقيقية وصلت لجهاز المستخدم خلال ثوانٍ → معالجة خطأ الرمز المنتهي بشكل صحيح ومسار «إعادة الإرسال» عمل بنجاح → إدخال الرمز الصحيح الثاني والضغط على «تحقق» أنتج **جلسة Supabase حقيقية فعلية**: الشاشة تحوَّلت تلقائياً من «زائر / سجّل الدخول لحفظ عناوينك وطلباتك» إلى «حسابي / 905533333362» بلا أي إعادة تحميل يدوية، زر «دخول» اختفى واستُبدل بـ«تسجيل الخروج»/«حذف الحساب» — يثبت أن `authStateChangesProvider` الجديد (المُضاف في نفس الإصلاح) نجح في جعل الشاشة تُعيد رسم نفسها تلقائياً كما صُمِّم بالضبط. **لم يُحذف** الحساب/الجلسة الناتجة من Supabase عمداً (حساب حقيقي لرقم المستخدم الفعلي، وليس بيانات اختبار مصطنعة) | لا شيء (تحقّق فقط) | `CLAUDE.md` | لا ينطبق (لا تعديل كود)
| 2026-07-23 | المستخدم أبلغ أن زر «دخول» في تبويب «حسابي» بتطبيق Flutter (`dukkanci_customer_app`) لا يستجيب عند الضغط | 🔴 **ليس عطلاً تقنياً (لا تعارض تصيير/hit-testing ولا استثناء مبلوع) بل ميزة ناقصة تُركت كعنصر نائب:** `profile_screen.dart` سطر 79 كان حرفياً `FilledButton(onPressed: () {}, child: ...)` — زر فعّال تماماً بصرياً (ripple يعمل) لكن `onPressed` دالة فارغة لا تفعل شيئاً. السبب أن `AppRoutes`/`app_router.dart` لا يملكان أي مسار/شاشة تسجيل دخول مستقلة إطلاقاً، بينما منطق OTP الفعلي (`AuthRepository.sendOtp`/`verifyLoginOtp`، يطابق تدفّق واتساب OTP في `app.js`) كان موجوداً لكن **مُضمَّناً حصراً** داخل `checkout_screen.dart` (بوابة تحقّق الطلب، لا تسجيل دخول حقيقي يُنشئ جلسة Supabase قابلة لإعادة الاستخدام خارج الدفع). 🔴 **علة ثانية مكتشَفة أثناء الإصلاح:** `ProfileScreen` تقرأ `authRepositoryProvider.currentSession` بـ`ref.read` وقت البناء فقط (لا `ref.watch` على أي Stream) — حتى لو أُصلح الزر ليفتح تسجيل دخول ناجحاً، الشاشة لن تُعيد رسم نفسها لتعكس الجلسة الجديدة تلقائياً | استُخرج تدفّق OTP في نافذة سفلية مستقلة قابلة لإعادة الاستخدام `lib/features/auth/presentation/login_sheet.dart` (`showLoginSheet(context)`): هاتف→إرسال رمز→تحقّق عبر `verifyLoginOtp` (تُنشئ جلسة Supabase حقيقية عبر `magiclink`، خلافاً لـ`verifyCheckoutOtp` المستخدَمة في الدفع)، بنفس معالجة أخطاء `Failure` ومعالجة `OtpSendResult.skippedWhatsappUnavailable` كخطأ حقيقي هنا (لا فرصة «طلب بلا توثيق» كما في الدفع، فلا يوجد رمز أصلاً للتحقق منه). أُضيف `authStateChangesProvider` (`StreamProvider` يغلّف `supabase.auth.onAuthStateChange`) في `app/providers.dart`، و`ProfileScreen` صارت تراقبه (`ref.watch`) فتُعاد رسمها تلقائياً فور نجاح تسجيل الدخول بلا أي كود يدوي إضافي بعد إغلاق النافذة. وُصل زر «دخول» فعلياً بـ`onPressed: () => showLoginSheet(context)`. **التحقّق:** `flutter analyze` أعاد **نفس الأربع ملاحظات المعلوماتية القديمة فقط بلا أي جديد** (خط الأساس الموثَّق سابقاً في هذا السجل)؛ لم يُشغَّل محاكي في هذه الجولة (تعديل تركيب/تدفّق واجهة فوق مسارات OTP مُتحقَّق منها حياً سابقاً في جلسات محاكي سابقة، لا تغيير في مخطط البيانات أو منطق الخادم) | `dukkanci_customer_app/lib/features/profile/presentation/profile_screen.dart`, `dukkanci_customer_app/lib/features/auth/presentation/login_sheet.dart` (جديد)، `dukkanci_customer_app/lib/app/providers.dart`, `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-23 | المستخدم أبلغ أن مجموعة متاجر أعطاها موافقة الإدارة («قبول») من تبويب «المتاجر»/«طلبات انضمام جديدة» تعود تظهر «بانتظار الموافقة» من جديد **عند كل فتح** للوحة الإدارة | 🔴 **سباق تصيير حقيقي، ليس فشل كتابة فعلياً — تحقّق مباشر على قاعدة الإنتاج الحقيقية (لا افتراض) أثبت أن `approval_status='approved'` محفوظ بشكل صحيح تماماً لكل المتاجر المذكورة.** الخلل في تسلسل الإقلاع: `render()` الأول يُنفَّذ **متزامناً** فور تحليل `app.js` (سطر ~16736) باستخدام مصفوفة `stores` كما بُنيت من الدمج الثابت `stores.push(...)` (سطر 37) — وهذا الدمج يضم حرفياً القيمة **المُجمَّدة وقت الإنشاء** `"approvalStatus": "pending"` من كل ملف `<slug>-data.js` (تحقّق: 9 متاجر — شام أوغلو/تركواز/بردايس/بيغ طاووق/عبد الحميد/فاتح الخير/قلعة الشام/ابن الوزير/شيكي ويكي — لا تزال تحمل هذا الحرف حرفياً في ملفاتها المُجمَّعة رغم اعتمادها لاحقاً في Supabase). الدالة الوحيدة التي تُحمَّل بسرعة عند الإقلاع (`loadStoresOnly()`، قبل التأخير المتعمَّد 1800ms لتحميل الكتالوج الكامل) كانت تُضيف فقط المتاجر **غير الموجودة أصلاً** بالمعرِّف (`if (!stores.some(s => s.id === r.id))`) ولا تُحدِّث حقول أي متجر **موجود بالفعل** في المصفوفة — فبقيت هذه المتاجر تعرض «pending» المجمَّد حتى يكتمل إعادة البناء الكامل من Supabase (`loadCatalogFromSupabase`، بعد ≥1.8 ثانية) الذي **يُصفِّر ويعيد بناء المصفوفة بالكامل** فيصحِّح الحالة أخيراً — أي أن أي فتح للوحة الإدارة يعرض لثوانٍ معدودة حالة قديمة زائفة لكل متجر اعتُمد بهذه الطريقة، مطابقاً تماماً لوصف المستخدم «تظهر من جديد بدون موافقة عند كل فتح» | أُصلح جذر السباق في `loadStoresOnly()` (`app.js`): لكل صف Supabase يطابق متجراً **موجوداً بالفعل** في المصفوفة، تُقارَن قيمة `approval_status` الحقيقية بقيمة الكائن المحلي وتُحدَّث فوراً إن اختلفت (بدل تجاهل كل متجر مطابق بالمعرِّف)، وتُعيد الدالة `true` أيضاً عند حدوث أي تصحيح كهذا (لا الإضافة فقط كما كانت) — فيُطلِق `initCatalog()` إعادة رسم فورية بعد هذا الاستعلام السريع (لا حاجة الآن لانتظار الـ1800ms+ لتصحيح الحالة). **تحقّق حي (محاكاة العطل الفعلي ثم إثبات الإصلاح، لا افتراض):** تعيين `approvalStatus="pending"` يدوياً على متجر شام أوغلو (id 108) في `stores` الحيّة (محاكاة تامة لحالة الدمج الثابت المجمَّدة) ثم استدعاء `loadStoresOnly()` المُعدَّلة — أعادت `true` وصحَّحت الحقل فوراً إلى `"approved"`؛ إعادة رسم لوحة «نظرة عامة» بعدها أظهرت «لا توجد طلبات انضمام جديدة حالياً» بدل إدراج المتجر ضمن القائمة زوراً. `node --check app.js` سليم و`npm run check:versions` ناجح | `app.js`, `index.html`, `sw.js`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v354→355، CACHE v273→274؛ styles.css بلا تغيير v192). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-22 | متابعة لنظام العناوين التركي أعلاه: المستخدم شغَّل ملف الـmigration يدوياً لكن `tr_provinces`/`tr_districts`/`tr_neighborhoods` بقيت غير موجودة فعلياً في Table Editor | 🔴 **اكتشافان متتاليان بالتحقّق لا بالافتراض:** (1) `PGRST205` مستمر رغم تشغيل `NOTIFY pgrst, 'reload schema'` مرتين (يدوياً من المستخدم، ثم بانتظار تلقائي) — تبيّن بطلب فتح Table Editor أن الجداول **لم تُنشأ أصلاً**، أي أن أول تنفيذ للـmigration فشل صامتاً (لا سجل للسبب، ربما لصق جزئي أو خطأ لم يُلاحَظ). (2) بعد إعادة التنفيذ يدوياً عبر المتصفح ونجاح الجداول الثلاثة والتعبئة الكاملة (33,374 صفاً)، تحقّق حي كشف علّة برمجية حقيقية في `app.js`: `fetchTrProvinces()`/`fetchTrDistricts()`/`fetchTrNeighborhoods()` كانت تُخزِّن نتيجة الفشل `[]` في الكاش (`trLocationCache.provinces = error ? [] : ...`) — و**`[]` صادقة (truthy) في JS**، فأي فشل عابر واحد فقط (مثال: `window.supabaseClient` لم يجهز بعد لحظة أول استدعاء) كان يُقفل القائمة على "لا نتائج" **إلى الأبد** لبقية عمر الصفحة، رغم أن نموذج Flutter المكافئ (`TrLocationRepository`) كان يتجنّب هذا الخطأ أصلاً (لا يُخزِّن الكاش إلا عند النجاح). 🔴 **درس أدوات إضافي:** لصق ملف الـmigration الكامل (بصيغته الأصلية متعددة الأسطر مع مسافات بادئة) دفعة واحدة في محرر SQL بالمتصفح الآلي تسبَّب مرة بضياع التركيز والتنقّل التلقائي لصفحة أخرى في اللوحة (على الأرجح تفاعل بين الإغلاق التلقائي للأقواس في Monaco ومفاتيح اختصار عامة في تطبيق الويب) — الحل: كل عبارة SQL كسطر واحد متصل بلا فواصل أسطر داخلية، وتنفيذها على دفعات منفصلة (3 جداول → 3 نتائج نجاح مؤكَّدة → فهارس+RLS+سياسات+إعادة تحميل الكاش في دفعة واحدة أخيرة) | نُفِّذ الـmigration فعلياً هذه المرة عبر Claude in Chrome (متصفح المستخدم الحقيقي المسجَّل دخوله في لوحة Supabase) بلصق كل عبارة كسطر واحد + تأكيد نجاح كل دفعة قبل التالية؛ تحقّق مباشر بعدها عبر `curl` أن `tr_provinces` أصبح HTTP 200. شُغِّل `node scripts/seed-tr-locations.mjs` فعلياً فأدرج **33,374 صفاً بالضبط** (81+1010+32283، مطابقة تماماً للمتوقَّع، بلا أي فشل دفعة). أُصلحت دالات الجلب الثلاث في `app.js`: صار الكاش يُخزَّن **فقط عند النجاح الفعلي** (لا يُخزَّن شيء عند أي خطأ/استثناء، فيُعاد المحاولة تلقائياً في الاستدعاء التالي بدل القفل الدائم). **تحقّق حي شامل بعد كل الإصلاحات ضد القاعدة الحقيقية المُعبَّأة بالكامل** (لا محاكاة هذه المرة): كتابة "Istanbul" بلا حروف تركية وجدت "İstanbul" فعلياً، "Basaksehir" وجدت "Başakşehir"، "Kayabasi" وجدت "Kayabaşı Mahallesi" — والمعاينة الحية طابقت تماماً مثال المواصفة الأصلي. حفظ عنوان كامل فعلي أنتج `structured.provinceId=78/districtId=929/neighborhoodId=28587` الحقيقية من القاعدة و`fullAddressTr` الصحيح | `app.js`, `index.html`, `sw.js`, `CLAUDE.md` (لا تعديل على مخطط قاعدة البيانات نفسه — فقط بيانات Supabase عبر لوحة التحكم والسكربت) | نعم — `npm run check:versions` ناجح (app.js v353→354؛ styles.css/CACHE من الجلسة السابقة لم يتغيّرا هذه المرة إلا CACHE→273 لمطابقة app.js). **⚠️ نظام العناوين الآن حيّ وفعّال بالكامل على قاعدة الإنتاج الحقيقية — لم يُنفَّذ `git commit`/push بعد لأي من التغييرين (بناء الميزة + إصلاح الكاش)، بانتظار تعليمات المستخدم**
| 2026-07-22 | المستخدم أرسل أمراً برمجياً كاملاً (18 قسماً) لبناء نظام عناوين تركي إداري كامل (İl/İlçe/Mahalle-Köy، حقول بناء/شقة/بلوك/طابق/رمز بريدي، خريطة غوغل، عرض تركي حصراً) على الموقع والتطبيق معاً | ميزة جديدة ضخمة، لا خطأ سابق — لكن اكتُشفت 3 قيود حقيقية حاكمة قبل التنفيذ: (1) موصل Supabase MCP هنا مربوط بمشروع غير ذي صلة (`iseldofsfhwvpfzltqet`)، ولا `DATABASE_URL`/`pg` محلياً، فلا يوجد أي مسار DDL تلقائي (نفس قيد [[migrations-need-manual-apply]]) — أي جدول جديد يحتاج تشغيلاً يدوياً واحداً في محرر SQL بسوبابيس. (2) لا يوجد مصدر بيانات تركيا كامل (81 ولاية/~1000 قضاء/~32 ألف حي وقرية) محلياً أو داخل المشروع — اضطُرّ الأمر لجلب مجموعة بيانات حقيقية من GitHub (`bertugfahriozer/il_ilce_mahalle`، مصدرها الرسمي TÜİK/İçişleri Bakanlığı حسب توثيق المستودع، ترخيص مفتوح للاستخدام التجاري) ومعالجتها برمجياً (توليد أكواد لوحات الولايات الـ81 الرسمية يدوياً، Title-Case تركي صحيح يميّز İ/I، فصل «كوى/Köy» عن «Mahalle»، تنظيف مسافات مزدوجة). (3) 🔴 اكتشاف مهم أثناء الفحص أزال حاجة DDL ضخمة: كلٌ من `customer_addresses.data` (الموقع) و`orders.delivery_details` (كلا المسارين) و`SavedAddress` في Flutter (محلي بالكامل عبر LocalCache) مرنة (jsonb/كائن Dart) أصلاً — فالحقول الجديدة كلها (نوع/اسم الشارع، اسم الموقع/البناء، البلوك، رقم الباب الخارجي/الداخلي + خيار «بلا رقم داخلي»، الطابق، الرمز البريدي، الوصف، مصدر الموقع) أُضيفت **بلا أي DDL** على هذين الجدولين — الـDDL الوحيد المطلوب هو 3 جداول مرجعية جديدة للقراءة فقط (`tr_provinces`/`tr_districts`/`tr_neighborhoods`) | **قاعدة البيانات:** migration جديد (`20260722_turkish_address_system.sql`، DDL فقط بلا بيانات ليبقى صغيراً وقابلاً للتشغيل اليدوي دفعة واحدة) + سكربت تعبئة منفصل `scripts/seed-tr-locations.mjs` (batched upsert عبر REST بمفتاح service role، آمن لإعادة التشغيل) يقرأ من `data/tr-locations/*.json` (81+1010+32283 صفاً، مولَّدة من مصدر GitHub أعلاه ومطابَقة يدوياً على أمثلة الأمر الأصلي حرفياً — عيّنة "Kayabaşı Mahallesi" في باشاك شهير تحقّقت مطابقتها تماماً). RLS: قراءة عامة فقط (نفس ثقة `stores`/`products`) — **لم يُنفَّذ الـmigration بعد، بانتظار تشغيل المستخدم اليدوي، ثم تشغيلي أنا لسكربت التعبئة بعده**. **الموقع (app.js/styles.css):** أُعيد بناء نافذة «تفاصيل العنوان» (`openAddressModal`) بالكامل: 3 قوائم بحث تركية متتالية (İl→İlçe→Mahalle/Köy، بحث غير حسّاس لللكنة التركية عبر `normTrSearch` يطابق "Basaksehir"→"Başakşehir") + خيار «Listede yok — يكتب يدوياً» يُعلَّم `settlementSource:'manual'` للمراجعة الإدارية لاحقاً، ثم نوع/اسم الشارع (بلاحقة نحوية صحيحة: Cadde→Caddesi، Sokak→Sokağı...) واسم الموقع/البناء والبلوك ورقمي الباب الخارجي/الداخلي (مع خيار «بلا رقم داخلي») والطابق والرمز البريدي والوصف، مع معاينة حيّة تركية بالكامل عبر `composeFullAddressTr()` (طابقت أمثلة الأمر الأصلي حرفياً في الاختبار الحي: "Kayabaşı Mahallesi, Adnan Menderes Caddesi,\nAvrupa Konutları, B Blok,\nDış Kapı No: 18/A, İç Kapı No: 12, Kat: 3,\nBaşakşehir / İstanbul, 34494"). خريطة غوغل الموجودة أصلاً (السحب/التمركز/البحث) أُبقيت بالكامل وتحاول الآن أيضاً مطابقة أفضل تخمين لـ İl/İlçe/Mahalle من نتيجة `googleReverseFull` تلقائياً (مساعدة فقط، لا تحجب الاختيار اليدوي أبداً). حقل الموقع (خريطة) أصبح **إلزامياً** (لم يكن كذلك سابقاً) تطبيقاً صريحاً لبند "Harita Konumu *" في الأمر. رسائل التحقّق التركية الحرفية من الأمر (`Lütfen il seçin.` إلخ) — الاستثناء الوحيد المتعمَّد لعربية باقي الموقع. **نسخة الطلب:** `structuredAddress`/`fullAddressTr` أُضيفا **إضافياً بحتاً** (بلا لمس `address`/`addressDetails` النصّيين القديمين) في `newOrder`/`pushOrderCloud`/`notifyOrderWhatsapp` بـ`app.js` وفي معالج `api/notify-order.js` الافتراضي (المسار الموثوق فعلياً) وفرع `create-order` غير الموصول — نفس مبدأ "لا تلمس مسار الطلب الحسّاس إلا إضافة" من [[orders-silent-save-failure]]. **Flutter:** `TrLocationRepository`/`TrProvince`/`TrDistrict`/`TrNeighborhood` (قراءة مباشرة من نفس جداول Supabase، مطابقةً لنمط `StoreRepository`) + `core/utils/turkish_address.dart` (نسخة طبق الأصل من نفس منطق `composeFullAddressTr`/`normTrSearch` بالموقع) + إعادة بناء `SavedAddress` بالكامل بنفس مجموعة الحقول (مع إبقاء قراءة الحقول القديمة كاحتياط توافقي محلي) + إعادة بناء `address_form_screen.dart` (قوائم بحث sheet قابلة للتمرير/البحث + widget خريطة `GoogleMap` حقيقي بدبوس مركزي ثابت فوق `Stack`، الحزمة كانت مثبَّتة بلا استخدام سابقاً + مفتاح API مُوصَّل مسبقاً في `AndroidManifest.xml`) + تحديث `checkout_screen.dart`/`OrderDraft` بنفس الحقلين الإضافيين. 🔴 **علة اكتُشفت وأُصلحت أثناء الكتابة قبل أي اختبار:** أول نسخة من قائمة اختيار الحي مرَّرت القيمة الخاصة "اكتب يدوياً" عبر `as T` (تحويل غير آمن كان سيرمي استثناء وقت التشغيل تحديداً عند اختيار هذا الخيار) — أُصلح بجعل نوع نتيجة القائمة `Object?` عاماً بدل `T?` وفحص `identical()`/`is T` بدل التحويل القسري. **التحقّق:** موقع — محاكاة كتالوج وهمي كامل في متصفح حي (السلسلة province→district→neighborhood→حقول→معاينة) أنتجت **مطابقة حرفية 100%** لمثالي الأمر الأصلي (شقة إسطنبول ومنزل أنقرة المستقل كلاهما)، واختبار حيّ لرسائل التحقّق (حفظ ناقص محظور فعلياً، حفظ كامل ينجح)، و`node --check app.js`/`api/notify-order.js` سليمان. Flutter — `flutter analyze` أعاد **نفس الأربع ملاحظات المعلوماتية القديمة فقط بلا أي جديد** (تحقّق فعلي، لا افتراض). ⚠️ **لم يُبنَ APK ولم يُشغَّل محاكي** (تكلفة زمنية عالية غير ضرورية بعد نجاح analyze الشامل). ⚠️ **لم تُبنَ لوحة إدارة مناطق كاملة** (دمج/تعطيل/رموز بريدية يدوية من قسم 16 بالأمر) — نطاق منفصل كبير، اكتُفي بأن عناوين الحي اليدوي (`settlementSource='manual'`) قابلة للفلترة لاحقاً من `customer_addresses.data` | `migrations/20260722_turkish_address_system.sql` (جديد)، `scripts/seed-tr-locations.mjs` (جديد)، `data/tr-locations/{tr_provinces,tr_districts,tr_neighborhoods}.json` (جديد)، `app.js`, `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `api/notify-order.js`, `dukkanci_customer_app/lib/core/utils/turkish_address.dart` (جديد)، `dukkanci_customer_app/lib/features/addresses/domain/{saved_address,tr_location}.dart`، `dukkanci_customer_app/lib/features/addresses/data/tr_location_repository.dart` (جديد)، `dukkanci_customer_app/lib/features/addresses/presentation/{address_form_screen,addresses_screen}.dart`، `dukkanci_customer_app/lib/features/checkout/{domain/order,presentation/checkout_screen}.dart`، `dukkanci_customer_app/lib/core/localization/app_strings.dart`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v352→353، styles.css v191→192، CACHE v271→272). **لم يُنفَّذ `git commit`/push — بانتظار تعليمات المستخدم، وبانتظار تشغيله اليدوي لملف الـmigration قبل أن تعمل القوائم المنسدلة فعلياً على الإنتاج (تعمل الآن محلياً فقط بلا بيانات، بلا أي خطأ)**
| 2026-07-22 | متابعة لتصميم حقول صندوق دخول التاجر أعلاه: طلب المستخدم اعتماد نفس التصميم (حدّ برتقالي + مربع أيقونة) على «باقي حقول الموقع» | **قبل التنفيذ:** فحص الموقع كشف أكثر من 60 قاعدة CSS منفصلة لتنسيق الحقول، وأن الكلاسات المشتركة الأساسية (`.modal-form`, `.account-form`, `.form-grid`) تُستخدم في آن واحد لنماذج عامة يملؤها الزوار/التجار (تسجيل الدخول، الانضمام كتاجر، الدفع، العنوان) **وأيضاً** لأدوات داخلية للوحة التحكم (البانرات، التصنيفات، الكوبونات، منتجات التاجر، الكوبون الإداري...). تطبيق تصميم واحد على الكلاس المشترك كان سيُغيِّر شكل عشرات النماذج الداخلية دفعة واحدة، فطُرح `AskUserQuestion` واختار المستخدم صراحة: «نماذج الزوار والتجار العامة فقط». 🔴 **اكتشاف حرج أثناء الفحص:** نافذة تسجيل دخول/إنشاء حساب الزبون (`.auth-modal`، أكثر نموذج عام استخداماً على كامل الموقع) كانت تحمل **نفس علّة الحقول الخام تماماً** التي أُصلحت في صندوق دخول التاجر (`getComputedStyle` أعاد `border: 2px inset rgb(118,118,118)` بلا `border-radius`) — حقولها (`<input>` داخل `.input-label` فقط، بلا `.modal-form`/`.account-form`) لم تكن منسَّقة إطلاقاً منذ إنشائها | **تعديل الأساس المشترك حيث كان آمناً فقط:** `.pw-input`/`.pw-toggle` (زر إظهار/إخفاء كلمة المرور) يُستخدم حصراً في 4 سياقات عامة (دخول التاجر، نافذة الزبون، الانضمام كتاجر) وصفر استخدام إداري — رُفعت القاعدة المشتركة نفسها للتصميم الجديد مباشرة بثقة، وأُزيلت نسخة مكرَّرة كانت مقصورة على صندوق التاجر فقط. كذلك `.coupon-row` (كود الخصم بالدفع + كود الدعوة بالحساب)، `.phone-input` (تسجيل دخول برمز واتساب)، و`.addr2 input` (نموذج العنوان + حقول العنوان داخل بوابة «أكمل بياناتك») — الثلاثة حصرية الاستخدام لنماذج عامة، فعُدِّلت مباشرة. **تعديل مقتصر بمعرِّف فريد حيث الكلاس مشترك مع الإدارة:** أُضيفت قواعد جديدة تستهدف `#customer-profile-form`، `#customer-complaint-form`، `#profile-setup-form .form-grid` بالمعرِّف تحديداً (بلا لمس `.account-form`/`.modal-form` العامة) — فتُصمَّم هذه الثلاثة فقط بينما تبقى عشرات نماذج الإدارة (`#category-form`, `#banner-form`, `#admin-coupon-form`, `#merchant-product-form`...) كما كانت تماماً (تحقّق حي: `#category-form` لا يزال بحدّ رمادي 1px). **إصلاح نافذة دخول الزبون:** أُضيفت قاعدة تنسيق أساسية جديدة `.auth-modal .input-label > input, .auth-modal .pw-input input, .auth-modal select` (حدّ برتقالي، حواف 15px، ارتفاع 48px) — بنفس منطق إصلاح صندوق التاجر، مع استثناء دقيق (`>` مباشر لا أي حفيد) لتفادي تطبيقها خطأً على حقل الهاتف داخل `.phone-input` (له تصميمه الخاص: الحدّ على الصندوق الحاوي لا الحقل نفسه). **أيقونة الهاتف الجديدة:** طُبِّقت على حقل الهاتف في `#customer-profile-form` (بيانات الحساب) و`#profile-setup-form` (بوابة الدفع) عبر غلاف `.field-icon` المشترك — أُعيد تعميم `.merchant-auth__card .field-icon`/`.field-icon__box` (كانتا مقتصرتين على صندوق التاجر فقط في الجلسة السابقة) لتصبحا كلاساً عاماً `.field-icon`/`.field-icon__box` قابلاً لإعادة الاستخدام. **لم تُضَف الأيقونة** لحقل هاتف الانضمام كتاجر (تخطيط ضيق مقصود 210px كان سيصطدم مع حجز 62px للأيقونة) ولا لحقول الاسم/البريد/العنوان (لا يوجد أيقونة مفردة مناسبة لها في `iconPaths`، والحدّ البرتقالي وحده كافٍ لتحقيق الاتساق البصري المطلوب). 🔴 **علّة أدخلتُها بنفسي أثناء إعادة التعميم واكتُشفت بالفحص لا بالافتراض:** تعميم `.field-icon input`/`.pw-input input` (تخصيص عام، أولوية CSS منخفضة نسبياً 0,1,1) كان سيُهزَم بصمت أمام أي قاعدة حدّ أحدث وأكثر تحديداً تُضاف لاحقاً لنموذج عام (مثل `#profile-setup-form .form-grid input` بأولوية معرِّف 1,1,1، أو حتى صندوق التاجر القديم 0,2,1) لأن الحدّ يُعرَّف بصيغة مختصرة `padding:` تطغى على `padding-left` المفرد رغم تأخره في الملف — لو تُرك كما هو كانت الأيقونة ستُرسَم فوق النص المكتوب بدل جانبه. أُضيف `!important` على إعلاني `padding-left` هذين تحديداً (لا أي خاصية أخرى) مع تعليق يشرح السبب. **التحقّق (حي عبر خادم محلي، بمقارنة قبل/بعد فعلية لكل نموذج):** صندوق دخول التاجر (فحص ارتداد) — لا يزال الحدّ والأيقونة وعرض الأزرار الثانوية (329px) سليمة كما كانت؛ نافذة دخول الزبون — الحدّ تحوَّل من `2px inset` خام إلى `2px solid` برتقالي بحواف 15px لحقلي البريد وكلمة المرور معاً، مع `padding-left` الحقل الأخير لا يزال 62px (لم يُطغَ عليه)؛ بوابة «أكمل بياناتك» — الاسم والهاتف (بأيقونته) والعنوان (المتداخل ضمن `.addr2`) الثلاثة بحدّ برتقالي متطابق؛ الانضمام كتاجر — اسم المتجر/التصنيف/الهاتف (**بعرضه المضغوط 210px سليماً بلا أيقونة**)/البريد/كلمة المرور (بأيقونتها) كلها برتقالية؛ صفحة «بياناتي» بالحساب — الثلاثة حقول برتقالية والهاتف بأيقونته؛ كود الخصم بالدفع — برتقالي؛ **نموذج إداري ضابط** (`#category-form`) — بقي بحدّه الرمادي الأصلي 1px بلا أي تغيير (تأكيد أن العزل نجح). صفر أخطاء كونسول عبر كل الاختبارات، صفر انزياح أفقي على 390px. `node --check app.js` سليم وتوازن أقواس `styles.css` محسوب برمجياً (عمق نهائي صفر) | `app.js`, `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v351→352، styles.css v190→191، CACHE v270→271). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-22 | متابعة لإصلاح حقول صندوق دخول التاجر أعلاه: المستخدم أرفق صورة مرجعية (تصميم) تُظهر حقل «رقم الهاتف أو البريد الإلكتروني» وحقل «كلمة المرور» بحدّ برتقالي 2px وحواف دائرية كبيرة، مع أيقونة داخل مربع صغير مُظلَّل بلون برتقالي فاتح وفاصل رأسي رفيع قبلها، وطلب تطبيق هذا الشكل على مربع المعلومات | ليس خطأ — طلب تصميم مباشر بصورة مرجعية دقيقة. حقل «كلمة المرور» كان يملك أصلاً بنية مشابهة جزئياً (`.pw-input`/`.pw-toggle` لزر إظهار/إخفاء كلمة المرور) لكن بحدّ رمادي عادي ومربع أيقونة بلا تظليل لوني؛ حقل «اسم المستخدم» لم يكن يملك أي أيقونة أو مربعاً إطلاقاً (`<input>` مباشرة بلا غلاف) | أُضيف غلاف جديد `.field-icon` (نفس بنية `.pw-input` القائمة: `position:relative;display:flex`) حول حقل اسم المستخدم مع مربع أيقونة هاتف جديد `.field-icon__box` (نفس تصميم `.pw-toggle` المُعاد تصميمه)، وأُعيد تصميم كليهما معاً: حدّ الحقل من رمادي 1px إلى برتقالي حقيقي `var(--orange)` بسماكة 2px وحواف 15px (بدل 11px)، ومربع الأيقونة أصبح خلفية `var(--orange-soft)` (لون موجود مسبقاً في متغيرات الجذر، لا لون مُخترَع) بحواف دائرية 12px ولون أيقونة `var(--orange)`، مع فاصل رأسي رفيع (`::before`، لون `var(--line)`) بين نص الحقل والمربع — يطابق الصورة المرجعية حرفياً. ارتفاع الحقل زيد من 44px إلى 50px ليتّسع المربع (40×40px) بارتياح. **التحقّق (حي عبر خادم محلي):** على 390px — حدّ الحقل `2px solid rgb(242,162,58)` (=`--orange`) وحواف 15px، مربع الأيقونة بخلفية `rgb(255,243,223)` (=`--orange-soft`) ولون `rgb(242,162,58)`، الفاصل الرأسي موجود فعلياً (`::before` بعرض 1px)، صفر انزياح أفقي؛ على 1280px — زر إظهار/إخفاء كلمة المرور لا يزال يعمل وظيفياً (تبديل `type` من password إلى text بنقرة حقيقية)، صفر أخطاء كونسول. `node --check app.js` سليم وتوازن أقواس `styles.css` محسوب برمجياً (عمق نهائي صفر) | `app.js`, `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v350→351، styles.css v189→190، CACHE v269→270). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-22 | متابعة لإعادة تصميم `/merchant` أعلاه: المستخدم أرفق لقطة شاشة موبايل حقيقية لصندوق الدخول وطلب تحسين توزيع كتله للحد الأعلى، تصغير الفونت داخله، «أخذه من اليمين إلى اليسار»، ووضع رقم واتساب إدارة دكانجي (05528000220) في الأسفل | 🔴 **الفحص الحي كشف علّة حقيقية لم تكن مجرد رأي تصميمي:** `getComputedStyle` على حقل الإدخال الفعلي أظهر `border: 2px inset rgb(118,118,118)` بلا أي `border-radius` — أي أن حقول هذا الصندوق (`<input>` داخل `.merchant-auth__card`) **بلا أي تنسيق مخصَّص إطلاقاً** وتُعرض بمظهر المتصفح الافتراضي الخام (UA default)، بينما الزر أسفلها منسَّق بالكامل (تدرّج أحمر، حواف دائرية) — هذا التناقض بين حقل خام وزر مصمَّم هو ما يجعل «التنسيق البصري سيئاً» فعلياً. السبب: بقية نماذج الموقع (`.modal-form input`, `.account-form input`) تحصل على تنسيقها عبر كلاس مشترك على عنصر `<form>` نفسه، لكن نموذج `/merchant` لا يحمل أياً من هذين الكلاسين (فقط `.merchant-auth__card`) — فجوة نطاق CSS لا أكثر. 🔴 **علّة ثانية من نفس الفصيلة:** أزرار `.secondary-button.full` («دخول بحساب بريد إلكتروني»، «ليس لديك متجر؟...») كانت أضيق من الزر الأساسي (275px من أصل ~330px) لأن قاعدة `width:100%` مُعرَّفة فقط على `.primary-button.full` لا `.secondary-button.full` — **نفس الفجوة الموثَّقة سابقاً في سجل 2026-07-15 (زر لصق رمز OTP)** ولم تُصلَح يومها إلا لذلك الزر تحديداً؛ فروقات عرض الأزرار هذه هي ما تُنتج فعلياً «توزيع كتل غير متناسق». «من اليمين إلى اليسار» فُهمت (بعد تعذّر تفسيرها كتبديل موضع، إذ الشطر الأيمن/الأيسر لسطح المكتب سليم من الجلسة السابقة) كطلب تمديد الصندوق أفقياً ليقترب من حافتي الشاشة (كان الهامش الكلي من حافة الشاشة حتى محتوى الحقل ٤٢px [١٦ خارجي + ٢٦ داخلي] على عرض ٣٩٠px، أي ~٢١٪ من العرض مهدوراً كل جهة) | **إصلاح حقول الإدخال:** قاعدة CSS جديدة مُقتصرة على نطاق `.merchant-auth__card input:not([type="checkbox"])`/`select` بنفس قيم `.modal-form input` (حدّ خفيف، حواف ١١px، خلفية بيضاء) لكن بحجم أصغر يناسب صندوقاً مضغوطاً (ارتفاع ٤٤px بدل ٤٧، خط ١٤px بدل ١٦ الافتراضي)، + حالة `:focus` بحدّ أحمر خفيف. **إصلاح عرض الأزرار الثانوية:** قاعدة مُقتصرة أيضاً `.merchant-auth__card .secondary-button.full{width:100%}` (نفس منطق إصلاح ٢٠٢٦-٠٧-١٥، بلا لمس الكلاس العام الذي قد يؤثر على أزرار أخرى في الموقع). **تصغير الفونت وتحسين توزيع الكتل:** عنوان البطاقة ٢٤px→١٩px، الفقرة الإرشادية ١٣.٥px→١٢.٥px، تسمية الحقل ١٣px→١١.٥px (مُقتصرة على هذا الصندوق فقط)، الأزرار ١٦px→١٤px، الفاصل ١٢px→١١.٥px مع تقليص هوامشه (١٩px→١٥px) لإيقاع رأسي أكثر انسجاماً. **التمديد الأفقي:** حشوة `.merchant-auth` الخارجية على الموبايل (٢٢px ١٢px بدل ٤٠px ١٦px) وحشوة البطاقة الداخلية (٢٤px ١٦px بدل ٣٢px ٢٦px)، فينخفض الهامش الكلي من الحافة حتى محتوى الحقل من ٤٢px إلى ٣١px لكل جهة. **رقم واتساب الإدارة:** استُبدل السطر النصي الغامض («تواصل عبر واتساب») بصندوق تواصل حقيقي (أيقونة واتساب بلون أخضر حقيقي `#1f9d57` مستخدَم مسبقاً في صفحة «لماذا دكانجي؟» — لا لون مُخترَع) + عنوان فرعي + رقم ظاهر وقابل للنقر **«+90 552 800 02 20»** (`dir="ltr"`) يفتح فعلياً `wa.me/905528000220`، في موضعي الظهور (شاشة دخول كلمة مرور الإدارة، وشاشة «لا يوجد متجر مرتبط»). **التحقّق (حي عبر خادم محلي، بمقارنة قبل/بعد فعلية لا افتراضية):** على ٣٩٠px — الحقل تحوَّل من حدّ خام بلا زوايا (٢٦px ارتفاعاً) إلى حدّ ١px برادius ١١px (٤٣px ارتفاعاً)، الزران الثانويان تحوّلا من ٢٧٥/٢٦٥px إلى ٣٢٧px متطابقَين مع الزر الأساسي، عرض البطاقة ٣٦١ من ٣٩٠ (بدل ٣٥٢)، صفر انزياح أفقي؛ على ١٢٨٠px (سطح المكتب) نفس الإصلاحات سليمة وبلا أي تراجع عن تصميم الشطرين المبني في الجلسة السابقة؛ نص واتساب الجديد ظاهر فعلياً «لم تستلم بياناتك أو تحتاج مساعدة؟ +90 552 800 02 20»، صفر أخطاء كونسول على كلا العرضين. `node --check app.js` سليم وتوازن أقواس `styles.css` محسوب برمجياً (عمق نهائي صفر) | `app.js`, `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v349→350، styles.css v187→189، CACHE v267→269). **مدفوع لـ`main`** بناءً على طلب المستخدم الصريح
| 2026-07-22 | المستخدم أبلغ أن صفحة دخول أصحاب المتاجر (`/merchant`) «فارغة إلا من نموذج طلب معلومات التاجر»، وأن العبارة الإرشادية داخل الصندوق تظهر بخط غامق وشكل غير مناسب، وطلب إعادة تصميم الصفحة مع حركة ومعلومات مناسبة | `document.body.classList.toggle("dashboard-view", route === "merchant" \|\| route === "admin")` يخفي الهيدر/الفوتر/شريط التنقّل السفلي على هذا المسار بالكامل (`.dashboard-view .site-header/.site-footer/.mobile-nav {display:none}`) — وهذا مقصود لصفحات لوحة التحكم الداخلية، لكنه ينطبق أيضاً على **شاشة الدخول نفسها** قبل أي مصادقة، فيترك الزائر أمام صندوق نموذج (`merchantLogin()`) وحيداً على خلفية فارغة بلا أي هوية أو سياق. **العبارة الغامقة:** تحقّق مباشر بـ`getComputedStyle` أكّد أن `.merchant-auth__card > p` كانت `font-weight:700` رغم عدم تصريح هذه القاعدة بذلك — السبب أن `body { font-weight:700 }` هو الوزن الافتراضي لكامل الموقع (خط Alyamama/Tajawal ثقيل بالتصميم)، والفقرة لم تكن تُصرِّح وزناً مختلفاً فورثت الوزن الثقيل غير المناسب لنص إرشادي طويل | أُضيفت شاشة غلاف مشتركة جديدة `merchantAuthAsideHTML()`/`merchantAuthShell()` في `app.js` تُغلِّف **كل** حالات `/merchant` (تسجيل الدخول بأنماطه الثلاثة: كلمة مرور الإدارة/بريد+Google/رمز واتساب، وكذلك `merchantResolving()` أثناء التحقق و`merchantNoStore()` عند عدم ربط متجر) بتخطيط شطرين (`.merchant-portal`، شبكة CSS): لوحة علامة تجارية داكنة (تدرّج بألوان الموقع الحقيقية `--green-950/--green-900` + بقعتا زخرفة عائمتان بالأحمر/البرتقالي الحقيقيين `--orange`/`rgba(227,6,19,...)` بحركة `@keyframes merchantFloat` بطيئة وغير مزعجة) تحوي شعار الموقع الحقيقي + عنوان + 4 نقاط فائدة حقيقية (إشعار فوري بالطلب، تحكّم بالمنتجات، توصيل بشروط التاجر، دعم واتساب) بحركة دخول متدرّجة (`merchantFadeUp` مع تأخير `--i` تصاعدي لكل عنصر)، **وشارة عدد متاجر حقيقي حيّ** `+{stores.filter(isStoreApproved).length} متجر يبيع على دكانجي الآن` مبنية من نفس مصفوفة `stores` التي يعرضها الموقع فعلياً (لا رقم مُختلَق — نفس مبدأ الصدق المتكرر في هذا المشروع)؛ وبطاقة النموذج نفسها حصلت على حركة دخول (`merchantCardIn`) بدل الظهور المفاجئ. أُصلحت الفقرة الإرشادية بتصريح `font-weight:500` صريح على `.merchant-auth__card > p` (بدل وراثة `700` من `body`) مع تحسين `line-height`/`margin`. على الموبايل (≤900px) تتقلّص اللوحة لشريط علوي مضغوط (شعار+عنوان+فقرة+الشارة فقط، **بلا** قائمة النقاط الأربع لتفادي إطالة التمرير قبل الوصول لحقول الدخول الفعلية) والتخطيط يعود لعمود واحد. **التحقّق (حي عبر خادم محلي):** `node --check app.js` سليم وتوازن أقواس `styles.css` محسوب برمجياً (عمق نهائي صفر)؛ على 1280px: الشطران بعرض 400px/880px، لوحة العلامة تظهر يميناً والنموذج يساراً (اتساق RTL صحيح، `document.documentElement.dir==="rtl"`)، الشارة تعرض فعلياً `+97 متجر يبيع على دكانجي الآن` (رقم حي من قاعدة البيانات لا وهمي)، ووزن خط الفقرة أصبح `500` محسوباً فعلياً بدل `700`؛ على 375px: عمود واحد، قائمة النقاط الأربع مخفية بشكل صحيح (`display:none`)، صفر انزياح أفقي (`scrollWidth===innerWidth===375`)؛ تبديل شاشة الدخول لوضع البريد/Google (`دخول بحساب بريد إلكتروني`) عرض شاشة `لوحة المتجر` بنفس اللوحة الجانبية بلا أي كسر؛ صفر أخطاء كونسول عبر كل الحالات المختبَرة | `app.js`, `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v348→349، styles.css v186→187، CACHE v266→267). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-22 | المستخدم أرفق ملف HTML تصميم جاهز (مبنيّ بألوان/خطوط غريبة عن المشروع: أحمر/زعفراني بخط Lalezar+Tajawal+IBM Plex Mono) وطلب تنفيذه على صفحة «لماذا دكانجي؟» (`/why-dukkanci`) **مع الحفاظ الكامل على الهوية البصرية الحقيقية** (الشعار، الخطوط، الألوان) | ليس خطأ — طلب إعادة تصميم. الفرق الجوهري: الملف المرفق تصميم كامل مستقل (`<html>`/`<head>`/خطوط Google Fonts خاصة به/شعار حرفي منسوج بـCSS) بينما `/why-dukkanci` فعلياً مسار SPA (`renderWhyDukkanciPage()` في `app.js`، الأسطر ~10210-10414) يُحقن فقط داخل `<main>` لِـ`index.html` الحيّ — الهيدر/الشعار/الفوتر الحقيقيون يأتيان مجاناً من قشرة الموقع نفسها ولا يجوز إعادة إنشائهما. لذا المطلوب هو استخراج *بنية وتخطيط* التصميم (بطاقة إيصال شفافية السعر، بطاقات مقارنة قبل/بعد، فواصل أقسام، إلخ) فقط، وإعادة بنائه بالكامل فوق متغيرات الألوان (`--green-800` #e30613 الأحمر الأساسي الحقيقي، `--green-900` العنّابي الغامق، `--orange`/`--orange-dark`، `--cream`/`--cream-dark`، `--ink`/`--muted`/`--line`) وخطوط الموقع الحقيقية (`Alyamama` + `Tajawal Display` + `IBM Plex Sans Arabic` عبر `var(--font-display)`/`var(--font-body)`) بدل أي من ألوان/خطوط الملف المرجعي، والشعار الحقيقي (`/assets/dukkanci-logo.png`) يبقى كما هو في الهيدر العام دون لمسه. **قرارا صدق بيانات إضافيان اتُّخذا أثناء التنفيذ:** (1) لم تُنقل فكرة "أشرطة نسبة مئوية لجودة الخدمة ٪٩٦" ولا "تقييم إجمالي ٤.٨" من التصميم المرجعي لأنها أرقام مُختلَقة لا تستند لأي مقياس حقيقي مجمَّع فعلياً على المنصة (يخالف مبدأ الصدق المتكرر في هذا الملف) — أُبقيت بطاقات التقييم الوصفية الأصلية بلا أي رقم مزيّف. (2) لم يُنقل الشريط المتحرك تلقائياً (marquee) للفئات — يخالف صراحة قراراً سابقاً موثَّقاً في هذا المشروع (قسم الفئات على الموبايل 2026-06-29: "auto-marquee REJECTED... horizontal strips = manual swipe, never auto-motion") — استُبدل بصف ثابت من رقائق (pills) للفئات الحقيقية (`homeCategoriesOrdered()`) بلا أي حركة، بنفس آلية `data-action="category"` المستخدمة أصلاً في `categoryCard()` فتُفتح صفحة القسم الصحيحة عند النقر بلا أي JS جديد. **إعادة الهيكلة:** أُعيد بناء 11 القسم بالكامل بمحتوى حقيقي محفوظ من النسخة القديمة (نفس نصوص المشكلة/الخطوات/المقارنة/الأسئلة الشائعة حرفياً، حفاظاً على تطابق محتوى قشرة SSR في `api/why-dukkanci.js`): (1) هيرو ببطاقة "إيصال" شفافية سعر مُعاد تصميمها (حافة ورق ممزّق + شارة عائمة + صف "عمولة دكانجي: صفر" مُبرَز — أرقام مثال موصوفة صراحةً بـ"مثال توضيحي" وليست ادّعاء ببيانات حقيقية)، (2) شريط فئات ثابت جديد، (3) بطاقتا "قبل/بعد" (مشكلة واتساب مقابل تجربة دكانجي) بدل شبكة 4 بطاقات قديمة، (4) قسم وضوح الأسعار مع بطاقة "٠٪ عمولة" غامقة (حقيقة تعريفية للنموذج التجاري، لا إحصائية)، (5-11) بقية الأقسام (تقييمات، خطوات، أول طلب بلا قلق، المقارنة، الفئات الحقيقية، الأسئلة الشائعة، الدعوة الختامية + رابط واتساب دعم حقيقي عبر `SUPPORT_WA` الثابت الموجود مسبقاً). حُذفت الأنماط الميتة المرتبطة بالأقسام المُستبدَلة (`.why-grid--4`, `.why-price*`, `.why-assure`) من `styles.css` وأُضيفت أنماط الأقسام الجديدة بنفس رموز الألوان الحقيقية (بما فيها لون النجاح الأخضر الحقيقي `#1f9d57` المستخدَم مسبقاً في نفس الصفحة، وليس لوناً مخترَعاً). **التحقّق (حي عبر preview محلي، لا افتراضات):** كل الأقسام الـ11 ظاهرة بعدّاداتها الصحيحة (3 ثقة، 8 فئات، بطاقتا vs بـ4 عناصر لكل منهما، 3 نقاط وضوح أسعار، 6 بطاقات تقييم، 5 خطوات، 5 صفوف مقارنة، 8 بطاقات فئة حقيقية، 5 أسئلة شائعة مع فتح أول سؤال تلقائياً)، الألوان المحسوبة فعلياً تطابق المتغيرات الحقيقية (`h1` = rgb(71,11,16) = `--green-900`، التظليل = rgb(227,6,19) = `--green-800`، الشارة = rgb(242,162,58) = `--orange`)، الخط المحسوب = `Alyamama, "Tajawal Display", "IBM Plex Sans Arabic"` (لا أثر لخطوط الملف المرجعي)، الشعار الحقيقي `/assets/dukkanci-logo.png?v=71` سليم في الهيدر، صفر انزياح أفقي على 1280px و375px (بطاقتا vs وقسم وضوح الأسعار تتكدّسان بعمود واحد على الموبايل مع دوران السهم 90 درجة)، النقر على رقاقة فئة حقيقية ("مطاعم") نقل فعلياً لـ`/category/restaurants` وعرض 100+ متجراً، تبديل الأسئلة الشائعة (توسيع سؤال ثانٍ مع بقاء الأول مفتوحاً) يعمل بشكل صحيح (`<details>` أصلية بلا JS إضافي)، والصفحة الرئيسية بلا أي أثر جانبي، صفر أخطاء كونسول على كل الصفحات المفحوصة. `node --check app.js` سليم وتوازن أقواس `styles.css` محسوب برمجياً (عمق نهائي صفر) | `app.js`, `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v347→348، styles.css v185→186، CACHE v265→266). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-20 | المستخدم أرسل لقطة شاشة حية لثلاثة منتجات مكدوس بمتجر سلسبيل تُظهر أغطية الجرار مقطوعة في شبكة المنتجات (متابعة لإصلاح صور المتجر في نفس اليوم) | `.product-card__image` في `styles.css` (سطر 2710) يفرض `aspect-ratio: 1.15` (أعرض قليلاً من المربع) مع `object-fit: cover` على الصورة — بينما صور سلسبيل المُنظَّفة كانت مربّعة تماماً (1000×1000) مع هامش 7% فقط أعلى/أسفل المنتج (`fill_ratio=0.86`). القصّ التلقائي لتغطية صندوق 1.15:1 من صورة مربّعة يحذف ~6.52% من الأعلى و~6.52% من الأسفل — وهذا أكبر بقليل من الهامش المتاح (7%)، فقصّ الغطاء/القاعدة فعلياً. **درس فرعي:** أول محاولة إصلاح استخدمت قناعاً مثلثياً واحداً لإزالة شارة وزن "٢كغ" (مطبَّق على كل الـ17 صورة لتبسيط الكود) نجح مع عبوة الزيتون لكنه قصّ فعلياً غطاء أوسع جرة (مكدوس 2.9كغ) لأن الجرة تقع أكثر يساراً/أعلى داخل إطارها، فاكتُشف بمحاكاة القصّ الفعلي على الـ17 صورة معاً (وليس عيّنة صغيرة) بدل الاكتفاء بفحص 2-3 عيّنات كما حدث في الجولة السابقة التي فاتها هذا العطل تحديداً | (1) خُفِّض `fill_ratio` من 0.86 إلى 0.72 (هامش 14% حول المنتج بدل 7%، ضعف الحدّ الأدنى المطلوب لتغطية قصّ الـ6.52%). (2) قناع إزالة شارة الوزن المثلثي صار يُطبَّق **فقط** على المنتج الوحيد الذي يحمل شارة فعلية (زيتون 2كغ) بدل تطبيقه على كل الـ17 افتراضياً. **التحقّق الحاسم هذه المرة:** محاكاة برمجية فعلية لقصّ `aspect-ratio:1.15 + object-fit:cover` (نفس معادلة CSS تماماً) على **كل الـ17 صورة معاً** في شبكة مراجعة واحدة قبل النشر — تأكَّد أن كل جرة/زجاجة/كيس تظهر كاملة (غطاء+قاعدة) بلا أي قصّ، مع بقاء إزالة شارة الوزن وبقايا بطاقة السعر سليمة. أُعيد أيضاً بناء صورة الغلاف (شبكة 6×3) من نفس الصور المُصحَّحة للاتساق. تحقّق حي إضافي عبر preview محلي: أبعاد حاوية الصورة الفعلية في DOM = نسبة 1.150 بالضبط (مطابقة لِـCSS)، الصور تُحمَّل بأبعاد 800×800 بلا أخطاء كونسول | `assets/photos/salsabeel/cover.jpg`, `assets/photos/salsabeel/p1.jpg`…`p17.jpg` (استبدال في مكانها مجدداً)، `CLAUDE.md` | لا ينطبق (صور فقط) — **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-20 | المستخدم طلب إعادة تصميم غلاف متجر سلسبيل ليشمل صور كل المنتجات، وتنظيف صور المنتجات الـ17 لتظهر على خلفية بيضاء نقية بزووم مناسب يُظهر المنتج كاملاً | ليس خطأ — تحسين بصري. صور المنتجات الأصلية (مقصوصة من بطاقات كتالوج PDF في الجلسة السابقة) كانت على خلفية كريمية (~#F9F6ED) لا بيضاء نقية، وبعضها (11 من 17) كان يحمل بقايا مرئية من عناصر البطاقة المجاورة: شريحة من صندوق السعر الأخضر/الأسود على الحافة اليمنى (كل الـ17 فعلياً بفروق متفاوتة)، وشارة وزن مائلة خضراء ("٢ كغ") في الزاوية العلوية اليسرى لمنتج واحد (زيتون 2كغ). الغلاف كان صورة منتج واحد فقط (زيتون بالليمون والزيت) لا يمثّل كامل الكتالوج | **تنظيف الصور:** أُعيد اشتقاق كل صورة من نفس صفحات PDF الأصلية (لا تدهور جودة إضافي) عبر: (1) قصّ أضيق لمنطقة الصورة داخل كل بطاقة (0.50 من عرض البطاقة بدل 0.58 السابقة، مع هامش داخلي أكبر 35-50px) لتفادي حافة صندوق السعر والحافة الدائرية للبطاقة نفسها من الأساس. (2) تسوية الخلفية للأبيض النقي: تحديد لون الخلفية المرجعي لكل صورة بأخذ الوسيط (median) من شرائط حواف الصورة (مقاوم لأي بقايا)، ثم مزج تدريجي (alpha blend) لأي بكسل قريب منه إلى أبيض خالص #FFFFFF — يحافظ على حواف الظل الناعمة تحت الجرة بدل قطعها بشكل خشن. (3) إزالة هندسية مستهدفة لِما تبقّى من عناصر البطاقة المجاورة: شريط عمودي في أقصى 18% يمين الإطار (حيث لا يصل أي منتج فعلياً) يُحذف إن كان أخضر/داكن/رمادياً محايداً، وقناع مثلثي قطري في الزاوية العلوية اليسرى (حيث تقع شارات الوزن) — نطاقاه (0.76 يميناً، مثلث قطره 0.50×0.24) اختُبرا يدوياً على أعرض جرّتين (مكدوس 2.9كغ/2كغ) للتأكد أنهما لا يقصّان الجرة نفسها، بعد أن أثبتت محاولة أولى أكثر عدوانية (0.70) قصّاً حقيقياً لحافة جرة دبس الفليفلة الحلوة. (4) اقتصاص محكم على المحتوى الحقيقي المتبقي بعد التنظيف (bounding box + هامش 5-6%) وتوسيط على قماشة بيضاء 1000×1000 بحيث يملأ المنتج ~86% من الإطار (زووم مناسب يُظهر كامل المنتج، لا صغيراً تائهاً ولا مقصوصاً). **تصحيح تلقائي فشل مرتين قبل النجاح:** أول محاولة استخدمت حدّ فاصل ثابت لكسر عرض البطاقة (0.58) فتبيّن بالقياس البرمجي أن شريط السعر يبدأ عند نسب متفاوتة بين البطاقات (0.46-0.57) بسبب اختلاف طول نص العنوان، فاعتُمد حدّ متحفّظ (0.50) بدل حدّ واحد "آمن للجميع" غير موجود عملياً؛ ومحاولة ثانية لحذف قناع الزاوية استهدفت الزاوية العلوية اليمنى خطأً (افتراض غير مُتحقَّق منه) بينما شارات الوزن الحقيقية في الزاوية العلوية **اليسرى** — اكتُشف ذلك بفحص القصّة الخام لبطاقة "زيتون 2كغ" مباشرة قبل تعميم الإصلاح. **الغلاف الجديد:** شبكة 6×3 (18 خانة) من الصور الـ17 النظيفة مرتّبة حسب فئتها (مكدوس، زيوت وزيتون، دبس ومربيات، أخرى) + شعار الشركة في الخانة الأخيرة على لوحة بالأخضر البرندي، بخلفية كريمية فاتحة موحّدة وخطوط فاصلة رفيعة بين الخانات، بأبعاد 1600×900 — تم **محاكاة القصّ الفعلي** (كما تفرضه خاصية `object-fit:cover` في الموقع) لكلا نسبتي الهيرو (~2.36:1) والبطاقة (~1.46:1) قبل الاعتماد: كلاهما يُظهر الشبكة كاملة بوضوح (فقدان جزئي متوقَّع للأعمدة/الصفوف الطرفية فقط، لا قصّ يُخفي مضمون الشبكة). تحقّق حي عبر preview محلي: كل الـ19 رابط صورة (17 منتج + شعار + غلاف) HTTP 200، صفر خطأ كونسول | `assets/photos/salsabeel/cover.jpg`, `assets/photos/salsabeel/p1.jpg`…`p17.jpg` (استبدال في مكانها، بلا تغيير على أسماء الملفات فلا حاجة لأي تعديل على `salsabeel-data.js`/`app.js`/إصدارات الكاش)، `CLAUDE.md` | لا ينطبق (صور فقط، بلا تغيير على app.js/styles.css/أسماء الملفات) — **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-20 | المستخدم طلب طريقة لإدراج «التوصيل متاح لكافة الولايات التركية» لبعض المتاجر، وتفعيلها فوراً لمتجر سلسبيل (منتجات مونة تُشحَن منطقياً عبر كارجو لعموم تركيا، لا توصيلاً محلياً بالكيلومتر فقط) | ميزة جديدة (لا خطأ سابق). قبل التصميم، تحقّقتُ من آلية التوصيل الحالية (`estimateDeliveryQuote`/`getDeliverySettings`) واكتشفت أنها تدعم أصلاً وضع `mode:"fixed"` (سعر ثابت بلا أي قيد مسافة) — لكنه لو طُبِّق كاملاً على متجر لألغى سعره المحلي الرخيص بالكيلومتر لزبائن إسنيورت القريبين لصالح سعر كارجو أعلى للجميع. كذلك تحقّقتُ أن `priceOnRequest` (المستخدَم سابقاً لـTwo2Bees/ماركت أيهم) يُسقط السلة والأسعار الحقيقية بالكامل ويحوّل كل شيء لواتساب — غير مناسب هنا لأن سلسبيل له أسعار حقيقية وسلة عاملة يجب الإبقاء عليها للزبائن المحليين | **حل إضافي لا استبدالي:** حقل جديد اختياري `nationwideFlatFee` داخل كائن `deliverySettings` لكل متجر (يعيش بالكامل في الملف المُجمَّع/`localStorage`، **صفر تعديل على قاعدة بيانات Supabase** — نفس طبيعة `ratePerKm`/`maxRoundTripKm` الموثَّقة أصلاً في [[delivery-settings-localstorage]]). في `estimateDeliveryQuote()`: عند تجاوز العنوان `maxRoundTripKm` (الذي كان سابقاً يحظر الطلب برسالة «خارج النطاق»)، إن كان `nationwideFlatFee` معرَّفاً يُعاد بدلاً من الحظر عرض سعر ثابت (`provider:"nationwide", exceedsMaxDistance:false`) — وبما أن `cartTotals()`/`updateCheckoutPricing()`/حارس الإرسال النهائي كلها تتحقق من `exceedsMaxDistance` فقط (لا من `maxRoundTripKm` مباشرة)، فهذا التعديل الوحيد في الوظيفة الأساسية كافٍ لفتح الطلب تلقائياً بلا أي تعديل آخر في مسار الدفع. الزبون القريب (داخل النطاق) يستمر يحصل على السعر المحلي الرخيص كما هو، ولا يتأثر أي متجر آخر (تحقّق انحدار: متجر 56 بلا `nationwideFlatFee` بقي يُحظَر تماماً كالسابق لعنوان بعيد). أُضيفت `hasNationwideShipping(store)` (مصدر حقيقة واحد يقرأ نفس الحقل، بلا علم منفصل يحتاج مزامنة) و**شارة مرئية جديدة** «📦 توصيل لكل الولايات التركية» (صنف CSS `.nationwide-badge`، أعلى يسار الغلاف — الزاوية الوحيدة الفارغة من الشارات الثلاث الموجودة أصلاً: `status-badge` أعلى يمين، `official-branch-badge` أسفل يسار، `offer-ribbon` أسفل يمين) على بطاقة المتجر (نسختين) وصفحته الكاملة. `renderDeliveryQuoteDetails()` حصلت على فرع جديد `provider === "nationwide"` يعرض رسالة صريحة «شحن عبر شركة الشحن (كارجو)» بدل رسالة الحظر القديمة. **رسم الشحن الثابت (350 ل.ت) طُلب صراحة من المستخدم عبر `AskUserQuestion`** (لم أخترعه — لا معرفة حقيقية بتكلفة كارجو سلسبيل الفعلية، ومنتجاته زجاجية ثقيلة نسبياً). تحقّق حي عبر preview محلي: عنوان قريب (إسنيورت) ⇐ سعر محلي بالكيلومتر ١٥٠ ل.ت كما هو؛ عنوان بعيد (أنقرة، ~479كم) ⇐ `exceedsMaxDistance:false` وسعر ثابت ٣٥٠ ل.ت وبطاقة «كارجو» الصحيحة، السلة/الدفع غير محظورين؛ الشارة ظاهرة بالنص الصحيح بلا تداخل مع الشارات الأخرى (تحقّق إحداثيات `getBoundingClientRect`)؛ متجر آخر (56) بلا الحقل الجديد بقي محظوراً تماماً كالسابق (صفر انحدار)؛ صفر أخطاء كونسول | `app.js`, `styles.css`, `salsabeel-data.js`, `index.html`, `merchant-app.html`, `customer-app.html`, `sw.js`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v346→347، styles.css v184→185، CACHE v264→265). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-20 | المستخدم طلب إضافة متجر «سلسبيل» (Selsebil Gıda، مصنع مونة عربية تركي) ضمن تصنيف «مواد غذائية متخصصة»، معطياً ملفي إكسل (قائمة أسعار 28 صنفاً بعمود «Purchase Rate») وPDF (كتالوج صور 7 صفحات، ~40 صنفاً) + هاتف (+90 534 489 54 66) + عنوان غوغل (baglarşişme Mah, Bağlarçeşme, 1183. Sk. 2A, Esenyurt) | ميزة إضافة جديدة (لا خطأ سابق) — أول مصدر لهذا المشروع بصيغة **ملفَي أوفيس منفصلين لا رابط ويب حي**: إكسل بأسعار حقيقية بلا صور، وPDF بصور حقيقية (كل صفحة صورة مفردة مصدَّرة من Photoshop، لا نص قابل للاستخراج) بلا كل الأسعار — **تقاطع الملفين لا اتحادهما هو الناشر الصحيح**: صنف له سعر بلا صورة (مربى كرز، يبرق ورق عنب، مربى باذنجان، زيتون مقطع سلطة 330غ = 6 أصناف) أو له صورة بلا سعر مطابق (كشك، لبنة، مخلل لفت 1/2كغ، مخلل خيار 1كغ [الإكسل يذكر وزناً مختلفاً 600/800غ لنفس الصنف — تضارب داخلي في ملف التاجر نفسه]، أحجام أخرى من الزيت/الزيتون/السمن) استُبعد بالكامل تطبيقاً حرفياً لقاعدة «لا سعر أو لا صورة = لا نشر» — 17 صنفاً فقط اجتازا التقاطع من أصل ~40+28. **قيد تقني حقيقي:** لا أداة تفريغ PDF (`pdftoppm`/poppler) متاحة في البيئة، فاستُخدم `PyMuPDF` (متوفر أصلاً على الجهاز) لتصيير كل صفحة كصورة، ثم **اشتقاق حدود شبكة البطاقات (٢×٣) برمجياً** عبر تحليل إسقاط صفوف/أعمدة قناع السطوع (`numpy`، عتبة "خلفية فاتحة") بدل تخمين الإحداثيات — كشف ذلك خطأً حقيقياً في أول محاولة (تعيين رقم صفحة خطأ لأربعة أصناف دبس فوقعت قصاصاتها الأولى فعلياً على منتجات زيتون من صفحة أخرى) اكتُشف بمراجعة بصرية لشبكة تصغير لكل الاقتصاصات الـ17 قبل الاعتماد، وحدود الصف الأخير (الأسفل) في كل صفحة كانت ممتدة بذيل زائد من عناصر الفوتر البيضاء (أيقونات التواصل) قبل تصحيحها بمقارنة صريحة مع لقطة كاملة للصف. **مطابقة Google Maps بدقة ROOFTOP على العنوان المُرسَل حرفياً** (Geocoding API) أعادت مكاناً حقيقياً بنفس الاسم ("شركة سلسبيل للمواد الغذائية")، ونفس رقم هاتف كتالوج PDF بالضبط (+90 537 600 68 70) في حقل «الهاتف العام» لغوغل — يؤكد نفس الكيان — لكنه **مختلف عن رقم واتساب الطلب الذي أرسله المستخدم صراحة** (+90 534 489 54 66)؛ اعتُمد رقم المستخدم لحقلي `phone`/`whatsapp` (قناة الطلب الفعلية) تطبيقاً لنفس سابقة قلعة الشام/بردايس (رقمان حقيقيان لقناتين مختلفتين، لا خطأ). تقييم غوغل منخفض العدد (3.8/4 تقييمات فقط) فاعتُمد `rating:0, reviews:0, newStore:true` (نمط ماركت أيهم) مع حفظ `googleRating/googleReviewsCount` منفصلة لميزة الدليل. الشعار قُصَّ من ترويسة الكتالوج نفسها (نص "Selsebil" الخطي + ورقة خضراء)، والغلاف صورة منتج حقيقية عالية الجودة (زيتون أخضر بالليمون والزيت 2كغ مع غصن زيتون وقطرات زيت) بعد **محاكاة قصّ فعلية بنسبتي الهيرو والبطاقة** أثبتت بقاء النص والجرة كاملين. 4 تصنيفات فرعية منطقية (مكدوس 5، زيوت وزيتون 4، دبس ومربيات 5، منتجات أخرى 3: خل تفاح+فريكة+سمنة). وُصلت الأسلاك الأربعة الإلزامية (`app.js`: `stores.push`, `products.push`, `salsabeelDeliverySettings` spread + `store-slugs.js` + `<script>` في `index.html` قبل `app.js` + `sw.js` `APP_SHELL`)، والدفع لـSupabase عبر سكربت Node مباشر بـ`SUPABASE_SERVICE_ROLE_KEY` (موصل Supabase MCP في هذه الجلسة مربوط بمشروع شخصي غير ذي صلة `iseldofsfhwvpfzltqet`، نفس العطل الموثَّق مراراً)، مع تحقّق `max(id)`/تصادم نطاق المنتجات مباشرة قبل الكتابة (المتجر 113، المنتجات 1930001-1930017، صفر تصادم). `approval_status='pending'` عمداً (طرف ثالث حقيقي غير مؤكَّد كشراكة). **التحقّق (حي على preview محلي):** 17 من 17 منتجاً ظاهرة بأسعارها وتصنيفاتها الصحيحة مطابقة حرفياً لصفوف Supabase (تحقّق برمجي صف-بصف)، 19 من 19 رابط صورة (17 منتج + شعار + غلاف) أعادت HTTP 200 فعلياً، العنوان/الساعات/الهاتف/البريد ظاهرة بشكل صحيح، `/category/specialty-food` **لا** يعرض المتجر إطلاقاً (سلوك `pending` صحيح، تحقّق مباشر `data-id="113"` غائب) بينما الرابط المباشر `/store/selsebil-esenyurt` يعمل، صفر أخطاء كونسول | `salsabeel-data.js` (جديد)، `assets/photos/salsabeel/**` (جديد، 19 ملف: 17 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 113 وصفوف `products` 1930001-1930017 في Supabase | نعم — `npm run check:versions` ناجح (app.js v345→346، store-slugs.js v262→263، CACHE v263→264؛ styles.css بلا تغيير v184). **لم يُنفَّذ `git commit`/push — بانتظار تعليمات المستخدم. المتجر `approval_status='pending'` عمداً** — يحتاج اعتماداً صريحاً ليظهر للزوار |
| 2026-07-20 | المستخدم أرفق لقطة نتائج غوغل لكلمة «دكانجي» وطلب أن تظهر الصفحات الأساسية كروابط فرعية (sitelinks): «اطلب من المتاجر»، «دليل المطاعم والمتاجر»، «عروض دكانجي»، «انضم إلى دكانجي كتاجر»، «المتاجر حسب المنطقة»، «تواصل معنا» | 🔴 **سببان مستقلان، وكلاهما يفسّر بدقة أي الصفحات ظهرت وأيها غاب — القياس حسم الأمر لا التخمين:** غوغل عرض بالضبط الصفحات التي تملك **معاً** عنواناً خادمياً فريداً **و**رابطاً داخلياً بمسار حقيقي. (1) **لا قشرة SSR لأربع صفحات:** `/stores` و`/offers` و`/join` و`/contact` كلها تسقط في catch-all بـ`vercel.json` إلى `index.html`، فتحقّق حي مباشر على الإنتاج أعاد **نفس العنوان حرفياً** («دكانجي | سوق الحي بين يديك») للصفحات الخمس مجتمعةً مع الرئيسية. `updateHead()` في `app.js` تضبط العناوين **من طرف العميل فقط**، وتصيير JS عند غوغل تمريرة مؤجَّلة لاحقة — أي أن التمريرة الأولى ترى خمس صفحات متطابقة فتُدمَج ولا تنافس على رابط فرعي. (2) 🔴 **الأخطر لأنه صامت تماماً: روابط التنقل كانت `#stores` و`#offers`** — وهذا **جزء (fragment) من الصفحة الرئيسية لا رابط مستقل**، فغوغل لا يعدّه رابطاً داخلياً إطلاقاً. النتيجة أن أهم صفحتين تجارياً على الموقع كان لهما **صفر رابط داخلي حقيقي في الموقع كله** (تحقّق بالعدّ: `href="#stores"` في 5 مواضع مقابل `href="/stores"` في صفر داخل `index.html`)، ولا يعرفهما غوغل إلا من sitemap. بالمقابل `/dalil` و`/why-dukkanci` مربوطتان بمسار حقيقي **ولهما قشرة SSR** — وهما بالضبط اللتان ظهرتا. (3) **ثغرات sitemap:** `/contact` و`/merchants` و`/ask-dukkanci` غير مُدرَجة إطلاقاً. (4) **«المتاجر حسب المنطقة» لا وجود لها كصفحة أصلاً** — فقط متغيّرات `/dalil?region=<slug>`، ولهذا عرض غوغل منطقة مفردة («دليل متاجر ومطاعم أورتاكوي») بدل محور. والأسوأ أن الثلاث عشرة صفحة منطقة كانت **يتيمة**: مرشِّح المنطقة في الدليل قائمة منسدلة لا روابط، فلا رابط داخلي واحد لأيٍّ منها رغم إدراجها في sitemap منذ 2026-07-12. (5) **تصادم عناوين:** `merchants.html` عنوانه «لماذا تنضم إلى دكانجي؟» يكاد يطابق `/why-dukkanci` («لماذا دكانجي؟») — صفحتان متشابهتان بصرياً تتنافسان على نفس الموضع | **⚠️ توضيح جوهري سُجِّل صراحةً للمستخدم: الروابط الفرعية يختارها غوغل خوارزمياً ولا يمكن فرضها؛ المتاح هو جعل الصفحات مؤهَّلة ومُشار إليها بقوة — وهذا ما نُفِّذ.** **(أ) أربع قشور SSR جديدة** (`api/stores.js`, `api/offers.js`, `api/regions.js`, `api/contact.js`) بنفس نمط `api/why-dukkanci.js`، مربوطة في `vercel.json` **قبل** الـcatch-all. كل قشرة تحمل عنواناً/وصفاً/canonical خاصاً + JSON-LD + **نصاً زاحفاً حقيقياً من بيانات الإنتاج**: `/stores` تسرد 95 متجراً معتمداً مجمَّعة على 8 تصنيفات (95 رابط `/store/<slug>`)، و`/offers` تسرد **422 خصماً فعلياً من 17 متجراً** (341 رابط `/product/<slug>`) — والرقمان يطابقان تماماً ما وثّقه سطر «عروض التطبيق» في هذا السجل، تصالب مستقل يؤكد صحة الترشيح. **⚠️ PostgREST لا يقارن عمودين**، فـ`old_price > price` رُشِّح من طرف الخادم لا في الاستعلام (نفس درس Flutter). **(ب) استُخرج نمط القشرة المكرَّر في `lib/ssr-shell.js`** (جلب القشرة + استبدال الوسوم + `sbGet` + breadcrumb) — القشور الخمس الأقدم **لم تُلمس عمداً** (حيّة ومُختبَرة، وإعادة توصيلها خارج النطاق)، لكن الجديدة لا تكرّره للمرة السادسة. **(ج) إصلاح الروابط الداخلية — جذر المشكلة الثاني:** حُوِّلت كل `href="#stores"`/`href="#offers"` (7 مواضع في `app.js` و`index.html`) إلى مسارات حقيقية، وأُضيف عمود فوتر جديد «تسوّق» (اطلب من المتاجر / عروض دكانجي / المتاجر حسب المنطقة / دليل المطاعم والمتاجر). شبكة الفوتر صارت 5 أعمدة بتعديل **قاعدة CSS واحدة** فقط — استعلاما الموبايل/اللوحي أصلاً `1fr 1fr` فلم يحتاجا شيئاً. **(د) صفحة جديدة `/regions`** («المتاجر حسب المنطقة») مسارَ SPA (`renderRegionsPage`) + قشرة SSR: محور يربط الثلاث عشرة منطقة ويُنهي يُتمها. **(هـ)** `merchants.html` أُعيد عنونته ليبدأ بـ«انضم إلى دكانجي كتاجر» (نصّ الرابط المطلوب نفسه) فيزول التصادم مع `/why-dukkanci`. **(و)** sitemap: أُضيفت `/regions`, `/contact`, `/merchants`, `/ask-dukkanci` (13 صفحة + 13 متغيّر منطقة). **(ز)** `SiteNavigationElement` JSON-LD في `index.html` يعلن الصفحات الست صراحةً — وبما أن القشور تستنسخ `index.html` فهو حاضر في كل صفحة. **(ح) اتساق العناوين:** عناوين `updateHead()` في `app.js` جُعلت **مطابقة حرفياً** لعناوين القشور — اختلافها كان سيعطي الزاحف عنواناً في التمريرة الأولى وآخر بعد التصيير، أي إشارة متضاربة تُضعف الترشيح. **علّة أدخلتُها بنفسي وكشفها `node --check` قبل أي اختبار:** أول تعديل على سلسلة `updateHead` حذف قوس إغلاق كتلة `category` فانكسر الملف كلياً (`Unexpected token else`) — أُصلح فوراً، ودرسه أن استبدال سطر يبدأ بـ`} else if` يجب أن يُعيد القوس. **التحقّق (كله فعلي على خادم محلي ببيانات إنتاج حقيقية):** القشور الأربع تُعيد 200 بعناوينها وcanonical وh1 الصحيحة؛ `/regions` تعرض 13 منطقة بعدّادات مطابقة لِما تعرضه صفحة المنطقة فعلاً (إسنيورت 26 ⇐ نقرة حقيقية أعطت 26 بطاقة)؛ **الأربعة روابط تنقّل تعمل عميلياً بلا إعادة تحميل** (`spaAlive:true` — الاختبار الحاسم بعد تحويل الـhash)؛ الفوتر 5 أعمدة بترتيب RTL صحيح على 1280px و2 عمود على 375px؛ **صفر انزياح أفقي** على المقاسين؛ **صفر خطأ كونسول**؛ **كل كتل JSON-LD الـ29 عبر الصفحات الخمس تُفسَّر بلا خطأ**؛ و`/dalil` و`/why-dukkanci` والرئيسية بلا أي تأثر (انحدار). توازن أقواس `styles.css` فُحص برمجياً بعد الإلحاق (درس قوس fbads المفقود). ⚠️ **لقطات الشاشة تعطّلت بمهلة 30 ثانية** (عطل بيئي موثَّق) فاعتُمد تحقّق DOM/هندسة/شبكة بدلاً منها. ⚠️ **لم يُنشأ SSR لـ`/join`** عمداً: المسار يعرض الرئيسية + نافذة منبثقة، فقشرته كانت ستكون محتوى شبه مكرَّر ومخاطرة cloaking — «انضم كتاجر» وُجِّه إلى `/merchants` وهي صفحة حقيقية بمحتوى حقيقي | `api/stores.js`, `api/offers.js`, `api/regions.js`, `api/contact.js`, `lib/ssr-shell.js` (الخمسة جديدة)، `api/sitemap.js`, `vercel.json`, `app.js`, `styles.css`, `index.html`, `merchants.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` (لا تعديل على قاعدة البيانات إطلاقاً — قراءة فقط) | نعم — `npm run check:versions` ناجح (app.js v344→345، styles.css v183→184، CACHE v262→263). **مدفوع لـ`main`** (`c6ae0432`، Vercel نجح). تحقّق حي بعد النشر: الصفحات الأربع تُعيد 200 بعناوينها الفريدة (كانت الأربع تُعيد عنوان الرئيسية حرفياً)، و`/merchants` صار «انضم إلى دكانجي كتاجر»، و**صفر `href="#stores"`/`#offers"` على الصفحة الحية** مقابل روابط بمسارات حقيقية، و100 رابط متجر في `/stores` و341 رابط منتج في `/offers` و13 رابط منطقة في `/regions`، وsitemap يحمل الـ13 صفحة، و6 عناصر `SiteNavigationElement`. **واختبار الانحدار الحاسم:** `/regions` الحية أقلع فيها التطبيق فعلاً (`spaBooted:true`، 99 متجراً) بصفر خطأ كونسول — وهو بالضبط وضع فشل حادثة `paradice-data.js`. ⚠️ الأثر على غوغل ليس فورياً — يحتاج غوغل إعادة زحف وفهرسة (أيام إلى أسابيع)؛ يُسرَّع بطلب الفهرسة لهذه الروابط في Search Console وبتشغيل `scripts/submit-store-urls.mjs` بنمطه** |
| 2026-07-20 | المستخدم طلب إضافة **ماركت أيهم** (EYHEM MARKET) من كتالوج واتساب بزنس (`+90 506 647 76 88`) | ميزة إضافة جديدة (لا خطأ سابق) — لكن **العائق الحاكم اكتُشف بالفحص لا بالافتراض: الكتالوج بلا سعر واحد إطلاقاً**. تحقّق بطبقتين مستقلتين: صفوف القائمة تحمل الاسم فقط (`cell-frame-title` + زر «+»، صفر عنصر سعر)، وفتح لوحة «Details» لمنتج حقيقي أظهر **صورة + اسم + زرّي «Message business»/«Add to cart» فقط، بلا سعر ولا وصف**. أي أن التاجر لم يُسعّر كتالوجه أصلاً. هذا يصطدم مباشرة بسياسة المشروع الثابتة «لا سعر = لا نشر» (التي أسقطت 12 صنفاً من شام أوغلو)، لكنه في الوقت نفسه ينطبق عليه حرفياً استثناء `priceOnRequest` المستخدَم سابقاً لكنافة البراء (68) وتو بيز (91). لأن القرار **يغيّر طبيعة المتجر جوهرياً** (بلا سلة ⇒ صفر طلبات عبر دكانجي ⇒ لا تتبّع ولا عمولة) لم أختر نيابةً عن المستخدم بل طرحته صراحةً عبر `AskUserQuestion` قبل تنزيل أي صورة، فاختار **«اعرضه بـالسعر عند الطلب»** | **المتجر رقم 112** (وليس 110 كما خُطِّط) — 🔴 **حارس التصادم أثبت قيمته عملياً للمرة الأولى بهذا الوضوح:** فحص `max(id)` أول الجلسة أعطى 109 فاعتُمد 110، لكن الفحص الإلزامي **مباشرة قبل الـINSERT** (المبني داخل سكربت الدفع نفسه كـ`throw` لا كتحذير) أجهض العملية: `ABORT: store id 110 already taken by "ccf4754a39@emailinbo.live"` — تبيّن أن **تسجيلين ذاتيين بعنوان بريد مؤقّت (emailinbo.live) حجزا 110 و111 في الساعة 10:22 و10:24 من نفس اليوم** أثناء عملي. أُعيد التوليد كاملاً على 112 (حزمة المنتجات 1920001–1920142 تحقّق أنها لا تزال فارغة). ⚠️ **يستحق انتباه المستخدم:** الصفّان 110/111 تسجيل ذاتي مزدوج من بريد مؤقّت واحد — نمط سبام تسجيل؛ **لم أحذفهما** (ليس قراري، وقد يكونان محاولة تاجر حقيقي). **الاستخراج:** 142 منتجاً فريداً (141 بصورة حقيقية + واحد بلا صورة استُبعد تلقائياً)، عبر وصفة زمزم المؤكَّدة (صورة كامل الدقة 1600px موجودة أصلاً كـ`background-image: url(blob:)` على صفّ القائمة نفسه، بلا حاجة نقر «Details» لكل صنف). 🔴 **علة أدخلتُها بنفسي واكتُشفت بالقياس لا بالمراجعة:** أول نسخة من المُجمِّع قرأت الاسم عبر `innerText` على `[role="listitem"]` فأعادت **صفر إضافات** رغم أن اللقطات تُظهر منتجات جديدة — لأن `innerText` يُعيد فراغاً للصفوف المُحاكاة خارج الشاشة؛ ثم نسخة ثانية بـ`textContent` على الأوراق التقطت 29 فقط لأن نطاقها كان خاطئاً؛ الحل النهائي: النطاق على `[data-testid="product-catalog-item"]` والاسم من `[data-testid="cell-frame-title"]` — قفز العدّ فوراً 29→57→142. **درس عام: «added:0 بينما الشاشة تُظهر جديداً» = خلل في المُجمِّع لا في التمرير — أوقف التمرير وافحص المُحدِّد.** التمرير نفسه استلزم أحداث `computer.scroll` حقيقية (التصفيح توقّف عند 16055px بثبات مؤكَّد بجولتين إضافيتين بلا نمو). **التصنيف:** التاجر أسند **3 أصناف فقط لكل قسم من أقسامه الخمسة (15 من 142)** — استُخرجت هذه الـ15 بمشي DOM مُرتَّب يتتبّع ترويسة القسم الحالية (أدقّ من النقر داخل كل قسم، وقد صحّح فعلاً وسماً خاطئاً لـ21 صنفاً نتج عن نقرة أخطأت هدفها)، والـ127 الباقية صُنِّفت بقواعد الأسماء إلى 9 أقسام (نفس نهج زمزم 96)، مع **تحقّق برمجي أن التقسيم يغطي كل منتج مرة واحدة بالضبط** (`throw` عند أي ازدواج أو نقص) قبل توليد الملف. **الهوية:** مطابقة Google Maps قاطعة — رابط خرائط نشره التاجر في ملفه على واتساب حُلّ إلى (41.0623333, 28.7077778)، وبحث الجوار أعاد **«(EYHEM MARKET) ماركت ايهم بزم 6 ميترو اسبارتكولا»** (4.4★/21) على بعد ~20م. **قرارا بيانات متعمَّدان:** (1) هاتف غوغل للمتجر **سوري (+963 959 826 267)** — خطأ إدخال من التاجر، لم يُستخدم إطلاقاً؛ اعتُمد رقم واتساب الذي أرسله المستخدم كقناة الطلب الوحيدة (سابقة قلعة الشام 94). (2) ساعات غوغل (11:00–01:30) تناقض ملف واتساب بزنس (10:00–22:00 مع «Open until 10:00 PM» حيّة) — اعتُمد واتساب (سابقة بيغ طاووق 102). (3) عنوان غوغل النصّي («Şahintepe/Başakşehir») يناقض الترميز الجغرافي العكسي لإحداثياته («Tahtakale/Avcılar/Ispartakule») واسم المتجر نفسه يقول «بزم 6 … اسبارتكولا» — اعتُمد Ispartakule/Bizimevler 6. **زوجا صور متطابقتان بالبايت** (كزبرة/بقدونس، ومدة سفرك/مدة سفرة نايلون) أُبقيا كملفين مستقلين — قاعدة `applyPublishingRules` على **المسار** لا البايتات (سابقة شام أوغلو/شيكي ويكي/الوكالة). **التحقّق (كله حي):** الدفع تحقّق حقلاً-بحقل بقراءة عكسية = **142/142 مطابقة تماماً**؛ وفي المتصفح: 142 بطاقة، **صفر زر «أضف للسلة» و142 زر واتساب** يحمل كلٌّ منها اسم منتجه ورقم التاجر، ونص «السعر عند الطلب» ظاهر، و**144/144 رابط صورة HTTP 200**، وعيّنة صور أُجبرت على التحميل فُكَّت فعلاً بأبعاد 800px بنصوص بديلة مطابقة، ومعدّل «٢٠ ل.ت/كم» والعنوان والساعات ظاهرة، وصفر انزياح أفقي على 375px، وصفر خطأ كونسول، و`/category/supermarket` **لا يعرض المتجر** (سلوك `pending` صحيح) بينما الرابط المباشر يعمل. ملاحظة بيئية: `screenshot` انتهت بمهلة 30 ثانية مرتين (عطل موثَّق) فاعتُمد تحقّق DOM/شبكة/هندسة بدلاً منها | `ayham-data.js` (جديد)، `assets/photos/ayham/**` (جديد، 144 ملف: 142 منتج + شعار + غلاف، 11MB)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 112 وصفوف `products` 1920001-1920142 في Supabase | نعم — `npm run check:versions` ناجح (app.js v343→344، store-slugs.js v261→262، CACHE v261→262؛ styles.css بلا تغيير v183). **لم يُنفَّذ `git commit`/push — بانتظار تعليمات المستخدم. المتجر `approval_status='pending'` عمداً** (طرف ثالث غير مؤكَّد كشراكة) — يحتاج اعتماداً صريحاً ليظهر للزوار |
| 2026-07-20 | المستخدم أبلغ أن **متاجر لها عروض لا تظهر في قسم «عروض وخصومات» بتطبيق Flutter**، وضرب مثالاً بمطعم باشا بيتزريا | 🔴 **العلّة عامة لا خاصة بمتجر واحد، وكشفها القياس لا قراءة الكود:** التطبيق كان يحدّد «هل للمتجر عرض؟» من علَم واحد هو `stores.has_offer`، وهو **علَم بانر يكتبه التاجر ولا يسجّل الخصومات الفعلية إطلاقاً**. مسح كامل للإنتاج أظهر التباين: **422 منتجاً مخفَّضاً فعلياً (`old_price > price`) موزّعة على 17 متجراً مرئياً، بينما `has_offer=true` لـ13 متجراً فقط — و6 متاجر لها خصومات حقيقية وعلَمها `false` فكان التطبيق يخفيها تماماً**: باشا بيتزريا (56، **30 بيتزا مخفَّضة** من حملة 2026-07-18 المسجَّلة في هذا السجل)، القاضي ماركت اسنيورت (22)، مندي اليمن (70)، عسل بطون النحل (79)، شركة الأحدب (81)، قرقوشة فرفوشة (85). **السبب الجذري الأعمق فرق معماري:** الموقع يشتقّ العروض من **المنتجات** لأنه يحمّل الكتالوج كله في الذاكرة، والتطبيق لا يستطيع ذلك (سقف الألف صف الموثَّق أعلاه) فاكتفى بالعلَم — وهو اختصار صامت لا يُنتج خطأ ولا تحذيراً، فبدا القسم يعمل بينما ينقصه ثلث متاجره | أُضيفت `fetchStoreIdsWithDiscountedProducts()` في `StoreRepository`: تجلب `store_id/price/old_price` للمنتجات المتاحة ذات `old_price` **مع تصفيح** (الحجم اليوم 422 صفاً أي دون السقف، لكن الخصومات تنمو فلا يجوز افتراض طلب واحد)، وترشّح **`old_price > price` من طرف العميل** لأن PostgREST لا يقارن عمودين ببعضهما، فلا يُحتسب صفّ خاطئ فيه `old_price` مساوٍ أو أقل (نفس ما يفعله الموقع). **مصدر حقيقة واحد لا ثلاثة:** كان الفحص `s.hasOffer` مكرَّراً حرفياً في ثلاثة مواضع (رفّ الرئيسية، `/category/offers`، مرشِّح البحث)، فاستُخرج في `Store.hasAnyOffer(discountedStoreIds)` تفادياً لأن يُصلَح موضع ويُنسى آخر — نفس فصيلة ازدواج `normalizeArabic` المُصلَحة في سطر البحث أعلاه. **تدهور آمن بالتصميم:** المزوّد `discountedStoreIdsProvider` يُقرأ بـ`.value ?? const <int>{}` والمجموعة الفارغة تُعيد السلوك القديم (العلَم وحده) بالضبط، فأثناء التحميل أو عند فشل الجلب **لا يمكن أن تظهر نتائج أقل من قبل، فقط أكثر**؛ والدالة نفسها تبتلع أخطاءها وتُعيد مجموعة فارغة فلا تُسقط الصفحة الرئيسية أبداً. `_HomeBody` بقي `StatelessWidget` وتُمرَّر له المجموعة من الأب (مطابقةً لنمط تمرير `stores` القائم) بدل تحويله لـ`ConsumerWidget`. **علتان أدخلتُهما وأصلحتهما بالتحليل قبل الاختبار:** `valueOrNull` **غير موجودة في Riverpod 3.3.2** (المتاح `.value` وهي nullable أصلاً — تحقّقت من مصدر الحزمة لا من الذاكرة)، و`ref` غير متاح داخل `_HomeBody` لأنه `StatelessWidget`. **التحقّق:** 4 اختبارات وحدة لقاعدة القرار (منها صراحةً: مجموعة فارغة ⇐ سلوك العلَم وحده، ومتجر 56 بلا علَم ⇐ يظهر بفضل الخصومات) + اختبار شبكي حقيقي على الإنتاج يؤكد أن المجموعة **تحوي 56 فعلاً** وأن `has_offer` له لا يزال `false` (وإلا فالمثال بطل ويحتاج تحديثاً) وأن هناك متاجر تُكتسب بالخصومات وحدها — **المجموع 55/55 اختباراً ناجحاً**، و`flutter analyze` عاد لخط الأساس (4 ملاحظات معلوماتية). **قياس الأثر قبل/بعد من الإنتاج: 13 متجراً ⇐ 19 متجراً** في «عروض وخصومات» | `dukkanci_customer_app/lib/features/stores/data/store_repository.dart`, `dukkanci_customer_app/lib/features/stores/domain/store.dart`, `dukkanci_customer_app/lib/app/providers.dart`, `dukkanci_customer_app/lib/features/home/presentation/home_screen.dart`, `dukkanci_customer_app/lib/features/category/presentation/category_screen.dart`, `dukkanci_customer_app/lib/features/search/presentation/search_screen.dart`, `dukkanci_customer_app/test/store_offers_test.dart` (جديد)، `dukkanci_customer_app/test/product_search_network_test.dart`, `CLAUDE.md` (لا تعديل على قاعدة البيانات إطلاقاً — قراءة فقط؛ **لم يُغيَّر `has_offer` لأي متجر عمداً**: العلَم بيان تسويقي يملكه التاجر، والإصلاح الصحيح أن يقرأ التطبيق الواقع لا أن نُعيد كتابة بيانات الإنتاج) | لا ينطبق (مشروع Flutter منفصل — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`) |
| 2026-07-20 | استكمال بحث المنتجات في تطبيق Flutter (السطر المدموج `abe58147`): نقل **مرادفات اللهجات** (`product_synonyms`) التي يستخدمها الموقع ولم تكن منقولة | ليس خطأ — إكمال ميزة. **قياس الجدوى أولاً بدل الافتراض:** العيّنة الأولى أوحت أن المرادفات مجرّد تكرار لاسم المنتج («مندي» لمنتج اسمه «لحم مندي»)، لكن مسحاً كاملاً أثبت العكس: **3,635 صفاً بمرادفات فعلية، منها 3,172 تضيف مصطلحاً غير موجود إطلاقاً في اسم المنتج أو صنفه** (2,404 مصطلحاً فريداً). وأعلاها قيمةً **تركية** (`döner`, `şiş tavuk`, `börek`, `mercimek çorbası`, `patates kızartması`, `sucuk`, `lokum`) و**أسماء ماركات** (`Dove`, `Pantene` — حاسمة لبحث السوبرماركت) و**لهجات** (`بطاطس مقليه` المصرية، `قهوه عربيه`) — أي أن الفائدة حقيقية لسوق في تركيا يخلط اللغتين | 🔴 **قيد تقني حاكم اكتُشف بالقياس لا بالتخمين: لا وجود لأي مطابقة جزئية على عمود `text[]` في PostgREST** — كل صيغ `ilike` (المباشرة و`ilike(any)` و`like(any)`) تفشل بـ`42883 no operator`، فلا يبقى إلا `cs`/`ov` أي **مطابقة عنصر كاملة وحسّاسة لحالة الأحرف** (تحقّق قاطع: `cs.{Dove}` يُرجع صفوفاً بينما `cs.{dove}` فارغ تماماً). ولأن الترحيلات هنا لا تُطبَّق تلقائياً (سطر [[migrations-need-manual-apply]])، رُفض بناء دالة Postgres تعطي مطابقة جزئية لأنها كانت ستشحن ميزة **ميتة** حتى يشغّل المستخدم SQL يدوياً — والمستودع فيه ترحيلان معلّقان أصلاً. **الحل:** `synonymMatchCandidates()` تُولّد مجموعة **محدودة** من التهجئات الحقيقية بدل نمط واحد فضفاض: بدائل حالة الأحرف (raw/lower/UPPER/Title) × توسيع الحروف العربية الملتبسة (ا/أ/إ/آ، ه/ة، ي/ى/ئ، و/ؤ) بسقف صارم يمنع الانفجار الأسّي، ثم تُرسَل كلها في `ov` واحد. **قياس التغطية قبل الاعتماد: 94.8% من المرادفات المضيفة للقيمة قابلة للوصول، و3,006 منتجاً يصير قابلاً للإيجاد** (الإخفاقات الـ210 كلها عبارات تركية مركّبة بحالة مختلطة مثل `Kaşar ile Kavurma`). **طلب واحد لا اثنان:** استُخدم ربط PostgREST المُضمَّن `products?select=*,product_synonyms!inner(...)` فيتم الفلترة على الجدول الابن وتُعاد صفوف الأب مباشرةً — تحقّق حي أن `döner` تُعيد فعلاً «شاورما - ساندويتش» و«اصبع شاورما». **مسار المرادفات يفشل ظريفاً** (يُعيد قائمة فارغة ولا يرمي) فلا يُسقط نتائج الاسم/الصنف أبداً. **في الواجهة:** الاستعلامان يُطلقان **معاً** بـ`(a,b).wait` لا بالتتابع، ونتائج المرادفات **لا تمرّ على مرشِّح الاسم/الصنف إطلاقاً** (وإلا لأسقطها بالتعريف، فالمصطلح ليس في الاسم — وهذا بالضبط جوهر الميزة)، وتُلحَق **بعد** مطابقات الاسم لأن مطابقة الاسم أوثق دلالةً، مع إزالة تكرار بـ`Set` لأن منتجاً واحداً قد يطابق المسارين معاً، ثم تمرّ على نفس بوابة المتاجر المعتمدة. **التحقّق:** 21 اختبار وحدة (منها 6 للمرادفات: حالة الأحرف، توسيع ه→ة، السقف) + 8 اختبارات شبكية حقيقية على قاعدة الإنتاج (منها: `döner` تصل منتجات عربية لا تحوي الكلمة، و**`dove` بحروف صغيرة تصل `Dove` المخزَّنة** — تسقط فوراً لو تراجعت بدائل الحالة) + **4 اختبارات ودجة جديدة** (`search_merge_test.dart`) تغطي المنطق الوحيد الذي يعيش في الشاشة: نتيجة مرادف تظهر بجانب نتيجة اسم، ومنتج يطابق المسارين يظهر **مرة واحدة**، ومنتج من متجر غير معتمد **لا يتسرّب**، ومرشَّح اسم غير مطابق فعلاً يُستبعَد. **المجموع 50/50 اختباراً ناجحاً**. ⚠️ **لم يُتحقَّق من الدمج على المحاكي**: بعد جولة طويلة صار System UI يرمي ANR متكرّراً يبتلع كل نقرة (حمل المضيف 58%، ولم تُجدِ إعادة الإقلاع ولا `force-stop`) — وهو عطل بيئي موثَّق سابقاً في هذا السجل لا عطل تطبيق؛ فاستُعيض عنه باختبارات الودجة الأربعة، وهي **أدقّ** من لقطة شاشة لأنها تؤكّد الثوابت صراحةً. مسار التصيير نفسه (`_ProductHit`) كان قد تحقّق حياً في نفس الجلسة بـ«pizza» (8 منتجات بأسعار مطابقة + تنقّل صحيح) | `dukkanci_customer_app/lib/core/utils/arabic.dart`, `dukkanci_customer_app/lib/features/stores/data/store_repository.dart`, `dukkanci_customer_app/lib/features/search/presentation/search_screen.dart`, `dukkanci_customer_app/test/arabic_search_test.dart`, `dukkanci_customer_app/test/product_search_network_test.dart`, `dukkanci_customer_app/test/search_merge_test.dart` (جديد)، `CLAUDE.md` (لا تعديل على قاعدة البيانات إطلاقاً — قراءة فقط، وصفر كتابة على `product_synonyms`) | لا ينطبق (مشروع Flutter منفصل — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`). `flutter analyze` نظيف (نفس الـ4 ملاحظات المعلوماتية القديمة فقط) |
| 2026-07-20 | المستخدم طلب التأكد أن **البحث الصوتي في تطبيق Flutter يعمل فعلاً على الموبايل** وإجراء الإصلاحات اللازمة لتفعيله | 🔴 **علة حجب حقيقية على مستوى النظام، لا في كود Dart إطلاقاً — ولهذا لم يكشفها أي `flutter analyze` نظيف ولا أي بناء ناجح:** `AndroidManifest.xml` كان يعلن `RECORD_AUDIO` لكن كتلة `<queries>` فيه تحوي `PROCESS_TEXT` **فقط** بلا أي إشارة لـ`android.speech.RecognitionService`. ومنذ أندرويد 11 (API 30) تحجب «رؤية الحزم» (package visibility) كل خدمات التطبيقات الأخرى افتراضياً، والتطبيق هنا `targetSdk=36` — فـ`SpeechRecognizer.isRecognitionAvailable()` (التي تُحلّ داخلياً عبر `queryIntentServices(RecognitionService)`) كانت تُرجع صفر خدمات، ولا يستطيع `createSpeechRecognizer()` الارتباط بخدمة التعرّف أصلاً. **تحقّق قاطع بثلاث طبقات مستقلة (لا استنتاج):** (1) ملف مانيفست الإضافة نفسها `speech_to_text-7.4.0/android/src/main/AndroidManifest.xml` **فارغ تماماً** — أي أنها لا تحقن `<queries>` نيابةً عن التطبيق، و`README` الخاص بها يشترط ذلك صراحةً (سطر 263: «إن كان `targetSDKVersion` 30 أو أحدث فعليك إضافة… `<action android:name="android.speech.RecognitionService" />`»). (2) **المانيفست المدموج للبناء الفعلي** (`build/app/intermediates/merged_manifests/debug/…`) كان يحوي **صفر** إشارة لـ`RecognitionService` — أي أن ما شُحن فعلاً كان محجوباً. (3) 🔴 **الدليل الحاسم من الجهاز نفسه:** `adb shell dumpsys package queries` على النسخة المثبَّتة القديمة أظهر أن `com.dukkanci.app` لا يرى إلا **حزمة واحدة** هي `com.google.android.apps.maps` (من إضافة الخرائط)، بينما `adb shell cmd package query-services -a android.speech.RecognitionService` أثبت أن خدمة التعرّف **موجودة فعلاً** على الجهاز وأن الافتراضية منها (`isDefault=true`) هي `AiAiSpeechRecognitionService` في حزمة `com.google.android.as` — أي أن **المُتعرِّف موجود والتطبيق أعمى عنه**. **وعلة ثانية مستقلة تُخفي الأولى:** `initialize()` لا تُرجع `false` عند تعذّر المُتعرِّف بل ترمي `PlatformException` (الكود الأصلي: `activeResult?.error(recognizerNotAvailable, …)`، وطبقة Dart تُمرّرها بلا التقاط)، و`_startListening()` كانت تستدعيها **بلا أي `try/catch`** — فالاستثناء يضيع في الفراغ غير المتزامن ويصبح الزر **ميتاً تماماً بلا أي رسالة ولا تغيّر حالة**، وهو بالضبط العرض الذي وصفه المستخدم. (ملاحظة: `false` تعني شيئاً مختلفاً — رفض إذن الميكروفون — وكانت مُعالَجة أصلاً.) **وعلة ثالثة صغيرة:** الإضافة تطلب `BLUETOOTH_CONNECT` مع `RECORD_AUDIO` ما لم يُمرَّر خيار، وهذا الإذن **غير معلَن في المانيفست إطلاقاً** فيرفضه النظام صامتاً في كل نقرة | **(أ) الإصلاح الجذري:** أُضيفت كتلة `<intent><action android:name="android.speech.RecognitionService" /></intent>` داخل `<queries>` القائمة (مع تعليق صريح يشرح أنها **إلزامية لا تجميلية** وأن حذفها يُعيد العطل، تفادياً لإزالتها في تنظيف مستقبلي). **(ب) تحصين الودجة:** استُخرجت التهيئة في `_ensureSpeechReady()` مع `on PlatformException catch` يميّز `recognizerNotAvailable` (جهاز بلا خدمة تعرّف فعلاً ⇐ «البحث الصوتي غير متاح على هذا الجهاز») عن أي خطأ آخر (⇐ «حدث خطأ ما، حاول مرة أخرى» لأنه قابل لإعادة المحاولة)، و`try/catch` حول `listen()` يُنهي شريط «جارٍ الاستماع» بدل تركه معلّقاً 15 ثانية فوق جلسة ميتة، وحارس `mounted` قبل استخدام `context` داخل `onResult` (كان يستعمل `context` بعد `setState` بلا حارس). **(ج)** مُرِّر `SpeechToText.androidNoBluetooth` فلا يُطلب إذن غير معلَن. **(د)** التلميح/تسمية إمكانية الوصول للزر كانت `voiceSearchListening` **دائماً** — أي أن قارئ الشاشة يُعلن «جارٍ الاستماع… تحدّث الآن» على زر لا يستمع؛ فُصلت `voiceSearchStart = 'البحث الصوتي'` للحالة الخاملة. **لم يُلمس** قرار `localeId:'ar'` المباشر بلا فحص لغات مسبق (إصلاح 2026-07-19 أدناه) — بل **أثبت السجل الحي صحته**. **التحقّق (كله فعلي على محاكي حقيقي Android 15/API 35، وليس افتراضاً):** `flutter analyze` = **4 ملاحظات مطابقة تماماً لخط الأساس** الذي قِيس قبل أي تعديل (صفر جديدة)، و`flutter build apk --debug` نجح فعلاً (exit 0)، والمانيفست المدموج بعد البناء يحوي الآن الكتلة الجديدة. 🔴 **الإثبات قبل/بعد على نفس الجهاز بنفس الأمر:** `dumpsys package queries` قبل الإصلاح: `com.dukkanci.app` يرى `com.google.android.apps.maps` فقط؛ بعد تثبيت النسخة الجديدة: أضيف له تحت «queries via component» كلٌّ من **`com.google.android.as`** (حاضنة خدمة التعرّف الافتراضية) و`com.google.android.googlequicksearchbox`. (لاحظ: `com.google.android.tts` بدا «ظاهراً» في فحص أولي متساهل ثم تبيّن أنه أثر تحليل نصّي عابر للأقسام، فأُعيد التحليل بدقة واستُبعد — لا يُذكر كنتيجة.) **والتشغيل الحي الكامل:** نقر فعلي على زر الميكروفون ⇐ ظهرت نافذة إذن أندرويد الحقيقية وطلبت **الصوت فقط** (تأكيد عملي أن `androidNoBluetooth` سرى)، وبعد المنح أظهر `logcat` سلسلة كاملة: `RecognitionService#onStartListening` ثم `Trigger application overridden to com.dukkanci.app` (أي أن الجلسة نُسبت لتطبيقنا فعلاً — دليل ارتباط ناجح) ثم `Initialize Soda [locale: ar]` (**اللغة العربية قُبلت ومُرِّرت فعلاً**) و`NetworkSpeechRecognizer: Online recognizer - start listening` (المسار الشبكي الذي تستخدمه الهواتف الحقيقية)، وانتهت الجلسة بـ`NO_SPEECH_DETECTED` لأن المحاكي بلا دخل صوت حقيقي — وهو السلوك الصحيح لا عطل. **صفر استثناء Dart/Flutter في `logcat`** وصفر خطأ من الإضافة. **والتسمية الخاملة تأكّدت حيّاً بعد الإصلاح:** `content-desc="البحث الصوتي"` بنفس إحداثيات الزر تماماً (`[42,309][168,435]`) بعد أن كانت «جارٍ الاستماع… تحدّث الآن». 🔴 **درس أدوات وقع فعلاً في هذه الجلسة:** أول قراءة بعد الإصلاح أظهرت التسمية القديمة فبدا أن التعديل لم يسرِ — والسبب أن `uiautomator dump` يكتب دائماً إلى `/sdcard/ui.xml` **ويُبقي الملف السابق إن فشل**، وكنت أكتم خطأه بـ`>/dev/null 2>&1` فقرأت لقطة قديمة؛ العلاج: `adb shell rm -f /sdcard/ui.xml` قبل كل لقطة وعدم كتم الخطأ. (درس ثانٍ: `adb shell` في Git Bash يحوّل `/sdcard/...` إلى مسار ويندوز — يلزم `MSYS_NO_PATHCONV=1`.) ⚠️ **ما لم يُختبر:** (1) نطق عربي حقيقي وتحويله نصاً — المحاكي يفتقر لحزمة اللغة العربية غير المتصلة (`SodaSpeechRecognizer: Failed to get language pack of required locale: error 12`) وليس له ميكروفون فعلي؛ ما ثبت هو أن الحاجز الذي كان يمنع الوصول للمُتعرِّف أصلاً قد زال وأن الجلسة تبدأ فعلياً باللغة العربية. (2) **تبدُّل التسمية/الأيقونة أثناء الاستماع نفسه** (`_listening == true`) لم يُلتقط: `uiautomator` لا يُصوّر إلا عند سكون النافذة، وأثناء الاستماع يعمل `AnimatedSwitcher` وشريط SnackBar متحرّك معاً — فلم أُثبته ولا أدّعيه (الشيفرة مجرّد شرط ثلاثي اجتاز التحليل، والمسار الوظيفي مُثبت بالسجل الحي). **ملاحظة نطاق:** البحث الصوتي في الموقع/PWA سليم ولم يُلمس — فُحص وتبيّن أنه تحسين تدريجي (`voiceSearchButton()` تُعيد نصاً فارغاً إن غاب `SpeechRecognition`) فيختفي بلطف على المتصفحات غير الداعمة، ولا يعاني أي حاجز مقابل | `dukkanci_customer_app/android/app/src/main/AndroidManifest.xml`, `dukkanci_customer_app/lib/core/widgets/voice_search_button.dart`, `dukkanci_customer_app/lib/core/localization/app_strings.dart`, `CLAUDE.md` (لا تعديل على قاعدة البيانات إطلاقاً) | لا ينطبق (مشروع Flutter منفصل — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`). **مدفوع لـ`main`** (`76998596`). دُفع من جلسة أخرى بعد التحقّق المستقل: `flutter analyze` أُعيد تشغيله فعلياً فأعطى **4 ملاحظات معلوماتية فقط مطابقة تماماً لخط الأساس** (إهلاكات `anonKey`/`Radio` قديمة، صفر جديدة)، ومُراجعة الفرق سطراً بسطر طابقت ما يوثّقه هذا الصفّ. `app_strings.dart` كانت جلسة الإشعارات قد التزمته أصلاً، فلم يبقَ إلا المانيفست والودجة. مشروع Flutter — Vercel لا ينشره، فلا أثر على الموقع الحي |
| 2026-07-20 | المستخدم شغّل ملف الترحيل وطلب التحقّق من عمل نظام الإشعارات فعلياً على الإنتاج | 🔴 **علّة تصفيح حقيقية اكتُشفت بالتحقّق الحي، لا بالمراجعة:** `sbRpcAll()` في `api/notifications.js` كانت تستخدم ترويسة `Range` للتصفيح — الطريقة الصحيحة لقراءة الجداول، لكن **PostgREST يتجاهلها تماماً على استدعاءات RPC**. النتيجة: كل "صفحة" كانت تُعيد نفس الألف صف الأولى بعينها، فتكرّر الحلقة نفسها حتى حد الأمان الداخلي. **الأثر المُقاس مباشرة:** شريحة `wa_contacts` (6,332 رقماً حقيقياً) كانت ستُستهدَف بـ1,000 رقم فقط مكرَّرة ~200 مرة — أي حملة واتساب كانت ستفقد 84% من الجمهور المستهدَف بصمت تام، بلا أي خطأ ظاهر في اللوحة. اكتُشفت هذه العلّة فقط لأن التحقّق قاس **العدد الفعلي بعد التنفيذ** لا مجرّد نجاح الاستدعاء (`HTTP 200`) — نفس الدرس المُوثَّق مراراً في هذا السجل بخصوص قصّ PostgREST الصامت، لكن هذه المرة الاستثناء أن آلية التفافه (ترويسة Range) لا تعمل على الدوال أصلاً لا الجداول. | استُبدلت آلية التصفيح بـ`?limit=&offset=` في رابط RPC نفسه — وهو ما يحترمه PostgREST فعلياً لدوال قابلة للإرجاع كجدول. **تحقّق ثلاثي الطبقات قبل الاعتماد:** (1) استدعاء الدالة الحقيقية المُصدَّرة من `api/notifications.js` نفسه (لا نسخة مُعاد كتابتها) ضد قاعدة الإنتاج — 6,332 صفاً بلا تكرار (كانت 1,000). (2) بعد الدفع والنشر، إعادة تنفيذ نفس نمط `limit/offset` مباشرة ضد قاعدة الإنتاج الحية — نفس النتيجة 6,332/6,332. (3) تحقّق أن الصفحات المتتالية (`offset=0` و`offset=1000`) تُعيد فعلاً أرقاماً مختلفة (صفر تداخل) لا نفس الصفحة مكرَّرة. **لم يُختبَر عبر النقطة المُصادَق عليها (`?action=segments` بترويسة إدارية)** لأن كلمة مرور `.env` المحلية كلمة تطوير مختلفة عمداً عن كلمة مرور الإنتاج الحقيقية — لم تُخمَّن ولم تُطلَب، والتحقّق المباشر على قاعدة البيانات أدق أصلاً لأنه يفحص آلية البيانات بمعزل عن طبقة الجلسة. 🔴 **اكتشاف جانبي مهم أثناء محاولة الحصول على توكن اختبار:** دالة `adminOk()` في `api/campaign.js` (ونسختي المنقولة منها حرفياً في `api/notifications.js`) تحمل علّة منطقية حقيقية وقائمة في الإنتاج: `if (tok) return verifyAdminToken(tok);` تُنفَّذ فوراً بمجرد وجود ترويسة `x-admin-key` مهما كانت قيمتها، فترجع نتيجة `verifyAdminToken` (تتوقّع توكناً موقَّعاً بصيغة `payload.signature`) بدل الوصول أبداً لفرع مقارنة كلمة المرور الخام الموثَّق في التعليق المجاور («accept raw password in x-admin-key for simpler cron callers») — أي أن **ذلك المسار الاحتياطي كان معطوباً منذ كتابته ولم يعمل قط لأي مستدعٍ**. بالمقابل `adminOk()` في `api/notify-order.js` (تسجيل دخول اللوحة الفعلي) مكتوبة بشكل صحيح (`if (verifyAdminToken(...)) return true; return adminPasswordOk(req);`) ولا تحمل نفس العلّة. لم يُصلَح (منطق مصادقة مشترك خارج نطاق هذه الجولة، ويحتاج تنبيه المستخدم صراحةً قبل لمسه) | `api/notifications.js` (سطر `sbRpcAll` فقط، بلا أي تغيير آخر) | لا ينطبق (خادم فقط). **مدفوع لـ main** (commit `55dbf073`)، Vercel نشر بنجاح، وتحقّق حي مباشر بعد النشر يؤكد استرجاع الـ6,332 كاملة |
| 2026-07-20 | المستخدم طلب **نظام إشعارات للتطبيق** + **إدارة لإرسال إشعارات ترويجية** بمحرر يستهدف العملاء حسب حالتهم (نزّل التطبيق ولم يطلب / وضع في السلة ولم يكمل / وما جرت عليه العادة في التطبيقات المشابهة) | ميزة جديدة، لكن تدقيقاً لقاعدة الإنتاج قبل أي كود كشف أن **الشريحتين المطلوبتين صراحةً غير قابلتين للحساب أصلاً**، فالمطلوب ليس واجهة بل بنية تحتية مفقودة: (1) 🔴 **لا يوجد أي سجلّ لتثبيت التطبيق إطلاقاً** — `orders.source` قيمته `web` في **13 من 13** صفاً و`tracking_events.event_source` قيمته `web` في **7,661 من 7,661** (تحقّق بمسح مُصفَّح كامل + استعلام `neq.web` أعاد صفراً في الحالتين)، أي أن التطبيق والموقع **لا يمكن تمييزهما في البيانات**؛ ولا وجود لأي جدول `devices`/`app_installs`/`fcm_tokens` (الثلاثة 404). (2) 🔴 **لا يوجد أي حفظ للسلة على الخادم** — `carts`/`cart_items`/`abandoned_carts` كلها 404، والسلة تعيش في `localStorage` (الموقع) و`SharedPreferences` (Flutter) فلا تصل الخادم أبداً؛ الأثر الوحيد `tracking_events.cart_id` وهو معرّف ارتباط تحليلي بلا محتوى ولا هاتف ولا هوية يمكن المراسلة بها. (3) **لا يوجد أي جدول تاريخ إشعارات للعميل** (`notifications`/`notification_log` 404) — فسؤال «ماذا أرسلنا لهذا العميل؟» بلا إجابة. (4) `push_subscriptions` (13 صفاً) **متصفحات فقط** لا FCM — دليل قاطع: عمودا `p256dh`/`auth` مفاتيح تعمية RFC 8291 خاصة بالمتصفح (رمز FCM الأصلي سلسلة مفردة بلا زوج مفاتيح)، وكل الـ13 `user_agent` متصفحات بصفر وكلاء Dart/Flutter؛ التوزيع `store:9, customer:2, admin:2` أي **عميلان اثنان فقط** يمكن الوصول إليهما بإشعار اليوم مقابل **6,334** رقم واتساب. (5) Firebase في Flutter **مُعلَن لا موصول**: الحزم في `pubspec.yaml` لكن صفر `google-services.json` وصفر `Firebase.initializeApp()` وصفر `import firebase_*` في `lib/` وصفر إضافة Gradle — و`main.dart:19` يوثّق ذلك صراحةً | **قرار معماري: البنية التحتية هي الميزة، والقنوات مُحوِّلات قابلة للفصل.** أُنشئ `migrations/20260720_notifications_system.sql`: `app_devices` (سجلّ التثبيتات — يجعل «نزّل ولم يطلب» قابلاً للحساب، ويوحّد Web Push وFCM كقيمتين لعمود `push_channel` بدل نظامين)، `cart_snapshots` (**ملخّص فقط** باختيار صريح من المستخدم: المتجر/العدد/الإجمالي/أسماء المنتجات — بلا خيارات ولا إضافات ولا ملاحظات ولا عنوان)، `notifications` (صندوق واحد يجمع المعاملاتي والترويجي كما تفعل التطبيقات المشابهة)، `notification_campaigns`+`notification_recipients`، ودالتا `notification_segment_devices()`/`notification_campaign_stats()`. **التجميع داخل Postgres وليس في JS** عمداً: PostgREST هنا `db-max-rows=1000` ويقصّ كل استجابة بصمت، وشريحة `wa_contacts` وحدها 6,334 صفاً — أي ربط في JS كان سيبني شريحة من 1,000 وتبدو سليمة تماماً. **9 شرائح** (كل الأجهزة، نزّل ولم يطلب، سلة متروكة، طلب مرة، متكررون، خاملون، كل من طلب، عملاء متجر، جهات واتساب) بتعريف **موحَّد** لـ«طلب» = صف حالته ليست `مرفوضة` — تعريف واحد لا اثنان كي لا تنشأ فجوة يسقط فيها من طلب ورُفض فلا هو عميل ولا هو «لم يطلب». **`api/notifications.js` جديد** بجدول `ACTIONS` يعلن الطريقة والصلاحية لكل إجراء — يجعل فخّ GET/POST الموثَّق (إجراء في كتلة POST يُنادى بـGET فيسقط في 403 عارٍ ويقفل اللوحة) **مستحيلاً بالتصميم**؛ والعدّادات **تُعاد قراءتها من جدول المستلمين ولا تُراكم أبداً** (حادثة 2026-07-19: حملة تعرض 65 بينما 305 أُرسلت فعلاً، والفجوات مضاعفات BATCH_SIZE بالضبط)، والحجز ذرّي بـ`status=eq.pending` في شرط الـPATCH. **حواجز الإزعاج** (ساعات هدوء + حد يومي + إلغاء اشتراك) في `site_settings` بصفر DDL؛ ساعات الهدوء **تؤجّل ولا تُلغي** (لا تستهلك مستلمين) وتُحسب بتوقيت إسطنبول لا UTC وإلا انزاحت النافذة 3 ساعات فبدأ الإرسال 03:00 محلياً. **واتساب لا يُعاد بناؤه**: البناء يُنشئ صف `wa_campaigns` مرتبطاً ويُرسَل من `api/campaign.js` المُجرَّب — مُرسِل واحد لا اثنان. `lib/webpush.js` نسخة مستقلة من تعمية RFC 8291/8292 **عمداً** بدل استيراد من `notify-order.js` (مسار الطلبات الحرج صاحب تاريخ الفقدان الصامت — لا يُعاد هيكلته داخل تغيير موضوعه ميزة). **الواجهة:** جرس بعدّاد في الهيدر + صندوق إشعارات للعميل (يعيد استخدام بنية `.notif-list` الموجودة)، وتبويب إدارة «الإشعارات» بمنتقي شرائح يعرض **حجم الجمهور الحي لكل شريحة وقابلية الوصول لكل قناة**، ويعطّل زر الإنشاء ويحذّر صراحةً حين لا تصل الحملة لأحد. **علل حقيقية اكتُشفت بالاختبار لا بالمراجعة:** (أ) شريحة السلة المتروكة كانت تقرأ الهاتف من صفّ السلة وحده، والسلة تُنشأ غالباً والزائر مجهول ثم يتعرّف لاحقاً فيُملأ الهاتف على **الجهاز** — فكانت الحملة تفقد قناة واتساب بصمت لكل سلة أُنشئت قبل تعرّف صاحبها وهي الحالة الغالبة؛ عولج بـ`coalesce(c.customer_phone, d.customer_phone)`. (ب) `markCartConverted` كان يتسابق مع مزامنة السلة المؤجَّلة فتُكتب `dismissed` فوق `converted` وتُمحى كل التحويلات من التقرير؛ عولج بإلغاء المؤقّت وبذر البصمة. (ج) `icon("alert")` و`icon("global")` **غير موجودين** و`icon()` يرتدّ لـ`box` **بصمت** فيرسم شكلاً خاطئاً مقنعاً بلا خطأ كونسول — أُضيفت أيقونة `alert` حقيقية واستُبدلت `global` بنص. (د) قواعد التوافق العددي العربي كانت خاطئة («قبل 5 دقيقة») — أُصلحت للمفرد/المثنى/الجمع/11+ بأرقام عربية-هندية متسقة. **التحقّق (ثلاث طبقات فعلية):** 20 اختباراً للترحيل على **Postgres حقيقي محلي** (`pglite`) بتشغيل الملف **بلا تعديل** بعد إنشاء الأدوار، وبيانات بذرة نتائجها محسوبة يدوياً — تشمل أن مرفوض-فقط يُعامَل «لم يطلب»، وأن معاملات العمر تعمل فعلاً، وأن شريحة مجهولة تُرجع صفراً لا الجميع؛ و45 اختباراً لمنطق القرار (أهمها: **هاتف واحد على جهازين ⇒ رسالة واتساب واحدة** لا اثنتين)؛ وتحقّق حي في المتصفح: الحارس يردّ **405 برسالة صريحة** لا 403 عارياً، وحقن XSS في عنوان حملة وفي كل حقول الإشعار أعطى **صفر عنصر `<img>` وصفر سمة `onerror`**، وصفر انزياح أفقي على 375px، وصفر أخطاء كونسول، و**كل التبويبات الـ22 لا تزال تُصيَّر** (لم تُكسر الخرائط المشتركة) | `migrations/20260720_notifications_system.sql` (جديد)، `api/notifications.js` (جديد)، `lib/webpush.js` (جديد)، `app.js`, `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` + طبقة Flutter كاملة (`dukkanci_customer_app/lib/features/notifications/**`) | نعم — `npm run check:versions` ناجح (app.js v338→340، styles.css v180→181، CACHE v256→258). ⚠️ **مطلوب من المستخدم خطوة يدوية واحدة: تشغيل `migrations/20260720_notifications_system.sql` في محرر SQL بـSupabase** — قبلها يعرض التبويب رسالة صريحة «نظام الإشعارات غير مُفعّل» بدل أصفار كاذبة. الملف نفسه **يُصلح عطلين قائمين**: `banner_events` (لوحة نقرات البانرات ميتة منذ 2026-07-19) و`stores.category_settings` (حفظ تصنيفات المتاجر يفشل بـ502 منذ 2026-07-18) — تحقّق حي أن كليهما غير مطبَّق فعلاً. 🔴 **جولة إكمال ثالثة (نفس اليوم) — أُغلقت ثغرة ثالثة أخطر:** **قناة FCM لم يكن لها مُرسِل على الخادم إطلاقاً** — كان `sendBatch` يكتفي بوسم المستلمين `skipped: fcm_not_configured`، أي أن إضافة Firebase وحدها **ما كانت لتكفي** ولا رسالة واحدة كانت ستصل شريط إشعارات أندرويد. أُنشئ `lib/fcm.js` (FCM HTTP v1 كاملاً على `crypto` بلا أي تبعية، مطابقةً لأسلوب `lib/webpush.js`): توقيع JWT بـRS256 من حساب خدمة ← تبديله بـaccess token مع تخزين مؤقّت 55 دقيقة (كي لا تنتهي صلاحيته وسط دفعة)، ثم `messages:send`، مع تنظيف الرمز الميت (`UNREGISTERED`) من `app_devices` كي لا يبقى الجهاز محسوباً كقابل للوصول في كل عدّ شرائح لاحق، وإسقاط التوكن المخزَّن عند 401 فتُعاد صياغته بدل فشل بقية الدفعة. **ووُصلت القناة في مكانين لا مكان واحد:** الحملات الترويجية (`api/notifications.js`)، **وتغيير حالة الطلب** (`api/notify-order.js`) — وهذا الثاني هو جوهر الطلب الأصلي: «خرج للتوصيل» يجب أن يرنّ على الجوال، لا أن ينتظر فتح التطبيق. **12 اختباراً لـ`lib/fcm.js`**، أهمها اكتشاف حقيقي: مفاتيح البيئة تُخزّن السطر الجديد كحرفَي `\\n`، و`crypto.sign` بهذه الصيغة **يرمي `DECODER routines::unsupported`** — أي أن فكّ الترميز في الملف **حامل للحمل لا تجميلي**، وأُثبت ذلك بتوليد مفتاح RSA حقيقي والتحقق أن الصيغة المهرَّبة تفشل والمفكوكة تنجح وتتحقّق. **درس أدوات:** كتابة ملف اختبار عبر heredoc في bash **تُفسد الشرطات المائلة العكسية بصمت** فتحوّل الاختبار إلى لا-عملية تمرّ كذباً (حدث فعلاً: ظهر سطر جديد حقيقي داخل اسم الاختبار فكشف الخلل) — استُخدمت أداة الكتابة المباشرة بدلاً منها. 🔴 **وأُصلح عطل السلة P0 الموصوف أدناه** (`storeId ?? this.storeId`) بعد عرضه مرتين بلا اعتراض؛ تحقّق بعده: `flutter analyze` = 4 ملاحظات (نفس خط الأساس)، و**37 اختباراً نجحت كلها**. ⚠️ **تصحيح لاستنتاج خاطئ سجّلتُه أولاً:** ظننتُ أن `widget_test.dart` «متقلّب زمنياً» لأنه فشل في جولة ونجح في أخرى — والحقيقة أنه **كان مكسوراً فعلاً وأُصلح في شجرة العمل** (`SplashScreen` يحمل تأخير إقلاع 700ms + `AnimationController`، وإنهاء الاختبار وأيٌّ منهما مسلَّح يُسقط `!timersPending`). أُثبت ذلك قطعياً بإعادة الملف لنسخة HEAD وتشغيله: **فشل**، ثم استعادة الإصلاح: **نجح**. الدرس: «فشل مرة ونجح مرة» ليس دليل تقلّب — بل سبب لإعادة الاختبار بعزل المتغيّر قبل الحكم. تحقّق حي نهائي: تحديث حالة الطلب يُعيد `ok:true` بينما تُبلّغ القنوات الثلاث عن تعطّلها كلٌّ على حدة (`push: vapid not configured`، `inbox: write failed`، `fcm: not configured`) — أي أن **أياً منها لا يستطيع تعطيل الطلب**، وصفر بيانات اختبار متبقية في الإنتاج (تحقّق: الطلبات لا تزال 13). 🔴 **ثغرتان في تسليمي الأول أُغلقتا بعد مراجعة المستخدم (2026-07-20، نفس الجلسة):** (1) **إشعارات حالة الطلب لم تكن موصولة بالصندوق إطلاقاً** — بُني الجدول ونوع `kind='order'` لكن **لا شيء كان يكتب فيه**، فتغيير الحالة كان يُطلق إشعار متصفح فقط (`pushOrderStatus`، موجود من قبل) ولا يصل صندوق التطبيق؛ أي أن «قيد التجهيز/خرج للتوصيل/تم التسليم» — وهو **جوهر الطلب الأصلي** — لم يكن يظهر في التطبيق. أُصلح بإدراج صفّ في `notifications` عند نقطة تغيير الحالة في `api/notify-order.js` (سطر ~2530)، **إضافي بالكامل وfail-open داخل `try/catch`** فلا يمكن أن يعطّل تحديث حالة الطلب مهما فشل (تحقّق حي: الجدول غير موجود ⇒ `inbox:{ok:false}` بينما `ok:true` والحالة تُحدَّث بنجاح 200). (2) **شريحة «كل من نزّل التطبيق» لم تكن موجودة** — كانت الشرائح تغطي «نزّل ولم يطلب» فقط و«كل الأجهزة» التي تخلط متصفحي الويب بالتطبيق، فلم تكن هناك طريقة لإرسال «عرض جديد/توصيل مجاني لكل من نزّل التطبيق» كما طُلب حرفياً؛ أُضيفت شريحة `app_installs` (كل `android/ios/pwa` بصرف النظر عن الطلبات) مع 5 اختبارات تُثبت تمايزها عن الشريحتين الأخريين. **درس:** بناء الحاوية ليس بناء الميزة — راجع الطلب الأصلي حرفاً بحرف قبل إعلان الإتمام. 🔴 **توضيح من المستخدم يُصحّح تأطيري:** الـ6,334 رقماً في `wa_contacts` هي **قوائم تسويق مرفوعة ولا علاقة لها بمستخدمي الموقع** — كنتُ أعرضها في الملخّصات كـ«وصول» وهو تأطير مضلِّل. تحقّق برمجي بعده: **صفر كتابة** عليها من كل الكود الجديد (قراءة فقط في موضع واحد)، واختبار عزل يُثبت **صفر تسريب** منها إلى أي شريحة عملاء (كل الشرائح الثماني الأخرى مصدرها `orders`/`app_devices`/`cart_snapshots` حصراً). يبقى خطر امتثال حقيقي: إرسال ترويجي لقائمة مرفوعة بلا موافقة صريحة يرفع بلاغات Meta وقد يُقيَّد رقم المنصة الذي تمرّ عبره إشعارات الطلبات الحقيقية. 🔴 **عطل P0 حيّ اكتُشف أثناء وصل مزامنة السلة في Flutter ولم يُصلَح (خارج النطاق، بانتظار قرار المستخدم):** `CartState.copyWith` في `dukkanci_customer_app/lib/features/cart/application/cart_controller.dart:22` مكتوبة `CartState(storeId: storeId, …)` بدل `storeId ?? this.storeId`، فأي استدعاء بلا تمرير المتجر صراحةً **يُصفّر `storeId`**. النتيجة: `updateQuantity` (سطر 93) و`removeLine` (سطر 104) تمحوان المتجر، ثم `_submit()` في `checkout_screen.dart:100` يبدأ بـ`if (cart.isEmpty \|\| cart.storeId == null) return;` — أي أن **زر «تأكيد الطلب» لا يفعل شيئاً إطلاقاً وبلا أي رسالة خطأ بعد أي تغيير كمية في السلة**. تحقّق مستقل بقراءة المسار الثلاثي كاملاً. لم يظهر سابقاً لأن `addItem` (سطر 76) تمرّر `storeId` صراحةً فتبدو السلة سليمة حتى أول تعديل كمية. الإصلاح سطر واحد (`storeId ?? this.storeId`) لكنه يمسّ دلالات السلة المشتركة. طبقة الإشعارات حُصِّنت منه بتمرير متجر ما قبل التعديل (`previousStoreId`) فلا تعتمد على السلوك المكسور. **التحقّق من طبقة Flutter:** `flutter analyze` = **4 ملاحظات مطابقة تماماً لخط الأساس قبل العمل** (كلها إهمال `anonKey`/`Radio` قديم، صفر جديدة — تحقّقت بنفسي بتشغيلها)، و`flutter build apk --debug` نجح فعلياً (exit 0)، و31 اختباراً ناجحاً؛ اختبار `widget_test.dart` الفاشل **سابق للعمل** (أُثبت بإعادة الملف لـHEAD وتكرار نفس الفشل ثم استعادته). ⚠ لم يُشغَّل محاكي ولم تُختبَر نقطة `/api/notifications` من التطبيق حيّاً. ملاحظة اتساق مقصودة: الوقت النسبي في Flutter بأرقام لاتينية (مطابقةً لبقية أرقام التطبيق) وفي الموقع بأرقام عربية-هندية (مطابقةً لبقية أرقام الموقع) — كل منصة متسقة داخلياً. **لم يُنفَّذ `git commit`/push — بانتظار تعليمات المستخدم**
| 2026-07-20 | مراجعة بشرية لـ16 نسخة عمل و30 فرعاً غير مدموج (قاعدة 3 في هذا الملف) — **فكشفت أن فرعين مهجورين يحملان إصلاحين لم يصلا `main` قط، أحدهما ثغرة أمنية حيّة** | 🔴 **(1) XSS مخزَّنة قابلة للاستغلال فعلاً:** `product.name`/`product.image`/`store.name`/`store.image`/`store.offer` كانت تُحقن مباشرة في قوالب `innerHTML` بلا تعقيم. هذه الحقول **يحرّرها التاجر نفسه** من لوحته، والتسجيل ذاتي عبر `/join`. تحقّق حي في المتصفح (لا نظري): قيمة `image = 'x" onerror="…" q="'` أنتجت عنصراً يحمل فعلاً السمات `src/onerror/z/alt/x` — أي كسر سمة حقيقي ينفَّذ عند كل زائر يرى البطاقة. الفرع `claude/mystifying-sanderson-b7c4f6` (2026-07-04) كان قد رصدها لكنه **لم يُدمج قط**، **وأصلح ثلاثة أسطر فقط** بينما بطاقة المفضّلة وعنصر السلة وملخّص الدفع بقيت مصابة ولم يعالجها أحد. 🔴 **(2) `/merchants` محجوبة عن غوغل:** `robots.txt` يحوي `Disallow: /merchant`، و«Disallow» **يطابق البادئة**، فكان يحجب أيضاً صفحة استقطاب التجار العامة `/merchants` (تُرجع 200 وهي صفحة تسويقية حقيقية موثَّقة في [[merchant-landing-page]])، وهي **غير مُدرَجة في sitemap كذلك**. الفرع `fix-robots-merchants` (2026-06-30) عالجها ولم يُدمج قط. **(3) درس منهجي:** قياس «هل الفرع مدموج» بـ`git branch --no-merged` أو `git diff main...branch` **مضلِّل تماماً** هنا — الأول يقارن المعرّفات فيَعُدّ كل فرع دُمج بالضغط (squash) «غير مدموج»، والثاني يُظهر ما أضافه الفرع بصرف النظر عن وجود مكافئه في `main`. الأداة الصحيحة **`git cherry main <branch>`** (`-` = رقعة مكافئة موجودة أصلاً، `+` = فريدة فعلاً): أعادت **20 من 30 فرعاً آمنة تماماً**، وحصرت المراجعة في 10 فقط | **(أ) إصلاح XSS — 29 موضعاً** بـ`escAttr()` داخل السمات و`esc()` في النص (سكربت استبدال نصّي دقيق يعدّ إصابات كل نمط بدل regex أعمى على ملف 12,884 سطراً)، ويشمل المواضع التي لم يعالجها الفرع الأصلي. **المسح النهائي: صفر حقن سمة غير معقَّم متبقٍّ في `app.js`.** تحقّق حي بحقن الحمولة في كائن منتج/متجر حقيقي وتصييره عبر **دوال البطاقات الخاصة بالتطبيق نفسه**: `xssFired:false`، صفر سمة `onerror`، والحمولة تُعرض نصاً. فحص انحدار: صفر نص مُعقَّم مرتين (خطر مضاعفة `esc`)، وصفر صورة **مرئية** فاشلة (الـ76 غير المحمَّلة كلها `loading=lazy` خارج الشاشة — تحقّق بـ`getBoundingClientRect` لا بالعدّ الخام)، وصفر رابط مشوَّه بالتعقيم، وصفر خطأ كونسول. **(ب) `robots.txt`:** أُضيف `Allow: /merchants` مع بقاء `/merchant` (لوحة التاجر) محجوبة. **(ج) تنظيف آمن مكتمل:** حُذف **29 فرعاً و14 نسخة عمل** — النتيجة **51→22 فرعاً، 30→1 غير مدموج، 16→2 نسخة عمل**. لم يُحذف شيء إلا بعد تحقّق **بالمحتوى لا بالمعرّف**: كل متجر (بابليمن/يمي يمي/رودي) أُكِّد وجود ملف بياناته + وصله في `index.html` + سلاگه؛ و`fix-customers-directory` أُكِّد بوجود مفتاح `"n:" + normalizeAr(name)` في `app.js` سطر 6622؛ و`security-hardening` بوجود معاملات قالب الحملات وزر URL الديناميكي؛ و`fix-delivery-zone-nan` بوجود التقييمات ونافذة إكمال البيانات؛ و`add-beyazit-store` تبيّن أنه **فرع مُسمّى خطأً** يحمل نفس التزام بكسل الخوالي (تحقّق أن الأسطر المضافة متطابقة تماماً بالبصمة، والفرق سياق فقط). وأُعيد تشغيل `git cherry` **مباشرة قبل كل حذف**، وتُحقّق أن كل نسخة عمل صفر تعديلات. 🔴 **(د) اكتشاف ثالث:** فرع `khawali-store-pixel` (بكسل ميتا خاص بمتجر واحد ليراقب التاجر زيارات صفحته) **لم يُدمج قط رغم أن ذاكرة المشروع كانت تسجّله «Live»** — تحقّق قاطع: `STORE_META_PIXELS` و`storeVisitPixel` ومعرّف البكسل كلها **صفر إشارة في `main`** بينما توجد في الفرع. صُحِّحت الذاكرة. **أُبقي الفرع عمداً بلا دمج** لأن شحنه **قرار خصوصية لا قرار تقني**: يُطلق بكسل طرف ثالث يملكه تاجر على صفحات يراها الزوار فيُرسل مشاهداتهم لحساب ميتا الخاص به (وإن كان يحترم `consentOK("marketing")`) — راجع تحذير تشتّت البكسلات في قسم 6 قبل إضافة رابع. **(هـ) ذاكرة ثانية مُصحَّحة:** سطر فهرس `fbads` كان يقول «غير ملتزَمة» بينما `api/fbads.js` مدموج في `main` فعلاً منذ PR #85. **(د) ما أُبقي عمداً:** `merchant-staff-roles` — 354 سطراً من عمل فريد (نظام صلاحيات موظفي المتجر، §18 المسجَّل «متبقٍّ»، و`main` خالٍ منه تماماً: صفر إشارة لـ`store_staff`/`merchantSectionAllowed`). **لا يُدمج كما هو:** تعارضاته بدت شاملة لكنها كانت **وهم نهايات أسطر** (النسخة CRLF و`main` صار LF) — بعد التطبيع تُطبَّق **20 من 27 جزءاً** وتبقى 4 تعارضات حقيقية، **ولا يوجد ترحيل لجدول `store_staff` إطلاقاً** (جانب قاعدة البيانات لم يُنشأ قط) | `app.js`, `robots.txt`, `index.html`, `sw.js`, `CLAUDE.md` + حذف 20 فرعاً و7 نُسخ عمل | نعم — `npm run check:versions` ناجح (app.js v337→338، CACHE v255→256؛ styles.css بلا تغيير). **مدفوع لـ`main`** (`82511bb2` للـXSS ثم `825d087e` لـrobots، Vercel نجح). تحقّق بعد النشر: `app.js?v=338` الحي يحمل 7 مواضع `escAttr(product.image)` و17 موضع `escAttr(store.name)` و**صفر حقن سمة غير معقَّم**، و`robots.txt` الحي يحمل `Allow: /merchants`، و`/merchants` تُرجع 200. ⚠ **بعد هذين الإصلاحين صار الفرعان `claude/mystifying-sanderson-b7c4f6` و`fix-robots-merchants` مُستهلَكين بالمحتوى وقابلين للحذف** |
| 2026-07-19 | المستخدم طلب «مراجعة كافة الجلسات وتنسيق الرفع حتى لا يحصل أي تضارب» — فتبيّن وجود **عملين يتيمين** لجلستين توقّفتا قبل الالتزام، أحدهما نقطة `create-order` (تحقّق سعر خادمي) في `api/notify-order.js` بـ**346 سطراً حقيقياً** | ليس خطأ برمجياً بل خلل تنسيق. **ثلاث حقائق حاكمة اكتُشفت بالمراجعة:** (1) **لا يوجد تضارب حيّ إطلاقاً** — الجلسات الأربعون كلها `isRunning:false` (تحقّق عبر `list_sessions`)، فخطر التزامن الموثَّق في [[concurrent-session-hazard]] تاريخي لا قائم؛ الخطر الحقيقي أن العمل اليتيم يضيع عند أي `reset`/`checkout` من أي جلسة لاحقة. (2) 🔴 **قنبلة فهرس حقيقية كانت موقوتة:** الفهرس المشترك كان يحمل شجرة **مطابقة بالبصمة تماماً** (`94c20b43`) لالتزام `12ed8216` أي ما **قبل** نظام البانرات — فأي `git commit` عارٍ من أي جلسة (حتى بلا `git add`) كان سيمحو **1503 سطراً**: نظام البانرات كاملاً (`app.js` 581 سطراً، `styles.css` 128، `api/notify-order.js` 71، ملفات Flutter الأربعة، ملف الترحيل) **وعمل الفوتر معاً**. السبب أن جلسة البانرات (وأنا) التزمنا عبر **فهرس مؤقّت** (`GIT_INDEX_FILE`+`commit-tree`، النمط الموثَّق لتفادي تشابك الجلسات)، وهو لا يُحدِّث الفهرس المشترك فيبقى مجمَّداً عند حالة قديمة. **درس عام: الفهرس المؤقّت يحلّ تشابك الجلسات لكنه يخلّف فهرساً متقادماً — لا تستخدمه إلا مع جلسة أخرى عاملة فعلاً، وإلا فـ`git add` انتقائي عادي.** (3) `create-order` **غير موصولة بأي عميل** (صفر إشارة في `app.js`/Flutter/`sw.js`) فهي خاملة تماماً عند النشر | **(أ) إبطال القنبلة:** `git reset` (mixed) يُعيد الفهرس إلى HEAD بلا لمس محتوى أي ملف — تحقّق بمقارنة بصمة محتوى 8 ملفات حسّاسة قبل/بعد: **الثمانية متطابقة تماماً، صفر تغيير**؛ وصارت ملفات البانرات متتبَّعة ونظيفة بدل «مُدرَجة كمحذوفة»، والفهرس فارغاً. حُفظت بصمة الشجرة المُجهَّزة (`902bd2fc`) في scratchpad كاحتياط. **(ب) رفع العمل اليتيم في التزامين منفصلين** بالترتيب الآمن (Flutter أولاً لأن Vercel لا ينشره، ثم الخادم وحده): `c1ca46eb` نقطة `create-order` — تُعيد حساب المجموع من أسعار المنتجات الحقيقية في القاعدة وترفع علماً حين يكون الإجمالي المُرسَل أقل بكثير من المتوقَّع، فيصل التاجر «⚠️ سعر مشبوه» في رسالة واتساب وفي عنوان إشعار الإدارة (+`action=retry-order-notification`). **فحص انحدار قبل الالتزام لأنه عمل غيري وملفٌ يخدم بانرات حيّة:** الأسطر الـ18 المحذوفة أثارت الشكّ فتُحقِّق أن كل رموز البانرات (`knownBannerIds`/`bannerIdCache`/`banner-event`/`banner-stats`/`banner_events`) وكل الـ`action` القائمة موجودة كما هي — نقل كتلة لا حذف، والتغيير **إضافي بالكامل**؛ و`node --check` سليم. **⚠ لم يُختبر منطق التحقّق من السعر حيّاً إطلاقاً** (لا عميل يستدعيه بعد) — ترحيل العملاء إليه خطوة لاحقة منفصلة وهو **الأهم أمنياً** لأن الأسعار لا تزال تُرسَل من المتصفح بلا تحقّق. **(ج) بروتوكول رفع مُثبَّت:** `git add` انتقائي صريح ← `git diff --cached --stat` والتأكد أن العدد يطابق المقصود ← `commit` عادي؛ التزام واحد لكل غرض، وبلا خلط ملفات الخادم بملفات Flutter. تحقّق بعد النشر: Vercel `success`، الفوتر الحي سليم، و`/api/notify-order?action=orders` لا يزال يردّ 403 بلا توكن (حارس المصادقة يعمل) | `api/notify-order.js`, `CLAUDE.md` (لا تعديل على قاعدة البيانات) | لا ينطبق (خادم/Flutter فقط — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`). **مدفوع لـ`main`** (`ba4141be` ثم `c1ca46eb`، Vercel نجح). ⚠️ **باقٍ:** CRLF/LF في `notify-order.js` (HEAD يخزّنه CRLF والشجرة LF بفعل `.gitattributes` غير المتتبَّع) جعل هذا الالتزام يُسجَّل 5668 سطراً بينما الحقيقي 346/18 — العلاج `git add --renormalize .` في التزام مستقل، **والشجرة نظيفة تماماً الآن لأول مرة** وهو الشرط الذي يشترطه `.gitattributes` نفسه |
| 2026-07-19 | زر البحث الصوتي في تطبيق Flutter كان يعرض «البحث الصوتي غير متاح على هذا الجهاز» على **هواتف حقيقية تتعرّف على العربية بشكل سليم** | 🔴 **تصحيح جوهري لتشخيص سطر 2026-07-18 (تفعيل البحث الصوتي) أدناه:** ذلك السطر استنتج من المحاكي أن صورة النظام تفتقر لحزمة اللغة العربية، وأن الرسالة سلوك سليم. الاختبار على هاتف حقيقي أثبت أن **الفحص نفسه هو العلة**: `SpeechToText.locales()` على أندرويد 13+ يُبلّغ فقط عن حزم اللغات **المُنزَّلة على الجهاز** (`checkRecognitionSupport().supportedOnDeviceLanguages`) لا عن قدرة التعرّف الفعلية — ولا أحد يُنزّل الحزمة غير المتصلة يدوياً، فكان الفحص يُعيد صفر مدخلات عربية على هواتف تعمل تماماً عبر المسار الشبكي المعتاد، فيمتنع الكود عن استدعاء `listen()` أصلاً ويُعلن «غير متاح» كذباً. أي أن منطق «الفشل بصدق» المقصود انقلب إلى **منع كاذب** على الأجهزة السليمة | حُذف `_findArabicLocale` وفحص اللغة المسبق كلياً؛ صار الزر يحاول `listen(localeId: 'ar')` مباشرة — نفس ما يفعله الموقع بـ`lang='ar'` — ولا يعرض رسالة فشل إلا إن **أخفقت محاولة الاستماع نفسها**، عبر `_handleListenError(SpeechRecognitionError)` يميّز كل حالة (`error_no_match` وغيرها). عمل جلسة سابقة توقّفت قبل الالتزام؛ **التُزم كما هو بلا أي تعديل مني**. تحقّق قبل الالتزام: توازن أقواس Dart صفر، و**التوقيع العام للودجة (`VoiceSearchButton({super.key, required this.onResult})`) مطابق حرفياً بين HEAD والشجرة** — أي أن `search_screen.dart` المُلتزَم في `abe58147` (بحث Flutter العربي) يعمل معه بلا كسر، وهو فحص ضروري لأن الجلسة التزمت شاشة البحث وتركت هذا الملف. **⚠ لم يُشغَّل `flutter analyze` ولا اختبار على جهاز في هذه الجلسة** — الثقة مبنية على قراءة الفرق وعلى أن الجلسة المالكة وثّقت اختباراً على هاتف حقيقي | `dukkanci_customer_app/lib/core/widgets/voice_search_button.dart`, `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل). **مدفوع لـ`main`** (commit `ba4141be`) |
| 2026-07-19 | المستخدم طلب إضافة أيقونة يوتيوب في الفوتر + حسابات تيك توك (`@dukkanci.tr`) وانستغرام (`dukkancitr`) وقناة واتساب | ليس خطأ — إضافة روابط. **ثلاث ملاحظات حقيقية اكتُشفت أثناء التنفيذ:** (1) 🔴 **لا توجد أيقونة `youtube` في `iconPaths` إطلاقاً**، و`icon(name)` يرتدّ صامتاً لـ`iconPaths.box` عند اسم غير معروف (`iconPaths[name] \|\| iconPaths.box`) — أي أن إضافة `data-icon="youtube"` وحدها كانت ستعرض **أيقونة صندوق** بلا أي خطأ في الكونسول؛ ولأن `box` يتكوّن من عنصرَين مثل أيقونة يوتيوب التي كتبتها، فإن مجرّد عدّ عناصر الـSVG **لا يميّز بينهما** — تحقّقتُ من `innerHTML` الفعلي (`<rect>+<path>` وليس مسار `m4 7 8-4…` الخاص بـbox) بدل الاكتفاء بالعدّ. (2) 🔴 **حسابا انستغرام/تيك توك المطلوبان مختلفان فعلاً عن الموجودَين** (`dukkancitr` مقابل `dukkanci.marketplace`، و`@dukkanci.tr` مقابل `@dukkanci.marketplace`) — أي أن «الإضافة» تحتمل معنيين؛ نُفِّذت أولاً كإضافة (8 أيقونات) مع طرح الالتباس صراحةً على المستخدم، فأجاب «استبدل الجديد بالقديم» فحُذف الحسابان القديمان `.marketplace` نهائياً وبقي الجديدان فقط (**6 أيقونات**: فيسبوك ×2 + انستغرام + تيك توك + يوتيوب + قناة واتساب). لاحظ أن **رابط يوتيوب نفسه لا يزال على نطاق `@dukkanci.marketplace`** (هكذا أرسله المستخدم حرفياً) — ليس تناقضاً بل قناة يوتيوب الحقيقية، فلا «تصحَّح» لاحقاً بالقياس على انستغرام/تيك توك. (3) 🔴 **عيب تخطيط حقيقي:** `.social-row` كانت `display:flex` بلا `wrap`؛ 8 أيقونات × 40px + 7 فجوات × 12px = **404px** بينما عمود العلامة 368px على سطح المكتب و349px على الموبايل — أي أن الصفّ **لم يكن يتّسع لثمانٍ في سطر واحد عند أي عرض إطلاقاً**. أول إصلاح (`flex-wrap:wrap`) أزال الفيضان لكن قياساً حياً أظهر أنه يوزّعها **7+1** على سطح المكتب فتبقى أيقونة قناة واتساب **وحيدة معلّقة** في سطر ثانٍ (تبدو كعطل لا كتصميم)، فحُوّلت لشبكة 4+4 متعمَّدة؛ **وبعد الاستبدال صارت 6 أيقونات = 300px فقط فتتّسع فعلاً في سطر واحد عند كل عرض**، فأُعيدت لـ`flex` بسطر واحد (مع `wrap` كشبكة أمان لو أُضيفت أيقونات لاحقاً) | (أ) `app.js`: أُضيف مسار أيقونة `youtube` حقيقي بنفس أسلوب بقية الأيقونات (خطوط `stroke` بلا تعبئة، `viewBox 0 0 24 24`): مستطيل مُدوّر + مثلث تشغيل. (ب) `index.html`: أُضيفت الروابط الأربعة داخل `.social-row` بـ`target="_blank" rel="noopener"` مطابقةً للموجود، ومرتّبة بحيث يلي كل حساب جديد شقيقه في نفس المنصة. (ج) `styles.css`: أُضيف `flex-wrap:wrap` لـ`.social-row` (كشبكة أمان — الصفّ كان `flex` بلا التفاف فيفيض) + `justify-content:center` في كتلة الموبايل لأن عمود العلامة مُوسَّط هناك أصلاً — **حجم اللمس 40px بقي كما هو ولم يُصغَّر** (البديل المرفوض كان تقليص الدوائر لحشر الأيقونات في سطر واحد، وهو تراجع في إمكانية الوصول على الموبايل). **التحقّق (قياس حي، لا افتراض):** الروابط الستة بمساراتها الصحيحة وأيقوناتها المُصيَّرة فعلاً وصفر أثر للحسابين القديمين (`hasOldMarketplaceAccounts:false`)؛ أيقونة يوتيوب **ليست ارتداد box** (تحقّق من `innerHTML`)؛ كلها بنفس المقاس والحدّ و`stroke-width` (17px/1.8px)؛ **سطر واحد بعرض 300px عند 375 و1280** (مُوسَّط داخل عمود العلامة على الموبايل)؛ صفر انزياح أفقي (`scrollWidth == innerWidth`)؛ ترتيب RTL صحيح (الأول يميناً)؛ صفر أخطاء كونسول. 🔴 **درس بيئي تكرّر:** بعد تعديل `styles.css` **بعد** رفع الإصدار، ظلّ المتصفح يخدم `?v=180` القديمة فأظهر `display:flex` رغم أن الملف على القرص يحمل `grid` — لم يظهر التعديل إلا بحقن الملف برابط كاسر للتخزين المؤقت (نفس درس «التعديل بلا رفع `?v=` لا يظهر محلياً» الموثَّق مراراً). كذلك تأكّد مجدداً أن **`scrollIntoView`/`window.scrollTo` ولقطات الشاشة معطّلة في متصفح الجلسة** (الصفحة 21,077px ومحاولات التمرير تُبقي `scrollY=0`، و`screenshot`/`zoom` تنتهيان بمهلة 30 ثانية) — فاعتُمد التحقّق الهندسي عبر `getBoundingClientRect` والأنماط المحسوبة | `app.js`, `index.html`, `styles.css`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v336→337، styles.css v179→180، CACHE v254→255). **لم يُنفَّذ `git commit`/push — بانتظار تعليمات المستخدم** |
| 2026-07-19 | المستخدم أبلغ أن البحث في تطبيق Flutter **يبحث في المتاجر فقط ولا يبحث في المنتجات**، وطلب توسيعه ليشمل المنتجات | حقل البحث في الصفحة الرئيسية مجرّد زر تنقّل لشاشة البحث (`context.push(AppRoutes.search)`)، وشاشة البحث كانت ترشّح `_allStores` فقط (مطابقة على `name`/`category` للمتجر)، و`StoreRepository` **لم يكن فيه أي دالة بحث منتجات إطلاقاً** (تعليق `searchStores` نفسه كان يقول «انقل منطق getMatchingProducts لاحقاً» — أي دَين تقني موثَّق ولم يُنفَّذ). مفارقة مؤكِّدة للبلاغ: `AppStrings.homeSearchHint` كان أصلاً **«ابحث عن متجر أو منتج...»** أي أن التطبيق يَعِد بالبحث في المنتجات ولا يفعله. 🔴 **القيد الحاكم الذي منع نسخ حل الموقع حرفياً:** الموقع يحمّل كل المنتجات في الذاكرة ويرشّحها بـJS (`getMatchingProducts`)، لكن PostgREST هنا مضبوط بـ`db-max-rows=1000` يقصّ **كل** استجابة مهما كان `&limit=` — **تحقّق حي مباشر: `products?select=id&limit=5000` أعاد 1000 صفاً بالضبط من أصل 11,725**. أي أن استنساخ أسلوب الموقع كان سيبحث في **~9% من الكتالوج فقط** بينما يبدو سليماً تماماً (نفس فصيلة «القصّ الصامت» الموثَّقة في سطرَي `notify-order.js`/`campaign.js` أعلاه) | **البحث صار من طرف الخادم (Postgres) لا من طرف العميل.** ملف جديد `core/utils/arabic.dart` يوحّد منطق المطابقة (كان مكرَّراً بنسختين **متباينتين** ناقصتين في `search_screen.dart` و`store_repository.dart`): `normalizeArabic` منقولة حرفاً بحرف عن `normalizeAr` في `app.js:3275` كي لا يختلف التطبيق والموقع على معنى «مطابق»، و`matchesAllTerms` تطابق `terms.every(includes)`. **`arabicIlikePattern` هو جوهر الحل، وكل قرار فيه مبني على قياس حي لا تخمين:** (1) البحث الحرفي بـ`ilike` **يفشل صامتاً على العربية** — `*شاورمة*` يُرجع **صفر** صفاً بينما 367 منتجاً تطابقها فعلاً، والبيانات نفسها تحوي الإملاءين (`أرز` 55 صفاً و`ارز` 20 صفاً)، فكل موضع «ملتبس» (ا/ه/ي/و — لأن أإآ→ا وة→ه وى/ئ→ي وؤ→و) يصير `_`. (2) التطبيع **يحذف** التطويل والحركات ولا يستبدلها، ولا يمكن لأي بديل أحادي أن ينوب عنها، فتُوصَل الأحرف بـ`*`: هذا وحده رفع دقة «حمص» من **156/161 إلى 161/161** (استرجع فعلياً «حمّص» و«حـمـص بيـروتـي» الحقيقيين). (3) أي محرف غير حرف/رقم يصير `_` أيضاً، فلا يستطيع مستخدم كسر صياغة `or=(…)` بفاصلة/قوس ولا توسيع النمط بـ`%`/`*`. **التعدد اللفظي:** كل كلمة تُرسِل `or=(name.ilike…,category.ilike…)` مستقلاً، وPostgREST يَعطف المكرَّرات بـAND (تأكَّد من مصدر الحزمة: `appendSearchParams` يُلحق بقائمة) — رفع «دجاج مشوي» من **35/42 إلى 42/42** وجعل ترتيب الكلمات غير مؤثِّر (نمط واحد مدموج كان يفرض ترتيباً). الأنماط **متعمَّدة التساهل**، فما يعود من الخادم **مرشَّحون لا نتائج**، ثم يطبّق العميل `matchesAllTerms` للدقة — الصحة من المرشِّح الثاني، والخادم عليه فقط أن يكون مجموعة فائقة رخيصة. 🔴 **بوابة اعتماد المتاجر (وليست تفصيلاً تجميلياً):** نتائج المنتجات تُضامّ مع خريطة المتاجر المرئية (`fetchApprovedStores`)، وأي منتج متجره غائب يُسقَط — **قياس حي: 8 منتجات «شاورما» و1 «دجاج مشوي» تخصّ متاجر غير معتمدة كانت ستتسرّب للزبون** لولا هذه البوابة (نفس فصيلة تسريب المتجر المرفوض 20 الموثَّقة في قسم 6). **الواجهة** تحاكي ترتيب الموقع حرفياً: المنتجات أولاً ثم المتاجر، بنفس صيغ العدّ (`N منتج` + `نتائج البحث عن «q»`، ثم `N متجر` + `مطابق لبحثك`) وبنفس سقف الـ60 وشارة `60+`. بطاقة المنتج **صف لا مربع** عمداً: البحث يعبر كل المتاجر فاسم المتجر معلومة أساسية لا زينة. أُضيف حارس استجابة متأخرة (`query != _query`) فلا يطمس طلب بطيء سابق نتيجة ما يكتبه المستخدم الآن، وقسم المنتجات يفشل «داخل التدفّق» برسالة وزر إعادة محاولة فلا يُخفي نتائج المتاجر تحته. **التحقّق (ثلاث طبقات، كلها فعلية):** (أ) 15 اختبار وحدة (`test/arabic_search_test.dart`) — **فشل منها اثنان أول مرة وكان توقّعي أنا الخاطئ لا الكود**: نسيت أن `و` ملتبسة (ؤ→و)، و**افترضت خطأً أن «شاورمة» تطابق «شاورما»** بينما التطبيع يطوي ة→ه ويترك ا فتبقيان مختلفتين — تأكَّد ذلك بالبيانات (query «شاورمة» = **صفر** نتيجة على الموقع أيضاً)، فسُجِّلت كـ«حد معروف» باختبار صريح بدل إخفائها. (ب) 5 اختبارات شبكية حقيقية (`test/product_search_network_test.dart`) تمرّ عبر **عميل supabase-dart الفعلي** على قاعدة الإنتاج — تسدّ الثغرة الوحيدة التي لا يغطيها أي اختبار مُحكَم: التطبيق **لم يُرسل نصاً عربياً كمرشِّح لـPostgREST من قبل قط**، فترميز الـURL وسلوك `or` المكرَّر لا يُثبتهما إلا رحلة حقيقية (ملاحظة بيئية تستحق التوثيق: `flutter_test` يزرع `HttpOverrides` يردّ 400 على كل طلب، فلزم `HttpOverrides.global = null`). (ج) تشغيل حي على المحاكي: `pizza` أظهر **«8 منتج»** و**«0 متجر»** مطابقَين تماماً للحقيقة المحسوبة مسبقاً من القاعدة، بأسعار مطابقة صفاً بصف (420/570/410/470/495/395/390/495)، وصور حقيقية، ورسالة «لا توجد متاجر مطابقة»، والنقر على «Margarita pizza» فتح صفحة المنتج الصحيحة بسعرها ومتجرها وزر الإضافة للسلة — وصفر استثناء Dart في `logcat`. ⚠️ **لم يُختبر إدخال عربي من المحاكي**: `adb shell input text` يرمي `NullPointerException` على غير ASCII (قيد أندرويد مؤكَّد هنا مجدداً، وموثَّق سابقاً في سطر «كمّل على صفحة البحث»)، ولا خدمة `cmd clipboard` على هذه الصورة — لذا اختُبرت الواجهة بنص لاتيني، وسُدَّت الفجوة العربية بالطبقة (ب) لا بالافتراض. **لم تُنقل مرادفات اللهجات** (`product_synonyms`، 6,998 صفاً يقرؤها الموقع) — تحسين مستقبلي حقيقي لكنه خارج نطاق الطلب ويكلّف 7 طلبات عند الإقلاع. ملاحظة غير متعلقة رُصدت أثناء الاختبار: `BannerRepository.reportEvent` يردّ 400 في `logcat` (فشل ظريف مقصود) لأن ترحيل `banner_events` لم يُشغَّل بعد | `dukkanci_customer_app/lib/core/utils/arabic.dart` (جديد)، `dukkanci_customer_app/lib/features/stores/data/store_repository.dart`, `dukkanci_customer_app/lib/features/search/presentation/search_screen.dart`, `dukkanci_customer_app/lib/core/localization/app_strings.dart`, `dukkanci_customer_app/test/arabic_search_test.dart` (جديد)، `dukkanci_customer_app/test/product_search_network_test.dart` (جديد)، `dukkanci_customer_app/dart_test.yaml` (جديد)، `CLAUDE.md` (لا تعديل على قاعدة البيانات إطلاقاً — قراءة فقط) | لا ينطبق (مشروع Flutter منفصل — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`). `flutter analyze` نظيف (نفس الـ3 ملاحظات المعلوماتية القديمة + رابعة مطابقة لها في ملف الاختبار الجديد لاستخدامه نفس `anonKey` الذي يستخدمه `supabase_bootstrap.dart`). **لم يُنفَّذ `git commit`/push — بانتظار تعليمات المستخدم؛ ⚠ شجرة العمل تحمل أيضاً تعديلات جلسة أخرى غير مرتبطة (`providers.dart`, `voice_search_button.dart`, `home_screen.dart`, `features/banners/**`) لم تُلمس، فاعزل الملفات بـ`git add` انتقائي عند الالتزام** |
| 2026-07-19 | المستخدم طلب بناء **نظام إدارة بانرات** للموقع وللتطبيق: التحكم من لوحة الإدارة، فترات ظهور (بداية/نهاية)، التحكم بعدد البانرات، ولوحة بيانات للنقرات لقياس كفاءة كل بانر | ميزة جديدة (لا خطأ سابق)، لكن سبقها تدقيق كشف **3 حقائق حاكمة للتصميم**: (1) 🔴 **لا يوجد أي مسار DDL متاح لي إطلاقاً** — موصل Supabase MCP في هذه الجلسة مربوط بمشروع `iseldofsfhwvpfzltqet` (goldengttr-hub) لا بمشروع دكانجي `tzcqnqzltrjemdnkzpzn` (نفس العطل الموثَّق مراراً)، ولا `DATABASE_URL` في `.env` (المفاتيح الثلاثة الوحيدة: ADMIN_PASSWORD وSUPABASE_SERVICE_ROLE_KEY وOPENAI_API_KEY)، ولا Supabase CLI، وتحقّق مباشر أن لا وجود لأي RPC من نوع `exec_sql`/`execute_sql`/`exec`/`run_sql` (الأربعة أعادت PGRST202). أي أن أي جدول جديد يحتاج تشغيلاً يدوياً من المستخدم. (2) 🔴 **ترحيل الأمس `20260719_store_category_settings.sql` لم يُطبَّق فعلياً على الإنتاج** — تحقّق حي: `stores?select=category_settings` يردّ `42703: column stores.category_settings does not exist`، أي أن ميزة «تصنيفات المتاجر» المبنية أمس **معطَّلة الآن في الإنتاج** (كل حفظ يردّ 502). هذا يؤكد نمطاً: ملفات الترحيل تتراكم بلا تطبيق، فبناء ميزة تعتمد على DDL جديد = بناء ميزة معطَّلة. (3) `tracking_events` (7,225 صفاً، حيّ ويجمع فعلاً) **لا يملك أي عمود لمعرّف البانر** ولا عمود jsonb عام — فحشر معرّف البانر فيه يعني تلويث `utm_content`، والأهم أن تخزينه محكوم بموافقة الكوكيز (`api/track.js` سطر 259-261) فكل زائر يرفض التتبّع لا تُحسب نقراته = أرقام أقل من الحقيقة بشكل منهجي في الميزة التي جوهرها القياس | **قرار معماري مزدوج يفصل ما يحتاج DDL عمّا لا يحتاجه، فتعمل الميزة فوراً:** **(أ) تعريفات البانرات في `site_settings.banners` (jsonb) — صفر DDL.** الجدول مقروء أصلاً في كل تحميل صفحة (`loadSiteSettings()`)، والكتابة تمر بـ`save-settings` الموجود مسبقاً الذي يحوّل أي صورة base64 تلقائياً لرابط مستضاف (`offloadJsonImages`) فلا تتضخم الصفّ. النتيجة: كل CRUD البانرات والجدولة والعدد يعمل فور النشر بلا انتظار أي شيء من المستخدم. **(ب) أحداث النقر/الظهور في جدول `banner_events` جديد** (الملف الوحيد الذي يحتاج تشغيلاً يدوياً) — **مجهول تماماً بقرار خصوصية متعمَّد: بلا `dukkanci_uid` ولا IP ولا user-agent، خلافاً لـ`tracking_events`**، فهو بيانات مجمّعة غير شخصية بطبيعتها، **ونتيجته العملية أن العدّ غير محكوم بموافقة الكوكيز فأرقام الكفاءة صحيحة لكل الزوار لا لعيّنة منهم**. التجميع عبر دالة `banner_stats(days)` داخل Postgres (وليس جلب كل الصفوف وعدّها في JS) مع `security invoker` فتخضع لـRLS (anon يُرجع صفراً، service_role يُرجع الكل) + `revoke execute` كحزام إضافي. **`migrations/20260719_banners_and_banner_events.sql` يتضمن أيضاً استدراك عمود `category_settings`** لإصلاح عطل الأمس في نفس الجولة. **المحرك (`app.js`):** `BANNER_PLACEMENTS` (5 أماكن: home_top/home_mid/offers_top/category_top/app_home) + `activeBanners(placement, opts)` كمصدر حقيقة واحد يرشّح بـ: مفعّل، داخل نافذة التاريخ، له صورة أو عنوان، **وهدف الرابط لا يزال صالحاً فعلاً** (متجر موجود ومعتمد، منتج موجود ومتاح ومتجره معتمد، قسم موجود) — نفس قاعدة «التحقق وقت العرض» المتّبعة في `dailyDeal` و`offersPageStorePromoBanners`، فبانر هدفه حُذف يختفي بدل أن يصير نقرة ميتة. `bannerStripHTML()` يرسم بانراً واحداً أو شريطاً أفقياً **بالسحب اليدوي فقط بلا أي حركة تلقائية** (قرار المنتج الثابت). 🔴 **علة تصميم حقيقية في التاريخ اكتُشفت وأُصلحت:** `<input type="date">` يعطي تاريخاً بلا وقت، و`Date.parse("2026-08-01")` = منتصف ليل **بداية** ذلك اليوم — أي أن بانراً «ينتهي 1 أغسطس» كان سيختفي في **بداية** 1 أغسطس لا نهايته؛ عولج بـ`bannerTime(value, endOfDay)` التي تمدّ تاريخ النهاية لآخر اليوم (وطُبِّق نفس الإصلاح حرفياً في Dart). **الإدارة:** تبويب جديد «البانرات» (`adminBanners`) بلوحة أداء (ظهور/نقرات/CTR لكل بانر + مدى 7/30/90 يوم) وبطاقة لكل مكان عرض فيها حقل «أقصى عدد معروض» (**التحكم بعدد البانرات**) وتحذير صريح حين يتجاوز عدد البانرات الحد فلا يُعرض الزائد، ونموذج كامل (صورة، عنوان، وصف، نص زر، نوع الرابط وقيمته، تاريخا بداية/نهاية، تفعيل، ترتيب) مع **7 قواعد تحقّق تمنع الحفظ** بدل ترك البانر يختفي صامتاً (متجر/منتج غير موجود، slug قسم خاطئ، رابط لا يبدأ بـhttps/`/`— يرفض `javascript:`، قسم مطلوب لمكان category_top، بداية بعد نهاية، بلا صورة ولا عنوان). 🔴 **علتان حقيقيتان أدخلتهما بنفسي واكتُشفتا بالاختبار الحي قبل الاعتماد:** (1) وضعت عدّاد النقر داخل موزّع `[data-action]` — لكن بانرات القسم والرابط الخارجي تُرسم كـ`<a href>` **بلا `data-action` إطلاقاً**، فكان الموزّع يخرج مبكراً (`if (!target) return`) ولا تُحسب نقراتها أبداً؛ أُصلح بمستمع مستقل على `[data-banner-id]` في طور الالتقاط يغطي الأنواع الثلاثة (تحقّق: الثلاثة تُحسب الآن بعد أن كان واحد فقط). (2) وضعت `banner-stats` في قسم **POST** من `api/notify-order.js` ثم ناديتها بـGET — والملف فيه **قسمان منفصلان**: كتلة GET تنتهي بـ`return res.status(403).end()` عارية بلا جسم، ثم كتلة POST؛ فكان النداء يسقط في ذلك الـ403 الصامت ويقفل لوحة الإدارة كاملة (`lockAdmin`). **درس عام: أي action يُضاف لقسم POST في هذا الملف يجب أن يُنادى بـPOST وإلا يفشل بـ403 بلا رسالة.** 🔴 **اكتشاف بيئي جديد يستحق التوثيق:** في متصفح الجلسة الآلي **`IntersectionObserver` و`requestAnimationFrame` وأحداث `scroll` جميعها لا تُطلَق إطلاقاً** (تحقّق قاطع: مراقب IO **ضابط** على عنصر ظاهر بالكامل أعاد صفر استدعاءات، و`scrollIntoView` لم يحرّك التخطيط أصلاً) — لذا استُبدل قياس الظهور من IntersectionObserver إلى **فحص هندسي صريح بخانق زمني (setTimeout) وليس rAF**، يفصل مستمعيه تلقائياً بمجرد عدّ كل بانرات الصفحة فتكون كلفته المستقرة صفراً. القرار مقصود: عدّاد ظهور يصفّر بصمت كان سيُفسد كل أرقام CTR في اللوحة، فالصحة أولى من الأناقة. **التحقق (كله حي، لا افتراضات):** الترشيح — بانر نشط بلا تواريخ يظهر، وينتهي-اليوم يظهر (إصلاح نهاية اليوم)، والمجدول والمنتهي والمتوقف والمكسور الرابط كلها تُسقَط، وبانرات الأقسام تظهر في قسمها فقط (`restaurants`/`sweets` صح، `butcher` صفر). الظهور — لا يُحسب تحت الطيّة، يُحسب عند وصول ≥50% للشاشة، **لا يُحسب عند 30% ويُحسب عند 71%**، ولا يتكرر عبر إعادة الرسم (`sessionStorage`). النقر — الأنواع الثلاثة تُحسب مع حمولة صحيحة. الحفظ الحقيقي — أُنشئ بانر **متوقف** عبر النموذج الحقيقي فوصل فعلاً لصف `site_settings` في الإنتاج (تحقّق مباشر بـREST) **وتحقّق أنه لا يُعرض لأي زائر**، ثم حُذف عبر مسار الحذف الحقيقي وتأكَّد أن الصفّ عاد `{"items":[],"limits":{}}` — أي **صفر بيانات اختبار متبقية في الإنتاج**. النقطة العامة `banner-event` — تردّ 200 دائماً (فشل مفتوح، لا تعطّل الزبون أبداً) وتسجّل `recorded:false` حين يكون الجدول غير موجود، **وترفض معرّف بانر مخترعاً** (قائمة سماح مخزَّنة 60 ثانية من `site_settings`) والنوع الخاطئ والمعرّف الفارغ والمفرط الطول. الإدارة — التبويب يعرض الأماكن الخمسة و5 حقول حد أقصى، **ورسالة صريحة «تتبّع البانرات غير مُفعّل» مع اسم ملف الترحيل بدل أصفار كاذبة** (تحقّق أن لا `kpi-grid` يُرسم في تلك الحالة). الموبايل 375px — صفر انزياح أفقي، الشريط قابل للسحب، الأنماط مطبَّقة فعلياً (`border-radius` 16px، `overflow:hidden`، نص أبيض). عزل الأماكن — 3 بانرات `app_home` **مفعّلة** في الإنتاج أعطت **صفر** بانر على `home`/`offers`/`category`/`stores` (تأكيد أن بانرات التطبيق لا تتسرب للموقع). **تطبيق Flutter:** طبقة جديدة كاملة (`features/banners/`): نموذج `AppBanner` (سُمّي كذلك عمداً لأن `Banner` ودجة مدمجة في Flutter) يعكس منطق الويب حرفياً بما فيه إصلاح نهاية اليوم، و`BannerRepository` يقرأ **نفس صفّ `site_settings.banners`** (أول مرة يقرأ التطبيق `site_settings` إطلاقاً) ويُسقط البانرات ميتة الهدف ويحلّ متجر المنتج **باستعلام واحد مجمّع** لا استعلام لكل بانر، و`BannerStrip` بالسحب اليدوي فقط بنقاط مؤشّر، وعدّاد أحداث بـ`source:'app'` عبر **Dio منفصل عمداً عن `ApiClient`** كي لا يُرفق توكن العميل بنقطة لا تحتاجه. أُضيف القسم للرئيسية بعد `PromoHero` ولا يرسم شيئاً إطلاقاً حين لا توجد بانرات. `flutter analyze` نظيف (نفس الـ3 ملاحظات المعلوماتية القديمة فقط) | `migrations/20260719_banners_and_banner_events.sql` (جديد)، `app.js`, `styles.css`, `api/notify-order.js`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `dukkanci_customer_app/lib/features/banners/**` (جديد: domain/data/presentation)، `dukkanci_customer_app/lib/app/providers.dart`, `dukkanci_customer_app/lib/features/home/presentation/home_screen.dart`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v331→336، styles.css v178→179، CACHE v249→254). **⚠ مطلوب من المستخدم خطوة واحدة يدوية: تشغيل `migrations/20260719_banners_and_banner_events.sql` في محرر SQL بـSupabase لتفعيل لوحة النقرات (البانرات نفسها تعمل بدونه). الملف يُصلح أيضاً عطل `category_settings` القائم من الأمس.** |
| 2026-07-19 | المستخدم طلب التأكد أن **كل الطلبات تظهر في لوحة الإدارة** | **تدقيق كامل للسلسلة من قاعدة البيانات حتى الجدول المعروض، بنتيجتين متمايزتين:** (1) 🔴 **قنبلة موقوتة صامتة من نفس فصيلة خطأ `wa_contacts` المُصلَح صباح اليوم:** `api/notify-order.js` كان يقرأ طلبات الإدارة بـ`sbGet("orders?...&limit=1000")` وطلبات التاجر بـ`limit=500` — لكن PostgREST على هذا المشروع مضبوط بـ`db-max-rows=1000` يقصّ **كل** استجابة مهما كان `&limit=`. **تحقّق حي مباشر ضد الإنتاج:** `products?select=id&limit=5000` أعاد **1000 صفاً بالضبط من أصل 11,725** — إثبات قاطع أن السقف حقيقي وفعّال. أي أن الإدارة كانت ستفقد بصمت كل طلب بعد الألف (والتاجر بعد الخمسمئة)، **وكل تقرير/إجمالي مالي في اللوحة مبني على `state.orders` كان سيُبلِّغ رقماً أقل من الحقيقة بلا أي خطأ ظاهر** — لأن `loadOrdersFromSupabase` يستبدل `state.orders` كاملةً بما تُعيده النقطة (`state.orders = json.orders.map(mapDbOrder)`، استبدال مباشر بلا فلترة). ملاحظة مهمة: `api/campaign.js` نال دالتَي `sbCount`/`sbGetAll` في إصلاح صباح اليوم، لكن **`notify-order.js` لم ينلهما إطلاقاً** فبقي على نفس العلة. (2) **الوضع الفعلي اليوم سليم:** عدّ دقيق (`count=exact`) أعطى **13 طلباً فقط** في `orders`، فالسقف **لم يكن قد أضرّ بعد** — لا يوجد أي طلب مخفي حالياً. **لكن التدقيق كشف تبايناً حقيقياً:** `merchant_notifications` = **18 صفاً** مقابل 13 طلباً محفوظاً؛ مطابقة كل إشعار برقم طلبه أظهرت **6 إشعارات يتيمة** (أُشعر التاجر وأُرسل واتساب، لكن الصف لم يُحفظ في `orders` قط) — وهي أثر حادثة «فقدان الطلبات الصامت» الموثَّقة في [[orders-silent-save-failure]]: `DK-634719193` (سامي، 400 ل.ت، متجر 85)، `DK-629672744` (خالد الرفاعي، 350 ل.ت، متجر 85)، `DK-542608162` (Ahmed Alaa، 1,500 ل.ت، متجر 85) = **ثلاثة طلبات زبائن حقيقية ضائعة نهائياً**، بالإضافة إلى `DK-545213624`/`DK-456999459` (كلاهما «Test User»، طلبا اختبار) و`DK-4172774138` (متجر الاختبار 999، حُذف صفّه عمداً في جلسة محاكي Flutter بتاريخ 2026-07-16 فبقي إشعاره يتيماً) وإشعارين لمتجر وهمي `999999` بلا رقم طلب. 🔴 **كل الستة قبل 2026-07-10 حصراً — صفر فقدان بعد ذلك التاريخ، أي أن علة الحفظ الصامت مُصلَحة فعلاً ومؤكَّدة بالبيانات لا بالافتراض** | **نُقلت `sbCount` و`sbGetAll` حرفياً من `api/campaign.js` (كود مجرَّب) إلى `api/notify-order.js`** (لم تكونا موجودتين فيه إطلاقاً)، واستُبدل بهما القراءتان: `action=orders` و`action=store-orders` صارتا تُصفِّحان بترويسات `Range` حتى النفاد بدل `limit` واحد مقصوص. أُضيف `id.desc` كفاصل تعادل بجانب `created_at.desc` كي لا يتكرر/يُسقَط صفّ عبر حدود الصفحات عند تساوي الطابع الزمني (تفصيل صحّة حقيقي في أي ترقيم صفحات). وتُعيد كلتا النقطتين الآن `total` **معدوداً باستقلال عن الصفوف** + علم `truncated` صريح، فلا يمكن أن يعود القصّ صامتاً أبداً (نفس مبدأ «اشتقاق العدّاد من مصدر الحقيقة» في إصلاح الحملات صباح اليوم). **التحقّق (كله ضد قاعدة الإنتاج الحقيقية، وبلا أي كتابة):** (أ) اختُبرت الدالتان **باستخراج نصّهما الفعلي من الملف المشحون** (لا نسخة أُعيدت كتابتها) وتشغيلهما على `products`: أعادتا **11,725 من 11,725 صفاً فريداً بلا تكرار** (كانت 1000) — إثبات مباشر أن التصفيح يتجاوز السقف فعلاً. (ب) النقطة الحيّة عبر `server.js` محلياً بمنفذ مستقل (4899، تفادياً لخادم جلسة متزامنة أخرى) بمصادقة إدارية حقيقية: HTTP 200، **13 طلباً، `total`=13، `truncated`=false**، صفر معرّف مكرَّر، والترتيب الأحدث-أولاً سليم. (ج) **حارس المصادقة لا يزال يردّ 403 بلا توكن** (تحقّق انحدار صريح). (د) `sbCount` على استعلام فاشل تُعيد `null` لا `0` (تمييز «غير معروف» عن «صفر حقيقي»). (هـ) طبقة العرض دُقِّقت بالقراءة: `adminOrders()` تُمرِّر القائمة المُصفّاة كاملةً لـ`renderOrdersTable` **بلا أي `slice`**، والفلاتر الافتراضية «الكل»، و`mapDbOrder` لا تُسقط صفّاً أبداً (تُرجع كائناً دائماً) — أي أن اللوحة تعرض فعلاً كل ما تُعيده النقطة. (الـ`slice(0,10)`/`(0,4)`/`(0,5)` الموجودة في الكود كلها ودجات «أحدث الطلبات» في لوحة التاجر/النظرة العامة، مقصودة وليست جدول الإدارة). **الإشعارات اليتيمة لم تُحذف عمداً** — هي السجل الوحيد الباقي لتلك الطلبات الضائعة، وحذفها يمحو الدليل بلا فائدة | `api/notify-order.js`, `CLAUDE.md` (لا تعديل على قاعدة البيانات إطلاقاً — التدقيق كله قراءة فقط) | لا ينطبق (خادم فقط — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`؛ نفس حالة سطرَي `notify-order.js`/`campaign.js` السابقين). `npm run check:versions` ناجح (كل الأرقام متطابقة أصلاً). **لم يُنفَّذ `git commit`/push — بانتظار تعليمات المستخدم؛ ⚠ الملف يحمل أيضاً ~328 سطر تعديلات غير مُلتزَمة من جلسة متزامنة أخرى، فاعزل التغييرات بـ`git add` انتقائي/`git apply --cached` عند الالتزام** |
| 2026-07-19 | بعد تدقيق التكرار (السطر التالي أدناه) رُصد تباين في عدّادات الحملات، فطلب المستخدم تتبّعه وإصلاحه: اللوحة تعرض أرقاماً **أقل من الواقع** (مثال: «عروض باشا 4» تعرض `65/73` بينما 305 من مستلميها استلموا الرسالة فعلاً، و«الخوالي - ساكنات برستيج» تعرض `0/191` بينما 144 استلموا من أصل 335) | **ثلاث علل متمايزة في `api/campaign.js`، جميعها من فصيلة «عدّاد محفوظ يدوياً بدل اشتقاقه من مصدر الحقيقة»:** (1) 🔴 **سباق «قراءة-تعديل-كتابة» (lost update)** — `sendBatch` كان يكتب `sent_count = campaign.sent_count + sent` حيث `campaign` مقروء في **أول** الدالة أي قبل إرسال 8 رسائل (~2.4 ثانية)؛ فأي دفعة تنتهي في تلك الأثناء تُطمَس زيادتها. حجز الصفوف ذرّي فعلاً (فلا أحد يُراسَل مرتين — تأكَّد ذلك في تدقيق التكرار) لكن **العدّاد نفسه** يتسرَّب. **دليل قاطع على أن هذا هو السبب:** فروق الانحراف في البيانات الحقيقية كانت **مضاعفات 8 بالضبط** (43−35=8، 335−319=16، 684−668=16، 305−289=16، 347−338=8) — و`BATCH_SIZE=8`، أي أن كل فجوة = دفعة كاملة أو دفعتان ضاعت زيادتهما. هذه بقايا حادثة تداخل الدفعات الموثَّقة أصلاً في تعليق حلقة الإرسال بـ`app.js` («corrupted sent_count via lost updates») — عولجت الحلقة في العميل وحُصِّن الحجز في الخادم، لكن **حساب العدّاد لم يُصلَح قط** فبقي عرضةً لأي إعادة محاولة أو تبويب ثانٍ أو نداء cron، وبقيت البيانات التالفة كما هي. (2) 🔴 **`total_recipients` يُحسب من `pending` وحده** عند «جاهزة» (`update-params`) مع **تصفير `sent_count`** في نفس اللحظة، بينما الصفوف المُرسَلة فعلاً تبقى `sent` — فحملة أُعيد تسليحها تُبلِّغ «191 مستلماً / 0 مُرسَل» بينما قائمتها 335 و144 منهم استلموا. (3) `build` يحذف كل صفوف المستلمين ويعيد إدراجها `pending` لكنه **لا يُصفّر** `sent_count`/`failed_count`، فتظهر حملة معاد بناؤها بعدّاد إرسال قديم أمام صفر صفوف مُرسَلة | **اشتقاق العدّادات من جدول `wa_campaign_recipients` بدل حفظها يدوياً** (لا حساب تراكمي إطلاقاً): `sendBatch` صار يُعيد عدّ `sent`/`failed`/الإجمالي/المتبقّي عبر `sbCount` بعد كل دفعة — عملية **مُتعادِلة (idempotent) ومحصَّنة من السباق وتُصلح البيانات التالفة ذاتياً**؛ وشرط الإنهاء صار `remaining === 0` (صفر مؤكَّد فقط — `null` الناتج عن فشل عدّ لا يُقرأ أبداً كـ«انتهى») بدل حساب `sent+failed >= total` الذي كان ينهي الحملة مبكراً بإجمالي مغلوط. `update-params` صار يُعيد عدّ كل الدلاء بعد إعادة `failed`/`sending` إلى `pending` (والصفوف `sent` تُترك عمداً فلا يُراسَل أحد مرتين)، فالعدّاد صار يجيب «كم شخصاً من قائمتي استلم» لا «كم بقي في هذه الجولة». `build` صار يُصفّر العدّادين مع إعادة البناء. أُبقي احتياط صريح: إن فشل نداء العدّ (`null`) تُستخدم القيمة التراكمية القديمة بدل كتابة صفر فوق قيمة سليمة. **تحقّق:** تشغيل جاف (بلا أي كتابة) بالمنطق الجديد على قاعدة الإنتاج كشف انحرافاً في **12 من 28 حملة**، ثم صُحِّحت الـ12 عبر سكربت يكتب `sent_count`/`failed_count`/`total_recipients` فقط (لا يمسّ `status` ولا صفوف المستلمين ولا أي شيء قد يُطلق إرسالاً) بعد حفظ نسخة احتياطية للقيم السابقة **خارج المستودع** في scratchpad؛ إعادة التشغيل الجاف بعدها أعادت «صفر انحراف — كل حملة عدّاداتها مطابقة لصفوفها»، وتوزيع الحالات بقي `done:22, ready:2, canceled:4` بلا أي حملة في `sending` (تأكيد أن الإصلاح لم يُطلق أي إرسال). **مهم:** الرسائل كانت تصل فعلاً طوال الوقت — الصفوف `sent` صحيحة؛ الخلل كان في العرض فقط، فلا رسائل ضائعة ولا مكرَّرة | `api/campaign.js`, `CLAUDE.md` + تصحيح `sent_count`/`failed_count`/`total_recipients` لـ12 صفاً في `wa_campaigns` بـSupabase | لا ينطبق (خادم فقط — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`، فلا حاجة لرقم إصدار؛ نفس حالة سطر 2026-07-12 الخاص بـ`notify-order.js`) |
| 2026-07-19 | المستخدم أبلغ (بلقطة شاشة من تبويب «الحملات» في لوحة الإدارة تُظهر شارة برتقالية بالقيمة `0` بجانب زر «إدارة الأرقام المرفوعة») أن **الرقم الحقيقي للأرقام المحمَّلة لا يظهر** | 🔴 **`wa_contacts` لا يملك عمود `id` إطلاقاً** — مفتاحه الأساسي هو `phone` نصاً (`scripts/campaign-schema.sql` سطر 43-48)، لكن `api/campaign.js` كان يَعُدّ الأرقام بـ`select=id` في ثلاثة مواضع؛ PostgREST يرد **HTTP 400** (`column wa_contacts.id does not exist`)، و`sbGet` يبتلع الخطأ ويُعيد `null`، فيصبح `(null || []).length` = **صفر دائماً** مهما بلغ عدد الأرقام. تحقّق حي مباشر ضد قاعدة الإنتاج: **6,334 رقماً حقيقياً موزّعة على 26 مجموعة** بينما كل الواجهة تعرض صفراً (الشارة + بطاقة KPI «رقم مرفوع» + `count-chip` + خيار «الأرقام المرفوعة» في قائمة الجمهور + رسالة ما بعد الرفع). **وعلة ثانية مستقلة في نفس الميزة:** الأرقام نفسها لم تكن تُعرض في أي مكان — `contacts-list` يُرجع فعلاً قائمة صفوف حقيقية يخزّنها `loadContacts()` في `state.adminContacts.preview`، لكن `app.js` **لا يُصيّرها إطلاقاً** (بحث `grep` عن `.preview` = صفر تطابق)؛ اللوحة كانت تعرض بطاقات المجموعات وعدّاداتها فقط، بينما لوحة «الأرقام المستبعدة» أسفلها مباشرة تعرض جدول أرقام حقيقياً — تباين يؤكد أن العرض سقط سهواً. **ثلاث علل إضافية من نفس الفصيلة اكتُشفت أثناء الفحص وأُصلحت:** (1) 🔴 `db-max-rows=1000` في PostgREST يقصّ **كل** استجابة مهما كان `&limit=` (تحقّق حي: `wa_contacts?select=phone&limit=10000` أعاد 1000 صفاً بالضبط) — فـ`buildRecipients` كان يبني قوائم المستلمين من **أول 1000 رقم فقط من أصل 6,334، أي أن 5,334 رقماً لم تكن لتصلها أي حملة إطلاقاً بصمت**؛ نفس القصّ أصاب `all_customers`/`no_order_30d` (limit=5000) و`optout-list` (2000) و`total_recipients` (10000). (2) 🔴 عدّاد الحد اليومي (`sentToday`) كان يَعُدّ بـ`.length` على `limit=2001` فيتوقف عند 1000 — أي أن سقف Meta (2000/يوم) **لم يكن ليُفعَّل أبداً**، وهو خطر وشيك مع قائمة بهذا الحجم. (3) 🔴 زر «حذف الكل» كان يُرسل `DELETE wa_contacts?id=not.is.null` → 400 → **لا يحذف شيئاً** بينما تعرض الواجهة رسالة نجاح | **(أ) `api/campaign.js` — دالتان جديدتان تعالجان جذر الفصيلة كلها:** `sbCount(path)` تَعُدّ عبر ترويسة `Prefer: count=exact` وتقرأ الرقم من `Content-Range: 0-0/<total>` (الطريقة الوحيدة الصحيحة؛ تُعيد `null` لا `0` عند الفشل كي يميّز المستدعي «غير معروف» عن «صفر حقيقي»)، و`sbGetAll(path, max)` تُصفِّح بترويسات `Range` حتى النفاد (نفس نمط `getContactGroups` الموجود مسبقاً والذي كان صحيحاً بالفعل). استُبدلت بها كل مواضع العدّ الخاطئ: `contacts-count`، `contacts-list`، `upload-contacts`، `optout-add`، `total_recipients`، وعدّاد الحد اليومي؛ وكل استعلامات بناء المستلمين صارت تُصفِّح. `contacts-clear` صار يرشّح بـ`phone=not.is.null` (المفتاح الأساسي NOT NULL) **ويُرجع 500 عند الفشل بدل ادّعاء النجاح**. أُضيف أيضاً بارامتر `group` لـ`contacts-list` مع `scopeTotal`. **(ب) `app.js`:** صُيِّرت أخيراً قائمة الأرقام الحقيقية (جدول رقم/مجموعة/تاريخ يعيد استخدام `campaigns-table-wrap`+`admin-table` نفسها التي تستخدمها لوحة المستبعدين، بلا CSS جديد للجدول)، مع سطر شفافية «عرض أحدث ٢٠٠ رقم من أصل ٦٬٣٣٤»؛ وبطاقات المجموعات صارت قابلة للنقر لتصفية القائمة على مجموعة واحدة (+زر «عرض كل المجموعات» يظهر فقط عند التصفية). 🔴 **علة أدخلتُها بنفسي واكتُشفت بالاختبار قبل الاعتماد:** أول نسخة مرّرت المجموعة بإلحاقها نصياً على اسم الـaction (`contacts-list&group=...`)، لكن `campaignApi` يبني الرابط بـ`new URLSearchParams({action})` الذي **يُرمّز القيمة بالكامل** فكان الـaction يصل حرفياً `contacts-list%26group%3D...` ولا يطابق أي فرع؛ أُصلح بإضافة خيار `params` صريح للدالة. **(ج) `styles.css`:** أنماط القائمة الجديدة وحالة البطاقة النشطة. 🔴 **اكتشاف عابر يستحق التوثيق:** المتغيّرات `--accent`/`--bg`/`--text`/`--radius` **غير معرَّفة في المشروع كله** (تحقّق: صفر ملف يعرّفها، و`getComputedStyle` يُعيد فارغاً) رغم أن كتلة أنماط الحملات كلها تستند إليها — فكل تلك التصريحات ميتة صامتة منذ كتابتها (يشمل `.group-card:hover`)؛ لم أُصلح ذلك (يُعيد تلوين لوحة الإدارة كلها، خارج النطاق) لكن أنماطي الجديدة تستخدم رموزاً موجودة فعلاً (`--green-700`/`--green-50`/`--cream-dark`/`--ink`). **التحقّق (كله ضد قاعدة الإنتاج الحقيقية، لا افتراضات):** استدعاء المعالج الحقيقي بتوكن إداري موقَّع أعاد `contacts-count`=**6,334** و`contacts-list.total`=**6,334** (كانا صفراً)، ومجموع عدّادات الـ26 مجموعة = 6,334 بالضبط (تقاطع مستقل يؤكد اتساق العدّادين)، والتصفية بمجموعة أعادت `scopeTotal`=658 مطابقاً لعدّاد تلك المجموعة وكل صفوفها تخصّها فعلاً، و`sbGetAll` أعادت **6,334 صفاً فريداً بلا تكرار** (كانت 1000)، وحارس المصادقة لا يزال يردّ 403 بلا توكن. وفي المتصفح: الشارة وبطاقة الـKPI تعرضان 6,334، جدول الأرقام يُصيَّر فعلاً بأرقام حقيقية LTR، نقرة حقيقية على بطاقة مجموعة تُطلق طلب التصفية الصحيح (`action=contacts-list&group=<اسم عربي سليم>`)، وزر حذف المجموعة المتداخل لا يزال يُحلّ إلى `contacts-delete-group` لا إلى التصفية (`closest('[data-action]')`)، والجدول لا يتجاوز حاويته (`overflow-x:auto`)، وصفر أخطاء كونسول. ⚠️ **لم تُختبر مسارات الكتابة حيّاً** (`contacts-clear`، `upload-contacts`، بناء حملة فعلي) تفادياً للمساس ببيانات إنتاج حقيقية أو إنشاء حملة/إرسال واتساب حقيقي — الثقة فيها مبنية على أن إصلاحها هو نفس `sbCount`/`sbGetAll` المُتحقَّق منهما حيّاً + تصحيح مرشّح `phone` المؤكَّد أن `id=not.is.null` يردّ 400 فعلاً | `api/campaign.js`, `app.js`, `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` (لا تعديل على قاعدة البيانات إطلاقاً — العلة كانت في الاستعلامات لا في البيانات) | نعم — `npm run check:versions` ناجح (app.js v328→331، styles.css v177→178، CACHE v248→249). **لم يُنفَّذ `git commit`/push — بانتظار تعليمات المستخدم.** ⚠️ **ملاحظة تزامن:** بدأت هذه المهمة بينما كانت جلسة أخرى تحمل تعديلات غير مُلتزَمة على نفس `app.js`/`index.html`/`sw.js` (حذف «يمن شيف» + بوابة المتجر المرفوض)؛ التزمَت تلك الجلسة عملها أثناء عملي (`27f022c9`) فذاب التشابك تلقائياً وصار التزامي فوقها مباشرة بلا أي تداخل. **درس:** أعِد فحص `git diff` مباشرة قبل الالتزام بدل الاعتماد على قراءة أول الجلسة — كما أن نسختي من صفّهم في `CLAUDE.md` كانت قد تجمّدت على نصّهم *قبل* مراجعتهم الأخيرة له، فلو التزمتُ ملفي كما هو لأعدتُ صفّهم إلى نسخة قديمة؛ أُعيد بناء `CLAUDE.md` من `HEAD` + صفّي وحده تفادياً لذلك.
| 2026-07-19 | المستخدم طلب إزالة مطعم «يمن شيف» (id 20، `/store/yemen-chef-restaurant`) بشكل كامل | نفس فئة الخطأ الموثَّقة في قسم 6 («حذف متجر من Supabase لا يكفي وحده»)، لكن هذه المرة **بأثر تشغيلي حقيقي وليس تجميلياً**: صفّ المتجر كان أصلاً `approval_status='rejected'` و`open=false` (مرفوض منذ تدقيق سابق)، لكن ملف `yemenchef-data.js` المُجمَّع بقي محمَّلاً عبر `<script>` في `index.html` ومُدرَجاً في `sw.js` ومدموجاً في `app.js` بالأسطر الثلاثة (`stores.push`/`products.push`/`initialDeliverySettings`) — فبقيت صفحة المتجر حيّة وقابلة للطلب فعلياً. **الدليل القاطع:** وصل **طلب حقيقي غير منفَّذ يوم 2026-07-19 الساعة 13:31** (`DK-467890575`، Jawad Karajeh، 980 ل.ت، بطاقة عند التسليم، `is_test_order=false`، `closedWhenOrdered=true`) لمتجر «مرفوض ومغلق» — أي أن الزبون طلب فعلاً من متجر يُفترض أنه غير موجود. **اكتشافان إضافيان أثناء التنفيذ:** (1) 🔴 `orders_store_id_fkey` هو **RESTRICT وليس CASCADE** — فحذف صفّ المتجر **مستحيل تقنياً** ما دامت له طلبات تاريخية (`23503: Key (id)=(20) is still referenced from table "orders"`) — أي أن أي متجر باع ولو طلباً واحداً لا يمكن حذف صفّه أبداً، وسيعود دائماً إلى مصفوفة `stores` من Supabase مهما نُظِّفت الحزمة الأمامية. (2) نتيجةً لذلك، `renderStorePage()` و`updateHead()` كانتا تعرضان أي متجر يجده `getStore(id)` **بلا أي بوابة اعتماد إطلاقاً** — فظلّ `/store/20` يعرض صفحة متجر كاملة (العنوان «دكانجي - مطعم يمن شيف»، الوصف، شارة «متجر رسمي موثق») حتى بعد حذف كل منتجاته وسلاگه | **(أ) قاعدة البيانات** (عبر سكربت Node بـ`SUPABASE_SERVICE_ROLE_KEY` + PostgREST مباشرة — موصل Supabase MCP في هذه الجلسة كان مربوطاً بمشروع `iseldofsfhwvpfzltqet` غير ذي صلة، نفس العطل الموثَّق مراراً): حُذف 125 منتجاً + `store_credentials` (فلا يستطيع التاجر الدخول) + `merchant_notifications`. صفّ `stores` **بقي قسراً** بسبب قيد المفتاح الأجنبي أعلاه، لكنه صار خاملاً تماماً (مرفوض + مغلق + صفر منتجات + بلا بيانات دخول). **الطلبان أُبقيا سليمين بقرار صريح من المستخدم** (`AskUserQuestion`: «احتفظ بسجلّي الطلبات») — تحقّق برمجي بعد الحذف أن كليهما لا يزالان موجودين، مع نسخة احتياطية JSON كاملة (94KB: المتجر + 125 منتجاً + الطلبان + بيانات الدخول + الإشعار) حُفظت **خارج المستودع** في مجلد scratchpad عمداً لأنها تحوي بيانات زبائن شخصية (أسماء/هواتف/عناوين) لا يجوز أن تدخل git. **(ب) الحزمة الأمامية:** حُذف `yemenchef-data.js` + `assets/photos/yemenchef/` (132 ملفاً، **112 ميغابايت**) عبر `git rm`، وأُزيلت الأسطر الثلاثة من `app.js` ووسم `<script>` من `index.html` وسطرا `APP_SHELL` + صورتا الغلاف/الشعار من `sw.js` والإدخال `20:` من `store-slugs.js`. **(ج) إصلاح جذري للتسريب (يمنع تكرار الحادثة لأي متجر مرفوض مستقبلاً):** أُضيفت بوابة `if (store.approvalStatus === "rejected") return renderNotFound();` في أول `renderStorePage()`، وبوابة مقابلة في `updateHead()` (`s && s.approvalStatus !== "rejected"`) كي لا يتسرّب اسم المتجر/صورته إلى عنوان التبويب ووسوم `og`/`twitter` رغم أن الجسم يعرض «غير موجودة». **`pending` غير مشمول عمداً** — الرابط المباشر لمتجر قيد الانتظار هو وسيلة مراجعته قبل الاعتماد، وهو سلوك مقصود وموثَّق في عشرات أسطر هذا السجل. `api/store.js` (SSR) كان محصَّناً أصلاً (سطر 54 يُسقط أي متجر غير معتمد) — تأكيد حي: `curl` للرابط على الموقع الحي كان يُعيد العنوان العام قبل أي تعديل مني. تحقّق شامل عبر preview محلي: `/store/20` يعرض «الصفحة غير موجودة» بعنوان تبويب عام وog/twitter عامّين وصفر ذكر لاسم المتجر، `/store/yemen-chef-restaurant` لم يعد يُحَل إطلاقاً (`STORE_SLUG_TO_ID` = null)، `/category/restaurants` (90 بطاقة) و`/dalil` والرئيسية بلا أي أثر، `products.filter(storeId===20).length === 0`، خريطة السلاگ بلا 20، متجر ضابط معتمد («باشا بيتزريا») لا يزال يُعرض بشكل صحيح (حارس انحدار)، صفر أخطاء كونسول، والـsitemap خالٍ. 🔴 **درس بيئي متكرر تأكَّد مجدداً:** تعديل `app.js` دون رفع `?v=` لا يظهر محلياً إطلاقاً — الـService Worker وذاكرة HTTP (`max-age=300`) يخدمان النسخة القديمة بنفس الرابط؛ حتى إلغاء تسجيل الـSW ومسح `caches` لم يكفِ، ولم يظهر الإصلاح إلا برفع رقم الإصدار فعلياً (تحقّق مباشر: `renderStorePage.toString().includes('rejected')` كان `false` بينما الملف على القرص يحويه) | `app.js`, `index.html`, `sw.js`, `store-slugs.js`, حذف `yemenchef-data.js` + `assets/photos/yemenchef/**` (132 ملفاً/112MB), `CLAUDE.md` + حذف 125 صف `products` و`store_credentials` و`merchant_notifications` للمتجر 20 في Supabase (ثم — بطلب صريح لاحق من المستخدم («احذف كل البيانات») — حُذف الطلبان وصفّ `stores` نفسه نهائياً، فلم يبقَ للمتجر 20 أي أثر في القاعدة إطلاقاً (المجموع صار 96 متجراً)) | نعم — `npm run check:versions` ناجح (app.js v327→328، store-slugs.js v260→261، CACHE v247→248). **مدفوع لـ`main`** (commit `27f022c9`، Vercel نشر بنجاح، تحقّق حي: `/store/yemen-chef-restaurant` و`/store/20` يعيدان العنوان العام، وملف البيانات والصور تردّ HTML [ارتداد SPA] أي محذوفة فعلاً، وsitemap خالٍ). ⚠️ تعديلاتي تتعايش في نفس شجرة العمل مع جلسة متزامنة أخرى تُضيف متجر «تشيكي ويكي» (id 109) — تحقّقت أن كل أسلاكها سليمة ولم تُلمس، فاعزل الملفات بـ`git add` انتقائي عند الالتزام |
| 2026-07-19 | المستخدم طلب إضافة مطعم «شيكي ويكي» (Chiki Wiki) من كتالوج واتساب بزنس (`wa.me/c/905527866000`) | ميزة إضافة جديدة (لا خطأ سابق) — لكن اكتُشف **نمط مصدر جديد غير موثَّق من قبل: التاجر نفسه رافعٌ صوراً سوداء بالكامل (ضجيج خالص 1200×1600، ~120KB) لخمسة أصناف في كتالوجه** — تحقّق قاطع بثلاث طرق مستقلة: (1) فك ترميز البلوب داخل الصفحة عبر canvas (متوسط سطوع 7-8/255، قنوات لا تتجاوز 39)، (2) إعادة الالتقاط من محرك متصفح نظيف بعد إعادة تحميل كاملة أعادت **نفس البايتات حرفياً**، (3) لقطة شاشة لواجهة واتساب نفسها تعرض المصغّرات سوداء تماماً في صفحة «See all» لفئة PİLAV MENÜ. الأصناف الخمسة (كامل فئة PİLAV MENÜ الثلاثة + قطعة الدجاج المقرمش المفردة + برغر الدجاج المفرد من Yan Ürünleri) **استُبعدت** طبقاً لسياسة «لا صورة حقيقية = لا نشر». **درس تشغيلي جديد لواتساب ويب:** بعد أتمتة مكثفة يتدهور درج الكتالوج نهائياً (النقر على أيقونة المتجر/الرجوع يغلق اللوحة بدل التنقل، ولقطات الشاشة تنتهي بمهل CDP متكررة) — الحل الوحيد الفعّال: إعادة تحميل كاملة + إعادة فتح المحادثة عبر «Continue to WhatsApp Web» من صفحة هبوط `wa.me` بنقرة حقيقية (تفتح تبويباً جديداً بمحرك نظيف)، مع **تصدير النصوص المستخرَجة فوراً بعد كل فئة** (بلوكات JSON مطبوعة في نتائج الأداة) كي لا تضيع عند أي إعادة تحميل — أنقذ هذا فعلاً بيانات 6 فئات لمّا انهار التبويب الأول. كذلك: العنصر الرابع الزائد الظاهر في عدّ `[role="listitem"]` بالصفحة الرئيسية للكتالوج هو **صف ترويسة الفئة نفسه** (يحمل نفس الـrole) وليس منتجاً مفقوداً — لُوحظ عدّ 38 بدل 28 وحُسم بمشي TreeWalker مرتَّب أثبت التطابق التام مع نتائج «See all» | الكتالوج الكامل 28 صنفاً عبر 10 فئات (فُتحت «See all» لكل فئة وسُكرلت حتى استقرار العدّ 3 دورات متتالية — درس شام أوغلو مطبَّق حرفياً؛ الفئات هنا صغيرة فعلاً: أغلبها 3 أصناف مؤكَّدة بلقطات شاشة بفراغ أبيض تحتها). نُشر **23 منتجاً عبر 9 فئات** (منيو الأطفال 3، البرغر 3، الراب 3، دجاج بالعظم 1، دجاج بدون عظم 3، منيو العائلة 3، السلطات 3، أطباق جانبية 1، السندويشات 3) بترجمة عربية أمينة للأسماء والأوصاف التركية. تكرار صورة واحد حقيقي من التاجر (منيو كرانشي/منيو ميغا يتشاركان صورة دلو العائلة نفسها — تطابق MD5، أُبقيا كملفين مستقلين طبقاً لسابقة الوكالة/شام أوغلو)، وسلطة تشوبان قُبلت بصورتها الحقيقية الصغيرة (227×170، أقصى دقة متاحة في المصدر — سابقة شعار بيت حلب 100×100). **مطابقة Google Maps بوضوح نادر:** سلسلة متعددة الفروع (3 فروع على غوغل: بيليك دوزو/باشاك شهير/فاتح تطابق قائمة الفروع المطبوعة على منشورات التاجر نفسه) — فرع بيليك دوزو `ChIJb02EqVxftRQRhT-NXHzK_f8` طابق **رقم الهاتف حرفياً** (+90 552 786 60 00 = رقم واتساب المُرسَل بالضبط) والعنوان (Kavaklı, Yeşilyurt Cd. No:20/2) وبمسافة ~100م من إحداثيات geocoding لعنوان واتساب، تقييم 4.0 (207)؛ **تأكيد فيزيائي إضافي:** صورة غلاف المتجر (واجهة المحل الحقيقية من ملف واتساب بزنس) تُظهر نفس الرقم مطبوعاً على اللافتة («ALO PAKET 0552 786 60 00»). ساعات غوغل اعتُمدت (يومياً 12:00 م – 11:00 م، الجمعة من 2:00 م) على ساعات واتساب (13:00-23:59) — سابقة شام أوغلو. الشعار (612×612→400×400) والغلاف (1138×640→1280×542) من ملف تعريف واتساب بزنس (fetch→blob→anchor، بلا طباعة روابط CDN الموقوتة). المتجر رقم **109** (تحقّق `max(id)`=108 ثلاث مرات: أول الجلسة + داخل سكربت الإدراج مباشرة قبل الكتابة مع حارس ينفجر لو تحرك الرقم)، حزمة المنتجات **1910001–1910023**. صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82 mozjpeg، 25 ملفاً = 1.9MB). `approval_status='pending'` عمداً. وُصلت الأسلاك الأربعة: `app.js` (`stores.push`, `products.push`, `chikiwikiDeliverySettings` spread) + `store-slugs.js` (`109: "chiki-wiki-beylikduzu"`) + `<script src="/chikiwiki-data.js?v=1">` في `index.html` قبل `app.js` + `sw.js` (`APP_SHELL`). الدفع عبر `scripts/insert-chikiwiki-supabase.js` (service-role REST، تخطيط أعمدة منسوخ حرفياً من سكربت تركواز المجرَّب) — تحقّق برمجي لاحق حقلاً-بحقل: 23/23 صف مطابق تماماً. تحقّق شامل عبر preview محلي بمنفذ مستقل: `/store/chiki-wiki-beylikduzu` يعرض العنوان الصحيح («دكانجي - شيكي ويكي»)، «23 من 23 منتج» عبر 9 فئات من Supabase، «٢٠ ل.ت/كم»، كل الـ25 رابط صورة HTTP 200 (fetch HEAD جماعي)، `/category/restaurants` لا يعرض المتجر إطلاقاً (49 بطاقة بلا `data-id="109"` — سلوك pending صحيح)، صفر أخطاء كونسول. `npm run check:versions` ناجح | `chikiwiki-data.js` (جديد)، `assets/photos/chikiwiki/**` (جديد، 25 ملف: 23 منتج + شعار + غلاف)، `scripts/process-chikiwiki-images.js` (جديد)، `scripts/insert-chikiwiki-supabase.js` (جديد)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 109 وصفوف `products` 1910001-1910023 في Supabase | نعم — `npm run check:versions` ناجح (app.js v326→327، store-slugs.js v259→260، CACHE v246→247). **مدفوع لـ`main`** بطلب المستخدم الصريح — عُزلت التعديلات عن أعمال الجلسات المتزامنة غير المدفوعة المتشابكة نصياً في نفس الملفات (حذف يمن شيف المُرحَّل في الفهرس + بوابة المتجر المرفوض + واجهة حملات جديدة في `app.js`/`api/campaign.js`/`api/notify-order.js`) عبر **فهرس git مؤقت** بُني محتواه من HEAD + تعديلات شيكي ويكي فقط (`git hash-object` + `update-index --cacheinfo` + `commit-tree`، بلا لمس فهرس الجلسات الأخرى أو ملفات العمل إطلاقاً) — commit `b9630a7c`، Vercel نجح، وتحقّق حي: كل أصول شيكي ويكي HTTP 200 والإصدارات المخدومة متطابقة. **تحديث لاحق (نفس اليوم):** بطلب المستخدم الصريح «اعتمد المتجر»، حُدِّث `approval_status='approved'` مباشرة عبر Supabase REST — تحقّق حي فوري: SSR للرابط يعرض العنوان الصحيح («دكانجي - شيكي ويكي»)، المتجر دخل `sitemap-stores.xml`، و`/category/restaurants` الحي يعرض بطاقته فعلاً (`data-id="109"`، 50 بطاقة). ملاحظة: جلسة متزامنة دفعت التزامين فوق التزامي مباشرة (`27f022c9` إزالة يمن شيف + `1bb7c35e` إصلاح عدّاد الحملات) — تحقّق أن app.js الحي بعدهما لا يزال يحمل أسلاك شيكي ويكي الثلاثة سليمة (الدمج النصي على سطر `stores.push` المشترك نجح تلقائياً)
| 2026-07-19 | المستخدم طلب التأكد أن ترتيب/دمج تصنيفات صفحة المتجر (المرحلتان 1 و2 أعلاه، مبنيتان في `app.js` للموقع فقط) مطبَّق أيضاً في تطبيق دكانجي للجوال (Flutter، `dukkanci_customer_app/`) | ليس خطأ — تطبيق Flutter مشروع Dart منفصل تماماً بنماذج بياناته الخاصة (يقرأ نفس جداول Supabase مباشرة، لا يستدعي `app.js` إطلاقاً)، فميزتا المرحلة 1/2 لم تكونا مطبَّقتين هناك تلقائياً بمجرد بنائهما في الموقع. تحقّق بحثي (وكيل استكشاف) أن صفحة المتجر في Flutter (`store_screen.dart`) كانت تملك فعلاً شريط تصنيفات لاصق قافز يعمل (`_CategoryBarDelegate`/`_syncActiveCategory`/`_scrollToCategory`، ميزة حقيقية من إعادة تصميم سابقة، وليست نصاً تخطيطياً فقط كما قد يُفهم من وصف `README.md`) — لكنه كان يجمّع المنتجات فقط حسب `product.category` الخام **بترتيب أول ظهور في نتيجة استعلام قاعدة البيانات**، بلا أي ترتيب رسمي أو دمج تصنيفات صغيرة أو تجاوزات إدارية/تاجر، وبلا حقل `categorySettings` في نموذج `Store` إطلاقاً | نُقل منطق `buildStoreCategoryPlan` (ومحيطه: قوائم الترتيب الرسمية، جداول المرادفات، قواعد الدمج بالكلمات المفتاحية، `normalizeCategoryKey`، `resolveCategoryOverride`، `filterHiddenCategoryProducts`، `applyManualCategoryOrder`) بالكامل إلى Dart حرفياً بنفس القيم والمنطق تماماً — ملف جديد `dukkanci_customer_app/lib/features/stores/domain/store_category_plan.dart`. أُضيف حقل `categorySettings` (`StoreCategorySettings`/`CategoryOverride`) لنموذج `Store` (`store.dart`) متبعاً نمط تفسير jsonb الموجود مسبقاً لـ`payment_methods` (`is Map` ثم `Map<String,dynamic>.from`). في `store_screen.dart`: استُبدلت عملية التجميع الخام (`Map<String, List<Product>>` ببساطة حسب `p.category`) بنتيجة `buildStoreCategoryPlan` (مبنية كـ`LinkedHashMap` بنفس شكل البيانات القديم للحفاظ على كل آلية الشريط اللاصق/التمرير كما هي بلا أي تعديل عليها)، وأُضيف استدعاء `filterHiddenCategoryProducts` في أول السطر قبل أي اشتقاق آخر (يشمل وضع البحث أيضاً — مطابقةً تماماً لترتيب التنفيذ في `renderStorePage` بـ`app.js`). **لم يُنقَل** الجزء الإداري/التاجر من المرحلة 2 (لا واجهة تحكم داخل تطبيق العميل — ذلك يبقى حصراً في لوحتي الإدارة/التاجر على الموقع)؛ التطبيق يقرأ فقط نتيجة `category_settings` الجاهزة من Supabase ويطبّقها للعرض. تحقّق: `flutter analyze` نظيف (نفس 3 تنبيهات معلوماتية قديمة غير متعلقة فقط). **تحقّق حي كامل على المحاكي** (AVD `dukkanci_test`، `JAVA_HOME=...Android Studio1\jbr`): بناء `flutter run --release` ناجح فعلياً (لم يفشل رغم تحذير KGP القديم المعروف)، فتح متجر حقيقي متعدد التصنيفات («مطعم الخوالي») من قائمة «مطاعم» الحية أظهر شريط تصنيفات صحيحاً: «المقبلات» (تصنيف رسمي مطابق حرفياً لعنصر في `RESTAURANT_CATEGORY_ORDER`) تصدَّر الشريط فعلاً أمام تصنيفات التاجر المخصَّصة («شاميات»، «مشروبات»، «وجبات غربية»، «ساندويتش...») التي بقيت بترتيبها الطبيعي بعده — يطابق تماماً سلوك الموقع لنفس الحالة (تصنيف رسمي واحد وسط تصنيفات مخصَّصة). صفر استثناءات Dart في `logcat` أثناء التصفح. لم يُختبَر مسار التجاوزات اليدوية (Phase 2) حياً على المحاكي لعدم وجود بيانات `category_settings` حقيقية بعد (الـmigration لم يُطبَّق على قاعدة الإنتاج بعد — نفس القيد الموثَّق في السطر السابق) | `dukkanci_customer_app/lib/features/stores/domain/store.dart`, `dukkanci_customer_app/lib/features/stores/domain/store_category_plan.dart` (جديد), `dukkanci_customer_app/lib/features/stores/presentation/store_screen.dart`, `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل، لا علاقة بأرقام إصدار app.js/styles.css). **بانتظار تنفيذ `git commit`**
| 2026-07-19 | المستخدم طلب إظهار المتاجر الأربعة المدفوعة (ماركت صفا الشام 50، مطعم الخوالي 31، باشا بيتزريا 56، ملحمة الدوماني 84) في «الهيدر الأساسي للتطبيق» — العبارة تحتمل أكثر من موضع فطُرح سؤال توضيح (`AskUserQuestion`) واختار المستخدم **المواضع الثلاثة معاً**: تطبيق Flutter + هيرو الصفحة الرئيسية للموقع + شريط التنقل العلوي للموقع | ميزة عرض/إعلان جديدة (لا خطأ سابق) — امتداد لميزة أولوية المتاجر المدفوعة `PAID_PRIORITY_STORE_IDS` (2026-07-18) التي رتّبت القوائم لكنها لم تُظهر المتاجر بالاسم في أي هيدر | (1) **`index.html`:** شريط ثابت جديد `.featured-stores-bar` داخل `<header class="site-header">` مباشرة بعد `trust-bar` — 4 روابط نصية ثابتة `<a href="/store/<slug>" data-action="open-store" data-id>` (النمط المزدوج href+data-action المستخدَم مسبقاً في الكود: SEO/SSR قبل تحميل JS + تنقّل SPA بعده)، يظهر في **كل** صفحات الموقع وعلى كل المقاسات، بتمرير أفقي بلا شريط تمرير مرئي على الموبايل. (2) **`app.js` (`renderHome`):** صف رقائق جديد `.h2-featured` داخل الهيرو (بعد `search-chips`) — رقاقة لكل متجر بشعاره الحقيقي (`storeAvatar`) واسمه الحي من بيانات Supabase، مبنية من **نفس ثابت `PAID_PRIORITY_STORE_IDS`** (مصدر حقيقة واحد للترتيب؛ متجر مفقود/غير معتمد لا يُرسم إطلاقاً)؛ الصف مخفي عمداً ≤640px لأن شريط الهيدر أعلاه يعرض نفس المتاجر (تفادي التكرار المزدوج في مسار الشراء الموبايلي، والهيرو نفسه شبه مخفي هناك أصلاً). (3) **`styles.css`:** كتلة أنماط جديدة في نهاية الملف (تحقّق توازن الأقواس برمجياً قبل وبعد الإلحاق — درس قوس fbads المفقود 2026-07-12). (4) **Flutter (`home_screen.dart`):** ثابت جديد `_paidHeroIds = [31,56,84,50]` في `_HomeBody` — شرائح `PromoHero` (هيرو الشاشة الرئيسية تحت الهيدر الأحمر) تتصدّرها المتاجر الأربعة بترتيبها الثابت ثم يُكمل السلوك السابق (متاجر العروض وإلا الأكثر رواجاً) حتى 6 شرائح مع إزالة التكرار. تحقّق شامل عبر preview محلي بمنفذ مستقل تلقائي (خادم جلسة أخرى كان على 4173): الشريط يعرض الروابط الأربعة الصحيحة والرقائق الأربع بشعارات محمَّلة فعلياً (98 متجراً من Supabase)، نقرة فعلية على «ملحمة الدوماني» من الشريط نقلت لـ`/store/domani` بالعنوان الصحيح والشريط باقٍ على الصفحات الداخلية، موبايل 375px: صفر انزياح أفقي + الشريط قابل للتمرير (560/349px) + رقائق الهيرو مخفية، سطح مكتب 1280px: الرقائق ظاهرة بأبعاد صحيحة، صفر أخطاء كونسول (لقطات الشاشة معطّلة بمهلة — نفس العطل البيئي الموثَّق مراراً، اعتُمد تحقّق DOM/الأنماط المحسوبة). Flutter: `flutter analyze` نظيف (نفس الـ3 ملاحظات المعلوماتية القديمة فقط) — **لم يُشغَّل المحاكي** (التغيير تركيب قائمة فقط فوق `PromoHero` المُتحقَّق منه حياً في جلسات سابقة، بلا أي تغيير نموذج/مخطط؛ ملاحظة: `store.dart` كان يحمل تعديلات غير محفوظة من جلسة متزامنة أخرى — لم يُلمس) | `index.html`, `app.js`, `styles.css`, `sw.js`, `merchant-app.html`, `customer-app.html`, `dukkanci_customer_app/lib/features/home/presentation/home_screen.dart`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v325→326، styles.css v176→177، CACHE v245→246). **مدفوع لـ`main`** (commit `872dde38`، بطلب المستخدم الصريح — عُزل بـ`git add` انتقائي للملفات الثمانية فقط عن تعديلات الجلسات المتزامنة غير المرتبطة في نفس الشجرة؛ ملاحظة: الدفع حمل معه حتماً التزام «المرحلة 2» المحلي غير المدفوع `11d610d8` من جلسة أخرى لأن تعديلاتي مبنية فوقه نصياً، وفشل حفظه بلا migration ظريف وموثَّق). تحقّق بعد النشر: Vercel نجح (حالة commit = success)، والصفحة الرئيسية الحية تحمل شريط `featured-stores-bar` بالروابط الأربعة و`app.js?v=326`/`styles.css?v=177`/`CACHE v246` كلها مخدومة فعلاً، والشريط ظاهر أيضاً في قشور SSR للصفحات الداخلية (`/store/domani`, `/offers`) |
| 2026-07-19 | المستخدم طلب تنفيذ «المرحلة 2» من محرك تنظيم التصنيفات (المرحلة 1: 2026-07-18 أعلاه) — تحكّم إداري/تاجر يدوي في الترتيب/الدمج/الإخفاء لكل تصنيف، حسب بند «سابعاً: الأولوية اليدوية» و«عاشراً: متطلبات واجهة الإدارة» من المواصفة الأصلية | ميزة جديدة (لا خطأ سابق). لا يوجد عمود مرن غير مستخدَم على `stores` لإعادة استخدامه (تحقّق بحث شامل + وكيل بحث مخصَّص) — أقرب مرجع هو `payment_methods` (jsonb مسطّح ثابت الحقول). `save-store` العام upsert كامل الصف بلا قائمة سماح للحقول — إعادة استخدامه لهذا الحقل كانت ستخاطر بأن حفظاً من طرف (تاجر) يمحو تعديلات الطرف الآخر (إدارة) بنسخة قديمة محلية؛ النمط الآمن الموجود مسبقاً لنفس المعضلة هو `save-store-zones` (قراءة-تعديل-كتابة من طرف الخادم) | **قاعدة البيانات:** migration جديد (`20260719_store_category_settings.sql`): عمود `stores.category_settings jsonb default '{}'` بالشكل `{admin:{[raw]:override}, merchant:{[raw]:override}}`. **لم أستطع تطبيقه بنفسي** — لا اتصال Postgres مباشر (لا `DATABASE_URL` في `.env`)، `SUPABASE_SERVICE_ROLE_KEY` يمنح REST فقط (لا DDL)، ولا Supabase CLI مثبَّت، وموصل Supabase MCP في هذه الجلسة مربوط بمشروع مختلف تماماً (تحقّق مباشر: `execute_sql` على `tzcqnqzltrjemdnkzpzn` أعاد «You do not have permission» — نفس العطل الموثَّق مراراً في جلسات سابقة) — طُلب من المستخدم تنفيذ الـmigration يدوياً عبر محرر SQL في Supabase. **الخادم:** action جديد `save-store-categories` في `api/notify-order.js` (بعد `save-store-zones` مباشرة) — قراءة-تعديل-كتابة على مستوى الدور فقط (`admin` أو `merchant`، يُحدَّد حسب `adminOk`/`merchantOk`)، فتجاوز أحد الطرفين لا يمحو تجاوز الآخر أبداً، مع تعقيم صارم للحقول المقبولة (`mergeInto/disableAutoMerge/forceVisible/hidden/sortOrder` فقط، وتجاهل أي صف كامل القيم الافتراضية). **`app.js`:** `mapDbStore()` يقرأ `category_settings` كـ`store.categorySettings` **للقراءة فقط** — عمداً **غير موجود** في `toDbStore()` (نفس نمط `approvalStatus`/`subscriptionActive` الموجود مسبقاً للحقول المُدارة خادمياً) حتى لا يمحوه حفظ عام من نموذج «بيانات المتجر». ثلاث دوال جديدة تُطبَّق داخل/حول `buildStoreCategoryPlan` (مرحلة 1): `resolveCategoryOverride(store,raw)` (تجاوز الإدارة يفوز كاملاً على تجاوز التاجر لنفس التصنيف الخام — لا دمج حقل-بحقل، القيد كاملاً هو الفائز)، `filterHiddenCategoryProducts()` (يُستدعى **قبل** أي اشتقاق من `allStoreProducts` في `renderStorePage` — العروض، الأكثر طلباً، العدادات — كي يختفي تصنيف مخفي من كل مكان بثبات)، `applyManualCategoryOrder()` (رقم صريح يفوز، غير المخصَّص يُرتَّب تلقائياً بعده بفارق 1000، «تصنيفات أخرى» تبقى أخيرة دائماً). **تُطبَّق التجاوزات على كل أنواع المتاجر** (ليس فقط مطاعم/سوبر ماركت من المرحلة 1) — حتى مسار الـfallback (أنواع أخرى) يدعم الآن `mergeInto` يدوياً + الإخفاء + الترتيب، توسيعاً مفيداً لم يكن ضمن نطاق المرحلة 1. 🔴 **علة حقيقية اكتُشفت ذاتياً بالاختبار الحي (وليس افتراضاً):** أول نسخة استخدمت خريطتين منفصلتين (`official`/`custom`) — تصنيف خام غير رسمي (مثال: «بيتزا عادية» 8 منتجات) وتصنيف آخر أُعيد توجيهه يدوياً (`mergeInto`) لنفس النص يقعان في خريطتين مختلفتين فيظهران كزرّين مكرَّرين بنفس الاسم بدل الاندماج في واحد؛ أُعيد التصميم لخريطة واحدة موحَّدة مفتاحها التسمية النهائية مع علم `isOfficial` لكل حزمة بدل خريطتين منفصلتين — تحقّق حي بعد الإصلاح: 8+1=9 بشكل صحيح بزر واحد. **واجهة الإدارة/التاجر:** تبويب جديد `تصنيفات المتاجر` (إدارة، بجانب «التوصيل»، محاكاة تامة لنمط `adminDeliveryZones()`: قائمة متاجر قابلة للتوسيع) و`تصنيفات متجرك` (تاجر، بجانب «إعدادات المتجر»، بلا منتقي متجر لأنه متجره فقط) — دالة عرض مشتركة واحدة `categorySettingsEditorHTML(store,scope)` لكلا السياقين (تفادياً لتكرار الكود)، جدول لكل تصنيف خام حقيقي (الاسم، العدد، التصنيف التلقائي كمرجع، دمج مع [نص حر + `datalist` اقتراحات للفئات الرسمية]، منع الدمج، إظهار دائماً، الترتيب، إخفاء)، مع ملاحظة شفافية إن كان الطرف الآخر (تاجر/إدارة) ضبط تجاوزاً لنفس التصنيف أيضاً. زر «معاينة» يستدعي `buildStoreCategoryPlan` بالقيم الحالية غير المحفوظة مباشرة من DOM (بلا أي طلب شبكة) ويعرض شريط الأزرار الناتج فعلياً — يطابق بند «معاينة قبل الحفظ» في المواصفة حرفياً. زر «حفظ» يرسل فقط الصفوف غير الافتراضية لـ`save-store-categories` بترويسة `x-admin-token` أو `x-merchant-token` حسب السياق. CSS جديد صغير جداً (9 أسطر) لضبط عرض الحقول داخل الجدول. تحقّق شامل عبر preview محلي (تسجيل دخول إدارة `dukkanci-dev`): متجر «باشا بيتزريا» (31 منتجاً، 9 تصنيفات خام) — معاينة دمج+ترتيب+إخفاء أعادت 28 من 28 (استبعاد 3 منتجات بطاطا مخفية بثبات)، «الفلافل» تصدَّر الترتيب برقم يدوي 1، «بيتزا عادية» أظهرت 9 (8+1 مدموجة، صفر تكرار بعد الإصلاح). محاكاة القيم كأنها محفوظة فعلاً (`getStore(56).categorySettings=...` ثم `render()`) على صفحة المتجر الحية (`/store/pasa-pizzeria-restaurant`) أثبتت المسار الكامل من القراءة حتى العرض يعمل بشكل صحيح: 28 من 28 في الترويسة، البطاطا غائبة تماماً، والزر المدموج «بيتزا عادية» يُظهر فعلياً 9 منتجات من كلا التصنيفين الخامين عند النقر (`data-raw-categories` صحيحة). محاولة «حفظ» فعلية أعادت 502 (متوقَّع تماماً — العمود غير موجود بعد في القاعدة الحقيقية لغياب الـmigration) والتُقطت بأمان (Toast خطأ، صفر استثناء JS غير معالَج في الكونسول) — يثبت أن الفشل ظريف وليس عطلاً. لم يُختبَر تبويب التاجر حياً (يحتاج جلسة تاجر حقيقية بكلمة مرور)؛ الثقة فيه مبنية على إعادة استخدام نفس دالة العرض/القراءة المُتحقَّق منها في مسار الإدارة حرفياً بلا أي فرع كود خاص بالتاجر. `npm run check:versions` ناجح | `migrations/20260719_store_category_settings.sql` (جديد)، `api/notify-order.js`, `app.js`, `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v325، styles.css v176، CACHE v245). **لم يُنفَّذ `git commit`/push بعد — وبانتظار تشغيل المستخدم لملف الـmigration يدوياً قبل أن يعمل الحفظ فعلياً على الموقع الحي**
| 2026-07-18 | المستخدم طلب إضافة مطعم «شام أوغلو» (Şamoğlu Restoran) من كتالوج واتساب بزنس (`wa.me/c/905434888818`)، باشاك شهير، إسطنبول | ميزة إضافة جديدة (لا خطأ سابق) — لكن اكتُشف **خطأ استخراج جوهري جديد لم يُوثَّق من قبل**: معاينات الفئات الثلاثية (3 عناصر) في الصفحة الرئيسية للكتالوج **غير موثوقة إطلاقاً** لعدد العناصر الحقيقي — فئة «فتة و فول» بدت 3 عناصر في المعاينة لكن صفحة «See all» الكاملة كشفت 7 عناصر فعلية، و«المقبلات الساخنة» بدت 3 وكانت فعلياً 9، و«المقبلات الباردة» 3→10، و«فطائر» 10→11 (بعد تمرير كامل حتى استقرار العداد). **الدرس الحاسم: يجب فتح «See all» لكل فئة والتمرير حتى استقرار عدد `[role="listitem"]` عبر JS، لا الاكتفاء بمعاينة الفئة الرئيسية أبداً** — يُضاف هذا كتصحيح فوق درس فستق حلب بيليكدوزو (2026-07-18 أدناه) الذي حذّر فقط من نقص «All items» بينما هذا الكتالوج لا يملك قسم «All items» إطلاقاً (تحقّق عبر `find` صريح: "There is no All items category"). **خطأ استخراج ثانٍ أخطر اكتُشف لاحقاً بالمقارنة بالـMD5:** أول محاولتين لتنزيل صور دفعة كاملة (`querySelectorAll('[role=listitem]')` ثم حلقة `await downloadCategoryImages` مع `setTimeout` بين كل تنزيل) أنتجتا صوراً **متطابقة بايتياً (نفس MD5) لعناصر مختلفة تماماً** (مثال: 5 من أصل 10 عناصر فطائر مختلفة الاسم/السعر حصلت على نفس ملف الصورة) — تبيّن أن السبب هو تأخر إعادة رسم/تدوير عناصر DOM الافتراضية (list virtualization) أثناء التمرير والتنزيل المتتالي، وليس خطأ في محدد العنصر نفسه (تحقّق: نفس العنصر[0] كان يحمل قيمة `backgroundImage` مختلفة تماماً بين قراءتين متتاليتين بفارق أجزاء ثانية فقط). **الحل الموثوق:** قراءة كل روابط الصور (`getComputedStyle().backgroundImage`) في **تمريرة واحدة متزامنة بلا أي `await`/تنزيل بينها** أولاً (تُخزَّن في `window.__pending`)، ثم تنزيلها لاحقاً من المصفوفة المخزَّنة — لكن حتى بعد هذا الإصلاح، تكرار الصور بقي في 6 مجموعات (17 عنصراً) عبر فئات فطائر/فتة/مقبلات ساخنة/صندويش/بيتزا؛ فُتح عنصر واحد من كل مجموعة عبر لوحة Details (وهي طريقة استخراج مستقلة تماماً عن القائمة الافتراضية) فتأكَّد أن **الصورة المكرَّرة هي فعلاً ما رفعه التاجر نفسه** (نفس صورة الكولاج الترويجي لثلاثة سندويشات مختلفة، ونفس صورة فطيرة عامة لخمسة أصناف فطائر مختلفة) — أي أن التكرار بيانات تاجر حقيقية وليس خطأ استخراج، فاعتُمدت كما هي (يطابق سابقة فستق حلب/زمزم في نفس القسم). **9 عناصر أخرى (من أصل الفئات المفحوصة) كانت صورة شعار المطعم العام** (رسم كاريكاتوري أسود لطباخ + «ŞAM OĞLU») بدل صورة المنتج الحقيقية (كبة نية «هبرة نية»، باذنجان مقلي، شنكليش، صحن محمرة، سكالوب) — استُبعدت هذه المنتجات كلياً (لا صورة تمثيلية حقيقية، يطابق سياسة عدم نشر صورة شعار عامة كصورة منتج). **12 عنصراً إضافياً بلا سعر ثابت** (كيلو مشاوي بالكامل 8 أصناف، فرن و صفيحة بالكامل 5 أصناف، المطبخ الشامي بالكامل 5 أصناف [بعضها يتقاطع]) استُبعدت كلياً (لا سعر = لا نشر، يطابق سياسة المشروع الثابتة). المحصلة النهائية: **87 منتجاً حقيقياً** عبر 15 فئة، بعد فحص شامل لـ18 فئة مُسمّاة (فتة و فول 7، الكبة 3، المقبلات الساخنة 8، المقبلات الباردة 8، السلطات 6 [+منتجان فريدان مُكتشَفان فقط داخل فئة «جديدنا»: متبل الشمندر وسبانخ بالزيت]، الشوربات 4، المشاوي 8، الوجبات الغربية 7، الصندويش 3، الفخارات 5، بيتزا 5، فطائر 11، مناقيش 6، المشروبات 4). **مطابقة Google Maps مؤكَّدة بدقة فائقة (نادرة الوضوح):** بحث نصي بالاسم بالقرب من إحداثيات العنوان الجغرافي (المُستخرج من ملف تعريف واتساب بزنس نفسه: «Başakşehir, in front of Carrefour, Başakşehir 1. Etap») أعاد تطابقاً حرفياً **مزدوجاً** — نفس الاسم بالضبط («ŞAM OĞLU RESTORAN مطعم شام أوغلو») **و**نفس رقم الهاتف بالضبط (+90 543 488 88 18) **و**نفس نص العنوان بالضبط، تقييم 4.2 (543 تقييماً)، `place_id`: `ChIJNxXaRQWvyhQR-BbNfdKg8sI`. ساعات العمل من Google Place Details (يومياً 11:00 ص – 11:30 م) اعتُمدت بدل نص واتساب بزنس («10:00-23:59») لأنها أكثر دقة/حداثة. الشعار والغلاف حُمِّلا من لوحة «Contact info» في واتساب ويب نفسها (روابط CDN مباشرة `media-*.whatsapp.net`، وليست blob — تطلَّب `fetch→blob→createObjectURL` بدل تنزيل مباشر بـ`anchor.href` الذي فشل بصمت على روابط عابرة للنطاقات) — الغلاف صورة حقيقية لصالة استقبال ولائم كبرى (طاولة طويلة بكراسي ذهبية وأطباق مُجهَّزة)، والشعار رسم الطباخ الكاريكاتوري نفسه. المتجر رقم **108** (تحقّق `max(id)` مباشرة قبل الإدراج: 107 — لا تصادم رغم تقدُّم الفرع بـ5 متاجر [103-107] منذ آخر تحقّق في بداية الجلسة، يطابق نمط «تحقّق دائماً مباشرة قبل التنفيذ»)، حزمة معرّفات المنتجات **1900001–1900087**. صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82 mozjpeg، شعار 400×400، غلاف 1280×542 بقص «attention»-aware). `approval_status='pending'` عمداً. وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`108: "shamoglu-restaurant-basaksehir"`) + `<script src="/shamoglu-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع لـSupabase عبر سكربت Node مخصَّص (`scripts/insert-shamoglu-supabase.js`، service-role REST مباشرة — موصل Supabase MCP في هذه الجلسة كان لا يزال مربوطاً بمشروع مختلف تماماً `goldengttr-hub`، نفس القيد الموثَّق في كل الجلسات السابقة). تحقّق شامل عبر preview محلي: `/store/shamoglu-restaurant-basaksehir` يعرض العنوان الصحيح (`دكانجي - شام أوغلو`)، «87 من 87 منتجاً» عبر 15 فئة بأسعار مطابقة تماماً لـSupabase، معدّل توصيل «٢٠ ل.ت/كم» صحيح، كل الـ89 رابط صورة (87 منتج + شعار + غلاف) أعادت HTTP 200 فعلياً (تحقّق جماعي `fetch HEAD` من نفس الصفحة الحية محلياً)، `/category/restaurants` لا يعرض المتجر إطلاقاً ضمن الـ48 متجراً المعروضة (تحقّق مباشر `document.body.innerText.includes('شام أوغلو')===false`، سلوك `pending` صحيح)، الصفحة الرئيسية بلا أي أثر وبلا أخطاء كونسول. `npm run check:versions` ناجح | `shamoglu-data.js` (جديد)، `assets/photos/shamoglu/**` (جديد، 89 ملف: 87 منتج + شعار + غلاف)، `scripts/process-shamoglu-images.js` (جديد)، `scripts/insert-shamoglu-supabase.js` (جديد)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 108 وصفوف `products` 1900001-1900087 في Supabase | نعم — `npm run check:versions` ناجح (app.js v322→323، store-slugs.js/index.html v258→259، CACHE v243→244). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-18 | المستخدم أرسل تعليمة برمجية شاملة لتنظيم تصنيفات المطاعم/السوبر ماركت (ترتيب رسمي، إخفاء الفارغة، دمج الصغيرة، توحيد المسمّيات المكرَّرة، قسم «الأكثر طلباً»، تحكم إداري يدوي). بسبب حجم المواصفة وحالة `git status` غير النظيفة وقت الطلب (جلسات متزامنة أخرى تعمل على نفس الملفات)، طُرح سؤالان توضيحيان صريحان قبل أي تعديل: تابع بلا لمس الموجود (بدل التوقف)، وتنفيذ **«المرحلة 1: منطق العرض فقط»** (بلا واجهة إدارة، بلا أعمدة Supabase جديدة، بلا تحليل طلبات فعلي) | ميزة جديدة (لا يوجد خطأ سابق) — لم يكن هناك أي نظام دمج/ترتيب لتصنيفات المنتجات داخل صفحة متجر واحد: فقط شريط أزرار فلترة أفقية (pills) مبني مباشرة من `product.category` الخام (نص حر يكتبه التاجر بلا قيد، عبر `<select>` + خيار «إضافة تصنيف جديد»)، بلا أي تطبيع أو ترتيب أو إخفاء للفارغ (أصلاً لا يمكن أن يظهر تصنيف فارغ لأن القائمة مبنية من التصنيفات الموجودة فعلاً — لكن لا يوجد أي دمج لتصنيفات متشابهة/صغيرة). قسم «الأكثر طلباً» كان مقصوراً على الصفحة الرئيسية فقط، ومبنياً على `MOST_ORDERED_PRODUCTS` (اختيار يدوي من الإدارة عبر `site_settings.mostOrdered`، موثَّق صراحة في تعليق الكود بأنه «لا توجد بيانات ترتيب مبيعات حقيقية بعد») — تحقّق بحث شامل في `app.js` وكل ملفات `migrations/` أن `product.is_popular`/أي عمود مكافئ **غير موجود إطلاقاً** | بُني محرك تنظيم عرض بالكامل داخل `app.js` (بلا أي تعديل على Supabase أو على `product.category` الفعلي — دمج طبقة عرض فقط، يُعاد حسابه في كل تصيير): `buildStoreCategoryPlan(store, allStoreProducts)` تبني قائمة تصنيفات مرتّبة غير فارغة، عبر: (1) قائمتا ترتيب رسمي `RESTAURANT_CATEGORY_ORDER` (24 فئة) و`SUPERMARKET_CATEGORY_ORDER` (40 فئة) مطابقتان حرفياً لترقيم المواصفة (باستثناء «العروض»/«الأكثر طلباً»/«موسمية» المدارة كأقسام خاصة منفصلة لا كأزرار فلترة). (2) `normalizeCategoryKey()` يحذف «أل» التعريف من **كل** كلمة (لا الأولى فقط) فتتوحّد تلقائياً صيغ مثل «المشروبات»/«مشروبات» و«المقبلات الباردة»/«مقبلات باردة» دون قوائم استثناء يدوية لكل حالة. (3) جداول مرادفات صريحة (`RESTAURANT_CATEGORY_ALIASES`/`SUPERMARKET_CATEGORY_ALIASES`) للأمثلة الحرفية من المواصفة (مشروب، الحلو، عصير طبيعي، أنواع السلطات، صلصات، طحينة...). (4) جداول دمج تصنيفات صغيرة (`RESTAURANT_MERGE_TARGETS`/`SUPERMARKET_MERGE_TARGETS`) تُطبَّق **حصراً** على تصنيف لم يُطابق اسماً رسمياً **وعدد منتجاته ≤2** (حمص/متبل→المقبلات الباردة، عدس/فاصولياء→البقوليات...) — أي تصنيف رسمي معروف (حتى لو صغيراً) لا يُدمَج أبداً، تطبيقاً حرفياً لتحذير المواصفة الصريح («لا يجوز دمج اللحوم في المجمدات لمجرد قلة المنتجات»)؛ ما لا يجد تطابقاً واثقاً يذهب لـ«تصنيفات أخرى» في آخر القائمة دائماً بدل تخمين دمج خاطئ. متاجر الفئات الأخرى (حلويات/ملاحم/عصائر...) تبقى بسلوكها الأصلي (بلا ترتيب/دمج) لأن المواصفة حدَّدت مطاعم/سوبر ماركت فقط. `renderStorePage()`: أُعيد بناء شريط أزرار الفلترة وعدّاداتها من نتيجة `buildStoreCategoryPlan` بدل القائمة الخام، مع سمة جديدة `data-raw-categories` (JSON بكل الأسماء الخام المدموجة تحت كل زر) — ضرورية لأن النقر على زر فلترة يُنفَّذ فعلياً بجافاسكربت مباشر على DOM (`card.dataset.category`، بلا إعادة رسم كامل للسرعة) فيحتاج معرفة أي الأسماء الخام تخصّ الزر المدموج؛ عُدِّل معالج `product-category` ليقرأ هذه السمة، مع تحقّق أن مسارَي الفلترة (النقر السريع بـDOM وإعادة الرسم الكاملة `categoryFiltered`) يستخدمان بالضبط نفس خريطة الدمج فلا يتعارضان. أُضيف أيضاً قسم أفقي جديد «الأكثر طلباً من {المتجر}» (يعيد استخدام تصميم/أصناف CSS شريط العروض `.store-offers-slider` الموجود أصلاً حرفياً — صفر تعديل CSS) يظهر مباشرة بعد شريط العروض، ومصدره `MOST_ORDERED_PRODUCTS` (نفس الآلية اليدوية للرئيسية) مُصفَّاة لمنتجات هذا المتجر فقط — لا يظهر إطلاقاً إن كان فارغاً. **قرارات نطاق مسجَّلة صراحة (تطبيقاً لاختيار «المرحلة 1»):** لا تحكّم إداري يدوي (`manual_sort_order`/`disable_auto_merge`/`force_visible`/`display_parent_id`/`canonical_category_id` — بند «سابعاً» في المواصفة) ولا تحليل طلبات فعلي (بند «ثانياً - الأفضل تقنياً») في هذه الجولة — كلاهما يحتاج أعمدة Supabase جديدة خارج نطاق «عرض فقط»؛ كذلك لم يُلمَس بند المواصفة «7» («لا تظهر منتجات غير متاحة») لأن الموقع يعرض عمداً منتجات غير متوفرة بشارة رمادية «غير متوفر» بدل إخفائها بالكامل — سلوك تصميمي موجود مسبقاً وأوسع من نطاق تصنيفات هذه المهمة تحديداً. تحقّق شامل عبر preview محلي: متجر «فريندز» (554 منتجاً/30 تصنيفاً خاماً، مطعم) — 4 تصنيفات تطابقت رسمياً وتصدَّرت بترتيبها الصحيح (السندويشات، البيتزا، المشروبات الساخنة، المشروبات الباردة)، البقية (تصنيفات مخصَّصة بأكثر من عنصرين) بقيت مستقلة بترتيبها الأصلي كما هو متوقَّع (تصميم متحفّظ يتفادى دمجاً غير واثق)، وتصنيف صغير وحيد («أطباق سمك»، عنصران) ذهب لـ«تصنيفات أخرى» بنجاح لعدم وجود كلمة مفتاحية مطابقة؛ متجر «زمزم ماركت» (سوبر ماركت) — تأكيد حي أن دمج التصنيفات الصغيرة يعمل فعلياً («الصلصات ومعجون الطماطم»=2 و«العسل والمربى»=1 تجمَّعا من فئات فرعية صغيرة عبر الكلمات المفتاحية)، ومجموع كل الأزرار (2+1+3+5+7+7=25) طابق تماماً عدّاد «الكل». تحقّق مزدوج أن مساري الفلترة (النقر السريع + إعادة الرسم الكاملة عبر استدعاء `render()` مباشرة) متطابقان تماماً على نفس التصنيف المدموج، وأن العودة لـ«الكل» تُعيد كل المنتجات (554/554). صفر أخطاء كونسول على كل الصفحات المختبَرة. 🔴 **حادثة تزامن حقيقية أثناء العمل:** جلسة أخرى أضافت متجراً جديداً بالتوازي (`shamoglu-data.js`) ورفعت `store-slugs.js`/`CACHE`/`app.js` في `index.html`/`sw.js` بمعزل عني — رُصد فوراً عبر `system-reminder` تلقائي على كل تعديل، وأُعيد تزامن رقم إصدار `app.js` فقط بين `index.html`/`sw.js` (321→322 محلياً ثم 322→323 مطابقةً لبَصمهم) بلا لمس مطلقاً لتعديلاتهم (`shamoglu-data.js` وربطه في `APP_SHELL`/`store-slugs.js`) — تحقّق `node --check app.js` و`grep` لدوال المحرك الجديد بعد كل تعديل خارجي رُصد، تأكيداً أن كودي لم يُطمَس. `npm run check:versions` ناجح نهائياً (app.js v323، styles.css v175 — لم يتغيَّر) | `app.js`, `index.html`, `sw.js`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح. **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-18 | 🔴 المستخدم أبلغ أن الموقع «متجمد لا يفتح الصفحة الرئيسية» — عطل حي فعلي مؤثِّر على كل زائر | نفس فئة الخطأ الموثَّقة في قسم 6 («حذف متجر لا يكفي وحده») لكن بالاتجاه المعاكس: `index.html` (مُلتزَم على `main` ضمن commit `9f5e2cf5`) أضاف `<script src="/paradice-data.js?v=1">` **بينما الملف نفسه وصور المتجر لم تُرفعا معه** (بقيتا `??` غير متتبَّعتين محلياً — أثر تصادم كتابة بين جلستين متزامنتين وقت ذلك الالتزام، موثَّق أيضاً في سطري بردايس/تركواز أعلاه). على الموقع الحي، طلب الملف المفقود يرتد لصفحة `index.html` نفسها (SPA fallback من Vercel) فيفشل تفسيرها كـJS بخطأ `Unexpected token '<'`، فيبقى `paradiceStore` غير معرَّف، فيرمي أول سطر `stores.push(...)` في `app.js` استثناء `ReferenceError` **يوقف تنفيذ الملف بأكمله قبل أي `render()`** — الهيدر/الفوتر ثابتان في `index.html` فيظهران، لكن `<main>` (كل محتوى الصفحة) يبقى فارغاً للأبد. تحقّق مباشر ضد `https://www.dukkanci.com.tr/` (وليس افتراضاً محلياً) أثبت العطل حياً: استدعاء `loadCatalogFromSupabase()` يدوياً رجع `Cannot access 'allProducts' before initialization` (نفس أثر توقّف السكربت المبكر — سطر التصريح 158 لم يُنفَّذ إطلاقاً). فحص كل الملفات المُشار إليها في `index.html` (curl لكل رابط `<slug>-data.js?v=`) كشف أن **نفس العطل بالضبط كان جاهزاً ليتكرر** لملفي `turkwaz-data.js` و`ibnalwazeer-data.js` (كلاهما مُشار إليهما في نفس الالتزام لكن غير مرفوعين وقتها) | (1) `commit 2230c02`: رفع `paradice-data.js` + `assets/photos/paradice/` (173 ملف) فقط — عزل عن بقية الملفات المعدَّلة/غير المتتبَّعة في نفس شجرة العمل (`CLAUDE.md`, `api/notify-order.js`, `dukkanci_customer_app/`) عبر `git add` انتقائي بمسارين محدَّدين فقط. (2) `commit 272f47b` (إكمال فوري بعد اكتشاف نفس العطل جاهزاً للتكرار): رفع `turkwaz-data.js` + `ibnalwazeer-data.js` + صورهما (245 ملف) + سطور `store-slugs.js` الناقصة (105/106/107) — كلاهما تحقّق `node --check` وعدّ صور مطابق تماماً (153=153، 89=89) قبل الرفع. تحقّق شامل بعد كل التزام: `npm run check:versions` ناجح، `git show HEAD:app.js` يُفسَّر بلا أخطاء صياغة (`node --check`)، كل ملف `<slug>-data.js?v=` في `index.html` مؤكَّد وجوده فعلياً في `HEAD` (حلقة `git cat-file -e`)، Vercel نشر بنجاح (حالة commit = success عبر `gh api`)، وتحقّق حي كامل على `https://www.dukkanci.com.tr/` بتبويب متصفح **جديد** (لتفادي تراكم سجلّ كونسول قديم من قبل النشر): `Supabase: loaded 95 stores, 10675/11740 products` بلا أي تحذير TDZ، `<main>` يحمل 327556 حرفاً، 61 بطاقة متجر ظاهرة. (3) بطلب المستخدم لاحقاً: `commit da2aadc` — أُضيف slug نظيف لفرعي حلويات السلطان العالمية الحقيقيين رقم 9 (بيليكدوزو) و11 (سلطان غازي) اللذين كانا يظهران بتحذير «بلا رابط نظيف» أثناء نافذة الـ1.8 ثانية قبل اكتمال استبدال `stores` بالكامل ببيانات Supabase (`loadCatalogFromSupabase` يُصفِّر `stores` ويعيد بناءها من صفوف Supabase فقط — أي متجر مُجمَّع محلياً بلا صف Supabase مطابق يختفي تلقائياً بعد هذه النافذة). أما id 72 (حضرموت اليمن) — تحقّق مباشر عبر Supabase REST (`stores?id=eq.72`) أكّد أن صفّه **لا يزال محذوفاً فعلياً** كما وثَّق تدقيق 2026-07-10 — فبدل منحه slug لمتجر ميت، أُزيل الملف المُجمَّع بالكامل (`hadramout-data.js` + 153 صورة + وصل `app.js`/`index.html`) نهائياً، مطابقاً لسياسة تنظيف المتاجر الميتة الموثَّقة في قسم 6 (نفس ما حدث مع حلاب 1881 سابقاً) بدل ترك دَين تقني إضافي. تحقّق preview محلي + حي بعد الدفع: صفر تحذير «بلا رابط نظيف» متبقٍّ، صفر مرجع لـ`hadramout` في أي من الملفات الأربعة | `paradice-data.js`, `turkwaz-data.js`, `ibnalwazeer-data.js` (رُفعت كملفات موجودة مسبقاً محلياً من جلسات سابقة) + صورها، `store-slugs.js` (9/11 مضافان، 105/106/107 مكتملة)، `app.js`, `index.html`, `sw.js` (حذف مرجع hadramout + رفع الإصدار)، حذف `hadramout-data.js` + `assets/photos/hadramout/**` (153 ملف)، `CLAUDE.md` | نعم — `npm run check:versions` ناجح على كل التزام (app.js انتهى عند v321، store-slugs.js v258، CACHE v242). **دُفع لـ`main` بالكامل** (٤ التزامات: `2230c02`، `272f47b`، ثم التزام تركواز/ابن الوزير/بردايس التوثيقي من الجلسة المتزامنة، ثم `da2aadc`) — Vercel نشر بنجاح على كل خطوة، تحقّق حي مباشر بعد كل نشر
| 2026-07-18 | المستخدم طلب إضافة مطعم «تركواز» (Turkwaz Balık Restaurant)، رابط `turkwaz.macho.menu/en/zoe5n-alwr6-cd4bv`، حي كاياباشي، باشاك شهير، إسطنبول | ميزة إضافة جديدة (لا خطأ سابق) — أول مصدر من نوع **macho.menu** (منصة Next.js من "Lamaz System"/lamazapp.com) لهذا المشروع | **الاستخراج:** الموقع Next.js App Router بصفحات مصيَّرة خادمياً لكل فئة (`/en/<branch>/<catId>`) — بيانات RSC الخام (POST داخلي بصيغة `0:...\n1:{json}`) كانت تحوي `list`/`categories` كاملة لكل فئة، لكن استخراجها المباشر عبر `read_network_requests` اصطدم بسقف اقتصاص طول الاستجابة لعدم كفاية العرض الظاهري؛ بدلاً منه اعتُمدت شجرة إتاحة الوصول (`read_page`) + استعلام DOM مباشر (`querySelectorAll('main a[href*="/CATID/"]') img.src`) لكل فئة على حدة — أنظف وأسرع من كتالوجات واتساب المعتادة (لا حاجة تمرير/نقر تفصيلي، كل صف يحمل الاسم الإنجليزي + السعر + بديل الصورة العربي + رابط الصورة 500px مباشرة). 10 فئات رئيسية + قسم عروض «Turkwaz Summer Offers» بثلاث فئات فرعية زمنية (فطور/غداء/عروض كأس العالم) — فئتا الفطور والغداء كانتا فارغتين فعلياً وقت الاستخراج (عروض مجدولة بساعات لم تُفعَّل)، وفئة «World Cup Set Menu» (4 قوائم بخصم حقيقي 1120/1270→799/899) استُبعدت بالكامل عمداً رغم وجود خصم حقيقي: الأربعة تتشارك **صورة واحدة طابقة بالحرف** (تصميم AI عام لا صورة طبق حقيقية)، وقاعدة نشر المنتجات تُخفي صور مكرَّرة داخل نفس المتجر أصلاً فلن يظهر منها إلا واحد بلا مسوّغ لعرضه، إضافة لكونه عرضاً موسمياً (كأس العالم) شارف على الانتهاء وقت الإضافة. استُبعدت أيضاً 3 عناصر في «المقبلات الساخنة» (أرز/صيادية/برياني بلا صورة، `product-placeholder.jpg`) و4 عناصر في «المشروبات الباردة» (آيس فرابتشينو×2/آيس موكا بالشوكولا البيضاء/كولد برو، نفس السبب) طبقاً لسياسة عدم نشر منتج بلا صورة حقيقية. المجموع النهائي **151 منتجاً فريداً عبر 12 فئة حقيقية** (المقبلات الباردة 10، السلطات 8، المقبلات الساخنة 16، الشوربات 3، أطباق السمك 22، أطباق القريدس 10، أطباق الدجاج 4، مشروبات ساخنة 19، مشروبات باردة 16، مشروبات غازية وخفيفة 9، إفطار صباحي 29، حلويات 5) — عدة عناصر فطور (حمّص/متبل/أصبع الكراب/لفائف الربيع) تتشارك صوراً وأسعاراً حرفية مع نظائرها في «المقبلات»، أُبقيت كصفوف منفصلة (نفس الصنف يظهر فعلاً في قسمي الكتالوج المصدر بنفس السعر) بدل حذفها، تاركاً لقاعدة الصور المكرَّرة إخفاء الزائد تلقائياً وقت العرض. **مطابقة Google Maps مؤكَّدة بدقة عالية جداً:** بحث نصي بالاسم أعاد نتيجة واحدة «مطعم تركواز - Turkwaz Restaurant» بتطابق هاتف **حرفي كامل** مع رقم واتساب المتجر (`+90 534 299 30 00`) وساعات عمل مطابقة تماماً لِما يعرضه الموقع نفسه (يومياً 11:00–00:00) — تقييم 4.6 (822 تقييماً)، `place_id`: `ChIJmft-4vCvyhQRGYmVBfT9dU0`، إحداثيات دقيقة (41.1169983, 28.7746913)، عنوان (`Kayabaşı, Adnan Menderes Bulvarı A3 Blok No: 7/D-53, Başakşehir/İstanbul`). **لا شعار عالي الدقة مخصَّص في المصدر** (فقط علامة أفقية 81×25px في ترويسة الموقع) وموارد `generate_image` نفدت (رصيد الجلسة صفر، فشلت محاولة توليد شعار بديل)؛ بدلاً منها اعتُمد شعار حقيقي مقصوص من إحدى صور Google Places المساهَم بها (صورة واجهة المحل الليلية الحقيقية تُظهر لافتة "TURKWAZ BALIK" مضاءة بالنيون الفيروزي + شارة دائرية "TURKWAZ" بزخرفة عثمانية) — قُصَّت الشارة الدائرية (`sharp extract` + `lanczos3` + `sharpen`) وحُشيت بخلفية داكنة مطابقة للون الأصلي لتفادي تشويه القصّ، بدقة نهائية محدودة (مصدرها صورة 1280px فقط) لكنها حقيقية وواضحة القراءة — نفس سياسة قبول جودة أقل من مصدر حقيقي بدل شعار عام (سابقة بيت حلب/الوكالة). **الغلاف من نفس مصدر Google Places:** صورة مساهَم بها حقيقية لصينية مأكولات بحرية فعلية من المطعم (سمك وقريدس مشوي على أرز، تاكو قريدس، سلطة كراب، حلقات بصل مقلية) — لا صورة توليد اصطناعي، مصوَّرة على طاولة المطعم نفسه. المتجر رقم **107** (تحقّق `max(id)` أربع مرات متتالية بسبب تصادمات حيّة متكرّرة مع جلسات متزامنة أخرى تعمل بالتوازي على نفس المستودع: أول تحقّق أعاد 104 حراً فاعتُمد، ثم اكتُشف عند إعادة التحقّق مباشرة قبل الإدراج أن 104 **و** 105 **و** 106 صارت جميعها محجوزة خلال دقائق [متاجر «بردايس»/«فريندز»/«ابن الوزير» من جلسات أخرى — الأخيرة وثّقت في سطر لاحق أدناه نفس فئة الحادثة]، فأُعيد الاختيار لـ107 مع تحقّق فوري إضافي أنه لا يزال فارغاً)، وحزمة معرّفات المنتجات **1890001–1890151** (بعد أن تبيَّن أن نطاق 1860001+ المخطَّط أصلاً صار مستخدَماً جزئياً من نفس الجلسات المتزامنة؛ تحقّق نهائي مباشر قبل التنفيذ الفعلي أن كلا المعرّفين لا يزالان فارغين). 🔴 **حادثة كتابة متزامنة متكرّرة أثناء الوصل (نفس فئة الحادثة الموثَّقة في سطر ابن الوزير أدناه):** بعد كل تعديل على `store-slugs.js`، إعادة قراءة فورية عبر `system-reminder` تلقائي كشفت أن الملف **أُعيد كتابته بالكامل** من جلسة أخرى متزامنة (مرتين متتاليتين) بلا إضافتي (`107: "turkwaz-balik-basaksehir"`) — حُلّ بإعادة تطبيق نفس الـ`Edit` فوراً بعد كل رصد حتى استقرت الإضافة (ثبتت أخيراً بعد أن أدرجت الجلسة الأخرى إضافاتها الخاصة `105`/`106` بنفسها في نفس اللحظة، فتعايشت الإضافتان بلا تعارض). **تعقيد إضافي غير مسبوق في هذه الجلسة:** بعد إتمام وصل `app.js`/`index.html`/`sw.js` والتحقّق المحلي الكامل، تبيَّن عبر `git status`/`git log` أن جلسة متزامنة أخرى نفّذت `git commit` محلياً (`2230c022`) شمل بصمت التعديلات الثلاثة **بما فيها إضافاتي** (كانت لا تزال في نفس نسخة العمل المشتركة وقت `git add` الخاص بهم) تحت رسالة التزام لا تذكر تركواز إطلاقاً — تحقّق أن الالتزام **محلي فقط ولم يُدفع بعد** لـ`origin/main` (`git rev-list` أظهر تقدُّماً بالتزام واحد فقط، صفر أثر حي) نفى أي خطر نشر مكسور فعلي؛ الملفات الثلاثة الحرجة استقرت مؤكَّدة بالكود الصحيح رغم ذلك (تحقّق `git show HEAD:app.js`/`index.html` مباشرة يحوي `turkwazStore`/`turkwazProducts`/`turkwazDeliverySettings` و`<script src="/turkwaz-data.js?v=1">` بشكل صحيح)، بينما `store-slugs.js` و`turkwaz-data.js` والصور والسكربتات بقيت **غير مُلتزَمة** (خارج نطاق ذلك الالتزام العرضي). `approval_status='pending'` عمداً (متجر طرف ثالث حقيقي غير مؤكَّد كشراكة دكانجي). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `turkwazDeliverySettings` spread) + `store-slugs.js` (`107: "turkwaz-balik-basaksehir"`) + `<script src="/turkwaz-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع لـSupabase عبر سكربت Node مخصَّص (`scripts/insert-turkwaz-supabase.js`، `SUPABASE_SERVICE_ROLE_KEY` من `.env` المحلي + PostgREST REST API مباشرة؛ Supabase MCP في هذه الجلسة مربوط بمشروع شخصي غير ذي صلة `iseldofsfhwvpfzltqet` فلا فائدة منه لدكانجي)، تحقّق سباق نهائي مباشر قبل التنفيذ الفعلي (المعرّفان 107 ونطاق 1890001+ لا يزالان فارغين) ثم إدراج صف المتجر ومعاملات 151 صف منتج (3 دفعات: 60+60+31، الكل 201). تحقّق شامل عبر preview محلي: `/store/turkwaz-balik-basaksehir` يعرض العنوان الصحيح (`دكانجي - تركواز`)، الوصف، العنوان الحقيقي، ساعات العمل، معدل التوصيل (٢٠ ل.ت/كم)، كل الفئات الاثنتي عشرة بعناصرها الـ151 وأسعارها الصحيحة **من Supabase مباشرة بعد تحميل الكتالوج غير المتزامن** (تحقّق إضافي بعد تأخير 2.5 ثانية أن `products.filter(storeId===107).length === 151` بعد اكتمال `loadCatalogFromSupabase`)، كل الـ153 رابط صورة (151 منتج + شعار + غلاف) أعادت HTTP 200 فعلياً (تحقّق جماعي `fetch HEAD` من صفحة الجلسة الحية)، `/category/restaurants` لا يعرض المتجر إطلاقاً (سلوك `pending` صحيح — تحقّق مباشر `data-id="107"` غائب)، صفر أخطاء كونسول على الصفحتين. `npm run check:versions` ناجح | `turkwaz-data.js` (جديد)، `assets/photos/turkwaz/**` (جديد، 153 ملف: 151 منتج + شعار + غلاف)، `scripts/process-turkwaz-images.js` (جديد)، `scripts/gen-turkwaz-data.js` (جديد)، `scripts/insert-turkwaz-supabase.js` (جديد)، `app.js`, `index.html`, `sw.js` (الثلاثة مُلتزَمة محلياً بالفعل ضمن التزام جلسة أخرى غير مرتبط `2230c022`، غير مدفوعة)، `store-slugs.js` (غير مُلتزَم)، `CLAUDE.md` + صف `stores` id 107 وصفوف `products` 1890001-1890151 في Supabase | نعم — `npm run check:versions` ناجح (app.js/CACHE v320/v240 وقت التحقّق، متأثرة أيضاً بجلسات متزامنة أخرى تعمل على نفس الملفات بالتوازي). **لم يُنفَّذ `git commit`/push للأجزاء المتبقية (`store-slugs.js`, `turkwaz-data.js`, الصور، السكربتات) بعد — بانتظار تعليمات المستخدم؛ `app.js`/`index.html`/`sw.js` ملتزَمة محلياً فقط عرضياً ضمن التزام آخر، لم تُدفع لـ`origin`**
| 2026-07-18 | المستخدم طلب إضافة مطعم «ابن الوزير» (Ebn Alwazeer)، رابط `ebnalwazeer.net`، حي كاياباشي، باشاك شهير، إسطنبول | ميزة إضافة جديدة (لا خطأ سابق) — أول مصدر من نوع موقع **OpenCart** ذاتي البرمجة لهذا المشروع (مختلف عن minidine/qrlist/akillikurye/tillymenu المعتادة) | **الاستخراج:** الموقع بلا API مخفي — لكن صفحات الفئة (`?route=product/category&path=<id>`) وصفحات المنتج (`?route=product/product&product_id=`/الرابط النظيف `/<pid>`) كلها HTML مُصيَّر خادمياً عادياً، فاستُخرج كل شيء بحلقة `fetch` من نفس التبويب (بلا حاجة أي أداة تصفّح تفاعلي): 10 فئات → 91 صفاً خاماً → صور 550×550 من صفحة كل منتج. **بدّل الموقع لغته للعربية أولاً** (كان افتراضياً إنجليزياً وترجم اسم فئة «وجبات غربية» آلياً إلى "Watches" — خطأ ترجمة آلية مضلِّل، حُلّ بالتبديل لواجهة عربية أصلية بدل الاعتماد على النص الإنجليزي). **رقم واتساب الحقيقي لم يكن ظاهراً كنص** — زر واتساب في الترويسة رابط قصير `api.whatsapp.com/message/<id>` (لا رقم في الرابط نفسه)؛ فُتح الرابط في تبويب منفصل وقُرئ الرقم الحقيقي من `href` الفعلي لزر «Continue to WhatsApp Web» (`wa.me/send?phone=905555535040`) — **تأكيد إضافي حاسم عبر Google Places:** بحث نصي بالاسم أعاد نتيجة واحدة فقط «مطعم إبن الوزير» برقم هاتف مطابق **حرفياً** لرقم واتساب المُستخرَج (`0555 553 50 40`) وموقع إلكتروني مطابق (`ebnalwazeer.net`) — تقييم حقيقي 4.2 (846 تقييماً)، `place_id`: `ChIJpcVfgMGvyhQR6z2RnQGdCr4`. صفحة «About Us» في الموقع كانت تحمل بريد/رقم مختلفَين تماماً (`turkeyxpress@gmail.com`, `+905342303170`) — تُرك دون استخدام لأنه محتوى قالب متبقٍّ من منشئ الموقع نفسه (اسم النطاق مختلف تماماً)، وليس بيانات المطعم الحقيقية. **دمج منتجات مزدوجة الفئة حقيقي واحد فقط:** 91 صنفاً خاماً استُبعد منها 3 (فئة «المشروبات» بالكامل: عيران/ماء/ميرندا) لأن الثلاثة تستخدم شعار المتجر نفسه كصورة placeholder عامة بدل صورة منتج حقيقية (يطابق سياسة عدم نشر منتج بصورة عامة/مكرَّرة). **تحقّق دقيق قبل حذف أي "تكرار" ظاهري:** عدة أزواج منتجات تشارك نفس الصورة عبر فئتين (مثال: «أصبع شاورما» 120 ل.ت و«سندويش شاورم 75 g» 175 ل.ت يتشاركان صورة واحدة) — هذه **ليست تكرارات** بل أحجام/أوزان مختلفة حقيقية لنفس الطبق بصورة تمثيلية واحدة (نفس نمط موثَّق سابقاً في جلسة فاتح الخير)؛ الدمج اقتصر فقط على تطابق **الاسم+السعر+الصورة معاً بالضبط** (منتج واحد حقيقي: «مندي شاورما» 350 ل.ت ظهر حرفياً مرتين بمعرّفين مختلفين تحت فئتي «وجبات عربية» و«شاورما» — أُبقي مرة واحدة فقط). المجموع النهائي **87 منتجاً فريداً عبر 8 فئات حقيقية** (وجبات عربية 5، وجبات غربية 17، سندويش غربي 17، شاورما 16، بيتزا 10، مقبلات 12، مقبلات ساخنة 6، صوصات 4). **الشعار والغلاف من صور الموقع الحقيقية لا اختلاق:** شعار دائري عنّابي/ذهبي حقيقي (`logo/55555-*.png`) استُخدم كما هو مع حشوة شفافة لمربع 400×400؛ الغلاف بانر تسويقي حقيقي من سلايدر الصفحة الرئيسية (1600×900، يحمل اسم العلامة «Ebn Alwazeer» + «منذ 1995» + شعار «بيحكي عن حالو») — تحقّق مسبق بمحاكاة `sharp` لكلا نسبتي القصّ الحيّتين (هيرو 4:1 وبطاقة ~1.66:1) أن كل النص يبقى ظاهراً بلا بتر قبل الاعتماد. المتجر رقم **106** (تحقّق `max(id)` مرتين مع تصادمين حيّين فعليين أثناء العمل بفعل جلسات متزامنة: أول تحقّق أعاد 103 محجوزاً، ثاني تحقّق أعاد 104 و105 محجوزين أيضاً خلال دقائق قليلة — نفس نمط الوكالة الموثَّق سابقاً)، وحزمة معرّفات المنتجات **1880001–1880087** (بعد أن تبيَّن أن نطاق 1860001+ المخطَّط أصلاً صار محجوزاً بالكامل من نفس الجلسات المتزامنة؛ تحقّق نهائي مباشر قبل الكتابة الفعلية أن كلا المعرّفين لا يزالان فارغين). 🔴 **حادثة كتابة متزامنة حقيقية أثناء الوصل (وليس فقط تصادم معرّفات):** بعد إتمام تعديل `app.js`/`index.html`/`sw.js` بنجاح، إعادة قراءة لاحقة كشفت أن الثلاثة **أُعيد كتابتها بالكامل من طرف جلسة/جلسات أخرى** لحظة العمل، مما محا إضافاتي (سطر `ibnalwazeer-data.js` ورفع رقم `app.js`) ومحا أيضاً إضافات جلسة أخرى غير مرتبطة («فريندز» id 105) في نفس اللحظة — تكرر هذا **3 مرات متتالية** خلال دقائق (كل مرة أُعاد رصدها بمقارنة `grep` مباشرة بعد كل تعديل، لا افتراض نجاح)؛ حُلّت نهائياً بإعادة تطبيق التعديلات الثلاثة (سطر السكربت + رفع رقمي `app.js`/`store-slugs.js`) مرة أخيرة فوق أحدث حالة على القرص حتى استقرت وتحقّق `npm run check:versions` ناجحاً — درس تشغيلي جديد يُضاف لملف [[concurrent-session-hazard]]: في فترات الذروة (عدة جلسات تُضيف متاجر بالتوازي)، التحقّق الواحد بعد التعديل لا يكفي؛ يلزم إعادة `grep` فورية بعد كل عملية Edit للتأكد أن التعديل نجا من إعادة كتابة لاحقة قبل الانتقال للخطوة التالية. `approval_status='pending'` عمداً (متجر طرف ثالث حقيقي غير مؤكَّد كشراكة دكانجي). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`106: "ibn-alwazeer-basaksehir"`) + `<script src="/ibnalwazeer-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع لـSupabase عبر سكربت Node مخصَّص (`SUPABASE_SERVICE_ROLE_KEY` من `.env` المحلي + PostgREST REST API مباشرة؛ Supabase MCP في هذه الجلسة مربوط بمشروع شخصي غير ذي صلة `iseldofsfhwvpfzltqet` فلا فائدة منه لدكانجي — نفس العطل الموثَّق في جلسات أخرى بنفس اليوم)، تحقّق سباق نهائي مباشر قبل التنفيذ الفعلي (المعرّفان لا يزالان فارغين) ثم إدراج صف المتجر ومعاملة 87 صف منتج. تحقّق شامل عبر preview محلي: `/store/ibn-alwazeer-basaksehir` و`/store/106` يعرضان العنوان الصحيح (`دكانجي - مطعم ابن الوزير`)، الوصف، الشعار والغلاف (HTTP 200)، كل الفئات الثمانية بعناصرها الـ87 وأسعارها الصحيحة من Supabase (87/87 مطابقة تماماً)، كل الـ89 رابط صورة (87 منتج + شعار + غلاف) أعادت HTTP 200 فعلياً (تحقّق جماعي `fetch HEAD`)، `/category/restaurants` لا يعرض المتجر إطلاقاً (سلوك `pending` صحيح — تحقّق مباشر `data-id="106"` غائب)، الصفحة الرئيسية بلا أي أثر، صفر أخطاء كونسول على الصفحات الثلاث. `npm run check:versions` ناجح | `ibnalwazeer-data.js` (جديد)، `assets/photos/ibnalwazeer/**` (جديد، 89 ملف: 87 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 106 وصفوف `products` 1880001-1880087 في Supabase | نعم — `npm run check:versions` ناجح (app.js→319، store-slugs.js→257، CACHE→239، أرقام متأثرة أيضاً بجلسات متزامنة أخرى تعمل على نفس الملفات بالتوازي). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-18 | المستخدم طلب إضافة مطعم «فريندز» (Friends Restaurant & Cafe) من رابط `tillymenu.com/180/friendstr`، مركز أفجلار، إسطنبول | ميزة إضافة جديدة (لا خطأ سابق) — **أكبر كتالوج يُضاف حتى الآن (554 منتجاً عبر 30 فئة)**، أضعاف حجم أكبر إضافة سابقة (بيت حلب 162). tillymenu.com مصدر جديد لهذا المشروع: `GET /backend/public/api/clients/<client_id>` (الـid يُستخرج من طلبات الشبكة الأولى عند فتح الصفحة، هنا `180`) يُرجع **كل شيء دفعة واحدة** بلا حاجة لأي تصفّح/نقر تبويبات — اسم/هاتف/واتساب/مدينة/فئات/منتجات/خيارات كائن JSON واحد ضخم (494KB)؛ 554/554 صنفاً بصورة حقيقية (صفر امتناع)، 14 صنفاً فقط (فئة Pizza بالكامل) لها أحجام حقيقية متعددة (`options[].length>1`) تُبنى بمخطط `{name,values,extra}` القياسي، البقية صنف واحد بسعر ثابت. **تحدٍّ الترجمة:** أسماء الأصناف كانت مزيجاً حقيقياً من تركي/إنجليزي/عربي (أدخلها التاجر نفسه بلا نمط ثابت) — بدل ترجمة كل الـ554 يدوياً بالتتابع (مستهلك جداً للسياق)، قُسِّم الكتالوج لـ6 دفعات حسب الفئات وفُوِّض لـ6 وكلاء Agent مستقلين بالتوازي (كل واحد بقاموس مصطلحات عربي-تركي مشترك لضمان اتساق الترجمة عبر الدفعات، مثل döner=دونر، izgara=مشوي، kaşarli=بالجبنة)، كل وكيل كتب ناتجه لملف JSON مستقل؛ تحقّق برمجي لاحق أن كل الـ554 معرّفاً ظهرت مرة واحدة بالضبط في كل دفعة (صفر فقدان). **تحقّق جودة الترجمة غير المتوقَّع:** أسماء ملفات صور المصدر نفسها كانت غالباً وصفية بالعربية (مثال: `وجبة-كوسا-بلبن-مع-رز.jpg` لصنف "Süt ve pirinç ile kabak")، فأعطت تصالباً مستقلاً أثبت صحة الترجمات المُولَّدة بلا حاجة لمراجعة كل صنف يدوياً. **مطابقة Google Maps مؤكَّدة بقوة استثنائية عبر نص التقييمات نفسه** (وليس فقط الاسم/الموقع): مراجعة عميل ذكرت حرفياً "friends special" بيتزا XL بالجبنة والدجاج المسلوق والفلفل الأخضر (يطابق صنف "FRİENDS pizza" الحقيقي تماماً)، ومراجعة أخرى ذكرت "منسف اردني جميد" (يطابق صنف "Özel ürdün Mansaf" الحقيقي) — حسم هذا الشك رغم اختلاف طفيف بين رقم هاتف واتساب المصدر (`905300405915`) ورقم غوغل المسجَّل (`0530 040 53 26`، فرق آخر 4 أرقام فقط)؛ الشك انحل تماماً عند العثور على بانر Google-Business الرسمي للمطعم (صورة مرفوعة من حساب "Friends Restaurant & Cafe" نفسه) يُظهر **كلا الرقمين معاً** كرقمي طلب/شكاوى منفصلين — لا خطأ، رقمان حقيقيان لقناتين مختلفتين (نفس نمط سابقة قلعة الشام/بردايس). هذا البانر (شعار ذهبي + اسم عربي/إنجليزي + قائمة تصنيفات + شعار "قعدة حلوة والاكل من القلب") اعتُمد كغلاف المتجر مباشرة (أفضل من أي صورة طبق عشوائية) بعد محاكاة قصّ فعلية بـ`sharp` على نسبة 16:9 (تأكيد أن كل العناصر المهمة تبقى ظاهرة). الشعار مأخوذ من صورة ملف تعريف واتساب بزنس نفسها (مربعة 1600×1600، نفس الهوية الذهبية). ساعات العمل ودرجة التقييم (3.4/653) من Google Places API الحقيقي (يومياً 10ص–1ص). 🔴 **اكتشاف تشغيلي مهم:** موصل Supabase MCP في هذه الجلسة مربوط بمشروع Supabase شخصي مختلف تماماً (`iseldofsfhwvpfzltqet`، لا يملك جداول `stores`/`products` إطلاقاً) وليس مشروع دكانجي الحقيقي (`tzcqnqzltrjemdnkzpzn`) — اكتُشف مستقلاً قبل رؤية توثيق سابق لنفس العطل في نفس اليوم (بردايس)؛ حُلَّ بنفس الطريقة: قراءة `SUPABASE_SERVICE_ROLE_KEY` من `.env` المحلي والكتابة مباشرة عبر PostgREST REST API. **تصادم معرّفات حي حقيقي أثناء العمل:** أول فحص `max(id)` أعاد متجر 102/منتج 1840059، لكن فحصاً تالياً بعد ثوانٍ كشف أن جلسة متزامنة أخرى أضافت فعلاً المتجرين 103 و104 (بمنتجات حتى 1860171) بينهما — أُعيد فحص `max(id)` **مباشرة قبل كل إدراج** (المرة الأخيرة قبل التنفيذ الفعلي مباشرة)، فاعتُمد أخيراً المتجر **105** وحزمة معرّفات المنتجات **1870001–1870554** (تحقّق صفر تصادم في النطاق قبل الكتابة). صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82 mozjpeg، شعار 400×400 PNG، غلاف 1280×720 JPEG q85). `approval_status='pending'` عمداً (متجر طرف ثالث حقيقي غير مؤكَّد كشراكة دكانجي). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread — تداخل مع تعديل متزامن أضاف متجر 106 "ابن الوزير" لنفس الأسطر أثناء العمل، اندمج الاثنان بلا تعارض) + `store-slugs.js` (`105: "friends-restaurant-avcilar"`) + `<script src="/friendstr-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع عبر REST مباشر (`service_role`، صف المتجر ثم 554 صف منتج على 6 دفعات من 100). تحقّق شامل عبر preview محلي: `/store/friends-restaurant-avcilar` يعرض العنوان الصحيح (`دكانجي - فريندز`) والوصف والفئات الثلاثين بعناصرها الصحيحة (554 من 554 منتجاً)، عيّنة عشوائية من روابط الصور (شعار/غلاف/عدة منتجات موزّعة) كلها HTTP 200 فعلياً، صنف البيتزا يعرض السعر الأساسي الصحيح (250 ل.ت لأصغر حجم)، `/category/restaurants` لا يعرض المتجر إطلاقاً (سلوك `pending` صحيح)، صفر أخطاء كونسول. لم يُختبَر فتح نافذة تفاصيل المنتج (modal) بالنقر الحي بنجاح كامل (عطل تفاعل في بيئة المتصفح الخاصة بالجلسة، غير متعلق ببيانات المتجر — نفس فئة أعطال screenshot/interaction الموثَّقة سابقاً)؛ الثقة في مخطط `options` مبنية على تطابق حرفي تام مع نفس المخطط المستخدَم فعلاً وحيّاً في متجر باشا بيتزريا. `npm run check:versions` ناجح | `friendstr-data.js` (جديد)، `assets/photos/friendstr/**` (جديد، 556 ملف: 554 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 105 وصفوف `products` 1870001-1870554 في Supabase | نعم — `npm run check:versions` ناجح (app.js →317، store-slugs.js →255، CACHE →237، أرقام متأثرة أيضاً بجلسات متزامنة أخرى). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-18 | المستخدم طلب إضافة مطعم «بردايس» (Paradice) من رابط `paradice.miniandmore.co/ar/categories/?branch=paradice`، حي مولا كوراني، الفاتح، إسطنبول | ميزة إضافة جديدة (لا خطأ سابق) — ثاني مصدر من عائلة **minidine/miniandmore** بعد فلوكا (2026-07-12)، أكّد أن الوصفة الموثَّقة تعمم جيداً: `customer_name` (هنا `"paradice"`، مطابق للـslug هذه المرة خلافاً لفلوكا) يُستخرج من `customerName:"..."` داخل حزمة JS المصغَّرة (`_next/static/chunks/*.js`)، ثم `POST auth0.miniandmore.co/clients/token` (multipart، حقول `customer_name`/`service=qrmenu`/`origin_url`) يتطلب أيضاً ترويسة `Origin`/`Referer` صريحة هذه المرة (رفض الطلب برسالة مضلِّلة "origin_url field is required" رغم إرسال الحقل فعلياً حتى أُضيفت الترويستان) قبل أن يُرجع `access_token`/`url`/`storage_url`. القائمة **مسطّحة بالكامل بلا أي خيارات/إضافات** (`modifiers`/`attributes` فارغة على كل الـ258 صنفاً — تحقّق برمجي شامل قبل البناء بدل افتراض ذلك). **اكتشاف مفيد: فئات بلا صور تُستبعَد تلقائياً بصفر تدخّل يدوي** — 4 فئات كاملة (اراكيل/أرجيلة 10 أصناف، سموذي 8، موهيتو 7، كريب 4 = 29 صنفاً) كانت **صفر منها بصورة حقيقية** في كتالوج التاجر نفسه، فحُذفت طبيعياً عبر `applyPublishingRules` (imageless auto-hidden) بلا حاجة لأي قرار يدوي بشأن استبعاد الأرجيلة (كان سيُثار كسؤال سياسة كالمقارنة مع استبعاد الكحول في حلاب 1881، لكن غياب الصور حسم الأمر تلقائياً). من أصل 258 صنفاً، **171 فقط بصورة حقيقية فريدة** (صفر تكرار مسارات — تحقّق MD5/اسم ملف قبل البناء) عبر 21 فئة حقيقية. صور المنتجات على `storage.de.cloud.ovh.net/v1/AUTH_.../minidine-prod/<customer_name>/items/<image.image>` (بلا مصادقة، رابط مباشر). لا شعار في حزمة الـstorage — الشعار الفعلي وُجد كصورة `og:image` على الموقع نفسه (`paradice.miniandmore.co/seo.png`، تصميم نخيل/شمس/بحر احترافي عالي الدقة). لا بانر غلاف مخصَّص في المصدر، فاعتُمدت صورة منتج حقيقية («ميكس مقبلات» — صحن مقبلات باردة عريض 1600×899 يُظهر أيضاً صندوق قائمة العلامة التجارية «بردايس» في الخلفية) بعد **محاكاة فعلية للقصّ بـ`sharp`** على نسبة الهيرو 4:1 (وليس تخميناً بصرياً) للتأكد أن المحتوى الأساسي يبقى مرئياً وسط الإطار قبل الاعتماد. **مطابقة Google Maps مؤكَّدة بدقة عالية جداً:** حلّ رابط `goo.gl/maps/...` المُضمَّن في بيانات إعداد الموقع نفسه (`contact_info.google_map_url`) أعاد اسم المكان حرفياً «مطعم وكافيه بردايس» (يطابق `pageTitle` في حزمة JS المصدر تماماً)، ثم `findplacefromtext` بنفس الاسم قرب نفس الإحداثيات أعاد تطابقاً تاماً (تقييم 4.5، 1391 تقييماً — من أعلى تقييمات المتاجر المضافة حتى الآن)، ساعات عمل شبه يومية موحّدة (6:00 ص – 2:30 ص، فرق طفيف 30 دقيقة في الثلاثاء أُهمل كتفصيل غير جوهري بنفس سياسة سوابق مشابهة). 🔴 **رقم هاتف Google (`0545 556 75 55`) يختلف عن رقم الطلب الفعلي في واجهة القائمة الإلكترونية نفسها (`+90 555 110 13 26`)** — اعتُمد رقم القائمة (قناة الطلب الحقيقية) للحقلين `phone`/`whatsapp`، بنفس سياسة قلعة الشام (2026-07-14): رقمان حقيقيان مختلفان لقناتين مختلفتين، لا خطأ. عنوان Google (`Molla Gürani, Turgut Özal Millet Cd. No:16 A`) اختلف عن عنوان الشارع الثابت في إعدادات موقع القائمة نفسها (`Sadi Çeşmesi Sk. No:29`) — اعتُمد عنوان Google المؤكَّد بالإحداثيات بدل نص الإعداد الثابت المحتمل تقادمه. **موصل Supabase MCP في هذه الجلسة مربوط بمشروع شخصي مختلف تماماً** (`iseldofsfhwvpfzltqet`، نفس عطل بيغ طاووق/عبد الحميد/ليالي شامية في نفس اليوم) — استُخدم نفس الحل الموثَّق: سكربت Node بمفتاح `SUPABASE_SERVICE_ROLE_KEY` من `.env` المحلي يكتب مباشرة عبر PostgREST REST API لمشروع دكانجي الحقيقي (`tzcqnqzltrjemdnkzpzn`)، مع تحقّق `max(id)` وتصادم النطاق **مباشرة قبل التنفيذ** (لا تصادم هذه المرة: 103 مأخوذ من جلسة متزامنة أخرى «بروستد عبد الحميد»، فاعتُمد **104** لصفّ المتجر وحزمة معرّفات المنتجات **1860001–1860171**). صُغِّرت الصور بـ`sharp` (منتجات ≤800×800 JPEG q82 mozjpeg، شعار 400×400 PNG، غلاف 1280px عرضاً JPEG q85 mozjpeg). وحدة القياس (`unit`) لكل صنف أُخذت حرفياً من حقل `measurement_unit.unit_name` الحقيقي في استجابة الـAPI نفسها (غالبيتها «قطعة»، 7 أصناف «صحن»، صنف واحد «كغ») بدل اختراع وحدات مختلفة حسب الفئة — أمانة للبيانات المصدرية. `approval_status='pending'` عمداً (متجر طرف ثالث حقيقي غير مؤكَّد كشراكة دكانجي). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`104: "paradice-fatih-restaurant"`) + `<script src="/paradice-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`، بلا صور فردية مُدرَجة — نفس نمط المتاجر الحديثة كثيرة الصور). **تحقّق تطابق برمجي كامل بعد الدفع** (مقارنة كل الحقول الخمسة الأساسية لكل صف بين الملف المحلي والقاعدة): 171/171 صف مطابق تماماً، صفر انحراف. تحقّق شامل عبر preview محلي: `/store/paradice-fatih-restaurant` يعرض العنوان الصحيح (`دكانجي - مطعم وكافيه بردايس`)، الوصف، العنوان الحقيقي، ساعات العمل، الهاتف، كل الفئات الـ21 بعناصرها الصحيحة (171 من 171 منتجاً، عدّاد كل فئة مطابق تماماً لحساب الاستبعاد)، كل الـ173 رابط صورة (171 منتج + شعار + غلاف) أعادت HTTP 200 فعلياً (تحقّق جماعي `fetch HEAD` من صفحة المتجر الحية)، `/category/restaurants` لا يعرض المتجر إطلاقاً (سلوك `pending` صحيح — تحقّق مباشر `approvalStatus==='pending'` و`data-id="104"` غير موجود في DOM)، الصفحة الرئيسية بلا أي أثر، صفر أخطاء كونسول على الصفحات الثلاث (لقطات الشاشة تعطّلت بمهلة — عطل بيئي معروف موثَّق سابقاً — فاعتُمد التحقق النصي/DOM/شبكة/كونسول بدلاً منها). `npm run check:versions` ناجح | `paradice-data.js` (جديد)، `assets/photos/paradice/**` (جديد، 173 ملف: 171 منتج + شعار + غلاف)، `scripts/insert-paradice-supabase.js` (جديد)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 104 وصفوف `products` 1860001-1860171 في Supabase | نعم — `npm run check:versions` ناجح (app.js v314→315، store-slugs.js v252→253، CACHE v235→236). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-18 | المستخدم طلب إضافة مطعم «عبد الحميد طاووق» (بروستد عبد الحميد / Abdulhamit Tavuk) من رابط قائمة إلكترونية (`amenu.net/restaurant363`)، باشاك شهير، إسطنبول | ميزة إضافة جديدة (لا خطأ سابق) — أول مصدر من نوع **amenu.net** لهذا المشروع؛ خلافاً لكل مصادر QR-menu السابقة (qrlist/minidine/akillikurye/finedinemenu) لا يوجد أي API خفي هنا: القائمة الكاملة (52 صنفاً عبر 5 فئات: Yemekler الوجبات، Sandviç السندويش، Salatalar السلطات، Mezeler المقبلات، مشروبات içicekler) مُصيَّرة بالكامل كـHTML ثابت في DOM من أول تحميل، فاستُخرجت مباشرة عبر تكرار DOM (كل بطاقة = عنصر فئة `span` + اسم `h1` + وصف `p` اختياري + سعر `h2` + صورة `img` بترتيب ثابت) بلا أي تمرير/انتظار/تصفّح تفصيلي. 🔴 **موصل Supabase MCP في هذه الجلسة مربوط بمشروع Supabase شخصي مختلف تماماً** (`iseldofsfhwvpfzltqet`, «goldengttr-hub's Project»، فشل فوري `relation "stores" does not exist`) — نفس العطل الذي واجهته جلستا «ليالي شامية»/«بيغ طاووق» في نفس اليوم بالضبط، فاستُخدم نفس الحل الموثَّق: سكربتات Node تقرأ `SUPABASE_SERVICE_ROLE_KEY` من `.env` المحلي وتكتب مباشرة عبر PostgREST REST API لمشروع دكانجي الحقيقي (`tzcqnqzltrjemdnkzpzn`). 🔴 **علة برمجية حقيقية اكتُشفت في سكربت التوليد الخاص بي ذاتي (وليس في المصدر):** أول نسخة من `scripts/gen-abdulhamit-data.js` بنت نص ملف `abdulhamit-data.js` بأكمله (مصفوفة `lines`) بشكل *فوري (eager)* عند تحميل الوحدة (top-level)، قبل أن يُسنَد اسم ملف الصورة (`item._file = 'pN'`) لكل عنصر من سكربت التنزيل اللاحق `process-abdulhamit-images.js` — فالنتيجة أن كل الـ52 مساراً كُتبت حرفياً كـ`/assets/photos/abdulhamit/undefined.jpg` رغم أن الصور الفعلية نُزِّلت وسُمِّيت بشكل صحيح تماماً على القرص (p1.jpg…p52.jpg)، وتسرّب هذا الخلل فعلياً إلى صفوف `products.image` في Supabase عبر الإدراج الأول قبل أن يُكتشف — تحقّق preview محلي أظهر «0 من 0 منتجاً» (المنتجات موجودة في القاعدة لكن التصنيف/الفلترة تعتمد على وجود صورة صالحة) رغم صحة العنوان/الوصف/العنوان الحقيقي/الشعار/الغلاف. **الإصلاح الجذري:** نقل إسناد `item._file = 'p' + (i+1)` (الترقيم دائماً حسب ترتيب الاستخراج الأصلي من DOM، مطابق تماماً لترتيب التنزيل) إلى أعلى ملف التوليد مباشرة بعد تعريف المصفوفة (قبل أي بناء لنص الملف)، فأصبح بناء الأسطر يقرأ القيمة الصحيحة دائماً بصرف النظر عمّن يستدعي `writeFile()` ومتى. أُعيد توليد `abdulhamit-data.js` بمسارات صحيحة (`p1.jpg`…`p52.jpg`) ثم أُصلحت الـ52 صفاً المتضرّرة في Supabase مباشرة عبر سكربت `PATCH` مستقل (لا حاجة لإعادة الإدراج الكامل) — **درس عام يستحق التعميم:** أي سكربت توليد يبني نص ملف اعتماداً على حقول تُسنَد لاحقاً من سكربت آخر يجب أن يبني ذلك النص *كسولاً (lazily)* داخل دالة تُستدعى بعد اكتمال كل الإسنادات، لا فورياً عند التحميل. مطابقة Google Maps مؤكَّدة بدقة عالية جداً: بحث نصي بالاسم قرب إحداثيات رابط Apple Maps المُضمَّن في الموقع نفسه (41.108003, 28.789488) أعاد تطابقاً حرفياً كاملاً لرقم واتساب (`+90 531 361 12 88`)، الاسم الرسمي «بروستد عبد الحميد Abdulhamit tavuk»، تقييم حقيقي مرتفع 4.5 (818 تقييماً)، وساعات عمل يومية موحّدة تقريباً على مدار الساعة (00:00–23:00) من `opening_hours.weekday_text`. الأسماء العربية للمنتجات استُخرجت من الشطر العربي لكل تسمية ثنائية اللغة (تركي/عربي) الموجودة أصلاً في مصدر القائمة نفسه (نفس نمط مستخدَم سابقاً)، والأوصاف تُرجمت بأمانة من النص التركي المصدري (أو أُبقيت كما هي حين كانت عربية أصلاً في المصدر). لا يوجد بانر تسويقي مخصَّص في المصدر (فقط خلفية عامة `bannerbackground.webp` مشتركة لكل متاجر amenu.net)، فاستُخدمت صورة منتج حقيقية عالية الجودة («فروج مشوي كامل») كغلاف بدلاً منه — نفس نمط متاجر سابقة بلا بانر مخصَّص. المتجر رقم **103** (تحقّق `max(id)`=102 مباشرة قبل الإدراج، صفر تصادم رغم أن معرّفي 101/102 استُخدما في نفس اليوم من جلستين متزامنتين أخريين)، حزمة معرّفات المنتجات **1850001–1850052** (تحقّق صفر تصادم في النطاق). صُغِّرت الصور بـ`sharp` (منتجات ≤800×800 JPEG q82 mozjpeg، شعار 400×400، غلاف ≤1280px q85). `approval_status='pending'` عمداً (متجر طرف ثالث حقيقي غير مؤكَّد كشراكة دكانجي). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`103: "abdulhamit-tavuk-basaksehir"`) + `<script src="/abdulhamit-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). تحقّق شامل عبر preview محلي (منفذ مستقل 4791 لتفادي تعارض مع خادم جلسة متزامنة أخرى كان يعمل على منفذ آخر لنفس المجلد): `/store/abdulhamit-tavuk-basaksehir` يعرض العنوان الصحيح (`دكانجي - بروستد عبد الحميد`)، الوصف، العنوان الحقيقي، ساعات العمل، الهاتف، كل الفئات الخمس بعناصرها الصحيحة (22+13+4+7+6=52 مطابقة تماماً)، بعد إصلاح علة `_file`: «52 من 52 منتجاً» ظاهرة بأسعارها الصحيحة، كل الـ54 رابط صورة (52 منتج + شعار + غلاف) أعادت HTTP 200 فعلياً (تحقّق جماعي `fetch HEAD`)، الصفحة الرئيسية بلا أي أثر للمتجر (سلوك `pending` صحيح)، صفر أخطاء كونسول. `npm run check:versions` ناجح | `abdulhamit-data.js` (جديد)، `assets/photos/abdulhamit/**` (جديد، 54 ملف: 52 منتج + شعار + غلاف)، `scripts/gen-abdulhamit-data.js` (جديد)، `scripts/process-abdulhamit-images.js` (جديد)، `scripts/insert-abdulhamit-supabase.js` (جديد)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 103 وصفوف `products` 1850001-1850052 في Supabase | نعم — `npm run check:versions` ناجح (app.js v313→314، store-slugs.js v251→252، CACHE v234→235). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-18 | المستخدم طلب إضافة مطعم «ليالي شامية البوسفور» من رابط قائمة إلكترونية (`qr.finedinemenu.com/layali-shamiya-bosphorus/...`) + هاتف (05526621119) | ميزة إضافة جديدة (لا خطأ سابق) — أول مصدر من نوع **finedinemenu.com** لهذا المشروع، وأول تصادم حي فعلي حصل من **جهة المتجر الآخر** لا من جهتي (راجع السطر التالي أدناه لمتجر «بيغ طاووق» الذي اصطدم بمعرّفي 101 بعد إدراجي مباشرة، فأُعيد اختياره تلقائياً لـ102) | **مصدر finedinemenu.com (React SPA):** لا حاجة لأي تتبّع API/WebSocket خفي — كامل القائمة (18 فئة رئيسية + 4 فئات فرعية تحت «المقبلات والشوربات والسلطات»، 197 صفاً خاماً) تُصيَّر بالكامل في DOM ويمكن استخراجها نصياً دفعة واحدة عبر `get_page_text`/`document.body.innerText`، والفئة الحقيقية لكل صنف تُستنتج من التمشّي التسلسلي عبر `TreeWalker` على عنصري العنوان المميَّزين بصنف CSS محدَّد (`text-xl font-bold text-center text-primary lg:text-start lg:w-full` للفئة الرئيسية، `text-start text-lg font-semibold ... text-highlight_color` للفئة الفرعية) — أدق وأوثق من افتراض الترتيب. قسم «الأكثر إعجابًا» (5 عناصر أولى، `main=null` قبل أي عنوان فئة حقيقي) هو تكرار كامل لعناصر موجودة أصلاً في فئاتها الحقيقية — استُبعد تلقائياً بفلتر `main !== null` (بلا فقدان بيانات، كل الخمسة لها نظير حقيقي لاحق). **صور المنتجات على وكيل صور `imgproxy` حقيقي يقبل أي مقاس URL مباشرة** (`media.finedinemenu.com/filters:strip_exif()/filters:format(webp)/<WxH>/<hash>.jpeg`) — إعادة كتابة الحجم في الرابط (`900x0` بدل `104x78`/`592x0` الأصليين، وحذف `filters:blur(125)` من الروابط التي لم يكتمل تحميلها الحقيقي بعد في المتصفح) تُرجع نفس الصورة الحقيقية بدقة أعلى مباشرة عبر `curl`/`fetch` عاديين بلا أي حاجة لتصفّح/تنزيل عبر المتصفح — أول مصدر يسمح بذلك مباشرة دون قيود CORS أو سقف تنزيلات. **4 منتجات استُبعدت لغياب صورة حقيقية تماماً** (لا رابط `<img>` إطلاقاً حتى بعد تمرير حقيقي والتحقّق المباشر من العنصر): بطاطا مقلية، سرفيس خضرا، صحن مخلل، كبسة لحمة — 188 منتجاً فريداً من أصل 197 صفاً خاماً نُشر. **لا شعار ولا غلاف تسويقي في مصدر القائمة نفسه** (فقط اسم نصي في الهيدر، صفر عناصر `background-image` في كامل الصفحة) — عُوِّض بمصدرين مختلفين: (1) **شعار مُصمَّم يدوياً بـ`sharp`+SVG** (دائرة خضراء داكنة + حدّ ذهبي + زخرفة زهرة + نص عربي «ليالي شامية / البوسفور» بذهبي/أبيض) بدل توليد بالذكاء الاصطناعي — قرار متعمَّد لتفادي مخاطر تشويه الحروف العربية المولَّدة (موثَّقة سابقاً)، مع تحقّق بصري مباشر لنص SVG قبل الاعتماد (Arial يدعم العربية بشكل صحيح على بيئة التنفيذ). (2) **غلاف من صورة Google Places حقيقية موثَّقة** (واجهة المبنى ليلاً، لافتة «LAYALI SHAMI... RESTAURANT» مضاءة بوضوح) بدل صورة منتج — نفس نمط استخدام صورة حقيقية بديلة عن بانر تسويقي غائب، لكن هذه المرة من واجهة المبنى نفسه بدل طبق طعام لتعزيز التعرّف على العلامة. **مطابقة Google Maps بثقة قصوى نادرة:** بحث نصي بالاسم أعاد **تطابقاً حرفياً كاملاً للعنوان** (`Yavuz Sultan Selim, Abdülezelpaşa Cd. No:31, 34083 Fatih/İstanbul` — مطابق حرفاً بحرف لعنوان المصدر نفسه المُستخرَج من نافذة «معلومات» داخل القائمة) والاسم العربي الرسمي «مطعم ليالي شامية البوسفور»، تقييم حقيقي مرتفع 4.6 (2496 تقييماً)، ساعات عمل موحّدة يومياً (11:30 ص – 2:00 ص عبر كل أيام الأسبوع السبعة بلا استثناء) من `opening_hours.weekday_text`. المتجر رقم **101** (تحقّق `max(id)`=100 مباشرة قبل التنفيذ، ثم إعادة تحقّق فورية بعد الإدراج أن الصف لا يزال 101 باسمي أنا تحديداً — راجع حادثة التصادم أدناه)، حزمة معرّفات المنتجات **1830001–1830188** (تحقّق صفر تصادم في النطاق قبل وبعد الإدراج). 🔴 **موصل Supabase MCP في هذه الجلسة مربوط بمشروع Supabase شخصي مختلف تماماً** (`iseldofsfhwvpfzltqet`, «goldengttr-hub's Project») لا علاقة له بدكانجي إطلاقاً (فشل فوري: `relation "stores" does not exist`) — اكتُشف ذلك فوراً بمحاولة استعلام تجريبي أول الجلسة، فاستُبدل بالكامل بسكربتات Node تقرأ `SUPABASE_SERVICE_ROLE_KEY` من `.env` المحلي وتكتب مباشرة عبر PostgREST REST API لمشروع دكانجي الحقيقي (`tzcqnqzltrjemdnkzpzn`, مقروء من `supabase-config.js`) — نفس الحل الذي استخدمه أيضاً متجر «بيغ طاووق» في نفس اليوم (سطر تالٍ أسفله) لأنه واجه نفس العطل بالضبط. **🔴 تصادم `store-slugs.js` حي حقيقي أثناء العمل (وليس على مستوى Supabase هذه المرة):** بعد إدراج صفّي (Supabase، id 101) بنجاح، اكتُشف عبر `git diff` أن جلسة متزامنة أخرى كانت قد أضافت سطراً غير محفوظ `101: "bigtavuk-fatih-restaurant"` في نفس الملف **قبل** أي إدراج فعلي لها في Supabase (تأكَّد بالاستعلام: لا صف بهذا الـslug في القاعدة) — أي أن سطرها كان مجرَّد حجز مسبق من طرف العميل بلا سند قاعدة بيانات، بينما صفّي أنا كان الإدراج الفعلي الفائز فعلاً. طُبِّقت نفس قاعدة «الفائز بالإدراج الفعلي يحتفظ بالمعرّف» الموثَّقة سابقاً في سجلّ المشروع: تُركت مهمة تصحيح الفارق للجلسة الأخرى (التي بالفعل صحَّحت نفسها ذاتياً لاحقاً إلى `102` بعد إعادة تحقّق `max(id)` من طرفها، دون أي تنسيق مباشر بيننا)، وأُضيف سطري الخاص (`101: "layali-shamiya-bosphorus"`) بعد تأكّد أن سطرها انتقل فعلاً لـ102. **إصلاح إضافي مكتشَف أثناء التحقّق البصري النهائي:** المتجر (فئة «مطاعم»، Supabase-only بلا ملف `<slug>-data.js` مُجمَّع كنمط متجر 100) ظهر برسوم توصيل **١٥ ل.ت/كم** رغم تعيين `stores.delivery=20` في Supabase — تأكَّد عبر قراءة الكود أن عمود `stores.delivery` في القاعدة **لا علاقة له إطلاقاً** بـ`ratePerKm` الفعلي المعروض (`getDeliverySettings()` في `app.js` يقرأ حصراً من `state.deliverySettings`/`initialDeliverySettings` [كائن JS في الحزمة الأمامية] أو `DEFAULT_DELIVERY_SETTINGS` الاحتياطي `ratePerKm:15` — لا من أي عمود Supabase) — امتداد جديد لفئة خطأ location/mapUrl الموثَّقة في قسم 6 (بعض الحقول تُقرأ حصراً من الحزمة الأمامية حتى مع Supabase)، لكن هذه أول مرة يُكتشف أنه ينطبق على **رسوم التوصيل** أيضاً لمتجر بلا ملف مُجمَّع إطلاقاً. أُصلح بإضافة إدخال مباشر (بلا حاجة لملف `<slug>-data.js` كامل) `101: {mode:"distance", fixedFee:35, ratePerKm:20, prepMinutes:30, maxRoundTripKm:120}` داخل كائن `initialDeliverySettings` نفسه في `app.js` (بعد كل الانتشارات الحالية، بنفس نمط الإدخال المباشر الموجود مسبقاً لمعرّف 5) — تحقّق حي بعد الإصلاح أظهر «٢٠ ل.ت/كم» بشكل صحيح. تحقّق شامل عبر preview محلي: `/store/layali-shamiya-bosphorus` يعرض العنوان الصحيح (`دكانجي - ليالي شامية البوسفور`)، الوصف، العنوان الحقيقي، ساعات العمل، رسوم التوصيل الصحيحة بعد الإصلاح، كل الفئات الـ21 بعناصرها الصحيحة (188/188 من Supabase مطابقة تماماً لكل فئة)، الشعار والغلاف كلاهما HTTP 200، عيّنات من صور المنتجات (أول/منتصف/آخر) كلها HTTP 200، `/category/restaurants` يعرض المتجر ضمن القائمة، الصفحة الرئيسية بلا أي أثر سلبي، صفر أخطاء كونسول على الصفحات كافة. **ملاحظة:** `approval_status` أُدرِج `'pending'` عمداً (متجر طرف ثالث حقيقي غير مؤكَّد كشراكة) لكنه ظهر `'approved'` عند التحقّق النهائي — على الأرجح اعتماد خارجي (يدوي من المستخدم أو الإدارة) أثناء الجلسة، نفس نمط حالات سابقة موثَّقة (فستق حلب، بيت حلب، الوكالة...)، وليس فعلاً مني. `npm run check:versions` ناجح | `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 101 وصفوف `products` 1830001-1830188 في Supabase | نعم — `npm run check:versions` ناجح (app.js v312→313، store-slugs.js v250→251، CACHE v233→234). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-18 | المستخدم طلب إضافة مطعم «بيغ طاووق» (Bigtavuk / BIG TAVUKمطعم بيك طاووق) من كتالوج واتساب بزنس (+90 534 230 43 01)، حي خرقة شريف، الفاتح، إسطنبول | ميزة إضافة جديدة (لا خطأ سابق) — لكن اعترضتها عقبتان تقنيتان: (1) موصل Supabase MCP المتاح في هذه الجلسة مربوط بمشروع Supabase **مختلف تماماً** («goldengttr-hub's Project»، `iseldofsfhwvpfzltqet`) لا يحتوي جدول `stores` إطلاقاً — وليس مشروع دكانجي الحقيقي (`tzcqnqzltrjemdnkzpzn`، مقروء من `supabase-config.js`)؛ لم يُستخدم `execute_sql` MCP إطلاقاً لهذا السبب، واستُبدل بسكربتات Node تقرأ `SUPABASE_SERVICE_ROLE_KEY` من `.env` المحلي وتكتب مباشرة عبر PostgREST REST API (نفس المفتاح المستخدَم من سكربتات معالجة الصور في جلسات سابقة). (2) **تصادم معرّف حي حقيقي أثناء التنفيذ:** `max(stores.id)` تحقّق عدة مرات = 100 (صفر تصادم)، لكن محاولة الإدراج الفعلي على المعرّف المخطَّط 101 رجعت `23505 duplicate key` — جلسة متزامنة أخرى كانت قد أدرجت متجر «ليالي شامية البوسفور» بالمعرّف 101 بالضبط فيما بين آخر تحقّق والتنفيذ (خلال ثوانٍ) — أُعيد الاختيار فوراً للمعرّف **102** وحزمة منتجات بعيدة عن نمط `storeIndex×10000+1` المتوقَّع أن تستخدمه الجلسة الأخرى (تفادي تصادم ثانٍ محتمل على 1830001+، فاختير 1840001+ بدلاً منه)، مع تحقّق نهائي مباشر قبل الإدراج الفعلي أن كلا المعرّفين لا يزالان فارغين. **درس استخراج جديد لكتالوج واتساب:** صفوف القائمة نفسها (`[role="listitem"]`) تحمل صورة المنتج بدقة كاملة (720×720 حتى 1600×1200) كـ`background-image: url(blob:...)` مباشرة — بلا حاجة لأي نقر «Details» فردي (نفس درس زمزم/عمر ماركت)؛ لكن سقف التمرير البرمجي (`scrollTop=`) توقّف عند 60 عنصراً بينما تمرير حقيقي واحد إضافي (`computer.scroll` فعلي) كشف عن استقرار إضافي لنفس الحمولة (لا عناصر جديدة فعلياً هذه المرة، لكن التحقّق الإضافي كان ضرورياً ليقين العدد النهائي 59). فئة كتالوج «All items» أعادت سرد كل شيء (بما فيها عنصر واحد زائف `المنيو` بسعر 0.00 — استُبعد لأنه ليس منتجاً حقيقياً) بجانب 3 فئات معاينة مسمّاة (سندويش، صحون البطاطا، وجبات) تحوي كل منها 3 عناصر فقط ظاهرة (تكرار كامل لعناصر موجودة أصلاً في «All items» — لا فقدان بيانات). لا يوجد شعار مخصّص منفصل في الكتالوج: صورة الملف الشخصي لواتساب بزنس (640×640) كانت لقطة واجهة محل كاملة (حائط طوب + شعار «BIG TAVUK» دائري + طاولة جلوس)، فاستُخرج شعار نظيف بقصّ الشارة الدائرية فقط (400×400) بدل استخدام اللقطة الكاملة كشعار. غلاف المتجر (600×337 حقيقية) هو صورة استوديو احترافية منفصلة (برغرين مقرمشين) مأخوذة من صورة الغلاف («Cover Photo») الفعلية لحساب واتساب بزنس نفسه — قُصَّت لنسبة الهيرو (1280×542) بنجاح بلا فقدان تفاصيل الطبق. 59 منتجاً فريداً عبر 4 فئات حقيقية (سندويش 29، صحون البطاطا 11، وجبات 12، مشروبات 7) — التصنيف اليدوي (لا فئات مصدر فرعية داخل «All items» بذاتها) اعتمد على بادئات الأسماء (سندويش/برغر/شاورما/شيش → سندويش؛ صحن/بطاطا → صحون البطاطا؛ وجبة/وجبه → وجبات؛ الباقي → مشروبات)، تحقّق حسابي أن المجموع 29+11+12+7=59 قبل الإدراج. **مطابقة Google Maps مؤكَّدة بدقة عالية جداً:** Geocoding API على عنوان واتساب بزنس نفسه (`Hırka-i Şerif, Balipaşa Cd No:158, 34091 Fatih/İstanbul`) أعاد دقة ROOFTOP، ثم بحث نصي بالاسم قرب هذه الإحداثيات أعاد تطابقاً حرفياً كاملاً لرقم الهاتف (`0534 230 43 01` = نفس رقم واتساب المُرسَل بالضبط) ورقم المبنى (No:158 A) — الاسم الرسمي على غوغل «BIG TAVUKمطعم بيك طاووق»، تقييم 4.6 (55 تقييماً)، `place_id`: `ChIJfbzw6Gi7yhQR5yvgff2n9wg`. **ساعات العمل من غوغل غير موثوقة (بيانات متضاربة فعلياً: الثلاثاء «مغلق»، الأربعاء «11م–12ص» عابر لمنتصف الليل)** — اعتُمدت بدلاً منها الساعات المصرَّح بها من ملف تعريف واتساب بزنس نفسه («Open now، 00:00 - 23:00») كأصدق وأبسط مصدر، بدل بيانات غوغل المشوَّشة. الاسم المعتمد للمتجر «بيغ طاووق» (طلب المستخدم صراحة) رغم أن اسم الشعار/غوغل يظهر «BIG TAVUK» لاتينياً — يطابق سياسة استخدام الاسم الحقيقي الذي يزوّده الطالب حين يتطابق مع المصدر (لا اختلاق ولا ترجمة حرة). صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82 mozjpeg، شعار 400×400، غلاف 1280×542 بقص «attention»-aware). `approval_status='pending'` عمداً (متجر طرف ثالث حقيقي غير مؤكَّد كشراكة دكانجي، نفس سياسة كل المتاجر المشابهة). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`102: "bigtavuk-fatih-restaurant"`) + `<script src="/bigtavuk-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع لـSupabase عبر سكربت Node مخصَّص (`scripts/insert-bigtavuk-supabase.js`، service-role REST، معاملتان منفصلتان: صف المتجر ثم 59 صف منتج) بدل MCP (غير متاح لمشروع دكانجي الحقيقي في هذه الجلسة). تحقّق شامل عبر preview محلي: `/store/bigtavuk-fatih-restaurant` و`/store/102` يعرضان العنوان الصحيح (`دكانجي - بيغ طاووق`)، الوصف، العنوان الحقيقي، ساعات العمل، كل الفئات الأربع بعناصرها الـ59 وأسعارها الصحيحة من Supabase (59/59، مطابقة تماماً للمخطَّط)، كل الـ61 رابط صورة (59 منتج + شعار + غلاف) أعادت HTTP 200 فعلياً (تحقّق جماعي `fetch HEAD`)، `/category/restaurants` لا يعرض المتجر إطلاقاً (سلوك `pending` صحيح — تحقّق مباشر `approvalStatus==='pending'`)، الصفحة الرئيسية بلا أي أثر، صفر أخطاء كونسول على الصفحات الثلاث. `npm run check:versions` ناجح | `bigtavuk-data.js` (جديد)، `assets/photos/bigtavuk/**` (جديد، 61 ملف: 59 منتج + شعار + غلاف)، `scripts/gen-bigtavuk-data.js` (جديد)، `scripts/insert-bigtavuk-supabase.js` (جديد)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 102 وصفوف `products` 1840001-1840059 في Supabase | نعم — `npm run check:versions` ناجح (app.js v311→312، store-slugs.js v249→250، CACHE v232→233؛ أرقام الإصدار ارتفعت لاحقاً تلقائياً إلى v313/v251/v234 بفعل تعديلات الجلسة المتزامنة على نفس الملفات المشتركة، تحقّق نهائي أن كل الأرقام لا تزال متطابقة تماماً قبل الدفع). **مدفوع لـ`main`** (commit `145d8a78`، `git push origin main`) بناءً على طلب المستخدم «اعمل commit وادفعها لـ main» — عُزلت التعديلات عن كل الملفات الأخرى غير ذات الصلة الموجودة في نفس شجرة العمل وقتها (`api/notify-order.js`، تعديلات `dukkanci_customer_app/`، مجلدات Flutter/Capacitor/docs غير متتبَّعة) عبر `git add` انتقائي للملفات السبعة + مجلد الصور فقط. تحقّق بعد النشر: Vercel نشر بنجاح (GitHub commit status = success)، وكل الـ61 رابط صورة + رابط `/store/bigtavuk-fatih-restaurant` أعادت HTTP 200 فعلياً على الموقع الحي؛ عنوان SSR الخام لا يزال العنوان العام (متوقَّع تماماً بسبب `approval_status='pending'`)، لكن تصيير SPA الفعلي (بعد تحميل JS) يعرض العنوان والمحتوى الصحيحين كما تحقَّق محلياً
| 2026-07-18 | المستخدم طلب إضافة فرع ثانٍ لمتجر «فستق حلب» (id 88، إسنيورت) في بيليك دوزو، معطياً رابط Google Maps مختصر + رابط كتالوج واتساب (`wa.me/c/905550211222`) | ميزة إضافة جديدة (لا خطأ سابق) — لكن رابط Google Maps المختصر أعاد نتيجة **خاطئة تماماً** (صيدلية "Enerji Eczanesi" لا علاقة لها بالمتجر) — لم يُعتمد، واستُخدم بحث نصي بالاسم بدلاً منه فأعاد فرعين حقيقيين بنفس الاسم "فستق حلب Fistik Halep" مختلفين بالهاتف: الأول (0555 101 12 22) يطابق متجر 88 الموجود، والثاني (0555 021 12 22) يطابق رقم واتساب الجديد المُرسَل حرفياً — تأكيد إضافي حاسم: فتح رابط الكتالوج نفسه أظهر اسم حساب واتساب بزنس "**صالة بيليكدوزو**" مطابقاً تماماً لما وصفه المستخدم. **اكتشاف جوهري أثناء الاستخراج:** قسم "All items" في هذا الكتالوج **غير شامل** (يعرض ~30 عنصراً فقط من أصل 62 منتجاً حقيقياً موجودة فعلاً موزّعة على 6 فئات مُسمّاة) — عكس كل الجلسات السابقة (فستق حلب إسنيورت، زمزم، فاتح الخير...) حيث كان "All items" هو المصدر الأشمل دائماً؛ فئات الكتالوج المُسمّاة (قشاطي، حلويات العيد، معمول العيد، شوكولا ريد روز الفاخرة، ضيافات، مأكولات) كل واحدة كانت تحوي فعلياً حتى 10 عناصر (وليس 3 كما بدا للوهلة الأولى من أول ظهور غير مُمرَّر بالتمرير — درس مهم: التحقّق الأول لفئة "قشاطي" عبر لقطة شاشة ثابتة أظهر 3 عناصر فقط بفراغ ظاهري أسفلها فاعتُمد خطأً كنهاية القائمة، بينما التمرير الفعلي (`computer.scroll` حقيقي، البرمجي `scrollTop=` لا يعمل كالمعتاد) كشف 7 عناصر إضافية مخفية في نفس الفئة) — أُعيد فحص **كل** الفئات الست بتمرير كامل حتى استقرار العدّاد، ثم قورنت بقائمة "All items" لاستخراج 9 منتجات إضافية موجودة فقط هناك (شراب اللوز، فيصليات بالفستق، تمرية بالمكسرات، غريبة سميد، شعيبيات قشطة، هريسة بالقرفة، غريبة سنيورة، شوكولا فستق ملوكي، شوكولا بندق بنت) — المجموع النهائي **62 منتجاً حقيقياً** بلا تكرار. استُبعدت عمداً 3 عناصر بلا سعر ثابت (صندوقا مناسبات "يجب التواصل قبل 5 أيام" بسعر 0.00، ومشروبات رمضان بأسعار متعددة غير محدَّدة) اتساقاً مع سياسة عدم نشر منتج بلا سعر حقيقي. **اكتشاف تصحيحي أثناء المراجعة:** منتج "هريسةS" بدا كخطأ كتابة (حرف S معزول) — فتح صورته الفعلية كشف أنه اسم منتج حقيقي ومقصود: هريسة لوز مُشكَّلة حرفياً على هيئة حرف S (صورة توضح ذلك بوضوح مع نص "حرف (S)")، فاعتُمد كما هو (مع مسافة فقط: "هريسة S") بدل استبعاده أو "تصحيحه" خطأً. المتجر رقم **100** (تحقّق `max(id)`=99 مرتين، صفر تصادم)، حزمة معرّفات المنتجات **1820001–1820062**. صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82 mozjpeg، شعار 400×400 من صورة الملف الشخصي المربّعة 640×640 لواتساب بزنس، غلاف 1600×679 مقصوص من صورة منتج حقيقية عالية الجودة "عش البلبل بالفستق" على صينية نحاسية — لا بانر تسويقي منفصل في المصدر). `approval_status='approved'` مباشرة (الصور على Supabase Storage، لا حاجة انتظار نشر Vercel). **هذا الفرع مغاير معمارياً عن أغلب المتاجر السابقة:** متجر 88 (الفرع الأول) لا يملك ملف `<slug>-data.js` مُجمَّعاً إطلاقاً (بيانات Supabase حصراً منذ إضافته)، فاتُّبع نفس النمط للفرع الجديد — **لا حاجة لأي تعديل على `app.js`** (لا `stores.push`، لا `products.push`، لا `initialDeliverySettings`)، فقط سطر واحد في `store-slugs.js` (`100: "fistik-halep-sweets-beylikduzu"`) + رفع رقم إصداره في `index.html`/`sw.js` + `CACHE`. **لم تُطبَّق آلية `branchGroup`/تجميع الفروع الموثَّقة في [[multi-branch-stores]]** عمداً — تفعيلها الكامل يتطلّب تعديل اسم المتجر 88 الحيّ والمعتمد أصلاً (`stores.push`/`branch_group`) خارج نطاق الطلب الأصلي؛ الفرعان يظهران حالياً كمتجرين مستقلّين تماماً (لا تجميع في بطاقة واحدة، لا اختيار أقرب فرع تلقائي) — تُرك هذا كتحسين مستقبلي إن طُلب صراحة. تحقّق شامل عبر preview محلي: `/store/fistik-halep-sweets-beylikduzu` و`/store/100` يعرضان العنوان الصحيح (`دكانجي - حلويات فستق حلب صالة بيليكدوزو`)، كل الفئات الثمانية (قشاطي 11، حلويات العيد 10، نواشف و معمول 13، شوكولاته 15، هريسة اللوز 5، مأكولات 3، مشروبات 1، تسالي 4 = 62) بعناصرها وأسعارها الصحيحة من Supabase، الشعار والغلاف كلاهما HTTP 200 وأبعاد صحيحة (400×400 و1600×679)، عيّنات عشوائية من روابط صور المنتجات (p30, p45, p62) كلها HTTP 200، `/category/sweets` يعرض 9 متاجر حلويات بينها الفرعان معاً بأسماء مميَّزة واضحة، صفر أخطاء كونسول جديدة (التحذيرات الموجودة عن متاجر 9/11/72 سابقة وغير متعلقة). `npm run check:versions` ناجح | `store-slugs.js`, `index.html`, `sw.js`, `scripts/process-fistikhalep2-images.js` (جديد), `scripts/upload-fistikhalep2-images.js` (جديد), `CLAUDE.md` + صف `stores` id 100 وصفوف `products` 1820001-1820062 في Supabase + 64 ملف صورة (62 منتج + شعار + غلاف) في Supabase Storage | نعم — `npm run check:versions` ناجح (app.js/styles.css لم يتغيّرا فعلياً؛ `store-slugs.js` v248→249 و`CACHE` v231→232 يدوياً بالتحقّق النصي). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-18 | المستخدم طلب «تفعيل البحث الصوتي في التطبيق» — طُرح سؤال توضيح واحد عبر `AskUserQuestion` (الموقع/PWA يملك ميزة بحث صوتي مُفعَّلة أصلاً منذ 2026-06-29 عبر Web Speech API — تحقّق مباشر من `integration_settings.feature_voice_search.is_enabled=true` في Supabase قبل السؤال — بينما تطبيق Flutter الجديد لا يملكها إطلاقاً)، فاختار المستخدم صراحة **تطبيق Flutter** | ميزة جديدة (لا يوجد خطأ سابق). لا يوجد أي مكافئ لفحص `window.SpeechRecognition` المجاني في المتصفح — حزمة `speech_to_text` على أندرويد تحتاج استدعاء `initialize()` (الذي يُطلق نافذة صلاحية RECORD_AUDIO الحقيقية) لمعرفة توفر الميزة أصلاً، فلا يمكن إخفاء الزر مسبقاً كما فعل الموقع؛ الحل: إظهار الزر دائماً والفشل بصدق (رسالة واضحة) عند النقر لا محاولة إخفاء مستحيلة مسبقاً | حزمة `speech_to_text` (تتعرّف الكلام محلياً عبر محرك أندرويد نفسه، بلا رفع صوت لأي خادم — نفس مبدأ «بلا تخزين صوت» على الموقع) + صلاحية `RECORD_AUDIO` في AndroidManifest.xml (تُطلب فقط عند الضغط الصريح على زر الميكروفون، لا عند فتح التطبيق — نفس سياسة صلاحية الموقع المُتَّبعة أصلاً). ودجة جديدة قابلة لإعادة الاستخدام `VoiceSearchButton` (`core/widgets/voice_search_button.dart`): تهيئة عند أول نقر، بحث عن أقرب لغة عربية متاحة على الجهاز (`locales()` ثم مطابقة أي `localeId` يبدأ بـ`ar`)، حالة استماع مرئية (تلوين الأيقونة + شريط سفلي «جارٍ الاستماع... تحدّث الآن» لمدة تطابق `listenFor`)، وثلاث رسائل فشل صادقة منفصلة (لا صلاحية / لا دعم على الجهاز / لم يُسمع كلام) بدل رسالة عامة واحدة. النص المُتعرَّف عليه يُمرَّر لنفس مسار `_submitSearch` الذي يستخدمه الإدخال اليدوي (حفظ في «عمليات بحث سابقة» + تحديث نتائج البحث)، فلا يوجد منطق بحث مكرَّر. زر الميكروفون يحل محل زر المسح («×») في `suffixIcon` فقط عندما يكون الحقل فارغاً (نفس نمط تطبيقات البحث الشائعة)، ووُصل حصراً في شاشة البحث الحقيقية (`search_screen.dart`) — تحقّق أن حقل البحث في الصفحة الرئيسية مجرَّد زر تنقّل ثابت بلا `TextField` فعلي (`onTap: () => context.push(AppRoutes.search)`) فلا حاجة لأي زر ميكروفون إضافي هناك. تحديث أيضاً لواجهة `speech_to_text` v7.4.0 غير المُهملة (`SpeechListenOptions` بدل المعاملات المباشرة المُهملة) — `flutter analyze` نظيف بلا أي تحذير جديد. **تحقّق حي حقيقي وكامل على المحاكي (وليس افتراضاً):** بناء APK استغرق 785 ثانية (~13 دقيقة، أطول من المعتاد بسبب أول ربط لكود أندرويد أصلي جديد من `speech_to_text` — تحقّق عبر مراقبة استهلاك المعالج الفعلي للعملية بدل الانتظار الأعمى، تأكيداً أنه بناء حقيقي جارٍ لا عملية عالقة). النقر على زر الميكروفون أطلق **نافذة صلاحية أندرويد حقيقية** («Allow دكانجي to record audio?» بخيارات While using the app / Only this time / Don't allow) — تؤكد أن ربط الصلاحية يعمل فعلياً وبالتوقيت الصحيح (عند الضغط الصريح لا عند الإطلاق). بعد منح الصلاحية، سجلّ `logcat` أظهر فحصاً حقيقياً لدعم التعرّف الصوتي عبر خدمة Google (`AiAiSpeechRecognition`/`RecognitionServiceImpl`) نجح (`onEndCheckRecognitionSupport success`) ثم أُغلق الاتصال **قبل** استدعاء `startListening` إطلاقاً — يطابق تماماً منطق الكود (لم يُعثر على أي لغة عربية، فلم يُستدعَ `listen()`)؛ تأكيد إضافي: بحث نصي شامل في السجل عن أي رمز لغة عربي (`ar-SA`/`ar-EG`/...) لم يُعِد أي نتيجة إطلاقاً بينما ظهرت لغات أخرى (`ja-JP`) فعلياً في نفس الفحص — يثبت أن صورة نظام هذا المحاكي تحديداً تفتقر فعلياً لحزمة اللغة العربية الصوتية لخدمة Google، وليس خللاً في كود التطبيق. النقر مجدداً (بعد منح الصلاحية) أظهر فعلياً الشريط السفلي بنص «البحث الصوتي غير متاح على هذا الجهاز» بدقة — تأكيد بصري كامل للمسار السليم بلا أي تجمّد أو خطأ JS. **لم يُختبَر التعرّف الفعلي على كلام عربي حقيقي** لعدم توفر حزمة اللغة العربية على صورة نظام هذا المحاكي تحديداً — على جهاز حقيقي (أو محاكي بحزمة لغة عربية مُثبَّتة) يُفترض أن يتجاوز الكود هذا الفحص وينتقل فعلياً لـ`listen()` الحقيقي، لكن هذا لم يُثبَت مباشرة في هذه الجلسة | `dukkanci_customer_app/pubspec.yaml`, `dukkanci_customer_app/pubspec.lock` (إضافة `speech_to_text`), `dukkanci_customer_app/android/app/src/main/AndroidManifest.xml` (صلاحية RECORD_AUDIO), `dukkanci_customer_app/lib/core/widgets/voice_search_button.dart` (جديد), `dukkanci_customer_app/lib/core/localization/app_strings.dart`, `dukkanci_customer_app/lib/features/search/presentation/search_screen.dart`, `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل). **بانتظار تنفيذ `git commit`**
| 2026-07-18 | المستخدم طلب بانراً في صفحة `/offers` لمطعم باشا بيتزريا (id56): «خصم 10% على أطباق البيتزا لعملاء دكانجي» | ميزة/حملة تسويقية جديدة، لكن فحص بيانات Supabase قبل البناء (`select * from products where store_id=56`) كشف أن 17 من أصل 30 صنف بيتزا في المتجر (3 فئات: بيتزا عادية، البيتزا الايطالية، البيتزا محشية الاطراف) لم تكن مخفَّضة إطلاقاً (13 فقط `old_price` محدَّد مسبقاً) — بانر يدّعي خصماً على «أطباق البيتزا» ككل بجانب أصناف كاملة السعر كان سيكون ادّعاءً كاذباً جزئياً، فطُرح سؤال توضيح (`AskUserQuestion`) عن نطاق الخصم، ولم يُجب المستخدم (تجاهل السؤال) — طُبِّق الخيار الأكثر أماناً واتساقاً مع سياسة الصدق المتّبعة في المشروع: تطبيق الخصم الحقيقي على كل الأصناف الثلاثين وليس فقط بناء نص تسويقي. **اكتشاف إضافي أثناء التحقق:** من الـ17 صنفاً المخفَّضة حديثاً، 15 كانت بلا صورة حقيقية (`image=""`) و2 تستخدم صورة عامة بديلة (`store-market.jpg`) — أي أن `applyPublishingRules` تخفيها أصلاً عن الواجهة (13 فقط من أصل 30 ظاهرة فعلياً للعميل) — هذا يعني أن كل صنف بيتزا **ظاهر** للعميل كان مخفَّضاً 10% أصلاً قبل هذه المهمة، وأن التوسيع لبقية الـ17 صنفاً احتياطي/مستقبلي (يصبح فعالاً تلقائياً متى أُضيفت لها صور حقيقية) لا يغيّر شيئاً في تجربة العميل الحالية لكنه يضمن صدق الادّعاء حتى لو ظهرت لاحقاً | (1) Supabase: `UPDATE products SET old_price=price, price=floor(price*0.9)` على الـ17 صنفاً (نفس نمط تقريب `floor` المستخدَم أصلاً في الـ13 صنفاً السابقة: 249→224، 299→269، 419→377، 449→404). (2) `app.js`: ثابت جديد `OFFERS_PAGE_STORE_PROMOS` (قابل للتوسعة لمتاجر أخرى مستقبلاً) + دالة `offersPageStorePromoBanners()` تتحقق **وقت العرض** من وجود ≥1 منتج فعلي متوفر بخصم حقيقي (`oldPrice`+`available`) مطابق للفئة قبل عرض أي بانر — بانر لا يجد منتجات مطابقة يختفي تلقائياً بصمت بدل عرض ادّعاء بلا سند. بانر جديد في `renderOffers()` (بين الهيرو وقسم «خصومات اليوم») بصورة غلاف المتجر الحقيقية + رابط `data-action="open-store"` لصفحة باشا بيتزريا مباشرة — مستقل تماماً عن آلية «عرض اليوم» الإدارية الموجودة أصلاً (تلك مربوطة بمنتج واحد فقط لا فئة كاملة لمتجر). (3) `styles.css`: تصميم بطاقة أفقية جديدة (`.store-promo-banner*`) بنفس لغة `.dd-hero` الموجودة (تدرّج غامق فوق صورة الغلاف، شارة صفراء، زر انتقال دائري). تحقّق شامل عبر preview محلي: البانر يظهر فعلاً على `/offers` بالنص الصحيح، النقر عليه ينقل فعلاً لصفحة `/store/pasa-pizzeria-restaurant`، صفر انزياح أفقي على 375px، صفر أخطاء كونسول، وتأكيد مباشر من Supabase أن الـ30 صنفاً بيتزا كلها `old_price` محدَّد الآن (`select count(*) filter(where old_price is not null)`=30/30). `npm run check:versions` ناجح | `app.js`, `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` + `old_price`/`price` لـ17 صف `products` (store_id=56) في Supabase | نعم — `npm run check:versions` ناجح (app.js v310→311، styles.css v174→175، CACHE v230→231). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-18 | المستخدم طلب إظهار 4 متاجر دفعت (مطعم الخوالي id31، باشا بيتزريا id56، ملحمة الدوماني id84، صفا الشام id50) في مقدمة قوائم المتاجر وتصدير عروضها قبل أي عروض أخرى على الموقع | ميزة/سياسة عرض جديدة (لا يوجد خطأ سابق) — لكن 3 من الأربعة كانت `featured=true` بالفعل بينما id84 (الدوماني) لم يكن، فلم تكن الأربعة موحّدة الظهور في رفّ "متاجر قريبة منك" على الرئيسية أصلاً | أُضيف ثابت عام `PAID_PRIORITY_STORE_IDS=[31,56,84,50]` في `app.js` (بعد `QUICK_SEARCH_CHIPS`) + دالتا فرز مستقرّ `sortStoresByPaidPriority`/`sortProductsByPaidPriority` (المتاجر/المنتجات غير المُدرجة تحتفظ بترتيبها النسبي القديم، الأربعة فقط تتقدّم بترتيب القائمة). طُبِّق في 5 مواضع: (1) رفّ "متاجر قريبة منك" على الرئيسية (`featuredStores`, بعد فرز المسافة)، (2) قسم "عروض اليوم" على الرئيسية (أول 8 عروض)، (3) صفحة `/offers` (عروض الأربعة تتصدّر دائماً بلا تعرّض لعشوائية `seededShuffle` اليومية؛ الباقي فقط يُخلَط)، (4) `getFilteredStores()` لصفحة `/stores` — فقط في وضع الفرز الافتراضي "الأنسب لك"، لا يتجاوز فرزاً صريحاً يختاره الزائر (تقييم/الأقرب/توصيل أقل...)، (5) `renderCategoryPage()` لكل صفحات `/category/<slug>` (كل متجر من الأربعة يتصدّر فقط ضمن تصنيفه الحقيقي). أُضيف أيضاً `featured=true` لمتجر id84 في Supabase (كان `false`، الثلاثة الآخرون بالفعل `true`) ليظهر في رفّ الرئيسية مثل البقية. **لا يُطبَّق على `/dalil`** (دليل تقييمات Google — ترتيب عضوي بحت بتصميمه الأصلي، مُوثَّق صراحة في تعليق الكود الجديد). تحقّق شامل عبر preview محلي: الرئيسية (الأربعة بالترتيب المطلوب أول 4 بطاقات من أصل 64)، `/stores` (نفس الترتيب أول 4 من 68)، `/category/restaurants` (الخوالي وباشا بيتزريا أول اثنين من 43 — الآخران غير معروضين بشكل صحيح لأنهما من تصنيف مختلف)، قسم عروض الرئيسية (تحقّق أن باشا بيتزريا هو المتجر الوحيد من الأربعة الذي يملك عروضاً حالياً [13 منتجاً بـ`oldPrice`] وظهرت أول 8 عروضه فعلاً؛ الثلاثة الآخرون بلا عروض حالياً فلن يظهروا حتى يضيفوا خصماً — سلوك صحيح وليس عطلاً)، صفر أخطاء كونسول على كل الصفحات، `npm run check:versions` ناجح | `app.js`, `index.html`, `sw.js`, `CLAUDE.md` + عمود `stores.featured` للمتجر id84 في Supabase | نعم — `npm run check:versions` ناجح (app.js v309→310، CACHE v229→230). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-18 | المستخدم طلب إضافة سوبر ماركت «ماركت عمر» (Omar Market)، حي قنارية (Kanarya)/كوچوك چكمجة، معطياً رقم واتساب (05397398078) وعنوان (Kanarya, Kaşıkçın Sk. No:5, 34290 Küçükçekmece/İstanbul) وطلب صريح «قم بتنزيل المتجر من خلال كتالوج الواتساب» | ميزة إضافة جديدة (لا يوجد خطأ سابق) — تأكيد إضافي لدرس زمزم ماركت (2026-07-14): كتالوج واتساب عبر WhatsApp Web كان لا يزال أنظف طريقة استخراج (لا حاجة لأي نقر "Details" فردي). **درس جديد يُضاف:** أول محاولة تلقائية لاستخراج الصور بحثت عن عناصر CSS `background-image` بصيغة `blob:` على مستوى الصفحة كلها (`document.querySelectorAll('div')`) واستقرت خطأً على رقم ثابت "16" (تحقّق لاحق أظهر أنها كانت 6 عناصر معاينة فئة + 10 صفوف "All items" أولى، لا أكثر) — أُصلح بتضييق البحث لعنصر `[role="listitem"]` نفسه (شجرة إتاحة الوصول تعطي بنية أنظف بكثير من `innerText`/DOM الخام لهذا الكتالوج تحديداً: أسماء/أوصاف/أسعار كحقول `generic` منفصلة داخل كل `listitem`)، مع مطابقة الصورة (`getComputedStyle` على عناصر ابنة للـ`listitem` نفسه فقط، لا الصفحة كلها) لتفادي التقاط صور أفاتار/رأس الصفحة بالخطأ. تكرار حقيقي داخل قسم "All items" (كل صنف يظهر أيضاً في معاينة فئته) عولج بمفتاح تكرار نصي كامل (اسم+وصف+سعر) لا رقم تسلسلي. سقف تنزيلات Chrome التلقائية (~10 لكل دفعة، موثَّق سابقاً من جلسة فستق حلب) لم يُصطدم به هذه المرة (31 صورة نزلت بنجاح عبر عدة دفعات صغيرة بفعل `await` طبيعي بين كل تنزيل). **لا تطابق Google Maps موثوق:** بحث نصي بالاسم + الجوار أعاد عدة أسواق أخرى بأسماء مختلفة تماماً على نفس الشارع (Kaşıkçın Sokak) — أقربها "Marin Market" برقم مبنى مختلف (8 لا 5) وساعات عمل مختلفة عن ما ذكره حساب واتساب بزنس نفسه (07:30-23:00 مقابل 09:30-23:55) — لم يُعتمد كتطابق قسراً؛ استُخدم بدلاً منه Geocoding API لعنوان الشارع الدقيق كما ورد حرفياً (`Kaşıkçın Sk. No:5, Kanarya, Küçükçekmece` → إحداثيات 41.0103952/28.7806203 بدقة `street_address`)، مع ترك `google_place_id`/`google_rating` فارغة (نفس سياسة حالات id 22/36 السابقة عند غياب تطابق واثق). كل الـ31 منتجاً حملت خصماً حقيقياً معلَناً من التاجر نفسه (`oldPrice` أعلى من `price` في كل صف)، فظهر المتجر تلقائياً في قسم «العروض» بلا أي إضافة يدوية. المتجر رقم **99** (تحقّق `max(id)` ثلاث مرات: عند البدء، بعد التخطيط، ومباشرة قبل تنفيذ الإدراج — 98 في كل مرة، صفر تصادم)، حزمة معرّفات المنتجات **1810001–1810031**. صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82 mozjpeg، شعار ≤400×400، غلاف ≤1280px q85 — الغلاف والشعار صورتان حقيقيتان من ملف تعريف واتساب بزنس نفسه: صورة رفّ سوبر ماركت حقيقية كغلاف، وشعار ذهبي مزخرف كصورة شخصية). `approval_status='pending'` عمداً (متجر طرف ثالث حقيقي غير مؤكَّد كشراكة دكانجي). فئتان حقيقيتان مطابقتان تماماً لتقسيم كتالوج التاجر نفسه («الارز والنواشف» 20 صنفاً، «قهوة وشاي وزهورات ومتة» 11 صنفاً). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`99: "omar-market-kanarya"`) + `<script src="/omarmarket-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع عبر Supabase MCP `execute_sql` (صف المتجر + 31 صف منتج، `jsonb_to_recordset` مع `price::numeric`/`old_price::numeric` cast، معاملتان منفصلتان). تحقّق شامل عبر preview محلي: `/store/omar-market-kanarya` يعرض العنوان الصحيح (`دكانجي - ماركت عمر`)، الوصف، العنوان الحقيقي، ساعات العمل، الفئتان بعناصرهما الصحيحة (31/31 من Supabase)، قسم «عروض ماركت عمر» يعرض كل الـ31 منتجاً بنسب خصم صحيحة محسوبة تلقائياً، كل الـ33 رابط صورة (31 منتج + شعار + غلاف) أعادت HTTP 200 فعلياً (تحقّق جماعي `fetch HEAD` من الصفحة الحية)، `/category/supermarket` لا يعرض المتجر إطلاقاً بعد اكتمال تحميل كتالوج Supabase (سلوك `pending` صحيح — تحقّق مباشر عبر `stores.find(s=>s.id===99).approvalStatus==='pending'`)، صفر أخطاء كونسول على الصفحتين. `npm run check:versions` ناجح | `omarmarket-data.js` (جديد)، `assets/photos/omarmarket/**` (جديد، 33 ملف: 31 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 99 وصفوف `products` 1810001-1810031 في Supabase | نعم — `npm run check:versions` ناجح (app.js v308→309، store-slugs.js v247→248، CACHE v228→229). **مدفوع لـ`main`** (commit `a5fe0fe0`، `git push origin main`) بناءً على طلب المستخدم «اعمل commit وادفعها لـ main» — عُزلت التعديلات عن سطر آخر غير مرتبط كان موجوداً مسبقاً بلا التزام في `CLAUDE.md` (سجل جلسة Flutter منفصلة) عبر `git apply --cached` لتجنّب سحبه في نفس الالتزام. تحقّق بعد النشر: Vercel نشر بنجاح (GitHub commit status = success)، ورابط `www.dukkanci.com.tr/store/omar-market-kanarya` وكل صور المتجر الـ33 (منتجات+شعار+غلاف) أعادت HTTP 200 فعلياً على الموقع الحي؛ عنوان SSR الخام لا يزال العنوان العام (متوقَّع تماماً بسبب `approval_status='pending'` — نفس سلوك متاجر مماثلة سابقة)، لكن تصيير SPA الفعلي (بعد تحميل JS) يعرض العنوان والمحتوى الصحيحين كما تحقَّق محلياً
| 2026-07-18 | استكمالاً لسلسلة إعادة تصميم Flutter: (1) المستخدم أرسل لقطة مرجعية لشاشة بداية (خلفية كريمية + رسم خطي لقوس/باب مزدوج + شعار «دكانجي/Dukkanci Marketplace» + شعار فرعي) وطلب «اعتمد هذه لتكون شاشة البداية»؛ (2) ثم أرسل 4 لقطات إضافية (تفاصيل الطلب، حالة فارغة للمفضلة بتبويبين Restoranlar/Marketler، «Adreslerim» دفتر عناوين بـ5 عناوين وأزرار تعديل/حذف، وقائمة «Siparişlerim» بصور مصغّرة للأصناف وزر «Siparişi Tekrarla») بلا طلب تنفيذ فوري، فطُرح سؤال توضيح واحد فقط عبر `AskUserQuestion` حول نطاق ميزة «عناويني» تحديداً (دفتر كامل أم تأجيل)، فاختار المستخدم **الميزة الكاملة (إضافة/تعديل/حذف)** | ليس خطأ — سلسلة ميزات/تصاميم جديدة. لم يُعثر على أي ملف مصدر فعلي لتصميم شاشة البداية (بحث في Downloads/Pictures/Desktop لم يُظهر شيئاً)، فأُعيد رسم القوس/البابين يدوياً كـ`CustomPainter` (Path بخطوط Bézier) بدل توليد صورة بالذكاء الاصطناعي (لا تضمن نفس الشكل حرفياً في كل تشغيل). **خطأ حقيقي اكتُشف فقط عبر لقطة شاشة حية (لا افتراضاً):** أول نسخة استخدمت `logo.png` (الذي يتضمن أصلاً «دكانجي» + «Dukkanci Marketplace» مدموجين في الصورة نفسها) مقصوصاً لمربع صغير (`BoxFit.cover`) **مع** إضافة نص Text منفصل فوقه بنفس الكلمات — نتج عن ذلك نص متراكب/مكرَّر ومقطوع ("kkanci Marketpl...")، أُصلح بعرض الشعار كاملاً بنسبته الطبيعية (`BoxFit.contain`) وحذف عناصر Text المكرِّرة. لصفحة «عناويني»: نموذج `Address` قديم غير مستخدَم إطلاقاً كان موجوداً مسبقاً في `location/domain/address.dart` (13 حقلاً حسب «قسم 9» من مواصفات المشروع، بينها `lat/lng` تتطلّب منتقي خريطة لا يملكه التطبيق بعد) — بدل تفعيله كما هو (كان سيترك حقلي الإحداثيات صفراً دائماً بصمت)، حُذف كملف ميت (تحقّق `grep` صفر استخدام في كامل المشروع) واستُبدل بنموذج أخفّ يطابق تماماً الحقول التي تستهلكها شاشة الدفع الحقيقية أصلاً (`addressText`/`addressDetails` نصّان حرّان، لا حقول جغرافية مجزّأة تحتاج منتقياً غير موجود). لصفحة تفاصيل الطلب: `OrderSummary.fromJson` كان يتجاهل عمداً/سهواً عمود `delivery_details` (jsonb) بالكامل رغم أن نقطة `customer-orders` في `api/notify-order.js` (تحقّق مباشر من كود الموقع نفسه في نفس المستودع، سطر 1356/1361: `select=*`) تُعيده فعلاً — فكانت صفحة التفاصيل تعرض فقط الخط الزمني للحالة بلا أي عناصر/عنوان/دفع رغم توفر البيانات الحقيقية في الاستجابة أصلاً. | **شاشة البداية:** `logo.png` بنسبته الطبيعية + وسم لاتيني `Dukkanci Marketplace` (تعليق صريح يوضّح أنه ليس عودة لنظام ترجمة، فقط جزء من الشعار المعتمد) + تأخير 700ms حقيقي (ليس وهمياً) قبل التنقّل. **عناويني (ميزة كاملة جديدة):** نموذج `SavedAddress` (`lib/features/addresses/domain/saved_address.dart`: label/addressText/addressDetails/recipientName/recipientPhone/isDefault) + `AddressesController` (Notifier يغلّف `LocalCache.saveAddressesJson/readAddressesJson` الموجودتين مسبقاً وغير المستخدمتين من قبل — أول عنوان يُحفظ يصبح تلقائياً افتراضياً، وحذف العنوان الافتراضي يُرقّي التالي تلقائياً بدل ترك القائمة بلا افتراضي) + `AddressesScreen` (بطاقة لكل عنوان: أيقونة حسب النوع، شارة «افتراضي»، تعديل/حذف بأيقونتين + تأكيد حذف بنفس نمط `AlertDialog` المستخدَم في السلة، رابط «تعيين كافتراضي»، حالة فارغة بزر «إضافة عنوان جديد») + `AddressFormScreen` (رقائق اختيار Home/Work/Other بنفس نمط `_FulfillmentTile` الموجود في الدفع، حقل اسم مخصّص عند اختيار «آخر»، حماية من الضغط المزدوج بعلم `_saving` مطابقة لنمط `_submitting` في شاشة الدفع). **ربط حقيقي وليس ميزة معزولة:** شاشة الدفع تُحمِّل تلقائياً العنوان الافتراضي عند الفتح (`initState`)، وتعرض زر «استخدام عنوان محفوظ» يفتح قائمة سفلية لاختيار أي عنوان آخر يملأ حقول العنوان **و**الاسم/الهاتف معاً، مع رابط «إدارة العناوين» للوصول السريع لدفتر العناوين الكامل دون مغادرة الدفع فعلياً (ينتقل ويُمكن الرجوع). **تفاصيل الطلب:** `OrderSummary`/`OrderLineItem` جديدان يقرآن `delivery_details.{lineItems,fulfillment,address,addressDetails,payment,notes}` الحقيقية (تحقّق حقول `lineItems` من `app.js` سطر 13834-13836 نفسه لضمان تطابق الأسماء تماماً: `name/qty/price/options/notes`) — قائمة أصناف حقيقية (بأيقونة حقيبة عامة بدل صورة، لأن اللقطة التاريخية لا تحفظ رابط صورة إطلاقاً — نفس مبدأ الصدق المُتَّبع سابقاً في رفض خريطة سائق وهمية لهذه الشاشة تحديداً) + بطاقة عنوان/استلام + بطاقة طريقة الدفع + ملاحظات إن وُجدت. **قائمة الطلبات:** سطر معاينة أصناف حقيقي (`order.itemsPreview`، مثال «٢× دجاج مشوي، أرز») + زر «اطلب مرة أخرى» **صادق** (ينقل لصفحة المتجر نفسه بدل محاولة إعادة بناء سلة مطابقة تماماً — الخيارات/الإضافات المختارة سابقاً محفوظة كنص مُسطَّح `options` غير قابل لإعادة الربط بمعرّفات حقيقية، فإعادة تركيبها كانت ستكون تخميناً لا استعادة). **المفضلة:** الحالة الفارغة أصبحت تحمل زر «تصفّح المتاجر» (نفس النص/المسار المستخدَم في حالة السلة الفارغة) بدل رسالة بلا فعل. **لم يُنسَخ من المرجع:** تبويبا Restoranlar/Marketler الثابتان في حالة المفضلة الفارغة (مُغطّى فعلاً بصف الفئات الديناميكي الموجود أصلاً لما تكون المفضلة غير فارغة)، وأي حقل إحداثيات/خريطة في نموذج العنوان (لا منتقي خرائط مُفعَّل بعد). تحقّق: `flutter analyze` نظيف طوال الجلسة (نفس 3 تنبيهات معلوماتية قديمة فقط) بعد كل دفعة تعديلات، بناء APK ناجح 3 مرات. **تحقّق حي شامل على المحاكي بعد إعادة تنصيب (المحاكي كان قد توقّف بصمت بين الجلستين، أُعيد تشغيله + عولجت نافذتا ANR للمشغّل/System UI الروتينيتان المعروفتان):** عناويني بكامل السيناريو (إضافة عنوانين بتصنيفين مختلفين، تعديل، حذف، تعيين افتراضي، ورجوع الحالة الفارغة لعرض زر واحد فقط بعد إصلاح ازدواج زر الإضافة الذي رُصد أول مرة في نفس هذه الجلسة)، وربط الدفع الحي (تعبئة تلقائية لعنوان/اسم/هاتف من العنوان الافتراضي فور فتح الدفع + قائمة «استخدام عنوان محفوظ» السفلية + رابط «إدارة العناوين») — كلها عملت بلا أي خطأ. **درس أدوات:** أول محاولتي كتابة نص طويل عبر `adb shell input text` تلتها فوراً `keyevent 4` (رجوع) بلا مهلة استقرار أدَّتا لبتر النص المحفوظ فعلياً («Test Stree» بدل «Test Street 123») — تبيّن أنه تعارض توقيت في أداة الاختبار نفسها (سباق بين حقن النص والرجوع الفوري) وليس خللاً في كود الحفظ: إعادة الكتابة بمهلة `sleep` قبل أي إجراء لاحق أعادت النص كاملاً وصحيحاً في كل محاولة تالية. 🔴 **حادثة يجب الإفصاح عنها:** أثناء اختبار زر «استخدام عنوان محفوظ»، ضغطة تمرير تالية بإحداثيات غير محسوبة بدقة (نفس خطأ معامل تحويل 1.2× للقطة الشاشة الموثَّق سابقاً في هذه الجلسة) وقعت فعلياً على زر «تأكيد الطلب» وشغَّلت مسار الدفع الحقيقي (`sendOtp`) على رقم هاتف اختباري مُختلَق («905559876543»، ليس رقم مستخدم حقيقي ولا رقم تاجر) — تم إيقاف التنفيذ فوراً (رجوع بلا إدخال رمز التحقق) بمجرد ملاحظة الشاشة الانتقالية لإدخال OTP؛ **لم يُنشأ أي طلب فعلي ولم يصل أي إشعار لأي تاجر حقيقي** (إنشاء الطلب يحدث فقط بعد نجاح التحقق من الرمز، وهذه الخطوة لم تُكمَل مطلقاً). تفاصيل الطلب وقائمة الطلبات (معاينة الأصناف + زر «اطلب مرة أخرى») **لم تُختبر بالنقر الحي** لعدم وجود أي طلب حقيقي على جهاز الاختبار هذا (حالة فارغة صحيحة «لا توجد طلبات بعد» تحقّقت فعلاً) — الثقة فيهما مبنية على مطابقة حرفية لأسماء حقول `delivery_details.lineItems` مع كود إنشاء الطلب الحقيقي في `app.js` (سطر ~13834-13836) ومسار `customer-orders` في `api/notify-order.js` (`select=*`، سطر 1356/1361) نفسيهما في نفس المستودع، وليس تخميناً | `dukkanci_customer_app/lib/{core/cache/local_cache,core/localization/app_strings,core/routing/app_router,core/routing/app_routes,core/theme/app_colors,features/checkout/domain/order,features/checkout/presentation/checkout_screen,features/favorites/presentation/favorites_screen,features/orders/presentation/order_detail_screen,features/orders/presentation/orders_screen,features/profile/presentation/profile_screen,features/splash/presentation/splash_screen}.dart` (معدَّلة) + `dukkanci_customer_app/lib/features/addresses/**` (جديد: domain/application/presentation) + حذف `dukkanci_customer_app/lib/features/location/domain/address.dart` (ميت غير مستخدَم) + `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل). **بانتظار تنفيذ `git commit`** |
| 2026-07-18 | استكمالاً لسلسلة إعادة تصميم Flutter: المستخدم طلب «كمّل على صفحة البحث» **بلا إرفاق أي لقطة مرجعية جديدة** هذه المرة (أول جولة في السلسلة بلا مرجع مباشر) | ليس خطأ — قرار تصميم. بدل التوقف لطلب لقطة، طُبِّق **نفس** لغة التصميم المُثبَّتة فعلاً عبر 8 صفحات سابقة (رقائق مُحاطة بحدود من ثيم التطبيق، تصنيف `HomeCategory` المشترك) بدل اختراع أسلوب جديد أو الانتظار. حقل البحث كان مضمَّناً في AppBar بحدود مُلغاة (`filled:false, border:InputBorder.none`) فبدا مختلفاً بصرياً عن كل حقول البحث الأخرى في التطبيق (الرئيسية/المتجر) رغم أن ثيم الإدخال العام للتطبيق موحَّد أصلاً. | حُرِّك حقل البحث خارج AppBar لجسم الصفحة (يرث تلقائياً نمط الحقل الأبيض المُقوَّس من ثيم التطبيق) + زر مسح («×») فعلي. **إضافة حقيقية جديدة:** «عمليات بحث سابقة» — عبر 3 دوال جديدة في `LocalCache` (`recentSearches`/`addRecentSearch`/`clearRecentSearches`، نفس نمط `myOrderIds` الموجود: قائمة محدودة بـ8 مع نقل المكرَّر للمقدمة) تُسجَّل **فقط عند الإرسال الصريح** (`onSubmitted`) لا مع كل ضغطة مفتاح مع التأخير الزمني، لتفادي تسجيل أجزاء بحث غير مكتملة؛ رقائق قابلة للنقر تُعيد تشغيل نفس البحث + زر «مسح الكل». أُضيف أيضاً شريط اختصارات فئات (`HomeCategory` نفسها، يتنقّل لصفحة الفئة الحقيقية `/category/:key`) في حالة الهبوط قبل البحث — فوق شبكة «كل المتاجر» الموجودة أصلاً وليس بديلاً عنها. **لم يُضَف قسم «الأكثر بحثاً»** — دكانجي لا يجمّع إحصاءات بحث عبر كل العملاء، فكان سيتطلب بيانات مُختلَقة خلافاً لـ«عمليات بحث سابقة» (حقيقية 100% وخاصة بكل جهاز). تحقّق شامل حي على المحاكي: حالة الهبوط (شريط الفئات + الشبكة الكاملة، بلا رأس قسم فارغ لعمليات بحث سابقة لأنها فارغة فعلاً)، التنقّل الفعلي من اختصار فئة إلى `/category/restaurants`، بحث فعلي ("zamza" نص لاتيني اختباري بسبب عجز `adb shell input text` عن كتابة العربية) أعاد «لا توجد نتائج» بشكل صحيح ثم ظهر كرقاقة بعد العودة، والنقر على الرقاقة أعاد تعبئة الحقل وكرَّر البحث بنجاح. `flutter analyze` نظيف طوال الجلسة (المحاكي لم ينهَر هذه الجولة خلافاً للجولة السابقة) | `dukkanci_customer_app/lib/{core/cache/local_cache,core/localization/app_strings,features/search/presentation/search_screen}.dart`, `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل). **بانتظار تنفيذ `git commit`**
| 2026-07-17 | استكمالاً لجلسة إعادة تصميم Flutter بأسلوب Yemeksepeti (السطر التالي أدناه): المستخدم أرسل 6 لقطات مرجعية إضافية تغطي «كل السلال» (Tüm sepetler)، تفاصيل السلة، «حسابي» (Hesabım)، «المفضلة» (Favoriler)، و«مركز المساعدة» (Yardım Merkezi)، وطلب محاكاتها | ليس خطأ — ميزة/تصميم. **اكتشاف معماري مهم:** لقطة «كل السلال» تعرض 3 سلال من 3 متاجر مختلفة (Baboş Döner + Köfteci Yusuf + McDonald's) في آن واحد — هذا **يناقض مباشرة** قاعدة دكانجي المعمارية الصلبة «سلة واحدة من متجر واحد فقط» (قسم 14 من مواصفات Flutter، `AddToCartResult.otherStoreConflict`، حوار التعارض المُطبَّق في كل شاشة أُعيد تصميمها هذه الجلسة) — لم تُحاكَ هذه الشاشة إطلاقاً لأنها لا تعبّر عن نقص ميزة بل تناقض حقيقي مع بنية التطبيق. كذلك رُفض تقليد: بانر عضوية مدفوعة «Ypro» (لا اشتراك عملاء في دكانجي)، قسم «Cüzdan» محفظة رصيد داخل التطبيق (الدفع نقداً/بطاقة عند الاستلام/تحويل بنكي فقط، لا رصيد مسبق الدفع)، قسم «Fırsatlar» (Ypro/YeClub/Kuponlarım — لا برنامج ولاء ولا نظام كوبونات، تحقّق سابق بالبحث في الكود)، أيقونة الإعدادات (⚙) في رأس «حسابي» (لا شاشة إعدادات لتفتحها)، وشارات خصومات الحملات المُختلَقة على بطاقات المفضلة («400₺'ye 300₺ indirim» إلخ). | **المفضلة** (`favorites/presentation/favorites_screen.dart`): صف فلاتر فئات **ديناميكي** يُبنى من الفئات الحقيقية الموجودة فعلاً بين متاجر مفضّلة المستخدم نفسه (عبر `HomeCategory`، وليس تبويبين ثابتين "مطاعم/سوبرماركت" كالمرجع) + بطاقة عمود واحد غنية (غلاف/شارة مفتوح/تقييم/وقت توصيل/رسوم لكل كم/حد أدنى/بانر عرض حقيقي إن وُجد) تحل محل الشبكة القديمة بعمودين. **حسابي** (`profile/presentation/profile_screen.dart`): 3 بطاقات وصول سريع (طلباتي/المفضلة/عناويني — الأخيرة تُوجّه بصدق لشاشة اختيار المنطقة الموجودة فعلاً، إذ لا دفتر عناوين منفصل)، قسم «المزيد» يفتح صفحتي الشروط والخصوصية **الحقيقيتين** على الموقع (`/terms` و`/privacy.html`، تحقّق بالبحث في `index.html` وليس تخميناً) عبر `url_launcher`، «عن التطبيق» عبر `showAboutDialog` المدمجة في Flutter (تعرض الشعار والاسم والإصدار الحقيقيين)، ورقم إصدار حقيقي في التذييل عبر حزمة `package_info_plus` (أُضيفت كتبعية مباشرة صريحة في `pubspec.yaml` بعد أن كانت غير مباشرة فقط). زر «حذف الحساب» (كان بلا وظيفة فعلية `onTap: () {}`) أصبح يفتح واتساب الدعم برسالة جاهزة لطلب الحذف — بما أنه لا توجد نقطة حذف ذاتي، التوجيه لموظف حقيقي قادر على التنفيذ أصدق من نافذة داخل التطبيق لا تُنجز شيئاً. **مركز المساعدة** (`support/presentation/support_screen.dart`): نفس أنواع المشاكل التسعة الحقيقية (توجّه لواتساب فعلياً كما كانت) أُعيد تنظيمها كقائمة مصنَّفة قابلة للبحث بدل رقائق متناثرة. تحقّق شامل حي على المحاكي (وليس افتراضاً): حسابي بكل أقسامه + نافذة «عن التطبيق» بالشعار والإصدار الحقيقيين، المفضلة (أُضيف متجر زمزم ماركت فعلياً من صفحة المتجر وظهر بالبطاقة الغنية الصحيحة بعد العودة)، الدعم (البحث وبطاقة واتساب والقائمة). **درس بيئي:** المحاكي انهار فعلياً (segfault) مرتين أثناء هذه الجولة بعد ~9 دورات بناء متتالية في نفس الجلسة الطويلة — تعافى بإعادة الإقلاع بلا فقدان بيانات (السلة والمفضلة نجتا). `flutter analyze` نظيف طوال الجلسة | `dukkanci_customer_app/lib/{core/localization/app_strings,features/favorites/presentation/favorites_screen,features/profile/presentation/profile_screen,features/support/presentation/support_screen}.dart` (معدَّلة) + `dukkanci_customer_app/pubspec.yaml`, `pubspec.lock` (إضافة `package_info_plus`) + `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل). **تم `git commit` محلياً بناءً على طلب المستخدم — بانتظار تعليمات `push` منفصلة**
| 2026-07-17 | المستخدم فحص تطبيق Flutter على المحاكي (استكمالاً لجلسة `2026-07-16` — إعادة تشغيل المحاكي بعد إعادة إقلاع الجهاز) ووصف التصميم بـ«ضعيف جداً في كل الأقسام»، ثم أرسل على 5 دفعات متتالية لقطات مرجعية من تطبيق منافس (Yemeksepeti) لكل من: الرئيسية، صفحة المتجر، صفحة تفاصيل المنتج (خيارات/إضافات)، السلة، والدفع — وطلب في كل مرة «محاكاة» نفس الأسلوب البصري لكن بهوية دكانجي الحمراء («قم بتغيير الهوية») وحسب إمكانيات دكانجي الفعلية («حسب إمكانيات دكانجي») | ليس خطأ برمجياً — الواجهة السابقة (شبكة بطاقات مسطّحة بلا هيرو ولا فئات ولا صفوف) كانت وظيفية لكن فقيرة بصرياً. **القرار المعماري الحاكم لكل الجلسة:** استنساخ *طريقة العرض والبنية* من المرجع حصراً، مع **رفض صريح ومتكرر** محاكاة أي عنصر مرجعي لا تدعمه بيانات دكانجي الفعلية بدل اختلاقه — تحقّق ذلك عبر استعلامات Supabase مباشرة قبل كل تصميم (توزيع الفئات/عدد العروض الحقيقية/إلخ) بدل الافتراض. عناصر رُفض تقليدها عمداً ووُثّق السبب في كل صفحة: (١) الرئيسية: بلا تغيير (لا عنصر محتاج رفض). (٢) المتجر: بلا تغيير. (٣) المنتج: «Bu hızlı seçenekleri deneyin» (تركيبات جاهزة) و«Yanında iyi gider» (توصيات شراء مرتبطة) — لا بيانات تركيبات/توصيات في `OptionGroup`/`Addon`. (٤) السلة: بانر اشتراك مدفوع «توصيل مجاني» (لا اشتراك عملاء في دكانجي) وحقل كوبون (تحقّق بالبحث: لا نظام كوبونات في التطبيق) — استُبدل قسم «اشتراها آخرون معاً» (لا بيانات شراء مشترك) بصف صادق «أضف المزيد من المتجر» بمنتجات حقيقية غير مُضافة بعد. (٥) الدفع: خيارات توصيل متعددة السرعات بأسعار مختلفة (Direct/Standard/Scheduled — دكانجي معدّل توصيل واحد ثابت لكل كم فقط، و`scheduleDay`/`scheduleTime` مُرسَلة فارغة دائماً في `OrderDraft` الحقيقي فلا توجد بنية جدولة أصلاً) وإجمالي «شامل الرسوم والضريبة» بخصم مُختلَق — استُبدل بإجمالي صادق (=المجموع الفرعي) مع توضيح «يتواصل معك المتجر عبر واتساب لتأكيد رسوم التوصيل» (يطابق فعلياً كيف تُحسم رسوم التوصيل على المنصة، بشرياً عبر واتساب لا آلياً). | **الرئيسية** (`home_screen.dart` + `home/domain/home_category.dart` جديد + 3 ودجات جديدة `promo_hero.dart`/`category_strip.dart`/`store_rail.dart`): هيدر أحمر متدرّج ممتد لأعلى الشاشة + هيرو بالسحب اليدوي فقط (بلا حركة تلقائية، نفس قرار رفض الحركة التلقائية الموثَّق سابقاً لشريط فئات الموبايل) من عروض حقيقية + شريط 8 فئات (تُجمَّع فئات `stores.category` القديمة المكرَّرة: «ملاحم»/«ملحمة ومشاوي»، «بن ومكسرات»/«مكسرات وبهارات») + صفوف أفقية (عروض/الأكثر رواجاً/مطاعم/حلويات/سوبر ماركت) بعرض بطاقة نسبي (بطاقتان كاملتان + إطلالة على الثالثة، بعد أن كانت أول نسخة تعرض 1.4 بطاقة مقطوعة بشكل غير لائق). صفحة فئة جديدة `/category/:key` (`features/category/presentation/category_screen.dart`) تُغذّى من مفاتيح حقيقية أو اصطناعية (`offers`/`popular`/`all`). **المتجر** (`stores/presentation/store_screen.dart`): غلاف SliverAppBar بأزرار دائرية (رجوع/مفضلة) + بطاقة معلومات (تقييم/وقت توصيل/رسوم لكل كم/حد أدنى/بانر عرض) + بحث داخل القائمة + تبويبات أقسام لاصقة تتبّع القسم المعروض عند التمرير + شبكة منتجات بعمودين مع زر «+» للإضافة السريعة + شريط سلة سفلي دائم. **المنتج** (`products/presentation/product_screen.dart`): شارة خصم % محسوبة تلقائياً من `oldPrice`/`price` + أقسام خيارات بشارة «مطلوب» سوداء وأقسام إضافات بشارة «اختياري» رمادية (كل صف يعرض دائماً كلمة سعر — «مجاناً» بدل إخفاء التسمية عند فرق سعر صفري) + حقل ملاحظات منسَّق. **السلة** (`cart/presentation/cart_screen.dart`): هيدر بزر إغلاق + اسم المتجر الحقيقي + صف «أضف المزيد من {المتجر}» + **إنفاذ حقيقي جديد** لـ`Store.minOrder` (تحذير برتقالي + تعطيل زر الدفع تحت الحد الأدنى — بيانات كانت موجودة بلا استخدام قط). **الدفع** (`checkout/presentation/checkout_screen.dart`): خطوات تقدّم «القائمة→السلة→الدفع» (تمثيل صادق للتنقّل الفعلي، مُصحَّح تلقائياً لاتجاه RTL) + تفعيل حقل `OrderDraft.addressDetails` الحقيقي غير المُستخدَم سابقاً («تفاصيل إضافية — الدور/الشقة») + مفتاح «اترك الطلب عند الباب» جديد (يُطوى كنص صادق داخل `notes` التي يقرأها التاجر فعلياً، لا عمود وهمي) + بطاقات طرق دفع منسَّقة (استبدال `RadioListTile` الافتراضي). **عطلان حقيقيان اكتُشفا وأُصلحا أثناء اختبار زر «+» في السلة/المتجر حياً (وليس افتراضاً):** (١) زر «+» متداخل داخل `PressScale` البطاقة كان يخسر النقرة دائماً (السلف يبقى في حلبة الإيماءات دوماً، و`HitTestBehavior.opaque` على الابن لا يُقصي سلفاً) — أُصلح بفصل طبقة نقر البطاقة (`Positioned.fill`) عن زر «+» كشقيقين في نفس `Stack` بدل علاقة أب/ابن. (٢) 🔴 **خلل حقيقي في كود السلة المشترك** (`cart/domain/cart_item.dart`): `CartItem.lineKey` كان يستدعي `...selectedAddonIds..sort()` الذي **يُعدِّل القائمة في مكانها**، فيرمي `Cannot modify an unmodifiable list` كلما كانت القائمة الافتراضية الثابتة `const []` (تحديداً زر «+» السريع الذي لا يمرّر إضافات) — كان كامناً لأن المسار الوحيد السابق (تفاصيل المنتج) يمرّر دائماً قائمة قابلة للتعديل. أُصلح بفرز نسخة `[...selectedAddonIds]..sort()` بدل الأصل. **درس تشخيصي مهم:** بعد إعادة التصميم، بدا زر «تأكيد الطلب» في صفحة الدفع معطوباً تماماً عبر ~8 محاولات نقر — تبيّن عبر `adb logcat`/`uiautomator dump` أن السبب **تجمّد فعلي للتطبيق (نافذة نظام «isn't responding»)** من تراكم دورات بناء/تنصيب الجلسة الطويلة، وليس خللاً برمجياً — تأكَّد الزر يعمل تماماً بعد ضغط «Wait» واستقرار التطبيق (رسالة تحقق «يرجى إدخال اسمك للتواصل» ظهرت بشكل صحيح ودون أي اتصال شبكي فعلي). تحقّق شامل حي على المحاكي لكل الصفحات الخمس عبر لقطات فعلية وتفاعل مباشر (نقر/سحب/تبديل)، وليس افتراض نجاح — بما في ذلك اختبار حساب إجمالي حي دقيق مرتين (354₺=269+60+25 في المنتج، 454₺=220+75+29+130 في السلة). `flutter analyze` نظيف طوال الجلسة (نفس 3 تحذيرات معلوماتية قديمة غير كاسرة فقط، لا علاقة لها بالتعديلات) | `dukkanci_customer_app/lib/{app/providers,core/localization/app_strings,core/routing/app_router,core/routing/app_routes,features/cart/domain/cart_item,features/cart/presentation/cart_screen,features/checkout/presentation/checkout_screen,features/home/presentation/home_screen,features/products/presentation/product_screen,features/stores/presentation/store_screen}.dart` (معدَّلة) + `dukkanci_customer_app/lib/features/{category/presentation/category_screen,home/domain/home_category,home/presentation/widgets/category_strip,home/presentation/widgets/promo_hero,home/presentation/widgets/store_rail}.dart` (جديدة)، `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل عن `app.js`/`styles.css`/`index.html`/`sw.js`). **تم `git commit` محلياً بناءً على طلب المستخدم — لم يُنفَّذ `git push` بعد (طلب صراحة: بدون push)**
| 2026-07-17 | المستخدم طلب إضافة مطعم/ملحمة «ATA ET» عبر رابط Linktree (`linktr.ee/ata_et`) — أول رابط Linktree كمصدر لهذا المشروع (وليس رابط قائمة/كتالوج مباشر) | ليس خطأ — ميزة إضافة جديدة. Linktree احتوى فقط 4 روابط فرعية (قائمة، خريطة، واتساب، انستغرام/فيسبوك) بلا أي بيانات مباشرة | **مصدر جديد ضمن عائلة trybany.com المعروفة مسبقاً (سبق استخدامها لمتجر Orange Turkey id 34)، لكن بخاصية جديدة لم تظهر سابقاً:** رابط القائمة في اللينكتري (`app.trybany.com/ar/ata-pide-f/...`) لا ينتهي برقم (خلافاً لنمط `orange-turkey-166` الموثَّق سابقاً حيث `idString` = الرقم اللاحق للـslug) — بل `idString` الحقيقي كان الحرف المفرد **`f`**، واكتُشف عبر `GET https://trybany.com/api/v1/entities/<slug-كامل>` (وليس اقتطاع رقم من الslug) الذي يُعيد التوجيه/يحل الكيان الصحيح مباشرة من الـslug الكامل. بقية الوصفة مطابقة تماماً للموثَّق سابقاً: `GET entities/<id>` (فئات+هاتف+واتساب+شبكات)، ثم `GET entities/<id>/categories/<catId>/products` لكل فئة. 118 منتجاً عبر 9 فئات (فطائر شامية 33، فطائر اللحوم 18، اللحوم النية 15، البيتزا 16، سواريه 8، لحومات بالفرن 4، الدجاج 5، المفرزات 9، المشروبات 10) — 113 منها بصورة حقيقية فريدة (5 بلا صورة استُبعدت تلقائياً عبر `applyPublishingRules`، لا تدخّل يدوي). **مطابقة Google Maps مؤكَّدة بدقة عالية جداً:** بحث نصي بالاسم بالقرب من إحداثيات اللينكتري أعاد تطابقاً حرفياً لرقم الهاتف بالكامل (`+90 552 299 61 27` = نفس رقم واتساب المُرسَل تماماً) — **الاسم العربي الرسمي على غوغل «ملحمة ومعجنات العطا»** (وليس "ATA PIDE"/"ata_et" كما تظهر أسماء حسابات التواصل الاجتماعي فقط) اعتُمد كاسم المتجر وفق قاعدة «الاسم الحقيقي من المصدر لا اختراع» — يؤكده أيضاً شعار المتجر نفسه («ALATA ET» + شعار فرعي «Kasap - Fırın» أي بقّال/ملحمة-فرن، مطابق تماماً لتصنيف غوغل). **تصنيف هجين حقيقي (ملحمة+مخبز/بيتزا):** الكتالوج يحوي 15 صنف لحم نيء يُباع بالكيلو (هبرة/فخد/كتف/موزات/لية) بجانب 98 صنف طعام جاهز (فطائر/بيتزا/دجاج/مفرزات/مشروبات) — اعتُمدت فئة **"ملاحم"** (مطابقة لاسم المتجر الرسمي على غوغل الذي يبدأ بكلمة «ملحمة» وللشعار الذي يضع «Kasap» أولاً) بدل "مطاعم"، رغم أن سابقة مشابهة (ميت موت، id 71) صُنِّفت "مطاعم" — قرار حالة بحالة وليس قاعدة صارمة. المتجر رقم **98** (تحقّق `max(id)`=97 مباشرة قبل التنفيذ)، حزمة معرّفات المنتجات **1800001–1800113**. صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82 mozjpeg، شعار 400×400، غلاف 1280×853 من صورة منتج حقيقية "بيتزا العطا كبير" لعدم وجود بانر تسويقي مخصَّص في المصدر — نفس نمط متاجر سابقة بلا غلاف). ساعات العمل ودرجة التقييم (4.4/130) من Google Places API الحقيقي (`opening_hours.weekday_text`: يومياً 9-9 والثلاثاء مغلق). `approval_status='pending'` عمداً (طرف ثالث حقيقي غير مؤكَّد كشراكة). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`98: "alata-et-basaksehir"`) + `<script src="/alataet-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع عبر Supabase MCP `execute_sql` (صف المتجر + 113 صف منتج، `jsonb_to_recordset`، `price::numeric` cast، معاملتان منفصلتان). تحقّق شامل عبر preview محلي: `/store/alata-et-basaksehir` يعرض العنوان الصحيح (`دكانجي - ملحمة ومعجنات العطا`) والوصف والشعار والغلاف وكل الفئات التسع بعناصرها الصحيحة (113/113 من Supabase، عدّاد كل فئة مطابق تماماً لحساب الاستبعاد اليدوي)، كل الـ115 رابط صورة (113 منتج + شعار + غلاف) أعادت HTTP 200 فعلياً (تحقّق جماعي `fetch HEAD` من الصفحة الحية)، `/category/butcher` والصفحة الرئيسية بلا أي أثر للمتجر (سلوك `pending` صحيح)، صفر أخطاء كونسول على الصفحات الثلاث. `npm run check:versions` ناجح | `alataet-data.js` (جديد)، `assets/photos/alataet/**` (جديد، 115 ملف: 113 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 98 وصفوف `products` 1800001-1800113 في Supabase | نعم — `npm run check:versions` ناجح (app.js v307→308، store-slugs.js v246→247، CACHE v227→228). **مدفوع لـ`main`** (commit `3d82db91`، `git push origin main`) بناءً على طلب المستخدم «اعمل commit وادفعها لـ main» — عُزلت التعديلات عن ملفات أخرى غير ذات صلة موجودة في نفس شجرة العمل (`api/notify-order.js`, تعديلات `dukkanci_customer_app/`, ملفات/مجلدات غير متتبَّعة أخرى) بـ`git add` انتقائي للملفات الستة + مجلد الصور فقط، لم تُلمس. تحقّق بعد النشر: Vercel نشر بنجاح (`app.js?v=308` و`assets/photos/alataet/*` كلها HTTP 200 على `www.dukkanci.com.tr`)؛ عنوان SSR الخام لـ`/store/alata-et-basaksehir` لا يزال العنوان العام (متوقَّع تماماً بسبب `approval_status='pending'` — `api/store.js` سطر 54 يُسقط بيانات SEO لأي متجر غير `approved` عمداً)، لكن تصيير SPA الفعلي (بعد تحميل JS) يعرض العنوان والمحتوى الصحيحين كما تحقَّق محلياً. **تحديث لاحق (نفس اليوم):** بطلب صريح من المستخدم «اعتمد المتجر»، حُدِّث `approval_status` إلى `'approved'` مباشرة عبر Supabase (`update stores set approval_status='approved' where id=98`) — تحقّق حي أن SSR `/store/alata-et-basaksehir` أصبح يعرض العنوان الصحيح فوراً (`دكانجي - ملحمة ومعجنات العطا`) وأن صف Supabase يعكس الاعتماد (`approval_status='approved', category='ملاحم'`)؛ المتجر أصبح ظاهراً الآن في قسم "ملاحم" والصفحة الرئيسية لكل الزوار
| 2026-07-16 | المستخدم طلب «create the emulator and test the order flow — you can use my computer». **تدفّق الطلب في تطبيق Flutter لم يكن قد اختُبر حياً قط** (أُعيدت كتابته على المخطط الحقيقي في الجلسة السابقة لكن بلا تشغيل فعلي) | 4 أعطال حقيقية إضافية لم يكشفها `flutter analyze` النظيف ولا البناء الناجح: (1) **السلة تُنسى عند إغلاق التطبيق** — `CartController.build()` كان يستدعي `_restore()` **غير متزامنة** تضبط `state` ثم يُعيد `const CartState()` فارغة **تطمسها**؛ لعدم وجود `await` داخل `_restore` كان الترتيب معكوساً دائماً. (2) **المنطقة المختارة تُنسى كل تشغيل** (`LocationController` في الذاكرة فقط). (3) 🔴 **شاشة السلة فارغة تماماً (P0 — يمنع الشراء نهائياً)**: شريط الملخّص كان يلفّ عمود النص بـ`Expanded` داخل `Row`؛ النتيجة أن **الشريط بأكمله والقائمة فوقه توقّفا عن الرسم بلا أي استثناء في السجل** — أعراض مضلِّلة تماماً (بيانات صحيحة `items=1`، AppBar يظهر، صفر أخطاء، والخلفية كريمية لا بيضاء). (4) **انقطاع واتساب كان سيرفض كل الطلبات**: `sendOtp` كان يرمي دائماً بينما الموقع (`startCheckoutOtp`) يمرّر الطلب عند `soft`. | **البيئة:** لا محاكي ولا `cmdline-tools` ولا صور نظام — نُزِّلت من مستودع جوجل الرسمي + صورة android-35 (3.5GB) وأُنشئ AVD (`dukkanci_test`، Pixel 7). **عقبة مهمة للجلسات القادمة:** Android Studio **حدّث نفسه أثناء الجلسة** (~04:20) فترك JBR مبتوراً في `Android Studio\jbr` (بلا `jvm.cfg`) ونسخة سليمة في **`Android Studio1\jbr`** — استُخدمت الأخيرة كـ`JAVA_HOME`. **الأمان قبل الاختبار (الأهم):** أي طلب اختباري يُرسل **واتساب حقيقياً لتاجر حقيقي** + نسخة لأرقام الإدارة، و`is_test_order` **لا يكبح الإشعار** (مجرد عمود). لذا: متجر اختبار `id=999` بـ`pending` (غير مرئي لأحد) وبلا هاتف، + الاختبار ضد **خادم محلي** (`.env` بلا مفاتيح واتساب/VAPID → صفر إشعارات لأي أحد). **التشخيص:** بعد فرضيتين خاطئتين مني (`MainAxisSize.min`، ثم «شريط أبيض يملأ الشاشة» — دُحضت بعيّنة بكسل: كريمي `(255,249,245)` لا أبيض)، حُسم بالتنصيف: جسم أحمر+شريط أزرق ⇒ يعملان؛ القائمة الحقيقية+شريط بسيط ⇒ تعمل ⇒ الجاني `_summaryBar` حصراً. أُعيد بناؤه بنمط `product_screen.dart` المُثبَت. **تحقّق حي كامل بلقطات:** تعريفية → مناطق → رئيسية بأغلفة حقيقية → رابط عميق → منتج → سلة → دفع → **طلب حقيقي `DK-4172774138` وصل `orders`** بكل الحقول صحيحة → شاشة تتبّع بالخط الزمني. الكتابة المزدوجة تصرّفت كما صُمِّمت: الأساسي نجح، والثانوي فشل بـRLS 42501 (متجر الاختبار `pending` — أثر اختبار) و**ابتُلع بلا كسر الطلب** كما يوجب [[orders-silent-save-failure]]. **التنظيف:** حُذف المتجر 999 والمنتج 9990001 وكل طلبات الاختبار (تحقّق `max(id)=97`)، أُوقف الخادم المحلي، أُزيل التطبيق من المحاكي، صفر كود تشخيصي متبقٍّ | `dukkanci_customer_app/lib/{core/auth/auth_repository,core/cache/local_cache,features/cart/application/cart_controller,features/cart/presentation/cart_screen,features/checkout/presentation/checkout_screen,features/location/application/location_controller,features/splash/presentation/splash_screen}.dart`، `dukkanci_customer_app/README.md`، `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل). **مهام مفتوحة (README):** (1) 🔴 `source` يُسجَّل `'web'` لكل طلبات التطبيق (النقطة المنشورة لا تقرأ `body.source`) فلا تمييز في التقارير، (2) 🔴 السعر يُرسَل من العميل ورسوم التوصيل غير محسوبة — **يجب الترحيل لنقطة `create-order`** (تحقّق سعر خادمي، قسم 14) فور نشر الجلسة الأخرى لها، (3) إشعار واتساب للمتجر لم يُختبر عمداً |
| 2026-07-16 | المستخدم طلب تعديل مربع الاشتراك بالنشرة عبر واتساب في الفوتر: جعل بادئة «+90» على اليسار وزر «اشترك» على اليمين (كان العكس) | ليس خطأ — طلب تصميم مباشر. ترتيب DOM في `index.html` كان `prefix, input, button`؛ لأن الصفحة `dir="rtl"`، أول عنصر في DOM يظهر أقصى اليمين تلقائياً بلا أي `flex-direction`/`order` مضبوطة، فكانت النتيجة: «+90» أقصى اليمين، الزر أقصى اليسار — عكس المطلوب | حُلّ بـCSS فقط (بلا تعديل HTML): أُضيف `order` على العناصر الثلاثة داخل `.footer-subscribe__field` في `styles.css` (الزر `order:1`، الحقل `order:2`، البادئة `order:3`) فيصبح الزر أقصى اليمين والبادئة أقصى اليسار. بُدِّل جانب الفاصل البصري (`border-inline-end`→`border-inline-start`, وكذلك `padding`/`margin`) على `.footer-subscribe__prefix` وحواف `.footer-subscribe__field` (`padding-inline-start`/`end`) لأن جار البادئة تغيّر جهته. أُضيف أيضاً `order` مقابل داخل استعلام الموبايل (`≤640px`) ليبقى ترتيب التكديس القديم كما هو (الزر بعرض كامل في الأسفل) بلا أي تغيير هناك. تحقّق عبر preview محلي (لقطات الشاشة معطّلة في بيئة الجلسة، نفس العطل الموثَّق سابقاً — التحقّق تم عبر `getBoundingClientRect`): سطح المكتب أظهر الزر أقصى اليمين (x 353-444) والبادئة أقصى اليسار (x 39-76) والحقل بينهما؛ الموبايل (375px) أظهر تكديس الصف الأول (بادئة+حقل) والزر أسفله بعرض كامل، مطابقاً للتصميم الأصلي قبل التعديل؛ صفر أخطاء كونسول في كلا العرضين | `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (styles.css v173→174، CACHE v226→227). **مدفوع لـ`main`** بناءً على طلب المستخدم «اعمل commit وادفعها لـ main» |
| 2026-07-16 | **أول اختبار فعلي لتطبيق Flutter على جهاز حقيقي** (Samsung SM-N985F، أندرويد 13) كشف أن التطبيق «أساسي جداً بلا وظائف، والتصميم سيء وبه أخطاء كثيرة وبلا أي تجربة استخدام» (نص المستخدم)، وأظهر خطأ «حدث خطأ ما، حاول مرة أخرى» على الصفحة الرئيسية | 🔴 **الدرس الجوهري لهذه الجلسة: `flutter analyze` نظيف + `flutter build apk` ناجح لا يعنيان أن التطبيق يعمل إطلاقاً.** كل النماذج (`Store`/`Product`/`OrderDraft`) كُتبت في جلسة البناء الأولى على مخطط قاعدة بيانات **مُخمَّن** بدل قراءة المخطط الحقيقي عبر `information_schema` وبيانات فعلية — والنتيجة 5 أعطال حقيقية لا يكشفها أي فحص ثابت: (1) `stores.time` نوعه **text** («45 - 75 دقيقة») لا رقم، فـ`(json['time'] as num?)` رمى `type 'String' is not a subtype of type 'num?'` وأسقط الصفحة الرئيسية بالكامل. (2) الاستثناء الحقيقي **لم يظهر في أي سجل** لأن كل مستودع كان يبتلعه بـ`catch (_)` صامت ويستبدله برسالة عامة — لهذا استغرق تشخيصه جلسة كاملة بدل دقيقة. (3) مسارات الصور في القاعدة **نسبية** (`/assets/photos/…`) تعمل على الموقع لأنه نفس الأصل، وتفشل صامتة في تطبيق جوال بلا أصل. (4) نموذج خيارات/إضافات المنتج كان **مُخترعاً بالكامل** (`{id, choices[], required}`) بينما الحقيقي `{name, values[], extra[]}` + إضافات مسطّحة `{name, price}` — كان سيُسقط أي منتج بخيارات. (5) `OrderDraft` كُتب على أعمدة **غير موجودة أصلاً** (`subtotal`/`delivery_fee`/`contact_phone`/`address`) بينما الجدول الحقيقي `id` نصي بصيغة `DK-…` + `delivery_details` jsonb + `items` عدد صحيح (عدد الأصناف لا قائمتها) + حالات **عربية** (`طلب جديد`…) لا enum إنجليزي — أي أن **الطلب لم يكن ليُحفظ إطلاقاً**. كما أن التصميم كان مكوّنات Material افتراضية بلا نظام تصميم (لا سلّم خطوط، لا إيقاع مسافات، سبينر بدل هياكل تحميل، `Text()` عارية لكل حالة خطأ) | **(أ) تشخيص حقيقي وليس تخميناً:** بعد فشل USB (تعريف Samsung ADB غير مثبَّت على ويندوز — «Unknown USB Device») استُخدم **Wireless debugging** (`adb pair` بكود + `adb connect`)، ثم `flutter run` حي على الجهاز؛ أُضيف `debugPrint(e, st)` لكل `catch` أولاً فظهر الاستثناء الحقيقي فوراً في السطر الأول من السجل. **(ب) قراءة المخطط الحقيقي:** عبر Supabase MCP `execute_sql` على `information_schema.columns` لجداول `stores`/`products`/`orders` + عيّنات بيانات فعلية + قراءة `app.js` نفسه (`mapDbStore`, `mapDbProduct`, `newOrder`, `pushOrderCloud`, `notifyOrderWhatsapp`, قائمة الحالات سطر ~11250) — كل نموذج أُعيدت كتابته على الحقيقة الموثَّقة بتعليق يشرح المصدر. `OrderRepository` الآن يطبّق **نفس الكتابة المزدوجة** الحقيقية: `POST /api/notify-order` (الحفظ الموثوق service-role + إشعار واتساب) + upsert مباشر `onConflict: id` (نسخة احتياطية، فشلها مبتلَع) — مع تعليق صريح يشير لحادثة فقدان الطلبات الصامت [[orders-silent-save-failure]]. `fetchMyOrders` يستخدم `action=customer-orders` الحقيقي (بالهاتف + معرّفات الطلبات المحفوظة محلياً) **وليس** استعلام «كل الطلبات» من العميل (تفادي إعادة إنتاج ثغرة الخصوصية المُصلَحة خادمياً سابقاً). **(ج) الوظائف الناقصة:** المفضلة تُحفظ فعلاً (`LocalCache`)، فلاتر البحث تعمل فعلاً (`مفتوح الآن`/`الأعلى تقييماً`/`عروض` + `الأقرب` عبر haversine حقيقي)، `LocationController` يحفظ الموقع المختار ويظهر في رأس الرئيسية، قائمة مناطق الإطلاق الحقيقية (قسم 41)، شريط أقسام لاصق قافز في صفحة المتجر (قسم 12)، سحب-للحذف في السلة، خط زمني حقيقي لحالة الطلب (بلا خريطة سائق وهمية — قسم 18)، ورقم خدمة العملاء الحقيقي من `index.html` بدل رقم وهمي. **(د) نظام تصميم كامل** عبر skill `ui-ux-pro-max` (نمط Food Delivery الموصى به، مطبَّق على ألوان دكانجي الحقيقية من `styles.css` لا على لوحة بديلة): `app_spacing.dart` (إيقاع 4/8dp + أنصاف أقطار الموقع + رموز حركة)، `app_text_styles.dart` (سلّم صريح + أرقام جدولية للأسعار)، `app_theme.dart` موسّع، ومكوّنات مشتركة `PressScale`/`ShimmerBox`+`StoreCardSkeleton`/`AppErrorView`+`AppEmptyView`+`AppLoadingView`/`OpenClosedBadge`+`RatingPill` (لا لون وحده يحمل المعنى، لمس ≥44pt). أُعيد تصميم **كل** شاشة (13 شاشة). **التحقق:** `flutter analyze` صفر أخطاء/تحذيرات (5 معلومات ترقية API غير كاسرة)، و`flutter build apk --debug` ناجح فعلياً → `app-debug.apk` **209MB** حقيقي على القرص. ⚠ **لم يُختبر تدفّق الطلب حياً بعد** (الاتصال اللاسلكي انقطع تكراراً أثناء تثبيت 209MB) — موثَّق في README كأهم بند للجلسة القادمة | `dukkanci_customer_app/**` (13 شاشة معاد تصميمها + 8 ملفات نظام تصميم/مكوّنات جديدة + إصلاح 3 نماذج بيانات + مستودعان)، `dukkanci_customer_app/README.md` (سجل إصدارات + قائمة صادقة لما هو حقيقي/ناقص)، `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل عن `app.js`/`styles.css`/`index.html`/`sw.js`). **لم يُنفَّذ `git commit` بعد — بانتظار تعليمات المستخدم**
| 2026-07-16 | متابعة للسطر التالي أدناه: المستخدم طلب صراحة «Keep Capacitor running, start Flutter as v2 in parallel» — الشروع الفعلي في بناء تطبيق Flutter الأصلي الموصوف في `docs/تطبيق-اندرويد-Flutter-متطلبات.md`، بمعزل تام عن Capacitor الحالي | ليس خطأ — أول جلسة تنفيذ فعلي لمشروع Flutter (لا يوجد سبب جذري، فقط قرار معماري كبير: v2 مستقل بمجلده الخاص، v1/Capacitor يبقى حياً بلا لمس). **قرار معماري جوهري اكتُشف أثناء العمل ووُثِّق بالتفصيل:** الموقع الحالي لا يملك `/api/v1` نظيفاً كما تفترض المواصفات — `app.js` يقرأ `stores`/`products` مباشرة من Supabase (anon key + RLS) ويكتب الطلبات بنفس الطريقة مع إشعار واتساب منفصل عبر `/api/notify-order`؛ اتُّبع نفس النمط في تطبيق Flutter بدل اختراع طبقة API غير موجودة فعلياً | **البيئة:** لا Flutter SDK مثبَّتاً على الجهاز أصلاً — استُنسخ Flutter 3.44.6 (stable) عبر `git clone --depth 1 -b stable` إلى `C:\projects\flutter-sdk` (خارج مجلد المستودع عمداً). Android SDK/JDK كانا موجودَين مسبقاً من تثبيت Android Studio السابق لمشروع Capacitor (platform 36.1، build-tools، وJBR OpenJDK 21 كافيان لـGradle الحديث) — لم يحتج الأمر تثبيت JDK منفصل. **المشروع:** أُنشئ `dukkanci_customer_app/` (مشروع Flutter كامل مستقل، `flutter create --platforms=android`) بجذر المستودع، بمعزل تام عن `android/`/`ios/`/`capacitor.config.json`/`www-shell/` الخاصة بـCapacitor (لم يُلمس أيٌّ منها). `applicationId`/`namespace` عُدِّلا يدوياً بعد الإنشاء من الافتراضي (`com.dukkanci.dukkanci_customer_app`) إلى `com.dukkanci.app` (نفس معرّف Capacitor v1 — قرار: نفس الهوية النهائية، Flutter وCapacitor يتقاسمانها أثناء التطوير المتوازي، لا تعارض لأن لا نشر فعلي متزامن للاثنين على نفس الجهاز عادة) — شمل ذلك نقل `MainActivity.kt` لمسار الحزمة الجديد يدوياً. **إصدارات Android/Gradle كلها افتراضية من Flutter 3.44.6 الحالي نفسه بلا أي تعديل يدوي**: `compileSdk`/`targetSdk`=36، `minSdk`=24، `ndkVersion`=28.2.13676358 (يدعم 16KB page size)، AGP 9.0.1، Kotlin 2.3.20، Gradle 9.1.0 — مطابقة **حرفياً** لكل أرقام قسم 3 من المواصفات دون الحاجة لأي رفع يدوي (لأن Flutter الحالي حديث بما يكفي وقت الكتابة). **الحزم المضافة** (عبر `flutter pub add`، لا أرقام إصدار مخمَّنة يدوياً): `flutter_riverpod`, `go_router`, `dio`, `freezed_annotation`/`freezed`/`build_runner`/`json_serializable` (كتبعية جاهزة، لكن النماذج الحالية مكتوبة يدوياً بلا codegen فعلي تفادياً لهشاشة build_runner في دفعة أولى)، `flutter_secure_storage`, `shared_preferences`, `cached_network_image`, `google_maps_flutter`, `geolocator`, `connectivity_plus`, `supabase_flutter`, `intl` (مثبَّت على 0.20.2 قسراً بسبب تعارض إصدار مع `flutter_localizations` من الـSDK)، `firebase_core`/`messaging`/`crashlytics`/`analytics` (حزم فقط، **لا تهيئة فعلية بعد** — تحتاج مشروع Firebase حقيقي)، `url_launcher`, `uuid`, `flutter_launcher_icons` (استُخدم فعلاً لتوليد الأيقونة من `resources/icon.png` الحقيقي). **البنية الكاملة** (41 ملف Dart): `lib/core/{api,auth,cache,config,errors,localization,routing,theme,utils}` + `lib/features/{splash,onboarding,location,home,stores,products,search,cart,checkout,orders,favorites,profile,notifications,support}` كل واحدة بطبقات `data/domain/application/presentation` حسب المطلوب في قسم 29. **معمارية القراءة/الكتابة الحقيقية** (وليست بيانات وهمية): `StoreRepository`/`Store`/`Product` تقرأ مباشرة من نفس جداول Supabase (`stores`/`products`) بنفس مفتاح anon publishable الموجود فعلاً في `supabase-config.js` بجذر الموقع؛ `CartController` (Riverpod `Notifier`) يطبّق قاعدة "سلة من متجر واحد فقط" (قسم 14) بحرفية نص الحوار المطلوب؛ `AuthRepository`/`OrderRepository` يستخدمان **نفس** مسارات `/api/notify-order?action=send-order-otp/verify-order-otp/verify-login-otp` الحقيقية المستخدَمة في `app.js` (وليس OTP عام مخترَع)، و`OrderRepository.submit()` يطبّق **الكتابة المزدوجة** (INSERT مباشر في `orders` + استدعاء إشعار واتساب منفصل، مع ابتلاع فشل الإشعار فقط) — تعليق صريح في الكود يشير لحادثة فقدان الطلبات الصامت الحقيقية الموثَّقة في [[orders-silent-save-failure]] كتحذير من تبسيط هذا لكتابة واحدة. `Idempotency-Key` (UUID) يُولَّد مرة واحدة لكل محاولة تأكيد طلب ويُعاد استخدامه عبر إعادة المحاولات (قسم 16). موقع تحديد الموقع يطلب صلاحية Foreground-only فقط عند الضغط الصريح (لا صلاحية خلفية إطلاقاً، قسم 9). RTL بالكامل + عربي فقط (`Directionality` مفروضة + `AppStrings` مركزية بلا أي `.arb`/تبديل لغة، حسب تعديل اللغة في نفس ملف المواصفات). `AndroidManifest.xml` أُضيفت له أذونات `INTERNET`/`ACCESS_NETWORK_STATE`/`ACCESS_FINE_LOCATION`+`COARSE`(لا `BACKGROUND`)/`POST_NOTIFICATIONS` + meta-data مفتاح خرائط جوجل (عبر `manifestPlaceholders`، فارغ افتراضياً، يُمرَّر عبر `-PmapsApiKey`/`MAPS_API_KEY`) + intent-filter لروابط `https://www.dukkanci.com.tr/store|product|order/*` (App Links، `autoVerify=true`، يحتاج نشر `assetlinks.json` لاحقاً) + مخطط داخلي احتياطي `dukkanci://`. **تحقّق فعلي مزدوج (ليس افتراض نجاح):** (1) `flutter analyze` نظيف تماماً (صفر أخطاء/تحذيرات، 9 معلومات `info` فقط عن ترقيات API غير كاسرة مؤجَّلة: `Supabase.anonKey→publishableKey`، `Radio.groupValue/onChanged→RadioGroup`) بعد إصلاح 3 أخطاء حقيقية اكتُشفت (استيراد `flutter_localizations` مفقود لـ`GlobalMaterialLocalizations.delegates`، `uuid` تبعية عابرة غير مصرَّح بها مباشرة، ملف الاختبار الافتراضي لا يزال يشير لـ`MyApp` المحذوفة). (2) **`flutter build apk --debug` نجح فعلياً من الصفر** (لا Gradle مهيَّأ مسبقاً) خلال ~38 دقيقة (تثبيت NDK 28.2 + Build-Tools 36 + منصات 34/35/36 تلقائياً أثناء البناء)، أنتج `app-debug.apk` حقيقياً (170MB، تحقّق `file`/حجم فعلي على القرص) — يثبت أن تسمية الحزمة/المانيفست/إصدارات SDK/كل التبعيات الأصلية (Firebase, Maps, geolocator, secure_storage) تُبنى فعلاً معاً بلا تعارض، وليس مجرد كود لم يُختبر تجميعه. تحذير غير حاجب من Gradle: `firebase_analytics` لا يزال يطبّق Kotlin Gradle Plugin بالطريقة القديمة (لا Built-in Kotlin) — إصدارات Flutter مستقبلية قد ترفض هذا، يحتاج متابعة تحديثات الحزمة. لم يُشغَّل `flutter run` على جهاز/محاكي فعلي (لا محاكي مهيَّأ في هذه الجلسة) ولا Firebase/مفتاح خرائط حقيقيَّين — موثَّق كخطوات يدوية متبقية في `dukkanci_customer_app/README.md` | `dukkanci_customer_app/**` (مشروع Flutter كامل جديد، غير مضاف لـgit بعد)، `CLAUDE.md` | لا ينطبق (مشروع Flutter منفصل تماماً عن `app.js`/`styles.css`/`index.html`/`sw.js` للموقع). **لم يُنفَّذ أي `git add`/`commit` لمجلد `dukkanci_customer_app/` — بانتظار تعليمات المستخدم قبل إدراجه في التتبّع**
| 2026-07-16 | المستخدم أرسل وثيقة تعليمات تنفيذ كاملة لتطبيق دكانجي على أندرويد (Flutter أصلي، عميل فقط) + تعديل لاحق يحصر اللغة بالعربية فقط RTL بلا تركية/إنجليزية | ليس خطأ — استلام مواصفات مشروع جديد ضخم بلا طلب تنفيذ فوري مرفق. **اكتشاف تعارض حقيقي مهم أثناء المراجعة:** المستودع يحتوي بالفعل مساراً منشوراً/موثَّقاً لتطبيق الجوال (`android/`, `ios/`, `capacitor.config.json`, `www-shell/`, موثَّق في `docs/دليل-تطبيق-الجوال.md` بتاريخ 2026-07-09) مبنياً بالكامل على **Capacitor** (`server.url` يفتح `https://www.dukkanci.com.tr` — أي غلاف WebView حول الموقع الحي) — وهذا بالضبط ما تمنعه الوثيقة الجديدة صراحة كأول وأهم قرار فيها ("ممنوع تحويل الموقع الحالي إلى WebView ووضعه داخل APK... النسخة الأولى الناجحة ليست... WebView على أنه تطبيق احترافي"). لم يُطلب تنفيذ فوري، وبناء تطبيق Flutter كامل مهمة متعددة الجلسات ضخمة الحجم لا يجوز البدء بها بلا تفويض صريح | حُفظت الوثيقتان كاملتين (المواصفات + تعديل اللغة) في `docs/تطبيق-اندرويد-Flutter-متطلبات.md` مع توثيق صريح للتعارض مع Capacitor الحالي وخيارين للمستخدم (استبدال كامل لاحق أو إبقاء Capacitor مؤقتاً وبناء Flutter كنسخة v2 موازية). **لم يبدأ أي تنفيذ فعلي** (لا مشروع Flutter، لا حذف Capacitor) — بانتظار قرار صريح من المستخدم بشأن أي المسارين، ثم البدء بمرحلة تخطيط/scaffold منفصلة عند التفويض | `docs/تطبيق-اندرويد-Flutter-متطلبات.md` (جديد)، `CLAUDE.md` | لا ينطبق (لا تعديل على app.js/styles.css/index.html/sw.js) |
| 2026-07-16 | المستخدم طلب «اعمل لوغو للمطعم» لمتجر «بيت حلب» (97) — الشعار الحالي كان صورة بروفايل إنستغرام منخفضة الدقة (100×100، اضطرارية لعدم وجود شعار عالي الدقة في مصدر الاستخراج الأصلي، موثَّق في سطر 2026-07-15 أدناه) | ليس خطأ — طلب تصميم جديد. الرصيد المتاح لتوليد الصور في هذه الجلسة كان شبه منتهٍ (1.14 نقطة بعد التوليد) فتعذّر توليد أكثر من محاولة واحدة أو بدائل عند الحاجة | وُلِّد شعار دائري بهوية عنّابي/ذهبي (يليق باسم «بيت حلب» ومدينة حلب) عبر nano_banana_pro: برج قلعة حلب في المنتصف + زخرفة هندسية إسلامية أسفل الدائرة + اسم المطعم بخط عربي حر أعلى الدائرة. **خطوة تحقّق إلزامية قبل الاعتماد (درس من فئة "التحقق من نص عربي مولَّد بالذكاء الاصطناعي" الموثَّقة سابقاً في المشروع):** الخط المتموّج الحر معروف بأنه قد يُنتج حروفاً غير دقيقة رغم مظهره المقنع بصرياً؛ لم أعتمد الشعار مباشرة بل بنيت صفحة مراجعة (Artifact) تعرض الشعار كاملاً + تقريب مكبَّر على منطقة النص، وطلبت من المستخدم تأكيد صحة القراءة قبل أي تطبيق حي — أكّد المستخدم صراحة أن «النص واضح وصحيح» قبل المتابعة. صُغِّر الشعار من 1024×1024 الخام إلى 400×400 PNG (نفس معيار شعارات المتاجر الأخرى) وحلّ محل `logo.jpg` القديم (100×100) في `assets/photos/beythalep/logo.png`. وُصلت الأسلاك: مسار الشعار في `beythalepStore.logoImage` (`beythalep-data.js`) + رفع رقم إصدار ملف البيانات نفسه (`?v=1→2`) في `index.html` و`sw.js` (`APP_SHELL`) — ضروري لأن `loadCatalogFromSupabase()` يستبدل مصفوفة `stores` بالكامل ببيانات Supabase بعد ~1.8 ثانية من تحميل الصفحة (`stores.length=0` ثم إعادة البناء من `mapDbStore`)، فالمصدر النهائي الفعلي لحقل `logoImage` هو عمود `stores.logo_image` في Supabase وليس الملف المُجمَّع — الملف المُجمَّع يؤثر فقط على اللحظة الأولى قبل اكتمال تحميل Supabase؛ لذا حُدِّث العمود مباشرة أيضاً عبر Supabase MCP `execute_sql`. تحقّق شامل عبر preview محلي: `store.logoImage` في المتصفح الحي يعكس المسار الجديد، طلب الشبكة لـ`/assets/photos/beythalep/logo.png` أعاد HTTP 200، عنصر `<img>` الفعلي في صفحة `/store/beit-halep-aksaray` حمَّل الصورة بأبعاد 400×400 كاملة (`complete:true`)، صفر أخطاء كونسول جديدة (التحذيرات الموجودة عن متاجر 9/11/72 بلا slug سابقة وغير متعلقة). `npm run check:versions` ناجح | `assets/photos/beythalep/logo.png` (جديد، يستبدل `logo.jpg` المحذوف)، `beythalep-data.js`, `index.html`, `sw.js`, `CLAUDE.md` + عمود `stores.logo_image` للمتجر id 97 في Supabase | نعم — `npm run check:versions` ناجح (app.js/styles.css لم يتغيّرا فعلياً هذه المرة، فقط تأكيد التطابق: app.js v307، styles.css v173). **مدفوع لـ`main`** (commit `94eea76e`) بناءً على طلب المستخدم «اعمل commit وادفعها لـ main» — الفرع كان بالفعل `main` نفسه ومتزامناً تماماً مع `origin/main` وقت الدفع (لا حاجة لأي cherry-pick هذه المرة)
| 2026-07-15 | المستخدم طلب إضافة مطعم «بيت حلب» (Beit Halab)، اكسراي/يوسف باشا، فاتح، إسطنبول، معطياً هاتف (05528008008) ورابط الموقع الخاص بالمطعم (`beythalep.com`) وطلب سحب المنيو والصور من الموقع + الموقع الجغرافي وكل المعلومات اللازمة من غوغل | ميزة إضافة جديدة (لا يوجد خطأ سابق) — أول مصدر من نوع موقع مطعم خاص ذاتي البرمجة (وليس منصة قوائم SaaS كـ minidine/qrlist/akillikurye المعتادة في الجلسات السابقة) | **استخراج المنيو:** خلافاً لكل المصادر السابقة، كل الـ17 تبويب (categories) كانت مُصيَّرة بالكامل في DOM من البداية (Bootstrap tabs، فقط تبويب واحد ظاهر بصرياً عبر CSS `.active`) — لا حاجة لأي نقر تبويبات أو انتظار تحميل تدريجي؛ استُخرجت كل الفئات الـ17 والعناصر بصورها وأسعارها دفعة واحدة عبر `querySelectorAll('.special-card')` على كامل الصفحة. **8 عناصر استُبعدت عمداً** (فئة «ايس كريم» بالكامل [5 عناصر] + عنصرين من «الامبراطور» + عنصر من «الكوكتيلات») لأنها تشارك نفس صورة placeholder العامة للموقع (`setting/78b8783e...png`) بدل صورة حقيقية — يطابق سياسة عدم نشر منتج بصورة مكررة/عامة. 162 منتجاً عبر 16 فئة، كل منتج بمسار صورة مستقل خاص به. **ملاحظة صور مصدر إضافية (لا تخالف قاعدة «صورة فريدة لكل مسار» — نفس السابقة الموثّقة في جلسة زمزم 2026-07-14):** 3 فئات كاملة (سلطات فواكه 9 عناصر، كريب 12 عنصراً، الوافل 9 عناصر = 30 منتجاً) استخدم فيها الموقع المصدر نفسه صورة استوديو عامة واحدة لكل فئة (بايت مطابق تماماً — تحقّق MD5) بدل صورة مخصّصة لكل نكهة، رغم أن كل صنف له رابط UUID مستقل على الموقع المصدر؛ اعتُمد نفس نمط الملفات المستقلة (`p126.jpg`…`p134.jpg` إلخ، كل واحد بمساره الخاص في المستودع) بدل استبعادها، لأن كل منتج يملك مسار ملف فريد فعلياً (هذا هو المعيار المتّبع، وليس تطابق البايتات). **الشعار:** لا يوجد شعار عالي الدقة على الموقع نفسه (فقط أيقونة favicon صغيرة 95×57)، فاستُخدم بدلاً منه og:image الحقيقي لحساب إنستغرام الرسمي للمطعم (`@beyt.halep.ist`، 100×100) بعد اكتشاف أن تنزيله المباشر بـ`curl` يفشل (توقيع URL موقّت مرتبط بجلسة المتصفح: "URL signature mismatch") — حُلّ بجلب الصورة كـ`fetch`→`base64` من داخل نفس صفحة إنستغرام عبر `javascript_tool` ثم فك التشفير محلياً. **الغلاف:** لا بانر تسويقي مخصّص في المصدر، فاستُخدمت صورة Google Places الحقيقية الأولى (رفعها المطعم نفسه، نفس اسم صاحب الصورة "مطعم بيت حلب" كاسم المكان — 1920×1080) عبر Place Photo API. **مطابقة غوغل مؤكَّدة بدقة عالية:** بحث `findplacefromtext` بالاسم أعاد تطابقاً حرفياً لرقم الهاتف (`0552 800 80 08` = `05528008008` المُرسَل) — «مطعم بيت حلب»، 3.9★ (1883 تقييماً، عدد كبير نسبياً)، `place_id` (`ChIJucM8W4W7yhQRAsa-MSE3XDA`)، إحداثيات دقيقة (41.0100636, 28.9479836)، عنوان (`Yusufpaşa, Molla Gürani, 34093 Fatih/İstanbul`) — لافت أن المتجر يقع فعلياً داخل نفس المبنى/الطابق لمتجر آخر («Kat: G · Anas Chicken Aksaray Şubesi» — فرع أناس تشيكن الموجود مسبقاً في المنصة بمعرّف مختلف، موقع مشترك وليس نفس الكيان). ساعات العمل الحقيقية من Google Places API (`opening_hours.weekday_text`): يومياً تقريباً 10:00 ص – 2:00 ص (استُخدم النمط السائد؛ استثناء الأربعاء 10:30 كان بسبب عطلة رسمية تركية يوم الاستخراج فقط، لم يُعتمد كقاعدة دائمة). المتجر رقم **97** (تحقّق `max(id)`=96 قبل التنفيذ مباشرة، صفر تصادم)، حزمة معرّفات المنتجات **1790001–1790162** (تحقّق صفر تصادم في النطاق). صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82 mozjpeg، شعار ≤400px، غلاف ≤1280px q85). `approval_status='pending'` عمداً (متجر طرف ثالث حقيقي غير مؤكَّد كشراكة دكانجي، نفس سياسة كل المتاجر المشابهة). سلاگ نظيف مختار بعناية لتفادي التباس مع متجر مختلف تماماً بنفس الاسم الإنجليزي المشابه («Beit Beyrut» id 89، مطعم لبناني منفصل تماماً): `beit-halep-aksaray` (وليس `beit-halep-restaurant` أو ما شابه القريب من beit-beyrut). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`97: "beit-halep-aksaray"`) + `<script src="/beythalep-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع عبر Supabase MCP `execute_sql` مباشرة (صف المتجر بأعمدة `google_place_id/google_rating/google_reviews_count/google_maps_url/google_rating_updated_at` + 162 صف منتج، `jsonb_to_recordset`، معاملتان منفصلتان — تحقّق مسبق من أسماء أعمدة `stores`/`products` الفعلية عبر `information_schema.columns` قبل الكتابة لتفادي أخطاء تسمية كـ`delivery_fee` بدل `delivery` الصحيح). تحقّق شامل عبر preview محلي: `/store/beit-halep-aksaray` يعرض العنوان الصحيح (`دكانجي - بيت حلب`)، الوصف، العنوان الحقيقي، ساعات العمل، معدل التوصيل (٢٠ ل.ت/كم)، كل الفئات الـ16 بعناصرها الـ162 وأسعارها الصحيحة من Supabase مطابقة تماماً (162 من 162 منتجاً)، كل الـ164 رابط صورة (162 منتج + شعار + غلاف) أعادت HTTP 200 فعلياً (تحقّق جماعي `fetch HEAD`)، `/category/restaurants` لا يعرض المتجر إطلاقاً (سلوك `pending` صحيح — الرابط المباشر يعمل رغم ذلك)، الصفحة الرئيسية بلا أي أثر، صفر أخطاء كونسول على الصفحات الثلاث. `npm run check:versions` ناجح | `beythalep-data.js` (جديد)، `assets/photos/beythalep/**` (جديد، 164 ملف: 162 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 97 وصفوف `products` 1790001-1790162 في Supabase | نعم — `npm run check:versions` ناجح (app.js v306→307، store-slugs.js v245→246، CACHE v225→226). **مدفوع لـ`main`** بناءً على طلب المستخدم «اعمل commit وادفعها لـ main» (ملاحظة: فرع العمل `mobile-redesign-home-store-cart-checkout` كان متقدّماً بميزة إعادة تصميم الموبايل غير المؤكَّدة بعد للنشر — دُفع هذا التغيير وحده إلى `main` بمعزل عنها عبر cherry-pick على فرع نظيف من `origin/main`). **تحديث لاحق (نفس اليوم):** بطلب صريح من المستخدم «اعتمد المتجر»، حُدِّث `approval_status` إلى `'approved'` مباشرة عبر Supabase (`update stores set approval_status='approved' where id=97`) — تحقّق حي أن `/category/restaurants` أصبح يعرض بطاقة المتجر فعلياً (`data-id="97"` قابل للنقر)، بلا أخطاء كونسول
| 2026-07-15 | متابعة لميزة تعبئة رمز OTP تلقائياً (السطر التالي أدناه، نُشرت ودُفعت لـ`main` بالفعل) — طلب المستخدم اختبارها فعلياً على الموبايل بعد النشر | فحص زر «لصق الرمز من واتساب» على العرض الحي بعرض موبايل (375px) عبر preview كشف عيبين حقيقيين لم يظهرا في تحقّق الجلسة السابقة (كانت الاختبارات محاكاة JS بلا فحص أبعاد بصرية فعلية): (1) الزر لم يكن بعرض كامل رغم صنف `full` — تبيّن أن قاعدة `.primary-button.full { width:100% }` في `styles.css` **لا تغطي** `.secondary-button.full` إطلاقاً (خلل تسمية صنف عام موجود مسبقاً بالكود ويؤثر أيضاً على أزرار أخرى قديمة غير ذات صلة بهذه المهمة مثل أزرار دخول التاجر — لم تُلمس تفادياً لتغيير مظهر أماكن أخرى في الموقع لم يطلبها المستخدم؛ أُصلح عبر قاعدة CSS مخصّصة لزر OTP فقط). (2) ارتفاع الزر 39px (بسبب صنف `compact`) أقل من الحد الأدنى الموصى به لمساحة اللمس (44px) لزر بهذه الأهمية. أثناء التحقق المحلي أيضاً: `app.js`/`styles.css` لم يظهر عليهما أي تغيير للوهلة الأولى بعد إعادة تشغيل السيرفر المحلي — السبب اكتُشف أنه **Service Worker** يخدم نسخة مخزَّنة سابقاً من `app.js?v=305` من Cache Storage (`dukkanci-v224`)، بالإضافة لتخزين HTTP عادي للمتصفح (`Cache-Control: public, max-age=300` على نفس رابط `?v=305` غير المتغيّر) — كلاهما لا علاقة له بكود المشروع نفسه، فقط قيد بيئة الاختبار المحلي؛ رفع رقم الإصدار (المطلوب أصلاً للنشر) كسر الكاشين معاً وأظهر التغيير فوراً | حُذفت `compact` من صنف الزر (فأصبح ارتفاعه 45px الافتراضي)، وأُضيف `width:100%` صراحة لقاعدة `.otp-paste-button` في `styles.css` (بدل تعديل قاعدة `.full` العامة المشتركة مع أزرار أخرى). تحقّق شامل عبر preview محلي بعرض 375px بعد رفع الإصدار (لضمان تحميل نسخة طازجة متجاوزة الكاشين): الزر أصبح 335px عرضاً (يطابق عرض النموذج بالكامل) و45px ارتفاعاً (يفي بحد اللمس 44px)، صفر انزياح أفقي (`body.scrollWidth`=375=`viewportWidth`)، تدفّق لصق الرمز من الحافظة + الإرسال التلقائي لا يزال يعمل بشكل صحيح (رمز محاكى `758302` من نص عربي محيط)، نفس النتائج على نافذتي OTP (تسجيل الدخول وتأكيد الطلب)، وصفر أخطاء كونسول. `npm run check:versions` ناجح | `app.js`, `styles.css`, `index.html`, `sw.js`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v305→306، styles.css v172→173، CACHE v224→225) |
| 2026-07-15 | المستخدم أبلغ أن العملاء يواجهون صعوبة إتمام الشراء: عند وصول رمز تأكيد الطلب عبر واتساب، يفتح العميل واتساب ليقرأ الرمز ثم يجد صعوبة في العودة لنافذة الموقع التي تطلب الرمز، وطلب سحب الرمز تلقائياً من واتساب ووضعه في مربع التأكيد | ميزة تحسين تجربة استخدام (لا يوجد خطأ سابق في منطق OTP نفسه — راجع [[checkout-otp]]). **قيد تقني حقيقي يجب توضيحه للمستخدم:** لا توجد طريقة برمجية لصفحة ويب لقراءة محتوى رسالة واتساب مباشرة (لا يوجد إذن/API لذلك، وWebOTP الخاص بالمتصفح يعمل فقط مع رسائل SMS التي يحللها النظام تلقائياً، وليس واتساب) — فالحل الكامل تلقائياً بمعنى "بلا أي تدخل من العميل" غير ممكن تقنياً؛ الأقرب المتاح هو تفعيل نسخ/لصق الرمز تلقائياً بمجرد أن ينسخه العميل من واتساب ويعود للموقع | أُضيفت `setupOtpAutofill()` في `app.js` (قبل `openOtpModal` مباشرة) ووُصلت بكل من نافذة تسجيل الدخول (`openOtpModal`/`#otp-form`) ونافذة تأكيد الطلب (`openOrderOtpModal`/`#order-otp-form`): (1) عند عودة التبويب/التطبيق للواجهة (`visibilitychange`/`focus` — أي لحظة رجوع العميل من واتساب) تحاول الدالة قراءة الحافظة صامتاً (`navigator.clipboard.readText()`) وتستخرج الأرقام منها بتعبير نمطي، فتملأ مربع الرمز وتُرسل النموذج تلقائياً بمجرد اكتمال طول الرمز (`maxlength`) — يعمل تلقائياً بلا أي نقرة إضافية بشرط أن يكون العميل قد نسخ الرمز من واتساب أولاً (ضغطة مطوّلة → نسخ). (2) اعتراض حدث `paste` الأصلي على الحقل لنفس الاستخراج عند اللصق اليدوي. (3) زر ظاهر جديد «لصق الرمز من واتساب» (`.secondary-button.full.otp-paste-button`, `data-action="paste-otp"`, معالج في نفس مكان `resend-order-otp`) يقرأ الحافظة مباشرة عند الضغط — هذا هو المسار الأكيد لأن الضغط نفسه لفتة مستخدم حقيقية تتجاوز حجب iOS Safari للقراءة الصامتة بلا لفتة. المستمعون يوقفون أنفسهم تلقائياً (`input.isConnected`) بعد إغلاق النافذة فلا حاجة تنظيف يدوي إضافي (`closeModal()` يُفرغ `modalRoot.innerHTML` أصلاً). أُضيف أيضاً أيقونة `clipboard` جديدة في `iconPaths`، وقاعدة CSS `.otp-paste-button{margin-bottom:14px}` للمسافة بين الزر والحقل التالي. تحقّق شامل عبر preview محلي (`javascript_tool`، ليس عبر واتساب حقيقي — الحافظة زُيِّفت برمجياً `navigator.clipboard.readText = () => Promise.resolve(...)`): (أ) نص محاكي "رمزك هو: 482913 لا تشاركه مع أحد" عبر الزر اليدوي → استخراج صحيح لـ`482913` وإرسال تلقائي للنموذج. (ب) نفس النص عبر محاكاة `visibilitychange` صامتة (تمثّل عودة العميل من واتساب دون لمس أي زر) → نفس النتيجة (استخراج+إرسال تلقائي) بلا أي تدخل يدوي. (ج) صفر أخطاء كونسول في كلتا نافذتي OTP (تسجيل الدخول وتأكيد الطلب)، والمستمعون توقّفوا بأمان بعد `closeModal()` بلا أي استثناء JS. `npm run check:versions` ناجح | `app.js`, `styles.css`, `index.html`, `sw.js`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح (app.js v304→305، styles.css v171→172، CACHE v223→224). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-14 | المستخدم طلب سحب سوبر ماركت «زمزم ماركت» من كتالوج واتساب بزنس (`+90 553 440 02 75`)، مشيراً أن العنوان والموقع وساعات العمل كلها في الكتالوج | ميزة إضافة جديدة (لا يوجد خطأ سابق) — استُخدم حساب Chrome الحقيقي المسجَّل (`claude-in-chrome`) لفتح كتالوج واتساب ويب (بيانات عامة قابلة للمشاهدة). اصطدمت الجلسة بنفس عطل **تدهور رسم التبويب الآلي** بعد الاستخدام المكثّف (إغلاق الكتالوج بين الاستدعاءات، مهلات CDP 45 ثانية، تصفير `scrollTop` عند الرجوع من التفاصيل، ومرة استدعاء مكالمة صوتية للتاجر بالخطأ بفعل خلط إحداثيات screenshot مقابل CSS — لم تتصل) | **الاكتشاف التقني المفصلي (يُضاف كأنظف طريقة استخراج واتساب حتى الآن، يتفوق حتى على درس «CSS background-image» في فرايز زون):** صفوف قائمة كتالوج واتساب نفسها تحمل الصورة بدقة كاملة (1200×1600) كـ`background-image: url("blob:...")` على عنصر الصف — لا حاجة إطلاقاً لنقر «Details» فردي لكل منتج (خلافاً لدروس فستق حلب/فاتح الخير التي نقرت كل صنف على حدة وانتظرت blob بطيئاً). استُخرجت كل الـ161 صورة دفعة واحدة عبر `getComputedStyle(el).backgroundImage` ثم `fetch(blobUrl)→blob→FileReader.readAsDataURL` بلا أي تصفّح تفصيلي. **ملاحظة معايرة إحداثيات:** عرض الـscreenshot 1568px مقابل viewport 1920px ⇒ معامل قياس 0.8167 (screenshot = css × 0.8167) — سبب خلط النقر الذي أطلق مكالمة صوتية بالخطأ. **درس NBSP يتكرر (من فستق حلب):** فراغ غير منقسم ` ` بين "TRY" والسعر كسر مطابقة مفتاح `(name, price)` لتصحيح 4 أسماء استُخرجت كأوزان بدل الاسم (Buldak، موتزاريلا مصري، دومتى جبن فيتا بطعم الزيتون، لب سوبر مصري) — حُلّ بتطبيع الفراغات `re.sub(r'[\s ]+',' ',price)` قبل المطابقة، وتحقّق أن الأربعة طُبِّقت. **درس Postgres جديد:** عمود `products.price` نوعه `numeric` وليس `text` — أول `insert ... select ...,price,...` فشل بـ`column "price" is of type numeric but expression is of type text`، حُلّ بـ`price::numeric` في الـSELECT (مع إبقاء عمود الـrecordset `text`). **العنوان/الإحداثيات/الساعات عبر Google Maps الحقيقي:** Place ID `ChIJ_yXMTTtftRQR5xkwIunAi2o`، تقييم 4.8 من 19، إحداثيات دقيقة (41.0215683, 28.6603221)، عنوان `Fly Garden Residence, Yeşilkent, 1953. Sk. No:42A, Esenyurt` (مفتاح Maps الحي من `/api/maps-key`). الغلاف تصميم مطابق للهوية «زمزم ماركت — منتجات عربية أصيلة» (1600×679)، شعار 400×400. **16 فئة** حقيقية مشتقّة من قواعد الأسماء (قسم الاجبان، حلويات بيتي، منتجات من مصر، بن، حلاوة وطحينة، الصعيدي، مجمدات، سمن وزبدة، تمور وفواكه مجففة، مسك وعود، اندومي كوري، عسل طبيعي، زينة رمضان، منتجات بيازيد، مخبوزات، البوادى). المتجر رقم **96** (تحقّق `max(id)`)، حزمة معرّفات المنتجات **1780001–1780161**. صُغِّرت الصور بـPIL (منتجات ≤800px JPEG q82، شعار ≤400px PNG). `approval_status='pending'` عمداً عند الإدراج (طرف ثالث غير مؤكَّد كشراكة) — **لكنه قُلِب لـ`approved` حياً أثناء التحقق** (على الأرجح من المستخدم/الإدارة، نفس نمط الوكالة 90/فاتح الخير 93/قلعة الشام 94)، فأصبح ظاهراً بشكل صحيح كبطاقة متجر على `/category/supermarket`. وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`96: "zamzam-market"`، v244→v245) + `<script src="/zamzam-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع عبر Supabase MCP `execute_sql` (صف المتجر + 161 صف منتج، `jsonb_to_recordset`، معاملتان). تحقّق شامل عبر preview محلي بمنفذ مستقل (50180): `/store/zamzam-market` يعرض العنوان الصحيح (`دكانجي - زمزم ماركت`) والعنوان الحقيقي وساعات العمل والهاتف ومعدل التوصيل (٢٠ ل.ت/كم)، كل الـ16 فئة بعناصرها الـ161 من Supabase (البيانات المحمَّلة محلياً فارغة عمداً)، **161 بطاقة صورة منتج مرسومة فعلياً**، كل الـ163 رابط صورة (161 منتج + شعار + غلاف) أعاد HTTP 200 (تحقّق `fetch HEAD` جماعي)، الغلاف يحمَّل بدقة 1600px، صفر أخطاء كونسول، وبطاقة المتجر على `/category/supermarket` نقّارة عبر `<button data-action="open-store" data-id="96">`. لقطات الشاشة تعطّلت بمهلة (عطل بيئي موثَّق) فاعتُمد التحقق البرمجي DOM/شبكة/كونسول. `npm run check:versions` ناجح (app.js v304، store-slugs.js v245، CACHE v223) | `zamzam-data.js` (جديد)، `assets/photos/zamzam/**` (جديد، 163 ملف: 161 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 96 وصفوف `products` 1780001-1780161 في Supabase | نعم — `npm run check:versions` ناجح. **مدفوع لـ`main`** (commit e2236d41، `git push origin main`) بناءً على طلب المستخدم «اعمل بوش» — تحقّق Vercel deploy status عبر `gh api` أعاد `success`، وتأكيد حي على `www.dukkanci.com.tr/store/zamzam-market` (HTTP 200، العنوان الصحيح، وصور المنتجات/الشعار/الغلاف كلها HTTP 200) |
| 2026-07-14 | المستخدم طلب إضافة مطعم «فرايز زون» (Fries Zone Restaurant)، فرع اسنيورت، معطياً عنوان (Nazim Hikmet Blv. 62C, 34515 Istanbul, Esenyurt Turkey) + هاتف (+90 533 683 98 28) + إشارة أن الكتالوج على واتساب بلا رابط مباشر | ميزة إضافة جديدة (لا يوجد خطأ سابق) — لكن جلسة استخراج الكتالوج اصطدمت بنفس عطل **تضارب جهاز واتساب ويب متعدد** الموثَّق في السطر التالي أسفله لمتجر قلعة الشام في نفس اليوم، لكن بشكل أشد إصراراً هذه المرة: عشرات محاولات "Use here" المتتالية (بما فيها بعد انتظار 90 ثانية كاملة عبر `Monitor`) استمرت تُرجَع فوراً لنافذة "WhatsApp is open in another window" — ما يؤكد وجود جلسة واتساب ويب حقيقية أخرى نشطة فعلياً على جهاز المستخدم تستعيد السيطرة خلال ثوانٍ من كل استحواذ. طُرح هذا صراحة على المستخدم عبر `AskUserQuestion` (توقّف عن إعادة المحاولة العمياء بدل الاستمرار بلا جدوى)، فأبلغ المستخدم أنه أغلق الجلسة الأخرى، ونجحت المحاولة التالية فوراً | **الرابط لم يُرسَل مباشرة** — استُنتج رابط الكتالوج من رقم الهاتف نفسه بنمط `wa.me/c/<رقم>` (بلا شرطات/مسافات) الذي تبيَّن أنه يعمل فعلاً لهذا الحساب (`wa.me/c/905336839828`)، أُكِّد ذلك أولاً عبر صفحة الهبوط العامة لواتساب (`whatsapp.com/catalog/...`) التي أظهرت عنوان الصفحة "View fries zone's Catalog on WhatsApp" قبل محاولة تسجيل الدخول الكامل. **درس استخراج جديد يُضاف لدروس الجلسات السابقة (قلعة الشام، فاتح الخير، فستق حلب):** الكتالوج هنا كان الأنظف تقنياً حتى الآن — لا حاجة لأي نقر "Details" فردي أو انتظار blob: كل صور المنتجات كانت CSS `background-image` على عناصر `<div>` (وليست وسوم `<img>` ولا blob مباشرة) بروابط مباشرة قابلة لـ`fetch()` فوري؛ استُخرجت جميعها دفعة واحدة عبر تكرار DOM على كل `[role="listitem"]` واستخلاص `getComputedStyle(...).backgroundImage`، مع تفادي طباعة الروابط نفسها في مخرجات الأداة (رفضتها كمحتوى يشبه كوكيز/استعلام) بتنزيلها مباشرة داخل نفس استدعاء JS بدل عرضها كنص. **قسم "All items" هنا هو الأصدق والأشمل فعلاً** (خلافاً لقسم "الوجبات"/"البرجر"/... في الصفحة الرئيسية التي تبقى محدودة بـ3 عناصر معاينة فقط ولا يفتح "See all" الخاص بها أكثر من ذلك — عطل واجهة موثَّق هنا لأول مرة: الروابط الفرعية "Collection X, see all" تُحمِّل نفس الـ3 عناصر فقط وتتوقف عن التمرير الإضافي، بينما التمرير المباشر لأسفل الصفحة الرئيسية للكتالوج نفسها يُحمِّل تدريجياً كل شيء بلا سقف)؛ اعتُمد التمرير التراكمي المباشر (لا الدخول لأي "See all") لاستخراج كامل قسم "All items" (يُعيد سرد كل المنتجات الحقيقية من كل الفئات مرة أخرى بلا فئات، مطابقاً لنمط فاتح الخير) — كشف ذلك أن الفئات الحقيقية (`MAIN MEAL`, `BURGER AND SANDWİCH`) تحتوي كل منها ضعف عدد العناصر الظاهرة في معاينتها المحدودة (٩ عناصر رئيسية بدل 3، 10 برجر/ساندويتش بدل 3) — لولا مضاهاتها بقسم "All items" الكامل لفات نصف الكتالوج تقريباً. 38 منتجاً فريداً بعد إزالة التكرار (بعض العناصر ظهرت 2-3 مرات في "All items" لأنها تنتمي منطقياً لأكثر من عرض). **عروض التوفير الستة (Offer 1-6) بلا وصف محتوى نهائياً** (فُتح تفصيل كل عرض بصفحة "Details" فردية فتبيَّن أنه اسم+سعر+صورة فقط، بلا نص "يحتوي على..." من التاجر) — اعتُمدت التسمية الحرفية "عرض توفير N" بدل اختلاق محتوى العرض. **البانر والشعار صورة واحدة مطابقة تماماً بالبايت (MD5)** — الشعار الدائري الأحمر/الأسود "FRIES ZONE — MORE THAN JUST FRIES — 2021" هو نفسه المستخدَم كغلاف الكتالوج (لا بانر تسويقي منفصل)، فاستُخدم كشعار فقط، واعتُمدت صورة منتج حقيقية عالية الجودة (فرايز زون اكستريم، تصوير استوديو احترافي 1600×900 مع اسم المنتج بالعربي/الإنجليزي مدموجاً) كغلاف بدلاً منه — نفس نمط توبيز/فاتح الخير/فلوكا حين لا يوجد بانر مخصَّص، وتأكَّد بمحاكاة قصّ `object-fit:cover` عبر `sharp` لكلا نسبتي الهيرو (4:1) والبطاقة (~1.46:1) أن العنوان والطبق يبقيان ظاهرين في نسبة الهيرو الأوسع دون بتر. **عنوان المتجر مطابَق بدقة عبر Google Maps الحقيقي:** بحث نصي بالاسم أعاد "Fries Zone Restaurant" (تقييم حقيقي 4.5 من 259 تقييماً)، هاتف مطابق حرفياً للرقم المُرسَل (+90 533 683 98 28) يؤكد صحة المطابقة رغم اختلاف رقم المبنى (غوغل: No:58، المستخدم: 62C — على الأرجح نفس المجمع العمراني بمدخلين)، فاعتُمد نص عنوان مدموج (حي/شارع من غوغل + رقم المبنى كما أرسله المستخدم) مع إحداثيات غوغل الدقيقة (41.0190493, 28.6520397) وساعات العمل الرسمية (يومياً 1:00 ظهراً – 1:00 صباحاً) بدل التخمين. المتجر رقم **95** (تحقّق `max(id)` ثلاث مرات بسبب طول الجلسة بفعل عطل واتساب: 93 عند التخطيط، ثم **94 تصادم فعلي مع متجر قلعة الشام** الذي أُضيف بالتوازي أثناء الانتظار، ثم 94 مجدداً تأكيداً قبل التنفيذ المباشر — اعتُمد 95)، حزمة معرّفات المنتجات **1770001–1770038** (تحقّق صفر تصادم في النطاق قبل الإدراج). صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82 mozjpeg، شعار ≤400px PNG، غلاف ≤1280px JPEG q85). `approval_status='pending'` عمداً (متجر طرف ثالث حقيقي غير مؤكَّد كشراكة دكانجي، نفس سياسة كل المتاجر المشابهة). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread — بعد إعادة قراءة الملف لتضارب تحرير حي مع جلسة متزامنة أضافت قلعة الشام إلى نفس المواضع) + `store-slugs.js` (`95: "fries-zone-esenyurt"`، مع رفع رقم إصداره أيضاً v243→v244 لأن الملف عُدِّل) + `<script src="/friyszone-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع عبر Supabase MCP `execute_sql` مباشرة (صف المتجر + 38 صف منتج، `jsonb_to_recordset` بصيغة كائنات JSON وليس مصفوفات موضعية — محاولة أولى بمصفوفات فشلت بخطأ Postgres `must be an array of objects`). تحقّق شامل عبر preview محلي بمنفذ مستقل تلقائي: `/store/fries-zone-esenyurt` يعرض العنوان الصحيح (`دكانجي - فرايز زون`) والوصف والعنوان الحقيقي وساعات العمل ومعدل التوصيل (٢٠ ل.ت/كم) بشكل صحيح، كل الفئات الخمس (الوجبات الرئيسية 9، البرجر والساندويتش 10، الأطباق الجانبية 10، مشروبات تشيل زون 3، قائمة التوفير 6 = 38) بعناصرها وأسعارها الصحيحة من Supabase مطابقة تماماً، صور الغلاف/الشعار/كل المنتجات الـ40 رابطاً أعادت HTTP 200 فعلياً (تحقّق برمجي بـ`fetch HEAD` لكل رابط دفعة واحدة)، `/category/restaurants` لا يعرض المتجر إطلاقاً (سلوك `pending` صحيح)، الصفحة الرئيسية بلا أي أثر، صفر أخطاء كونسول على الصفحات الثلاث جميعاً، و`npm run check:versions` ناجح (app.js v303، store-slugs.js v244، CACHE v222) | `friyszone-data.js` (جديد)، `assets/photos/friyszone/**` (جديد، 40 ملف: 38 منتج + شعار + غلاف)، `scripts/process-friyszone-images.js` (جديد)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 95 وصفوف `products` 1770001-1770038 في Supabase | نعم — `npm run check:versions` ناجح. **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم** |
| 2026-07-14 | المستخدم طلب إضافة مطعم «قلعة الشام» (Şam Kalesi / Qalaat Al-Sham) من كتالوج واتساب، معطياً رقم واتساب (+90 552 594 72 49) + عنوان (حي حريية، شارع محمود بيه، رقم 115، باهتشلي أوغلو) + ساعات عمل (12-23) | ميزة إضافة جديدة (لا يوجد خطأ سابق) — لكن الجلسة اصطدمت بعطل بيئي حاد: **تضارب جهاز واتساب ويب متعدد** (نافذة "WhatsApp is open in another window. Click 'Use here'") ظهر بشكل متكرر جداً (كل نقرة "Use Here" كانت تُستبدَل بنفس النافذة خلال ثوانٍ قليلة)، على الأرجح بسبب جلسة/جهاز آخر حقيقي يستخدم نفس حساب واتساب بالتوازي وينافس على "فتحة" الجلسة النشطة — أُعيد النقر على "Use Here" مرات عديدة إلى أن استقرت الجلسة لفترة كافية لإكمال الاستخراج. **درس تقني جديد يُضاف لدروس استخراج كتالوج واتساب السابقة:** كتالوج هذا المتجر (28 عنصراً بلا فرز فئات في القائمة) لا يُخفي الصور خلف blob بطيء التحميل كما في الجلسات السابقة (فستق حلب، فاتح الخير) — بل الصورة الكبيرة في لوحة "Details" (بعد النقر المباشر على كل صف) كانت متاحة فوراً بدقة عالية (1600×1000 تقريباً تلقائياً)، فكان التنزيل مباشراً (`fetch(blobUrl)→createObjectURL→anchor.click()`) بلا حاجة لانتظار إضافي، وبمعدل تنزيل واحد لكل نقرة حقيقية دون الاصطدام بسقف الـ10 تنزيلات المعروف من جلسات سابقة (على الأرجح لأن التنزيلات كانت متباعدة زمنياً بفعل تضارب الجهاز نفسه). **قائمة العناصر بلا فئات فرعية حقيقية في المصدر** (28 عنصراً متتالياً بلا عناوين أقسام)، فصُنِّفت يدوياً إلى 5 فئات منطقية (مقبلات، سندويشات، برغر، وجبات شاورما ودجاج، المناسف) حسب طبيعة كل صنف. **تكرار حقيقي واحد مؤكَّد ومدموج:** "سندويش مكسيكانو" ظهر مرتين بنفس الاسم والسعر بالضبط (200 ل.ت) بصورتين مختلفتين قليلاً لنفس الصنف (زاوية/إضاءة مختلفة) — اعتُمد النسخة الأولى فقط وأُهملت الثانية (27 منتجاً فريداً من أصل 28 عنصراً في القائمة). 🔴 **شذوذ سعر حقيقي غير محلول عمداً:** منتج "يالنجي" (ورق عنب) مسعَّر **20,000 ل.ت** في كتالوج التاجر نفسه — رقم غير منطقي مقارنة ببقية الأطباق المماثلة (كبة طرابلسية 80 ل.ت، حتى منسف لحم لأربعة أشخاص 2,400 ل.ت فقط) — على الأرجح خطأ إدخال من التاجر (ربما قصد 200 ل.ت)، لكن اتُّبعت سياسة عدم اختلاق/تصحيح بيانات التاجر من تلقاء نفسي؛ نُقِل السعر كما ورد حرفياً مع توثيق الشذوذ هنا للفت انتباه التاجر لاحقاً. **تحقّق عنوان/تقييم مزدوج عبر Google Maps:** البحث باسم "Şam Kalesi Restoran" (الاسم اللاتيني الظاهر فعلياً على واجهة المحل في أول صورة منتج "يالنجي" التي كانت في الحقيقة صورة واجهة المحل الحقيقية من الشارع مع طبق يالنجي في المقدمة، وليست صورة استوديو) أعاد نتيجة مطابقة تماماً: **"ŞAM KALESİ LOKANTASI"**، تقييم حقيقي 3.9 (515 تقييماً)، فئة "Arab restaurant"، عنوان **"Hürriyet, Mahmutbey Cd. No:117"** (وليس 115 كما أرسل المستخدم — فرق طفيف، اعتُمد رقم غوغل الأدق 117)، إحداثيات دقيقة (41.0012849, 28.8420208)، وساعات "Closes 11:30 PM" (قريبة جداً من نص المستخدم "حتى 23" فاعتُمد نص المستخدم الصريح "يومياً 12:00 ظهراً – 11:00 مساءً"). **رقم هاتف مختلف مكتشَف على واجهة المحل نفسها وعلى غوغل مابس (0552 291 51 75) عن رقم واتساب الذي أرسله المستخدم (+90 552 594 72 49)** — اعتُمد رقم واتساب المُرسَل صراحةً كحقل `phone`/`whatsapp` الوحيد للمتجر (هو قناة الطلب الفعلية)، ولم يُستخدم رقم غوغل/اللافتة إطلاقاً في أي حقل. صورة "يالنجي" (صورة واجهة محل حقيقية بلافتة "RESTAURANT ŞAM KALESİ / قلعة الشام" واضحة تماماً) اعتُمدت كغلاف المتجر أيضاً (بجانب كونها منتجاً) — نفس نمط إعادة استخدام صورة منتج حقيقية كغلاف حين لا يوجد بانر تسويقي مخصَّص (سابقة توثَّقت مراراً: توبيز، فاتح الخير). الشعار (أيقونة قلعة خضراء متجهة نظيفة على خلفية فاتحة، مأخوذة من صورة الملف الشخصي لواتساب بزنس 640×640) استُخدم كما هو. المتجر رقم **94** (تحقّق `max(id)`=93 مرتين: أول مرة عند التخطيط، وأخرى مباشرة قبل التنفيذ — صفر تصادم هذه المرة)، حزمة معرّفات المنتجات **1760001–1760027** (27 منتجاً بالضبط بعد استبعاد التكرار). صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82 mozjpeg، شعار ≤400px PNG، غلاف ≤1280px JPEG q85 — من ~9.5MB خام لـ29 ملفاً إلى 2.4MB إجمالاً). `approval_status='pending'` عمداً (متجر طرف ثالث حقيقي غير مؤكَّد كشراكة دكانجي، نفس سياسة كل المتاجر المشابهة السابقة). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`94: "qalaat-alsham-restaurant"`) + `<script src="/qalaatalsham-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع عبر Supabase MCP `execute_sql` مباشرة (صف المتجر + 27 صف منتج، `jsonb_to_recordset`، معاملتان منفصلتان). تحقّق شامل عبر preview محلي بمنفذ مستقل تلقائي (`autoPort`، لتفادي تعارض «خادم جلسة أخرى يعمل على هذا المجلد» المذكور في تنبيه النظام): `/store/qalaat-alsham-restaurant` يعرض العنوان الصحيح (`دكانجي - مطعم قلعة الشام`) والوصف والعنوان الحقيقي وساعات العمل والهاتف، كل الفئات الخمس بعناصرها الـ27 وأسعارها الصحيحة من Supabase مطابقة تماماً، صور الغلاف/الشعار/كل المنتجات الـ27 (29 رابطاً إجمالاً) أعادت HTTP 200 فعلياً (تحقّق برمجي بـ`fetch HEAD` لكل رابط دفعة واحدة)، `/category/restaurants` لا يعرض المتجر إطلاقاً (سلوك `pending` صحيح مطابق للسوابق — الرابط المباشر يعمل رغم ذلك)، الصفحة الرئيسية تحمَّلت بلا أي أثر، صفر أخطاء كونسول على الصفحات الثلاث جميعاً | `qalaatalsham-data.js` (جديد)، `assets/photos/qalaatalsham/**` (جديد، 29 ملف: 27 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 94 وصفوف `products` 1760001-1760027 في Supabase | نعم — `npm run check:versions` ناجح (app.js v302، store-slugs.js v243، CACHE v221). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-14 | المستخدم طلب إضافة مطعم «فاتح الخير» (Fatih Al-Khair)، فرع فاتح، معطياً رابط كتالوج واتساب (`wa.me/c/905527373795`) + رابط Google Maps (حُلّ مسبقاً من جلسة التوجيه: Place ID `ChIJNef7wiG6yhQR45rZXbk_K1Q`، تقييم 3.7/640، إحداثيات دقيقة 41.0203663/28.9486926) | ميزة إضافة جديدة (لا يوجد خطأ سابق) — لكن ظهرت 3 مشاكل تقنية حقيقية أثناء التنفيذ: (1) **اكتشاف تصحيحي مهم في تسمية الفئات:** جلسة التوجيه (orchestrator) كانت قد نقلت اسمي فئتين كـ«سندويشات غربية»/«وجبات غربية» استناداً لتحويل نصي سابق، لكن الفحص المباشر لعناوين الفئات الحقيقية في كتالوج واتساب (بالقراءة البصرية من `screenshot`/`zoom`، وليس `read_page`/`get_page_text` النصي المعروف بخلط حرفي غ/ع سابقاً — موثّق كدرس من جلسة الوكالة 2026-07-13) أظهر أن الاسمين الحقيقيين هما **«سندويش عربي»** و**«وجبات عربي»** (وليس «غربية» رغم أن الأصناف كريسبي/زنجر/اسكلوب غربية الطابع — كذلك سمّاها التاجر نفسه). استُخدمت التسميات الصحيحة المؤكَّدة بصرياً في الفئتين. (2) **كتالوج واتساب بلا فرز حقيقي بالفئات في القائمة الرئيسية** (فئات المعاينة العلوية لكل قسم تعرض 3 عناصر فقط من كل قسم حقيقي في كتالوج التاجر — لا تطابق الـ59 صنفاً الكاملة)؛ الاستخراج الفعلي والموثوق كان فقط عبر قسم **«All items»** (يعيد سرد كل الأصناف الـ59 مرة واحدة لكل صنف تقريباً، مع تكرارات قليلة صريحة رُصدت وأُهملت — مثال: «وجبة شاورما عربي اصابع» ظهرت مكررة، وكذلك «صحن مسبحة» و«سارير كازوز»)، بترتيب مختلف تماماً عن ترتيب الفئات المنطقي المطلوب — استُخرجت الصور بالترتيب الفعلي للقائمة ثم أُعيد ربطها بالاسم/السعر الصحيحين من القائمة المُعدَّة مسبقاً وليس بالترتيب. (3) 🔴 **خطأ صور متسلسل حقيقي اكتُشف بالمراجعة اللاحقة (وليس افتراض النجاح):** القائمة (list view) في واتساب تعرض صوراً مصغَّرة 96×96 فقط غير كافية، فاستُخدم النقر في كل صنف لفتح لوحة «Details» التي تحمّل صورة عالية الدقة كـ`blob:` URL (حتى 1600×1067) — لكن تحميل هذا الـblob **يتأخر أحياناً عن تحديث نص الاسم/السعر المعروضين في نفس اللوحة**، فأول محاولة تنزيل فورية بعد النقر (بلا انتظار كافٍ) التقطت أحياناً صورة الصنف **السابق** المفتوح بينما كان النص المقروء صحيحاً للصنف الجديد؛ اكتُشف ذلك لاحقاً بفحص MD5 لكل الصور الـ59 بعد التصغير: مجموعة تطابق تام غير منطقية إطلاقاً بين «كولا 330 مل» (مشروب) و«فروج مشوي مع بطاطا» (طبق دجاج مشوي) — صورتان لا يُعقل أن يتشاركهما التاجر أبداً (خلافاً لِـ6 مجموعات تطابق أخرى منطقية تماماً: نسخ عادي/حار من نفس الطبق، أو مقاسات مختلفة لنفس الساندويتش، التي أُبقيت كما هي). أُعيد فتح تفصيل «فروج مشوي مع بطاطا» تحديداً مع انتظار 2 ثانية إضافية والتحقق البصري من الصورة المعروضة **قبل** التنزيل (لقطة شاشة تؤكد طبق دجاج مشوي حقيقي مع بطاطا وشيبس) ثم تنزيلها وإعادة معالجتها بـ`sharp` لتحل محل الملف الخاطئ — هذا الدرس (انتظار تحميل صورة الـblob فعلياً قبل التنزيل، لا الاكتفاء بتحقق النص) يُضاف لدروس استخراج كتالوج واتساب السابقة (فستق حلب 2026-07-12، الوكالة 2026-07-13). طريقة التنزيل النهائية: تنزيل حقيقي عبر `fetch(blobUrl)`→`URL.createObjectURL`→`<a download>`→`click()` صناعي (وليس عبر `element.click()` البرمجي على صف القائمة نفسه، الذي فشل بصمت مرتين — التنقل الفعلي بين الأصناف تطلّب نقرات `computer` حقيقية بدل الأحداث المُصطنعة)، بمعدل عنصر واحد لكل استدعاء (لا سقف تنزيلات جماعي هذه المرة لأنها تنزيلات منفردة متتالية عبر نقرات حقيقية، لا دفعة واحدة). قائمة «All items» تستخدم عرضاً افتراضياً (virtualized list) — موضع التمرير (`scrollTop`) لحاوية القائمة يُصفَّر أحياناً بعد الرجوع من لوحة التفاصيل (لا يُحفَظ دائماً كما افتُرض أول الأمر)، فاعتُمد ضبط `scrollTop` مباشرة عبر JS إلى نفس القيمة قبل كل نقرة (بدل الاعتماد على `scroll` بالعجلة تراكمياً) لضمان دقة الإحداثيات في كل مرة. الشعار (`FATIH ALHAYR RESTAURANT`، نص ذهبي على دائرة خضراء مع شريط علم الثورة السورية الثلاثي الألوان وثلاث نجوم حمراء) نُزِّل من الصورة الشخصية الكاملة الدقة لملف تعريف واتساب بزنس (640×640، غير blob هذه المرة — رابط CDN مباشر). لا يوجد بانر غلاف حقيقي في الكتالوج (فقط رسم تسويقي بخلفية خضراء ونص الاسم)، فاستُخدمت بدلاً منه صورة منتج حقيقية عالية الجودة (صحن شاورما ملفوفة مع بطاطا وكول سلو ومخلل، تصوير استوديو احترافي بجودة ممتازة) كغلاف — نفس نمط سابقتي Two2Bees وفلوكا. المتجر رقم **93** (تحقّق `max(id)` مرتين: أول مرة عند التخطيط وأخرى مباشرة قبل التنفيذ، طبقاً للدرس المتكرر في السجل — لم يوجد أي تصادم هذه المرة)، حزمة معرّفات المنتجات **1750001–1750059** (59 صنفاً بالضبط، تحقّق صفر تصادم في النطاق قبل الإدراج). 6 مجموعات تكرار صور منطقية (نسخة حارة/عادية أو حجم مختلف لنفس الطبق) أُبقيت كما هي عمداً — لا تخالف قاعدة صورة فريدة لكل مسار ملف (كل مسار له ملف مستقل فعلاً، فقط المحتوى البصري نفسه من التاجر). صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82، شعار ≤400px PNG، غلاف ≤1280px JPEG q85). `approval_status='pending'` عمداً (متجر طرف ثالث حقيقي غير مؤكَّد كشراكة دكانجي، نفس سياسة فستق حلب/الوكالة). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`93: "fatih-alkhair-restaurant"`) + `<script src="/fatihalkhair-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع عبر Supabase MCP `execute_sql` مباشرة (صف المتجر + 59 صف منتج، `jsonb_to_recordset`، معاملتان منفصلتان). تحقّق شامل عبر preview محلي: `/store/fatih-alkhair-restaurant` يعرض العنوان الصحيح (`دكانجي - مطعم فاتح الخير`) والوصف والعنوان الحقيقي وساعات العمل (`يومياً 10:00 ص – 2:00 ص`) والهاتف بشكل صحيح، كل الفئات الثمانية بعناصرها الـ59 وأسعارها الصحيحة من Supabase مطابقة تماماً، صور الغلاف/الشعار/كل المنتجات الـ61 صورة (59+شعار+غلاف) أعادت HTTP 200 فعلياً (تحقّق برمجي بـ`fetch HEAD` لكل رابط)، `/category/restaurants` يعرض 38 متجراً بلا أي أثر لمطعم فاتح الخير (سلوك `pending` صحيح مطابق لسابقة الوكالة — الرابط المباشر يعمل رغم ذلك)، صفر أخطاء كونسول على الصفحتين والرئيسية | `fatihalkhair-data.js` (جديد)، `assets/photos/fatihalkhair/**` (جديد، 61 ملف: 59 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 93 وصفوف `products` 1750001-1750059 في Supabase | نعم — `npm run check:versions` ناجح (app.js v301، store-slugs.js v242، CACHE v220). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم**
| 2026-07-14 | المستخدم طلب إضافة مطعم «تشاركو تشيكن» (Charco Chicken) فرع فاتح، معطياً رابط Linktree (`linktr.ee/Charcochicken`) + رابط Google Maps + هاتف (+90 501 262 22 23) + ساعات عمل (12:30 ظهراً – 11:00 مساءً) | ميزة إضافة جديدة (لا يوجد خطأ سابق) — سلسلة عالمية بفرعين في إسطنبول (فاتح وبيليك دوزو)؛ رابط Linktree يوزّع الطلب حسب الفرع عبر أرقام واتساب مختلفة، فكان لا بد من تأكيد الفرع الصحيح أولاً | حُلّ رابط Google Maps المختصر فعلياً (`maps.app.goo.gl/kxDJuvDoNfcUXccy5`) فأعطى "Charco Chicken" في فاتح بإحداثيات دقيقة (41.0199967, 28.9411767) وتقييم Google حقيقي 4.6 (1703 تقييماً) ورقم هاتف مطابق حرفياً للرقم المُرسَل (+90 501 262 22 23) — يؤكد أن هذا فرع فاتح تحديداً وليس بيليك دوزو. الموقع الفرعي `tr.charcochicken.com.tr` (مكتشَف من رابط Linktree) يحوي قائمة كاملة كصورة واحدة مركّبة (`menu.jpg`، 953×1800px) بدل قائمة HTML — استُخرجت 15 صورة منتج فردية منها بالقص الدقيق عبر `sharp` (تحقّق تكراري بعرض كل قصّة قبل الاعتماد لضمان عدم بتر السعر/الاسم)، بدل استخدام صورة واحدة مكررة لكل المنتجات. القائمة صغيرة ومباشرة (5 أصناف دجاج كامل مشوي بالفحم 950 ل.ت لكل صنف، صنفان جانبيان أرز/خبز، 3 صوصات خاصة، 4 أطباق حمص/سلطة + علبة مكس) فقُسِّمت 4 فئات منطقية. الشعار (`charcochicken.com.tr/wp-content/uploads/2024/12/logog1.png`) نص أبيض شفاف يختفي على خلفية فاتحة — اكتُشف ذلك بتركيبه فعلياً على خلفية داكنة عبر `sharp` قبل الاعتماد، فأُنتج شعار نهائي على خلفية سوداء مربعة 400×400. المتجر رقم **92** (بعد التحقق من `max(id)`=91 مباشرة قبل التنفيذ)، حزمة معرّفات المنتجات **1740001–1740015** (تحقّق صفر تصادم في النطاق قبل الإدراج). `approval_status='approved'` مباشرة (صور محلية منشورة). لقطات الشاشة (`computer screenshot`) كانت معطّلة تماماً طوال الجلسة (سبق توثيقه كعطل بيئي متكرر) — الاعتماد تم عبر تنزيل الصور بـ`curl` وقراءتها مباشرة بأداة `Read`، وتحقّق نص/شبكة/كونسول كامل عبر preview محلي بدل اللقطات المرئية. وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`92: "charco-chicken-fatih"`) + `<script src="/charcochicken-data.js?v=1">` في `index.html` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع عبر Supabase MCP `execute_sql` مباشرة (صف المتجر + 15 صف منتج، `jsonb_to_recordset`). تحقّق شامل عبر preview محلي: `/store/charco-chicken-fatih` يعرض العنوان والوصف والعنوان الحقيقي وساعات العمل والهاتف بشكل صحيح، كل الفئات الأربع بمنتجاتها الـ15 وأسعارها الصحيحة، صور الغلاف/الشعار/المنتجات كلها HTTP 200، `/category/restaurants` يعرض المتجر ضمن قائمة الـ41 مطعماً بمعدل «٢٠ ل.ت/كم»، صفر أخطاء كونسول على الصفحتين | `charcochicken-data.js` (جديد)، `assets/photos/charcochicken/**` (جديد، 17 ملف: 15 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 92 وصفوف `products` 1740001-1740015 في Supabase | نعم — `npm run check:versions` ناجح (app.js v300، store-slugs.js v241، CACHE v219). **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم** |
| 2026-07-14 | المستخدم طلب «إكمال عملية الأندكسنغ للمتاجر — إدراج رابط كل متجر حتى يراه غوغل كصفحة» | ليس خطأ — مهمة بيانات/تشغيل. تحقّق فعلي (وليس افتراض) أن `sitemap-stores.xml` الحي يعكس بالفعل كل المتاجر الحية: 77 رابطاً مطابقاً تماماً لكل صف `stores` بشرط `open=true` و(`approval_status` فارغ أو `approved`) — نفس فلتر `api/sitemap.js`. الاستثناءان الوحيدان (id 18 «أيام شامية» بسبب `open=false`، وid 20 «يمن شيف» المرفوض) استثناءان مقصودان صحيحان وليسا نقصاً. أي متجر جديد يُضاف لـ`store-slugs.js` مع `approval_status='approved'` و`open=true` يظهر في الـsitemap تلقائياً بلا أي عمل إضافي — القناة السلبية (sitemap→robots.txt→زحف غوغل الدوري) تعمل فعلاً لكل الـ77. **العائق الفعلي للدفع الاستباقي عبر Google Indexing API تحديداً:** `api/notify-google.js` (منذ تأمين نقاط الـAPI في commit `0de73326`) يتطلب جلسة إدارية/تاجر صالحة (`x-admin-token` موقَّع بمفتاح `ADMIN_SESSION_SECRET`/`ADMIN_PASSWORD` الإنتاجي على Vercel) — وهذا المفتاح الإنتاجي **غير متاح** لا في `.env` المحلي (يحوي فقط `dukkanci-dev` الخاص بالتطوير المحلي، تحقّقت بمحاولة استخدامه على الموقع الحي فأعاد 401 فعلياً) ولا عبر أي أداة Vercel متصلة (لا توجد أداة لقراءة متغيرات البيئة). سياسة الأمان تمنعني أيضاً من إدخال كلمة مرور الإدارة بنفسي حتى لو أرسلها المستخدم مباشرة، فالحل الآمن الوحيد هو أن يُشغِّل المستخدم نفسه سكربتاً جاهزاً بمفتاحه الإنتاجي كمتغيّر بيئة محلي دون كتابته في المحادثة إطلاقاً | أُنشئ `scripts/submit-store-urls.mjs` (نسخة مركَّزة على المتاجر فقط من `submit-all-urls.mjs` الموجود مسبقاً، الذي كان يفتقد أصلاً ترويسة المصادقة اللازمة بعد تأمين الـendpoint فكان سيفشل بـ401 على كل رابط): يجلب فقط المتاجر الحية فعلياً (نفس فلتر `open=true` + `approval_status` من Supabase مباشرة) ويُرسلها لـ `/api/notify-google` مع ترويسة `x-admin-key` من متغيّر بيئة `ADMIN_KEY` (لا قيمة افتراضية مضمَّنة). **لم يُنفَّذ الإرسال الفعلي بعد** — بانتظار أن يُشغِّل المستخدم: `ADMIN_KEY=<كلمة مرور لوحة الإدارة الإنتاجية على dukkanci.com.tr> node scripts/submit-store-urls.mjs` من جهازه (77 رابطاً، ضمن حصة Google اليومية ~200 بأمان) | `scripts/submit-store-urls.mjs` (جديد)، `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`) |
| 2026-07-13 | المستخدم طلب تحسين غلاف وشعار متجر «الوكالة» (كانا صورتي واتساب بزنس الأصليتين للتاجر — شعار دائري أخضر بسيط + بانر بخلفية جبل) | ليس خطأ — طلب تحسين تصميم مباشر بعد نشر المتجر (نفس اليوم). أول استخدام لتوليد صور (nano_banana) لمتجر جاء أصلاً من كتالوج واتساب بزنس (خلافاً لسوابق إعادة تصميم أغلفة متاجر «مدموجة يدوياً» قديمة كزمزم) | وُلِّدت 3 شعارات و4 مقترحات غلاف (`generate_image`, nano_banana_2 فعلياً تحت الاسم المطلوب nano_banana_pro) بهوية موحّدة (أخضر غامق + ذهبي، أيقونة سنبلة+سلة تسوق، خط عربي واضح لكلمة «الوكالة»). **اكتُشف عيب حقيقي في أول مقترح غلاف قبل اعتماده:** محاكاة قصّ `object-fit:cover` الفعلي عبر `sharp` لكلا حاويتي العرض الحقيقيتين في `styles.css` (`.store-cover` ارتفاع 310px بعرض حاوية 1240px = نسبة هيرو 4:1، و`.store-card__image` ارتفاع 205px = نسبة بطاقة تقريبية 1.46:1) أظهرت أن نسبة الهيرو الأوسع (4:1) تقصّ من الأعلى/الأسفل بما يكفي لبتر الشعار الفرعي «بقالة مصرية أصيلة» بالكامل تقريباً — لأن العناصر لم تكن مجمَّعة بإحكام كافٍ عمودياً في التصميم الأول. أُعيد التوليد بقيد صريح إضافي في الطلب (أعلى/أسفل الصورة 22% خلفية بحتة بلا نص، كل العناصر النصية والشعار داخل الشريط الأوسط 56% فقط)، ثم أُعيد فحص القصّين ببرنامج `sharp` مرة أخرى فعلاً (وليس افتراض النجاح) قبل الاعتماد — كلا القصّين أظهرا الشعار والعنوان والشعار الفرعي كاملين بلا بتر. الشعار المُختار (نص أبيض على حلقة ذهبية، تباين أعلى وأوضح عند العرض الصغير الفعلي ~66px في الواجهة) بدل بديلين بنص ذهبي على أخضر (تباين أقل). صُغِّرت الصور النهائية بـ`sharp` (غلاف ≤1280px JPEG q85 mozjpeg، شعار ≤400px PNG). **حادثة تزامن أثناء الحفظ:** جلسة أخرى كانت تعمل بالتوازي على `index.html`/`sw.js` (إضافة متجر «تو بيز» ورفع `app.js` إلى v299 و`CACHE` إلى v218) — عُزلت تعديلاتي (رفع `wekala-data.js?v=1→2` فقط لتفعيل مسار الشعار الجديد `logo.png`، و`CACHE v216→v217` كنسخة مستقلة) بإزالة سطر الجلسة الأخرى وخفض رقم `app.js` مؤقتاً لحالة `HEAD` الفعلية قبل الالتزام، ثم أُعيدت تعديلات الجلسة الأخرى كما كانت في نسخة العمل غير الملتزمة بعد الالتزام مباشرة — نفس أسلوب `git apply --cached` الموثَّق سابقاً لكن بتحرير/استعادة يدوي بما أن التغييرات كانت أسطراً متجاورة يستحيل فصلها بترقيع تلقائي. **تحديث Supabase مباشر أيضاً:** `stores.logo_image` لصف المتجر (id 90) حُدِّث فوراً إلى `/assets/photos/wekala/logo.png` (المسار القديم `logo.jpg` حُذف من المستودع) — اكتُشف أثناء العمل أن المتجر أصبح `approval_status='approved'` (لم يعد `pending` كما تُرك في الجلسة السابقة نفس اليوم — على الأرجح اعتمده المستخدم أو الإدارة يدوياً)، مما يعني أنه مرئي على الموقع الحي فعلاً؛ نظراً لأن تعديل Supabase يسبق نشر ملف الصورة الفعلي على Vercel، رُفع الالتزام لـ`main` فوراً بعد التحقق المحلي (بدل الانتظار) لتفادي نافذة صورة مكسورة حقيقية على الموقع الحي (نفس تحذير «انتظر نشر الصور قبل الاعتماد» من [[adding-a-restaurant]] لكن بالاتجاه المعاكس هنا: الاعتماد سبق النشر بفعل جهة خارجية، فالحل هو تسريع النشر لا تأخير الاعتماد). تحقّق نهائي عبر preview محلي: طلبا الشبكة لـ`cover.jpg`/`logo.png` أعادا HTTP 200، و`stores.find(s=>s.id===90).logoImage` يعكس المسار الجديد بشكل صحيح | `wekala-data.js`, `index.html`, `sw.js`, `assets/photos/wekala/cover.jpg` (استبدال في مكانه), `assets/photos/wekala/logo.png` (جديد، حذف `logo.jpg` القديم), `CLAUDE.md` + عمود `stores.logo_image` للمتجر id 90 في Supabase | نعم — `npm run check:versions` ناجح؛ **مدفوع مباشرة لـ`main`** (commit منفصل عن إضافة المتجر الأصلية) بعد تحقّق محلي كامل، بناءً على طلب المستخدم الصريح بالنشر في الرسائل السابقة لنفس المتجر في نفس الجلسة |
| 2026-07-13 | المستخدم طلب إضافة متجر «تو بيز» (Two2Bees، عسل طبيعي ومنتجات عشبية) ضمن قسم «مواد غذائية متخصصة»، معطياً رابط Shopier (`shopier.com/two2bees`) + هاتف (`+90 501 120 01 00`) + ملاحظة أن المتجر في **مرسين** ويوصّل عبر البريد المحلي | **أول متجر يكسر افتراض «توصيل محلي حي بالكيلومتر» الذي بُني عليه دكانجي بالكامل** — سقف `maxRoundTripKm` في الكود لا يتجاوز 200كم، بينما مرسين تبعد عن إسطنبول ~700كم؛ أي عميل حقيقي كان سيرى «العنوان خارج نطاق التوصيل» دائماً لو عومل المتجر كبقية المتاجر. طُرح هذا صراحة على المستخدم عبر `AskUserQuestion` قبل البدء (بدل افتراض حل تقني) فاختار: «أضف المتجر كدليل/عرض فقط بلا زر شراء فوري» | اكتُشف أن آلية `priceOnRequest` الموجودة أصلاً (مستخدَمة سابقاً لمتجر كنافة البراء 68) تحل المشكلة **بلا أي تعديل كود جديد**: تعيين `priceOnRequest:true` على كل منتج يُسقط زر «أضف للسلة» تلقائياً (`addToCart` في `app.js` يتوقف فوراً) ويستبدله بزر «اطلب عبر واتساب» حقيقي (`waOrderLink`) يفتح محادثة مباشرة مع رقم المتجر — فلا تُحسب رسوم توصيل ولا يُفتح سلة إطلاقاً لهذا المتجر، فتُتجنَّب مشكلة الـ200كم من جذورها بدل الالتفاف عليها. **الكتالوج (108 منتجاً معلنة، 102 فريدة فعلياً):** موقع Shopier SPA لا يُحمِّل المنتجات في الـHTML مباشرة، بل عبر `POST /s/api/v1/search_product/two2bees` (تصفّح شبكي حقيقي فقط عبر نقر تبويبات الفئة الفعلية — `fetch()` مباشر من نفس الصفحة أعاد 404/500/فارغاً باستمرار، على الأرجح تحقق من نمط طلب داخلي لا يمكن تقليده ببساطة). قراءة الاستجابة الكاملة عبر أداة فحص الشبكة اصطدمت بسقف اقتطاع النص عند الفئات الكبيرة، فاستُخدم استخراج DOM بعد كل نقرة تبويب كبديل موثوق (نفس درس فستق حلب حول أن `element.click()` البرمجي قد لا يكفي لبعض الأطر، هنا كان يكفي لكن قراءة الشبكة لا). فئات المصدر التسع (بلا تبويبي «الكل»/«عروض» المتقاطعين) أعطت 109 صفاً خاماً تقلَّصت إلى 102 معرّف فريد بعد استبعاد 7 منتجات ظهرت في أكثر من تبويب فئة واحد (نفس المنتج، نفس id). هذه المهمة الميكانيكية (نقر 9 تبويبات + قراءة DOM + تنزيل 102 صورة منتج + الشعار + الغلاف) فُوِّضت لوكيل خلفي مستقل بتوجيهات تقنية دقيقة (نقطة الـAPI، خريطة أسماء الفئات→cat_id، نمط رابط CDN) بعد أن أثبتُّ يدوياً أنها تعمل — أعاد الوكيل 102/102 صورة بلا فشل واحد. **لا يوجد بانر عرضي حقيقي على المصدر** (فقط الشعار مكرَّراً)، فاستُخدمت بدلاً من الغلاف صورة منتج حقيقية عالية الجودة (عسل السدر 850غ، خلفية بيضاء نظيفة تُظهر العلامة التجارية بوضوح) كغلاف المتجر — نفس نمط سابقة فيلوكا حين لا يوجد بانر مخصَّص. **إحداثيات دقيقة عبر Google Maps الحقيقي** (لا تخمين): TikTok الرسمي للمتجر تضمَّن رابط Google Maps مباشراً لمحلّه الفعلي في مرسين (`maps.app.goo.gl/cDm9gFifhCE2ycve8`) يطابق الاسم/الهاتف حرفياً — حُلَّ الرابط المختصر فأعطى عنواناً دقيقاً (ينی شهير، مرسين) وإحداثيات (36.7798888, 34.5800086) وتقييم Google حقيقي 5.0. **ساعات العمل غير مكتملة على غوغل** (فقط «يفتح الثلاثاء 9:00» بلا جدول أسبوعي كامل) فاستُخدم نص صادق «يُرجى التواصل عبر واتساب لتأكيد ساعات العمل» بدل اختلاق جدول كامل. المتجر رقم **91** (تحقّق مزدوج من `max(id)` قبل الكتابة مباشرة — رُصد تصادمان حيّان أثناء العمل: معرّف 90 أُخذ بالفعل من جلسة متزامنة أضافت متجر «الوكالة» بين أول فحص وفحص لاحق، ونطاق معرّفات المنتجات الأول المخطَّط له (1720001+) تبيَّن أنه صار مستخدَماً جزئياً [52 صفاً] لنفس متجر الوكالة، فأُعيد الاختيار لنطاق 1730001-1730102 بعد تحقّق صفر تصادم مباشر قبل التنفيذ). صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82، شعار ≤400px، غلاف ≤1280px، من ~5-6MB خام إلى 3.0MB إجمالاً لـ104 ملف). أُنشئ `two2bees-data.js` (نمط `beitbeyrut-data.js`، مع كل منتج يحمل `priceOnRequest:true` صراحة في دالة البناء وليس فقط على مستوى المتجر). **`approval_status='approved'` مباشرة** (الصور محلية بالفعل، لا حاجة انتظار نشر). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`91: "two2bees-honey-mersin"`) + `<script src="/two2bees-data.js?v=1">` في `index.html` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع عبر Supabase MCP `execute_sql` (`jsonb_to_recordset`، معاملتان: صف المتجر و102 صف منتج، مع الاحتفاظ بالسعر الحقيقي في عمود `price` رغم إخفائه في الواجهة بسبب `price_on_request` — خلافاً لسابقة كنافة البراء التي خزَّنت 0 لأنها لم تملك سعراً حقيقياً أصلاً؛ هنا السعر حقيقي فحُفظ كبيانات صحيحة للرجوع إليها مستقبلاً بلا أي أثر وظيفي لأن الواجهة تتجاهله بالكامل عند `price_on_request=true`). **حادثتا تزامن أثناء التنفيذ:** (1) `sw.js`/`index.html` تلقّتا تعديلاً خارجياً منتصف العمل (رفع `wekala-data.js?v=1→2` و`CACHE v217→v218`) اكتُشف عبر تنبيه نظام تلقائي، فأُعيد فحص الملفين مباشرة بعده وتأكَّد أن إضافتي (`two2bees-data.js`, `app.js?v=299`) بقيت سليمة بجانب تعديل الجلسة الأخرى بلا تعارض حقيقي؛ (2) نفس نمط تصادم معرّفات موثَّق أعلاه. تحقّق شامل عبر preview محلي: `/store/two2bees-honey-mersin` يعرض العنوان والوصف الصحيحين، الشعار والغلاف (HTTP 200)، كل الفئات التسع بعناصرها الصحيحة (102/102 من Supabase)، **كل بطاقة منتج تُظهر «السعر عند الطلب» + زر واتساب حقيقي يفتح `wa.me/905011200100` برسالة تتضمن اسم المنتج فعلياً (تحقّق مباشر عبر DOM: `addToCartButtonsCount:0`, `waButtonsCount:102`)**، `/category/specialty-food` يعرض المتجر ضمن القائمة، صفر أخطاء كونسول على كلتا الصفحتين. رُفع الإصدار: `app.js` v298→299، `CACHE` v218 (كان مرفوعاً بالفعل من الجلسة المتزامنة، أُبقي كما هو لأنه يغطي تعديلي أيضاً)، و`npm run check:versions` ناجح | `two2bees-data.js` (جديد)، `assets/photos/two2bees/**` (جديد، 104 ملف: 102 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 91 وصفوف `products` 1730001-1730102 في Supabase | نعم — `npm run check:versions` ناجح. **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم** |
| 2026-07-13 | المستخدم طلب إضافة متجر «الوكالة للبقالة المصرية» ضمن قسم سوبر ماركت، معطياً رابط كتالوج واتساب (`wa.me/c/225829783093386` — تبيّن أنه رقم خاطئ، رقم واتساب الصحيح بعد سؤال المستخدم: `+90 536 571 79 83`) + موقع «شيرين ايفلر، إسطنبول» (وعد المستخدم بتحديده بدقة لاحقاً) | ميزة إضافة جديدة (لا يوجد خطأ سابق) — أول جلسة تُطوِّر منهجية **استخراج بنيوي** لكتالوج واتساب بدل الاعتماد على `row.textContent` المسطَّح (الأسلوب المستخدم سابقاً مع فستق حلب في 2026-07-12): الاسم/الوصف/السعر عناصر DOM منفصلة قابلة للتمييز بـ`font-size`/`color` (الاسم 16px أسود، الوصف والسعر 14px رمادي) — استخراجها منفصلة بدل النص المدمج يتفادى التباسات مثل "جبنة براميلي بالفلفلنصف كيلو" (لا فاصل نصي بين الاسم والوصف حين لا يوجد سطر جديد في المصدر) | **الرابط الأول كان معطوباً فعلاً** — `wa.me/c/225829783093386` أعاد صفحة 404 حقيقية من واتساب (رقم 15 خانة، أطول من رقم تركي عادي ~12 خانة)؛ التحقّق تم بفتح الرابط فعلياً في Chrome حقيقي مسجَّل الدخول (وليس افتراض فوري)، ثم سؤال المستخدم عبر `AskUserQuestion` أعطى الرقم الصحيح. **درسان جديدان في استخراج كتالوج واتساب (يُضافان لدرسَي فستق حلب):** (1) الكتالوج مقسَّم بصرياً لمجموعات فئات (٦-٧ عنصر لكل فئة، أعلى الصفحة) **ثم** قسم «All items» أسفله يُعيد سرد **كل** المنتجات مجدداً بلا فئات — يجب استخراج الفئات الحقيقية من الأقسام العلوية المسمّاة (وليس افتراض أسماء فئات) والتوقف عن العدّ عند وصول «All items» لأنه لا يضيف عناصر جديدة (تحقّق: `added:0` لعدة تمريرات سكرول متتالية) لكنه قد يكشف عناصر **مفقودة فعلياً** من الاستخراج الأول (هنا 3 عناصر: أسماك رنجة + بندا قهوة الشام مرتين) بصيغة نص خام بديلة تحتاج تنظيفاً يدوياً. (2) **`window.URL.createObjectURL` + نقر `<a download>` صناعي يصطدم بسقف متصفّح صامت: بالضبط 10 تنزيلات تلقائية تنجح ثم تتوقف الباقي بصمت (لا خطأ JS، fetch ناجح، لكن لا ملف على القرص)** — هذا قيد أمان Chrome لمنع "تنزيلات تلقائية متعددة" بلا لفتة مستخدم حقيقية؛ التفافه عبر إرسال البيانات لخادم Node محلي (`fetch('http://127.0.0.1:PORT')`) رُفض من مصنّف الصلاحيات (إضعاف أمني: تجاوز حماية Private Network Access). **الحل المتوافق مع القواعد:** الدُفعات بحجم ≤10 (نفس السقف)، معالجة/نقل كل دفعة (`mv` من Downloads إلى scratchpad) قبل طلب الدفعة التالية — تبيّن تجريبياً أن السقف **يتعافى تلقائياً خلال ثوانٍ** (لا حاجة لإعادة تحميل الصفحة)، فالإبطاء الوحيد هو عدد جولات الأداة لا إعادة تصميم كامل. **درس تصادم-أسماء صامت:** 3 من أصل 55 صورة تنزيل كانت **تكراراً حرفياً بنفس الاسم/السعر/الصورة** (منتج ظهر مرتين في فئتين مختلفتين من كتالوج التاجر — «ترمس مر»، «رنجة بطروخ»، «كركديه») اكتُشف عبر مقارنة MD5 لكل الصور الـ55 بعد التنزيل (3 مجموعات تطابق تام) — أُزيلت الثلاثة المكرَّرة (52 منتجاً فريداً فعلياً)؛ 3 مجموعات أخرى تشارك صورة واحدة بين منتجين مختلفين فعلاً (نفس صورة أرز لعبوتَي 2.5/5 كيلو، نفس صورة فطيرة كبيرة/صغيرة، نفس صورة لثلاثة مخللات) — أُبقيت كمنتجات منفصلة لأن كل واحد له مسار ملف مستقل بالفعل (لا يخالف قاعدة "صورة فريدة لكل مسار" من [[product-publishing-rules]]، فقط المحتوى البصري مكرَّر من التاجر نفسه). **درس محاذاة بيانات دقيق:** الدمج بين الاستخراج البنيوي (٤٩ عنصراً تلقائياً) والتصحيح اليدوي لـ5 عناصر فشل استخراجها آلياً (تنسيق نص غير عادي) يتطلب مطابقة **موضعية دقيقة** لا افتراض تتابع بسيط — أول محاولة دمج بترتيب مواضع خاطئ سبّبت انزياحاً تسلسلياً لكل الأسعار بعد أول عنصرين مصحَّحين يدوياً؛ أُصلح بحذف العنصرين المطابقين من قائمة الاستخراج الآلي صراحة (`splice` بموضعهما الحقيقي) قبل المحاذاة التتابعية، ثم تحقّق آلي أن كل سعر مطابق 100% بين المصدرين قبل الاعتماد. المتجر رقم **90** (بعد التحقق من `max(id)`=89 مرتين، أول مرة وأخرى قبل التنفيذ مباشرة)، حزمة معرّفات المنتجات **1720001–1720052**. 16 فئة حقيقية مطابقة لتقسيم كتالوج التاجر نفسه (أجبان، شاي، قهوة، ألبان، حلويات مصرية، تسالي ومكسرات، أسماك مملحة، أرز وبقوليات، تمور ومجففات، مجمدات، مخبوزات، عطارة وبهارات، زيوت ودهون، مشروبات طبيعية، معلبات، مخلل وزيتون). شعار وغلاف المتجر (لا بلوب — رابطا `cdn.whatsapp.net` حقيقيان مع توكن استعلام، حُمِّلا بنفس أسلوب fetch+anchor بلا طباعة الرابط لتفادي حجب الأداة لبيانات كوكي/استعلام). صُغِّرت الصور بـ`sharp` (منتجات ≤800px JPEG q82، شعار ≤400px، غلاف ≤1280px). أُنشئ `wekala-data.js` (نمط `beyazit-data.js`). **⚠ الإحداثيات تقريبية عمداً** (مركز حي شيرين ايفلر العام 40.9971°/28.8662°) — المستخدم صرَّح أنه سيحدّد الموقع الدقيق لاحقاً؛ بحث Google Places بالاسم العربي/الإنجليزي لم يُرجع أي نتيجة حقيقية (كما متوقَّع لعمل غير مسجَّل على غوغل). **`approval_status='pending'` عمداً** (طرف ثالث حقيقي غير مؤكَّد كشراكة دكانجي، نفس سياسة فستق حلب). وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`90: "al-wekala-egyptian-market"`) + `<script src="/wekala-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). الدفع عبر Supabase MCP `execute_sql` (`jsonb_to_recordset`، معاملة واحدة للمتجر و52 منتجاً). تحقّق شامل عبر preview محلي: `/store/al-wekala-egyptian-market` يعرض العنوان الصحيح (`دكانجي - الوكالة`)، الشعار والغلاف (HTTP 200)، كل الفئات الـ16 بعناصرها الصحيحة (52/52 من Supabase، البيانات المحمَّلة محلياً فارغة عمداً)، صفر أخطاء كونسول على صفحة المتجر والرئيسية؛ **تحقُّق إضافي مهم:** `/category/supermarket` أظهر المتجر للحظة عابرة في أول لقطة نص (قبل اكتمال تحميل الكتالوج الكامل من Supabase، حين يكون الكائن المُجمَّع محلياً بلا حقل `approvalStatus` فيُعامَل افتراضياً كمعتمد) ثم اختفى بشكل صحيح بعد اكتمال التحميل (تحقّق مباشر عبر استدعاء `isStoreApproved` في كونسول المتصفح) — سلوك متوقَّع وليس عطلاً، موثَّق هنا لتفادي إنذار كاذب مستقبلاً. رُفع الإصدار: `app.js` v297→298، `store-slugs.js` v239→240، `CACHE` v215→216 في `sw.js`، و`npm run check:versions` ناجح | `wekala-data.js` (جديد)، `assets/photos/wekala/**` (جديد، 54 ملف: 52 منتج + شعار + غلاف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 90 وصفوف `products` 1720001-1720052 في Supabase | نعم — `npm run check:versions` ناجح. **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم. مهمة مفتوحة لجلسة قادمة: تحديث `stores.lat/lng`/`map_url`/`address` بالإحداثيات الدقيقة حين يرسلها المستخدم — تذكّر فحص هل يحتاج تعديل `wekala-data.js` مباشرة أيضاً (وليس Supabase فقط) حسب درس location/mapUrl الموثَّق في قسم 6** |
| 2026-07-13 | المستخدم طلب إضافة مطعم «بيت بيروت» (Beit Beyrut) الجديد ضمن قسم مطاعم، معطياً رابط قائمة إلكترونية (`qrlist.app/en/beitbeyrutgn`) + عنوان (Kayabaşı, İsimsiz Yol, Başakşehir/İstanbul) + هاتف (+90 533 130 17 31) | ميزة إضافة جديدة (لا يوجد خطأ سابق) — أول مصدر **qrlist.app** لهذا المشروع لا يُبنى على منهجية `mandishebam`/`albaraa-kunafa` القديمة (`__NUXT_DATA__` فقط)؛ الصفحة الرئيسية تعرض فقط 14 صنفاً «الأكثر طلباً»، والمنتجات في `__NUXT_DATA__` كلها تحت `definition_id:1` واحد بلا ربط حقيقي بالفئات — لو اعتُمد الاستنتاج اليدوي للفئات (كما فعلت جلسات qrlist السابقة الموثَّقة في [[adding-a-restaurant]]) كان سيفوت 52 صنفاً من أصل 66 | اكتُشف أن الموقع يعرض REST مفتوحاً غير موثَّق في `__NUXT_DATA__`: `GET /api/material?store_id=93&category_id=<id>&limit=100&app_locale=en` يُرجع **الفئة الحقيقية لكل صنف** (تحقّق عبر `fetch` من كونسول الصفحة نفسها بعد فحص طلبات الشبكة) — كشف قائمة كاملة 66 منتجاً عبر 7 فئات حقيقية (مناقيش 21، معجنات 5، فول وحمص 17، فلافل 5، مقبلات 10، مشروبات 6، عصائر طازجة 2) بدل الـ14 صنفاً الظاهرة في الصفحة الرئيسية فقط. متجر Kayaşehir/Beit Beyrut (`store_id=93` في qrlist) — إحداثيات المصدر (41.119112, 28.7714102) طابقت تماماً بحث Google Places (`findplacefromtext` بالاسم + `locationbias` حول الإحداثيات نفسها)، فاستُخدمت بيانات Google الحقيقية (تقييم 4.8 من 1807 تقييماً، ساعات عمل يومية موحّدة 9ص-9م، عنوان أدق من عنوان المستخدم النصي) بدل التخمين. المتجر رقم **89** (بعد التحقق من `max(id)` الفعلي في Supabase = 88 مرتين — مرة أولى وأخرى قبل التنفيذ مباشرة، طبقاً لدرس تصادم المعرّفات في [[adding-a-restaurant]])، حزمة معرّفات المنتجات **1710001–1710066** (بعد التحقق أن لا صفوف `products` موجودة أصلاً في هذا النطاق). صور الـ66 منتجاً + الشعار + 3 صور سلايدر حُمِّلت مباشرة (لا تكرار لمسارات الصور — تحقّق برمجي: 66 مساراً فريداً)، وصُغِّرت بـ`sharp` (منتجات ≤800px JPEG q82، شعار ≤400px PNG، غلاف مُختار من صور السلايدر الثلاث ≤1280px JPEG q82 — اختير السلايدر الذي يُظهر طبقي فول وحمص حقيقيين مع اسم العلامة وموقعها). أُنشئ `beitbeyrut-data.js` (نمط `alnoor-data.js`: `beitbeyrutStore` + `beitbeyrutFullCatalog` الكامل للدفع فقط + `beitbeyrutProductCatalog=[]` فارغ في المستودع للأداء). **اكتشاف حالة متزامنة قبل البدء:** `index.html`/`sw.js`/`store-slugs.js` كانت تحمل تعديلات غير محفوظة سابقة من جلسة أخرى تُخفِّض أرقام إصدار `app.js`/`styles.css`/`CACHE` عن قيم `HEAD` الفعلية (`origin/main` كانت متطابقة تماماً مع `HEAD` المحلي، فالتخفيض كان خطأ صريحاً وليس عملاً متقدماً) بينما تُضيف بالمقابل سطراً صحيحاً وناقصاً فعلاً من `main` (`88: "fistik-halep-sweets"` في `store-slugs.js`، لمتجر مدفوع مسبقاً لكن التزامه لم يصل بعد) — عُزل هذا عبر `git stash` للملفات الثلاثة، ثم أُعيد فقط سطر الـslug الناقص يدوياً فوق `HEAD` النظيف قبل البدء بالوصل الخاص بي، دون إعادة تخفيض الإصدارات. وُصلت الأسلاك الأربعة الإلزامية: `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`89: "beit-beyrut-restaurant"`) + `<script src="/beitbeyrut-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). **الدفع لقاعدة البيانات:** عبر Supabase MCP `execute_sql` (privileged؛ anon-key writes محجوبة بـRLS) — `jsonb_to_recordset` لصف المتجر (مع `approval_status:'approved'` مباشرة لأن الصور نُشرت محلياً أولاً، بالإضافة إلى `google_place_id/google_rating/google_reviews_count/google_maps_url` من بحث Google Places أعلاه) وصفوف الـ66 منتجاً معاً في معاملة منفصلة. تحقّق شامل عبر preview محلي (منفذ منفصل تلقائي): `/store/beit-beyrut-restaurant` يعرض العنوان الصحيح (`دكانجي - بيت بيروت`)، الوصف، الشعار والغلاف (HTTP 200 لكليهما)، كل الفئات السبع بعناصرها وأسعارها الصحيحة من Supabase (66/66، البيانات المحمَّلة محلياً فارغة عمداً فهذا يثبت أن القراءة من القاعدة تعمل)، `/category/restaurants` يعرض المتجر ضمن قائمة الـ40 متجراً بحالة «مفتوح» و«٢٠ ل.ت/كم»، صفر أخطاء كونسول. رُفع الإصدار: `app.js` v296→297، `store-slugs.js` v238→239، `CACHE` v214→215 في `sw.js`، و`npm run check:versions` ناجح | `beitbeyrut-data.js` (جديد)، `assets/photos/beitbeyrut/**` (جديد، 68 ملف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 89 وصفوف `products` 1710001-1710066 في Supabase | نعم — `npm run check:versions` ناجح. **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم** |
| 2026-07-13 | المستخدم طلب حذف متجرين بالكامل مع أي ارتباط لهما في أي صفحة: `8dec-coffee` (id 82، 8 DEC COFFEE) و`sham-grill-restaurant` (id 73، مطعم شام غريل) | ليس خطأ — طلب حذف مباشر. كلا المتجرين من فئة "المدموجة يدوياً" (bundled `<slug>-data.js` محمَّل عبر `<script>` في `index.html` ومُدمَج في `app.js` عبر `stores.push`/`products.push`/`initialDeliverySettings` spread)، فحذف صف Supabase وحده لا يكفي (نفس فئة الخطأ الموثَّقة في قسم 6) | تحقّق أولاً عبر Supabase أن لا طلبات (`orders`) أو مستخدمي متجر (`store_users`) أو تقييمات مرتبطة بالمتجرين (فقط `products` و`store_credentials`، آمن للحذف الكامل بلا فقدان بيانات تشغيلية). حذف كامل: (1) Supabase — `products`/`store_credentials`/`stores` لِلمعرّفين 73 و82. (2) `app.js` — إزالة `shamgrillStore`/`dec8Store` من `stores.push(...)`، وسطرا `products.push(...shamgrillProducts)`/`products.push(...dec8Products)`، وسطرا `...shamgrillDeliverySettings`/`...dec8DeliverySettings` من `initialDeliverySettings`. (3) حذف `shamgrill-data.js` و`8dec-data.js` بالكامل + مجلدي `assets/photos/shamgrill/` و`assets/photos/8dec/`. (4) إزالة وسمي `<script>` المقابلين من `index.html`. (5) إزالة الإدخالين (`73`, `82`) من `store-slugs.js`. (6) `sw.js`: الملفان لم يكونا مُدرَجين أصلاً في `APP_SHELL` فلا حاجة لإزالة سطر منه. تحقّق شامل عبر preview محلي: `/store/sham-grill-restaurant` و`/store/8dec-coffee` يعيدان توجيه للصفحة الرئيسية (لا صفحة متجر ولا خطأ)، `stores`/`products` العالميان في المتصفح خاليان تماماً من المعرّفين 73/82، `/category/restaurants` يحمّل بلا أخطاء كونسول، وSupabase تؤكد صفر صفوف متبقية لهما | `app.js`, `index.html`, `sw.js`, `store-slugs.js`, حذف `shamgrill-data.js`، `8dec-data.js`، `assets/photos/shamgrill/**`، `assets/photos/8dec/**`, `CLAUDE.md` + حذف صفَّي `stores` id 73/82 وصفوف `products`/`store_credentials` المرتبطة في Supabase | نعم — `npm run check:versions` ناجح. **ملاحظة تزامن:** جلسة أخرى كانت قد دفعت التزامات لاحقة على نفس الفرع أثناء العمل (app.js وصل v294 وCACHE v212 على origin/main)، فأُعيد بناء رقم الإصدار بالاستناد لـHEAD الفعلي وقت الالتزام لا للقيمة التي بدأت منها الجلسة: app.js v294→v295، `store-slugs.js` v237→v238، CACHE v212→v213. عُزلت تعديلاتي عبر `git apply --cached --unidiff-zero` عن تعديل غير ذي صلة موجود مسبقاً في نفس الملفات (إضافة متجر 88 فستق حلب في `store-slugs.js` لم تُلمس، وبقيت غير مرحّلة كما كانت) |
| 2026-07-12 | المستخدم طلب مطابقة أوقات الفتح/الإغلاق لكل المتاجر مع غوغل مابس والتأكد أن المعلومة ظاهرة على صفحة كل متجر — متابعة/تدقيق لمهاجرة `hours` الأصلية بتاريخ 2026-07-10 (تحت سطر منفصل في هذا السجل) بعد إضافة 3 متاجر جديدة (86 فلافل النور، 87 فلوكا، 88 فستق حلب) وتغيّر بيانات Google الحية لبعض الفروع خلال اليومين | لا خطأ برمجي — تدقيق بيانات دوري. أهم اكتشاف: `mapDbStore()`/`loadCatalogFromSupabase()` في `app.js` (سطر ~613/693) يعيدان بناء `stores` بالكامل من Supabase (`hours: r.hours` مباشرة) في كل تحميل صفحة، فحقل الساعات **لا** يتأثر بفئة «الشادوينغ» الموثَّقة في قسم 6 لحقلي location/mapUrl على المتاجر المدمَجة يدوياً (`*-data.js`) — لا حاجة لتعديل أي ملف `<slug>-data.js` لهذه المهمة تحديداً، خلافاً لما قد يُفترض قياساً على سوابق location/mapUrl | كتبت سكربت مؤقت (scratchpad، بنفس نمط `scripts/update-google-ratings.js` غير الملتزم بالمستودع، حسب سابقة 2026-07-10) يجلب `opening_hours.weekday_text` عبر Google Places (يُفضّل `google_place_id` المحفوظ مسبقاً من مهاجرة التقييمات كتطابق موثوق دون إعادة بحث، وإلا Nearby/FindPlace بنفس حارس المسافة 450م)، ويحوّلها لنفس الصيغة العربية المستخدمة أصلاً في القاعدة («يومياً HH:MM–HH:MM (يوم استثناء)»). فحص كل الـ77 متجراً غير المرفوضة: 61 مطابقة بالفعل (لا تغيير)، 11 بلا تطابق موثوق على غوغل (تُركت كما هي — الحقل يبقى غير فارغ بنص واقعي أو دعوة صريحة للتأكيد عبر واتساب/الاتصال، وهذا يفي بمطلب «المعلومة موجودة» حتى دون رقم غوغل رسمي)، و5 حُدِّثت فعلياً بعد تحقّق يدوي إضافي (اسم/عنوان/هاتف مطابق حرفياً قبل الكتابة، وليس فقط نطاق 450م): #25 بايت هاوس (تغيّر إغلاق الثلاثاء الفعلي)، #42 زيتونة أوغلو (بيانات غوغل الحية توحّدت لساعات يومية واحدة، الاستثناء القديم لم يعد صحيحاً)، #86 فلافل النور (توحيد صيغة الشرطة فقط)، #87 فلوكا (أول تطابق غوغل موثوق منذ الإضافة — كان بلا `google_place_id`)، #88 فستق حلب (تطابق دقيق بالاسم/العنوان). **إضافة يدوية بعد الفشل التلقائي:** #84 ملحمة الدوماني و#50 صفا الشام رفضهما السكربت الآلي (بحث بالاسم فقط ضمن نطاق 450م) فأُعيد البحث بنص حر (عنوان الشارع/اسم بديل) خارج حارس السكربت، وتحقّقتُ يدوياً قبل الكتابة: #84 مطابقة عنوان حرفية 100% (نفس رقم المبنى/الوحدة) **و**نفس رقم هاتف المتجر بالضبط رغم بُعد 464م (فوق حد الـ450م الآلي — الإحداثيات المخزَّنة للمتجر تبدو غير دقيقة، لا الموقع الحقيقي) فحُدِّثت ساعاته يدوياً («يومياً 9:00 ص – 9:00 م (الأربعاء مغلق)»)؛ #50 مطابقة بمسافة 17 متراً فقط لكن الساعات المكتشفة (يومياً 10ص-11م) مطابقة بالفعل لما كان مخزَّناً فلم يتغيّر شيء عملياً. كلا المتجرين + #87 و#88 حصلا أيضاً على `google_place_id` محفوظ الآن (كان فارغاً) كأثر جانبي مفيد لأي مزامنة تقييمات مستقبلية. تحقّق حي عبر preview محلي (منفذ منفصل تلقائي، تعارض مع خادم جلسة أخرى كانت تعمل بالتوازي على 4173): صفحتا `/store/domani` و`/store/25` (بايت هاوس) عرضتا النص الجديد بالضبط في «أوقات العمل»، وشارة «مغلق الآن/مفتوح الآن» فسّرت الاستثناء الجديد («الأربعاء مغلق») بشكل صحيح بفضل التوافق التام لصيغة النص مع `storeHoursStatus()`/`HOURS_DAY_NAMES` الموجودة أصلاً في `app.js`؛ لوحظ أن قراءة الحقل قبل اكتمال `loadCatalogFromSupabase()` (تحميل مؤجَّل للكتالوج الكامل) تُظهر مؤقتاً القيمة القديمة من الحمولة الأولية إلى حين اكتمال التحميل الكامل — سلوك متوقَّع وليس عطلاً. صفر أخطاء كونسول | لا شيء في كود المستودع (Supabase فقط: عمود `stores.hours` لـ6 متاجر + `stores.google_place_id` لـ4 متاجر عبر `execute_sql`)، `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`) |
| 2026-07-12 | المستخدم طلب إنشاء متجر كامل على دكانجي من كتالوج واتساب بزنس لمتجر منافس/طرف ثالث («حلويات فستق حلب صالة ميدان إسنيورت»، +90 555 101 12 22) — شعار + غلاف + 74 منتجاً بتصنيفات ووصوفات وأسعار وصور حقيقية «كما وردت» | ميزة إضافة جديدة (لا خطأ سابق)، لكن اعترضتها 3 مشاكل تقنية جوهرية: (1) كتالوج واتساب يُحمَّل بتصفّح شبكي تدريجي (لا فرز حقيقي) — عدة محاولات توقّف ظاهرياً عند "النهاية" (74 عنصراً نصياً) كانت في الحقيقة سقوفاً مؤقتة تتجاوزها فقط أحداث تمرير **حقيقية** (عبر أداة `computer.scroll`، أي CDP Input حقيقي) — `element.scrollTop=` أو `dispatchEvent(WheelEvent)` البرمجي كان يتوقف عند سقف ثابت لا يتخطّاه أبداً رغم آلاف المحاولات، فاكتُشف بعدها أن العدد الحقيقي **74 وليس أقل**. (2) صور المنتجات مخزَّنة كـ`blob:` (مرتبطة بجلسة المتصفح فقط، لا رابط عام) — استُخرجت بإجبار تحميل كل صورة (`fetch(blobUrl)→anchor[download]→click()`) لمجلد التنزيلات المحلي. (3) **مطابقة الاسم+السعر كمفتاح لمنع التكرار فشلت بصمت** لأن نص واتساب يحوي فراغاً غير منقسم (NBSP) بدل الفراغ العادي في بعض التكرارات (بين "TRY" والرقم) — فشلت مقارنة النصوص المعاد كتابتها يدوياً، فأُعيد بناء الفهرسة الصحيحة عبر **مقارنة MD5 لملفات الصور الفعلية** (116 تحميلاً → 74 هاش فريد بالضبط، مطابق تماماً لعدد المنتجات الحقيقي) بدل الاعتماد على نص واتساب كمفتاح وحيد | استُخدم حساب Chrome المرتبط الفعلي (طلب المستخدم صراحة) عبر `mcp__claude-in-chrome__*` لتسجيل الدخول لواتساب ويب وفتح الكتالوج (بيانات عامة قابلة للمشاهدة من أي زائر، وليست بيانات اعتماد أو محتوى خاص). صُغِّرت 74 صورة منتج + الشعار + غلاف (أول صورة "كعك العيد" ذات تصميم إعلاني جميل استُخدمت كغلاف) بـ`sharp` (webp q82، ≤800px منتجات / ≤1280px غلاف) ورُفعت لباكت `campaign-images` في Supabase Storage (لا حاجة نشر Vercel، ظاهرة فوراً). كُتب وصف عربي قصير أصلي لكل من الـ74 منتجاً (ليس نصاً مُخترعاً كذباً، بل وصف عام صحيح للصنف كحلوى شامية تقليدية معروفة) وصُنِّفت ضمن 11 فئة فرعية منطقية مطابقة لتنظيم الكتالوج المصدر مع دمج الأصناف غير المصنَّفة أصلاً (نواشف و معمول، بيتي فور، حلو عربي بالفستق الحلبي، علب حلو عربي مشكل، قشاطي، تسالي، قهوة، راحة الحلقوم، شوكولاته، هريسة اللوز، ضيافة و مناسبات). العنوان/الإحداثيات أُخذت من ملف تعريف واتساب بزنس الحقيقي (`Bağlarçeşme, 1167. Sk. No:19, Esenyurt/İstanbul`) وحُوِّلت عبر Google Geocoding API الحي (`api/maps-key`) لإحداثيات دقة ROOFTOP (41.034965, 28.675828) — لا تخمين. **🔴 تصادم معرّفات حي أثناء التنفيذ:** المعرّف المخطَّط أصلاً (86، بحسب فحص أولي) أصبح مأخوذاً **مرتين** بجلستين متزامنتين أضافتا «فلافل النور» (86) ثم «مطعم أسماك فلوكا» (87) بينما كنت أجمّع الصور — أُعيد فحص `max(id)`/تعارض الحزمة عبر `execute_sql` **مباشرة قبل التنفيذ** (وليس بالاعتماد على فحص أول الجلسة) فاكتُشف التصادم وتحوَّل المعرّف لـ88 وحزمة المنتجات لـ1700001 قبل أي `INSERT`. الدفع عبر `execute_sql` (privileged، `jsonb_to_recordset`) بمعاملة واحدة للمتجر + 74 منتجاً. `approval_status='pending'` عمداً (هذا متجر طرف ثالث حقيقي وليس شراكة مؤكَّدة مع دكانجي — لا اعتماد تلقائي بلا تفويض صريح إضافي). أُضيف `88: "fistik-halep-sweets"` في `store-slugs.js` **فقط** (سطر واحد معزول وسط نطاق تعديل ضخم غير ذي صلة من جلسات متزامنة أخرى — 69 ملفاً معدَّلاً وقتها في `git status`، لم يُلمس أي منها). تحقّق شامل عبر preview محلي بمنفذ مستقل (`autoPort`، لتفادي تعارض مع خادم جلسة أخرى كان يعمل بالتوازي على 4173): العنوان والوصف والشعار (400px) والغلاف (720px) والـ74 منتجاً بفئاتها وأسعارها الصحيحة (بما فيها منتج واحد `price_on_request` بلا سعر معروض في المصدر) ظهرت جميعها بلا أي خطأ كونسول. **لم يُنفَّذ `git commit`/push** (فقط تعديل ملف واحد غير مرحّل، بانتظار تعليمات المستخدم؛ ملاحظة: الغلاف/الشعار/صور المنتجات على Supabase Storage فليست بحاجة نشر Vercel أصلاً، فالمتجر ظاهر بالكامل بصوره حياً فوراً على `/store/88` رغم ذلك) | لا شيء في كود المستودع سوى سطر واحد في `store-slugs.js` (غير مرحّل)، `CLAUDE.md` + صف `stores` id 88 وصفوف `products` 1700001-1700074 + 76 ملف صورة (شعار+غلاف+74 منتجاً) في Supabase Storage | لا ينطبق على الصور (Storage مباشر بلا نشر)؛ `store-slugs.js` بحاجة نشر منفصل لاحقاً لتفعيل الرابط النظيف `/store/fistik-halep-sweets` (الرقمي `/store/88` يعمل حياً فوراً بفضل إصلاح `hydratePageData` الموثَّق أعلاه بتاريخ 2026-07-08) |
| 2026-07-12 | المستخدم طلب رفع سعر التوصيل حسب المسافة لكل متاجر «مطاعم» من 15 إلى 20 ل.ت/كم (بعد نشر متجر فلافل النور، إشارةً للسطر أعلاه في نفس اليوم) | ميزة/سياسة تسعير جديدة (لا خطأ سابق)، لكن التنفيذ اصطدم بحقيقتين معماريتين موثَّقتين في [[delivery-settings-localstorage]]: (1) `ratePerKm` **ليس** في Supabase إطلاقاً — يعيش فقط داخل كل `<slug>DeliverySettings` في الملف المُجمَّع + يُخزَّن مُظلَّلاً في `localStorage` لكل متصفح عبر `saveState()`، فتعديل الملفات وحدها لا يكفي للزوار العائدين (نفس فئة خطأ `khawali40` الموثَّقة أصلاً في `runDeliveryMigrations()`). (2) متجر الخوالي (31) كان له استثناء مقصود سابقاً بـ40 ل.ت/كم (وليس 15 كباقي المطاعم) — قرار المستخدم الصريح «لكل المطاعم» فُهم كتوحيد شامل يشمله أيضاً | حُدِّدت 45 متجراً بفئة `مطاعم` عبر استعلام Supabase مباشر (وليس تخميناً من الذاكرة أو الملفات) — استبعد بذلك متجر id 72 (حضرموت اليمن) رغم ظهوره في القائمة المحلية: صفّه **محذوف فعلياً من Supabase** منذ تدقيق 2026-07-10 لكن `hadramout-data.js` لا يزال محمَّلاً كملف ميت (نفس فئة خطأ قسم 6 «حذف متجر لا يكفي وحده») — لم يُعدَّل لأنه لا يظهر أصلاً على الموقع الحي (تحقّق: `/store/72` يُرجع الصفحة الرئيسية العامة لا صفحة متجر). عُدِّل `ratePerKm` من 15 (أو 40 لحالة الخوالي) إلى 20 في 38 ملف `<slug>-data.js` (بينها 3 ملفات متعددة الفروع alagar/anas/wingi تُحدَّث بسطر قالب واحد يغطي كل فروعها). أُضيف مهاجرة جديدة `restaurantRate20` في `runDeliveryMigrations()` (نفس نمط `khawali40`) تفرض `ratePerKm=20` على أي نسخة `localStorage` سابقة محفوظة للـ45 معرّفاً، بلا انتظار انتهاء صلاحية التخزين المؤقت. رُفعت أرقام إصدار كل ملف من الـ38 (+1 لكل واحد) في `index.html` وما يقابله في `sw.js` (17 ملفاً منها فقط مُدرَجة أصلاً في `APP_SHELL`)، ورُفع `CACHE` مرة إضافية لضمان وصول كود المهاجرة الجديد. **حادثة تزامن حادة أثناء التنفيذ:** جلسة أخرى كانت تعمل بالتوازي على نفس `app.js`/`index.html`/`sw.js`/`store-slugs.js` (ميزة الذكاء الاصطناعي + إضافة متجر فلوكا 87، أنهت والتزمت الأولى فعلاً منتصف هذه المهمة) — عُزلت تعديلاتي بدقة عبر `git apply --cached --unidiff-zero` على ررقعات مبنية يدوياً (فرق كل ملف بصفر سياق `-U0` لتفكيك كل سطر تعديل كهنك مستقل، ثم تصفيتها بكلمات مفتاحية لاستبعاد أسطر فلوكا/store-slugs.js تحديداً) بدل `git add` كامل للملفات المشتركة؛ تحقّق نهائي عبر `git diff --cached`/`git diff` أن المرحلتين منفصلتان بدقة 100%. تحقّق شامل عبر preview محلي (منفذ منفصل): محاكاة زائر عائد بقيمة `localStorage` قديمة (15 لمتجر 17، 40 لمتجر الخوالي 31) ثم إعادة تحميل الصفحة أظهرت كلا القيمتين محدَّثتين فعلياً إلى 20 بعد المهاجرة، صفحات `/category/restaurants` وصفحة متجر الخوالي الفردية تعرضان «٢٠ ل.ت/كم» بدل «١٥»/«٤٠» بلا أي أخطاء كونسول، و`npm run check:versions` ناجح | 38 ملف `<slug>-data.js` (قائمة كاملة في هذا السطر)، `app.js` (مهاجرة جديدة فقط)، `index.html`, `sw.js`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح. **لم يُنفَّذ `git commit`/push بعد — بانتظار تعليمات المستخدم** |
| 2026-07-12 | المستخدم طلب إضافة مطعم «مطعم أسماك فلوكا» (Feluka Seafood Restaurant) الجديد ضمن قسم مطاعم، معطياً رابط قائمة إلكترونية (`feluka.minidine.com/ar/categories/?branch=feluka-seafood-restaurant`) + هاتف (+90 537 047 71 10) + Plus Code (3X9P+523 Şişli, İstanbul) | ميزة إضافة جديدة (لا يوجد خطأ سابق) — أول مصدر من نوع **minidine.com/miniandmore.co الجديد** (نطاق white-label جديد، وليس نفس نمط bitehaus/ruman المُوثَّق سابقاً بنفس العائلة) | **مصدر جديد — feluka.minidine.com (Next.js/Turbopack SPA فوق auth0.miniandmore.co):** الواجهة تُخفي القائمة خلف طلب تبادل توكن أولاً؛ استُخرج المسار من حزم JS المصغَّرة (`_next/static/chunks/*.js`، بحث عن `customerName`/`clients/token`): `POST https://auth0.miniandmore.co/clients/token` بجسم **multipart form-data** (وليس urlencoded) يحوي `customer_name` (بشرطة سفلية **وليس** شرطة — القيمة الفعلية `feluka_seafood_restaurant` مختلفة عن slug الرابط `feluka-seafood-restaurant` الذي تظهر به الصفحة، اكتُشف بالفحص الدقيق لملف JS بعد فشل أول محاولتين: أولاً "the origin url field is required" ثم "Customer not found") + `service=qrmenu` + `origin_url` (رابط الموقع نفسه) → يُعيد `{access_token, url, storage_url}`؛ ثم `GET {url}/categories` و`/menu/items` و`/contact_info` بترويسة `Authorization: Bearer <token>` و`X-Data-Lang: ar` تُعيد كل شيء (19 تصنيفاً، 80 عنصراً، 79 منها بصورة فريدة — عنصر واحد بلا صورة استُبعد). صور العناصر على S3: `{storage_url}/feluka_seafood_restaurant/items/<name>.<ext>` (بلا مصادقة، لكن حساسة لمسار خاطئ: أول محاولة بنَت الاسم بامتداد مكرَّر `<name>.<ext>.<ext>` فأعادت 403 AccessDenied لكل الملفات، صُحِّح بإعادة بناء الاسم من `image.image` مباشرة الذي يتضمّن الامتداد أصلاً). بعض العناصر (25/80) تحمل `attributes[]` وصفية (حجم/نوع) لكنها **ليست خيارات سعر بديلة** — كل عنصر بهذه الصفة له سجل واحد فقط بلا شقيق بسعر مختلف، فأُلحقت كنص وصفي بالاسم («- إكسترا»/«- كبير») بدل بناء مجموعة خيارات وهمية، مع تصفية القيم المكرَّرة أصلاً داخل الاسم (كـ«جمبري مشوي كبير - كبير») لتفادي التكرار. إحداثيات المتجر مأخوذة مباشرة من رابط iframe خرائط غوغل المُضمَّن في استجابة `/contact_info` (`embed_map_src`، 41.067719911170165 / 28.985088271163935) لا حاجة لبحث Places منفصل — طابقت منطقة الـPlus Code التي أرسلها المستخدم (شيشلي) تماماً. الشعار المُنزَّل من `feluka.minidine.com/logo.png` تبيَّن أنه **الشعار الفعلي للعلامة التجارية** (دفة سفينة + سمكة + صياد، نص "FELUKA RESTAURANT") وليس شعاراً عاماً للمنصة كما بدا مبدئياً — استُخدم مباشرة بلا حاجة لصورة بديلة. **المتجر رقم 87** (بعد التحقق من `max(id)` الفعلي في Supabase = 86 وليس أي رقم من الذاكرة أو الملف الحالي لفرع العمل، لأن متجر فلافل النور 86 كان قد دُفع لقاعدة البيانات فعلاً في جلسة سابقة رغم بقاء `git commit` معلَّقاً)، حزمة معرّفات المنتجات **1690001-1690079**. أُنشئ `feluka-data.js` (نفس نمط `alnoor-data.js`: `felukaStore` + `felukaFullCatalog` الكامل للدفع فقط + `felukaProductCatalog=[]` فارغ في المستودع للأداء) + صور محمَّلة محلياً (`assets/photos/feluka/`: شعار PNG + غلاف JPG (لقطة صينية سي فود كاملة بديكور بحري — قوارب وأصداف تطابق هوية العلامة) + 79 صورة صنف مضغوطة بـ`sharp` إلى ≤1000px JPEG q82، إجمالي ~6.6MB). وُصلت الأسلاك الأربعة الإلزامية في `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`87: "feluka-seafood-restaurant"`) + `<script src="/feluka-data.js?v=1">` في `index.html` قبل `app.js` + إدراج مماثل في `sw.js` (`APP_SHELL`). **الدفع لقاعدة البيانات:** عبر Supabase MCP `execute_sql` (privileged) — `jsonb_to_recordset` لصف المتجر وصفوف الـ79 منتجاً في معاملتين منفصلتين؛ **خطأ نسخ يدوي واحد اكتُشف وأُصلح فوراً بالتحقق البرمجي** (مقارنة كل صف بالمصدر الأصلي): معرّف صورة منتج «ورق عنب (مقلي) فردين» فُقدت منه أرقام أثناء كتابة استعلام SQL يدوياً («6111777694» بدل «6111767777694»)؛ صُحِّح بـ`UPDATE` مباشر ثم أُعيد التحقق البرمجي الكامل (79/79 صف مطابق تماماً للمصدر: الاسم، السعر، التصنيف، مسار الصورة) وتأكيد وجود كل ملفات الصور الـ79 فعلياً على القرص. **approval_status = 'pending' عمداً** (خلافاً لسطر فلافل النور أعلاه الذي اعتُمد مباشرة) — الصور لم تُنشر بعد لأن هذه الجلسة لم تُنفِّذ `git commit`/push، فاعتماد المتجر الآن كان سيُظهره على الموقع الحي بصور مكسورة (نفس التحذير الموثَّق في [[adding-a-restaurant]]: "انتظر نشر الصور قبل الاعتماد"). **حادثة تزامن أثناء التنفيذ:** جلسة أخرى كانت تعمل بالتوازي على نفس الملفات (`app.js`/`index.html`/`sw.js`/`store-slugs.js`) وأنهت والتزمت (commit) إضافة فلافل النور منتصف هذه الجلسة، مما **حذف سطر `store-slugs.js` (87) الذي كنت أضفته للتو** (استُرجع فوراً بعد ملاحظة `system-reminder` تلقائي عن تعديل خارجي على الملف) ورفعت أرقام إصدار عدة ملفات `*-data.js` أخرى في `sw.js` لا علاقة لها بهذه المهمة — لم تُلمس تلك التعديلات، واقتصر التدخل على استعادة سطري الخاص فقط. تحقّق شامل عبر preview محلي (منفذ منفصل لتفادي تعارض مع خادم الجلسة الأخرى): `/store/feluka-seafood-restaurant` يعرض العنوان الصحيح، الوصف، الشعار والغلاف (HTTP 200 لكليهما)، كل التصنيفات الـ19 بعناصرها الصحيحة (79 منتجاً)، حقل «أوقات العمل» (لا وجود لساعات حقيقية في المصدر، فاستُخدمت الصيغة القياسية «يومياً — يرجى التأكيد عبر الاتصال» المتَّبعة في متاجر أخرى بلا ساعات مصدر بدل ترك الحقل `null` الذي كان سيظهر حرفياً في الواجهة)، صفر أخطاء كونسول، و`/` و`/category/restaurants` بلا أي تأثر. رُفع الإصدار: `app.js` v284→285، `store-slugs.js` v236→237، `CACHE` v202→203 في `sw.js`، و`npm run check:versions` ناجح | `feluka-data.js` (جديد)، `assets/photos/feluka/**` (جديد، 81 ملف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 87 وصفوف `products` 1690001-1690079 في Supabase | نعم — `npm run check:versions` ناجح. **لم يُنفَّذ `git commit`/push بعد، والمتجر لا يزال `pending` (غير ظاهر على الموقع الحي) بانتظار تعليمات المستخدم للنشر** |
| 2026-07-12 | المستخدم طلب إضافة مطعم «فلافل النور» (Al Noor Falafel) الجديد ضمن قسم مطاعم، معطياً رابط قائمة إلكترونية (`menu.akillikurye.com/AlNoor-Restaurant`) + عنوان (Hacı Bayram Veli Caddesi 87B، مهترچشمة، إسنيورت) + هاتف | ميزة إضافة جديدة (لا يوجد خطأ سابق) — أول مصدر من نوع **akillikurye.com** لهذا المشروع | **مصدر جديد — akillikurye.com (Angular SPA):** الواجهة تُخفي البيانات خلف Angular، لكن REST بسيط مكشوف: base URL يُستخرج من حزمة `main-*.js` (`Cn.domin` = `https://backend.akillikurye.com/`)، ثم `GET {domin}api/menu/of/{slug}` يُعيد كل شيء دفعة واحدة (JSON خام بلا مصادقة): بيانات المتجر (`lat/lng/address/logo/banner` كمسارات نسبية تحت `{domin}storage/`) + `categories[].items[]` (كل عنصر `name.{ar,en,tr}`, `price`, `photo`, `sizes[]`, `addons[]`). لا خيارات/إضافات هنا (65 منتجاً بسيطة، صورة فريدة لكل عنصر). إحداثيات المصدر (41.0213625, 28.6648594) تطابقت شبه حرفياً (~3م فرق) مع نتيجة Google Places (`findplacefromtext` بالاسم+نطاق قريب من الإحداثيات) على "مطعم النور" (rating 4.4، 130 تقييم، ساعات عمل يومية 9ص-10م) — استُخدمت ساعات Google الحقيقية بدل تخمين نص عام. **المتجر رقم 86** (بعد التحقق من `max(id)` الفعلي في Supabase = 85 وليس أي رقم من الذاكرة)، حزمة معرّفات المنتجات **1680001-1680065** (بعد التحقق أن أعلى معرّف منتج مستخدم فعلياً هو 1670111). أُنشئ `alnoor-data.js` (نمط `rody-data.js`/`saj-data.js`: `alnoorStore` + `alnoorFullCatalog` الكامل للدفع فقط + `alnoorProductCatalog=[]` فارغ في المستودع للأداء) + صور محمَّلة محلياً (`assets/photos/alnoor/`: شعار + غلاف + 65 صورة صنف، إجمالي 4MB، كل الملفات أقل من 260KB فرداً). وُصلت الأسلاك الأربعة الإلزامية في `app.js` (`stores.push`, `products.push`, `initialDeliverySettings` spread) + `store-slugs.js` (`86: "alnoor-falafel-restaurant"`) + `<script src="/alnoor-data.js?v=1">` في `index.html` قبل `app.js`. **الدفع لقاعدة البيانات:** عبر Supabase MCP `execute_sql` (privileged) — anon-key writes محجوبة بـRLS كما هو موثَّق — `jsonb_to_recordset` لصف المتجر (مع `approval_status:'approved'` مباشرة بما أن الصور نُشرت أولاً محلياً، بالإضافة إلى `google_place_id/google_rating/google_reviews_count/google_maps_url/google_rating_updated_at` من نفس بحث Google Places أعلاه) وصفوف الـ65 منتجاً معاً بمعاملة واحدة. تحقّق شامل عبر preview محلي: `/store/alnoor-falafel-restaurant` يعرض العنوان الصحيح، الوصف، الشعار والغلاف (HTTP 200 لكليهما)، كل الأصناف الخمسة (الأطباق ١٦، الأصناف ١١، السندويشات ١٢، الأطباق الخارجية ١٦، المشروبات ١٠ = ٦٥) بالأسعار الصحيحة من Supabase (البيانات المحمَّلة محلياً فارغة عمداً، فهذا يثبت أن القراءة من القاعدة تعمل)، صفر أخطاء كونسول، و`/category/restaurants` يعرض المتجر ضمن قائمة الـ٣٩ متجراً. رُفع الإصدار: `app.js` v282→283، `store-slugs.js` v235→236، `CACHE` v200→201 في `sw.js`، و`npm run check:versions` ناجح | `alnoor-data.js` (جديد)، `assets/photos/alnoor/**` (جديد، 67 ملف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 86 وصفوف `products` 1680001-1680065 في Supabase | نعم — `npm run check:versions` ناجح. **ملاحظة تزامن:** جلسة أخرى كانت تعمل بالتوازي على نفس `app.js`/`index.html`/`sw.js` (ميزة الذكاء الاصطناعي في «اسأل دكانجي» + رفع `styles.css`)، فعُزلت تعديلاتي عبر `git apply --cached` (تصحيحات دقيقة تستهدف فقط أسطر إضافة فلافل النور) بدل `git add` الكامل للملف، حفاظاً على عمل الجلسة الأخرى غير مؤكَّد الاستقرار بعد كتعديلات غير مرحّلة (staged) منفصلة. مدفوع لـ main بموافقة المستخدم الصريحة | `alnoor-data.js` (جديد)، `assets/photos/alnoor/**` (جديد، 67 ملف)، `app.js`, `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 86 وصفوف `products` 1680001-1680065 في Supabase | نعم — `npm run check:versions` ناجح |
| 2026-07-12 | متابعة للسطر التالي أدناه: المستخدم طلب «صحّح إحداثيات متجر 17» بعد أن تبيّن في المراجعة اليدوية أنها مقرَّبة (41.004, 28.63) وتبعد ~2.4كم عن الموقع الحقيقي المؤكَّد (هاتف+عنوان مطابقان حرفياً) | **اكتشاف حرج أثناء التنفيذ:** تحديث `stores.lat/lng` في Supabase وحده **لم يظهر على الموقع إطلاقاً** — فحص `stores` العالمي في المتصفح بعد التحديث أظهر لا يزال القيم القديمة (41.004, 28.63) ومapUrl قديماً مختلفاً كلياً عمّا كُتب في القاعدة. السبب: متجر 17 من فئة المتاجر القديمة "المدموجة يدوياً" الموثَّقة في قسم 6 أعلاه (`afgan-data.js` محمَّل عبر `<script>` في `index.html` ومُدرَج في `sw.js`، ويُدمَج في `app.js` عبر `stores.push(afganStore)`) — حقلا `location`/`mapUrl` مصدرهما هذا الملف الثابت حصرياً وليس Supabase، فتحديث القاعدة وحده بلا لمس الملف كان سيبقى بلا أي أثر ظاهر إلى الأبد رغم نجاح استعلام SQL. **هذا امتداد جديد لنفس فئة الخطأ الموثَّقة في قسم 6 (ليس فقط "حذف متجر لا يكفي" بل أيضاً "تعديل حقل موقع/إحداثيات لا يكفي من Supabase وحده" لأي متجر من هذه الفئة)** | (1) صُحِّحت `stores.lat/lng` في Supabase إلى الإحداثيات المؤكَّدة (41.0192502, 28.6497621) — أُبقيت كسجل مرجعي لا كمصدر عرض فعلي لهذا المتجر تحديداً. (2) **الإصلاح الفعلي:** عُدِّل `afgan-data.js` مباشرة: `location.lat/lng` لنفس القيم المؤكَّدة، و`mapUrl` من رابط بحث نصي عام ("Afgan+Kebap+Restaurant+Beylikduzu...") إلى رابط إحداثيات دقيق (`?api=1&query=41.0192502,28.6497621`) بنفس نمط بقية المتاجر. رُفع `?v=126→127` في الموضعين (`index.html` وسطر `sw.js` ضمن `APP_SHELL`) لضمان كسر أي تخزين مؤقت قديم للملف. تحقّق كامل عبر preview محلي: `stores` العالمي في المتصفح بعد الإصلاح يحمل الإحداثيات الصحيحة الجديدة و`mapUrl` الجديد فوراً، `googleRating` (من الجلسة السابقة نفس اليوم) لا يزال سليماً 4.2، صفحة المتجر تُعرض بلا أي خطأ كونسول وبلا أي تغيّر بصري غير متوقَّع، و`npm run check:versions` ناجح | `afgan-data.js`, `index.html`, `sw.js`, `CLAUDE.md` + صف `stores` id 17 في Supabase | نعم (يدوياً بالبحث النصي؛ `check:versions` لا يتحقق من ملفات `*-data.js` الفردية، فقط app.js/styles.css) |
| 2026-07-12 | متابعة للسطر التالي أدناه: المستخدم طلب «حاول مراجعة id 17 و22 و36 و66 يدوياً» — الأربعة التي تركها السكربت الآلي بلا تقييم لأن مطابقتها الاسمية بدت مشبوهة | الأسباب الجذرية اختلفت لكل حالة (اكتُشفت بالتحقق اليدوي الفعلي، وليس تخميناً): (1) id 17 أفغان كباب — إحداثيات المتجر في القاعدة مقرَّبة لـ3 خانات عشرية فقط (41.004, 28.63) فتبعد ~2.4كم عن الموقع الحقيقي، فتجاوزت حارس الـ450م في السكربت الآلي رغم أن المكان صحيح 100%. (2) id 66 الكنافة الفلسطينية — لا سبب واضح، غالباً نفس نوع خطأ حارس المسافة أو ترجيح اسم بديل؛ تبيّن أن `stores.map_url` كان يحمل أصلاً رابط Google Maps صحيحاً 100% لم يستخدمه السكربت (يعمل بالإحداثيات فقط لا بـmap_url). (3) id 36 زيتونة باشاك شهير — يوجد مكان باسم مشابه صدفة "Zeytunaoğlu" على بعد 7 أمتار فقط من إحداثيات المتجر المخزَّنة (يفسّر كيف أنتج السكربت مطابقة قريبة incorrect)، لكنه عمل مختلف تماماً (هاتف مختلف كلياً +90 530 342 37 82 لا يطابق هاتف المتجر +90 505 567 90 00، ولا يحمل اسم "Zaitoune/زيتونة" كباقي فروع السلسلة السبعة المؤكَّدة). (4) id 22 القاضي ماركت — عنوان المتجر في القاعدة نفسه غامض أصلاً ("اسنيورت / بيليك دوزو" بلا شارع)، ولم يُعثر على أي نشاط تجاري باسم قريب من "قاضي/Kadı Market" ضمن نطاق معقول من إحداثياته — على الأرجح لا يملك صفحة Google Business أصلاً | **مراجعة يدوية فعلية لكل حالة عبر Places API مباشرة (find place/text search/place details) + خرائط Google، بمطابقة الهاتف والعنوان والإحداثيات وليس الاسم فقط:** (1) **id 17 ✅ حُدِّث** — AFGAN KEBAP مطعم رز بخاري، 4.2★ (1227 تقييم)، مؤكَّد بتطابق حرفي مزدوج: نفس رقم الهاتف بالضبط (+90 555 080 46 40) ونفس عنوان الشارع بالضبط (2066 Sk 17/A). (2) **id 66 ✅ حُدِّث** — Filistin Künefesi، 4.3★ (145 تقييم)، مؤكَّد بحلّ رابط `stores.map_url` الموجود أصلاً في القاعدة مباشرة (بدل البحث بالاسم) ثم تطابق الهاتف حرفياً (+90 530 102 11 36) والإحداثيات لسبع خانات عشرية بالضبط. (3) **id 36 ⏭ تُرك بلا تقييم عمداً** — "Zeytunaoğlu" القريب مرفوض رغم قرب الإحداثيات لأن الهاتف مختلف تماماً والاسم لا يطابق نمط تسمية السلسلة؛ لا وجود لفرع باسم "Zaitoune باشاك شهير" ضمن نتائج بحث نصي شامل لعلامة "Zaitoune" في نطاق 1.5كم. (4) **id 22 ⏭ تُرك بلا تقييم عمداً** — لا نتيجة موثوقة رغم 4 محاولات بحث مختلفة (نص عربي/إنجليزي، بحث بالهاتف، بحث قريب بكلمة مفتاحية)؛ يوافق قرار جلسة تدقيق العناوين 2026-07-10 السابقة برفض "Çağrı Market" لنفس السبب. **درس تشغيلي (بوابة صلاحيات):** مصنّف الصلاحيات حجب أول محاولتين (كتابة id 66، ثم حتى استعلام قراءة بحت لاحق) لأن طلب المستخدم الأصلي «راجع يدوياً» فُهم كطلب تحقّق وليس تفويضاً تلقائياً للكتابة؛ تم إيقاف العمل والسؤال عبر AskUserQuestion، فاختار المستخدم صراحة «طبّق 17، وأكمل التحقق من 22/36 واكتب ما يثبت بثقة عالية» — وبهذا التفويض المحدَّد استُؤنف العمل حتى الإكمال. **درس معماري:** السكربت الآلي (`scripts/update-google-ratings.js`) لا يستخدم `stores.map_url` عند توفره إطلاقاً رغم أنه أدق مصدر ممكن عند وجوده (كما ثبت مع id 66) — يستحق تحسيناً مستقبلياً: تفضيل حلّ `map_url` أولاً قبل البحث بالاسم/الإحداثيات المقرَّبة | لا شيء في كود المستودع (تحديث يدوي مباشر لصفَّي `stores` id 17 و66 عبر Supabase MCP فقط)، `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`) |
| 2026-07-12 | تعبئة تقييمات Google الحقيقية لدليل دكانجي (/dalil) كانت معلّقة منذ جلسة بناء الميزة (نفس اليوم): dry-run نجح (65/74 تطابق) لكن الكتابة الفعلية حُجبت مرتين من مصنّف الصلاحيات رغم موافقة عامة سابقة «قم بذلك» — الكتابة الجماعية على ~70 صف إنتاج حي تطلّبت تأكيداً محدداً بالاسم (موثّق في memory) | ليس خطأ برمجياً — بوابة موافقة متعمّدة فقط. المستخدم أرسل التأكيد المحدَّد بالضبط بالصيغة الموثَّقة مسبقاً في memory كمثال مطلوب: «شغّل تحديث تقييمات الـ70 متجراً باستثناء 17 و22 و36 و66» (الاستثناءات الأربعة = مطابقات Google مشبوهة اكتُشفت في dry-run: id 17 أفغان كباب⇐NAVOI، id 22 قاضي ماركت⇐Çağrı Market، id 36 زيتونة باشاك شهير⇐تصادم مع id 42، id 66 الكنافة الفلسطينية⇐حلويات سنابل) | نُفِّذ `node scripts/update-google-ratings.js --exclude=17,22,36,66` فعلياً (لا dry-run) على 74 متجراً معتمداً ناقص الأربعة المستثناة = 70. النتيجة: **61 تحديث ناجح** (HTTP OK من Supabase PATCH لكل صف) و**9 تخطٍّ آمن** (لا تطابق موثوق ضمن نصف قطر 450م فتُركت الحقول فارغة كما هو مصمَّم — بلا كتابة خاطئة: ids 12 حلويات زيتونة-بهشة ايفلر، 13 حلويات عز الدين، 15 نور الشام، 19 قاضي ماركت باشاك شهير، 24 أبو الزلف، 50 صفا الشام، 64 فلافل صاج، 84 ملحمة الدوماني، 85 قرقوشة فرفوشة) و**صفر فشل**. تحقّق حي عبر preview على `/dalil`: `stores` global في المتصفح يحمل `googleRating/googleReviewsCount/googlePlaceId/googleMapsUrl/googleRatingUpdatedAt` فعلياً وطازجة (`2026-07-12T15:22:41Z`)، وصفحة الدليل تعرض "74 نتيجة في الدليل · 61 متجراً بتقييم Google موثّق" مع ترتيب "الأعلى تقييماً" صحيح (باشا بيتزيريا 4.9، ريحان للمكسرات 4.9، أجبان بيازيد 4.9...). **بيانات فقط عبر السكربت الموجود مسبقاً — لا تعديل كود** | لا شيء في كود المستودع (Supabase فقط: أعمدة `stores.google_*` عبر السكربت الموجود)، `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`) |
| 2026-07-12 | المستخدم أرفق حزمة Next.js جاهزة (`ask-dukkanci-feature.zip`) وطلب إنشاء قسم «اسأل دكانجي» — مساعد يبني اقتراح طلب كامل من عدد الأشخاص/الميزانية/الوصف الحر. **اكتشاف حرج أثناء البدء:** المستودع كان في حالة `git merge dalil-guide` **عالقة منذ جلسة سابقة** (`MERGE_HEAD` موجود) بثلاثة ملفات تحوي علامات تعارض Git حرفية (`<<<<<<<`/`=======`/`>>>>>>>`) غير محلولة: `CLAUDE.md`، `styles.css`، `sw.js` — الأخيران لم يظهرا كـ"معدَّلين" في `git status` المختصر أول القراءة لأن الإخراج كان مقصوصاً (2K حرف)؛ التفصيل الكامل كشف `UU CLAUDE.md` و«You have unmerged paths». `styles.css` تحديداً كان يحتوي الكتلة الحية لتصميم `/category/<slug>` على الموبايل (من جلسة سابقة اليوم) **مقطوعة منتصف قاعدة CSS** (بلا قوس إغلاق) داخل علامات التعارض، تليها كتلة `/dalil` الكاملة (527 سطراً) خلف `>>>>>>> dalil-guide` — أي نشر بهذه الحالة كان سيُسقط جزءاً من كلا الميزتين بصمت (متصفحات CSS تتجاهل الأسطر المعطوبة) | تعارض الدمج نشأ لأن فرعي `main` و`dalil-guide` أضافا كتلتي CSS مختلفتين تماماً في نفس نقطة نهاية الملف (لا تعارض منطقي حقيقي، فقط تعارض نصي). لم يُكتشف سابقاً لأن جلسة `dalil-guide` وثّقت هذا بالضبط في سجلها (سطر 2026-07-12 السابق أدناه): إصلاح قوس `@media` مفقود لـfbads كان قد حُلّ تلقائياً بلا تعارض (لا تداخل مع main)، لكن كتلتي الإلحاق في ذيل الملف تداخلتا فعلاً ولم يُكمل أحد `git commit` لإنهاء الدمج | **حلّ التعارض (بلا حذف — دمج كِلا الجانبين):** استخرجت النسخة الكاملة غير المقطوعة من كل جانب مباشرة عبر `git show HEAD:styles.css` (نهاية main، ٧٥ سطراً) و`git show MERGE_HEAD:styles.css` (كتلة `/dalil` الكاملة، ٥٢٧ سطراً) وألصقتهما خلف الجزء المشترك غير المتعارض، فحصت توازن الأقواس برمجياً (عمق نهائي = صفر) قبل الاعتماد. `CLAUDE.md`: أُبقي سطرا سجل الإصلاحات كليهما (لا حذف تاريخ أبداً) بترتيب HEAD ثم dalil-guide. `sw.js` كان محلولاً فعلاً في مجلد العمل (غير مُضاف فقط) فتم قبوله كما هو. نُفِّذ `git add` للثلاثة (**لم يُنفَّذ `git commit` — الدمج لا يزال "قيد التنفيذ" عمداً**، بانتظار مراجعة المستخدم لأن الدمج يحمل تغييرات غير متعلقة بمهمة هذه الجلسة: `/dalil`, ترحيل تقييمات Google، إلخ). **الميزة:** بُنيت بالكامل داخل بنية SPA الحالية (وليس Next.js من الحزمة المرفقة — المشروع الفعلي vanilla JS): مسار `/ask-dukkanci` (`renderAskDukkanciPage`)، ومحرك اقتراحات **من طرف العميل بالكامل** (`askDukkanciBuildOptions` وما يتفرّع عنه) يعمل مباشرة على `products`/`stores` المحمَّلة أصلاً في المتصفح (`applyPublishingRules`, `isStoreApproved`, `isStoreOpenNow`, `store.subscriptionActive`) — بلا اختراع منتج/سعر/متجر إطلاقاً. المطابقة النصية تعيد استخدام `getMatchingProducts` الموجودة (فهرس مرادفات اللهجات) مع طبقة ترجيح جديدة (تكرار الكلمة عبر الكتالوج كمقياس تخصيص: كلمة نادرة كـ"مشاوي" تتفوّق على كلمة عامة كـ"مقبلات" التي تطابق عناصر عشوائية كموزة/خبز) بعد اكتشاف أن التطابق الخام كان يُرجّح أرخص عنصر بصرف النظر عن الصلة. الكمية تقديرية من `product.unit` (وزن للكيلو، تقسيم أشخاص/2 أو /3 للقطعة) موصوفة صراحة كتقدير قابل للتعديل. كل باقة **من متجر واحد إلزامياً** (لا تحايل على قيد السلة أحادي المتجر — بل تصميم يتجنّبه من الأساس)، ورسوم التوصيل حقيقية عبر `activeDeliveryQuote` عند توفر عنوان العميل، وإلا تصنيف صريح "(تقديري)". زر «أضف الباقة» يتحقق أولاً من `isProfileComplete()` قبل الحلقة (نافذة «أكمل بياناتك» الموجودة تدعم منتجاً واحداً معلّقاً فقط، فاستدعاء `addToCart` في حلقة كان سيفتحها عدة مرات ويدّعي نجاحاً كاذباً لسلة فارغة — أُصلح ليعرض تنبيهاً صادقاً بدل ذلك). تتبّع Analytics عبر `window.DUKKANCI_TRACKING.track()` الموجود بالأسماء الخمسة المطلوبة تماماً (`ask_dukkanci_opened/submitted/result_viewed/bundle_added/checkout_completed` — الأخير عبر علم جلسة `askDukkanciCartOrigin` يُقرأ عند نجاح `commitOrder` بجانب حدث `Purchase` الموجود). رابط في `desktop-nav` والفوتر (لم يُضَف لِـ`mobile-nav` — نفس نمط `/dalil` الذي اتُّبع بالفعل، الشريط السفلي محجوز عمداً عند 7 عناصر)؛ بانر CTA مدمج في الصفحة الرئيسية بعد "متاجر قريبة منك" (مرئي على الموبايل خلافاً لأقسام التسويق الأخرى المخفية هناك، لأنه مسار شراء مباشر لا نص تسويقي). **إصلاح أداء حرج أثناء الاختبار:** أول تنفيذ لـ`askDukkanciFindAddon` (البحث عن حلويات/مشروبات إضافية) كان يستدعي `getMatchingProducts` (تفحص الكتالوج الكامل ~8500 منتج) لكل كلمة مفتاح × لكل متجر مؤهّل — بلا وصف نصي وميزانية كبيرة (كل المتاجر الـ75 مؤهّلة) هذا يعني ~75×14 فحصاً كاملاً للكتالوج فتجمّد المتصفح فعلياً (>30 ثانية)؛ أُصلح بتحديد البحث لمنتجات المتجر نفسه فقط (أُصبح <500ms حتى لأقصى حالة: 100 شخص، بلا وصف، حلويات+مشروبات). تحقّق شامل عبر preview محلي: بحث "مشاوي" أعاد أطباق مشاوي حقيقية من ملحمة الهلال/نور الشام (وليس موزة/خبز عشوائيَين كما في أول تنفيذ)، تدفق كامل حي (تعبئة → إرسال → 3 بطاقات نتائج حقيقية → إضافة للسلة الحقيقية مع أسعار/كمية/توصيل صحيحة رياضياً → فتح درج السلة)، بوابة إكمال البيانات تعمل بصدق بدل الادّعاء الكاذب، صفر أخطاء كونسول على الصفحة والرئيسية و`/stores` و`/dalil` و`/category/restaurants` (تأكيد عدم كسر شيء من حل تعارض `styles.css`)، صفر انزياح أفقي على 375px، `npm run check:versions` ناجح. **لم يُختبر آلياً:** `ask_dukkanci_checkout_completed` (يتطلب تجاوز بوابة OTP واتساب الحية) — تحقّق بمراجعة الكود فقط أنه بجانب استدعاء `Purchase` الموجود مباشرة | `app.js`, `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` (ميزة اسأل دكانجي) + `styles.css`, `sw.js`, `CLAUDE.md` (حلّ تعارض دمج `dalil-guide`، **لم يُنفَّذ `git commit` بعد — راجع الأمر قبل إكمال الدمج**) | نعم — `npm run check:versions` ناجح (app.js v277، styles.css v169، CACHE v197)؛ **دمج `dalil-guide` لا يزال قيد التنفيذ محلياً، لم يُدفع، ولم يُكتمل بـcommit عمداً** |
| 2026-07-12 | المستخدم طلب إنشاء حملة إعلانات فيسبوك لمتجر قرقوشة فرفوشة (85) بنفس طريقة حملة باشا بيتزيريا السابقة (نطاق 2 ميل حول موقع المتجر، 50 ليرة/يوم، خصم 10% بمناسبة الافتتاح)، ثم طلب نشرها فعلياً، ثم طلب التأكد أن بكسل فيسبوك (Meta Pixel) مرتبط بكل الحسابات الإعلانية وربطه إن لم يكن كذلك | (1) لا توجد أداة مخصّصة لإنشاء ad set منفصلة في أدوات Marketing API المتاحة — تطلّب تمرير `adset_spec` كـJSON داخل استدعاء `ads_create_ad` مع `ad_set_id` وهمي. (2) **اكتشاف جوهري:** البكسل الحقيقي النشط فعلاً على الموقع ("Dukkanci Website Pixel"، `3926149691027789`، يستقبل أحداثاً حية) يتبع Business Manager مختلف تماماً ("Dukkanci Marketplace -New"، `1356340829738702`) عن الحسابين الإعلانيين المستخدَمين للحملات — تحقّق عبر API (`ads_get_datasets` لكل حساب) **و**يدوياً داخل Meta Business Suite (تبويب Connected assets: "You don't have any connected assets", 0 partners). محاولة مشاركته (Assign partner) مع الـBusiness الآخر رفضها فيسبوك نفسه: **"Businesses new to Facebook Products can share this asset... after several weeks"** — هذا الـBusiness أُنشئ 2026-06-26 (~أسبوعين فقط). كبديل، محاولة توليد Conversions API access token جديد للبكسل الاحتياطي المرئي فعلاً للحساب الإعلاني ("Dukkanci Pro - Pixel"، `2102305900374021`) اصطدمت بقيد صلاحية **منفصل**: "You must be an admin or developer for this business portfolio to create an access token" (حساب Chrome المسجَّل دخوله ليس admin/developer على الـBusiness المالك لتطبيق CAPI `1417611033524816`) | **الحملة:** أُنشئت حملة → ad set → إعلان كاملة عبر Marketing API على الحساب `938042394454448` (نفس حساب باشا بيتزيريا): `قرقوشة فرفوشة - AR 18-55` (OUTCOME_TRAFFIC، ميزانية على مستوى ad set/ABO)، استهداف `custom_locations` نطاق 2 ميل حول إحداثيات المتجر الفعلية في Supabase (41.015167, 28.686551 — تطابق شبه تام مع Plus Code الذي أرسله المستخدم)، الأعمار 18-55، الإعلان يعيد استخدام غلاف المتجر المستضاف فعلاً كصورة + نص "خصم 10% بمناسبة افتتاح المتجر على دكانجي" + زر ORDER_NOW → رابط `/store/karkousha-farfousha-sweets`. كل الكيانات أُنشئت PAUSED افتراضياً (لا إنفاق) ثم فُعِّلت من الأعلى للأسفل (حملة←ad set←إعلان) فقط بعد طلب المستخدم الصريح «انشر الاعلان»؛ تحقّق حي عبر `ads_get_ad_entities` أظهر توصيلاً فعلياً خلال ساعات (442 ظهور، 8 نقرات رابط، إنفاق 16.55 ليرة). **البكسل (حل مؤقت بموافقة المستخدم الصريحة «بدّل البكسل إلى Dukkanci Pro مؤقتاً»):** بدّلت `integration_settings.meta_pixel_id` من البكسل المحظور إلى `2102305900374021` (البكسل المرئي فعلاً لحساب الحملات) — يسري فوراً على المتصفح لأن `integrations.js` يقرأ هذا الجدول مباشرة عبر Supabase client بلا حاجة نشر كود. وعطّلت `meta_capi_token` (قيمة فارغة + `is_enabled=false`) بدل تركه يفشل بصمت بمفتاح لا يطابق البكسل الجديد — تتبّع الخادم (Conversions API) متوقّف مؤقتاً، تتبّع المتصفح فقط يعمل حالياً. البكسل الأصلي وتاريخ أحداثه المتراكم لم يُمسّا، وسيُعاد التبديل إليه بمجرد انتهاء فترة حظر المشاركة أو الحصول على صلاحية admin على الـBusiness الآخر | لا شيء في كود المستودع (كائنات Meta Ads عبر API + صف `integration_settings` في Supabase فقط)، `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`). **مهمة مفتوحة لجلسة قادمة:** إعادة `meta_pixel_id` للبكسل الأصلي + إعادة تفعيل `meta_capi_token` بعد حل قيد المشاركة، وتوحيد البكسلات الثلاثة المتناثرة (Dukkanci Website Pixel / Dukkanci pixels / Dukkanci Pro - Pixel) في بكسل واحد مربوط بشكل صحيح |
| 2026-07-12 | المستخدم طلب فحص باقي المتاجر بحثاً عن صور ضخمة كغلاف قرقوشة فرفوشة (2.4MB) الذي اكتُشف وأُصلح في نفس اليوم | مسح شامل (HEAD لكل رابط) لـ122 صورة فريدة من حقول `stores` الثلاثة (75 متجراً) + كل الصور المتداخلة في `site_settings` كشف **11 ملفاً >300KB داخل `assets/photos/` بالمستودع** بمجموع 6.4MB، أسوأها: غلاف ملحمة الهلال (id 5) **1969KB** بأبعاد 2400×1600 PNG فوتوغرافي، شعار مطعم رودي (id 51) 732KB بأبعاد 1207×1029 لعرض 66px، غلاف الوادي 639KB (JPEG 800×637 غير مضغوط إطلاقاً!)، وصورة بلاطة قسم الخضار على الرئيسية (`product-vegetable-basket.jpg`، من `site_settings.categories`) 608KB — **6 من الـ11 مُدرَجة في `APP_SHELL` بـ`sw.js` فتُنزَّل مسبقاً على جهاز كل زائر يثبّت الـSW** | تصغير الملفات الـ11 **في مكانها** (نفس المسار والامتداد فلا يتغيّر أي مرجع في DB أو كود أو `sw.js`) عبر `sharp`: الأغلفة ≤1280px بـmozjpeg q78 (وPNG الهلال بـpalette PNG q90)، الشعارات ≤400px palette PNG. النتيجة 6.4MB→1.6MB (توفير 4.8MB). تحقّق بصري فعلي (فتح الصور المصغّرة) أكّد عدم وجود تشوه. **قرار متعمد: لم يُرفَع `CACHE` في `sw.js`** لأن جلسة متزامنة أخرى كانت تحمل تعديلات غير محفوظة على `sw.js` نفسه لحظة العمل (خطر تداخل موثّق) — الزوار الجدد وغير مثبّتي الـSW يستفيدون فوراً، ومثبّتو الـSW الحاليون سيلتقطون النسخ الخفيفة تلقائياً مع أول رفع `CACHE` قادم من أي مهمة | 11 ملف صورة تحت `assets/photos/`, `CLAUDE.md` | لا ينطبق (لم يُعدَّل أي كود؛ رفع CACHE مؤجَّل عمداً لأول نشر قادم — انظر السبب) |
| 2026-07-12 | بعد إصلاح متجر "ملحمة الدوماني" (id 84)، طلب المستخدم أولاً التأكد أن الروابط القديمة لا تزال تعمل، ثم طلب تنظيف اسم المتجر القديم في `store-slugs.js` | `store-slugs.js` كان يحمل `84: "zamzam-market"` — اسم قديم خاطئ أُضيف في 2026-07-08 رغم أن المتجر id 84 كان بالفعل "ملحمة الدوماني" منذ إنشائه في 2026-07-05 (تحقّق من Supabase: لا وجود لأي متجر باسم "زمزم"/"Zamzam" على المنصة حالياً)، على الأرجح خطأ نسخ عند إضافة slugs للمتاجر 84/85 معاً في نفس الالتزام. `getStore()` في `app.js` يتحقق من `STORE_SLUG_TO_ID` (هذا الملف) أولاً، فكان `/store/zamzam-market` يعمل بالصدفة رغم عدم توافقه مع `store.slug` الفعلي في Supabase ("domani") | تحقّق أولاً أن كل الروابط الشرعية للمتجر (`/store/84` الرقمي، `/store/domani` الحالي، وحتى `/store/zamzam-market` القديم) تعمل قبل أي تعديل. ثم بعد طلب التنظيف الصريح: عدّل `store-slugs.js` من `84: "zamzam-market"` إلى `84: "domani"` ليطابق `stores.slug` في Supabase (تأكدت أولاً عبر بحث Supabase أن لا متجر حقيقي آخر يستخدم اسم "زمزم" فيحتاج هذا الـslug). النتيجة: `/store/84` و`/store/domani` يعملان كما هما (لم يتغيّر شيء فعلياً بالنسبة لهما لأن `store.slug` في Supabase كان يغطيهما أصلاً)، بينما `/store/zamzam-market` أصبح 404 عمداً — تحقّقت أنه لا يوجد أي رابط عام معروف/مُشارك يعتمد عليه (المتجر جديد، لا تقييمات، لا تاريخ تسويقي بهذا الاسم). تحقّق كامل عبر preview محلي (`localhost:4173`) للحالات الثلاث قبل الدفع. رُفع الإصدار: `store-slugs.js` v234→v235 (في `index.html` ومصفوفة `APP_SHELL` بـ`sw.js`)، و`CACHE` v190→v191 في `sw.js` | `store-slugs.js`, `index.html`, `sw.js`, `CLAUDE.md` | نعم (يدوياً بالبحث النصي في الملفين؛ `check:versions` لا يزال غير مربوط بـpackage.json على main) |
| 2026-07-12 | المستخدم أبلغ (بلقطة شاشة من موبايله لصفحة `/category/restaurants`) أن صفحة القسم على الموبايل مليئة بالفراغات والمساحات الفارغة وغير جذابة، وطلب إعادة تصميمها بشكل لائق | ثلاثة مصادر للفراغ على الموبايل تحديداً: (1) هيرو الصفحة (`.page-hero.compact`) ورث حواشي سطح المكتب الضخمة (42px فوق/تحت + عنوان 34px) فيصنع شاشة شبه فارغة قبل أي محتوى؛ (2) بانر "أعلن عن متجرك" (المُضاف في جلسة 2026-07-11 لملء فراغ حقيقي كان بين الهيدر والشبكة) كان على الموبايل بطاقة عمودية مكدّسة بالوسط (دائرة أيقونة 130px + عناوين كبيرة + زر) تشغل كامل الشاشة الأولى تقريباً قبل ظهور أي متجر قابل للنقر — عكس فلسفة "الشراء أولاً" المعتمدة للموبايل؛ (3) عنوان القسم كان يتكرر مرتين متتاليتين (h1 الهيرو ثم عنوان الشبكة مباشرة) | إعادة تصميم موبايل فقط (≤640px عبر media query جديد آخر `styles.css`، سطح المكتب دون أي تغيير): تصغير هيرو القسم (فئة جديدة `category-hero` كي لا تتأثر صفحات `page-hero` الأخرى؛ حواشٍ 16/12px وعنوان 26px)، إخفاء عنوان الشبكة المكرر، نقل البانر ليأتي بعد شبكة المتاجر وقبل "منتجات مختارة" عبر flex order على حاوية القسم (ترتيب DOM بقي كما هو حفاظاً على تخطيط سطح المكتب المعتمد — لُفّت الشبكة والمنتجات في `.category-stores-block`/`.category-products-block` داخل `renderCategoryPage`)، وتحويل البانر لشريط أفقي مدمج (أيقونة 56px + نص يمين + زر بعرض كامل؛ ارتفاعه أصبح ~171px بدل شاشة كاملة). تحقّق عبر preview محلي: موبايل 375px — الشاشة الأولى تعرض العنوان+العدّاد وبطاقات المتاجر مباشرة (الشبكة تبدأ عند 332px من أعلى المستند)، البانر المدمج يظهر بعد المتاجر بالشكل الجديد، صفر أخطاء كونسول وصفر انزياح أفقي (scrollWidth=375)، واختُبر قسمان فعلياً (`restaurants` بـ35 متجراً و`butcher` بمتجرين)؛ سطح المكتب بقي بالتخطيط الأصلي تماماً (بانر أعلى الصفحة بأعمدته الثلاثة + عنوان الشبكة ظاهر + هيرو 54/58px). ملاحظة بيئة: التقاط لقطات الشاشة في Browser pane كان يفشل بمهلة متكررة أول الجلسة (مشكلة معروفة من جلسات سابقة) فاعتُمد التحقق بقياسات getComputedStyle/getBoundingClientRect ثم نجحت اللقطات لاحقاً وأكّدت النتيجة بصرياً. رُفع الإصدار: `app.js` v271→v272 و`styles.css` v167→v168 (في المواضع الأربعة) و`CACHE` v189→v190 في `sw.js` | `app.js`, `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم (يدوياً بالبحث النصي؛ `check:versions` غير مربوط بـpackage.json على main) |
| 2026-07-12 | (تكملة لسطرَي اليوم أدناه، بموافقة المستخدم «نفّذ إصلاح جذر المشكلة») إغلاق جذر فئة أخطاء base64-في-القاعدة نهائياً: كل رفع صورة من التاجر/الإدارة/نموذج الانضمام كان يُخزَّن كنص base64 خام داخل صفوف `products`/`stores`/`site_settings` فيعيد إنتاج مشكلة تضخّم/تجمّد الصفحات مع كل تاجر جديد | `readImageFileResized()` في `app.js` (6 مواضع استدعاء: شعار نموذج الانضمام ×2، صورة المنتج، غلاف المتجر، صور الأقسام والمحتوى بالإدارة) تحوّل الصورة لـ`data:` URL يُرسَل كما هو داخل JSON الحفظ، وكل مسارات الكتابة الثلاثة في `api/notify-order.js` (`save-product`, `save-store`, `save-settings`) كانت تكتب القيمة للقاعدة بلا أي تحويل | **اعتراض من طرف الخادم** (قرار تصميمي: نقطة واحدة تغطي كل المسارات الحالية والمستقبلية بلا أي تعديل على `app.js` وبلا endpoint جديد مفتوح — المصادقة الموجودة أصلاً في المعالجات تبقى هي البوابة): أُضيفت لـ`api/notify-order.js` دوال `offloadInlineImage()` (فك base64 → `sharp` resize ≤1280px WebP q80 — lazy require فلا تكلفة إقلاع إلا عند وصول صورة فعلاً — → رفع لباكت `campaign-images` بمفتاح service role بنمط `api/campaign.js` → إرجاع الرابط) و`offloadRowImages()` (لحقول صف مسطّحة) و`offloadJsonImages()` (مشي تكراري لأي عمق داخل JSON الإعدادات، بسقف 20 صورة/طلب ضد الإساءة)، واستُدعيت قبل `sbWrite` في المعالجات الثلاثة: `save-product` (حقل `image`)، `save-store` (`image`, `cover_image`, `logo_image` — يغطي نموذج الانضمام لأنه يمر بنفس المسار عبر x-sb-token)، `save-settings` (كامل `value` تكرارياً — الجدول يُجلب في كل فتح صفحة فأهميته قصوى). **سياسة الفشل fail-open:** أي خطأ رفع → تبقى القيمة الأصلية (السلوك القديم) مع تحذير سجلّ، فلا يُحجب حفظ التاجر أبداً بسبب عطل تخزين. `sharp` تبعية إنتاج مثبتة تعمل فعلاً على Vercel (`api/catalog-process-image.js` يستخدمها). اختُبر كاملاً على الخادم المحلي (`server.js` :4173، admin token حقيقي، بيانات تجريبية أُنشئت ثم حُذفت مع ملفاتها المرفوعة): منتج تجريبي بصورة base64 → حُفظ برابط `store84_p9990001_image_*.webp` يُرجع HTTP 200 فعلياً؛ إعدادات بصور متداخلة (كائن + داخل مصفوفة) → كل الصور تحوّلت لروابط؛ `save-store` أثبت التحويل أيضاً (ظهر الرابط المحوَّل في رسالة خطأ NOT NULL لأن الاختبار أرسل صفاً ناقصاً عمداً — لا يحدث واقعياً لأن `toDbStore` يرسل الصف كاملاً، وupsert فشل ذرّياً فلم يمسّ متجر 84) | `api/notify-order.js`, `CLAUDE.md` | لا ينطبق (خادم فقط — لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`، لا حاجة لأي رقم إصدار). **مدموج في `main` محلياً، غير مدفوع بعد — يحتاج push ليصل للموقع الحي** |
| 2026-07-12 | (تكملة للسطر التالي أدناه بموافقة المستخدم الصريحة «نفّذ ترحيل صور base64 المتبقية») ترحيل آخر 37 صورة منتج base64 + شعار متجر 85 التي كانت تتضخّم بها استجابات جلب كل المنتجات في كل صفحات الموقع | نفس فئة الجلسة السابقة: صور رُفعت عبر لوحة التاجر/الانضمام الذاتي فخُزّنت كنص base64 خام داخل صفوف `products.image` و`stores.logo_image` — 20 صورة لصفا الشام (50)، 6 لباشا بيتزريا (56)، 11+شعار لقرقوشة فرفوشة (85)، بمجموع ~2.6MB. **اكتشاف إضافي أثناء التحقق:** غلاف متجر 85 (`cover_image`) كان مستضافاً كرابط لكن الملف نفسه 2.4MB بأبعاد 2172×724 (صورة ChatGPT رُفعت كما هي بلا ضغط) — نفس فئة مشكلة شعار الدوماني | سكربت `migrate-base64.js` (scratchpad الجلسة): جلب كل صف `image LIKE 'data:%'` بمفتاح service role، تصغير بـ`sharp` إلى ≤800px WebP جودة 80 (الشعارات 320px)، رفع لباكت `campaign-images` بأسماء `store{id}_p{pid}_{ts}.webp`، ثم PATCH للصف بالرابط القصير — 38/38 نجحت بلا أخطاء، والأحجام انخفضت ~50-60% إضافية عن base64 الأصلي بفضل WebP. غلاف متجر 85 صُغّر أيضاً إلى 1280px WebP = 100KB (من 2399KB) وحُدّث `cover_image`. تحقّق نهائي: استعلام SQL يؤكد **صفر** صفوف base64 متبقية في `products` و`stores` على مستوى المنصة كلها؛ الصفحات الحية للمتاجر الثلاثة (`karkousha-farfousha-sweets`, `safa-alsham-market`, `pasa-pizzeria-restaurant`) تعرض عناوينها وكل صورها، عينات الملفات الجديدة تُرجع HTTP 200، وصفر صور base64 في DOM أي صفحة | لا شيء في كود المستودع (Supabase فقط: Storage + `products.image` + `stores.logo_image`/`cover_image`)، `CLAUDE.md` | لا ينطبق (لم يُعدَّل أي ملف واجهة) |
| 2026-07-12 | المستخدم أبلغ أن صفحة `/store/domani` لا تزال "تتجمد قليلاً عند الفتح" رغم إصلاح base64 في جلسة 2026-07-11، وطلب فحص كل الأخطاء وإصلاحها | قياس فعلي على الموقع الحي (Resource Timing + Long Tasks): **شعار المتجر الذي رُفع في جلسة 2026-07-11 نفسها (`domani_logo_1783781744838.png`) كان 1.28MB بأبعاد 1254×1254 بينما يُعرض 66×66 فقط** — أكبر ملف في الصفحة كلها (أكبر من كل صور المنتجات الـ11 + الغلاف مجتمعة)، تنزيله ~2 ثانية وفكّ ترميز 1.57 مليون بكسل على الخيط الرئيسي يجمّد الموبايل. صور المنتجات والغلاف كانت سليمة (~100-150KB). **اكتشاف جانبي أثناء الفحص:** لا تزال 37 صورة منتج base64 (~2.6MB) في 3 متاجر أخرى (85 قرقوشة فرفوشة: 11 صورة + شعار المتجر نفسه، 50 صفا الشام: 20، 56 باشا بيتزريا: 6) تتضخّم بها استجابات جلب كل المنتجات المقسّمة صفحات (لوحظت صفحة offset=5000 تستغرق 24 ثانية) — تؤثر على التحميل الخلفي في كل الصفحات | تصغير الشعار بـ`sharp` (متوفرة في node_modules) إلى 320×320 WebP جودة 82 = **11KB (تخفيض 99.2%)**، رفعه لباكت `campaign-images` باسم `domani_logo_optimized_320.webp` بمفتاح service role من `.env` المحلي، وتحديث `stores.logo_image` للمتجر 84 عبر Supabase MCP. تحقّق حي: الصفحة تحمّل الشعار الجديد (HEAD يؤكد 11KB image/webp HTTP 200، naturalWidth 320)، 11 منتجاً ظاهرة، صفر أخطاء كونسول. **لم تُنفَّذ** ترحلة الـ37 صورة base64 للمتاجر 50/56/85 — مصنّف الصلاحيات رفضها كتوسيع نطاق خارج طلب المستخدم (سكربت جاهز في scratchpad الجلسة `migrate-base64.js`)، تحتاج موافقة صريحة من المستخدم في جلسة قادمة. **درس مستفاد:** أي رفع صورة مستقبلي لـ Supabase Storage يجب أن يمرّ بتصغير/ضغط أولاً (sharp resize ≤800px + webp) — رفع الأصل كما هو أعاد إنتاج نفس فئة مشكلة التجمد التي كان يُفترض أنه يحلّها. الجذر الأعمق لم يُعالَج بعد: `readImageFileResized` في نموذج انضمام التاجر لا يزال يخزّن base64 خاماً في الصفوف (مصدر صور متجر 85) | لا شيء في كود المستودع (Supabase فقط: Storage + `stores.logo_image`)، `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`) |
| 2026-07-12 | المستخدم طلب قسماً جديداً «دليل دكانجي» على `/dalil`: دليل متاجر بتقييمات Google الرسمية (بلا scraping) مع بحث بالاسم/المنتج/المنطقة، فلترة بالمنطقة والتصنيف و«مفتوح الآن» و«التوصيل متاح»، ترتيب بالأعلى تقييماً/الأكثر تقييمات/الأقرب، مقارنة حتى 3 متاجر على `/dalil/compare?ids=..`، بطاقة تعرض التقييم وعدد التقييمات وتاريخ التحديث ورابطَي المتجر والاتجاهات، حد أدنى 50 تقييماً (قابل للتغيير من الإعدادات) لتصدّر «الأعلى تقييماً»، وسم «مميز» بلا خلط بالترتيب العضوي، وSEO كامل | ميزة جديدة (لا يوجد سبب جذري)، لكن اكتُشف وأُصلح **خللان قائمان مسبقاً** أثناء البناء: (1) 🔴 كتلة `@media (max-width: 640px)` لأنماط fbads (سطر ~11463 في `styles.css`) **بلا قوس إغلاق منذ إنشائها**، فكانت تحبس كل ما بعدها في الملف (~490 سطراً: كل أنماط "Homepage conversion additions" — trust-bar، search-chips، product rails، whatsapp-assist...) داخل شرط ≤640px بصمت — بدا الموبايل سليماً لأن معظمها أنماط موبايل أصلاً، بينما كانت ميتة تماماً على سطح المكتب في الموقع الحي (شرائح البحث مثلاً كانت بلا تصميمها المقصود على الشاشات الواسعة رغم "إصلاح" لونها في 2026-07-11 الذي جرى التحقق منه على 375px فقط). أي CSS جديد يُلحق بنهاية الملف كان سيُبتلع في نفس الفخ — وهذا ما حدث فعلاً لأنماط الدليل قبل اكتشافه. (2) `scripts/check-versions.js` الموثّق في قسم 4 أعلاه **لم يكن متتبَّعاً في git إطلاقاً** (موجوداً في مجلد العمل فقط) — لهذا كان `npm run check:versions` "غير مربوط" في كل الجلسات السابقة | **(أ) الميزة:** أعمدة جديدة آمنة في `stores` عبر migration (`google_place_id/google_rating/google_reviews_count/google_maps_url/google_rating_updated_at` — طُبّقت على Supabase باسم `store_google_ratings_dalil`) + صف `site_settings.dalil` بقيمة `{"minGoogleReviewsForTop":50}` يقرؤه العميل مع احتياطي 50. ملف مشترك جديد `dalil-regions.js` (13 منطقة بكلمات مطابقة عربية/تركية، اشتقاق منطقة المتجر من لاحقة اسمه ثم عنوانه، نفس نمط `category-slugs.js`: `<script>` للواجهة + `require` لـSSR/sitemap). في `app.js`: راوت `dalil` + `renderDalilPage`/`renderDalilComparePage` + فلترة/ترتيب عضوي بحت (المؤهَّلون ≥الحد الأدنى يتقدمون دائماً في «الأعلى تقييماً»؛ `featured` شارة «مميز» فقط ولا يرفع الترتيب أبداً) + بحث يشمل المنتجات عبر `getMatchingProducts` الموجودة + مزامنة كاملة بين الفلاتر ورابط الصفحة (`?category=&region=&q=&open=&delivery=&sort=`) + `updateHead` يعطي عنواناً/وصفاً/canonical ديناميكياً لكل تركيبة تصنيف/منطقة وItemList JSON-LD حيّاً (يُزال خارج القسم) وnoindex لصفحة المقارنة. قشرة SSR جديدة `api/dalil.js` (نمط `api/store.js`: shell + استبدال meta + ItemList/Breadcrumb JSON-LD + روابط `/store/<slug>` داخل المصدر للزحف) مربوطة في `vercel.json` (`/dalil` و`/dalil/compare`) — محلياً تُختبر عبر `/api/dalil` مباشرة مع `SSR_SHELL_ORIGIN`. sitemap: `/dalil` + متغيرات التصنيف والمنطقة في `sitemap-pages`. سكربت `scripts/update-google-ratings.js` (Places API بنفس مفتاح `api/maps-key` الحي: Nearby+FindPlace حول إحداثيات المتجر مع **رفض أي تطابق أبعد من ~450م** — ترك الحقل فارغاً أفضل من تقييم متجر خاطئ؛ `--dry-run` و`--only=ids`؛ إعادة تشغيله لاحقاً تحدّث التقييمات عبر place_id المحفوظ مباشرة). **التعبئة الكاملة لم تُنفَّذ**: مصنّف الصلاحيات رفض تعديل 75 صفاً حياً دون إذن صريح — dry-run كامل نجح (65/74 تطابق، 9 تخطٍّ آمن، ومطابقات تحتاج مراجعة: id 22 قاضي ماركت⇐Çağrı Market مرفوضة في تدقيق 2026-07-10، id 17، 36، 66)، وزُرع متجران فقط (56 باشا بيتزريا 4.9/731، 58 برياني بالاس 4.4/3855 بقيم حقيقية من dry-run) للتحقق البصري. **(ب) الخللان:** أُغلق قوس fbads المفقود (مع تعليق شرح مكانه)، وأُدخل `scripts/check-versions.js` في git ورُبط أخيراً بـ`package.json` (`npm run check:versions` يعمل الآن فعلاً). تحقّق شامل عبر preview محلي: موبايل 375px (بطاقات وفلاتر وعربة مقارنة)، فرز فعلي بالحالتين، منع المتجر الرابع من المقارنة برسالة، إزالة متجر من المقارنة تحدّث ids والعودة للدليل تحت متجرين، «مفتوح الآن» أخفى كل المغلق، بانر «حدّد موقعي» عند «الأقرب» بلا موقع، رابط البطاقة ينقل لصفحة المتجر، سطح مكتب 885px (شبكة عمودين + شرائح البحث بألوانها المقصودة أخيراً)، `/stores` و`/category/restaurants` و`/offers` بلا أي تأثر، وSSR curl لكل المتغيرات (canonical/noindex صحيحة). ⚠ لقطات الشاشة معطّلة في بيئة الجلسة فكان التحقق نصياً/محسوباً (سابقة موثّقة في ذاكرة المشروع) | `app.js`, `styles.css`, `index.html`, `sw.js`, `vercel.json`, `package.json`, `api/dalil.js` (جديد), `dalil-regions.js` (جديد), `api/sitemap.js`, `scripts/update-google-ratings.js` (جديد), `scripts/check-versions.js` (أُدخل في git), `migrations/20260712_store_google_ratings.sql` (جديد), `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح ✅ (يعمل من package.json لأول مرة): app.js v271→272، styles.css v167→168 (في المواضع الأربعة)، CACHE v189→190، وأُضيف `dalil-regions.js?v=1` لـAPP_SHELL |
| 2026-07-11 | المستخدم أبلغ أن متجر "ملحمة الدوماني" (`/store/domani`) مكرَّر (يوجد متجر آخر بنفس الاسم) وأن صفحته "تتجمد قليلاً عند الفتح"، وطلب حذف المتجر المكرر ورفع شعار جديد (صورة "أبو ياسين" ذهبية/سوداء) لصفحة المتجر | (1) استعلام مباشر على Supabase أظهر متجرين بنفس الاسم "ملحمة الدوماني": id 84 (slug `domani`، أُنشئ 2026-07-05، 11 منتجاً) وid 86 (slug تلقائي `store-86`، أُنشئ في نفس يوم الطلب، صفر منتجات/طلبات) — كلاهما بنفس رقم هاتف التاجر في `store_credentials`، ما يؤكد أنه تسجيل مكرر لنفس التاجر وليس متجرين حقيقيين. (2) بالتدقيق في سبب التجمد: من أصل 75 متجراً معتمداً على المنصة، كان متجر id 84 هو **الوحيد** الذي يخزّن صورة الغلاف (`cover_image`) وصور الـ11 منتجاً كلها كنص base64 خام (~1.9MB إجمالياً) مباشرة داخل صفوف جدولي `stores`/`products`، بدل روابط خفيفة (~30 حرفاً) مستضافة كبقية كل متجر آخر على المنصة — هذا يحدث تلقائياً عندما يرفع التاجر صوره عبر نموذج الانضمام الذاتي (`readImageFileResized` في `app.js` يحوّل الصورة مباشرة إلى `data:` URL ويُخزَّن كما هو، بلا رفع فعلي لـ Supabase Storage)، فيضخّم حمل صفحة هذا المتجر تحديداً بشكل كبير جداً مقارنة بأي متجر آخر عند كل فتح | (1) حذف متجر id 86 بالكامل (الصف من `stores` + `store_credentials` + `store_users`) عبر Supabase MCP `execute_sql` بعد التأكد من خلوّه من منتجات/طلبات ومطابقة رقم هاتفه لـ id 84. (2) سكربت Node لمرة واحدة (محفوظ في scratchpad الجلسة، غير جزء من المستودع) يستخدم `SUPABASE_SERVICE_ROLE_KEY` من `.env` المحلي لتحويل كل base64 (الغلاف + 11 صورة منتج) إلى ملفات مرفوعة لباكت `campaign-images` في Supabase Storage (نفس النمط المستخدم فعلاً في `api/campaign.js`)، ثم تحديث الصفوف لتستخدم الروابط القصيرة الناتجة بدل base64 — أنزل ذلك حمل صفحة المتجر من ~1.9MB نص base64 إلى عشرات البايتات من الروابط، مطابقاً لبقية المتاجر. (3) رفع الشعار الجديد الذي أرفقه المستخدم في المحادثة — لم يكن متاحاً كملف عبر أي مسار مرفقات مباشر، فتم تحديده يدوياً كأحدث ملف صورة في مجلد التنزيلات المحلي (`ChatGPT Image Jul 11, 2026, 05_40_32 PM.png`) بمطابقة بصرية، ثم رفعه لنفس الباكت وتحديث `stores.logo_image`. تحقّق كامل عبر Browser pane على الموقع الحي: الصفحة تعرض 11 منتجاً بلا أخطاء JS، الروابط الجديدة (غلاف + شعار + منتجات) تُرجع HTTP 200 فعلياً، و`stores` أصبحت 75 صفاً بعد حذف المكرر | لا شيء في كود المستودع (Supabase فقط عبر REST/SQL مباشرة بمفتاح service role، بيانات حية تُطبَّق فوراً بلا نشر)، `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`/`index.html`/`sw.js`، فقط بيانات Supabase) |
| 2026-07-11 | المستخدم أرفق نسخة من تطبيق منافس (ملف "bundle" ذاتي التشغيل) وطلب أن تصبح نسخة الموبايل من دكانجي "تطبيق بيع بدون كلام" — يوجّه الزائر نحو الشراء المباشر بدل استعراض فقرات تسويقية طويلة قبل الوصول للمتاجر والمنتجات؛ ثم بعد المعاينة طلب أن تُصمَّم الفئات كبلاطات كبيرة بصور (كالمرجع)، ثم لاحظ مساحة فارغة كبيرة عند فتح صفحة أي قسم (مثال: `/category/restaurants`) وطلب ملأها ببانر "أعلن عن متجرك هنا" مع استبدال عنوان "المتاجر" الثابت باسم القسم الفعلي — لكل الأقسام | (1) الصفحة الرئيسية على الموبايل كانت تكرّس ~9 أقسام متتالية قبل أي محتوى قابل للشراء فعلياً: فقرة الهيرو الترويجية المتحركة (4 شرائح)، صف بطاقات ثقة مكرّر، قسم "اطلب بثقة" (4 بطاقات، يكرّر نفس الرسائل)، "طلبك في ثلاث خطوات" (شرح بلا فعل شراء)، بانر مساعدة واتساب، وبانر توظيف تجار — كلها "كلام" لا يقود لشراء مباشر. (2) صفحة `/category/<slug>` كانت تترك فراغاً حقيقياً (~162px) بين الهيدر وقائمة المتاجر بسبب تراكم `padding` افتراضي (82px قسم + 38px هامش عنوان) بلا أي محتوى بينهما، والعنوان كان نصاً ثابتاً "المتاجر" بدل اسم القسم. (3) عند التدقيق تبيّن أن **3 من أصل 8 أقسام حيّة فعلياً** (مكسرات وبهارات، مواد غذائية متخصصة، مطابخ منزلية) لم يكن لها `slug` مخصّص إطلاقاً في `category-slugs.js` فكانت تنقل الزائر لصفحة "كل المتاجر" العامة بدل صفحة القسم — وأن قسم "ملاحم" كان يملك `slug` لكنه **معطوب بصمت منذ إنشائه**: كان يطابق النص الثابت "ملحمة ومشاوي" بينما القسم الفعلي في `HOME_CATEGORIES`/الموقع الحي اسمه "ملاحم"، فصفحته كانت تعرض صفر متاجر دائماً رغم وجود متاجر (ملحمة الهلال) تنتمي له فعلياً؛ فلتر `/stores` العام (`getFilteredStores`) كان يتعامل مع هذا بمطابقة خاصة (`store.category.includes("ملحم")`) لم تُستنسخ أبداً في صفحة القسم المخصّصة | **(1) الصفحة الرئيسية (موبايل ≤640px فقط، سطح المكتب دون أي تغيير):** أُخفيت الأقسام الستة المذكورة أعلاه بالكامل عبر media query جديد في `styles.css`، وأُضيفت مكانها بلاطات كبيرة بصورة خلفية (`shopTile()` في `app.js`، أول بلاطة كبيرة بعرض كامل ثم البقية بشبكة عمودين) تستخدم نفس بيانات/صور الأقسام الحقيقية الموجودة أصلاً (`homeCategoriesOrdered()`) — لا صور جديدة. أُخفي أيضاً شريط الأقسام الصغير القديم منتصف الصفحة على الموبايل تفادياً للتكرار (لا يزال يظهر على سطح المكتب). **(2) صفحة `/category/<slug>`:** استُبدل عنوان "المتاجر" الثابت بـ`${catText}` (اسم القسم الفعلي)، وأُضيفت بلاطة "لأصحاب المتاجر" (بنفس مكوّن `.merchant-cta` الموجود أصلاً بالصفحة الرئيسية، بلا CSS جديد) بنص ديناميكي "أعلن عن {اسم مفرد للقسم} هنا" عبر خريطة `CATEGORY_MERCHANT_PITCH` الجديدة (تغطي كل الأقسام السبعة القابلة للتخصيص + احتياطي عام "متجرك" لأي قسم مستقبلي غير مُدرَج) تملأ الفراغ بدل تركه فارغاً. **(3) تغطية الأقسام الثمانية الحية بالكامل:** أُضيفت 3 مداخل ناقصة في `category-slugs.js` (`nuts-spices`، `specialty-food`، `home-kitchens`)، وصُحِّح خلل "ملاحم" جذرياً باستخراج منطق المطابقة المشترك في دالة جديدة `storeMatchesCategory()` يستخدمها كل من `getFilteredStores()` و`renderCategoryPage()` معاً (بدل تكراره ناقصاً في مكان واحد فقط)، وصُحِّح مفتاح `category-slugs.js`/`CATEGORY_MERCHANT_PITCH` من "ملحمة ومشاوي" إلى "ملاحم" ليطابق القيمة الفعلية. **بيئة العمل:** كل التنفيذ تم في worktree + فرع منفصلين تماماً (`mobile-buy-first-experiment`) بناءً على طلب المستخدم الصريح عدم لمس أي شيء حي حتى الموافقة، ثم أُعيد ترتيبه (`git rebase`) فوق آخر حالة لـ`main` (كانت قد تقدّمت بـ3 التزامات من جلسة متزامنة أخرى أثناء العمل) قبل الدمج. تحقّق شامل عبر preview محلي (موبايل 375px): كل الأقسام الثمانية (بالنقر الفعلي من بلاطات الصفحة الرئيسية، وليس فقط بالرابط المباشر) أظهرت بانراً وعنواناً صحيحين ومتاجر فعلية ظاهرة (بما فيها "ملاحم" التي كانت صفراً دائماً سابقاً)؛ سطح المكتب (1280px) لم يتأثر بأي من تعديلات المرحلة (1) وأظهر البانر بتخطيط 3 أعمدة الأصلي في المرحلتين (2) و(3) بشكل صحيح؛ لا أخطاء JS في أي مرحلة. رُفع الإصدار: `app.js` v270→v271، `styles.css` v166→v167، `category-slugs.js` v82→v83 (في المواضع الأربعة: `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`)، و`CACHE` v188→v189 في `sw.js` — وصُحِّح بالمناسبة انحراف قديم كان يجعل `sw.js` يخزّن `category-slugs.js?v=81` بينما الموقع يستخدم إصداراً أحدث فعلياً | `app.js`, `styles.css`, `category-slugs.js`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم (يدوياً بالبحث النصي في الملفات الخمسة؛ `check:versions` غير مربوط بـpackage.json على main) |
| 2026-07-11 | المستخدم طلب التأكد أن مشاركة رابط أي متجر (مثال: `https://www.dukkanci.com.tr/store/bab-touma-restaurant`) تُظهر صورة معاينة (كوفر) للمتجر عند اللصق في واتساب/فيسبوك/تويتر لكل المتاجر | تحقيق كامل عبر Explore agent + استعلام مباشر على جدول `stores` في Supabase (76 صفاً) أثبت أن آلية `og:image` في `api/store.js` (SSR لمسار `/store/:id`) تعمل بشكل صحيح فعلاً لكل المتاجر الـ76 (لا يوجد صف بدون `cover_image`/`image`، وكلها تُحوَّل لرابط مطلق عبر `SITE + img`) — بما فيها `bab-touma-restaurant` (id 33) تحديداً. **الثغرة الحقيقية الوحيدة المكتشفة:** وسم `<meta name="twitter:image">` (ومعه `og:image:type`) لم يكونا ضمن قائمة الاستبدال في `api/store.js`، فبقيا دائماً على قيمة أيقونة الموقع العامة الثابتة في `index.html` (512×512 PNG) حتى لو كانت `og:image` صحيحة لكل متجر — يعني معاينات تويتر/X تحديداً كانت تُظهر شعار دكانجي العام بدل صورة المتجر الفعلية، بينما واتساب/فيسبوك (اللذان يعتمدان `og:image` فقط) كانا سليمين أصلاً | أُضيف استبدال `<meta name="twitter:image">` بنفس قيمة `og:image` المطلقة للمتجر، وأُضيف حساب `og:image:type` ديناميكياً حسب امتداد ملف الصورة الفعلي (png/webp/jpeg) بدل تركه ثابتاً على `image/png`. لم تُمسّ منطق `og:image`/`og:image:secure_url` الأصلي لأنه كان سليماً بالفعل. تحقّق عبر تشغيل `server.js` محلياً (مع `SSR_SHELL_ORIGIN` لتفادي قيد https/host المحلي) وطلب `/api/store?id=bab-touma-restaurant` مباشرة: النتيجة أظهرت `og:image`, `og:image:secure_url`, `twitter:image` جميعها = `https://www.dukkanci.com.tr/assets/photos/babtoma/cover.jpg` و`og:image:type = image/jpeg` بشكل صحيح؛ تأكّد أيضاً أن هذا الرابط يُرجع HTTP 200 فعلياً على الموقع الحي. **ملاحظتان جانبيتان غير معالجتين عمداً (سلوك متوقّع وليس عطلاً):** (1) متجرا id 20 (مرفوض) وid 86 (قيد الانتظار) لا يحصلان على أي OG مخصّص أصلاً بتصميم الكود (`noindex` فقط) — هذا صحيح لأنهما غير معتمدين. (2) إن كان المستخدم لا يزال يرى معاينة قديمة/فارغة لرابط `bab-touma-restaurant` تحديداً في واتساب فالسبب الأرجح هو ذاكرة تخزين مؤقت قديمة لدى واتساب لنفس الرابط (تُحلّ بإعادة فحص الرابط عبر أداة Facebook Sharing Debugger)، وليس عطلاً في الكود | `api/store.js`, `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`، فقط `api/store.js`) |
| 2026-07-11 | بعد إصلاح شرائح البحث السريع (السطر التالي أسفله)، أكّد المستخدم أن المشكلة الأصلية كانت أيضاً (أو بالأساس) في **النص المكتوب/المتحرك داخل مربع البحث نفسه** (تأثير الكتابة التلقائية «ابحث عن بريان...» عبر `setupHeroSearchTyper()` في `app.js` الذي يحرّك خاصية `input.placeholder`) والذي يظهر أبيض غير مقروء على جهازه الفعلي، رغم أن فحص `getComputedStyle` من داخل متصفح الجلسة (Chromium بدون وضع داكن مفروض) أظهر لوناً رمادياً داكناً `#6b7280` سليماً ظاهرياً | لم تكن هناك أي قاعدة CSS تصرّح `color-scheme: light` لا في `:root` ولا عبر `<meta name="color-scheme">` — هذا يجعل متصفحات الموبايل التي تطبّق «الوضع الداكن التلقائي/القسري» على المواقع التي لا تُصرّح صراحة أنها مصمَّمة للفاتح فقط (شائع في Samsung Internet وAndroid Chrome) تعيد تلوين نص placeholder الأصلي (وأحياناً `-webkit-text-fill-color` بشكل منفصل عن `color`) بلون فاتح/أبيض حتى لو كانت خلفية العنصر نفسها لم تُعكَس (لهذا ظهر الصندوق أبيض والنص أبيض فوقه — عكس تناقض جزئي وليس انعكاساً كاملاً للصفحة) — نفس فئة الخطأ التي عولجت جزئياً في 2026-07-10 بتغيير لون placeholder الثابت لكنها لم تكن كافية لأن ذلك الإصلاح لم يتضمّن تعطيل الوضع الداكن القسري نفسه ولا `-webkit-text-fill-color` | (1) أُضيف `color-scheme: light;` داخل `:root` في `styles.css` بالإضافة إلى `<meta name="color-scheme" content="light">` في `<head>` كل من `index.html`, `customer-app.html`, `merchant-app.html` — هذا يخبر المتصفح صراحة أن الصفحة مصمَّمة للوضع الفاتح فقط فيتجاوز عن تطبيق أي انعكاس/إعادة تلوين تلقائي على عناصرها. (2) قُوِّيت قاعدتا `.hero-search input` و`.hero-search input::placeholder` في `styles.css`: اللون أصبح صريحاً `var(--ink)` (#231f20، أسود تقريباً بدل الرمادي `#6b7280` السابق) مع `!important`، وأُضيفت خاصية `-webkit-text-fill-color: var(--ink)` (تتجاوز أي محاولة WebKit/Samsung Internet لفرض لون نص مختلف عن `color` تحديداً على placeholder) و`caret-color` صريح لثبات شكل المؤشر. تحقّق عبر preview محلي (viewport موبايل 375px): فحص `getComputedStyle(input)` و`getComputedStyle(input, '::placeholder')` أظهر كلاهما `color: rgb(35,31,32)` و`webkitTextFillColor: rgb(35,31,32)` مطابقَين تماماً لـ`--ink`، و`getComputedStyle(document.documentElement).colorScheme` أعاد `"light"` مؤكِّداً تفعيل الخاصية. لم يمكن اختبار وضع Android/Samsung Internet القسري فعلياً من بيئة العمل (لا جهاز فعلي متاح)، لكن `color-scheme: light` هو الحل الموثّق رسمياً (MDN/web.dev) لتعطيل هذا النوع من الانعكاس القسري في كل المتصفحات الحديثة. رُفع الإصدار: `styles.css` v165→v166 (في المواضع الأربعة)، `CACHE` v187→v188 في `sw.js` | `styles.css`, `index.html`, `customer-app.html`, `merchant-app.html`, `sw.js`, `CLAUDE.md` | نعم (يدوياً بالبحث النصي؛ `check:versions` غير مربوط بـpackage.json على main) |
| 2026-07-11 | المستخدم أرفق لقطة شاشة من نسخة الموبايل تُظهر كلمات البحث السريع (chips: «دجاج مشوي»، «رز»، «لحوم»، «حلويات»، «خضار»، «ماركت عربي») تحت مربع البحث بلون أبيض تقريباً وغير مقروء | صنف `.search-chip` في `styles.css` كان مصمَّماً أصلاً لخلفية هيرو داكنة (`color:#fff` + خلفية شبه شفافة `rgba(255,255,255,.08)` + حدّ `rgba(255,255,255,.35)`)، لكن القسم الوحيد الذي يستخدمه فعلياً (`.hero2` في `app.js` عبر `QUICK_SEARCH_CHIPS`) له خلفية فاتحة/كريمية (`var(--cream)` + تدرّجات وردية فاتحة)، فالنص الأبيض على خلفية شبه بيضاء أصبح شبه غير مرئي | غُيِّرت ألوان `.search-chip` لتناسب خلفية `.hero2` الفاتحة: خلفية بيضاء صريحة `#fff`، نص رمادي داكن `#5b5052` (نفس لون `.h2-lead` المستخدم في نفس القسم)، وحدّ بلون العلامة التجارية الخافت `rgba(154,0,0,.16)`؛ حالة hover أصبحت خلفية وردية فاتحة `#fff1ed` ونص أحمر العلامة `#9a0000` بدل التدرّجات البيضاء شبه الشفافة السابقة. تحقّق عبر preview محلي (viewport موبايل 375px): فحص `getComputedStyle` لعنصر `.search-chip` الفعلي على الصفحة الحية أظهر `color: rgb(91,80,82)` و`background: rgb(255,255,255)` و`border: rgba(154,0,0,.16)` مطابقة تماماً للتعديل، ونص الشرائح («دجاج مشوي» إلخ) موجود وقابل للقراءة في محتوى الصفحة. لم تُستخدم `npm run check:versions` (غير مربوط بـ`package.json` على `main`، ملاحظة معروفة) — تحقّق يدوي بالبحث النصي أن `styles.css?v=165` متطابق في `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`، وأن `CACHE` أصبح `v187` في `sw.js`. رُفع الإصدار: `styles.css` v164→v165 (في المواضع الأربعة)، `CACHE` v186→v187 في `sw.js` | `styles.css`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم (يدوياً بالبحث النصي في الملفات الأربعة؛ `check:versions` غير مربوط بـpackage.json على main) |
| 2026-07-11 | بعد نشر نافذة اقتراح تثبيت التطبيق للموبايل (المهمة السابقة أعلاه)، طلب المستخدم: (1) تصغير صندوق تلك النافذة قليلاً، و(2) تصغير إشعار قبول الكوكيز (البانر السفلي) | (1) نافذة التثبيت كانت تستخدم نفس className `welcome-location-modal` الخاص بنافذة الترحيب/تحديد الموقع (440px)، فلا يمكن تصغيرها وحدها دون التأثير على تلك الأخرى. (2) بانر الكوكيز (`#dk-consent` في `tracking.js`) كان بمقاسات كبيرة نسبياً (padding 18px، عنوان 1.05rem، فقرة 0.9rem، أزرار 11×18px) لم تُصمَّم لتصغير لاحق | (1) فُصلت نافذة التثبيت لclassName مستقل `install-app-modal` (بدل مشاركة `welcome-location-modal`)، وأُضيف بلوك CSS جديد في `styles.css` يقلّص العرض من 440px إلى 360px + padding 26px + أيقونة/عنوان/نص أصغر (58px/19px/13.5px) — دون أي تأثير على نافذة الترحيب بتحديد الموقع التي بقيت 440px كما هي. (2) قُلِّصت كل مقاسات بانر الكوكيز في `tracking.js` (`injectStyles`): padding 18px→13px، فجوة العناصر 18→14px، عنوان 1.05rem→0.92rem، فقرة 0.9rem→0.8rem (line-height 1.7→1.55)، أزرار 11×18px→8×14px (border-radius 12→10px) بخط 0.82rem، لوحة الإعدادات (padding/فجوات الصفوف) و مفتاح التبديل (dk-switch) صُغِّر من 46×26 إلى 42×24 مع تعديل نقطة الإزاحة (translateX) والدائرة الداخلية من 20px إلى 18px تناسقاً. تحقّق عبر preview محلي (viewport موبايل 375px): صندوق التثبيت أصبح عرضه 345px فعلياً (من أصل 375 شاشة، مطابق لـ`min(360px, 100%-30px)`) بدل 345px+ سابقاً المشترك مع نافذة الترحيب، وبانر الكوكيز (بعد مسح كوكي `dukkanci_consent` لإجباره على الظهور) أظهر padding 13px/12px وعنوان 14.72px (0.92rem) وفقرة 12.8px (0.8rem) وأزرار padding 8px/14px — كلها مطابقة للتعديل. لا أخطاء JS في الكونسول. رُفع الإصدار: `app.js` v269→v270، `styles.css` v163→v164 (في index.html وsw.js وأيضاً merchant-app.html وcustomer-app.html اللذين يحمّلان نفس styles.css)، `tracking.js` v6→v7 في index.html فقط (غير مُدرَج أصلاً في مصفوفة APP_SHELL بـsw.js)، و`CACHE` v185→v186 في sw.js | `app.js`, `styles.css`, `tracking.js`, `index.html`, `sw.js`, `merchant-app.html`, `customer-app.html`, `CLAUDE.md` | نعم (يدوياً بالبحث النصي في الملفات الأربعة) |
| 2026-07-11 | المستخدم طلب: عند تصفّح الموقع من الموبايل، إظهار نافذة منبثقة (بوب أب) تقترح تنزيل الموقع كتطبيق لتجربة استخدام أفضل | لم تكن هناك أي مبادرة تلقائية لاقتراح التثبيت على زوار الموبايل — آلية `beforeinstallprompt`/`state.deferredInstall` وزر `data-action="install-app"` كانا موجودين مسبقاً لكن **مدفونين** فقط داخل فوتر الصفحة الرئيسية (زر يدوي يعمل فقط إن ضغطه الزائر بنفسه)، فمعظم زوار الموبايل لا يكتشفون إمكانية التثبيت إطلاقاً | أُضيفت `showInstallAppModal()` (تعيد استخدام تنسيق `.welcome-modal` الموجود مسبقاً بلا أي CSS جديد؛ عنوان "ثبّت تطبيق دكانجي" + زرّي "تثبيت التطبيق الآن" `data-action="install-app"` الموجود أصلاً و"لاحقاً" `data-action="close-modal"` الموجود أصلاً) و`initInstallPrompt()` التي تستدعيها بعد تأخير 3.5 ثانية وفق شروط: عرض الشاشة موبايل فقط (`matchMedia max-width:767px`)، التطبيق ليس مثبَّتاً أصلاً (`isStandaloneDisplay()` مطابقة لمنطق `isMerchantPwaContext` الموجود)، ليس على رابط حسّاس (admin/merchant/checkout)، ولم تُعرض من قبل على هذا الجهاز (`localStorage["dukkanci-install-prompted"]`، مرة واحدة فقط للأبد كنمط `dukkanci-welcomed` الموجود). كما تتحقق عند إطلاقها فعلياً أن لا نافذة منبثقة أخرى مفتوحة حالياً (مثل نافذة الترحيب بتحديد الموقع) فلا تتزاحم الاثنتان. استُدعيت `initInstallPrompt()` بعد `initUserLocation()` مباشرة في تسلسل الإقلاع. تحقّق كامل عبر preview محلي (viewport موبايل 375px): النافذة ظهرت بعد ~3.5 ثانية بالمحتوى الصحيح، زر "لاحقاً" أغلقها فعلياً عبر معالج النقر الحقيقي بالتفويض، إعادة تحميل الصفحة بعدها لم تُعِد إظهارها (العلَم محفوظ)، وعلى عرض سطح مكتب (885px) لم تظهر إطلاقاً ولم يُحفظ العلَم. لم تُستخدم `npm run check:versions` لأن السكربت غير مربوط بـ`package.json` على `main` حالياً (نفس الملاحظة من سجل سابق) — تحقّق يدوي بالبحث النصي أن `app.js?v=269` متطابق في index.html وsw.js وأن `CACHE` أصبح `v185` | `app.js`, `index.html`, `sw.js`, `CLAUDE.md` | نعم (يدوياً، تحقّق بالبحث النصي — `check:versions` غير مربوط بـpackage.json على main) |
| 2026-07-11 | المستخدم طلب رابط مباشر لتطبيق الزبائن ليُرسله لمن يسأل عنه — تبيّن أن تطبيق الزبون العام (خلافاً لتطبيق التاجر) لا يملك أي رابط/صفحة تنزيل مستقلة، فقط زر "تثبيت التطبيق" JS-driven مدفون داخل فوتر الصفحة الرئيسية، غير قابل للمشاركة كرابط منفصل | لم تكن هناك صفحة هبوط مخصّصة لتطبيق الزبون العام مطلقاً — فقط `manifest.json` + زر `data-action="install-app"` يعمل فقط إن كان الزائر أصلاً على الصفحة الرئيسية وتلقّى المتصفح حدث `beforeinstallprompt` | أُنشئت `customer-app.html` (نسخة مطابقة تماماً لبنية `merchant-app.html` من الجلسة السابقة، بنفس أزرار "تثبيت التطبيق على الشاشة الرئيسية" + تعليمات آيفون اليدوية) لكن **بمحتوى وقيم مختلفة**: تستخدم `manifest.json` العام الموجود مسبقاً (وليس مانيفست جديد، فالتطبيق العام ليس بحاجة هوية PWA منفصلة عن الموقع نفسه)، وزر رئيسي "تصفّح المتاجر الآن" (→ `/`) بدل رابط لوحة تحكم. أُضيف رابط `/app` في `vercel.json` و`server.js` (نفس نمط `/merchant-app`↔`merchant-app.html`). أُضيف أيضاً سطر رابط نصي قابل للمشاركة "رابط للمشاركة: dukkanci.com.tr/app" أسفل زر التثبيت الحالي في فوتر `index.html` (لم يُغيَّر سلوك الزر الأصلي `data-action="install-app"` تفادياً لأي تراجع) مع نمط CSS جديد `.footer-apps__share-link` في `styles.css`. تحقّق عبر preview محلي: `/app` يعرض الصفحة الصحيحة بعنوان ومحتوى مخصّص للزبون، الزر الرئيسي يوجّه لـ `/`، ورابط المشاركة بالفوتر يشير لـ `/app` بشكل صحيح. رُفع رقم الإصدار v163→ لـ styles.css (لم يكن هناك تعديل على app.js هذه المرة) وv183→v184 لثابت CACHE في sw.js | `customer-app.html` (جديد), `vercel.json`, `server.js`, `index.html`, `styles.css`, `sw.js`, `merchant-app.html` (تحديث مرجع رقم إصدار styles.css فقط), `CLAUDE.md` | نعم (يدوياً) |
| 2026-07-10 | تاجر ثبّت تطبيق دكانجي للتجار (PWA) على موبايله لكن واجهته عند الدخول بدت مطابقة لواجهة تطبيق الزبون العادي بدل شاشة دخول مخصّصة برقم الموبايل وكلمة المرور | `renderMerchant()` كان يتخطّى شاشة `merchantLogin()` بالكامل بمجرد وجود أي جلسة Supabase قائمة (`state.user`) — بغضّ النظر عن كونها جلسة زبون عادي بقيت محفوظة في نفس تخزين المتصفح على نفس الجهاز (localStorage/cookies مشتركة لأن تطبيق التاجر وتطبيق الزبون على نفس النطاق)، فيقفز مباشرة لمحاولة `resolveMerchantStores()` التي تفشل لجلسة زبون فعلياً وتنتهي بشاشة "جارٍ التحقق…" أو "لا يوجد متجر مرتبط بحسابك" — لا تشبه لوحة تاجر مخصّصة إطلاقاً | أُضيفت `isMerchantPwaContext()` (تتحقق من علامة `localStorage["dk-merchant-app"]` التي يضبطها `merchant-app.html` عند أي زيارة، **و**كبديل احتياطي للتطبيقات المثبَّتة قبل هذا الإصلاح: أي نافذة `display-mode: standalone`/`navigator.standalone` تفتح على `/merchant` — فلا حاجة لإعادة تثبيت التطبيق) و`hasMerchantGoogleIntent()` (علامة `localStorage` تُضبط فقط عند نقر التاجر الصريح على زر "دخول بحساب بريد إلكتروني (Google)"، ضرورية لأن `signInWithOAuth` يُعيد تحميل الصفحة بالكامل فيصفّر أي متغيّر حالة عابر). عدَّلت شرط `renderMerchant()`: داخل سياق تطبيق التاجر تحديداً (`isMerchantPwaContext()`)، إن كان وضع الدخول لا يزال الافتراضي `"admin"` ولم يختر التاجر صراحة Google/بريد إلكتروني هذه الزيارة، تُعرض شاشة `merchantLogin()` دائماً — حتى لو وُجدت جلسة زبون سابقة — بدل القفز الصامت لمحاولة الحل عبرها؛ سلوك الموقع العادي (رابط "لوحة المتجر" بالفوتر/الهيدر خارج تطبيق التاجر) **لم يتغيّر** لتفادي أي تراجع لتجار يستخدمون تسجيل الدخول بـGoogle/البريد بشكل معتاد. كما غُيِّر عنوان شاشة الدخول بوضع `"admin"` من "دخول إدارة دكانجي" إلى "أهلاً بكم في شاشة التحكم بالمتاجر" بناءً على طلب المستخدم الصريح للنص. تحقّق عبر preview: محاكاة `state.user` (جلسة زبون وهمية) + علامة `dk-merchant-app` مضبوطة → عرضت شاشة الترحيب برقم الموبايل/كلمة المرور بدل القفز للحل التلقائي (مطابقة). نفس المحاكاة بدون العلامة (سياق الموقع العادي) → السلوك القديم بقي كما هو (تراجع آمن). محاكاة `display-mode: standalone` بدون علامة `localStorage` → `isMerchantPwaContext()` أعادت `true` (يُصلح التطبيق المثبَّت مسبقاً فوراً دون إعادة تثبيت). رُفع رقم الإصدار v267→v268 لـ app.js وv182→v183 لثابت CACHE في sw.js (سكربت `check:versions` غير موجود ضمن `package.json` على هذا الفرع فتحقّقت يدوياً من تطابق الأرقام الثلاثة) | `app.js`, `merchant-app.html`, `index.html`, `sw.js`, `CLAUDE.md` | نعم (يدوياً) — **مدفوع لفرع قصير `merchant-app-login-fix` ودُمج فوراً في `main` عبر fast-forward بناءً على طلب المستخدم الصريح «ارفع»**، حي على الموقع |
| 2026-07-10 | طلب المستخدم تطبيق موبايل مخصص لأصحاب المتاجر فقط (بدون ميزات الزبون/الإدارة العليا) لتسهيل متابعة الطلبات بصوت تنبيه وإدارة المنتجات من الموبايل بدون كمبيوتر، مع رابط تنزيل يُشارَك عبر فوتر الموقع | فحصنا أولاً غلاف Capacitor العام الموجود مسبقاً (`android/`, `ios/`, `capacitor.config.json`) فتبيّن أنه غير مخصص للتاجر (يفتح كامل الموقع من `#home`) وإشعاراته الأصلية (FCM/APNs) غير مفعّلة ولا يمكن بناء .apk/.ipa فعلي من هذه البيئة (لا Android SDK ولا Mac). بالمقابل، لوحة التاجر (`renderMerchant`) ونظام Web Push (VAPID) وصوت التنبيه المولَّد بـ Web Audio (`merchantOrderTick`/`newOrderChime`) **موجودة ومفعّلة بالكامل فعلاً** ولا تحتاج بناء من الصفر — طلب المستخدم اختيار مسار PWA (تطبيق ويب مثبَّت) بدل التطبيق الأصلي عبر متاجر التطبيقات | أُنشئ `manifest-merchant.json` (هوية PWA مستقلة: `start_url:"/merchant"`, اسم "دكانجي للتجار") وصفحة تنزيل خفيفة `merchant-app.html` (تصميم مطابق لبطاقة `merchant-auth__card` الموجودة، بدون تحميل `app.js` أو حزم بيانات المتاجر لسرعة أعلى) تعرض مزايا التطبيق وزر "فتح لوحة التاجر" (→ `/merchant`، يدخل مباشرة لشاشة تسجيل دخول التاجر بفضل `dashboard-view` الذي يخفي هيدر/فوتر/تنقل الزبون تلقائياً) وزر تثبيت يلتقط `beforeinstallprompt` مع تعليمات يدوية لآيفون (Safari لا يدعم التثبيت التلقائي). أُضيف رابط `/merchant-app` في `vercel.json` (rewrite بنفس نمط `/merchants`↔`merchants.html`) **و**`server.js` (جدول `cleanUrls` المحلي كان ناقصاً هذا المسار فكان يُعيد `index.html` بدل الصفحة الجديدة محلياً — لوحظ ذلك أثناء الاختبار عبر preview وأُصلح). أُضيف رابط "تطبيق دكانجي للتجار" في قسم "تطبيقاتنا" بفوتر `index.html` بجانب زر تثبيت تطبيق الزبون الموجود مسبقاً. **حادثة أثناء التنفيذ:** منتصف الجلسة تغيّر الفرع المفعّل من `supermarket-baseline-categories` إلى `main` بفعل عملية خارجية (جلسة Claude أخرى تعمل بالتوازي على نفس المستودع على الأرجح)، مما تسبب بقراءة/تعديل عن طريق الخطأ لنسخة `index.html`/`vercel.json` الأقدم على `main`؛ تم اكتشاف ذلك عبر `git status`/`git reflog` قبل أي ضرر (لا فقدان commits، والتعديل غير المحفوظ حُفظ تلقائياً في `git stash`)، والعودة لـ `supermarket-baseline-categories` وإعادة التنفيذ هناك بشكل صحيح. تحقّق كامل عبر preview محلي: `/merchant-app` يعرض الصفحة الصحيحة، `beforeinstallprompt` يُفعِّل زر التثبيت تلقائياً، النقر على "فتح لوحة التاجر" ينقل مباشرة لشاشة "دخول إدارة دكانجي" بدون أي واجهة زبون، ورابط الفوتر يظهر بشكل صحيح | `manifest-merchant.json` (جديد), `merchant-app.html` (جديد), `vercel.json`, `server.js`, `index.html`, `CLAUDE.md` | لا ينطبق (لم يُعدَّل `app.js`/`styles.css`) — **مدفوع (7b38af0) لفرع `supermarket-baseline-categories` البعيد** بناءً على طلب المستخدم «اعمل commit وادفعها» (الفرع البعيد كان محذوفاً فأُعيد إنشاؤه بالدفع) — **لم يُدمج بعد في `main`**، فلن يظهر على الموقع الحي حتى يُدمج الفرع |
| 2026-07-10 | المستخدم طلب إضافة حقل «تاريخ الطلب» و«ساعة الطلب» في جدول الطلبات بلوحة التاجر ولوحة إدارة دكانجي العليا (أرفق لقطة شاشة من «إدارة الطلبات» في لوحة التاجر تُظهر عمود «الوقت» يعرض «الآن» لطلب قديم) | عمود «الوقت» في `renderOrdersTable()` (المشتركة بين لوحتي التاجر والإدارة عبر `context`) كان يعرض حقل `order.time` الذي يُخزَّن كنص ثابت `"الآن"` وقت إنشاء الطلب فقط (`app.js` عند بناء `newOrder`) ولا يتحدّث أبداً — بينما التاريخ/الوقت الحقيقي (`order.createdAt`، مبني من `dd.createdAt \|\| r.created_at`) كان موجوداً في البيانات ولم يُستخدم في الجدول إطلاقاً | أُضيفت دالتا تنسيق `formatOrderDateOnly(iso)` و`formatOrderTimeOnly(iso)` (بجانب `formatOrderDate` الموجودة مسبقاً) في `app.js`، واستُبدل عمود «الوقت» الوحيد في `renderOrdersTable()` بعمودين «تاريخ الطلب» و«ساعة الطلب» مبنيين من `order.createdAt` (مع fallback لـ `order.time`/"—" عند غياب `createdAt`). كما حُدِّث `exportCsv("orders")` ليضيف نفس العمودين لملف تصدير الطلبات. تحقّق بصري عبر preview: تسجيل دخول admin محلي (`localhost:4173/#admin`، كلمة مرور dev) وفتح تبويب «الطلبات» — نفس الطلب من لقطة شاشة المستخدم (`DK-644234315`, Hamada hilal) ظهر بعمودين «١٠ يوليو ٢٠٢٦» و«٠٣:٤٣ ص» بدل «الآن» الثابتة. لوحة التاجر تستخدم نفس الدالة `renderOrdersTable()` فتستفيد من الإصلاح تلقائياً دون تعديل إضافي. رُفع رقم الإصدار v266→v267 لـ app.js وv181→v182 لثابت CACHE في sw.js، وتشغيل `npm run check:versions` (نجح ✅). دُفع (41856ae5) لفرع `supermarket-baseline-categories` البعيد بناءً على طلب المستخدم «commit and push it» — **لم يُدمج بعد في `main`**، فلن يظهر على الموقع الحي (Vercel ينشر من `main` فقط) حتى يُدمج الفرع | `app.js`, `index.html`, `sw.js`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح؛ **مدفوع لفرع `supermarket-baseline-categories`، غير مدموج بـ main بعد** |
| 2026-07-10 | تاجر أبلغ (عبر لقطة شاشة من لوحة متجره — صفحة «بيانات الحساب البنكي/طرق الدفع/التوصيل التلقائي») أن حفظ التعديلات يفشل برسالة «تعذّر حفظ بيانات المتجر، تحقّق من الاتصال» رغم أن اتصاله بالإنترنت سليم | جلسة التاجر (`x-merchant-token`، تسجيل دخول برقم هاتف+كلمة مرور) تنتهي صلاحيتها بعد 12 ساعة من طرف الخادم (`signMerchantToken` في `api/notify-order.js`)، لكن الواجهة الأمامية لم تكن تتحقق من هذا إطلاقاً — فقط من غياب التوكن كلياً (جلسات قديمة قبل نظام التوكن). فبعد 12 ساعة يبقى التاجر «مسجّلاً دخول» ظاهرياً بينما كل طلب حفظ يفشل بـ403 صامتاً، وتظهر رسالة عامة مضلِّلة تلوم الاتصال بدل تسجيل الدخول من جديد. تأكدنا عبر Vercel MCP (`get_runtime_logs`) من نمط 403 متكرر على `/api/notify-order` كل ~15 ثانية (استطلاع الطلبات الدوري `_merchantOrderWatch`) بالتزامن مع محاولات POST متقطعة (محاولات الحفظ الفعلية للتاجر) في نفس الفترة الزمنية التي أبلغ فيها التاجر عن المشكلة | أضيفت `handleMerchantSessionExpired()` في `app.js` تُفرّغ `state.merchantPwAuth` وتُعيد التاجر لشاشة الدخول برسالة صريحة «انتهت صلاحية الجلسة، الرجاء تسجيل الدخول من جديد» بدل الصمت. `pushStoreCloud()` غُيِّر عائدها من boolean إلى `{ok, status, error}` (مطابقةً لنمط `pushProductCloud` الموجود مسبقاً) وتستدعي هذا المعالج تلقائياً عند 401/403 مع توكن تاجر؛ نفس المعالج أُضيف في مسار التاجر داخل `loadOrdersFromSupabase()` (الاستطلاع الدوري) ليكتشف انتهاء الجلسة خلال ثوانٍ بدل أن تبقى صامتة لساعات/أيام. حُدِّثت نقطتا استدعاء `pushStoreCloud` الأخرى (نموذج بيانات المتجر، وتبديل «مميّز» من لوحة الإدارة) لتتعامل مع الشكل الجديد للعائد. اختُبر عبر محاكاة توكن تاجر مزوَّر في المتصفح المحلي — تأكّدنا أن `pushStoreCloud` أعادت `{ok:false,status:403}` وأن الواجهة قفزت تلقائياً لشاشة تسجيل الدخول دون أخطاء JS. رُفع رقم الإصدار v265→v266 لـ app.js وv180→v181 لثابت CACHE في sw.js، وتشغيل `npm run check:versions` (نجح ✅). دُفع مباشرة لـ main (578b7cfe) بناءً على طلب المستخدم الصريح «ارفع هذا الإصلاح فقط لـ main» — استُثنيت عمداً كل التعديلات الأخرى غير المرتبطة الموجودة على نفس الفرع وقتها (package.json/package-lock.json من `npm install` غير مرتبط، وملفات/مجلدات غير متتبَّعة مثل `.agents/`, `android/`, `ios/`, `catalog/`, `scripts/`, ملفات docx/md بحثية) فلم تُدفع معها | `app.js`, `index.html`, `sw.js`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح؛ **مدفوع لـ main** (578b7cfe، فوق 1680e396 حذف حلاب 1881 الذي كان الالتزام الوحيد السابق غير المدفوع على نفس الفرع) |
| 2026-07-10 | متجر حلاب 1881 (id 49، مُغلَق) كان لا يزال يظهر فعلياً على الموقع رغم حذف صفه من Supabase في تدقيق العناوين السابق (نفس اليوم) | حذف صف Supabase وحده لا يكفي لهذا المتجر تحديداً: عكس أغلب `*-data.js` "الميتة" في قسم 2، ملف `hallab-data.js` كان **مُحمَّلاً فعلياً** عبر `<script>` في `index.html` ومُدرَجاً في `sw.js` (APP_SHELL)، ومُدمَجاً يدوياً في `app.js` عبر `stores.push(hallabStore, ...)`, `products.push(...hallabProducts)`, `...hallabDeliverySettings` — تحقّقت أيضاً أن Supabase (كل الجداول: stores/products/orders/store_credentials/store_reviews/store_staff/store_users/merchant_notifications/whop_subscriptions/site_settings) خالٍ تماماً من أي أثر لـ id 49 أو "حلاب" أو "1881" | حذف `hallab-data.js` وكامل مجلد `assets/photos/hallab/` (240 ملف) من git، وإزالة الأسطر الثلاثة المرتبطة به من `app.js`، وإزالة وسم `<script>` من `index.html`، وإزالة الإدخال من مصفوفة `APP_SHELL` في `sw.js`؛ رفع رقم الإصدار (v264→v265 لـ app.js، وv179→v180 لثابت CACHE في sw.js) وتشغيل `npm run check:versions` (نجح ✅). أُضيف بند تحذيري جديد في قسم 6 لتفادي تكرار هذا النمط مستقبلاً مع أي متجر مُدمَج يدوياً بنفس الطريقة. **ملاحظة:** نُسخة عمل منفصلة `.worktree-fbads-2/` لا تزال تحتوي على نفس الملفات القديمة لحلاب 1881 — لم تُلمس لأنها متعلقة بعمل fbads منفصل غير مدموج | `app.js`, `index.html`, `sw.js`, حذف `hallab-data.js` و`assets/photos/hallab/**`, `CLAUDE.md` | نعم — `npm run check:versions` ناجح |
| 2026-07-10 | مزامنة حقل `hours` لكل متاجر دكانجي (76 متجراً نشطاً) مع ساعات العمل الفعلية المسجَّلة في غوغل مابس، بدل نصوص عامة/تخمينية | لا يوجد Google Places API key محلي متاح مباشرة؛ `api/maps-key.js` يُخفي المفتاح خلف endpoint حي فقط | جلب مفتاح Maps الحي عبر `https://www.dukkanci.com.tr/api/maps-key`، ثم مطابقة كل متجر ببحث Places (place_id مباشر من `map_url` عند توفره، وإلا Nearby/Text Search بالاسم اللاتيني + الإحداثيات)، وجلب `opening_hours.weekday_text` عبر Place Details، وتحويلها لعبارة عربية واحدة موحّدة الأسلوب، وتحديث `stores.hours` عبر Supabase MCP لـ 72 متجراً. **4 متاجر استُثنيت عمداً (لم تُعدَّل ساعاتها):** id 12 (حلويات زيتونة - بهشة ايفلر: لا يوجد فرع مطابق قريب، أقرب نتيجة "Zaitoune Factory" تبعد 5.1 كم)، id 22 (القاضي ماركت - اسنيورت: أقرب نتيجة "Çağrı Market Beylikdüzü" اسم مختلف تماماً، رُفضت)، id 30 (الأغر شاورما - شيرين ايفلر: الفرع مطابق لكنه بلا ساعات عمل منشورة على غوغل)، id 85 (قرقوشة فرفوشة: لا يوجد `map_url` أصلاً — مطبخ منزلي). **ملاحظة شكّ:** id 51 (مطعم رودي) ساعات غوغل الرسمية معكوسة منطقياً ("12:00 ص – 12:00 م" أي منتصف الليل حتى الظهر لمطعم) — رُحّلت كما هي بالحرف تنفيذاً لأمر "مطابقة 100%"، لكنها تستحق تنبيه صاحب المتجر لتصحيحها في غوغل مابس إن كانت خطأ إدخال. سكربتات المطابقة (Node + Places API) محفوظة في scratchpad الجلسة وليست جزءاً من المستودع | لا شيء في الكود (تعديل بيانات Supabase فقط عبر `execute_sql`)، `CLAUDE.md` | لا ينطبق (لا تعديل على app.js/styles.css) |
| 2026-07-10 | نص البحث المتحرك (hero-search) في نسخة الموبايل يظهر بلون فاتح شبه أبيض وغير مقروء (لاحظه المستخدم عبر لقطة شاشة من Samsung Internet) | `.hero-search input::placeholder` كان بلون `#9ca49f` فاتح جداً بدون `::-webkit-input-placeholder` أو `opacity:1` صريحة، فبعض متصفحات الموبايل تعرضه شبه أبيض | تغميق اللون إلى `#6b7280` + إضافة selector مخصص لـ webkit + `opacity:1`؛ بالمناسبة رُفع رقم إصدار styles.css إلى v=162 وصُحّح انحراف app.js القديم (v176 في sw.js مقابل v264 في index.html، المذكور في قسم 4) إلى v264 في الموضعين | `styles.css`, `index.html`, `sw.js` | نعم — تحقّق `npm run check:versions` ناجح؛ تم الدفع مباشرة لـ main (11148cd0) بناءً على طلب المستخدم الصريح «ارفع» |
| 2026-07-10 | فحص 15 نسخة عمل (worktree) و52 فرعاً محلياً و45 فرعاً بعيداً لتحديد الآمن للحذف | تراكم فروع/نُسخ عمل قديمة دون تنظيف بعد كل مهمة (راجع قسم 3) | تأكيد أن كل الـ15 نسخة عمل مفقودة فعلياً من القرص (`gitdir file points to non-existent location`) فالحذف آمن؛ تصنيف 11 فرعاً محلياً و10 فروع بعيدة كمدموجة فعلاً في main (آمنة للحذف)؛ توثيق 39 فرعاً غير مدموج يحتاج مراجعة يدوية (قائمة كاملة في `scripts/git-cleanup.sh`) بدل حذفها عشوائياً. **ملاحظة مهمة:** محاولة تنفيذ الحذف الفعلي من بيئة فحص عن بُعد فشلت بخطأ صلاحيات (`Operation not permitted`) عند لمس `.git/worktrees/*`؛ كما لوحظ أن HEAD لفرع العمل الحالي تغيّر من `ea3d2117` إلى `bef602a1` أثناء الفحص نفسه — أي أن عملاً حيّاً كان يُكتب على نفس الفرع في تلك اللحظة. **لذلك: نفّذ `scripts/git-cleanup.sh` من جهازك مباشرة (طرفية محلية)، وليس عبر جلسة عن بُعد، وتأكد أولاً أن لا أحد يعمل على الفرع في تلك اللحظة.** | لا شيء بعد (السكربت لم يُنفَّذ محلياً بعد) | `CLAUDE.md`, `scripts/git-cleanup.sh` |
| 2026-07-10 | دراسة أولية شاملة لأسباب تكرار الأخطاء وضياع الإصلاحات | تشتت فروع/نُسخ Git، تعدد مواضع رقم الإصدار، غياب ذاكرة مشروع ثابتة، ملف app.js أحادي ضخم، مصدرا بيانات متعارضان، غياب .gitattributes، غياب اختبارات | إنشاء هذا الملف (CLAUDE.md) + `.gitattributes` + سكربت `scripts/check-versions.js` وربطه بـ `npm run check:versions` | `CLAUDE.md`, `.gitattributes`, `scripts/check-versions.js`, `package.json` | لا ينطبق (لا تعديل على app.js/styles.css) |

---
> Source: [dukkancitr-ctrl/dukkanci](https://github.com/dukkancitr-ctrl/dukkanci) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-08-12 -->
