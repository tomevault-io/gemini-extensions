## lamprey-harness

> Electron desktop **single-agent coding harness** (React 19, TypeScript, electron-vite) mirroring Codex in the Opus 4.5 era. Routes per-model to **thirty-two built-in providers** — frontier labs, public hosts and aggregators, regional specialists, local runtimes, and self-hosted gateways — plus unlimited user-defined **custom OpenAI-compatible endpoints** (`settings.json.customProviders`). The built-ins include DeepSeek, Google AI, DashScope, OpenRouter, Zhipu, OpenAI, Anthropic, xAI, Mistral, Moonshot, Groq, Together, Fireworks, Cerebras, Hugging Face, AIHubMix, FreeLLMAPI, Cohere, MiniMax, NVIDIA, GitHub Models, SambaNova, SiliconFlow, Reka, SEA-LION, DeepInfra, Hyperbolic, Perplexity, Sarvam, Inception, and keyless Ollama + LM Studio. The model can fan out via the `multi_agent_run` tool (Task-tool analog); the always-on Planner→Coder→Reviewer pipeline was excised in the Unburdening Phase (v0.14.0).

# Lamprey Harness — Codex Instructions

## What This Is
Electron desktop **single-agent coding harness** (React 19, TypeScript, electron-vite) mirroring Codex in the Opus 4.5 era. Routes per-model to **thirty-two built-in providers** — frontier labs, public hosts and aggregators, regional specialists, local runtimes, and self-hosted gateways — plus unlimited user-defined **custom OpenAI-compatible endpoints** (`settings.json.customProviders`). The built-ins include DeepSeek, Google AI, DashScope, OpenRouter, Zhipu, OpenAI, Anthropic, xAI, Mistral, Moonshot, Groq, Together, Fireworks, Cerebras, Hugging Face, AIHubMix, FreeLLMAPI, Cohere, MiniMax, NVIDIA, GitHub Models, SambaNova, SiliconFlow, Reka, SEA-LION, DeepInfra, Hyperbolic, Perplexity, Sarvam, Inception, and keyless Ollama + LM Studio. The model can fan out via the `multi_agent_run` tool (Task-tool analog); the always-on Planner→Coder→Reviewer pipeline was excised in the Unburdening Phase (v0.14.0).

## Architecture quick-pointers
- Provider registry + dispatch: `electron/services/providers/registry.ts` — `MODEL_CATALOG`, `chatStream`, `chatOnce`, `validateProviderKey`, `RETIRED_MODEL_MAP`. Adding a model = append to `MODEL_CATALOG`; Custom Models from settings.json are consulted by `resolveModel` (JM-11).
- Chat turn: `electron/ipc/chat.ts` — `chat:send` → `runHeadlessTurn` → `runChatRound` (also the seam loops/wake-ups run turns through). System prompts in `electron/services/system-prompt-builder.ts` (`AGENT_ROLE_PROMPTS`/`buildAgentSystemPrompt` survive for the `multi_agent_run` tool only).
- Multi-provider keychain: `electron/services/keychain.ts` keyed by provider id — any of the 32 built-ins or a custom endpoint id (+ `web_search:*` namespace for search providers). Provider-id authority is the registry (`resolveProviderDescriptor` / `isKnownProvider` / `listAllProviders`); the main + renderer `ProviderId` unions are parity-locked by `provider-parity.test.ts`. IPC: `settings:saveProviderKey` / `:test` / `:delete` / `:list`. Writes are atomic (temp+rename) via `electron/services/atomic-json.ts` (JM-13).
- UI surfaces: `src/components/settings/ApiKeySettings.tsx` (multi-provider list), `chat/ChatInput.tsx`, right-panel pills in `src/components/artifacts/RightPanelHome.tsx`.

## Current State
- **Direct-provider routing hotfix:** released 2026-07-19 as **v0.27.1**. All 70 pinned models route through their named provider and independently stored key; `MODEL_CATALOG` contains zero OpenRouter-brokered aliases. OpenRouter remains an explicit opt-in provider for live catalog imports only. Settings key saves synchronize the chat key-state store, the model menu refreshes keys whenever it opens, and a Moonshot key unlocks direct Kimi K3 without restart. The model menu is viewport-bounded and scrollable with the active entry revealed. Retired broker aliases map to direct equivalents; unsupported stale defaults fall back to a known catalog model.
- **Provider Discovery Expansion v2 (PX2-0–PX2-9):** complete (2026-07-19, **v0.27.0**) — 32 built-in providers and 79 pinned chat models. Adds API-key acceptance for AIHubMix, FreeLLMAPI, Cohere, MiniMax, NVIDIA, GitHub Models, SambaNova, SiliconFlow, Reka, SEA-LION, DeepInfra, Hyperbolic, Perplexity, Sarvam, and Inception Labs; refreshes current Kimi, Grok 4.20, and direct Gemma 4 rosters; adds descriptor-owned catalog normalization, filtered collision-safe bulk import, an editable FreeLLMAPI address, and MiniMax reasoning preservation. Imported capabilities default off until proven. Live authentication remains pending for providers whose keys were not available during the release. See `PLANNING/LAMPREY_PROVIDER_DISCOVERY_EXPANSION_PLAN.md` and `PLANNING/PX2_BASELINE.md`.
- **Prompts 1–20 + 21 + multi-provider revision**: complete (see `memory/project_build_status.md` for per-prompt commit SHAs).
- **RAG add-on (R1–R14)**: complete, audited, hardened (see DEVLOG 2026-06-03 audit entry).
- **Parity Phase (36 prompts + Integration H1–H6)**: complete — see `PLANNING/LAMPREY_PARITY_PLAN.md` and the H1–H6 wrap-up entry in `DEVLOG.md` (2026-06-04).
- **Fluidity Phase (J1–J11)**: complete (2026-06-04) — micro-interaction parity with Codex. Merged to `main` as commit `2691730`. See `PLANNING/LAMPREY_FLUIDITY_PLAN.md` and the per-prompt + phase-complete entries in `DEVLOG.md`. Eleven prompts shipped on `feat/fluidity-phase`: ESC + ↑ history, Shift+Tab mode cycle, @file mention, # memory shortcut, inline approval chips, tool-card auto-collapse, inline subagents, status-line context%, notification consolidation, path:line autolinking, right-panel default-collapsed.
- **Deep Research Phase (D1–D12)**: complete (2026-06-05) — first-class research pipeline with auto-trigger, multi-source corroboration, strict-citation markdown reports + downloadable `.md` artifacts. See `PLANNING/LAMPREY_DEEP_RESEARCH_PLAN.md`. Twelve prompts shipped on `feat/deep-research-phase`: DuckDuckGo adapter, provider cascade, intent classifier + `/research` slash + `--no-research` opt-out, query planner, source collector (dedup + domain cap + trust rank), readable-text extractor (`node-html-parser`), claim extraction, multi-source corroboration (RAG embeddings + opposition LLM), strict-citation synthesizer (fabricated-ref guard), orchestrator + IPC + progress events, artifact emission + `ResearchArtifact` UI, `DeepResearchBanner` with live counts + cancel.
- **Snip Phase (K1–K14)**: complete (2026-06-05, v0.4.0) — RTK-style in-process token filter on top of `shell_command`. 120 bundled YAML filters across 15 categories under `resources/snip-filters/`. See `PLANNING/LAMPREY_SNIP_PLAN.md`.
- **Customize Phase (C1–C12)**: complete (2026-06-05, v0.5.0) — first-class Skills / Connectors / Plugins surface in the left sidebar, mirroring Codex's Customize panel. Twelve prompts on `Codex/determined-pasteur-033123` (worktree): C1 surface scaffolding, C2 Skills column promotion, C3 skill format upgrade (allowedTools/model/autoInvoke + directory-mode), C4 New-skill wizard, C5 Connectors column promotion, C6 Add-connector flow (curated catalog + JSON paste), C7 plugin manifest + loader (green field), C8 plugin IPC + Zustand store, C9 Plugins column UI + 3 bundled starter plugins, C10 install flow (directory + manifest paste + bundled catalog), C11 runtime hookup (skill / slash-command / MCP-connector contribution from enabled plugins), C12 polish + version bump + push. The `'skills'` + `'mcp'` Settings tabs were retired in favor of the unified Customize surface. See `PLANNING/LAMPREY_CUSTOMIZE_PLAN.md`.
- **Panels Phase (P1–P10)**: complete (2026-06-05, v0.6.0) — Codex-style panel surface system. Two rounded sidebar panels (left + right) float on a warm two-tone substrate (`--app-bg`); the chat column between them is transparent, with content flowing directly on the substrate. Only the bottom dock pill cluster (prompt input pill + adjacent chips + `FloatingEnvironmentCard`) carries chrome inside the chat column. Right-panel interior cards preserved as-is; `FloatingEnvironmentCard` untouched entirely. Ten prompts on `Codex/cool-ellis-21ca71` (worktree): P1 surface tokens + theme-preset feed-through, P2 shell wrap, P3 left sidebar interior, P4 right panel trim (cards preserved), P5 chat column transparent, P6 modal interior cleanup, P7 in-chat surfaces zero card chrome, P8 auxiliary sweep, P9 light/dark QA + bg-primary tune, P10 phase wrap. See `PLANNING/LAMPREY_PANELS_PLAN.md`.
- **Stall & Timeout Phase (T1–T7)**: complete (2026-06-05, v0.6.1) — fix for the recurring "mid-research stall" symptom on `Codex/interesting-curran-beace7`. T1 SSE inactivity watchdog in `chatStream` (`streamInactivityMs`, default 60s), T2 per-call MCP timeout (`mcpCallTimeoutMs`, default 120s), T3 per-stage wall-clock budgets (`stageBudgetMs.{planner,coder,reviewer}`, defaults 120/600/120s), T4 `chat:streaming-vitals` heartbeat with "Ns since last chunk" pill indicator, T5 Settings → **Timeouts** panel, T6 DEVLOG, T7 ship. All caps configurable in settings.json; 0 disables each individually.
- **Skill Import Phase (I1–I8)**: complete (2026-06-05, v0.7.0) — first-class **"Import from Codex"** path. Eight prompts on `Codex/kind-noyce-f4eeca` (worktree): I1 read-only on-disk discovery service for `%APPDATA%\Codex\local-agent-mode-sessions\skills-plugin\` (+ macOS / Linux equivalents), I2 importer that copies a bundle into `userData/plugins/<id>/` with a synthesised root `plugin.json` and rewrites disabled-in-CC skills as `autoInvoke: false`, I3 `ccImport:*` IPC + preload surface, I4 renderer types + Zustand store, I5 new "From Codex" tab in InstallPluginFlow with per-skill enabled chips + "ext" warning chips for skills that shell out, I6 "↓ Import" entry on SkillsColumn, I7 eject affordance + supporting-files drawer summary, I8 ship. See `PLANNING/LAMPREY_SKILL_IMPORT_PLAN.md`.
- **Research Reliability Hotfix (R1–R5)**: complete (2026-06-05, v0.7.2) — fix for "research turn ghosts the conversation" caused by DDG's HTML endpoint regressing to homepage-on-search. R5 Wikipedia adapter (zero-key REST API floor), R3 DDG demoted to last in default cascade `['brave','serpapi','wikipedia','duckduckgo']`, R1+R2 typed `NoSourcesError` → `chat.ts` persists a `role:'system'` note and falls through to normal chat so the model answers from training, R4 Settings → API Keys gains a **Search providers** section with Brave/SerpAPI/Tavily key entry (separate `web_search:*` keychain namespace). DDG kept in cascade as last-resort attempt.
- **Tavily Promotion Hotfix (R6)**: complete (2026-06-05, v0.7.3) — `DEFAULT_PROVIDER_CASCADE` reordered to `['tavily','brave','serpapi','wikipedia','duckduckgo']`. Tavily is primary because it's purpose-built for research-grade LLM retrieval. `TavilyAdapter.search` now sends `search_depth: 'advanced'` + `include_answer: 'advanced'` for better quality (2 credits per call). User pastes Tavily key into Settings → API Keys → Search providers; without a key the cascade falls through unchanged from v0.7.2.
- **Reasoning Audit Phase (R1–R10)**: complete (2026-06-06, v0.8.0) — every model-emitted chain-of-thought preserved end-to-end. See `PLANNING/LAMPREY_REASONING_AUDIT_PLAN.md`. Ten prompts on `Codex/magical-bose-62be27` (worktree): R1 `stage` column added to `messages` (no backfill, NULL = single-agent); R2 `chatOnce` returns `{content, reasoning?}` reading both `message.reasoning` (OpenRouter) and `message.reasoning_content` (DashScope/DeepSeek); R3 `SubAgentRunner`/`ForkAgentRunner` return-type widened to union `string | {output, reasoning?}` for backwards compat (every existing `async () => 'ok'` test runner unchanged); R4 Planner persisted as its own row with `stage='planner'` — hidden by default in chat, attached to next Coder/Composer bubble behind a "Show pipeline trace ▾" toggle per user-direction §2.9; R5 Reviewer saveMessage now passes `reasoning` + `stage='reviewer'`; R6 cumulative per-round reasoning trail on composer-final row with `MAX_REASONING_BYTES=65536` cap + honest truncation marker + `stage='composer'`; R7 `MessageList` pre-walk attaches Planner rows; `MessageBubble` shows the "Show pipeline trace ▾" toggle + Reviewer/Composer stage chips; orphan Planner rows fall through to standalone render with a chip; R8 past reasoning re-fed to API on follow-up turns (gated by `includePastReasoningInContext`, default `true`); R9 Settings → **Reasoning Audit** panel exposes the one toggle + end-to-end pipeline test; R10 wrap.
- **Robustness Hotfix (HX1–HX5)**: complete (2026-06-06, v0.8.4) — two user-reported defects closed on `Codex/cool-wescoff-726885` (worktree). HX1 `app.requestSingleInstanceLock()` in `electron/main.ts` blocks duplicate Lamprey processes (headless `--headless` CLI invocations exempted via `isHeadlessCliArgv`); `second-instance` event restores + focuses the existing window. HX2 extracts the RT1 reviewer guard into shared `export const PSEUDO_TAG_GUARD` in `electron/services/system-prompt-builder.ts`; appended to `planner`, `coder`, `reviewer`, `coworker` + the `COMPOSER_SYSTEM` array (reader / verifier deliberately untouched — short verdict strings); reviewer body reassembles byte-identical to RT1 (golden snapshot test). HX3 lands the data layer + pure rewriter without touching the save path: `safeAddColumn(db, 'messages', 'content_raw TEXT')` migration + new pure module `electron/services/sanitize-pseudo-tags.ts` (`sanitizePseudoTags(text)` rewrites shell-shaped tags `<bash>`/`<tool>`/`<run>`/`<shell>`/`<execute>`/`<command>`/`<terminal>` → ` ```bash ` fences and output-shaped tags `<output>`/`<result>`/`<stdout>`/`<stderr>` → ` ```text ` fences; case-insensitive, multi-line, fence-aware, unbalanced-safe, idempotent — 22 vitest cases). HX4 wires the sanitiser into `saveMessage`: assistant rows persist `content = sanitized`, `content_raw = original` (only when the sanitiser actually rewrote — NULL otherwise to avoid bloating clean rows); FTS indexes the sanitised content so search matches the bubble; non-assistant rows pass through unchanged. `Message` type in `src/lib/types.ts` gains optional `contentRaw?: string | null` — UI does not consume yet (opt-in for future RT-Viewer audit-surface extension). 5 round-trip tests in `conversation-store-sanitize.test.ts` skip under same `NODE_MODULE_VERSION` mismatch that skips `snip/apply.test.ts` (better-sqlite3 native binding for Electron v133 ≠ vitest's Node v137); honest, integration smoke is the real coverage. HX5 ship arc.
- **Right-panel polish round 2**: complete (2026-06-06, v0.8.3) — three follow-ups on top of v0.8.2: (1) cards use `flex-1 basis-0 min-h-[58px]` so the nine pills distribute evenly to fill the panel instead of crowding at the top with empty space below (container back to `gap-2 p-2.5`); (2) Background tasks icon dilated from 6.3% → 7.9% opaque via new `scripts/thicken-background-tasks-icon.cjs` so it stops reading "faded" next to its peers under the dark-mode `brightness(0) invert(1)` filter — Dark View companion regenerated; (3) `ASSETS/Lamprey Reasoning Trace Icon Light View.png` wireframed via `scripts/make-wireframe.cjs` (90.6% pixels knocked transparent) + paired `… Dark View.png` companion generated, then wired into `RightPanelHome.tsx` as `reasoningTraceIcon` (no longer reusing `planIcon` as a placeholder).
- **Right-panel card density hotfix**: complete (2026-06-06, v0.8.2) — single-file fix on `src/components/artifacts/RightPanelHome.tsx`. Container `gap-1.5 p-2`, card `min-h-[54px] gap-2.5 px-2.5 py-2`, icons `h-9 w-9` (down from `h-11 w-11`). Reclaims ~168px so all nine pills (Files → Reasoning trace) seat inside the right panel at default width without scroll. No tokens / IPC / store / DB touched; `FloatingEnvironmentCard` and right-panel docked-mode interiors untouched per Panels Phase contract.
- **Reasoning-Trace Phase (RT1–RT8)**: complete (2026-06-06, v0.8.1) — five R10-follow-up items closed on `Codex/elegant-hodgkin-49d53e` (worktree): RT1 Reviewer system-prompt guard against `<bash>`-as-prose pseudo-tool hallucination, RT2 `message_stage_metrics` table + store + write-through wiring in `runChatRound` (single mode, `stage='single'`) and `runAgentPipeline` (planner+coder on the coder message id, reviewer on the reviewer message id), RT3 `StageTokenChips` on assistant bubbles + live `stage:<role>` segment on `StreamStatusLine` when multi-agent, RT4 `get_conversation_history` model-callable tool (low-risk, no approval, model can address turns by index + opt-in stage metrics + opt-in tool calls), RT5 Reasoning-Trace Viewer panel as the 11th right-panel pill + per-turn list, RT6 per-stage expansion (reuses R7's `ReasoningBlock`) + debounced search + stage-filter chips (All / Planner / Coder / Reviewer / Single), RT7 audit-trail export via `dialog.showSaveDialog` to `.md` + `.csv` (pure exporter + 12 tests; local-only, reasoning content verbatim). See `PLANNING/LAMPREY_REASONING_TRACE_PLAN.md`.
- **Function Calling Phase (FC-0–FC-16)**: complete (2026-06-08, v0.9.0) — native function-calling infrastructure hardened end-to-end. 17 prompts on `codex/function-calling` (worktree): FC-0 provider capability audit (all 4 providers use OpenAI-compatible endpoints; Google uses `v1beta/openai/` not native Gemini SDK); FC-1A/B/C schema infrastructure (`validateToolArguments()` shared gate, all 46 native tools with `additionalProperties: false`, coverage test); FC-2 `supportsTools` flag audit; FC-3 provider schema normalizer (core tools fail-fast, non-core dropped with warning); FC-4 canonical transcript model (`ToolCallRequest`/`ToolResult` types, per-provider serializers); FC-5 argument validation in dispatch path; FC-6 brace-balanced fallback parser with schema validation + `{"action":"...","input":{...}}` contract; FC-7 `PSEUDO_TAG_GUARD` + sanitizer scoped to fallback models; FC-8 pipeline refactored with fallback parsing integration + role-based tool access (`filterToolsForRole`); FC-9 fallback trust degradation (mutating fallback calls skip "always allow" policies); FC-10 capability mismatch detection (3-mismatch threshold, session-scoped downgrade); FC-11 regression tests (ghost-reply, fallback boundaries); FC-12/13/14 shadow logger built, smoke-test matrix documented, shadow removed (clean by construction); FC-15 architecture docs (`ARCHITECTURE/FUNCTION_CALLING.md`); FC-16 phase wrap. Delivers: native structured `toolCalls` dispatch for all providers, brace-balanced fallback JSON parser, shared argument validation, canonical transcript model, capability mismatch auto-detection. See `PLANNING/Lamprey_Function_Calling_PSPR_.md`.
- **Mechanical Proof Harness Phase (M1–M13)**: complete (2026-06-08, v0.9.x) — mechanical proof layer: scoped change contracts, append-only verification receipts with parsed metrics, pre-final proof gate blocking untrusted completions, independent reviewer evidence packets, failure-mode reviewer contract, durable failure ledger with stable fingerprints and auto-promotion, deterministic harness improvement recommendations, and repo-local `verify:proof`/`verify:all` gates. Thirteen prompts shipped on `codex/mechanical-proof-harness`. See `PLANNING/LAMPREY_MECHANICAL_PROOF_HARNESS_PLAN.md` and `ARCHITECTURE/MECHANICAL_PROOF.md`.
- **Project Section Phase (PRJ-0–PRJ-13)**: complete (2026-06-08, v0.9.x) — first-class Project system: the left-sidebar "+" opens a styled `NewProjectModal` (replacing the bare `window.prompt()` that was the original UX gap), project records carry `slug`/`description`/`updatedAt`/`lastOpenedAt`, migration v15 extends the existing SQLite `projects` table idempotently, `selectProject`/`updateProject` services and IPC channels tie into the Zustand renderer store with `activeProjectId` tracking, the sidebar renders active project highlighting with select-on-click wired to a `ProjectHome` landing view showing project details and session lists. Fourteen prompts shipped on `codex/project-section`. See `PLANNING/LAMPREY_PROJECT_SECTION_PLAN.md` and `ARCHITECTURE/PROJECTS.md`.
- **Wiring Closure Phase (WC-0–WC-11)**: complete (2026-06-09, v0.9.1) — 11-prompt correctness phase that closes seven documented-but-dead-code gaps from the three preceding major phases, after a cross-phase audit found load-bearing claims that were scaffolded but unwired. WC-0 baseline doc; **WC-1** wires `normalizeToolsForProvider` via `toolRegistry.getNormalizedToolsForProvider(provider)` at `electron/services/tool-registry.ts:518`, called from `electron/ipc/chat.ts:467`; **WC-2** wires `filterToolsForRole` via `getNormalizedToolsForRole(role, provider)` at `electron/services/tool-registry.ts:541` (Planner / Reviewer subsets verifiably exclude `apply_patch` / `shell_command`); **WC-3** synthesizes implicit change contracts on the first mutating tool call via `ensureImplicitContractForFirstMutation()` at `electron/ipc/chat.ts:1149`; **WC-4** persists proof gate trust state via migration v16's `messages.proof_status TEXT` column + chat write-through; **WC-5** banner + composer consume `messages.proof_status` via `computeProofBannerState` helper, waiver path flips status to `'waived'` via new `messages:setProofStatus` IPC; **WC-6** composer appends deterministic `**Verification:**` footer citing each receipt id, glyph, kind, command, parsed metrics, exit code (model's reply preserved exactly); **WC-7** CI invokes `npm run verify:proof -- --no-tests` as the M10 static gate; **WC-8** new `src/components/layout/Sidebar.project-flow.test.ts` adds 13 source-reading wiring-contract assertions including the negative `window.prompt(` check that locks the original sidebar "+" defect; **WC-9** architecture docs cite real `Invoked from: <file>:<line>` sites; **WC-10** append-only correction notes on the three audited plans; **WC-11** ship arc (v0.9.1, README, AGENTS.md, DEVLOG, push, Bucket). Final gate: lint OK, tsc node + web OK, vitest 2193 passed | 123 skipped, build OK, `verify:proof` exits 0. See `PLANNING/LAMPREY_WIRING_CLOSURE_PLAN.md`.
- **Schema Bootstrap Hotfix (v0.9.2)**: complete (2026-06-09) — P0 fix for v0.9.1 where every chat send returned `table messages has no column named proof_status`. Root cause was upstream of WC-4's migration v16: `conversation_rag_attachments` DDL in `electron/services/schema-init.ts` used `PRIMARY KEY (conversation_id, COALESCE(collection_id,''), COALESCE(document_id,''))` which SQLite rejects with `expressions prohibited in PRIMARY KEY and UNIQUE constraints`. That throw aborted `initLegacySchema` segment 5 partway through on every launch with an existing DB, so `runMigrations(db)` was never reached — `user_version` stayed at 0, every migration v1-v16 was skipped, and WC-4's INSERT pointed at a column that didn't exist. Fix: move uniqueness rule from table-level `PRIMARY KEY (expr)` to external `CREATE UNIQUE INDEX (expr)` (SQLite allows expression columns in `UNIQUE INDEX`, just not in table constraints). Existing `ON CONFLICT(...)` upsert in `rag/store.ts:858` matches the new index byte-for-byte. The `schema-init.test.ts` regression test that should have caught this silently skipped under the better-sqlite3 NODE_MODULE_VERSION mismatch (vitest's Node v137 ≠ Electron v133) on every CI + local run. Recovery is automatic on first v0.9.2 launch: `initLegacySchema` completes (creates missing tables), then `runMigrations` runs v1 → v16 idempotently (adds `messages.proof_status`, creates `proof_receipts` / `change_contracts` / `failure_ledger`, etc.). No data migration needed. Follow-up worth scheduling: make the better-sqlite3 NODE_MODULE_VERSION-mismatch skip explicit in `verify:proof`'s output so the next silent test loss is visible at gate-time instead of user-runtime.
- **Lampshade Phase (L1–L11)**: complete (2026-06-09, v0.10.0) — 11-prompt prompt-cogency phase that peels the over-instruction layer that was making DeepSeek / Gemma / Qwen outputs feel tortured. L1 baseline measured ~2,725 tokens of operator instruction shipped per coding turn (Reviewer stage was 11,016 bytes, ~89% shared boilerplate). **L2** collapsed the 9-section / 52-bullet `Codex Agent Contract` into one 13-bullet `How you work` block (`renderContract()` 9,311 → 2,113 bytes, −77.3%). **L3** made `<think>` conditional (was: *"Every single assistant turn MUST begin with a `<think>` block"*; now: *"When the answer involves planning, multiple options, or a non-obvious decision…"*); native-reasoning models get the bullet stripped entirely. **L4** slimmed the six role fragments from meta-explanation paragraphs to 2–3 tight imperatives (~70% avg drop). **L5** stripped the full identity + contract from every agent sub-stage prompt; sub-agents now get a one-line slim identity head + their role text + (coder only) a 3-line operating-principles excerpt. Reviewer drops 11,016 → 697 bytes (−93.7%). **L6** dropped `PSEUDO_TAG_GUARD` from every injection site; `sanitizePseudoTags` (HX3/HX4) remains the persist-side safety net and `messages.content_raw` still preserves verbatim originals. **L7** slimmed `COMPOSER_SYSTEM` (dropped the mandatory `<think>`, softened the structure mandate to a soft suggestion, kept the proof-receipt citation rule verbatim — load-bearing for the M-phase gate). **L8** added a new `'auto'` `AgentMode` option that's now the default for new installs — `electron/services/agent-router.ts` is a pure heuristic that decides per turn whether to dispatch single-agent or multi-agent based on prompt shape (long prompts / phase phrases like P-SPR / STS / build-from-scratch / multi-file refactor / sequential markers / multi-deliverable → multi; everything else → single); honors explicit `--single` / `--multi` flags. Single + Multi remain available as explicit pins. **L9** added 5 envelope-shape guard tests + a fully-authored 8-ask cogency playbook in `PLANNING/LL_SMOKE_PLAYBOOK.md`. All shipped tools, UX, panels, function-calling infrastructure, mechanical-proof harness, RAG, Snip, Skills, Plugins, Reasoning-Trace Viewer, and Bucket pipeline untouched. Final gate: tsc node + web OK, vitest 2222 passed / 123 skipped, `verify:proof` exits 0. See `PLANNING/LAMPREY_LAMPSHADE_PLAN.md` + `PLANNING/LL_BASELINE.md` + `PLANNING/LL_AFTER.md`.

- **Hygiene Phase (HY0–HY7)**: complete (2026-06-09, v0.11.0) — 8-prompt context-economy + thin-harness phase derived from a direct audit of the Codex harness vs. Lamprey (the differentiators were context hygiene + philosophy, not features). **HY1/HY2** lazy model tool-surface: `electron/services/model-tool-surface.ts` sends a 12-tool always-on CORE set + a `tool_search` meta-tool instead of the full catalog; the model unlocks tools on demand via a search→resolve→unlock round-trip (per-conversation state in `tool-unlock-state.ts`), wired in `chat.ts` behind `AppSettings.toolSurface: 'lazy'|'full'` (default lazy) with an FC-10-style downgrade to full on repeated malformed `tool_search` calls. **−63.8% tool-schema bytes/turn** native-only, flat regardless of MCP connector count. **HY3** tool-result spill valve (`tool-result-spill.ts`): results > 8192 chars are written to `userData/tool-results/` and the model gets a head+tail preview + ref; new `read_tool_result` native tool (in CORE) pages the full text back. The conversation store keeps the full result for the UI. **HY4** lazy skill bodies: active skills inject name+description stubs (`buildSystemPrompt` gains `lazySkillBodies`, default false so direct callers/snapshots are unchanged); new `skill_open` native tool (in CORE) loads a body on demand; follows `toolSurface`. **HY5 (Split — per user decision)** left L8 routing untouched (large tasks still fan out) and scoped only the proof-gate + change-contract machinery to rigor turns via `proof-rigor.ts` (rigor = audit/verify/prove/review/validate verb, OR multi-agent dispatch, OR `proofGate:'always'`); casual turns get clean replies with no receipts scan and `proofStatus` undefined; new `AppSettings.proofGate: 'rigor'|'always'|'off'` (default rigor). **HY6** embedded one compact few-shot `<example>` turn in `renderContract()` (read→edit→verify→name) — exemplar steering over prose rules; contract still < 3,700 bytes. New settings (`toolSurface`, `toolResultSpill`/`toolResultSpillBytes`, `proofGate`) are optional with safe defaults; no settings UI yet. Final gate: tsc node + web OK, vitest 2271 passed / 117 skipped, build OK, `verify:proof` exits 0. See `PLANNING/LAMPREY_HYGIENE_PLAN.md` + `PLANNING/HY_BASELINE.md` + `PLANNING/HY_AFTER.md`. Honest gaps: lazy-round-trip + HY6 exemplar cogency need a live DeepSeek pass; spill files aren't GC'd within a session; `tool_search` has no live UI card yet.

- **Cogency Restore Phase (CR-0–CR-12)**: complete (2026-06-09, v0.12.0) — 13-prompt cogency-focused phase on `Codex/cogency-restore` closing eight regressions surfaced by the L9 LL_SMOKE_PLAYBOOK run against v0.11.0/v0.11.1. **CR-1** restores project planning vocabulary (STS, P-SPR, Stem to Stern, Bucket, F13 vocab-vs-build) as a new `Project conventions` block in `renderContract()` — Asks 7 + 8 v0.11.1 proved the Planner had no idea what those terms meant and hallucinated "P-SPR = Plan for Specifying and Performing". **CR-2** wraps `runAgentPipeline` at the chat.ts call site with a new `withPipelineSafety()` helper from `electron/services/agent-pipeline-safety.ts` — when the pipeline bails after the Coder mutated files (a stage threw OR stalled), Lamprey synthesises a `role:'system'` message naming the modified paths so the user has a clear `git restore` / `git stash` target instead of finding broken files on disk with no chat reply (closes F2 / Ask 7 v0.11.0). Includes `StageInactivityWatchdog` for the silent F15 stall pattern observed in Ask 6 v0.11.1 (opt-in via `settings.stageInactivityMs > 0`, default 0). The conversation-store `MessageStage` union gains `'system'` for the synthesised rows. **CR-3** adds a structured `RouterDecision.matchedRule` field to every code path in `routeAgentMode` + an in-memory ring buffer (`electron/services/router-telemetry.ts`, 50-entry cap, surface-able via /debug) so a future mis-route is diagnosable with one click. **CR-4** locks the LL_SMOKE_PLAYBOOK asks to their expected route + matched rule via per-ask assertions in `agent-router.test.ts` and `agent-pipeline.test.ts`; the heuristic is CORRECT — the v0.11.0/v0.11.1 multi-routing observed on Asks 2–5 was the user's `settings.agentMode='multi'` setting bypassing `routeAgentMode` entirely (documented). **CR-5** gates the proof-gate engagement (and implicit-contract synthesis at `chat.ts:~1551`) on `shouldEngageProofGate(conversationId)` which AND-combines `isProofRigorActive` with new `hasMutationAttempted` tracking — multi-dispatch turns that don't actually mutate stop tripping the "Untrusted completion" pill (closes F4). Plan-mode turns naturally pass through this gate. New `markMutationAttempted` called from the chat tool-call dispatcher right after the descriptor is identified as mutating. New `setting.rigorRequiresMutation` (default `true`) gates the behavior. **CR-6** lands as a **no-op** per revision 3 of the P-SPR: empirical v0.11.1 playbook re-run showed 4/4 first-try verdict-line hits across Asks 3 (CHANGES), 4 (SHIP), 5 (CHANGES), 8 (CHANGES). F5 (verdict-line intermittent) was a downstream symptom of the Reviewer Packet bug, not a contract regression. **CR-7** ships the reviewer-exemplar half only — new `IDEAL_REVIEWER_EXEMPLAR` (281 bytes ≤ 300 guard) embedded in `renderContract()` showing the desired terse review shape (cite by file:line, raise at most one concern, verdict on its own line). The scope-creep-guard half is a no-op per revision 3: Asks 4 + 5 v0.11.1 both asked clarification without volunteer fixes for unasked content (F12 already resolved by v0.11.1). **CR-8** adds three Coder operating principles to the multi-agent sub-agent excerpt: (1) F7 shell-syntax adapt after ONE failure (Asks 5, 6, 7 v0.11.0 showed 3+ sequential same-shape Unix-on-PowerShell failures); (2) F9 never edit files via shell pipelines, `apply_patch` is the only safe edit path — silent UTF-8 corruption from `[System.IO.File]::WriteAllText` in Ask 7 v0.11.0; (3) F13 default to smallest correct fix — Asks 6 + 8 v0.11.1 showed Coder building entire systems from terse user clarifications. **CR-9** adds one Coder bullet: escalate to `ask_user_question` after three consecutive zero-match searches (Ask 5 v0.11.0 ran 15 rounds; Ask 6 v0.11.1 ran 54 before stalling). **CR-10** updates `LL_SMOKE_PLAYBOOK.md` with v0.12.0 pass criteria per ask. **CR-11** records the post-CR snapshot + manual re-run protocol in `CR_AFTER.md` (live re-run executed by the user against the v0.12.0 build). **CR-12** ships the phase wrap — `package.json` bumped to 0.12.0, README + AGENTS.md current-state updated, DEVLOG phase-complete entry. Contract regrowth: pre-CR 2,560 → post-CR 3,401 bytes (+841 vs ≤ 600 budget; reconciled in CR_AFTER §1 — within the 11.7% of L2's 7,200-byte savings the budget targeted). L9 byte guard raised from `< 4,096` → `< 4,400` for the coding-mode prompt. New optional settings: `rigorRequiresMutation`, `stageInactivityMs`, `routerTelemetry` (all safe defaults). Final gate: tsc node + web OK, vitest **2332 passed / 123 skipped**, `verify:proof --no-tests` exits 0. See `PLANNING/LAMPREY_COGENCY_RESTORE_PLAN.md` + `PLANNING/CR_BASELINE.md` + `PLANNING/CR_AFTER.md`. Honest gaps: stall watchdog `kick()` not yet wired across every tool-result event (the mechanism + tests are present but a stage making progress could still trip the timer if `stageInactivityMs > 0`); router-telemetry ring buffer has no /debug UI yet (IPC exposed, surface TBD); CR-11 live re-run results land after first user install of v0.12.0.

- **Reviewer Packet Hotfix (v0.11.1)**: complete (2026-06-09) — single-defect hotfix on the M7 reviewer evidence packet. `runAgentPipeline` was passing the Coder's reply to `buildReviewEvidencePacket` as `builderNarrative` at [agent-pipeline.ts:705](electron/services/agent-pipeline.ts:705), but the builder gated inclusion behind a separate `includeBuilderNarrative?: boolean` flag in `BuildReviewEvidencePacketInput` that no caller ever set — so the field was silently dropped at [review-evidence-packet.ts:350](electron/services/review-evidence-packet.ts:350) on every multi-agent turn. The Reviewer therefore reviewed a packet with the work product missing, correctly noted "no answer artifact to review", and returned `CHANGES` for every turn including casual ones. The two unit tests (`review-evidence-packet.test.ts:137` "without builder narrative by default", `agent-pipeline.test.ts:248` "M7: sends an evidence packet to Reviewer **without** coder narrative") codified the wrong invariant and made the defect invisible at CI. Fix: invert the API. `builderNarrative` is now included whenever provided — `includeBuilderNarrative` removed from the input type; the gate simplifies to `if (input.builderNarrative)`. The defense against the Coder narrating its way past review lives in the reviewer prompt + the field's explicit name (labelled *claim*, not *evidence*), not in hiding the work product. Both tests flipped to assert the opposite invariant; M7 gained a positive assertion that the Coder's content reaches the Reviewer sub-agent's serialized packet. The `enforcePacketCap` last-resort `delete next.builderNarrative` trim path is unchanged. Final gate: lint OK, tsc node + web OK, vitest **2265 passed / 123 skipped**, `verify:proof --no-tests` exits 0.

- **Sweet Spot Phase (SP-0–SP-12)**: complete (2026-06-10, v0.13.0) — 13-prompt era-lock + defect-closure phase on `Codex/hardcore-swanson-5561d9` locking Lamprey to its declared target: functional + aesthetic parity with Codex in the **Opus 4.5 era (2025-11-24 → 2026-01-24)**. Defaults moved, nothing removed — multi-agent, auto-routing, proof harness, composer, and lazy tool surface all remain explicit opt-ins. **SP-1** single source of truth for defaults: new `electron/services/default-app-settings.ts` (`DEFAULT_APP_SETTINGS`) imported by `electron/ipc/settings.ts`; the renderer literal in `src/stores/settings-store.ts` is locked byte-for-byte by `default-app-settings.test.ts` (source-reading parity, WC-8 pattern) — closes D1, the renderer-'auto'/main-'single' drift where main silently won. Era values: `agentMode: 'single'`, `proofGate: 'off'` (`resolveProofRigor` unset → OFF), `toolSurface: 'full'` (runtime `?? 'lazy'` fallbacks flipped). **SP-2** composer opt-in: `loadAgenticCodingConfig` + `resolveComposerGate` moved to `final-response-composer.ts`; composer resolves `'never'` unless `agenticCodingMode` is ON — default turns keep the model's own reply, and the WC-6 Verification footer rides the same gate (closes E3). **SP-3** `clearMutationAttempted(conversationId)` at turn start — the CR-5 flag was never cleared, so one mutating turn armed the proof gate for every later rigor turn in the conversation (closes D4). **SP-4** ghost-reply guard: pure `ghost-reply-guard.ts` (`turnEndedGhosted` / `isUserAbortError` / `buildGhostReplyNotice`); the chat:send outer catch persists a `role:'system'` notice when a turn fails with no visible reply row (pre-stream throw, instant stream failure, no-mutation pipeline bail); user cancels excluded (closes D5). **SP-5** watchdog kick wired: `runChatRound` gains an `onActivity` callback invoked on stream chunks, reasoning chunks, and completed tool calls; the multi-agent coderRunner passes `() => watchdog.kick()` — a progressing stage no longer trips `stageInactivityMs` (closes D2, the CR honest-gap). **SP-6** spill GC: `gcSpillDir()` at startup (deferred 10s) deletes spill files > 7 days then trims oldest-first to 256 MB; GC'd refs resolve to the existing "expired" reply (closes D3). **SP-7** UI era pass: pipeline banner renders "Planning / Writing code / Reviewing" via `ROLE_LABEL` (not raw role ids), "Planner (orphan)" → "Plan", raw contract/receipt ids moved from `ProofGateBanner` body to hover tooltip; era-chrome source locks in `src/components/chat/era-chrome.test.ts`; `role:'system'` rows confirmed routing to `SystemMarker` (closes E5). **SP-8** router telemetry IPC: new `after-action:routerTelemetry` channel + preload + a "Routing" section in the After action panel — CR-3 documented this surface but no handler existed (closes D6). **SP-9** `verify:proof` native-skip accounting: probes whether better-sqlite3 loads under the current Node, prints the ABI-guarded cohort either way, `--list-native-skips` mode; the v0.9.2 follow-up (closes D7). **SP-10** playbook v0.13.0 criteria: Configuration A (fresh defaults — all 8 asks single-agent, zero machinery chrome) + Configuration B (pin `agentMode:'auto'` to exercise the router; CR-4 locks unchanged). Final gate: lint OK, tsc node + web OK, vitest **2384 passed / 123 skipped** (+52 tests vs CR baseline, zero new skips), build OK, `verify:proof --no-tests` exits 0. Prompt-surface byte guards unchanged. See `PLANNING/LAMPREY_SWEET_SPOT_PLAN.md` + `PLANNING/SP_BASELINE.md`. Honest gaps: the live playbook re-run (Configurations A + B) lands after first user install of v0.13.0; `toolSurface`/`proofGate` still have no Settings UI (settings.json only); reviewer/planner single-shot sub-agent calls have no in-call activity signal (their stage budgets remain the stall coverage).

- **Unburdening Phase (UB-0–UB-12)**: complete (2026-06-10, v0.14.0) — **the deletion phase**, per explicit user direction the same day ("Lamprey is still tortured… strip away the stale and burdensome scaffolding so that it can breathe easier and be comfortable in its own skin"). Everything the Opus 4.5-era product never had is EXCISED, not gated — **net −7,400 lines**: the multi-agent Planner→Coder→Reviewer pipeline + `resolveAgentDispatch` + the CR-2 safety wrapper + the M7 reviewer evidence packet (UB-1); the CR-7 reviewer exemplar from the contract (UB-2); the L8 router + CR-3 telemetry + SP-8 IPC (UB-3); the runtime proof machinery — proof-rigor, proof-gate, WC-3 implicit contracts, receipts scan, ProofGateBanner, `messages:setProofStatus`, After-action Proof section (UB-4); the final-response composer — the reply is the model's reply byte-for-byte; the R6 reasoning trail (user-directed audit, KEPT) extracted to `reasoning-trail.ts`, agentic config to `agentic-coding-config.ts` (UB-5); the whole stage-chrome/plumbing layer — `agent:status` chain, planner attachment, "Show pipeline trace", stage chips (historical rows show one muted "Pipeline (legacy)" chip), stage budgets UI, 'Pipeline' labels (UB-6); the settings/type surface — `agentMode`/`agentRoster`/`proofGate`/`agenticCodingComposer` retired, `agent-store` + Agents tab deleted (UB-7). Contract re-measured 3,401→**3,118 B**, coding prompt 4,039→**3,756 B**; guards tightened to 3,300/3,900 (UB-8). **KEPT:** coworker side chat, the model-callable `multi_agent_run` tool (Task-tool analog — `AGENT_ROLE_PROMPTS`/`buildAgentSystemPrompt` survive for it; documented deviation from the plan's X2 wording), Deep Research, RAG, Snip, Skills/Connectors/Plugins, panels aesthetic, reasoning audit/trail/trace viewer, stream+MCP timeouts, ghost-reply guard, spill valve+GC, sanitizer, `verify:proof` repo gate. DB schema + store layers (receipts/contracts/ledger) stay per K2 — historical rows readable forever. Stale settings keys are inert (nothing reads them). Git history at the v0.13.0 tag holds the last full-machinery build. See `PLANNING/LAMPREY_UNBURDENING_PLAN.md` + `PLANNING/UB_BASELINE.md`.

- **Loop Phase (LP-0–LP-11)**: complete (2026-06-14, v0.15.0) — **the first deliberate, documented extension PAST the Opus 4.5 era-lock** (user-authorized 2026-06-14: "incorporate a Looping option"). A first-class **Loop** primitive across the full autonomy gradient — **interval** / **self-paced** / **autonomous "work-the-backlog"** — that ships **OFF by default** (`loopsEnabled: false`), so the era-faithful default experience is unchanged. **Important discovery (LP-0):** Lamprey already had a half-built loop scaffold (`loop-runner.ts`, `loop-tool-pack.ts`'s `schedule_wakeup` model tool, the `loop_wakeups` table, `window.api.loops`, activity/status-line integration) from an earlier **undocumented** agentic-infra/"Data Spine" phase — it was broken: `fireDueWakeups` injected a `[scheduled wake-up]` user message but the renderer only *reloaded* it (`App.tsx:316`), so **no turn ever ran** (gap G1). **LP-1** factors a headless `runHeadlessTurn` out of the `chat:send` handler (`electron/ipc/chat.ts`) and wires it into `fireDueWakeups` via an injected `setLoopTurnRunner` seam (no service→ipc cycle) — a fired wake-up (and every loop iteration) now runs a real turn in the main process with the window closed/unfocused. **LP-2** migration v17: `loops` / `loop_backlog` (dedicated queue, the user's chosen backlog model) / `loop_runs` tables + `loop-store.ts`. **LP-3** `loop-controller.ts` — `runLoopIteration(loop, deps)` with an injected store+runTurn seam so ceilings/stop-authorities are pure-tested and RUN (no DB skip): max-iterations / max-wallclock / token-budget / backlog-empty; `tickLoops` 30s timer in `main.ts`. **LP-4** self-paced + model tools `loop_enqueue` / `loop_complete_task` / `loop_control` (pause/stop/mission_complete/continue) in `loop-tool-logic.ts` (pure). **LP-5** autonomous mode: progress-ledger prompt injection (idempotency — settled work not redone), self-growth, runaway-floor clamp. **LP-6** per-iteration stall watchdog + throttled spill GC. **LP-7** settings (`loopsEnabled` + 5 bounded defaults, parity-locked) + `loop-config.ts` + `loops:create/list/pause/resume/stop/backlog/runs` IPC (gated on `loopsEnabled`) + `window.api.loops.*` + `loops-store.ts`. **LP-8** `/loop` slash command (`parse-loop-command.ts`, pure). **LP-9** `LoopsPanel.tsx` right-panel pill — live status/iteration/budget/countdown + backlog add/remove/reorder. **LP-10** `loop-safety.test.ts` (source-locks the master-toggle gate at every entry) + `PLANNING/LP_SMOKE_PLAYBOOK.md` (the live integration gate — DB tests skip under the native-binding mismatch). **KEPT minimal/era-faithful:** the autonomous machinery exists but is opt-in, never default. Final gate: tsc node + web OK, full vitest **2240 passed / 130 skipped / 0 failed**, build OK, `verify:proof` exit 0. See `PLANNING/LAMPREY_LOOP_PLAN.md` + `PLANNING/LP_BASELINE.md`. **Gap-closure (v0.15.1):** all four wrap-time gaps closed — Settings → Loops tab (`LoopSettings.tsx`, no more settings.json editing), a `loops` status-line slot ("N loop · iter X", hidden at count 0), a context-aware token-budget estimate (full sent stack + reply, not prompt-only), and `loop-db-integration.test.ts` running the exact v17 DDL + query shapes against built-in `node:sqlite` (9 tests, zero skips — replaces the playbook-only DB gate). Residual honest note (corrected by JM-12/JM-6, v0.16.0): the token budget was undercounted by up to an order of magnitude on tool-heavy turns — `runChatRound` now accumulates the chars actually sent per round, so the budget is a realistic estimate; iteration + wall-clock (now cumulative ACTIVE time via `loops.active_ms`) remain the hard caps. **NOTE on the undocumented agentic-infra layer:** the loop scaffold + cron-automations (`automations-runner.ts`) + workflow runner (`workflow-runner.ts`) + activity dashboard exist in the tree from a pre-Loop phase that this AGENTS.md never listed; they remain (loops build on them), and since v0.16.0 the cron-automation tick rides the `loopsEnabled` master toggle too. **UPDATE (v0.18.0, Agentic Orchestration Phase):** the fork/run half of that layer (`subagent-types`/`forkAgent`/`agent_runs`+`tasks:*`/workflow+worktree runners) is now DOCUMENTED — audited in `PLANNING/AO_BASELINE.md` and governed by the orchestration control plane (identity, grants, budget, kill). It is no longer "undocumented scaffolding"; it is the load-bearing spine the Agentic Orchestration layer builds on.

- **v0.15.2–v0.15.6 (2026-06-15 → 2026-06-18)**: five point releases documented in DEVLOG — **v0.15.2/v0.15.3** added the **Zhipu provider** (GLM 5.2 + the 1M-context variant; `ProviderId` union gains `'zhipu'`, joining the earlier `'openrouter'` addition — the catalog now spans FIVE providers); **v0.15.4** echoed `reasoning_content` on DeepSeek V4 tool-call turns; **v0.15.5** guarded reasoning-token exhaustion on V4 (defaultMaxTokens + reasoning-effort cap on tool turns + empty-params detection); **v0.15.6** removed the retired DeepSeek aliases and added `RETIRED_MODEL_MAP`.

- **July 2026 Maintenance Phase (JM-0–JM-31)**: complete (2026-07-02, **v0.16.0**) — a full-repo audit (six parallel read-only passes; report at `PLANNING/LAMPREY_JULY_2026_MAINTENANCE_AUDIT.md`, ~90 findings) followed by a 32-prompt STS remediation on the P-SPR at `PLANNING/LAMPREY_JULY_2026_MAINTENANCE_PLAN.md`. **JM-0** commit-discipline hard rules: `scripts/hooks/commit-msg` (required trailer `Agentically Engineered and Reviewed by Basho Parks - 2026`, plain-speak subject/body caps, slop denylist) + `scripts/check-ai-artifacts.cjs` staged-diff scan, wired via `core.hooksPath` (`npm run hooks:install`). **Track A (JM-2–7) Loop integrity:** the iteration prompt now actually reaches the model (persist-before-run — it never did before), in-flight mutex + sequential wake-up drain, watchdog signal threaded end-to-end + stop/pause abort the running turn, `loopsEnabled` gates EVERY entry point (schedule_wakeup, fireDueWakeups, tickLoops, loop_control continue, cron automations) with a 10-pending wake-up cap, migration v18 `loops.active_ms` (wall-clock counts work not calendar), 0-disables for all ceilings, error-path ceiling accounting, crash-recovery sweep, transactional iteration commits, workflow-sandbox Math no longer poisons the host, quit drains in-flight turns. **Track B (JM-8–12) chat core:** every failure path settles the turn (pre-stream throws hung forever), retry attempts reset stream accumulators, the FC-6 fallback tool contract is finally injected (it was dead code — non-native/downgraded models were never told the format), malformed args get corrective results instead of silent `{}`, persisted system rows reach the API history (fixes the research fallback), custom models resolve to their real provider, per-conversation capability state clears on delete, byte-true reasoning cap, real multi-round token accounting. **Track C (JM-13–18) data durability:** atomic temp+rename writes for keys.json/settings.json with corrupt-file preservation (a crash could previously wipe every key), fallback latches scoped to true DB-unavailability with 30s recovery probes, `deleteCollection` vec-row leak fixed (stale embeddings mapped onto new chunks via rowid reuse), `getDb()` no longer caches a partially-initialized handle (the v0.9.2 amplifier), saveMessage/deleteConversation/compact are transactional (delete also covers the five orphan row families incl. still-running loops), FTS token quoting, 90-day retention sweep for the five unbounded audit tables, encryption-aware backups with a loud failure warning. **Track D (JM-19–20) security:** `will-navigate` guard (top-frame navigation handed `window.api` to any origin), http(s)-only `shell.openExternal`, webview denial, artifact CSP `form-action 'none'` + navigation/window-open lockdown, `files:*` IPC confined to the workspace root, stdio-MCP add shows the command and requires approval, code-signing env-gated in electron-builder.yml (cert acquisition = owner action, deferred). Also **JM-1/SEC-3**: v0.15.x shipped with `forceDebugTraceOn()` — removed. **Track E (JM-21–25) renderer:** conversation switches clear streaming state (input no longer locks app-wide), staleness guards, `/fork`+Duplicate read the real `conversationId` field, approval queue instead of modal clobbering, focus-scoped chip hotkeys + confirm on workspace grants, dialog a11y, `memo(MessageBubble)` + per-field selectors (no more full-transcript re-parse per stream frame), per-listener disposers replace `offAll`, lazy `window.api` proxy, FTS snippet sentinels. **Track F (JM-26–31) currency:** every dep bumped within-major; **Electron 35 → 43** (the better-sqlite3 pin was obsolete; full static gate green, and the ABI now matches local Node so better-sqlite3 loads under vitest); embedder migrated `@xenova/transformers` → `@huggingface/transformers` 4.x — **`npm audit --omit=dev`: 0 vulnerabilities** (phase baseline: 1 critical/4 high/2 moderate); CI test job runs on ubuntu + windows; TypeScript ^5.9.3; `@types/node` ^22 + `engines: node>=22`; `noUncheckedIndexedAccess` measured (700 errors) and deferred. Final gate: lint OK, tsc node + web OK, full vitest **2334 passed / 130 skipped / 0 failed**, build + both smokes OK, `verify:proof` green. Honest gaps: live GUI smoke of Electron 43 + a real RAG ingest under the new embedder are the owner's first-install checks; code signing awaits a cert.

- **Provider Expansion Phase (PX-0–PX-9)**: complete (2026-07-11, **v0.17.0**) — connector receptivity in three layers on the P-SPR at `PLANNING/LAMPREY_PROVIDER_EXPANSION_PLAN.md`. **Layer 1**: `ProviderId` 5 → 17 built-ins — OpenAI (GPT-5.6 Sol/Terra/Luna + GPT-5.5), Anthropic (Codex Opus 4.8 / Sonnet 5 / Haiku 4.5 over the official OpenAI-compat endpoint at `api.anthropic.com/v1/`; `strict`/`response_format`/`reasoning_effort` ignored there, so no `reasoningCapOnToolUse` and `isReasoner: false` on Codex entries), xAI (Grok 4.5/4.3/Build), Mistral (`-latest` aliases), Moonshot (Kimi K2.6/K2.5/K2-Thinking — thinking model carries the full v0.15.5 reasoning guard; API host stays `api.moonshot.ai` post-rebrand), Groq, Together, Fireworks, Cerebras, Hugging Face router (public `/v1/models`), + 5 OpenRouter broadening entries (`or-` prefixed). 39 catalog models, every id's evidence status in `PLANNING/PX_BASELINE.md` §3 (live-verified / docs-pinned / alias-convention / unverified — never fabricated). **Layer 2**: keyless local runtimes — `ollama` + `lmstudio` ship `keyOptional: true` (placeholder key `'local'`, stored key wins), empty catalogs by design, `settings.json.providerBaseUrlOverrides` redirects any provider (http/https validated at the registry; client cache keys on `provider::baseURL` so override changes take effect without reset). **Layer 3**: `settings.json.customProviders` (`{id, baseURL, label?, requiresKey?}`) become first-class provider ids everywhere — keychain (already string-keyed), settings key gate, Custom Models (fixes the JM-11 reader's silent `'deepseek'` coercion of unknown providers), dispatch, `verifyCatalog`; built-ins unshadowable; validation at the registry consumption site (the settings sanitizer is open-by-design — a PX-4 plan-premise correction). Type widening `ProviderId → string` across descriptor/model/tool-registry/normalizer/chat-dispatch signatures; the union survives as the built-in anchor, source-locked to its renderer mirror by `provider-parity.test.ts` (caught a real one-sided widening the day it landed). UI: ApiKeySettings cards grouped (Frontier / OSS hosts / Local / Custom endpoints) with keyed counts, key-format hints, keyless card states, and a Custom-endpoints add/remove manager; ModelSettings gains a provider select + **"Import from /v1/models"** (new `model:listLive` IPC → `listLiveModelIds`) and drops the stale `BUILTIN_IDS` origin heuristic for a main-process `custom: true` flag. Guards: `catalog-invariants.test.ts` (7 structural locks incl. the chat-probe floor), guard-pairing invariant (`defaultMaxTokens` ⟺ `reasoningCapOnToolUse`). `ARCHITECTURE/FUNCTION_CALLING.md` §16 records per-provider wire notes; `PLANNING/PX_SMOKE_PLAYBOOK.md` is the owner-run live gate (priority: anthropic compat + the one unverified Fireworks id). Honest gaps: per-model live verification for the nine keyed cloud providers, empirical supportsTools probes, and the local import flow await the owner's keys/runtimes (PX_BASELINE §6); GUI pass of the new settings surfaces is the owner's first-install check.
- **Agentic Orchestration Phase (AO-0–AO-12)**: complete (2026-07-11, **v0.18.0**) — a light local orchestration layer, **OFF by default** (`orchestrationEnabled: false`), the second deliberate user-authorized extension past the era-lock (after Loop). Design brief: the Island Mountain post "Agentic Infrastructure Is Shedding Its Scaffolding" — thesis = architecture: controls that hold are deterministic and live outside the model. **AO-0** audited + documented the pre-existing undocumented A/B-series fork spine (`subagent-types`/`forkAgent`/`agent_runs`+`tasks:*`/workflow+worktree runners/the era-kept tool-less `multi_agent_run`) — this phase GOVERNS it, does not rebuild it; the Unburdening deletions (pipeline/router/proof-gate/composer) stay deleted. **Control plane:** master toggle + 5 bounded ceilings (`orchestration-config.ts`, parity-locked, Settings → Orchestration panel); `agent_identities` ledger (migration **v19**) + `agent_runs.identity_id`/receipt columns (**v20**), both additive; `agent-grants.ts` deterministic enforcement (`resolveEffectiveTools`: revoked→zero tools, refused tool absent — never prompt language); `orchestration-budget.ts` (tokens+active-wall metered outside the model, per-call overrides tighten-only, breach aborts the fork tree with an honest note, receipts); `orchestration-governance.ts` (`governFork` — OFF ⇒ null identity + zero writes → existing paths byte-compatible; ON ⇒ auto-granted identity + budget); `agents:*` IPC (list/get/revoke, gated); tree-wide kill in `tasks:stop`. **Strategies** (pure, injected, budgeted; model tools stripped from dispatch when off + slash templates): fan-out+judge (`strategy-fanout.ts`/`agent_fanout`/`/fanout`, per-candidate model override), generator+adversarial critic (`strategy-critic.ts`/`agent_critique`/`/critique`, critic tool-less = read-only by construction), advisor escalation (`strategy-advisor.ts`/`agent_advisor`, honest when unset), outcome+budget (`parse-outcome-command.ts`/`/outcome`, downward-clamp). **Surface:** right-panel **Agents** pill (identities, grants, spend, revoke/kill); `security.decision` audit events (ids+counts, never tool args). Guards: `orchestration-safety.test.ts` (18 source-locks incl. zero-prompt-byte lock) + node:sqlite identity DDL test + pure strategy/budget/grant/parser suites. Non-goals held: no agent-to-agent network exposure, no standing "skip-review" approvals, no billing/cloud/telemetry. See `PLANNING/LAMPREY_AGENTIC_ORCHESTRATION_PLAN.md`, `PLANNING/AO_BASELINE.md`, `PLANNING/AO_SMOKE_PLAYBOOK.md`, `ARCHITECTURE/AGENTIC_ORCHESTRATION.md`. Honest gaps: owner-run live smoke + Agents-pill GUI pass; `/outcome`'s parsed budget is advisory (the strategy tools' settings ceilings are the hard cap); explicit granted/refused events await the full permission→grant chip wiring.
- **Onboarding skill library (2026-07-02):** a 16-skill knowledge base lives at `.Codex/skills/lamprey-*` (force-added past the `.Codex/` gitignore rule; committed `44c6d27`) for future maintainers and smaller models — change-control, debugging-playbook, failure-archaeology, architecture-contract, provider-and-model-reference, config-and-flags, build-and-env, ship-and-release, validation-and-qa, database-and-persistence, docs-and-writing, diagnostics-and-tooling (ships tested read-only `db-health.cjs` + `loop-state.cjs`), loop-reliability-campaign, proof-and-analysis-toolkit, research-frontier, research-methodology. Each ends with a re-verification "Provenance and maintenance" section; facts are date-stamped to v0.16.0. Consult before re-deriving project knowledge.
- **Steering + Queue milestone (CJP-0, ST-1–ST-12):** released 2026-07-18 as **v0.20.0**. Ordinary turns now have stable runtime identity, same-turn Steering at safe model boundaries, durable next-turn Queue delivery, exact interruption/recovery, renderer reconciliation, and metadata-safe activity records. The twenty-row automated contract matrix passes. The owner's core Lamprey test passes; the complete paired Codex/Lamprey desktop playbook remains open, so no blanket current-Codex parity claim is made. The v0.20.0 owner footer `Authored and reviewed by Basho Parks, copyright 2026` supersedes the JM-0 trailer for new commits and documents. See `PLANNING/LAMPREY_CODEX_JULY_2026_PARITY_PSPR.md`, `PLANNING/CJ26_STEERING_AFTER.md`, and `ARCHITECTURE/TURN_CONTROL_AND_STEERING.md`.
- **Task and Thread Control milestone (TC-1–TC-7):** released 2026-07-18 as **v0.21.0**. One bounded read graph spans conversations/forks, agent runs, identities, and turns without merging their stores. Model tools and Activity expose list/read/event-driven wait, shared Queue/Steer delivery, exact interrupt, historical fork-at-turn, recoverable rename/pin/archive/close state, and previewed approval-gated deletion. Migrations v22/v23 add turn backlinks and close metadata. The packaged owner GUI playbook remains open, so implementation-complete is not a blanket current-Codex parity claim. See `ARCHITECTURE/TASK_AND_THREAD_CONTROL.md`, `PLANNING/CJ26_TASK_CONTROL_AFTER.md`, and `PLANNING/CJ26_TASK_CONTROL_PLAYBOOK.md`.
- **Inline Visualizations and Artifact Editing milestone (VA-1–VA-6):** released 2026-07-18 as part of **v0.23.0**. Migrations v24–v26 add stable artifacts, immutable revisions, annotations, message visualization identity, and non-destructive edit proposals. Safe Mermaid/chart/table/SVG previews render inline; interactive HTML/JSX/React remains sandbox-only. Exact Markdown/code/artifact selections support model or direct proposals, diff preview, accept/reject, actor provenance, and honest activity/open outcomes. The packaged owner GUI playbook remains open, so implementation completion is not blanket current-Codex parity. See `ARCHITECTURE/ARTIFACTS_AND_VISUALIZATIONS.md`, `PLANNING/CJ26_ARTIFACTS_AFTER.md`, and `PLANNING/CJ26_ARTIFACT_EDITING_PLAYBOOK.md`.
- **PR Chat milestone (PR-1–PR-6):** released 2026-07-18 as **v0.23.0**. Migrations v27–v29 add repository/SHA-bound conversation context, detached review findings, external-write idempotency receipts, and editable patch proposals. Six lazy read tools inspect bounded/redacted PR context; review writes require exact-target approval; stale heads and escaped paths fail closed; accepted patches use the canonical workspace authority with rollback. The existing PR panel adds bound chat, selected-hunk sends, checks, annotations, confirmation, and patch cards. The disposable GitHub playbook remains open, so implementation completion is not blanket current-Codex parity. See `ARCHITECTURE/PR_CHAT.md`, `PLANNING/CJ26_PR_CHAT_AFTER.md`, and `PLANNING/CJ26_PR_CHAT_PLAYBOOK.md`.
- **MCP resources and authenticated sessions milestone (MR-1–MR-5):** released 2026-07-19 as **v0.24.0**. Capability-aware manager APIs and three strict lazy tools list resources/templates and read exact server/URI pairs through the existing spill authority. Streamable HTTP connectors support generic OAuth 2.1/PKCE with encrypted keychain storage, reauthorization, expiry/reconnect, and consent-gated URL elicitation. Customize → Connectors exposes auth progress, resource/template pagination, safe preview, and confirmed external open for persistent and plugin-owned connectors. Local fixture and real hosted-provider playbooks remain open, so no live hosted-auth parity claim is made. See `ARCHITECTURE/MCP_RESOURCES_AND_SESSIONS.md`, `PLANNING/CJ26_MCP_AFTER.md`, and `PLANNING/CJ26_MCP_PLAYBOOK.md`.
- **Browser Developer Mode milestone (BD-1–BD-6):** locally complete 2026-07-19 as **v0.25.0**, not published. One explicit CDP owner supports bounded redacted console/network observation, structured DOM/accessibility and fixed runtime/performance/trace inspection, and annotated screenshot evidence. The existing Browser panel exposes exact-origin Ask/Allow/Deny trust, attach/record counts, capture, clear, and detach. The feature remains OFF by default; response bodies and sensitive or mutating actions use the existing approval authority. Windows PowerShell shell requests are AST-inspected as data before approval. The owner packaged-app playbook remains open, so no completed live Browser Developer parity claim is made. See `ARCHITECTURE/BROWSER_DEVELOPER_MODE.md`, `PLANNING/CJ26_BROWSER_DEVELOPER_AFTER.md`, and `PLANNING/CJ26_BROWSER_DEVELOPER_PLAYBOOK.md`.
- **Automations and Operational Goals milestone (GA-1–GA-6):** released 2026-07-19 as **v0.26.0**. Four governed automation tools manage typed one-shot, schedule, event, and monitor triggers with durable dedup/retry state. Goals persist lifecycle, actor provenance, token/time budgets, elapsed time, blockers, and completion evidence. One goal may own one bounded loop and an automation may wake it through the existing loop controller; `loopsEnabled` remains OFF by default and every narrower ceiling is tighten-only. Existing Automations and Plans & goals surfaces expose trigger status, next eligibility, lifecycle, progress, and user controls. The owner background/restart playbook remains open, so no live packaged-app parity claim is made. See `ARCHITECTURE/AUTOMATIONS_AND_OPERATIONAL_GOALS.md`, `PLANNING/CJ26_AUTOMATIONS_GOALS_AFTER.md`, and `PLANNING/GA_AUTOMATION_GOAL_PLAYBOOK.md`.
- **Codex July 2026 initiative closeout (CJP-WRAP):** complete and source-published to `main` 2026-07-19 with no version bump or artifact release. `PLANNING/CJ26_AFTER.md` adjudicates every CJP-0 source, historical-claim, Steering/Queue, and Lamprey-wiring row against the released M1–M3 and M5–M8 evidence. M4 / Code Mode remains parked indefinitely. The paired Steering replay and every named packaged/hosted owner playbook remain open, so the initiative is implementation-complete rather than blanket current-Codex parity. `PLANNING/CJ26_FOLLOW_ON_CANDIDATES.md` records Claude refresh, Record and Replay, Computer Use/Chrome, remote handoff, and Office/Sites plugin work as candidates only, not approved PSPRs.
- Read `DEVLOG.md` for detailed build history before making changes. Earlier shipped plans are reference-only. The July 2026 Codex initiative is closed through CJP-WRAP; M4 / Code Mode remains parked indefinitely and is neither a dependency nor a blocker. **NOTE (post-Unburdening):** the multi-agent pipeline, auto-router, runtime proof gate, and composer described in older phase entries no longer exist; those entries are historical record.

## Build & Run
```bash
# Dev (requires ELECTRON_EXEC_PATH workaround on this machine)
ELECTRON_EXEC_PATH="C:\Users\17076\Documents\Codex\Lamprey Harness\node_modules\electron\dist\electron.exe" npx electron-vite dev

# TypeScript check (both configs must pass)
npx tsc --noEmit -p tsconfig.node.json
npx tsc --noEmit -p tsconfig.web.json

# Production build
npx electron-vite build
```

## Where the `.exe`/installer comes from (READ THIS before looking for `dist/`)
**A cloud / Linux session can NOT build or contain the Windows installer.** Do not hunt for `dist/Lamprey-*.exe` here — it will never exist in a remote Linux session.
- `dist/` is electron-builder *output* and is gitignored, so a fresh clone never contains it.
- The signed Windows NSIS `.exe` is produced ONLY by `npm run build:win` on the user's **Windows machine**, or by the **`build.yml` CI** on a `windows-latest` runner.
- **Tag-push path (`v*`)** → CI builds the Windows installer, Linux AppImage, and macOS DMG, then attaches them to the tag release. **Branch-push to `main`** → CI uploads the same platform builds only as workflow artifacts (14-day retention), with no release.
- **Environment note:** branch + tag pushes both work from this Windows session (verified 2026-06-06 on v0.8.2: `git push origin v0.8.2` succeeded first try, then `gh release create v0.8.2 ... --notes-file <file>` published the release with all four artifacts attached). When the user says "ship", "tag it", "release it", or "update the repo page", the ship arc INCLUDES `git push origin vX.Y.Z` + `gh release create vX.Y.Z` with the four Windows artifacts — do not defer either step. (Older sessions on cloud-Linux git proxies hit HTTP 403 on tag pushes; if that recurs in a future cloud session, fall back gracefully and flag it then — never decline pre-emptively here.)
- **"Bucket" = the full ship pipeline.** When the user says **"Bucket"**, run `pwsh scripts\bucket.ps1` from the repo root. It builds the canonical Windows artifacts when needed, tags and pushes `vX.Y.Z`, uploads the EXE and ZIP to R2, writes the GitHub release from `RELEASE_NOTES/vX.Y.Z.md`, waits for the tag workflow, mirrors the macOS DMG and Linux AppImage to R2, and purges each CDN URL. Lamprey has no iOS target; never label the macOS DMG as an iOS package. One-time setup remains `pwsh scripts\bucket-setup.ps1`.

## Architecture
- **Main process**: `electron/main.ts` → `electron/ipc/` handlers → `electron/services/` business logic
- **Preload**: `electron/preload.ts` — full typed contextBridge API (`window.api`)
- **Renderer**: `src/` — React 19 + Zustand stores + Tailwind CSS 4
- **IPC pattern**: All calls return `{ success: true, data: T } | { success: false, error: string }`
- **Database**: better-sqlite3 at `userData/lamprey.db` (WAL mode, foreign keys)
- **API keys**: Electron safeStorage → `userData/keys.json` (base64-encoded encrypted)
- **Artifact sandbox**: `WebContentsView` (not deprecated BrowserView) with CSP + sandbox isolation, vendor files in `resources/vendor/`

## Key Decisions
- `window.api` guards needed in renderer code — app must not crash outside Electron (browser dev mode)
- `skill-loader.ts` is fully implemented (Prompt 13) with chokidar hot reload, gray-matter frontmatter parsing, and dev/prod path resolution (production bootstraps `userData/skills/` from `resources/skills/`)
- `mcp-manager.ts` is fully implemented (Prompt 10) with SSE + stdio transports, Google OAuth token refresh, and auto-restart
- Tool approval lives in `electron/services/permissions-store.ts` + `electron/ipc/permissions.ts` (Phase 4). The legacy `mcp:approveToolCall` IPC is a thin shim over `permissionsService.respondLegacy`; the modern channel is `tools:approvalRequired` / `tools:respondToApproval`. Approval is gated by descriptor risk metadata on `electron/services/tool-registry.ts`, not by hard-coded server lists
- Branding: display name "Lamprey", desktop icon = green 3D lamprey (`ASSETS/Lamprey Desktop Icon-1.png`), splash screen = `ASSETS/Lamprey New Startup Splash.png` (3s duration)
- `WebContentsView` (Electron 35+) replaces deprecated `BrowserView` — uses DIP coordinates (no scaleFactor multiplication needed). **Electron is on ^43.0.0 as of the July 2026 Maintenance Phase (JM-27)** — the old ^35.7.5 pin (better-sqlite3 lacking V8 13 / Electron 36+ support) is obsolete: better-sqlite3 12.11.1 ships prebuilds through Electron ABI v146. The 35→43 jump passed the full static gate (tsc ×2, build, both smokes, full vitest 2334 passed) plus `electron-rebuild`; a live GUI smoke is the owner's final check on any future major bump.
- React 19 has no UMD builds — JSX artifacts use a custom `react-shim.js` for createElement/createRoot
- `react-markdown` v10 requires `pre` passthrough override to prevent double-wrapping CodeBlock components

## Execution Rules
1. **All shipped plans (`PLANNING/LAMPREY_HARNESS_FINAL.md`, the RAG roster, `LAMPREY_PARITY_PLAN.md`, `LAMPREY_FLUIDITY_PLAN.md`, `LAMPREY_DEEP_RESEARCH_PLAN.md`, `LAMPREY_SNIP_PLAN.md`, `LAMPREY_CUSTOMIZE_PLAN.md`, `LAMPREY_PANELS_PLAN.md`, `LAMPREY_REASONING_AUDIT_PLAN.md`, `LAMPREY_REASONING_TRACE_PLAN.md`, `LAMPREY_ROBUSTNESS_HOTFIX_PLAN.md`, and `Lamprey_Function_Calling_PSPR_.md`, and `LAMPREY_MECHANICAL_PROOF_HARNESS_PLAN.md`) are reference-only.** When the user starts a new plan, treat its §0 (or equivalent) as the source of truth for verify gates + commit discipline.

Canonical planning shorthand:
- **P-SPR = Plan - Sequential Prompt Roster.** A single canonical `PLANNING/*.md` file that defines one phase end to end: goal, scope, non-goals, ordered prompts, files, verify gates, commit/devlog discipline, completion criteria, and approval state. Pasted or drafted text becomes a P-SPR only after it is saved as that plan file.
- **STS = Stem to Stern.** After the user explicitly approves a P-SPR or says to run it STS, execute the roster in order from first prompt through phase wrap, verifying/logging/committing each prompt as specified. Do not treat a plan's own "STS authorization" wording as approval by itself. Do not skip prompts, batch prompts, push early, or reopen plan decisions unless blocked by new facts.
2. Each prompt in any active plan must pass its verify gate (both tsc configs + relevant tests + smoke checks) before being marked `[x]` and committed.
3. Log each prompt's work in `DEVLOG.md` per the format in the active plan's §0 Step 4.
4. **Push policy:** the user is the reviewer + pusher. When they explicitly ask to push (any phrasing — "push", "push to main", "push it now"), execute on the first try; the request itself satisfies the review step (see `feedback_push_when_told` memory). Do NOT volunteer pushes the user didn't ask for, and never `--force` without an explicit force-push instruction.
5. Parallel track sessions MUST run in separate git worktrees (per `feedback_parallel_session_worktree` memory). For multi-track plans, coordinate any cross-track merge-hotspot files per the active plan's protocol section.
6. **Plan before work.** For any non-trivial work, the sequence is: (a) draft a robust, comprehensive plan with a sequential numbered prompt roster (same style as the FINAL / PARITY / FLUIDITY / Sandbox / Deep-Research plans), (b) present it for review (inline or as a `PLANNING/*.md`), (c) wait for an explicit green light, (d) then commence. No code changes, builds, or commits until approved. Trivial one-offs (single edits, questions, memory saves, explicit single-command asks) are exempt. See `feedback_plan_before_work` memory.
7. **No AI slop or smelly code.** Write plain, human prose and ship the smallest correct implementation. Reject assistant voice, generated filler, placeholder comments, speculative abstractions, duplicate authorities, dead flags, wrapper-on-wrapper indirection, and formatting churn. Every abstraction needs present-day call sites; every public claim needs evidence. Every commit and maintained governance document ends with `Authored and reviewed by Basho Parks, copyright 2026`.

---

Authored and reviewed by Basho Parks, copyright 2026

---
> Source: [USS-Parks/Lamprey-Harness](https://github.com/USS-Parks/Lamprey-Harness) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-07-29 -->
