## pi

> 编码器校准完成后的速度环 PI 调参规程


# 速度环 PI 调参指南

> **前提**: 电流环校准 + KTH71 ANLC + 电角度零点标定 **全部完成**
> 未完成编码器校准就切速度模式, 电角度错误会导致力矩方向不对, PI 无论怎么调都没用。

---

## 一、Live Watch 变量清单 (复制粘贴)

开始前先把以下变量全部加到 Live Watch，后续操作直接改值。

### M1 速度环调参

```
┌─ 变量名 ──────────────── 用途 ─────────────────┐
│ g_FollowM1M2              主从随动开关 (=0)     │
│ g_VofaMotorSel            VOFA 电机选择 (=1)    │
│ g_VofaSrc                 VOFA 数据源 (=1)      │
├─ 控制 ─────────────────────────────────────────┤
│ g_M1_CtrlMode             0=开环, 1=速度        │
│ g_M1_RPM_Cmd              目标转速 (RPM)        │
│ g_M1_Iq_Ref_mA            开环力矩 (默认50mA)   │
├─ 速度 PI ──────────────────────────────────────┤
│ g_M1_SpeedPI              展开看 kp/ki/积分/输出 │
│ g_M1_RpmRampRate          斜坡速率 (默认2RPM/ms)│
├─ 反馈 ─────────────────────────────────────────┤
│ g_Enc1_SpeedFilt          编码器滤波转速 (RPM)   │
└─────────────────────────────────────────────────┘
```

### M2 速度环调参

```
┌─ 变量名 ──────────────── 用途 ─────────────────┐
│ g_FollowM1M2              主从随动开关 (=0)     │
│ g_VofaMotorSel            VOFA 电机选择 (=0)    │
│ g_VofaSrc                 VOFA 数据源 (=1)      │
├─ 控制 ─────────────────────────────────────────┤
│ g_M2_CtrlMode             0=开环, 1=速度        │
│ g_M2_RPM_Cmd              目标转速 (RPM)        │
│ g_M2_Iq_Ref_mA            开环力矩 (默认100mA)  │
├─ 速度 PI ──────────────────────────────────────┤
│ g_M2_SpeedPI              展开看 kp/ki/积分/输出 │
│ g_M2_RpmRampRate          斜坡速率 (默认2RPM/ms)│
├─ 反馈 ─────────────────────────────────────────┤
│ g_Enc2_SpeedFilt          编码器滤波转速 (RPM)   │
└─────────────────────────────────────────────────┘
```

> `Iq_Ref_mA` 切速度模式后不生效, 留着方便随时切回开环对比。
> `SpeedPI` 是结构体, Live Watch 展开可看 kp/ki/integ_limit/out_limit/integral/output。

---

## 二、速度环架构概览

```
  g_M1_RPM_Cmd / g_M2_RPM_Cmd ──→ [斜坡限速] ──→ rpm_target
                                                      │
                                                      ▼
                    ┌──────────────────────────────┐
                    │  SpeedPI_Run (1kHz, TIM3)    │
                    │                              │
                    │  error = rpm_target - speed   │
                    │  P = kp × error              │
                    │  I += ki × error (三重限幅)   │
                    │  output = P + I → Iq (mA)    │
                    └──────────────────────────────┘
                                  │
                                  ▼
                     g_M1_Idq_Ref.q / g_M2_Idq_Ref.q (Q15)
                                  │
                                  ▼
                      电流环 PI (17kHz, ADC ISR)
                                  │
                                  ▼
                              HRTIM PWM
```

**关键特征:**
- 运行频率: 1kHz (TIM3 ISR)
- 输入: RPM 误差
- 输出: Iq 电流指令 (mA)
- 斜坡限速: `g_M1_RpmRampRate` / `g_M2_RpmRampRate` RPM/ms (默认 2, 即 0→1000RPM 需 500ms)
- Anti-windup: 积分限幅 + 输出反算法退饱和 + 输出限幅 (三重保护)

---

## 三、参数含义

`SpeedPI_Float_t` 结构体 (Live Watch 展开 `g_M1_SpeedPI` 或 `g_M2_SpeedPI`):

- **kp** — 比例增益, 单位: mA/RPM
  - 含义: 每 1 RPM 误差产生多少 mA 力矩电流
  - 默认: 0.5
  - 直觉: kp=0.5 → 100RPM 误差产生 50mA Iq

- **ki** — 积分增益, 单位: mA/(RPM·ms)
  - 含义: 每 1 RPM 误差持续 1ms 积分贡献多少 mA
  - 默认: 0.01
  - 作用: 消除摩擦/齿槽导致的稳态转速偏差

- **integ_limit** — 积分限幅 (mA)
  - 默认: 100
  - 作用: 防堵转时积分飙到天花板 → 松开后巨大过冲

- **out_limit** — 输出限幅 (mA)
  - 默认: 300
  - 作用: 最大允许力矩电流, 保护电机不过热
  - 硬上限: `M1_IQ_LIMIT_MAX_MA` / `M2_IQ_LIMIT_MAX_MA` = 600 (M1/M2 各自独立定义)

- **integral** — 积分当前值 (只读诊断, VOFA I4)
- **output** — 输出当前值 (只读诊断, VOFA I5)

**斜坡相关:**
- **g_M1_RpmRampRate / g_M2_RpmRampRate** — 斜坡速率, 单位: RPM/ms, 默认 2, 范围 1~100
- **g_M1_RPM_Cmd / g_M2_RPM_Cmd** — 目标转速, 范围 ±1000 RPM

---

## 四、调参操作步骤

### 3.0 准备

1. 确认 VOFA 连接, `g_VofaSrc = 1` (NORMAL)
2. `g_VofaMotorSel` = 0(M2) 或 1(M1), 选择要调的电机
3. 当前模式为开环: `g_M1_CtrlMode = 0` / `g_M2_CtrlMode = 0`

### 3.1 切换到速度模式

Live Watch 设置:
```
g_M1_RPM_Cmd  = 0  / g_M2_RPM_Cmd  = 0       (先给 0, 防止突然起跳)
g_M1_CtrlMode = 1  / g_M2_CtrlMode = 1       (切速度模式)
```

> 切入瞬间 PI 积分自动清零, 斜坡目标从当前速度开始 — 不会有突变。

### 3.2 给目标转速, 观察 VOFA

```
g_M1_RPM_Cmd = 200  / g_M2_RPM_Cmd = 200      (从低速开始)
```

**VOFA 通道含义 (速度模式):**
- **I0** — 原始转速 (RPM), 有噪声
- **I1** — 滤波转速 (RPM), PI 实际用的反馈
- **I2** — 斜坡后目标 (RPM), PI 实际跟的指令
- **I3** — 速度误差 (RPM), = I2 - I1
- **I4** — PI 积分值 (mA), 观察积分动态
- **I5** — PI 输出 Iq (mA), 最终力矩指令

**健康波形特征:**
- I1 (滤波速度) 平滑跟上 I2 (斜坡目标)
- I3 (误差) 稳态时接近 0 (允许 ±5 RPM)
- I4 (积分) 稳态后收敛到一个小值 (补偿摩擦)
- I5 (Iq) 稳态后稳定, 不振荡

### 3.3 先调 kp (纯比例, 关积分)

```
g_M1_SpeedPI.ki = 0  / g_M2_SpeedPI.ki = 0      (暂时关掉积分)
g_M1_SpeedPI.kp = 0.5 / g_M2_SpeedPI.kp = 0.5   (默认起点)
g_M1_RPM_Cmd    = 200 / g_M2_RPM_Cmd    = 200
```

**观察 I1 (滤波速度) 的响应:**

- **响应太慢 (爬了好几百 ms 还没到)** → kp 增大, 试 1.0, 2.0
- **响应快但振荡 (I1 在目标附近来回摆)** → kp 过大, 减半
- **响应快且平滑, 有稳态偏差** → kp 合适, 下一步加 ki 消偏差

**kp 粗调参考范围 (步进电机):**
- NEMA08 (20mm): 0.3 ~ 1.0
- NEMA17 (42mm): 0.5 ~ 2.0
- NEMA23 (57mm): 0.5 ~ 3.0

> 步进电机因高电感, kp 范围偏小。三相伺服因低电感, kp 可大得多。

### 3.4 加 ki (消除稳态误差)

kp 调好后, 逐步加 ki:

```
g_M1_SpeedPI.ki = 0.005  / g_M2_SpeedPI.ki = 0.005   (从小值开始)
```

**观察:**
- I3 (速度误差) 稳态是否趋近 0
- I4 (积分) 是否缓慢收敛到一个稳定值

**调整方向:**
- **误差消除太慢 (>2 秒才到 0)** → ki 加倍, 试 0.01, 0.02
- **过冲后回摆 (I1 超过目标再落回)** → ki 太大, 减半
- **稳态震荡 (积分 I4 在 ± 来回)** → ki 远过大, 降到 1/4

**ki 典型范围:**
- 一般: 0.005 ~ 0.05
- 高摩擦负载: 可适当加大, 但 integ_limit 要配合

### 3.5 调 integ_limit (积分抗饱和)

```
g_M1_SpeedPI.integ_limit = 100  / g_M2_SpeedPI.integ_limit = 100   (默认)
```

**设置原则:**
- integ_limit 应 **远小于** out_limit
- 太大 → 堵转后积分飙高, 松开时巨大过冲 (电机猛转)
- 太小 → 积分无法补偿大摩擦, 稳态偏差消不掉
- 经验: integ_limit ≈ 1/3 × out_limit (100/300 就是这个比例)

### 3.6 调 out_limit (最大力矩)

```
g_M1_SpeedPI.out_limit = 300  / g_M2_SpeedPI.out_limit = 300   (默认)
```

- 增大 → 允许更大力矩, 加速更猛, 抗扰能力强, 但发热增加
- 减小 → 限制力矩, 更安全, 但加速慢
- **不要超过** `M1_IQ_LIMIT_MAX_MA` / `M2_IQ_LIMIT_MAX_MA` (600), while(1) 有硬限保护

### 3.7 验证: 变速测试

kp/ki 调好后, 做变速扫描:

```
g_M1_RPM_Cmd / g_M2_RPM_Cmd = 100   → 观察跟踪
g_M1_RPM_Cmd / g_M2_RPM_Cmd = 400   → 观察斜坡加速
g_M1_RPM_Cmd / g_M2_RPM_Cmd = -400  → 观察反向
g_M1_RPM_Cmd / g_M2_RPM_Cmd = 0     → 观察刹车
```

**每个阶段检查:**
- I1 平滑跟随 I2 (无大超调)
- I3 稳态 < ±5 RPM
- I9 (VqSat) 保持 0 (不饱和)

### 3.8 验证: 抗扰测试

稳态运行时用手指捏电机轴 (加负载扰动):

- I3 (误差) 应短暂偏离后被 PI 拉回
- I4 (积分) 应上升补偿负载
- 松手后速度应平滑恢复, 无大过冲

---

## 五、常见问题

- **切速度模式电机不动**
  - 原因: 编码器校准未完成, 电角度错误
  - 排查: 先回开环确认能转, 再检查零点标定

- **速度剧烈振荡 (I1 大幅摆动)**
  - 原因: kp 过大, 或编码器 INL 导致速度信号有毛刺
  - 排查: 降 kp 到 0.1 观察是否缓解; 检查 I0 (原始速度) 噪声幅度

- **稳态有固定偏差 (I3 ≠ 0)**
  - 原因: ki=0 或 ki 太小, 摩擦/齿槽力矩未被补偿
  - 排查: 增大 ki; 若已经很大但仍有偏差 → 增大 integ_limit

- **加速时过冲严重 (I1 超过 I2 很多)**
  - 原因: ki 过大 / integ_limit 过大 / 斜坡太慢导致积分积累
  - 排查: 减 ki; 减 integ_limit; 加快 RpmRampRate

- **VOFA I9 出现 1000 (Vq 饱和)**
  - 原因: 目标转速太高, 电机反电动势吃掉了大部分母线电压
  - 排查: 降低 RPM_Cmd; 或提高 Vbus

- **加负载后速度掉很多, 恢复慢**
  - 原因: out_limit 限得太紧, PI 输出饱和
  - 排查: 增大 out_limit (注意电机温度)

---

## 六、快速操作清单

### M1 速度环调参

```
┌─ 准备 ──────────────────────────────────────────┐
│ 1. g_VofaMotorSel   = 1        (选 M1)         │
│ 2. g_VofaSrc        = 1        (NORMAL)         │
├─ 开环基线 (可选, 建立对比) ────────────────────┤
│ 3. g_M1_CtrlMode    = 0        (开环)           │
│ 4. g_M1_Iq_Ref_mA   = 50       (开环力矩)      │
│ 5. g_M1_RPM_Cmd     = 200      (开环转速)       │
│    → 记录 VOFA I8(Iq_fbk), 作为速度环对比基线    │
├─ 切速度模式 ───────────────────────────────────┤
│ 6. g_M1_RPM_Cmd     = 0        (防突变)         │
│ 7. g_M1_CtrlMode    = 1        (切速度模式)      │
├─ 调 kp (关积分) ────────────────────────────────┤
│ 8. g_M1_SpeedPI.ki  = 0                        │
│ 9. g_M1_SpeedPI.kp  = 0.5      (起点)          │
│10. g_M1_RPM_Cmd     = 200      (给速度)        │
│    → 观察 I1 响应, 对比开环 Iq=50mA 的表现       │
│    → 太慢加 kp, 振荡减 kp                       │
├─ 加 ki (消偏差) ────────────────────────────────┤
│11. g_M1_SpeedPI.ki  = 0.01     (起点)          │
│12. 观察 I3→0 速度 → 太慢加 ki, 过冲减 ki        │
├─ 验证 ──────────────────────────────────────────┤
│13. g_M1_RPM_Cmd = 100 → 400 → -400 → 0        │
│14. 手捏电机轴, 观察扰动恢复                      │
└──────────────────────────────────────────────────┘
```

### M2 速度环调参

```
┌─ 准备 ──────────────────────────────────────────┐
│ 1. g_VofaMotorSel   = 0        (选 M2)         │
│ 2. g_VofaSrc        = 1        (NORMAL)         │
├─ 开环基线 (可选, 建立对比) ────────────────────┤
│ 3. g_M2_CtrlMode    = 0        (开环)           │
│ 4. g_M2_Iq_Ref_mA   = 100      (开环力矩)      │
│ 5. g_M2_RPM_Cmd     = 200      (开环转速)       │
│    → 记录 VOFA I8(Iq_fbk), 作为速度环对比基线    │
├─ 切速度模式 ───────────────────────────────────┤
│ 6. g_M2_RPM_Cmd     = 0        (防突变)         │
│ 7. g_M2_CtrlMode    = 1        (切速度模式)      │
├─ 调 kp (关积分) ────────────────────────────────┤
│ 8. g_M2_SpeedPI.ki  = 0                        │
│ 9. g_M2_SpeedPI.kp  = 0.5      (起点)          │
│10. g_M2_RPM_Cmd     = 200      (给速度)        │
│    → 观察 I1 响应, 对比开环 Iq=100mA 的表现      │
│    → 太慢加 kp, 振荡减 kp                       │
├─ 加 ki (消偏差) ────────────────────────────────┤
│11. g_M2_SpeedPI.ki  = 0.01     (起点)          │
│12. 观察 I3→0 速度 → 太慢加 ki, 过冲减 ki        │
├─ 验证 ──────────────────────────────────────────┤
│13. g_M2_RPM_Cmd = 100 → 400 → -400 → 0        │
│14. 手捏电机轴, 观察扰动恢复                      │
└──────────────────────────────────────────────────┘
```

---

## 七、默认参数速查

```
g_M1_SpeedPI / g_M2_SpeedPI:
  .kp          = 0.5     (mA/RPM)
  .ki          = 0.01    (mA/(RPM·ms))
  .integ_limit = 100     (mA)
  .out_limit   = 300     (mA)

g_M1_RpmRampRate / g_M2_RpmRampRate = 2  (RPM/ms)

M1_RPM_CMD_MAX / M2_RPM_CMD_MAX       = 1000  (RPM, 各自 #define)
M1_IQ_LIMIT_MAX_MA / M2_IQ_LIMIT_MAX_MA = 600   (mA, 各自 #define)
```

> 这些默认值对 M2 (NEMA08, 50pp) 实测可用。
> 换电机后 kp/ki 可能需要根据电机惯量和摩擦重新调整。

---
> Source: [TigerBruce/Double_SPET_FOC](https://github.com/TigerBruce/Double_SPET_FOC) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-06-25 -->
