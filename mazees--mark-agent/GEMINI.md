## mark-agent

> **Project Name:** MARK (Metacognitive Artificial Relational Knowledge) — v4.0.0

# AI Context & Planning (AGENTS.md)

## 1. Project Overview

**Project Name:** MARK (Metacognitive Artificial Relational Knowledge) — v4.0.0
**Description:** A privacy-first, local-based autonomous AI OS companion designed to assist user productivity, automate tasks, and provide lifelike companionship. It uses a hybrid AI engine (Local LLM via LM Studio or Cloud API, plus a native Gemini Web RPC Engine) and features agentic planning with ReAct loop execution, **Durable Agent Tasks** (UI: **Agent Workflows**, branding: **Mission Control**) for persistent multi-step work, autonomous physical browser automation, a hybrid Full-Text & Vector Memory Management System (MMS) with Orama & Dexie, document RAG pipeline, OS-level Awareness Engine, dynamic 4D Relational Growth, a native Plugin System with Monaco Editor, Telegram Bot integration via Telegraf, Voice Activity Detection with Groq Whisper STT, Edge-TTS, and webcam vision capabilities.
**Environment:** Electron 39 desktop application optimized for Windows (Windows 10/11).
**Author:** Mazees | **Homepage:** https://github.com/Mazees/mark-agent/

## 2. Technology Stack & Core Dependencies

- **Framework:** Electron 39, React 19, Vite 7, electron-vite 5
- **UI/Styling:** Tailwind CSS 4 (via `@tailwindcss/vite`), DaisyUI 5 (theme: `forest`), Poppins + Inter fonts, React Markdown, React Syntax Highlighter (Prism, oneDark), Monaco Editor (`@monaco-editor/react`), Driver.js (guided tours), Lucide React, React Icons
- **AI Backend:** Gemini Web RPC (Native Bridge) / Groq API / LM Studio (Local, `localhost:1234`) / Cerebras / Custom OpenAI-compatible Endpoint
- **Embeddings/Memory:** `@huggingface/transformers` (Transformers.js) for fully local embeddings via WASM (`Xenova/paraphrase-multilingual-MiniLM-L12-v2`, 384 dimensions)
- **Local Database & Vector Search:** `dexie` (IndexedDB wrapper, v14 schema with 6 stores) and `@orama/orama` for Hybrid Full-Text & Vector search
- **Web Capabilities:** Physical background `BrowserWindow` automation (`src/main/browser-agent.js`) with DOM parsing, animated cursor injection, and React-compatible input binding
- **Voice/Audio:** Groq API (Speech-to-Text via `whisper-large-v3`), Edge-TTS (`msedge-tts`, voice: `id-ID-ArdiNeural`), Web Audio API (Voice Activity Detection via `useVAD.js`, 16kHz sample rate, RMS threshold 0.015)
- **Media/Integrations:** `youtube-transcript-plus`, `ytmusic-api` (YouTube Music with ad-blaster), `yt-search`, `youtube-dl-exec` + `ffmpeg-static`
- **Communication:** `telegraf` (Telegram Bot Framework)
- **Packaging:** `electron-builder` (NSIS installer, `appId: com.mark.agent`, `asarUnpack` for ffmpeg/yt-dlp binaries)

## 3. Project Architecture & File Structure

### `src/main/` — Electron Main Process

| File                           | Purpose                     | Key Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------------ | --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `index.js`                     | Main entry point            | 40+ IPC handlers, window management, System Tray with 16x16 icon extraction, global shortcut `Ctrl+Alt+M`, TTS via `MsEdgeTTS`, YouTube search/transcript/music, `desktopCapturer` screenshots (720p), single-instance lock, Chromium GPU mitigations (`disable-gpu-process-crash-limit`, `CalculateNativeWinOcclusion`, `disable-background-timer-throttling`)                                                                                                                                                                                                                  |
| `ai-bridge.js`                 | Centralized AI HTTP client  | `fetchAI()` with multi-provider routing (Groq/Cerebras/Custom/LM Studio), 3-tier JSON format fallback (`json_schema` → `json_object` → unrestricted), rate limiting (`CLOUD_DELAY_MS=3000`), 429/503 exponential backoff with auto model swap to `openai/gpt-oss-20b`, vision payload sanitization (strips old images), DeepSeek `<think>` tag extraction, `cleanAndParse()` with `jsonrepair`, 5-minute timeout for local LLM                                                                                                                                                   |
| `browser-agent.js`             | Physical browser automation | Hidden `BrowserWindow(1280x800, show:false)`, `DOM_PARSER_SCRIPT` tagging max 80 interactive elements with `data-mark-id`, glassmorphism blocking overlay, animated SVG cursor with ripple effect on click, React 18+ compatible typing (prototype property descriptors + synthetic InputEvent), `browser-ask-user` unblock mode with title-based communication (`MARK_UNBLOCK_DONE:`), popup blocking via `setWindowOpenHandler`, 60s load timeout, base64 preview thumbnails                                                                                                   |
| `pc-agent.js`                  | Physical desktop automation | Zero-Vision cost Windows PC Automation Engine via native UIAutomation tree (`read-ui.ps1`) and local WinRT OCR fallback (`ocr-region.ps1`). Executes mouse clicks, keyboard typing (SendInput Unicode with VK_RETURN newline mapping), shortcut keys, scrolling, app opening, and window management via Win32 API (`win-action.ps1`). Exposes 9 IPC tools: `os-read`, `os-click`, `os-type`, `os-key`, `os-scroll`, `os-open`, `os-list-windows`, `os-focus-window`, `os-ask`. Features floating lock overlay and global `Ctrl+S` emergency stop with interactive reason dialog. |
| `native-tools.js`              | OS tool registry            | `NATIVE_TOOLS` object: `read-file` (line slicing, max 400 lines), `write-file`, `replace-lines`, `delete-file`, `list-dir`, `grep-search` (Windows `findstr`), `run-powershell` (with `DANGEROUS_KEYWORDS` blacklist: `Remove-Item`, `rm`, `del`, `shutdown`, etc.), browser-\* wrappers. Query format: double-pipe `\|\|` delimiter                                                                                                                                                                                                                                             |
| `awareness/window-tracker.js`  | OS activity monitoring      | `active-win` every 60s, `powerMonitor.getSystemIdleTime()` idle detection (>180s), 30-entry ring buffer, timestamps in `id-ID` locale                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `plugins/plugin-loader.js`     | Plugin lifecycle manager    | Scans `~/Documents/Mark Plugins/`, dynamic ESM import with cache-busting (`?t=Date.now()`), `plugin.json` + `index.js` structure, auto `npm install` on creation, code extraction for Monaco Editor UI, IPC endpoints for CRUD/toggle/reload                                                                                                                                                                                                                                                                                                                                     |
| `telegram/telegram-service.js` | Telegram Bot Engine         | `Telegraf` bot framework, polling mode, `/start` and `/register` admin approval flow, text and media processing, screenshot triggers, music command bridge, and direct UI notification integration.                                                                                                                                                                                                                                                                                                                                                                              |
| `services/gemini-web.js`       | Gemini Web RPC Engine       | Connects directly to Google's Gemini Web RPC backend. Supports models like `gemini-3.6-flash`, `gemini-3.5-flash-thinking`, `gemini-flash-lite`, and `gemini-auto`. Integrated directly into `ai-bridge.js`.                                                                                                                                                                                                                                                                                                                                                                     |

### `src/preload/`

| File       | Purpose                                                                                                                                                                                                                                                                                                                                                                                 |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `index.js` | `contextBridge` bridge exposing 60+ IPC methods to renderer via `window.api`: AI (fetchAI, abort, syncConfig), System (screenshot, notification, activity buffer), Media (TTS, YouTube, Music), WhatsApp (start/stop/QR/message/admin), Native Tools (execute, approval check), Plugins (CRUD, execute), Browser Automation (navigate/read/action/close/show/preview), Document Parsing |

### `src/renderer/src/api/` — Core Logic Layer

| File              | Purpose                       | Key Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `db.js`           | Dexie database (v14 schema)   | 6 stores: `memory` (type, memory, vector, timestamp), `sessions` (id, title, data), `config` (25+ fields including personality, aiProvider, groqApiKey, awarenessEnabled, cameraDeviceId, cameraEnabled), `chatArchive` (summary, timestamp, topic, vector), `documents` (docName, chunkIndex, content, vector), `relationships` (userId, warmth, sarcasm_level, trust, energy, evalCount). 19 exported functions. `VALID_TYPES`: profile/preference/notes/learn. `DEFAULT_TRAITS`: all 0.5 |
| `vectorMemory.js` | Embedding engine              | Model: `Xenova/paraphrase-multilingual-MiniLM-L12-v2` (384d). `generateVector()` (max 512 tokens), `cosineSimilarity()`, `getRelevantMemory()` (core: profile+preference), `searchExtendedMemory()` (notes+learn, threshold **0.3**, top 3, auto-heals dimension mismatches), `getUnifiedContext()` (merges core + Orama archives top 3 + documents top 5)                                                                                                                                  |
| `oramaStore.js`   | Hybrid vector+fulltext search | `archiveIndex` + `documentIndex` (both 384d vectors). `hydrateFromDexie()` on boot with migration flag. `searchArchives()` (vector similarity, threshold **0.25**). `searchDocuments()` (hybrid term+vector, threshold **0.25**). `findSimilarMemoryClusters(threshold)` (groups similar profile & preference memories). Insert/delete operations synced with Dexie                                                                                                                         |
| `ragPipeline.js`  | Document ingestion            | `ingestDocument()`: max 50MB, `.pdf`/`.docx` via IPC `parseDocument` (mammoth/pdf-parse), `.txt`/`.md` raw read. `splitTextIntoChunks()` (500 chars, 50 overlap). Dual-save to Dexie + Orama. Progress callback. Duplicate replacement                                                                                                                                                                                                                                                      |
| `scraping.js`     | Web scraping engine           | `scrapeGoogle()`: CAPTCHA detection, AI overview + top 5 organic results. `deepSearch()`: DOM cleanup (strips nav/footer/ads/scripts), extracts article text, 1500 char cap                                                                                                                                                                                                                                                                                                                 |
| `groq.js`         | Groq STT client               | `pcmToWav()` Float32→16-bit WAV. `transcribeAudioGroq()` via OpenAI client pointing to Groq, model `whisper-large-v3`, Indonesian language hints                                                                                                                                                                                                                                                                                                                                            |

### `src/renderer/src/api/ai/` — AI Brain

| File                | Purpose                     | Key Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `core.js`           | AI IPC wrapper              | `fetchAI()` with `AbortSignal` support via IPC. `cleanAndParse()`: multi-stage JSON parser (strip markdown fences → extract `{}` substring → clean control chars/trailing commas → `jsonrepair` fallback)                                                                                                                                                                                                                                                                                                                                         |
| `planning.js`       | Agentic planner & router    | **`CATEGORY_TEXTS`** (7 categories: coding/files/music/search/system/browser/capabilities) with vector matching (threshold **0.35**). `getNextAction()`: assembles dynamic system prompt with persona, tools, plugins, memory context. Truncates history by provider (128k custom, 4k standard). Enforces JSON schema response: `thought`, `action` (tool+query), `answer`, `mood`, `active_topic`, `memory` (save/update/delete). `MAX_RETRIES=2`. **YouTube tools (`yt-search`, `yt-summary`) are permanently grouped with core browser tools** |
| `persona.js`        | Character & personality     | `getPersonaPrompt()`: Windows Electron PC identity, Jarvis vibe, sarcasm scaling (`<0.65` = subtle sarcasm, `>=0.65` = full roasting), adaptive tone (lu/gue vs Saya/Anda), TTS formatting (no comma before name), 10 mood types. `getTraitContext()`: injects 4D trait values into prompt. `describeLevel()`: maps 0-1 → descriptive labels                                                                                                                                                                                                      |
| `relationship.js`   | Relational growth evaluator | `evaluateTraitDrift()`: `TRAIT_KEYS` = warmth/sarcasm_level/trust/energy. `MAX_DRIFT=0.01` per evaluation step. `FLOOR` = warmth:0.15, trust:0.15. Clamps values 0-1. Returns reasoning + optional relational memory note                                                                                                                                                                                                                                                                                                                         |
| `awareness.js`      | Autonomous awareness AI     | `getAwarenessResponse()`: evaluates OS activity buffer, memory refs, current time, music state. Prompt includes capabilities list (natural language, no tool names), ethics rules (don't disturb focused user, proactively play music when idle, don't repeat observations). JSON schema output: `should_act`, `message`, `autonomous_prompt`, `mood`                                                                                                                                                                                             |
| `chatSummarizer.js` | Chat archival               | `summarizeAndArchive()`: distills recent messages into 2-3 sentence summary via LLM, generates embedding vector, dual-saves to Dexie `chatArchive` + Orama `archiveIndex`                                                                                                                                                                                                                                                                                                                                                                         |
| `memoryGroomer.js`  | Memory Groomer Engine       | `buildGroomerPrompt(clusters)`, `parseGroomerResponse(raw)`, `runBatchConsolidation()`. Merges chronological profile & preference memories without losing history                                                                                                                                                                                                                                                                                                                                                                                 |
| `tools.js`          | YouTube & Music AI          | `getYoutubeSummary()`: fetches transcript via IPC, chunks at 4000 chars, 12s delay between chunk summaries (Groq TPM protection). `getBestMusicMatch()`: AI ranker selects best song from search results                                                                                                                                                                                                                                                                                                                                          |
| `utils.js`          | Shared utilities            | `getCurrentTimeInfo()` (Indonesian locale), `playVoice()` (TTS via IPC, sets `window.isMarkSpeaking`), `formatForWhatsApp()` (custom `marked` renderer → WA native formatting: `*bold*`, `_italic_`, `` `code` ``)                                                                                                                                                                                                                                                                                                                                |

### Durable Agent Tasks / Agent Workflows

- `taskClassifier.js` routes requests to `direct`, `ephemeral`, or `durable` using Gemini Flash Lite.
- `taskPlanner.js` creates a bounded multi-step outline for durable work.
- `taskStore.js` persists tasks and steps in Dexie, including checkpoint, retry, pause/resume, cancel, validation, artifact path, and content hash.
- `taskExecutor.js` centralizes deterministic single-step validation/checkpoint decisions without duplicating the ReAct tool dispatcher.
- `useMarkPlan.js` remains the ReAct executor; durable mode wraps the existing loop and advances one active step at a time.
- Artifacts are written under `Documents/Mark Tasks/<task-id>/` through the approved `write-file` native tool.
- `ProcessPanel` shows live progress and failed durable executions; persistent task controls are planned for a dedicated task panel.

### `src/renderer/src/hooks/` — React Hooks (Agent Orchestration)

| File                           | Purpose                                     | Key Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------ | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `useAwareness.js`              | Periodic autonomous check-in                | `CHECKIN_INTERVAL=10min`, `INITIAL_DELAY=1min`, **9-minute cooldown** (`lastCheckInRef`). Reads activity buffer → fetches vector memory → calls `getAwarenessResponse()` → dispatches autonomous action via `handlePlanningCommand` or pushes proactive chat bubble. OS notification. Orb `'nudge'` animation (3s)                                                                                                                                                                                                                                                                                                                      |
| `useChatArchiver.js`           | Auto-archival on completion                 | `MIN_MESSAGES_TO_ARCHIVE=10` (5 conversation pairs). Triggers when `isLoading` transitions false→true→false. Filters out system status items. Calls `summarizeAndArchive()`                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `useConfirm.jsx`               | Promise-based confirmation dialog           | Returns `{ confirm(config), ModalComponent }`. Resolves `{ isConfirmed: boolean }`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `useMemoryGroomer.js`          | Hippocampus Engine Orchestrator             | Auto-grooming scheduled 10s after startup and manual trigger via Memory Map. Uses global schedule flag and shared event listener Set for UI reactivity                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `useMarkAgent.js`              | Top-level facade hook                       | Coordinates YouTube, Music, Planning, Awareness, Relational Growth, Chat Archiver, Telegram subsystems. Boot greeting on `isChatLoaded`. TG event listener (`tg-admin-message`). TG response forwarding. Camera capture ref. `handleSubmit()` with mid-execution intervention support                                                                                                                                                                                                                                                                                                                                                   |
| `useVAD.js`                    | Voice Activity Detection                    | 16kHz sample rate, 4096 buffer, RMS speech threshold **>0.015**, 2s silence timeout, min speech 0.5s (8000 samples), trailing silence trim 1.5s (24000 samples, prevents Whisper hallucinations). Sends Float32 PCM to `transcribeAudioGroq()`                                                                                                                                                                                                                                                                                                                                                                                          |
| `agent/useMarkPlan.js`         | **Core autonomous loop engine** (787 lines) | `handlePlanningCommand()`: prepares session history → injects activity buffer + unified vector context → runs `while(!isDone)` loop with `getNextAction()`. Tool dispatch: YT search/summary, Music (play/next/prev/toggle/search with AI ranker), TG send/screenshot, Memory CRUD, Speech TTS, Vision (`analyze-screen` multi-monitor, `camera-look` webcam), Browser automation (`navigate/read/click/type/scroll/ask-user/close`), File CRUD, PowerShell, Plugins. Security approval via `checkToolApproval()`. Intervention buffer for mid-execution user messages. Proactive message merging. Active task state routing injection. |
| `agent/useMarkMusic.js`        | Music player control                        | `handleMusic(action, query)`: dispatches `music-next/prev/toggle` to YT Music context. For `music-play`: searches via IPC → AI ranker `getBestMusicMatch()` → `playUrl()`. For `music-search`: renders result card in chat                                                                                                                                                                                                                                                                                                                                                                                                              |
| `agent/useMarkState.js`        | Centralized state management                | 15 state variables (chatData, config, message, isLoading, isAgentBusy, isSpeak, orbStatus, currentResponse, notifications, activeProcesses, inputSource, activeTopic, isChatLoaded, isBooting, sessionId). Auto-save chatData to Dexie. Config reload listener                                                                                                                                                                                                                                                                                                                                                                          |
| `agent/useMarkYoutube.js`      | YouTube search & summary                    | `handleYoutubeSearch()` via IPC. `getYoutubeData()` via oEmbed. `handleYoutubeSummary()` calls `getYoutubeSummary()`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `agent/useRelationalGrowth.js` | Trait drift evaluation                      | Triggers every **15 clean messages**. Loads relationship → `evaluateTraitDrift()` → `saveRelationship()` → optionally `insertMemory()` for relational notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `telegram/useTelegramBot.js`   | Telegram bot UI state                       | IPC listeners for connection, thinking, message, reply. `startBot()/stopBot()/logout()`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

### `src/renderer/src/pages/` — Application Pages

| Page                   | Lines | Purpose                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `MarkHome.jsx`         | 304   | Main dashboard: OrbVisualizer, ThoughtNeuralFlow, ResponseArea (TLDR + HoloCard), InputBar, FloatingMenu, StatusIndicator, ProcessPanel, BrowserPreviewWidget, YT Music overlay with album art & visualizer bars                                                                                                                                                         |
| `Configuration.jsx`    | 1381  | Settings: AI provider selection (LM Studio/Groq/Cerebras/Custom), API keys, model selection, temperature/context sliders, TTS rate/pitch with test playback, camera device selector with live preview, Awareness toggle, memory management (view/edit/delete), relational trait meters with reset, TG admin approval queue, chat export/clear. Driver.js onboarding tour |
| `Knowledge.jsx`        | 236   | Document RAG: file upload (.pdf/.txt/.md/.docx) with progress bar, document list with chunk counts, dual-cleanup deletion (Dexie + Orama)                                                                                                                                                                                                                                |
| `LiveAudio.jsx`        | 464   | Real-time voice interface: Web Audio API, VAD with barge-in detection (interrupts AI speech), 1.2s silence detection, Groq Whisper STT, auto TTS playback, animated pulse rings & waveform bars                                                                                                                                                                          |
| `Guidebook.jsx`        | 915   | 7-section documentation: system overview, ReAct architecture diagram, tool catalog (ToolCard components), Awareness Engine docs, emotion/relational growth docs, plugin creation guide with code sample, FAQ accordion                                                                                                                                                   |
| `Plugins.jsx`          | 598   | Plugin manager: card grid with toggle/edit/folder, creation modal with Monaco Editor, real-time syntax validation (`AsyncFunction` constructor), driver.js tour, auto `npm install`                                                                                                                                                                                      |
| `RelationalGrowth.jsx` | 295   | Trait visualization: 4 TraitRing SVG circular gauges, personality summary text, reasoning log, eval stats, trait reset                                                                                                                                                                                                                                                   |
| `TelegramBot.jsx`      | 157   | Telegram bot dashboard: connection status dot, config inputs, chat thread with Markdown rendering & tool badges                                                                                                                                                                                                                                                          |

### `src/renderer/src/components/` — UI Components

| Component                        | Purpose                  | Key Details                                                                                                                                                                                                                                 |
| -------------------------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `core/ResponseArea.jsx`          | AI response display      | 200ms/300ms fade transitions, short response (centered text) vs long response (TLDR + expandable HoloCard), Markdown with custom CodeBlock, PluginExecutionBubble                                                                           |
| `core/HoloCard.jsx`              | Sci-fi expandable card   | Glassmorphic bg + backdrop-blur, animated holo-border-flow, corner accent borders, scanline overlay, expand/collapse (200px → 60vh) with overflow detection                                                                                 |
| `YoutubeMusicPlayer.jsx`         | Music player widget      | Floating bottom-right, Electron `<webview>` loading `music.youtube.com`, **Ad-Blaster script** (500ms check: mutes ads, 16x speed, auto-skip, dismiss idle popups), IPC playback control listeners, CSS injection hiding ads/premium/upsell |
| `GlobalCameraManager.jsx`        | Camera capture bridge    | Promise-based: connects `requestCameraCaptureRef` from ChatContext to CameraPreview. Resolves with base64 JPEG or null on cancel                                                                                                            |
| `camera/CameraPreview.jsx`       | Webcam capture modal     | `getUserMedia`, countdown timer, resolution scaling (max 1280x720), 70% JPEG quality, white flash on capture. **Autonomous mode**: invisible 1x1 container, 800ms auto-snap for background AI vision                                        |
| `Chat/CodeBlock.jsx`             | Code syntax highlighting | `react-syntax-highlighter` (Prism, oneDark theme), clipboard copy with 2s feedback                                                                                                                                                          |
| `Chat/PluginExecutionBubble.jsx` | Tool execution display   | Collapsible `<details>` accordion showing action name, query, and output                                                                                                                                                                    |

### `src/renderer/src/contexts/` — React Contexts

| Context                   | Purpose                                                                                                                                                      |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `ChatContext.jsx`         | Wraps `useMarkAgent()` → provides all agent state & methods to entire app tree                                                                               |
| `ApprovalContext.jsx`     | Promise-based security modal for dangerous tool approvals (file write, PowerShell, etc.)                                                                     |
| `YoutubeMusicContext.jsx` | YouTube Music state: webview ref, 1s DOM polling for track metadata (title/artist/thumbnail/pause state), `playUrl()`, `nextTrack()/prevTrack()/playPause()` |

### `src/renderer/src/App.jsx` — Root Application

- **Boot Sequence**: `initOramaIndices()` → `hydrateFromDexie()` → download embedding model (`getExtractor` with progress) → `getAllConfig()` → `syncConfig` to main process
- **Routes**: `/` (MarkHome), `/config`, `/plugins`, `/live-audio`, `/whatsapp-bot`, `/knowledge`, `/guidebook`, `/relational`
- **Global Components**: `YoutubeMusicPlayer` (hidden on WA page), `GlobalCameraManager`, hidden `<webview id="global-ai-search-webview">` for background search
- **Global Listeners**: `onLiveAudioShortcut` (Ctrl+Alt+M), `onWaAdminRequest`, `route-to-config`, `onWaRequestAgentExecution`

### Root Config Files

| File                       | Key Settings                                                                                             |
| -------------------------- | -------------------------------------------------------------------------------------------------------- |
| `package.json`             | v4.0.0, Electron 39, React 19, 50+ deps                                                                  |
| `electron.vite.config.mjs` | `@renderer` alias, Tailwind v4 plugin, dev port 5173, `host: true`                                       |
| `electron-builder.yml`     | `appId: com.mark.agent`, NSIS installer, `asarUnpack` for ffmpeg/yt-dlp                                  |
| `index.html`               | DaisyUI `forest` theme, CSP, Inter + Poppins fonts                                                       |
| `main.css`                 | Tailwind v4 + DaisyUI, custom markdown styles, orb/holo animations, driver.js theme, glass design tokens |

## 4. Key Implementation Invariants & Gotchas

### Critical Constants & Thresholds

| Constant                   | Value                     | Location                 | Purpose                             |
| -------------------------- | ------------------------- | ------------------------ | ----------------------------------- |
| Vector Similarity (Memory) | **0.3**                   | `vectorMemory.js`        | Filter irrelevant extended memories |
| Vector Similarity (Orama)  | **0.25**                  | `oramaStore.js`          | Archive & document search threshold |
| Category Router            | **0.35**                  | `planning.js`            | Intent matching for tool exposure   |
| Trait Drift                | **0.01** max              | `relationship.js`        | Max trait change per evaluation     |
| Trait Floor                | **0.15** (warmth, trust)  | `relationship.js`        | Minimum allowed trait values        |
| Awareness Cooldown         | **9 minutes**             | `useAwareness.js`        | Prevents check-in spam              |
| Awareness Interval         | **10 minutes**            | `useAwareness.js`        | Check-in loop period                |
| Chat Archive Threshold     | **10 messages**           | `useChatArchiver.js`     | Min messages before archiving       |
| Relational Eval Interval   | **15 clean messages**     | `useRelationalGrowth.js` | Trait evaluation trigger            |
| Cloud Rate Limit Delay     | **3000ms**                | `ai-bridge.js`           | Min gap between cloud API calls     |
| Activity Buffer Size       | **30 entries**            | `window-tracker.js`      | Ring buffer cap                     |
| WA Message Store           | **50 per JID**            | `message-store.js`       | Per-chat memory limit               |
| DOM Parser Elements        | **80 max**                | `browser-agent.js`       | Max interactive elements tagged     |
| RAG Chunk Size             | **500 chars, 50 overlap** | `ragPipeline.js`         | Document chunking params            |
| YT Summary Chunk           | **4000 chars**            | `tools.js`               | Transcript chunk boundary           |
| VAD Speech Threshold       | **RMS > 0.015**           | `useVAD.js`              | Voice detection sensitivity         |
| VAD Silence Timeout        | **2000ms**                | `useVAD.js`              | Auto-cut after silence              |

### Model Selection & Persona Guidelines

- **Recommended Models for Indonesian Persona:**
  - ✅ **Llama 3 (3.1 / 3.3)** — Best for slang ("lu/gue"), banter, sarcasm, and emotional range
  - ✅ **Qwen 2.5** — Excellent Asian language understanding, natural Indonesian expression
  - ⚠️ **Mistral** — **NOT recommended**. Exhibits severe butler/formal bias in Indonesian and is prone to autoregressive repetition loops in long single-session contexts
- **Single-Session Design:** Mark uses a persistent single-session chat (never auto-resets). This amplifies LLM pattern-matching bias over time. If the AI becomes repetitive/kaku, clearing chat history (`clearChat`) is the remedy.

### JSON Parsing Resilience

`ai-bridge.js` implements a 3-tier JSON response format fallback:

1. `json_schema` (structured output) → if provider rejects:
2. `json_object` (response format) → if validation fails:
3. Unrestricted format with schema injected into system prompt

Additionally, `cleanAndParse()` in both `ai-bridge.js` (main) and `core.js` (renderer) applies multi-stage parsing: direct parse → strip markdown → extract substring → clean control chars → `jsonrepair`. **Do not remove these fallback parsers.**

### Browser Automation Architecture

- The browser agent creates a **physical hidden Chromium window** (`show: false`), NOT a headless browser.
- `DOM_PARSER_SCRIPT` injects `data-mark-id` attributes on up to 80 interactive elements and creates a full-screen glassmorphism blocking overlay.
- Click actions animate a custom SVG cursor to the target element before clicking.
- Type actions use prototype property descriptors (`HTMLInputElement.prototype`) to work with React 18+ and other reactive frameworks.
- `browser-ask-user` pauses automation, shows the hidden window, and presents an overlay card for manual user input. Completion is signaled via `document.title = 'MARK_UNBLOCK_DONE:...'`.
- Popup/new-window blocking via `setWindowOpenHandler` returning `{ action: 'deny' }`.

### Plugin System

- Plugins live in `~/Documents/Mark Plugins/` with `plugin.json` manifest + `index.js` handler.
- Dynamic ESM import with `?t=Date.now()` cache-busting for hot-reload.
- Plugin actions are exposed to the AI planner via vector similarity matching against `CATEGORY_TEXTS`.
- The Plugins page provides a Monaco Editor for live code editing with real-time syntax validation.

## 5. Development Guidelines for AI Agents

- **Read Before Modify:** Always read the corresponding `api/` or `hooks/` file entirely before changing state or UI logic. The codebase has deep interconnections (e.g., `useMarkPlan.js` alone is 787 lines with tool dispatch logic for 15+ tool types).
- **Maintain Privacy-First Paradigm:** Avoid adding third-party tracking, analytics, or mandatory cloud dependencies. Everything must be able to fall back to a 100% offline local state.
- **Robust Error Handling:** Do not assume APIs will always return `200 OK`. Implement robust error handling, especially for rate limits (429), traffic errors (503), and offline scenarios. The existing retry logic in `ai-bridge.js` handles exponential backoff with automatic model swapping.
- **Formatting:** Write clean, consistent code and preserve any existing user comments and docstrings.
- **Electron Process Boundary:** Never mix Node.js native APIs (like `fs`, `path`) directly in the `renderer/` folder. All OS-level interactions must pass through `preload/index.js` → IPC → `main/`.
- **UI Design System:** The UI uses Tailwind CSS 4 + DaisyUI 5 (`forest` theme) with custom glassmorphism/holographic design tokens defined in `main.css`. Do not introduce ad-hoc CSS files. Use existing design tokens (`--glass-bg`, `--glass-border`, `--color-holo-border`).
- **Single-Session Chat:** Mark's chat is persistent and never auto-resets. Be cautious about operations that could corrupt the main thread in Dexie (`sessions` table, id: 1).

---
> Source: [Mazees/mark-agent](https://github.com/Mazees/mark-agent) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-08-12 -->
