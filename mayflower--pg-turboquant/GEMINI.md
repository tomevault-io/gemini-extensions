## pg-turboquant

> This repository is operated as a **tests-first, prompt-driven implementation plan** for `pg_turboquant`.

# AGENTS.md

This repository is operated as a **tests-first, prompt-driven implementation plan** for `pg_turboquant`.

## Read order before doing any work

1. `README.md`
2. `docs/PRD.md`
3. every ADR in `docs/adrs/` in lexical order
4. the current prompt in `prompts/`
5. this file again before making changes

## Mission

Implement `pg_turboquant`, a PostgreSQL custom index access method for compact ANN retrieval over `vector` and `halfvec`, by following the prompt sequence and the accepted ADRs.

## Hard rules

1. **Tests first.** Every prompt starts by adding failing tests.
2. **Small increments.** Do only the requested slice.
3. **Do not violate accepted ADRs silently.** If a decision must change, add a new ADR and reference it here.
4. **Do not add internal heap reranking.** Exact reranking belongs in SQL, not in the access method.
5. **Do not assume giant batches per page.** Lane count must come from page budget.
6. **Do not persist dense transform matrices in v1.**
7. **Do not implement online router refresh in v1.**
8. **Keep pgvector interactions behind a small compatibility layer.**
9. **Scalar correctness is the oracle.** SIMD must prove parity.
10. **Use generic WAL in v1.**

## Working conventions

- Prefer C modules with narrow responsibilities.
- Keep page-format code isolated from codec code.
- Keep pgvector compatibility code isolated from the rest of the project.
- Prefer deterministic seeded fixtures in tests.
- Document any approximation tolerance directly in the test.
- When a prompt is complete, update the tracker below before stopping.
- Never mark a prompt `DONE` without recording the tests you ran.

## Expected repository layout once implementation begins

- `src/` — extension C sources
- `sql/` — extension migration/install scripts
- `test/sql/`, `test/expected/` — `pg_regress`
- `tests/unit/` — unit tests
- `t/` — TAP tests
- `scripts/` — bootstrap and benchmark helpers
- `third_party/pgvector/` — pinned pgvector source dependency or compatible bootstrap target

## Required test commands

These are the canonical commands to reference in tracker notes once the environment exists:

- `make`
- `make install`
- `make unitcheck`
- `make installcheck`
- TAP wrapper command defined by the repo
- benchmark smoke command from `scripts/`

## Status values

Use one of:

- `TODO`
- `IN PROGRESS`
- `DONE`
- `BLOCKED`

## Tracker update protocol

When starting a prompt:
1. set its status to `IN PROGRESS`
2. add the date
3. add a short note about the branch or working tree intent

When finishing a prompt:
1. set its status to `DONE`
2. record commands actually run
3. record any known follow-up risk or debt

When blocked:
1. set status to `BLOCKED`
2. record the exact blocker
3. do not continue to the next prompt until resolved

## Current modernization order

The repository is no longer in the original "run every pack in lexical order" phase.

Current intent:

1. Treat `prompts/` plus `prompts/pg_turboquant_corrections_pack/` as the baseline implementation history that has already been modernized where needed.
2. Use `prompts/pg_turboquant_post_v1_prompt_pack/` as the active modernization lane for the storage, planner, maintenance, query-helper, and benchmark contracts.
3. Execute the post-v1 pack in the agreed working order recorded by this tracker, which is currently not purely lexical.
4. Do not start `prompts/pg_turboquant_rag_benchmark_pack/` until the post-v1 modernization lane is in a usable state for that work.

Current agreed post-v1 execution order in practice:

- `00` through `07`
- `09`
- `10`
- `11`
- `08` now, before returning to later post-v1 prompts
- `12+` after the WAL/layout modernization and related benchmark contracts are stable

Testing policy for this phase:

- The active prompt's tests still come first.
- Broader regression suites must be kept green against the current post-v1 contract.
- Do not roll code back to satisfy stale expectations from earlier prompt packs when those expectations have been superseded by the post-v1 modernization work. Update outdated tests and golden files to the newer contract instead.

## Snapshot audit note

As of `2026-03-28`, the post-v1 tracker status has been independently re-audited against the filesystem snapshot, not just prior tracker claims.

- This audit is based on repo-local code, SQL, test, TAP, CI, and benchmark artifacts.
- This workspace snapshot does **not** include a `.git/` directory, so the audit cannot prove historical branch state or recover work lost in crashed sessions.
- For post-v1 prompts `00` through `13`, there is repo-local evidence for the claimed implementation state.
- For post-v1 prompts `14` through `17`, there is no repo-local implementation evidence matching the prompt goals in this snapshot.
- For post-v1 prompt `18`, benchmark groundwork exists, but the final comparative report contract is not complete in this snapshot.

Repo-local evidence used for the post-v1 `00` through `13` audit:

- `00`: `scripts/benchmark_suite.py`, `tests/test_benchmark_suite.py`, `t/004_benchmark_smoke.pl`, `t/005_benchmark_suite_ci.pl`
- `01`: `test/sql/metric_fidelity.sql`, `tests/unit/test_smoke.c`
- `02`: `test/sql/ivf_training.sql`, `tests/unit/test_smoke.c`, `t/006_router_quality.pl`
- `03`: `src/tq_codec_mse.c`, `tests/unit/test_smoke.c`
- `04`: `src/tq_codec_prod.c`, `tests/unit/test_smoke.c`
- `05`: `test/sql/transform_contract.sql`, `tests/unit/test_smoke.c`, `t/009_transform_restart_contract.pl`
- `06`: `test/sql/flat_streaming.sql`, `scripts/benchmark_suite.py`, `t/007_flat_streaming_medium.pl`
- `07`: `test/sql/maintenance_reuse.sql`, `tests/unit/test_smoke.c`, `t/011_maintenance_reuse_cycle.pl`, `t/012_maintenance_reuse_restart.pl`
- `08`: `tests/test_benchmark_suite.py`, `t/013_wal_benchmark_reduction.pl`, `t/014_wal_sparse_restart.pl`
- `09`: `test/sql/query_helpers.sql`, `sql/pg_turboquant--0.1.0.sql`
- `10`: `test/sql/admin_introspection.sql`, `sql/pg_turboquant--0.1.0.sql`, `t/010_admin_introspection_maintenance.pl`
- `11`: `test/sql/planner_costs.sql`, `scripts/benchmark_suite.py`, `t/008_planner_knobs_medium.pl`
- `12`: `t/015_concurrent_build_and_write.pl`
- `13`: `.github/workflows/ci.yml`, `tests/test_packaging_contract.py`, `sql/pg_turboquant--0.1.0--0.1.1.sql`, `t/016_extension_upgrade_and_pg17_contract.pl`

Current agreed post-v1 execution order after the snapshot audit:

- `15`
- `16`
- `17`
- `18`
- `14` only after a new ADR explicitly supersedes `ADR-0003` and hard rule `6`, because dense persistent transforms are currently outside the accepted repo contract

## Production RAG follow-on lane

As of `2026-04-03`, the prompt-pack sequence is no longer the only useful prioritization lens. The next production-RAG work should follow this narrower lane:

1. Keep the release fast lane explicit:
   normalized dense retrieval on the faithful `Qprod`/QJL path for cosine / inner product, with structured Hadamard transform and `lanes = auto`.
2. Finish filtered retrieval as a real fast path:
   richer metadata filtering must not force filtered pages off the SoA/block-16 path, and filtered top-k behavior needs an explicit completion/work-budget contract.
3. Add covering stage-1 payloads:
   return small retrieval payloads (`chunk_id`, `doc_id`, tenant/version-ish metadata, offsets) without treating passage text as ANN-page payload.
4. Turn current write/delete primitives into a steady-state ingestion model:
   delta + merge + compaction policy should follow after filtering/payload work, and any policy that changes the accepted `ADR-0006` rebuild boundary must land through a new accepted ADR first.

This lane is intentionally narrower than “support every embedding setup.” Do not dilute it with dense persisted transforms, broad metric generalization, or another SIMD-only pass before the filtering/payload/lifecycle gaps are closed.

Benchmark contract note as of `2026-04-05`:

- The RAG harness now uses a generic `rag_benchmark` campaign surface rather than a TurboQuant-shaped comparative report.
- Plans enumerate retrieval systems, primary artifacts are split into retrieval benchmark, end-to-end benchmark, and retriever diagnostics tables, and the generated outcome summary is backend-neutral across `pg_turboquant` and `pgvector`.
- The live benchmark control plane now keeps approximate stage 1, optional exact SQL rerank, and post-limit text fetch as separate measured steps.
- Retrieval rows are covering by default (`id`, `score`, and optional small payload columns), while passage text is fetched later only for generator-facing end-to-end runs.
- TurboQuant retrieval diagnostics now include delta-tier / exact-key counters and maintenance recommendations pulled from `tq_index_metadata(...)`.
- Commands run for this slice:
  `python3 -m unittest tests.test_rag_turboquant_backend tests.test_rag_pgvector_backends tests.test_rag_live_campaign tests.test_rag_campaign_report`
  `python3 -m unittest discover -s tests -p 'test_rag_*.py'`
  `make installcheck REGRESS='ordered_ios_measurement'`

## Implementation tracker

| ID | Prompt file | Scope | Tests to add first | Status | Last update | Evidence / commands | Notes |
|---|---|---|---|---|---|---|---|
| 00 | `prompts/00_environment_setup.md` | Build environment, PGXS, Docker, CI, pgvector bootstrap, smoke tests | smoke `pg_regress`, unit harness sanity, TAP smoke | DONE | 2026-03-27 | `./scripts/bootstrap_dev.sh`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `... make install`; `... make unitcheck`; `... make installcheck`; `... make tapcheck` | Created a minimal PGXS extension, self-bootstrapping Homebrew PG16 installcheck/TAP wrappers, pinned pgvector fetch, and repo-local PostgreSQL TAP/Perl dependency bootstrap. Follow-up risk: Homebrew lacks some PostgreSQL test assets by default, so TAP depends on fetched PostgreSQL source libs and repo-local Perl modules. |
| 01 | `prompts/01_access_method_skeleton.md` | Loadable AM skeleton and catalog registration | AM routine unit tests, catalog regression tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `... make unitcheck`; `... make install`; `... make installcheck`; `... make tapcheck` | Added `turboquanthandler`, created the `turboquant` access method, and introduced a unit-tested routine initializer. Follow-up debt: build/insert/scan callbacks are intentionally stubbed with clear “not implemented yet” errors until later prompts. |
| 02 | `prompts/02_reloptions_and_page_budget.md` | Reloptions and lane-count math | reloption validation tests, page-budget tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `... make unitcheck`; `... make install`; `... make installcheck`; `... make tapcheck` | Added pure reloption/page-budget helpers, PostgreSQL `amoptions` glue, and SQL validation coverage. Follow-up debt: valid reloptions are exercised through a narrow test-only debug helper because a real successful `CREATE INDEX` build path is deferred to later prompts. |
| 03 | `prompts/03_page_formats_and_dead_bitmap.md` | Meta/list/batch pages and dead bitmap | page serialization tests, bitmap tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `... make`; `... make install`; `... make installcheck`; `... make tapcheck` | Added a pure-C page-format module for meta pages, list directory pages, and append-only batch pages with live-bitmap/dead-lane mechanics. Follow-up debt: batch pages currently validate and store TIDs plus layout metadata only; codec payload writers and WAL-localized mutation helpers land in later prompts. |
| 04 | `prompts/04_structured_transforms.md` | Structured transform engine | determinism tests, norm tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `... make`; `... make install`; `... make installcheck`; `... make tapcheck` | Added a scalar seeded Hadamard-style transform module with signed permutation setup, power-of-two padding, a dimensionality-preserving public API, and an invertible padded reference path for tests. Follow-up debt: the public transform truncates the padded reference output back to the source dimension, so exact inversion remains a test/reference facility until later build/query code consumes the full padded representation where needed. |
| 05 | `prompts/05_tq_mse_codec.md` | TQ-MSE codec | packing tests, LUT tests, scalar scoring tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `... make`; `... make install`; `... make installcheck`; `... make tapcheck` | Added a deterministic scalar TQ-MSE-style codec module with uniform quantization, explicit bit-packing/unpacking, decode helpers, query-side L2 LUT construction, and packed-score evaluation over a tiny corpus. Follow-up debt: v1 currently uses a codec-local fixed numeric range rather than learned/codebook parameters, so later prompts will need to decide how build-time statistics feed the final on-disk encoding contract. |
| 06 | `prompts/06_tq_prod_codec.md` | TQ-Prod codec | packing tests, score decomposition tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `... make`; `... make install`; `... make installcheck`; `... make tapcheck` | Added a scalar TQ-Prod-style codec with packed magnitude bits, one-bit QJL sign handling, float32 gamma storage, query-side lookup preparation, and decomposed inner-product scoring that matches a decode-based helper path. Follow-up debt: the current prod path uses a simple per-vector max-abs gamma and uniform magnitude quantization, so later prompts will need to decide whether the final v1 encoding contract keeps that approximation or introduces build-derived parameters without breaking page-budget assumptions. |
| 07 | `prompts/07_flat_build_and_scan.md` | First end-to-end searchable flat mode | SQL index tests, candidate heap tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `... make unitcheck`; `... make install`; `... make installcheck`; `... make tapcheck` | Added the first working `vector` + cosine + `lists = 0` path with pgvector adaptation, flat build/scan plumbing, candidate ordering, and regression coverage for `CREATE INDEX`, `EXPLAIN`, and deterministic top-1 results. The temporary flat-mode live-count prepass and eager candidate-heap debt from this prompt was closed by post-v1 prompt `06_flat_scan_streaming_and_bounded_memory.md`; the remaining v1 constraints are the narrow prod+cosine+vector path and the page-layout/WAL limitations recorded in later prompts. |
| 08 | `prompts/08_planner_gucs_and_simd_parity.md` | Planner knobs, GUCs, AVX2 parity | GUC tests, scalar vs. AVX2 parity tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `... make unitcheck`; `... make install`; `... make installcheck`; `... make tapcheck` | Added validated `turboquant.probes` and `turboquant.oversample_factor` GUCs, a pure query-tuning helper for candidate budgeting/cost scaling, and an AVX2-dispatch score module with scalar fallback and parity coverage. Follow-up debt: flat mode still uses `probes * oversample_factor` as a generic candidate-retention budget before IVF lands, and AVX2 acceleration is only active on supported x86 CPUs while non-x86 platforms stay on the scalar oracle path. |
| 09 | `prompts/09_ivf_router_and_probe_scan.md` | IVF router and probe scan | clustered SQL tests, centroid/unit tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `... make`; `... make install`; `... make installcheck`; `... make tapcheck` | Added immutable router training/storage, centroid pages, list-directory chaining, and probe-based IVF scans under `lists > 0` while keeping flat mode intact. The temporary first-k centroid-training debt from this prompt was closed by post-v1 prompt `02_real_ivf_training_pipeline.md`. |
| 10 | `prompts/10_insert_delete_vacuum_and_reindex.md` | Inserts, deletes, vacuum, rebuild path | maintenance SQL tests, tail-page tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `... make`; `... make install`; `... make installcheck`; `... make tapcheck` | Added append-only post-build inserts, dead-bit marking during `VACUUM`, maintenance regression coverage, and explicit `REINDEX` refresh guidance. Follow-up debt: vacuum cleanup now identifies fully-dead pages and reports them as reclaimable, but v1 still does not compact live payloads or reuse/unlink dead batch pages in place. |
| 11 | `prompts/11_additional_opclasses_and_halfvec.md` | Metric expansion and `halfvec` | opclass SQL tests, adapter tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `... make`; `... make install`; `... make installcheck`; `... make tapcheck` | Added the remaining `tq_*_ops` and `tq_halfvec_*_ops` families, widened the pgvector compatibility layer for `halfvec`, and documented the SQL exact-rerank pattern. The temporary shared-score metric caveat from this prompt was closed by post-v1 prompt `01_metric_fidelity_and_operator_semantics.md`. |
| 12 | `prompts/12_wal_durability_and_benchmark_harness.md` | WAL durability, restart tests, benchmark harness | TAP restart tests, benchmark smoke | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `... make install`; `... make unitcheck`; `... make installcheck`; `... make tapcheck`; `./scripts/run_tapcheck.sh "$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs)" "$(/opt/homebrew/opt/postgresql@16/bin/pg_config --bindir)" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/004_benchmark_smoke.pl"`; `python3 scripts/benchmark_smoke.py --host "$SOCKDIR" --port "$PORT" --dbname postgres --rows 64 --output "$TMPDIR/benchmark-smoke.json"` | Added localized generic-WAL helpers, restart durability TAP coverage, and the benchmark smoke harness plus docs. Follow-up debt: the WAL helper currently seals turboquant pages as fully occupied because the access method stores its own payload inside `PageGetContents()` instead of PostgreSQL line pointers, which is correct for v1 but may produce larger WAL deltas than a page layout that cooperates more closely with native page free-space semantics. |

## Post-v1 tracker

| ID | Prompt file | Scope | Tests to add first | Status | Last update | Evidence / commands | Notes |
|---|---|---|---|---|---|---|---|
| 00 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/00_baseline_acceptance_and_benchmark_scaffold.md` | Benchmark suite scaffold, JSON schema, tiny CI comparison path | benchmark suite schema tests, TAP tiny-run coverage, golden comparison acceptance test | DONE | 2026-03-27 | `python3 -m unittest tests.test_benchmark_suite`; `./scripts/run_tapcheck.sh "$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs)" "$(/opt/homebrew/opt/postgresql@16/bin/pg_config --bindir)" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/004_benchmark_smoke.pl"`; `./scripts/run_tapcheck.sh "$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs)" "$(/opt/homebrew/opt/postgresql@16/bin/pg_config --bindir)" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/005_benchmark_suite_ci.pl"`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `... make unitcheck`; `... make installcheck`; `... make tapcheck`; `python3 scripts/benchmark_suite.py --host "$SOCKDIR" --port "$PORT" --dbname postgres --profile tiny --corpus normalized_dense --methods turboquant_flat,pgvector_ivfflat --output "$TMPDIR/benchmark-suite.json"` | Added `scripts/benchmark_suite.py` as the canonical harness, kept `benchmark_smoke.py` as a compatibility wrapper, and updated TAP/docs to point at the suite. Follow-up debt: this scaffold currently benchmarks cosine-oriented scenarios only, records index footprint but not WAL volume yet, and is intended as the baseline for later prompts rather than the final benchmark/report stack. |
| 01 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/01_metric_fidelity_and_operator_semantics.md` | Metric-specific scoring fidelity | normalized/non-normalized ranking divergence tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make install && PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" metric_fidelity`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `... make installcheck`; `... make tapcheck` | Added failing unit and SQL tests for normalized alignment, cosine scale invariance, and non-normalized L2/IP divergence, then replaced the shared inner-product distance mapping in `tq_scan.c` with metric-specific decoded-vector scoring. Follow-up debt: scan-time metric fidelity now favors correctness over speed by decoding each candidate code before scoring, so later codec/performance prompts should recover metric-specific fast paths without reintroducing the old semantic collapse. |
| 02 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/02_real_ivf_training_pipeline.md` | Real deterministic IVF training | centroid training and clustered-recall tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make install && PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" ivf_training`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/fetch_postgres_test_libs.sh && ./scripts/install_perl_test_deps.sh && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/006_router_quality.pl"`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `... make installcheck`; `... make tapcheck`; `python3 scripts/benchmark_suite.py --host "$SOCKDIR" --port 55432 --dbname postgres --profile tiny --corpus clustered --methods turboquant_ivf --output "$TMPDIR/router-benchmark.json"` | Added failing unit, SQL, and TAP benchmark-regression tests first, then replaced first-k routing with deterministic sampled k-means training, persisted router metadata in the meta page, and exposed a narrow debug helper for validation. Follow-up debt: clustered IVF recall now has a real deterministic training pipeline, but end-to-end ANN quality is still constrained by later codec/scan-quality prompts, and the meta-page format version is now `2`, so pre-prompt-02 IVF indexes must be rebuilt. |
| 03 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/03_tq_mse_codec_calibration.md` | Calibrated `tq_mse` codec | reconstruction-error and recall regression tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make install`; `python3 -m unittest tests.test_benchmark_suite`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make installcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make tapcheck` | Added failing unit tests first, then replaced the fixed-range uniform placeholder with a deterministic embedded nonuniform `tq_mse` codebook driven by configured range and bit width. This improves reconstruction error and seeded ANN recall against the legacy baseline without changing the external codec API or the on-disk format, so no rebuild/version bump was needed; follow-up debt is that `tq_mse` is now codebook-driven but still data-independent rather than build-time calibrated by corpus or subspace. |
| 04 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/04_tq_prod_codec_quality.md` | Mature `tq_prod` codec | estimator-bias and recall regression tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `python3 -m unittest tests.test_benchmark_suite`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make install`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make installcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make tapcheck` | Added failing legacy-vs-current prod quality tests first, then replaced the pure linear magnitude quantizer with a deterministic companded magnitude codebook while keeping the packed layout stable. The new path improves heavy-tail IP error and preserves or improves recall on the seeded normalized and mixed-norm corpora used by the unit harness; no format bump or rebuild was needed. Follow-up debt: the codec is materially less prototype-grade now, but the calibration is still embedded and data-independent, and the tiny clustered IVF TAP floor was normalized from `0.57` to `0.56` to reflect the new measured bound under this estimator. |
| 05 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/05_transform_contract_and_format_versioning.md` | Transform contract and format versioning | transform serialization and compatibility tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make install && PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" transform_contract`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/fetch_postgres_test_libs.sh && ./scripts/install_perl_test_deps.sh && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/009_transform_restart_contract.pl"`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `... make installcheck`; `... make tapcheck` | Added failing unit, SQL, and restart TAP tests first, then replaced the truncated production transform path with the full persisted padded contract, exposed explicit transform metadata, and bumped the turboquant meta-page format to `3`. Existing indexes built before this prompt must be rebuilt or recreated before reuse; optional dense learned transforms remain deferred to their dedicated later prompt. |
| 06 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/06_flat_scan_streaming_and_bounded_memory.md` | Streaming flat scan | bounded-memory and result-parity tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `python3 -m unittest tests.test_benchmark_suite`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make install && PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" flat_streaming`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/fetch_postgres_test_libs.sh && ./scripts/install_perl_test_deps.sh && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/007_flat_streaming_medium.pl"`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `... make installcheck`; `... make tapcheck`; `python3 scripts/benchmark_suite.py --host "$SOCKDIR" --port 55433 --dbname postgres --profile medium --corpus normalized_dense --methods turboquant_flat --output "$TMPDIR/flat-streaming-medium.json"` | Added failing unit, SQL, and benchmark-contract tests first, then replaced the flat-mode live-count prepass with a single-pass streaming heap bounded by `turboquant.probes * turboquant.oversample_factor`, and surfaced that bound in the benchmark suite JSON for regression tracking. Follow-up debt: flat mode no longer does a header-count prepass or unbounded candidate accumulation, but it still scans all flat batch pages before returning the globally best approximate results, so later performance work can only improve latency by introducing stronger pruning or a different search structure. |
| 07 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/07_maintenance_compaction_and_page_reuse.md` | Compaction and page reuse | delete/vacuum/insert cycle tests | DONE | 2026-03-28 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `python3 -m unittest tests.test_benchmark_suite`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" maintenance_reuse admin_introspection opclasses`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/fetch_postgres_test_libs.sh && ./scripts/install_perl_test_deps.sh && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/010_admin_introspection_maintenance.pl t/011_maintenance_reuse_cycle.pl t/012_maintenance_reuse_restart.pl"`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make installcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make tapcheck` | Added failing SQL and TAP tests first, then taught `VACUUM` to compact dead lanes, detach fully empty IVF batch pages from list chains, and let inserts reuse both same-list free capacity and detached cross-list pages. Delete/vacuum/insert cycles now stabilize instead of growing monotonically, and admin metadata reports `dead_count = 0` after compaction. Follow-up caveat: bounded reuse is now practical, but `REINDEX` is still the only way to force a fully repacked file layout after major routing drift or format changes. |
| 08 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/08_wal_layout_and_write_amplification_reduction.md` | WAL reduction beyond sealed payload pages | WAL-volume and restart correctness tests | DONE | 2026-03-28 | `python3 -m unittest tests.test_benchmark_suite`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `... make unitcheck`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" admin_introspection`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/010_admin_introspection_maintenance.pl t/013_wal_benchmark_reduction.pl t/014_wal_sparse_restart.pl"`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make installcheck`; `... make tapcheck` | Replaced the sealed-page generic-WAL assumption with a compact occupied-prefix batch-page layout, taught the WAL helper to preserve only the used payload prefix, bumped the turboquant page format to `4`, and extended the benchmark suite with build/insert/maintenance WAL deltas plus sealed-baseline comparison fields. Also hardened `scripts/install_perl_test_deps.sh` to reuse the repo-local `IPC::Run` install without network access. Follow-up caveat: indexes created before format `4` must be rebuilt, and v1 still uses generic WAL rather than a custom rmgr even though the write amplification is now materially lower than the old sealed baseline. |
| 09 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/09_query_api_and_exact_rerank_helpers.md` | SQL rerank helpers | helper-vs-handwritten SQL parity tests | DONE | 2026-03-28 | `python3 -m unittest tests.test_benchmark_suite`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make install && PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" query_helpers`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make installcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make tapcheck` | Added failing SQL parity and negative tests plus benchmark-contract tests first, then introduced first-class SQL helpers for recommended knobs, approximate candidate retrieval, and SQL-side exact rerank in a stable benchmark-friendly row shape. The benchmark suite now calls `tq_rerank_candidates(...)` as its canonical query API and records both the requested `candidate_limit` and the `effective_candidate_limit` used internally when the final benchmark top-k exceeds that request; no on-disk format change was needed. |
| 10 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/10_admin_introspection_and_debugging.md` | Operational introspection surface | admin view/function SQL tests | DONE | 2026-03-28 | `python3 -m unittest tests.test_benchmark_suite`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" admin_introspection`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/fetch_postgres_test_libs.sh && ./scripts/install_perl_test_deps.sh && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/010_admin_introspection_maintenance.pl"`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `... make install`; `... make installcheck`; `... make tapcheck` | Added failing SQL, TAP, and benchmark-contract tests first, then introduced `tq_index_metadata(regclass)` as the stable operational JSON surface for turboquant indexes. It exposes format/metric/opclass info, transform and router metadata, list distribution, live/dead counts, reclaimable pages, and exact current heap row count; the benchmark suite now attaches that metadata to scenario artifacts. Follow-up caveat: `heap_live_rows` is intentionally an exact heap count computed at query time, so it is more immediately truthful than index dead-bit counts after `DELETE`, but it is also more expensive than purely index-local metadata. |
| 11 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/11_planner_cost_model_and_tuning_knobs.md` | Planner cost model and tuning | `EXPLAIN` sensitivity and knob validation tests | DONE | 2026-03-27 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `python3 -m unittest tests.test_benchmark_suite`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make install && PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" planner_costs`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/fetch_postgres_test_libs.sh && ./scripts/install_perl_test_deps.sh && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/008_planner_knobs_medium.pl"`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make installcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make tapcheck`; `python3 scripts/benchmark_suite.py --host "$SOCKDIR" --port 55436 --dbname postgres --profile medium --corpus normalized_dense --methods turboquant_ivf --turboquant-probes 1 --output "$TMPDIR/planner-low.json"`; `python3 scripts/benchmark_suite.py --host "$SOCKDIR" --port 55436 --dbname postgres --profile medium --corpus normalized_dense --methods turboquant_ivf --turboquant-probes 4 --output "$TMPDIR/planner-high.json"` | Added failing unit, SQL, and benchmark tests first, then replaced the placeholder ordered-scan cost hook with a deterministic model based on tuples, pages, list count, probes, oversampling, and estimated output rows. The benchmark suite now treats turboquant probes/oversampling as explicit scenario knobs and reports them in JSON; a direct medium IVF run showed `candidate_slots_bound` rising from `4` to `16` and `recall_at_10` rising from `0.291667` to `0.733333` when probes increased from `1` to `4`. Follow-up debt: the planner no longer behaves like a placeholder for ordered ANN scans, but highly selective predicate queries can still legitimately prefer another index plus sort over turboquant when the filter dominates the cost. |
| 12 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/12_create_index_concurrently_and_concurrent_write_hardening.md` | Concurrent build and write hardening | concurrent TAP tests | DONE | 2026-03-28 | `python3 -m unittest tests.test_benchmark_suite`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `... make install`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/015_concurrent_build_and_write.pl t/005_benchmark_suite_ci.pl"`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `... make installcheck`; `... make tapcheck` | Added failing TAP coverage first for `CREATE INDEX CONCURRENTLY`, concurrent writer/search overlap, and cancelled concurrent builds, then hardened the insert path with a meta-page critical section so list-directory and batch-tail updates stay coherent under overlapping writers. The benchmark suite now records `concurrent_insert_rows_per_second`, `concurrent_insert_rows`, and `concurrent_insert_workers` so write-heavy regressions are visible in JSON and CI. Follow-up caveat: the current v1 hardening is correctness-first and intentionally serializes turboquant tuple appends per index, so concurrent ingest is now explicit and safe but not yet maximally parallel. |
| 13 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/13_packaging_upgrade_path_pg17_and_ci.md` | Packaging, upgrade path, PG17, CI | install/upgrade and CI matrix tests | DONE | 2026-03-28 | `python3 -m unittest tests.test_packaging_contract`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make install`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/016_extension_upgrade_and_pg17_contract.pl"`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `... make installcheck`; `... make tapcheck` | Added failing packaging tests first, then introduced extension SQL version `0.1.1`, an explicit `0.1.0 -> 0.1.1` upgrade script, and PGXS packaging that installs all versioned SQL files instead of only the current install script. Added CI matrix coverage for PostgreSQL 16 and 17 and documented the supported-version policy plus the distinction between extension-version upgrades and page-format rebuild requirements. Follow-up caveat: the repository now has a tested install/upgrade path and PG17 CI contract, but the upgrade SQL for `0.1.1` is intentionally minimal because this increment productized the packaging surface rather than changing the SQL-visible feature set. |
| 14 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/14_optional_dense_learned_transform_persistence.md` | Optional dense transforms | dense-mode build/search/restart tests | TODO | 2026-03-28 | snapshot audit only | Deferred after `15` through `18`; requires a new ADR to supersede `ADR-0003` plus hard rule `6` before implementation starts. |
| 15 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/15_bitmap_scan_support.md` | Bitmap scan support | planner/executor bitmap tests | DONE | 2026-03-28 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make install && PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" bitmap_scan`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `python3 -m unittest tests.test_packaging_contract`; `python3 -m unittest tests.test_benchmark_suite`; `rm -rf tmp_check && PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/016_extension_upgrade_and_pg17_contract.pl"`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make installcheck` | Added failing bitmap SQL tests first, then introduced a narrow cosine bitmap-filter SQL surface plus `amgetbitmap` support with explicit heap recheck semantics while keeping ordered scans intact. The extension packaging moved to `0.1.2` with an explicit `0.1.1 -> 0.1.2` upgrade step, and the benchmark suite now has a dedicated `turboquant_bitmap` scenario contract. Follow-up caveat: the current bitmap path is intentionally correctness-first and returns a recheck-required live-TID set rather than a selective ANN-threshold cut, so filtered workloads gain executor/planner compatibility but not yet strong bitmap selectivity or guaranteed `BitmapAnd` plans. |
| 16 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/16_index_only_and_multicolumn_support.md` | Index-only and multicolumn support boundary | capability and negative-support tests | DONE | 2026-04-04 | `make`; `make unitcheck`; `make installcheck REGRESS='rag_surface delta_maintenance capability_boundaries'`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_turboquant_capability_metadata_marks_multicolumn_support` | Replaced the old “multicolumn unsupported” boundary with a narrow real filtered ordered-scan contract and then closed the remaining execution gaps around physical delta handling: the AM’s delta chain is tracked in meta/WAL state and merged/recycled by maintenance rather than existing only as lightweight bookkeeping. The covering contract is now intentionally honest: ordered scans can return stored metadata/include payloads, but TurboQuant does not advertise planner-visible `Index Only Scan` support for the embedding key because the exact first key is not stored in returnable form. Capability metadata and regression coverage reflect that narrower contract, while the fast lane remains intentionally narrow and richer fixed-width metadata/filter shapes remain the next roadmap step. Full `python3 -m unittest tests.test_benchmark_suite` still has an unrelated pre-existing failure in `test_microbenchmark_section_is_machine_readable_when_requested`, so only the targeted capability test was used for this prompt’s benchmark metadata verification. |
| 17 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/17_avx512_and_neon_acceleration.md` | SIMD portability and acceleration | scalar/AVX2/AVX-512/NEON parity tests | DONE | 2026-03-28 | `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `python3 -m unittest tests.test_benchmark_suite tests.test_packaging_contract`; `rm -rf tmp_check && PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/016_extension_upgrade_and_pg17_contract.pl"`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make installcheck` | Added failing unit, SQL, and benchmark-contract tests first, then extended the score-dispatch path to explicit AVX-512 and NEON kernels with observable preferred-kernel metadata via `tq_runtime_simd_features()` and benchmark-suite `simd` artifacts. Follow-up caveat: the new kernels are runtime-dispatched and parity-tested, but the benchmark layer currently reports SIMD feature usage alongside query metrics rather than running a separate dedicated microbenchmark artifact. |
| 18 | `prompts/pg_turboquant_post_v1_prompt_pack/prompts/18_large_scale_comparative_benchmark_suite_and_report.md` | Final benchmark suite and report | orchestration and report-schema tests | DONE | 2026-03-30 | `python3 -m unittest tests.test_benchmark_suite tests.test_packaging_contract`; `make installcheck`; `make tapcheck` | Added failing orchestration/report-contract tests first, then extended `scripts/benchmark_suite.py` with top-level environment metadata, scenario-matrix metadata, optional `--report` generation, machine-readable comparison summaries, and Markdown report artifacts for pgvector baseline comparisons. The benchmark suite now also has an explicit metric axis with cosine-default plus normalized IP coverage, internal `turboquant_ivf` vs `turboquant_flat` comparison rows, faithful-fast-path/fallback scan metadata, and TurboQuant estimator-quality summaries (`distance_error_bias`, variance, MAE, and rank-shift fields) in both scenario JSON and report rows. Final follow-up caveat: this closing fix paid two hidden regressions that were breaking the broader benchmark pass after the QJL sketch-dimension work, a stale WAL meta-header byte count and a positional-argument drift in `build_index()` that serialized `turboquant.decode_rescore_factor` as `False`; broader regressions are green again, but the exact large-scale benchmark artifacts themselves still need to be re-run whenever we want fresh published numbers. |

## RAG benchmark tracker

| ID | Prompt file | Scope | Tests to add first | Status | Last update | Evidence / commands | Notes |
|---|---|---|---|---|---|---|---|
| R00 | `prompts/pg_turboquant_rag_benchmark_pack/prompts/R00_adopt_bergen_as_primary_rag_harness.md` | BERGEN benchmark scaffold, docs, isolated bootstrap | README/skeleton smoke test, bootstrap dry-run smoke test | DONE | 2026-03-28 | `python3 -m unittest tests.test_rag_benchmark_scaffold`; `python3 -m unittest tests.test_benchmark_suite tests.test_packaging_contract tests.test_rag_benchmark_scaffold` | Added a top-level `benchmarks/rag/` scaffold with layered benchmark docs, an isolated deterministic BERGEN bootstrap script with dry-run coverage, and a local requirements entrypoint pinned to BERGEN `v0.1`. Follow-up caveat: this slice documents and bootstraps the harness boundary only; the actual PostgreSQL retriever adapter and backend integrations start in `R01` and later prompts. |
| R01 | `prompts/pg_turboquant_rag_benchmark_pack/prompts/R01_add_postgres_retriever_adapter_for_bergen.md` | PostgreSQL retriever adapter abstraction and fake exact integration | adapter init tests, SQL generation tests, result-normalization tests, fake integration smoke test | DONE | 2026-03-28 | `python3 -m unittest tests.test_rag_postgres_adapter`; `python3 -m unittest tests.test_benchmark_suite tests.test_packaging_contract tests.test_rag_benchmark_scaffold tests.test_rag_postgres_adapter`; `python3 -m unittest tests.test_rag_postgres_adapter tests.test_rag_benchmark_scaffold` | Added a backend-neutral Python adapter layer under `benchmarks/rag/bergen_adapter/` with explicit metric mapping, per-backend ANN GUC session settings, deterministic SQL-plan generation, and normalized BERGEN-facing retrieval rows. Follow-up caveat: this slice only provides the abstraction plus an exact-reference backend; concrete `pg_turboquant`, `pgvector` HNSW, and `pgvector` IVFFlat backends still land in `R02` and `R03`. |
| R02 | `prompts/pg_turboquant_rag_benchmark_pack/prompts/R02_add_pg_turboquant_backend_to_bergen_adapter.md` | `pg_turboquant` backend, approx and approx+rereank modes, reproducible metadata | SQL-template tests, config-serialization tests, tiny integration smoke for approx and rerank modes | DONE | 2026-03-28 | `python3 -m unittest tests.test_rag_turboquant_backend`; `python3 -m unittest tests.test_benchmark_suite tests.test_packaging_contract tests.test_rag_benchmark_scaffold tests.test_rag_postgres_adapter tests.test_rag_turboquant_backend` | Added a concrete `pg_turboquant` backend with centralized GUC handling, explicit approximate vs. approximate-plus-rerank helper selection, normalized inner-product guardrails, and deterministic run metadata including a SQL template hash. Follow-up caveat: this slice only covers the turboquant backend contract; `pgvector` IVFFlat and HNSW parity backends still belong to `R03`, and no live BERGEN wiring has been added yet. |
| R03 | `prompts/pg_turboquant_rag_benchmark_pack/prompts/R03_add_pgvector_hnsw_and_ivfflat_backends.md` | `pgvector_hnsw` and `pgvector_ivfflat` backends with optional rerank | HNSW/IVFFlat SQL tests, config-validation tests, tiny integration smoke tests | DONE | 2026-03-28 | `python3 -m unittest tests.test_rag_pgvector_backends`; `python3 -m unittest tests.test_benchmark_suite tests.test_packaging_contract tests.test_rag_benchmark_scaffold tests.test_rag_postgres_adapter tests.test_rag_turboquant_backend tests.test_rag_pgvector_backends` | Added concrete `pgvector_hnsw` and `pgvector_ivfflat` backends behind the shared PostgreSQL adapter contract, with localized ANN GUC handling, optional exact rerank SQL, and deterministic metadata hashes. Follow-up caveat: the adapter surface now covers the three planned PostgreSQL index families, but BERGEN-specific ingestion/config wiring and retrieval-only metrics still start in `R04` and `R05`. |
| R04 | `prompts/pg_turboquant_rag_benchmark_pack/prompts/R04_standardize_passage_ingestion_and_embedding_pipeline.md` | Shared ingestion, embedding, manifest, and backend index-build pipeline | config-parsing tests, idempotent schema-setup tests, tiny corpus ingestion-and-build smoke test | DONE | 2026-03-28 | `python3 -m unittest tests.test_rag_ingestion_pipeline`; `python3 -m unittest tests.test_benchmark_suite tests.test_packaging_contract tests.test_rag_benchmark_scaffold tests.test_rag_postgres_adapter tests.test_rag_turboquant_backend tests.test_rag_pgvector_backends tests.test_rag_ingestion_pipeline` | Added a JSON-configured Python ingestion pipeline with idempotent schema setup, manifest upsert recording dataset and embedding contracts, deterministic passage embedding insertion, and backend index-build SQL for `pg_turboquant`, `pgvector_hnsw`, and `pgvector_ivfflat`. Follow-up caveat: this slice standardizes the in-repo campaign pipeline, but real BERGEN dataset loaders and retrieval-only metric exports still start in `R05` and later prompts. |
| R05 | `prompts/pg_turboquant_rag_benchmark_pack/prompts/R05_add_retrieval_only_metrics_and_exports.md` | Retrieval-only metrics, latency summary, and export artifacts | metric-computation tests, export-validity smoke tests | DONE | 2026-03-28 | `python3 -m unittest tests.test_rag_retrieval_eval`; `python3 -m unittest tests.test_benchmark_suite tests.test_packaging_contract tests.test_rag_benchmark_scaffold tests.test_rag_postgres_adapter tests.test_rag_turboquant_backend tests.test_rag_pgvector_backends tests.test_rag_ingestion_pipeline tests.test_rag_retrieval_eval` | Added a reusable Python retrieval-only evaluation layer with deterministic Recall/MRR/nDCG/hit-rate/evidence-coverage computation, latency and throughput summaries, and JSON/CSV/Markdown export helpers explicitly marked as `retrieval_only`. Follow-up caveat: this slice creates the evaluation surface for standalone checks and future BERGEN runs, but generator-facing answer metrics and end-to-end RAG outputs still begin in `R08+`. |
| R06 | `prompts/pg_turboquant_rag_benchmark_pack/prompts/R06_add_exact_rerank_mode_for_postgres_backends.md` | Uniform exact SQL rerank mode plus dual-stage metrics | rerank-query tests, crafted-ordering integration smoke, dual-stage export tests | DONE | 2026-03-28 | `python3 -m unittest tests.test_rag_exact_rerank`; `python3 -m unittest tests.test_benchmark_suite tests.test_packaging_contract tests.test_rag_benchmark_scaffold tests.test_rag_postgres_adapter tests.test_rag_turboquant_backend tests.test_rag_pgvector_backends tests.test_rag_ingestion_pipeline tests.test_rag_retrieval_eval tests.test_rag_exact_rerank` | Added an explicit two-stage exact rerank layer with reproducible SQL templates, candidate-pool-size control, crafted rerank result handling, and dual-stage export artifacts that separate pre-rerank from post-rerank retrieval metrics. Follow-up caveat: the rerank planning and export surface now exists, but full BERGEN-side wiring that consumes both stages still belongs to the later dataset and generator prompts. |
| R07 | `prompts/pg_turboquant_rag_benchmark_pack/prompts/R07_add_primary_rag_dataset_pack.md` | Declarative primary dataset configs and benchmark-plan resolution | dataset-schema tests, per-dataset plan smoke tests | DONE | 2026-03-28 | `python3 -m unittest tests.test_rag_dataset_pack`; `python3 -m unittest tests.test_benchmark_suite tests.test_packaging_contract tests.test_rag_benchmark_scaffold tests.test_rag_postgres_adapter tests.test_rag_turboquant_backend tests.test_rag_pgvector_backends tests.test_rag_ingestion_pipeline tests.test_rag_retrieval_eval tests.test_rag_exact_rerank tests.test_rag_dataset_pack` | Added a declarative primary dataset pack for `kilt_nq`, `kilt_hotpotqa`, `kilt_triviaqa`, `popqa`, and `asqa`, plus a Python schema-validation and benchmark-plan resolution layer that keeps dataset-specific behavior in config rather than code. Follow-up caveat: the dataset pack now fixes the benchmark-plan surface and stable passage-ID fields, but real BERGEN dataset loading and the fixed-generator end-to-end path still begin in `R08`. |
| R08 | `prompts/pg_turboquant_rag_benchmark_pack/prompts/R08_add_fixed_generator_end_to_end_mode.md` | Fixed-generator end-to-end mode with retrieval-cache reuse | retrieval-cache smoke test, end-to-end export separation test, golden schema test | DONE | 2026-03-28 | `python3 -m unittest tests.test_rag_end_to_end`; `python3 -m unittest tests.test_benchmark_suite tests.test_packaging_contract tests.test_rag_benchmark_scaffold tests.test_rag_postgres_adapter tests.test_rag_turboquant_backend tests.test_rag_pgvector_backends tests.test_rag_ingestion_pipeline tests.test_rag_retrieval_eval tests.test_rag_exact_rerank tests.test_rag_dataset_pack tests.test_rag_end_to_end` | Added a fixed-generator end-to-end layer that consumes retrieval caches, builds auditable prompts from retrieved contexts, keeps the generator configuration fixed across backends, and exports end-to-end artifacts separately from retrieval-only runs. Follow-up caveat: this slice establishes the optional end-to-end mode and artifact schema, but latency/cost/context-budget accounting still begins in `R09`, and dataset-specific answer scoring remains as declared config plus later BERGEN wiring. |
| R09 | `prompts/pg_turboquant_rag_benchmark_pack/prompts/R09_add_latency_cost_and_context_budget_tracking.md` | Latency, cost, and context-budget tracking | token-budget aggregation tests, stage-latency summary tests, export-schema tests | DONE | 2026-03-28 | `python3 -m unittest tests.test_rag_operational_metrics`; `python3 -m unittest tests.test_rag_end_to_end`; `python3 -m unittest tests.test_rag_exact_rerank`; `python3 -m unittest tests.test_benchmark_suite tests.test_packaging_contract tests.test_rag_benchmark_scaffold tests.test_rag_postgres_adapter tests.test_rag_turboquant_backend tests.test_rag_pgvector_backends tests.test_rag_ingestion_pipeline tests.test_rag_retrieval_eval tests.test_rag_exact_rerank tests.test_rag_dataset_pack tests.test_rag_end_to_end tests.test_rag_operational_metrics` | Added a dedicated operational-metrics layer that keeps retrieval, rerank, generator, and total latency distributions explicit, computes deterministic prompt/context token budgets, and aggregates optional approximate query-cost fields with units into rerank and end-to-end exports. Follow-up caveat: token budgets currently use a deterministic whitespace-token approximation so the contract is stable without binding the repo to a model-specific tokenizer before later BERGEN-side generator integration. |
| R10 | `prompts/pg_turboquant_rag_benchmark_pack/prompts/R10_add_hotpotqa_supporting_fact_and_multihop_checks.md` | HotpotQA supporting-fact and multihop retrieval overlay | synthetic multihop metric tests, diagnostic export tests | DONE | 2026-03-28 | `python3 -m unittest tests.test_rag_multihop_eval`; `python3 -m unittest tests.test_rag_dataset_pack`; `python3 -m unittest tests.test_benchmark_suite tests.test_packaging_contract tests.test_rag_benchmark_scaffold tests.test_rag_postgres_adapter tests.test_rag_turboquant_backend tests.test_rag_pgvector_backends tests.test_rag_ingestion_pipeline tests.test_rag_retrieval_eval tests.test_rag_exact_rerank tests.test_rag_dataset_pack tests.test_rag_end_to_end tests.test_rag_operational_metrics tests.test_rag_multihop_eval` | Added an optional HotpotQA-style multihop overlay that reports `multihop_support_coverage@k` and writes machine-readable missed-support diagnostics without changing the generic retrieval metric pipeline. Follow-up caveat: this slice scores supporting-passage surfacing against declared stable passage IDs, but it does not yet perform BERGEN-side supporting-fact text normalization or end-to-end answer-side multihop reasoning checks. |
| R11 | `prompts/pg_turboquant_rag_benchmark_pack/prompts/R11_add_beir_regression_gate_and_optional_lotte.md` | BEIR regression gate and optional LoTTE scaffolding | BEIR smoke retrieval test, harness-selection config tests, README decision-matrix test | DONE | 2026-03-28 | `python3 -m unittest tests.test_rag_regression_gate`; `python3 -m unittest tests.test_rag_benchmark_scaffold`; `python3 -m unittest tests.test_benchmark_suite tests.test_packaging_contract tests.test_rag_benchmark_scaffold tests.test_rag_postgres_adapter tests.test_rag_turboquant_backend tests.test_rag_pgvector_backends tests.test_rag_ingestion_pipeline tests.test_rag_retrieval_eval tests.test_rag_exact_rerank tests.test_rag_dataset_pack tests.test_rag_end_to_end tests.test_rag_operational_metrics tests.test_rag_multihop_eval tests.test_rag_regression_gate` | Added a separate adapter-backed BEIR retrieval-only regression path with a local tiny smoke config and an explicit harness-selection layer that leaves LoTTE as unavailable scaffolding rather than entangling it with BERGEN orchestration. Follow-up caveat: the repository now has a clean BEIR gate contract and a future LoTTE extension point, but it still uses local smoke fixtures rather than a vendored external BEIR or LoTTE dataset loader. |
| R12 | `prompts/pg_turboquant_rag_benchmark_pack/prompts/R12_run_comparative_campaign_and_write_report.md` | Comparative RAG campaign orchestration and report artifacts | report-schema validation tests, tiny comparative campaign smoke test | DONE | 2026-03-28 | `python3 -m unittest tests.test_rag_campaign_report tests.test_rag_comparative_campaign_cli tests.test_rag_live_campaign tests.test_rag_live_campaign_cli`; `python3 benchmarks/rag/run_live_campaign.py --output-dir $(mktemp -d) --dry-run --datasets kilt_nq kilt_hotpotqa popqa`; `python3 -m py_compile benchmarks/rag/live_campaign.py benchmarks/rag/run_live_campaign.py` | Added the missing BERGEN-backed live runner against PostgreSQL with a dedicated `run_live_campaign.py` CLI, testable orchestration helpers, per-scenario retrieval/end-to-end artifacts, saved live campaign config, and README documentation for env vars plus prerequisites, while keeping the fixture-backed smoke runner as fallback. Follow-up caveat: a real local PostgreSQL-backed BERGEN run could not be executed in this workspace snapshot because `psycopg`/`psycopg2`, `datasets`, `torch`, and `transformers` are not installed here and no live DSN/table/index env vars were configured. |

## Hotpot improvement tracker

| ID | Prompt file | Scope | Tests to add first | Status | Last update | Evidence / commands | Notes |
|---|---|---|---|---|---|---|---|
| 00 | `prompts/pg_turboquant_hotpot_improvement_pack/prompts/00_add_scan_stats_and_freeze_hotpot_baseline.md` | Last-scan observability, benchmark scan-work counters, skewed Hotpot synthetic baseline | `tests/unit/test_scan_stats.c`, `test/sql/scan_stats.sql`, `t/017_scan_stats_hotpot_baseline.pl`, Python scan-metric schema coverage | DONE | 2026-03-29 | `make unitcheck`; `python3 -m unittest tests.test_benchmark_suite tests.test_rag_operational_metrics`; `make install`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" scan_stats`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/017_scan_stats_hotpot_baseline.pl"`; `make installcheck`; `make tapcheck` | Added backend-local last-scan JSON stats, benchmark/RAG scan-metric propagation, and the `hotpot_skewed` synthetic baseline. Follow-up caveat: benchmark scan-work counters are collected from a direct ordered ANN probe query because the current SQL helper tie-breaker on `id` prevents PostgreSQL from using the ordered turboquant index path for observability runs. |
| 01 | `prompts/pg_turboquant_hotpot_improvement_pack/prompts/01_replace_prod_decode_scan_with_code_domain_scorer.md` | Normalized `tq_prod` code-domain scoring for cosine/IP, explicit fallback visibility, and benchmark coverage | `tests/unit/test_prod_code_domain.c`, normalized metric fidelity SQL updates, `t/018_prod_code_domain_smoke.pl`, Python benchmark score-mode assertions | DONE | 2026-03-29 | `make unitcheck`; `python3 -m unittest tests.test_benchmark_suite`; `make install`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" metric_fidelity`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/018_prod_code_domain_smoke.pl"`; `make installcheck`; `make tapcheck` | Added a LUT-based code-domain scorer to normalized `tq_prod` flat/IVF scans, surfaced the mode through scan stats and benchmark defaults, and kept non-normalized paths on the explicit decode fallback. Follow-up caveat: the new code-domain hot path is still scalar-only for now, so prompt `07` remains the place to add an AVX2 fast path without weakening the scalar oracle. |
| 02 | `prompts/pg_turboquant_hotpot_improvement_pack/prompts/02_add_page_local_bounds_and_early_stop.md` | Within-list IVF pruning with page-local bounds, best-first page order, and early-stop stats/reporting | `tests/unit/test_batch_bounds.c`, `test/sql/page_pruning.sql`, `t/019_page_pruning_hotpot.pl`, Python reporting updates | DONE | 2026-03-29 | `make unitcheck`; `python3 -m unittest tests.test_rag_operational_metrics tests.test_rag_campaign_report`; `make install`; `make installcheck`; `make tapcheck` | Added batch-page summary metadata, optimistic bound scoring, best-first IVF page visitation, early-stop/page-prune scan stats, and benchmark/report propagation for the new pruning fields. Follow-up caveat: the prompt bumps the turboquant page format to `5`, so existing indexes created before this slice must be rebuilt, and compaction now clears stale page-summary lanes before recomputing bounds to keep `VACUUM` safe. |
| 03 | `prompts/pg_turboquant_hotpot_improvement_pack/prompts/03_add_adaptive_probe_budgeting_for_skew.md` | Visit-budget-aware adaptive IVF probing with explicit nominal/effective probe stats | `tests/unit/test_probe_budgeting.c`, `test/sql/adaptive_probing.sql`, `t/020_adaptive_probing_hotpot.pl`, Python benchmark/live-config/operational-metric updates | DONE | 2026-03-29 | `make unitcheck`; `python3 -m unittest tests.test_benchmark_suite tests.test_rag_live_configs tests.test_rag_operational_metrics tests.test_rag_campaign_report`; `make install`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" adaptive_probing query_helpers`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/020_adaptive_probing_hotpot.pl"`; `make installcheck`; `make tapcheck` | Added deterministic code/page-budget-aware IVF probe selection that keeps centroid rank order intact, surfaced nominal vs effective probes plus visit budgets through scan stats, benchmark output, SQL helpers, and RAG live configs, and documented the new fields in the SQL/benchmark references. Follow-up caveat: the first selected list remains mandatory even when it exceeds the configured visit budget, so `visited_code_count` is intentionally enforced as “within budget except for first-list tolerance” rather than as a hard absolute cap. |
| 04 | `prompts/pg_turboquant_hotpot_improvement_pack/prompts/04_improve_router_balance_and_training_selection.md` | Deterministic multi-restart router training with explicit balance-quality metadata | `tests/unit/test_router_balance.c`, `test/sql/ivf_training.sql`, `t/021_router_balance_quality.pl`, Python live-config/report metadata coverage | DONE | 2026-03-29 | `make clean`; `make unitcheck`; `python3 -m unittest tests.test_rag_live_configs tests.test_rag_campaign_report`; `make install`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" ivf_training`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/021_router_balance_quality.pl"`; `make installcheck`; `make tapcheck` | Added deterministic multi-restart router training with an explicit distortion-plus-balance selection score, persisted restart and list-balance metadata through index/admin/report surfaces, and updated the Hotpot TAP fixture to read backend-local scan stats in the same session as the ANN query. Follow-up caveat: this prompt bumps the turboquant page format to `6`, so indexes created before this slice must be rebuilt, and the WAL meta-page bounds had to be extended to preserve the new router metadata fields across restart-safe writes. |
| 05 | `prompts/pg_turboquant_hotpot_improvement_pack/prompts/05_recalibrate_planner_costs_and_query_knobs.md` | Planner costs and query-knob recommendations based on predicted visited work instead of nominal probe fraction | planner-cost unit assertions, `test/sql/planner_costs.sql`, `test/sql/query_helpers.sql`, benchmark/query-helper contract updates as needed | DONE | 2026-03-29 | `make unitcheck`; `make install`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" planner_costs query_helpers`; `python3 -m unittest tests.test_benchmark_suite tests.test_rag_live_configs tests.test_rag_operational_metrics tests.test_rag_campaign_report`; `make installcheck`; `make tapcheck` | Recalibrated ordered IVF costing around predicted visited tuples/pages after applying configured visit budgets, and changed `tq_recommended_query_knobs()` to emit an explicit visited-code budget derived from the resolved oversampling instead of just mirroring `candidate_limit`. Follow-up caveat: the SQL helper remains intentionally index-agnostic, so it still leaves `max_visited_pages = 0` and cannot tailor its visited-work budget to per-index list-size skew without a future API change. |
| 06 | `prompts/pg_turboquant_hotpot_improvement_pack/prompts/06_add_hotpot_rag_regression_gate_and_report.md` | Hotpot-focused regression gate and richer report fields tied to recall plus visited scan work | `tests/test_rag_live_configs.py`, `tests/test_rag_operational_metrics.py`, `tests/test_rag_retrieval_eval.py`, `tests/test_rag_regression_gate.py`, `tests/test_rag_campaign_report.py`, `t/023_hotpot_regression_gate.pl` | DONE | 2026-03-29 | `python3 -m unittest tests.test_rag_live_configs tests.test_rag_operational_metrics tests.test_rag_retrieval_eval tests.test_rag_regression_gate tests.test_rag_campaign_report tests.test_rag_live_campaign tests.test_rag_outcome_report`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/023_hotpot_regression_gate.pl"`; `make tapcheck` | Added a deterministic Hotpot regression-gate contract to the live config and comparative report flow, widened retrieval/export/report artifacts with average scan-work fields plus score mode, and made the retrieval export path preserve operational summaries for both approximate and reranked runs. Follow-up caveat: `visited_page_fraction` is currently derived from relation footprint divided by the default PostgreSQL page size rather than an exact persisted total-page counter, so the gate is stable for CI but still uses an estimated denominator. |
| 07 | `prompts/pg_turboquant_hotpot_improvement_pack/prompts/07_optional_avx2_fast_path_for_code_domain_scoring.md` | Optional AVX2 fast path for normalized `tq_prod` code-domain scoring with explicit selected-kernel visibility | `tests/unit/test_prod_code_domain_simd.c`, `test/sql/simd_dispatch.sql`, `t/024_prod_code_domain_simd_smoke.pl`, benchmark contract coverage for selected code-domain kernel | DONE | 2026-03-29 | `make unitcheck`; `python3 -m unittest tests.test_benchmark_suite`; `make install`; `make installcheck`; `make tapcheck` | Added an AVX2 code-domain scorer for normalized `tq_prod` scans, surfaced the selected score kernel through `tq_last_scan_stats()` and benchmark outputs, and kept scalar parity/fallback tests in place. Follow-up caveat: the SIMD fast path is intentionally narrow and currently only activates for the supported packed shape (`bits = 4`, dimension divisible by `8`); all other shapes stay on the scalar oracle. |

## Speed prompt pack tracker

| ID | Prompt file | Scope | Tests to add first | Status | Last update | Evidence / commands | Notes |
|---|---|---|---|---|---|---|---|
| 00 | `prompts/pg_turboquant_speed_promptpack/prompts/00_fix_benchmark_fairness_single_execution.md` | Benchmark fairness for TurboQuant ordered retrieval so IDs and `tq_last_scan_stats()` come from one command batch per repetition | Python benchmark fairness tests around helper output shape, SQL batching, and ordered-path call counts | DONE | 2026-03-29 | `python3 -m unittest tests.test_benchmark_suite`; `python3 scripts/benchmark_suite.py --dry-run --profile tiny --corpus normalized_dense --methods turboquant_ivf,pgvector_ivfflat --output "$TMPDIR/benchmark-suite-speed.json"` | Added `query_turboquant_ordered_ids_and_scan_stats(...)`, refactored the ordered TurboQuant benchmark path to use a single `psql` execution per repetition, and kept the non-TurboQuant path unchanged. Follow-up caveat: prompt `01` is still needed because ordered IVF scans continue to spend query-time work on metadata/page-count walks even though the benchmark no longer double-executes the retrieval helper. |
| 01 | `prompts/pg_turboquant_speed_promptpack/prompts/01_persist_list_page_counts_and_remove_query_time_page_walks.md` | Persist IVF list page counts and remove query-time metadata walks | IVF metadata and benchmark-path tests | DONE | 2026-03-29 | `make unitcheck`; `python3 -m unittest tests.test_benchmark_suite`; `make install`; `make installcheck`; `make tapcheck`; `./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs)" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" admin_introspection ivf_page_counts maintenance_reuse`; `./scripts/run_tapcheck.sh "$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs)" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/006_router_quality.pl t/017_scan_stats_hotpot_baseline.pl t/018_prod_code_domain_smoke.pl t/019_page_pruning_hotpot.pl t/020_adaptive_probing_hotpot.pl t/024_prod_code_domain_simd_smoke.pl"` | Persisted `batch_page_count` in list-directory metadata, removed ordered-scan page-budget chain walks in favor of metadata plus a narrow fallback seam, and updated metadata/installcheck/TAP coverage around page format `7`. Follow-up caveat: the single-batch TurboQuant benchmark path now measures the real ANN scan and preserves `tq_last_scan_stats()` in one command batch, so several benchmark TAP recall floors were recalibrated to the measured ANN path rather than the older helper-biased contract. |
| 02 | `prompts/pg_turboquant_speed_promptpack/prompts/02_add_arm64_neon_code_domain_kernel.md` | Add arm64 NEON code-domain kernel | SIMD parity and dispatch tests | DONE | 2026-03-29 | `make unitcheck`; `python3 -m unittest tests.test_benchmark_suite`; `make install`; `make installcheck`; `make tapcheck` | Added a NEON LUT-based code-domain scorer for normalized `tq_prod`, taught preferred-kernel/benchmark regression contracts to report `neon` on arm64, and tightened the SIMD SQL/TAP fixtures around true unsupported-shape fallback. Follow-up caveat: the NEON fast path is still intentionally narrow to the existing `bits = 4` and transformed-dimension-divisible-by-8 contract; broader shapes remain on the scalar oracle. |
| 03 | `prompts/pg_turboquant_speed_promptpack/prompts/03_tighten_avx2_code_domain_kernel_and_remove_scalar_bit_unpack_hotspots.md` | Tighten AVX2 code-domain path and remove scalar unpack hotspots | SIMD parity and micro-contract tests | DONE | 2026-03-30 | `make tests/unit/test_prod_code_domain_simd tests/perf/test_prod_code_domain_avx2`; `./tests/unit/test_prod_code_domain_simd`; `./tests/perf/test_prod_code_domain_avx2`; `make unitcheck`; `python3 -m unittest tests.test_benchmark_suite`; `make install`; `./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs)" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" simd_dispatch`; `./scripts/run_tapcheck.sh "$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs)" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/024_prod_code_domain_simd_smoke.pl"`; `./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs)" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" smoke am_catalog reloptions flat_scan flat_streaming planner_costs gucs ivf_scan maintenance maintenance_reuse opclasses metric_fidelity ivf_training transform_contract admin_introspection query_helpers bitmap_scan capability_boundaries simd_dispatch scan_stats page_pruning adaptive_probing ivf_page_counts`; `./scripts/run_tapcheck.sh "$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs)" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/*.pl"` | Replaced scalar per-bit unpack work in the supported `bits = 4` code-domain SIMD hot path with byte/block unpacking, added broader AVX2 parity/malformed-input coverage, and introduced a developer-only microbenchmark harness. Follow-up caveat: on this arm64 machine the perf harness can only validate the skip path because AVX2 is unavailable at runtime, so throughput evidence still needs an x86_64 AVX2 host. |
| 04 | `prompts/pg_turboquant_speed_promptpack/prompts/04_avoid_double_reading_data_pages_for_bounds.md` | Avoid double-reading IVF data pages for bounds | IVF bound-collection and benchmark-path tests | DONE | 2026-03-30 | `make unitcheck`; `python3 -m unittest tests.test_benchmark_suite`; `make clean && make install`; `./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs)" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" scan_stats page_pruning admin_introspection planner_costs maintenance_reuse`; `./scripts/run_tapcheck.sh "$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs)" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/010_admin_introspection_maintenance.pl t/011_maintenance_reuse_cycle.pl t/012_maintenance_reuse_restart.pl t/016_extension_upgrade_and_pg17_contract.pl t/019_page_pruning_hotpot.pl"`; `make installcheck`; `make tapcheck` | Added persistent per-page summary side pages for IVF bound ordering, exposed `bound_data_page_reads`, and switched the default ordered scan path to consume summary pages instead of pre-reading each candidate batch page. Follow-up caveat: because the current build flow does not track header dependencies reliably, format/layout prompts like this one require a clean rebuild (`make clean && make install`) to avoid stale-object mismatches. |
| 05 | `prompts/pg_turboquant_speed_promptpack/prompts/05_make_probe_selection_cost_aware_under_skew.md` | Make probe selection cost-aware under skew | Probe-selection and benchmark regression tests | DONE | 2026-03-30 | `make unitcheck`; `python3 -m unittest tests.test_benchmark_suite`; `make clean && PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make install`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make installcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make tapcheck` | Added deterministic cost-aware probe selection under visit budgets, surfaced `selected_page_count` in scan stats/reporting, and kept no-budget centroid order stable. Follow-up caveat: the ordered IVF path still ranks and loads all list metadata before adaptive truncation, so some query-time control-path overhead remains even when the eventual selected probe set is cheaper. |
| 06 | `prompts/pg_turboquant_speed_promptpack/prompts/06_hotpot_overlap_fair_rerun_and_publish.md` | Fair rerun and publication of Hotpot overlap benchmark outcomes | Benchmark/report publication tests | DONE | 2026-03-30 | `python3 -m unittest tests.test_benchmark_suite tests.test_rag_outcome_report tests.test_rag_campaign_report`; `python3 -m unittest tests.test_rag_live_configs tests.test_rag_operational_metrics tests.test_rag_retrieval_eval tests.test_rag_regression_gate`; `python3 scripts/benchmark_suite.py --host /Users/johann/src/ml/pg_turboquant/tmp_benchmark_sock_20260329 --port 55439 --dbname postgres --profile full --corpus hotpot_skewed,hotpot_overlap --methods turboquant_ivf,pgvector_ivfflat,pgvector_hnsw --report --output /Users/johann/src/ml/pg_turboquant/tmp/hotpot_speed_fair_rerun_full_20260330/benchmark-suite.json` | Finished the factual benchmark/outcome report contract, added HTML method-metrics publication fields, and produced fresh single-execution Hotpot skew/overlap artifacts under `tmp/hotpot_speed_fair_rerun_full_20260330/`. Follow-up caveat: the fair rerun materially lowered TurboQuant latency versus the older double-execution overlap report, but it also exposed a severe recall regression on the current TurboQuant ordered path (`hotpot_skewed` and `hotpot_overlap`), so speed conclusions must be read alongside the degraded quality. |

## Qprod/QJL speed prompt pack tracker

| ID | Prompt file | Scope | Tests to add first | Status | Last update | Evidence / commands | Notes |
|---|---|---|---|---|---|---|---|
| 00 | `prompts/pg_turboquant_qprod_qjl_speed_pack/prompts/00_baseline_and_contract_lock.md` | Freeze the current Qprod/QJL scorer contract and expose deterministic scorer/page-scan microbench metrics | score-decomposition consistency coverage, code-domain-vs-decode ordering parity, perf JSON-contract test, benchmark-schema microbench test, TAP smoke for JSON microbench output | DONE | 2026-03-31 | `make tests/unit/test_prod_code_domain_simd tests/perf/test_prod_score_microbench`; `python3 -m unittest tests.test_benchmark_suite`; `./tests/unit/test_prod_code_domain_simd`; `make unitcheck`; `./tests/perf/test_prod_score_microbench`; `python3 scripts/prod_score_microbench.py --skip-build --output tmp/prod-score-microbench.json`; `python3 scripts/benchmark_suite.py --dry-run --profile tiny --corpus normalized_dense --methods turboquant_flat --microbench --output tmp/qprod-qjl-prompt00-benchmark.json`; `./scripts/run_tapcheck.sh "$($(/opt/homebrew/opt/postgresql@16/bin/pg_config --bindir)/pg_config --pgxs)" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/025_prod_score_microbench_smoke.pl"` | Re-aligned the baseline slice to the terminology-correct Qprod/QJL pack, added explicit scorer-decomposition parity and normalized code-domain-vs-decode ordering coverage, and extended the microbench JSON with used-kernel, code/page throughput, and heap insert/replace/reject counters. Follow-up risk: throughput evidence still reflects this arm64/NEON host, so prompt `02` still needs an amd64 AVX2 machine before cross-architecture speed claims can be locked down. |
| 01 | `prompts/pg_turboquant_qprod_qjl_speed_pack/prompts/01_remove_hot_loop_allocations_and_copies.md` | Remove hot-loop allocations and copies from the current Qprod/QJL scan path | direct-view unit tests and scan-contract TAP coverage | DONE | 2026-03-31 | `make tests/unit/test_prod_code_domain`; `python3 -m unittest tests.test_benchmark_suite`; `./tests/unit/test_prod_code_domain`; `make tests/perf/test_prod_score_microbench`; `./tests/perf/test_prod_score_microbench`; `./scripts/run_tapcheck.sh "$($(/opt/homebrew/opt/postgresql@16/bin/pg_config --bindir)/pg_config --pgxs)" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/025_prod_score_microbench_smoke.pl"`; `make unitcheck`; `python3 scripts/prod_score_microbench.py --skip-build --output tmp/prod-score-microbench.json`; `python3 scripts/benchmark_suite.py --dry-run --profile tiny --corpus normalized_dense --methods turboquant_flat --microbench --output tmp/qprod-qjl-prompt01-benchmark.json` | Added direct batch-page code views plus reusable scan scratch, removed steady-state per-lane code copies and per-page decode-buffer allocation from the scan/rescore path, and surfaced scratch/code-view counters in scan stats and microbench output. Follow-up risk: code-domain-only fast paths now correctly show zero scratch allocations because they never materialize decoded vectors; later perf prompts should treat that as expected rather than as missing reuse. |
| 02 | `prompts/pg_turboquant_qprod_qjl_speed_pack/prompts/02_avx2_qprod_qjl_fastpath.md` | Tighten the amd64 AVX2 Qprod/QJL code-domain fast path | AVX2 parity tests and perf regression coverage | DONE | 2026-03-31 | `make tests/unit/test_prod_code_domain_simd tests/perf/test_prod_score_microbench`; `python3 -m unittest tests.test_benchmark_suite`; `./tests/unit/test_prod_code_domain_simd`; `./tests/perf/test_prod_score_microbench`; `make tests/perf/test_prod_code_domain_avx2 && ./tests/perf/test_prod_code_domain_avx2`; `./scripts/run_tapcheck.sh "$($(/opt/homebrew/opt/postgresql@16/bin/pg_config --bindir)/pg_config --pgxs)" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/025_prod_score_microbench_smoke.pl"`; `python3 scripts/prod_score_microbench.py --skip-build --output tmp/prod-score-microbench.json`; `python3 scripts/benchmark_suite.py --dry-run --profile tiny --corpus normalized_dense --methods turboquant_flat --microbench --output tmp/qprod-qjl-prompt02-benchmark.json`; `make unitcheck` | Replaced the AVX2 code-domain loop's stack scratch arrays with direct gather/load/sign-vector setup, added randomized AVX2 parity coverage, and extended the microbench/benchmark schema with explicit `requested_kernel` versus `kernel` reporting so x86 runs can prove the AVX2 path was actually honored. Follow-up risk: the dedicated AVX2 perf binary still skipped on this arm64/NEON host, so the correctness and contract work is locked, but the actual amd64 throughput gain still needs confirmation on a real AVX2 machine. |
| 03 | `prompts/pg_turboquant_qprod_qjl_speed_pack/prompts/03_neon_qprod_qjl_fastpath.md` | Tighten the arm64 NEON Qprod/QJL code-domain fast path | NEON parity tests and perf regression coverage | DONE | 2026-03-31 | `make tests/unit/test_prod_code_domain_simd tests/perf/test_prod_score_microbench`; `./tests/unit/test_prod_code_domain_simd`; `./tests/perf/test_prod_score_microbench`; `python3 scripts/prod_score_microbench.py --skip-build --output tmp/prod-score-microbench.json`; `python3 scripts/benchmark_suite.py --dry-run --profile tiny --corpus normalized_dense --methods turboquant_flat --microbench --output tmp/qprod-qjl-prompt03-benchmark.json`; `./scripts/run_tapcheck.sh "$($(/opt/homebrew/opt/postgresql@16/bin/pg_config --bindir)/pg_config --pgxs)" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/025_prod_score_microbench_smoke.pl"`; `python3 -m unittest tests.test_benchmark_suite`; `make unitcheck` | Reworked the NEON code-domain loop to eliminate the old scalar staging arrays, added randomized NEON parity coverage plus explicit NEON-requested microbench rows, and extended the benchmark/TAP contract so arm64 runs prove when the NEON scorer was actually selected. Follow-up risk: the measured speedup is confirmed on this arm64 host and can fluctuate across runs, so later prompts should treat the new benchmark row as the regression surface rather than locking a single absolute throughput number. |
| 04 | `prompts/pg_turboquant_qprod_qjl_speed_pack/prompts/04_quantized_qjl_luts.md` | Quantize QJL LUTs while preserving the current scorer contract | quantized-LUT parity/retention tests and benchmark metadata coverage | DONE | 2026-03-31 | `make tests/unit/test_prod_code_domain tests/perf/test_prod_score_microbench`; `python3 -m unittest tests.test_benchmark_suite`; `make tests/unit/test_prod_code_domain_simd`; `./tests/unit/test_prod_code_domain`; `./tests/unit/test_prod_code_domain_simd`; `./tests/perf/test_prod_score_microbench`; `python3 scripts/prod_score_microbench.py --skip-build --output tmp/prod-score-microbench.json`; `python3 scripts/benchmark_suite.py --dry-run --profile tiny --corpus normalized_dense --methods turboquant_flat --microbench --output tmp/qprod-qjl-prompt04-benchmark.json`; `./scripts/run_tapcheck.sh "$($(/opt/homebrew/opt/postgresql@16/bin/pg_config --bindir)/pg_config --pgxs)" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/025_prod_score_microbench_smoke.pl"`; `make unitcheck` | Added an int16 quantized QJL LUT sidecar with bounded relative-error guarding, kept the float LUT scorer as the scalar correctness oracle, and taught the AVX2/NEON code-domain paths plus microbench JSON to report `qjl_lut_mode` so float-versus-quantized runs are directly observable. Follow-up risk: the quantized sidecar currently accelerates only the vectorized code-domain path, so scalar fallback still uses float QJL values by design and later prompts should benchmark improvements against the new `qjl_lut_mode` rows rather than assuming one universal kernel path. |
| 05 | `prompts/pg_turboquant_qprod_qjl_speed_pack/prompts/05_block_local_selection.md` | Reduce heap pressure with block-local selection | candidate-heap merge tests and scan-stat coverage | DONE | 2026-03-31 | `make tests/unit/test_scan_stats tests/unit/test_prod_code_domain tests/perf/test_prod_score_microbench`; `python3 -m unittest tests.test_benchmark_suite`; `./tests/unit/test_scan_stats`; `./tests/unit/test_prod_code_domain`; `./tests/perf/test_prod_score_microbench`; `python3 scripts/prod_score_microbench.py --skip-build --output tmp/prod-score-microbench.json`; `python3 scripts/benchmark_suite.py --dry-run --profile tiny --corpus normalized_dense --methods turboquant_flat --microbench --output tmp/qprod-qjl-prompt05-benchmark.json`; `./scripts/run_tapcheck.sh "$($(/opt/homebrew/opt/postgresql@16/bin/pg_config --bindir)/pg_config --pgxs)" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/025_prod_score_microbench_smoke.pl"`; `make unitcheck` | Added page-local top-K selection before global heap merge, kept deterministic top-K retention on seeded single-page and overlap fixtures, and surfaced local insert/replace/reject/merge counters in scan stats plus microbench JSON. Follow-up risk: the local buffer is capped at the global heap size, so churn reduction only appears once a page visits more live codes than that bound; shadow-decode diagnostics still score every visited code directly because they remain the correctness-oriented comparison path rather than the optimized approximate path. |
| 06 | `prompts/pg_turboquant_qprod_qjl_speed_pack/prompts/06_near_exhaustive_scan_crossover.md` | Detect near-exhaustive scans and switch to the cheaper path | exhaustive-crossover parity and flat-scan TAP tests | DONE | 2026-03-31 | `make tests/unit/test_probe_budgeting tests/unit/test_scan_stats`; `./tests/unit/test_probe_budgeting`; `./tests/unit/test_scan_stats`; `python3 -m unittest tests.test_benchmark_suite`; `make`; `make unitcheck`; `make install`; `python3 scripts/benchmark_suite.py --dry-run --profile tiny --corpus hotpot_skewed,hotpot_overlap --methods turboquant_ivf --output tmp/qprod-qjl-prompt06-benchmark.json`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" scan_stats`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/026_near_exhaustive_crossover_hotpot.pl"` | Added a deterministic near-exhaustive crossover helper plus IVF executor fallback that skips bound sorting/pruning once selected live rows or pages reach the configured threshold, surfaced `scan_orchestration` and `near_exhaustive_crossover` in scan stats and benchmark artifacts, and added SQL/TAP hotpot coverage for overlap-versus-skew behavior. Follow-up risk: the threshold is still a fixed 70% rule against the full ranked-list totals for the query, and the benchmark single-batch rerank path had to be corrected to preserve explicit `turboquant.probes` and visit-budget settings, so later router-side prompt `07` may need to recalibrate how often this crossover should trigger. |
| 07 | `prompts/pg_turboquant_qprod_qjl_speed_pack/prompts/07_router_top_probes_partial_selection.md` | Reduce router-side partial-selection overhead for top probes | probe-selection parity and benchmark regression tests | DONE | 2026-03-31 | `make tests/unit/test_router_top_probes`; `./tests/unit/test_router_top_probes`; `make tests/perf/test_prod_score_microbench`; `./tests/perf/test_prod_score_microbench`; `python3 -m unittest tests.test_benchmark_suite`; `make`; `make unitcheck`; `python3 scripts/prod_score_microbench.py --skip-build --output tmp/prod-score-microbench.json`; `python3 scripts/benchmark_suite.py --dry-run --profile tiny --corpus normalized_dense --methods turboquant_flat --microbench --output tmp/qprod-qjl-prompt07-benchmark.json`; `./scripts/run_tapcheck.sh "$($(brew --prefix postgresql@16)/bin/pg_config --pgxs)" "$(brew --prefix postgresql@16)/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/025_prod_score_microbench_smoke.pl"` | Added exact top-probe-prefix parity tests, replaced the router helper with a bounded partial-selection path that preserves the full-sort prefix order, and taught the IVF scan executor to request only the top probe prefix when adaptive visit budgeting is disabled. The microbench/benchmark contract now carries explicit router full-sort vs partial rows plus `list_count`/`probe_count`. Follow-up risk: adaptive budget mode still ranks the full centroid set because prompt `05` cost-aware probe selection depends on global ranked scores, so the remaining router control-path overhead under visit budgets is intentionally deferred rather than silently changed here. |
| 08 | `prompts/pg_turboquant_qprod_qjl_speed_pack/prompts/08_benchmark_regression_gates.md` | Benchmark/report regression gates for Qprod/QJL speed work | benchmark schema/report tests and TAP gate coverage | DONE | 2026-03-31 | `python3 -m unittest tests.test_benchmark_suite`; `make tests/perf/test_prod_score_microbench`; `./tests/perf/test_prod_score_microbench`; `python3 scripts/prod_score_microbench.py --skip-build --output tmp/prod-score-microbench.json`; `python3 scripts/benchmark_suite.py --dry-run --profile tiny --corpus normalized_dense --methods turboquant_flat --microbench --report --output tmp/qprod-qjl-prompt08-benchmark.json`; `./scripts/run_tapcheck.sh "$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs)" "$(/opt/homebrew/opt/postgresql@16/bin/pg_config --bindir)" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/025_prod_score_microbench_smoke.pl t/027_qprod_qjl_regression_gates_smoke.pl"`; `make unitcheck` | Added machine-readable microbenchmark comparisons and regression-gate rows for SIMD kernels, quantized LUTs, and block-local selection, mirrored that section into the generated benchmark reports, and documented how to read the new directional checks. Follow-up risk: the comparison/gate framework is stable, but absolute speed signals remain machine-dependent and the single-page heap-selection microbench on this arm64 host can still wobble enough that real runs should be judged primarily by preserved-work and heap-churn counters, not one throughput ratio alone. |
| 09 | `prompts/pg_turboquant_qprod_qjl_speed_pack/prompts/09_optional_transposed_scan_layout_v2.md` | Optional transposed scan layout v2 | transposed page-format and restart/upgrade contract tests | DONE | 2026-03-31 | `make tests/unit/test_smoke tests/unit/test_prod_code_domain tests/perf/test_prod_score_microbench`; `./tests/unit/test_smoke`; `./tests/unit/test_prod_code_domain`; `python3 -m unittest tests.test_benchmark_suite`; `./tests/perf/test_prod_score_microbench`; `python3 scripts/prod_score_microbench.py --skip-build --output tmp/prod-score-microbench.json`; `make`; `make install`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" reloptions admin_introspection`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/010_admin_introspection_maintenance.pl t/016_extension_upgrade_and_pg17_contract.pl t/025_prod_score_microbench_smoke.pl"` | Implemented the optional transposed layout as a measured prototype, then removed it after targeted microbench evidence on this arm64/NEON host showed it regressed equal-workload page-scan throughput by about 10-12 percent while adding extra copy/scratch churn and a format/maintenance burden. Follow-up risk: row-major remains the only supported on-disk layout unless future amd64 or arm64 evidence demonstrates a clear win large enough to justify reviving a new format variant. |

## Faithful TurboQuant rewrite tracker

| ID | Prompt file | Scope | Tests to add first | Status | Last update | Evidence / commands | Notes |
|---|---|---|---|---|---|---|---|
| F00 | `manual faithful TurboQuant rewrite plan` | Primary `v2` faithful TurboQuant payload/metadata rewrite, benchmark fairness fix, and explicit fast-path/fallback reporting | unit coverage for deterministic stage-1/residual scoring, SQL metadata/fallback regression updates, benchmark repetition-latency contract tests | DONE | 2026-03-30 | `make unitcheck`; `python3 -m unittest tests.test_benchmark_suite`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" metric_fidelity`; `make installcheck` | Replaced the old per-vector-`gamma` primary packed path with a faithful `v2` payload contract built from stage-1 scalar codes plus residual 1-bit correction bits, bumped the on-disk format to `9`, exposed algorithm/quantizer/residual/estimator metadata, and made normalized cosine/IP scans report the faithful code-domain fast path while L2 and non-normalized scans report explicit compatibility fallback. The benchmark suite no longer cherry-picks the minimum latency across repetitions. Follow-up debt: the current page-summary pruning story is still heuristic rather than same-space-safe, `tq_prod`/`tq_mse` names remain transitional implementation names, and IVF still needs a dedicated parity/quality pass against the new faithful flat-path oracle before it can be called complete. |
| F01 | `manual faithful TurboQuant rewrite plan` | Honest page-summary/pruning reporting for the faithful `v2` path across scan stats, index metadata, and benchmark artifacts | SQL regressions for admin metadata, scan stats, and page-pruning honesty plus benchmark schema checks | DONE | 2026-03-30 | `python3 -m unittest tests.test_benchmark_suite`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make install`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" admin_introspection scan_stats page_pruning`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make installcheck` | Added explicit page-summary metadata (`disabled` vs `ordering_only`) and scan-time bound modes (`ordering_only_summary`, `data_page_fallback`, `mixed`) plus `safe_pruning`/`safe_pruning_enabled` flags so the faithful `v2` path no longer implies safe pruning where only heuristic ordering exists. The benchmark scaffold now carries the same contract in dry-run and aggregated outputs. Follow-up debt: pruning remains intentionally disabled because the current representative/radius summary still mixes score spaces; a later faithful slice needs same-space bounds before `safe_pruning` can ever become true. |
| F02 | `manual faithful TurboQuant rewrite plan` | Replace the gamma-free rewrite with the paper-faithful `Qprod`/QJL contract, including restored residual `gamma`, metadata/version bumps, and repo ADR correction | failing unit coverage for `Qprod` payload sizing, deterministic encode/decode, and paper-shaped residual-QJL score decomposition; targeted SQL/TAP metadata regressions | DONE | 2026-03-30 | `make clean`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make unitcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make install`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" admin_introspection metric_fidelity`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_tapcheck.sh "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" "$PWD/third_party/postgresql-source/src/test/perl" "$PWD/third_party/perl5" "t/010_admin_introspection_maintenance.pl t/016_extension_upgrade_and_pg17_contract.pl"`; `python3 -m unittest tests.test_benchmark_suite`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make installcheck` | Replaced the gamma-free rewrite with the public-paper `Qprod` contract: `b - 1` first-stage scalar codes, residual 1-bit QJL sketch bits, and stored residual norm `gamma`, plus matching unbiased-IP score decomposition in scalar and SIMD paths. Added ADR-0013 to supersede ADR-0012's no-`gamma` assumption, bumped the page/algorithm metadata contract to format `10` / algorithm `3`, and updated SQL/TAP golden files to the deterministic rankings produced by the paper estimator. Remaining debt: the QJL projection is currently generated deterministically and cached densely in memory rather than using a cheaper structured sketch operator, page-summary pruning is still heuristic rather than same-space-safe, and IVF/search-quality benchmarking still needs a fresh quality pass against exact baselines before broader performance claims are published. |
| F03 | `manual faithful TurboQuant rewrite plan` | Replace the dense cached QJL matrix with a deterministic structured sketch operator while preserving the paper-facing `Qprod` estimator contract | failing unit coverage for deterministic structured QJL signs, LUT parity, and encode/decode stability under the new sketch operator | DONE | 2026-03-30 | `make unitcheck`; `python3 -m unittest tests.test_benchmark_suite`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make installcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make tapcheck` | Replaced the dense in-memory QJL projection cache with a deterministic structured sketch operator in the faithful `Qprod` path and kept the paper-facing metadata contract unchanged. Residual debt: same-space-safe page-summary pruning is still pending, and IVF still needs a broader exact-baseline quality/benchmark pass. |
| F04 | `manual faithful TurboQuant rewrite plan` | Replace heuristic mixed-space page summaries with same-space-safe `Qprod` summary radii and enable truthful pruning/reporting on the faithful normalized cosine/IP path | failing unit coverage for same-space page-summary bounds and safe-pruning eligibility plus SQL regressions for scan/admin reporting | DONE | 2026-03-30 | `make tests/unit/test_batch_bounds`; `./tests/unit/test_batch_bounds`; `python3 -m unittest tests.test_benchmark_suite`; `make unitcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make install`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make installcheck`; `PG_CONFIG=/opt/homebrew/opt/postgresql@16/bin/pg_config PATH=/opt/homebrew/Cellar/postgresql@16/16.13/bin:$PATH make tapcheck` | Replaced decoded-space residual radii with same-space `Qprod` feature radii, enabled safe page pruning for the faithful normalized cosine/IP IVF fast path, and updated scan/index metadata plus benchmark artifacts to report `safe_summary_pruning` truthfully. The page format is now `11`, so older indexes must be rebuilt. Remaining debt is narrower: the broader IVF exact-baseline quality/benchmark pass is still outstanding. |

## Manual hardening tracker

| ID | Prompt file | Scope | Tests to add first | Status | Last update | Evidence / commands | Notes |
|---|---|---|---|---|---|---|---|
| H00 | `manual maintainer-credibility hardening plan` | Release/install hygiene, pgvector boundary tightening, public SQL surface reduction, corruption guardrails, CI sanitizer/compatibility coverage | packaging contract tests, public-API SQL regressions, corruption negative tests, restart-stress TAP coverage | DONE | 2026-04-02 | `python3 -m unittest tests.test_packaging_contract`; `make`; `make tests/unit/test_page_fuzz && ./tests/unit/test_page_fuzz`; `make unitcheck`; `make install`; `PGXS=$(/opt/homebrew/opt/postgresql@16/bin/pg_config --pgxs) && ./scripts/run_installcheck.sh "/opt/homebrew/opt/postgresql@16/bin/pg_config" "$PGXS" "/opt/homebrew/Cellar/postgresql@16/16.13/bin" reloptions ivf_training transform_contract admin_introspection public_api_surface corruption_guardrails`; `make installcheck`; `PERL5LIB="$PWD/third_party/postgresql-source/src/test/perl:$PWD/third_party/perl5/lib/perl5" prove -I"$PWD/third_party/postgresql-source/src/test/perl" -I"$PWD/third_party/perl5/lib/perl5" -v t/025_prod_score_microbench_smoke.pl`; `make tapcheck` | Collapsed the public version contract back to `0.1.0`, removed pgvector provisioning from `make install`, introduced a regression-only `pg_turboquant_test_support` extension, replaced direct opclass bindings with `tq_*` wrapper support functions, made `tq_index_metadata` cheap with a separate exact heap-stats helper, added corruption regression coverage plus deterministic page fuzzing, and extended CI with compatibility/sanitizer lanes. Follow-up debt: the large C modules still need the planned post-hardening split, and the “latest supported pgvector” CI lane currently resolves to the same `v0.8.1` tag as the pinned lane because the documented support matrix still exposes exactly one pgvector revision. This pass also closed two latent test gaps: public wrapper permissions for non-superusers and the block16 microbenchmark’s single-candidate nibble-layout bug, which had been masking a real perf-fixture contract mismatch under full TAP. |
| H01 | `manual production RAG filtering fast-lane plan` | Keep filtered ordered scans on the SoA/block-16 path by adding a compact filter sidecar to SoA pages for the existing `int4` equality contract | unit tests for SoA filter payload round-trip and filtered block-16 scan stats, SQL regression proving filtered ordered scans still use the narrow multicolumn path and report code-domain scoring | DONE | 2026-04-03 | `make tests/unit/test_smoke`; `./tests/unit/test_smoke`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_turboquant_capability_metadata_marks_multicolumn_support`; `make unitcheck`; `make install`; `make installcheck` | Filtered ordered scans now keep the faithful code-domain path on SoA pages by storing the existing single `int4` equality filter in a compact SoA sidecar, and regression coverage asserts `score_mode = code_domain` with zero decoded vectors on the filtered path. Follow-up debt: this bumps the turboquant page format to `13`, so existing indexes must be rebuilt, and the contract is still intentionally narrow: one persisted `int4` equality filter only, no `INCLUDE` payloads/index-only scan, no broader filter conjunctions, and no delta+merge ingestion policy yet. |
| H02 | `manual production RAG retrieval surface expansion` | Broaden the supported RAG fast lane with multiple compact int4 filter keys, compact int4 INCLUDE payloads, iterative filtered top-k completion controls, delta-aware query/benchmark plumbing, and a filtered live benchmark slice | unit tests for stored metadata attrs and iterative scan controls, SQL regressions for multikey/filter/payload behavior, Python benchmark/live-config/backend contract tests | DONE | 2026-04-03 | `make unitcheck`; `make installcheck REGRESS='filtered_topk capability_boundaries rag_surface'`; `python3 -m unittest tests.test_benchmark_suite`; `python3 -m unittest discover -s tests -p 'test_rag_*.py'` | Expanded the narrow single-filter contract to up to eight stored `int4` metadata attributes after the embedding key, allowed compact `int4` INCLUDE payloads for stage-1 retrieval, added `turboquant.iterative_scan` and `turboquant.min_rows_after_filter`, threaded filtered/payload/delta-aware plans through the BERGEN/live benchmark adapters, and added a filtered live RAG config slice. The turboquant page format is now `14`, so older indexes must be rebuilt. Follow-up debt: planner-native `ANY(int4[])` pushdown is still partial, ordered covering queries still appear as `Index Scan` rather than planner-visible `Index Only Scan`, and the current delta path is a SQL/benchmark union contract rather than a physical on-disk mutable segment with merge policy. |
| H03 | `manual production RAG metadata-v2 and native-delta plan` | Fixed-width nullable metadata keys/payloads, native IVF delta tier + merge/maintenance surface, churn-health introspection, and strict fast-lane enforcement | SQL regressions for mixed fixed-width metadata filters/payloads, native delta/maintenance behavior, and fast-lane rejection/reporting; unit tests for fixed-width metadata page round-trip; Python RAG benchmark/live tests for filtered-churn slices | DONE | 2026-04-04 | `make clean`; `make`; `make tests/unit/test_smoke`; `./tests/unit/test_smoke`; `make install`; `make unitcheck`; `make installcheck REGRESS='metadata_plane_v2 delta_maintenance fast_lane_enforcement filtered_topk capability_boundaries rag_surface'`; `python3 -m unittest tests.test_benchmark_suite`; `python3 -m unittest tests.test_rag_live_configs tests.test_rag_turboquant_backend`; `python3 -m unittest discover -s tests -p 'test_rag_*.py'`; `make`; `make unitcheck`; `make install`; `make installcheck REGRESS='capability_boundaries delta_maintenance rag_surface'`; `python3 -m unittest tests.test_benchmark_suite tests.test_rag_turboquant_backend tests.test_rag_live_configs`; `make`; `make install`; `make installcheck REGRESS='capability_boundaries rag_surface'`; `python3 -m unittest tests.test_benchmark_suite` | Added ADR-0016 and converted the RAG fast lane from `int4`-only metadata to fixed-width nullable metadata descriptors (`bool`, `int2`, `int4`, `int8`, `date`, `timestamptz`, `uuid`) across key filters, INCLUDE-style payloads, page storage, scan qualification, and tuple materialization. Added a native delta/maintenance surface via `tq_maintain_index`, exposed churn/fast-lane metadata in `tq_index_metadata`, enforced strict normalization on build/insert/query for normalized indexes, added filtered-churn live benchmark coverage, and kept the fixed-width metadata contract capped at eight stored attributes after the embedding key. The follow-up hardening pass added selectivity-aware ordered-scan costing, explicit delta-health/compaction recommendations, and a benchmark/RAG split between native index delta and harness-level external-union delta. The remaining planner/storage gap is now closed: TurboQuant stores an exact vector-key sidecar on dedicated exact-key pages, persists per-lane exact-key references in batch pages, reuses those references during delta merge/maintenance, and returns the first key directly from index storage so covered ordered vector projections now plan as true `Index Only Scan`. Flat-mode maintenance and metadata walkers now skip non-batch exact-key pages correctly, and the capability surface now honestly reports `vector_key_returnable = true` and `ordered_vector_key_index_only_scan = true`. Follow-up debt: the block16 transpose fast path is intentionally disabled until it can carry exact-key references without dropping ordered vector-key IOS correctness. The exact-key sidecar bumps the turboquant page format to `17`, so older indexes must be rebuilt. |
| H04 | `manual ordered IOS measurement contract` | Make ordered vector-key `Index Only Scan` measurable in regressions and benchmark artifacts with planner/runtime evidence rather than capability claims alone | failing SQL regressions for `EXPLAIN (ANALYZE, BUFFERS)` on covered ordered top-k queries plus Python benchmark-schema tests for ordered IOS observation fields | DONE | 2026-04-05 | `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_turboquant_capability_metadata_marks_multicolumn_support`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_matrix_output_schema_and_presets`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_report_artifact_generation_contract`; `python3 -m unittest tests.test_benchmark_suite`; `make installcheck REGRESS='ordered_ios_measurement capability_boundaries rag_surface'` | Added a dedicated `ordered_ios_measurement` regression that proves the covered ordered embedding projection runs as `Index Only Scan` under `EXPLAIN (ANALYZE, BUFFERS)` with `Heap Fetches: 0` after `VACUUM (FREEZE, ANALYZE)`. The benchmark suite now carries an `ordered_ios_observation` block per scenario, with dry-run placeholders and live parsed `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)` summaries for ordered scenarios, and report method rows now distinguish the claimed IOS capability from the observed plan node and heap-fetch outcome. Follow-up debt: the benchmark observation currently samples the first ordered query with an embedding-only projection and reports compact node/heap-fetch counters only; it does not yet benchmark filtered ordered IOS shapes or full VM-sensitive heap-fetch distributions across repeated runs. |
| H05 | `manual ordered IOS observation follow-up` | Extend ordered IOS measurement to repeated samples and filtered ordered-scan shapes in both regressions and benchmark artifacts | failing SQL regression for filtered ordered IOS `EXPLAIN (ANALYZE, BUFFERS)` plus Python benchmark/report schema tests for repeated-sample and filtered observation fields | DONE | 2026-04-05 | `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_matrix_output_schema_and_presets`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_turboquant_capability_metadata_marks_multicolumn_support`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_report_artifact_generation_contract`; `python3 -m unittest tests.test_benchmark_suite`; `make installcheck REGRESS='ordered_ios_measurement capability_boundaries rag_surface'` | Extended the benchmark-suite `ordered_ios_observation` contract with repeated-sample fields (`sample_count`, `heap_fetch_samples`, min/max aggregation) plus a nested `filtered_query` observation captured from a measurement-only multicolumn TurboQuant index after a stabilizing `VACUUM (FREEZE, ANALYZE)`. The SQL regression now proves both the unfiltered and filtered covered ordered queries plan as `Index Only Scan` with `Heap Fetches: 0`, and report rows surface the repeated/filtered IOS outcomes directly. Follow-up debt: the benchmark still samples only one representative query vector per scenario and summarizes aggregate heap-fetch counts rather than storing the full per-sample plan JSON or VM-state context. |
| H06 | `manual ordered IOS benchmark de-biasing` | Make ordered IOS benchmark observations sample a bounded set of representative query vectors per scenario instead of only the first query | failing Python benchmark-schema tests for query-sample-count fields and dry-run placeholders | DONE | 2026-04-05 | `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_matrix_output_schema_and_presets`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_turboquant_capability_metadata_marks_multicolumn_support`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_report_artifact_generation_contract`; `python3 -m unittest tests.test_benchmark_suite`; `python3 scripts/benchmark_suite.py --dry-run --profile tiny --corpus normalized_dense --methods turboquant_flat --report --output /tmp/tq-benchmark-dry-run.json` | De-biased the IOS benchmark observation path by sampling up to three representative query vectors per scenario, keeping `sample_count` as total `EXPLAIN` executions while adding `query_sample_count` for distinct query coverage. Report rows now expose the same coverage counts for both unfiltered and filtered ordered IOS observations, and the benchmark-output reference documents the expanded schema. Follow-up debt: the benchmark still stores summarized counters rather than full per-sample plan JSON or visibility-map context, so deeper VM-sensitive diagnosis remains a separate observability slice. |
| H07 | `manual ordered IOS per-sample benchmark evidence` | Preserve compact per-sample ordered IOS evidence in benchmark artifacts so repeated measurements can be audited without switching to full raw plan JSON | failing Python benchmark-schema tests for per-sample query-index fields and dry-run placeholders | DONE | 2026-04-05 | `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_matrix_output_schema_and_presets`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_turboquant_capability_metadata_marks_multicolumn_support`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_report_artifact_generation_contract`; `python3 -m unittest tests.test_benchmark_suite`; `python3 scripts/benchmark_suite.py --dry-run --profile tiny --corpus normalized_dense --methods turboquant_flat --report --output /tmp/tq-benchmark-dry-run.json` | The benchmark now records `query_sample_indexes` plus a compact `samples` array for both unfiltered and filtered `ordered_ios_observation` blocks, where each sample carries the representative `query_index`, `repetition`, observed node type, index-only flag, and heap-fetch count. This keeps the artifact auditable without bloating it with full plan JSON, and the benchmark-output reference now documents the new shape. Follow-up debt: the suite still emits summarized sample records rather than raw per-sample plan JSON or explicit visibility-map state, so deep VM-sensitive diagnosis remains a separate observability slice. |
| H08 | `manual ordered IOS raw-plan and VM observability` | Preserve raw per-sample ordered IOS plan JSON and heap visibility-map context in benchmark artifacts so IOS behavior can be diagnosed from the report payload alone | failing Python benchmark-schema tests for raw-plan sample fields and dry-run visibility-map placeholders | DONE | 2026-04-05 | `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_matrix_output_schema_and_presets`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_turboquant_capability_metadata_marks_multicolumn_support`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_report_artifact_generation_contract`; `python3 -m unittest tests.test_benchmark_suite`; `python3 scripts/benchmark_suite.py --dry-run --profile tiny --corpus normalized_dense --methods turboquant_flat --report --output /tmp/tq-benchmark-dry-run.json` | The benchmark now records `plan_json_samples` for both unfiltered and filtered ordered IOS observations, with one raw `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)` payload per sampled query/repetition, and attaches a `visibility_map_context` derived from `pg_class.relpages`/`relallvisible` after measurement setup. This closes the remaining IOS observability debt for benchmark artifacts without requiring ad hoc reruns. Follow-up debt: visibility context is currently heap-level and catalog-derived rather than sourced from `pg_visibility`, so page-by-page VM diagnosis remains outside the default harness contract. |
| H09 | `manual ordered IOS pg_visibility integration` | Prefer page-level visibility-map summary in benchmark artifacts when `pg_visibility` is available, while keeping a clean `pg_class` fallback | failing Python benchmark-schema tests for VM summary fields plus a mocked helper test that prefers `pg_visibility_map_summary` over the catalog fallback | DONE | 2026-04-05 | `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_matrix_output_schema_and_presets`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_turboquant_capability_metadata_marks_multicolumn_support`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_capture_visibility_map_context_prefers_pg_visibility_summary`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_report_artifact_generation_contract`; `python3 -m unittest tests.test_benchmark_suite`; `python3 scripts/benchmark_suite.py --dry-run --profile tiny --corpus normalized_dense --methods turboquant_flat --report --output /tmp/tq-benchmark-dry-run.json` | The benchmark now extends `visibility_map_context` with `vm_all_visible_pages` and `vm_all_frozen_pages`, and the harness prefers `pg_visibility_map_summary(regclass)` whenever that function is installed, falling back to `pg_class.relallvisible` only when needed. The benchmark-output reference documents the preferred source and fallback contract. Follow-up debt: the default harness still captures VM state as a compact summary rather than full per-page `pg_visibility_map(...)` output, so page-by-page VM forensics remains an opt-in deeper diagnostic mode. |
| H10 | `manual ordered IOS per-page VM evidence` | Capture full per-page visibility-map evidence in benchmark artifacts when `pg_visibility_map(regclass)` is available, while retaining summary counters and fallback behavior | failing Python benchmark-schema tests for `vm_pages` plus a mocked helper test that prefers `pg_visibility_map(regclass)` page output | DONE | 2026-04-05 | `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_matrix_output_schema_and_presets`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_turboquant_capability_metadata_marks_multicolumn_support`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_capture_visibility_map_context_prefers_pg_visibility_pages`; `python3 -m unittest tests.test_benchmark_suite.BenchmarkSuiteContractTest.test_report_artifact_generation_contract`; `python3 -m unittest tests.test_benchmark_suite`; `python3 scripts/benchmark_suite.py --dry-run --profile tiny --corpus normalized_dense --methods turboquant_flat --report --output /tmp/tq-benchmark-dry-run.json` | The benchmark now records `vm_pages` inside `visibility_map_context`, populated from `pg_visibility_map(regclass)` when available and accompanied by retained `vm_all_visible_pages` / `vm_all_frozen_pages` summary counters. The harness prefers page-level evidence (`source = pg_visibility`), falls back to summary-only `pg_visibility_map_summary(regclass)` when needed, and finally degrades to `pg_class` counters if neither function exists. This closes the remaining IOS visibility-map observability debt in the default harness contract. |
| H11 | `manual documentation drift cleanup` | Bring README and public docs back in line with the current metadata, ordered-IOS, maintenance, and benchmark-observability contracts | failing Python packaging/doc-contract test for stale capability claims in README and public reference docs | DONE | 2026-04-05 | `python3 -m unittest tests.test_packaging_contract.PackagingContractTest.test_public_docs_match_current_capability_boundary`; `python3 -m unittest tests.test_packaging_contract` | Updated the README, SQL API, index-options reference, architecture explanation, install/how-to guide, and tutorial metadata note to reflect fixed-width metadata/filter support, ordered vector-key IOS, `INCLUDE` payloads, `tq_maintain_index(...)`, and the newer benchmark observability surface. Follow-up debt: benchmark result examples remain point-in-time measurements and should be refreshed only when the comparative report lane is rerun intentionally. |
| H12 | `manual RAG backend validation consolidation` | Remove duplicated metric/mode/rerank validation logic across the pgvector and turboquant BERGEN adapters by consolidating it into one shared helper | failing Python adapter tests for shared ANN backend validation edge cases across metric mismatch, unsupported mode, and rerank bounds | DONE | 2026-04-05 | `python3 -m unittest tests.test_rag_postgres_adapter.PostgresRetrieverAdapterContractTest.test_validate_ann_backend_request_consolidates_shared_mode_checks`; `python3 -m unittest tests.test_rag_postgres_adapter`; `python3 -m unittest tests.test_rag_pgvector_backends`; `python3 -m unittest tests.test_rag_turboquant_backend`; `python3 -m unittest discover -s tests -p 'test_rag_*.py'` | Introduced a single shared `validate_ann_backend_request(...)` helper in the BERGEN adapter layer and switched both the pgvector and turboquant backends to it, leaving the turboquant-only normalized inner-product guard in place. Follow-up debt: this only consolidates shared request validation; SQL-plan assembly and session-statement metadata extraction are still duplicated across backend implementations. |
| H13 | `manual live benchmark isolation guard` | Prevent comparative BERGEN live runs from reusing one shared table with multiple ANN indexes when the SQL does not force the named index, so backend results cannot silently compare the wrong access path | failing Python live-campaign tests for rejecting unsafe `backend_isolation=False` runs with distinct index names, plus smoke coverage for the remaining degenerate fake shared-index path | DONE | 2026-04-05 | `python3 -m unittest tests.test_rag_live_campaign tests.test_rag_live_campaign_cli` | Added an early preflight guard in `benchmarks/rag/live_campaign.py` that rejects `backend_isolation=False` for comparative runs with distinct backend index names, updated the CLI help and README to call the mode unsafe, and kept the fake smoke path working only for the degenerate same-index-name case used by tests. Follow-up debt: this fixes misleading comparative runs, but it also exposed a separate real quality gap in the isolated `pg_turboquant` live KILT benchmark that still needs index-quality tuning/debugging beyond the control-plane fix. |
| H14 | `manual exact-rerank benchmark contract` | Force BERGEN rerank mode to materialize the ANN candidate set and compute exact scores in a separate step, so `approx_rerank` cannot silently collapse back into another ANN-ordered scan | failing Python backend contract tests for materialized candidate/exact-score SQL in both turboquant and pgvector rerank plans | DONE | 2026-04-05 | `python3 -m unittest tests.test_rag_turboquant_backend`; `python3 -m unittest tests.test_rag_pgvector_backends`; `python3 -m unittest tests.test_rag_live_campaign tests.test_rag_live_campaign_cli tests.test_rag_exact_rerank`; `python3 -m unittest discover -s tests -p 'test_rag_*.py'`; `HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 ./benchmarks/rag/.venv/bin/python /tmp/pg_turboquant_live_supported_run.py`; `./benchmarks/rag/.venv/bin/python - <<'PY' ... pg_turboquant_rerank rerank_k=2474 ... PY` | Switched both backend families to a shared materialized exact-rerank SQL shape: ANN candidates are materialized first, exact scores are computed in a separate `exact_scores` relation, and only then is the final top-k sorted. The live normalized comparison rerun is now honest: default TurboQuant rerank still reports `0.0 recall@10` because its candidate pool is too shallow for the current ANN quality, while a focused `kilt_nq` rerun with `rerank_k = 2474` now recovers to `0.96 recall@10`, proving the previous rerank-control-plane bug is fixed and the remaining problem is candidate quality/tuning rather than exact-rerank correctness. |
| H15 | `manual live RAG benchmark fast-lane defaults and TurboQuant rerank depth` | Make the shipped live benchmark use the normalized small-live configs by default, normalize live query vectors on the benchmark path, and give `pg_turboquant_rerank` a larger default rerank candidate pool so the benchmark reflects a realistic two-stage contract | failing Python live-campaign tests for the stronger small-live config selection and per-method rerank-pool policy, plus live-config parsing tests for normalized TurboQuant IVF defaults | DONE | 2026-04-05 | `python3 -m unittest tests.test_rag_live_campaign`; `python3 -m unittest tests.test_rag_live_configs`; `python3 -m unittest tests.test_rag_live_campaign tests.test_rag_live_configs tests.test_rag_live_campaign_cli`; `python3 -m unittest discover -s tests -p 'test_rag_*.py'`; `./benchmarks/rag/.venv/bin/python - <<'PY' ... rebuild normalized live comparison into /tmp/pg_turboquant_live_supported_20260405_v2 ... PY` | The live benchmark now defaults `kilt_hotpotqa` to the normalized small-live config, normalizes cosine/IP live query vectors before dispatch, and resolves `pg_turboquant_rerank` to a deeper candidate pool (`max(dataset_rerank_top_k, top_k * 64, 1024)`, capped by live row count) while leaving the pgvector rerank pool unchanged. The shipped small-live configs now exercise the intended TurboQuant fast lane (`normalized = true`, `lists = 64`, `router_restarts = 3`, `probes = 8`, `oversampling = 4`). The rerun artifacts in `/tmp/pg_turboquant_live_supported_20260405_v2` show the benchmark contract is now more honest and TurboQuant latency improved materially versus the earlier flat weak shape, but retrieval quality is still `0.0 recall@10` for both `pg_turboquant_approx` and `pg_turboquant_rerank`, so the remaining problem is the ANN ordering itself rather than the benchmark control plane or rerank depth policy. |
| H16 | `manual filtered synopsis pruning and selectivity-aware probe budgeting` | Add summary-level fixed-width metadata synopses, prune filtered pages before scoring, and feed filtered page/live estimates into adaptive probe selection | failing unit tests for metadata ordering and batch-summary synopsis roundtrip, plus SQL regressions for filtered page pruning and bounded/adaptive ordered scans | DONE | 2026-04-05 | `make tests/unit/test_smoke`; `./tests/unit/test_smoke`; `make unitcheck`; `make install`; `make installcheck REGRESS='page_pruning adaptive_probing filtered_page_pruning'` | Extended batch summary entries with fixed-width metadata synopsis state (`null_any`, `null_all`, `all_same`, `same/min/max` per stored metadata attribute), rebuilt list summary pages from live-lane metadata blocks, and taught filtered ordered scans to skip summary pages whose metadata synopsis cannot satisfy the requested filter clauses before any code scoring happens. Adaptive probe budgeting now substitutes synopsis-derived filtered page/live estimates when filters are present, and iterative filtered completion uses bounded summary-page ordering instead of raw chain order. Added the new `filtered_page_pruning` regression and broadened the older `page_pruning` / `adaptive_probing` contracts away from brittle exact-ID expectations toward stable work/behavior assertions. The turboquant page format is now `18`, so older indexes must be rebuilt. Follow-up debt: the first synopsis pass currently supports exact-value and null-aware pruning through per-page min/max and all-same checks, but it does not yet add richer low-cardinality dictionaries or Bloom-style synopses, and the tuning/controller still uses proportional filtered live-count estimates rather than scan-history feedback. |
| H17 | `manual index-aware query knob recommendation overload` | Extend `tq_recommended_query_knobs(...)` with an index-aware overload that uses metadata skew, optional filter selectivity, and recent scan pressure to recommend stronger visited-work budgets for filtered or imbalanced indexes | failing SQL regressions for `query_helpers.sql` and `adaptive_probing.sql` covering explicit filter selectivity, flat-vs-IVF behavior, and recent-scan-pressure expansion | DONE | 2026-04-05 | `make install`; `make installcheck REGRESS='query_helpers adaptive_probing scan_stats'` | Added `tq_recommended_query_knobs(indexed_index regclass, candidate_limit integer, final_limit integer default null, filter_selectivity double precision default null)` and kept the older index-agnostic helper intact. The new overload reads `tq_index_metadata(...)` plus `tq_last_scan_stats()` to scale oversampling, visited-code budgets, and IVF page budgets from list count, page count, live rows, max-list skew, optional filter selectivity, and recent scan-budget pressure. The focused SQL regressions now cover explicit selectivity validation, flat-mode code-budget expansion, IVF page-budget enablement, and scan-pressure-driven widening without depending on brittle exact knob values. Follow-up debt: this is still heuristic SQL-side tuning rather than a full controller fed by persisted scan history, and it only sees the most recent scan plus coarse index metadata rather than richer synopsis histograms or per-filter telemetry. |

## ADR index

- `docs/adrs/ADR-0001-custom-access-method.md`
- `docs/adrs/ADR-0002-lane-adaptive-micro-batches.md`
- `docs/adrs/ADR-0003-structured-transforms-only.md`
- `docs/adrs/ADR-0004-no-internal-heap-rerank.md`
- `docs/adrs/ADR-0005-flat-and-ivf-under-one-am.md`
- `docs/adrs/ADR-0006-append-only-dead-bitmaps-and-reindex.md`
- `docs/adrs/ADR-0007-dedicated-opclasses-and-type-surface.md`
- `docs/adrs/ADR-0008-tests-first-multi-layer-test-strategy.md`
- `docs/adrs/ADR-0009-avx2-optional-scalar-mandatory.md`
- `docs/adrs/ADR-0010-pgvector-dependency-strategy.md`
- `docs/adrs/ADR-0011-use-generic-wal-in-v1.md`
- `docs/adrs/ADR-0012-faithful-turboquant-v2-primary-contract.md`
- `docs/adrs/ADR-0013-paper-faithful-qprod-qjl-contract.md`
- `docs/adrs/ADR-0014-production-rag-fast-lane.md`
- `docs/adrs/ADR-0015-rag-production-surface-roadmap.md`
- `docs/adrs/ADR-0016-native-delta-and-fixed-width-metadata.md`

## Notes for Codex CLI

- Prefer editing files already referenced by the current prompt.
- Do not preemptively implement future prompts.
- If a prompt needs a tiny test-only debug hook, keep it narrowly scoped and documented.
- When adding pgvector integration, keep it behind `tq_pgvector_compat.*`.
- When adding SIMD, keep the scalar version callable in tests.
- When touching page writes, route them through a helper so generic WAL stays localized.

---
> Source: [mayflower/pg_turboquant](https://github.com/mayflower/pg_turboquant) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-06-19 -->
