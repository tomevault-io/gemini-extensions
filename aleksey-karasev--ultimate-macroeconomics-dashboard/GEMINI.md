## ultimate-macroeconomics-dashboard

> This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project overview

`Ultimate Macroeconomics Dashboard` is a Docker stack: a **multi-page Streamlit dashboard** (`app/`) backed by Postgres + Qdrant, with FastAPI micro-services for the AI analyst, forecasting, clustering, on-demand data ingestion, a sandboxed Python executor, and document conversion, plus an NVIDIA Triton Inference Server that hosts all forecasting/clustering model inference (GPU where possible, CPU python backend otherwise). The Streamlit app talks **directly** to the backend services (the read-only Postgres role, Qdrant, and the agent / forecaster / clustering HTTP APIs) — there is no backend-for-frontend. Read `README.md` for the full description; the sections below cover only what isn't obvious from the code.

Planning docs: `TODO.md` is the flat, actionable backlog of remaining work.

## Running the stack

Linting/formatting is done with `ruff` and type-checking with `ty` (both Astral); each service ships a `[dependency-groups] dev` block with both plus `pytest` and `pytest-cov`. Ruff and ty are configured **once at the repo root** in `ruff.toml` and `ty.toml` (single source of truth) — the per-service `pyproject.toml` files intentionally carry no `[tool.ruff]`/`[tool.ty]` tables, so running `ruff`/`ty` from any service directory walks up and resolves the root config (each service's own `.venv` is still used for import resolution). The root `ruff.toml` turns on import sorting (isort `I` with `combine-as-imports` + `force-sort-within-sections`) plus formatter niceties (`lf` line endings, docstring code formatting). Tests live under `<service>/tests/` and run via `uv run pytest` — `[tool.pytest.ini_options].addopts` enables coverage by default (`--cov --cov-report=term-missing`), with per-service `[tool.coverage.run].source` pointing at that service's package(s). Everything runs inside containers via Docker Compose. Every service is Python 3.12 and uses [uv](https://docs.astral.sh/uv/) for dependency management — each service has its own `pyproject.toml` + `uv.lock`, and the Dockerfile runs `uv sync --frozen` into `/opt/venv`.

```bash
# Full stack (build + run, foreground)
docker compose up --build

# Single service rebuild
docker compose build agent
docker compose up -d agent

# Logs
docker compose logs -f app
docker compose logs -f agent
```

For local iteration without rebuilding the image, work in any service directory:

```bash
cd agent          # or forecaster, downloader_extra, docling, etc.
uv sync           # creates .venv from pyproject.toml + uv.lock
uv run uvicorn main:app --reload   # FastAPI services
uv add <package>      # add a dependency (updates pyproject.toml + uv.lock)
uv lock --upgrade     # refresh the lockfile
```

The **Streamlit dashboard** (`app/`) is a uv-managed Python service like the rest. Work in `app/`:

```bash
cd app
uv sync
uv run streamlit run app.py   # dev server on :8501 (talks directly to the services)
uv run pytest                 # unit tests
# end-to-end (Playwright, separate pnpm project): cd app/e2e && pnpm install && pnpm exec playwright test
```

First boot requires `_container_data/.env` (copy from `_container_data/.env.example`) and a populated LLM section in `_container_data/config.yaml`. The `postgres:18` image creates the superuser (`POSTGRES_USER` / `POSTGRES_PASSWORD` / `POSTGRES_DB`) natively on first volume init; `downloader_general` then upserts the read-only LLM role via `src/utils/db_bootstrap.py` (also grants `SELECT` on `public` plus default privileges, so future tables are readable automatically) and runs the ingestion (~1–2h) for World Bank + Yahoo Finance + Webz.io news. The dashboard is at `http://localhost:8501`.

The bootstrap step runs on **every** `downloader_general` container start (cheap, idempotent), so rotating `POSTGRES_LLM_PASSWORD` or adding new tables in a future release takes effect on the next `docker compose up -d downloader_general` without wiping volumes. Only the **initial** bulk download is one-shot, gated by `_container_data/downloader_general/.download_completed`. After that marker is written the container **does not exit** — it stays up running a per-source incremental update scheduler (see the `scheduler:` block in `config.yaml` and the "Data ingestion" section). Because the container is now long-running, its Compose `depends_on` gate flipped from `service_completed_successfully` to `service_healthy` (the healthcheck just tests for the marker file), and `restart:` is `unless-stopped`.

A working NVIDIA GPU + the NVIDIA Container Toolkit are **required**: all model inference runs inside the `triton` service, which reserves the GPU via its `deploy:` block in `docker-compose.yaml`. Chronos + XGBoost run on CUDA, clustering/dim-reduction on RAPIDS cuML, and the CPU-only models (ARIMA family, Prophet, moving-average) run on Triton's python CPU backend. There is no CPU-only fallback build — the `forecaster`/`clustering` containers are now GPU-free adapters, and the GPU reservation lives on `triton` alone.

## Architecture

### Service map and ports

| Service              | Port | Role                                                                                                                  |
| -------------------- | ---- | --------------------------------------------------------------------------------------------------------------------- |
| `db`                 | 5432 | Postgres 18 — World Bank + Yahoo Finance + Binance crypto + FRED US-state + Eurostat EU-NUTS2 tabular data            |
| `vector_db`          | 6333 | Qdrant — news + curated-news (Actually Relevant) + World Bank document embeddings                                     |
| `downloader_general` | —    | Initial one-shot ingest (bootstraps LLM role, ingests WB + Yahoo + Binance + FRED + Eurostat + news), **then long-running**: a per-source incremental update scheduler keeps both DBs fresh |
| `app`                | 8501 | Streamlit multi-page dashboard (entry: `app/app.py`); talks **directly** to the backend services — no BFF                |
| `agent`              | 8000 | FastAPI — LangGraph multi-agent AI analyst                                                                            |
| `forecaster`         | 8001 | FastAPI — thin adapter; forwards forecasting requests (ARIMA family, Prophet, Chronos, MA, XGBoost) to `triton` over gRPC |
| `clustering`         | 8002 | FastAPI — thin adapter; forwards clustering requests (KMeans / DBSCAN / …) to `triton` over gRPC                       |
| `triton`             | —    | NVIDIA Triton Inference Server — hosts every forecasting + clustering model (python backend). CUDA + cuML where supported, else CPU. No vLLM/VLM. Internal-only ports |
| `downloader_extra`   | 8003 | FastAPI — on-demand WB indicator / Yahoo ticker / Binance pair / FRED state-indicator / Eurostat NUTS-2 dataset ingestion (called by the agent) |
| `python_sandbox`     | 8004 | FastAPI — isolated executor for LLM-generated Plotly/Polars code                                                      |
| `docling`            | 8006 | FastAPI — document→Markdown conversion for the chat's file uploads: PDF via docling's VLM pipeline pointed at a **cloud OpenAI-compatible OCR endpoint** (`docling.vlm` in `config.yaml`); docx/pptx/xlsx via native backends. Called by the Streamlit `app` |
| `backup`             | —    | Scheduled cloud backups — `pg_dump` + full Qdrant snapshot → `rclone` remote (off by default; no port)               |
| `langfuse_web`       | 3000 | Self-hosted Langfuse UI/API — LLM observability (traces every agent worker, tool, LLM + embedding call with tokens/latency/cost) |
| `langfuse_worker`    | —    | Langfuse async ingestion worker (internal)                                                                            |
| `langfuse_clickhouse` / `langfuse_redis` / `langfuse_minio` | — | Langfuse's own backing stores (ClickHouse, Redis, MinIO/S3); its relational metadata lives in a `langfuse` database on the main `db` (no dedicated Postgres container) — internal to the network; minio API/console published on 9090/9091 |
| `otel-collector`     | —    | OpenTelemetry Collector (contrib) — **external** monitor's metrics source: `docker_stats` (per-container CPU/RAM/net/disk via docker.sock) + `hostmetrics` (host) + idle `otlp` receiver; re-exports Prometheus metrics internally on 8889 |
| `prometheus`         | 9092 | Prometheus — scrape + TSDB: the OTel Collector, blackbox health probes, and Triton's native `:8002/metrics` (host 9090/9091 taken by langfuse_minio, so published on 9092) |
| `blackbox_exporter`  | —    | Prometheus blackbox exporter — per-service HTTP/TCP health probes (internal) |
| `grafana`            | 3001 | Grafana OSS — dashboards (Containers / Host / Service health), login required (host 3000 taken by langfuse_web). Independent of the dashboard (no `depends_on` either way) |

Inside the Compose network, services address each other by container name and the port from `config.yaml` (e.g. `http://agent:8000`, `http://forecaster:8001`). The Streamlit `app` resolves its downstreams (agent / forecaster / clustering / docling) via `core/api_client.py`, which honours `<SERVICE>_BASE_URL` env vars as overrides and otherwise defaults to `http://<service>:<port>`; it connects to Postgres and Qdrant directly.

### Configuration

`_container_data/config.yaml` is the **single source of truth** for ports, hostnames, LLM/embedding settings, forecaster toggles, etc. It is bind-mounted read-only into every service.

Important: `docker-compose.yaml` duplicates the ports and bind-mount paths declared in `config.yaml`. Changing a port or path in one file requires changing it in the other. The two files are not auto-synced.

Each service parses the slice of `config.yaml` it needs through a small Pydantic model in its own `config.py` (`load_config(path)` → typed object; access is attribute-based — `CONFIG.postgres.host`, not `CONFIG.get("postgres", {}).get("host")`). Secrets are likewise typed per service via `pydantic-settings` `BaseSettings` in a `settings.py`, and each service declares and receives **only the secrets it actually uses** (least privilege): `clustering` / `forecaster` / `python_sandbox` get none, `agent` gets the read-only LLM role but not the superuser password, `docling` gets only `OPENAI_API_KEY` (the cloud OCR endpoint), and the Streamlit `app` gets the read-only LLM role + the Qdrant key + `OPENAI_API_KEY` (Whisper voice transcription) but never the superuser password (token/cost accounting lives in Langfuse, so the app writes nothing to Postgres). `docker-compose.yaml` enforces this at the container boundary: a per-service `environment:` block injects only that service's variables via `${VAR}` interpolation, and a root-level `.env` lets `docker compose up` resolve those `${VAR}`s. There is no shared config/secrets package; each service is its own container with its own `pyproject.toml` and its own copy of the tiny models.

Other config files in `_container_data/`:

- `.env` — secrets (Postgres creds, Qdrant API key, `OPENAI_API_KEY`, `FRED_API_KEY` for state indicators, plus the `LANGFUSE_*` block — project keys `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY` used by both the SDK clients and the auto-provisioner, and the self-host infra secrets `LANGFUSE_ENCRYPTION_KEY`/`_SALT`/`_NEXTAUTH_SECRET`/`_POSTGRES_PASSWORD`/`_CLICKHOUSE_PASSWORD`/`_REDIS_AUTH`/`_MINIO_PASSWORD`/`_INIT_USER_*`). Never commit; gitignored. No longer blanket-mounted into every container — each service receives only the secrets it uses (see the per-service scoping note above), and a gitignored root `.env` symlink points here so Compose `${VAR}` interpolation resolves. The two trace-emitting services (`agent`, `downloader_general`) each get only `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY`.
- `database_schema.yaml` — column-level documentation of Postgres tables; mounted into `agent` so the SQL worker can ground its queries.
- `_configs/world_bank_download_config.json` — list of WB indicators grouped by dashboard page. Append here to add indicators on next clean boot; or add at runtime via the AI analyst (it calls `downloader_extra`).
- `_configs/news_download_config.json` — news topics for the RAG corpus (Webhose GitHub dataset). Its collections carry a `_webhose` **source suffix** (`{topic}_{sentiment}_webhose`) so the source is identifiable in Qdrant alongside the curated single collections `actually_relevant` / `world_bank`; the agent RAG reader (`agent/agent/tools.py`) reconstructs the name with the same suffix, while the Streamlit news page lists Qdrant collections directly.
- `_configs/actually_relevant_download_config.json` — Actually Relevant curated-news ingestion (`base_url`, `page_size`, and the 5 macro `topics` mapping each `slug` → Qdrant `collection`, all set to the single `actually_relevant` collection). The downloader fetches **all** stories (keyless JSON API) and embeds them into that one `actually_relevant` collection, keeping each story's macro topic in the payload `topic` (resolved via the `/api/issues` child→parent taxonomy).
- `_configs/world_bank_articles_download_config.json` — World Bank documents (WDS) RAG ingestion tunables (`base_url`, `rows_per_query`, `lang`, `from_year`, optional `doc_types` filter) plus **anti-bot knobs** (`max_parallel_text_fetches`, `download_max_retries`, `download_retry_delay_seconds`, `inter_query_pause_seconds`, `fetch_jitter_min_seconds`, `fetch_jitter_max_seconds` — the document host is Cloudflare-fronted, so this source pauses/paces/retries harder than the others) and a `queries` list of `{query, collection}` (~30 query terms, every `collection` set to the single `world_bank`). Per query it fetches the top-N docs, pulls each doc's WB-provided plain text (`txturl` — no docling), builds one entry per doc, and the shared uploader chunks each into the single `world_bank` collection (all queries share it, deduplicated by document id; chunk sizes come from the global `shared.news_chunk_*` settings, not this file).
- `_configs/yahoo_download_config.json` — Yahoo Finance tickers.
- `_configs/binance_download_config.json` — Binance crypto ingestion tunables (`base_url`, `quote_asset`, `top_n`, `kline_interval`, `max_parallel_symbols`, `exclude_base_assets`). No curated coin list — the top-N coins are chosen at runtime by 24h quote volume.
- `_configs/fred_download_config.json` — FRED US-state indicators grouped by dashboard section. Each item is `{name, id (friendly slug), series_id (a representative single-state series like "CAUR")}`; the downloader resolves each `series_id` to a GeoFRED series group and fetches the whole 50-state + DC annual panel in one call. Append here to add indicators on next clean boot; or add at runtime via the AI analyst.
- `_configs/eurostat_download_config.json` — Eurostat EU NUTS-2 regional indicators grouped by dashboard section. Each item is `{name, id (friendly slug), dataset (a Eurostat dataset code like "nama_10r_2gdp"), filters (dims pinned to one category, e.g. {"unit":"EUR_HAB"}), units}`; the downloader fetches the whole NUTS-2 region×year panel in one keyless call (`geoLevel=nuts2`). Append here to add indicators on next clean boot; or add at runtime via the AI analyst.
- `_configs/nuts_level2_2021.geojson` — bundled GISCO NUTS-2 (2021) boundaries (`NUTS_RG_60M_2021_4326_LEVL_2`). Mounted read-only into `downloader_general` (builds the `eurostat_regions` catalogue from its properties — `NAME_LATN`, `NAME_ENGL`, `CNTR_CODE`, `LEVL_CODE`) and read directly by the Streamlit `app` (`core/assets.py:load_nuts_geojson`) for its Plotly NUTS-2 choropleth (matched to region codes via `NUTS_ID`). Boundaries © EuroGeographics. The world and US-state choropleths use Plotly's built-in geometries (no bundled GeoJSON).
- `themes.yaml` — **the Streamlit dashboard theme config** (read directly by `app/core/theming.py`). Top-level `active:` + `themes:` map; the active theme is fixed at deploy time via `active:`, exposed as color tokens across the pages and registered as a Plotly template named `app`. Swap `active:` (or add a theme) — no rebuild needed.
- `backup/rclone.conf` — the rclone remote definition for cloud backups (cloud credentials). **Gitignored**; a committed `backup/rclone.conf.example` shows the shape (generate the real one with `rclone config`). Bind-mounted read-only into the `backup` service at `_container_data/rclone.conf` and passed to rclone via `--config`. The remote *name* here must match `backup.rclone_remote` in `config.yaml`. The non-secret knobs (`enabled`, `interval_minutes`, `rclone_remote`, `rclone_path`, `retention_days`, …) live in `config.yaml`'s `backup:` block.

### `app` (Streamlit dashboard)

`app/` is a **multi-page Streamlit app** (Python, uv-managed) — the sole dashboard frontend. It talks **directly** to the backend services (no BFF): `core/postgres_client.py` reads Postgres with `connectorx` into Polars (read-only LLM role), `core/qdrant_client.py` reads Qdrant for the news/RAG pages, and `core/api_client.py` wraps the agent / forecaster / clustering / docling HTTP APIs (`<SERVICE>_BASE_URL` env override → `http://<service>:<port>` default). `app.py` builds the `st.navigation` shell (Dashboard / Other data / Regional Statistics / AI groups); pages live in `app/pages/` and shared logic in `app/core/`.

**Charts are Plotly** (`app/core/plotting.py` — builders that take theme tokens from `core/theming.py`; never hard-code a colour). `core/theming.py` loads `themes.yaml`, exposes the active theme's tokens, and registers a Plotly template named `app`; the active theme is fixed at deploy time (`active:`), no rebuild to switch. Maps are Plotly choropleths: world + US-states from Plotly's built-in geometries, NUTS-2 from the bundled GISCO GeoJSON (`core/assets.py`, matched via `NUTS_ID`).

Pages (`app/pages/`): the 10 WB dashboard pages (`page_utils.py` maps each to `world_bank_download_config.json` sections) render a shared panel — world choropleth + trend/distribution, log toggle, year slider, metadata, **forecasting** (`ForecastControls` → `api_client.forecast_timeseries` → `/predict`) and **LLM plot description** (Plotly PNG → `/plots/interpret`); `19_fred_regional` / `20_eurostat_regional` (catalogue picker + choropleth + ranking + region-trend); `14_yahoo_finance` / `16_crypto` (overview table, price trend, candlestick, correlation heatmap, treemap); `15_news` (browse + wordcloud + semantic search + embedding map; lists Qdrant collections directly); `13_clustering_sandbox` (country×indicator matrix → `/cluster` → 2D/3D scatter + cluster map, grouped under "Other data"); `11_ai_agent_chat` (SSE stream via `api_client.agent_chat_stream`, worker-step breadcrumb, Plotly plot + data-table artifacts, **multimodal input** — see below). Token/cost accounting lives in **Langfuse**; container/service monitoring is the external **Grafana/Prometheus** stack.

**Multimodal chat input** (`app/core/multimodal.py`): the AI chat accepts file attachments (via `st.chat_input(accept_file="multiple")`) and a voice recording (`st.audio_input`). `process_uploads` dispatches by extension — text decoded, images base64-encoded into data URIs (forwarded to the agent as `images` for the vision-capable supervisor), audio transcribed via the OpenAI-compatible **Whisper** endpoint (`config.whisper`, plain `httpx` POST to `/audio/transcriptions`), documents converted to Markdown by the **docling** service. Extracted text is appended to the user message under clear delimiters; a per-file failure degrades to an inline `[note]` rather than sinking the whole turn.

**Tests:** `app/tests/` (pytest: `test_config`, `test_settings`, `test_theming_smoke`, `test_multimodal`) + `app/e2e/` — a separate pnpm Playwright project that drives the running dashboard (shared `helpers.ts`; `smoke` shell + disclaimer gate, a `navigation` sweep that walks all 17 sidebar pages asserting each heading with **no `stException` traceback**, `dashboard` WB interactions — charts, cross-page country selector, settings popover —, `data-pages` charts for Yahoo/Crypto/FRED/Eurostat, and `interactive` for the chat's multimodal inputs + clustering/news). Needs the stack up (`docker compose up -d app`); run `cd app/e2e && pnpm install && pnpm exec playwright install chromium && pnpm exec playwright test`.

### `agent` (LangGraph supervisor)

`agent/agent/graph.py` is the heart of the AI analyst. The flow is:

1. **`GuardrailAgent`** — heuristic-first screen. Three regexes (auto-allow for short greetings + in-scope keywords, auto-block for clear red flags) decide most messages without an LLM call; only ambiguous ones escalate to the structured-output LLM.
2. **`MacroSupervisorAgent`** — plans, picks the next worker, and decides FINISH. Branches off `last_worker_status` (a `Literal["SUCCESS","EMPTY","ERROR","NEEDS_DOWNLOAD","BLOCKED","UNKNOWN"]` returned by every worker) rather than regex-matching prose. Static preamble + macro context block stay constant across turns.
3. **Workers** (one of `WORKER_NAMES` in `graph.py`) — every worker also receives the last ~3 chat turns so follow-ups disambiguate:
   - `sql_agent` — up-to-5-step exploration. Defaults to WDI (`db_id = 2`) and skips the database-lookup step for typical macro queries; carries 3 worked few-shot examples.
   - `plotly_agent` — generates Plotly code, runs it in `python_sandbox`, returns the figure as an artifact.
   - `table_agent` — Polars transformations on prior worker output.
   - `rag_agent` — Qdrant semantic search over the news corpus.
   - `web_search` — DuckDuckGo fallback.
   - `downloader_agent` — calls `downloader_extra` to ingest data on demand from one of five sources (WB indicator, Yahoo ticker, Binance pair, FRED state indicator, Eurostat NUTS-2 dataset). Triggered when `sql_agent` returns `last_worker_status = NEEDS_DOWNLOAD`. The worker extracts a `DownloadPlan` (`source` + the source's id) and POSTs the unified `/ingest` body. For WB the exact `indicator_id`/`db_id` come from `sql_agent`'s `world_bank_database_indicators` lookup; **Yahoo/Binance/FRED/Eurostat have no master catalogue**, so the worker infers the ticker / full USDT pair symbol / representative single-state FRED series id / Eurostat dataset code + filters (Apple→`AAPL`, Solana→`SOLUSDT`, state unemployment→`CAUR`, EU regional GDP per capita→`nama_10r_2gdp` + `{"unit":"EUR_HAB"}`) from the user request and `downloader_extra` validates it against the live API.
   - `chat_agent` — conversational synthesis / general-knowledge answers.

The graph builds **two `ChatOpenAI` instances** (`MacroAgentGraph.__init__`): a strong model (`shared.openai_llm_model`) for the reasoning-heavy roles — `supervisor` (planning + the final answer), `sql_agent`, `plotly_agent`, `chat_agent` — and a fast model (`shared.openai_llm_model_fast`) for the cheap `GuardrailAgent` screen and the lightweight `table_agent` / `rag_agent` / `web_search` / `downloader_agent`. Both share the base URL + API key and differ only by model name; when `openai_llm_model_fast` is unset it falls back to the strong model. Vision (`/plots/interpret`) stays on the strong model.

When the supervisor picks FINISH, it writes the **complete polished markdown answer** into `isolated_worker_task` and that draft is streamed to the user verbatim in ~24-char chunks (`MacroAgentGraph._stream_supervisor_draft`). There is no second synthesis LLM call — the supervisor's draft is the answer, with only a small line-level leak filter (`_sanitize_draft`) stripping any line that accidentally contains worker names / sandbox / traceback tokens.

Streaming protocol is SSE on `POST /chat/stream` with `step` / `token` / `final` / `error` events; the `final` event carries the answer plus an `artifacts` dict and a `usage` block. `ChatRequest` carries an optional `images: list[str]` of base64 data URIs (populated by the Streamlit chat's multimodal input): when present, `MacroAgentGraph._build_initial_state` turns the current turn into a multimodal `HumanMessage` (OpenAI content-parts: one `text` part + one `image_url` part per image, same shape as `/plots/interpret`) so the vision-capable **supervisor** sees the images. A `_message_text()` helper keeps the base64 out of the guardrail regex and the text-only worker history (only the supervisor receives the raw messages, so workers see the images' presence but not the pixels). `POST /plots/interpret` is a separate vision endpoint that reads a base64 Plotly screenshot with two modes (`no_hallucinations` strict description vs. analyst interpretation).

Per-LLM-call token accounting is attached via `UsageTracker` (`agent/agent/usage.py`) on every LangChain LLM in the graph (guardrail when it escalates, supervisor, each worker) — it's a graph-level callback, so it aggregates across both the strong and fast models automatically and labels each call with the model that served it. Worker output schemas live in `agent/agent/schemas.py`; prompt text lives in `agent/agent/prompts.py` and follows a stable-prefix / dynamic-tail layout.

External backend calls (sandbox, downloader_extra) use one shared `httpx.AsyncClient` from `agent.tools._get_httpx_client()` (closed in the FastAPI shutdown hook); the rendered database-schema text is `functools.lru_cache`d so it isn't re-serialised on every SQL step.

### Triton Inference Server (`triton`)

**All** forecasting + clustering model inference lives here, in one container built from `triton/` (`FROM nvcr.io/nvidia/tritonserver:25.10-py3` — the standard python-backend image, CUDA 13, so RAPIDS is pinned to the matching `cuml-cu13`/`cudf-cu13` 25.10 wheels; **no vLLM / VLM** — document OCR is offloaded to a cloud endpoint by the `docling` service). It is **not** a uv-managed service — Triton is NVIDIA's C++ server image, so its runtime deps are pip-installed from `triton/requirements.txt` (torch/chronos, GPU xgboost, RAPIDS cuML/cuDF, statsmodels/pmdarima/prophet, sklearn/umap) rather than locked with uv. After that install the Dockerfile **drops any numpy `*.dist-info` left without a `METADATA` file**: the base image ships an apt-installed numpy 1.26.4 that pip can't uninstall when RAPIDS upgrades numpy, and the leftover metadata-less dir shadows the real one so `importlib.metadata.version("numpy")` returns `None` — which crashes transformers' import-time version guard (`numpy>=1.17`, `found=None`) inside Chronos's `initialize()` and, because Triton exits when any model fails to load, takes the whole server down. The container's `CMD` is the standard `tritonserver --model-repository=/models`, serving HTTP/gRPC/metrics (8000/8001/8002) for the forecaster/clustering adapters and the `/v2/health/ready` probe. GPU is **required**; there is no CPU-only build.

Layout:

- `triton/model_repository/` — one directory per model, each a **python-backend** model (`config.pbtxt` + `1/model.py`). Every model speaks the **same tensor contract**: a single `TYPE_STRING` `INPUT` carrying one UTF-8 JSON request, and a single `TYPE_STRING` `OUTPUT` carrying one JSON response envelope (`{"ok": true, "result": …}` or `{"ok": false, "error_code": 400|500, "detail": …}`). `max_batch_size: 0` (one full fit per request).
- Model list + GPU/CPU placement (`instance_group`): `forecast_arima` / `forecast_sarima` / `forecast_auto_arima` / `forecast_moving_average` / `forecast_prophet` are **KIND_CPU** (no CUDA path exists for statsmodels/pmdarima/prophet); `forecast_xgboost` (trains with `device="cuda"`) and `forecast_chronos` (Torch/T5, weights preloaded in `initialize()`) are **KIND_GPU**; `cluster` is **KIND_GPU** so cuML has a device (its sklearn-only algorithms just run on CPU inside that instance). Chronos reads its checkpoint name from the bind-mounted `config.yaml` (`forecaster.CHRONOS_MODEL`).
- `triton/common/umd_common/` — the real fit/predict maths, baked onto `PYTHONPATH` so every `model.py` is a thin shell. `forecasting.py` (the six stateless forecasters as `run(model, payload)` + a preloaded `ChronosRunner`), `clustering.py` (feature-matrix build + estimator + 2D/3D projection; **cuML for KMeans/DBSCAN/PCA/t-SNE/UMAP, scikit-learn CPU fallback** for Mean-Shift/HDBSCAN/Spectral/Agglomerative/Kernel-PCA — every cuML path is wrapped so an import/runtime failure degrades to sklearn), `timeutil.py` (frequency inference), and `triton_io.py` (the `pb_utils` ↔ JSON glue, the only Triton-runtime-only import). This package is CPU-unit-tested under `triton/tests/` via a lean `triton/pyproject.toml` dev env (GPU-only deps `importorskip`).

Ports (HTTP 8000 / gRPC 8001 / metrics 8002) are **internal to the compose network** — nothing is published to the host; the two adapters reach Triton by container name over gRPC. Health is the Triton readiness endpoint `GET /v2/health/ready`.

### `forecaster` (adapter)

`forecaster/main.py` exposes the HTTP contract (`POST /predict`, `GET /models`, `GET /health`) but runs **no ML itself**. It validates + cleans the request (parse timestamps → reject unparseable ones with 400, dedup on timestamp keeping the last value, sort, truncate to trailing `n_prev`), enforces the `config.yaml` model toggles, forwards the prepared `dates`/`values` + `n_predict`/`alpha`/`model_params` to the matching Triton model (`forecast_<model_type>`) over gRPC via `triton_client.py` (`infer_json`), then reshapes the reply into the same `ForecastResponse`. A `TritonError(status_code, detail)` from the model maps straight onto the HTTP status (400 input errors, 500 model failures, 502 unreachable Triton). The container is GPU-free; its image carries only fastapi/uvicorn/pydantic/polars/tritonclient.

Per-model hyperparameters still travel in a single `model_params: dict[str, Any]` on `ForecastRequest`; the Triton-side functions in `umd_common/forecasting.py` pull the keys they need (`p`/`d`/`q`, `window`, `lags`, …). Adding a new forecasting model means: a branch in `umd_common/forecasting.py`, a `model_repository/forecast_<id>/` dir (`config.pbtxt` KIND_CPU/GPU + `1/model.py` subclassing `ForecastModelBase` with `MODEL = "<id>"`), the id added to the `ModelType` literal in `forecaster/schemas.py`, the forecasting UI in the Streamlit app (`app/core/plotting.py`, `_render_model_param_inputs` — the model appears automatically from `/models`), and a CPU smoke test in `triton/tests/test_forecasting.py`.

`ARIMA_AVAILABLE` / `PROPHET_AVAILABLE` / `CHRONOS_AVAILABLE` toggles in `config.yaml` still gate the three heavy-dep families — enforced now by the **adapter** (a disabled model 400s before Triton is contacted; Triton itself always loads every model in the repo). `auto_arima` / `arima` / `sarima` ride on `ARIMA_AVAILABLE`; `moving_average` and `xgboost` are always available.

### `clustering` (adapter)

`clustering/main.py` mirrors the forecaster adapter: same HTTP contract (`POST /cluster`, `GET /methods`, `GET /health`), same Pydantic `ClusterRequest`/`ClusterResponse` schema, but the whole validated request is forwarded to the Triton `cluster` model and the reply reshaped into `ClusterResponse`. Feature-matrix building, estimator selection, and the visual projection all live in `umd_common/clustering.py` now. Pydantic validation (e.g. non-empty `dataframe`) still fires in the adapter for fast 422s; input errors detected server-side (no numeric features, `k > n_rows`, …) come back as a `TritonError(400, …)`.

### Data ingestion

`downloader_general/src/` is split into `core/` (orchestration, schema validation in `utils/schema.py`, plus the shared `qdrant_uploader.py` embed-and-upsert mixin), `extractors/` (one module per source: `world_bank`, `yahoo`, `binance`, `fred`, `github` for the Webhose news repo, `actually_relevant_download` for the Actually Relevant curated-news API, `world_bank_articles_download` for World Bank documents), and `utils/`. The **initial** ingest is one-shot, gated by the `_container_data/downloader_general/.download_completed` marker (gitignored); re-running it from scratch requires removing that marker and the persistent volumes (`postgres_data`, `qdrant_data`). After the initial ingest the container stays up, running the incremental update scheduler described below.

**Incremental update scheduler.** After the initial ingest, `main.py` hands off to `src/scheduler.py:run_scheduler` (a SIGTERM/SIGINT-aware interval loop modelled on the `backup` service). Each source in the `config.yaml` `scheduler.sources` block has its **own** `interval_minutes` + `enabled`; every tick calls that source's incremental `update()` (append-only — **never** the destructive full `run()`, which drops+recreates). Both `run()` and `update()` share the same downloader objects built once by `build_downloaders()`. The whole scheduler is gated by `scheduler.enabled` (on by default; `run_on_start: false` waits one interval before a source's first update since the initial ingest just ran). A per-source `try/except` means one failing source is logged and retried next interval, never killing the loop; each tick re-runs `_initialize_connections(...)` first (cheap probe that also self-heals stale clients/transient outages) and skips the tick if it fails. What "un-downloaded" means per source: **time-series** (`yahoo`/`binance`/`world_bank`/`fred`/`eurostat`) read each item's latest stored period via `src/utils/incremental.py:group_max` (`max(date)`/`max(year)`) and append only strictly-newer rows into the existing tables (prior-year *revisions* are intentionally not re-pulled); `fred`/`eurostat` recover each concept's source id / dataset+filters from its `*_indicators` catalogue via `read_rows`. **RAG** (`news`/`actually_relevant`/`world_bank_articles`) dedup against Qdrant and upsert only new points **without** `recreate_collection` — via `qdrant_uploader.existing_payload_values` (scroll the dedup-key field) + `ensure_collection` + the mixin's `upsert_collections`; dedup keys are `archive_name` (news, per-archive), `article.id` (Actually Relevant story id), and `article.doc_id` (WDS document id). An `update()` whose value table is missing/empty (source never ingested) safely falls back to a full `run()`; `group_max`/`read_rows` return `None` only for a genuinely missing table (via `inspect().has_table`) and let transient DB errors propagate so a blip never triggers a destructive full re-ingest.

**News-file cleanup.** The Webhose news pipeline (`github_download.py`) clones + unzips into an **ephemeral, container-local scratch dir (`/app/news`)** that is deliberately **not** under the bind-mounted `_container_data`, so the multi-GB Webhose clone/unzip never touches the host volume (nor any named volume) — it's discarded with the container. Two layers keep it small: `_process_archive` deletes each `.zip` **immediately after extraction** (peak disk never holds every archive's zip *and* its unzipped JSONs at once), and `cleanup_downloaded_files()` wipes the whole scratch dir at the end of **both** `run()` and `update()` (after the Qdrant upload succeeds). Dedup uses Qdrant as the source of truth, so no on-disk state needs to survive. The other two RAG sources fetch from APIs straight to Qdrant and write no files.

Ingestion progress is reported **through the logs, not `tqdm`** (terminal progress bars don't render in container logs). Long loops wrap their iterable in `log_progress(iterable, label=..., total=...)` (`src/utils/downloads.py`), which emits a throttled INFO line (≤ one per 5 s, plus a final 100% line); git clone progress goes through the log-emitting `CloneProgress` in the same module. Reuse `log_progress` for any new progress reporting; don't introduce `tqdm`.

Network resilience is centralised in two retry wrappers that **every** source funnels through: the async `wb_client.call_with_retries` and its synchronous twin `downloads._call_with_retries`. Both use **exponential backoff with jitter** (`wb_client.compute_backoff_delay`, nominal delay doubles per attempt capped at `max_delay=60s`, plus up to 50% random jitter). The per-downloader budget is `download_max_retries = 5` / `download_retry_delay_seconds = 5` (base). WB pagination has an **extra inner layer**: `wb_client._fetch_list` retries a single failed page via `_get_with_page_retries` (5 attempts, base 2s) so a transient mid-pagination `400` doesn't discard the pages already fetched — the outer `call_with_retries` remains a final backstop. When tuning stability, prefer bumping these shared knobs over adding ad-hoc `try/except` in a single extractor.

World Bank access goes through a hand-rolled **async `httpx` client** (`downloader_general/src/utils/wb_client.py`) — it pages the documented `https://api.worldbank.org/v2/...` REST endpoints directly (`/source`, `/country`, `/indicator`, `/country/all/indicator/{id}`, `/sources/{db}/series/{id}/metadata` with a `/indicator/{id}` fallback) and returns plain dicts shaped exactly as the schema cast expects. Its client uses a generous read timeout (`DEFAULT_TIMEOUT = httpx.Timeout(60.0, connect=15.0)`). Aggregate economies (`region.id == "NA"`) are dropped and null observations kept; the indicator phase runs concurrently under an `asyncio.Semaphore(max_parallel_indicators)`. `downloader_extra` ships its own trimmed copy of the same client (`downloader_extra/wb_client.py`, data-fetch only) — duplicated per service, like the other tiny per-service models.

For incremental additions during a live stack, the agent's `downloader_agent` worker calls `downloader_extra` (port 8003), which writes directly into the running Postgres without touching the marker. `downloader_extra` is **multi-source**: its single `POST /ingest` dispatches on a `source` field (`worldbank` / `yahoo` / `binance` / `fred` / `eurostat`) to per-source clients (`client_wb.py`, `client_yahoo.py` using `yfinance`, `client_binance.py` + a trimmed `binance_client.py` copy, `client_fred.py` + a trimmed `fred_client.py` copy, `client_eurostat.py` + a trimmed `eurostat_client.py` copy). Each path short-circuits when the item is already present (`status="already_downloaded"`), else delete-then-inserts the freshly fetched rows (metadata row first to satisfy the FK). World Bank has the `world_bank_database_indicators` master catalogue so `sql_agent` finds the exact id before downloading; Yahoo/Binance/FRED/Eurostat have **no master table**, so the agent infers the ticker / full pair symbol / representative single-state FRED series id / Eurostat dataset code (+ filters) from world knowledge and `downloader_extra` validates it live (yfinance history for Yahoo, `/exchangeInfo?symbol=` for Binance, `geofred/series/group` for FRED) — an on-demand FRED item is stored under the upper-cased series id as its `indicator_id` slug with `category="On-demand (FRED)"`.

Binance crypto ingestion (`downloader_general/src/extractors/binance_download.py`) goes through its own async `httpx` client (`src/utils/binance_client.py`), hitting only the documented public spot endpoints (`/api/v3/exchangeInfo`, `/api/v3/ticker/24hr`, `/api/v3/klines`) — no API key. It selects the `top_n` USDT spot pairs by trailing-24h quote volume (dropping stablecoins via `exclude_base_assets` and leveraged UP/DOWN/BULL/BEAR tokens in code), writes the ranked master data to `binance_metadata`, then pages each pair's full daily candle history into `binance_historical_prices` (PK `[date, symbol]`, FK → `binance_metadata.symbol`) concurrently under an `asyncio.Semaphore(max_parallel_symbols)`. The "description" is synthesized from documented fields (the REST API exposes no prose coin descriptions). Like Yahoo/WB it's marker-gated and bootstraps **only** its own `binance` schema group, so bulk top-N re-ranking on an existing volume needs a clean boot. For adding a single coin to a live stack there is now an on-demand path via `downloader_extra` (`source=binance`). It reuses the shared retry helper `wb_client.call_with_retries`.

FRED US-state ingestion (`downloader_general/src/extractors/fred_download.py`) goes through its own async `httpx` client (`src/utils/fred_client.py`) hitting the documented **GeoFRED** endpoints (`geofred/series/group`, `geofred/regional/data`) with a `FRED_API_KEY`. For each configured indicator it resolves a representative single-state series (e.g. `CAUR`) to a *series group*, then fetches the **whole 50-state + DC annual panel in one `regional/data` call** (`frequency=a` aggregates monthly/quarterly/weekly series to annual). The critical gotcha (covered by a unit test): several groups use per-state `series_id`s that do **not** encode the abbreviation (`MEDDAYONMARCA` → FIPS `06` = California, not "ME"), so observations are mapped to states by their **FIPS `code`** via the static `FIPS_TO_STATE` reference (also the source of Census region/division), never by parsing the series id. It writes three tables (schema group `fred`, mirroring World Bank's `world_bank_countries`/`world_bank_metadata`/`world_bank_indicators`): `fred_states` (id = 2-letter USPS abbrev, name, fips, region, division), `fred_state_indicators` (one description row per indicator concept — friendly `indicator_id` slug, name, category, series_group, units, frequency, seasonal_adjustment, coverage), and `fred_state_indicator_values` (long annual panel `(state, year, value, indicator_id)`, FK → `fred_states.id` enforced). Like the other sources it's marker-gated and bootstraps only its own `fred` schema group. The Streamlit app reads it via `core/postgres_client.py` (`get_fred_states` / `get_fred_indicators_catalog` / `get_fred_indicator`); `downloader_extra` ships a trimmed data-fetch-only copy of `fred_client.py` for the on-demand path.

Eurostat EU-regional (NUTS-2) ingestion (`downloader_general/src/extractors/eurostat_download.py`) goes through its own **keyless** async `httpx` client (`src/utils/eurostat_client.py`) hitting the documented Eurostat dissemination API (`.../statistics/1.0/data/{dataset}`), which returns **JSON-stat 2.0**. For each configured indicator it fetches the whole NUTS-2 region×year panel in one call (`geoLevel=nuts2`, `time` omitted → all years); the config's `filters` pin every non-geo/non-time dimension (`unit`, `sex`, `age`, `nace_r2`, …) to one category so a single clean series comes back. `parse_jsonstat` flattens the flat JSON-stat `value` map by **row-major stride decoding**, keeping only cells whose pinned dimensions sit at index 0 (so an under-pinned dim collapses deterministically to its first category) and only geo codes of the target level. It writes three tables (schema group `eurostat`, mirroring World Bank / FRED): `eurostat_regions` (id = NUTS-2 code, name, country_code, country_name, nuts1_id, level — built from the bundled GISCO GeoJSON so the catalogue exactly matches the map polygons), `eurostat_indicators` (one description row per concept — friendly slug, name, category, dataset, filters JSON, units, frequency, coverage, source_label), and `eurostat_indicator_values` (long annual panel `(region, year, value, indicator_id)`, FK → `eurostat_regions.id` enforced). Values whose region code isn't in the NUTS-2021 catalogue (older-vintage codes some datasets still return) are dropped. Like the other sources it's marker-gated and bootstraps only its own `eurostat` schema group. The Streamlit app reads it via `core/postgres_client.py` (`get_eurostat_regions` / `get_eurostat_indicators_catalog` / `get_eurostat_indicator`) and draws the NUTS-2 choropleth from the bundled GISCO GeoJSON; `downloader_extra` ships a trimmed data-fetch-only copy of `eurostat_client.py` (+ `client_eurostat.py`) for the on-demand path (`source=eurostat`, keyed on the lower-cased dataset code, `category="On-demand (Eurostat)"`).

**Chunking (all news sources).** Every news source — Webhose, Actually Relevant and World Bank documents — splits each article's text into overlapping token windows via the single shared chunker in `qdrant_uploader.py` (`chunk_text` / `chunk_entries`, driven by the global `shared.news_chunk_size_tokens` + `news_chunk_overlap_tokens` in `config.yaml`) and stores **one Qdrant point per chunk** (a multi-chunk article's title gets a `"(part i/N)"` suffix and each point carries `article.chunk_index`/`total_chunks`). Long articles are therefore chunked, never truncated to the embedding limit (`_truncate_for_embedding` remains only a backstop). The Webhose `NewsDownloader` funnels through the same chunker even though it keeps its own (non-mixin) embed path.

The RAG corpus has **three** news sources beyond the Webhose GitHub dataset, all one-shot/marker-gated and all writing to Qdrant through the shared `src/core/qdrant_uploader.py` `QdrantEmbeddingUploaderMixin` (same `openai/text-embedding-3-small`, 1536-dim cosine space; `recreate_collection` + batched embed + upsert). Every point keeps the news payload shape — a nested `article` dict (`title`/`text`/`url`/`published`/`thread.site`) plus top-level `topic`/`sentiment`/`date` — so the Streamlit News page (collection-agnostic — it lists Qdrant collections directly) and the agent RAG worker read the new collections **unchanged**. (1) **Actually Relevant** (`src/extractors/actually_relevant_download.py` + `src/utils/actually_relevant_client.py`): a keyless curated-news JSON API that serves **analysis, not full article bodies** — so each point embeds a composed document (`compose_story_text`: title + summary + relevanceSummary + relevanceReasons + antifactors + quote + marketingBlurb) with the external `sourceUrl` kept as a link-out. Stories are paged from `/api/stories` and embedded into the single `actually_relevant` collection, with each story's macro topic (mapped from its granular `issue.slug` up to its macro parent via the `/api/issues` taxonomy) kept in the payload `topic`. (2) **World Bank documents** (`src/extractors/world_bank_articles_download.py` + `src/utils/wds_client.py`, distinct from the indicators `wb_client.py`): per configured query it fetches the top-N docs from the WDS search API and pulls each doc's WB-provided plain text (`txturl` — **no docling**, keeps the image light), builds one entry per doc and lets the shared chunker split it into the single `world_bank` collection (all ~30 query terms share it, deduplicated by document id; each query term kept in the payload `topic`). It also takes extra **anti-bot care** for the Cloudflare-fronted document host (`documents.worldbank.org`): UA rotation + a browser header set (`Sec-Fetch-*`/`DNT`/`Referer`) in `wds_client.py`, a per-query `__cf_bm` cookie warm-up, a re-warm on every 403/429 before the retry backs off, and config-driven concurrency/retries/pacing (see the config bullet). These curated single collections (`actually_relevant` / `world_bank`) don't follow the `{topic}_{sentiment}_webhose` convention, so the agent RAG search (`agent/agent/tools.py:_sync_qdrant_search`) **always folds them in** (see `ALWAYS_SEARCH_COLLECTION_PREFIXES`, matched via `str.startswith`) regardless of the topic/sentiment filter, then ranks all hits by score. The Webhose collections carry the `_webhose` source suffix (`NEWS_SOURCE_SUFFIX`), which the agent RAG reader appends when reconstructing a collection name from a topic/sentiment filter (the Streamlit news page lists collections directly instead).

### `docling` (document conversion)

`docling/` is a small FastAPI service (flat layout like `downloader_extra`, port `8006`, uv-managed with its own `pyproject.toml`/`uv.lock`/`Dockerfile`, pinned `docling==2.110.0`) that converts document uploads to Markdown for the Streamlit chat's file input. Its only secret is `OPENAI_API_KEY` (`settings.py`) — PDF OCR/VLM inference is offloaded to a **cloud OpenAI-compatible endpoint** (`docling.vlm` in `config.yaml`: `base_url` + `model`), authenticated with that shared key. Layout: `config.py` (typed `docling` view of `config.yaml`, incl. the `vlm` sub-block), `settings.py` (the `OPENAI_API_KEY`), `converter.py` (the version-sensitive docling API surface, isolated in one file: `build_converter` wires `VlmConvertOptions.from_preset("granite_docling", engine_options=ApiVlmEngineOptions(url="{base_url}/chat/completions", headers={"Authorization": "Bearer …"}, …))` into a `PdfFormatOption(pipeline_cls=VlmPipeline)` with `enable_remote_services=True`; `SUPPORTED_FORMATS` maps the four extensions), and `main.py` (`POST /convert` multipart → `{markdown, filename, format}`, `GET /formats`, `GET /health`). **PDF** runs the VLM pipeline against the cloud OCR endpoint; **docx/pptx/xlsx** use docling's native backends (no VLM, no torch inference). Unsupported extensions → 415; a docling/VLM failure → 502. The converter is built once in the FastAPI `lifespan`; tests inject a fake converter on `app.state` so they run offline (no cloud VLM/GPU). When upgrading docling, `converter.py` is the single place to reconcile the API.

### `backup` (cloud backups)

`backup/` is a small **long-running** service (flat layout like `downloader_extra`) that periodically backs up both databases to a cloud remote via `rclone`. It is **off by default** (`backup.enabled: false` in `config.yaml`) and gated only by that flag — `docker compose up` starts it either way; when disabled it logs and idles on a `threading.Event` (so `restart: unless-stopped` doesn't restart-loop a clean exit). There is **no OS cron** — `main.py` is a simple `interval_minutes` loop (optional `run_on_start`, SIGTERM-aware), matching the repo's Python-everywhere convention. Each run (`backup_runner.run_backup_once`) is wrapped so one failure is logged and retried next interval rather than killing the loop.

Per run it: (1) `pg_dump -Fc` the Postgres DB as the **superuser** role (`backup_postgres.py`, password via `PGPASSWORD`, custom compressed format → restorable with `pg_restore`); (2) takes a **full-storage** Qdrant snapshot over REST (`backup_qdrant.py`: `POST /snapshots?wait=true` → stream `GET /snapshots/{name}` to disk → `DELETE` it server-side so Qdrant's ephemeral `/qdrant/snapshots` layer stays clean); (3) `rclone copy`s the staging dir (`postgres/` + `qdrant/` subdirs) to `<rclone_remote>:<rclone_path>` and prunes the remote `--min-age <retention_days>d` (`rclone_runner.py` — pure argv builders + subprocess). Staging is wiped before and after each run so only the current run is uploaded. Postgres and Qdrant live in separate stores, so backups are **not** cross-store transactional — the two captures just run back-to-back (as close in time as possible).

Config split follows the repo rule (no secrets in `config.yaml`): the `backup:` block in `config.yaml` holds the non-secret knobs (`enabled`, `interval_minutes`, `run_on_start`, `rclone_remote`, `rclone_path`, `rclone_config_path`, `retention_days`, `staging_dir`) and reuses the existing top-level `postgres:`/`qdrant:` host/port blocks; cloud credentials live in the gitignored `_container_data/backup/rclone.conf` (mounted read-only, `--config`). The container reuses the existing `POSTGRES_*` + `QDRANT__SERVICE__API_KEY` env — **no new `.env` vars**. The image adds `postgresql-client-18` (PGDG apt, matched to the server major) + `rclone` (multi-stage `COPY --from=rclone/rclone`). Restore is a rare manual op: `restore.py` (`docker compose run --rm backup python restore.py --list|--postgres FILE|--qdrant FILE`) automates `pg_restore --clean --if-exists` and downloads + prints the Qdrant full-snapshot recovery steps (a live-instance full-snapshot restore needs a boot-time `--storage-snapshot`, so it can't be a single API call).

### Langfuse (LLM tracing / observability)

A **self-hosted Langfuse v3 server** stack (`langfuse_web` + `langfuse_worker` + `langfuse_clickhouse` / `langfuse_redis` / `langfuse_minio`) traces the AI agent — every worker, tool, LLM and embedding call with its tokens, latency and cost. The server images are the `:3` line (ClickHouse-based); the **Python SDK is v4** (`langfuse==4.13.0`, OpenTelemetry-based) and versioned independently. Langfuse's relational metadata lives in a dedicated `langfuse` **database on the main `db`** (there is no separate Postgres container): `DATABASE_URL` points at `db:5432/langfuse`, and the `langfuse` role + database are created on first volume init by `_container_data/db/init/10-langfuse.sh` (Postgres runs `/docker-entrypoint-initdb.d` scripts before opening its network port, so the DB exists before `langfuse_web` connects — no crash-loop). On an **already-initialised** `db` volume the script does not re-run, so create the `langfuse` role + database manually (`CREATE ROLE langfuse LOGIN PASSWORD …; CREATE DATABASE langfuse OWNER langfuse;`) or start from a clean volume. The five containers mirror Langfuse's official self-host compose (only `langfuse_web` :3000 UI + `langfuse_minio` 9090/9091 are published; the rest are internal). On first boot `langfuse_web` **auto-provisions** an org/project/login from `LANGFUSE_INIT_*` (idempotent), so the project keys in `.env` work immediately with no manual UI setup.

**MinIO hostname gotcha (why traces can silently vanish):** MinIO (≥ 2025 releases) rejects any request whose `Host` header isn't an RFC-valid hostname with `InvalidRequest / "invalid hostname"` — and `langfuse_minio` contains an underscore. With the S3 endpoints pointed at `http://langfuse_minio:9000`, `auth_check()` passed and traces looked fine, but every OTel event/media upload from `langfuse_web` 500'd and **nothing was ever persisted**. Fix: `langfuse_minio` carries a hyphenated network `alias: langfuse-minio`, and `LANGFUSE_S3_EVENT_UPLOAD_ENDPOINT` / `LANGFUSE_S3_MEDIA_UPLOAD_ENDPOINT` target `http://langfuse-minio:9000` (the `depends_on` service ref and the Prometheus `tcp_connect` probe keep the underscore name — they don't send a Host header). Symptom to watch for: `docker logs langfuse_web | grep "Failed to upload JSON to S3"`.

Non-secret knobs live in the `langfuse:` block of `config.yaml` (`enabled`, `host` = `http://langfuse_web:3000`, `environment`, `sample_rate`); the keys are secrets in `.env`. Every integration is **gated by `langfuse.enabled` + the keys** — missing either degrades to a no-op. **Core services never `depends_on` Langfuse**, so a Langfuse outage can't block the app (the SDK batches + drops off the hot path). To rebuild the two trace-emitting images: `docker compose build agent downloader_general && docker compose up -d agent downloader_general`.

Per-service integration (each service carries its own duplicated `tracing.py`, per the one-container-one-copy convention — no shared package):

- **`agent`** (`agent/agent/tracing.py`): the LangChain `CallbackHandler` from `langfuse.langchain` is attached at the **graph level** in `MacroAgentGraph.astream_events` (alongside the existing `UsageTracker`), so the whole LangGraph run is one trace — every worker + `ChatOpenAI` call captured with model/tokens/cost. Trace attributes ride in the run config `metadata` (`langfuse_session_id` from `ChatRequest.session_id`, `langfuse_tags`) with `run_name="macro-agent-chat"`. The **vision** endpoint (`/plots/interpret`) is traced with a **scoped manual generation** (`start_as_current_observation(as_type="generation")`, image never sent — only mode + context). The RAG query embedding is intentionally **not** separately instrumented: the `langfuse.openai` drop-in **patches the `openai` module process-wide**, which would double-count every `ChatOpenAI` call already captured by the callback — so the agent avoids the global patch entirely (embeddings there are negligible and covered by the `rag_agent` node span). The Streamlit chat page (`app/pages/11_ai_agent_chat.py`) sends a per-conversation `session_id` (rotated on "Clear chat") so turns group in the Langfuse UI.
- **`downloader_general`** (`src/core/tracing.py`): no LangChain, so the `langfuse.openai` global patch is used safely (no double-count risk). `tracing.openai_client_class()` / `async_openai_client_class()` returns the instrumented client class when enabled, and the embedding downloaders (`qdrant_uploader.py`, `github_download.py`) instantiate it. `flush()` runs on shutdown via `atexit`. (The Streamlit `app` emits no traces of its own — the agent's graph-level callback already traces every chat turn end to end.)

**Cost tracking:** Langfuse computes cost from token counts × per-model prices. The custom model names (`gpt-5.4`, `gpt-5.4-mini`) aren't in Langfuse's built-in price list, so define them once in the Langfuse UI (Settings → Models) for cost to populate; token/latency/trace data flow regardless.

### Monitoring (Grafana + Prometheus + OpenTelemetry)

Container + service health/resource monitoring is **external** to the dashboard — a fully-FOSS **Grafana + Prometheus + OpenTelemetry** stack (four containers). Every container is treated as an **external service**: both its resource usage and its health are tracked from the outside, with **no per-service instrumentation** (no `prometheus_client`/OTel SDK in any Python service). It keeps reporting when the dashboard is down, covers **all** containers, and keeps history/trends. The pieces:

- **`otel-collector`** (`otel/opentelemetry-collector-contrib`, config `_container_data/otel-collector/config.yaml`) is the metrics source. The **`docker_stats`** receiver reads the Docker socket (bind-mounted read-only) → per-container CPU/RAM/net/block-io for **every** running container, keyed by `container.name` — this is what makes each container a monitored "external service" for resources, including the endpoint-less ones (`downloader_general`, `backup`). The **`hostmetrics`** receiver reads the host `/` (mounted at `/hostfs`, `root_path: /hostfs`) → host CPU/mem/disk/fs/net/load. It re-exports everything in Prometheus format on `:8889` (internal), with `resource_to_telemetry_conversion` so `container_name` becomes a Prometheus label. The **`otlp`** receiver (4317/4318) is wired but **idle** — reserved for a future app logs/traces phase.
- **`blackbox_exporter`** (`prom/blackbox-exporter`, config `_container_data/blackbox/blackbox.yml`) is the **health** half: an `http_2xx` module + a `tcp_connect` module. Prometheus drives it; the **target lists live in `_container_data/prometheus/prometheus.yml`** (the `blackbox-http` job: `agent`/`forecaster`/`clustering`/`downloader_extra`/`python_sandbox`/`docling` `/health`, `app` `/_stcore/health` (Streamlit), `triton` `/v2/health/ready`, `vector_db` `/readyz`, `langfuse_web` `/api/public/health`; the `blackbox-tcp` job: Postgres + the Langfuse backing stores). It yields `probe_success`/`probe_duration_seconds`/`probe_http_status_code` per service. Add/adjust a probe by editing the target list in `prometheus.yml` and restarting `prometheus`.
- **`prometheus`** (`prom/prometheus`, host `:9092` — 9090/9091 are langfuse_minio; config `_container_data/prometheus/prometheus.yml`) scrapes the collector (`:8889`), the blackbox probes, and Triton's native `:8002/metrics` (inference/GPU metrics). TSDB history persists in the `prometheus_data` volume (15d retention).
- **`grafana`** (`grafana/grafana` OSS, host `:3001` — 3000 is langfuse_web) visualises it. **Login required** (`GRAFANA_ADMIN_PASSWORD` in `.env`, username `admin`). Datasource + three dashboards (Containers / Host / Service health) are provisioned from `_container_data/grafana/` (`provisioning/` + `dashboards/*.json`); the dashboards are hand-authored against the OTel/blackbox metric names. State persists in the `grafana_data` volume.

Like Langfuse it's an **independent observability stack**: on the default bridge (so it reaches internal-only services by name), with **no `depends_on`** in either direction, so a monitor outage can never block or be blocked by the app. Telemetry/update-checks are off. The only new secret is `GRAFANA_ADMIN_PASSWORD`; other non-secret knobs (host/port) sit in the `monitoring:` block of `config.yaml`, which nothing in Python reads (it documents the ports the way `langfuse:` does).

## Conventions worth knowing

- Python services use **Polars**, not Pandas. Don't introduce `pandas` in new backend code (the Streamlit `app` carries `pandas` only because some Streamlit/Plotly widgets require it).
- **First-party charts are Plotly** in the Streamlit `app` (`app/core/plotting.py` — builders take theme tokens from `core/theming.py`; never hard-code a colour). Agent chat plot artifacts are also **Plotly** JSON (produced by the `plotly_agent` in `python_sandbox`).
- Agent worker outputs are **Pydantic models** (`agent/agent/schemas.py`); structured-output LLM calls use `with_structured_output(...)`. Adding a worker means: schema in `schemas.py`, tool wrappers in `tools.py`, node + supervisor routing in `graph.py`, and the worker name in `WORKER_NAMES`.
- The Streamlit `app` talks **directly** to the backend services (no BFF): page reads go through `core/postgres_client.py` (Postgres) / `core/qdrant_client.py` (Qdrant), and service calls through `core/api_client.py`. Add a backend endpoint → add its wrapper in `core/api_client.py`; don't call a downstream service ad-hoc from a page.
- Two Postgres roles: the superuser `POSTGRES_USER` (created natively by the `postgres:18` image at first volume init) is used by `downloader_general` / `downloader_extra` for ingestion writes; the read-only `POSTGRES_LLM_USER` is used by the `agent` (SQL worker) AND by the Streamlit `app` for all page reads. `db_bootstrap.ensure_llm_role` grants the read-only role `USAGE` on `public` plus `SELECT` on all existing and future tables (via `ALTER DEFAULT PRIVILEGES`) — don't grant it anything more.
- DB access: the `agent`'s `sql_agent` worker keeps raw `text()` because the LLM generates dynamic SQL; the Streamlit `app` reads Postgres with `connectorx` (parameter-free SQL strings quoted via `_sql_string`, bulk-loaded straight into Polars — see `core/postgres_client.py`). New **backend** services should use SQLAlchemy ORM (`select`/`delete`/`Session`) on `Mapped` models (`downloader_general` / `downloader_extra` writes) — don't sprinkle new `text()` calls there.
- Postgres database name resolution: every client (`agent/main.py`, `downloader_extra/main.py`, `downloader_general/main.py`, and the Streamlit `app`'s `core/postgres_client.py`) reads `POSTGRES_DB` (env) with `config.postgres.database` as fallback. `POSTGRES_DB` in `.env` is the source of truth. The postgres image itself only honours `POSTGRES_DB` at first volume init — changing the value on a populated volume requires re-creating the DB or wiping the volume.
- News embedding map: the Streamlit News page (`app/pages/15_news.py`) scrolls a collection's vectors from Qdrant (`core/qdrant_client.py`), runs dim-reduction + clustering via the clustering service (`api_client.cluster_dataframe`), and renders a Plotly scatter + cosine-distance histogram — all server-side in the app process (raw vectors never reach a browser).

## Rules of repo

- Use `uv` for Python package management (`pnpm` only for the `app/e2e` Playwright project)
- Use `ruff` for linting/formatting Python code
- Use `ty` for static type checking, most code of should be strictly typed
- Use pydantic for all data types validation
- Use `SQLAlchemy` as ORM, all tables in postgresql should be descriped in ORM
- Never push to github
- In commits messages use short and simple messages
- Tests for each service should be written
- For UI testing use playwright (`app/e2e/`), don't finish until the web UI looks clean and smooth, without bugs and visual problems
- Work only in `dev` branch
- If something is not even slightly understood (or some changes may seem unplanned) by you - always ask user
- On each step of development, update `CLAUDE.md`, `TODO.md`, and `CHANGELOG.md` (only for v0.x commits with tags, only major commits should have tag, after user confirmation)
- The frontend is a **Streamlit** (Python) dashboard
- For visualizations use the Plotly library
- Don't use small, not popular and unmaintained libraries in both `Python` and `TypeScript`
- Try to use best programming practices when needed/possible: oop, coroutines, decorators, logs, exceptions, strict dependicies, architectural solutions etc

---
> Source: [aleksey-karasev/Ultimate-Macroeconomics-Dashboard](https://github.com/aleksey-karasev/Ultimate-Macroeconomics-Dashboard) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-08-13 -->
