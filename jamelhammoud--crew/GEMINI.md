## crew

> Pool LLMs with friends. Open a project and you are in, share it when you want somebody with you, and everyone's local LLM CLIs become shared agents. Chat, docs, and a dashboard, synced through git.

# crew

Pool LLMs with friends. Open a project and you are in, share it when you want somebody with you, and everyone's local LLM CLIs become shared agents. Chat, docs, and a dashboard, synced through git.

## Commands

- `yarn dev` — run the app
- `yarn build` — build main, preload, and renderer
- `yarn start` — run the built app, wearing the icon it ships with
- `yarn preview` — the same run, wearing the blueprint
- `yarn test tests/<name>.test.ts` — one suite (vitest), which is how tests are normally run
- `yarn test` — the whole suite, which takes a long time. Only when asked for it
- `yarn tsc --noEmit` — typecheck
- `yarn covers` — draw every music cover on one page
- `yarn selection-check` — select everything in a real window and see what it painted
- `yarn preview-check` — stand a page up in a real window and see what it reached
- `yarn codex-check` — run a real Codex turn and read what came back off it
- `yarn kimi-check` — run a real Kimi turn and read what came back off it
- `yarn dist` — build a mac dmg
- `crew` opens a crew on a folder from a terminal. It ships inside the app, and the This computer page in the settings puts it on PATH. `npm link` in this checkout does the same from here

## Writing

- No em dashes, and no semicolons used in their place. Write plain sentences.
- The app is called Crew, and it is capitalized wherever it is named. It is a name, not a word.
- Plain words. No over-selling, no narration. State things simply.
- UI copy is for everyone, not just engineers. Avoid engineering jargon.
- Copy never echoes the request that produced it. Placeholders and empty states describe what the user can do in general terms, never the specifics of what was asked for or how it was built.
- A heading stands on its own. A line under it saying the same thing again in longer words, or explaining what the field below it is for, is a row of type spent on nothing. Write a second line only when it says something the screen cannot say without it, and never one that starts by naming what the thing is.
- Every line has to say something the screen cannot say on its own. Naming back what a person is already looking at is the obvious half of a bad line, and the flourish tacked on the end is the other: "cursors and all", "and more", "in seconds". Both are filler, and one screen of it teaches people to stop reading the words everywhere else in the app. Take the filler out, and if nothing is left standing, the screen is better with no line on it.
- Never explain the machinery. A platform that will not hand over a key, a model coming down, a fallback that exists because the good path sometimes fails: none of that is the person's to carry, and a line that carries it is the conversation that built the feature read aloud in the product. Write the consequence for the person, or write nothing.
- The skill in `.claude/skills/crew-copy` is the rest of it: the test a line has to pass before it is written, and where each rule came from.
- A line written from the brief reads like the brief. Copy that hands back the parts a feature was asked for, "agents you mention in board chat draw here too", is the request read aloud, and everyone can hear it. Write from what is in front of the person and what they can do next, and name none of the machinery behind it: not the panel, not the mode, not the mechanism, not the model.

## Code

- No code comments, in any file, ever. Humans add comments when they want them. A file already full of them is not an invitation to add another: matching the surrounding style is the one reason that never counts, because every comment in there was written by somebody who wanted it and none of them were written by whoever is reading the diff. Why a thing works the way it does belongs in this file. Why a change was made belongs in the message it was committed under.
- Small files. SOLID. DRY. Readable over clever.
- TypeScript everywhere. React and Tailwind in the renderer.
- Integration tests over unit tests. Every feature ships with coverage of its behavior. Tests live in `tests/` and boot real servers and runners on loopback.

## Design

- Dark mode. Tokens live in `src/renderer/src/styles.css` (Tailwind `@theme`): ink scale for surfaces (`ink-900` background, `ink-800` raised, `ink-700` borders and sunken bars), fg scale for text (`fg`, `fg-secondary`, `fg-muted`, `fg-faint`).
- White is the single action color. `positive` and `danger` appear only for status.
- Type ramp: xs 11, sm 13, base 14, lg 16. System sans. The word "Crew" is set in mono.
- Mono set on the same line as the sans comes out sitting high: the two faces carry their text at different heights, and a row centers boxes rather than baselines. `.mono-inline` in `styles.css` is the pixel that puts them back on one line, and it is what any mono beside sans wears.
- Radii: `rounded-card` (20px) for cards, `rounded-shell` (30px) for the composer. Buttons, tabs, and inputs are pills.
- A field that holds a paragraph is not a pill, and it is not a card either. `TextArea` in `TextField.tsx` is the pair to `TextField`, solid and on glass, and it wears `rounded-xl`: the curve that reads as a pill on a 36 high row reads as a lozenge on a 70 high one, and a card's 20 on a box that size puts the first line of type inside the bend. Squarer than the pills around it and softer than the card it stands on.
- Icons are Crew's own, and they all come from `src/renderer/src/icons`. Nothing imports an icon library. Every mark is drawn through `glyph()` in `src/renderer/src/components/glyph.tsx`, on a 24 grid with a round 2 stroke, against the keylines in `src/renderer/src/icons/keylines.ts`: a square sits on 17, a circle on 18.5, a rect keeps `sqrt(w * h)` at 17, a diagonal takes the whole 19.5 live area, a bare run of line sits on 15, a solid on 16. Equal on the ruler is not equal to the eye, which is the whole reason a circle is not 17. `yarn icons` draws the set on one page and measures every mark against its own family, and `tests/icons.test.ts` holds it there. The skill in `.claude/skills/crew-icons` is the rest of it. Three sets stand beside `icons/` for now and are drawn the same way: `src/renderer/src/design/glyphs.tsx` for the canvas, `src/renderer/src/components/toolGlyphs.tsx` for what an agent did, and `src/renderer/src/components/doc/docGlyphs.tsx` for the blocks a doc is written in. The one mark that moves, `ThinkingMark.tsx`, is drawn to the same grid from the numbers `toolGlyphs.tsx` holds. Nothing else draws an icon by hand.
- A divider inside a popover, a menu or a hover card runs edge to edge. It bleeds back through the padding the card holds its content in, rather than stopping short of both sides. `MenuDivider` does that for a menu, `CardRule` for a hover card. A panel that draws its own rows, like the Actions palette or the review card, takes `flush` on the `Popover` and holds its own padding, so a rule on one of those rows is already the full width. Never hand a `Popover` a padding class to cancel the one it wears: the two weigh the same, which one wins is decided by the order Tailwind happened to write them in, and the divider quietly stops short on both sides.
- Anything that floats stands above whatever opened it, and the app has one ladder for that. Chrome is `z-30` and `z-40`, the toasts are `z-[45]`, panels and the things the huddle puts on screen are `z-50`, a dialog or a takeover is `z-[60]`, and `Popover`, `Tooltip` and `HoverCard` are `z-[70]` above all of it. That last one is the whole point of the ladder rather than a detail of it: a modal can hold a menu, the agent maker holds two, and a floating layer underneath the dialog layer draws every one of those behind the card that opened it, which reads as a button that does nothing. Nothing is ever drawn above `z-[70]`, and a modal is never opened from a popover that is still open, which is the one case the order could not settle.
- The page is one rung of that ladder rather than a set of them, and `isolate` on the column in `App` is what holds it there. A number written inside the page is the page's own from then on, so the design board's header standing over the top bar is a thing settled between those two and nothing else, and the rail, the window corner and everything above them stand over the whole page whatever it does. Without it the two `z-50`s were a tie broken by the order the tree happens to be written in, and the header drew over the rail hovering above it.
- A popover opened from inside a popover stands beside it in the body rather than inside it, so every click on the one on top used to read as a click outside the one underneath, and the pair went down together. `Popover` tells the ones it stands inside where it is, all the way up, so a click or a scroll anywhere in the stack is inside every popover in it. That comes with using `Popover` and nothing has to be handed around for it, which is what lets the photo picker's own menu open off the face in the profile menu.
- A popover never closes because the page under it moved. The thread scrolls itself while an agent works, and every menu opened over it went down with the first scroll event that reached the window, which reads as a menu vanishing mid-aim. It follows instead: one hung off a button reads that button's box every frame, and one opened at a point takes hold of whatever was under the point and moves by however far that has moved since. A scroll listener is not what does this, because content landing above a message moves it without anything scrolling at all. A menu whose anchor has gone off the top of the view holds its place rather than closing, and a click outside or Escape is still the way out.
- A tooltip and a hover card do go down when the page under them moves, and the page under them is the one they are standing on rather than any page in the app. A scroll is listened for on the window in the capture phase, so every scroller anywhere reaches every one of them: the thread following a run took down the tooltip on Show panel and the card on the composer's own header line, neither of which had moved a pixel. `movedBy` in `components/movedBy.ts` is the one rule, and it asks whether the scroller that fired really holds the thing the card or the tip hangs off. The same question decides the other direction, which is `hoverCardIn` rather than a plain `hoverCardOpen`: a thread holds its own scrolling still for a card being read inside it, and a card being read in the chrome above it is no reason for the run to stop following. `tests/floating-scroll-probe.test.ts` holds both.
- Popovers and menus use the `.glass` class: semi-transparent dark, backdrop blur and saturation, like Mobbin. Chrome that floats over the design canvas adds `.glass-strong`, because the artwork behind it can be any color and a white frame turns plain glass into pale grey. That one is deeper and blurs twice as hard: a white shape on the canvas reads straight through an ordinary tint, and the rule is that what comes through a floating panel is the color of what is behind it, never its edges. The look is held in `--glass-bg`, `--glass-line` and `--glass-shadow`, so anything that has to wear glass without the class, like the chrome a library draws for itself, reads the same three values.
- Nothing on glass is set in a solid grey. Glass lifts with whatever is behind it, and a photo under a panel raises it past `fg-faint` and `fg-muted`, which then vanish. Text and marks on a floating panel take the foreground color at an opacity, `text-fg/45` for a quiet label and `text-fg/70` for a row, so they always sit above the surface rather than beside it.
- Nothing in the app is selectable until it says it is. The app is a window rather than a page, so the base layer in `styles.css` takes `user-select` off the body, and a field, a textarea and anything editable get it back at once. A button, a tab, a label, a heading and the line under an empty screen are things to press and read, and a drag that started anywhere near one used to paint half the chrome blue. Content asks for it back with `select-text` on the box that holds it, and only there: what somebody wrote, what an agent said, a plan, a diff, a command and what it printed. A marker standing inside one of those boxes wears `select-none` of its own, the `+` and `−` down a diff and the `$` in front of a command, so what comes away is the code and not the gutter. A click that closes what it is written over, the way a thought folds up, asks `selecting()` first, or a drag to copy the words takes them off the screen as it lets go. A placeholder is the one thing in a field nobody wrote, so a field only says it is selectable once it holds something: empty, it is a line of the app's own words standing in a box, and being inside a field is what painted it over on a select all. Nothing is lost by shutting it, since an empty field has nothing to take away, and it opens again on the first keystroke. The placeholder rule stays outside the CSS layer so unlayered library styles cannot override it. `tests/selection-rule.test.ts` holds the default and `tests/selection-probe.test.ts` holds the pair, a name shut and a message open, and `yarn selection-check` builds the real stylesheet, selects everything in a real window, and reads the pixels.
- The selection band is written down once, as `--color-selection`, and `::selection` reads it. Anything opaque drawn over text a person can select hides that band and has to paint it back: the composer covers the system emoji with a patch of its own surface, so it takes the textarea's selection and paints `bg-selection` under the sprite for any emoji the highlight reaches. Without it a highlighted line reads as a hole where every emoji stands.
- A picture standing on a character takes the room that character takes and never a pixel more, and that rule is about the caret rather than about the spacing. An emoji anywhere somebody can type is the machine's own glyph with one of ours drawn over it, and the caret beside it is drawn against the character's own edge and runs the height of the line, so the size of the picture is the whole of whether a caret can be seen. Drawn as tall as the line it takes the caret away outright, which is what a patch the height of the composer's own row was doing. Drawn as wide as the character it takes away one of the two sides, since a caret is drawn just left of the place it marks: the one after an emoji lands inside the character's own box and the one before it lands in the character in front. So the picture is a square shorter than the line, and in a doc it gives back the column at each edge as well. A line with a picture in it that cannot be typed beside is a line people quietly stop using.
- Nothing verifies that, and it is worth saying why rather than leaving a gap. A caret only paints in a window that has focus, so the one check that could read it would take the screen off whoever is working, which is not a thing to run on somebody's machine. `tests/emoji-caret-rule.test.ts` holds the rule that decides it instead, read off the stylesheet and off the composer, the way `tests/selection-rule.test.ts` holds the selection default.
- `color: transparent` is what takes the machine's own emoji away, and it is the only thing that does: it is a bitmap glyph on a Mac, so nothing about the font size or the family hides it, and it is why a doc can draw the sheet over the character it was written as without touching what is written down. It works inside a textarea as well as in ordinary text.
- **Anything set in a transparent color has to say what color the caret beside it is.** A caret with nothing said about it takes the color of the text it stands in, and the caret after a character belongs to that character rather than to the word after it, so hiding a glyph hides the caret next to it and the line reads as one that can only be typed into from the left. The composer never had it because its field already wears `caret-fg`, and the doc had it until `caret-color` was said on the span. It is not a thing the size of anything drawn over the character can answer, which is what made it look like a painting problem for as long as it did.
- Reusable primitives in `src/renderer/src/components`: `Avatar`, `AvatarStack`, `AgentIcon`, `Pill`, `Spinner`, `Skeleton`, `InsetRing`, `Popover`/`MenuItem`, `Modal`, `ScreenSwap`, `Select`, `SearchField`, `Tooltip`, `HoverCard`, `Composer`, `TopBar`, `DayDivider`, `ZoomView`, `Toaster`. Use them before writing new ones. Never use the native `title` attribute, use `Tooltip`. When something should be a primitive and there is no primitive for it yet, write one rather than a one-off.
- `Skeleton` is what stands in for content that has not arrived. One shimmer, and every instance is pinned to the same start time the way `Spinner` is, so a stack of them breathes together instead of rippling. Its own class carries `position`, and a plain CSS rule beats a Tailwind utility whichever order they are written in, so a skeleton that has to float over something goes inside a positioned wrapper rather than being handed `absolute`. Handed it directly, it keeps `relative`, collapses to nothing, and reads as an empty box.
- A tooltip never shows while the popover it opens is open. Pass `disabled` to the `Tooltip` around any button that opens a `Popover`.
- A line cut down to fit stands its whole self up on hover. The head of a thread's composer holds one line for what the thread is about, and the message it was opened on runs to paragraphs, so hovering it opens the rest of it in a card rather than sending somebody to the top of the thread to read what they are already looking at. `ThreadAsk` is that line and `askHasMore` is when there is anything to open: cut off in its own box, cut short by the title, or written over more lines than the one the header draws. A card that only says the line again is a card earning nothing.
- A `HoverCard` handed nothing to say does not stand, and that is said in the card rather than by whoever wraps it. A caller that swaps the card out for the plain thing underneath takes that thing out of the tree and puts it back, which loses whatever it had measured about itself, so a line that opens a card once it knows it is truncated would measure itself, wrap, unwrap, and settle on saying nothing.
- A card holding more than it can show is scrolled inside itself, so the page moving under it takes it down and its own scrolling does not. Read the plain way, one turn of the wheel closes the card being read.
- The box a `Tooltip` puts around what it wraps is a flex one, so the button inside it is the whole of it. An inline box would carry a line of text it has no text for, and the few pixels a descender leaves under the button are what push it off center in every row it stands in. Anything else that wraps a control for positioning has to be flex for the same reason.
- Agents always render `AgentIcon` (a deterministic generated pet seeded by the agent id), never an initial `Avatar`. Humans keep `Avatar`.
- A pet stands white on a picture of its own. The background is the same scene the music covers are photographed in, worked out from the agent's id, and `GeneratedField` is the one place that field is built: a helper's mark wears it through its own silhouette and a face wears it through a circle, so the two are one picture in two shapes rather than the same three layers written twice. The pet over it is solid white with its eyes cut out rather than painted, the way the arrow is cut out of the tile it stands in, so what comes through them is whatever the picture is doing behind rather than one fixed color over a surface that changes under it. Nothing is drawn under the white: a shadow lifts a mark off a photograph and it also makes it an object standing on one, and a face is a shape rather than a sticker. The hue the pet was seeded with is still there and is still the whole of what a board cursor takes its color from, since a cursor has to be one legible color and not a picture.
- Nothing a pet is made of comes to a corner. There are two families so that not every one of them is a blob, the curved one closed with quadratics through its midpoints and the angular one with flat sides and a count, and the angular one is turned at every vertex by `CORNER` rather than pointed. The cut is held under a share of the shorter of the two edges meeting there, or a corner on a short edge reaches past the middle of it, the two cuts cross, and the outline folds inside out. The stroke's own round join was already trying to do this at 3.5 and could not be seen doing it at the size a pet is worn.
- The picture under a face is stood back from the light, `FIELD_LIGHT`, so the white shape is the brightest thing in the tile. It is taken off the picture rather than laid over it as an ink: less light keeps every color in the palette and what each one is beside the others, where a black scrim is a grey film that flattens the whole of it and would put back the near black the covers are written to keep out. A helper's mark is the picture itself and carries nothing white, so it is left at the light a cover is photographed in, and the number is `pet.ts`'s rather than the field's for that reason. It matters most where a cover never has to work: a cover is looked at on its own and a face is worn at 20 across in a row of them, so the pale palettes are the ones this is set for.
- How much of the tile the pet takes is `BODY`, and it is applied to the shape rather than built into it, because it is a drawing decision and not something the seed answers. At its own size the body fills the circle and what is left of the picture is a rim, which reads as a white shape on a colored ring rather than as a shape standing on a photograph.
- The gap between the two eyes is opened at the size the pet is really drawn at, never seeded wider. Two holes closer together than `MIN_BRIDGE` are one slot, and a slot across the middle of a white shape is not a face: at 20 across, which is what a pet is worn at in a mention menu, the narrowest pair a seed hands out leaves under half a pixel between them and they merge. Equal on the ruler is not equal to the eye, the same reason a circle is not 17 on the keylines, and opening it at draw time is what keeps one pet one pet at every size rather than redrawing everybody's face for the sake of the smallest one it is worn at.
- `art/pet.ts` holds the pet's numbers and `AgentIcon` draws them, the same split the covers and the games hold, which is what lets `yarn covers` draw every face on one page and a suite read one without a canvas in the room. The order `makePet` reads its stream in is never changed: the numbers come off one stream, and reading them another way is a different face for everybody who already has one, which is why the corners were turned inside the shape it already had rather than by drawing another. `tests/agent-face-probe.test.ts` pins it.
- Either one can wear a photo somebody uploaded, and both look it up themselves rather than being handed it: `AgentIcon` by the agent's id, `Avatar` by the person's name, which is what a message carries and what the host keys people by. The one exception is the tray panel, which has no session to look anything up in and is handed the picture. `PhotoPicker` is how a face is changed, and it goes around whichever mark it is: the face itself is the control, hovered and clicked, and nothing anywhere says photo in words. What comes up on it is the upload mark, since the one thing a click does is put a picture on, and the scrim it arrives with stops under the presence dot rather than over it: the dot says whether somebody is here, and a face being hovered is no reason to stop saying it. That is a `z-10` on the dot inside `Avatar` and `AgentIcon`, because the scrim is drawn by the picker standing outside them and would otherwise win on order alone. It stands wherever your own face does, on your row in the Crew tab and on the face the profile menu opens on. An agent's photo is the owner's to set, and a person's is nobody's but their own: `member.avatar` carries no id, so the only face it can reach is the one who sent it.
- A mark never says where it sits in the row it is handed to. `Avatar`, `AgentIcon` and `AgentPhoto` carried `self-start` of their own, which beats the `items-center` of every row they stand in, so the pet rode high in the mention menu, the reply quote, the thread bar and the ask row. The row decides: `items-center` for a face beside one line, `items-start` on the few that hold a face beside a paragraph, which is a chat message and a feed card.
- Small interactions matter: hover states on everything interactive, `animate-pop` for popovers, `animate-rise` for feed items, scale on press.
- A button never grows on hover. Press is where a scale belongs, and a bar the width of a column swelling under the pointer reads as a wobble rather than as a lift. A solid button says it is hovered by going off white, `hover:bg-fg/90`, and an outlined one by its border and a wash.
- A box only clips while something is really travelling through it. `ScreenSwap` in a card wears `overflow-hidden` for the length of a screen change and `overflow-visible` the rest of the time, because anything standing past its own edge at rest is cut: a pill that presses, a ring, a shadow. The whole way in was drawn with flat sides down both edges before this.
- Nothing that fills a scroller may move. Padding is inside a height, so a box at `min-h-full` is already the whole of what the scroller can hold, and eight pixels of an arrival push it past the bottom: a scrollbar comes for the length of the animation, takes ten pixels off the width while it is there, and hands them back, which reads as the page jerking sideways as it lands. The way in animates the content inside that box, which has the padding to move in, rather than the box itself. Anything else that arrives, rises or springs inside a scroller is held to the same rule.
- Labels like "You" go in a `Pill`, never in parentheses.
- No logo or branding beyond the word "Crew". No emoji in the UI. Gradients only as scrims where content scrolls under chrome.
- The app icon is three overlapping discs, the same stack of members the app draws everywhere else. All three are the same size, each cut out of the one behind it by a mask, so nothing shrinks a disc. The tile is glass like the system icons: a top-to-bottom gradient, a sheen over the top half, and a rim that catches light at the top and bottom. Black and white, and it flips with the theme picked in the app.
- The same three discs are the mark in the top left of the app, in place of the word "crew", and they stand at the head of the way in for the same reason. `CrewMark` draws them at whatever `currentColor` is, and `live` is the one in the top bar, which `CrewLogo` wraps in the button that hovers and clicks it. `CrewLogo` takes the height it should be, so the one on the way in is the same mark at a larger size rather than a second drawing of it. Nothing anywhere sets the word in type.
- Touching it, the crew arrives. The three discs come in left to right, each one sliding in from its own right and settling into the stack the way faces do as people join a session, and the light runs across them as the last one lands. It is the mask that moves rather than the paint: a disc carries its own bite with it, so the gaps between them stay the width they are however far into its arrival a disc has got. Animating the paint instead closes the gaps as the discs grow.
- A disc slides in from the right and never from the left, whichever way round the queue runs. Coming from the left it starts life inside the bite the disc in front has already taken out of it, so the first thing to arrive is a crescent under half a pixel across, which reads as a hairline flashing at the end of the mark. From the right it starts in open space and is only carved once it is big enough for the gap to be the gap.
- What is behind the mask while it is touched is a mesh, cool to warm across the whole width, so each disc is a window onto one field rather than three tinted discs. It is the app icon's glass splitting light, which is the one thing the mark can say in color without becoming a second brand. Nothing in the palette is an ink or a near white, the same rule the covers hold. At rest it is `currentColor` and nothing else, and the mesh only drifts while the pointer is on it.
- A disc springs past its own edge as it lands, so the live mark stands in a box with `ROOM` around it rather than in the mark's own box. Pinned to the mark, the overshoot is cut off flat at the viewport, and a circle cut by a straight line is a thin white bar standing at the end of the mark for the few frames the spring lasts. The box still clips, which is what keeps the light and the flash from painting past it, and the drawing is asked for by the height the mark should be rather than by a class, so the room never shrinks the mark. `BLEED` is the other half of it: the mask region and every rect inside it run wider than the box, so nothing runs out of paint on the way through.
- Nothing the mark paints may take the pointer, and it is never redrawn to replay. Overflowing paint is still hit tested, so the light crossing under the cursor read as arriving at the button and leaving it again for as long as it travelled, and remounting the drawing under a pointer already standing on it has React say it arrived once more, so between them a single hover fired forever. The button's own box is the whole of what can be hovered, `CrewLogo` restarts the three one-shot animations through `getAnimations` rather than through a `key`, and a pointer already inside is not an arrival.
- The sound is `crew.mark`, three bubbles panned the way the discs land and rising through A major, with the low one and the sparks arriving under the light. It is the same material as the music, so the voices it is built from stand above `STRIKES` in `sounds.ts` rather than below it.
- A build run from source wears a blueprint: vivid blue paper, ruled and lightly grained, with the same three discs on it lit like glass. It is the one place in the project that is blue, so a dev window is never mistaken for the installed app sitting beside it in the dock. Both dev icons stand on the same paper, so only the mark flips with the theme, black in dark and white in light, the way the shipping pair flips.
- A disc is lit from the upper left and built in layers: a body that falls away to the far side, a shade drawn in all round the edge, a light rim on the top where the light arrives, a softer one on the bottom where the tile throws it back, a specular where the light lands, and a shadow cast into the gap behind. The two rims are the same ring gradient held to opposite halves by a mask, which is what makes a flat circle read as a bead. Every gradient is in bounding box units, so one set of them shades all three discs the same way. Keep it gentle: a black edge and a blown specular turn glass into a toy.
- `src/main/from-source.ts` decides which of the two a window wears, from where the app is loaded. Never `app.isPackaged` for this: that only asks whether the binary is still called Electron, and `yarn dev` renames it to Crew, so a run from source claims to be packaged and wears the shipping icon. The one way past it is `CREW_SHIPPING_ICON`, which `yarn start` sets, so the shipping icon can be seen without installing. Nothing else loosens the path check.
- Each disc is cut only by the discs standing in front of it, one mask each. A single mask for the whole stack reopens the gaps, which is invisible on the filled icon and turns the blueprint outlines into crossing rings.
- `yarn icon` redraws all of it from `scripts/make-icon.mjs` into `resources/` (`icon.svg`, `icon-light.svg`, `icon-dev.svg`, `icon-dev-light.svg`, `crew-logo.svg`, `tray.svg`, `icon.icns`, `icon.png`), `src/main/icon-png.ts`, `src/main/tray-png.ts` and `src/renderer/src/components/crew-mark.ts`. That is the only place the geometry lives. Nothing there is edited by hand.

## Layout

- `src/shared` — protocol and event types, git exec helper, no dependencies
- `src/server` — host: ws server, session state, `.crew/` persistence, git sync
- `src/runner` — agent runner: CLI provider adapters, executes prompts, streams back, auto-pulls the repo while joined
- `src/main` — Electron main and preload, wires server and runner to the app
- `src/renderer` — React app
- `tests` — integration suites

## The toolbox

A panel of tiles under the top bar. The built-in ones are the app's own hand, a huddle, a voice, a terminal, the project files, the music, the games. The rest are the ones a crew builds for itself, and they are shared the way todos are, so everyone gets the same toolbox.

- A tool is one button: a name, a mark, and the single thing it does. It opens a page, runs a command, opens a file, a doc or a board, asks an agent, says something in the chat, adds a task, writes a line in a doc, puts music on, copies something, or runs several of the crew's own tools in a row. `cleanTool` in `src/shared/toolbox.ts` is the whole of what a tool may be, and one with no name or nothing to do comes back as null rather than as a button that sits there doing nothing.
- Both halves stand on the same three columns, so the app's own hand and the crew's own tools are the same button in the same grid. A tile is the size it has always been and the panel is as wide as three of them and the padding, rather than the columns being cut to fit a width somebody picked: change the count and the width is worked out from the tile, never the other way round. The crew's row ends on the empty slot that opens the builder, which is the way in and the empty state both, so the panel never holds a second way to do the same thing.
- A built-in tile lights the way a built one does, while the thing it opens is live: in the huddle, playing the music.
- The toolbox is a builder rather than chrome, so it is set on its own two radii, `rounded-tile` for a tile and `rounded-field` for the fields under it, and both are squarer than the pills the rest of the app wears.
- A mark is one of the app's own drawings or one emoji, and `ToolMarkView` draws both, so the two are always the same size. The size is read off the class it is handed, which is where a glyph already reads its own weight from.
- Choosing a mark is a screen inside the panel rather than a popover of its own, and so is choosing what a tool does. A menu hanging off the side of the panel is a second box to aim at for a choice that belongs to the tool being built, and the grid of marks is bigger than the panel it would hang off. The same goes for anything else the builder ever needs to open.
- `TOOL_KINDS` in `src/renderer/src/components/toolKinds.tsx` is the one table of what a tool can do. The screen that picks a kind and the row in the form that says which one is picked both read it, so the words a person chose from are the words they are shown afterwards.
- The emoji grid is the one the reactions use. It is handed its columns, its size and its words, because the same grid reacts to a message in one place and marks a tool in another.
- A command written over several lines is several commands. The newlines are handed to the shell as returns, so each line runs in turn.
- A page opens in the side panel, or in the machine's own browser for a site that will not sit in a frame.
- An ask is a message in the chat, aimed at an agent by id so it cannot be lost to a rename. The agent a tool names may be offline on the machine pressing it, so whoever is here takes the work rather than nothing happening. Saying something is the same message without the agent, so a line written to name somebody still reaches them.
- A line written in a doc lands at the end of the page it names, after whatever is already there, and a page that has since been deleted is nothing rather than a doc written back from a tool. A task goes on the crew's list, for anyone or for the agent it names.
- Music is what everyone here gets, not what the machine pressing it gets. A list plays from the top and carries the list with it, so Next walks the rest of it. The huddle is the app's own tile and nothing a crew builds: a second way into the call would be a button that either does nothing or hangs up on everyone, depending on who pressed it.
- A word in braces is a blank the tool asks for when it is pressed, on a screen inside the panel the way choosing a mark is, so one tool covers every search, every branch and every ticket rather than one for each. What is typed lands in every place the tool named it, and it goes into an address as a query rather than as it stands, so several words are still one link. A brace with a dollar in front of it is a shell variable and is left alone. The one line in the builder that says any of this is written in braces itself, which is the whole of the explaining it needs.
- A chain is the crew's own tools, in the order they were picked, and it is built out of what is already in the toolbox rather than out of a second kind of tool nobody can see. It never names the tool being built, a tool it has already reached is passed over, so a pair that name each other is a short run rather than a locked window, and `STEP_LIMIT` is the end of it. Its blanks are asked for once across the whole run.
- Where pressing a tool leaves you is `opensPanel`, which a chain answers for every step it holds. A page, a file and a terminal are things the side panel holds, and an ask is a message, so it takes you to the chat instead. A doc and a board have tabs of their own. Copying, saying something, adding a task, writing a line and putting a track on all leave you where you are, so `saidAfter` keeps the toolbox up and the tile says what happened rather than a panel closing over nothing.

## Settings

A card with a rail of pages down the side, opened by your own face in the top right. Everything that is yours alone and everything the crew shares is on it, so nothing about either has a tab of its own any more.

- The face is the whole way in. There is no menu between the press and the card: a list of the same things one press earlier is a list to read before getting to the thing, and it grew a row every time the app did.
- `SETTINGS_TABS` in `src/renderer/src/components/settings/tabs.ts` is the one table of what there is. The rail and the page it opens both read it, so the word somebody picked is the word at the top of what they get. `state/settings.ts` is which page is up, held outside the tree the way a toast is, so anything can raise it and the card never stands inside whatever opened it.
- Two groups, You and Crew. The first row wears your own face and your own name rather than a mark and a word, because the face is already what says who the page is about.
- Pages stand beside each other rather than in front of each other, so nothing travels between them: one is simply the one that is up, and it fades. A page that slides in says you went somewhere, and picking a row in a rail is not going anywhere. It cannot move either, since a page fills a scroller and eight pixels of arrival is a scrollbar coming and going for the length of it.
- The card is glass, so nothing on it is set in a solid grey. Every word and mark inside takes the foreground at an opacity, `Select` and the agent cards included, which is why those were turned over when they moved here.
- A page is `Page`, `Section` and `Row` from `settings/parts.tsx`, and a row is what it is called on the left and what you do about it on the right. A line under the label only where it says something the row cannot say on its own.
- A rail row has to say what you get, so there is no General and no Advanced, and a page is never one row on its own. This computer is what that rule folds into: whether the machine sleeps, what agents running here may do, and whether `crew` is on this PATH. Three switches that were three pages, and they are one page because they are one idea, what this computer does while Crew is open. Helpers came off the Crew side to reach it, which is also where it always belonged: a helper runs a real CLI on somebody's laptop and spends their tokens, so the group it stood in was telling people the wrong thing.
- Keeping this computer awake is off until somebody says otherwise, because a laptop that never sleeps is a decision nobody made, and the app held that assertion the whole time it was open before this: every machine in every crew was kept awake by nobody's choice, and the setting is that behavior turned into an answer somebody gives. It keeps the machine up and never the screen, since work carries on with the display asleep and a screen lit all night is a cost nobody asked for. `src/shared/awake.ts` is the rule and `src/main/awake.ts` is the hold, split the way the update pill is, so a suite drives it without electron anywhere near it. Several windows on one folder is one machine and several switches, so the hold stands while any window is asking and the last one to stop is what lets go, a window that died with it on included. It lives in that window's `localStorage`, is said again on every start because main holds no copy of its own, and is never sent.
- The crew command is one row on that page: the command ships inside the app and this is the press that puts it on this machine's PATH. A machine with nowhere to put it says so itself, `off` from `commandState`, and the row is left out rather than standing there greyed. That answer is the machine's own rather than the renderer reading what the browser calls the platform, which is a guess made in the one place that cannot know.
- Files is one row, how big a file may be, and it is on the Crew side because it is one number for everyone. The rail already says whose a page is, so the row needs no line under it saying so.
- The agents live here now. The way in stands at the end of the list rather than off in a corner of the page, so it is the empty state and the way in both, the way the toolbox ends its own row on the slot that opens the builder.
- Nothing here is a second way to do something the app already does elsewhere. The huddle is the toolbox's, and the music plays from its own panel: what the settings hold of the music is the one thing that is yours alone, which is how loud it is where you sit.
- What a working run says about itself is on the Appearance page: the count of its tokens, which is on, and what those tokens are worth, which is off. `state/prefs.ts` holds the pair the way `theme.ts` holds the theme, so they are yours alone and never sent, and the crew sees whatever each of them has chosen. Both numbers are gathered whichever way the pair is set, so turning one on says something about the run already going rather than only about the next one.
- A price comes from the CLI wherever the CLI works one out, because that is the real figure rather than an estimate of it: Claude prices its own turn against the model it really used, cached prefixes and all, and that stands in place of anything worked out here. Everything else is priced off `MODEL_RATES` in `src/shared/pricing.ts`, which is the one table of rates and the one place a model is added. A model with no row there draws no price at all rather than a zero: a wrong number costs more than a missing one, and a zero reads as a run that was free.
- What a call of a model cost is read once, by `usageFrom` in `src/runner/providers/tokens.ts`, because the CLIs disagree about one of the four numbers. Anthropic counts a cached prefix beside the input and OpenAI counts it inside, and read the wrong way round the same prefix is charged twice at full rate. The end of a turn is `total` and stands in place of the calls it is made of rather than beside them, or every token in it lands twice.
- Feedback is a row on the About page, since it is about the app rather than about you or about the crew. It opens the card a memory and an emoji are added on, because writing feedback and writing a memory are the same question asked once: one field, one press, and the words are never a second screen away. It is not a rail row of its own, since a page whose whole body is one field is a page that is one row.
- The card is the field and the two buttons and nothing else. Crew's version and the system go on the mail underneath what was written, and no line anywhere says so: a sentence explaining what is being attached is the machinery read aloud, and it stands over the one thing the card is for.
- There is nothing behind it, so it is a `mailto` and the machine's own mail app is what sends it. `src/shared/feedback.ts` is the whole of what one is, kept out of the card so it can be read without a window near it: the address, the subject, and the body.
- A machine with no mail app set up fails exactly the way one with a mail app succeeds, so `shell:openExternal` answers whether it really opened. Silence there is somebody's feedback that nobody ever receives, which is the same rule the music holds about a track that will not load. Refused, the card stays where it is with the words still in it, says the address, and copies the whole of it on one press.
- `FEEDBACK_LIMIT` is well under what a shell will read, because every space is three characters by the time the shell sees it and prose comes out around a quarter longer than it was written. The field stops taking words at the limit rather than the end being cut off on the way out, and anything past what a shell will take is caught by the copy either way.

## Toasts

A word from the app about what just happened, standing under the header on the right for a few seconds. `toast()` in `src/renderer/src/state/toast.ts` raises one from anywhere, in the renderer or out of a handler, and `Toaster` is the one stack, mounted once in `App` and standing in the body.

- It is for a moment, never for a record. Anything somebody has to come back to belongs in the chat, in a thread, or on the row it is about, and a toast that says what a screen already says is a row of type spent on nothing.
- The tones are plain, done, fail and busy, and each one brings its own mark: nothing, the check, the warning, the spinner. A busy one waits rather than running out, and the handle it hands back turns it over: `toast.busy('Sharing the session').done('Link copied')` is one row saying two things rather than two rows.
- `key` is one row per thing. Asked for again under the same key it is rewritten where it stands, so the same word said twice is said once. Four rows is the whole stack, and the oldest goes when a fifth arrives.
- The row itself is the way out, and a button on it is the button's own. Nothing runs out while it is being read: the pointer resting anywhere on the stack holds every clock in it.
- The card wears `glass glass-strong`, the same material as anything else that floats over whatever the app happens to be showing. It says its own `--glass-shadow` rather than the popover's: a popover is one card and throws a drop deeper than the gap between rows, so four of them pool into one dark box standing behind the stack.
- A row opens and closes on its own `grid-template-rows`, which is the one way a box as tall as its text can be animated, and nothing anywhere clips: a box that clips its card clips the shadow with it, and the card reads as standing in a hole. The card keeps its height the whole way out and the fade is what carries it off.
- The server has a word of its own for this, `notice`, which goes to the one socket it is about and is never written down. It is what says why a command did not happen, so nobody else scrolls past an answer to a question they never asked.
- Being named and being answered are the same news, so a reply to something you wrote is a word in the corner the way a mention is. `memberReplyAlert` is that one, beside `memberMentionAlert`, and both are the same two clauses in front of them: never your own message, and never the thread you already have open. A message that mentions you and replies to you is one word rather than two, which the order they stand in settles rather than a rule of their own, and the mention wins because being named outright is the louder of the two.
- Anything worth a notification in the corner of the screen is worth a row in the app. There is one decision, `finishedAlert`, `memberMentionAlert`, `memberReplyAlert` and `questionAlert` in `state/alerts.ts`, and two places it is said: the row in the app and the banner from the system, both every time. Whether the window is the one being looked at decides nothing, because a toast is a few seconds and a banner is what is still there when somebody comes back to their desk, so the window having focus at the moment a run landed is no reason for the only lasting word about it to be missing. What the screen already says is left out by the rule itself rather than by the window, which is the clause about the thread somebody has open.
- What is already on the screen is not said again. A run finishing or a mention landing in the thread somebody has open says it there, which is the whole of what keeps this from being a toast per event. Nothing else about it is hidden: an agent finishing on another thread reads in the corner while you work in this one.
- `alertToast` in `components/alertToast.tsx` is what turns one into a row: the agent's pet or the person's face, the work under the title, Open as the way in, and a key of the thread's own so a second word about it is written where the first one stands. Opening a thread from anywhere goes to the chat, which is `App` watching `openThreadId` rather than every caller remembering to switch the tab.

## Typing

Somebody at a keyboard, said over the composer they are writing in: "Jamel is typing" and the app's own ellipsis after it.

- It is never written down. It rides in memory on the host and in `typing.room`, the way a call does, so an afternoon of half finished sentences is not something the crew scrolls past later. Nothing about it is an event and nothing about it reaches the log.
- The host holds it keyed by the connection writing, because two windows on one folder are one member and two people at two keyboards, and one person is one typist on the way out however many windows they have open.
- A window says it once every couple of seconds while the box has something in it, and says it has stopped the moment the box empties or the message goes. A ping that says what was already said is only the clock being wound, so nothing goes out for it and a keystroke is never a message.
- A window that dies mid-word never says it stopped, so the host lets go of it on its own after `TYPING_TTL`. The sweep is armed off the oldest entry and only while there is one, rather than run on a clock that ticks through every quiet afternoon.
- A window arriving mid-sentence is told once, straight after the welcome. Typing is too short lived to ride in the snapshot and too quiet to reach a new window on its own, since a ping only goes out when it changes something.
- Nothing typed in a ghost thread is recorded at all, rather than recorded and filtered on the way out. A hidden thread whose typing is not hidden is a hidden thread that leaks.
- `where` is which composer somebody is writing in, a thread's id or a board's id, and absent for the chat itself. `typistsIn` is what a line asks for, and it leaves out your own keyboard and everyone writing somewhere else.
- The line stands under the composer, in the room the padding already leaves, so it covers nothing and nothing moves when somebody starts or stops. Above the composer it was a glass pill laid over the last thing anybody said, which is the one place on the screen everybody is reading. Under it there is a strip of the app's own chrome and no message ever goes there.
- It is the quietest thing in the column: the smallest size in the ramp, `fg-muted`, no surface of its own, and it fades in rather than popping. A half written sentence is somebody about to say something and not something that has happened, so it is read out of the corner of the eye or not at all. The row above the composer is left to the way back to the foot of the thread, which is a thing to press and has earned it.
- The ellipsis is the app's own: the same three dots on the same grid carrying the same wave a thought carries, from `THINKING_DOTS`. Standing still they read as a sentence that was abandoned rather than as one being written. They stand in front of the name rather than after it, so a glance lands on a mark that is moving instead of on somebody's name.

## Huddles

Voice, video and screen share, started from the toolbox. The host relays the handshake and nothing else. The media itself goes machine to machine, everyone to everyone, so there is no server to run and no stream passing through the host.

- The call is never written down. It rides in the session snapshot and in `huddle.*` messages, never in the event log, so no peer id, no handshake and nothing about the media is ever committed or replayed. `src/server/session.ts` holds it in memory, keyed by connection, because two windows on one folder are one member but two people in the call.
- The record of a call is written down, and it is a different thing: `huddle.started`, `huddle.joined` and `huddle.ended` in the log, carrying who started it, who came, and how long it ran. Nothing live goes in them, and someone who comes back to the same call is named once. The chat block folds the three back into one card, and it knows whether the call it names is the one happening now by matching the room's own `id`. A start with no end reads as live forever, so a session coming back up closes any call it finds still open.
- Whoever started a call can take its block out of the chat, and nobody else can. It is a tombstone the way a deleted message is, `huddle.deleted`, and `huddleRecordId` is what takes the three events of that record out together, on the host as it lands and again on the way back in, so it stays gone after a restart. Only a call that is over can go: while it is going the block is the way in, and removing it would take the way in off the screen of everyone who has not joined yet.
- Every connection has the same three slots, negotiated once at the start and in the same order: microphone, camera, screen. Turning a camera on swaps a track into a slot that already exists. Renegotiating mid-call is what makes calls drop, so nothing after the first offer does it.
- Only one end of a pair ever offers, and peer ids decide which. The other end adds nothing of its own and takes the three slots the offer makes for it. Both ends offering is a collision, and the rollback that settles one does not give the local slots back: the browser makes a second set for the offer it accepted, so each side ends up holding three slots nothing is ever sent on. The call reaches connected, every packet arrives, and the room is silent. A slot added by hand is never used for a line that was offered, so the answering side must not make any.
- `yarn media-check` is the only thing that can see this. It runs the real connection code in two Electron windows and looks at what comes out the other end: three slots each, and sound and pictures arriving on the ones the app is listening to. A fake cannot stand in for it, and the fake passed the whole time this was broken.
- Glare is settled by comparing peer ids, the standard polite and impolite pair, and it is a guard rather than a path anything takes. Only the side that offers restarts a connection that failed.
- Signals are handled one at a time, in order, and an address that turns up before the description it belongs to is held rather than handed over. Two of them in flight together, or one offered early, is how a connection ends up with nowhere to send anything and sits there ringing forever. `tests/helpers/fake-rtc.ts` is strict about this so the mistake cannot come back.
- A handshake that goes quiet is said again, a few times, until it lands. Only that first exchange ever repeats. Once the two ends agree, nothing renegotiates.
- Joining never waits on a device. Someone with no microphone still gets into the call, muted, and is told what to fix.
- Which microphone and which camera is asked from the control that runs it, by the notch on its corner or by right-clicking it, and `DeviceMenu` is that one menu for both. The choice is kept in `localStorage` and used by the next call, so it is a setting rather than something to pick again every time.
- A device is asked for as an `ideal`, never an `exact` one. A microphone that was chosen once and has since been unplugged would fail the whole request as an exact, which puts someone in a call with nothing at all rather than with whatever is there. What the machine actually hands over is what the menu ticks, read back off the track, because the two are not always the same device.
- Picking a device while it is live swaps the track into the slot it already holds, and the one it replaces is stopped only once the new one is in. Nothing renegotiates, and there is no gap in what the others hear. Picking one while it is off only remembers it.
- Tiles are widescreen everywhere, and a tile owns its own shape. Wrapping one in something that sets the shape from outside is how they ended up as slivers.
- A shared screen is zoomed the same way a picture is. Both are drawn inside `ZoomView`, which owns the pinch, the two finger scroll, the drag, the double click and the percentage in the corner, so the gesture means one thing wherever it is made. It is handed the size the content is really drawn at rather than the size of the box around it, since a screen is letterboxed inside its frame and panning into the black is not panning at all. For a screen that size is `videoWidth` and `videoHeight`, which `VideoStream` reports as it lands and again whenever the far end resizes what it is sharing.
- Marks like the ring on whoever is talking are painted inside the box they mark, with `InsetRing`. Anything drawn around the outside is cropped the moment the box lands in a scroller or a card that clips, which is where the dock, the rail beside a shared screen, and the screen picker all put it.

## Voice

Talking to an agent out loud, back and forth. It is a tile in the toolbox and `/voice` in the chat, and it opens a screen of its own with the orb on it. Everything that listens and everything that speaks runs on this machine and costs nothing: whisper for the ear, kokoro for the mouth, both fetched once from their own model repositories and kept by the browser after that. There is no service behind it and nothing said in a conversation leaves the machine except the words themselves, which go to the agent the way a typed message does.

- A conversation is an ordinary thread. What was said goes in the chat, the agent does real work on real files, and the crew reads it afterwards, so voice is a way into a thread rather than a room beside one. The thread wears `voice`, and the host adds `VOICE_INSTRUCTIONS` to every prompt in it exactly the way plan mode adds its own. Nothing about the brief is ever a message, so the chat holds what people said and not what the app told the agent.
- That brief is the whole of what makes a spoken answer sound spoken. Without it a model writes three paragraphs of markdown, and a voice reads out every asterisk in them. It asks for a sentence or two in plain words, and it is what teaches the agent to draw a card.
- Both models run in workers of their own. A turn of whisper is a second of solid arithmetic, and a second of that on the thread drawing the orb is a second of the orb standing still. Nothing heavy is imported anywhere but those two files, which is what keeps the runtime and its wasm out of the app's own bundle.
- The listener is told nothing about the language. Whisper's English-only model carries it in the model and refuses to be told again: a `language` or a `task` on one of those throws before it reads a sample, so every utterance failed on the way in, nothing was ever sent, and the orb sat working forever while the room said the words again louder. `askedOf` in `models.ts` is the whole of that rule. `yarn voice-check` is the only thing that can see it, because it hands the real model real speech and reads what comes back, and every unit suite passed the entire time this was broken.
- Nothing about a turn fails quietly. A listener that fell over and a room that said nothing are the same empty string on the way back, so `hear` hands back null for the first and the screen says so, or a fault in the one part nobody can watch reads as an agent that is ignoring you.
- The microphone stays open the whole time, the agent's turn included, and `echoCancellation` is what makes that possible. While the agent is speaking the gate asks for more before it believes it has been interrupted, or the agent talks itself to a stop. It is `ear.guarded` that arms that, off the sentence leaving the speakers rather than off the turn opening, because a turn opens while the agent is still working and the microphone is the room's until a word comes out.
- Cutting in stops the voice and nothing else. Whatever the agent is doing to the project it goes on doing, because an interruption is somebody talking over an answer and not somebody undoing work. What the model had already drawn and not yet played is dropped rather than let out afterwards.
- `VoiceGate` is where one thing somebody said starts and ends. A level alone cannot answer that, so it tracks the floor the room is sitting at and opens on whatever stands well above it for long enough. The floor only moves while nobody is talking: tracked through an utterance it climbs into the voice and closes the gate mid-sentence. A click is one frame and never opens it, a cough opens it and is dropped for being too short, and a microphone somebody left open lets go on its own.
- Every sample is kept, through an `AudioWorklet`. An analyser hands back the last window it holds and nothing about the gaps between reads, which is fine for a ring around whoever is talking and useless for keeping the words. The few hundred milliseconds before the gate opened are kept too, or every utterance is missing the front of its first word and whisper guesses at it.
- The answer is spoken while it is still being written, and two things stop that being a plain hand-off of whatever just landed. A sentence is the smallest thing worth saying, so a part one is held back. And nothing past an unclosed fence is ever handed over, so a card is either whole or not there: `VoiceReply` is both of those, and the line it stops on can never fall inside a card, because a line ending inside one is still a line ending and the front of a card read out loud is a line of JSON.
- A card is the thing worth looking at rather than hearing. `VoiceCard` in `src/shared/voice.ts` is the whole of what one may be, five kinds and no more, and one with nothing on it comes back as null rather than as an empty box beside the orb. They arrive as the agent writes them rather than at the end, and the options on a choice are answered by tapping one, which goes in as a turn of its own so it reads back the way saying it would have.
- The orb is drawn the way the games are: `orb.ts` holds the numbers, `drawOrb.ts` holds the picture, and neither knows about the other. It is read off a real analyser hung on whichever of the two is making sound, never off a shape standing in for one.
- A circle that only changes size is a circle changing size. What makes one of these read as a voice is that the edge itself moves, and differently at different parts of it, so the outline carries a harmonic per band of what is being heard, low bands on the slow harmonics and high on the fast. Never the first harmonic: one lobe around a circle is the circle moved sideways, and the orb would wander about the screen while it talked.
- The light it throws is its own outline, stood a little further out and blurred. Light carries the shape of whatever throws it, so a round glow around a shape that is not round reads as a smudge the orb is sitting on. It is held inside the box it is drawn in, blur and all, and how far it stands out is worked back from `orbReach` every frame: paint that runs past the canvas is cut off flat at all four sides, which is a grey square standing behind a sphere, and a loud orb swells into that room without warning.
- The mesh is the agent's voice and nothing else. Your own turn leaves the orb in the one color the mark wears at rest, so who is talking is read off it without a word anywhere saying so, and the colors are the mark's own `MESH_COLORS` rather than a palette this invents.
- The renderer's CSP has to carry `script-src 'self' 'wasm-unsafe-eval' blob:` and `worker-src 'self' blob:`, or the worklet never loads and the runtime never compiles. Both fail quietly and both look like a microphone that does nothing.
- `tests/voice-listening.test.ts` holds the gate to all of the above, `tests/voice-reply.test.ts` feeds a reply in one character at a time and holds it to saying every word once and never a line of a card, and `tests/voice-mouth.test.ts` runs the speaker against a clock it winds by hand.

## Scribe

Hold a key, talk, and what you said is written into whatever you were typing in, anywhere on the machine. It is the same whisper Voice already runs, so there is no second model and no service behind it. Where Voice is a way into a thread, this never touches the crew at all: nothing is written down, nothing goes over the wire, and the settings live in `localStorage` the way a terminal is this machine's shell and not the crew's.

- The key is the whole feature and it is the one kind of key electron cannot register, because `globalShortcut` needs a real key in the combination and a bare modifier is not one. `scribe-keys.ts` listens under the window through libuiohook, bound over N-API the way node-pty is, so the binary loads in electron without ever being rebuilt for it. It is loaded through `createRequire` when it is first wanted rather than at the top of the file: a native module that will not load throws on the import, and an app that cannot start because dictation is unavailable is worse than dictation being unavailable.
- **Fn cannot be had, on any Mac, by any app.** macOS keeps it below the tap every app reads and it is not in the public hot key API, and libuiohook has no keycode for it either: `input_hook.c` reads four masks on `flagsChanged` and Fn is not among them. Right Option is the default instead, because it is on the built-in keyboard and Right Control is not. Never put Fn back in the list without a native addon of our own to back it.
- Four rules make a bare modifier bearable, and they are numbers in `scribeLatch.ts` rather than anything that owns a keyboard, the way the games hold their rules. It does not arm until it has been held alone, so reaching for a shortcut never raises the pill. Any other key pressed while it is down drops the take and lets the shortcut through, which is the whole of what makes plain Control safe to dictate on. A tap latches, so a hold is a hold and a tap is hands free, one rule rather than a mode to pick. And a press while it is latched is what ends it. A tap too quick to have armed still latches, or the key only works for people who are slow about it.
- There is a second way in that never goes through the hook, `Control+Command+Space` and `Control+Alt+Space`, registered permanently beside it. The Windows hook is known to go quiet after `getUserMedia` runs in a focused window, which is exactly what this does, so there is always a key that works and the settings page says outright when the good one is not being heard.
- The pill is a window of its own at `#scribe`, warmed at boot the way the tray panel is, and for a bigger reason: it holds whisper, and a model fetched at the moment somebody starts talking is a first dictation that arrives a minute late. It never takes focus. `showInactive`, `type: 'panel'` on macOS and `focusable: false` everywhere else, or the caret leaves the app the words were going to. `#root` wears `bare` there, since the glass is the whole of it and a surface behind that glass would be Crew's own color rather than the color of what is really back there.
- The pill is moved by taking hold of it anywhere on it, and it opens where it was left. Never moved, it stands at the bottom middle of the screen the pointer is on, which is the screen the app being typed into is almost certainly on. `scribe-spot.ts` is that placing as numbers and nothing else, the way `tray-position.ts` is, so a screen unplugged since and a drag that ran off the edge of one are both answered without a window anywhere near it. Two rules hold it still. It is placed once, when it is shown, and never again: placing reads the pointer, and the pointer has moved on by the time the renderer has measured itself, which is what made the pill land somewhere different every time. And growing is not moving, so a pill that grew a line keeps its bottom edge, which is what leaves the two buttons under the pointer already on the way to them, and writes no spot down, or a pill that grew once on one screen would be pinned to it by nobody's decision.
- A drag is a screen distance handed to main rather than anything the page does to itself, and it is measured against the screen and never against the window. The window travels under the pointer the whole time, so read the other way round every frame would move the pill by however far it had already moved, and it would run off the edge of the screen on its own. Main holds where it stood when it was taken hold of and works every move from there, so a drag that runs into an edge comes back off it as the pointer does. Where it lands is this machine's own, in `userData` beside the rest of what the app remembers for itself, and nothing about it is ever sent.
- Nothing can blur what is behind the pill, and that decides what it is made of. A backdrop filter samples the page it is drawn in, and behind a transparent window there is no page, so a heavier blur buys nothing at all and `glass-strong` is only a darker slab. `glass-pill` is lit instead, the way the app icon is: the tint falls from a lighter top to a darker bottom and a rim of light catches the top edge, which is the whole of what makes it read as glass with nothing coming through it but color.
- How far off opaque it may sit is decided by the words underneath. Nothing shows through at 0.97 and the pill is a slab laid on the screen; at 0.9 the line under it is a shape rather than a sentence, which is what frosted glass over text looks like; by 0.84 the words are legible through the pill and it reads as a smudge over the page. Dictation happens over text, so the ceiling is text and never a photograph, where more would only be prettier.
- The window is the pill plus `PILL_ROOM` on every side. A shadow is paint thrown past the box it comes from and a window ends where its bounds end, so a drop from a pill that fills its own window is cut off flat at all four sides, which reads as a pale oblong standing behind the pill rather than as a shadow under it. The drop is the pill's own and tighter than the popover's, written to land inside that room. Every rule in `scribe-spot.ts` is about where the pill may stand rather than where the window may, so the screen a spot is held inside is the real one grown by the same room, or the pill would rest a whole shadow's width in from every edge.
- That room is empty and swallows the clicks that land in it, and it is meant to. Electron does not pass a click through a transparent window on either platform, and the only way round it, turning the window's mouse policy over as the pointer arrives and leaves, forwards moves and never buttons: a drag in flight would lose its pointer up to the app underneath, which is the app the dictation is going into and the one thing this window exists to protect. So the margin wears `pointer-events-none`, nothing in the page acts on it, and a click there is lost for the few seconds a dictation is on screen.
- While somebody is talking the pill is the bars and nothing else. The one thing on it to press is the X, in a slot at its left that is shut at rest and opens under the pointer, so the pill is as wide as what it has to say and no wider: it stands over somebody else's work, and a button waiting there the whole time for the few seconds anybody reaches for it is their window spent on nothing. It opens from its own middle, which is what leaves it where it was put whether it is open or shut, and the gap in front of the bars opens with the slot rather than standing there empty. There is no way to finish on it: the key opened the dictation and the key is what ends it, and with the words already landing there is nothing left to hand over. A failure is the one state that opens itself, since it holds a sentence and a way out, and it is the widest the pill ever gets, which is what the window is sized from. The marks wear `STROKE_BOLD` rather than the weight a mark carries inside the app, because they are read at arm's length over somebody else's window.
- The bars are the whole of what the pill says, so they say the reading too. Once the key is up there is no voice left to draw, and a row sitting at its floor is a line of dots that reads as a microphone that has stopped rather than as work still going on, so a crest travels through the row instead, low to high: the same thing the thinking mark says with its three dots. `waveBands` in `media/bands.ts` is that reading and `tests/scribe-bars.test.ts` holds it to always having a crest in it. The bars grow off their middle here and stand on a floor in the music, and that is a word `Levels` is handed rather than a class, since `items-center` and `items-end` weigh the same and which one wins is decided by the order Tailwind happened to write them in.
- The mark is the pill's own bars. Voice wears the microphone and the two stand beside each other in the toolbox, so this one is what a voice does rather than what it is spoken into. Four of them and not five, since five across the same width close into a block at 16, and every one is centred on the same line: a set of bars standing on a floor is read as quantities beside each other, and one growing both ways off a middle is read as loudness.
- A long dictation is read while it is still being said. `take.ts` watches for the pauses and hands a finished stretch to whisper as somebody carries on talking, so by the time the key is let go only the tail is left. Every piece carries where it began, and the chunks come back shifted onto the take's own clock, because the gaps between them are half of what the marks are placed from.
- It is written as it is said, and that is the default. A stretch is a pause's worth of speech, which is a whole thought, so `ScribeFlow` in `scribeLive.ts` tidies each one on its own and writes it the moment it has been read, and every stretch after the first carries the space that keeps it off the end of the one before it. Never tidy the whole dictation again on each stretch and paste the difference: the mark at the end of a sentence is placed from the silence after it and a correction reaches back a sentence, so what is already on somebody's screen would change under words nothing here can take back. What has gone out has gone out, and a correction reaches back over what is still being held and no further.
- The store walks those stretches one at a time down a single chain, so two of them can never land the wrong way round however they were read, and a stretch that has been written is off both the pieces and the sound. Only what never landed is left for Try again, so a failure halfway through writes the rest rather than the whole of it. A walk still waiting on a read nobody is coming back for is left where it is rather than cancelled: it comes back to a list that is not the one it set out down and stands itself down.
- The paste is one thing at a time and the clipboard is borrowed once for the run. Two pastes closer together than `SETTLE_MS` and the second overwrites the clipboard the first is still on its way to read, which lands one stretch twice and loses the other, and giving the clipboard back between two of them puts somebody's own text in front of a paste already in flight.
- Nothing is pasted while the key is down. The modifier being held is composed into the keystroke, so the app on the other side is handed a shortcut nobody pressed, which is why main holds a dictation on Hold back to the end and writes it the way it always did. And the app must never hear its own paste: that keystroke goes out through the same hook the key is read with, so a dictation held on Command, or on Control where Control is what pastes, would be ended by the words it was writing on the way past. `ownKey` in `scribe.ts` is the whole of that rule.
- Writing as you talk is only ever a paste. A dictation on its way to the clipboard has one place to land and lands there once, so the row is left out rather than greyed when Copy is picked.
- Nothing spoken is ever a mark. `tidy` in `scribeTidy.ts` places them from whisper's own punctuation and from the gaps, so "the period after the war" is a sentence about a war. It also drops the fillers, collapses a word said twice, and cuts a correction back: one standing at the head of a sentence takes the whole sentence in front of it, which is how people really dictate. One sentence back and never two.
- A bare "hold on" is not a correction. "Hold on, let me check" is somebody asking for a moment, and taking the line before it away is the one mistake here nobody can see happening. "Actually hold on" is a retraction and is in the list.
- The reading runs on `device: 'webgpu'` where there is a card and falls back to `wasm` where there is not, which Voice gets for free. It falls back twice: once at load, and again if the first inference throws, because a card that took the model and then could not run it is the one failure no check before that can see.
- A failure keeps the sound. Nothing said twice, so trying again costs one press, and what goes to the worker is a copy rather than the buffer itself, since handing a buffer over empties the one here.
- A dictation started with the caret nowhere is held rather than lost. Scribe writes into whatever has the caret, so one begun with nothing focused was read, tidied and pasted into the desk, and the words were gone: the one failure here that costs somebody the whole thing they said. They stand on a card instead, on the pill's own window, with the line about clicking into a box, the words, Copy and a way out. `landingFrom` in `src/shared/scribeLanding.ts` is the rule and `askCaret` in `src/main/scribe-caret.ts` is the asking, kept apart so the rule can be read without a keyboard anywhere near it.
- The two ways of being wrong do not cost the same, and that settles every line of the rule. Held words that could have been pasted are on screen, one press from where they were going. Pasted words that had nowhere to go are gone. So a doubt is held, and only an outright refusal holds anything back: `unknown`, which is every machine that is not a Mac and every machine that will not answer, pastes exactly as it always did.
- **A missing focused element is two answers, and the focused window is what tells them apart.** That one extra read is the difference between the feature working and it saying "click into a text box" over an open composer. With a focused window it is a Chromium application that has not built its accessibility tree: it builds one only once something asks the way it is waiting to be asked, nothing here does, and so Crew, Discord, VS Code and Chrome all hand back `missing value` the whole time a caret is sitting in a box. That is a silence, and a silence is `unknown`, so it pastes the way it always did. With no focused window nothing in that application is open, which is Mail, Messages, Terminal and ChatGPT with every window shut, and there really is nowhere for the words to go, so that is the `none` the card exists for. Read without the window, every field anybody really dictates into was held back while nothing at all was caught.
- Nothing ever turns another application's accessibility tree on. `AXManualAccessibility` is somebody else's process and the tree costs them for as long as it is on, almost nothing honours the write anyway, and Chrome wants `AXEnhancedUserInterface` and up to several seconds to answer after it, which is longer than anybody waits to start talking. The one app whose answer is worth having is answered without asking macOS at all.
- Crew answers for itself, because it is a page and a page already knows what has the caret in it. A focused window is the whole of what says the app is frontmost, since Electron answers null for every window while somebody is in another application, so a null is the caret being somewhere only macOS can say. That makes our own windows the one place a `none` is really earned: the caret is in Crew, it is not in anything anybody can type in, and the words are held. The side panel is the exception, because it holds a real browser: the caret can be in a field on somebody else's site while the page around it sees only the box that site is drawn in, so anything holding a page of its own is the doubt it really is rather than a `none`. `landingInPage` is that rule and `FOCUSED_IN_PAGE` is what reads the page, kept beside it so `tests/scribe-caret-probe.test.ts` can put a real document through the pair. Editable is asked twice, off the property and off the attribute, because the property is what carries the answer down into a doc's own box and the attribute is what a box that says so itself carries.
- A role cannot answer this and the caret's own attribute cannot either. A box somebody wrote themselves comes back as a group in one app and as a text area in the next, so a list of roles to trust is wrong on the next app anybody opens, and inside a page `AXSelectedTextRange` is handed to buttons and to the page itself, so the one thing that reads as a caret is on things nobody can type in. Inside a page, which is what `AXDOMClassList` and `AXDOMIdentifier` say, `AXEditableAncestor` is the whole answer. Outside one, a role that names a field is taken at its word and the caret attribute is the fallback. `AXRole` is read as an attribute and never through System Events' own `role`, because the two disagree: a page with nothing focused comes back as a group from the property and as the web area it really is from the attribute, and that web area is the case the whole feature exists for.
- It is asked twice and neither ask is in anybody's way. Once as the key goes down, which is free because the whole wait sits behind somebody talking, and once more only when that first answer was a refusal, since somebody who has clicked into a box since is somebody who wants their words in it. The second ask only ever costs the case that was about to lose them.
- Every name in the AppleScript is short and plain because it has to be. AppleScript has a reference form for half the words a person would reach for, so `front`, `focused`, `role` and `named` are property forms rather than variables, and each one fails to compile in a way that reads exactly like the Accessibility permission being refused.
- The words are held in main and nowhere else, because main is the one place both ends can be reached: the card is drawn in the pill's window and Copy is the machine's own clipboard, which a window that never takes focus cannot reach on its own. Stretches join there as they arrive, so the card says the whole of what was said rather than a row for each piece of it.
- The card outranks the setting that puts the pill away. Whoever is looking at it has not copied the words yet, and a window that rested on the way past would take the only copy of what they said with it. Starting to talk again is what lets go of it, since words held behind a dictation already running are words nobody is coming back for.
- It waits for the dictation to be over. Stretches arrive while somebody is still talking and the bars are the whole of what says Scribe is still listening, so a card opening over them mid sentence takes that away. A failure comes before it for the same kind of reason: a read that fell over halfway has stretches held and a Try again for the rest, and the card drawn over that would be the words so far standing where the way to the rest of them was. Closing the failure is what shows the card, since the words are still held.
- `yarn scribe-caret-check` is the only thing that can see the rule. It asks real applications what they really answer and prints the branch each one came out of, without activating anything, because the one thing a suite cannot say is whether a real Chromium button really hands back a caret attribute. It does, which is how that case was found.
- `yarn scribe-check` is the only thing that can see any of this. It hands the real model real speech, reads the timestamps it really returns, and puts them through the real tidying, and it says how it went on a line of its own rather than in its exit code: the runtime whisper runs on leaves threads that abort while node is shutting down, and an abort is a signal, which is a check that says it failed after saying every word of it passed.
- `tests/scribe-tidy.test.ts` is the suite that says the feature works, and it runs without a microphone anywhere near it. `tests/scribe-keys.test.ts` drives the key by hand and `tests/scribe-take.test.ts` drives the microphone against a worklet that never really runs. `tests/scribe-landing.test.ts` holds the rule to real roles and real attribute lists, `tests/scribe-held.test.ts` holds what is done with the answer, and `tests/scribe-held-card.test.ts` renders the card and presses both of its buttons.

## The menu bar

A mark in the macOS menu bar, and a panel under it holding who is here: the people online and the agents working, the same list the presence popover in the app shows.

- The icon is the same three discs, drawn once by `yarn icon` into `src/main/tray-png.ts` as black art on nothing. macOS reads a template image by its alpha alone and tints it to whatever the bar is wearing, so one image covers a light bar and a dark one, and nothing has to follow the system theme by hand. Everywhere else the taskbar has no template images, so the tray keeps the dot it always drew.
- The count of tasks waiting for review rides beside the mark as the item's own title, in the system's digit face. A badge painted into the artwork would be a smudge: a menu bar item is one line tall, and the number is the thing being read. It is the same count the button in the app carries, through the same `badgeText`, and it moves the moment the app's own badge does.
- No context menu is set on the mac tray. One would open on a left click too, and the left click belongs to the panel. Right click pops the menu itself.
- The panel is a frameless window that loads the renderer at `#tray`. It never connects to the session: a second connection would be a second member. The window that is open publishes who it can see, main holds it, and the panel is handed it. With no window open there is nothing to publish, so the panel says to open Crew rather than showing a list that stopped moving.
- It is built and loaded before anyone clicks, the way a terminal is warmed, so the first click opens a list rather than an empty card. It measures itself and asks main for the height it needs, so the window is only ever as tall as what it holds.
- Two things about that window empty the dock, and both are one line each. `skipTaskbar` turns the app into an accessory on macOS, and `setVisibleOnAllWorkspaces` does the same transform unless it is passed `skipTransformProcessType`. Either one and the icon leaves the dock and does not come back. `createPanelOptions` in `window-options.ts` is where the first is kept out.
- macOS will not hand out the highlight it draws behind a menu bar item unless the item is opening a native menu, and Electron dropped `setHighlightMode` in version 9. A panel of one's own does not get it.
- A menu bar item is as wide as its image plus eight points either side, and as tall as the bar, which is 24 on a normal display and 33 or more on a notched Mac. The image is centred in that and never scales it, so nothing drawn into the artwork can line up with the item's own edges.

## Design boards

Crew owns the board engine in `src/renderer/src/canvas`. It keeps the persisted record and snapshot format stable so existing boards, server messages, and agent operations need no migration.

- `canvas/index.ts` is the renderer-facing boundary. Design code imports canvas behavior from there and no external drawing engine sits behind it.
- Shapes are DOM elements under one camera transform. The overlay canvas paints selection chrome, resize and rotation handles, brushes, snap indicators, and shape handles in page space.
- The store separates document, session, and presence records. Remote changes never enter local undo history. Loaded version-2 schema metadata is preserved unchanged when a board is saved again.
- The canvas draws straight. Sans type, solid strokes, straight lines, no hand drawn wobble. `DESIGN_STYLE_DEFAULTS` feeds both `applyDesignDefaults` and `designops`.
- Defaults are applied on mount and after every snapshot so the next shape is styled consistently.
- Nothing wears a text outline. `shapeUtils.ts` turns it off for text, geo, and arrow labels.
- A frame is white in both themes unless its `meta.background` says otherwise. `frameFill.ts` owns the fill and contrasting outline.
- Artwork colors never follow the app theme. The editor stays in one color mode while Crew chrome and the surface around the artwork may flip.
- A board mention wears `FrameGlyph`. `BoardImage` renders its preview through the synchronous owned SVG exporter and holds a `Skeleton` over the image until it has painted.
- The left panel is layers when nothing is selected and the inspector when something is selected. The right panel is board chat. Boards already serve the role of pages, so there is no page switcher.
- The inspector follows Figma's section order: Position, Layout, Appearance, Typography, Fill, Stroke, Effects. Unsupported fields are omitted.
- Every number field supports arrow keys and horizontal scrubbing. `useNumberField` marks and squashes the whole scrub into one undo entry.
- Typography lives in `meta`, except alignment in shape props. `TextUtil.tsx` uses the same style for measurement and painting. `DesignTextUtil` measures again when a web font arrives.
- The contextual rich-text toolbar supports bold, italic, code, links, lists, and highlighting. Stored ProseMirror documents and marks render unchanged.
- There is one inspector for every shape kind. `nodeView.ts`, `designView`, `frameView`, `textType.ts`, and `paletteView` map each record into it.
- Shapes made with the shape tool are Crew design nodes. Their outlines live in `nodeShape.ts`. A rectangle alone supports corner radius and auto layout.
- Hidden fills, strokes, and effects remain in the record with `visible: false`. Every renderer and exporter honors it.
- `ColorPicker.tsx` owns saturation, hue, alpha, hex, eyedropper, and Crew swatches. `.design-picker` in `styles.css` sizes the picker.
- `wholePixels.ts` rounds position and box edges after create, move, and resize. Lines and freehand strokes only round their position.
- `SelectionOverlay.tsx` adds the size readout and Crew node radius handles. The owned overlay canvas provides the standard selection, brush, handle, and snapping chrome.
- Board cursors live in `cursors.tsx` and use `--crew-cursor-<type>`. `applyDesignCursors` writes them on the canvas container, while `applyToolCursor` updates the active placement cursor.
- Remote cursors are Crew's arrow plus a glass name tag, agent pet, or human avatar. Presence records still carry remote selection state.
- A cursor leaves when the pointer leaves the board while the person's selection remains until their page becomes null.
- Point and gap snapping are both live during translation and resize. Indicators are collected again after the snap so every resulting alignment is shown.
- Every canvas action lives in `DESIGN_COMMANDS`. Context menus, Actions, and keyboard shortcuts read the same catalogue.
- A mask reparents the rest of the selection into its bottom shape and clips them to that shape's sampled outline.
- Asking an agent is available from the context menu or `⇧⌘A`. The ask bar stays inside the stage and grows away from the selection.

## Tickets

`/tickets` opens a thread with a board beside it, in the side panel where the plan stands. It is what the work is broken down into, what is being worked on now, the questions the agent answered for itself, and what it has finished and wants a look at.

- The board is Crew's own, and that is the whole of why it works. It was read off the task list each CLI keeps for itself once, and one vendor renaming its own todo tool took the columns out overnight: the list stopped arriving as a list and started arriving one task at a time, so there was nothing to fold. The harness lives here now. `/tickets` on the host's own http is the way in, exactly as `/agents/spawn` is, so it is a curl line in the preamble rather than a feature of any one CLI and every provider gets the same board at once.
- Four calls and no more: put the tickets up and get an id for each, move one, name a decision, raise a question. `ticketPreamble` in `src/shared/tickets.ts` is the whole of what an agent is told, and it is written on the machine running the agent rather than on the host, the same reason the helpers and the design board write theirs there: only that side knows the address it reaches the session at. Every call names the promptId of the run asking, and the host takes it only while that prompt is one it is running right now, so a board is reachable from the thread it belongs to and nowhere else.
- Nothing about a board is state the host holds. Each call is written down as one event on the thread, `ticket.added`, `ticket.moved`, `ticket.decided` and `ticket.asked`, and `boardOf` folds the whole board back out of them, so it replays for free and is the same on every machine.
- They are left out of the chat's own events and carried in the snapshot as a list of their own, the way the shelf and the todos are, and the window holds them in a slice of its own for the same reason. Filtered out of the snapshot and nowhere else, a board is whatever has happened since the window last connected, which reads as every board emptying on a reload.
- The list a CLI keeps for itself is still read, and it is the fallback rather than the board. A thread that never put a ticket up gets rough columns off `stepTodos` in `src/runner/providers/detail.ts` for nothing, and the moment the agent puts its own tickets up those win outright. Never let the two run together, or the same work stands on the board twice under two names.
- The one line a step says about a list is still narrated, "Drawing the rows", because that is what a live step reads like. A ticket is the same item named rather than narrated, "Draw the rows", which is why `activeForm` wins in one place and nowhere else.
- A question never stops the work. The agent raises it, answers it for itself, and carries on, and the row says what it assumed. Answering costs one press, and what answering late would cost is on the row as the number of files that have changed since, so interrupting is a number rather than a feeling.
- A decision hangs off the ticket it names, or off whatever is on doing when it was written, so an agent that names nothing still lands it somewhere true.
- Which files a ticket touched is the ones that changed while it was the one on doing, which is why the steps and the events are read as one run of time rather than one after the other.
- In review is the column that pays for the rest. Everything else is already readable in the thread, and this is the agent saying a slice is finished. Both ways out are real messages: a sentence goes back as a steer aimed at that slice, and picking the ticket up again is what the board reads as the way out of review.
- What you have dealt with is yours rather than the crew's, so a question you answered and a review you have looked at are held in the window, in `state/board.ts`, and never written down. Two people each get their own review queue, which is what In review means.
- The columns run in the order they want you, not the order work travels: a review, then what is being worked on, then what is next, then what is done. The panel is 480 across and a ticket is a sentence, so they are sections down the panel rather than columns across it. Four columns at 480 is 105 pixels each.
- Claude and Codex take a steer mid-run, so anywhere else an answer joins the queue.

## Memory

What the crew has learned, in one list every agent on every machine reads before it starts. It is three events in the chat log, so it syncs for free, replays for free, and nobody scrolls past the moment a fact landed.

- The two halves are deliberately uneven. Reading is free: the host puts the whole list on the prompt itself, beside the tickets and the helpers, so every agent has it in front of it before it does anything and nothing has to be fetched, remembered or polled. Writing is a curl on the host's own http, the way a ticket and a helper are, so every provider gets it at once rather than one CLI's own tool. A store an agent has to remember to open is a store most runs never open, which is the whole reason the reading half is not a call.
- Every memory is in every prompt, so the list is a budget rather than a store. A line is `MEMORY_TEXT_LIMIT` and the list is `MEMORY_LIMIT`, both in `src/shared/memory.ts`, and anything longer than a sentence is a doc. A full list is refused with the move to make, "Take one out before adding another", rather than the oldest being dropped for somebody: a fact quietly falling off the end is worse than a call that failed and said why.
- One list, not one per agent. Cross agent is the whole point of it, and a scope would be a second thing to explain on every screen and in every preamble for a distinction nobody asked for.
- The same fact twice is one memory, keyed on the words the way `askKey` keys a question, so the second call comes back with the id the first one got rather than writing a second row that says the same thing.
- An id is six hex characters rather than a uuid. It is written out beside every line, sixty times over, in front of every agent on every run, and 36 characters of noise a row is a paragraph of nothing.
- A ghost thread is refused in words. Its events never reach the log, so a memory written from one would either vanish with the window or leak the hidden thread into the crew's own history, and there is no third answer. Refused, the model reads why instead of watching a call quietly not appear.
- A write over http is refused unless the promptId names a run the host has going right now, which is the credential the board and the helpers are already reached on. A write over the socket is refused from a runner, the way the music controls are: an agent's machine is connected the whole time it is joined, and the door it writes through is the live prompt.
- The three events fold off the snapshot the way the toolbox does, so the memory rides in `SessionSnapshot` as a list of its own and a fact learned months ago is still a fact after its event has fallen off the window.
- Whoever wrote what is there now owns the line, so rewriting one puts the new name on it. The row wears `AgentIcon` for an agent and `Avatar` for a person, which is the one thing a list of sentences cannot say about itself.
- The page is under Crew in the settings, since it is everyone's. The way in stands at the end of the list, so it is the empty state and the way in both, and adding and editing are one card because they are one question. What comes back from a refusal is said on that card rather than somewhere the person is not looking.
- The line under the heading is the one page that has earned one. A list of sentences and a button cannot say that every agent reads them before it starts, and that is the whole of what makes this different from a notes page.

## Docs

A page of the crew's own writing, block by block, in a BlockNote editor crew draws all the chrome for. Every panel BlockNote ships is turned off in `DocEditor` and replaced: `DocSlashMenu` for what `/` opens, `DocToolbar` for a selection, `DocSideMenu` for the gutter, `DocEmojiMenu` for `:`. They are crew's own glass menus, the same rows the mention menu uses.

- A doc is a markdown file the crew syncs, so the editor only offers blocks markdown can carry. `DOC_BLOCKS` in `docBlocks.ts` is that list, one entry holding the title, the mark, the aliases and the shortcut, and both the slash menu and the turn into menu read it. A toggle list is left out on purpose: it comes back from markdown as a bulleted list, and a block that quietly changes on the next sync is worse than one that was never offered. `tests/doc-blocks-probe.test.ts` puts every entry through a round trip, so nothing can be added that the file cannot hold.
- The marks are drawn in `docGlyphs.tsx`, on the same grid and the same 2 stroke as the rest of the app's icons. `glyph()` takes the weight, so a mark in this set can be drawn lighter where it has to be: the numerals beside the headings are, because a figure standing next to a letter is a smaller drawing than the letter is.
- The menu reads as its three groups or it reads as nothing. The text blocks are letterforms, the lists are rows with a different marker down the side, and the blocks are objects. Every mark has to be told from every other at 16, which is what decides these: quote is the quotation mark rather than a bar with lines, because a bar with lines beside it is what a list is; the numbered list carries two rows rather than three, because a numeral small enough for three of them is a smudge; and code is two chevrons with nothing between them, drawn closer than brackets would sit so the middle of the box is not empty.
- Letters share one cap height and one baseline, `CAP` and `BASE` in `docGlyphs.tsx`. The pilcrow, the three headings and the marks the toolbar wears are one face, so they are drawn to those two lines rather than each to its own, and the family sits under the box keyline on purpose: a letter grown until it fills a 17 box towers over the row it is read in. That is also why the set is not audited by `yarn icons`, which measures objects. The numbers are still printed.
- Punctuation is solid. A quotation mark is filled the way it is in every face it is set in, and a counter that small closes up at 16 regardless, so filled is the honest drawing of what it becomes.
- Code blocks are lit by the same shiki crew already uses for a diff, through `docCode.ts`. It hands BlockNote a highlighter that always asks for both themes at once, so a block carries `--shiki-light` and `--shiki-dark` and flips with the app instead of being highlighted again. The languages, their names and their aliases all come from `highlight.ts`, so a fence written as `ts` or `bash` lands on the same grammar the rest of the app uses.
- Placeholders, the language names and every label are crew's words, held in `docSchema.ts` as a dictionary over BlockNote's own.
- An emoji in a doc is the app's own picture, the same sheet the chat draws from, so a face is the same face wherever it is written. What is written down is untouched: the character stays the character, because a doc is a markdown file and what somebody typed is what the file holds. `docEmoji.ts` is a decoration over every emoji in the document and the sheet is painted over the glyph rather than in place of it, so the caret lands where it always did and a selection paints the band under it. Code is quoted as it was written, a fence and a run of inline code both, which is the rule prose is already drawn under. The crew's own `:name:` emoji are left as the words they were typed in: a name is ten characters and a picture standing on it would cover the rest of the line, which is the same reason the composer leaves them alone.
- Tab takes the emoji standing under the pointer, which is the first until somebody moves. It is caught on the way down so the editor never sees it, or a Tab written inside a list indents the row instead of finishing the word.
- The plus and the drag handle line up on the first line of the block, which centers them on a block that is one line tall and holds them near the top of one that is not. BlockNote anchors the gutter to the top of the block and nothing else, so `blockAnchor.ts` measures the block itself: the first line box of its text, or half the block's own height capped for something with no text to read, like a divider or a picture. A table of paddings per kind of block was what it was before, and it was wrong on the first block of a doc, on a divider, and on anything the table had never heard of.
- A block being dragged is a small card, not a copy of the block at full width. `.bn-drag-preview` in `styles.css` is the only handle on it, because BlockNote clones the block into the body where nothing else can reach it.
- Clicking below the last line puts the cursor at the end. That is bottom padding on the editor rather than a spacer under it, so the click lands inside the document.

## Talking to Codex

Codex runs on `codex app-server`, a JSON-RPC service inside the same `codex` binary everybody already has. Nothing extra is installed and the desktop app is not needed: the app-server is a subcommand of the CLI, and `codex --version` is the whole of what has to be there.

- `codex exec --json` was the door crew knocked on before, and it only ever says what has already happened. Reasoning arrives as one finished block after the fact, an answer arrives whole, a file change carries no diff at all, and a shell call carries a raw string with a login shell wrapped round it. Live thinking, a streamed answer, real diffs and a named command were four names for one thing, which is the transport, and they closed together the moment it moved.
- A run that has to answer what it reads is a `Dialog`, in `types.ts`. It says what goes out at the start, what to write back for each line that arrives, and what a steer looks like, and having one is what makes a provider steerable. Claude has one too, which is where the message shape it writes to stdin now lives: a file that spawns every CLI in the app has no business knowing one vendor's JSON.
- The handshake is four steps and each one waits on the answer to the last: `initialize`, then the `initialized` notification with `thread/start` behind it, then `turn/start` once the thread has an id. `codex-app.ts` is that walk and nothing else, so a suite drives it by hand with no process anywhere near it.
- The sandbox is said twice and in two different shapes. `thread/start` takes `sandbox: "danger-full-access"`, a plain word, and `turn/start` takes `sandboxPolicy: { type: "dangerFullAccess" }`, a tagged one. Handing either the other's shape is refused outright, and the refusal arrives as an ordinary response nobody was reading, so the run simply never starts.
- The app-server does not exit when a turn ends, so `turn/completed` is what ends the run rather than the process going. Closing stdin is what makes it exit, which is the same grace window a Claude run already had, and it is what leaves a steer already in flight somewhere to land.
- A steer is `turn/steer` with `expectedTurnId`, a precondition the server checks, so a message aimed at a turn that has already finished is refused rather than lost inside the next one. The turn id is let go of the moment `turn/completed` lands, so a message written a breath too late is queued for a run of its own instead of being sent into nothing.
- What a command was is Codex's own reading of it. `commandActions` is on the item at `item/started`, already parsed into `read`, `listFiles`, `search` or `unknown` with the shell wrapper taken off, so a step says "Read notes.txt" rather than "Ran /bin/zsh -lc 'cat notes.txt'". Those four are named so `toolActions.ts` already knows them, which is the whole reason nothing was added to that table. Only what is named `Shell` keeps what it printed, which is the rule a file read would otherwise break by pouring itself into the log the crew syncs.
- The diff is on the item at `item/started`, so a change draws the moment it begins rather than after it lands. There are no counts anywhere in the protocol and they are walked out of the diff itself. An update arrives as a unified hunk and is passed through as it is, and an add or a delete arrives as the file's own body with no markers at all, which draws as nothing until every line is marked, so those two are marked up here.
- Text sits on block 0 and the two reasoning channels stand above it on lanes of their own, a summary on the odd numbers and the raw chain on the even. Nothing may share a number, because closing a block closes every kind at that index: reasoning landing on 0 would take the answer down with it, and a `summaryIndex` and a `contentIndex` are counted separately on the one item, so read as plain numbers the first heading and the first paragraph land on the same block and close each other. Lanes are what keep that impossible however many of either arrive, where a base picked to sit above the summaries is only a guess at how many there will be.
- What Codex says on the way is not the answer. `phase` is `commentary` while it works and `final_answer` at the end, so the running commentary streams into the thread where it belongs and is kept out of what gets posted as the reply. A turn that ended without a final answer falls back to the commentary rather than to nothing.
- That commentary is the thinking, and it draws as one. It is the only prose channel Codex has: the reasoning summaries are bold headings and nothing else, while commentary is whole sentences about what the model is about to do, arriving token by token as it works. Read as message text it was the agent talking over its own answer, so `aside` on the block is what says which rail it belongs to, and `cli.ts` is where that is answered rather than in the parser, because the block ids the asides are kept under already live there. The bookkeeping stays on the text stream, since that is what the protocol calls it and what keeps it out of the reply; only the step that reaches the screen is a thought.
- The token count is the whole thread each time rather than the last call, so it stands in place of what has been counted rather than being added to it. The app-server says the four numbers in camelCase and counts the cached prefix inside the input the way the rest of OpenAI does, and `usageFrom` is still the one place any of that is read.
- An approval is accepted and anything else is refused in words. With the policy set to never none of them fire at all, but a server request nobody answers is a run that hangs forever, and a wrong answer to one that asks about somebody's account is worse than saying crew cannot answer it.
- The ceiling on the reasoning channel is the account rather than the transport, and it is measured rather than assumed. Every summary that channel has ever returned here is a bold heading, a median of 47 characters across 478,287 of them, and the raw chain comes back only as `encrypted_content`, which is there so the reasoning can be handed to the next turn and is readable by nobody. `item/reasoning/textDelta` is real surface and Crew draws it, on a lane of its own, proven against a provider that emits it, but only open weight models ever populate it. Codex never asks for raw reasoning and there is no parameter that would: the request carries `effort` and `summary`, and `include` is hardcoded to `reasoning.encrypted_content`. OpenAI's own terms say extracting it another way may cost the account, so nothing here tries.
- `reasoning_summary_format` is not a way out, and it is the opposite of what it reads as. It is dead client side, absent from the config schema and read by no code at all, surviving only as metadata per model, and `experimental` is the heading style rather than an escape from it. Forcing it on through `model_catalog_json`, which is the one lever that can rewrite that metadata, changes nothing, because nothing reads it. The one model marked otherwise is refused outright on a ChatGPT login, so the prose form of a summary is reachable only on an API key and a model that is not Codex tuned.
- The desktop app is not holding anything back, and its own bundle is what says so. `encrypted_content` appears in it nowhere at all, so nothing there decrypts the chain and nothing there is even handed it: the Rust core keeps the blob and posts it to the next turn, which is the whole of what it is for. What that bundle does hold is the list of notifications the app asks for, and it is the list Crew reads, `item/reasoning/textDelta` among them and every legacy `codex/event/*` name switched off beside it. Both ends listen on the same channel for the same thing, and the model is what does not send it. Its prose is the commentary, turned into steps by a detail level rather than by a channel nobody else gets.
- Never set the thinking effort from Crew's own default. The Model field offers Default and falls through to whatever the machine is configured with, and the Thinking field did not, so an effort was sent on every turn and `model_reasoning_effort` never applied to anybody. Both fields carry the empty option now, and it is the same rule twice rather than a special case.
- `tests/codex-app.test.ts` holds the handshake, the steer and every shape above, and `yarn codex-check` is the only thing that can see the rest of it: it runs a real turn against the real CLI and reads back whether reasoning arrived while the work was going, whether the answer streamed, whether a command was named and whether a diff came through with anything counted in it. Every one of those can be green in a suite and dead in the app.

## Talking to Kimi

Kimi runs on `kimi acp`, the Agent Client Protocol server inside the same `kimi` binary everybody already has. Nothing extra is installed, the same way nothing extra is installed for Codex: the server is a subcommand of the CLI, and `kimi --version` is the whole of what has to be there.

- `kimi -p --output-format stream-json` was the door crew knocked on before, and a whole turn came back as six lines of it. No reasoning at all, an answer whole at the end, a tool call whose arguments only arrive once it has already run, and not one token counted anywhere. Live thinking, a streamed answer and a named tool with real arguments were four names for one thing, which is the transport, and they opened together the moment it moved.
- The handshake is `initialize`, then `session/new`, then the settings one at a time, then `session/prompt`, and each step waits on the answer to the last. A setting sent alongside the turn would race the turn it is for.
- There is no flag to start a session on, so what a run is set to is said over the wire after the session exists. `session/set_config_option` takes `configId` rather than `id`, and `mode` and `model` both go through it. A setting the CLI will not take carries on to the turn rather than stalling it, since a model that has left `config.toml` is not worth losing a run over.
- Both file capabilities are declined, and so is the terminal. Offered them, Kimi hands its reading and writing back through the client and waits. Declined, it uses its own hands and the edit lands. Both halves are measured, and the first is why declining them is a decision rather than an omission: a client that answers a read with nothing gets a model that gives up on the file.
- Everything is approved before the turn starts, which is the posture every other CLI here runs under. The permission requests are still answered, because a request nobody answers is a run that hangs forever.
- The words arrive as a plain run of chunks with no block number on them, so the lanes are counted here. A block is a stretch of one kind, switching kind or reaching a tool closes the one that was open, and nothing reuses a number, because closing a block closes every kind standing at that index.
- The name a tool started under is the one that says what it is. Every update after the first carries a narrated title instead, "Reading math.js" where the tool is Read, so the first is kept and the rest are read for what they add. `rawInput` only lands on the last update before the tool completes, which is why a call draws as soon as it starts and fills in as it goes.
- **Kimi takes a steer, and it is the turn standing down and going again rather than a message pushed into one already running.** A second `session/prompt` mid-turn is refused outright as `turn.agent_busy`, in ten milliseconds, and discarded rather than deferred, so nothing is ever sent into a live turn. `session/cancel` is the way, a notification rather than a request, and the turn it stops answers its own prompt with `cancelled` in a few tens of milliseconds. Everything that turn had already done is still in the session: what it read, what the tools gave back, what it had said. So the prompt after it carries on with the work intact rather than from nothing, and the model knows it was interrupted and says so.
- The cancelled turn has to be let go of before the next one is asked for. A `session/prompt` written in the same breath as the cancel is swallowed by it and answers `cancelled` itself, doing nothing at all, with no error and no output to say so. That is the whole reason a steer is two moves rather than one: `steer` stashes what was typed and asks the turn to stand down, and the answer to that turn is what opens the next. Two written in one breath arrive together as one prompt, so nothing is said twice over.
- A turn cancelled to make room for a steer is not the end of the run, so the parser says nothing about it. Every other stop reason still is.
- What it costs is the tail of the turn that was interrupted, which a steer into a live turn would have kept. That is the trade, and it is the same shape as what a steer already means everywhere else in the app: somebody talking over an answer rather than undoing the work behind it.
- A dialog that answered the very line a turn ended on has opened another one in the same breath, so the run is not winding down and `cli.ts` holds `TURN_END_GRACE_MS` back for it. Without that the grace closes stdin under a turn that has only just started, which is the race a steer landing as the turn was already ending falls into: the cancel arrives too late, the turn ends of its own accord, and the message that was written gets three quarters of a second of a run to land in.
- **The local server is the other door, and it is the one the steering in the agent core was written for.** `kimi server` puts the same binary behind http, and its web UI sends every mid-turn message as `POST /api/v1/sessions/{id}/prompts` followed by `POST /api/v1/sessions/{id}/prompts:steer`, with no gesture of its own: a prompt written while a turn is going comes back `queued`, and the steer is what moves it in. Nothing is cancelled and no turn is started, the message is buffered and folded into the context at the next step, and a steer that arrives as the turn was about to stop extends that turn rather than ending it. ACP reaches none of it: it calls launch, which is what answers `turn.agent_busy`.
- It is still not the door to use, and the reason is when a steer lands rather than what it costs. That one folds the message in at a step boundary, which is a few seconds while tools are running and the whole of a generation when one is not: measured at 3.5 seconds against a run reading files, and at five minutes and twenty seconds against a run writing one long answer. Standing the turn down lands in tens of milliseconds every time. A steer nobody can feel is worse than one that costs the tail of a turn, and the tail is the part being talked over anyway. The rest of the price is the shape: a crew runs a CLI per run over stdio, and that is a daemon on a fixed port behind a bearer token, one for every run on the machine, with a websocket to read instead of a stream of lines.
- **Nothing about what a turn cost is on the wire.** Not a notification, not the prompt's own answer, not a field anywhere in a stream of two hundred messages. Kimi writes it down instead, in a log of its own beside the session, and `kimi-usage.ts` is the whole of reading it: `usage.record` lines under `~/.kimi-code/sessions/*/session_<id>/agents/main/wire.jsonl`, found by the session the handshake already knows rather than by working out the folder's hash. They are written live rather than at the end, so the count moves while the work is going.
- One record is one call of the model, so they are summed, and only the ones scoped to a turn are counted or the session's own total lands twice. Kimi counts the cached prefix beside the input the way Anthropic does, which `inputOther` says outright, so it is handed to `usageFrom` in that shape and the prefix is never charged twice at full rate.
- No price is drawn, and that is right rather than missing. There is no rate for these models in `MODEL_RATES`, nothing in the CLI or its config knows one, and the account this runs on is a subscription rather than a per-token bill. A wrong number costs more than a missing one.
- An error before the session exists ends the run and says why. Nothing has been asked for yet but the way in, so no turn is coming to end it, and a machine that never logged in would otherwise sit there for the whole ten minute idle timeout saying nothing.
- `tests/kimi-acp.test.ts` holds the handshake, the lanes, the tools, the stop reasons and the steer, the last of those against a real process: a fake CLI that ends its turn only once the cancel has arrived, which is the race, and the run is held to still saying what the steer asked for. `tests/kimi-usage.test.ts` holds what a turn cost and where it is read from, and `yarn kimi-check` is the only thing that can see the rest of it: it runs a real turn against the real CLI and reads back whether thinking arrived while the work was going, whether the answer streamed, whether a tool was named with its arguments, whether the edit really landed, and whether the tokens were on disk before the turn ended. Every one of those can be green in a suite and dead in the app.

## What an agent did

A step in a thread says what happened in plain words, wearing a mark of its own.

- Thinking has to be asked for. From Opus 4.7 on, a model keeps its reasoning to itself unless the request says otherwise: the blocks still arrive, signed, with an empty string where the words would be, so a run appears to think at length and say nothing. The interactive CLI asks for the summary and a headless one does not, and crew is headless, which is why the thread was silent for so long. `--thinking-display summarized` in `claude.ts` is the whole of the fix. Never drop it.
- A thinking block with nothing in it is not a step. `cli.ts` waits for the first word before it opens one, and closes only what it opened, so a model that is not showing its reasoning reads as working rather than as an empty card. That guard is also what leaves the complete block at the end of a message free to stand in, on a CLI that only sends it that way.
- Claude and Kimi stream their reasoning word by word. Codex streams a heading a stretch: OpenAI encrypts the raw chain of thought and sends down a short summary of each piece of work as it starts it, which is the same thing its own app shows, so crew posts each one as it lands and that is the ceiling. Grok hands back whole blocks at the end of a turn.
- `toolActions.ts` is the one table that turns a tool name into a mark and a phrase: "Reading" while it runs, "Read" once it is done, "Read files" when a run of them is folded up. Every CLI is read through it, so `Read`, `read_file`, `ReadFile` and `view_file` all land on the same line, and an mcp tool is named after the tool rather than the plumbing around it. A name nobody has heard of is set in words rather than shown raw.
- The marks are drawn in `toolGlyphs.tsx`, outlined on the same 24 grid as the design glyphs. They are sized by eye rather than by their box, so a page, a magnifier and a sparkle carry the same weight in a row. Nothing wears a container or a badge, with one exception, and it earns it: `ShowGlyph` is a tile, because showing a page is the one step that puts something on the screen outside the thread, and a tile is what an app is drawn as everywhere else. It is `TILE` in `keylines.ts` rather than `SOLID`, since a tile carries a mark cut out of it rather than its whole box as ink: it stands in a column beside the thought's ring, which paints its stroke outside itself and arrives at 19, and a square reads larger than a circle at the same measure, so 17.5 is where the two sit level. The arrow is cut out rather than painted on, or it would be one fixed color over a surface that changes under it, and what comes through it is whatever the row is standing on, the hover wash included.
- A live step lights its mark white and pulses it. Only `RunStatus` spins, at the foot of the run, so a long thread is not a field of spinners.
- A thought is the one mark that moves on its own. `ThinkingMark.tsx` draws it on the same 24 grid as the rest: three filled dots with a wave travelling through them while the model is thinking, and when the thought lands they gather into the middle as a ring blooms out of them and the check strokes itself in, which leaves the mark standing as `DoneGlyph`, the same check in a circle used everywhere else. The word turns over with it, "Thinking" to "Thought". The dots, the radii and the check path live in `toolGlyphs.tsx`, so the still glyph and the moving one are the same drawing.
- The ring's growth is a CSS transition and the dots' exit is an animation, and it has to be that way round. A transition does not fire on the first render, so a thought that was finished before it was ever on screen is simply drawn done rather than landing again as the thread is scrolled. Taking an animation off an element drops its values where they stand instead of easing them, which is why what the wave holds cannot be handed to a transition: the landing is keyframed, and it starts on the value the wave rests at so there is no jump. Only a mark that was watched thinking wears `data-landing`, or every old row would land again on the way past.
- A thought is set in italic, and a blank line between two of them is a paragraph rather than an empty row: `StepRow` splits on it and spaces the paragraphs instead, so a long thought reads as prose rather than as blocks pushed apart.
- Three or more of the same tool in a row fold into one line that opens: a pair is not clutter and stays where it can be read. Every step in the group keeps its own row and its own diff.
- An edit opens into the file it touched: the path and the counts on a bar, then the diff, red where lines went and green where they arrived, with the words that actually changed picked out. `diffRows` and `codeLine.tsx` do that work for the browser panel too, so a diff reads the same wherever it is shown.
- A command opens into a terminal: what was run after a `$`, then what it printed under it on the same surface, each with a copy button of its own. Every CLI hands back the result of every tool, so `cli.ts` keeps it only for a command, because a file read or a search would pour the whole file into the log the crew syncs. The result arrives without the name it was called under, so the name the tool started with is what decides.
- What a command printed is cut down by `output.ts` before it is ever sent: the colors and cursor moves a terminal would have eaten are dropped, and a long run keeps its head and its tail and loses its middle, since a failure is at the end. The card scrolls inside its own height rather than counting off the lines it is not showing, because unlike a diff there is nowhere else to go and see the rest.
- What a step says it did is what it did, not the summary the model wrote of it. `detail.ts` reads `command` before `description`, so a shell step shows the command itself.
- The panel a step opens carries no rail or bracket. It is a card, or it is plain text, sitting under the row that opened it.
- A row is drawn again only when it reads differently, and that is what keeps a thread usable once it is long. A thread worked in all day is three thousand rows, and every one of them was drawn again on every letter typed into the composer: the draft is held in the store, so a keystroke re-renders the view that holds the rows. Worse, a run streaming rebuilds every item in the thread on every delta, so the whole of it was drawn again several times a second while an agent worked. `ThreadItems` is memoized on the list it is handed and each row is memoized on `sameItem`, so typing draws nothing in the thread and a step landing draws the row it landed in.
- Nothing in the thread may be handed an inline callback. `onReply` is a `useCallback` in `ThreadView`, or a new function every render undoes the memo on every message under it, and the same goes for anything else passed down to a row.
- `sameItem` in `thread.ts` names every field rather than walking the object, because a field left out of it is a row that quietly stops updating, which is the one way this can be wrong. What the store owns is compared by its reference, the files on a step and the attachments, mentions and reply on a message, since those objects are only replaced when they really change. What `buildThread` makes fresh each time is compared by what is in it: the pages a run showed, the helpers it sent out, and the reactions on a message. `tests/thread-item-same.test.ts` types its fixture as `Required<ThreadItem>`, so a field added to the interface fails to compile until it is added here too, and it changes each field in turn to hold every one of them. `tests/thread-render-probe.test.ts` is the other half, and it types five letters into a thread of two hundred steps and reads back the count of what was drawn again, which is nothing.
- A hook that merges the page read back out of the log into the window's own events holds the two halves apart. `useThreadRead` had one memo over both, and a step lands about once a frame, so every frame re-ran the merge and handed back a fresh events array: every memo under it, `eventsOfThread`, `buildThread`, `stepsOfThread` and the two walks below, rebuilt a thread that had not changed, and the whole column drew again around it. Split, a step landing moves the steps and leaves the log where it is.
- A selector handed to `useCrew` runs on every write the store takes, once for each component subscribed, because that is how zustand decides who has to draw again. So a selector that walks the log is that walk times the rows on screen times the frames in a run: one screenful of reply quotes and helper rows read 399,060 events for a single step landing. `eventIndex.ts` is the answer and the shape to copy, one pass over the array held against the array's own identity, so every row on screen shares it. A selector is a field read, and the walk behind it belongs in a `useMemo` or in that index. The count on the review badge is the one to remember, since `App` and the top bar both read it and both are always standing: it asked `threadWorking` about every thread and that question walks every thread, so the badge was the square of the crew's threads twice over on every write. `workingThreads` answers for all of them in one pass, and the badge and the tasks panel read it.
- Memoizing the rows is half of a keystroke, and what the view around them does on every render is the other half. `ThreadView` read the run it is showing and the last one that ended straight out of the thread's own events, unmemoized, and a keystroke re-renders it because the draft it reads lives in the store: two walks of the thread's whole event list a letter, growing with the thread. The live case is the worst of both, since `lastEnd` finds no end while a run is going and walks all of it, and a `find` going forward reaches the run that has just started only at the far end. Both are held in a `useMemo` now and `lastStart` searches backwards beside `lastEnd`, so a run is found where it really is. Anything read off a list on every render of that view is the same mistake wearing another name.
- A box that grows with what is typed in it reads its own height once, never twice. `useAutoResize` writes `height: auto`, reads `scrollHeight`, and writes the height back, and it used to read `scrollHeight` a second time to decide the overflow. A read after a style write is a synchronous layout, and the composer stands in the same column as the thread, so that second read was a second full layout of every row on screen for a number the first read already answers.
- **A box that measures itself inside a scroller holds that scroller still while it does.** For the length of the read the box is one row tall, the browser clamps the scroll to the shorter content it now has, and writing the height back does not write the scroll back, so what is left is a view that has walked up by whatever the box was worth. Nothing is ever painted in between, which is why this reads as the page moving on its own rather than as a box collapsing: a message being edited in the middle of a thread pulled the view up on the first keystroke and on every one after it. `useAutoResize` finds the scroller once, on the first measure, and puts the top back if the read moved it. A composer with no scroller over it gets nothing and pays nothing.
- The box a message is edited in is the message, so it is never capped and never scrolls. A cap is a box shorter than the words it stands in place of, which moves everything under it and puts the rest of somebody's own sentence behind a scrollbar in a thread that already scrolls. Uncapped, `useAutoResize` sets the overflow to hidden itself, so the bar down the side, which offered somewhere to go that was not there, is gone. Focus is taken with `preventScroll`, or opening the box carries the view to it, and the caret lands at the end of what is already written rather than in front of it.
- A thread draws its tail rather than every row it holds. A window holds far more than the five hundred events `EVENT_LIMIT` names, because a step is not counted against that limit and one `agent.start` standing inside the window drags every step of its run in behind it: at rest that is seven thousand events, and a thread worked in all day is thousands of rows of them. `useDrawnTail` is the whole of the rule. Four hundred items to start, another four hundred as somebody comes up on the top, and what arrives lands above where they are standing rather than under them, so the line being read does not move. It grows by whatever lands at the end, or a step arriving would push the oldest drawn row off the top under somebody who had just reached back for it, and a tail too short to fill the scroller reaches back on its own, since scrolling is the only other way it would ever be asked.
- Two things draw the whole of a thread, and both are somebody asking for the whole of it. A search is one, because the find bar walks the text nodes that are really there and a row that was never drawn is a row it cannot reach, and it stays drawn after the field is cleared rather than taking the rows away under whoever just found something in them. The way to the head of the thread is the other, and it draws first and lands instantly rather than travelling: a smooth scroll to the top of seven thousand rows that arrived above you mid-flight is a scroll that never seems to end.
- A thread the window has scrolled past is asked for on its own. What a window holds is the tail of the log, and a thread worked in a month ago has none of its own events left in it, while the rail still lists the row, the chat still holds the card and the plan still rides in the snapshot: opening one drew a header over an empty column. `thread.history` names one thread and the host hands back what that thread is drawn from, the events naming it and the two a helper raises on its behalf, the tail of them where a thread has been worked in all year. It goes to the one who asked, and a ghost is answered for the window that opened it and nobody else, or a hidden thread leaks to whoever guesses its id.
- What says a thread is worth asking for is its own beginning. A thread starts once, so a window holding that event holds everything the thread has done since, and one that does not is a row with nothing under it. Asked once per thread per connection, which is what keeps opening the thread that just ran free, and a welcome empties the record of what has been asked since the session has come back up.
- What comes back is kept beside the window's own events rather than folded into them. The chat feed is drawn from the window, so a thread read back would stand a card from a month ago at the head of today's, above the way back into the history that would have filled in around it. `readEvents` and `readSteps` are that slice, and the window's own copy wins wherever the two hold the same event, since the window trims and this does not. `useThreadRead` puts the two together, and asking is part of reading rather than a call anybody has to remember, so anything that shows a thread gets both halves by naming it: the column, a helper, a board's chat and the work beside a thread.
- Steps arriving live are held for a frame and land as one write, because every write walks the thread again. The host already throttles one step id to a message every eighty milliseconds, so a burst is several step ids and several runs at once rather than one step saying itself twice: four messages in a tick were four rebuilds of the whole thread. `stepBuffer.ts` holds them in the order they arrived and arms a frame and a sixteen millisecond timer together, whichever comes first. Anything that reads the steps exactly flushes before it reads, which is every event, and a rebuild drops what is waiting rather than letting a stale delta land behind it.

## Music

Something to put on while the crew works. It is a tab in the side panel called Music, opened from the toolbox or the New button, and what is playing is playing for everyone: one loop, one place in it, and anyone can press anything.

- Crew plays two things and nothing else. Its own tunes, which are numbers rather than files, and a track somebody in the crew added themselves. There is no service behind it and nothing to fetch from the internet.
- A tune is written out as rows of steps in `songs.ts`, read by `tunes.ts`, and struck through the same bubble the rest of the app's sounds are made of. A note is its name, a dot is a rest, a dash holds the one before it, an x is a drum. `voices.ts` is the whole kit: the low one, the tune, the spark, and three with the tuning taken out of them for time.
- A row shorter than the loop is laid down again inside it, so a drum is written once and a melody is written out. A note nobody can spell comes back as nothing and is dropped, which is silence rather than a wrong note, so `tests/music-player.test.ts` spells every token of every song.
- What is playing is never written down. It rides in the session snapshot and in `music.room`, the way a call does, so a morning of skipping tracks is not something the crew scrolls past later. A session coming back up has nothing on.
- The shelf is the other way round. A track somebody added lasts, so it goes in the log as `music.added`, is replayed on the way back up, and is left off the shelf if its file has gone. Both kinds are trimmed out of the snapshot's own event list, the way the toolbox is.
- The file itself is kept beside the session in `.crew/music` and served over http by the host, exactly as an attachment is. Everyone plays their own copy from `/music/<file>`: nothing streams from whoever added it, and there is no second copy of the audio in the log.
- A picture in a message is drawn by a tag, which takes it from anywhere. A track is read with `fetch`, and the answer to a fetch is not handed to the page unless the host says who may read it, so `MEDIA_HEADERS` in `src/server/index.ts` says so. Without it a run from source, where the renderer is served on a port of its own, is refused the file and plays silence, while the same build installed reads it from `file://` and is fine. Only the two routes a member reads from carry it. The ones that write stay shut, or a page in somebody's browser could reach a board by guessing its name.
- A track that will not load says so, on the line under its name. Silence that looks exactly like music playing is the worst of both, and the fetch and the decode were both swallowed, so nothing anywhere said the file had never arrived.
- A playlist is a list somebody made out of what is already on the shelf, and the panel holds it on a tab of its own beside Songs. It keeps ids rather than tracks, so it is the order it was written in and nothing else, and a track that has left the shelf is read out of it rather than left as a row that plays nothing. Everyone sees every list and anyone can play one, and only whoever wrote it can write in it, which is the `by` on the list and `isMine`. A list lasts, so it goes in the log as `playlist.added`, `playlist.track`, `playlist.renamed` and `playlist.removed`, and those are trimmed out of the snapshot's own event list the way the shelf is.
- The tab is one list of every list there is, the app's own first, then yours, then everyone else's. Whose a list is rides on the row, beside the count, rather than in a heading over a section of them: sections spent a row each on saying a thing the rows themselves can say, and a crew of four turned a handful of lists into a page of titles.
- A list can be named again, by whoever wrote it. A name blanked is refused rather than taken, since `cleanPlaylistName` would hand back Untitled and that is the one way a rename loses something. Naming a new list and renaming an old one are the same question, so they are the same card: `PlaylistName` is it, and what is already there arrives selected.
- Whoever asks for a list names its id, in `playlistId` on `playlist.add`, so the window that made it opens it the moment it lands and a track can be filed into it in the same breath. The host takes the id it is handed only if it is a plain uuid nothing else answers to, and names the list itself otherwise, so an older host and a junk id both come back as a list rather than as nothing. `makePlaylist` waits for the list to really arrive before it hands the id back, since a list is not somewhere to stand until the host has written it down.
- Making a list goes all the way through. It is named on a card of the app's own, it is written down, and it opens on the list itself, so there is somewhere to be once it exists. Nothing is made by wandering off, and nothing typed is lost by it either. A new list asked for from a track's own menu takes that track with it, rather than putting the track down and sending you to another tab to write the list you were about to fill.
- A track put on from a list carries the list with it, in `playlistId` on the room, so Next and Back walk the list rather than falling back into the shelf after its first track. A list nobody has heard of plays the track and belongs to no list rather than being refused outright, and deleting the list that is playing leaves the track on.
- `MUSIC_SETS` is the app's own lists, made of its own tunes. A crew's lists are theirs and are written down as they are made, and these belong to nobody, so they sit in `src/shared/music.ts` rather than in the log, they are the same for every crew, and there is nothing about one for anyone to change. `by` is empty on them, which is the whole of what keeps the delete and the take out off the page: a list with no owner is never yours however you are named. `isMusicSet` is how the host knows to play from one, since a set is not in the lists it holds.
- Crew is the one set there is, and it is the whole of the slow tunes. They run sixteen bars rather than eight, two of them to a chord and none of them past seventy, so each is about a minute round rather than half of one, and the kit stays out of the way where there is one at all. Anything added there is written the same way, or the list is a minute of ambient followed by half a minute of something else.
- What is playing floats at the foot of the panel, and only while something is on. Nothing playing is nothing to say, so the tabs sit at the top of a list that has the whole panel, with no rule under them, and the bar arrives when a track does. It is glass with a margin round it and the list runs on underneath, so nothing is set in a solid grey on it: `barButton` and the words beside it take the foreground at an opacity. Volume goes with it, off the speaker on the bar, since there is nothing to be loud about when nothing is on.
- The bar is lit by the cover of whatever is playing, `meshOf` at a fifth of its strength behind the glass, so the foot of the panel carries the color of the track rather than a second grey. The list is given room to scroll past it and the fade under it is taller than the ordinary one: the rows have to be gone by the time they would show in the margin the bar floats in, or a row reappears under the bar a moment after passing behind it.
- A track plays on to the next one when it ends, and the host is what walks it: the clients read one clock, so a timer on the host moves the room the way a press of Next does, along the list it came from or along the shelf. Loop is the crew's setting rather than a window's, it rides in `music.room`, and it is the one thing that leaves a track to come round on its own. Nothing else about the end of a track is written down.
- A row has one menu, at the end of it, in a slot that is held whether or not the pointer is there. A button per action grew the row under the pointer and pushed the time along with it. The playlists are a screen inside that menu rather than a menu hanging off it: one card is one thing to aim at, and a list is picked by its cover and its name, the same row the Playlists tab holds. `ScreenSwap` is what carries one screen to the next, travelling the way you are going and taking the card's height with it.
- One screen of the panel giving way to the next is a movement rather than a cut, and it is the same `ScreenSwap` the row menu uses in a card, on `fill`. Songs and Playlists stand beside each other and a list stands inside them, so where a screen travels from falls out of the order they are named in: sideways between the two tabs, in and out of a list, and back out on its own if somebody deletes the list while you are reading it. The header goes with it, the tabs giving way to the way back. A card carries a change in its own height and a panel has none to give, so there the screen being left is held on for the length of the movement and watched out, quicker than the new one arrives, and the box it all happens in is clipped, since a screen on its way in stands past the edge of it while it travels. A panel is the size of the window and the few pixels that read as a movement in a card are no movement at all across one, so how far a screen travels is `--screen-travel` on the box it happens in.
- Every screen scrolls on its own rather than through one scroller the panel shares. That is what lets two of them stand one over the other while one gives way to the other, and it is what lets each keep its own place: coming out of a list puts the shelf back where you were reading it, and a list you have not opened before starts at the top of itself. The place is held by the panel, not by the screen, since the screen is taken down whenever you leave it.
- Searching is a mark until it is asked for. A field standing open across the top is the loudest thing on a page that is mostly covers, and most of the time nothing is being looked for. Leaving it empty puts it away and takes the search with it.
- Nobody is told a moment on somebody else's clock. The host says where in the loop it is right now, and each machine takes it from there on its own audio clock. A machine already within a third of a second of the crew leaves its music alone rather than jumping, or every message that changed nothing would be a hiccup in everyone's speakers.
- Volume is yours alone, and so is mute. Both are in `localStorage`, neither is ever sent, and the player keeps its place with everyone else's whatever you do to them.
- The controls belong to the people here. `music.set`, `music.off`, `music.loop`, `music.add` and `music.remove` are all refused from a runner, since an agent's machine is connected the whole time it is joined.
- The bars are the music's own loudness, band by band, low on the left and high on the right. Where it is playing they are read off an `AnalyserNode` hung before the volume, so turning it down where you sit does not stop them dancing. Where it is not, they are drawn from `levels.ts`, which works the same curve out of the notes themselves, so a machine with the sound off watches what everyone else watches. The analyser's window has to be wide: a narrow one measures in steps of a few hundred hertz and the lowest band is fifty across, which leaves the bass bar empty.
- Bars are written straight onto the elements in `Bars.tsx` rather than held in state. It runs every frame, and a render a frame would cost the whole panel.
- A cover is a photograph of a few petals held right up to the lens: a sky, a handful of shapes standing at different distances in front of it, and a light off one side. `coverSeed.ts` works the whole scene out from the track's id, so it is the same picture on everyone's screen and no two tracks look alike, and `coverArt.ts` photographs it in a fragment shader. A track somebody added takes a palette from `paletteFor`, so it has a cover too.
- It is shapes rather than a stack of gradients, and that is the whole difference between a picture and a tint. Gradients average, and the average of two colors is a third color, so a picture built out of them has no objects in it: every edge is both of the colors that meet there and neither one is in front. A petal hides what is behind it, carries its own color right up to its own edge, and is blurred by its own distance. All three of those are per pixel, which is why this is a shader and not CSS.
- The blur is the depth of field, and it belongs to the petal rather than to the picture. Every cover is spread end to end through its depth rather than rolled, so there is always something near the lens and something far from it, and they are laid down blurriest first. One blur over the finished picture is a soft photograph of a flat thing.
- Nothing in a palette is dark. `colors` is three petals, the light along their lit edges, and the sky they stand against, and the darkest place in a picture like this is a color in shade, never an ink. A near black anywhere in the five lands as a bruise in the corner of the tile, and since every palette used to carry one, every cover had the same bruise and they all read as the same picture in different colors.
- Nothing in the first three is a near white either, which is the same mistake at the other end. Those three are the things in the frame, and a thing has a color: pale enough and there is none left in it, so it stops being the light on a petal and becomes a hole cut in the picture, brighter than everything around it and made of nothing. Every palette carried one as its middle color, one of them a plain `#ffffff`, which is why a cover's pale shape read as belonging to a different picture. The fourth is the light itself and is meant to be near white.
- A petal is not one color. It is a curved surface, so it turns away from the light at both flanks and falls off toward each tip, and a normal drawn in the plane of the picture gives every part of it its own angle to the light. Lit, it goes toward the palette's light; turned away, toward the sky rather than toward an ink, because a leaf in its own shadow is still lit by the sky above it. Both are hung off the petal's own color so the middle of it stays what it was given. Flat, the same shape is a band of paint.
- A petal is thin at its rim and at both tips, so it lets go of what is behind it there rather than cutting it off. That is what takes the hardness off a near edge without blurring it: the outline stays where it is and stops being a wall. The thickness is measured against the petal's own width, never against its blur, or a far petal, which is a wide soft thing, never counts as thick anywhere and the softest object in the picture becomes the faintest.
- A lit edge never arrives whole. Let all the way through, the brightest part of a rim is the light itself and nothing of the petal, which draws a pale line around the shape and is the one thing that tells the eye an outline is there. The sky takes two scales of noise rather than one, for the same reason: one field over the whole frame is another ramp, and it is the quiet half of a cover that needs something to look at.
- Something in the frame is always a different color from the sky. A green leaf on a blue sky is what makes one of these read as a photograph; all five colors from one arc of the wheel is weather rather than a picture. The third petal is the one that carries it.
- The odd color out is always the furthest thing away, and the nearest petal is always the one that belongs. Given the nearest slot it is the hardest edge in the frame wearing the most unlike color, and it reads as a sticker however good the rest of it is. The colors are handed out furthest first and stretched across the depth with both ends pinned: cycled, a picture holding more petals than the palette holds colors comes back round to the odd one and hands it to the sharpest petal there is, and taken as a plain share of the way through, one holding fewer never reaches the quiet end at all.
- A thing further off is seen through more of whatever the sky is made of, so a petal takes on the sky's color as it goes back. That is what settles a leaf into a photograph instead of leaving it sitting on one, and it is why a far shape does not carry its color at full strength.
- A petal has to end inside the frame. Run long enough that its taper happens off the edge, what is left in the tile is a stripe of one width crossing corner to corner, which is the one shape that says at a glance that a picture was made rather than taken. One tip is enough, since a leaf that runs out of the picture at one end is what a photograph taken this close looks like.
- The length that does it is worked out rather than searched for: the frame is a box and the spine is a line, so the two places the line crosses the box are the whole answer, read the longer way along itself. The bend is the half that cannot be solved in one line, because it carries the tip sideways the whole way out and by more the further out it goes, so a petal with plenty of bend leaves the picture long before its own length says it should. It is walked down instead, and that ends because straightening one always brings it back to the length that was solved for. Shrinking the length instead is what does not end: both tips draw in toward the point the spine passes through, and on a spine passing outside the frame there is nothing there to draw in to. Every spine is held inside the picture for the same reason.
- The cover on the panel breathes while its track plays, and a picture that moves has to be bigger than the box it moves in. It is laid in past all four edges and the drift stays inside that margin. A turn is the dearest thing it can spend: a square turned four degrees has to be scaled by 1.067 before it covers its own box again, so a small rotation and a scale that never drops under one is what it is, and the tile showed the panel behind it when the two were not weighed against each other. Said as a size and a corner rather than as four insets, since a canvas is a replaced element and takes its own pixel size wherever its width is left to itself.
- Noise is drawn once, above the loop, never inside it. A driver unrolls a loop that reads a uniform by its counter, so one noise call written inside is really one per petal, and on a Mac that is enough to take the Metal shader compiler down with it: the whole panel comes back as empty tiles, and the only thing that says why is the console. The loop is handed three fields, coarse to fine, and mixes them.
- The shader counts the same number of petals every time, because a card cannot be handed a shorter loop, so an empty slot still runs. Fill one with numbers that mean something rather than leaving it at nought: a petal blurred by nothing asks the card to fade an edge from a point to the same point, which has no answer, and what comes back is not a number and spreads through everything it touches.
- `yarn covers` draws every cover on one page. A generator like this cannot be judged from its numbers, and `tests/music-cover.test.ts` only holds the rules above, never the look.

## Games

Something to play while you wait. It is a tab in the side panel called Games, opened from the toolbox or the New button, and it holds Tetris and Flappy Bird with room for more. What lasts is the score.

- `GAMES` in `src/shared/games.ts` is the whole list of what there is to play, so the panel, the leaderboard and the host all read the same table and a new game is a row in it plus the two files that draw and play it.
- A game is numbers. `tetris.ts` and `flappy.ts` hold the whole of the rules and neither one draws anything or listens for anything: a move is a function from one game to the next, which is what lets `tests/games-play.test.ts` play both of them through without a canvas anywhere near it. Anything that has to be shuffled or has to fall takes its randomness and its step as arguments, so a test steps them exactly.
- The drawing is beside the rules rather than inside them. `paint.ts` is the kit both games are painted with, the colors, the block, the pipe and the bird, and `drawTetris.ts` and `drawFlappy.ts` are each game's own picture. The row a game is opened from is painted with the same kit, so a row is a still of the game rather than an icon of one.
- A game is a picture, so its colors are pinned the way the design canvas pins its palette. The field is the same deep blue in a light window as in a dark one, and everything drawn over it is set in white at an opacity rather than in the theme's own foreground, which would be black text on a dark field in a light window.
- Every game stands on a field of one shape, `FIELD_RATIO`, whatever shape its own world is. It is the shape of the room the panel leaves, so the side panel is filled rather than nearly filled, and one game does not resize the panel's furniture on the way to another. A game whose world is another shape is centered in it and sinks its own well into the field, which is what Tetris does: a board left to be guessed from where the grid happens to stop is not a board.
- The field keeps its own shape whatever the panel is doing. `Field` measures the box it is given and works the game's size out from that ratio, because a canvas told to fill a box it does not have the shape of comes out stretched and the games are drawn in world units.
- The field is nothing on its first render and its real size a beat later, and a game that is not running has no loop to paint it again. `Field` says when its size changed and the game repaints, or the board keeps the one pixel it was first drawn at and is stretched over the whole field.
- The field takes focus as it arrives and eats the keys it is played with, or the arrows scroll the panel out from under whoever is playing.
- Nothing stands beside the field. The score for the round rides on the panel's own header line, beside the name of the game, and the board of high scores rides on the overlay that is over the field between games. Under the field, the board changes the size of the field the moment a game starts, and a table of scores beside a game being played is something to read instead of the game.
- A frame is worth what it really took, capped at a twentieth of a second. A window left in the background hands back one enormous step when it comes round again, and a bird moved by a second of gravity in one go is already in the ground before anyone can press anything.
- The leaderboard is one row per person per game, their best. A score that beats nothing is a round somebody played rather than something the crew keeps, so the host only writes one down when it beats that person's own, and `game.score` is trimmed out of the snapshot's event list the way the shelf is: a high score is a row on a board, not a moment to scroll past in the chat.
- Reading the log back keeps the last score for each person rather than the highest, because they went in in the order they were played and each one already beat the one before it.
- The board is the crew's own, so `game.score` is refused from a runner the way the music controls are. An agent's machine is connected the whole time it is joined.

## Attachments

Anything the crew has to hand goes on a message: a picture, a pdf, a log, a spreadsheet, a zip, a track, a clip. `src/shared/attachments.ts` is the one table of what a file is, and both sides of the wire read it.

- A picture is kept under the type it says it is, since that is the one thing every browser reports the same way twice. Everything else is kept under the extension it arrived with, because the type a browser reports for a csv is a guess and for plenty of files it reports nothing at all. The type written down is the type it will be served as, so what the app reads off the record is what really comes back off the wire, and a file with no extension to go on is handed over as a download rather than guessed at.
- Nothing a browser would run is ever served as itself. A page and a vector are markup, and an attachment is served from the host's own address, so one carrying a script would be reading the session from inside it. Both go over as text, which is still there to read with nothing left to run.
- Both are still drawn, and neither is drawn where it is served. A page is read out of the attachment and stood up in the app's own folder through the same `Previews` a file in the project is previewed with, so what somebody sent is a page in a window rather than a script inside the session, and it is handed no folder to read from since there is no file behind it and nothing beside it to reach. A vector is drawn as the picture it is, in an image tag, which runs nothing and reaches nothing. `markupOf` in `src/shared/attachments.ts` is what says which of the two a name is, because the type on both says text on purpose and the name is all that is left.
- A file written to be looked at is offered both ways, the thing it is and the words it is written in, the way a file in the project is. It opens on the page or the picture, since that is what it was written to be, and the switch stands in the file's own header wearing the mark of what it is when it is not text, so the mark holds still while the two are swapped and lights while that is the way it is being read. `bothWays` is which files those are, markdown included, and `tab.preview` is which way it is being read, the same flag a file in the project carries.
- A face is a picture and nothing else. `savePhoto` is the one way one is set, and the window that sends it already turns everything else away: the host asks again, because taking a window's word for it is what put a pdf on somebody's avatar the first time this was widened.
- The name somebody uploaded is a label and never a path. What is written down is a uuid and an extension, `isAttachmentFile` is the whole of what may be read or written under `.crew/attachments`, and the runner asks it again about a name that arrived over the wire, since that side is handed a name rather than making one.
- What an agent gets is a path per file and a line saying which of them are pictures. It reads them off the disk itself, so a log or a csv is work it can do rather than something described to it. A thread's own transcript says `[image: name]` or `[file: name]`, so a later turn knows which it was.
- Six to a message, the same for a picture and a file. How big each may be is the crew's own, on the Files page in the settings, and it starts at 10MB. Everything lands in the folder the crew syncs, so a big one is a big one in the history too.
- That number is one number for everyone, because the host is what turns a big one away: a window that raised its own would watch the host refuse the file anyway. It is written down as `attachment.limit` and folded off the log the way the toolbox is, so it rides in the snapshot, replays for free, and is refused from a runner the way the music controls are. `cleanAttachmentMb` is the whole of what may be asked for, and a junk number leaves the one standing where it is rather than being taken. Both ways in ask the host's own limit before they keep a byte, the http upload and a message's attachments, and nothing anywhere reads a constant of its own.
- A file left behind for being too big says so where it was dropped. It went quietly before, which reads as a drop that did nothing.
- An image draws where it stands. A file is a row that says what it is, what it is called and how big it is, and pressing it opens it in the side panel where the app can show it and hands it to the machine where it cannot.
- The preview of a picture waiting to be sent is built once and kept. It is the whole file written out as a data URL, so a ten megabyte picture is thirteen megabytes of string, and it was built again on every render of the composer, which is every letter typed while one is attached: two milliseconds a keystroke. `previewSrc` keeps it against the attachment's own id and `keepPreviews` lets go when the attachment goes. Never hand back an object URL in its place: `copyImage` in main reads a `data:` and an `http` address and answers false to anything else without saying so, so a blob would quietly take Copy image off a picture nobody has sent yet.

## GIFs

The plus button in the composer opens a menu, upload a file or pick a GIF, and picking one sends it straight out. `AddMenu.tsx` is the menu and `GifPicker.tsx` is a screen inside the same popover rather than a popover of its own.

- Klipy is the only service left to build this on. Google closed the Tenor API to everyone in June 2026 and Giphy stopped being free, so there is one choice and `gifs.ts` reads it.
- The key is a client key rather than a secret, and it sits in `CREW_KEY` in `gifs.ts` so it rides in the repo the crew already shares. That is what gets everyone who joined GIFs without being handed anything of their own, the same way they get the toolbox and the songs. `VITE_KLIPY_KEY` overrides it for a machine that would rather keep it out of the project.
- The grid takes the small copy and the message takes the large one, and both fall back down the sizes rather than coming back empty, since a GIF the service only holds one copy of is still a GIF.
- The message carries the picture itself rather than a link to it, the way every other image in the chat does, so it is still there when the service is not. The CDN answers a cross origin fetch, which is the whole of what makes that possible.
- A GIF is attached and sent in the same breath, so every send guard reads the attachment count live rather than off the last render. Read from state it is a render behind, and the send is refused for having nothing in it.
- A GIF that fails to send leaves the grid where it is. Only the send failed, so it is said on the tile that failed rather than in a line under the grid: the picture keeps its place, wears a ring in `danger` and the warning mark over it, and is cleared the moment another is picked or the search moves on. A message under the grid named nothing and cost a row of the panel to say a thing that is almost never true.
- The picker is a search and a grid, and nothing else. The search is a flush row with a hairline under it, the way the Actions palette is built, rather than a pill floating in its own padding, which is the shape a search takes in a panel. A popover whose whole content is the result of that search says so with the row rather than with a box drawn around it.
- There is no way back to the menu, because there is nothing to come back for. The two rows it was opened from are one press away again, and a chevron in front of the field is a control that spends the width of the search on going somewhere nobody goes.
- The picker clips to the corners the popover is wearing, `overflow-hidden rounded-[inherit]` on its own box. With a bar under the grid the tiles never reached the bottom of the panel; scrolling under a square corner is what showed once the bar came off.
- `SearchField` is that row and it is the app's search field, wherever one is wanted. It holds the magnifier, the field and a way to clear what was typed. It takes focus when it mounts, since it is the field of a screen that just opened. Seven other places still draw one of these by hand.
- A popover stands in the body, and its clicks still bubble through the React tree to whatever rendered it, which for anything in the composer is a shell that focuses the message box on every click it sees. Typing in the search field was thrown back to the composer by it. `clickToFocus` is that handler everywhere it appears: a click is the box's own only if it landed inside the box, and one that came out of a popover did not.
- The grid's padding and the gap between tiles are the same number, so the gutter reads evenly all round rather than as a frame of one width holding a grid of another.
- The scrollbar is off. A grid two columns wide is narrow enough that ten points of it comes out of the tiles, and the bar sits over the pictures at the one edge where they are already tight.
- Nothing is drawn around the outside of a tile. A GIF is a picture with any color at its edge, so what marks the one under the pointer is an `InsetRing` in white, and it turns up hard enough to be seen over a photograph. Growing the tile instead is what it was, and a tile that grows in a masonry column grows underneath the one after it.

## Emoji

Two things, and they belong to different people. The row of reactions that opens over a message is yours, and the emoji the crew drew themselves are everyone's. Both are set from the settings, on the Reactions page under You and the Emoji page under Crew, which is the rail saying whose each one is without a word anywhere on either page saying so.

- Your row is kept where you sit, in `state/quickReactions.ts`, and nothing about it is ever sent. Ten is the cap, because a row of one-tap reactions past ten is the picker with extra steps and the tray is as wide as what it holds. None is a real answer, so it is written down as one: nought and the default are the same absence otherwise, and the four it ships with would come back the moment somebody cleared the last one.
- A row of none still has a tray. The divider only stands while there is a reaction in front of it, or a rule with nothing on one side of it is a line at the edge of the tray, and the way in is the picker that was always there.
- A row of ten is wider than a message in the side panel, so the tray wraps and nothing in it gives up its own width for that: a button squashed to fit is a smaller thing to aim at than the one beside it. Measured in a real window, the old classes put a row of ten 388 across in a 280 message and hung it 108 past the left edge, because a nowrap row keeps its buttons at their own size and simply overflows.
- Wrapped, it is pinned by its bottom edge and grows upward, the way the ask bar on a board grows away from what it is asking about and the Scribe pill keeps its bottom edge when it gains a line. Anchored at the top, a second line reaches down over the message the tray is about, which takes away the one thing on screen it refers to.
- Holding shift turns the More button into a delete button, so taking a message back is one press instead of a menu and a row inside it. It is the same slot rather than a button beside it: the tray is as wide as what it holds and a row that grows a button under the pointer moves everything left of it, so the press somebody was already aiming at lands somewhere else. The menu keeps its own Delete, since holding a key is a thing a pointer does and the menu is the way there without one. It says More rather than Message actions: the old words named the row of buttons it was already standing in rather than saying what pressing it does.
- The swap stands down while the menu is open. A popover hangs off its button's own box and reads it every frame, so a button that goes to nothing under it leaves the menu standing over the message with nothing to point at.
- Held shift is an attribute on the root rather than state, `data-shift` written by `state/shift.ts` and read by the `shift:` variant in `styles.css`. Every message on screen draws a tray of its own, so held in React a capital letter typed into the composer would redraw every row in the thread twice, which is the one thing the thread is written to never do. Nothing re-renders for it at all. A window that loses focus mid-hold never hears the key come up, so a blur lets go of it, or it comes back with shift held by nobody.
- The picker opens on what you actually use, so `emojiRecents.ts` seeds itself from your own row rather than from the four in `reactions.ts`.
- An emoji the crew added is a picture with a name, written `:name:` wherever an emoji goes: in a message, as a reaction, on your own row, and in the picker for everyone. `shared/customEmoji.ts` is the whole of what one may be. GIFs are the point of half of them, so a GIF is first among the types rather than the exception, and one is drawn as it is rather than through anything that would take a still of it.
- The picture is kept beside the session in `.crew/emoji` and served over the host's own http with `MEDIA_HEADERS`, exactly as a track is, so everyone draws their own copy and nothing streams from whoever added it. Only the two routes a member reads from carry those headers, and this is now the third.
- It lasts, so it goes in the log as `emoji.added`, `emoji.renamed` and `emoji.removed`, and those are folded off the snapshot's own event list the way the shelf is: an emoji is a picture in the picker rather than a moment to scroll past. One whose file has gone is left out on the way back up, since a name that draws nothing is worse than no name.
- A name means one thing for the whole crew, so a name already answering to something is refused rather than shadowed. It is said rather than dropped, because whoever asked is holding a picture and a field, and it is said twice: the window checks before it sends and the host checks again, since taking a window's word for it is how the wrong thing gets written down.
- What somebody typed is taken rather than refused. The case, the colons they wrapped it in, the spaces and dots in the middle are all read as the name underneath, and only a name with nothing left in it once the characters that cannot be in one are gone is nothing. `cleanCustomEmojiName` is that rule and it is the only place a name is decided.
- `isCustomEmojiFile` is what keeps a name off the disk, and the store asks it again on the way in and on the way out. The name written down is a uuid and an extension, so there is nothing in it to walk out of the folder with.
- A reaction may be one off the sheet or one of the crew's own, and a `:name:` nothing here answers to is refused. That is the one rule that means a reaction can never stand for a picture that was never there.
- Naming one again keeps the picture where it is, and what was already reacted with under the old name still says the old name. That is the honest record of what somebody pressed, and rewriting the log to match a rename would be the app deciding it knows better.
- A megabyte is its own number rather than the crew's file limit. That one is about what people send each other; this one is a folder of pictures every machine pulls, drawn at 22 across.
- They are the crew's, so `emoji.add`, `emoji.rename` and `emoji.remove` are refused from a runner, the way the music controls and the leaderboard are. An agent's machine is connected the whole time it is joined.
- The sheet is looked up rather than handed down. `customEmojiSheet.ts` holds the crew's own and the address to read them from, fed by the store off the snapshot and off every `emoji.set`, because a picture drawn from a name has to be reachable from a DOM walk over rendered markdown and from a component a dozen callers deep. Threading the list and the address through every one of them to draw one picture is the plumbing that file exists instead of, and it is the same shape `emojiData.ts` already has for the sheet the app ships with.
- That sheet says when it has changed, and it has to. What draws a name is memoized on the words, and adding an emoji changes neither the words nor any slice a message subscribes to, so a name already on screen would have stayed the four characters it was written as until the message it sits in was unmounted. `useCustomEmoji` is that one hook, taken in `Emoji`, in `EmojiText` and in `Markdown`, which are the three funnels every drawn emoji comes through. It only says so when the list or the address really moved, since the store says it on every change it makes.
- A name the crew does not have is left as the words it was written in. `:shipit:` in a sentence is a picture where the crew has one and four characters of text where they do not, which is also what a reaction to a picture somebody has since removed reads as.
- A name the crew does have beats the sheet's own shortcode for it. Somebody who called their picture `tada` had it eaten on the way in and turned into 🎉, because the composer swaps a closed `:shortname:` for a character as it is typed, so that swap asks the crew's own first. The host cannot settle this, since the sheet lives in the renderer and it is the one place that knows the name is taken, which is why the rule is a lookup in front of the swap rather than a name refused at the door.
- Somebody's own words are drawn as pictures wherever they are read back, and not only in the message they were written in. A line cut down to a row is still the line they wrote, so the thread's own header, the row in the rail and the two lists in the Tasks panel all go through `EmojiText` the way a reply quote does. Left as text, one heart is the system's own glyph in the header and the app's picture an inch below it in the thread, which reads as two different apps on one screen.
- `quiet` is what holds the name back on one of those, and it is for the rows that already open something of their own. `EmojiText` wears a `Tooltip` per picture so somebody can learn what to type, and the header's ask line exists to open a hover card, so hovering the one emoji in it opened a name over the card it had just raised. It is the same rule as a tooltip standing down for the popover its button opens, and it is said on the caller rather than worked out here, since the picture never knows what it is standing in.

## The side panel

Everything the app can stand beside the chat: a web page, a terminal, the project files, what has changed since the last commit, the music, the games, and the thread's own plan, board and helpers. `SidePanel` holds it and `BrowserPanel` draws it.

- It is a place rather than a side effect of a tab existing. `open` on the browser store is the whole of whether it is standing, so it can be open on nothing, and putting it away keeps what is in it rather than throwing three tabs away for one press. Read off `tabs.length` it could not do either.
- Putting something in it opens it, and that rule is written once, in the `set` the store's actions are given. It counts what arrived rather than what is there, or a page that finished loading behind a panel somebody had just put away would bring it back on its own.
- The last tab out takes the panel with it. That is `settle`, and it covers a tab that goes because you moved, a plan that came with a thread you have left, and every tab closed by hand: nothing left in it is nothing to stand on. Opening it again on nothing is what Start is for, so closing the last thing never loses the way in.
- It is put away by dragging its own edge, and dragging is the whole gesture rather than a shortcut for a button. The edge stops at `MIN_WIDTH`, and carried on past `MINIMIZE_AT` the panel goes altogether, back the other way it stands up again, so putting it away and bringing it back is one drag that can be turned around mid-flight. Nothing snaps back at the end: where the pointer was when it was let go is what stands. The floor and the threshold are 120 apart on purpose, since an accidental collapse costs somebody the row of tabs they were reading and there is no gesture to undo it.
- What is put away is minimized rather than closed. The tabs are all still there, which is what `closePanel` already meant, and the only thing that really goes is the room it was taking.
- `PanelToggle` is the way back, and it stands only while the panel is away and there is something in it to come back to. It wears `PanelRightGlyph`, it is the last thing in the top bar, past your own face, so it stands at the end of the row hard against the edge the panel comes in from. Pressing it puts the panel back and takes the button off the row in the same moment: there is one button here, and it is the one for the state you are not in.
- Nothing stands there while the panel is up, and that is the point rather than an omission. The way to put it away is the edge and the close in the panel's own corner, both of which are already on the screen, and a third control in the top bar saying the same thing is a row of chrome standing there all day for a press somebody makes twice.
- It stands only on the chat, since that is the one tab the panel can stand beside. Docs has no panel and Design has a right panel of its own with a button of its own on it, so a second `PanelRightGlyph` in the top bar there is either a button that does nothing or two buttons for two different panels wearing one mark.
- A thread in a window of its own has no top bar, so the same button stands in that window's own drag band, at the right of it.
- It was one button per thing the panel could hold, the plan, the board and the helpers, and then one in the header of whichever thread was focused. Each of those was a control that came and went with what was under it, and none of them was on the screen from the chat with no thread open, which is where somebody who had closed the panel was left with no way back into it at all.
- `PANEL_OPENS` in `panelOpens.tsx` is the one table of what it can hold. The menu the plus opens and the Start tab both read it, so the words somebody picked from are the words they are shown either way, and there is one place to add to.
- The thread's own things stand first and come and go with the thread you are in. The rest never move, so nothing under the pointer travels as you go between threads. `scope` on a row is which of the two it is, and the menu draws its one divider off that pair rather than off the order alone.
- Start is that table as a list, and nothing else. A line over it naming what the rows are would be the screen read back to whoever is already looking at it, and no row promises a key, because nothing in the app listens for one. It wears a tab of its own, so what is on screen is always a tab, and it has no close: closing it is closing the panel, and that button is an inch to the right of it.
- The close in its own corner puts the panel away and keeps the tabs, the same as the button in the thread.

## Several threads at once

The chat holds up to ten threads side by side, as columns, and any one of them can be stood out into a window of its own. `openThreadIds` on the store is the row, left to right, and `openThreadId` is the one the caret is in.

- Opening a thread never closes one. It joins the row, or it is the column it already stands in, and a row with no room left says the move rather than dropping whichever column somebody was reading. That is one rule wherever a thread is opened from, a card, the rail, a task, a toast or a fork, so the way in to a second column is opening a second thread and there is no gesture to learn.
- `src/shared/threadViews.ts` holds the whole of it as numbers, the limit, what joining a row does, what closing one leaves, and where the caret goes afterwards, so `tests/thread-views.test.ts` reads the rules with no window anywhere near it and `tests/thread-columns-probe.test.ts` reads the columns in a real render.
- The way out of a column is the button that was the way back to the chat. One thread open is a way back and several is a close, so nothing was added to the row to say the same thing twice, and the chat is what is left once the last column goes.
- A right click on a thread says where it opens. `threadMenuActions` is that rule and it answers off the row rather than off the thread: nothing open is Open, something open is Open beside, one already in the row is a window of its own or Close, and a full row is left with the window alone, since a row that says Open beside and then refuses is a press that does nothing. A window of its own is always there, which is what makes it the way to read an eleventh thread.
- The thread itself is one of the things that can be right clicked, and that is where a window of its own is asked for now. It was a button in the header, standing beside the way out and the way to the panel in every column at once, for the one press somebody makes on a thread a day. The menu already said the same thing in words, and a right click is what says it everywhere else a thread is listed, so the row is two buttons rather than three and the gesture is the one already learned. A window standing on one thread says nothing of its own, since it is already the answer, which is what `alone` decides.
- What already answers a right click keeps it. Nothing is offered over a link, a picture, a field or anything editable, and nothing is offered over a selection, because that is where Copy lives and the words are the reason somebody is there. `ownsMenu` is that rule and `selecting()` is the other half, and left out the app would hand back its own menu in place of the one with Copy on it.
- The row ends on the way to another, the way the toolbox's own row ends on the slot that opens the builder. The chat mark stands at the right edge and pressing it stands the chat itself in the row as the last column, lit while it is there and pressing it again is what puts it away.
- It only stands while the pointer is near the edge it hangs off, `nearRight` and `NEAR_RIGHT`. A control for a move somebody makes now and then, standing in every thread all day, is a strip of the screen spent on nothing, and it was a column of its own before this: 44 points of empty gutter down the side of the work. Near the edge it arrives over the far right of the thread rather than beside it, which is room nobody has to pay for the rest of the time, and it comes from the edge rather than growing, the way the rail does. It is glass, so nothing on it is set in a solid grey. Without it the only list of every thread there is was the chat, and the chat is exactly what the columns take off the screen, so the first column closed the door behind it: the rail was the whole way to a second one, it holds a handful of live threads and none of the finished ones, and from the chat there was one thread anybody could ever open. It reads as opening a thread the ordinary way, which is what it was.
- The chat in the row is the chat, composer and all, and a card in it opens beside rather than in place, which is the same rule everywhere else. It goes with the last column, since the chat standing beside nothing is the chat, and the slot goes when the row is full, since there is nowhere left to put what it opens.
- What is shared is the menu rather than the row, the way the one in Files is: `ThreadOpenItems` is the rows, for a card that already has a menu of its own to stand them in, and `useThreadMenu` is the whole card, for a row that had none. The doing stays in the menu and only the opening is handed in, since a thread in the rail is opened by going to its project and a thread in the feed is opened where it stands.
- A column never goes narrower than `COLUMN_MIN`. It holds a composer, a diff and a terminal card, so past that the row scrolls sideways rather than squeezing them: a column too narrow to read is a column nobody opened on purpose.
- There is one side panel, so no column holds a control for it. The way back into a panel that has been put away stood in the focused column once, which kept a row of them from being several buttons for one box, and a control that moves from column to column as you click about is still a control that moves. It is in the top bar now, where it never moves and never has to say which column it means.
- The same goes for `⌘F`. Every column draws a find bar of its own, so the shortcut belongs to the column you are writing in, which is `listens` on `FindBar`. A bar already open still answers it, or the way to close the one in front of you would depend on which column was last touched.
- The panel's own thread tabs are read against the whole row rather than the one thread, which is `leaveThreads`. Read the old way, opening a second column took the first one's helpers off the screen.
- What is already on the screen is not said again, and with a row of them that is every thread in it. `finishedAlert`, `memberMentionAlert` and `memberReplyAlert` take the row, or a thread being read in the second column raises a banner about itself.
- A thread in a window of its own is the same session seen through one thread. The window is opened by main, looking at the crew the window that asked was looking at, and the thread rides in the hash so it survives a reload: `threadWindowHash` writes it and `main.tsx` is where a window with one in it becomes `ThreadWindow` rather than the app. It boots the way the app does and joins nothing extra, since two windows on one folder are already one member.
- It pins the store's row to its own thread, which is what makes everything downstream true there: a page an agent shows opens in that window, the panel holds that thread's things, and nothing raises a banner about the thread being read.
- Popping out takes the column with it. Standing in both places at once is two composers on one thread and nobody asked for the thread twice.
- A thread of another project pops out too, and that is the one way a thread from somewhere else is read without leaving where you are. The window is opened on the crew the thread belongs to rather than on the crew that asked, which is `popOutTarget` in `src/shared/popOut.ts`: no key named is the crew the asking window is in, a key named is taken only if that crew is running, and nothing opens otherwise rather than a window landing on the way in with a thread named in its URL. The rail only ever lists the threads of crews that are running, so the refusal is a guard rather than a path anything takes. `popped` is keyed by the crew and the thread together, or two projects could hold one window between them.
- A column cannot do the same, and the reason is the store rather than the layout. `useCrew` is one session whole, built on one socket, and `BLANK` is that whole session wiped in one go when a window changes crew: a row of columns from two projects is two live stores in one window, which means the toolbox, the design channel, the pending map, the helper prefs and the custom emoji sheet all stop being able to answer which crew they mean. Every one of those answers for free today because a window is in one project. A window of its own answers it for free too, which is why that is the way in.
- It closes for real. On a Mac the app's own window is put away rather than taken down, and a window standing on one thread wearing that close would be hidden with the thread still spoken for, so the next press would open nothing.
- Nothing this machine says about itself is said by a popped out window: the presence, the dictation settings, the awake switch and the dock's icon are the app's own window's, and the dock has one icon however many windows are open.

## Find

`⌘F` opens `FindBar` over a doc and over a thread. It walks the text nodes of whatever it is handed and paints the hits through `CSS.highlights`, so nothing anywhere is re-rendered to mark a match.

- A search reaches what a step is carrying, not what the step is drawing. A folded run of reads shows one line, a thought shows the word Thought, and a command shows what it ran and not what it printed, so a search that reads the screen misses most of a thread. `find.ts` is that rule: `stepText` is everything a step holds and `stepHidden` is the part of it that is closed, and a row or a run whose hidden part is what was typed opens itself.
- It sets the default rather than overruling. `open ?? (found || live)` is what every step wears, so a row somebody has already opened or closed keeps what they chose, and clearing the search puts the thread back the way it was.
- The query is published from the field's own handler, never from an effect. An effect runs after the commit, so the bar would walk the page a pass before the rows that had to open for it, count them out and say nothing was found. Both land in one commit, and the counter is what says so in `tests/thread-find-probe.test.ts`.
- The bar stands inside the column it searches rather than in the window, so a thread with the side panel open does not draw it over the panel. Whatever mounts one holds `relative`.
- It is glass, so nothing on it is set in a solid grey.

## Terminals

A terminal is one of the things the side panel can hold, beside a web page and a file. The New button asks which. It is the real thing: a pty running the login shell, started the way Terminal and Windows Terminal start it, in the project folder.

- `node-pty` opens the pty and xterm.js draws it. node-pty is built on N-API, so the binary in the package loads in Electron and in plain node alike and is never rebuilt for an Electron version. That is what lets the tests drive a real shell.
- The package hands over its `spawn-helper` without the execute bit, and without that every terminal fails to start with "posix_spawnp failed". `scripts/ensure-pty.mjs` puts it back on every install, and `asarUnpack` keeps node-pty out of the asar so the helper stays a file the system can run.
- Sessions live in `src/main/terminal.ts`, one `Terminals` per window, closed with the window and on quit. None of it is written down or shared. It is this machine's shell, not the crew's.
- A shell is only spoken for while it is the one standing under its name. React mounts a view twice while developing, so a tab is opened, closed, and opened again under the same name in one breath, and the first shell's exit lands after the second has taken the name. Striking the new one off on the old one's word leaves a terminal that prints but never listens, and prints "[Process completed]" before its own first prompt.
- The pty is named after the tab, so nothing has to be handed back before the terminal is wired up, and the first keystroke has somewhere to go.
- xterm measures itself against the box it is in, and a box with no size yet throws from deep inside it. The fit is left to a `ResizeObserver` rather than done at mount, which is also what tells the shell its new size when the panel is dragged wider.
- The palette is in `terminalTheme.ts` and follows the app theme. Red and green are the app's own danger and positive.
- Copy and paste on a Mac are the app menu's, and xterm answers the events they raise. Everywhere else it is Ctrl+Shift+C and Ctrl+Shift+V, because plain Ctrl+C has to reach the shell.
- A shell that ends says so and the tab stays open, the way Terminal does, so whatever it printed on the way out can still be read.
- One shell is always kept ready, so a tab opens on a prompt instead of a blinking cursor. A login shell reads the whole profile before it says anything, which is over three seconds cold on a machine that loads a version manager, and none of that wait belongs to the person who just asked for a terminal. `warm` starts the spare, `claim` hands it over, and every `open` starts the next one. Main decides when, in `warmTerminals`: once a window has loaded, and again whenever a session is started, joined or resumed, because that is when the folder a terminal would open in is known. The renderer is not told any of this and has nothing to call.
- A spare is only ever handed to a tab in the folder it was started in, and a shell that ended before anyone claimed it is passed over. Both fall back to starting a shell the old way, so a stale spare can never put a terminal in the wrong place.
- What the spare printed is held and replayed to the tab that takes it, through `replayable`. It is never replayed raw: zsh pads its prompt line out to the width it believes it has and then returns to the start of the line, so bytes drawn for an eighty column spare leave the end of line mark stranded on screen in a tab of another width. Only what follows the last carriage return of a line was ever meant to be read. Nothing here may assume the spare and the tab are the same size.
- The spare counts for nothing in `count()` and goes with `closeAll`, so a window that never opened a terminal still leaves no shell behind.

## Files

A file opened from a message that changed it opens on the change, showing the diff the way VS Code shows one. It is still a file you can type in.

- The lines that were taken out sit above the ones that replaced them, and the words that changed inside a line are tinted again over the line's own tint, the way VS Code marks what changed inside a line.
- A step only records the text an agent swapped, not where it landed. `baseline.ts` matches that text against the file on disk and reverses it, to get the file as it stood before. `diffRows.ts` diffs that against what is on screen, and does it again on every keystroke, so what you type is part of the diff from the moment you type it. VS Code waits 200ms, because its editor holds the document and draws the deleted lines beside it. Here they are text in the same box, so a keystroke has to land on the rows it was typed against and there is nothing to wait for.
- That box holds the deleted lines too, because it is the only way the text under the caret lines up with the rows drawn behind it. What is typed is mapped back onto the real file first with `toDoc`, and the caret is put back after with `toShown`. Never write the box's contents to disk. `docText` is the file and the rest is the diff.
- The caret never sits in a line that was taken out. It steps over the whole block, back or forward depending on which way it came, the way a cursor steps over a view zone in VS Code. Rubbing out at the head of a line under a block joins it to the line above, because that is the line above it in the file.
- Clicking dismisses nothing. It puts the caret where it landed and leaves the page where it was standing. Hide changes puts the diff away.

Files from the toolbox opens the same tab with the project standing beside it, and the button in the toolbar puts that pane away and brings it back.

- The tree and the folder listing are the same thing, so only one is ever on screen. `FileView` shows `DirRows` when the pane is closed and an empty state when it is open, rather than the same folder twice.
- A folder opens where it stands and is read only once it is asked for, so nothing walks the whole project to draw the top of it. Which folders are open rides on the tab, beside where it has been, so a pane closed and opened again is where it was left.
- Turning the pane on opens every folder down to the file already showing and scrolls to it. A tree that lands back at the top of the project when you were reading something four folders down is a tree nobody uses twice.
- The filter searches the whole project rather than the folders that happen to be open. `listRepoFiles` asks git what the project is made of, which leaves out everything `.gitignore` covers for free, and only a folder that is not a repository is walked by hand.
- What was typed is matched letter by letter, in order, by `matchFiles` in `src/shared/files.ts`. A run inside the file's own name beats one spread across the folders above it, and the letters that landed are handed back so they can be picked out in the row.
- A right click on a row opens what it names in a tab of its own. Three kinds of row do it, the folder listing, the tree and the filter's results, so what is shared is the menu rather than the row: `useFileMenu` in `fileMenu.tsx` hands each one a handler for its button and the card to stand beside it. It is a new tab every time rather than the one already holding that file, since keeping the file you are reading while this tab goes on somewhere else is half the reason to ask for one, and a row that says new tab and does nothing is worse than a second tab.
- A path nobody here can follow is shown as it was written, and Private file is the one exception. Being missing was what raised the chip before, so `/agents/spawn` and `/users/12/posts` came back as somebody's file and the words went off the screen: a slash is how an address is written as well as a folder, and the app cannot tell the two apart by looking for them on the disk. `personalPath` in `src/shared/files.ts` is the whole of the exception, a named file under somebody's own folder, and both halves have to hold. Everything else falls through as `local` and reads as text. The two ways of being wrong do not cost the same, which is what settles it: a full path shown where a chip would have done is a line somebody reads and moves past, and a chip standing over a sentence takes the sentence away.
- A path may have spaces in it, and a step that is nothing but a path is where that has to hold. `Application Support`, `Google Drive` and a folder somebody named in two words are all on an ordinary Mac, and a Read of one of them was a row that would not open: it fell through to a card holding the path as text, which is the one thing a person cannot follow. `parseFileRef` is where spaces are allowed and `fileTokens` is where they are not, and the split is what makes it safe. A whole string that is a path is unambiguous however many spaces are in it, and a path hunted for inside a sentence would swallow the words after it. A false positive costs one lookup and no link, since `openable` still asks whether the file is really there.

A file that is written to be read as a page is offered both ways, and one button in the toolbar is the switch. `canPreview` in `src/shared/files.ts` is which files those are, markdown and html, and `preview` rides on the tab, so walking from one page to the next keeps reading them the way you asked.

- The page is drawn from the words in hand rather than from the file on disk, so an edit typed in the text is on the page. There is no typing while it is up, since the box the caret lives in is the diff and a page is not one, and Save and Discard stay where they were.
- A markdown page is drawn by the app, in `MarkdownView`, and every picture it points at is read off this machine by `mdImages.ts`, because a page drawn in the app cannot reach a file by its path.
- An html page is drawn by the browser the panel already has, in a webview, because a page rendered by anything less than a browser is not the page. It is written out to the app's own folder first, by `Previews` in `src/main/preview.ts`, and never beside the file: a page written into the project would be committed and synced to the whole crew for the few seconds somebody spends looking at it. A window takes its own pages away when it goes, the way it takes its terminals.
- Standing somewhere else is what makes a base necessary. `withBase` in `src/shared/htmlPage.ts` hands the page the folder it really lives in, so the stylesheet, the pictures and the scripts beside it are still reached, and a page that says its own base keeps it. Without it the page still draws, unstyled and with a hole where every picture is, which is the failure no suite can see: `tests/html-preview-probe.test.ts` holds the words and the switch, and `yarn preview-check` is the only thing that can see the rest of it, since it stands a real page up in a real window and reads the color, the picture and the script back out of it.

## Review

What has changed since the last commit, in the side panel. It is where somebody working on their own spends the long half of the day: an agent finishes, and this is the screen the work is read on before any of it is kept. `ReviewView` is the panel and `useRepoWork` is what it reads, one call every three seconds that never lands on top of an action.

- Reading is the whole of it, so a change is drawn in the lines around it. Git already sends three either side of what moved and `rowsOf` threw all of them away: a line with nothing around it cannot be judged, and two stretches four hundred lines apart ran together as though they were neighbours. `reviewRows.ts` is the reader that keeps them, and `rowsOf` stays exactly as it was for a step in a thread, where a diff is a glance at what an agent did rather than the work itself.
- `line` on a row is what the highlighter reads and `at` is the file's own number. They are two fields because they are two things: the document handed to shiki is the one being drawn, so a file whose first stretch starts at line four hundred would hand it four hundred lines it does not have and every row would lose its colour.
- What stands between one stretch and the next is a rule rather than a count of the lines nobody is being shown, since the numbers either side already say how far it is. It carries what git worked out the lines below it sit inside, because landing partway down a file is the one thing that cannot be read off the lines themselves.
- **The list and the file being read stand one above the other, and the list is never taken away.** Every desktop client draws these two together: VS Code keeps its sidebar while the diff is in the editor, GitHub Desktop puts a 250 wide list beside one, and not one of them has a next-file key. That is not an omission, it is what having the list on screen means, since the list is how you get to the next file. Crew's panel is 480 across, narrower than GitHub Desktop's own floor for the pair, so the same two panes are stacked rather than laid side by side: the list keeps the width a path needs and the reading gets the whole of it for a line of code. The accordion this replaced was neither, a diff in a slice of a column under a list it pushed off the screen.
- One file is read at a time. Several open at once was the answer while the list was the only surface there was, and it cost the reading the bottom of the panel: the second file opened under the first and everything worth comparing was already scrolled away. Picking another puts it in place of the one before it, the row it stands on says which, and the arrows walk the list with the reading following as they go.
- `split.ts` is where the two panes meet, as numbers and nothing else. Both have a floor, because a list under a heading and a few rows and a diff under a hunk are furniture rather than somewhere to work, and a panel too short for both floors is halved rather than handing the list more than the window has. How tall the list stands is this machine's own, in `localStorage` the way the volume is, so it is never sent.
- The reading wraps and a step in a thread does not, which is `wrap` on `DiffLines` and the one place the two diverge. A glance at what an agent did is read past rather than read, and the line it cuts off is one nobody was going to reach. A file under review in a 480 column is the opposite: the line running off the right is the line being judged. GitHub Desktop, the one client built for a narrow pane, wraps and never scrolls sideways at all. What a wrapped line does with its second half is the whole of whether that is readable, so it hangs under the code: flush against it, every wrap reads as a new line of the file and a diff stops having a shape.
- Which files you have been through is yours rather than the crew's, so it is held on this machine in `state/reviewed.ts` and nothing about it is ever sent, the way an answered question on a board is. It is keyed on the place and on a digest of the diff, so a file an agent has written to again comes back unviewed: a mark held against the path alone is a promise the screen cannot keep.
- The row going quiet is what says it has been viewed, and the mark is the quietest thing on the row rather than the loudest. White is the one action color and this is not the action. Not viewed, there is no mark at all until the pointer is there, or a column of empty circles down a list of files is a column asking to be dealt with and most of them never are.
- Nothing about committing stands on the panel until there is something to commit. It was a field and a button at the top of the screen for the whole of the time there was nothing to save, which is a dead box and a dead press above the one thing anybody came here to do. What stands instead is the same button Implement plan is, in the same place at the foot of the same panel: one white pill the width of the column, standing in the layout rather than floating over it, and it opens the card the message is written on. On glass it was a pill inside a card, which is two rounded boxes for one press, and it covered the last lines of whatever was being read underneath.
- **Git's own words are the words here, and this is the one screen in the app where that beats writing plainly.** The rule about plain words holds everywhere Crew is speaking for itself. This screen is Crew putting a hand on somebody else's tool, and there the tool's own name is the plain word: everybody in front of it already has a repo, a branch and a remote, stage and discard and stash are the names of real places and real moves rather than jargon for them, and every client anybody has used for ten years calls them that. Translated, the panel lost the one thing that makes a tool usable on sight, which is knowing what it does before reading it. Going in did not say where, Take all out read like a delete, and somebody who knows git had to work out which of git's moves each line meant. Never translate them again.
- The groups are git's own groups and they are named the way VS Code names them: Merge Changes, Staged Changes, Changes, and Stashes for what is put by. An empty one is not drawn. Each folds away, since a group somebody is done with is a group in the way of the one they are not.
- A conflict stands in Merge Changes at the head of the list rather than in among the ordinary edits with a mark on it. It is the one thing on the screen that has to be settled by hand, and the way out of it is staging what has been settled: nothing offers to discard one, because taking a conflicted file back to what the index holds throws away both sides of it rather than one edit.
- Every row wears the letter git would give it, in front of the path, where `git status --short` puts it and where a client that reads like a terminal puts it. VS Code has it at the far end and it went there once for that reason, but the end of a row is also where the counts, the two moves and the viewed mark all have to stand in 480 pixels, and a column read as a gutter is scanned in one pass where the same column hunted for on the right is not. A column that says the same thing on most rows is noise only while it is loud: muted, it is a place to look rather than a thing to read, and the whole of its worth is the row where it differs. Only the two the row cannot otherwise carry are colored, an add and a delete, and a deleted name is struck through the way it is everywhere else.
- A row is a row rather than a card. It was `rounded-card` on `ink-800` with a gap under it, which is 46 pixels a file and seven files in a panel that had to hold forty: seven objects standing apart rather than one list to run an eye down. Flush rows at 28 with a wash under the pointer is what every client does and it is what makes the count of files on screen the count of files there are. VS Code's own row is 22 and GitHub Desktop's is 29, so 28 is the band rather than a number picked here.
- Untracked is worked out here rather than sent. A file git has never been told about comes back as an add that is not staged, since nothing can stage itself, so that pair is the whole rule and there is no kind to add to `RepoChange` for it. It is worth saying: a file nobody has added yet is a file that commits by nobody's decision.
- A row holds the two moves it is really for, and the rest is on a right click, the way it is in Files and the way it is in every client this is meant to feel like. The slot for them is held whether or not the pointer is there, so nothing on a row travels as it is reached for and a staged row with one move in it lines up with an unstaged row that has two.
- Discard stands last in a row's menu, under a divider, and never above Stage. VS Code has it the other way and that is an accident of its own sort rather than a decision: its menu items carry the same order number and the tie is broken on the title, where Discard comes before Stage in the alphabet. What is deliberate is Crew's rule, which is that the one press nobody can undo goes last.
- Viewed is the word, not read. It is what code review has called it since GitHub, and how far through you are reads as progress rather than as a debt. Marking a file viewed and moving to the next one is one press, in the pane where the reading really ends, because that is the whole of what a review is made of and doing it in two presses is doing it twice.
- How far through the reading somebody is stands as a line under the header rather than as a sentence. Said in words it is a row of the panel carrying a number that is only ever glanced at, and the branch, the size of the whole change and the two ways to move it already have that row. It wears no `Tooltip`, and that is a rule rather than an omission: a tooltip is an inline-flex box that shrinks to what is inside it, so a bar asking for the whole width comes out at nothing at all, and being inline it stands on a line box of its own, which was twenty pixels of nothing under the header. Anything full width that has to be measured has both of those problems.
- One header row, not three. It was the branch, then a summary, then the first heading, which is a hundred pixels of chrome over the one thing anybody came here for.
- Pressing Push commits the whole working tree first, `.crew` included, which is `commitWorkingTree` in `src/server/git.ts` rather than anything the panel decides. Nothing on this screen says so yet.
- `tests/review-panel.test.ts` holds the groups, the conflict standing in its own and offering no discard, the letter on every row, a group folding away, one file read at a time under the list, the arrows walking it, viewed moving to the next one, the pane following a file that has just been staged, the lines around a change, the numbers coming off the file, the wrap being the reading's and never a step's, the floors under both panes, the right click, the mark surviving a reload and coming undone when the diff moves, and the commit staying off the panel until there is something to commit.

## The boot

The first thing the app says. It opens travelling, drops out of it, and the crew lands on the other side. `views/Boot.tsx` is the moment and `components/boot/` is what it is made of.

- It happens once, on the way in, and never again. `App` holds a flag rather than reading the connection, so leaving a session goes back to the list and not back to the mark. It ends when the app is ready and the mark has landed, whichever is later: an app with nothing to load would otherwise flash the whole thing past in three frames.
- A window flies in once while the app is being worked on, and a reload lands straight in the app. Watching it go past on every save is what teaches people to sit through it with their eyes elsewhere, and it is the one thing that would make the way in worse the more it is looked at. `seen.ts` is that rule, in `sessionStorage` so it belongs to the window rather than to the machine: a window opened fresh flies again, which is what leaves the flight there to work on, and a build people run always flies.
- The rules are numbers and nothing else. `warp.ts` holds the field, the speed and the projection, `nebula.ts` holds the cloud, and neither one draws anything or knows what a canvas is, the same split the games hold. `drawWarp.ts` is the picture and `WarpField.tsx` is the canvas and the loop, which is `useGameLoop`, so a frame is worth what it really took.
- Nothing is held in state. The field is written straight onto the canvas every frame the way the music's bars are, because a render a frame would cost more than the picture is worth.
- A star is a line from where it was to where it is, and `TRAIL` stretches that line well past one frame's travel. One frame is a few pixels however fast the field is going, so without the stretch lightspeed is a screen of dots. As the field slows the two ends of the line close up until what is left is a point, and that closing is the whole of dropping out of lightspeed.
- The mark lands white. Only the light behind it is lit, so `data-crew-lit` goes on the glow and never on the boot itself: lit, the discs become windows onto the mesh and the mark stops being the mark. `data-crew-lit` is the one hook for this and `CrewLogo` wears it too, so the button in the top bar and the light on the way in turn the same field on.
- `CrewGlow` is that light. It is the mesh the mark is a window onto, drawn without the mask and thrown wide, and it carries no sky: a sky is a rectangle, and a blurred rectangle reads as a box standing behind the mark rather than as light coming off it.
- The cloud is worked out pixel by pixel, once, and then only ever drawn, so its detail costs nothing per frame. A handful of soft circles is a smudge wherever you put them. What makes a nebula read as one is filaments, and a filament is noise folded back on itself: the field is domain warped before it is read, and ridged after, which turns broad humps into thin veins with dark between them.
- It covers the whole window. The middle is brighter than the corners by a little, never by enough to leave the edges empty, and `tests/boot-nebula.test.ts` reads the picture in tiles and holds it there.
- It is animated by moving against itself. Layers at different sizes turn, breathe and drift their own ways, and what they are scaled by is the travel the stars have already made rather than a clock, so the cloud rushes while the field is at lightspeed and settles as it comes off it. One layer over a window is wallpaper however good it is, because nothing in it moves against anything else.
- Two clouds are drawn, and the second one on the frame after the first, so the flight starts on time rather than waiting on both.
- Color comes from `MESH_COLORS`, which is the mark's own mesh. The stars are white but for a few, the cloud is lit by the same table, and nothing here invents a palette of its own.
- Reduced motion stands the flight down and lands the mark where it is.

## Several projects at once

A rail down the left holding every place you have been, and under each one the threads running there. It is opened by the mark beside the Crew logo: hovering it stands the rail over the page, pressing it pins the rail and pushes the page over, the way the browser does on the right.

- The rail runs the whole height of the window, the title band included, and there is no gap over it. Started under the top bar it was a strip of somebody else's chrome sitting on the rail's head, and the wrapper standing there took the clicks meant for the button underneath, so the one control that puts the rail away could not be pressed while the rail was open. It stands above the top bar in the ladder, on `z-50` with the panels, which is also what keeps the chrome's own scrim off the first rows of the list.
- It is written last in the tree it stands over, and that half is about the drag region rather than the ladder. `-webkit-app-region` is flat geometry and a `z` means nothing to it: the bands are read in the order they are written, so a drag band written after the rail hands the rail's own rows back to the window to be dragged by. The design board's header is one of those, the width of the page, and the top of the first project in the rail sat inside it, which reads as a row that moves the window instead of opening the project. Last, the rail's `app-no-drag` is the final word on that band. What is painted over what is settled a rung above, by the page being one stacking context.
- **The mark and the button that opens the rail belong to the window's corner rather than to the top bar.** `WindowCorner` is that pair, standing at `z-[55]` above the rail and below a dialog, and it is the one rung the ladder gained: the rail covers the band they used to be drawn in, and a button that is one press from the pointer has to stay where the pointer is. Nothing about them moves, ever. The rail opening under them changes neither their place nor which element they are, so the pointer that opened the rail is still on the button that shuts it, and the `mac:pl-[64px]` beside the traffic lights is written once here instead of moving between the top bar and the rail.
- The corner holds the hover it does not start. Reaching from the rail up to the mark leaves the rail, and reaching from the button across to the mark leaves the button, so both would have shut it a breath later. The button is the only thing that opens it and the whole corner is what keeps it open, which is a press on it while it stands and no rail that opens for a pointer resting on the mark.
- **Pinned, the rail is a hole in the window rather than a surface in the page.** What comes through it is the macOS material behind the glass, and that only reaches the screen where nothing in front of it is painted, so `#root.railed` stops the app painting its own floor and the columns beside the rail paint theirs. The window was already transparent, so nothing else about it moves, and the material is `under-window` rather than `sidebar` because a sidebar's is the livelier of the two and this one stands under Crew's own tint.
- The tint is the app's own `--glass-bg` and nothing else, the same three values anything wearing glass without the class reads. Bare, the material is whatever is behind the window, which on a blue desktop is a blue rail, and a panel of the app's that takes its color from somebody's wallpaper is not the app's any more. Over the tint what is left of it is the light rather than the color.
- A hovered rail cannot have any of that, and it is right that it does not. It stands over the chat rather than beside it, and a backdrop filter samples the page it is drawn in, so the material behind the window is behind the thread as well and could never reach it. It wears `.glass` instead, the same tint over the app it covers, and adds `glass-strong` over the design canvas the way every other floating panel does.
- **A rail opened by a pointer has to be able to turn around, so it is never taken out of the tree.** A hover is a gesture somebody is still making: the pointer sweeps past the corner, reaches for the button, changes its mind halfway. A keyframe cannot answer any of that, because it starts at its own `from` however far the rail had already got, so an arrival caught halfway either ran to the end and vanished or began again from off the screen the next time. And a rail that is unmounted has no way out at all, which is what the grace in front of it was really covering: a hundred milliseconds of a panel standing still, and then it was simply not there. It is mounted whenever it is not pinned and shut with a transition instead, which moves from wherever the thing is standing, so leaving mid arrival turns it around from there. The grace is shorter for the same reason: the leaving is now the forgiveness, since coming back during it catches it.
- It comes from its own left edge rather than growing, so the rows inside it never move, and it goes quicker than it arrives, the rule the screen being left already holds. Both timings are on `.rail` and `.rail[data-open]` rather than on one of them, because a transition is written on the state being moved to and that is the only way in and out can differ. What is shut is `visibility` rather than an opacity: the glass throws a 48 pixel shadow, so a rail parked off the left edge at full strength paints a dark band down the side of the app forever, and hidden also takes it out of the tab order and out of reach of the pointer without anything having to be said about hit testing.
- Pinning from a hover is the one gesture where two rails exist at once, and the hovered one is held for the length of it. The pinned rail is revealed by a box growing from nothing, so dropping the hovered one at the press leaves it being unmasked from its left edge while an identical rail vanishes off the top of it. Held, the two are the same pixels and the only thing that moves is the page being pushed over. `PIN_MS` is that length and it is the width change's own, written once so the two cannot disagree.
- The material follows the theme Crew is wearing rather than the one the system is, which is `nativeTheme.themeSource` off the `app:theme` the renderer already sends. Left alone it is the system's answer, so a light Crew on a dark Mac is dark text on a dark frosted rail.
- Windows keeps the surface it always had. Its window is opaque, there is no material behind it, and a hole cut in an opaque window is a hole.
- **A project you switch away from keeps running.** That one rule is the whole feature and it is why this was main process work rather than a screen. `src/main/index.ts` held one `AppSession`, and `startHost` began with `await this.stop()`, so every switch killed the agents working in the project you left. A rail on top of that is a faster way back to the list rather than a way to work in two places.
- `src/main/crews.ts` is the registry, and `AppSession` now means one live crew and nothing else. The seven things that were about the machine rather than about a session moved onto it: the recent projects and joins, forgetting either, what opening a folder would do, what the CLIs on this machine can do, and installing one. Nothing there touches a live session, so keeping them on one was what made the class mean two things at once.
- A place is keyed the way the list already keys it, `project:<folder>` or `join:<link>`, from `src/shared/places.ts`. The row in the rail and the session running behind it are named the same thing, so nothing has to be matched up.
- Which crew a window is looking at is a map from the window's own id, the same shape `terminalsFor` already uses, so two windows on one machine can be in two different projects. Every handler that used to read the one session reads the one this window is in, and a window with none is handed a session that has never been started, which is what keeps "Open a project first." saying what it always said.
- **Crews are never remade to be switched between.** `switchTo` moves which crew a window is looking at and nothing else: no server closes, no runner reconnects, no git loop stops. The window closes its socket and opens one on the other crew, and the welcome hands it the whole session, so coming back is complete and instant with nothing cached and nothing to migrate.
- The connection goes straight from one crew to the other and never through home. That single line is what makes a switch seamless rather than a flash of the way in, since `App` unmounts everything the moment `connection` reads `home`.
- **A crew is only itself while `127.0.0.1` and its port belong to it alone, and that is a thing to check rather than a thing to assume.** Every window connects there, every runner connects there, and every curl line an agent is handed is written from it, so two crews at one address is two crews that are the same crew to everything that reaches one. Each of them asked for `PREFERRED_PORT` and only the address differed, shared taking `0.0.0.0` and private taking `127.0.0.1`, and macOS lets the second of those bind a port the first is already holding: it errors on nothing, and loopback goes to the narrower of the two. So a private crew opened second stood in front of a shared crew opened first and answered its windows. Nothing looked broken until somebody came back: the sockets already open carried on, the agents carried on, and the one window that reconnected was handed the wrong session, told "Wrong session code", and reconnected forever. `portToAsk` in `src/server/port.ts` is the rule, and it asks the one question that covers both addresses and other processes as well: does anybody answer on loopback here. Somebody does, and the crew takes a port of its own instead.
- Two of them opened in the same breath would both find it free, so `Crews.start` runs one at a time. That is also what makes one folder asked for twice at once one crew rather than two, which is the same race a beat earlier.
- **A rule like that cannot be held by a suite that runs on a machine with Crew open.** `tests/crews.test.ts` already asked for two different ports and got them the whole time this was broken, because the real app was sitting on the preferred one and both crews under test fell back to a port of their own. `tests/crew-port.test.ts` takes a port it knows is free and stands the collision up on purpose, so what is being tested is the rule rather than whatever the machine happened to have spare.
- Turning sharing on and off moves the listener and is meant to be the only thing that happens, which is true only while the crew comes back at the address it was reached at. The old listener is down for the length of it, so a port taken in that moment moves the crew, and the window is told where it went rather than reconnecting to where it used to be until somebody restarts the app.
- **What belongs to the machine stays, what belongs to the crew goes.** A terminal is this machine's shell rather than the crew's, so a terminal tab belongs to the window and is left standing through a switch: start a build in one project, switch, and watch it finish from another. `stash` and `restore` on the browser store partition on exactly that, the project's tabs are lifted out and the incoming project's land in their place, and the terminals hold the tail of the row so the one thing that persists is never the thing that jumps under the pointer. A terminal that was the tab you were on stays the tab you are on, which is the panel not changing at all when you switch, and that is right: you were typing in a shell and you still are.
- A terminal keeps the folder it was opened in, because the shell has a cwd and may have been `cd`'d anywhere since, and that is the truth rather than something to correct.
- The warm spare is a small map keyed by folder rather than one shell. `claim` refuses a spare warmed for another folder, which was free when a window saw one project a session and is three seconds of a blinking cursor every time somebody switches. Two is the project in view and the one before it, so moving back and forth between a pair never pays for a login shell twice, and `warmTerminals` runs on a switch off the folder now in view.
- The huddle ends when you leave the project it is in. A call belongs to a session, and the leave goes out before the socket closes so everyone else hears an ordinary departure rather than watching a tile hang about until the host notices the connection has gone.
- **Leaving is done with this crew rather than done with the app.** With another one still running the window lands there instead of on the list, and only the last one out goes home.
- The threads under a place are the ones really running there, read off that crew's own `CrewSession` in this process. A place that is not running shows no threads, which is honest: nothing here is guessing at what is in a project it has not opened. A crew somebody else is hosting shows none either, since its events are on their machine.
- `activeThreads` in `src/shared/threads.ts` is that fold, and it leaves out what is not somebody's own work: a helper, a question asked on the side, and anything finished or archived. A ghost thread is never in it without a rule of its own, because a ghost's events go to one socket and never reach the session's own list. It holds a handful rather than everything a long project ever had, since a rail of forty threads is a rail nobody reads.
- They stand in the order they were last worked in rather than the order they were opened, so a thread somebody has just come back to after a week is at the top rather than six rows down under everything opened since. `stirs` in `src/shared/threads.ts` is what counts as something happening in one: a run starting or ending in it, and a message landing in it. A step is left out on purpose, because a run has already put its thread at the top and nothing it says on the way can move it from there.
- `CrewSession.onEvent` is the one hook this hangs off, on `emit`, which is the single funnel every event already goes through. The list is only pushed again for the events that really reshape it, `RESHAPES_THREADS`, because a run emits a step every few hundred milliseconds and pushing the whole rail for each would be the rail redrawing all afternoon. Nothing may move a thread up the rail without being in that set, or the order on screen is right only until the next thing that is.
- What says a message landed is `message.route` rather than the message, which is why that one is in `RESHAPES_THREADS` and in `THREAD_EVENT_KINDS` beside it. It is five ids, so a joined window's rail is seeded with the real order and every chat message stays off the runner's own channel and out of what that window holds in memory.
- Pressing a thread in another project switches to it and opens that thread, and the opening waits for the welcome rather than happening at once, because until the welcome lands this window has no such thread to open.
- What a project keeps while you are away is its side panel and the thread you had open, in `state/projectMemory.ts`, and terminals are named there as the exception since that is the one place somebody would go looking for them. Everything else re-arrives in the welcome, so nothing else is worth keeping. Which tab of the top bar you were on is the window's rather than the project's, and stays where it is.
- **Where you were reading in the chat is the project's own, and it is the one thing a switch loses by the app never unmounting.** `useStickToBottom` keeps a place per key, and the chat's key was the word chat, so every project shared one. The feed is the same box the whole time: the events go, the content collapses, and the browser clamps the scroll to the top of what is left, which is a scroll event like any other and wrote that clamp over wherever somebody had really been. So the key is the place and the chat together, a key that has changed is a view standing somewhere else and restores again rather than only on a mount, and nothing is written down for a place until it has been restored into, or a project with no chats in it records a position it never had. Coming back pinned was always the bottom, since the bottom is worked out from the content rather than remembered; it was the reader who had scrolled up who came back to the top of a month of chat.
- The rail ends on the way to a crew you are not in yet, the way the toolbox ends its own row on the slot that opens the builder. It is one button rather than two, since opening a folder and joining with a link are one errand with two ways through it, and both rows wear the words the way in uses. Joining was on the way in and nowhere else before this, so a link somebody sent you meant leaving the project you were in to paste it. A join that does not happen is said on the card the link was typed on, since that is where the person is and the link is still in front of them.
- **A project is moved up the list by taking a copy of it rather than by pushing the row along.** What was picked up stays where it is and goes faint, a copy of it travels with the pointer, and a hairline with a dot at its head stands in the gap the drop would land in. Nothing in the list steps aside for it: the line is what says where it lands, and rows opening a slot as well is the same thing said twice. A tab in the side panel still carries itself, which is what a tab strip does everywhere, so `carry` on `useReorder` is what tells the two apart rather than the axis.
- **The pointer is what says where a carried one lands, and the box is what says it for one that travels.** A tab really moves with the pointer, so its own leading edge is what meets the one beside it and `landing` reads that edge. A project does not move at all, so there is no edge to read: the box left behind is as tall as the project and every thread under it, and its bottom edge stands a long way under the pointer, so the line lands a whole row before the cursor reaches the gap and the taller the project the further ahead it fires. `landingAt` is the pointer's own answer and it asks the plain question, which half of a row the cursor is really in.
- What is in hand is the row itself rather than a drawing of one, which is what `PlaceFace` is: the mark and the name, drawn once and worn in both places. It hugs what it holds rather than standing the width of the list, since it is a thing being carried and not a row that has left its slot.
- The line is a hairline and it is white. White is the single action color and a drop is the action, so it is white at full strength, and at two pixels across a column that narrow the same white reads as a rule cutting the list in half. The dot at its head is what gives it presence instead.
- What was picked up goes well under the opacity the rows around it rest at. A project that is not the one you are in already sits at 45, so a dim a little under that is a row that looks like every other quiet row, where what is left behind has to read as the hole the thing came out of.
- The gap between projects is the row's own rather than padding each one carries, or there is no gap for the line to stand in the middle of: `gapOf` reads it off the boxes the way `room` does, so nothing is told the number twice.
- The line stands in the list and what is in hand stands in the body. The list clips whatever runs past its own edge, and the line has to scroll with what it is pointing between, so neither one can be where the other is. Both are placed on the commit that mounts them rather than on the frame after, or the first thing anybody sees of a drag is the pill standing in the corner of the window on its way to the pointer.
- The list stops answering the pointer for the length of a drag and the cursor is the window's, so nothing lights up under a drag that is only passing over it.
- `tests/crews.test.ts` holds two windows in two projects in one process, `tests/sidebar-probe.test.ts` holds the hover, the press and the panel changing hands, `tests/sidebar-drag-probe.test.ts` holds the copy, the line and everything the drop takes off with it, and `tests/active-threads.test.ts` holds the fold.

## Where a crew lives

Opening a folder is the only way in, and there is no host and join to choose between any more. Every session is the same session: chat, agents, docs, boards, the toolbox, the music and the games, served over the same ws server. What changes is where the history is written and who can reach the socket, and those are two separate answers rather than one mode.

- A project is asked one question, the first time it is opened, and never again. In the project puts `.crew` in the folder, where it goes out with the repo and everyone who has the project gets the crew. In the Crew app puts the same `.crew` beside the app, so nothing is ever written into the project. Each answer is written from where the work is kept rather than from what the choice is not, so the pair reads as two places rather than as one place and its opposite, and both say the same three things, the chat, the docs and the designs, so the one word between them is where they live. `projectPlan` in `src/main/session.ts` is what the app asks before it opens anything, and a folder it already knows comes back with the answer it was given, so nothing is asked twice.
- `CrewHome` in `src/shared/project.ts` is that answer, and `Store` is handed a root rather than building one. Everything downstream hangs off `this.root`, so docs, boards, attachments and music follow without knowing which of the two they are in.
- A crew kept on this machine is keyed on the repo's first commit, never on where the folder happens to sit. `projectKey` reads `git rev-list --max-parents=0 HEAD`, so moving a project or cloning it again finds the same history, and a folder with no commit falls back to a hash of its path and is remembered from then on.
- Git runs when the project has git, because Push and Pull are somebody's own code and their own button. Only a crew that lives in the folder starts the loop, so a project kept on this machine is never committed to under anyone and no prompt waits on a sync. That is `auto` in `startHost`, and it is what `synced` says.
- A folder with no git in it is somewhere to work. It opens, on this machine, with nothing to sync. Nothing is turned away at the door: `listRepoFiles` already walks a folder by hand when git fails, and `GitSync.usable()` already stands down.
- Multiplayer never needed git. Whoever joins is handed the whole session in the welcome and everything after it comes over the wire, and attachments and music come over http from the host, so a crew that syncs nothing is still a crew. Git only moves code.
- Inviting people is the listener moving and nothing else. `setShared` closes the socket and opens it again on the same port, `0.0.0.0` instead of `127.0.0.1`, and the session, its code, its history and everyone in it stay exactly where they are. The only thing anybody sees is their own socket coming back a moment later, which it does on its own. Never remake the session to share it.
- Sharing is off for a crew kept on this machine and on for one that lives in the folder, and either way it is one item in the profile menu. Turning it on copies the link in the same breath, because those are one action and not two.
- The way in is a list of places rather than a form. Every project you have opened and every crew you have joined stand in one list, newest first, because picking one is the same choice whichever it is. `placesOf` in `src/renderer/src/views/home/place.ts` is that list and `PlaceRow` is the row, and where a project keeps its crew is the mark on the left rather than a word.
- Under the list are the only two ways to reach a new one: open a folder, or join with a link. Each screen the two of them lead to is a screen inside the same column, carried by `ScreenSwap` the way the music panel carries its own, so nothing opens a second box to aim at.
- The way back and who you are are the window's own corners rather than rows in the column, the way the top bar holds them once you are in: the back button on the left, past the traffic lights on a Mac with the same `mac:pl-[64px]` the top bar wears, and your face on the right. Both are `app-no-drag`, since the band they stand in is what the window is dragged by. Standing in the column they were rows the screen had to be laid out around, and every screen was pushed off centre by a chevron above it.
- Your name is asked once, on the way in, and after that it is the face in that corner. Nothing can be opened without one, so an empty name takes you to that screen rather than putting a line of red under the button.
- The one question a project is asked stands as two options side by side rather than as a column of rows, because it is a choice between two places and a column reads as a list. It is the only screen wider than the rest, which is `WIDTH` in `Home.tsx` beside `DEPTH`. Nothing stands under the two of them. A footnote there is a line about a choice already made on the cards, and one that is true of both answers made the two look different where they are the same.
- **You go by one name, and every place is opened under it.** A row still remembers what you were called there, and that used to be what it opened under, which meant the name screen was a dead end: a name set there was written over by the first row anybody pressed, and the crew you came back to had you as whoever you were the first time. Two Crews on one machine are two people only while each of them opens under its own name, so the host has one member a name and neither app can quietly take the other's. Renaming yourself in a session is the same act as setting it on the way in, so it is written down as the name you go by and the next place you open opens under it. The name is still handed to the join rather than read back off state, since state is a render behind, which is the same mistake the GIF picker's send guard is written to avoid.
- **A run from source keeps a profile of its own, and that is `app.setPath('userData', ...)` rather than a state folder beside the shipping one.** `runtimeStateDir` already put the saved session, the agents and the projects of a `yarn dev` run under `dev/`, and it was half the answer: the Chromium profile stayed where it was, so `yarn dev` and `yarn start` shared one `Local Storage`, and the name you go by lives in it. Two Electron processes on one profile is a leveldb lock, so whichever came second wrote a name that never landed, and both apps joined the same crew as the same person however many times either of them was renamed. The whole profile moves to the same folder Crew's own state was already in, so nothing is migrated and a run from source is a separate user with a separate name.
- Taking a place off the list is a right click on it. A button fading in over the arrow was a second control standing on top of a row that has one thing to press, and it cost the arrow that says where the row goes. A project and a crew somebody invited you to both go, by folder and by link, so no row on the list is one you are stuck with.

## A crew in a repo of its own

A project can be open to everyone and its crew not be. The code stays where it is and `.crew` moves into a repo only the crew can reach, so cloning gives a passer by the project and no chat. It is not a third answer to where a crew lives: it is a crew kept on this machine with a repo behind it.

- The address rides in the project, in `.crew.json`, one field and nothing else. No branch, no key and nobody's name, because anything more is something two machines can disagree about. It cannot live in the repo it points at, which is the whole reason it is in the project rather than in the crew.
- An address is not a secret. What decides who may read a crew is the repo it names, so anyone who clones is handed the way in and GitHub is the door. Knowing where a door is has never opened one, which is what makes this different from a key committed beside the thing it locks: everyone who cloned would have the ciphertext and the key together, and that is obfuscation rather than privacy.
- Every member commits and pushes, so every member is a replica and every member needs write. The repo's own list of people is the crew, GitHub administers it, and there is nothing for anybody to lose. A wrapped key per member would work and it would mean publishing a public key, waiting to be re-wrapped, and rotating the whole thing to remove somebody.
- `cleanCrewRemote` in `src/shared/project.ts` is the one place an address is decided, so the same rule holds wherever one is written down, read back or pushed to. The ssh forms are turned over to https, because an address is written for everybody else and a machine without that key is locked out of one that works fine for the rest of them.
- Not a submodule. A submodule pins a commit, and this one moves every five seconds.
- Opening a project that names a crew fetches it before anything opens, and a fetch that fails is said out loud. Nothing here ever falls back to a crew of this machine's own, because that is two histories under one name and it looks exactly like it worked. Starting one anyway is a press on the screen that says so, which is that answer picked on purpose rather than fallen into.
- Why it did not work is never guessed at. A repo that is not yours and one that is not there answer the same way on purpose, and a machine with no way out to the internet is a third nobody can tell from the other two, so all three read as a crew this computer cannot reach.
- A project that names a crew has already answered the one question a project is asked, so somebody who cloned it is never asked where to keep a crew they are about to be handed.
- Two loops run side by side, the crew's own and the project's own, and a prompt waits on whichever of them is running. Both are asked for by what is going right now rather than by what was going when the session opened, so turning one on mid-session is the whole of turning it on.
- Where a crew is kept and whether the code goes out with it are two answers rather than one. A crew that rides in the folder carries the code because it is the same repo. One kept outside it carries the code only where somebody said so, which is the switch on the People page, and it is off until it is asked for: a folder opened privately is still never committed to under anyone. The answer is the project's own and it is remembered, so it is asked once. Both ends have to have it on for work to travel both ways, since each machine only ever pushes and pulls for itself.
- `.crew/.gitattributes` is the union merge rule, written by the `Store` and standing inside the crew rather than at the top of a repo. That is what makes one rule cover a crew that rides in somebody's project and one that has a repo to itself. Nothing wrote it before this, so every crew living in a folder was taking a conflict on every crossing append and only the test helper was ever writing one.
- An empty repo is the only safe place to put a crew. Two crews pushed to one address is two histories that never met, and nothing here can merge that, so a remote with anything in it is refused and a push that fails takes the remote back off rather than leaving one pointing nowhere.
- A pass that gets nothing out is ordinary once. Three in a row is history piling up on one machine, and that reaches the screen as a toast rather than a line in a log nobody reads.
- Joining over a link needs none of it. Somebody who takes a link is a guest of a host and is handed the whole session over the wire, so two ways in stay two ways in: clone the project and be a replica, or take a link and be a guest.
- Moving a crew out of a folder does not unpublish anything. Every message already pushed is in that repo's history forever, and moving only decides where the next one goes. Taking the old ones back is a rewrite and a force push, not a setting.

## The crew command

`crew` in a terminal opens a crew on the folder you are standing in. It comes with the app, and one press on the This computer page in the settings puts it on PATH. `bin/crew.mjs` is the whole of what it does: plain node, no dependencies and no build step, so the one command works from a checkout and from the installed app alike.

- The folder is the one you are in unless you name another, and a relative one is worked out from where the command was run rather than from wherever the app happens to start. `--name` is who you are, `--in-project` and `--in-app` are the two answers to where the crew lives, `--share` and `--no-share` are who can reach it, and `--join <link>` joins somebody else's crew with this folder as the place your agents work. `--help` and `--version` answer for themselves and open nothing.
- Where a crew lives and who can reach it is the host's own, so `--join` is refused either of them rather than quietly ignoring them. Everything else is the same question the app asks the first time a project is opened and remembers from then on, which is why giving it again on a folder you have opened before changes nothing.
- What the command worked out rides in one flag, `--crew-open=` with the request as JSON, rather than as the row of flags the app would then have to learn the meaning of a second time. The command owns what its own words mean and the app only says whether it can use them: `cleanOpenRequest` in `src/shared/cli.ts` keeps the folder, the name, the two answers and the link and drops the rest, so nothing on a command line can reach further into the app than that. A plain node script cannot import the app's TypeScript, so the flag is the one thing written down twice, and `tests/crew-cli.test.ts` sends a real request through both halves and holds them to agreeing.
- A window launched for a folder opens that folder, so main leaves the last session where it is while a request is waiting. Resumed as well, the two would race and the one that lost would be the folder somebody just asked for.
- The renderer acts on it rather than main, because the name you go by lives in the window and the one question a project is asked is a screen in `Home`. `follow` walks the same path a press on a row does: no name goes to the name screen and comes back to the request when it has one, a project the app has never opened goes to the question, and a known one opens straight away. The request is handed over once and cleared, or leaving a session would land you back in the folder the terminal asked for.
- Which Crew it opens is `CREW_APP` if that is set, then the Crew it came with, then the installed app, then the one built in this checkout. Nothing is looked up on PATH, since the command is called `crew` too and a Linux install puts its own `crew` there.
- Every run is an app of its own, which is what lets the command open two folders as two crews. Nothing here takes a lock, so several windows on one folder is still supported.
- The command comes with the app. `extraResources` carries `bin` into `Resources/cli`, so what ships is the file in the repo and nothing about it is generated at build time. The version is read out of the app's own archive, because the command stands beside it and the thing running the command is the app, which reads into its own asar. Never copy `package.json` in beside it to save that lookup: electron-builder takes a file named in `extraResources` out of the archive it would otherwise be packed into, and a build whose asar has no `package.json` fails its own sanity check. Only a real `electron-builder --dir` says so, and every suite passes either way.
- Somebody who downloads Crew has no node, so `bin/crew` is what ships on PATH rather than `bin/crew.mjs`: a shell script that follows its own link home, finds the app it came with, and runs that app as node with `ELECTRON_RUN_AS_NODE`. Electron is a node, which is the whole reason this needs nothing installed. It sets `CREW_APP` to the app it came out of unless one is already set, so a command from `~/Applications` opens that Crew rather than whichever one is found first.
- That variable rides in the environment, so `childEnv` in `bin/crew.mjs` takes it off before the app is spawned. Passed on, the Crew being opened comes up as a node process with no window and nobody ever sees it.
- Putting it on PATH is one press, on the This computer page in the settings. It is a link into `/usr/local/bin`, made by the app where it may and asked for with a password where it may not: `osascript` on a Mac, `pkexec` elsewhere. A password dialog somebody dismissed is a decision rather than a failure, so nothing is said about it. The row turning over is what says it worked, so nothing else does either.
- The rules are in `src/shared/crewCommand.ts` and the doing is in `src/main/crew-command.ts`, because the rail in the settings reads the first one and nothing in the renderer may reach for node. That is also why the paths there are joined by hand.
- Windows is the one place it does not go. There is no folder every shell already reads, so the file ships and `commandScript` comes back null, which main answers as `off` and the row is left out. Nothing in the renderer decides this: it asks the machine and draws whatever it is told.
- It still reaches PATH from a checkout, with `npm link` or a symlink of `bin/crew.mjs`, and the `bin` entry in `package.json` is what npm reads. A build run from source links that same file, so `yarn start` has the command the shipping app has.
- `tests/crew-command.test.ts` runs the real launcher, through a real link, against a stand-in app that says what it was handed. A fake cannot stand in for a shell script, and every unit around it passes whether or not the thing ever finds the app.

## Commands

A word with a slash in front of it, lifted out of the box onto a chip beside the plus and sent as a field of its own. `commands.ts` is the one table of them, and `where` on each row says which composer offers it: the chat's open a thread, and a thread's are about the thread already open.

- Neither set is offered where the other belongs. `commandsIn` is what a composer asks for, and both `commandTyped` and `slashCandidates` are handed that list rather than reaching for the table themselves, so the menu and the word typed out can never disagree about what there is.
- A thread holds one at a time, because they are one choice about the one message being written. Picking another takes the place of the one before it, and what stands is read back off what the thread can still do rather than off what was picked, since the run it was picked against can end while the message is still being typed.
- `/steer` and `/queue` are only there while there is a turn to go into. Without one every message queues and runs, so offering either would promise a choice that was never there, and the pair is left out rather than greyed. `live` on the row is what says so, so what needs a turn is a word on the command rather than a list of names written out again in the filter.
- `/steer` is the default said out loud, since a message for the agent already running goes into that run whenever it can take one. `/queue` is the override, and it is the only way there is to hold a message back until the turn ends.
- `/btw` is a conversation beside a thread rather than in it. It opens in the panel, reads the thread for whatever the question needs, and says nothing back into it, so the work going on there is not interrupted and nobody scrolls past a question they never asked.
- One is a ghost, so it runs on your own machine and nowhere else, none of it is written down, and it goes with the window. A thread somebody else's agent is on is still one you can ask about: one of yours answers it, which is `startAside`.
- `aside` on the thread is the thread it was asked from, and `buildPrompt` reads that thread and then the talk beside it. They stand as two blocks rather than as one run of lines: a question and its answer are turns of their own, and after a few of them there is nothing to say where the thread ended and the conversation about it began. The thread is read again on every turn rather than remembered, so an answer keeps up with work that has carried on since. It answers itself and sends no work out, because helpers on a thread nobody can see is work nobody asked for.
- It is not the crew's work, so it is no card in the chat, no row in the task list, and no toast when it lands: the panel that opened to answer it is already on the screen.
- It is two way. `AsideView` is an ordinary thread in a panel, composer and all, so the next question goes where the last one was answered: an answer is rarely the end of it, and starting again in a tab of its own is being told to explain the whole thing a second time. Nothing typed there can reach the thread it is about, whatever is written in it, since it is a message into a ghost of its own and the thread is only ever read.
- A question asked from the thread's own composer opens a tab, so one asked while an answer is still being read leaves that answer where it is. Two of those are two conversations, and each one goes on where it stands.
- `/fork` is the thread so far, carried on in one of its own. What was said up to that moment is what the agent reads, and the thread it came from is left exactly as it was, so a good place can be gone on from several ways at once and none of them stands on the others. Nothing is ever said back into it.
- Nothing about a fork is copied. `forkedFrom` and `forkedAt` on `thread.started` are the whole of it, and `forkContext` folds the talk back out of the events up to that moment, so it replays for free, costs the log nothing, and a fork of a fork reads the whole line it came down and none of what any of those threads went on to do afterwards.
- It reads as one run of talk rather than as two blocks, which is the opposite of what an aside does and for the opposite reason. An aside is a conversation about a thread, and a fork is that conversation carrying on, so nothing in the prompt says a fork was made at all.
- It carries the mode, the plan and the board with it for the same reason. A fork of a ghost thread is a ghost, or a hidden thread leaks the moment somebody forks it, and a fork of a helper's thread is a thread of its own rather than a second helper, since nobody sent it out.
- Whoever asks for a fork names the id it opens under, in `forkId` on `chat.send`, the way whoever makes a playlist names that. The window that asked opens it the moment it lands, since a message was just typed into it, and no other window moves: everyone else has the card in the chat, the way they do for any thread.
- A fork reads as a thread that starts mid conversation, since what came before it is in the prompt and not on the screen, so `ForkedFrom` stands at the head of it and names the thread it carried on from. That is the way back as well as the one thing the screen cannot say on its own: whoever made it knows, and everybody else opened a card that answers a conversation they cannot see.
- `/fallback` names who takes over when a run in this thread ends badly. A CLI that has run out fails the run and says so, so an agent that failed and an agent that is out of tokens arrive at the same place, `agent.end` with `ok: false`, and there is no second path to build. Nothing here reads the usage bars, which are a guess about the next run rather than an answer about this one.
- Nothing reads it until a run falls over, so a thread with one named queues and steers exactly as it did before. Setting it is a send of its own, answered and returned before anything is enqueued, so it never competes for the queue with the message written after it.
- A run somebody stopped never falls over. `stopped` is already how the host tells a decision from a fault, and reading a decision as a fault hands the work straight back to whoever just called it off.
- One hop per message. The retry carries `fellBack`, so a fallback that fails too is the end of it rather than a pair of agents handing one message back and forth, which is the rule the toolbox chain already holds.
- The thread moves with it, so the next message goes to the agent that is now on it. That is `thread.agent` with no name on it, which is why `byName` there is optional: a hand-off nobody made has nobody to name, and the transcript draws that case as its own line rather than as somebody handing the thread on.
- A fallback nobody is running takes nothing. Pushed onto a thread whose agent is not here, the message would sit at the head of the queue holding up everything behind it, and the failure is already on the screen saying what happened.
- It is named by mentioning them, which is the one way the app already names an agent in a composer, and naming nobody takes it off. That one send is an empty box, so it is the single exception to nothing typed being nothing sent.
- Nothing is emitted into the thread as a message when one is set, because an arrangement is not something somebody said. The note in the transcript is the record and the only place a standing fallback is visible, which is right: it matters on the day something breaks, and a permanent row in the header would be chrome earning nothing on all the others.
- Messages already queued behind the failed one still name the agent that failed, so each falls over once before moving. That is self-correcting and costs a run each. Moving the whole queue would overrule somebody who named an agent by hand.

## Helpers

An agent splits work off to a helper and carries on with its own. It makes one up as it needs it, a short name and the whole of what the piece is, so the shape of the work decides how it is divided rather than a list somebody wrote last week. `/agents/spawn` on the host's own http is the whole of the way in, and every provider gets it at once because it is a curl line in the preamble rather than a flag on a CLI.

- A run of one is an ordinary thread with a parent. That is what gives it steps, thinking, diffs, tokens, cancel, steer and a place in the log without any of it being invented again, and it is what lets you open one in the side panel and type into it.
- The parent never blocks. It sends out, gets an id back, and keeps working. When a helper finishes the host pushes the answer into whatever the parent is doing: into the live turn through `sendSteer` where the agent is steerable, and as a turn of its own where it is not. `subagentReturn` in `src/server/session.ts` is that one decision.
- The handoff is not a chat message. `silent` rides on the steer and on the queued prompt, so no `message.route` is written pointing at nothing and nobody scrolls past a helper's raw answer pasted in as though it had been said. The chip is the record, and the chip opens onto the whole thing.
- Returns coalesce over `RETURN_COALESCE_MS`. Three helpers finishing in a breath are one interruption, because three steers into a live turn is three times the model is pulled off what it was doing. One line each under one heading, and `returnText` writes the whole of it on the host, since a breath of returns has no single name to frame it under.
- `subagent.ended` carries `ok` and `ms` and no text. The answer is already in the log as that thread's own `agent.end`, and writing it twice is a log that doubles for nothing.
- What the parent says and what the screen says are two different reports. The parent's line is intent, one line about who it sent where, and it streams as it is written. The chips are truth: each reads its own thread's state and its own latest step, so the row reports whether or not the agent ever mentions it. The line has to say what the screen cannot.
- Only `RunStatus` spins. A working chip lights its mark and pulses it, the way a live step does, or four helpers under one run is a field of spinners.
- A thread with helpers still out is not ready for review. `threadWorking` counts its children, or a parent whose own turn ended lands on the review badge and raises a finished toast while three agents are still going on its behalf.
- A helper is nobody's row in the feed and nobody's task in the panel. It reads inside the thread that sent it, which is where somebody would go looking for it.
- Its two events are the one pair that do not belong to the thread they name. `subagent.started` and `subagent.ended` carry the helper's own thread and read on `parentThreadId`, so `eventsOfThread` is what every view gathers a thread's events with rather than a filter written out again at each one. Read the plain way, the chip lands inside the helper pointing at itself and never lands in the thread that has something to say about it, which is a spawn that runs, returns, and leaves no mark on screen anywhere. `tests/subagent-chip-probe.test.ts` holds it.
- The mark is a picture, seeded by the run's own id, drawn the way the music covers are: the same shader, the same palette, seen through a shape of its own. `subagentShape.ts` holds the numbers and `SubagentMark.tsx` draws, the same split the covers hold. A lobed mark is a pointy form and takes the live area the way the star does; a disc and a hexagon sit on the circle keyline and a square on the square, so a flower and a disc carry the same weight in a row. `MIN_WAIST` is where a rosette stops being one shape and starts reading as scattered dots at 18 across. `yarn covers` draws every mark at the three sizes against both surfaces, because a generator like this cannot be judged from its numbers.
- Inside a helper's own thread the work is the helper's, so it stands under the name the agent made it up with and the mark drawn from its run's id, rather than under the machine that happened to run it. That is `as` on `buildThread`, and what it hands each of the agent's own items is a name and a seed and no agent id at all: nothing looks a helper up as a member or as an agent, so the hover card and the presence dot quietly stand down rather than naming somebody who is not there. What a person typed into the thread is still theirs. Read the plain way, every helper in the panel is a row of the same pet under the same name, and the one thing the screen is there to say, which of them you are reading, is the one thing it does not.
- The panel says a helper once. The way back and what it is doing stand on one row, and nothing under it repeats them: a mark, a name and a state over a transcript that already carries the words, the run status and the files is the same thing told three times down a 480 panel. The tab pill carries the name, `ScreenSwap` carries the row, and the list underneath is one line a helper.
- A thread's helpers are one tab, and everything opens it: a chip, the row under the chips, and the button in the thread's own header. `parentThreadId` on the tab is what makes that one thing rather than three, so `openSubagent` is handed the thread that sent the helper as well as the helper. Opening a second one takes the place of the first and the list is one press back, rather than a row of near identical pills down the top of the panel, and it is what lets the header button say whether the helpers are the thing being shown.
- A helper's own helpers stand in that same list rather than in a list of their own. A helper can send one out itself, and what it sent is still work the thread in the chat sent out, so `useSubagentRuns` walks the whole family and the list is the one place all of it is. Which helper sent which is the order and the indent, each one standing under the one that sent it, so the list says it without a word: read as a flat row of subjects, three helpers deep is three lines that could have come from anywhere. A tab is keyed by the thread in the chat and never by the helper a chip happens to stand in, which is `rootThread` in front of `openSubagent`. Without it a chip inside a helper opened a second helpers tab, keyed to a thread nobody is in, so `leaveThread` took it down on the next move and the way back out of it was a list that had never held it.
- That tab stands only while you are in the thread that sent them, the way the plan and the board do, so what is left in the row is about the thread on the screen. `leaveThread` is that rule and it runs off `openThreadId` rather than off the thread's own things, since a helper is opened by hand and never opens itself, so nothing else would ever take one down. Every way back is a press: a chip, the row under the chips, the button in the header, or the panel's own list.
- The chips arrive in the words they were sent under and scroll away with them, so a thread that has ever sent a helper out keeps a way in that stands still: a row in the side panel's own list, standing only for a thread that has sent one, wearing `GroupGlyph`, the mark a helper carries everywhere else in the panel.
- The panel never travels sideways. It is half the width the thread column is, so what stands past its edge is slack rather than something to go and read, and a bar under the words that scrolls to nothing is a control that does nothing. Anything in a transcript that really has more to the right owns a scroller of its own, a code fence, a diff, a terminal card and a table, so clipping the column costs none of it. The aside holds the same rule for the same reason.
- Every route names the promptId of the run asking, and the host takes it only while that prompt is one it is running right now. A board being drawn on is a different weight of thing from a shell running on somebody's laptop, so the live prompt is the credential: it costs one lookup and nothing has to be handed around. A helper is reachable only from the thread that sent it, however its id was come by.
- A CLI asked for by name is asked for. Nobody running it is a refusal rather than a quiet fall back to the parent's own, which would read as the request being ignored.
- What helpers may do on a machine belongs to whoever owns it, because a helper runs a real CLI there and spends their tokens on their files, which is why it stands on This computer rather than on the Crew side of the settings. Off means their agents never send work out and never take anybody else's. It lives in that window's `localStorage` and is said again on every connect, the way the volume is kept except this one has to reach the host to be worth anything. `FAN_LIMIT` is the crew's ceiling and `DEFAULT_FAN` is what a machine carries before anybody says otherwise.
- A helper inherits `ghost` from its parent without exception. A hidden thread whose children are not hidden is a hidden thread that leaks.
- Stopping a run stops the helpers it sent out, and a helper somebody stopped wakes nobody: its work was called off, so waking the parent would open a turn on a run that was just cancelled.
- `WAKE_LIMIT` sits beside `RUN_LIMIT` and `DEPTH_LIMIT`. Without it a parent that spawns on every wake is a loop the crew watches burn tokens all afternoon.
- A helper makes no sound at all, going out or coming home. It was a quiet pair of its own once, `helper.out` and `helper.home`, well back in the room and landing nowhere near the note a finish lands on, and being quiet was not enough: a crew of them out and back through an afternoon is a noise every few minutes about work nobody asked to hear about, over the top of the thread that sent them. The chips are on the screen already saying it, and they say it better, since each one reports its own state whether or not the agent ever mentions it. The helper's own `agent.end` is silent too, and that is a rule of its own in `soundFor` rather than something that falls out of the rest: read the plain way, a helper that stopped chimes in the voice that means the work somebody asked for is done.
- The one thing nothing here can merge is two agents editing one file at the same time. Half of that is words, in the preamble, which says to send out work that does not sit on what you are holding. The other half is the panel, which lists the files each helper changed.

## Showing a page

An agent puts something on the screen beside the chat: the file it has just written, the three it changed, the server it has started, or a site worth looking at. It is the browser and the file view the panel already holds, asked for from the outside, so nothing new is drawn and nothing is installed.

- `/page` on the host's own http is the whole of the way in, and `pagePreamble` in `src/shared/showPage.ts` is what an agent is told. It is written on the machine running the agent, the same reason the helpers and the board write theirs there: only that side knows the address it reaches the session at. Every call names the promptId of the run asking, which is the credential the board and the helpers are reached on.
- Nothing about it is state the host holds. The call is one `page.shown` on the thread, so it replays for free, and a ghost thread's page reaches the one window that opened it without any of that being said again here.
- It opens for whoever is reading that thread and nowhere else. Anyone else has the row in the thread, and a panel taken over by work somebody is not watching is the one way this becomes a thing people turn off.
- A file opens in the file view and only an address opens in the browser. `openShown` is that fork, off `filePathOf`, and it is what makes this any file rather than an html one: a stylesheet is lit, a picture is drawn, a log is read. Opened as a raw `file://` tab, everything that is not a page came out as text nobody could read, and half of them downloaded instead.
- A file is read the way it is written to be read. An html one and a markdown one open as the page they are, with the words they are written in one press away, and everything else opens as itself. That is `showFile` in `state/browser.ts` rather than the `openFile` a file link in a step uses, which lands on the text: going to read what changed and being handed the finished thing are two different errands, and the whole of what was on the screen before this was the html an agent showed, drawn as a page. A tab already standing on the file reads it again, since what is being shown is almost always what the run has just written.
- The same file arrives from another machine under that machine's own path, so a file is placed before it is opened, through the `locate` the whole app already reads paths with. That is what turns it into a path in this project, and one that lands nowhere here is left alone rather than opened onto nothing: the row says it is somebody else's file, which is the one thing it can honestly say.
- One call may name several, up to `PAGE_LIMIT`, and they all open in the order they were named, with the first of them standing at the front. Opened from the back instead, the row of tabs reads backwards, and the row somebody put a thing in is the order they put it in. A call naming one bad address is refused whole rather than opening the rest, or a model watches for the file that quietly never came.
- The row is the way back to it once the run has scrolled away, so it carries the addresses rather than tabs that may have been closed since. Pressing it loads the page again rather than standing at what it looked like before the change being shown, which is `showPage` in `state/browser.ts` and the reload `BrowserTabView` does off its own `generation`. An address the webview has tidied is the same address, or every showing is a second tab.
- It is a step and reads as one: the same column, the same word, the same rhythm as everything else the run did. `PageRow` is drawn from `StepRow`'s own parts for that reason, and several fold into one row that opens the way a run of edits does. It was a pill before this, which is what a helper wears, so a run that showed a file read as a run that sent an agent out.
- It closes up against the steps either side of it, which is `promptId` on `page.shown` and the page counting as a step in `follows`. Left out of that rule it sat a whole gap clear of its neighbours, and a row standing alone in a run reads as something that happened beside the work rather than as part of it. Which run it belongs to is never left to the field alone, since no page shown before that field existed carries one and every row already written down would stand apart forever. `runOfPages` reads it off the log instead: the call is made from inside a turn, so the run open on the thread when the event landed is the run that made it, which is the order the log is written in rather than a guess at it. A page with no run open when it landed is the one that keeps the wider gap, and it has earned it.
- One mark, whatever it put on the screen. It was a page, a stack of pages or a globe depending on what was named, which is the row saying in a second language what the words beside it already say: the mark is the tile and the words are the file.
- Three ways to name one and no others, in `pageUrl`: a real address, a machine it is already serving on like `localhost:5173`, and a full path to a file. A person typing in the address bar gets the benefit of the doubt and lands on a search; an agent gets none of it, because what it writes is opened on every machine in the crew without anybody pressing anything. Anything else is refused in words, so a model reads what to write instead of watching a page it named quietly not appear.
- The title is the agent's own line about what to look at. One page it said nothing about is named by its file or its host, which is the shortest true thing either one can be called. Several are named by the count, since one file's name is not a line about three.

## Ghost threads

`/ghost` in the chat opens a thread that belongs to the window it was typed in. Nobody else sees it, none of it is written down, and it is gone when the window is. Everything else about it is an ordinary thread: an agent takes it, it queues and it steers, and the work it does to the project is real work on real files.

- It is held in memory on the host, keyed by the socket that opened it, the way a call is. Two windows on one folder are one member and two different people here, so the other one is not shown it either.
- Every event a thread raises carries its id, and that is what lets one place decide all of this: `emit` hands a ghost's event to that one socket and puts it on the thread's own transcript instead of on the log. The three thread-scoped messages that are not events, the steps, the tokens and the queue, go through `toThread` for the same reason. Never broadcast one of those by hand.
- That transcript is what the agent's next turn is built from. `threadContext` reads it rather than the session, so the thread has a memory without the session having one.
- A run in flight rides in the snapshot as well as in the log, so `pooled` leaves a ghost's run out of it. Anyone connecting mid-run would otherwise be handed its steps.
- A thread somebody else opened is not there to be written in, however its id was come by. `hiddenFrom` is that guard, on the chat, on the status and on the plan. Reading one is the same rule: a reply and a reaction have to find what they point at, and that was never written to the session, so `reactionTarget` searches the transcripts the asker can see and no others. Handed every transcript there is, a reply is a way to read a line out of somebody else's thread by naming what it points at.
- Busy is the one thing about a run that reaches everyone, so `statusOf` does not count a ghost's. An agent reading as busy with no thread anywhere to point at says a hidden one is going.
- A picture sent to one is held in memory on the host and answered from there, never saved beside the session. The machine running it has to put one on disk for the agent to read at all, so the prompt says it is a ghost's and the runner writes it outside the project and takes it away with the run. Without both halves the one part of a ghost thread the crew syncs is its pictures.
- The window going takes the thread with it: whatever it was running is stopped, its pictures are dropped and the transcript with them. The entry it stood in stays behind empty, because a run still coming back from a machine somewhere has to land somewhere sealed, or its last steps would be written down as an ordinary thread's.
- It only ever goes to an agent on your own machine. A prompt reaches whatever machine runs the agent and the CLI there keeps its own record of it, so anywhere else the thread is hidden in the app and sitting in somebody else's folder. Naming somebody else's agent says so, to the one who asked and nobody else, and inside a thread it reads the way naming an agent who is not here reads.
- The agent still runs a CLI, so what that CLI keeps for itself on your own machine is out of the app rather than out of the world.
- It wears a dashed stroke wherever it is drawn. On the card in the chat it wears the Ghost chip, beside the Plan chip if it is also a plan, and the dashed header stays over the composer inside the thread.
- Inside the thread it says Ghost mode on the head of the composer, which is `GhostBar`: an `ink-800` band standing behind the header with the mark and the words centered on it in white, tucked the way the queue above it is so only its head shows. It is nothing to press and nothing to hover, and it says what is hidden on the box it is typed in rather than over the thread.
- Arming ghost turns the composer over before a word is written: the box drops to the page's own surface and takes the dashed stroke, so what is about to be hidden looks it. It wears the same thing inside the thread, under the dashed header. The border stands in both states so nothing shifts when it goes on, and the patch the composer paints behind an emoji reads the same surface, or a solid grey square stands in the field.
- A command is not text. Typing `/ghost` and a space lifts it out of the box onto a chip beside the plus, and it is sent as a field of its own, so the same word written inside a sentence is only a word and nothing is ever cut out of what somebody wrote. `commands.ts` is the whole of what a command may be and `CommandChip` is what one wears, in the composer and on the card. The host reads the field and never the message. Inside a thread the field is ignored, the way the text was.
- The chip in the composer stands in the row the plus stands in and is that button's height and outline, because it is a control rather than a label. Backspace on an empty box takes the last one off.
- The plus offers no huddle while ghost is armed. A call is the whole crew by definition, so a way into one standing under a message nobody else will see is the one thing in that menu that contradicts the box it is in.

## Syncing

Every machine commits its whole working tree, integrates, and pushes on a loop, host and joiner alike. Agents on different machines write to the same branch at the same time, so `GitSync` in `src/server/git.ts` has three hard rules. Each one is here because work was destroyed without it.

- Never stash. Commit everything first, so nothing is sitting uncommitted when the sync integrates. Autostash stranded people's work in stashes nobody ever went back for.
- Never rebase on the automatic path. A rebase replays every local commit and rewrites the working tree once per commit, landing on top of files an agent is writing right now. Pull with `--no-rebase`. A merge leaves local commits alone and touches only what came in.
- Never revert. Taking back someone's edits defeats the point of pooling agents. When a conflict cannot resolve itself, back out of it, put the pending files back, and try again on the next pass.

Only `.crew/session.json` resolves itself, by keeping the local copy. Logs ending in `.jsonl` union merge through `.gitattributes`. Several windows can share one folder, so a lock file in `.git` keeps them off each other, and an interrupted merge or rebase is finished or backed out at the start of every pass.

The chat log is written in segments, and that is a memory rule rather than a tidiness one. It used to be one `.crew/chat.jsonl` that every commit rewrote whole, so a 23MB file went into the log again every thirteen seconds and the history ended up holding eleven thousand copies of it. Nothing about that is expensive until git repacks, and then it is: a repack redoes the delta search across every one of those copies, with a window that is unbounded by default and a thread per core, which on a laptop is tens of gigabytes and a machine that dies rather than a slow pass. `chatLog.ts` seals a segment at a megabyte and opens the next, so a commit only ever writes the tail. `.crew/chat.jsonl` is still read and is never written again, which is what lets a session that predates this keep its history without a migration. Order comes back from each event's own `ts` rather than from the order lines happen to sit in, since a union merge across two machines interleaves them anyway, and an identical line from that merge is dropped rather than replayed twice.

A prompt waits for a sync before it starts, so the queue in front of it is what decides how long it takes an agent to say its first word. Nothing may put a pass on that queue on a clock. The loop arms the next pass once the last one has settled, and a request that arrives while a pass is already waiting is given that pass rather than one of its own, so the queue is never more than one running and one waiting. A pass on a busy folder takes longer than the interval between them, and a queue fed on a timer only ever gets longer: agents sat on "Starting" for seven minutes while the passes ahead of them drained.

## Updating

A new Crew is out and the app says so, on one pill in the top bar. It stands only while there is one, and pressing it is the whole update: the new Crew comes down and the app comes back on the other side. The releases on GitHub are the feed, and `electron-updater` is what reads them.

- One press and no second one. Pressing starts it, the pill fills as it comes, and the app relaunches itself the moment it lands. A download that finished and then waited to be let in is a second thing to press for something already decided, and the press was for the update rather than for the half of it that fetches. Whoever pressed it wanted the new Crew, so they get it.
- A restart never installs anything, and nothing installs itself on the way out. `autoInstallOnAppQuit` is off, and it was the wrong thing to lean on twice over: it is only read at the moment a download completes, and its handler returns early once `quitAndInstall` has been called at all, so with the press installing at once the flag was already dead. Worse, it would install at a quit, which is exactly when nobody is watching and other Crews may be live. Crew decides when the app may be replaced instead. Nothing is lost by that, since the download stays on the disk: the press after a restart lands straight on ready and installs in a moment.
- The install waits for every other Crew on the machine. `crew` opens one Crew per folder, so several are ordinary and none of them can see the others through electron, and the Windows installer force-kills every one it finds: `--updated` is precisely what skips the "app is running" prompt and jumps to `taskkill /im`, after 1300ms of grace. There is no runtime option off that, only a custom NSIS macro at build time, so pressing update in one window used to force-kill the others mid-run, which is the one thing the rules for agents say never to do. `mayInstall` in `src/shared/update.ts` is the rule and `OtherInstances` in `src/main/instances.ts` is the count, kept as pid files in `userData/live` the way the sync keeps a lock file, since that is the only thing every platform can be asked the same way. A folder that cannot be read is nobody rather than somebody, or an unreadable folder would hold every update up forever.
- Only an installed Crew marks itself, because only an installed Crew is one the installer replaces. A run from source stands nowhere the installer will reach, and marking one meant every machine with a checkout open held its own updates: `yarn dev` is running on those machines the whole time, so the pill was pressed, the install was refused by a process nobody thought of as a Crew, and the app sat there. `shipping` in `src/main/index.ts` is that one answer and it is the same `fromSource` the icon, the dev tools and the updater read.
- A mark older than the machine coming up is nobody. A pid is handed out again after a reboot, so a Crew that died without tidying up leaves a number that something else is wearing by the time anybody reads it, and the file answers as a live Crew from then on with nothing anywhere to say why the updates stopped. `outlivedBoot` in `src/shared/instances.ts` is the rule, read against `os.uptime()`. The margin is which way to be wrong: reading a live Crew as gone is what lets an installer take its work down, so a mark has to be clearly older than the boot rather than a moment older, and a clock that moved under us is not enough to make one stale.
- A Crew that has not been marked cannot be counted, so the first release carrying any of this is the one update where another Crew still running an older build is not seen and is killed the way it always was. Every one after it is covered. Nothing can be done about that from here, since the count is written by the app that is running.
- Held, it says so on the press rather than on the pill. The pill is still standing there offering the same thing, so what says why is a toast, the way the server's own `notice` says why a command did not happen. That means the stage stands too: an install that did not happen leaves it on ready and writes only the reason, because the bytes are here and the restart is the one thing left to do. Standing back down to a stage that says an update is available read as the whole download coming undone under the press. It is landing that asks for the install rather than being landed, or a hold read off the stage alone would ask again for the thing just refused, and answer itself, forever. Being held a second time is worth saying a second time, which is why `told` is part of the state: keyed alone, the same word said twice would be written once and a second press would read as a button that does nothing.
- Whatever quitting puts down is put down before the app is replaced under it, and once. `settle` in `src/main/index.ts` is that one shutdown, shared by the quit and by the install, so nothing is shut down twice when `quitAndInstall` quits the app itself. `GitSync.stop` only stands the loop down, so `quiet` is what waits out a pass that is already committing and pushing: without it the Windows installer's `taskkill` arrives about 1300ms later and cuts a commit and push in half.
- An install that did not happen says nothing for itself, on either platform, so the app is what says it. The Windows installer is spawned detached with its exit code never read, and there is no `exit` listener anywhere in the package, so a silent installer that took the cancel default is unobservable. On macOS `quitAndInstall` does nothing at all when Squirrel never staged the update: no quit, no error, no timeout. What says it did not happen is the app still standing here 30 seconds later, and a Squirrel refusal at verify time is heard sooner than that, because the constructor re-emits every native error and one arriving while an install is going is that install failing. Landed was terminal before this, so the pill sat on the offer of a restart forever and nothing anywhere said why.
- `autoUpdater.logger` is a real file, `updates.log` in `userData`. The default logger is `console`, which a packaged app writes to nowhere, so every update failure out there left no trace at all. Three methods are required and `debug` is optional. It is truncated past half a megabyte and a log that cannot be written is never worth taking an update down for.
- The check keeps going while the pill stands. It stopped for good once anything was found, so a window left open for a week never heard about the release after that one. `worthChecking` is the rule: only a download in flight and one already down are left alone, since neither has anything to learn.
- The pill is the whole of the way in. There is no row in the settings and no item in a menu, because either would be a second way to do a thing that is already one press away, and neither could say the one thing that matters, which is that there is something to press at all.
- It is solid white, the way every other action in the app is. White is the single action color and this is the one thing in that row worth pressing, so a quiet pill in the same `bg-fg/[0.06]` as the chrome around it was a button nobody saw: it read as another piece of furniture, and it is on screen for the few days between a release and somebody taking it. It is the only white thing in the top bar, which is what makes it the whole of the announcement without a badge, a dot or a color the app does not otherwise wear.
- While the new Crew is coming down the pill is not a button, so it drops to the quiet track and the white is what travels across it. The word is written twice, once on each side of the fill, and the two copies stand at the same size in the same place with the top one clipped to the fill: dark on the white that has passed and light on the track it has not reached. One copy in either color vanishes for half the download, and a percent beside the word is the fill saying the same thing again in figures.
- What the pill can say lives in `src/shared/update.ts` as a plain move from one stage to the next, and `src/main/updates.ts` is the thin part that hands `electron-updater`'s words to it. That split is the only way any of it is tested: main imports electron and a suite cannot, so `tests/update.test.ts` drives every stage by hand without a release, a network or an app anywhere near it. `tests/update-pill-probe.test.ts` is the other half, and it renders the pill through the real store at every stage and reads the words it says.
- The three ways it does not happen are three different sentences, so `why` rides in the state and the words are picked off it where they are drawn. A download that did not arrive and an update that would not go on are the app's own failures and wear the warning mark. The other Crews are neither: that one is an instruction, so it goes in the plain tone.
- `autoUpdater` is never imported by name. Main is bundled as ESM and `electron-updater` is a CommonJS module that hands that one export over as a getter on its exports rather than as a plain assignment, so the lexer node reads a CommonJS module's names with cannot see it: every other export of that package imports by name, and this one throws on the way in and takes the whole app with it before a window is ever made. The module comes in whole and the getter is read off it. Reading it is what builds the platform updater, so it is read once, behind the same gate that says the run is packaged, and a run from source never builds one at all. Nothing about this is visible in a suite, because a suite cannot import main, and `tsc` reads the package's types where the export is a plain one, so both were green while the built app would not start. The build is what says: `yarn build` and open it.
- A check that could not reach the internet says nothing at all. Only a download somebody asked for and did not get is worth a word, and that word is a toast rather than the pill, because the pill is still standing there offering the same thing again. Read the plain way, every laptop that closed its lid on a wifi network would wake to a failure it never asked for.
- Nothing is checked from a run out of source. There is no release behind one and no signature to hold it to, so `fromSource` is the same gate the icon uses. It stops looking once it has found one, and looks again every hour until then.
- macOS will not install an update to an app that is not signed, and nothing about a build says so on the way past. An unsigned run signs nothing, notarizes nothing, warns about neither, and produces a dmg, a zip and a whole `latest-mac.yml`, so the workflow goes green and ships a release every Mac quietly refuses. `forceCodeSigning` in `build.mac` is what turns that into a build that fails instead.
- Signing is held by the build and notarizing is not, so the workflow is what holds it. electron-builder skips notarization with a warning when the Apple credentials are not in the environment, so a build missing one secret is signed, unnotarized and green, and every Mac that opens it is told Crew is damaged. The step after the build reads the app back instead: `codesign` has to name a Developer ID Application authority, and `xcrun stapler validate` has to find a ticket on the bundle. Both are read off what was built rather than off what was passed in, which is the only thing that says the notarizing really happened.
- The credentials are five repo secrets and nothing else. `CSC_LINK` is a Developer ID Application certificate exported as a base64 `.p12`, `CSC_KEY_PASSWORD` is what it was exported under, and `APPLE_ID`, `APPLE_APP_SPECIFIC_PASSWORD` and `APPLE_TEAM_ID` are what notarytool is handed. Developer ID Application is the one certificate that works here: Apple Development runs a build on your own machine and Apple Distribution goes to the App Store, and neither of them can be notarized for anybody else to open.
- Hardened runtime is enforced the moment the app is really signed, and it is what makes the entitlements load bearing. Ad hoc signed nobody checks any of them, so the app that ran here for months carried none and worked. Scribe reads the caret by sending an Apple Event to System Events, which macOS refuses a hardened app unless it carries `com.apple.security.automation.apple-events` and an `NSAppleEventsUsageDescription` in the Info.plist. Without the pair, dictation reads as "click into a text box" over an open composer, in a build that passed every check. The microphone and the camera are the same shape of rule, and `disable-library-validation` is what lets the three native modules load at all.
- What the Mac remembers about Crew is keyed on the signature, so it is a different app to macOS the first time it is really signed. Accessibility, the microphone, the camera and screen recording are all asked for again, and the stale row in System Settings has to come out before the new one takes: left there, the switch reads as on and the key hook hears nothing. Nothing in the app can do this, and nothing about it is a bug.
- macOS updates from a zip and never from the dmg, so `build.mac.target` carries both. The dmg is what a person downloads once and the zip is what every update after it rides on.
- What ships is `dependencies` and nothing else, which is why there are five of them. electron-builder packs the whole production closure into the asar whatever `files` says, and the renderer is bundled by vite, so anything the renderer imports is already inside `out/renderer` and a second copy of it in `node_modules` is weight nobody ever loads. Every package the app uses was in `dependencies` once, which is 284 of them and 830MB, `onnxruntime-node` alone 218MB with nothing in main, preload, server, runner or shared importing it. Cut to the five that are really loaded at runtime it is 23 packages and 71MB, and that took the app from 899MB to 292MB and the zip every update rides on from 347MB to 116MB. A package belongs in `dependencies` only if something outside the renderer imports it, and everything else is dev.
- The prebuilds for other platforms are already gone, so nothing here excludes them by hand. electron-builder drops `prebuilds/<platform>-<arch>` for every platform but the one being built, which is what takes node-pty from the 62MB in the package to the 2.8MB a Mac ships, and a rule written here to do it again would be a rule saying what the builder already does.
- The Windows installer is built as `Crew Setup 0.1.0.exe` and written into `latest.yml` as `Crew-Setup-0.1.0.exe`, because the feed records the name that is safe to put in a URL. electron-builder renames it as it uploads, and `--publish never` means it never gets to, so the workflow renames every file with a space in it before the release is made. Without that the app asks for a file the release does not have and the update is a quiet 404.
- `--publish never` still writes the feed. The update files are made because there is a `publish` block in `package.json`, not because anything is being published, which is what lets both platforms build on their own runners and land on one release afterwards. Two matrix jobs publishing to one tag race each other, so nothing here ever does.
- The release must not be a draft or a prerelease. The app asks GitHub for the latest release and GitHub does not count either of those as one.
- A Crew that was never signed cannot update itself to a signed one. Squirrel checks what came down against the running app's own requirement, and an ad hoc signature cannot be satisfied by a Developer ID one, so a locally built Crew installs the dmg by hand once and every update after that is ordinary. It stays ordinary only while `com.jamel.crew` and the Team ID hold: changing either voids all four permissions for everyone and takes the update path with them.
- Never add `@electron/fuses` with its security defaults. `RunAsNode` is what the `crew` command and the Fn key listener are both built on, so turning it off takes away the way in from a terminal and the way in from a bare modifier at once, and the asar integrity pair breaks the listener's own entry point inside `app.asar`.
- Nothing is ever rebuilt from source, which is `npmRebuild: false` in the build config. Every native module here is N-API and ships a prebuild for each platform in the package, so there is nothing to build and the ABI is stable across node and electron anyway. Left on, electron-builder hands each one to `@electron/rebuild`, which shells out to node-gyp, whose bundled gyp imports `distutils`, which Python dropped in 3.12, so the build dies on the first native module on any runner with a current Python while passing on a laptop still running 3.9. The rebuild is worse than unnecessary: node-pty and uiohook-napi both look in `build/Release` before `prebuilds/`, so what a rebuild leaves behind is what loads, whatever architecture it was really built for.
- The Mac build is arm64 and nothing else. Half of what a native module ships is a package per architecture, `@koromix/koffi-darwin-arm64` and `@img/sharp-darwin-arm64`, and only the one matching the machine is installed, so an x64 app cross-built on an arm64 runner carried arm64 binaries and nothing that could load them. That is a runner rather than a flag, so the target is `--mac --arm64` and the check reads `lipo` back off the app it built. Windows is not affected, since it builds x64 on an x64 runner and installs the x64 packages for itself.
- The build installs with the yarn this project is on, which is 1.22.22, and `--frozen-lockfile`. `yarn.lock` here is a v1 lockfile, and yarn 4 reads one by converting it to its own format, which counts as changing it, so an immutable install refuses and the build fails on its first step before anything is compiled. The lockfile rule is what should be strict: a release resolving different versions from the machines it was written on is not the app anybody tested. Never install with corepack yarn 4 without migrating the lockfile with it.

## Dev tools

A shipped Crew has none. `fromSource(app.getAppPath())` is the gate, the same one the icon and the updates read, so a run from source keeps them and only the packaged app loses them. `inspectable` in `src/main/index.ts` is that answer, worked out once and handed to everything that needs it.

- It is off in two places at once, because either one on its own is half a door. `devTools` in `webPreferences` is what makes `openDevTools` do nothing at all, and it goes on every window the app makes: the main one, the tray panel and the Scribe pill, which all read it off the `PanelPage` they are handed. The row in the menu is the other half, since a switch left on with only the row taken away is a window anything in main could still open an inspector on, and a row left standing over a dead switch is a menu item that does nothing.
- The View menu is written out in `appMenuTemplate` rather than taken as the `viewMenu` role. That role carries Toggle Developer Tools and its accelerator, and there is no way to keep the rest of it and drop that one row. Reload and the zoom stay, since `⌘R` is the app's own and the canvas is written around it.
- A page in the side panel is a real browser carrying preferences of its own, and a webview inherits none of this from the window it stands in, so `will-attach-webview` says it again for whatever a window embeds.
- There is no way to loosen it, and no environment variable stands beside `CREW_SHIPPING_ICON` for this. An escape hatch is the ability, which is the thing being taken away.
- `tests/window-options.test.ts` holds all of it. It walks the whole menu tree rather than one submenu, so nothing can put the row back under another menu unnoticed.

## The installer

What somebody sees the first time they meet Crew: a disk image window with the app on the left, Applications on the right, and one line saying what to do. The ground is a light grey, the words and the arrow are ink, and the colour is one aurora anchored off the left edge with half of it past the frame, so Crew stands at the leading edge of the light and Applications is calm grey at the other end.

- The light comes from beyond the window. `BLOOM.x` is negative, so the source itself is never in the picture and what is in the picture is falloff, which is the rule a rim light is lit by. It falls off as a Gaussian rather than as a `smoothstep`, because a smoothstep reaches nought at a place and that place is a visible boundary: on a light ground it read as a cloud with an edge sitting in the left third, rather than as light. A Gaussian has no edge to find. It is a little taller than it is wide, so it hugs the whole left side rather than reading as a semicircle somebody cropped.
- **On a light ground a glow cannot add light, it can only stain.** There is nowhere above the paper for it to go, so a screen or an add blend is invisible here and the composite is a lerp toward the colour. That has a consequence worth naming: every mesh colour is darker than the paper, so what reads as a bloom is a controlled darkening, and the layers with the least lightness between them and the paper are the ones that read as light. The violet and the base were the two turning light into ink, and both are raised toward the paper's lightness with their chroma held. Lowering their opacity instead only makes them grey.
- The shading agrees with where the light is. The one wash over the ground darkens toward the bottom right, away from the source, which is what stops the grey reading as flat paint. It is a few per cent and no more.
- The grain is over everything, ground and colour alike, and it is paper rather than dither. Dither is one 8-bit level and this is many times that on purpose. It is monochrome, added to all three channels together, and centred so the mean brightness is what it was.
- Each scale is shot at its own size. One shot at `@2x` rasterised down to `@1x` averages the grain away, so the file most Macs would show had half the tooth of the one it was tuned on. `make-icon.mjs` shoots the mesh twice, once per scale, and builds a picture from each.
- **Finder pins the icon labels to near-black, so the ground has to stay light enough to read them.** That was the whole reason the dark ground failed, and it is a rule rather than a preference now: `tests/dmg-window.test.ts` holds the label colour and the headline ink above 4.5:1 against `GROUND`. It also holds `GROUND` and the shader's own `PAPER` to being one colour, which is written down twice because a shader is a browser global that cannot import a drawing.

- **One picture ships, and it is a still by choice rather than by necessity.** `DMG.at` is the moment it is cut from, and `yarn dmg-preview` stands the whole thing up as a real page so a moment is picked by watching rather than by guessing a number.
- **Finder does animate a disk image background, which is worth knowing and was not obvious.** An animated GIF and an animated PNG both play behind the icons when handed to `background picture of options`, and it was measured rather than assumed: a volume wearing a still png photographed four times over six seconds came back byte identical every time, and the same volume wearing an animated one came back different every time. It is not shipped because of what it costs. The grain is the whole of that cost: it is noise, noise does not compress, and a second of it at retina runs to megabytes a frame however it is encoded. A paletted, undithered, ping-ponged animated png at 8 frames a second was the best of them and still eleven megabytes against the still's three quarters of one. If it is ever wanted, that is the recipe, and the frames have to be spaced at the real frame rate: sampled seconds apart in mesh time it does not read as drift, it reads as flashing between pictures.
- **A background is drawn one image pixel to the point, so a retina picture has to say it is one.** Handed a 1320 by 800 file with nothing else said, Finder fills 1320 by 800 points and the window shows the top left quarter of it, which reads as a headline running off the edge. A `pHYs` chunk saying 144 dots to the inch is what makes the same file land at 660 by 400 points with every pixel of its detail, and `tagDpi` in `scripts/dmg-art.mjs` is the whole of writing it. That is one file rather than the pair a multi-representation tiff needed, and the suite holds the picture to being `DMG.retina` times the window and tagged to match.
- The grain is a fixed size on the page rather than one device pixel. Tied to the device pixel it halves physically at retina, so the ground was a different material at each scale, and it is also what makes an animation impossible to compress. `uGrainCell` is the block it is drawn in, worked out from the width being rendered, so the paper looks the same whatever it is drawn at.
- **The title bar cannot be taken off and its words cannot be chosen.** Finder's own dictionary gives a window `toolbar visible`, `statusbar visible`, `pathbar visible` and `sidebar width`, and nothing for the title bar, which is drawn whatever anybody asks. What it says is the volume name, and the volume name is what somebody reads on the desktop and in the sidebar when they go to eject it, so it stays legible on purpose.
- The line at the top is not an instruction. Six of the nine real installers opened while this was written say nothing at all, Docker names the gesture rather than aiming it, and Warp writes two words that could not belong to another app. The rule Crew already holds is what settles it: "Drag Crew into Applications" said Crew twice and Applications once within a few inches of both words, and the arrow had already said drag, so four words broke the one rule three times.
- **Finder pins the icon labels to near-black in both appearances the moment a background picture is set.** Measured off the pixels: the words under both icons come back `#030303` in Light and `#030303` in Dark, byte for byte, while the same volume with no background picture tracks the appearance properly and reads `#dddddf` on dark. There is no label colour key in the view options blob, so there is nothing to write and nothing to opt out of. The two words under the icons are the one part of this window that cannot be designed, and on a dark ground they were near-black on near-black. That is the constraint the light ground answers rather than works around: near-black on light grey is the one legible thing Finder was ever going to give, so the ground was moved to meet it instead of the design being built around a floor it could not clear.
- The title bar takes `DMG.chrome` off the bottom of the picture, so the bottom 48 of what is drawn is never seen. The window is the height of the picture rather than the picture plus a title bar, because a picture taller than the content area is cropped and one shorter leaves a band of nothing at the bottom, and a title bar is not the same height on every Mac. Everything is composed inside `height - chrome` and `tests/dmg-window.test.ts` holds it there.
- `src/renderer` is not involved. The mesh is a shader in `scripts/dmg-mesh.js`, written as a browser global with no imports, so the same file drives the live page and the frame that ships. `scripts/dmg-shoot.mjs` renders a frame in Electron, which is the only place here with a real GL context: headless Chrome hands back an empty canvas, and an empty canvas is a background that looks like nothing went wrong.
- Uniform arrays are indexed in `main` and the values passed into `band`, never indexed by a function parameter. GLSL ES 1.00 only allows a constant index expression, a loop counter counts and a parameter does not, and the whole shader fails to compile over it.
- The bloom is contained. A glow that runs to the edges is cut flat by the frame and reads as a stain rather than as light, so it falls to nothing well inside the window and only a whisper of colour reaches the rest.
- The words are set in the system sans, one face and one size. A hand was tried and it undid the rest: the grain, the glass tile and the aurora are a considered picture, and a casual face standing over them reads as a novelty rather than as the same piece of design.
- `resources/dmg-background.png` is the one file, written by `yarn icon` along with everything else. Nothing in `resources/` is edited by hand.
- Where the icons stand is written down twice, in `DMG` and in the `dmg` block of `package.json`, because one is a drawing and the other is a build config that cannot import it. The suite holds the two to agreeing.
- `yarn dmg-check` is the only thing that can see the rest of it. It builds a real disk image with the real background, sets it up the way electron-builder does, and photographs the mounted window, because whether Finder really lines the picture up with the icons is not a thing any suite can answer.

## Rules for agents working here

- `src/server`, `src/runner`, and `src/shared` must never import electron. Tests import them directly.
- Run `yarn tsc --noEmit` and the tests that cover what you touched before considering a change done. Name them: `yarn test tests/huddle.integration.test.ts`. There are over a hundred suites and the whole run takes a long time, so never finish a change by running `yarn test` on its own. Run the full suite only when someone asks for it.
- People keep more than one crew window open on the same folder, and that is supported. Never kill or quit a running instance to get a quiet repo. Work with it running, and expect your edits to be committed under you while you work.
- No real address is ever written into a suite. A join link, an attachment host and a photo all want an address that is somebody else's machine, and the one to hand is whatever the machine you are sitting at happens to be on, so the crew's own home network went into a dozen suites and out to a public repo with it. `127.0.0.1` where the test really means this machine, and `192.0.2.10` where it means another one: that range and the two beside it, `198.51.100.x` and `203.0.113.x`, are reserved for writing things down and reach nothing. Never a `192.168.x`, a `10.x`, a `172.16.x` through `172.31.x`, or a plain routable address off the internet, whether or not it is one of ours. `tests/address-rule.test.ts` reads every file under `tests/` and holds it.

---
> Source: [JamelHammoud/crew](https://github.com/JamelHammoud/crew) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-08-02 -->
