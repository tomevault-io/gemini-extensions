## simple-store

> This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

SimpleStore is a small e-commerce sample built on **.NET 10** and orchestrated with **.NET Aspire**. The monolith → microservices migration is now complete:

- **Catalog** is a standalone minimal-WebApi service (`SimpleStore.Catalog.API`) that owns `catalogdb` (Postgres). Storefront browsing is anonymous; admin write endpoints require a JWT with the `Admin` role.
- **Identity** (`SimpleStore.Identity.API`) owns `identitydb` (Postgres) and issues JWT bearer tokens (HS256) plus refresh tokens. Web and Admin call it over HTTP for register/login/passkey/profile.
- **Order** (`SimpleStore.Order.API`) owns `orderdb` (Postgres). Storefront endpoints (`/api/v1/order/orders`) require the caller's JWT; admin endpoints (`/api/v1/order/admin/...`) require the `Admin` role.
- **Cart** (`SimpleStore.Cart.API`) is backed by **Redis** (`cart-redis`). It is the first non-DB-backed microservice. Anonymous browsers identify a cart with the opaque `ss_cart` HttpOnly cookie issued by `SimpleStore.Web`, which it passes to Cart.API as the `X-Cart-Id` header; authenticated callers are keyed by the JWT `sub` claim. A `/api/v1/cart/merge` endpoint (auth required) folds the anonymous cart into the user's cart on login.
- **Inventory** (`SimpleStore.Inventory.API`) is the first **event-sourced + CQRS** service and, since v8, the **single source of truth for stock**. The write side appends domain events to **KurrentDB** (formerly EventStoreDB; same open-source product, renamed in 2025); the read side is `inventorydb` (Postgres), populated asynchronously by a `BackgroundService` projector. Admin HTTP surface under `/api/v1/inventory` tracks delivery notes (stock OUT) and receipt notes (stock IN). In v8 it also has a `Reservation` aggregate: it consumes `ReserveStockRequestedEventV1` from the checkout saga, checks projected stock, and publishes `StockReservedEventV1` / `StockReservationFailedEventV1`. Its projector publishes `StockLevelChangedEventV1` whenever stock changes.
- **Checkout** (`SimpleStore.Checkout.API`) is the v8 **MassTransit Saga State Machine** orchestrator. It owns `checkoutdb` (Postgres, saga state only — no business data) and has **no HTTP surface and no JWT** (pure RabbitMQ consumer). Since v12 it drives the create-order → reserve-stock → **process-payment** → confirm/cancel flow, and on a payment failure it runs a **compensation** (release the reserved stock) before cancelling.
- **Payment** (`SimpleStore.Payment.API`) owns `paymentdb` (Postgres) and is the v12 addition. It holds a prepaid **account** (one balance per user, auto-provisioned at zero) plus a transaction ledger. Customers deposit funds (Web "Wallet" page); admins can top up on a customer's behalf (Admin "Payments" page). It consumes `ProcessPaymentRequestedEventV1` from the checkout saga and publishes `PaymentSucceededEventV1` (sufficient balance, debited) or `PaymentFailedEventV1` (insufficient) — making the **account balance the controllable gate** that decides whether a checkout confirms or cancels. User endpoints (`/api/v1/payment/account/...`) require the caller's JWT; admin endpoints (`/api/v1/payment/admin/...`) require the `Admin` role.

**Event-driven flows** ride a **RabbitMQ** broker via **MassTransit**. Shared event records live in `SimpleStore.Contracts` (references only `MassTransit.Abstractions` for the `[MessageUrn]` attribute, added in v11). Every event has a `Vn`-suffixed CLR type, an `int Version` field, and a pinned `MessageUrn`; see [docs/versioning.md](docs/versioning.md). The wired flows:
- **Checkout saga (v8, payment step added v12)** — Order.API publishes `OrderSubmittedEventV1` (carries a `Guid CorrelationId`); Checkout.API's saga consumes it, publishes `ReserveStockRequestedEventV1`; Inventory.API consumes that and publishes `StockReservedEventV1` or `StockReservationFailedEventV1`. On `StockReserved` the saga (v12) moves to `AwaitingPayment` and publishes `ProcessPaymentRequestedEventV1`; Payment.API replies `PaymentSucceededEventV1` → saga publishes `OrderConfirmedEventV1`, or `PaymentFailedEventV1` → saga moves to `CompensatingStock` and publishes `StockReservationCancelRequestedEventV1` (the **compensation**); Inventory releases the hold and replies `StockReservationCancelledEventV1`, after which the saga publishes `OrderCancelledEventV1`. Order.API consumes the confirm/cancel events to set `Order.Status`. Two 30 s timeouts (Quartz, persistent ADO store in `checkoutdb` — survive restarts) bound the stock and payment waits; the payment timeout also runs the stock-release compensation.
- **Stock release / compensation (v12)** — the saga publishes `StockReservationCancelRequestedEventV1`; Inventory.API's `CancelReservationRequestedConsumer` rehydrates the reservation aggregate, appends `StockReservationCancelledV1` to KurrentDB, and its projector adds the held quantity back to `stock_levels.OnHand`, then publishes `StockReservationCancelledEventV1` (back to the saga) + `StockLevelChangedEventV1` (cache refresh). Implements the reservation-cancel the v8 docs marked "deferred to v9".
- **Payment (v12)** — the saga publishes `ProcessPaymentRequestedEventV1`; Payment.API debits the account inside its transaction and publishes `PaymentSucceededEventV1` / `PaymentFailedEventV1` (EF inbox makes the consume exactly-once, so no double-charge on redelivery).
- **`StockLevelChangedEventV1` (v8)** — Inventory.API's projector publishes it whenever `stock_levels.OnHand` changes (reservations, receipt/delivery notes); Catalog.API consumes it to refresh the denormalized `Product.Stock` cache. (Replaces the v6–v7 flow where Catalog consumed `OrderSubmittedEventV1` to decrement stock directly.)
- **`ProductUpdatedEventV1`** — Catalog.API publishes it after an admin product edit; Cart.API consumes it to refresh the denormalized `ProductName` / `UnitPrice` / `ImageUrl` on every cart line referencing that product.

Web and Admin no longer host any DbContext — they only talk HTTP. Cross-service auth is **JWT-bearer (HS256)**. The shared `Jwt:Issuer` / `Jwt:Audience` / `Jwt:Key` (`Jwt__*` env vars) are propagated to every service by the AppHost so any service can validate any token. Web and Admin store JWTs **server-side** in `IDistributedCache` keyed by an opaque HttpOnly `ss_session` cookie — the browser never holds the JWT itself (BFF pattern). For outbound cross-service HTTP, a `BearerTokenHandler` `DelegatingHandler` stamps `Authorization: Bearer` and transparently refreshes expired access tokens.

There are no test projects.

### Migration changelog

- **v12** — Added **`SimpleStore.Payment.API`** (Postgres `paymentdb`) and inserted a **payment step + real compensation** into the checkout saga. Payment.API mirrors the Order.API template: `PaymentAccount` (one prepaid balance per user, auto-provisioned at zero, unique `UserId`) + `PaymentTransaction` ledger (`Deposit` / `Payment`), `IPaymentService`/`PaymentService` (deposit + saga-driven `DebitForOrderAsync`, both wrapped in `IExecutionStrategy`), a `ProcessPaymentRequestedConsumer` (EF inbox → exactly-once, no double-charge), admin + user endpoints under `/api/v1/payment`, and a `SimpleStore.Payment.API.Client`. The **saga** (`CheckoutSagaStateMachine`) gained two states — `AwaitingPayment` and `CompensatingStock` — plus a second Quartz-backed `PaymentTimeout`; on `StockReserved` it now publishes `ProcessPaymentRequestedEventV1` instead of confirming directly, and on `PaymentFailed`/payment-timeout it publishes `StockReservationCancelRequestedEventV1` (the compensation) and waits for `StockReservationCancelledEventV1` before cancelling. `CheckoutSagaState` got `Amount` + `PaymentTimeoutTokenId` columns (migration `AddPaymentToSaga`). **Inventory** implemented the reservation-release that v8 deferred: `Reservation.Cancel(now)` + `StockReservationCancelledV1` domain event (wire `simplestore.inventory.reservation.cancelled.v1`, registered in `EventTypeRegistry`), a `CancelReservationHandler` (rehydrate + expected-revision append → idempotent) + `CancelReservationRequestedConsumer`, and `InventoryProjector.ApplyStockReservationCancelledAsync` (returns stock to `OnHand`, marks the read row `Cancelled`, publishes `StockReservationCancelledEventV1` + `StockLevelChangedEventV1`) — no Inventory migration (read schema already had `ReservationRow.Status` + `StockMovementRow.MovementType`). `SimpleStore.Contracts` gained 5 events (`ProcessPaymentRequestedEventV1`, `PaymentSucceededEventV1`, `PaymentFailedEventV1`, `StockReservationCancelRequestedEventV1`, `StockReservationCancelledEventV1`) + a `ReservationCancelled` `StockChangeCause`. Web got a customer **Wallet** page; Admin got a **Payments** page (deposit on a customer's behalf, view ledger). The point: the account balance is the **controllable gate** for demoing both checkout success and orchestrated saga compensation. Full notes in [docs/v12-changes.md](docs/v12-changes.md) and [docs/payment-service.md](docs/payment-service.md).
- **v11** — **API & Event Versioning pass** (no new features). Lets contracts evolve without breaking consumers. **HTTP APIs**: adopts `Asp.Versioning.Http` across all 5 backends (new shared helper `SimpleStore.ServiceDefaults/ApiVersioningExtensions.cs` providing `AddSimpleStoreApiVersioning()` + `MapApiV1Group(serviceSegment)`); backends now serve `/api/v{version:apiVersion}/<service>/...` natively (was unversioned `/api/<service>/...`). Gateway routes simplified — every `Transforms` array dropped from `Gateway/appsettings.json` because the public URL and backend URL are now the same. OpenAPI is per-version via `AddOpenApi("v1")` — future `AddOpenApi("v2", ...)` slots in. `ReportApiVersions = true` emits `api-supported-versions` headers on every response. **Integration events**: all 8 records in `SimpleStore.Contracts` renamed with a `Vn` suffix, given `public int Version { get; init; } = 1;`, and pinned with `[MessageUrn("urn:message:SimpleStore.Contracts:<OriginalTypeName>")]` so the wire URN is unchanged from pre-v11 (queues, in-flight messages, and routing all keep working). `SimpleStore.Contracts` now references `MassTransit.Abstractions` for the `[MessageUrn]` attribute. **Inventory domain events**: convention was already correct from v7 (`Vn` suffix + `EventTypeRegistry` wire-string map + projector skip-and-log for unknown wire types); v11 adds an `IEventUpcaster<TOld, TNew>` scaffold for future V2 introductions, a `simplestore.inventory.projector.unknown_events` counter, and an `inventory.unknown_event` activity tag so a botched V2 rollout is loud instead of silent. The policy doc — additive vs breaking, deprecation, upcast vs replay — lives in [docs/versioning.md](docs/versioning.md). Full notes in [docs/v11-changes.md](docs/v11-changes.md).
- **v10** — **Observability pass** (no new features). v9 made the system resilient; v10 makes it queryable. Adds OpenTelemetry instrumentation for the four IO surfaces v9 left dark — EF Core (`SetDbStatementForText = true`), gRPC (KurrentDB), Redis (Cart), and MassTransit — all wired in `SimpleStore.ServiceDefaults/Extensions.cs` via 3 new NuGet packages. Introduces a `ParentBased(TraceIdRatioBased)` sampler with the `OTEL_TRACES_SAMPLER_ARG` env-var knob (default 1.0). Every service gets an inline `Observability/Telemetry.cs` static class (per-service `ActivitySource` + `Meter` named `SimpleStore.{Service}`, picked up by the ServiceDefaults wildcards `AddSource("SimpleStore.*")` / `AddMeter("SimpleStore.*")`); 9 business metrics added across Order / Inventory / Cart / Web / Admin (incl. the `simplestore.identity.token_refresh.coalesced` counter that measures the effectiveness of v9 §8 and a `simplestore.inventory.projector.lag` observable gauge that surfaces projector freshness). `InventoryProjectionService.ApplyOneAsync` is wrapped in a new activity (`InventoryProjector.Apply`) so the per-event projection transaction + MassTransit publish become nested children of one parent span. `CheckoutSagaStateMachine` — silent until now — logs every state transition with `CorrelationId`/`OrderId` and stamps `saga.state.from`/`saga.state.to`/`saga.correlation_id` tags on the MassTransit consumer-dispatch span (no nested span — the bus span from §2 already exists). Consumers and `OrderService.CreateOrderAsync` open `CorrelationId` log scopes for end-to-end joining; three hot-path methods (`InventoryProjector.Apply*Async`, `ProductUpdatedConsumer.Consume`, `ReserveStockRequestedConsumer.Consume`) adopt `LoggerMessage` source generation. Health-check tagging gains a `"ready"` tag convention + a new `/ready` endpoint that filters to dependency probes only (Aspire's Npgsql/Redis/MassTransit checks auto-tagged via `PostConfigure<HealthCheckServiceOptions>`; KurrentDB check tagged explicitly). YARP's active health-check probe is turned on per-cluster in `Gateway/appsettings.json` so a downed backend now produces a clean 503 from the edge instead of a confusing 401 (Identity-cluster gets stricter `Threshold: 1`). Full notes in [docs/v10-changes.md](docs/v10-changes.md).
- **v9** — **Resilience pass** (no new features). Hardens every transient-failure path the v8-and-earlier code left bare. EF Core: `EnableRetryOnFailure(maxRetryCount: 5, maxRetryDelay: 10s)` on all 5 DbContexts; the 4 multi-step transaction sites (`OrderService.CreateOrderAsync`, `CatalogService.UpdateProductAsync`, `CreateReservationHandler.HandleAsync`, `InventoryProjectionService.ApplyOneAsync`) wrapped in `IExecutionStrategy.ExecuteAsync`. Inventory projector: `await foreach` over `IEventStore.SubscribeAllAsync` wrapped in an outer reconnect loop with exponential backoff (1 s → 30 s) so a KurrentDB disconnect no longer stops projection. MassTransit: `UseMessageRetry` (5× exponential) + `UseCircuitBreaker` + Rabbit heartbeat on every Rabbit endpoint across all 5 services that use MassTransit. Health checks: `/alive` (liveness) vs `/health` (readiness with dep probes) exposed in **all** environments (was dev-only); Aspire auto-registers the Postgres/Redis checks, MassTransit auto-registers the bus check, and a new `KurrentDbHealthCheck` plugs the last hole. Startup: new `SimpleStore.ServiceDefaults.StartupMigrationRunner.RunAsync` wraps every service's `MigrateAsync`+seeder in a 5-attempt bounded backoff. Cart: `RedisCartStore.TryLoadItemsAsync` degrades reads to an empty cart on Redis hiccups; new `RedisExceptionMiddleware` turns Redis exceptions on write paths into a clean `503` with `Retry-After`. Web+Admin: new `TokenRefreshCoordinator` (singleton, `ConcurrentDictionary<refreshToken, Lazy<Task<LoginResponse?>>>`) coalesces concurrent refresh rotations so N parallel page loads make **one** call to Identity instead of N. Full notes in [docs/v9-changes.md](docs/v9-changes.md).
- **v8b** — Made the **checkout saga timeout durable**. Quartz now uses a **persistent ADO store** in `checkoutdb` instead of the in-memory `RAMJobStore`: migration `AddQuartzTables` creates the `qrtz_*` schema (lowercase to match the configured `qrtz_` `TablePrefix`); `Program.cs` calls `AddQuartz(q => q.UsePersistentStore(s => { s.UseProperties = true; s.UsePostgres(...); s.UseSystemTextJsonSerializer(); }))` (new package `Quartz.Serialization.SystemTextJson`); a new `CheckoutDbContextFactory` (`IDesignTimeDbContextFactory`) keeps `dotnet ef migrations add` working now that `Program.cs` requires the `checkoutdb` connection string at startup. Scheduled `ReservationTimeoutExpired` triggers now survive a Checkout.API restart (Quartz reloads + fires misfired triggers on boot). Single-replica only — multiple replicas would also need `s.UseClustering()` + `SchedulerId = "AUTO"`. Full notes in [docs/v8b-durable-store-for-saga-timeouts.md](docs/v8b-durable-store-for-saga-timeouts.md).
- **v8a** — Code-quality maintenance pass (no new features). Highlights: `Order.Status` is now an `OrderStatus` **enum** (EF `HasConversion<string>()`, migration `AddOrderStatusEnum`); fixed an N+1 in `CatalogService.GetCategoriesAsync`; added explicit DB indexes (`Products.CategoryId`, `stock_movements (ProductId, MovementType)`); added `[Required]`/`[MaxLength]` annotations; added `StockChangeCause` constants in `SimpleStore.Contracts`; added validation + observability logs. Full notes in [docs/v8a-changes.md](docs/v8a-changes.md).
- **v8** — Wired Inventory into the system as the **single source of truth for stock** and added the **checkout Saga**. New `SimpleStore.Checkout.API` hosts a **MassTransit Saga State Machine** (`checkoutdb`, saga state only, no HTTP) that orchestrates `OrderSubmittedEventV1` → `ReserveStockRequestedEventV1` → `StockReserved`/`StockReservationFailed` → `OrderConfirmed`/`OrderCancelled`, with a 30 s in-memory-Quartz timeout. Inventory.API gained a third event-sourced aggregate, **`Reservation`** (stream `reservation-{guid}`, event `StockReservedV1`), a `ReserveStockRequestedConsumer` + `CreateReservationHandler` (Postgres `SELECT … FOR UPDATE` on `stock_levels` for the availability check), and RabbitMQ wiring; its projector now also **publishes integration events** (`StockReservedEventV1`, `StockLevelChangedEventV1`) inside the projection transaction, gated on an `IsLive` flag (from KurrentDB's `CaughtUp` marker) so a cold-start replay doesn't re-publish history. Order.API added a `Guid CorrelationId` column + `OrderConfirmedConsumer`/`OrderCancelledConsumer`. Catalog.API **deleted `OrderSubmittedConsumer`**, added `StockLevelChangedConsumer` (refreshes the now read-only `Product.Stock` cache), and dropped `Stock` from its create/update API (new `CreateProductRequest`/`UpdateProductRequest`). Full design spec in [docs/checkout-saga.md](docs/checkout-saga.md). Reservation commit/cancel are deferred to v9. **Note for future versions:** integration events live in `SimpleStore.Contracts`; Inventory publishes them from the projector (not the command handlers, except the failure path) because the write side is KurrentDB, not EF.
- **v7** — Added `SimpleStore.Inventory.API` — the first **event-sourced + CQRS** service. Write side uses **KurrentDB** (Aspire `kurrentdb` resource via `CommunityToolkit.Aspire.Hosting.KurrentDB`), one stream per aggregate (`deliveryNote-{guid}`, `receiptNote-{guid}`). Read side is `inventorydb` (Postgres) populated by an asynchronous `InventoryProjectionService` `BackgroundService` that subscribes to `$all` with a stream-name prefix filter and writes the read tables in a single transaction alongside its `(commit, prepare)` checkpoint. Admin-only HTTP surface under `/api/v1/inventory`; no RabbitMQ wiring in v7 (added in v8). The `IEventStore` port hides `KurrentDB.Client` so swapping event stores is one-file work. Stock-on-hand is NOT an aggregate — it's a projection over the events; the truth is in the event store, the Postgres tables are caches.
- **v6** — Added RabbitMQ broker (Aspire `rabbitmq` resource, Management plugin) and MassTransit. New `SimpleStore.Contracts` class library holds shared event records. Catalog.API consumes `OrderSubmittedEventV1` (decrements `Product.Stock`); Cart.API consumes `ProductUpdatedEventV1` (refreshes denormalized line items). Order.API and Catalog.API publish through the **MassTransit EF Core transactional outbox** (the event row commits in the same DB transaction as the entity write); Cart.API has no DbContext and relies on at-least-once delivery + an idempotent consumer.
- **v5** — Introduced `SimpleStore.Gateway` (YARP) as the single entry point for Web and Admin; backend services moved off direct service references.
- **v4** — Extracted `SimpleStore.Order.API` (Postgres, `orderdb`) and `SimpleStore.Cart.API` (Redis, `cart-redis`); deleted `SimpleStore.Data`. Web and Admin are now pure HTTP clients.
- **v3** — Extracted `SimpleStore.Identity.API`; Web/Admin stopped referencing `IdentityDbContext`.
- **v2** — Extracted `SimpleStore.Catalog.API`; introduced the `<Service>.API.Client` library template.
- **v1** — Separate database per service.
- **v0** — Initial version.

## Common commands

Run everything (Aspire orchestrates Postgres + PgWeb + Redis + RedisInsight + RabbitMQ + Management UI + KurrentDB + Identity API + Catalog API + Order API + Cart API + Inventory API + Checkout API + Payment API + Gateway + Web + Admin):

```pwsh
dotnet run --project src/SimpleStore.AppHost
```

One-time secret setup (AppHost user-secrets — `jwt-key` must be a base64-encoded byte array, 32+ bytes):

```pwsh
dotnet user-secrets set Parameters:jwt-key       "<base64 of 32 random bytes>" --project src/SimpleStore.AppHost
dotnet user-secrets set Parameters:jwt-issuer    "simple-store"                --project src/SimpleStore.AppHost
dotnet user-secrets set Parameters:jwt-audience  "simple-store"                --project src/SimpleStore.AppHost
```

Build the solution:

```pwsh
dotnet build SimpleStore.slnx
```

Run a single project directly (requires the relevant connection strings / service URIs / `Jwt__*` in user-secrets/env — normally Aspire injects them):

```pwsh
dotnet run --project src/SimpleStore.Identity.API
dotnet run --project src/SimpleStore.Catalog.API
dotnet run --project src/SimpleStore.Order.API
dotnet run --project src/SimpleStore.Cart.API
dotnet run --project src/SimpleStore.Inventory.API
dotnet run --project src/SimpleStore.Checkout.API
dotnet run --project src/SimpleStore.Payment.API
dotnet run --project src/SimpleStore.Web
dotnet run --project src/SimpleStore.Admin
```

EF Core migrations — one DbContext per database, each owned by its API project. The `--context` and `--output-dir` flags must always be specified so migrations land in the right folder:

```pwsh
# Catalog (lives in SimpleStore.Catalog.API)
dotnet ef migrations add <Name> --project src/SimpleStore.Catalog.API --startup-project src/SimpleStore.Catalog.API --context CatalogDbContext --output-dir Migrations
dotnet ef database update       --project src/SimpleStore.Catalog.API --startup-project src/SimpleStore.Catalog.API --context CatalogDbContext

# Identity (lives in SimpleStore.Identity.API)
dotnet ef migrations add <Name> --project src/SimpleStore.Identity.API --startup-project src/SimpleStore.Identity.API --context IdentityDbContext --output-dir Migrations
dotnet ef database update       --project src/SimpleStore.Identity.API --startup-project src/SimpleStore.Identity.API --context IdentityDbContext

# Orders (lives in SimpleStore.Order.API)
dotnet ef migrations add <Name> --project src/SimpleStore.Order.API --startup-project src/SimpleStore.Order.API --context OrderDbContext --output-dir Migrations
dotnet ef database update       --project src/SimpleStore.Order.API --startup-project src/SimpleStore.Order.API --context OrderDbContext

# Inventory read side (lives in SimpleStore.Inventory.API; the write side is KurrentDB, not EF)
dotnet ef migrations add <Name> --project src/SimpleStore.Inventory.API --startup-project src/SimpleStore.Inventory.API --context InventoryReadDbContext --output-dir Migrations
dotnet ef database update       --project src/SimpleStore.Inventory.API --startup-project src/SimpleStore.Inventory.API --context InventoryReadDbContext

# Checkout saga state (lives in SimpleStore.Checkout.API; CheckoutDbContext holds saga state + MassTransit outbox tables)
dotnet ef migrations add <Name> --project src/SimpleStore.Checkout.API --startup-project src/SimpleStore.Checkout.API --context CheckoutDbContext --output-dir Migrations
dotnet ef database update       --project src/SimpleStore.Checkout.API --startup-project src/SimpleStore.Checkout.API --context CheckoutDbContext

# Payment (lives in SimpleStore.Payment.API; PaymentDbContext holds accounts + transaction ledger + MassTransit outbox)
dotnet ef migrations add <Name> --project src/SimpleStore.Payment.API --startup-project src/SimpleStore.Payment.API --context PaymentDbContext --output-dir Migrations
dotnet ef database update       --project src/SimpleStore.Payment.API --startup-project src/SimpleStore.Payment.API --context PaymentDbContext
```

Cart.API has no DbContext — Redis schema is implicit.

## Architecture

Aspire AppHost ([src/SimpleStore.AppHost/AppHost.cs](src/SimpleStore.AppHost/AppHost.cs)) is the entry point. It provisions one `postgres` resource with PgWeb and six logical databases (`catalogdb`, `orderdb`, `identitydb`, `inventorydb`, `checkoutdb`, `paymentdb`), one `cart-redis` resource with RedisInsight, one `rabbitmq` resource with the Management plugin, and one `kurrentdb` resource (with a `kurrentdb-data` volume) for the Inventory write side; each microservice is the only resource that touches its own data store, `order` / `catalog` / `cart` / `inventory` / `checkout` / `payment` reference `rabbitmq`, and `inventory` references `kurrentdb`. The AppHost also defines three parameters — `jwt-key` (secret), `jwt-issuer`, `jwt-audience` — and propagates them as `Jwt__Key` / `Jwt__Issuer` / `Jwt__Audience` env vars to every service that issues or validates JWTs (including the `gateway` and `payment`). The `checkout` service is the exception — it has no HTTP surface and validates no tokens, so it gets no `Jwt__*`. Web and Admin only reference the `gateway` (YARP reverse proxy) — they no longer reference backend services directly. The `gateway` references every HTTP backend (`identity`, `catalog`, `order`, `cart`, `inventory`, `payment`) but NOT `checkout` (nothing calls the saga over HTTP).

Projects:

- **SimpleStore.Identity.API** — Minimal WebApi (`Microsoft.NET.Sdk.Web`). Owns `identitydb` end-to-end: `IdentityDbContext` + `ApplicationUser` (with `FullName`) + `RefreshToken` entity, `IIdentityService`/`IdentityService`, `IJwtTokenService`/`JwtTokenService` (HS256), `IRefreshTokenService`/`RefreshTokenService` (rotate-on-use, SHA-256 hashed), and minimal-API endpoints in `Endpoints/IdentityEndpoints.cs` under `/api/v1/identity`. Migrates and seeds on startup (`IdentitySeeder`) — creates roles `Admin` + `Customer` and two users: `admin@simplestore.local`/`Admin123!` (Admin) and `demo@simplestore.local`/`Demo123!` (Customer). OpenAPI surface in development at `/openapi/v1.json`. Identity schema v3 is enabled so the passkey table is included. The service validates its own tokens (for `/me`, `/passkeys`, `/users` admin endpoints) using the same `Jwt:*` config it issues with.
  - Anonymous: `POST /register`, `POST /login`, `POST /refresh`, `POST /logout`, `POST /passkey/assertion-options`, `POST /passkey/assertion`.
  - Authenticated: `GET/PUT /me`, `POST /passkey/creation-options`, `POST /passkey/attestation`, `GET /passkeys`, `DELETE /passkeys/{credentialIdBase64}`.
  - Admin (`Admin` policy = role `Admin`): paged `GET /users`, `GET /users/count`, `GET/PUT /users/{id}`, `POST /users/{id}/lock|/unlock`.
- **SimpleStore.Identity.API.Client** — Shared class library referenced by Identity.API, Web, and Admin. Holds DTOs (`LoginRequest`, `LoginResponse`, `RegisterRequest`, `RefreshRequest`, `UserInfo`, `UpdateProfileRequest`, `UserSummary`, `UserPasskeyInfo`, `PasskeyAssertionRequest`, `PasskeyAttestationRequest`), a local `PagedResult<T>`, and the typed `IIdentityApiClient` / `IdentityApiClient`. The `AddIdentityApiClient` extension on `IHostApplicationBuilder` registers the typed HttpClient with `BaseAddress = new Uri("https+http://identity")` — Aspire service discovery + standard resilience from `ServiceDefaults` apply automatically.
- **SimpleStore.Catalog.API** — Minimal WebApi. Owns `catalogdb` end-to-end (same template as Identity.API). Endpoints under `/api/v1/catalog`; reads are anonymous, writes require `RequireAuthorization("Admin")`. Validates tokens issued by Identity.API using the shared `Jwt:*` config. **Publishes `ProductUpdatedEventV1`** from `CatalogService.UpdateProductAsync` via the EF Core outbox (only on update). **Consumes `StockLevelChangedEventV1`** (v8) via `Consumers/StockLevelChangedConsumer`, which overwrites `Product.Stock` with the authoritative `NewOnHand` from Inventory; the EF Core inbox makes the consume idempotent. `Product.Stock` is now a **read-only cache** — the v7 `OrderSubmittedConsumer` is gone, and the create/update API no longer accepts `Stock` (see the new request DTOs below).
  - Anonymous reads: paged `GET /products` (`?page=1&pageSize=20&categoryId={int?}&search={string?}`, `pageSize` clamped to 100), `GET /products/{id}`, `GET /products/count`, paged `GET /categories`, `GET /categories/{id}`, `GET /categories/count`.
  - Admin writes: `POST/PUT/DELETE /products/{id}` (POST/PUT bind `CreateProductRequest`/`UpdateProductRequest` — no `Stock`) and `POST/PUT/DELETE /categories/{id}`. `DELETE /categories/{id}` returns `409 Conflict` when the category still has products.
- **SimpleStore.Catalog.API.Client** — Same template as Identity.API.Client: DTOs (`ProductDto` response with read-only `Stock`/`CategoryName`; write models `CreateProductRequest` / `UpdateProductRequest` with no `Stock`; `CategoryDto` with flat `CategoryName` / `ProductCount`), `PagedResult<T>`, typed `ICatalogApiClient` / `CatalogApiClient`, `AddCatalogApiClient` extension with `BaseAddress = new Uri("https+http://catalog")`.
- **SimpleStore.Contracts** — Tiny class library; v11 added one transitive dependency on **`MassTransit.Abstractions`** (for the `[MessageUrn]` attribute). Holds the shared event records that ride the RabbitMQ bus: `OrderSubmittedEventV1` (+ `OrderSubmittedLineItem`, with a `Guid CorrelationId`), `ProductUpdatedEventV1`, the v8 saga/stock events — `ReserveStockRequestedEventV1` (+ `ReservationLineItem`), `StockReservedEventV1`, `StockReservationFailedEventV1` (+ `ShortageLine`), `StockLevelChangedEventV1`, `OrderConfirmedEventV1`, `OrderCancelledEventV1` — and the v12 payment + compensation events: `ProcessPaymentRequestedEventV1`, `PaymentSucceededEventV1`, `PaymentFailedEventV1` (+ `PaymentFailureReason` constants), `StockReservationCancelRequestedEventV1`, `StockReservationCancelledEventV1`. Every event has a `Vn` suffix on the CLR type, an `int Version` field (defaulted to its `Vn`), and `[MessageUrn(...)]` pinning the wire URN (since v11) to the MassTransit default — `urn:message:SimpleStore.Contracts:<TypeNameWithoutVn>` — so the rename is invisible on the bus and a future `Vn+1` gets its own URN. Referenced by `Order.API`, `Catalog.API`, `Cart.API`, `Inventory.API`, `Checkout.API`, and `Payment.API`. Keep types immutable (`init` setters, `IReadOnlyList<>`) and additive within a `Vn` — breaking changes go to `Vn+1` as a new type with a new URN. Policy: [docs/versioning.md](docs/versioning.md).
- **SimpleStore.Order.API** — Minimal WebApi. Owns `orderdb` end-to-end: `OrderDbContext` + `Order` (with a `Guid CorrelationId` — the saga key, unique-indexed) + `OrderItem` (with `ProductName` denormalized at order-creation time so views never call Catalog), `IOrderService`/`OrderService`, and minimal-API endpoints in `Endpoints/OrderEndpoints.cs` under `/api/v1/order`. Migrates on startup (`OrderSeeder` only calls `MigrateAsync` — orders are user-generated, no seed data). Validates tokens with the same `Jwt:*` config. **Publishes `OrderSubmittedEventV1`** (carrying `CorrelationId`) at the end of `OrderService.CreateOrderAsync` through the MassTransit EF Core outbox: the publish is wrapped in `Database.BeginTransactionAsync` so the order row and the `OutboxMessage` row commit atomically. **Consumes `OrderConfirmedEventV1` / `OrderCancelledEventV1`** (v8) via `Consumers/OrderConfirmedConsumer` / `OrderCancelledConsumer`, which look the order up by `CorrelationId` and set `Status`. Status flow: `Pending` (at creation) → `Confirmed` | `Cancelled` (set by the saga) → `Delivered` (admin action).
  - Authenticated (owner = `sub` claim): `GET /orders`, `GET /orders/{id}`, `POST /orders` (body `CreateOrderRequest { ShippingAddress, Items[] }`).
  - Admin (`Admin` policy): paged `GET /admin/orders`, `GET /admin/orders/count`, `GET /admin/orders/{id}`, `PATCH /admin/orders/{id}/status` (body `UpdateOrderStatusRequest { Status }`), `GET /admin/orders/stats` (single-trip aggregate: `TotalCount`/`PendingCount`/`ConfirmedCount`/`CancelledCount`/`CompletedCount`/`TotalRevenue` — used by the Admin dashboard), and `GET /admin/orders/counts-by-user` (bulk per-user counts dictionary — used by the Admin Customers page).
- **SimpleStore.Order.API.Client** — Same template: DTOs (`OrderDto`, `OrderItemDto`, `CreateOrderRequest`, `UpdateOrderStatusRequest`, `OrderStatsDto`), `PagedResult<T>`, typed `IOrderApiClient` / `OrderApiClient`, `AddOrderApiClient` extension with `BaseAddress = new Uri("https+http://order")`. Because the entity name `Order` collides with the second segment of the `SimpleStore.Order.API.*` namespace tree, `OrderDbContext` and `OrderService` use a `using OrderEntity = SimpleStore.Order.API.Models.Order;` alias — keep that convention if you add more code that touches the EF entity directly.
- **SimpleStore.Cart.API** — Minimal WebApi backed by Redis. Pulls in both `Aspire.StackExchange.Redis.DistributedCaching` (for `IDistributedCache`) and `Aspire.StackExchange.Redis` (for `IConnectionMultiplexer`, needed by SCAN) — both target the same `cart-redis` resource. No DbContext — `RedisCartStore` serializes a `List<CartItemDto>` to JSON under the key `cart:{ownerKey}` with 30-day sliding expiration. Endpoints in `Endpoints/CartEndpoints.cs` under `/api/v1/cart` are mostly `.AllowAnonymous()` so anonymous shoppers work; the per-endpoint `ResolveOwner` helper picks the owner key from `User.FindFirst("sub")` (preferred) or the `X-Cart-Id` header (fallback), and returns `400 Bad Request` if neither is present. Web sends fully enriched line items (`{ProductId, ProductName, UnitPrice, ImageUrl, Quantity}`) — Cart.API does not call Catalog. The `POST /api/v1/cart/merge` endpoint is the one exception: it requires a JWT, takes `{ AnonymousCartId }`, and folds the anonymous cart into the user's cart. **Consumes `ProductUpdatedEventV1`** via `Consumers/ProductUpdatedConsumer`, which **enumerates every cart key with Redis SCAN** (`IConnectionMultiplexer.GetServer(...).KeysAsync("cart:*")`, exposed as `ICartStore.EnumerateOwnerKeysAsync`) and refreshes the denormalized `ProductName` / `UnitPrice` / `ImageUrl` on any line whose `ProductId` matches. SCAN is non-blocking on the server; the design is linear in cart count and acceptable while the cart-key count is small/medium. **No reverse index is maintained** — keeps cart writes unchanged at the cost of a fan-out scan on each product update. The consumer has no inbox (Cart has no DbContext); duplicate delivery is harmless because rewriting identical fields is idempotent.
  - Items: `GET /api/v1/cart`, `POST /api/v1/cart/items`, `PUT /api/v1/cart/items/{productId}`, `DELETE /api/v1/cart/items/{productId}`, `DELETE /api/v1/cart`, `GET /api/v1/cart/count`, `GET /api/v1/cart/total`.
  - Merge: `POST /api/v1/cart/merge` (auth).
- **SimpleStore.Cart.API.Client** — Same template: DTOs (`CartDto`, `CartItemDto`, `AddCartItemRequest`, `UpdateCartItemRequest`, `MergeCartRequest`), typed `ICartApiClient` / `CartApiClient`, `AddCartApiClient` extension with `BaseAddress = new Uri("https+http://cart")`. The client never sets `X-Cart-Id` itself — that is the consumer's `CartIdHandler` `DelegatingHandler` (see SimpleStore.Web).
- **SimpleStore.Inventory.API** — Event-sourced + CQRS minimal WebApi; the single source of truth for stock. Folder layout maps 1:1 to the layering: `Domain/` (aggregates `DeliveryNote`, `ReceiptNote`, and v8's `Reservation` with `Issue`/`Record`/`Reserve` factories + `Apply` rehydration; value object `InventoryLine`; events `DeliveryNoteIssuedV1`, `ReceiptNoteRecordedV1`, `StockReservedV1`, `StockReservationCancelledV1`), `Application/` (plain DI command handlers — no MediatR — including `CreateReservationHandler` + `CancelReservationHandler`), `Consumers/` (`ReserveStockRequestedConsumer`, `CancelReservationRequestedConsumer`), `EventStore/` (technology-agnostic `IEventStore` port + the single `KurrentEventStore.cs` adapter that imports `KurrentDB.Client`), `Projections/` (the async `InventoryProjectionService` + pure `InventoryProjector` + `CheckpointStore`), `Data/ReadModels/` (EF entities for the read tables — `delivery_notes`, `delivery_note_lines`, `receipt_notes`, `receipt_note_lines`, `reservations`, `reservation_lines`, `stock_levels`, `stock_movements`, `projection_checkpoints` — plus the MassTransit outbox tables), `Endpoints/` (composed via `MapInventoryEndpoints` — all under `/api/v1/inventory`, all `RequireAuthorization("Admin")`). Aggregate streams are `deliveryNote-{guid}`, `receiptNote-{guid}`, `reservation-{guid}` (KurrentDB category convention). Concurrency is enforced by `StreamState.NoStream` on append — a retry collapses onto the same stream and returns 409. **v8 RabbitMQ wiring:** `CreateReservationHandler` (driven by `ReserveStockRequestedConsumer`) opens a Postgres transaction, `SELECT … FOR UPDATE`s the `stock_levels` rows, and either appends `StockReservedV1` (success — the projector publishes `StockReservedEventV1`) or publishes `StockReservationFailedEventV1` directly (failure — no domain event). The projector publishes `StockReservedEventV1` + `StockLevelChangedEventV1` inside its projection transaction, **gated on `EventEnvelope.IsLive`** (from KurrentDB's `CaughtUp` marker) so a cold-start replay rebuilds the read tables without re-publishing history. `StockReservedV1` decrements `stock_levels.OnHand`. **v12 reservation release (the saga compensation):** `Reservation.Cancel(now)` emits `StockReservationCancelledV1` (wire `simplestore.inventory.reservation.cancelled.v1`); `CancelReservationRequestedConsumer` → `CancelReservationHandler` rehydrates the aggregate and appends it with an expected-revision condition (idempotent — a redelivered cancel collapses to a no-op), and the projector's `ApplyStockReservationCancelledAsync` adds the held quantity **back** to `OnHand`, flips the read row to `Cancelled`, and publishes `StockReservationCancelledEventV1` (to the saga) + `StockLevelChangedEventV1`. Reservation **commit** (convert to a delivery note on confirm) is still future work. POST returns a DTO built from the in-memory aggregate, not the read DB; the projector is async, so a GET microseconds later may briefly 404. Cold-start projector = full replay from `FromAll.Start`: wipe the read tables, restart the service, the projector rebuilds everything. `InventorySeeder` appends one receipt note per seeded product (deterministic GUIDs) so its read model matches Catalog's seeded `Product.Stock`.
- **SimpleStore.Inventory.API.Client** — Same template as the other client libs: DTOs (`CreateDeliveryNoteRequest`, `CreateReceiptNoteRequest`, `DeliveryNoteDto`, `ReceiptNoteDto`, `InventoryLineDto`, `StockLevelDto`, `StockMovementDto`, local `PagedResult<T>`), typed `IInventoryApiClient` / `InventoryApiClient` calling `api/v1/inventory/...` through the gateway, `AddInventoryApiClient(builder, serviceName = "gateway")` extension. Note IDs are **client-supplied** `Guid`s so retries are idempotent at the event-store level.
- **SimpleStore.Checkout.API** — v8 checkout **Saga orchestrator** (MassTransit Saga State Machine). Owns `checkoutdb` (Postgres) holding only `CheckoutSagaState` (+ the MassTransit outbox tables) via the EF saga repository. **No HTTP surface, no client library, no JWT** — it is a pure RabbitMQ consumer (`Program.cs` only calls `MapDefaultEndpoints()`). Layout: `Sagas/` (`CheckoutSagaState` + `CheckoutSagaStateMachine`), `Timeouts/` (`ReservationTimeoutExpired`, `PaymentTimeoutExpired`), `Data/CheckoutDbContext`. States (v12): `AwaitingStock` → `AwaitingPayment` → `Confirmed`, or `→ CompensatingStock → Cancelled`, all correlated by the `Guid CorrelationId` minted by Order.API. On `OrderSubmittedEventV1` it publishes `ReserveStockRequestedEventV1` + schedules a 30 s `ReservationTimeoutExpired`; on `StockReservedEventV1` it publishes `ProcessPaymentRequestedEventV1` + schedules a 30 s `PaymentTimeoutExpired`; on `PaymentSucceededEventV1` it publishes `OrderConfirmedEventV1`; on `PaymentFailedEventV1`/payment-timeout it publishes `StockReservationCancelRequestedEventV1` (compensation) and waits for `StockReservationCancelledEventV1` before publishing `OrderCancelledEventV1`; on `StockReservationFailedEventV1`/stock-timeout it cancels directly. Both timeouts use the Quartz scheduler (`MassTransit.Quartz`) backed by a **persistent ADO store** in `checkoutdb`'s `qrtz_*` tables, so they survive a restart (v8b). Full design spec: [docs/checkout-saga.md](docs/checkout-saga.md).
- **SimpleStore.Payment.API** — v12 minimal WebApi. Owns `paymentdb` end-to-end (same template as Order.API): `PaymentDbContext` + `PaymentAccount` (one prepaid balance per user, unique `UserId`, auto-provisioned at zero) + `PaymentTransaction` ledger (`Deposit` / `Payment`), `IPaymentService`/`PaymentService`, and minimal-API endpoints in `Endpoints/PaymentEndpoints.cs` under `/api/v1/payment`. Validates tokens with the shared `Jwt:*` config. **Consumes `ProcessPaymentRequestedEventV1`** via `Consumers/ProcessPaymentRequestedConsumer` → `PaymentService.DebitForOrderAsync`, which debits the account and **publishes `PaymentSucceededEventV1`** (sufficient balance) or **`PaymentFailedEventV1`** (insufficient) through the MassTransit EF outbox inside the same transaction as the balance change; the EF inbox makes the consume exactly-once (no double-charge on redelivery). The account balance is the controllable gate for demoing checkout success vs. saga compensation.
  - Authenticated (owner = `sub` claim): `GET /account` (auto-provisions), `POST /account/deposit` (body `DepositRequest { Amount }`), `GET /account/transactions`.
  - Admin (`Admin` policy): `GET /admin/accounts` (paged), `GET /admin/accounts/count`, `GET /admin/accounts/{userId}`, `POST /admin/accounts/{userId}/deposit`, `GET /admin/accounts/{userId}/transactions`.
- **SimpleStore.Payment.API.Client** — Same template as the other client libs: DTOs (`AccountDto`, `TransactionDto`, `DepositRequest`, local `PagedResult<T>`), typed `IPaymentApiClient` / `PaymentApiClient` calling `api/v1/payment/...` through the gateway, `AddPaymentApiClient(builder, serviceName = "gateway")` extension. Referenced by Web (customer Wallet) and Admin (Payments page).
- **SimpleStore.Gateway** — YARP reverse proxy. Single entry point for Web and Admin; backend services are no longer referenced directly by the UIs. Routes are config-driven (`appsettings.json` → `ReverseProxy`) and match `/api/v1/<service>/{**catch-all}` per backend — **no path transform** (since v11): backends serve `/api/v1/<service>/...` natively (via `Asp.Versioning.Http` URL-segment versioning), so the gateway forwards verbatim. One cluster per backend resolved via Aspire service discovery (`https+http://identity` etc.). **Auth is enforced at the edge** — `Program.cs` registers JWT-bearer validation (same shared `Jwt:*` config) and two named policies (`AuthenticatedUser`, `Admin`) that routes opt into via `AuthorizationPolicy`. Conventions baked into the route table: Catalog reads are anonymous and writes are `Admin`; Order user endpoints require auth and `/admin/*` requires `Admin`; Identity has explicit anonymous routes for login/register/refresh/logout/passkey-assertion + an `Admin` route for `/users/*` + an `AuthenticatedUser` fallback for everything else; Cart is mostly anonymous except `/cart/merge`; Inventory is fully `Admin`; Payment `/admin/*` is `Admin` and the rest requires auth (v12). **Backend services still enforce their own policies** — the gateway is defense in depth, not a substitute. There is no `SimpleStore.Gateway.Client` library; clients hit the gateway via Aspire service discovery (`https+http://gateway`).
- **SimpleStore.Web** — Customer storefront. ASP.NET Core MVC + Razor Pages. Default route is `Catalog/Index`. **No DbContext**. `CatalogController` and `CartController` go through `ICatalogApiClient` / `ICartApiClient`; `OrdersController` goes through `IOrderApiClient`; `WalletController` (v12, customer balance + deposit + ledger) goes through `IPaymentApiClient`; the Razor Pages under `Areas/Identity/Pages/Account/**` go through `IIdentityApiClient`. JWT bearer is the only auth scheme — `OnMessageReceived` reads the token from `ITokenStore` (server-side cache keyed by `ss_session`) and auto-refreshes it. Outbound calls to Catalog/Identity/Order/Payment ride `BearerTokenHandler`; outbound calls to Cart ride `BearerTokenHandler` + `CartIdHandler`. Anonymous cart support: `Services/Cart/CartCookieManager` issues the `ss_cart` HttpOnly cookie (GUID, 30-day) on first cart write for anonymous users; `Services/Cart/CartIdHandler` stamps `X-Cart-Id` on outbound Cart.API calls when the cookie is present; `Services/Cart/CartMergeMiddleware` runs after `UseAuthentication` and, on the first authenticated request after login, calls `ICartApiClient.MergeAsync(anonCartId)` and clears the cookie.
- **SimpleStore.Admin** — Blazor Server admin dashboard. **No DbContext** — `Pages/Orders.razor` now uses `IOrderApiClient.GetOrdersAsync` / `UpdateOrderStatusAsync` (was direct EF), and `Pages/Home.razor` uses `IOrderApiClient.GetStatsAsync` (was four direct count queries). The customer-name lookup in Orders.razor still bulk-fetches from `IIdentityApiClient.GetUsersAsync` and dictionary-joins in memory. Authentication is JWT bearer with the same server-side cookie/cache trick used by Web (`Services/Auth/ITokenStore`, `CircuitTokenStore` caches the token at circuit start because `IHttpContextAccessor.HttpContext` isn't reliable during interactive Blazor operations). A v12 `Pages/Payments.razor` uses `IPaymentApiClient` (merged with `IIdentityApiClient.GetUsersAsync`) to list every customer's wallet balance, deposit on their behalf, and view their ledger — the demo-setup surface for forcing checkout success/failure. Login/Logout are Razor Pages at `/Account/Login` and `/Account/Logout`. All Blazor pages are gated with `[Authorize(Roles = "Admin")]` and the `FallbackPolicy = Admin` enforces it. Admin does **not** reference `SimpleStore.Cart.API.Client` — there is no cart UI in admin.
- **SimpleStore.ServiceDefaults** — Standard Aspire shared library: OpenTelemetry (OTLP), service discovery, HTTP resilience, default health endpoints. Every service (`Identity.API`, `Catalog.API`, `Order.API`, `Cart.API`, `Inventory.API`, `Checkout.API`, `Payment.API`, `Gateway`, `Web`, `Admin`) calls `builder.AddServiceDefaults()` and `app.MapDefaultEndpoints()`. Distributed traces propagate end-to-end across all service boundaries automatically.

There are no cross-database foreign keys. `Order.UserId` is a soft reference to `AspNetUsers.Id` in `identitydb`; `OrderItem.ProductId` is a soft reference to `Products.Id` in `catalogdb`. Cross-DB joins are done in application code (load + dictionary lookup) and never at the SQL level.

## Conventions

- Solution file is the new `.slnx` format (`SimpleStore.slnx`), not `.sln`.
- AppHost uses the top-level `AppHost.cs` file (no `Program.cs`).
- When a new microservice is extracted, its DbContext (if any), models, services, migrations, and seeder all move with it; the existing client library pattern (`<Service>.API.Client` with DTOs + typed HttpClient + `Add<Service>ApiClient` extension on `IHostApplicationBuilder`) is the template for new ones.
- When adding a new shared NuGet package or service that both UIs need, update both `SimpleStore.Web` and `SimpleStore.Admin` — there is no shared application-services project.
- **Future APIs that need authentication** default to JWT bearer with the shared `Jwt:Issuer` / `Jwt:Audience` / `Jwt:Key` config (propagated from AppHost via `Jwt__*` env vars) and the named `"Admin"` policy:
  ```csharp
  builder.Services.AddAuthentication(JwtBearerDefaults.AuthenticationScheme).AddJwtBearer(o =>
  {
      o.MapInboundClaims = false;
      o.TokenValidationParameters = new() { /* ValidIssuer/Audience/Key from Jwt:* */
          NameClaimType = "sub", RoleClaimType = "role", ClockSkew = TimeSpan.FromSeconds(30) };
  });
  builder.Services.AddAuthorization(o =>
      o.AddPolicy("Admin", p => p.RequireAuthenticatedUser().RequireRole("Admin")));
  ```
  Mark write endpoints with `.RequireAuthorization("Admin")`. Set `MapInboundClaims = false` so the raw `sub`/`role`/`name` claim names survive — Web, Admin, and every API agree on this.
- **API versioning** (since v11): every backend's HTTP routes live under `/api/v{version:apiVersion}/<service>/...` via `Asp.Versioning.Http` (URL-segment reader). The shared `SimpleStore.ServiceDefaults.ApiVersioningExtensions` provides `AddSimpleStoreApiVersioning()` (call from `Program.cs`) and `MapApiV1Group("<service>")` (call from `Endpoints/*Endpoints.cs` in place of `MapGroup("/api/<service>")`). OpenAPI is per-version: `AddOpenApi("v1")` → `/openapi/v1.json`; future `AddOpenApi("v2", ...)` slots in. The gateway forwards versioned paths verbatim (no path transform). To deprecate a version: `versionSet.HasDeprecatedApiVersion(...)` — the `Sunset` response header is emitted automatically. Full policy + V2 recipe: [docs/versioning.md](docs/versioning.md).
- **Inbound auth in server-side apps (Web, Admin)**: JWT lives in `IDistributedCache` keyed by an HttpOnly `ss_session` cookie. The browser never holds the JWT. `JwtBearerOptions.Events.OnMessageReceived` reads the token from `ITokenStore`, auto-refreshing when within 30s of expiry. Login/logout in Web is a Razor Page handler that calls `IIdentityApiClient.LoginAsync` and writes the result through `ITokenStore`; Admin does the same via `/Account/Login`.
- **Outbound auth (any server-side app → any protected API)**: register a typed HttpClient with `.AddHttpMessageHandler<BearerTokenHandler>()`. `BearerTokenHandler` reads the JWT from `ITokenStore` and stamps `Authorization: Bearer`; it auto-refreshes via `IIdentityApiClient.RefreshAsync` when the access token is near-expiry.
- **Anonymous-friendly APIs (Cart.API)**: when an API intentionally allows anonymous access, endpoints use `.AllowAnonymous()` and resolve identity per request (`User.FindFirst("sub")` first, then a fallback header). The consumer wires both `BearerTokenHandler` and a per-API "id handler" (`CartIdHandler` for Cart) onto the typed HttpClient: `builder.AddCartApiClient().AddHttpMessageHandler<BearerTokenHandler>().AddHttpMessageHandler<CartIdHandler>()`. Cart-specifically, the `ss_cart` cookie lives only in `SimpleStore.Web` and is cleared after merge on first authenticated request via `CartMergeMiddleware`.
- **Blazor Server caveat**: `IHttpContextAccessor.HttpContext` is only reliable during the initial HTTP request that establishes the SignalR circuit. Admin's `CircuitTokenStore` wraps `DistributedCacheTokenStore` and caches the token on the scoped instance so interactive button clicks can still fetch the JWT without a live `HttpContext`.
- **Event contracts** live in `SimpleStore.Contracts` as immutable records. v11 added one transitive dependency on `MassTransit.Abstractions` for `[MessageUrn]`. Every event has a `Vn`-suffixed CLR type, an `int Version` field, and a pinned `MessageUrn` (the wire URN is the cross-service contract — the CLR type name is internal). Additive within a `Vn` is safe; renaming, removing, or retyping fields requires a new `Vn+1` type with a new URN. Policy + recipe in [docs/versioning.md](docs/versioning.md).
- **Event publishing** (Order, Catalog): use `IPublishEndpoint.Publish` inside a `BeginTransactionAsync` block so the entity insert and the `OutboxMessage` row commit atomically. MassTransit's hosted `BusOutbox` delivers from the table asynchronously — crashes between save and deliver are recovered on the next start. Don't publish from anywhere that can't carry the EF Core outbox interceptor (e.g. fire-and-forget background services).
- **Event publishing from an event-sourced service** (Inventory, v8): because the write side is KurrentDB (not EF), integration events are published from the **projector** — `InventoryProjectionService` runs each domain event through a Postgres transaction (read-model write + checkpoint), and `InventoryProjector` calls `IPublishEndpoint.Publish` in that same transaction via the EF bus outbox on `InventoryReadDbContext`. Publishing is gated on `EventEnvelope.IsLive` (KurrentDB's `CaughtUp` marker) so a cold-start replay rebuilds the read model without re-emitting history. The one exception is a **rejected** command (insufficient stock): it has no domain event, so `CreateReservationHandler` publishes `StockReservationFailedEventV1` directly through its own outbox transaction.
- **Event consuming**: consumers live under `Consumers/<EventName>Consumer.cs` and implement `IConsumer<T>`. Services with a DbContext (Catalog) get an EF Core **inbox** via `AddEntityFrameworkOutbox<TDbContext>` so consumes are exactly-once; services without one (Cart) **must** write idempotent handlers because duplicate delivery is possible. MassTransit's `ConfigureEndpoints(ctx)` is the only endpoint config needed — it auto-binds queues per consumer.
- **Cart fan-out**: `RedisCartStore` is keyed only by `cart:{ownerKey}` with no secondary index. Consumers that need to update many carts use **Redis SCAN via `IConnectionMultiplexer.GetServer(...).KeysAsync("cart:*")`** (exposed through `ICartStore.EnumerateOwnerKeysAsync`), **not** a maintained reverse index. SCAN is non-blocking on the Redis server and acceptable while cart-key count is small/medium; revisit only if production cart-key counts grow into the high tens of thousands.
- **Event sourcing (Inventory.API only, v7+)**: domain events live in `Domain/<Aggregate>/Events/*.cs` and are **internal** to the bounded context — they do NOT belong in `SimpleStore.Contracts` (that namespace is reserved for cross-service integration events). Wire-type strings follow `simplestore.<context>.<aggregate>.<verb>.v1` with the `.v1` suffix as the versioning anchor (additive changes keep `.v1`; breaking changes bump to `.v2` and the projector handles both). Streams follow KurrentDB's hyphen-category convention `<category>-<aggregateId>` (e.g. `deliveryNote-{guid}`, `receiptNote-{guid}`, `reservation-{guid}`). The `IEventStore` port hides the SDK; only `EventStore/KurrentEventStore.cs` imports `KurrentDB.Client`. New domain event types are registered in `EventTypeRegistry` (one line each). v8's `StockReservedV1` carries the saga `CorrelationId` as provenance so the projector can stamp it onto the outgoing `StockReservedEventV1`. v11 added an `IEventUpcaster<TOld, TNew>` scaffold (`EventStore/IEventUpcaster.cs`) for future V2 introductions, and a `simplestore.inventory.projector.unknown_events` counter so a botched V2 rollout is loud — the projector still gracefully skips unknown wire types as it did in v7. Full V2 recipe: see the doc-comment at the top of `EventTypeRegistry.cs` or [docs/versioning.md](docs/versioning.md).
- **Async projector (CQRS)**: `InventoryProjectionService` subscribes to KurrentDB's `$all` with a stream-name prefix filter and writes the read tables + `(commit, prepare)` checkpoint in a single Postgres transaction. Per-event "have I seen this NoteId already?" guards make re-applies idempotent (safe to crash and resume). Empty checkpoint = full replay from `FromAll.Start`. v7 runs a single replica; multi-replica would require KurrentDB persistent subscriptions with a consumer group.
- **Date/time conventions** (new in v7): **business dates** (e.g. `Order.OrderDate`, `Inventory note.Date`) use `DateTime`, normalized to midnight UTC. EF maps these to Postgres `DATE`, dropping the time portion. **Audit instants** (e.g. `Inventory event IssuedAt`/`RecordedAt`) use `DateTimeOffset` — explicit UTC offset, no UTC-vs-local ambiguity. Inventory.API is the first service to adopt this split; future services should follow it. Older services (Order, Catalog) may be migrated opportunistically — not now.

---
> Source: [daohainam/simple-store](https://github.com/daohainam/simple-store) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-07-22 -->
