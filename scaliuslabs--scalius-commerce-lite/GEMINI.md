## scalius-commerce-lite

> Turborepo monorepo: TanStack Start admin dashboard + Astro SSR storefront + standalone Hono API, all deployed as Cloudflare Workers. Admin and storefront communicate with the API worker through Cloudflare Service Bindings in production, with HTTP fallbacks for local development.

# Scalius Commerce

## Overview

Turborepo monorepo: TanStack Start admin dashboard + Astro SSR storefront + standalone Hono API, all deployed as Cloudflare Workers. Admin and storefront communicate with the API worker through Cloudflare Service Bindings in production, with HTTP fallbacks for local development.

## Monorepo Structure

```
apps/
  admin-v2/       # @scalius/admin-v2 — TanStack Start admin dashboard Worker
  api/            # @scalius/api — Hono API Worker, queues, cron, WidgetDesignAgent DO
  storefront/     # @scalius/storefront — Astro 7 SSR storefront Worker
packages/
  api-client/     # @scalius/api-client — generated SDK from OpenAPI spec
  core/           # @scalius/core — domain services, auth, providers, integrations, search
  database/       # @scalius/database — Drizzle schema, D1 client, migrations
  shared/         # @scalius/shared — shared utilities
  tsconfig/       # @scalius/tsconfig — shared TS config package
```

## Quick Commands

| Command | Purpose |
|---------|---------|
| `pnpm dev` | Start API :8787, admin :4323, storefront :4322 via `scripts/dev.sh` |
| `pnpm dev:all` | Alias for `pnpm dev` |
| `pnpm dev:api` | Start API :8787 via `scripts/dev.sh` with local migrations/readiness |
| `pnpm dev:admin` | Start admin :4323 + API :8787 |
| `pnpm dev:storefront` | Start storefront :4322 + API :8787 |
| `pnpm build` | Run `prebuild` (`scripts/copy-flags.mjs`) then Turbo build for workspaces with build scripts |
| `pnpm typecheck` | Run type checks through Turbo |
| `pnpm lint` | Run lint through Turbo for the seven code workspaces |
| `pnpm test` | Run all Vitest tests directly with `vitest run --passWithNoTests` |
| `pnpm test:watch` | Run Vitest in watch mode |
| `pnpm dev:setup` | Install deps, create local env files, apply local D1 migrations, create default local admin |
| `pnpm dev:reset` | Wipe local state, re-apply migrations, recreate default local admin |
| `pnpm dev:admin:create` | Create default local admin if none exists |
| `pnpm dev:admin:reset` | Reset local auth/admin credentials without wiping catalog/order data |
| `pnpm dev:admin:status` | Check whether a local admin exists |
| `pnpm dev:doctor` | Non-mutating local readiness check for env files, shared secrets, ports, services, and Wrangler state |
| `pnpm dev:doctor:api` | Require local API to be live |
| `pnpm dev:doctor:admin` | Require local API + admin to be live |
| `pnpm dev:doctor:storefront` | Require local API + storefront to be live |
| `pnpm dev:doctor:all` | Require API + admin + storefront to be live |
| `pnpm db:generate` | Generate Drizzle migrations via the API workspace |
| `pnpm db:migrate:local` | Apply D1 migrations to local Wrangler state |
| `pnpm db:migrate:remote` | Apply D1 migrations to remote D1 |
| `pnpm db:studio` | Drizzle Studio DB browser |
| `pnpm --filter @scalius/database check:migrations` | Verify migration SQL/journal/snapshot metadata and the manual snapshot-gap allowlist |
| `pnpm check:env` | Verify Wrangler binding/var names match Worker `Env` declarations |
| `pnpm check:dist-secrets` | Fail if app `dist/` outputs contain local env files such as `.dev.vars` or `.env*` |
| `pnpm generate:sdk` | Regenerate API client from the API OpenAPI spec |
| `pnpm run deploy` | Typecheck, build, migrate remote D1, deploy API + admin + storefront |
| `pnpm run deploy:api` | Typecheck, build API, migrate remote D1, deploy API |
| `pnpm run deploy:admin` | Typecheck, build admin, deploy admin |
| `pnpm run deploy:storefront` | Typecheck, build storefront, deploy storefront |
| `pnpm run deploy:api -- --dry-run` | Typecheck, build, and dist-check API without applying D1 migrations or deploying |

## Architecture

### Apps

- **Admin (`apps/admin-v2/`)**: TanStack Start + React 19 admin dashboard. Server functions live in typed domain slices under `src/lib/api-functions/`; query options live in domain modules under `src/lib/api-query-options/`; mutation hooks live in domain modules under `src/lib/api-mutations/`, with `api.mutations.ts` kept only as a compatibility re-export. The broad `api.queries.ts` file has been removed. Use fresh `rg` scans for volatile counts instead of trusting prose. Uses `env.API` service binding in production and Vite proxy/HTTP to `localhost:8787` in local dev. Also has direct D1/KV/R2 bindings for auth, RBAC, and storage initialization.
- **API (`apps/api/`)**: Standalone Hono `OpenAPIHono` app mounted at `/api/v1`. Exports a `WorkerEntrypoint` with `fetch`, `queue`, and scheduled maintenance for orphan inventory expiry, stale hosted-payment cleanup, old/empty abandoned-checkout pruning, and notification-outbox flushing. Owns public/admin API routes, webhook ingestion, OpenAPI spec, queue consumer, and `WidgetDesignAgent` Durable Object for widget AI generation.
- **Storefront (`apps/storefront/`)**: Astro 7 SSR + React 19 customer storefront. Owns product, category, cart, checkout, search, customer auth proxy, SEO, sitemaps, error pages, and L1/L2 caching. Uses `env.BACKEND_API` service binding in production; intentionally skips service binding in local dev because separate Miniflare processes cannot reliably share the Fetcher. The source `wrangler.jsonc` is adapter input only; Astro builds the deployed Worker entrypoint and `dist/server/wrangler.json`, and storefront deploys must use that generated config after `astro build`. Custom domains/routes are managed outside the source config until an IaC owner is added. Do not reintroduce a custom `src/worker.ts` entrypoint.

### Packages

- **`@scalius/api-client`**: Generated SDK from OpenAPI. Current generated spec has 261 paths / 359 operations after the payment-session processing-response additions; regenerate with `pnpm generate:sdk` after API contract changes instead of trusting prose counts. Uses `@hey-api/openapi-ts` with the bundled Fetch client generator; do not add the deprecated `@hey-api/client-fetch` runtime package back. The generator removes the development-only `/api/v1/media/{key}` passthrough so the generated contract matches production OpenAPI. Do not hand-edit files in `packages/api-client/src/generated/**`.
- **`@scalius/database`**: Drizzle schema and D1 `getDb(env)` client factory. Current schema has 13 schema files and 10 table-defining files; use fresh `rg`/Drizzle scans for volatile table/migration counts instead of trusting prose.
- **`@scalius/core`**: Domain modules in `src/modules/`, Better Auth config, RBAC, providers, integrations, FTS5 search, and cache utilities.
- **`@scalius/shared`**: Shared utilities. It has external runtime deps, but no internal workspace deps.
- **`@scalius/tsconfig`**: Exports `base.json`, `worker.json`, and `astro.json`. Some apps use local framework configs instead of extending it directly.

Packages are JIT-consumed from TypeScript source by Workers/Vite/Astro. Most package manifests expose `.ts` subpaths and do not have build scripts.

## Tech Stack

- TanStack Start (admin) on Cloudflare Workers
- Astro 7 SSR (storefront) with Cloudflare adapter
- Vite 8.1 for admin and the Astro storefront, and React 19
- Hono + `@hono/zod-openapi` + Swagger UI
- Cloudflare D1 (SQLite) + Drizzle ORM + FTS5
- Tailwind CSS v4 + shadcn/ui/Radix-style components
- Better Auth (email/password + optional 2FA)
- Cloudflare KV, R2, Queues, Cache API, Durable Objects, Workers AI binding
- AI SDK providers for OpenRouter/OpenAI/Gemini/Cloudflare widget generation
- Cloudflare Service Bindings
- Turborepo + pnpm workspaces

## Key Conventions

- **Thin HTTP layer is the target**: Normal `apps/api/src/routes/**` handlers validate/authenticate and delegate to `@scalius/core`. Some webhook and streaming routes still do inline DB work or dispatch because they are integration boundaries.
- **Public listing query ownership**: `/api/v1/products` and `/api/v1/categories/{slug}/products` must keep product SQL in `@scalius/core/modules/products/products.storefront.ts`. Routes may resolve category metadata, validate query params, and call `resolvePublicAttributeFilters()`, but do not reintroduce route-local product predicates, sort SQL, row/count reads, image enrichment, or discount mapping.
- **Product SKU/inventory model**: Products are merchandising containers; SKUs live in `product_variants` and are the only sellable/inventory identities. Simple products use exactly one persisted hidden/default no-option SKU (`isDefault = true`, no size/color); the literal database id `default` is a legacy placeholder and must never count as a sellable SKU. Optioned products use merchant-created SKUs with at least one customer option. Option fields are generic merchant choices: keep the UI as `Option 1`/`Option 2` with size/weight/color/style examples, never as size/color-only semantics. Do not add product-level inventory or buyer/admin order paths that create new variantless order items. Storefront, quick-buy, COD-only cart submit, and admin manual orders must go through cart/SKU validation or explicit SKU selection. Admin manual order create/update must validate each submitted `(productId, variantId)` pair against an active, non-deleted product and a non-deleted variant owned by that product before using `trackInventory` to skip reservation. Merchant-created variants/imports/duplicates must not create extra active no-option SKUs, and the hidden simple SKU should be protected from generic duplicate/delete/bulk-edit flows. Option SKU stock-limit state (`trackInventory`) must be explicit anywhere merchants create or mass-edit options: row edit, spreadsheet edit, bulk generator, CSV import/export, and preview. Product option add/edit and spreadsheet rows should feel like compact grid-cell editing: stable SKU-as-row columns, a dedicated `Stock limit` cell (`Track stock` / `No stock limit`), quantity in `On Hand`, availability read-only, barcode/details behind a popover, and icon-only save/cancel actions that do not widen or heighten the row. Admin surfaces must describe untracked simple/default/option SKUs as `No stock limit`, not `Always available`, `Not tracked`, or a red zero-available state.
- **API route pattern**: New documented JSON endpoints should use `OpenAPIHono`, `createRoute()`, and `app.openapi()`. Exceptions exist for webhooks, health/readiness/root/docs/OpenAPI, Partytown proxy, redirects, text responses, and streaming/widget-generation routes.
- **OpenAPI timestamp schemas**: Use shared helpers from `apps/api/src/schemas/timestamps.ts` for mixed string/number/null timestamp fields. Do not hand-roll nullable timestamp unions that regenerate SDK fields as `unknown`.
- **Standardized API errors**: Import API-facing error classes from `apps/api/src/utils/api-error.ts` (re-exported from `@scalius/core/errors`).
- **Standardized API responses**: Use `ok(c, data)`, `created(c, data)`, and `noContent(c)` from `apps/api/src/utils/api-response.ts` for normal JSON endpoints.
- **Response envelope contract**: Normal success JSON endpoints return `{ success: true, data: T }`. The `T` passed to `ok(c, T)` is the final payload; do not nest another `{ success, data }` inside it. Explicit exceptions: root/health/readiness/OpenAPI/docs, webhooks, proxies, redirects, text responses, and `204` no-content responses. `/api/v1/health` is shallow liveness; `/api/v1/readyz` is non-cacheable deep readiness for D1, KV, R2, Queue/DO bindings, and required public runtime vars. Readiness must stay read-only and must not send queue messages or expose secrets.
- **202 Accepted responses**: Must include top-level `success: true`; use `c.json({ success: true, data: {...} }, 202)` because `ok()` forces 200.
- **Admin API behavior**: Admin server functions in `apps/admin-v2/src/lib/api.server.ts` unwrap API envelopes and return `data` to components. The browser proxy route `apps/admin-v2/src/routes/api/v1/admin/$.ts` passes API responses through unchanged. Admin read-only API calls (`GET`/`HEAD`) use `apps/admin-v2/src/lib/admin-api-timeout.ts` to bound stalled service-binding/HTTP reads through response body consumption; writes intentionally do not get this timeout because a mutation may have committed before the client gives up.
- **Admin dashboard data loading**: The admin home loader must not block SSR or client route entry on dashboard metrics. It skips server-side summary warming, starts `/api/v1/admin/dashboard/home-summary` (`stats` + `recentOrders`, no lifetime `totalRevenue` scan) and `/api/v1/admin/dashboard/activity` as client prefetches, and renders `DashboardSummaryLoading`/chart pending states until real data arrives. Keep `/api/v1/admin/dashboard` and `/api/v1/admin/dashboard/summary` backward-compatible for legacy/full-summary callers; do not make first paint wait on daily activity data again. The chart panel must distinguish `pending`, `success` with empty/all-zero rows, and `error` through `getDashboardActivityPanelState()` so new stores show a settled empty state instead of a permanent skeleton.
- **Admin shell RBAC**: The admin shell uses permission-based RBAC, not legacy `user.role === "admin"`. `apps/admin-v2/src/lib/admin-access.ts` is the pure guard helper; only `/admin/access-denied` remains reachable for authenticated users with no admin permissions. The hot `/admin` guard resolves sessions through `apps/admin-v2/src/lib/admin-session.server.ts`, a narrow D1 lookup for the Better Auth session cookie; do not pull `auth.server.ts`/Better Auth back into `apps/admin-v2/src/lib/auth.fns.ts` for normal route guards. `loadUserPermissions(userId, role, knownIsSuperAdmin)` must keep the known-super-admin fast path before any Cloudflare/database/core RBAC dynamic imports. Non-super-admin RBAC auto-seed uses a versioned six-hour KV marker from `packages/core/src/auth/rbac/auto-seed.ts`; seed definition changes move the marker key, and marker expiry forces periodic D1 truth checks.
- **First-admin setup coordination**: `/api/v1/setup` bypasses normal admin auth only for initial bootstrap, but D1 is the sole authority for concurrency and throttling. `packages/core/src/auth/admin-setup.ts` uses `admin_setup_claims` as a singleton claim ledger and `admin_setup_rate_limits` for the 5/hour/IP throttle. Do not reintroduce Cloudflare KV `get`/`put` locks or KV-only rate limits for setup; KV is eventually consistent and is acceptable only for optional RBAC seed markers after setup.
- **Scanner QR auth coordination**: Scanner QR token minting stays in the admin worker but token consumption is D1-authoritative through `scanner_token_claims` and `packages/core/src/auth/scanner-token-claims.ts`. QR exchange must atomically update an unconsumed, unexpired claim before writing a KV `scanner:session:*` row; never reintroduce KV token `get`/session `put`/token `delete` as the single-use authority. Scanner session payloads should include `claimTokenHash`, and scheduled API maintenance owns bounded cleanup of expired/old consumed claims.
- **Transient D1 overloads**: Use `@scalius/core/utils/transient-d1` for known Cloudflare D1 overload/queue-timeout detection and short read-path retries. Do not duplicate string checks or blindly replay auth/payment/order write paths where the first attempt may have committed.
- **Storefront checkout proxies**: Payment session proxy endpoints unwrap `.data` because the checkout handlers read top-level gateway fields. `apps/storefront/src/pages/api/checkout/create-order.ts` intentionally returns `{ success: true, data: { id } }`, preserves structured `details` on failures, and the browser helper accepts either enveloped or legacy top-level IDs. Normal browser checkout must not request `initialPaymentSession` on order creation; handlers should let the committed order response return quickly, then call the gateway-specific same-origin proxy with only `orderId` plus receipt proof. The create-order proxy still supports explicit `initialPaymentSession: true` as an opt-in path, and if used it must create that session only after the backend order commit succeeds and must send only `orderId` plus receipt proof to Stripe/SSLCommerz/Polar session endpoints; browser handlers should consume the attached session and must not retry the provider endpoint when the committed order response already contains `initialPaymentSessionError`. `apps/storefront/src/pages/api/checkout/validate-cart.ts` is the same-origin proxy for the protected `/api/v1/orders/cart-validation` preflight, uses the shared unsafe-cookie Origin guard, and the browser cart page should use it before COD submit or transfer to `/checkout`. `/buy/{slug}` must also call the storefront `validateCartItems()` helper before writing `quickBuyData` or firing quick-buy analytics; stale product/variant/stock/price/quantity failures redirect to product detail with buyer-safe repair copy.
- **Storefront order creation hot path**: Public checkout order creation requires a stable storefront `checkoutRequestId`, builds the D1 `checkout_attempts` identity, and runs the read-only `resolveExistingCheckoutAttempt()` precheck before mutable checkout policy or IP/phone rate-limit gates. Same-key/same-payload retries replay the committed `201` response or return a pollable `202` while the first request is still processing; same-key/different-payload retries are `409` conflicts. New/non-replay attempts must pass `validateStorefrontCartItems()` before checkout policy, rate limits, or `claimCheckoutAttempt()`; stale product/variant/stock/price issues return one `ValidationError` with `details.itemIssues` and no checkout attempt, KV status, inventory reservation, or payment work is started. Missing, failed, or stale processing attempts must still pass fresh checkout/auth/payment policy and rate limits before `claimCheckoutAttempt()` creates or reclaims the row. Keep the post-claim replay/processing branches for races where another request wins after the read-only lookup. The attempt row reserves the canonical `orderId` and checkout/receipt token so stale or failed attempts can be reclaimed without creating duplicate orders. `createStorefrontOrder()` is the final catalog authority for buyer checkout: it must fresh-read active, non-deleted products, reject inactive/unpublished products, reject variants whose `productId` does not match the submitted item product, resolve variantless simple-product lines only through a real persisted hidden/default SKU whose id is not `default`, reject optioned products without an explicit selected SKU, and reject SKU-less products. Public checkout commits the D1 order synchronously before returning `201`, then runs COD tracking, order-notification enqueue, and availability cache purge work after commit through `executionCtx.waitUntil()` when available. Discount validation endpoints and app prechecks are advisory; D1 `discount_usage` triggers are the commit-time authority for `maxUses` and one-per-customer coupons, and one-per-customer identity is the immutable `discount_customer_redemptions.customer_key` captured from checkout phone proof, not mutable later `orders.customerPhone` edits. Trigger aborts and late reservation failures must map to buyer-friendly `ValidationError`s with structured details and release any reserved stock before returning. KV checkout status/receipt rows are fast recovery hints; `checkout_attempts` is the fallback authority for `/orders/status/:token` and receipt validation. Do not reintroduce `ORDER_INGEST_QUEUE` or browser polling on the normal buyer path; the storefront 202 polling branch exists only for duplicate in-flight or older async responses.
- **Customer auth policy**: Phone number collection is mandatory platform-wide for customer identity, checkout, delivery, fraud checks, SMS OTP, and WhatsApp OTP. Do not add a merchant setting that disables phone collection. Advanced customer auth policy is stored in `settings.customer_auth/policy` with independent OTP channels (`email`, `sms`, `whatsapp`), email collection mode (not collected/optional/required), and a default OTP channel; `siteSettings.authVerificationMethod` remains only a legacy summary/fallback (`email`, `sms_otp`, `whatsapp_otp`, `both`). Storefront customer auth must keep explicit `sign_in` vs `sign_up` intent: sign-in never creates accounts and should direct unknown customers to create an account, while sign-up rejects duplicate phone/email before mutating OTP challenge state. Customer OTP send throttling is D1-backed in `customer_auth_otp_rate_limits`; `/send-otp` must prefer `CF-Connecting-IP`, trust `X-Forwarded-For` only in loopback local development, hash IP buckets, and treat unknown identity as a shared fail-closed bucket. Customer OTP challenges are D1-backed in `customer_auth_otp_challenges` with channel-scoped keys (`cust_otp:{channel}:{identifier}`), an HMAC-SHA256 code hash, pinned phone/email contact fields, 5-minute expiry, 2-minute resend cooldown, and guarded D1 updates that atomically consume correct codes or increment wrong-code attempts; do not move OTP verification or send-rate authority back to KV. New sign-up customers must keep `profile_completion_required_at` set until name, phone, address, city, and zone are complete; `/me`, `/verify-otp`, `/profile`, and `customer-login` event payloads must return the canonical D1 profile projection including address, city/zone/area IDs, resolved labels, `profileComplete`, and `needsProfileCompletion`. `updateCustomerProfile()` must validate active city-zone-area hierarchy through `delivery_locations`, ignore forged display labels, clear `profile_completed_at` only when the required profile is complete, and AuthModal must resume `profile_setup` after reload/close until the customer saves or signs out. AuthModal passive session hydration must not dispatch `customer-login`; emit it only after an active login/profile completion so account/cart surfaces refresh in place without reload loops. Customer OTP channel policy saves and `/send-otp` runtime must fail closed when a selected provider is not locally ready: SMS readiness requires an active supported SMS provider plus decryptable required credentials, and WhatsApp readiness requires token, phone number id, and template. Admin Auth & Access must save SMS provider credentials before enabling an SMS policy and surface provider readiness blockers beside the selected channels.
- **Checkout payment settings**: Public checkout config must honor `settings.category = "payment_methods"` / `enabled_methods` as the outer storefront gateway allowlist, while still requiring each gateway's own settings to be enabled/configured. Stripe is checkout-usable only when the gateway is enabled and secret key, webhook secret, and publishable key are all present; keep `getStripeSettings()` tolerant for existing server-side payment/webhook/refund work, but never let Stripe enter storefront payment methods or public checkout config without `publishableKey`. SSLCommerz is checkout-usable only when provider-enabled with store ID and store password; Polar is checkout-usable only when provider-enabled with access token, product ID, and webhook secret. Admin `GET /payment-methods` must return raw merchant selection (`enabledMethods`, `defaultMethod`), checkout-flow-filtered buyer-visible selection (`activeMethods`, `activeDefaultMethod`), and per-gateway `configured`/`providerEnabled`/`checkoutSelected`/`checkoutVisible` readiness fields. The saved default method must be selected and visible under the current checkout flow; partial payment and `gateways_only` cannot keep COD as the effective default. Public checkout config may expose `activeDefaultMethod` only when that gateway survives allowlist, checkout-flow, and provider readiness, and the storefront should preselect only a rendered active default. The Payment Gateways UI must keep provider credential saves decoupled from checkout visibility saves; never let a provider switch silently mutate the storefront allowlist or let a checkout visibility switch imply provider readiness. Explicit merchant allowlists fail closed when no selected method is usable; only legacy missing `enabled_methods` defaults to COD. Checkout modes must have compatible usable payment methods before checkout-flow or payment-method/gateway saves can commit: `guest_cod_only` requires COD, `gateways_only` requires at least one enabled/configured online gateway, and `all` requires at least one usable method through the payment allowlist/config rules. Partial payment is an online-payment flow: it requires a positive `siteSettings.partialPaymentAmount`, rejects `guest_cod_only`, hides COD from public checkout config, and must have at least one enabled/configured online gateway before checkout-flow or payment-method/gateway saves can commit. Public checkout config assembly must pass `bypassMemoryCache: true` to payment-method/gateway settings reads because the assembled response is already cached by API KV and storefront L1/L2; never let a stale per-isolate gateway cache repopulate fresh public checkout caches. Payment settings writes (`payment-methods`, Stripe, SSLCommerz, Polar) must invalidate the `checkout` cache group in API KV and purge storefront checkout prefixes (`checkout_config`, shipping/location/checkout-language keys). The admin checkout-flow page uses `queryKeys.settings.checkoutFlow()` even though it reads `/settings/auth`; do not merge it back into the Auth & Access query key, and keep payment-gateway saves invalidating both payment-method and checkout-flow query keys. Gateway credential/settings caches are in-memory only; legacy `gw:*` KV cleanup must be best-effort and must not make an already-committed admin save return a false 500. `/api/v1/checkout/config` assembly/read failures must return non-cacheable `503 CHECKOUT_CONFIG_UNAVAILABLE`; storefront checkout must fail closed with a temporary unavailable state instead of guessing COD availability. Storefront checkout must intersect any session-transferred gateway snapshot with the fresh SSR checkout config, discard empty stale intersections in favor of fresh config, and initial online payment-session calls should send only `orderId` plus receipt proof so the API derives `paymentType`/`depositAmount` from the committed order. Low-value carts must fall back to full online payment instead of creating invalid deposit sessions.
- **Settings/content cache invalidation**: Auth/checkout settings writes must invalidate the `checkout` group because public checkout config is cached. Delivery-provider create/update/delete writes are checkout-affecting and must also invalidate the `checkout` group; the admin delivery-provider UI must invalidate `queryKeys.settings.deliveryProviders()` after successful saves/deletes. CSP/security settings writes must invalidate the `layout` group because `/storefront/csp` and storefront `global_security_settings` are cached; any ancillary CSP KV write must use `getOptionalExecutionContext(c)` or another defensive `waitUntil` guard so local/unit contexts without Hono `ExecutionContext` do not fail committed writes. Category and CMS page writes must also invalidate `layout` because storefront fallback navigation is derived from public categories/pages when no explicit header navigation config exists. Committed admin writes should await API KV invalidation, then schedule the storefront purge with `executionCtx.waitUntil()` so a downstream purge/network failure cannot turn an already-committed mutation into a false 500. Use `invalidateCatalogCaches(domain, c, { htmlPaths })` for catalog writes, `invalidateApiAndScheduleStorefrontGroups(groups, c, { htmlPaths })` for settings/content/reference writes that need exact warm paths, and `triggerStorefrontPurgeForPrefixes(..., getOptionalExecutionContext(c))` for widget exact-prefix purges after API pattern invalidation. Product and discount catalog writes warm canonical `/search`; product create/update/delete/restore/permanent-delete and variant writes must also pass affected `/products/{slug}` exact paths before category/listing paths; category catalog writes warm canonical `/search` plus affected `/categories/{slug}` paths; product writes should pass old/new affected category paths when the route already has or can cheaply resolve them. CMS page create/update/bulk-publish/restore/single-restore should pass post-write public `/{slug}` exact paths through `invalidateApiAndScheduleStorefrontGroups(["pages", "layout"], c, { htmlPaths })`; delete/unpublish paths keep freshness invalidation but should not rewarm now-hidden pages. Attribute writes use `invalidateApiAndScheduleStorefrontGroups(["attributes", "products"], c, { htmlPaths: ["/search"] })` because public filter sidebars derive from attribute assignments and search facets. Product catalog writes must invalidate the `attributes` group too because public filter sidebars derive from product category/attribute assignments. Scoped widget purges for product/category/page/collection placements must include exact rendered `htmlPaths`; widget writes must also resolve published CMS pages containing matching widget shortcodes and purge those exact page render/HTML paths. Only homepage/global widget changes should bump the global storefront version and warm the homepage, except shortcode scans that exceed the exact path cap or fail after a committed write may bump globally for freshness. Storefront exact `htmlPaths` are relative-only, canonicalized with the same HTML cache-key rules before dedupe/capping, capped by `MAX_STOREFRONT_EXACT_HTML_PATHS` / `MAX_EXACT_HTML_WARM_PATHS` (`20`), and the storefront warms them in small batches so `waitUntil()` work remains bounded. Stock-changing order/payment/delivery/cron paths should use `invalidateProductAvailabilityCaches()` or preload with `resolveProductAvailabilityCacheSubjects()` before destructive order item writes, then call `invalidateProductAvailabilityCacheSubjects(subjects, c, db)` after commit; this clears exact product detail/search API KV and sends storefront exact data keys plus `/products/${slug}` and any CMS pages containing matching product shortcodes without broad catalog busting. Admin inventory stock-only routes (`/{variantId}/adjust`, `/stock-adjust`, `/stock-set`) should invalidate by affected `variantId` through the product availability helper, not broad catalog groups. Use `invalidateApiAndStorefrontGroups()` or `purgeStorefrontForGroups()` only when the route intentionally needs immediate storefront purge success before responding. `CACHE_TTLS.NONE` is a true cache bypass; do not rely on downstream KV TTL clamping for no-cache routes.
- **Delivery provider activation readiness**: Delivery-provider create and update-as-create saves must default to inactive drafts. Active saves must call `assertDeliveryProviderReadyForActivation()` from `@scalius/core/modules/delivery/provider-readiness` after masked credentials are restored and before DB writes/cache invalidation. Pathao requires `baseUrl`, `clientId`, `clientSecret`, `username`, `password`, and `storeId`; Steadfast requires `baseUrl`, `apiKey`, and `secretKey`. The admin delivery-provider panel must use `getDeliveryProviderActivationBlockers()` for the same missing-field checklist and must not imply that a live courier network test is required to save an inactive draft.
- **Public hero cache variance**: `/api/v1/hero/sliders` may use API KV cache only when callers pass an explicit `type=desktop` or `type=mobile`; it must vary by query. Untyped slider-list requests are user-agent derived and must bypass cache, otherwise mobile and desktop slider payloads can cross-serve. Slider detail reads that are not the list endpoint may stay normally cached.
- **API query cache keys**: API `cacheMiddleware({ varyByQuery: true })` canonicalizes query params before building KV keys. Public routes with schema defaults should pass `queryDefaults` so default query strings do not create duplicate cache entries, but use path-aware defaults when a router has multiple routes with different defaults.
- **API KV cache fences**: `cacheMiddleware()` stores API response bodies under versioned physical keys from `apps/api/src/utils/api-cache-fence.ts`; invalidators must bump the matching fence scope before deleting old keys so in-flight `waitUntil()` miss writes cannot resurrect stale data. Group/pattern invalidation should use `bumpApiCacheFences()` before `deleteCacheByPattern()`, and exact no-query keys such as product details should use `deleteVersionedCacheKeyFamily()` instead of broad prefix deletion. Do not reintroduce `_last_cleared:*`; `/api/v1/cache/last-cleared` reads fence `updatedAt` values.
- **Storefront cache namespace, query keys, and generations**: Storefront KV version keys and exact/data generation keys must use the canonical cache namespace from `CACHE_NAMESPACE`, then `STOREFRONT_URL` hostname, then request hostname. Cache API keys still use the actual request hostname/origin so local/staging hostnames work. HTML cache keys and product-list L2 keys must use `apps/storefront/src/lib/cache-key.ts` plus `apps/storefront/src/lib/product-list-query.ts` so surviving query params are deterministic, repeated render-affecting params collapse to the last surviving value before redirects/cache lookup, tracking/unknown params do not fragment HTML/L2 caches, product client-side `size`/`color` selections do not create separate product HTML entries, and category/search dynamic attribute filters are kept only when the current filter metadata proves the key/value is valid. Exact/data purges should read the old generation before bumping, delete old generation-specific L2/HTML keys best-effort, bump KV generations for freshness, and warm affected HTML paths after canonicalizing them through `@scalius/shared/storefront-cache-path` so noisy query variants do not consume the bounded warm budget. Checkout-only data purges are generation-scoped (`checkout_config`, `global_checkout_language`, `global_shipping_cities`, `global_shipping_methods`, `shipping_zones_`, `shipping_areas_`) and must not bump the shared HTML/global version or warm `/`; unknown prefix-only purges still bump globally as the safe fallback.
- **Storefront deploy warming**: `scripts/deploy.mjs` must warm critical storefront HTML paths after a successful storefront deploy and live `/health` verification. Keep `/`, `/search`, and the bounded public product/category sample in that post-deploy warm set unless a broader bounded warm planner replaces it, because build-id changes intentionally make the previous HTML cache generation cold.
- **Storefront health path**: `/health` must stay a fast middleware bypass. It should not initialize storefront HTML/API cache context, fetch CSP settings, attach the full page CSP, or wait on backend/API/KV calls; deploy smoke checks depend on this route returning quickly.
- **Storefront failure caching**: Cacheable storefront HTML/XML routes must never turn backend read failures into empty successful `200` output. Required SSR data for `/`, `/search`, and `/categories/{slug}` returns a non-cacheable `503` with `X-Storefront-Data-State: unavailable`; legitimate empty product lists are allowed only when the API returned a successful empty payload. Product sitemap, sitemap index, and Meta/Facebook feed routes must return `503 no-store` when `getAllProducts()` returns `null` or a later paginated product read fails, rather than publishing partial/empty XML that the edge can cache.
- **Storefront inline JSON**: Never interpolate raw `JSON.stringify()` into executable inline scripts, JSON-LD script tags, `type="application/json"` data islands, or generated HTML strings that include `<script>`. Use `serializeJsonForInlineScript()` from `apps/storefront/src/lib/safe-json.ts` for any JSON value assigned through `set:html`/`is:inline`, and cover `</script>` breakout payloads with DOM-parser and source-boundary tests.
- **Credentialed API CORS and cookie Origin guards**: Merchant CSP/security domains (`settings.security/csp_allowed_domains` and `security:csp_allowed_domains`) are CSP-only and must never expand credentialed API CORS. `getCorsOriginContext()` allows exact first-party runtime origins plus explicit exact URL origins from credentialed-CORS env vars, with no wildcard env entries, URL credentials, or CSP/KV reads. Loopback wildcard origins (`localhost`, `127.0.0.1`, `[::1]`) are allowed only when a first-party runtime URL is itself loopback; production first-party URLs must not implicitly allow local origins. Unsafe cookie-bearing browser requests to API/admin/customer-auth/order surfaces must pass `cookieOriginGuardMiddleware` or the shared same-origin proxy guard before route/auth/session mutation logic runs; service-binding/server-to-server calls without a browser `Origin` continue through normal route auth.
- **Storefront theme colors**: Admin-configured theme colors must pass through `@scalius/shared/storefront-theme` before storage and before rendering. API saves should reject unknown theme variable keys and unsafe CSS color values; storefront layout/render services must sanitize legacy rows defensively before building any `<style set:html>` block.
- **Storefront DOM text rendering**: Localized, customer-entered, admin-configured, or provider-configured strings must render through `textContent`, text nodes, framework text interpolation, or a sanitizer designed for rich HTML. Do not concatenate those strings into `innerHTML`; keep any `innerHTML` templates static or fully escaped for the target context.
- **Customer post-sale account surface**: Storefront `/account` order history must distinguish loading, unauthenticated, empty, and error states; never render an API/session failure as “No orders yet.” Authenticated customer order reads must remain scoped by `customerId` only and may expose a latest-shipment summary for tracking display, but full payment/refund/notification timelines should go through a dedicated customer order-detail endpoint rather than bloating the capped list response.
- **Payment session proof and policy**: Stripe, SSLCommerz, and Polar session creation must require the order receipt token, validate it against `order_receipt:{token}` before gateway settings/provider calls, and derive callback/success/cancel URLs from trusted runtime config such as `PUBLIC_API_BASE_URL`, not request-body URLs. Payment amount/type/currency/capture behavior must be resolved through `apps/api/src/routes/payment/payment-session-policy.ts` and server settings/order state: reject soft-deleted, cancelled, returned, refunded, partially-refunded, fully-paid, or payment-refunded orders before gateway settings/provider calls; derive omitted `paymentType` from the committed order total plus current partial-payment settings; reject deposits when partial payment is disabled, a requested deposit does not match configured `siteSettings.partialPaymentAmount`, money has already been collected, or an existing plan is not pending with matching amounts; allow `balance` only when `orders.paymentStatus = partial`, `paidAmount > 0`, and `payment_plans.status = deposit_paid` with matching balance; ignore caller currency for session creation; keep public Stripe sessions `manualCapture: false`; and generate a unique SSLCommerz merchant `tran_id` per full/deposit/balance payment attempt while carrying the canonical order id separately in `value_b`/trusted callback `order_id`. Deposit payment-session routes must persist the pending `payment_plans` row before any provider call and fail before provider calls if that write fails. Session creation must fresh-read the outer payment-method allowlist and gateway credentials with `FRESH_GATEWAY_SETTINGS_READ_OPTIONS`, so stale checkout tabs cannot create new external payment sessions after a merchant disables or rotates a gateway. `payment_session_attempts` uses exact proof/return-target replay for created sessions, but live provider calls are D1 single-flight per `(order_id, gateway, payment_type)`; caller retry metadata such as `retryKey` must never fragment the durable identity. `markPaymentSessionAttemptCreated()` is the durable replay point and must stay awaited before returning a provider response; the best-effort `orders.paymentIntentId` recovery hint may be scheduled through a guarded `executionCtx.waitUntil()` only after that attempt row is created, falling back to await in local/test contexts without an execution context.
- **Payment webhook idempotency**: Stripe, SSLCommerz, and Polar webhook routes must fresh-read gateway credentials with `FRESH_GATEWAY_SETTINGS_READ_OPTIONS` before signature/IPN validation, then use the durable `webhook_events` claim-before-side-effect helpers in `apps/api/src/utils/webhook-idempotency.ts` before enqueueing `PAYMENT_EVENTS_QUEUE`. Settings-read failures must be retryable 5xx responses and must not claim an event; invalid signatures/IPN validation failures must not claim an event. Duplicates return success without enqueueing; queue failures mark the event `failed` and return retryable `503`. Fresh `processing` claims dedupe, stale `processing` claims are lease-reclaimable, and `queued`/`processed` claims remain terminal. SSLCommerz IPN must validate `val_id` first, use canonical validated `tran_id`/`val_id`/payment fields, resolve the canonical order id from validated `value_b` or parsed scoped `tran_id`, and resolve payment type against server-side order/payment-plan state instead of trusting form metadata such as `tran_id` or `value_a`.
- **Delivery provider/webhook authority**: Delivery webhook verification must use exactly one active `delivery_providers` row for the courier type; zero or multiple active rows fail closed before signature/IP checks. Webhook shipment lookup must scope by the verified `providerId`, provider type, and provider external identifier, not just `externalId`/`trackingId`. Admin shipment creation and shipment status polling must reject inactive provider IDs before provider API calls or local placeholder/status writes. Every admin shipment status-check endpoint must run the shared API sync path so provider polling also updates `lastChecked`, reconciles buyer-visible order status/inventory, invalidates affected product availability caches, and enqueues order-status notifications when a committed order status change maps to a customer template.
- **Payment refund provider freshness**: Admin refund and auto-return refund dispatch must fresh-read Stripe/SSLCommerz/Polar gateway settings with `FRESH_GATEWAY_SETTINGS_READ_OPTIONS` after the guarded local refund claim and before the external provider call. If fresh settings are missing, disabled, or unreadable, `processRefund()` must release the local refund claim and restore the previous order payment state before surfacing the error.
- **Payment event consumers**: Confirmed gateway events use `processPaymentConfirmed()` and gateway-ID partial unique indexes. `processPaymentConfirmed()` must reject soft-deleted, cancelled, returned, refunded, partially-refunded, fully-paid, or payment-refunded orders before creating/promoting a local payment claim, validate the incoming leg (`full`, `deposit`, or `balance`) against order amount/payment-plan state, and include the payable-state plus matching payment-plan predicates in the final CAS/batch success contract. Deposit plan updates only move pending matching plans to `deposit_paid`; balance plan updates only move `deposit_paid` plans to `completed`, and a zero-row plan update is not a successful payment application. SSLCommerz confirmed-payment idempotency is by canonical `sslcommerzValId`; `sslcommerzTranId` is an attempt/correlation field and cannot be the split-payment uniqueness key. These non-payable late successes are non-retryable queue outcomes for manual reconciliation; they must not mutate order totals/status. Failed gateway events must be idempotent and must not block a later successful event for the same retryable intent. Payment cancellation inventory release goes through `applyInventoryForStatusChange(..., OrderStatus.CANCELLED)`. Polar webhook refunds must CAS-update payment and order status through refund state-machine rules before releasing pre-fulfillment inventory.
- **Secrets and env**: Secrets (`API_TOKEN`, `JWT_SECRET`, `BETTER_AUTH_SECRET`, `PURGE_TOKEN`, `CREDENTIAL_ENCRYPTION_KEY`) come from Cloudflare runtime env/secret bindings. Never read secrets from `import.meta.env`. `pnpm dev:setup` creates `.dev.vars` for all three apps and `.env.development` only for admin/storefront build-time public values; it reuses existing shared local secrets for missing files and fails if existing API/admin/storefront secrets disagree. Use `pnpm dev:setup --env-only` to repair missing or blank local env files without installing dependencies, applying migrations, or creating an admin. Use `pnpm dev:setup --force --env-only` only when intentionally regenerating local env files or repairing shared-secret drift without touching the database/admin state.
- **Credential encryption**: Runtime credential reads use `getEncryptionKey()` with `CREDENTIAL_ENCRYPTION_KEY` preferred and JWT as legacy fallback. New credential writes must use `requireEncryptionKey()` when real provider secrets are submitted, and must fail before DB writes/cache invalidation if the dedicated `CREDENTIAL_ENCRYPTION_KEY` secret is missing. Legacy migration writes and cleanup mutations must receive an explicitly dedicated credential key such as `migrationEncryptionKey`; never pass `getEncryptionKey()` fallback output to a write/migration path. `upsertEncryptedSetting()`, `saveDeliveryProvider()`, `saveFraudProvider()`, Firebase service-account saves, and WhatsApp legacy-token migration are write-strict and must never store plaintext or JWT-fallback encrypted values.
- **Credential-hardening backlog**: The 2026-06-19 provider-secret audit slices for payment gateways, delivery providers, fraud checker, Firebase service accounts/FCM OAuth tokens, and WhatsApp legacy access-token migration are addressed. Reads may still tolerate legacy plaintext/JWT-era rows only for availability; they must not create encrypted rows or clear legacy secret columns unless a dedicated `CREDENTIAL_ENCRYPTION_KEY`-derived write key is present. Browser FCM registration tokens are a separate at-rest design question, not part of provider-secret storage.
- **Transactional email providers**: Cloudflare Email Service is the built-in/default Workers-native email provider through the `EMAIL` `send_email` binding on API/admin Workers. Resend is an optional encrypted-key fallback. Any future paid/external email provider must keep Cloudflare available in runtime selection, admin settings UI, Env/Wrangler bindings, docs, and focused provider-selection tests.
- **Order notification outbox and delivery receipts**: Order-created, admin status/COD, delivery webhook/status-refresh, and provider shipment notifications must go through the durable D1 `order_notification_outbox` before `ORDER_NOTIFICATIONS_QUEUE.send()`. `packages/core/src/modules/notifications/order-notification-outbox.ts` owns dedupe keys, enqueue/processing claims, queue replay, and sent/failure marks; API helpers in `apps/api/src/utils/order-notification-queue.ts` are the route-facing entrypoint. The API cron flushes pending/failed/stale claimed rows every 15 minutes. Channel delivery is fenced by `order_notification_delivery_receipts`: customer email, SMS, Meta WhatsApp order template sends, and admin FCM push create deterministic per-outbox/channel/recipient receipts, claim before provider calls, store provider message IDs where available, skip accepted/skipped receipts on retry, and only let the parent outbox become `sent` after enabled receipt targets are terminal. Admin FCM permanent stale-token responses include exact registration-token codes plus Firebase variants such as `Device unregistered`/`NotRegistered`; treat them as skipped receipts and deactivate the token only after the receipt is terminal, while preserving transient `unknown-error`/server failures as retryable. Resend receives the receipt key as its `Idempotency-Key`; GenNet receives a deterministic `csms_id`; Cloudflare Email, Meta WhatsApp, and FCM store provider receipts but do not expose a first-class upstream idempotency key, so a crash after provider acceptance but before receipt persistence can still duplicate on those providers.
- **Analytics providers**: Cloudflare Web Analytics is the first-class Cloudflare-native page analytics option. `cloudflare_web_analytics` scripts accept a Cloudflare site token or the official beacon snippet, normalize token-only saves to the `static.cloudflareinsights.com/beacon.min.js` snippet, force `usePartytown = false`, and default to `body_end` because the beacon reads browser performance timing. Storefront analytics events also bridge to Cloudflare Zaraz when `window.zaraz` is available; do not make Zaraz a hard dependency or send customer PII through the Zaraz bridge.
- **Local admin credentials**: `pnpm dev:setup` and `pnpm dev:reset` create `admin@local.scalius.test` / `ScaliusLocal123!` by default through the real `/api/v1/setup` endpoint. Override with `--admin-email`, `--admin-password`, `--admin-name`, or `LOCAL_ADMIN_*` env vars. Use `pnpm dev:admin:reset` to reset only local auth/admin credentials.
- **First-admin setup recovery**: If Better Auth inserted the first local user but setup failed before admin promotion, rerunning setup/admin-create should complete that user as the first super-admin instead of returning a 500. The setup KV lock must be released in `finally`.
- **First-admin duplicate recovery**: When `/api/v1/setup` encounters an existing user row before any admin exists, it must verify the submitted password against that existing Better Auth account before promotion. Do not promote a duplicate email unless the requester proves control of the account.
- **2FA schema, method changes, and session rotation**: Better Auth's `twoFactor` table requires the `verified` column in `packages/database/src/schema/auth.ts` and migrations. Admin/API flows that call Better Auth with `returnHeaders: true` must forward replacement `Set-Cookie` headers back to the dashboard domain; API routes append Better Auth cookies to the Hono response and admin server functions propagate API `Set-Cookie` values through `apps/admin-v2/src/lib/api.server.ts`. Changing the preferred admin 2FA method through `/api/v1/admin/auth/2fa/method` must either verify a target-method `code` inside the API route or accept a same-origin Better Auth `sessionToken` proof that matches the current session id, user id, and token. The unverified-2FA API gate exempts only the exact completion/status endpoints: `GET /api/v1/admin/auth/2fa/info`, `POST /api/v1/admin/auth/2fa/verify`, `POST /api/v1/admin/auth/2fa/complete-verification`, and `POST /api/v1/admin/auth/2fa/method`. Admin login 2FA currently uses `trustDevice: false`; both `/api/v1/admin/auth/2fa/verify` and the same-origin Better Auth catch-all verification paths reject `trustDevice: true` until trusted-device bypass is reconciled with the custom `session.twoFactorVerified` policy. Pending Better Auth two-factor method hints are stored only in `sessionStorage` and are cleared after successful verification or sign-out.
- **Dependency freshness and storefront Astro stack**: Use a fresh `pnpm outdated -r` scan before dependency sweeps; package releases move quickly and stale prose should not be treated as a clean report. Storefront is on Astro 7, `@astrojs/cloudflare` 14, `@astrojs/react` 6, Vite 8.1, Wrangler 4.104.0, root/workspace TypeScript 6.0.3, and `@cloudflare/workers-types` 4.20260623.1. Astro `7.0.1`/`7.0.2` were intentionally not adopted on 2026-06-24 because the repo's minimum-release-age policy rejected their fresh transitive `@astrojs/markdown-satteri@0.3.2`; retry after the policy window or after an explicit supply-chain review. TypeScript `7.0.1-rc` was smoke-tested but intentionally not adopted because current `typescript-eslint` peers declare `<6.1.0`; revisit after stable TS7 and ecosystem peer support. Future storefront dependency sweeps must run peer checks, storefront typecheck/build, generated Wrangler deploy dry-run, local Astro dev smoke, and generated Worker smoke before claiming safe. Targeted storefront deploys must verify the latest Worker deployment is serving one version at `100%` and then hit live `/health`. Security overrides belong in `pnpm-workspace.yaml`; for example, `js-yaml` is pinned to 4.2.0 to avoid the transitive OpenAPI-generator advisory. Do not replace ESLint/Prettier with Oxfmt/Oxlint as a blind migration yet: Oxfmt is promising, but official Oxc compatibility still says Astro formatting requires Prettier plugin support that is not available, so keep `prettier-plugin-astro` and ESLint until a focused migration proves Astro/template/plugin parity.
- **Service bindings**: Admin uses `env.API`; storefront uses `env.BACKEND_API`. Production uses bindings. Local dev must fall back to HTTP/Vite proxy; admin treats a localhost `PUBLIC_API_BASE_URL` as a signal to ignore the service binding even if Wrangler exposes it.
- **API local Wrangler config**: `apps/api/package.json` uses `apps/api/wrangler.local.jsonc` for `pnpm --filter @scalius/api dev`. This local config intentionally omits the remote Workers AI binding so API/admin/storefront can boot without a Cloudflare remote proxy session. Production deploy/build still use `apps/api/wrangler.jsonc`.
- **Ports**: Admin 4323, storefront 4322, API 8787. Wrangler inspector ports are also cleaned by `scripts/dev.sh`.
- **RBAC auto-seed**: Permissions/roles auto-seed on first admin access via `apps/admin-v2/src/middleware/rbac.server.ts`. Keep `autoSeedRbacIfNeeded(db, kv)` wired to the admin/API `CACHE` binding so fresh Worker isolates can skip the expensive current-seed batch when the versioned KV marker is still valid.
- **FTS5**: Text search uses SQLite FTS5 helpers in `packages/core/src/search/fts5.ts`.
- **Inventory expiry**: `releaseExpiredReservations()` is for orphaned reservation movements whose order row is missing. Do not use that orphan-reservation sweeper to cancel stale existing orders; existing orders must move inventory through explicit order transition logic so `orders.inventoryAction`, `productVariants`, and movement logs stay consistent. The sweep is intentionally bounded (`limit` default `50`, max `200`) and returns `hasMore` for backlog visibility. Expiry releases use deterministic movement ids inside a D1 `safeBatch()` claim+counter update so overlapping cron runs cannot double-apply the same orphan release; do not replace that with best-effort random movement logging.
- **Abandoned-checkout/stale hosted-payment cleanup**: Existing stale hosted-payment orders are handled only by `archiveStaleIncompleteOrders()` in `@scalius/core/modules/orders/stale-incomplete-orders`. It targets `stripe`/`sslcommerz`/`polar` only, with `status = incomplete`, `paymentStatus IN (unpaid, failed)`, `paidAmount <= 0`, `deletedAt IS NULL`, no active shipment claim, no pending/succeeded `order_payments`, no live `payment_session_attempts` processing lease, and a matching stale cutoff. Orders with `inventoryAction` `reserved` or `deducted` then release/restore through `applyInventoryForStatusChange(db, orderId, OrderStatus.CANCELLED)`. If release fails, roll the cleanup claim back to `incomplete`; do not archive or soft-delete. The final archive must be guarded, pending payment-plan cancellation must be fenced on the order actually being finalized, and the `abandoned_checkouts` archive row is inserted only after finalization. Old/empty abandoned-checkout row pruning lives in the bounded `cleanupStaleAbandonedCheckouts()` scheduled helper; admin `GET /api/v1/admin/abandoned-checkouts` must remain side-effect-free because view-only admins can call it. Never hard-delete stale incomplete orders before inventory release succeeds.
- **Order status inventory reconciliation**: Status, fulfillment, COD, delivery, refund, and return paths must call `applyInventoryForStatusChange()` even when retry sees the order already at the requested/mapped terminal status. Same-status retries must reconcile stale `orders.inventoryAction`; do not return early before inventory repair. Full-refund retries may release pre-fulfillment reservations for already-cancelled orders, but must not auto-restore fulfilled/deducted inventory.
- **Order inventory transition idempotency**: `applyInventoryForStatusChange()` owns `reserved -> deducted/restored`, `deducted -> restored`, and `restored -> reserved` transitions. Deduct/release/restore transitions must use deterministic, movement-generation-based `inventory_movements.id` claims (`transition:*`) plus the variant `stockVersion` CAS update in one D1 `safeBatch()`; exact duplicate claims are idempotent retries, mismatched claims fail closed for manual reconciliation, and the final `orders.inventoryAction` update must be CAS-guarded against the action observed before the stock transition. Do not key these claims on `orders.version`, because unrelated order edits must not make a crash retry double-apply stock. Do not reintroduce the old best-effort `deductMultiple` / `releaseMultiple` / `restoreDeductedMultiple` path for order status reconciliation.
- **COD payment state**: Generic order status updates must never synthesize COD payment state. COD collection must go through `processCodAction(..., { action: "collected" })` / `recordCODCollection()` so `cod_tracking`, `order_payments`, `orders.paymentStatus`, `orders.paidAmount`, and `orders.balanceDue` move together. Generic COD `delivered`/`completed` transitions may proceed only when successful COD payment and collected COD tracking evidence already exist and the order is fully paid.
- **Admin full order edits**: `updateOrder()` must preserve old item context until inventory deltas are safely applied. Positive deltas are reserved/deducted before the order CAS; negative reserved/deducted deltas and terminal release/restore are also applied before replacing `order_items`. Delta failures must reject the edit, not log-and-succeed. Item replacement uses one D1 batch so insert failure cannot strand the order with deleted old items, and pre-write inventory compensation must run when later writes fail.
- **Trash restore inventory policy**: `restoreOrder()` clears `deletedAt`; it is not a hidden order-status transition. Use the shared reservable-status policy from `isStockReservableStatus()`: `incomplete`, `pending`, `processing`, and `confirmed` may re-reserve stock and become `inventoryAction = "reserved"` when variant items exist, or `none` when no variant inventory exists. `cancelled`, `returned`, and `refunded` remain `restored`; shipped/delivered/completed/partially-refunded restored orders must reject until inventory/status are explicitly reconciled. Existing `reserved` and `deducted` actions are allowed only with compatible statuses, and a successful re-reservation must be released if the final restore CAS fails.
- **Shipment creation**: Route-facing provider shipment creation must go through `@scalius/core/modules/orders` fulfillment helpers. Bulk/provider shipment creation owns an order-level `shipmentClaimId` / `shipmentClaimExpiresAt` lock linked to the insert-first `delivery_shipments` row. Admin order/status/COD/fulfillment/edit/delete/refund/payment-session/shipment-refresh paths must reject active claims; cleanup should skip them; queue/webhook paths must retry rather than acknowledge skipped external truth. Provider success with failed local finalization marks the shipment `reconcile_required` and keeps the order claim active for reconciliation. `packages/core/src/modules/delivery/delivery.service.ts#createShipment()` is a low-level primitive and assumes the caller has already performed the durable order claim/transition.
- **Pathao delivery location mappings**: Delivery locations store provider-specific IDs in `externalIds`; the admin location form must expose `externalIds.pathao` as a first-class positive-integer Pathao ID field for city/zone/area instead of asking merchants to edit JSON. Pathao shipment creation must preflight required positive-integer city/zone mappings before provider token work or insert-first shipment placeholder creation; area mapping is optional and included only when present.
- **Manual fulfillment**: `createFulfillmentShipment()` must first acquire a private `shipmentClaimId` without publishing `status = shipped` or `fulfillmentStatus = complete`. The manual `delivery_shipments` insert, scoped `order_items` updates, and final order status/fulfillment/claim-clear update must be committed together in one D1 batch. Fulfillment `itemIds` must be non-empty, unique, and belong to the same order; item updates must include `orderId` in the predicate. If the batch fails before the shipment row exists, clear the private claim and leave inventory untouched.
- **Shipment deletion**: Delete shipments only through `deleteShipmentRecord()`. It must reject active order shipment claims, `reconcile_required` rows, and unresolved expired matching claims; only failed/cancelled stale claimed shipments may clear the old order claim before deletion.
- **Order ingest queue isolation**: `handleOrderIngestBatch()` may batch DB writes for throughput, but stock reservation, DB-write fallback, checkout status, ack/retry, and rollback decisions must remain per order. A rejected/acked message must not be retried because another message in the same queue batch failed. Before reserving, classify active `inventory_movements` for the order: exact active reservations are reused for crash/redelivery replay, missing entries are reserved with `reserveStockBatch(..., { reservationKey: "checkout-ingest:v1" })`, and quantity/stray movement mismatches fail closed for manual reconciliation. After a shared DB batch failure, re-check whether each order row committed before mutating inventory, reuse any reservation already acquired for isolated replay, and only retry after that order's known reservations are confirmed released. If release is uncertain or `releaseMultiple()` reports failure, fail the checkout closed for manual reconciliation instead of redelivering into a second reservation.
- **Batch reservation claims**: `reserveStockBatch()` writes reservation movement claims and variant counter updates in the same D1 `safeBatch()`. Movement inserts are gated by the pre-read `stockVersion`; empty movement/update returns are rolled back and retried. Deterministic `reservationKey`/`movementId` claims are opt-in for replay-sensitive flows, exact duplicate deterministic claims are idempotent success, and mismatches must surface `manualReconciliationRequired` rather than silently reserving again. Keep `inventory_movements.order_id` non-FK-enforced because checkout reservation claims are written before the queued order row commits.
- **Order ingest legacy queue**: `ORDER_INGEST_QUEUE` remains configured for legacy/internal queued ingest handlers, but it is not on the normal storefront checkout path. If async ingest is intentionally reintroduced, status and receipt proof must be D1/checkout-intent authoritative rather than KV-only, and browser polling must not block payment handoff on best-effort queue state.
- **Storefront shipping verification**: Storefront order creation must require a valid active, non-deleted `shippingMethods` row whenever shipping applies and derive `shippingCharge` from that method fee. Browser-provided `shippingCharge` is not authoritative. Free-delivery products may explicitly waive shipping method and charge.
- **Delivery webhook/status semantics**: Pathao and Steadfast webhooks must claim durable lease-backed `webhook_events` before shipment/order side effects. Delivery event identities must include enough provider event/status/update data to allow later status changes for the same shipment; Steadfast delivery-status identities include the raw status. Delivery webhooks and refresh routes should call `updateOrderStatusFromShipment()` even when the shipment row already has the provider status so failed prior attempts can reconcile order inventory; customer notifications still require an actual order status change. Any status emitted by `packages/core/src/modules/delivery/status-mapper.ts` should either be handled explicitly in `updateOrderStatusFromShipment()` or documented as shipment-only. `shipped -> confirmed` is allowed only for failed carrier delivery retry semantics.
- **Generated files**: Do not edit `apps/admin-v2/src/routeTree.gen.ts` or generated API client files by hand.
- **Database migration metadata**: `packages/database/scripts/check-migration-metadata.mjs` enforces SQL/journal/snapshot consistency and documents the allowed manual migrations without snapshots. Update the allowlist only when intentionally adding a manual migration without a Drizzle snapshot.
- **Storefront import boundary**: Storefront may import `@scalius/shared` and `@scalius/api-client`; do not add `@scalius/core` or `@scalius/database` imports there without an architecture decision.
- **Storefront build ID**: `apps/storefront/src/config/build-id.ts` is generated and git-ignored. `apps/storefront/scripts/generate-build-id.js` must run before storefront typecheck/build/deploy. It prefers commit SHA env vars and otherwise hashes stable source/config inputs; do not reintroduce timestamp-only build IDs.
- **Package subpath imports**: `@scalius/database` and `@scalius/shared` do not expose useful root imports. Use subpaths such as `@scalius/database/client`, `@scalius/database/schema`, and `@scalius/shared/utils`.
- **Cloudflare bindings/types**: Wrangler configs are the source of truth for Worker bindings and vars. Keep each app's `env.d.ts`/`hono-env.d.ts` in sync and run `pnpm check:env` after binding/var changes. The check also guards allowed secret-only Env names; update its allowlist only when code really reads a runtime secret or dashboard-only override.

## Admin App Notes

- Local auth routes under `apps/admin-v2/src/routes/api/auth/$.ts` are handled by the admin worker, not the API worker.
- The `/admin` route guard intentionally runs during SSR so unauthenticated requests server-redirect to `/auth/login` before the admin shell HTML is emitted. Do not re-add `ssr: false` to the admin route without replacing that redirect behavior.
- `@/` and `~/` both alias to `apps/admin-v2/src`.
- Admin server functions live in typed domain slices under `apps/admin-v2/src/lib/api-functions/`, with route-facing query wrappers in domain modules under `apps/admin-v2/src/lib/api-query-options/` and route-facing mutation hooks in domain modules under `apps/admin-v2/src/lib/api-mutations/`. Direct proxy/fetch exceptions exist for media uploads, abandoned checkout serialization, FCM token registration, scanner flows, and widget AI streaming.
- Admin read-only API transport must stay bounded by `ADMIN_API_READ_TIMEOUT_MS` in `apps/admin-v2/src/lib/admin-api-timeout.ts`. Keep the timeout active until JSON/text body consumption finishes, and keep `POST`/`PUT`/`PATCH`/`DELETE` plus streaming widget/AI/import operations outside this timeout.
- Add new admin server functions to a domain slice under `apps/admin-v2/src/lib/api-functions/` with normal typechecking; do not recreate a broad legacy barrel.
- Add new admin query options to narrow domain modules under `apps/admin-v2/src/lib/api-query-options/`; do not recreate or import a broad query barrel.
- The admin home route must import dashboard summary/activity query options from `apps/admin-v2/src/lib/api-query-options/dashboard-home.ts`, not the full dashboard query module. Keep `DashboardStats`, `RecentOrders`, and `WelcomeBanner` behind `React.lazy()` boundaries with stable skeleton fallbacks, keep `QuickActions` eager, and keep `useCurrency()` on `api-query-options/currency.ts` so common dashboard/order components do not pull the broad settings graph.
- Route error boundaries should import `RouteErrorComponent` from `apps/admin-v2/src/lib/route-error.tsx`, not `list-helpers.tsx`. `list-helpers.tsx` must stay dependency-free and expose `createListSearchValidator()` plus primitive normalizers instead of Zod schemas because TanStack's generated route tree eagerly imports every route's `validateSearch` code.
- Account settings must use the parent `/admin` route's effective permission context. Do not feed the full RBAC permission catalog into a nested `PermissionProvider`.
- The `/admin` route uses a client-side stale-while-revalidate admin guard cache to keep idle-tab navigation from blocking on repeated auth/RBAC server functions. `ADMIN_ROUTE_CONTEXT_FRESH_MS` is the 1-minute no-work window, `ADMIN_ROUTE_CONTEXT_STALE_MS` is the 4-hour hard cap, and stale-but-not-expired context returns immediately while a single background refresh updates it. Any UI path that changes the current user's profile, 2FA/security state, session, or effective permissions must call `refreshAdminRouteContext(router)` from `apps/admin-v2/src/lib/admin-route-context.ts` before relying on fresh shell/header context; the helper clears the cache and invalidates the router, and the epoch guard prevents older in-flight refreshes from repopulating cleared context. Auth/setup/2FA success flows must navigate with TanStack Router instead of `window.location.href` so the hydrated app is not discarded after authentication.
- Auth routes are first-load hot paths and must stay on first-party forms (`LoginForm`, `ResetPasswordForm`, `SetupForm`, and the 2FA forms), not `AuthCard` or `@daveyplate/better-auth-ui`. Keep Better Auth UI CSS out of global CSS and route code, and do not add Better Auth client plugins such as `adminClient()` to `apps/admin-v2/src/lib/auth-client.ts` unless a route-reachable runtime caller actually needs them.
- Admin auth guards cache only positive "admin exists" reads for a short isolate TTL in `apps/admin-v2/src/lib/auth.fns.ts`; never long-cache false because first-admin setup and local reset recovery must see a newly promoted admin immediately. The guard probe must stay `SELECT 1 ... LIMIT 1`, share one in-flight read per Worker isolate, and remain backed by `user_role_idx` / `user_super_admin_idx`; API setup probes use the same existence shape. Empty-cookie auth routes must not instantiate Better Auth bindings or session reads: `getSessionInfo()` / `redirectIfAuthenticated()` return `null`, `loginPageGuard()` renders login after setup detection, and `adminRouteGuard()` redirects to `/auth/login` before auth/RBAC work. `adminRouteGuard()` passes the fresh Better Auth `user.isSuperAdmin` value into `loadUserPermissions()` so `apps/admin-v2/src/middleware/rbac.server.ts` can skip permission expansion and the duplicate super-admin D1 read for known super-admins after RBAC auto-seed, while still falling back when the auth payload omits the field.
- The admin shell should not eagerly import interaction-only header/sidebar actions or the full core RBAC catalog. Keep `CacheNukeButton`, `NotificationDropdown`, and `UserMenu` behind idle-scheduled lazy boundaries in `AdminHeader` so dropdown/avatar/auth-client code is not part of the first shell graph; keep the sidebar storefront link behind `apps/admin-v2/src/components/admin/layout/StorefrontFooterLink.tsx`, loaded after idle from `AppSidebar`; keep the mobile sidebar Sheet/Dialog code isolated in `apps/admin-v2/src/components/ui/sidebar-mobile-sheet.tsx`, lazy-loaded only after the mobile drawer first opens; and keep storefront URL reads on `apps/admin-v2/src/lib/api-query-options/storefront-url.ts` instead of the broad settings query module. `AdminNav` and `admin-access.ts` use local permission-name mirrors from `apps/admin-v2/src/lib/admin-permissions.ts`; `apps/admin-v2/src/lib/route-graph-boundaries.test.ts` and `admin-access.test.ts` compare those mirrors against core RBAC so shell bundles stay small without silent permission drift.
- Admin QueryClient defaults keep warm data in memory but must not refetch every stale active query on window focus or network reconnect. Keep global `refetchOnWindowFocus: false` and `refetchOnReconnect: false` in `apps/admin-v2/src/lib/admin-query-client.ts`, then opt in only for truly realtime query options. Keep TanStack Router's global `defaultPreload` set to `false`; opt in per link/route only after proving the preload cannot trigger protected admin auth/RBAC/data work while a merchant is idling or hovering. The orders list auto-refresh toggle is visibility-aware: intervals must not refresh while `document.hidden`, and visible-tab resume should call only the active orders list query's `refetch()` through `activeOrderListRefreshRef`; do not broad-invalidate `queryKeys.orders.list()` for idle/timer resume. Keep `orderListFetchingRef`, `orderListRefreshInFlightRef`, and `ORDER_AUTO_REFRESH_DEBOUNCE_MS` guards so resume/timer events skip while a read is already running or just started. Order mutations may still invalidate the broad order-list prefix because they change data across list variants. Order detail may poll order/shipment data with `refetchInterval`, but must inherit the global focus/reconnect defaults so returning to an idle tab does not refetch the old detail page during navigation. The admin shell's nested scroller is `#admin-main-scroll`, registered through TanStack Router `scrollToTopSelectors` with instant scroll restoration. `useAdminNestedScrollRestoration()` snapshots the nested scroller before route loads, resets normal client navigation to top, and restores saved positions only for browser Back/Forward. Do not re-add ad hoc layout-level route-change `scrollTo()` effects.
- URL-search-driven list routes must declare `loaderDeps` and prefetch with `mapParams(deps)` so deep links warm the same query keys components render.
- SSR/admin loaders must await query data that first-render UI consumes. Do not fire-and-forget prefetch query keys that can affect the dehydrated first render; use deterministic `ensureQueryData()`/awaited warming or render a stable placeholder. When a route already renders stable component-level loading states, keep secondary reads as client-only `prefetchQuery()` guarded by `typeof window !== "undefined"` so SSR does not race dehydration and navigation does not wait on nonessential data. Timestamp-only text may use a tight `suppressHydrationWarning` on the timestamp node.
- Products, abandoned checkouts, cache settings, and inventory routes should not block route entry on secondary/self-owned reads. Products waits only on the product list via `warmRouteQuery()`; category options and stats are client-only background prefetches. Abandoned checkouts is a shell route; `AbandonedCheckoutsManager` owns the list query and loading/empty states. Cache settings and inventory render their own loading states and use client-only background prefetches for their default data.
- Mutations that change products, customers, or orders must invalidate `queryKeys.dashboard.all` because the dashboard summary/activity are derived from those domains. Category mutations must invalidate `queryKeys.products.stats()` because product stats include the category count. Keep direct server-function mutation paths aligned with the domain hooks in `apps/admin-v2/src/lib/api-mutations/*`; `apps/admin-v2/src/lib/api.mutations.ts` is only a compatibility re-export barrel and should not be imported by route-reachable code.
- Cache settings at `/admin/settings/cache` warm stats, last-cleared timestamps, and group metadata in the route loader. `CacheManager` must consume the same TanStack Query options instead of directly refetching cache server functions on mount; clear actions live in `apps/admin-v2/src/lib/api-mutations/cache.ts`. UI wording uses `Warms HTML` for directly HTML-affecting groups and `Data prefixes` for groups that primarily clear data prefixes; critical pages are warmed whenever the storefront purge actually bumps the global cache version.
- Checkout settings should preload only the default checkout-flow auth settings. Payment gateway and shipping tab data should lazy-load from their tab components unless the tab mounting behavior changes.
- Self-loading settings routes such as notifications, theme, and hero sliders should not await route-loader data just to warm local form state. Their components own stable loading states and direct fetch/query flows, so route files should remain shell-only unless first render truly consumes dehydrated data.
- Hero-slider settings must keep drag-and-drop interaction code behind the explicit Reorder path. `HeroSliderContainer` lazy-loads `SliderTab`; `SliderTab` renders plain `SlideRow` editors by default and lazy-loads `SortableSlidesEditor` only when reordering, so ordinary `/admin/settings/hero-sliders` loads must not directly import `@dnd-kit/*`, `SortableSlide`, `SlideOverlay`, `createPortal`, or media dialog internals.
- General settings Header/Footer builders should keep hidden-subtab code out of first load. `HeaderBuilder` may import default Branding/Announcement/Contact pieces eagerly, but header social links, header navigation, footer navigation menus, and footer social media should stay behind `React.lazy()` boundaries. Keep `header-builder/index.ts` and `footer-builder/index.ts` as narrow `HeaderBuilder`/`FooterBuilder` plus type barrels so `NavigationBuilder`, social-link sections, and `@dnd-kit`-backed navigation tooling do not get pulled back into the default General Settings route graph.
- Order list first load must not eagerly import interaction-only heavy UI. Keep the date-range picker/calendar, bulk-shipping dialog, delete confirmation dialog, order-items popover, and fraud-check popover behind user-triggered lazy boundaries; verify plain `/admin/orders` load has no initial `DateRangePickerWithPresets`, `react-day-picker`, `BulkShipDialog`, `DeleteOrderDialog`, `OrderItemsPopover`, or `FraudCheckIndicator` module request before moving similar code back into the route graph.
- Server-backed admin tables clear row selection when the hashed TanStack Query key changes. Keep `useServerTable()` using `hashKey(qOpts.queryKey)`, an immediate empty `rowSelectionForCurrentScope` on scope changes, and `deselectIds()` for partial-success bulk actions so stale selections from a prior page/filter/search cannot drive hidden-row mutations. Order list bulk shipping must guard re-entry while `isShipping`, disable bulk controls during shipping/delete pending states, keep the dialog open while a shipment request is in flight, and deselect only successfully shipped orders after partial bulk-shipment success. Mobile order cards must not advertise range selection unless the range behavior is actually implemented.
- Shared admin list URL validators must normalize `page` and `limit` through `createListSearchValidator()` before query construction; direct API list services should also clamp those values before building SQL offsets. Route-specific optional filters should use the primitive normalizers from `apps/admin-v2/src/lib/list-helpers.tsx` instead of route-local Zod. `DataTable` must receive `error`/`onRetry` for server-backed lists whose query can fail, and should render the retryable `Could not load this list` state instead of stale rows/pagination while a list query is errored. Order list corrects out-of-range pages after pagination data is known. COD collection mutations must invalidate `queryKeys.orders.list()` because order-list payment badges derive from COD/payment state.
- Order list date filters are date-only `YYYY-MM-DD` values, not ISO timestamps. Admin serializes picker selections with `formatDateOnly()` and parses them with `parseDateOnly()`; API order filters convert those dates to exact Bangladesh calendar-day UTC bounds before calling core; core `listOrders()` must use those API-provided bounds exactly. Keep quick-select presets on calendar-day starts (`Yesterday`, last 7 days, last 30 days) so they do not inherit the current clock time.
- Order list search uses explicit `sort=relevance` for new non-empty searches. Clearing a relevance search falls back to `updatedAt desc`, and clicking a physical column while searching must keep that physical sort instead of silently reverting to FTS rank. `orders_fts` includes `customer_email`, and the default non-trash list path is covered by `orders_list_updated_at_idx` on `(deleted_at, updated_at)`.
- Order creation at `/admin/orders/new` must block only on the first product-list page. Do not reintroduce loader-side `productQueryOptions()` fanout, `Promise.all()` detail warming, or per-product loops before first render; variants should lazy-load through `productVariantsQueryOptions(product.id)` only after a merchant selects a product.
- Shared admin `DataTable` first load must not eagerly import drag-and-drop libraries. Keep `@dnd-kit/*` and sortable row code inside the lazy `SortableDataTableContent` path, and verify ordinary list routes do not request `SortableDataTableContent` or `sortable.esm` while drag-enabled `/admin/collections?sort=sortOrder&order=asc` still does.
- Discount edit first load must load only the form for the current discount type. Keep `AmountOffProductsContainer`, `AmountOffOrderForm`, and `FreeShippingForm` behind type-specific lazy boundaries in `apps/admin-v2/src/routes/admin/discounts/$discountId/edit.tsx`. For `amount_off_products`, selected product/collection label hydration is optional: the loader may browser-prefetch lightweight `/admin/products/by-ids` and `/admin/collections/by-ids` lookups, but must not await them or suspend route entry. Render stable ID fallbacks and patch labels into selected state when query data arrives. Do not preload collection form-options or broad product/collection pages just to name selected badges. SSR-visible discount date text that uses `formatDateShort()` can differ between the Worker timezone and the browser timezone; use tight `suppressHydrationWarning` spans or deterministic timezone formatting on those timestamp nodes.
- Collection new/edit forms should preload only category options (`/admin/collections/category-options`). Selected product summaries for stored `productIds`/`featuredProductId` are optional client hydration: use ID fallbacks, non-suspense query reads, and browser-only prefetches so an unavailable label endpoint cannot block opening the edit form. Product and featured-product pickers should lazy-load paginated product search on open; do not reintroduce the old upfront 500-product collection form-options dependency.
- Media picker consumers should import `MediaManager` from `apps/admin-v2/src/components/admin/media-manager`, which lazy-loads the heavy dialog implementation only after the trigger is clicked. That barrel is intentionally picker-only and must not export `MediaManagerPage`; the standalone `/admin/media` route lazy-loads `MediaManagerPage` directly. Preserve custom `trigger` support when changing media-picker call sites.
- Visible admin rich-text fields should render `DeferredTiptapEditor` from `apps/admin-v2/src/components/ui/tiptap/DeferredTiptapEditor`. The read-only shell must render saved content through the sanitized `RichContent` preview, while the Tiptap/ProseMirror editor bundle loads only after the user chooses to edit. Do not import `TiptapEditor` directly into route-reachable form chunks unless the immediate editor load is an intentional, measured tradeoff.
- Product form first load must not eagerly import image drag-and-drop or additional-info builder code. Keep `DraggableImageGallery`, `AdditionalInfoManager`, and their `@dnd-kit`/sortable dependencies behind lazy boundaries so `/admin/products/new` starts on lightweight form sections and loads those chunks only when images exist or the `Additional Sections` tab is opened.
- Product variant manager first load must not eagerly import interaction-only tools. Keep `VariantSortModal`, `bulk-generator`, and `utils/csvHelpers` behind user-triggered lazy/dynamic imports, keep `apps/admin-v2/src/components/admin/product-form/variants/index.ts` as a narrow `VariantManager`/types barrel, and verify product edit chunks do not statically import those heavy modules before moving them back into the default route graph.
- Widget create/edit first load must not eagerly import the fullscreen editor, history modal, paste modal, prompt helper, or standalone prompt wrapper. Keep `FullScreenEditor`, `WidgetHistoryModal`, and `WidgetPasteModal` behind `React.lazy()` in `WidgetForm`, and dynamically import `@scalius/core/modules/ai/prompt-helper-v2` plus `widget-form/standalone-prompt` only inside the copy-prompt action.
- Order detail routes must use `prefetchOrderDetailQueries()` from `apps/admin-v2/src/lib/order-detail-prefetch.ts`. The order itself is required first-render data and may redirect when missing; shipments, delivery providers, payment history, currency settings, and COD tracking for COD orders are optional warmups that should log/continue on failure instead of redirecting away from an otherwise valid order. Components that consume optional provider/shipment data must render a stable fallback such as an empty provider or shipment list.
- Keep server-function payloads route-shaped and JSON-serializable. If generated SDK responses include index signatures, strip them with local DTO helpers instead of adding file-level `@ts-nocheck`.

## Storefront Notes

- Runtime Cloudflare env is made request-scoped via `apiContext`/AsyncLocalStorage in `apps/storefront/src/middleware.ts`; API base URL resolution is lazy in `apps/storefront/src/lib/api/client.ts`. Browser API calls require configured `PUBLIC_API_URL`/injected `window.__API_BASE_URL__`; the storefront intentionally does not fall back to a fake same-origin `/api/v1` proxy.
- Storefront listing/detail pages must preserve their cold-cache promise boundaries. Product detail starts layout and product reads together, then chains product-scoped widgets from the product promise so widgets do not wait for layout. Search starts layout, `getAllProducts(productListOptions)`, and search filter metadata together after URL options are built when there are no dynamic attribute params. Category pages start layout, `getProductsByCategory(slug, productListOptions)`, filter metadata, and category-widget reads together on the normal path; they must use the full category object returned by `/api/v1/categories/{slug}/products` and must not issue a separate `getCategoryBySlug()` read on the normal path. Category/search URLs with candidate dynamic attribute params must validate through `resolveProductListQueryState()` after scoped filter metadata is available, redirect noisy/invalid key-value pairs to the canonical URL, then start the product-list fetch immediately with only validated filters while the already-started layout promise continues in parallel. Category/search pagination and sort navigation must use `buildProductListHref()` / `buildProductListPaginationHref()` from the resolved canonical `currentFilters`; never rebuild listing links from raw `Astro.url` or `window.location` because that preserves tracking/default/invalid query noise or drops validated filters. Category/product widgets may chain from the entity promise because they require the entity id. CMS pages must trust `getPageRenderData(slug).widgets` and must not refetch scoped page widgets when the render endpoint returns none. Category/search page URL query `q` must be normalized with `normalizeSearchQuery()`, category `q` must map to API `search`, and `q` must not be forwarded as an attribute filter. Keep `apps/storefront/src/lib/page-data-boundaries.test.ts`, `apps/storefront/src/lib/product-list-query.test.ts`, `apps/api/src/routes/categories-boundaries.test.ts`, and `packages/core/src/modules/products/products.storefront-boundaries.test.ts` aligned with these latency contracts until consolidated render-data endpoints replace the separate calls.
- API `cacheMiddleware` reads KV before route handlers, but cache-miss writes must use `executionCtx.waitUntil()` when available and fall back to awaiting only in local/test contexts without a Hono execution context. Do not move KV writes back onto the response path for public product/category/layout/attribute reads.
- Same-origin customer auth must go through `apps/storefront/src/pages/api/customer-auth/[...path].ts` so `Set-Cookie` works on the storefront domain. Logout clears host-only cookies in `apps/storefront/src/pages/api/auth/logout.ts`.
- Auth/account/checkout/admin settings forms that carry credentials, OTPs, email, phone, tokens, discount codes, payment secrets, tracking secrets, or other sensitive buyer/admin input must never be able to fall back to native `GET` submission. Use explicit `method="post"`/route actions or non-form controls, keep pre-hydration controls disabled when React owns submission, and preserve no-JS/source tests proving sensitive values cannot enter the URL. Public discount validation is POST-only JSON through `postApiV1DiscountsValidate`; do not reintroduce `GET /discounts/validate`, JSON-encoded cart items in query params, or named visible discount-code inputs on the cart form.
- Customer account GETs (`/api/v1/customer-auth/me` and `/api/v1/customer-auth/orders`) return private customer/order data and must keep explicit `Cache-Control: private, no-cache, no-store, must-revalidate` headers. Storefront callers should also request them with `cache: "no-store"`.
- HTML caching is allowlisted in middleware. Cart, checkout, account, health, API routes, private sessions, and payment-sensitive flows must bypass cache. Cacheable HTML URLs should be canonicalized through `buildHtmlCacheBaseUrl()` before adding `cache_v`/`cache_gen`; do not hand-build equivalent Cache API URLs in middleware or purge deletion paths.
- Product, category, collection detail, search, and CMS-page HTML are cacheable through `apps/storefront/src/middleware.ts`. The same Cache API lane also covers explicit generated public XML/XSLT/text assets (`/sitemap.xml`, `/sitemap-*.xml`, `/sitemap.xsl`, `/robots.txt`, and `/api/facebook-feed.xml`) through `isCacheablePublicResponse()`, while JSON/API/private/payment-sensitive responses must stay excluded by path, content type, `Set-Cookie`, and `Cache-Control` guards. Collection detail must stay in `CACHEABLE_PATHS` because collection-scoped widget purges already send exact `/collections/{id}` `htmlPaths` and `htmlPathCacheKeyFromPath()` already assigns exact generations to those paths.
- Storefront `withEdgeCache()` must keep in-flight request dedupe even when KV cache-version lookup is unavailable, but it must not populate L1/L2 storage without a cache version. KV/version failures should be an origin-gentle cache bypass, not a stale-data fallback.
- Scoped widget purges that include exact `htmlPaths` must not bump the global storefront cache version just because they also clear L1/data prefixes. Bump exact generations for `widget_*`, `widgets_scope_*`, `page_render_*_${BUILD_ID}`, and exact entity HTML paths (`/products/*`, `/categories/*`, `/collections/*`, CMS slugs), then warm the supplied paths. Prefix-only purges for unknown/non-generation-scoped prefixes still bump the global version because L2 Cache API cannot delete by prefix.
- `/api/purge-cache` is mutating only through `POST`. `GET` must stay non-mutating and return `405 Allow: POST` except for query-string credential rejection. Purge credentials in query strings are rejected; send `Authorization: Bearer ...` or the configured purge-token header. Full POST purges bump the KV cache version and clear L1. Selective unknown prefix POST purges also bump the KV cache version so L2 Cache API keys move; whenever the global version moves, critical pages must be warmed immediately. Checkout-only selective purges are data-generation scoped and must bump the relevant `g:{namespace}:...` keys without moving `v_{namespace}` or warming `/`. Exact product purges must bump per-key KV generations for `product_slug_*` / `product_variants_*`, then may best-effort delete local Cache API keys and warm affected product paths; do not rely on `cache.delete()` alone for global freshness. Cache warming must use `url.origin` so local/staging ports are preserved.
- CMS shortcode parsing must tolerate sanitized attribute quote entities (`&quot;`, `&#34;`, `&apos;`, and numeric variants) because rich content is sanitized before expansion. Product/widget shortcode resolution should dedupe by shortcode type and id/slug inside a single render so repeated placeholders do not fan out duplicate API reads, while still replacing every occurrence in the rendered HTML.
- Public CMS page visibility is centralized in `packages/core/src/modules/pages/pages.service.ts`; public routes and page sitemaps must enforce `isPublished`, not deleted, and `publishedAt` null or not in the future.
- Public collection detail product resolution is centralized in `packages/core/src/modules/collections/collections.service.ts`. Manual `productIds` and configured `categoryIds` must be mapped back into the merchant-configured order after DB reads because SQL `IN (...)` result order is not stable; missing/inactive products may be skipped, but later visible products should still fill up to `maxProducts`.
- Cart supports COD server POST and multi-gateway sessionStorage redirect to `/checkout`; checkout gateway handlers live in `apps/storefront/src/lib/checkout/`. Preserve cart contents for external-gateway cancel/failure recovery, but clear raw checkout transfer state as soon as the browser no longer needs it. Completed-order redirects clear checkout state and cart; SSLCommerz/Polar external gateway redirects clear checkout session state after order/session creation while leaving cart intact. If SSLCommerz/Polar session setup fails after the order commits, redirect to receipt-token recovery with `result=failed`; Stripe card/session failures should stay on checkout until receipt-page Stripe retry exists, because `/order-success` retry controls are hosted-gateway-only.
- Storefront Meta CAPI browser dispatch may include only browser identifiers such as `_fbp`, `_fbc`, and user agent by default. Do not persist checkout/customer PII in standalone `sessionStorage` analytics keys or auto-enrich broad events (`ViewContent`, `Search`, `AddToCart`, `InitiateCheckout`, `AddPaymentInfo`) from checkout state. Any PII in `userData` must be explicitly supplied for a narrow conversion event and reviewed with the relevant privacy/consent context. Pixel/CAPI paired events must share the same browser event id: Pixel gets Meta's `eventID` option and `/api/v1/meta/events` gets `eventId`. Product and purchase CAPI dispatch must not be gated on `window.fbq`; CAPI still needs to send when Pixel is blocked.
- Meta Graph API integrations must use `META_GRAPH_API_VERSION` from `packages/core/src/integrations/meta/conversions-api.ts`; do not hardcode old versions in CAPI or WhatsApp senders. As of 2026-06-19 this is `v25.0`. Meta CAPI settings are analytics-scoped (`ANALYTICS_VIEW`/`ANALYTICS_EDIT`), new CAPI access-token saves are encrypted with `CREDENTIAL_ENCRYPTION_KEY`, settings writes invalidate the `layout` cache group, public `/api/v1/meta/events` validates `eventSourceUrl` against `STOREFRONT_URL` and rate-limits through API KV when present, and CAPI request logs must redact `user_data`, `test_event_code`, and event-source URL queries before storage/display. `/order-success` purchase tracking is guarded by `claimMetaPurchaseTracking(order.id)` so browser reloads do not replay the same Purchase event. WhatsApp OTP and order notification senders share the same Graph API version and resolve Meta Cloud API credentials through `getWhatsAppCloudApiSettings()`: the access token is stored encrypted at `settings.whatsapp/access_token` with `CREDENTIAL_ENCRYPTION_KEY`, while `site_settings.whatsapp_access_token` is legacy fallback only. WhatsApp legacy migration and cleanup must pass a dedicated `migrationEncryptionKey` from `getCredentialEncryptionKey()`; `getEncryptionKey()` may be used only for tolerant reads. The WhatsApp phone-number ID and auth template name remain non-secret `site_settings` fields. Auth OTP queue payloads must never include Meta tokens, phone-number IDs, or template settings; the API queue consumer resolves credentials at send time. Order template settings live under `settings.notifications` as `whatsapp_order_template_name` plus `whatsapp_order_template_language`. The notification settings API/dashboard reject enabling WhatsApp order channels until Meta credentials exist.
- Cart saved-location prefill goes through `LocationSelector`'s `location-prefill` event API. Do not drive nonexistent native `select[name="city"]`/`select[name="zone"]` elements.
- `/order-success` requires both `orderId` and a receipt `token`; public receipt rendering must use `GET /api/v1/orders/receipt/{id}?token=...` and a minimal receipt DTO. The old public `GET /api/v1/orders/{id}` full-detail route has been removed; do not reintroduce raw order-by-ID customer reads outside admin or customer-session scoped endpoints.
- Storefront order payloads use `CreateOrderPayload = OrderPostRequest` from the generated SDK. Do not reintroduce a hand-maintained local order request interface, and do not call the removed `/discounts/usage` endpoint from the storefront.

## Widget AI / Agents

- Widget AI provider settings live under `packages/core/src/modules/ai/` and API routes under `apps/api/src/routes/admin/settings/ai.ts`, `ai.ts`, `ai-models.ts`, `ai-prompts.ts`, and `ai-context.ts`.
- Long-running/staged widget generation uses `apps/api/src/agents/widget-design-agent.ts` (`WidgetDesignAgent` Durable Object) and `apps/api/src/routes/admin/widget-generation-runs.ts`.
- Admin UI pieces live under `apps/admin-v2/src/components/admin/widgets/widget-form/` and use streaming helpers such as `widget-generation-run-stream.ts`.
- AI provider credentials are encrypted; API credential reads prefer `CREDENTIAL_ENCRYPTION_KEY`, with JWT kept only as a legacy fallback for already-encrypted values.

## Settings Storage Patterns

- **`siteSettings` table**: Singleton row for always-present typed config such as guest checkout, checkout mode, header/footer config JSON, storefront URL, and SEO fields.
- **`settings` table**: Category/key/value store for optional/provider-specific config such as payment gateways, currency, email, Firebase, OpenRouter/widget AI, fraud checker, theme, phone, SMS, notification channels, and business info.

## How-To Recipes

### Add a New Field to an Entity

1. Schema: `packages/database/src/schema/{domain}.ts` — add column.
2. Migration: `pnpm db:generate`.
3. Validation: `packages/core/src/modules/{domain}/{domain}.validation.ts` — update Zod schemas.
4. Service: `packages/core/src/modules/{domain}/{domain}.admin.ts` or equivalent service — update selects/inserts/updates.
5. API route: usually no change if it already passes validated service data through; otherwise update route schemas.
6. Admin form schema/component: update the relevant form files in `apps/admin-v2/src/components/admin/`.
7. Storefront display: update storefront-facing core service/API client/storefront UI as needed.
8. SDK: run `pnpm generate:sdk` if the OpenAPI surface changed.

### Add a New Payment Gateway

See `packages/core/src/modules/payments/README.md`. Current payment reality is mixed: legacy payment modules/gateway registry are active, and the universal provider registry has a Stripe adapter. Coordinate storage, admin settings, webhook routes, queue events, SDK regeneration, and tests.

### Add a Notification Type

1. Add the key and label to `packages/core/src/modules/notifications/notification-types.ts`.
2. Add template/channel handling in `packages/core/src/modules/notifications/notifications.service.ts`.
3. Confirm defaults and validation in `packages/core/src/modules/settings/settings.service.ts` still merge the new type.
4. Queue message typing in `apps/api/src/queue-consumer.ts` should pick it up through `OrderNotificationType`.
5. Trigger by enqueueing `{ type: "order.notification", ... }` to `ORDER_NOTIFICATIONS_QUEUE` from the route/service that owns the state transition.
6. Update admin notification settings UI only if the shared labels/ordering are not sufficient.

### Add a New Admin Settings Tab

1. Component: create `apps/admin-v2/src/components/admin/settings/MySettingsBuilder.tsx`.
2. Route: add sub-route under `apps/admin-v2/src/routes/admin/settings/`.
3. API: create or extend `apps/api/src/routes/admin/settings/*`.
4. Storage: use `siteSettings` for typed singleton fields and `settings` for provider/category config.
5. Add query/mutation wrappers and invalidate the right query keys.

## Queue Bindings

| Queue | Binding | Message Types | Handler |
|-------|---------|---------------|---------|
| `order-ingest` | `ORDER_INGEST_QUEUE` | `order.ingest` | Legacy/internal `handleOrderIngestBatch()` in `packages/core/src/modules/orders/orders.queue.ts`; not the normal public checkout path |
| `payment-events` | `PAYMENT_EVENTS_QUEUE` | `payment.stripe.confirmed/failed/canceled/refunded`, `payment.sslcommerz.confirmed/failed`, `payment.polar.confirmed/failed/refunded` | `processPaymentConfirmed()`, `processPaymentFailed()`, `releaseOrderInventory()`, `processPolarWebhookRefund()` |
| `order-notifications` | `ORDER_NOTIFICATIONS_QUEUE` | `order.notification` | `sendOrderNotificationEmail()` and `sendOrderNotification()` in `notifications.service.ts` |
| `auth-otp` | `AUTH_OTP_QUEUE` | `auth.send_otp` | inline OTP branch in `apps/api/src/queue-consumer.ts` (email, WhatsApp, configured SMS provider) with D1 delivery receipt fencing |

All queues are consumed by `apps/api/src/queue-consumer.ts`. `ORDER_INGEST_QUEUE` uses a batch strategy for legacy/internal ingest; buyer-facing checkout should commit the order synchronously and reserve Queues for post-commit side effects, webhooks, notifications, OTP, and durable background work. Other queues process messages independently with ack/retry.

## Import Conventions

```typescript
// Admin/API package imports
import { getDb } from "@scalius/database/client";
import { products } from "@scalius/database/schema";
import { getCurrencyConfig } from "@scalius/core/modules/settings/settings.service";
import { createAuth } from "@scalius/core/auth";
import { ftsMatch } from "@scalius/core/search";
import { cn } from "@scalius/shared/utils";

// SDK types and generated methods
import type { Product, Category } from "@scalius/api-client/types";
import { getApiV1Products } from "@scalius/api-client/sdk";
import { createClient, createConfig } from "@scalius/api-client/factory";

// Local app aliases
import { SomeAdminComponent } from "@/components/SomeAdminComponent";
import { queryKeys } from "~/lib/query-keys";
```

Storefront should use its configured SDK clients from `apps/storefront/src/lib/api/client.ts`, not the default generated singleton client, when requests need storefront runtime env, retry behavior, auth, or service-binding transport.

## Key URLs (Local Dev)

- Admin UI: `http://localhost:4323/admin`
- Storefront: `http://localhost:4322`
- API: `http://localhost:8787/api/v1/**`
- Swagger UI: `http://localhost:8787/api/v1/docs`
- OpenAPI spec: `http://localhost:8787/api/v1/openapi.json`

## Important File Paths

- API Worker entry: `apps/api/src/worker.ts`
- Hono app entry: `apps/api/src/app.ts`
- API queue consumer: `apps/api/src/queue-consumer.ts`
- API response helpers: `apps/api/src/utils/api-response.ts`
- API error classes: `apps/api/src/utils/api-error.ts`
- Widget Design Agent: `apps/api/src/agents/widget-design-agent.ts`
- Admin Vite config: `apps/admin-v2/vite.config.ts`
- Admin router: `apps/admin-v2/src/router.tsx`
- Admin generated route tree: `apps/admin-v2/src/routeTree.gen.ts`
- Admin server functions: `apps/admin-v2/src/lib/api-functions/`
- Admin API server helper: `apps/admin-v2/src/lib/api.server.ts`
- Admin query options: `apps/admin-v2/src/lib/api-query-options/`
- Admin mutation hooks: `apps/admin-v2/src/lib/api-mutations/`
- Admin mutation compatibility barrel: `apps/admin-v2/src/lib/api.mutations.ts`
- Admin auth server helpers: `apps/admin-v2/src/lib/auth.server.ts`
- Admin auth functions: `apps/admin-v2/src/lib/auth.fns.ts`
- Admin RBAC: `apps/admin-v2/src/middleware/rbac.server.ts`
- Storefront Astro config: `apps/storefront/astro.config.mjs`
- Storefront middleware/cache context: `apps/storefront/src/middleware.ts`
- Storefront API client: `apps/storefront/src/lib/api/client.ts`
- Storefront edge cache: `apps/storefront/src/lib/edge-cache.ts`
- Storefront customer auth proxy: `apps/storefront/src/pages/api/customer-auth/[...path].ts`
- Storefront purge endpoint: `apps/storefront/src/pages/api/purge-cache.ts`
- API Wrangler config: `apps/api/wrangler.jsonc`
- API local Wrangler config: `apps/api/wrangler.local.jsonc`
- Admin Wrangler config: `apps/admin-v2/wrangler.jsonc`
- Storefront Wrangler config: `apps/storefront/wrangler.jsonc`
- Drizzle config: `packages/database/drizzle.config.ts`
- Auth config: `packages/core/src/auth/auth.ts`
- Auth client: `apps/admin-v2/src/lib/auth-client.ts`
- SDK package: `packages/api-client/`
- OpenAPI source artifact: `packages/api-client/openapi.json`
- SDK generated types: `packages/api-client/src/generated/types.gen.ts`
- Customer auth service: `packages/core/src/modules/customers/customer-auth.service.ts`
- DB schema: `packages/database/src/schema/`
- Migrations: `packages/database/migrations/`

## Dependency Graph

```
@scalius/shared          → external utility deps only
@scalius/database        → drizzle-orm
@scalius/core            → @scalius/database, @scalius/shared, better-auth, zod, stripe, etc.
@scalius/api-client      → generated SDK only (dev generator: @hey-api/openapi-ts)
@scalius/api             → @scalius/core, @scalius/database, @scalius/shared, hono
@scalius/admin-v2        → @scalius/core, @scalius/database, @scalius/shared, @scalius/api-client, TanStack Start, React
@scalius/storefront      → @scalius/shared, @scalius/api-client, Astro, React
```

## Production Domains

```
dashboard.scalius.com  → scalius-admin-v2 (Admin Worker)
api.scalius.com        → scalius-api (API Worker)
storefront.scalius.com → scalius-storefront (Storefront Worker)
cloud.scalius.com      → scalius-media R2 bucket/CDN/Image Resizing
```

These mappings are inferred from Worker names and `wrangler.jsonc` vars. Custom-domain/route attachments are managed in Cloudflare and are not declared in this repo.

## Dev Server Notes

- Dev commands use `scripts/dev.sh`.
- Run `pnpm dev:doctor` before debugging vague local failures. It checks env-file presence, shared-secret drift, and local URL values for the expected API/admin/storefront localhost ports. Use `pnpm dev:doctor:api`, `pnpm dev:doctor:admin`, `pnpm dev:doctor:storefront`, or `pnpm dev:doctor:all` after starting the matching local stack.
- The wrapper applies pending local D1 migrations before starting API unless `SCALIUS_SKIP_DEV_MIGRATIONS=1`.
- The wrapper kills stale processes on app ports `8787`, `4322`, `4323`, and inspector ports `9229-9233`.
- Full `pnpm dev` starts API, waits for `/api/v1/setup`, then starts admin and storefront with a small stagger to avoid inspector port conflicts.
- `pnpm dev:api`, `pnpm dev:admin`, and `pnpm dev:storefront` all run through `scripts/dev.sh`, apply pending local D1 migrations unless skipped, and wait for API readiness before starting dependent apps.
- Astro 7 may start storefront dev as a background process in non-interactive agent sessions. `scripts/dev.sh` treats that as expected: it follows `astro dev logs --follow` so storefront logs remain visible, and cleanup runs `astro dev stop` before killing dev ports. For Astro-only debugging, use `pnpm --filter @scalius/storefront exec astro dev --background --host 127.0.0.1 --port 4322`, inspect with `astro dev status` / `astro dev logs`, and stop with `astro dev stop`; public storefront pages still need the API worker on `:8787` for normal data.
- Dev and root Turbo scripts must not assume `pnpm` is on PATH. They resolve the current Corepack/pnpm shim through `SCALIUS_PNPM_BIN`, PATH, the running Node install, or known local pnpm locations, and `pnpm dev:doctor` should report the resolved command. Root Turbo gates (`pnpm lint`, `pnpm typecheck`, `pnpm build`, migration helpers, and SDK generation) run through `scripts/turbo-run.mjs` so Turbo child processes can find a real `pnpm` executable even when Corepack launches via `pnpm.mjs`.
- `scripts/dev.sh` only kills processes on Scalius dev ports by default. Set `SCALIUS_DEV_KILL_ALL_WORKERD=1` for the old all-`workerd` cleanup behavior.
- Set `SCALIUS_WRANGLER_STATE=/tmp/some-path`, or pass `--state /tmp/some-path` to setup/reset/admin helper scripts, to run against a disposable local Wrangler state directory. Script `--state` paths are normalized from the repo root, so absolute paths are safest in handoffs.
- If ports are stuck: `lsof -tiTCP:8787 -iTCP:4322 -iTCP:4323 -iTCP:9229 -iTCP:9230 -iTCP:9231 -iTCP:9232 -iTCP:9233 -sTCP:LISTEN | xargs kill -9`, then kill lingering `workerd` if needed.

## Current Highlights

- Monorepo migration is complete: three Workers apps plus five shared packages.
- API standardization is mostly in place: normal routes use `ok()`/`created()`/`ApiError`; edge routes have documented exceptions.
- Schema is at 51 SQL migrations with 59 table declarations.
- SDK generation is integrated into admin/storefront and should be regenerated after API surface changes.
- Payments include durable webhook idempotency, gateway-payment CAS/atomic updates, refund validation, COD handling, SSLCommerz redirect validation, and Polar refund webhook processing.
- Orders use status state-machine validation, CAS on status/fulfillment paths, queue ingest, and durable notification outbox enqueueing for status transitions.
- Inventory uses `stockVersion`, reservations, restore/release logic, a scheduled orphan-reservation expiry sweep, guarded stale hosted-payment cleanup, and bounded old/empty abandoned-checkout pruning every 15 minutes.
- Delivery uses provider/factory implementations, durable webhook replay protection keyed by provider event identity, `CREDENTIAL_ENCRYPTION_KEY`-required encrypted provider credentials, shipment status CAS, and durable notification outbox enqueueing in active webhook/admin paths.
- Admin order detail now supports own-courier/manual fulfillment for Bangladesh merchants without requiring Pathao/Steadfast setup; manual shipment rows keep provider refresh disabled because there is no external tracker.
- Customer auth uses normalized phone numbers, OTP queues, same-origin storefront cookie proxying, D1 OTP delivery receipts, and SMS/email/WhatsApp delivery paths.
- Storefront uses L1 in-memory + L2 Cache API caching keyed by canonical KV version namespace plus deterministic generated build ID, exact product cache generations, and purge-cache warming.
- Admin widget AI supports provider/model settings, AI context, streaming/staged generation, and a Durable Object agent.

## Perfection Log

- **2026-06-20 transactional email fallback redaction**: Missing Cloudflare Email/Resend no longer logs email HTML/text bodies. `sendEmail()` and the deprecated universal Resend adapter now log only masked from/to metadata, content lengths, provider preference, and `contentLogged: false`, while preserving non-delivery results so OTP queue work remains retryable/fail-closed. This closes the audit path where customer OTPs, admin 2FA codes, reset links, and temporary admin passwords could land in Worker logs during provider misconfiguration. Verified with focused email/queue tests (`23` tests), source-boundary guard against raw body logging, core/API typechecks and lints, root typecheck/lint/test (`226` files / `1445` tests), root build, Worker env/dist-secret guards, source scan, API deploy `185b1a67-b614-4db6-a193-dc58fc2cd0c1`, admin deploy `3be4718f-2feb-4ae0-9933-77c70224d9d1`, live API health `200`, live admin login `200`, and live unauthenticated `/admin` `307`.
- **2026-06-20 admin safe-method cleanup**: `GET /api/v1/admin/abandoned-checkouts` is now a pure list read. The stale hosted-payment archive stays in scheduled maintenance where product-availability cache invalidation already runs, and old/empty abandoned-checkout pruning moved into bounded core helper `cleanupStaleAbandonedCheckouts()` called by the 15-minute API cron. The route summary no longer advertises cleanup, and a source-boundary regression guards the GET handler against `delete`/`update`/`insert`/archive calls returning. Verified with focused cleanup/scheduler/safe-method tests (`7` tests), core/API typechecks and lints, root typecheck/lint/test (`225` files / `1444` tests), root build, Worker env/dist-secret guards, API deploy `cae463cd-6cf3-4add-af00-5b53347d5119`, live API health `200`, live OpenAPI summary `List abandoned checkouts`, and live unauthenticated admin list `401`.
- **2026-06-20 discount redemption concurrency hardening**: Discount validation remains buyer-friendly and advisory, while commit-time redemption is now fully D1-authoritative. The existing `discount_usage_max_uses_guard` trigger remains the global usage cap, migration `0050_discount_customer_redemptions` adds immutable per-phone redemption claims and replaces `discount_usage_one_per_customer_guard` so later admin edits to `orders.customerPhone` cannot reopen one-per-customer coupons, and both synchronous checkout plus the legacy queue fallback map trigger aborts to clean `ValidationError`s after releasing reserved stock. Read-only parallel audits also logged the next verified customer-flow/payment/cache/admin blockers in `audit/REMEDIATION_TRACKER.md`, including P0 partial-payment balance ordering, stale browser payment-type selection, checkout button recovery, auth/profile completion, refund allocation, and category/search pagination/filter gaps. Verified with focused core/API tests, database/core/API typechecks, core/API lints, migration metadata guard, local D1 migration/schema proof, API dry-run build, Worker env/dist-secret guards, remote D1 migration/schema proof, root Vitest (`215` files / `1382` tests), API deploy `39af927b-019c-4f19-8bc9-ec56d73d8c8e`, live API health `200`, live docs `200`, live OpenAPI `259` paths with `/api/v1/orders`, and deployment-list proof at `100%`.
- **2026-06-20 checkout catalog availability hardening**: `createStorefrontOrder()` now filters public checkout product reads by `products.isActive = true` as well as `deletedAt IS NULL`, keeps an explicit returned-row active guard for testable fail-closed behavior, and rejects submitted variants whose `variant.productId` differs from the submitted `item.productId`. Stale carts and direct API payloads can no longer order unpublished products or mix an active product with another product's variant before checkout-status/receipt KV proof or order commit. Verified with focused core order-builder tests (`11` tests), focused API order-route tests (`20` tests), core/API typechecks and lints, root typecheck/lint/test (`214` files / `1376` tests), root build, Worker env/dist-secret/migration guards, local API health/OpenAPI smoke, API deploy `a31d68f0-5a77-443b-bdca-4c46494cbdab`, live API health/OpenAPI smoke (`259` paths), and deployment-list proof at `100%`.
- **2026-06-20 payment-settings readiness and checkout UX hardening**: SSLCommerz and Polar now have checkout-readiness helpers matching Stripe's fail-closed model; Polar requires a webhook secret before checkout publication, and the gateway registry no longer exposes unusable SSLCommerz/Polar settings to checkout callers. Admin `GET /settings/payment-methods` returns raw merchant preferences separately from effective active/default methods plus `providerEnabled`, `checkoutSelected`, `checkoutVisible`, `usable`, `missingFields`, and `blockedReason`; preference saves preflight gateway readiness and persist `enabled_methods`/`default_method` through one D1 `safeBatch()`. The Payment Gateways UI separates Provider enabled from Show at checkout, avoids misleading environment badges before credentials are loaded, and renders blocked/hidden-by-flow states. Checkout Flow copy now frames partial payment as online advance payment, says COD is hidden, explains low-cart full-online behavior, and labels guest-disabled COD-only mode as authenticated COD-only. Verified with focused core/API tests, affected package typechecks/lints, SDK regeneration, root typecheck/lint/test (`214` files / `1372` tests), root build, Worker env/dist-secret/migration guards, `git diff --check`, API deploy `238ae4cf-59f9-44e4-b82c-d80cb1a49d8b`, admin deploy `82de08eb-5695-4a9b-a278-5cb60f1c2c31`, live OpenAPI schema smoke, deployed admin chunk markers, and live Checkout Flow/Payment Gateways browser smoke.
- **2026-06-20 checkout replay-before-policy hardening**: `POST /api/v1/orders` now builds the checkout attempt identity and calls read-only `resolveExistingCheckoutAttempt()` before guest/auth/payment policy and IP/phone rate limits, so lost-response retries for committed orders replay `201` and active duplicates return `202` even after merchant settings or throttle state change. New, failed, or stale attempts still run fresh policy/rate checks before `claimCheckoutAttempt()` can insert or reclaim a row, and the route keeps post-claim replay/processing handling for races. Verified with focused core checkout-attempt tests (`7` tests), focused API order-create tests (`19` tests), affected core/API typechecks and lints, root typecheck/lint/test (`214` files / `1367` tests), root build, Worker env/dist-secret/migration guards, `git diff --check`, API deploy `3d71df1e-dc16-41d4-b984-51139bf9feb9`, live API health `200`, live OpenAPI `/api/v1/orders` contract smoke (`259` paths with `201/202/409/429/503`), and deployment-list proof at `100%`. Production order mutation was intentionally avoided until `OPS-006` provides a staging/disposable-order policy.
- **2026-06-20 stale hosted-payment cleanup**: `archiveStaleIncompleteOrders()` now lives in core and is called from API scheduled maintenance every 15 minutes with a 60-minute grace period and `25`-order limit. It targets only Stripe/SSLCommerz/Polar `incomplete` orders with `paymentStatus` `unpaid`/`failed`, `paidAmount <= 0`, no soft delete, no active shipment claim, no pending/succeeded `order_payments`, and no live `payment_session_attempts` processing lease. Each order wins a guarded cancelled claim before inventory release, rolls back if release fails, finalizes through D1 `safeBatch()`, fences pending payment-plan cancellation on the finalized order version, archives only after finalization, and invalidates product availability caches for archived order ids. Verified with full core/API package tests, root typecheck/lint/test/build/safety gates, API deploy `91f6bf32-1dfb-4ada-b7de-c46ffccff135`, live API health/OpenAPI smoke, deployment-list proof, and read-only remote D1 stale-candidate query returning `0` with `changed_db=false`. Buyer-facing retry/pay-balance/cancel UX remains tracked under `CHECKOUT-011`.
- **2026-06-20 hosted-payment receipt recovery**: SSLCommerz and Polar hosted-payment success/fail/cancel callback URLs now carry the private `receipt_token`, `payment_type`, and deposit amount when applicable; SSLCommerz fail/cancel and Polar cancel redirect buyers back to `/order-success` with `result=failed|cancelled` instead of dropping them into cart/checkout without proof. `/order-success` shows a hosted `Retry payment` action for SSLCommerz/Polar payment-issue receipts or gateway fail/cancel returns, preserves cart/session/purchase-tracking finalization gates for unpaid orders, and now reuses the durable payment-session identity instead of sending random per-click `retryKey` values that bypass replay. Storefront hosted-session proxies no longer auto-retry POSTs, and Stripe session proxy retries were later disabled under the provider-deadline slice. Verified with focused API payment-session tests, storefront order-success retry/side-effect tests, SDK regeneration from live local OpenAPI (`259` paths), root typecheck/lint/test/build/safety gates, API deploy `76de3e9a-c8b5-4eaa-b155-6d81ebbaafe4`, storefront deploy `af8ea9b6-b52f-464a-9730-d57b551c531a`, live OpenAPI retry-key smoke, live gateway cancel redirect smokes, and storefront recovery page smoke showing `data-order-finalize="false"`. Remaining customer/admin recovery work is tracked under `CUST-005`, `PAY-014`, and `ADMIN-018`.
- **2026-06-20 payment-session provider deadlines**: Stripe, SSLCommerz, and Polar session creation now runs under bounded provider deadlines below the storefront proxy timeout. Stripe receives an SDK request timeout with network retries disabled, SSLCommerz raw fetch and Polar SDK calls receive abort signals, timeout-shaped provider results mark the D1 payment-session attempt failed and return retryable `503`, and storefront session proxies preserve backend status while mapping proxy aborts to retryable buyer copy. Receipt-page hosted retries no longer send random `retryKey` values, so normal retries reuse durable replay identity. Verified with focused API payment-session/deadline tests, focused storefront retry side-effect test, affected and root typecheck/lint/test/build/safety gates, API deploy `017c24fa-2d69-42c8-8ddb-39635a955b8f`, storefront deploy `f908095c-ac04-47e5-9302-e122f10445c5`, live API health/payment-proof smokes, live storefront `/` and `/cart` smokes, and live order-success recovery marker proof.
- **2026-06-24 payment-session same-order single-flight**: D1 migration `0063_payment_session_live_singleflight` adds a partial unique index on live `payment_session_attempts` for `(order_id, gateway, payment_type)` after deduplicating historical live rows. Fresh same-order live attempts now return the normal retryable `202 processing` response even when the exact proof/return-target key differs, stale same-order live attempts are reclaimed into the new canonical identity, stale competing live rows are cleared before reclaiming an old exact failed attempt, and created replay remains exact to the original proof/return-target context. SSLCommerz/Polar public session schemas no longer expose caller retry metadata, and retry metadata is not part of hosted session identity. Verified with focused core/API payment tests, regenerated SDK/OpenAPI (`261` paths / `359` operations), root typecheck/lint/test/build/safety gates, remote migration/index/invariant checks, API deploy `09465ea3-9253-4f69-8327-200f77ead981`, live health/readiness, live OpenAPI no-`retryKey`/`202` smoke for Stripe/SSLCommerz/Polar, and live invalid Stripe proof rejection `400`.
- **2026-06-20 Stripe checkout-readiness hardening**: Stripe can no longer be selected or published to buyers with only server-side secrets. `getActivePaymentMethods()`, the gateway registry, and public checkout config require enabled Stripe settings plus secret key, webhook secret, and publishable key before Stripe is checkout-usable; admin payment-method saves and Stripe enabled saves reject missing effective fields before DB writes/cache invalidation; `/settings/payment-methods` now exposes `usable`, `missingFields`, and `blockedReason`; and the Payment Gateways UI shows blocked selected methods with the reason instead of a misleading configured state. Verified with focused core/API payment tests, affected package typechecks/lints, SDK regeneration from local API OpenAPI (`259` paths), root typecheck/lint/test (`207` files / `1309` tests), root build, Worker env/dist-secret/migration guards, `git diff --check`, full deploy to API `d051b91d-1645-4700-835a-8241baadf02a`, admin `f8a1ef0d-6322-41ed-8f6f-65ac06d35296`, storefront `2fc4b4b7-a8cb-4286-bd5b-bcef920e3512`, live API health/OpenAPI schema smoke, live checkout config smoke proving any published Stripe gateway has a non-empty publishable key, live storefront `/cart`, live dashboard checkout redirect, and deployed Payment Gateways chunk marker smoke.
- **2026-06-20 checkout-flow settings hardening**: Partial payment is now enforced as an online-payment-only flow across admin settings, public checkout config, and storefront payment handlers. The shared checkout-flow validator rejects partial payment without a positive advance amount, with `guest_cod_only`, or without an enabled/configured online gateway; gateway saves cannot disable the last usable online gateway while partial payment is active; explicit merchant payment allowlists fail closed instead of silently falling back to COD; checkout-flow settings use a separate React Query key from Auth & Access; storefront checkout intersects transferred gateway snapshots with fresh SSR config; and low-value carts derive `paymentType: "full"` instead of sending invalid deposits. Verified with focused checkout/payment/settings tests, root typecheck/lint/test (`201` files / `1249` tests), root build, Worker env/dist-secret/migration guards, commit `d38db484`, API deploy `3d279336-3c87-476e-b44a-4d28b7177572`, admin deploy `5adfc6e7-8b8c-45bb-9115-4a8179a60be9`, storefront deploy `070035f3-46ef-4024-836b-5f4619a98d2a`, live checkout config showing partial payment `150` with only Stripe/SSLCommerz/Polar gateways, live cart/checkout HTTP smoke, and deployed storefront/admin asset marker checks.
- **2026-06-20 customer post-sale account tracking/error-state hardening**: Authenticated customer order history now returns a latest shipment/provider summary per order from `customers.service.ts`, the API/OpenAPI/SDK contract documents `latestShipment`, and storefront `/account` renders shipment/tracking state plus a real order-history failure panel instead of collapsing read failures into the empty “No orders yet” state. The account page initialization is idempotent across initial loads and Astro page transitions, avoids nested `<main>` markup, and `OrderSuccessButtons` now uses the readable `cs_auth=1` mirror cookie for the “View Your Orders” CTA instead of checking the HttpOnly customer token. Verified with the focused customer-auth cache route test, core/API/storefront/api-client typechecks, API/core/storefront lint, SDK regeneration from a live local API, root typecheck/lint/test (`198` files / `1191` tests), root build, Worker env/dist-secret/migration guards, local API/storefront HTTP and browser smoke, API deploy `2b57cee5-fdbe-4ad8-aaf5-7eae173851aa`, storefront deploy `5c722c8c-5514-4f84-86f1-b1fc3e06e529`, live API health/OpenAPI contract smoke, and live `/account` desktop/mobile browser smoke with one `<main>`, no horizontal overflow at `390px`, and no captured console warnings/errors. This does not make post-sale perfect: `ORDER-018`, `ORDER-019`, `CUST-002`, and `OPS-006` remain blockers for refund/COD race safety, customer order timelines/recovery, and reproducible post-sale smoke fixtures; admin detail resilience from `ADMIN-015` was resolved on 2026-06-22.
- **2026-06-20 storefront inline JSON and theme-color hardening**: Storefront executable inline scripts, JSON-LD blocks, `type="application/json"` data islands, and quick-buy generated script HTML now use `serializeJsonForInlineScript()` instead of raw `JSON.stringify()` interpolation. Admin theme-color saves reject unknown theme variable keys and unsafe CSS color values through `@scalius/shared/storefront-theme`, while core/storefront reads sanitize legacy rows before building the layout style block. Verified with focused shared/storefront/API tests (`17` tests), shared/storefront/API/core typechecks, API/storefront lints, SDK generation from a live local API, root `pnpm typecheck`, root lint, root Vitest (`198` files / `1190` tests), root build, `pnpm check:env`, `pnpm check:dist-secrets`, migration metadata check, source-boundary scans, local API/storefront HTTP and browser smoke, API deploy `11c603b1-443d-425b-90ac-faefc3a4fbb9`, storefront deploy `9ff70280-a464-46cd-9f87-009afba8d771`, live API health/OpenAPI smoke, and live storefront `/search`, `/products/fish`, and `/categories/drinks` browser smoke with parseable script payloads and no captured console errors. The scratch audit files `alpha-audit.md` and `audit/independent-audit.md` were reduced to verified tracker entries and deleted.
- **2026-06-20 queued checkout reservation idempotency**: `reserveStockBatch()` now writes strict reservation movement claims and variant counter CAS updates in one D1 `safeBatch()`, with movement inserts gated by the pre-read `stockVersion` so stale variants create neither phantom movements nor counter changes. Replay-sensitive callers can pass `reservationKey`; queued checkout uses `checkout-ingest:v1` with release-count generations, so active crash/redelivery duplicates become idempotent while a retry after confirmed release can reserve with a fresh claim. `handleOrderIngestBatch()` now classifies existing `inventory_movements` before reserving: exact active reservations are reused, missing entries reserve normally, and quantity/stray reservation mismatches fail closed for manual reconciliation. `inventory_movements.order_id` stays intentionally non-FK-enforced in the TypeScript schema because checkout reservation claims are written before the queued order row commits; the remote D1 table also reports no enforced FKs. Verified with read-only subagent audits, focused order-queue/batch-reservation Vitest (`21` tests), expanded order/inventory Vitest (`3` files / `51` tests), core/database typechecks, core lint, migration metadata check, root `pnpm typecheck`, root lint, root Vitest (`197` files / `1180` tests), `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, local API setup/docs/OpenAPI/health smoke, API deploy `0ad23d64-3c6d-4fd6-9905-59033ea7c44d`, live API health/docs/OpenAPI/setup smoke, and remote D1 `PRAGMA foreign_key_list(inventory_movements)` returning no enforced FKs.
- **2026-06-20 notification settings bundle narrowing**: `NotificationChannelsBuilder` no longer imports the broad `@scalius/core/modules/notifications` barrel, which re-exported backend notification services, delivery receipts, and outbox helpers into the browser chunk. It imports only `notification-types`, and `route-graph-boundaries.test.ts` now scans admin components and routes to keep exact broad-barrel imports out of the client route graph. The production client chunk is `NotificationChannelsBuilder-DvkRSoGT.js` at `7,484` bytes and has no `drizzle:entityKind`, `order_notification_outbox`, `getDb`, or `@scalius/database` markers. Verified with a read-only subagent audit, focused route-graph Vitest (`22` tests), admin TypeScript and ESLint, admin production build, root `pnpm typecheck`, root lint, root Vitest (`197` files / `1174` tests), `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, local API/admin sign-in/settings/notification-channel HTTP smoke, admin deploy `36d5d85b-5c3c-4fc5-9a74-fc08ec0cc8aa`, live deployed chunk marker smoke, and live demo sign-in/settings/notification-channel smoke. Keep admin notification UI imports pointed at `@scalius/core/modules/notifications/notification-types` unless the UI genuinely needs server-only notification behavior.
- **2026-06-20 bounded inventory expiry sweep**: `releaseExpiredReservations()` is now a bounded orphan-reservation cleanup path instead of an unbounded cron sweep. It processes `50` groups by default (`200` max), returns/logs `hasMore`, rechecks order existence and terminal movements after candidate selection, and claims each expiry release with deterministic movement id `expiry_release:{orderId}:{variantId}` plus the reserved-stock decrement in D1 `safeBatch()`. The API scheduled handler passes the explicit limit and logs `limit`/`hasMore`. Verified with focused core expiry tests (`6` tests), core/API typechecks and lints, API dry-run build, root `pnpm typecheck`, root lint, root Vitest (`197` files / `1173` tests), `pnpm check:env`, `pnpm check:dist-secrets`, `git diff --check`, local API health/docs/scheduled smoke showing `found=0, released=0, limit=50, hasMore=false`, API deploy `2c99229c-fc5d-4411-8653-6246cadbefab`, live API health/docs/setup smoke, and current Cloudflare Workers best-practice review. Parallel audits opened follow-ups for queued checkout reservation idempotency, inventory transition concurrency, notification-channel bundle leakage, and category/search dynamic-filter cold-path parallelization in `audit/REMEDIATION_TRACKER.md`.
- **2026-06-20 admin hero-slider interaction chunk slimming**: `/admin/settings/hero-sliders` no longer pulls drag-and-drop or media-page implementation code into the normal route/component path. `HeroSliderContainer` lazy-loads `SliderTab`; `SliderTab` renders plain `SlideRow` editors by default and loads `SortableSlidesEditor` only after the Reorder action, where `@dnd-kit/*`, `SortableSlide`, `SlideOverlay`, and `createPortal` now live. The media-manager barrel is picker-only and no longer re-exports `MediaManagerPage`, so picker consumers cannot accidentally couple to the standalone media route. Verified with Galileo/Rawls read-only audits, focused route-graph Vitest (`1` file / `21` tests), admin typecheck/lint/build, direct client/server chunk import scans (`hero-sliders-BUpu0FwN.js` imports `SliderTab-D-5iRhRc.js`; `SortableSlidesEditor-BIzW3Pj0.js` owns `sortable.esm`; media picker imports stay separate from `MediaManagerPage-B24_PLi-.js`), root `pnpm typecheck`, root lint, root Vitest (`197` files / `1170` tests), `pnpm check:env`, `pnpm check:dist-secrets`, `pnpm run deploy:admin -- --dry-run`, local `pnpm dev:admin` smoke (`/auth/login` `200`, sign-in `200`, `/admin/settings/hero-sliders` `200`, hero-slider API `200`), admin deploy `dfd511fe-ec98-4ea5-8bd7-ec81f04799b4`, live authenticated route/API smoke (`/auth/login` `200`, anonymous hero-slider route `307`, sign-in `200`, authenticated route `200`, API `200`), and live SHA-256 matches for `hero-sliders-BUpu0FwN.js`, `SliderTab-D-5iRhRc.js`, `SortableSlidesEditor-BIzW3Pj0.js`, `SlideRow-GvsQFUXr.js`, `media-manager-DcOz5ZHs.js`, `MediaManager-BkVwzaIM.js`, and `MediaManagerPage-B24_PLi-.js`.
- **2026-06-19 admin shell interaction chunk slimming**: `AdminHeader` now defers `UserMenu` behind the same idle-scheduled lazy boundary as the cache/notification actions, preserving a stable initials/name fallback while keeping dropdown/avatar/auth-client code out of the first shell graph. `apps/admin-v2/src/components/ui/sidebar.tsx` no longer imports the Sheet/Dialog stack directly; the mobile drawer loads `sidebar-mobile-sheet.tsx` only after the drawer first opens, then keeps it mounted for smooth close/reopen behavior. `route-graph-boundaries.test.ts` now guards these boundaries. Production build artifact scan showed `UserMenu-ka83BRvS.js` (`3101` bytes client) and `sidebar-mobile-sheet-q9mWc3ez.js` (`2696` bytes client) as separate lazy chunks, with no direct static `UserMenu`, `sidebar-mobile-sheet`, `dropdown-menu`, `avatar`, or `sheet` import from admin/sidebar shell chunks. Verified with focused Vitest (`1` file / `20` tests), admin typecheck/lint/build, admin deploy dry-run including root typecheck, root lint, root Vitest (`197` files / `1169` tests), `pnpm check:env`, `pnpm check:dist-secrets`, local `pnpm dev:admin` smoke (`/auth/login` `200`, anonymous `/admin` `307`, same-origin sign-in `200`, authenticated `/admin` `200`, `/admin/orders` `200`), admin deploy `24d63582-4471-4a1a-88ca-870e4b6e60f1`, live dashboard smoke (`/auth/login` `200`, anonymous `/admin` `307`, demo sign-in `200`, authenticated `/admin` `200`, `/admin/orders` `200`), and live chunk SHA-256 matches for `UserMenu-ka83BRvS.js` and `sidebar-mobile-sheet-q9mWc3ez.js`.
- **2026-06-19 admin shell footer/RBAC graph slimming**: The authenticated admin shell no longer starts a storefront URL server-function read just to render the sidebar footer. `AppSidebar` renders a stable disabled footer fallback, waits for idle, then lazy-loads `StorefrontFooterLink`, which owns `storefrontUrlQueryOptions()` and the external-link icon. `AdminNav` and `admin-access.ts` no longer import core RBAC modules at runtime; both use the small local `apps/admin-v2/src/lib/admin-permissions.ts` mirror, while `route-graph-boundaries.test.ts` and `admin-access.test.ts` compare the local names/page map against core RBAC to catch drift. Production manifest inspection after build showed the `/admin` layout component dynamically imports `StorefrontFooterLink`, the static admin layout closure is `732.2 KiB`, and its static dependency walk contains no `permissions`, `storefront-url`, or `query-keys` assets. Verified with Hilbert/Erdos read-only audits, focused Vitest (`2` files / `29` tests), admin typecheck/lint/build, full monorepo typecheck, full lint, full root Vitest (`195` files / `1145` tests), Worker env guard, dist-secret check, local `pnpm dev:admin` smoke (anonymous `/admin` `307`, sign-in `200`, `/admin` `200` around `0.80s`, canonicalized `/admin/orders` follow `200` around `1.02s`, home-summary `200` around `0.04s`), in-app browser smoke confirming the footer starts disabled then becomes a `_blank` `Visit Storefront` link after idle with no console errors, admin deploy `9f95d3fd-9f7e-4551-9435-81d74073bdf7`, live `/auth/login` `200`, live unauthenticated `/admin` `307 Location: /auth/login`, and live lazy footer asset `/assets/StorefrontFooterLink-DO0TNjhL.js` `200`. Live authenticated smoke was not run because no live credential env vars were available in this compacted runtime and the local default credential correctly returned `401`; keep the next admin-runtime slice focused on lazy-loading `UserMenu` dropdown/avatar interaction code. Erdos also logged the next storefront cache slice: product writes should include exact `/products/{slug}` rewarm paths, CMS page writes should warm edited page paths, and custom-domain warming should be checked against Cache API host variance.
- **2026-06-19 admin guard cold-runtime hardening**: The authenticated `/admin` route guard no longer imports Better Auth on the hot path. `apps/admin-v2/src/lib/admin-session.server.ts` verifies the Better Auth session cookie through a narrow direct D1 query with expiry/ban predicates, `auth.fns.ts` uses that helper for session/login/admin guards, `UserMenu` lazy-loads the auth client only on sign-out, and the admin toaster is deferred behind `DeferredToaster`. Admin route `validateSearch` handlers were moved from Zod schemas to dependency-free `createListSearchValidator()`/primitive normalizers so TanStack's eager route tree does not pull Zod into ordinary route startup. RBAC now returns known super-admin context before Cloudflare/database/core imports, and non-super-admin auto-seed writes a versioned six-hour `CACHE` KV marker so fresh isolates avoid the expensive seed-current D1 batch after one confirmed seed. Boundary tests now guard against Better Auth in `auth.fns`, Zod in eager route validators, and static DB/core RBAC imports in `rbac.server`. Final production bundle inspection: server entry static closure `841.6 KiB`, router static closure `552.1 KiB` with no Zod/Better Auth, admin home route closure `356.3 KiB`, actual auth server-function closure `23.6 KiB`, and RBAC static closure `1.1 KiB`. Verified with focused Vitest (`6` files / `57` tests), package typechecks for core/API/admin, full `pnpm lint`, full `pnpm typecheck`, full root Vitest (`195` files / `1143` tests), full `pnpm build`, `pnpm check:env`, repeated `pnpm check:dist-secrets`, local `pnpm dev:admin` smoke (`/auth/login` `200`, anonymous `/admin` `307`, sign-in through `/api/auth/sign-in/email` `200`, authenticated `/admin` `0.76s` then `0.07s`, `/admin/orders` `0.81s`, dashboard home-summary `0.10s`), API deploy `fe04b129-d640-4189-8715-00db24ad88a5`, and admin deploys `d7231bb2-f3ba-437a-afd0-ba3c8e722157` then final `c3273021-bce6-4e95-864e-70b08eda2a07`. Live smoke after the first deploy intentionally measured the marker-fill path: first authenticated non-super `/admin` was still `17.16s`, then warm `/admin` `0.34-0.45s`, `/admin/orders` `0.58s`, and home-summary `0.35s`; remote KV then contained `rbac:seed-current:v1:de0f4359 = 1`. After redeploying with that marker present, first authenticated `/admin` was `1.77s`, warm `/admin` `0.21s`, `/admin/orders` `0.89s`, and home-summary `0.73s`.
- **2026-06-19 admin dashboard route graph slimming**: The admin dashboard home now uses dedicated summary/activity server-function and query-option slices (`dashboard-home`) instead of importing the full dashboard graph, and currency reads/writes moved into dedicated `currency` slices so `useCurrency()` and order-detail warmups no longer import the broad settings graph. `/admin/` lazy-loads `DashboardStats`, `RecentOrders`, and `WelcomeBanner` behind stable skeleton fallbacks while keeping `QuickActions` eager. `apps/admin-v2/src/lib/route-graph-boundaries.test.ts` now guards against broad dashboard/settings imports and static homepage component imports returning to the route entry. Verified with Hegel and Archimedes read-only audits, focused Vitest (`3` files / `25` tests), admin typecheck/lint/build, production chunk inspection showing separate `DashboardStats`, `RecentOrders`, `WelcomeBanner`, `currency`, and `use-currency` assets, full root Vitest (`193` files / `1135` tests), Worker env guard, dist-secret check, full deploy-admin typecheck/build, admin deploy `015cad43-6574-4947-92fa-855d4c7fe3e7`, live `/auth/login` `200`, unauthenticated `/admin` `307 Location: /auth/login`, live sign-in `200` with no 2FA redirect, authenticated `/admin/orders` around `0.69s`, dashboard home-summary proxy around `0.25s`, and warm authenticated `/admin` repeats around `0.26s`. The first authenticated `/admin` immediately after deploy still spiked around `15.08s`; keep digging into root/admin server import and Cloudflare isolate cold-start cost before calling dashboard cold-load solved.
- **2026-06-19 admin auth/setup existence probe hardening**: Admin/setup existence checks now use indexed `SELECT 1 ... LIMIT 1` probes instead of `COUNT(*)`, backed by `user_role_idx` and `user_super_admin_idx` in migration `0047_auth_admin_exists_indexes.sql` plus a matching Drizzle snapshot. `apps/admin-v2/src/lib/auth.fns.ts` memoizes the Cloudflare env import, dedupes concurrent cold admin-exists reads per Worker isolate, and still caches only positive results so first-admin setup/local reset recovery can observe newly promoted admins immediately. API setup routes share the same `firstAdminExists()` shape. `apps/admin-v2/src/middleware/rbac.server.ts` still runs RBAC auto-seed, then skips permission expansion and the duplicate super-admin lookup when Better Auth has already supplied `user.isSuperAdmin === true`; omitted auth payloads still fall back to authoritative D1 reads. Verified with Hooke/Wegener read-only audits, focused Vitest (`3` files / `33` tests), admin/API/database typecheck, admin/API lint, migration metadata check, full root Vitest (`193` files / `1135` tests), Worker env guard, dist-secret check, local D1 migration `0047`, remote D1 migration `0047`, remote index inspection for both new indexes, API deploy `da9048e4-ccfb-440c-bae2-937e5757e2d0`, admin deploy `98588a7c-20e3-4803-ba54-f76f717ce2c1`, live API health `200`, live `/api/v1/setup` reporting `adminExists: true`, unauthenticated `/auth/login` `200`, unauthenticated `/admin` `307 Location: /auth/login`, live sign-in `200` with no 2FA redirect, warm authenticated `/admin` around `0.26s`, and warm canonical `/admin/orders` around `0.57s` after one `1.00s` run. A first authenticated `/admin` immediately after deploy still spiked around `14.7s`; keep route-graph/server-bundle splitting as the next admin runtime slice rather than treating this as solved.
- **2026-06-19 admin dashboard non-blocking home summary**: The admin home route no longer blocks SSR or client route entry on dashboard metrics, closing the 15s read-timeout-shaped cold `/admin` path. `/admin/` now skips server summary warming, client-prefetches the new `/api/v1/admin/dashboard/home-summary` endpoint, and renders `DashboardSummaryLoading` until metrics arrive. The new endpoint uses `getDashboardSummaryStats()` and intentionally omits the lifetime `totalRevenue` scan; legacy `/api/v1/admin/dashboard/summary` and `/api/v1/admin/dashboard` still return the full `getDashboardStats()` contract. `0046_dashboard_order_indexes.sql` creates missing `orders_deleted_at_idx` and `orders_dashboard_agg_idx` with `IF NOT EXISTS`, and the SDK/OpenAPI contract was regenerated. Verified with Turing and Harvey read-only audits, focused Vitest (`2` files / `20` tests), migration metadata check, api/admin/core/api-client typecheck and lint, API/admin builds, Worker env guard, dist-secret check, full root Vitest (`193` files / `1133` tests), full deploy typecheck, remote D1 migration `0046`, API deploy `ffb5b60b-555b-4ff7-8873-e7f36545269c`, admin deploy `79075b34-7555-420e-bd02-680e7782fd7c`, live API health `200`, live OpenAPI containing `/api/v1/admin/dashboard/home-summary`, live unauthenticated home-summary returning the expected admin `401`, and valid fresh demo dashboard loads around `278ms` for `/admin`, `343ms` for `/admin/`, and `733ms` for `/admin/orders` after the initial post-deploy cold spike.
- **2026-06-19 catalog listing cache rewarm hardening**: Catalog and attribute writes now send bounded exact storefront `htmlPaths` through the prefix purge transport instead of leaving listing pages cold after a global catalog version bump. `invalidateCatalogCaches()` always canonicalizes and warms `/search` for product/category/discount writes, category routes include affected `/categories/{slug}` paths, product routes include old/new affected category paths for product/variant mutations, and attribute metadata/value writes schedule `/search` warming through `invalidateApiAndScheduleStorefrontGroups(..., { htmlPaths: ["/search"] })`. Storefront purge behavior remains bounded: `bumpVersion: true` moves the global HTML/L2 version, clears relevant L1 prefixes including exact category HTML generation keys, and warms canonical listing paths without relying on old-key deletion. Verified with Pascal and James read-only cache audits, focused Vitest (`6` files / `66` tests), API typecheck/lint/build dry-run, storefront typecheck/build, full root Vitest (`193` files / `1132` tests), Worker env guard, dist-secret check, API deploy `530589ee-eb73-44c2-8eed-084c6fae806b` after D1 retry reported no migrations, live API health `200` around `0.39s`, live demo dashboard login `200` around `0.76s`, live temporary attribute create `201` and permanent delete `204`, Wrangler tail showing two storefront purge POSTs that exact-warmed `/search` in about `1.26s` and `1.46s`, and live `/search` returning `200` around `83ms` with `CF-Cache-Status: HIT` / storefront `X-Cache-Status: HIT; v=1780000085`.
- **2026-06-19 admin first-load route hardening**: Post-auth setup/login/2FA success paths now use TanStack Router navigation instead of full document reloads, including `TwoFactorSetup`, and `apps/admin-v2/src/lib/auth-client.ts` no longer hard-redirects from the Better Auth 2FA callback. `apps/admin-v2/src/lib/auth.fns.ts` caches only positive admin-exists D1 reads for a short isolate TTL, never missing-admin reads, and `adminRouteGuard()` passes the fresh Better Auth `user.isSuperAdmin` value into `loadUserPermissions()` so `apps/admin-v2/src/middleware/rbac.server.ts` skips the duplicate super-admin D1 read while preserving a fallback. The dashboard home loader now uses `warmRouteQuery()` for summary data so cold SSR still renders correct metrics, while client/post-auth transitions prefetch without blocking and show `DashboardSummaryLoading` instead of fake zero metrics. Verified with two subagent audits, focused Vitest (`5` files / `31` tests), admin typecheck/lint/build, full root Vitest (`193` files / `1123` tests), Worker env guard, dist-secret check, Wrangler `4.100.0`, admin deploy `aacdf1f9-cd0c-4f97-a5fe-28ac6e503a56`, live unauthenticated `/auth/login` `200` and `/admin` `307 Location: /auth/login`, and deployed assets: `login-vPyZg5vv.js`, `setup-2fa-BcsBm5PM.js`, and `auth-BSzMPHN6.js` have no `window.location.href`, while `admin-Bqn818Pg.js` contains `Loading dashboard summary`. Cold `/admin` SSR remains authoritative and can still wait on real auth/session/RBAC/dashboard reads.
- **2026-06-19 admin no-cookie auth fast path**: Empty-cookie admin auth guards now skip Better Auth binding initialization and session lookup entirely after setup detection. `checkAdminExists()` reads the shared D1 binding directly, `getSessionInfo()` and `redirectIfAuthenticated()` return `null` without auth initialization, `loginPageGuard()` still checks setup/admin-exists before rendering login but skips auth work when no cookie exists, and `adminRouteGuard()` redirects anonymous `/admin` requests to `/auth/login` before auth/RBAC imports. Verified with Dirac's no-cookie auth audit, focused admin Vitest (`3` files / `32` tests), admin typecheck/lint/build, full root Vitest (`193` files / `1129` tests), Worker env guard, dist-secret check, admin deploy `a5d9850c-cbd5-49f4-af1c-512f4bf815a0`, live unauthenticated `/auth/login` `200` around `0.90s`, live unauthenticated `/admin` `307 Location: /auth/login` around `65ms`, live demo login `200` around `0.52s`, authenticated `/admin` `200` around `16.35s`, `/admin/orders` `200` around `0.70s`, `/admin/products` `200` around `0.89s`, and dashboard summary API `200` around `0.27s`. Cold authenticated `/admin` SSR remains slow and belongs to the next admin-performance slice.
- **2026-06-19 admin idle-tab route guard hardening**: The admin shell's client route-context cache now keeps already-verified in-tab auth/RBAC context usable for up to 4 hours while refreshing in the background after the 1-minute fresh window, instead of hard-blocking the first navigation after 5 minutes idle on admin-exists, Better Auth session, RBAC auto-seed, permission, and super-admin reads. Server-side SSR guards and all API authorization remain authoritative; `refreshAdminRouteContext(router)` still clears the cache for profile, 2FA/security, session, and permission changes. Two read-only subagent audits confirmed list route query warming/focus-refetch policy was not the main idle blocker and logged remaining first-load work: post-login full document reloads, admin-exists/RBAC guard caching, and dashboard summary blocking. Verified with focused Vitest (`2` files / `20` tests), admin typecheck, admin lint, admin production build, full monorepo typecheck, full lint, full root Vitest (`190` files / `1112` tests), root production build, Worker env guard, dist-secret check, Wrangler `4.100.0`, admin deploy `8efcfab9-eac0-47aa-96b2-50dd40187e41`, live unauthenticated `/auth/login` `200` and `/admin` `307 Location: /auth/login`, live authenticated `/admin`, `/admin/orders`, and `/admin/products` `200`, and deployed client asset `index-D1cE9w_p.js` containing the 4-hour route-context marker plus `refetchOnWindowFocus: false` and `defaultPreload: false`. Live first authenticated `/admin` SSR still took about `16.9s`; treat that as the next admin-performance slice rather than solved by this idle-navigation fix.
- **2026-06-19 WhatsApp legacy migration write-key hardening**: WhatsApp credential reads may still use the tolerant `getEncryptionKey()` output for encrypted, legacy plaintext, or JWT-era rows, but `getWhatsAppCloudApiSettings()` now requires a separate `migrationEncryptionKey` before it can create an encrypted migration row or clear `site_settings.whatsapp_access_token`. Admin auth settings reads, customer WhatsApp OTP preflight, auth OTP queue sends, and order WhatsApp notification sends thread `getCredentialEncryptionKey()` separately from the read key; explicit token saves still use `requireEncryptionKey()`. Verified with a read-only subagent audit, focused Vitest (`4` files / `51` tests), full root Vitest (`190` files / `1111` tests), full monorepo typecheck, full lint, root production build, Worker env guard, dist-secret check, API deploy `3185f653-1de4-4ac8-9bca-457377419360`, live API health `ok`, live OpenAPI auth-settings path present, authenticated dashboard-proxy `/api/v1/admin/settings/auth` returning masked `whatsappAccessToken`, present phone-number ID, `auth_otp` template, and `0` suspected secret leaks, plus remote D1 smoke showing `0` legacy WhatsApp token rows, `1` encrypted `enc:` token row, and `0` writes during inspection. No real WhatsApp message was sent and no production settings were mutated during smoke.
- **2026-06-19 Firebase/FCM credential hardening**: Firebase service-account saves now validate `client_email`, `private_key`, and `project_id`, require the dedicated `CREDENTIAL_ENCRYPTION_KEY` before non-empty writes, and store encrypted `enc:` AES-GCM values under `settings.firebase/service_account`; masked saves skip the secret and empty saves clear it. Notification runtime reads decrypt `enc:` rows, tolerate legacy plaintext/bare encrypted rows only for migration, and return `undefined` for unreadable ciphertext so the FCM client falls back to `FIREBASE_SERVICE_ACCOUNT_CRED_JSON` instead of trying to parse ciphertext. FCM OAuth access tokens now use per-instance memory and write to `SHARED_AUTH_CACHE` only as encrypted `enc:` values when the dedicated key is configured; when the key is absent, no raw bearer token is persisted. Verified with a parallel read-only subagent audit, focused Vitest (`4` files / `31` tests), full root Vitest (`190` files / `1106` tests), full monorepo typecheck, full lint, root production build, Worker env guard, dist-secret check, API deploy `0a2c3f41-d67b-457f-8fc9-f0d2386731ff`, live API `/api/v1/health` `200`, live OpenAPI `200` with Firebase settings path present, and authenticated live dashboard-proxy smoke where `/api/v1/admin/settings/firebase` returned `200`, masked `serviceAccount`, expected public config keys, and `0` private-key/client-email leaks. No live Firebase settings were mutated during smoke.
- **2026-06-19 fraud-checker credential storage hardening**: Fraud-checker provider configs now store encrypted JSON blobs under `settings.fraud-checker`; `saveFraudProvider()` rejects missing `CREDENTIAL_ENCRYPTION_KEY` before insert/update, and admin create/update routes call `requireEncryptionKey()` while list/test/lookup routes pass `getEncryptionKey()` for encrypted plus legacy plaintext reads. Admin responses still mask `apiKey`/`apiSecret`, masked updates restore existing decrypted secrets, and full lookup/test runtime paths receive decrypted provider credentials without exposing them to the dashboard. A parallel credential audit confirmed payment, delivery, SMS, Resend email, Meta CAPI admin, widget AI, and this fraud-checker slice now use dedicated-key write paths; it also logged the next backlog: Firebase service-account D1 encryption, FCM OAuth token KV caching, and WhatsApp legacy-token migration avoiding JWT-fallback writes. Verified with focused Vitest (`3` files / `13` tests), full root Vitest (`189` files / `1094` tests), full monorepo typecheck, full lint, root production build, Worker env guard, dist-secret check, API deploy `2d7c4cc4-5bd0-4e5d-a160-21f0b298f7b5`, live API health/docs/OpenAPI fraud-checker checks `200`, and authenticated live dashboard-proxy smoke where `/api/v1/admin/fraud-checker` returned `200` with `0` configured providers and `0` leaked secret fields. No live fraud-checker settings or lookups were mutated during smoke.
- **2026-06-19 delivery-provider credential write/read hardening**: Delivery provider credential saves are now write-strict: `saveDeliveryProvider()` rejects missing `CREDENTIAL_ENCRYPTION_KEY` before insert/update, and the admin delivery-provider create/update routes call `requireEncryptionKey()` before DB writes or checkout-cache invalidation. List/get/update paths now decrypt existing provider credentials before masking or merging masked fields, so encrypted rows are not returned as ciphertext and masked placeholders are not persisted as real credentials; `webhookSecret` is masked and preserved with `clientSecret`, `password`, `apiKey`, and `secretKey`. Runtime provider reads still use `getEncryptionKey()`/`decryptCredentialsGraceful()` for legacy plaintext/JWT-encrypted rows only. Verified with a parallel read-only subagent audit, focused Vitest (`2` files / `17` tests), full root Vitest (`187` files / `1086` tests), full monorepo typecheck, full lint, root production build, Worker env guard, dist-secret check, API deploy `0501ef2c-747c-40c4-b457-ad03f70b4afb`, live API health/docs/OpenAPI delivery-provider checks `200`, and authenticated live dashboard-proxy smoke where `/api/v1/admin/settings/delivery-providers` returned `200` with `3` parseable providers, `8` masked sensitive fields, and `0` plaintext/ciphertext leaks. No live delivery settings were mutated during smoke.
- **2026-06-19 payment credential write fail-closed hardening**: `upsertEncryptedSetting()` now rejects provider-secret writes unless a dedicated `CREDENTIAL_ENCRYPTION_KEY` is available, so missing key configuration cannot silently persist plaintext gateway credentials. Stripe, SSLCommerz, and Polar admin saves now call `requireEncryptionKey()` only when a real unmasked secret is submitted, fail before DB writes/cache invalidation if the key is missing, and keep non-secret or masked saves on the existing no-key path. Gateway reads still use `getEncryptionKey()` with JWT/plaintext tolerance for legacy rows only. Verified with focused Vitest (`2` files / `19` tests), full root Vitest (`187` files / `1081` tests), full monorepo typecheck, full lint, root production build, Worker env guard, dist-secret check, API deploy `17566513-f7c8-4a26-a2b4-47a4aff640ba`, live API health/docs/OpenAPI payment-settings checks `200`, and authenticated live dashboard-proxy smoke where `/api/v1/admin/settings/payment-methods` returned `200` with four configured methods. No live payment settings were mutated during smoke. The adjacent delivery-provider credential save review is tracked in the delivery credential hardening log entry.
- **2026-06-20 customer OTP atomic verification**: Customer OTP verification authority moved from read-modify-write KV JSON to D1 `customer_auth_otp_challenges`. `sendOtp()` stores only an HMAC-SHA256 code hash plus channel/intent/pinned-contact metadata with 5-minute expiry and 2-minute resend cooldown; `/send-otp` deletes the exact D1 challenge on queue handoff failure; `verifyOtp()` uses guarded D1 updates to atomically consume correct codes or increment/lock wrong-code attempts; and scheduled maintenance sweeps expired/stale challenge rows in bounded batches. Do not restore a used OTP after account-policy errors and do not move OTP verification back to KV. Verified with local/remote migration `0051_customer_auth_otp_challenges`, focused auth/API tests, root typecheck/lint/test/build/platform guards, API deploy `2a7b6ee4-55e3-411e-8edb-c3aa099ba531`, live API health/OpenAPI, live invalid-verify fail-closed smoke, remote D1 table-info, and deployment-list proof at `100%`.
- **2026-06-19 encrypted WhatsApp credential runtime hardening**: Meta WhatsApp Cloud API access tokens now save to encrypted `settings.whatsapp/access_token` through `CREDENTIAL_ENCRYPTION_KEY`; admin `/settings/auth` still exposes the compatible masked `whatsappAccessToken` field, skips masked saves, encrypts new token saves, and clears both encrypted and legacy token storage on empty saves. `getWhatsAppCloudApiSettings()` is now the single resolver for auth OTP, order WhatsApp, and notification-channel enablement; it reads encrypted storage first, uses `site_settings.whatsapp_access_token` only as legacy fallback, migrates plaintext only after a successful encrypted write, and does not clear a legacy fallback when an existing encrypted row cannot decrypt with the current key. Auth OTP queue payloads no longer carry Meta tokens, phone-number IDs, or template names; `sendOtp()` validates WhatsApp configuration before mutating OTP challenge state, and the API queue consumer resolves/decrypts credentials at send time before calling the shared WhatsApp template sender with the OTP body/button parameters. Order WhatsApp notifications use the same encrypted resolver while keeping non-secret phone-number ID/auth template fields in `site_settings` and order template settings in `settings.notifications`. The OpenAPI/admin UI contract did not change, so no SDK regeneration was needed. Verified with focused Vitest (`5` files / `44` tests), full root Vitest (`187` files / `1073` tests), full monorepo typecheck, full lint, root production build, Worker env guard, dist-secret check, API deploy `5926d885-de2c-4fbb-ad9c-e345f1d87d37`, live API health/docs/OpenAPI `200`, and authenticated live demo smoke where `/api/v1/admin/settings/auth` returned `200` through the dashboard proxy with a masked WhatsApp token, phone-number ID present, and `auth_otp` template. No real WhatsApp message was sent during smoke.
- **2026-06-19 durable customer OTP delivery receipts**: Customer OTP sends now generate a per-attempt `deliveryKey` and `otpExpiresAt`, validate the selected transport before mutating OTP challenge/rate-limit state, delete the exact D1 challenge if `AUTH_OTP_QUEUE.send()` fails, and claim `auth_otp_delivery_receipts` before email/SMS/WhatsApp provider work. Queue retries skip accepted/delivered/skipped receipts, mark expired OTP attempts as skipped instead of sending stale codes, fail/retry provider-unavailable email fallback instead of ACKing an undelivered OTP, pass `deliveryKey` to Resend as `Idempotency-Key`, pass deterministic receipt-derived SMS client references for GenNet `csms_id`, parse/store WhatsApp message IDs, and never store plaintext OTP codes in D1. Verified with generated/applied migration `0045_rare_steel_serpent`, local and remote D1 migration success, local/remote `auth_otp_delivery_receipts` table/index inspection, migration metadata check (`46` SQL files / `46` journal entries / `31` snapshots), focused core/API Vitest, full root Vitest (`186` files / `1060` tests), API/core/database TypeScript checks, API/core ESLint, Worker env guard, API Wrangler dry-run, API dist-secret check, deployed `scalius-api` version `f9836431-b4b2-4357-9e74-15c4fc141443`, and live API health/docs/OpenAPI plus non-delivering invalid OTP validation smoke. Remaining boundary: Cloudflare Email and WhatsApp do not expose a first-class upstream idempotency key in this path, so a Worker crash after provider acceptance but before D1 receipt update can still duplicate on retry; Resend and GenNet have provider-side dedupe inputs.
- **2026-06-19 real order WhatsApp notifications**: Customer order WhatsApp is now a real Meta Cloud API template send instead of a skipped placeholder. `packages/core/src/integrations/whatsapp.ts` owns the current Graph API template-message request/response handling; `sendOrderNotificationEmail()` reads `orders.customerPhone`, resolves runtime Meta credentials through the shared encrypted WhatsApp settings helper, uses notification-scoped template settings (`settings.notifications/whatsapp_order_template_name` and `whatsapp_order_template_language`), and records accepted/skipped/failed D1 delivery receipts. Missing/invalid order phones, missing credentials, paused templates, and non-retryable provider 4xx responses become terminal skipped receipts; malformed 200 responses, 408/409/429, 5xx, and network failures remain retryable. The admin notification settings endpoint returns `whatsappTemplate` plus `whatsappConfigured`, rejects enabling WhatsApp order channels until Meta credentials exist, and the dashboard disables WhatsApp checkboxes when credentials are absent. Regenerated `@scalius/api-client` from live local OpenAPI. Verified with official Meta Cloud API message docs, focused core Vitest (`38` files / `183` tests), full root Vitest (`187` files / `1068` tests), core/API/admin/api-client TypeScript checks, core/API/admin/api-client ESLint, root production build, Worker env guard, dist-secret checks, local OpenAPI schema smoke, deployed `scalius-api` version `9d1849b2-624f-4816-8ce5-d054a307b4c6`, deployed `scalius-admin-v2` version `e08edb2c-08fb-495b-b229-9415dee2b54f`, live API health/docs/OpenAPI schema smoke, live admin login smoke, and deployed notification-settings bundle marker smoke. No real WhatsApp message was sent during smoke. Remaining hardening: revisit Meta if it exposes a first-class idempotency key.
- **2026-06-19 durable order notification outbox**: Customer/admin order notifications now record a D1 `order_notification_outbox` row before queue handoff. Storefront order ingest inserts `order_created` outbox rows in the same D1 batch as the order, direct admin status/COD sends now use durable outbox records, delivery webhook/status-refresh helpers use transition-version dedupe keys when available, provider shipment creation uses shipment-scoped keys, queue messages carry `outboxId`, the consumer claims/marks rows `sent` or retryable `failed`, and the existing 15-minute API cron flushes pending/failed/stale rows. Verified with generated/applied migration `0043_cuddly_maddog`, local and remote D1 migration success, database/core/API/admin/api-client/shared/storefront checks, focused Vitest (`6` files / `57` tests), full root Vitest (`182` files / `1041` tests), touched-file ESLint, API Wrangler dry-run, API dist-secret check, deployed `scalius-api` version `fd852673-f9a0-49f2-b011-0f9122f4a892`, live API health/docs `200`, and remote D1 table/index inspection for `order_notification_outbox`. Remaining boundary: channel providers are still at-least-once unless their individual adapters add provider-level idempotency receipts.
- **2026-06-19 durable order notification delivery receipts**: Order notification queue processing now records per-channel D1 `order_notification_delivery_receipts` before customer email/SMS/WhatsApp/admin FCM provider work. Retries skip already accepted/skipped receipt targets, retry only failed/busy targets, and keep the parent `order_notification_outbox` retryable until enabled targets are terminal. Cloudflare Email returns and stores `messageId`, Resend sends the deterministic receipt key through `Idempotency-Key`, SMS receipts store provider refs, GenNet maps receipt-derived client references to deterministic `csms_id`, FCM stores REST message `name` per token, and invalid FCM tokens become skipped receipts before deactivation. Verified with generated/applied migration `0044_famous_guardian`, local and remote D1 migration success, local/remote receipt table/index inspection, focused Vitest (`6` files / `27` tests), full root Vitest (`184` files / `1049` tests), touched-file ESLint, core/API/database TypeScript checks, migration metadata check, API Wrangler dry-run, API dist-secret check, deployed `scalius-api` version `8eaff196-62d4-4f7b-a3b4-d79c8fb9899c`, and live API health/docs `200`. Superseded boundary: order WhatsApp now sends real Meta template messages; Cloudflare Email, Meta WhatsApp, and FCM can still duplicate if the Worker crashes after provider acceptance but before D1 receipt update because those providers do not currently expose a durable send idempotency key.
- **2026-06-19 payment runtime gateway freshness hardening**: Stripe, SSLCommerz, and Polar provider-boundary reads now bypass per-isolate gateway memory for public session creation, webhook signature/IPN validation, and admin refund dispatch. Public session routes also fresh-read `payment_methods.enabled_methods` before gateway credentials so stale checkout tabs cannot create new external payment sessions after a merchant disables a method; webhook settings-read failures return retryable 5xx responses before durable idempotency claims; invalid Stripe/Polar signatures and failed SSLCommerz validation do not claim events; refund dispatch releases the local refund claim if fresh settings fail; and Polar's SDK singleton is keyed by token plus sandbox/production server. Verified against current Cloudflare Workers best-practice guidance and `@cloudflare/workers-types@4.20260617.1`, focused Vitest (`7` files / `73` tests), core/API TypeScript checks, touched-file ESLint, API Wrangler dry-run, API dist-secret check, deployed `scalius-api` version `27d25c1a-2309-47f4-8991-dbe29ad9bef2`, live API health `200`, live checkout config `200`, live invalid Stripe webhook `400`, live invalid Polar webhook `403`, live SSLCommerz IPN missing `val_id` `200 OK`, and live Stripe session missing receipt proof `400` before gateway work.
- **2026-06-19 checkout config fail-closed freshness hardening**: Public checkout config assembly now bypasses per-isolate gateway/settings memory caches for payment-method discovery, gateway credential checks, and admin payment-method status reads, so a payment settings save cannot repopulate fresh API/storefront checkout caches from stale isolate memory. `/api/v1/checkout/config` moved to the `api:checkout:config:v2:` cache lane and now returns a non-cacheable `503 CHECKOUT_CONFIG_UNAVAILABLE` on config assembly failure instead of caching a guessed COD-only success response; storefront cart/checkout helpers treat unavailable config as a hard checkout-disabled state rather than silently offering COD. The checkout cache group invalidates both legacy and v2 API prefixes, and OpenAPI/SDK were regenerated with the documented 503. Verified with focused Vitest (`5` files / `37` tests), core/API/api-client TypeScript checks, touched-file ESLint, storefront `astro check`, storefront production build, dist-secret cleanup/check, API/storefront Wrangler dry-runs, deployed `scalius-api` version `cd42df2d-1b27-4a0e-93a8-644b79dff41f`, deployed/current `scalius-storefront` version `c4f15b05-ab85-4af0-bc15-deae5232371f` with build `src-b095f3f7e0362143`, live API health smoke, live checkout config moving from `MISS` to repeated `HIT` with `stripe`, `sslcommerz`, `polar`, and `cod` enabled, live OpenAPI exposing the 503 response, live cart/checkout HTML carrying healthy config with no unavailable copy, and browser cart smoke with zero console errors.
- **2026-06-19 storefront exact HTML cache-path canonicalization**: Storefront/API exact `htmlPaths` now normalize through shared `@scalius/shared/storefront-cache-path` before dedupe/capping, so product option noise, tracking params, default listing params, and repeated render-affecting params cannot consume the bounded warm budget or delete/warm a different key than middleware serves. HTML cache keys, purge-cache deletes/warms, API purge payload normalization, and product-list query parsing now share the same last-surviving-value query contract. Verified with focused Vitest (`5` files / `59` tests), shared/API TypeScript checks, touched-file ESLint, storefront `astro check`, storefront production build, dist-secret cleanup/check, API/storefront Wrangler dry-runs, deployed `scalius-api` version `4a8acbbc-61ed-49d1-9cf7-e6217bb17e49`, deployed/uploaded `scalius-storefront` version `bef7f4ca-883b-4598-aa41-4cb6c08a4d59` with build `src-4c50c50fc6f67dc4`, live `/api/v1/health` smoke, live `/products/fish` moving from `MISS` to `HIT`, live `/products/fish?size=XL&color=red&utm_source=ad` hitting the same product HTML cache key, live repeated `/search?q=apple&q=fish&page=2&page=1&sortBy=price-desc&sortBy=newest&utm_source=ad` hitting the warmed canonical fish search cache, purge endpoint `405`/`401` guard checks, and browser smoke showing product canonical URL clean, search input/title resolved to `fish`, no `apple` content, and zero console errors.
- **2026-06-19 admin order-list Bangladesh date-filter hardening**: Order-list date filters now travel as date-only `YYYY-MM-DD` values instead of browser-local ISO timestamps. Admin date helpers preserve calendar dates without timezone conversion, quick-select presets use calendar-day starts, the API validates date-only query params and converts them to exact Asia/Dhaka UTC start/end bounds, and core `listOrders()` uses those bounds without re-expanding `endDate`. Verified with focused Vitest (`5` files / `12` tests), touched-file ESLint, admin/API/core TypeScript checks, admin production build plus dist-secret cleanup/check, API/admin Wrangler dry-runs, deployed `scalius-api` version `0ebf1cca-cc2c-4434-80eb-330587c01943`, deployed `scalius-admin-v2` version `6acfe235-d4ef-43cc-8285-6c843cb3c58d`, live `/api/v1/health` and `/api/v1/docs` smoke, unauthenticated ISO date links being canonicalized by stripping invalid date params, date-only links surviving to auth redirect, and authenticated browser smoke showing `/admin/orders?...&startDate=2026-06-19&endDate=2026-06-19` with visible `Jun 19, 2026 - Jun 19, 2026`, settled `0 orders`, and zero console errors.
- **2026-06-19 admin order-list pagination/error/COD freshness hardening**: Shared list search schemas now clamp `page`/`limit` before query construction, direct core `listOrders()` clamps page/limit again before SQL offset/limit, and `/admin/orders` corrects out-of-range pages after pagination metadata is known. `useServerTable()` now exposes query `error`/`isError`/`refetch`, `DataTable` renders an explicit retryable list-error state instead of stale rows/pagination, and COD collection mutations invalidate `queryKeys.orders.list()` so order-list payment badges refresh after COD actions. Verified with focused Vitest (`6` files / `11` tests), touched-file ESLint, admin/core/API TypeScript checks, admin production build plus dist-secret cleanup/check, API/admin Wrangler dry-runs, deployed `scalius-api` version `aae30d03-2d78-40f9-b49e-5225584a2046`, deployed `scalius-admin-v2` version `833c10e7-077a-4c4b-ab05-c7a97c3e185d`, live smoke (`/api/v1/health` 200, `/api/v1/docs` 200, `/auth/login` 200, protected admin API 401), unauthenticated orders deep links canonicalizing bad limits, and authenticated browser smoke where `page=999&limit=999` corrected to `page=1&limit=100` showing `54 orders`, while `page=999&limit=10` corrected to `page=6` showing rows 51-54 with zero console errors.
- **2026-06-19 admin order-list selection and bulk-action hardening**: Server-backed table selection now clears synchronously whenever the hashed query scope changes, so changing status filters, search/sort/page/limit, or any other query-key dependency cannot leave stale selected rows active. `useServerTable()` now exposes `deselectIds()` for partial-success bulk operations, the order toolbar disables bulk delete/ship while shipping or delete work is pending, the bulk-shipping dialog ignores close/re-entry during `isShipping`, partial shipment success deselects shipped IDs while keeping failed IDs selected, and mobile order cards no longer advertise unsupported Shift range selection. Parallel order-list audits also logged the next backlog items: pagination/limit canonicalization, visible list-fetch error states, timezone-safe date filters, search-sort semantics, COD invalidation, and a default sort index review. Verified with focused admin Vitest (`3` files / `16` tests), touched-file ESLint, admin TypeScript, admin production build plus dist-secret cleanup/check, admin Wrangler dry-run, deployed `scalius-admin-v2` version `6915a179-db5b-421c-86cf-8790cfec8a4c`, live unauthenticated smoke (`/auth/login` 200, `/admin` 307 to `/auth/login`, `/api/v1/admin/products` 401, `/admin/orders` canonicalizes then redirects to `/auth/login`), and authenticated live browser smoke on `/admin/orders`: row select shows `Move to Trash (1)` and `Ship Orders (1)`, status filter changes clear selection/bulk controls, select-all selects two confirmed rows and unselect clears them, page-next navigation clears selection, and console errors stayed at zero.
- **2026-06-19 CMS shortcode reverse-dependency cache invalidation**: Storefront shortcode parsing now lives in `@scalius/shared/shortcodes` so API invalidation and CMS rendering use the same supported grammar. Product availability purges resolve published CMS pages containing affected product shortcodes and add exact page-render API patterns, `page_render_*` generation prefixes, and CMS `htmlPaths`; widget writes do the same for shortcode-only widget references without widening normal scoped placement purges. Exact page paths stay capped, with global HTML-version fallback when the affected page set exceeds the warm budget or a post-commit widget shortcode scan fails. Verified against current Cloudflare Workers best-practice docs and `@cloudflare/workers-types@4.20260617.1`, focused Vitest (`4` files / `39` tests), shared/API/storefront TypeScript, touched-file ESLint, API Wrangler dry-run, storefront `astro check`, storefront production build, API/storefront dist-secret checks, storefront Wrangler dry-run, deployed `scalius-api` version `724949eb-bfc3-4543-9206-ed3d492809d5` and `scalius-storefront` version `a84dbd44-5ef2-4642-afae-1934dab4e87f` with build `src-87621548f3a9eadd`; live smoke confirmed API health/docs `200`, `/pshortcode-test` served `MISS` then Cloudflare `HIT` on the new build, rendered `1` `product-shortcode-container`, and had no literal `[product` or `shortcode-error`.
- **2026-06-19 admin abandoned-checkouts route-entry deferral**: `/admin/abandoned-checkouts` no longer awaits the default incomplete-order list before navigation can settle. The route is now a shell around `AbandonedCheckoutsManager`, which already owns the `useQuery()` list read, loading spinner, empty state, refresh, sorting, pagination, and selection state. A route-graph guard prevents `abandonedCheckoutsQueryOptions`, `ensureQueryData()`, or `prefetchQuery()` from returning to the route file. Verified with focused admin route-graph Vitest (`13` tests), admin TypeScript, touched-file ESLint, admin production build, dist-secret cleanup/check, Wrangler dry-run, deployed `scalius-admin-v2` version `dd28d42f-766e-40aa-83da-4a4fa52fb2dd`, live unauthenticated smoke (`/auth/login` 200, `/admin` 307, `/api/v1/admin/products` 401, `/admin/abandoned-checkouts` 307), built route component chunk exporting only the page shell, and authenticated live browser smoke showing `Incomplete Orders`, search/refresh controls, a loaded checkout row, no route-error UI, and zero console errors.
- **2026-06-19 storefront generated public feed cache lane**: Generated public XML/XSLT/text endpoints now use the storefront Cache API lane instead of cold-rendering every request. `isCacheablePublicResponse()` keeps the existing `200`/no `Set-Cookie`/non-private `Cache-Control` guards while allowing `text/html`, XML, XSLT, and plain text only, and `CACHEABLE_PATHS` explicitly includes `/robots.txt`, `/sitemap.xsl`, and `/api/facebook-feed.xml` alongside sitemap XML routes. Verified against current Cloudflare Workers best-practice docs and `@cloudflare/workers-types@4.20260617.1`, focused storefront cache/purge Vitest (`4` files / `17` tests), touched-file ESLint, storefront `astro check`, storefront production build, dist-secret cleanup/check, API cache-invalidation regression Vitest, storefront Wrangler dry-run, deployed `scalius-storefront` version `8c2d4ae2-f860-431c-a7aa-f9186d535b1c` with build `src-6a8aff38883e0309`, dist markers for `isCacheablePublicResponse()` in `virtual_astro_middleware.mjs`, and live `/sitemap.xml`, `/sitemap-products.xml?page=1`, `/sitemap.xsl`, `/robots.txt`, and `/api/facebook-feed.xml?page=1&limit=1000` all moving from build-versioned `MISS` to `HIT` with valid content bodies.
- **2026-06-19 admin self-loading settings route entry**: Notifications, theme, and hero-slider settings no longer block route entry on data reads they already perform inside their own components. The route files now stay as shell routes, while `FirebaseSettingsForm`, `ThemeSettingsPage`, and `HeroSliderContainer` keep their existing loading states and fetch/query ownership. A route-graph guard locks these routes out of `ensureQueryData()`/`prefetchQuery()` so future settings polish does not reintroduce duplicate route-entry fetches. Verified with focused admin route-graph Vitest (`12` tests), admin TypeScript, touched-file ESLint, admin production build, dist-secret cleanup/check, Wrangler dry-run, deployed `scalius-admin-v2` version `3e02cbc0-3af5-4187-9955-303b7fe68fff`, live unauthenticated smoke (`/auth/login` 200, `/admin` 307 to `/auth/login`, `/api/v1/admin/products` 401), built route chunks with no `firebaseSettingsQueryOptions`/`themeSettingsQueryOptions`/`heroSlidersQueryOptions`/`ensureQueryData()` leftovers, and authenticated live browser smoke showing the notifications form, theme editor, and hero-slider tabs render without error-boundary UI or console errors.
- **2026-06-18 admin edit label hydration deferral**: Discount and collection edit routes no longer block route entry on secondary selected product/collection label reads. Discount edit still requires the discount itself and collection edit still requires collection detail plus category options, but selected product/collection names now hydrate through browser-only prefetches and non-suspense `useQuery()` calls with stable ID fallbacks. `AmountOffProductsContainer` patches hydrated labels into selected state only when matching IDs still exist, so removing a chip while hydration is in flight does not re-add it. Verified with focused admin route-graph Vitest (`11` tests), admin TypeScript, touched-file ESLint, admin production build, dist-secret cleanup/check, Wrangler dry-run, deployed `scalius-admin-v2` version `1d8bf0ca-a520-42ab-acf2-f5a8115233b2`, live unauthenticated smoke (`/auth/login` 200, `/admin` 307 to `/auth/login`, `/api/v1/admin/products` 401), and deployed chunks containing the `Discount product label prefetch skipped`, `Discount collection label prefetch skipped`, `Collection product label prefetch skipped`, and `isSameSelectedProduct` markers.
- **2026-06-18 API KV cache resurrection fence**: API response caching now uses scoped fence generations so stale in-flight miss writes scheduled with `waitUntil()` cannot repopulate the current cache after an admin invalidation. `cacheMiddleware()` reads/writes versioned physical keys from `api-cache-fence.ts`, group/pattern/product-availability invalidators bump matching fence scopes before deleting old bodies, exact product detail invalidation deletes only the stable key plus `${key}#f:*` family, and `/api/v1/cache/last-cleared` reports fence `updatedAt` instead of legacy `_last_cleared:*` keys. Verified with focused API Vitest (`4` files / `37` tests), API TypeScript, touched-file ESLint, Wrangler dry-run, dist-secret cleanup/check, deployed `scalius-api` version `828cc594-74e5-44f8-be4a-f986f58d8ba3`, live `/api/v1/health` 200, `/api/v1/openapi.json` 200, `/api/v1/docs` 200, and live canonical product requests moving from `MISS` to `HIT` with matching payload shape on the deployed fence code.
- **2026-06-18 admin dashboard empty-activity state**: The dashboard activity chart now receives explicit background-query load state instead of inferring loading from an empty row array. `getDashboardActivityPanelState()` separates pending, chart, empty, and unavailable states, so new/low-volume stores show a calm `No daily activity yet` panel and transient activity failures show `Activity unavailable` without blocking summary cards or pulling Recharts into the first paint path. Verified with focused admin Vitest (`2` files / `9` tests), admin TypeScript, touched-file ESLint, admin production build, dist-secret cleanup/check, Wrangler dry-run, deployed `scalius-admin-v2` version `f98cee46-a377-497c-8604-1e1fe14b6e3d`, live unauthenticated smoke (`/auth/login` 200, `/admin` 307 to `/auth/login`, `/api/v1/admin/products` 401), and deployed dashboard chunks containing the new `No daily activity yet` / `Activity unavailable` markers plus `getDashboardActivityPanelState()`.
- **2026-06-18 admin order-detail shipment resilience**: Order detail now treats shipment reads as optional warm/poll data instead of required route-entry data. `prefetchOrderDetailQueries()` still requires the order itself, but shipment fetch failures log `Order shipment prefetch skipped` and the route stays loadable; the component uses non-suspense `useQuery()` for shipments with an empty-list fallback while keeping 30s polling for webhook-driven updates. Verified with focused admin Vitest (`2` files / `15` tests), admin TypeScript, touched-file ESLint, admin production build, dist-secret cleanup/check, Wrangler dry-run, deployed `scalius-admin-v2` version `1bcbff04-b119-4637-ad28-5cc8b06dd2ea`, live unauthenticated smoke (`/auth/login` 200, `/admin` 307 to `/auth/login`, `/api/v1/admin/products` 401), and deployed order-detail chunks showing the shipment prefetch warning plus the route chunk retaining polling.
- **2026-06-18 collection HTML edge-cache enablement**: Storefront collection detail pages now participate in the same HTML Cache API lane as products/categories/CMS pages by adding `/collections/{id}` to the middleware cacheable allowlist. This makes collection-scoped widget/page exact `htmlPaths` and warmups effective instead of always falling through to origin rendering. Guard tests assert collection detail stays between category and search in the cache policy and that exact generation keys still recognize `/collections/col_1`. Verified with focused storefront Vitest (`2` files / `4` tests), touched-file ESLint, storefront `astro check`, storefront build, dist-secret cleanup/check, Wrangler dry-run, deployed `scalius-storefront` version `c20d73eb-93c5-4cbb-b86c-3dc11670cab5` with build `src-c0dd0ed59e266fd2`, and live `/collections/9dHdb5uI8o7t3kKdIyUr8` moving from `MISS; gen=0` at about `2.82s` TTFB to Cloudflare `HIT` at about `0.07s` TTFB with collection content intact.
- **2026-06-18 collection resolver order hardening**: Public collection detail resolution now preserves merchant-configured manual `productIds` order and configured category metadata order after SQL reads, instead of trusting arbitrary `IN (...)` result order. Missing/inactive manual products are skipped while later visible products can still fill the collection up to `maxProducts`, matching the homepage batch resolver behavior. Verified with focused core Vitest (`2` tests), core/API TypeScript, touched-file ESLint, API Wrangler dry-run, dist-secret check, deployed `scalius-api` version `1517c736-4f10-4405-baf3-39b9af70d7ec`, live API health `200`, live `/api/v1/collections` `200` on a fresh KV key, and live `/api/v1/collections/3kWSvLH321MIcj7G7zO9C` preserving its configured product order.
- **2026-06-18 admin new-order product fanout removal**: `/admin/orders/new` now blocks only on the paginated product list and maps list rows into order-form products with empty variant arrays plus `variantCount`. Product variants are fetched lazily with `productVariantsQueryOptions(product.id)` after selection, guarded by a stale-load token, with a stable loading state that prevents adding an item before variant data settles. Product search badges use `variantCount` so merchants still see variant availability before the lazy detail read. A route-graph guard prevents `productQueryOptions()`/detail fanout from returning to the new-order loader. Verified with focused route-graph Vitest (`10` tests), admin TypeScript, touched-file ESLint, admin production build, dist-secret cleanup/check, Wrangler dry-run, deployed `scalius-admin-v2` version `49ba8cf1-ec0a-4cd7-8996-077a11f15742`, live unauthenticated smoke (`/auth/login` 200, `/admin` 307 to `/auth/login`, `/api/v1/admin/products` 401), and built `OrderForm` client/server chunks containing `Loading Variants` plus no `productQueryOptions(` reference.
- **2026-06-18 scoped widget purge blast-radius reduction**: Storefront purge policy now keeps scoped widget purges local when the API supplies exact `htmlPaths`; prefixes plus exact paths no longer bump the global `v_*` cache version or warm only `/`. Exact generations now cover `widget_*`, `widgets_scope_*`, page render keys, and exact entity HTML paths, while middleware reads those generations for product/category/collection/CMS-page HTML cache keys. Prefix-only purges still bump globally for freshness because Cache API cannot delete by prefix. Verified with focused storefront Vitest (`4` files / `17` tests), touched-file ESLint, storefront `astro check`, storefront build, dist-secret cleanup/check, Wrangler dry-run, deployed `scalius-storefront` version `763bc4d9-e85e-4e1c-9fad-d759ba95a773` with build `src-b8c6a14fa5afc013`, built chunk markers for exact HTML path generations and `exactHtmlWarmScheduled`, regression coverage proving a scoped page-widget purge bumps exact generation keys, deletes/writes exact `/about-us` cache keys, warms `/about-us`, and does not write `v_storefront.example.com`, and live `/`, `/products/fish`, `/categories/drinks`, `/pshortcode-test`, and `/search?q=fish` smokes showing product/category/CMS pages carry `gen=0`, search does not, and all warmed from `MISS` to Cloudflare `HIT`.
- **2026-06-18 edge cache outage coalescing**: Storefront `withEdgeCache()` now keeps in-flight request dedupe active even when KV cache-version lookup is unavailable, while still refusing to populate L1/L2 storage without a version. During a transient KV/version outage, identical concurrent reads now share one origin/API fetch instead of stampeding, but every completed call remains an uncached bypass so stale data cannot be resurrected. Verified with focused storefront Vitest (`5` files / `14` tests), touched-file ESLint, storefront `astro check`, storefront build, dist-secret cleanup/check, Wrangler dry-run, deployed `scalius-storefront` version `2a38831b-ceb4-407b-a0b7-c0b7d368fe1f` with build `src-b4fa0b2a25eb3ab9`, deployed chunk markers for `runInflightOnly`, and live `/` plus `/products/fish` moving from `MISS` to Cloudflare `HIT` on the new build.
- **2026-06-18 CMS shortcode render hardening**: Storefront CMS shortcode expansion now normalizes sanitized quote entities in shortcode attributes and dedupes product/widget resolution by shortcode type plus id/slug within a single render. Repeated product shortcodes still render every placeholder, but only trigger one product detail read per unique slug, and live rich-content output such as `[product slug=&quot;monster-energy-drink&quot;]` now expands instead of leaking literal shortcode text. Verified with focused storefront Vitest (`2` files / `6` tests), touched-file ESLint, storefront `astro check`, storefront build, dist-secret cleanup/check, Wrangler dry-run, deployed `scalius-storefront` version `69e0f97d-fdeb-4574-ac54-8b8b779bc4ed` with build `src-cfa8d69c00763eb3`, built chunk markers for quote normalization and dedupe, live API product smoke for `monster-energy-drink`, and live `/pshortcode-test` rendering `1` `product-shortcode-container`, no literal `[product`, no shortcode error, then Cloudflare `HIT`.
- **2026-06-18 customer order read hardening**: Removed the old public SDK/OpenAPI `GET /api/v1/orders/{id}` full-detail customer route so storefront order-success and payment proof flows can only use tokenized receipt/status endpoints or customer-session scoped order history. Storefront dropped the unused raw order-detail helper/types, and the generated API client no longer exports `getApiV1OrdersById`. Customer account GETs (`/api/v1/customer-auth/me` and `/api/v1/customer-auth/orders`) now emit explicit private no-store headers, and storefront order-history fetches request `cache: "no-store"`. Verified with focused Vitest (`3` files / `8` tests), API/core/api-client TypeScript, storefront `astro check`, touched-file ESLint, regenerated OpenAPI/SDK (`256` paths), API Wrangler dry-run, storefront build/dry-run, dist-secret checks, deployed API version `34d7e61a-1778-4c93-944a-6c76425154d7`, deployed/current storefront version `6979ad04-a792-44d7-8295-ff05880da333` with build `src-0806507f3fa00cd4`, live OpenAPI showing `/api/v1/orders/{id}` absent and receipt/status present, live API/storefront customer-auth no-store headers, and live storefront `/` warmed from `MISS` to `HIT`.
- **2026-06-18 product filter cache invalidation**: Product catalog writes now invalidate the `attributes` group in addition to product/search/collection caches, so create/update/delete/restore/permanent-delete paths that change product category or product attribute values also clear public filter metadata (`api:attributes:filterable`, category/category-slug/search-filter caches) and storefront `filterable_attrs_*` L1/L2 prefixes. Regression tests cover admin path mapping, API KV prefix deletion, and storefront purge payload prefixes. Verified with focused Vitest (`2` files / `23` tests), touched-file ESLint, API TypeScript, API Wrangler dry-run, deployed API version `ffab9b01-6039-4048-8d32-6391cc05a29c`, live API health `200`, live `/api/v1/attributes/filterable` `200`, live `/api/v1/attributes/category-slug/drinks` `200`, and deployed Worker bundle markers showing `products: ["products", "search", "collections", "attributes"]`.
- **2026-06-18 category cold path dedupe**: Category pages no longer fetch `/api/v1/categories/{slug}` separately from `/api/v1/categories/{slug}/products`; the category-products endpoint now documents and returns the full storefront category object (`description`, image, SEO, `createdAt`, `updatedAt`) and the storefront helper carries that category plus an explicit `categoryNotFound` marker for 404 preservation. Transient category-products failures now return `null` instead of caching empty product lists. Guard tests assert category pages do not reintroduce `getCategoryBySlug()` and API category-products responses keep full metadata. Verified with focused Vitest (`3` files / `10` tests), touched-file ESLint, API/core/api-client TypeScript, storefront `astro check`, API Wrangler dry-run, storefront production build/dry-run, dist-secret cleanup/check, regenerated OpenAPI/SDK, deployed API version `7fdc8320-b169-4be8-a2e2-ed840e47a1e5`, deployed storefront version `b8714e74-a5ca-4bab-8019-d666e77dfcd9` with build `src-7c6a74ccdb602dbf`, live category-products metadata smoke for `drinks`, live `/categories/drinks` `MISS` then `HIT` (`TTFB 0.077s` on warm hit), sitemap categories `200`, and built storefront chunks containing `productsPromise.then`/`categoryNotFound` with no category-page `getCategoryBySlug`.
- **2026-06-18 admin order detail resume hardening**: Order detail keeps the 30s order/shipment polling needed for webhook-driven payment and delivery updates, but no longer opts into `refetchOnWindowFocus` or `refetchOnReconnect`. Returning to a long-idle tab now follows the global no-stampede QueryClient defaults instead of refreshing the old detail page while the merchant navigates away. Guard tests assert polling remains and focus/reconnect overrides stay absent. Verified with focused route-graph Vitest (`9` tests), admin TypeScript, touched-file ESLint, admin build, Wrangler dry-run, deployed admin version `10bb2634-e934-4e41-a9de-ca28e43ac43d`, live unauthenticated smoke (`/auth/login` 200, `/admin` 307 to `/auth/login`, `/api/v1/admin/products` 401), and built order-detail chunks containing `refetchInterval: 3e4` with no order-detail focus/reconnect overrides.
- **2026-06-18 admin secondary loader hardening**: Product list, cache settings, and inventory route loaders no longer block route entry on secondary data reads. Products still warms the primary list, but category options and product stats move to client-only background prefetches; cache settings and inventory now rely on their component loading states while starting client-only prefetches. Guard tests assert these routes do not reintroduce blocking `ensureQueryData()` for nonessential reads. Verified with focused route-graph Vitest (`9` tests), admin TypeScript, touched-file ESLint, admin Vite production build, dist-secret cleanup/check, and admin Wrangler dry-run. Deployed `scalius-admin-v2` version `0b47415a-5bc0-4190-904d-180c0dd5c825`; live smoke confirmed `/auth/login` `200`, unauthenticated `/admin` `307 Location: /auth/login`, and unauthenticated `/api/v1/admin/products` `401`, while local deployed dist shows `prefetchQuery()` for products/cache/inventory route loaders.
- **2026-06-18 admin read timeout hardening**: Admin service-function reads and the browser `/api/v1/admin/*` proxy now bound `GET`/`HEAD` API fetches with a 15s timeout that stays active through JSON/text body consumption, while write methods and POST-based widget/AI/import streams remain unbounded to avoid false mutation uncertainty. Timeout responses use the `ADMIN_API_READ_TIMEOUT` envelope at the proxy boundary. Verified with focused admin Vitest (`2` files / `7` tests, including slow-body and service-binding cases), route-graph Vitest, admin TypeScript, touched-file ESLint, admin Vite production build, dist-secret cleanup/check, and admin Wrangler dry-run. Deployed `scalius-admin-v2` version `dfbfc89f-8f84-43e1-8520-53ab7a6abb28`; live smoke confirmed `/auth/login` `200`, unauthenticated `/admin` `307 Location: /auth/login`, unauthenticated `/api/v1/admin/products` `401`, and local deployed dist contains `ADMIN_API_READ_TIMEOUT` wired into the server router.
- **2026-06-18 admin intent preload hardening**: Admin TanStack Router now keeps global `defaultPreload: false`, removing the idle/hover path where `defaultPreload: "intent"` could start protected route loaders and refresh `/admin` auth/RBAC context after the short client guard cache expired. Route graph guards now assert the disabled global preload alongside the existing focus-refetch and nested-scroll invariants. Verified with focused admin Vitest, admin TypeScript, touched-file ESLint, admin Vite production build, dist-secret cleanup/check, and admin Wrangler dry-run. Deployed `scalius-admin-v2` version `24a1c237-3f2c-446a-a0ef-58ecf419d2ae`; live smoke confirmed `/auth/login` `200`, unauthenticated `/admin` `307 Location: /auth/login`, and deployed admin assets contain `defaultPreload:!1`.
- **2026-06-18 storefront cold search/page trim**: Storefront search/category `q` values now normalize whitespace through `normalizeSearchQuery()` before SSR data fetches, product-list L2 key construction, search-filter attribute cache keys, HTML Cache API keys, pagination links, sort links, and filter form submissions. Equivalent URLs such as `/search?q=fish` and `/search?q=%20fish%20&page=1&sortBy=newest&utm_source=ad` now share the same edge HTML/data cache lanes. CMS page routes also trust consolidated page render-data widgets instead of issuing a second `getActiveWidgetsForScope("page", id)` call when no page widget is present. Verified with focused storefront Vitest (`2` files / `16` tests), storefront `astro check`, touched-file ESLint, storefront build/dry-run, and dist-secret cleanup/check. Deployed `scalius-storefront` version `50dc8ec8-4934-46fb-9e2e-629a641bce4d` with build `src-5c1b770be9954453`; live smoke confirmed `/search?q=fish` filled the new build namespace as `MISS`, then the noisy equivalent URL returned Cloudflare/storefront `HIT` on the same build.
- **2026-06-18 exact purge warming bounds**: Storefront exact `htmlPaths` are now sanitized at both the API invalidation boundary and the storefront purge endpoint: only relative paths are accepted, duplicates collapse in original order, and exact HTML warming is capped at `20` paths. The storefront warms those exact paths in batches of `4`, keeping `executionCtx.waitUntil()` work bounded while exact product data generations still cover all supplied exact data keys. Verified with focused storefront/API Vitest (`3` files / `32` tests), storefront `astro check`, API TypeScript, touched-file ESLint, storefront build/dry-run, API Wrangler dry-run, and dist-secret check. Deployed `scalius-api` version `a27af754-dfe4-4176-b7b7-11adf7c097f2` and `scalius-storefront` version `e79b7dba-36a4-4fa9-a228-aa08bd10d931` with build `src-1498bc0a5a0ca7e4`; live smoke confirmed API health `200`, `/api/purge-cache` GET `405 Allow: POST`, JSON POST without token `401`, and `/products/fish` moved from `MISS` to Cloudflare/storefront `HIT` on the new build.
- **2026-06-18 cache query canonicalization**: Storefront HTML Cache API keys, exact HTML purge-delete keys, storefront product-list L2 keys, and API KV `varyByQuery` keys now canonicalize query params so equivalent URLs do not fragment caches by parameter order, tracking params, or explicit defaults. Product detail HTML still ignores only client-side `size`/`color`, while category/search pages keep those names as real attribute filters. API `queryDefaults` are path-aware for `/products` versus `/products/search`, and category/search defaults are declared at their route middleware. Verified with focused storefront/API Vitest (`5` files / `44` tests), storefront `astro check`, API TypeScript, touched-file ESLint, API Wrangler dry-run, storefront build/dry-run, and dist-secret check. Deployed `scalius-api` version `6432e3d7-6980-4d24-844d-cb390bef6a00` and `scalius-storefront` version `070611c5-39c6-455b-817d-aa08cc1eae3d` with build `src-3ca651b24a939323`; live smoke confirmed API product query defaults/order moved to KV `HIT` after propagation, storefront search default/tracking/reordered URLs went `MISS` then `HIT`, and `/products/fish?size=XL&color=red&utm_source=ad` shared the base product HTML cache entry.
- **2026-06-18 selective prefix purge warming**: Storefront selective prefix purges now warm critical pages whenever they move the global KV cache version, closing the cold-bust gap where data-prefix purges invalidated all versioned HTML/L2 keys but skipped homepage/product warmup. Admin cache settings copy now labels non-HTML groups as `Data prefixes` instead of implying they never warm, and storefront cache docs describe the version-bump/warm behavior. Verified with focused Vitest (`3` files / `30` tests), storefront `astro check`, admin TypeScript, touched-file ESLint, storefront/admin production builds, storefront/admin Wrangler dry-runs, and dist-secret cleanup/check. Deployed `scalius-storefront` version `58b42517-22e8-4b24-97d9-70d2fa9c26e2` with build `src-d4927df3916c51aa` and `scalius-admin-v2` version `f8d1bee5-f4ab-445f-ad7e-f176a9419df7`; live smoke confirmed `/api/purge-cache` GET `405 Allow: POST`, unauthorized POST `401`, storefront `/` and `/products/fish` Cloudflare `HIT` with build `src-d4927df3916c51aa`, unauthenticated `/admin` SSR redirect, demo sign-in `200`, and `/admin/settings/cache` rendered `Data prefixes` without the old `Prefix only` wording.
- **2026-06-18 inventory cache blast-radius reduction**: Admin inventory stock-only writes (`/{variantId}/adjust`, `/stock-adjust`, `/stock-set`) now call `invalidateProductAvailabilityCaches(db, { variantIds: [...] }, c)` after the core stock operation commits instead of `invalidateCatalogCaches("products", c)`. This keeps stock/scanner/stocktake changes fresh for exact product detail/search/storefront product HTML without broad product/search/collection/homepage version busting. `ADMIN_PATH_TO_GROUPS` no longer maps `/api/v1/admin/inventory/*` to catalog groups, so the admin cache metadata matches route behavior. Verified with focused Vitest (`2` files / `24` tests), API TypeScript, touched-file ESLint, API Wrangler dry-run, and API dist-secret check. Deployed `scalius-api` version `f841e54d-e758-4dbf-a65d-a96e4c6298da`; live smoke confirmed API health/docs `200`, authenticated `/api/v1/cache/groups` `200` with no inventory path mapping and product mapping intact, and authenticated scanner lookup reached route logic and returned expected `404` for a dummy code.
- **2026-06-18 settings save false-500 hardening**: Payment gateway/settings invalidators now clear the in-memory cache first and treat legacy `gw:stripe`, `gw:sslcommerz`, `gw:polar`, and `gw:payment_methods` KV deletes as best-effort cleanup, so a transient KV delete failure cannot make an already-committed merchant settings save look failed. Currency settings writes now best-effort delete legacy `gw:currency` and always continue to layout/checkout group invalidation. Docs now state gateway credentials/settings caches are in-memory only and legacy KV cleanup is not the success boundary. Verified with focused Vitest (`3` files / `18` tests), API/core TypeScript checks, touched-file ESLint, API Wrangler dry-run, and API dist-secret check. Deployed `scalius-api` version `80b7c8ce-f444-4af2-84ed-e2950bb61152`; live smoke confirmed API health/docs `200` and `/api/v1/checkout/config` `200` with gateway/currency data and KV `HIT` after propagation.
- **2026-06-18 storefront/API cache freshness hardening**: API hero slider caching now varies by explicit `type=desktop/mobile` and bypasses the untyped User-Agent-derived list path; hero timestamp serialization tolerates invalid legacy `Date` values instead of returning a customer-facing 500. Scoped admin widget writes now send exact rendered `htmlPaths` for product/category/page/collection placements while reserving global version bumps for homepage/global widget changes. Storefront KV version and exact generation keys now use a canonical namespace (`CACHE_NAMESPACE`, else `STOREFRONT_URL`, else request host), exact purges delete old generation-specific L2/HTML keys after reading the pre-bump generation, and docs capture the Cache API/KV split. Verified with focused Vitest (`7` files / `43` tests), API TypeScript, storefront `astro check`, touched-file ESLint, storefront build, API/storefront Wrangler dry-runs, and dist-secret checks. Deployed `scalius-api` version `a1c33eac-ef8a-44b2-9754-aa74da3cdecc` and `scalius-storefront` version `b069c5f4-d263-453d-8a1c-0cdbedecb24a` with build `src-9937dddd4e971f12`; live smoke confirmed API health/docs `200`, hero desktop/mobile typed `200` with later `HIT`, untyped mobile-UA hero `200` with no `X-Cache`, storefront `/` and `/products/fish` `HIT` under the canonical project marker, `/api/purge-cache` GET `405 Allow: POST`, unauthorized POST `401`, and the malformed production desktop hero `createdAt` safely returns `null`.
- **2026-06-18 own-courier fulfillment UI/contract**: Admin order detail now exposes an Own Courier fulfillment dialog backed by the existing `POST /api/v1/admin/orders/{id}/fulfill` core flow, with per-item fulfillment status in the API/core/admin contract so already-shipped/delivered items cannot be reselected. Manual fulfillment records courier name, tracking URL, note, shipment amount, shipment items, and final-shipment intent; shipment history renders those fields and hides provider refresh actions for manual rows. The generated OpenAPI/SDK contract was regenerated after adding the fulfillment/shipment fields. Verified with focused admin/core Vitest, admin/API/core/api-client TypeScript checks, touched-file ESLint, API/admin production builds and Wrangler dry-runs, dist-secret cleanup/check, live API health/docs/OpenAPI smokes, live unauthenticated fulfillment `401`, and production dashboard asset byte checks. Deployed `scalius-api` version `66c860c6-f74a-4d8d-8b9a-7cd54302a3d7` and `scalius-admin-v2` version `b2e64811-3335-4931-9ed0-316281594a41`.
- **2026-06-18 admin reconnect/refetch stampede hardening**: Admin TanStack Query defaults now live in `apps/admin-v2/src/lib/admin-query-client.ts` with `refetchOnWindowFocus: false` and `refetchOnReconnect: false`; the router consumes that factory instead of constructing ad hoc defaults. Realtime surfaces opt in explicitly: cache settings stats/metadata and order detail/shipment polling set both focus and reconnect refetch, while the orders list keeps its merchant-controlled auto-refresh toggle but pauses invalidations while `document.hidden` and performs one explicit refresh when the tab becomes visible. Guard tests cover the defaults, opt-ins, and visibility-aware auto-refresh markers; invoke focused admin tests directly when working outside the root Vitest config. Verified with focused admin Vitest, admin TypeScript, touched-file ESLint, admin Vite build, dist-secret cleanup/check, Wrangler dry-run, and live asset byte checks. Deployed `scalius-admin-v2` version `432ae5b6-564a-4ecc-90d5-efab0913cf57`; live smoke confirmed `/auth/login` 200, `/admin` unauthenticated SSR redirect to `/auth/login`, live `index-BntOKWc-.js` byte-matches local dist and contains `refetchOnReconnect: false`, and live `orders-Deiqdz3w.js` byte-matches local dist with `document.hidden` / `visibilitychange` auto-refresh guards and no `refreshIntervalRef` symbol.
- **2026-06-18 FCM push fanout hardening**: Admin push notification fanout now sends Firebase HTTP v1 requests with bounded concurrency instead of one-token-at-a-time delivery. `FCMMessagingService` preserves response order so `sendOrderNotification()` can still deactivate invalid FCM tokens by original token index, uses Web Crypto-backed retry jitter instead of `Math.random()`, and exposes optional API runtime tuning through `FCM_SEND_CONCURRENCY` clamped to 1-20 with default 8. Firebase and notification docs now document the Cloudflare Email default/Resend fallback truth and keep Firebase-only admin push and external-only SMS as explicit provider gaps. Verified with focused core Vitest, core/API TypeScript checks, touched-file ESLint, `node scripts/check-worker-env.mjs`, API Wrangler dry-run, and dist-secret checks. Deployed `scalius-api` version `e7f53ca1-bf5e-4717-acb8-9d994e0d3d42`; live smoke confirmed `/api/v1/health` 200, `/api/v1/openapi.json` 200, and `/api/v1/docs` 200. No real FCM notification was sent during smoke; the live FCM path is covered by the ordered fanout unit test.
- **2026-06-18 exact product cache generations**: Exact product storefront purges no longer depend on Cloudflare Cache API `cache.delete()` for cross-colo freshness. `apps/storefront/src/lib/cache-generations.ts` defines per-host KV generation keys for `product_slug_*` and `product_variants_*`; product data L1/L2 keys include that generation, product HTML cache keys include `cache_gen`, and generation lookup failure bypasses exact product caching instead of serving a possibly stale L2 entry. `/api/purge-cache` now bumps exact product generations, keeps local Cache API deletes as best-effort cleanup, clears exact L1 prefixes, and warms affected product paths without bumping the global storefront version. Verified with focused storefront cache Vitest, API cache-invalidation Vitest, storefront TypeScript, `astro check`, touched-file ESLint, storefront Astro build, Wrangler dry-run, and dist-secret cleanup/check. Deployed `scalius-storefront` version `1ba15a62-76fa-46d8-847c-dd1001efe1c8` with build `src-4d472f79420d56e1`; live smoke confirmed `/products/fish` returned `MISS; ... gen=0` then `HIT; ... gen=0`, `/` returned MISS then HIT without a generation marker, and `/api/purge-cache` GET stayed `405 Allow: POST`.
- **2026-06-18 Cloudflare-native analytics and Meta version hardening**: Analytics scripts now support first-class `cloudflare_web_analytics` in core validation, admin forms/lists, OpenAPI, and the generated SDK. Cloudflare Web Analytics token-only config is normalized to the official `static.cloudflareinsights.com/beacon.min.js` snippet, `usePartytown` is forced off, and the admin defaults the script to `body_end`. Storefront browser analytics now passively bridges product/search/checkout/order events to Cloudflare Zaraz when `window.zaraz` is present, without making Zaraz mandatory or sending explicit CAPI PII through that bridge. Meta CAPI and WhatsApp OTP now share `META_GRAPH_API_VERSION = "v25.0"` instead of hardcoded expired `v19.0`. Docs capture the remaining CAPI reliability/privacy backlog: shared browser/server dedup IDs, purchase reload idempotency, public `/meta/events` abuse control, Meta token encryption, CAPI log redaction, and settings cache/RBAC consistency. Verified with focused core/storefront Vitest, core/API/admin/api-client TypeScript checks, storefront Astro check, touched-file ESLint, API Wrangler dry-run, admin/storefront production builds, and dist-secret checks. Deployed `scalius-api` version `422248ef-4ad7-48ae-b15c-650ac7d5ccc2`, `scalius-admin-v2` version `ae3622b7-f3c0-4a25-a222-f409bb33208c`, and `scalius-storefront` version `5781f9c7-d830-493b-9fbc-d8778ce3ea38`; live smoke confirmed `/api/v1/health` 200, live OpenAPI includes `cloudflare_web_analytics` and keeps analytics update booleans required, `/api/v1/docs` 200, storefront `/` 200 with Cloudflare Insights CSP hosts, the deployed admin `AnalyticsForm` bundle includes the Cloudflare Web Analytics UI markers, and the deployed storefront analytics bundle includes Zaraz/Product Added/Order Completed markers.
- **2026-06-18 Meta CAPI reliability/privacy hardening**: Pixel/CAPI browser events now share Meta-compatible deduplication ids (`eventID` for Pixel, `eventId`/`event_id` for CAPI), `/order-success` uses a local browser guard keyed by order id to prevent purchase reload replays, and product/purchase CAPI dispatch no longer depends on `window.fbq` being available. The public `/api/v1/meta/events` route accepts caller event ids, validates `eventSourceUrl` against `STOREFRONT_URL`, rate-limits through API KV when available, uses shared client-IP extraction, and safely falls back when Hono has no Worker `executionCtx`. Meta access tokens are encrypted on new admin saves with `CREDENTIAL_ENCRYPTION_KEY` while reads gracefully tolerate legacy plaintext; Meta settings writes invalidate the `layout` cache group and the admin route/nav now require analytics permissions. CAPI logs redact `user_data`, `test_event_code`, and event-source URL queries before write and again on admin reads for legacy rows; the generated OpenAPI/SDK contract now exposes `eventId`, `429`, and `responsePayload`. Verified with focused storefront/API/core Vitest, full root Vitest (`144` files / `868` tests), API/admin/core/api-client/storefront typechecks, storefront Astro check, touched-tree ESLint, API Wrangler dry-run, admin/storefront production builds, and dist-secret checks. Deployed `scalius-api` version `31e640ab-d472-41a6-8509-31e9e21026c9`, `scalius-admin-v2` version `5bc7fbd1-450e-4bd2-a0c3-aefcdfef18b2`, and `scalius-storefront` version `2cad9f03-bc97-4b3c-9b2a-592229ab12d9`; live smoke confirmed `/api/v1/health` 200, `/api/v1/docs` 200, storefront `/` 200, live OpenAPI has CAPI `eventId`, `429`, `responsePayload`, and no `responseBody`, deployed storefront assets include the purchase replay guard plus Pixel `eventID`, deployed admin Meta logs render `responsePayload`, and an untrusted live `/api/v1/meta/events` request returns `400 VALIDATION_ERROR` before dispatch.
- **2026-06-18 Cloudflare-native transactional email default**: Transactional email now defaults to Cloudflare Email Service through the API/admin Workers `EMAIL` `send_email` binding, with Resend retained as an encrypted-key fallback. Runtime email selection reads `settings.email_provider`, decrypts `resend_api_key` with the Worker credential key, receives DB/env context from queues, Better Auth callbacks, order notifications, and admin invitations, and fails with metadata-only provider-unavailable logs when no provider is configured. The admin email settings UI now exposes Cloudflare/Resend selection, binding/key status, and shared sender validation; `/api/v1/admin/settings/email` returns provider/binding status and skips masked Resend keys on save. The Worker Env guard recognizes `send_email[].name`; OpenAPI and the generated API client were regenerated. Verified with focused Vitest for provider selection, queue context, notification context, and settings API behavior; core/API/admin/api-client TypeScript checks; touched-file ESLint; `node scripts/check-worker-env.mjs`; API/admin Wrangler dry-runs showing `env.EMAIL (unrestricted) Send Email`; API/admin builds and dist-secret checks. Deployed `scalius-api` version `4dc9133c-fa05-4360-a4f3-c424031d98dc` and `scalius-admin-v2` version `7bf4208f-56c9-48bd-9b5b-7ec17b4470dc`; live smoke confirmed `/api/v1/health` 200, live OpenAPI includes `provider`, `cloudflareBindingConfigured`, and `resendConfigured`, unauthenticated `/api/v1/admin/settings/email` returns the expected 401 guard, and the live admin login shell plus deployed settings bundle contain the Cloudflare Email UI. No real transactional email was sent during smoke.
- **2026-06-18 public listing query consolidation**: Commit `ffda7a71` moves product/category listing query ownership into `packages/core/src/modules/products/products.storefront.ts`: `/api/v1/products` now uses shared `resolvePublicAttributeFilters()`, `/api/v1/categories/{slug}/products` delegates to `getStorefrontCategoryProducts()`, category products reuse shared predicates/sort/attribute subqueries without global-list variant/category enrichment, and discount sorting now guards flat-discount division by zero. `/api/v1/attributes/search-filters` is API KV-cached through `api:attributes:search-filters`, invalidated by both search and attribute groups, and now selects distinct matching category IDs without the old 100-product cap. Storefront category pages map URL `q` to API `search` instead of forwarding it as a fake attribute filter. Verified with focused Vitest guards, API/core TypeScript checks, ESLint for touched files, API Wrangler dry-run, storefront `astro check`, storefront build with generated build id `src-b40ef73385b0d2d7`, and dist-secret checks. Deployed `scalius-api` version `6b2dd94a-9041-4a4b-b7cf-4f29906a2c9b` and `scalius-storefront` version `e85324f2-83b7-4488-a73b-bcbb50e6c0ac`; live smoke confirmed `/api/v1/health` 200, `/api/v1/products?search=fish` and `/api/v1/categories/laptop/products?...search=fish` return 200 and move to KV HIT, `/api/v1/attributes/search-filters?q=fish` returns MISS then HIT, and `/categories/laptop?q=fish` returns 200 with storefront MISS then HIT under build `src-b40ef73385b0d2d7` while the generated category-products API query carries `appliedFilters.search = "fish"`.
- **2026-06-18 storefront cold-path/read-cache latency pass**: Commit `ea8cb036` removes avoidable cold-cache waterfalls by starting product detail layout/product/scoped-widget reads in the first dependent wave, making search/all-products layout/product/filter reads explicit first-wave promises, parallelizing storefront product-list row/count reads plus image/category enrichment, parallelizing category route category/attribute and row/count reads, and moving API `cacheMiddleware` cache-miss KV writes into `executionCtx.waitUntil()` when available. Added regression guards in `apps/storefront/src/lib/page-data-boundaries.test.ts`, `apps/api/src/routes/categories-boundaries.test.ts`, `apps/api/src/middleware/cache.test.ts`, and `packages/core/src/modules/products/products.storefront-boundaries.test.ts`. Verified with focused Vitest, API/core TypeScript checks, storefront `astro check`, lint for touched files, API Wrangler dry-run build, storefront build with regenerated build id `src-f229bbe0cee842bd`, and dist-secret checks. Deployed `scalius-api` version `c112e77c-0708-41f7-b267-59418a05f625` and `scalius-storefront` version `8703bec9-32cc-4864-aaf1-3bc775aadf1b`; live smoke confirmed API `/api/v1/health` 200 and `/products/fish`, `/categories/laptop`, and `/search?q=fish` each returned 200 with MISS then HIT under build `src-f229bbe0cee842bd`.
- **2026-06-18 exact storefront product purge lane**: Commit `1aca2a9a` extends API storefront purge payloads with `exactKeys` and `htmlPaths`, and teaches `apps/storefront/src/pages/api/purge-cache.ts` to delete current-version L2 API cache keys plus exact product HTML Cache API entries without bumping the global KV cache version. Broad prefix purges still keep the version-bump safety behavior because Workers Cache API cannot globally delete by prefix. Verified with focused Vitest for API cache invalidation and storefront purge route, API TypeScript check, storefront `astro check`, lint for touched files, API Wrangler dry-run build, storefront Astro build, and dist-secret checks. Deployed `scalius-api` version `49bc28fa-49c4-408b-a48b-802ae8e3c61a` and `scalius-storefront` version `7a2640ee-4760-462b-a9f5-c5dd159e21ca`; live smoke confirmed `/api/v1/health` 200, `/api/purge-cache` GET returns 405 with `Allow: POST`, and `/products/fish` served MISS then HIT under cache version `1780000082`. Production authorized purge was not invoked because the purge token is a runtime secret not exposed to the shell; exact purge semantics are covered by unit tests against the route handler and Cache API delete calls.
- **2026-06-18 targeted product availability cache invalidation**: Commit `2d01292a` adds `invalidateProductAvailabilityCaches()` and subject preloading helpers so stock-changing order, payment, delivery, queue, and cron paths clear only affected API product detail/search keys plus exact storefront `product_slug_` / `product_variants_` prefixes. Destructive order edits/deletes preload old product subjects before committed writes, delivery reconciliation invalidates even when same-status retries only repair `inventoryAction`, and the order-ingest queue also purges from attempted variant IDs after it finishes isolated ack/retry handling. Verified with `./node_modules/.bin/vitest run apps/api/src`, `./node_modules/.bin/tsc --noEmit -p apps/api/tsconfig.json`, root ESLint for `apps/api/src/`, API Wrangler dry-run build, and `node scripts/clean-dist-env-files.mjs apps/api --check`. Deployed `scalius-api` version `ab086239-80b6-4254-9871-17ac50e4f835`; live smoke confirmed `/api/v1/health` 200, product list 200 with KV middleware, and `/api/v1/products/fish` moved from `x-cache: MISS` to `x-cache: HIT` after KV propagation.
- **2026-06-18 admin idle navigation + scroll restoration**: Commit `03396af2` disables global TanStack Query window-focus refetches, opts volatile cache stats queries back into focus refetch, registers `#admin-main-scroll` with TanStack Router scroll restoration, and removes the layout-level manual route-change `scrollTo()` race. Verified with `./node_modules/.bin/vitest run apps/admin-v2/src/lib/route-graph-boundaries.test.ts`, `./node_modules/.bin/tsc --noEmit -p apps/admin-v2/tsconfig.json`, admin Vite production build, and `scripts/clean-dist-env-files.mjs apps/admin-v2 --check`. Deployed `scalius-admin-v2` version `e1618880-c53b-47eb-aecc-a4a787ef36de`; live smoke confirmed `/admin` unauthenticated SSR redirects to `/auth/login`, `/auth/login` returns 200 at about `0.16s` TTFB, and the live `index-Dw_NwViz.js` plus `admin-ethQrfXv.js` assets contain the new `refetchOnWindowFocus: false`, `scrollToTopSelectors`, and `admin-main-scroll` restoration markers. Follow-up commit `40375de4` adds `useAdminNestedScrollRestoration()` so normal client navigation resets the nested scroller to top while browser Back/Forward restores saved positions. Verified again with the focused Vitest guard, admin TypeScript check, admin Vite production build, and dist-secret check; deployed `scalius-admin-v2` version `e4088938-3ca6-4504-9272-3cdab8475272`. Authenticated live smoke on `dashboard.scalius.com` scrolled Products to `533.5`, navigated Categories at `0`, then browser Back restored Products to `533.5` with no console errors.
- **2026-06-18 storefront category cold-cache loading**: Commit `55a7dd3e` starts category layout, category metadata, product list, filter attributes, and widget reads in the first promise wave for `apps/storefront/src/pages/categories/[slug].astro`, removing the avoidable category-page waterfall. Verified with `./node_modules/.bin/vitest run apps/storefront/src/lib/page-data-boundaries.test.ts`, storefront build ID generation, `apps/storefront/node_modules/.bin/astro check`, `apps/storefront/node_modules/.bin/astro build`, and `scripts/clean-dist-env-files.mjs apps/storefront --check`. Deployed `scalius-storefront` version `1a36bb95-4a45-48b1-b8e4-23c181bdcbfd`; live `/categories/laptop` smoke showed first GET `x-cache-status: MISS; v=1780000082; build=src-4d7f97f6b8864dfd` at about `1.56s` TTFB, then Cloudflare HIT at about `0.06s` TTFB with product/category content intact.
- **2026-06-19 delivery notification queue hardening**: Delivery webhook/admin shipment status notification enqueue now lives in one API helper instead of duplicated route-local maps, and the dead `notifyShipmentStatusChange()` log placeholder was removed from core. Pathao, Steadfast, admin shipment check, order-scoped shipment refresh, single provider shipment creation, and bulk shipment creation now use the shared `ORDER_NOTIFICATIONS_QUEUE` path; already-shipped inventory-only bulk retries no longer send duplicate shipped messages. Admin FCM push payloads now receive the queued `notificationType` and label shipped/delivered/cancelled/refunded pushes correctly instead of always saying "New Order Created". The delivery webhook verifier now rejects malformed stored provider credential/config JSON as auth failure instead of crashing before unsigned-request rejection. Storefront Wrangler config now declares `CACHE_NAMESPACE = "storefront.scalius.com"` so the env guard and cache namespace contract agree. Verified against current Cloudflare Workers best-practice docs and `@cloudflare/workers-types@4.20260619.1`, focused Vitest (`6` files / `32` tests), API/core/storefront TypeScript checks, touched-file ESLint, `node scripts/check-worker-env.mjs`, API Wrangler dry-runs, storefront `astro check`, storefront production build with build `src-7825621b7217ee7a`, storefront Wrangler dry-run, API/storefront dist-secret checks, deployed `scalius-api` version `61c5553d-bea0-45ea-928d-f8312eabf447`, deployed/uploaded `scalius-storefront` version `34747b4e-b448-4833-926f-74117acc6b15`; live smoke confirmed API health/docs `200`, unsigned Pathao and Steadfast delivery webhook requests both `401`, and storefront `/` served Cloudflare `HIT` with `build=src-7825621b7217ee7a` and `project=storefront.scalius.com`.
- **2026-06-19 storefront exact write rewarm**: Admin product create/update/delete/restore/permanent-delete and variant writes now include affected `/products/{slug}` exact HTML paths in catalog invalidation before category/listing paths, so product detail pages are warmed after freshness bumps instead of staying cold. Admin CMS page create/update/bulk-publish/restore/single-restore now resolves post-write public `/{slug}` paths and forwards them through the existing `["pages", "layout"]` invalidation; delete/unpublish paths still invalidate freshness but intentionally skip rewarming pages that should disappear. Regression coverage added route-level product/page cache payload tests plus canonical product path assertions. Verified with focused Vitest (`5` files / `70` tests), API typecheck/lint, root typecheck/lint/test (`197` files / `1169` tests), `check:env`, dist-secret checks, API Wrangler dry-run, local API health/OpenAPI/docs `200`, deployed `scalius-api` version `dfdbadde-c402-4998-bebd-d70df121a3e1`, and live smoke: health/OpenAPI/docs `200`, authenticated pages list `200`, temporary published CMS page create `201`, first storefront request for that new page already `HIT; v=1780000086; build=src-7825621b7217ee7a; gen=0`, cleanup soft/permanent delete `204/204`, and the cleaned URL returned `404` with cache skipped.
- **2026-06-22 invited-admin onboarding hardening**: Admin user creation must never email or return temporary passwords. The invite path creates only an unshared random bootstrap password to satisfy Better Auth account creation, sends a Better Auth reset/setup link, and marks invited admins with `user.must_change_password` plus `user.must_enroll_two_factor`. API admin middleware live-reads those flags before RBAC and blocks admin endpoints until password setup and 2FA enrollment are complete; the admin dashboard direct-D1 guard redirects flagged sessions to password setup or 2FA setup. Future auth form work must also preserve the no-JS/pre-hydration guarantee from `AUTH-033`/`AUTH-034`/`AUTH-035`: credential, OTP, email, phone, password, token, payment secret, tracking secret, cart, customer, and discount values must not be able to enter URLs through native form fallback or GET validation endpoints. Treat the user-reported historical login submit fallback that encoded credentials into the URL as a recurring regression-audit target whenever admin, customer auth, checkout contact, payment, or discount forms are touched. Verified with focused/root gates, local browser invite smoke, remote migration `0060_admin_onboarding_flags`, API deploy `76e564cd-fa47-4b20-9a10-3156a67d09d2`, admin deploy `a1458364-4568-4728-934a-381745934456`, live OpenAPI/D1/auth-asset/no-JS login smokes, and live unauthenticated admin-users `401`.
- **2026-06-22 order-detail optional panel isolation**: Admin order detail required data is the order itself; optional secondary panels such as payment history, COD tracking, delivery providers, shipment history, and future timelines must either use card-local non-suspense query states or a tight card boundary. Do not let a payment/provider/timeline read failure bubble into the page-level `OrderView` error boundary and blank the merchant's post-sale workspace. `ADMIN-015` converted payment history to card-local loading/error/retry UI and was verified with focused/root gates, admin deploy `c4d49c8b-4d18-46a1-a526-0eaa37c0cdd2`, and live order detail smoke for `1HBUHH`.

## Known Backlog / Limitations

- **Active audit backlog**: Check `audit/REMEDIATION_TRACKER.md` before choosing the next remediation slice. The prior live storefront missing-CDN-object reference was fixed through the admin product update path and verified against API data, D1 rows, CDN response, and storefront HTML.
- **Mixed provider systems**: Universal provider registry currently has Stripe payment + Resend email adapters, while transactional email runtime selection now uses Cloudflare Email Service by default with Resend fallback. SMS still uses the legacy integrations registry with smsnetbd, bdbulksms, mimsms, and gennet. Delivery uses legacy factory/provider files; universal delivery provider exports are type-only.
- **In-memory state**: Storefront L1 caches and shared layout cache are in-memory and reset on Worker isolate restart. Generic low-risk throttles may still use the shared KV rate limiter, but auth, setup, checkout, payment, and inventory authorities must use D1/DO/queue-backed coordination instead of KV get-then-put.
- **Notification/OTP delivery receipts**: Order notifications and customer OTP delivery now have D1 delivery receipts, Resend idempotency keys, deterministic GenNet `csms_id`, Meta WhatsApp message-id capture, and provider receipt recording. Remaining work: revisit Cloudflare Email/FCM/WhatsApp if first-class provider idempotency keys become available.
- **Scanner app**: Standalone `/scanner` route exists with D1 single-use QR-token auth and KV scanner sessions; still needs focused UI polish beyond the auth exchange.
- **Type safety**: Admin still has notable `any` usage in some UI edges and broad DTO adapters. Improve locally when touching relevant code, but do not broad-refactor casually.
- **Test coverage**: There are dozens of Vitest files across apps/packages, not a comprehensive suite. Add focused tests for risky service, queue, payment, inventory, response-envelope, AI, or storefront cache changes.
- **Generated docs are non-authoritative**: Trust `packages/api-client/openapi.json`, generated SDK files, Drizzle schema, and migration metadata over prose counts.

## Agent Team Guidelines

When working as part of an agent team on this codebase:

- **Avoid file conflicts**: coordinate so each teammate owns different files/modules.
- **Domain boundaries**: `packages/core/src/modules/` is organized by domain; each teammate should own a complete domain when possible.
- **Run type checks**: use `pnpm typecheck` for meaningful validation. `pnpm build` bundles Workers/apps and can miss type errors that `tsc`/`astro check` catch.
- **Run lint honestly**: root `pnpm lint` intentionally filters out `@scalius/tsconfig` and runs real `lint` scripts for API, admin, storefront, api-client, core, database, and shared. Keep it warning-free; if a future warning is truly package/toolchain-dependent, document the exact reason instead of normalizing warning noise.
- **Run focused tests**: use `pnpm test` or direct Vitest filters for touched areas.
- **Run dependency audit**: use `pnpm audit --audit-level moderate` after dependency or lockfile changes.
- **Do not touch env files**: `.dev.vars` and `.env.development` can contain secrets.
- **Keep Turbo env cache inputs current**: app-local `.dev.vars`/`.env*` files belong in `turbo.json` `globalDependencies`, and build-time env names belong in `globalEnv`. Do not add secret-only names to `globalEnv`. Run `pnpm exec vitest run scripts/turbo-config.test.mjs` and a Turbo dry run after changing build env/config.
- **Schema changes need migrations**: after modifying `packages/database/src/schema/`, run `pnpm db:generate`.
- **API surface changes need SDK regeneration**: after changing OpenAPI route schemas, run `pnpm generate:sdk`.
- **Package changes need manifests**: when adding imports from a new package, update the consuming workspace `package.json`.
- **Storefront shared imports only**: do not add `@scalius/core` or `@scalius/database` imports to storefront without coordination.
- **Generated files are off-limits**: do not hand-edit `routeTree.gen.ts` or `packages/api-client/src/generated/**`.
- **Cloudflare bindings must stay synchronized**: update Wrangler config and Env declarations together.
- **Deploy shortcuts stay safety-gated**: root and package-local deploy scripts route through `scripts/deploy.mjs --only ...`; use `pnpm run deploy*` from the root to avoid pnpm's built-in `deploy` command, and keep typecheck, dist-secret checks, and required migration gates when changing deploy scripts.

---
> Source: [scaliuslabs/scalius-commerce-lite](https://github.com/scaliuslabs/scalius-commerce-lite) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-06-26 -->
