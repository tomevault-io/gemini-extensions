## termory

> This file is for Claude Code working in this repository. Keep it limited to current codebase facts, implementation rules, and verification steps.

# CLAUDE.md

## Scope

This file is for Claude Code working in this repository. Keep it limited to current codebase facts, implementation rules, and verification steps.

## Current App

Tauri v2 desktop app for browsing local history records from supported AI tool clients, plus their on-disk memory and skill files.

Supported sources in the current code:

- Codex
- Claude Code
- Gemini CLI
- OpenCode
- Grok Build (xAI — sessions + memory (`<grok-home>/memory/`) + skills + provider switching + session/project delete + official-quota (weekly/monthly credit window); no migrate)

Top-level UI shell — six activity-rail routes (⌘1..6 in order):

1. **Providers** — third-party API platform switcher (per CLI). Default landing route. One tab per managed app: Claude Code / Codex / Gemini / OpenCode / Grok Build (the five CLIs) **plus Claude Desktop** — the GUI app, managed via its 3P gateway profile (direct mode only); its tab always shows (like the other apps), and install/platform gates activation + the InstallGuide + gateway binding, NOT tab visibility. See "Claude Desktop (3P gateway profile)" under Providers.
2. **Records** — chat / transcript history. Three internal panes:
   - *Sessions* — per-tool transcripts
   - *Memories* — on-disk memory files (CLAUDE.md, AGENTS.md, GEMINI.md, `~/.codex/memories/`, `~/.claude/projects/<slug>/memory/`, etc.)
   - *Skills* — `SKILL.md` files under each tool's skills directory plus the cross-tool `.agents/skills/` location
3. **Favorites** — saved per-message snapshots
4. **Search** — substring search + Cmd-K palette
5. **Stats** — KPI cards + all-history calendar heatmap + a Tokens chart (Type / Model breakdown)
6. **Settings** — Appearance / Storage / Search history / Keyboard shortcuts / About

Plus a **system-level macOS menu-bar tray** (outside the in-app rail) for quick provider switching — see "Menu-bar tray" below.

Current alignment target: data acquisition and message preview formatting should follow the official tools. UI layout, source filters, project grouping, search, stats, cross-source sorting, and the Memory/Skills views are app behavior and do not need official UI parity.

## Tech Stack

- Desktop framework: Tauri v2
- Frontend: React 18, TypeScript, Vite
- UI / styling: Tailwind CSS v4 + shadcn/ui (Radix primitives); charts via `recharts`; virtualized lists via `@tanstack/react-virtual`; theming via `next-themes`; toasts via `sonner`
- Backend: Rust 2021
- Database access: `rusqlite` with bundled SQLite
- JSON parsing: `serde`, `serde_json`
- Filesystem scanning: `walkdir`, `dirs`
- Time handling: `chrono`
- Icons: `lucide-react` plus inline SVG brand icons

## Code Map

- Frontend: `src/main.tsx`
- Styles: `src/styles.css`
- i18n (en / 简体 / 繁體): `src/i18n/index.tsx` (`<I18nProvider>` + `useT()`) + `src/i18n/locales/{en,zh-Hans,zh-Hant}.ts` — see "Internationalization (i18n)" under UI conventions
- Frontend local-store wrapper: `src/config.ts` (routes `getConfig`/`setConfig` to the three backing files via IPC — `providers` → providers.json, `favorites` → favorites.json, everything else → config.json)
- Tauri IPC commands: `src-tauri/src/lib.rs`
- Session/Memory/Skill scanning, parsing, and formatting: `src-tauri/src/sessions.rs`
- Provider switching (activate / deactivate / reverse-derive / test / fetch-models): `src-tauri/src/providers.rs`
- Claude **Desktop** 3P gateway-profile materialization (direct mode only — apply / restore-official / reverse-derive / installed-detect): `src-tauri/src/claude_desktop.rs` (inline tests at the bottom) — see "Claude Desktop (3P gateway profile)" under Providers
- Codex "follow sessions" on provider switch (re-tag rollout-file + threads `model_provider` so `codex resume` keeps finding a project's history): `src-tauri/src/codex_follow.rs` (+ frontend `src/components/providers/CodexFollowDialog.tsx`) — see "Codex 'follow sessions' on provider switch"
- Official-account subscription quota (read CLI OAuth creds → query official usage endpoint): `src-tauri/src/quota.rs` (inline tests at the bottom)
- Codex multi-account management (save / switch / delete login snapshots, token refresh, needsRelogin flag, cancel login via `CodexLoginCancel` managed state): `src-tauri/src/accounts.rs` (inline tests at the bottom); UI: `src/components/providers/OfficialAccountsSection.tsx`
- macOS menu-bar tray (build menu / rebuild / click handler): `src-tauri/src/tray.rs`; icon assets `src-tauri/icons/tray-icon.png` (36×36 template) + `tray-template.svg` (vector source)
- Terminal launching (detect installed terminals / open-and-resume): `src-tauri/src/terminal.rs` (per-OS `#[cfg]`; chosen via Settings → Terminal, `terminal` config key)
- Local KV store (config.json + providers.json + favorites.json under `~/.termory/`, chmod 0600): `src-tauri/src/config.rs`
- Filesystem watcher (static CLI-data dirs + dynamic recursive session-cwd watches → debounced re-scan → `termory:sources-changed`): `src-tauri/src/watcher.rs`
- Stats aggregations (pure, window-accurate): `src/lib/stats-utils.ts` (+ `stats-utils.test.ts`)
- Stats UI: `src/components/stats/{StatsPage,OverviewSection,TokensChart}.tsx` + `heatmap-shared.ts` (+ `StatsPage.test.tsx`)
- Favorites helpers (pure, snapshot-based): `src/lib/favorites.ts` (+ `favorites.test.ts`)
- Favorites UI: `src/components/favorites/FavoritesPage.tsx` (+ `FavoritesPage.test.tsx`); star button + scroll-to-message hook live on `src/components/MessageList.tsx`
- In-session find (⌘F): `src/components/TranscriptFindBar.tsx` (bar UI) + `src/components/DocDetailView.tsx` (memory/skill doc detail with occurrence nav) + `src/lib/highlight-term.ts` (rehype `<mark>` pass) + `matchingLoweredIndices` in `src/lib/search-utils.ts`; state + ⌘F routing in `App.tsx` / `CommandPalette.tsx` — see "In-session find" section
- Tauri config: `src-tauri/tauri.conf.json`
- Rust parser/formatter tests: inline tests at the bottom of `src-tauri/src/sessions.rs`
- Rust provider/store tests: inline tests at the bottom of `src-tauri/src/providers.rs` and `src-tauri/src/config.rs`
- Rust watcher test: inline at the bottom of `src-tauri/src/watcher.rs` (`dynamic_paths_from_sessions` — absolute-path filter + dedup)

Current Tauri IPC commands (57 registered in `generate_handler!`), called from the frontend with `invoke(...)`:

**Scan & detail**
- `scan_all_sessions` — returns `ScanResult { projects: Project[], records: AppSession[] }` (see "First-class projects" — `projects` are enumerated folders/entities; `records` are session/memory/skill entries distinguished by `source`)
- `load_session` — loads one entry by `{ source, id }` (the path is resolved backend-side via `SESSION_PATH_INDEX`, never passed by the frontend)
- `search_all_sessions(query)` — case-insensitive substring search across all loaded session/memory/skill bodies (minimum query length: ONE character — a single CJK char is a valid query, user decision 2026-07-17; hits aggregate one per record); each `SearchHit` carries `first_match_index` for scroll-to-message

**CLI detection**
- `detect_clis()` — runs `which`-style probes for `claude` / `codex` / `gemini` / `opencode` binaries; returns `{ [app]: boolean }` for the Providers page InstallGuide gate. **Codex counts as installed when EITHER the CLI binary OR the desktop app is present** (they share `~/.codex/` auth + config, so provider management works with just the app). The desktop app is detected by macOS bundle id `com.openai.codex` scanned under `/Applications` + `~/Applications` (`codex_app_info_in`) — NEVER by `.app` name: since the 2026-07-09 merge the Codex app IS the unified ChatGPT desktop app (existing installs updated in place, so it's named `ChatGPT.app`; ChatGPT Classic keeps `com.openai.chat`). Windows distribution of the app is **Microsoft-Store-only** (no direct `.exe`/`.msi` on openai.com/download per OpenAI's own docs), so an MSIX package is the only install form to check (unlike macOS's `/Applications` scan). **Windows detection is split by cost, because `probe_codex_installs` feeds `detect_install_snapshot`'s HOT PATH (every tray-menu open / watcher rescan, on the caller/main-event thread) and a `Get-AppxPackage` spawn there loads the Appx module (0.5–1.5s cold) — it would freeze the tray on every click, whereas macOS's equivalent is a cheap plist read**: (1) **presence** (`providers::codex_appx_installed_in`, the hot-path `app: bool`) is a pure filesystem `read_dir` of `%LOCALAPPDATA%\Packages\` matching a `OpenAI.Codex_<publisherhash>` dir (the PackageFamilyName — publisher-hash suffix varies per signing identity, so prefix-match `OpenAI.Codex_`, never a hardcoded hash; this per-user Packages dir is readable, unlike `WindowsApps\`) — the SAME mechanism as Claude Desktop's `msix_package_roaming_parents`, path-injected + compiled off-Windows so it's unit-tested on any host; (2) the app **version** (`providers::codex_appx_version`, PowerShell `Get-AppxPackage -Name 'OpenAI.Codex' | … PackageFullName`, silent via `output_with_timeout`, parsed from the `PackageFullName`'s second `_`-segment by `parse_appx_package_version`) is fetched ONLY on the cold path via `providers::probe_codex_installs_detailed` (the `detect_codex_installs` IPC — Providers page load + Recheck; `app_version` is `None` in the hot-path `probe_codex_installs` and is discarded by `detect_install_snapshot` anyway, so nothing hot pays for it). Detect by package NAME (`OpenAI.Codex`), never the DisplayName — Windows brands the taskbar/Start entry "ChatGPT" (shared with the unified app) but the package identity stays `OpenAI.Codex` (confirmed on a real installed package name, `OpenAI.Codex_26.707.9564.0_x64__2p2nqsd0c76g0`, openai/codex#32772). **No Windows `bundledCli`**: MSIX packages install into the ACL-restricted `C:\Program Files\WindowsApps\`, which a normal process can't `read_dir`/open into, so there's no Windows equivalent of `<bundle>/Contents/Resources/codex` — `codex_binary()` stays standalone-CLI-only on Windows. Like Claude Desktop's Windows paths, the PowerShell version fetch + real package layout are unverified on real hardware (the filesystem presence scan + version parse ARE unit-tested off-Windows).
- `detect_codex_installs()` — Codex's install forms split: `{ cli, app, appVersion, bundledCli }`, probed backend-side by `providers::probe_codex_installs_detailed` (the cold-path variant: on macOS a single bundle resolution (`codex_app_info_in`) feeds `app`/`appVersion`/`bundledCli` from one Info.plist read; on Windows the cheap `probe_codex_installs` presence scan plus a PowerShell version fetch — see the `detect_clis` bullet for the hot/cold split). `bundledCli` is the runnable codex CLI shipped INSIDE the desktop app (`<bundle>/Contents/Resources/codex`, a self-contained native binary on the app's release channel — alpha builds) — the **fallback for app-only installs**: `providers::codex_binary()` = standalone CLI first, else bundled, and every codex spawn/launch seam routes through it (the `codex login` spawn in accounts.rs; terminal resume / New Session via `codex_shell_invocation()` in terminal.rs, which uses bare `codex` when the standalone CLI exists and the bundled binary's single-quoted absolute path otherwise — the bundled branch is macOS-gated, no Windows quoting path exists). Account add / re-login disable only when BOTH `cli` and `bundledCli` are false (`codexCliMissing` in ProvidersPage / `reloginUnavailable` on OfficialAccountsSection) — in practice that state means "no codex at all" (the tab shows InstallGuide), so the disabled-button tooltip is defensive. The frontend "Copy resume command" intentionally keeps the canonical bare `codex resume <id>` (it documents the command, not the fallback path). **Tray plumbing (`providers::InstallSnapshot`)**: `detect_install_snapshot()` returns `{ map: HashMap<CliApp,bool>, codex_terminal }` in one probe pass — `codex_terminal` = standalone CLI (INCLUDING the interactive-shell fallback, since the user's terminal resolves what the fixed-dir scan may miss) OR bundled CLI. The tray stores/compares the WHOLE snapshot (`INSTALLED` cache + `menu_installed_stale`), so a codex-terminal flip (standalone CLI added/removed while the app stays) triggers a full rebuild, and `terminal_clis` reads `snapshot.codex_terminal` with ZERO filesystem work on the main thread (the probe always runs on the caller's thread: watcher / IPC worker / tray-click handler).
- `detect_cli_versions_cmd()` — invokes each detected CLI with `--version` so the Providers page can show actual installed versions. The Codex entry is the CLI version ONLY; the frontend composes the Official card's version line from both forms via `codexVersionText` (provider-utils) → `v0.142.5 (CLI) · v26.707.31428 (App)` (whichever installed; labels are deliberate keep-English i18n keys `providers.codexVersion{Cli,App}`). `ProviderOfficialCard.version` is now preformatted text rendered verbatim (callers add the `v` prefix).
- `detect_terminals()` — lists the mainstream terminals installed on this OS (+ "auto") for the Settings → Terminal dropdown (`terminal::detect`)
- `resume_session_in_terminal(source, id, project)` — opens a session in the chosen terminal and resumes it (`terminal::resume_session`); driven by the Records / Favorites right-click menu, same path as the tray's recent-session click. Returns `Result<(), String>` so a launch failure (terminal missing / spawn error) surfaces as a toast in the right-click flow; the tray ignores the result (logs only)
- `new_session_in_terminal(source, project)` — opens the chosen terminal in `project`'s cwd and launches `source`'s CLI **fresh** (no resume, `terminal::new_session` — same path as the tray's New Session). Driven by the Records sidebar project-row "New session" action; failure → `menu.terminalError` toast

**Records migration & deletion (see "First-class projects, migration & deletion")** — frontend sends `(project, rel)`, never a raw path
- `migrate_claude_project(old_path, new_path, delete_old)` — move a renamed project's whole slug dir (sessions + companion `<id>/` dirs + auto-memory) into the new path's slug dir; returns `ClaudeMigrationResult { sessions, memory_files, old_dir, new_dir, new_project }`
- `migrate_claude_session(project, rel, new_path, delete_old)` / `migrate_claude_memory(project, rel, new_path, delete_old)` — migrate one session (+ companion dir) / one auto-memory; located by `project + rel`
- `migrate_codex_session(id, new_path)` / `migrate_codex_project(project, new_path)` — re-point a Codex session / a whole cwd's sessions to `new_path` by rewriting the rollout `payload.cwd` (authoritative — the `threads` table is a backfill cache) + `UPDATE threads.cwd`; returns `CodexMigrationResult { sessions, new_project }`. Pure metadata rewrite (no file move — Codex rollout files stay in `~/.codex/sessions/`). Project form includes archived. See "Migrate (Claude + Codex + Gemini)".
- `migrate_gemini_project(project, new_path)` — re-point a Gemini project's history to `new_path` by rewriting its `.project_root` ownership markers (tmp + history base dirs); gemini self-heals its `projects.json` registry from the markers on next run, so no file move + no registry write. Project-level only (Gemini sessions are project-scoped). Returns `GeminiMigrationResult { sessions, new_project }`. See "Migrate (Claude + Codex + Gemini + OpenCode)".
- `migrate_opencode_session(id, new_path)` / `migrate_opencode_project(project, new_path)` — re-point an OpenCode session / a whole cwd's sessions to `new_path` via `UPDATE session SET directory = ?, path = NULL` (by id / by directory); `project_id` left intact (no empty-project resurrection, no registry write). Returns `OpencodeMigrationResult { sessions, new_project }`. Regrouping convenience (resume-by-id works across renames). See "Migrate (…)".
- `migrate_grok_session(id, new_path)` / `migrate_grok_project(project, new_path)` — MOVE a Grok session / a whole cwd's session dirs into the destination cwd's `encode_cwd_dirname`-derived dir (`fs::rename`) + rewrite each `summary.json` info.cwd. Grok resumes per-cwd by scanning the encoded dir, so files physically move (paths change → `old_dir`/`new_dir` in the result for the frontend remap). Returns `GrokMigrationResult { sessions, new_project, old_dir, new_dir }`. See "Migrate (…)".
- `delete_claude_project(project)` — `remove_dir_all(~/.claude/projects/<sanitize(project)>)` (never touches the cwd)
- `delete_claude_session(project, rel)` / `delete_claude_memory(project, rel)` — delete one record, path built from `project + rel` + `is_safe_rel`
- `delete_gemini_project(project)` / `delete_gemini_session(project, rel)` / `delete_gemini_memory(project, rel)` — same, under `~/.gemini/tmp/<id>/` matched by `.project_root`.
- `delete_codex_session(id)` / `delete_codex_project(project)` — **real delete of sqlite rows by id/cwd + the rollout files**. Codex's `threads` table is a CACHE rebuilt from rollout files (see codex_follow.rs), so deleting only the row is reverted by backfill — we `fs::remove_file` each `rollout_path` FIRST, then `DELETE FROM threads WHERE id|cwd = ?`. **File-first is deliberate:** a crash after the committed DELETE but before file removal would let backfill resurrect the row; removing the authoritative file first means a crash leaves only a harmless re-deletable orphan row, never a resurrection. Project delete removes ALL of a cwd's threads INCLUDING archived ones (user-confirmed "delete = full"). `delete_codex_memory(rel)` removes one `.md` under `~/.codex/memories/` (bounded by `is_safe_rel`; Termory never reads `memories_1.sqlite`, so nothing to sync).
- `delete_opencode_session(id)` / `delete_opencode_project(project)` — `DELETE FROM session WHERE id` / `DELETE FROM project WHERE worktree` with `PRAGMA foreign_keys = ON` so the `ON DELETE CASCADE` to message/part (and project→session) fires; the session table is a durable primary store (NOT a replayed projection — verified OpenCode never replays events on startup), so a direct DELETE is permanent. Session delete also removes the session's **sub-agent children recursively** (`opencode_session_tree` walks the `parent_id` tree — `parent_id` has NO FK cascade, so we mirror the official `session.remove()` which recurses into `children()`). ALSO clears the `event`/`event_sequence` log for every removed aggregate (`clean_opencode_events`, skipped on older DBs without those tables) — mirrors `sync.remove()`. OpenCode has **no deletable memory** (its memory is the user's own AGENTS.md/CLAUDE.md project + global instruction files, never touched). All Codex/OpenCode DB writes open RW with a 1.5s `busy_timeout` and surface "database is locked — quit the running {app}" (`map_db_locked`) when the app holds the lock. For Codex this means either the CLI or the Codex app (they share auth and state). Frontend sends `{ id }` / `{ project }` / `{ rel }` (NOT `(project, rel)` — these are DB rows, not files); wired in `ListItemMenu.tsx` (session + Codex memory) and the sidebar project row in `App.tsx`. **Local list update keys by `sessionKey` (`source:path:id`), NOT `path`:** DB-backed sources (OpenCode) share ONE db-file path across all sessions, so a path-keyed `removeRecordsLocally`/tombstone (`App.tsx`) wrongly cleared the whole list and the deleted rows reappeared on rescan — the id in `sessionKey` disambiguates. Tests: `sessions::tests::delete_codex_*` / `delete_opencode_*`.
- `delete_grok_session(id)` / `delete_grok_project(project)` — Grok is FILE-based (dir-per-session). `delete_grok_session` resolves the session's `<grok-home>/sessions/<encoded-cwd>/<uuid>/` dir via the scan path index (the scanned log file — `chat_history.jsonl` or `updates.jsonl` — → parent), bounded to the grok sessions root, and `remove_dir_all`s it — the SAME local removal as the official `JsonlStorageAdapter::delete_session` (jsonl/mod.rs:1197, NotFound-tolerant). It then mirrors the official delete's second step (`delete_session_history` → `notify_session_updated` → `delete_doc`, persistence.rs:2458 / search_fts.rs:286): `evict_grok_search_docs` runs `DELETE FROM session_docs WHERE session_id = ?` against the ONE GLOBAL `<sessions-root>/session_search.sqlite` (FTS5 follows via the schema's AFTER DELETE trigger — "callers only touch session_docs"). Eviction is BEST-EFFORT (locked/missing DB → log-only): the index is a self-healing derived cache (per-session refresh drops missing docs; `reindex_all` prunes orphans). The official delete's THIRD step — removing the cloud writeback copy via `BackendClient::delete_session_data` — is deliberately NOT done: Termory manages local data only, and deleting a local record ≠ intent to destroy the cloud copy (a synced session may still show as `remote` in grok's own list). `delete_grok_project` removes every `<encoded-cwd>` dir whose sessions record that cwd (`info.cwd`, authoritative — the encoded dir name is never decoded) and evicts each removed uuid's search doc (dir names ARE session ids; the index lives at the sessions ROOT, so it does NOT die with the cwd dir); the cwd's OWN skills under `<cwd>/.grok/skills` are NOT touched (grok delete only removes session history). `migrate_grok_session(id, new_path)` / `migrate_grok_project(project, new_path)` MOVE the `<id>/` session dir(s) into the destination cwd's `encode_cwd_dirname`-derived dir (`fs::rename`) + rewrite each `summary.json` info.cwd — grok resumes per-cwd by scanning that encoded dir, so the files must physically move; returns `GrokMigrationResult { sessions, new_project, old_dir, new_dir }` (see "Migrate (…+ Grok)"). `delete_grok_memory(rel)` removes one `.md` under `<grok-home>/memory/` ($GROK_HOME-aware, bounded by `is_safe_rel`) — Grok's cross-session memory is human-readable Markdown on disk (xai-grok-memory `storage.rs:58` = `grok_home().join("memory")`; global `.md` at the root, workspace-scoped under `{project-slug}-{blake3_hash8}/` subdirs, `storage.rs:24-31`), so it mirrors `delete_codex_memory` (a plain `fs::remove_file`). **There IS a per-workspace `index.sqlite`** (RAG chunks + FTS, `storage.rs:110`) but Termory deliberately does NOT evict from it — unlike the grok SESSION delete (which clears `session_search.sqlite`), grok's memory index has a **first-party self-heal**: `xai-grok-memory/watcher.rs:3-9` runs a live fs watcher on `<grok-home>/memory/` that calls `MemoryIndex::delete_path` on a `.md` REMOVE event, and `grok memory reindex`/`doctor` prune orphaned chunks (`index.rs:1182`, official regression test). So a Termory-side delete is reconciled by grok itself on next run; replicating the more complex memory-index schema here would be higher-risk for no durability gain (session-search has no such watcher, which is why THAT one is evicted). `$GROK_HOME` is a real official override (`xai-grok-config/paths.rs:35` — env var first, else `~/.grok`), so the frontend detection is $GROK_HOME-robust by design (below). Frontend: `ListItemMenu.tsx` (session, `delete_grok_session`; auto-memory `isGrokAutoMemory` — unlike the marker-based Claude/Gemini/Codex memory branches this is **$GROK_HOME-robust**: a grok memory record's `project` IS the `<grok-home>/memory` root (the scan passes it verbatim), so the rel is `relUnderRoot(project, path)` regardless of where grok-home lives, and the `"grok"` tool tag — passed as `memoryTags` ONLY from the Memories pane, and grok never tags a project-instruction file — identifies it without a hardcoded `.grok` path → `delete_grok_memory`) + the sidebar project row in `App.tsx` (`delete_grok_project` + `migrate_grok_project`). Tests: `sessions::tests::grok_delete_session_and_project_scoped`, `delete_grok_memory_in_removes_md_and_guards_path`, frontend `ListItemMenu.test.tsx` (default + custom-$GROK_HOME rel) + `session-utils.test.ts` (`relUnderRoot`).
- `claude_project_registered(path)` — **read-only** check of `~/.claude.json` `projects` for `path` (canonicalizes first). The migrate confirm uses it: when the target project isn't registered, the confirm dialog adds a note (`menu.migrateConfirmUnregistered`) that `claude --resume` (command line) won't list the moved sessions until the user opens Claude in that dir once (registration happens on entry, not from the migrated files; `/resume` inside a running Claude shows them regardless). Termory **never writes** `~/.claude.json` (Claude's live config — a concurrent write would clash).

**Providers — switch CLI to a third-party platform**
- `provider_active_state(app, providers)` / `provider_active_states(providers)` — reverse-derive which Provider is currently active by reading the CLI's live config files; nothing about "active" is stored backend-side
- `activate_provider(provider, providersForApp)` / `deactivate_provider(app, providersForApp)` — materialize a Provider into the CLI's live config (or clear Termory-injected fields)
- `delete_provider(provider)` — remove a stored Provider AND deactivate it if it was the active one
- `set_opencode_default_provider(provider)` — OpenCode-specific: pick which of multiple configured `auth.json` entries opencode uses by default
- `test_provider_api(provider)` — connectivity probe to the provider's base URL (returns `{ ok, status, latencyMs, message }`)
- `fetch_provider_models(provider)` — hits `/v1/models` (or `/v1beta/models?key=` for Gemini) and returns the available model ids for the editor's autocomplete
- `fetch_subscription_quota(app)` — official-account rate-limit usage for one CLI, read from its EXISTING OAuth login (never writes credentials). Claude + Codex + Gemini + Grok implemented (`quota.rs`; OpenCode has no official subscription quota — returns `not_found`). Shared plumbing: `quota_for_credential` (per-CLI credential-status scaffold), `quota_http_client` / `read_json_or_error` (HTTP skeleton + API-error-message extraction). Claude: macOS Keychain `Claude Code-credentials` → `CLAUDE_CONFIG_DIR`/`~/.claude/.credentials.json`, then `GET https://api.anthropic.com/api/oauth/usage` with `anthropic-beta: oauth-2025-04-20`. `parse_claude_usage` reads the flat top-level windows (`five_hour`/`seven_day`, + any unknown window key passed through verbatim) AND the newer `limits` array: each `weekly_scoped` entry is a MODEL-scoped weekly limit named by `scope.model.display_name` (e.g. "Fable"), added as a tier verbatim (`percent`→utilization) — so any model surfaces dynamically with zero per-model code. `session`/`weekly_all` array entries are skipped (they duplicate the flat `five_hour`/`seven_day`). The legacy flat `seven_day_opus`/`seven_day_sonnet` are now null server-side; Anthropic moved model-scoped limits into the array. Codex: macOS Keychain `Codex Auth` → `~/.codex/auth.json` (gated on `tokens` PRESENCE — the ChatGPT login — NOT on `auth_mode`, which only selects what the CLI uses per `resolved_mode` manager.rs:980-988; the Termory round-trip custom→Official leaves tokens with no auth_mode and the quota must survive it. Pure API-key login has no tokens → `not_found`; >8 days since `last_refresh` → try-then-Expired), then `GET https://chatgpt.com/backend-api/wham/usage` with `ChatGPT-Account-Id` (endpoint per codex-rs `backend-client/src/client.rs:296`, window shape per `rate_limit_window_snapshot.rs`); `rate_limit.{primary,secondary}_window` map to `five_hour`/`seven_day` via `window_seconds_to_tier_name` so the same labels/glyphs apply; returns `SubscriptionQuota { app, credentialStatus, success, tiers: [{name, utilization, resetsAt}], plan?, extraUsage?, error?, queriedAt }` — `plan` is the subscription tier display name (Claude: `subscriptionType` in the credentials file, auth.ts:1225; Codex: `plan_type` in the usage response, protocol/src/account.rs; Gemini: a plan keyword extracted from loadCodeAssist `paidTier.name ?? currentTier.name` (official precedence, setup.ts:221 — Google One AI Pro reports `currentTier: standard-tier` with the real plan in `paidTier`) (the FULL marketing string the official CLI prints — \"Gemini Code Assist in Google One AI Pro\" → \"Pro\", `gemini_tier_keyword`), else the id; tidied by `display_plan`, e.g. \"Max\"/\"Plus\"/\"Free\"), shown as a Badge next to \"Official\" on the card and as `Official (Max)` in the tray title where tier names are `five_hour` / `seven_day` / model-scoped brand names like `Fable` (+ unknown windows passed through; legacy `seven_day_opus`/`seven_day_sonnet` if non-null). Gemini: macOS Keychain `gemini-cli-oauth`/`main-account` (keytar JSON) → `~/.gemini/oauth_creds.json` (`storage.ts:22`); Google access tokens last ~1h so an expired one is re-minted from the refresh token (public installed-app client, `oauth2.ts:76-85`; never persisted), then POST `v1internal:loadCodeAssist` + `v1internal:retrieveUserQuota` (`server.ts:263/363`) — per-MODEL buckets (`BucketInfo {remainingFraction, resetTime, modelId}`, types.ts:255-265) grouped to `gemini_pro`/`gemini_flash`/`gemini_flash_lite` (lowest remaining per class wins; card labels "Gemini Pro"/"Gemini Flash"/"Gemini Flash Lite", tray keeps the short "Pro"/"Flash"/"Lite" — brand-English in all locales). Surfaced on the Providers page Official card (gated by `QUOTA_SUPPORTED` in `ProvidersPage.tsx` — mirror of `quota::SUPPORTED`) as per-window donut rings; successful results cache 2 min (`QUOTA_STALE_MS`), failures 60s (`QUOTA_ERROR_RETRY_MS`), manual refresh has a 120s cooldown (`QUOTA_MIN_INTERVAL_MS`); failures surface ONLY as a two-line toast on manual refresh (background fetches are silent), and `not_found` (no OAuth login) hides the whole section. **A `410 Gone` from any usage endpoint also maps to `not_found`** (shared `quota_error_for_status` in quota.rs): the account no longer has this resource — e.g. Gemini CLI stopped serving individual logins 2026-06-18 — so the card hides cleanly instead of toasting an error; enterprise logins still serve quota and are unaffected. Codex's free-plan `30_day` window is labeled "Monthly" (i18n); other generated `{n}_hour`/`{n}_day` ids humanize to "{n}h"/"{n}d"
- `fetch_provider_favicon(url)` — proxies a one-shot `<host>/favicon.*` fetch through the backend (avoids leaking the hostname to a third-party favicon service); returns a `data:image/...;base64,...` URL cached on the Provider record

**Gateways — see the "Gateways" subsection under Providers**
- `detect_gateway_apis(baseUrl, apiKey)` — probe which API modes the gateway speaks (OpenAI `/v1/models`, OpenAI Responses `/v1/responses`, Anthropic `/v1/models`, Gemini `/v1beta/models`); returns `GatewayCapabilities`. Probes run concurrently and never spend tokens.

**Codex multi-account management** (login snapshots saved to `~/.termory/accounts.json`, `{ "version": 2, "accounts": [...] }` — never writes Codex's own `auth.json` except on switch/login). Each entry's `payload` is the **parsed auth.json as a JSON object** (`live.doc`), NOT a JSON-encoded string — `switch_codex` takes a `&JsonValue` (tolerating the legacy string form defensively). `ACCOUNTS_SCHEMA_VERSION = 2`: v1 stored `payload` as a stringified blob; `migrate_account_entries` converts a string `payload` → object for `version < 2`, and `read_store` **persists that upgrade to disk** on the first read of an old file (so historical data is actually rewritten, not just fixed in memory). Tests: `saved_payload_is_a_json_object_not_a_string`, `migrates_v1_string_payload_to_object_and_persists`.
- `list_accounts(app)` — returns `AccountsState { current, accounts, storageWarning }`: current live login + all saved snapshots with `active` + `needsRelogin` flags. Snapshot MANAGEMENT (save/switch/delete/login) is Codex-only (frontend `isManaged = app === "codex"`); Claude, Gemini and Grok return a DISPLAY-ONLY `current` (Claude: `~/.claude.json` `oauthAccount` email/name; Gemini: `~/.gemini/oauth_creds.json` id_token JWT claims; Grok: plain `email`/`first_name`/`last_name` in the `https://auth.x.ai::<uuid>`-scoped entry of `<grok-home>/auth.json` — no JWT decode, verified on the real install) with an empty `accounts` list. The section renders for `ACCOUNT_SUPPORTED` (ProvidersPage — quota apps + grok); quota props stay gated on `QUOTA_SUPPORTED`, so Grok shows the account row with no quota rings
- `save_account(app)` — snapshot the current live login into `accounts.json` (upsert by `id`)
- `switch_account(id)` — restore a saved snapshot to `auth.json`, always attempt token refresh first; `AuthFailure` (4xx non-429) marks `needsRelogin: true` on the entry
- `delete_account(id)` — remove a saved snapshot from `accounts.json` (never touches `auth.json`)
- `login_and_save_codex_account()` — snapshot current → clear auth.json → spawn `codex login` (5 min timeout) → save new login → restore previous; rollback (via `restore_auth`) on any failure including cancel. Uses `tokio::select!` with three arms: child exit / 5-min timeout / cancel signal. A `CodexLoginCancel` Tauri managed state (`Mutex<Option<Arc<Notify>>>`) holds the active notify; the cancel arm calls `child.kill()`, `restore_auth`, and clears the state. The frontend shows a Cancel button both in the URL dialog and on the Official card while login is in progress.
- `cancel_codex_login()` — fires the `Arc<Notify>` stored in `CodexLoginCancel`; the `login_and_save_codex_account` select! cancel arm restores auth.json and returns `Err("Login cancelled")`; frontend `finally` block refreshes the account list so the restored account shows as active immediately
- `mark_account_relogin(id, needed)` — set or clear `needsRelogin` on a saved entry; frontend calls with `true` on switch failure, cleared automatically by `save_account` upsert

**App-local KV stores** (all `chmod 0600` on Unix)
- `read_app_config` / `write_app_config` — `~/.termory/config.json` (UI prefs)
- `read_app_providers` / `write_app_providers` — `~/.termory/providers.json` unified `providers` array, entries with `kind != "gateway"` (per-CLI provider library, contains API keys)
- `read_app_gateways` / `write_app_gateways` — same `~/.termory/providers.json` unified `providers` array, entries with `kind == "gateway"` (gateway entries with `bindings`)
- `read_app_favorites` / `write_app_favorites` — `~/.termory/favorites.json` (saved message snapshots, may contain PII / pasted secrets)

## Project Commands

- Package manager: npm (`package-lock.json` is present).
- Web dev server: `npm run dev`
- Tauri dev app: `npm run tauri:dev`
- Frontend production build: `npm run build`
- Tauri bundle build: `npm run tauri:build`
- Rust tests: `cargo test --manifest-path src-tauri/Cargo.toml --lib`
- Rust format: `cd src-tauri && cargo fmt`

The Tauri binary is renamed via `[[bin]] name = "Termory"` in `src-tauri/Cargo.toml` plus `mainBinaryName: "Termory"` in `tauri.conf.json`, so the macOS menu bar shows "Termory" rather than the lowercase Cargo package name.

## Release & Updater

CI: `.github/workflows/test.yml` runs on every push/PR (docs-only changes — `**.md`, `docs/**` — are paths-ignore'd) — vitest + `npm run build` (type-check) on ubuntu, and `cargo test --lib` on an ubuntu + macos + **windows** matrix (the release workflow only builds; the Windows leg is what actually executes the `#[cfg(windows)]`-gated tests, e.g. `windows_open_command`/`executable_candidates`). `tauri::generate_context!` needs `../dist` to exist at compile time, so the rust job stubs `dist/index.html` first.

GitHub Actions workflow at `.github/workflows/release.yml` is triggered when a `v*` tag is pushed (e.g. `git tag v0.2.0 && git push --tags`) or manually via `workflow_dispatch`. It uses `tauri-apps/tauri-action@v0` and builds installers for:

- macOS Apple Silicon (`aarch64-apple-darwin`)
- macOS Intel (`x86_64-apple-darwin`)
- Linux x86_64 (`x86_64-unknown-linux-gnu`) — with apt deps for webkit2gtk-4.1, soup-3.0, javascriptcoregtk-4.1, etc.
- Windows x86_64 (`x86_64-pc-windows-msvc`)

A draft GitHub Release is created with the platform installers attached plus `latest.json` (the updater manifest). Review & publish the draft.

### In-app updater (`tauri-plugin-updater`)

- Plugin registered in `lib.rs`: `tauri_plugin_updater::Builder::new().build()` + `tauri_plugin_process::init()` for `relaunch()`.
- `tauri.conf.json` declares `bundle.createUpdaterArtifacts: true` and `plugins.updater.endpoints` pointing at `https://github.com/chats-is/termory/releases/latest/download/latest.json`.
- `capabilities/default.json` grants `updater:default` + `process:default`.
- Frontend: `Settings` page exposes "Check for updates" → `@tauri-apps/plugin-updater::check()` → "Download and install" → `update.downloadAndInstall()` → `@tauri-apps/plugin-process::relaunch()`.

### One-time signing key setup (DONE as of v0.2.6)

The updater only installs artifacts whose signature matches a pubkey baked into the binary. Without a keypair, in-app updates won't work (but the GitHub Actions builds still produce installers — users can download manually).

**Status: configured.** The keypair lives at `~/.tauri/termory.key` (local, password-protected — back it up; losing it means no more signed updates for existing users, and the locked bundle id makes that unrecoverable). The pubkey is baked into `tauri.conf.json` `plugins.updater.pubkey`, `bundle.createUpdaterArtifacts` is `true`, and the GitHub repo secrets `TAURI_SIGNING_PRIVATE_KEY` + `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` are set. Signed updater artifacts (`.sig` + `latest.json`) ship from v0.2.6 on, so **auto-update works only for users already on ≥0.2.6** — earlier installs (no baked pubkey) must download the new version manually once. CI still creates a **draft** release; publish it (un-draft) so `…/releases/latest/download/latest.json` resolves.

The original setup steps, kept for reference / re-keying:

```sh
# Generates ~/.tauri/termory.key (private, password-protected) + termory.key.pub
npx @tauri-apps/cli signer generate -w ~/.tauri/termory.key
```

Then:

1. Paste the contents of `~/.tauri/termory.key.pub` into `tauri.conf.json` `plugins.updater.pubkey`.
2. Add GitHub repo secrets:
   - `TAURI_SIGNING_PRIVATE_KEY` = contents of `~/.tauri/termory.key` (the private file)
   - `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` = the password you set during generation
3. Tag and push: `git tag v0.2.0 && git push --tags` — the workflow builds, signs, and publishes a draft release.

Bump `src-tauri/tauri.conf.json` `version` and `src-tauri/Cargo.toml` `version` together before tagging so the binary self-reports the right version.

macOS bundle identifier: `is.chats.termory` (reverse DNS of the `chats.is` domain the project ships under). Do NOT change this after a public release — macOS treats a different identifier as a different app, so existing user data and the Tauri updater would break.

The repo also contains `.audit-sources/` (gitignored) with shallow clones of `openai/codex`, `google-gemini/gemini-cli`, `sst/opencode`, `xai-org/grok-build`, `videcoding/cli` (legacy Claude Code reference), and `farion1231/cc-switch` (reference for provider-switcher behavior). This is the source-of-truth for path/behavior verification when official docs disagree with implementation — grep here instead of WebFetching docs.

## Upstream References

Use upstream implementations as the reference for history data and message preview behavior:

- Codex official source: https://github.com/openai/codex
- Claude Code referenced CLI implementation: https://github.com/videcoding/cli
- Gemini CLI official source: https://github.com/google-gemini/gemini-cli
- OpenCode official source: https://github.com/anomalyco/opencode
- Grok Build official source: https://github.com/xai-org/grok-build (Rust; the token-usage ledgers are `crates/codegen/xai-chat-state/src/usage.rs` + `.../actor/state.rs`, the ACP wire shapes `crates/codegen/xai-grok-shell/src/extensions/notification.rs`)

For Memory paths:

- Claude Code memory: https://code.claude.com/docs/en/memory
- Codex AGENTS.md guide: https://developers.openai.com/codex/guides/agents-md
- Codex memories: https://developers.openai.com/codex/memories
- Gemini GEMINI.md: https://github.com/google-gemini/gemini-cli/blob/main/docs/cli/gemini-md.md
- Gemini memory tool: https://github.com/google-gemini/gemini-cli/blob/main/docs/tools/memory.md
- OpenCode rules: https://opencode.ai/docs/rules/

For Skills paths:

- Codex skills docs: https://developers.openai.com/codex/skills
- Gemini CLI skills docs: https://github.com/google-gemini/gemini-cli/blob/main/docs/cli/skills.md
- OpenCode skills docs: https://opencode.ai/docs/skills/

For TUI tool-message rendering (every Termory branch cites a line here):

- Codex exec/shell render: `.audit-sources/codex/codex-rs/tui/src/exec_cell/render.rs`, bash highlight alias at `codex-rs/tui/src/render/highlight.rs:533`
- Claude tool-use wrapper: `.audit-sources/claude-code/src/components/messages/AssistantToolUseMessage.tsx:152` (assembles `<bold>{userFacingName}</bold>({renderToolUseMessage})`); per-tool: `src/tools/<Tool>/UI.tsx` (`userFacingName` + `renderToolUseMessage`)
- Gemini ToolInfo render: `.audit-sources/gemini-cli/packages/cli/src/ui/components/messages/ToolShared.tsx:202`; type at `packages/cli/src/ui/types.ts:119` `IndividualToolCallDisplay`
- OpenCode tool components: `.audit-sources/opencode/packages/tui/src/routes/session/index.tsx` — the per-tool renderers (now named functions: `Shell`/`Glob`/`Read`/`Grep`/`WebFetch`/`WebSearch`/`Write`/`Edit`/`ApplyPatch`/`TodoWrite`/`Question`/`Skill`/`Task`/`GenericTool`) + the `BlockTool` / `InlineTool` helpers. **OpenCode 1.17.x extracted the whole TUI into the `packages/tui` package** (the old `…/feature-plugins/system/session-v2.tsx` is gone). Citations in the OpenCode verb-mapping section have been re-verified against 1.17.11; message-type DATA now lives in `packages/tui/src/context/data.tsx`. See the audit-version note under "Per-platform verb mapping → OpenCode".

When behavior differs by version, match the locally installed or explicitly requested target version and cover it with a focused test. Tool-message rendering should reference the TUI source files above, not the doc sites — docs lag behind the actual UI for many of these tools.

## Current Data Sources

### Sessions

- Codex
  - List: `~/.codex/state_5.sqlite`, table `threads`, rows where `archived = 0` AND `preview <> ''` AND `source IN ('cli', 'vscode', 'atlas', 'chatgpt')`. The four sources match `INTERACTIVE_SESSION_SOURCES` in `codex-rs/rollout/src/lib.rs`; the `preview <> ''` clause matches `push_thread_filters` in `codex-rs/state/src/runtime/threads.rs`. Same filter is applied when loading a single thread by id.
  - Messages: each selected thread's `threads.rollout_path` JSONL file.
- Claude Code
  - List: `CLAUDE_CONFIG_DIR/projects/**/*.jsonl` when `CLAUDE_CONFIG_DIR` is set, otherwise `~/.claude/projects/**/*.jsonl`. Filename must be a UUID (`is_uuid_like`), first line must NOT contain `"isSidechain":true`, and the session must have at least one of customTitle/aiTitle/lastPrompt/summary/firstPrompt — same filter as videcoding/cli `parseSessionInfoFromLite`.
  - Messages: the selected project JSONL file.
- Gemini CLI
  - List: `~/.gemini/tmp/*/chats/session-*.jsonl` and `~/.gemini/tmp/*/chats/session-*.json`. Sessions must have a non-empty `sessionId`, **`hasResumableContent`**, and `kind !== 'subagent'`. `hasResumableContent` mirrors `getAllSessionFiles` gating on `isResumableMessageRecord` (`sessionUtils.ts:284` + `chatRecordingService.ts:107`): a `user` turn counts unless its content is *ignored* (`isIgnoredUserContent`, `sessionUtils.ts:97` — empty / `/` slash command / `?` shell passthrough / `<session_context>` / `<hook_context>`), a `gemini` turn counts when it has text, tool calls, or thoughts. This REPLACED the earlier "any user/gemini message" rule (the CLI tightened the filter to skip startup-only / command-only / context-only shells; helpers `gemini_is_resumable_message` / `gemini_is_ignored_user_content`, test `gemini_list_filter_gates_on_resumable_content`). The `~/.gemini/tmp/<id>/` dir id is now a **slug** (was a sha256 hash pre-v0.50; managed by `ProjectRegistry` + `~/.gemini/projects.json`, old hash dirs auto-migrated) — but Termory reads the `.project_root` marker for the cwd and never decodes the id, so hash→slug is transparent. When `startTime`/`lastUpdated` are missing on the record, Termory falls back to the file's mtime (then to `Utc::now()`) — also mirrors `getAllSessionFiles`.
  - Project path: sibling/related `.project_root` file under the Gemini temp project directory.
  - Messages: the selected session JSONL/JSON chat file.
- OpenCode
  - List: `~/.local/share/opencode/opencode.db`, table `session`, rows where `parent_id IS NULL` and `time_archived IS NULL`, ordered by `time_updated DESC, id DESC`. Mirrors `listByProject` in `packages/opencode/src/session/session.ts`.
  - Messages: `~/.local/share/opencode/opencode.db`, tables `message` and `part`; `session_message` is only a fallback when `message`/`part` are unavailable (a real compat path for older databases — `session_message` is otherwise the projections table per `projectors-next.ts`).
  - Compatibility storage: JSON files under `~/.local/share/opencode/**/storage`; use only for older/alternate local layouts and never before the current SQLite path.

- Grok Build (xAI)
  - Grok Build **is open source** (`xai-org/grok-build`, cloned to `.audit-sources/grok-build`) — cite it like the other CLIs. (It was closed-source when Termory's grok support was first written, so older notes here said the on-disk layout was the only reference; the layout survey still holds, re-verified on real data at grok 0.2.99.)
  - List: `$GROK_HOME`-aware (`providers::grok_home_dir`, default `~/.grok`) — `<grok-home>/sessions/<percent-encoded-cwd>/<uuid>/summary.json` — read VERBATIM (`info.id`, `info.cwd`, `created_at`/`updated_at`, `num_chat_messages`, `current_model_id`). **Title = official `display_title()`** (persistence.rs:958): `generated_title` (LLM/manual `/rename`, trimmed non-empty) preferred over `session_summary`. **Hidden sessions are skipped** per the official `is_hidden()` (persistence.rs:949): explicit `hidden` bool wins, default = `session_kind` starts with `"subagent"` — grok's sidechain analog, same list-time rule as Claude's isSidechain. The encoded-cwd dir name is percent-encoding for short cwds but `{slug}-{blake3_16}` + a `.cwd` metadata file when the encoded form exceeds 255 bytes (paths.rs `encode_cwd_dirname`) — Termory never decodes dir names (reads `info.cwd`), so both encodings just work. A session dir is valid iff it has `chat_history.jsonl` or `updates.jsonl`. **Empty sessions follow the OFFICIAL rule** (`dedup_empty_sessions`, session/merge.rs — grok opens a NEW session dir on every CLI entry, so shells abound): with sessions sorted newest-first (`last_active_at` else `updated_at`, `effective_sort_time`), each cwd keeps ONLY its most recent `num_messages == 0` session (the "new session" resume entry grok's own picker shows) and older empties drop; cwd keys are trailing-slash-trimmed (`normalize_cwd`). The emptiness signal is the summary's `num_messages` (NOT `num_chat_messages`, which is 2 even for a fresh shell: system prompt + the synthetic `system_reminder` user turn). Implemented as a post-scan `retain` in `scan_grok_sessions_in` over `GrokScanned { session, empty, sort_key }` (an earlier content-sniffing `grok_chat_has_content` that hid ALL empties was replaced by this official rule, user decision 2026-07). Sibling noise skipped: `session_search.sqlite`, per-cwd `prompt_history.jsonl`, `.lock` files.
  - Messages — grok has TWO on-disk formats; `parse_grok_session` dispatches by the scanned file's name. **CURRENT: `chat_history.jsonl`** (`{type, content}` per line — written by the interactive TUI, present in EVERY session, so it's preferred). `parse_grok_chat_history`: `system` → `*[System prompt]*` notice; `user` → the TUI's display form via `grok_user_display_text`: the LLM-prompt envelopes are never shown raw — `<user_query>` unwraps to the message text (the pager's own extractor, app/effects/helpers.rs:376), every other envelope (`<user_info>` preamble — the "marker-less non-synthetic" item conversation.rs:2513, `<system-reminder>`, `<skill_information>`, …) becomes an italic `*[tag]*` notice (never-hide rule), and a synthetic turn (`synthetic_reason`, conversation.rs:77) or all-envelope message renders with the SYSTEM role like Codex `<environment_context>` / Gemini `<session_context>` (content is a string or `[{type:text,text}]`, flattened by `grok_chat_content_text`); `reasoning` → `summary[].text` as a `> *…*` blockquote; `assistant` → text + one `**{Label}**(arg)` card per `tool_calls[]` entry (cards via the shared `grok_tool_card(name, input)` — used by BOTH formats. **Verbs are the pager's per-block TUI headers** (xai-grok-pager/src/scrollback/blocks/tool/ + acp/tracker.rs `tool_call_to_block`), NOT the `_meta`/taxonomy `presentation_name` — the pager never renders that label in the transcript: read_file→**Read** (+ ` ({start}-{end})` range from offset/limit, tracker.rs:1465), search_replace/edit→**Edit** + a ```diff fence from old_string/new_string (the TUI shows hunks, edit.rs), write→**Creating** (tracker.rs:1530) + all-insert diff, run_terminal_command/bash→**Run** with the DESCRIPTION as title and the command as a secondary `$ …` fence when a description exists (execute.rs:353, default Label style config.rs:699), grep/glob→**Search** with `"{pattern}"[ in {glob}][ in {path}]` (trivial pattern promotes the glob, search.rs:225-280), list_dir→**List** (list_dir.rs:116), **web_fetch→Fetch** (web_fetch.rs:128 — the taxonomy says "Web Fetch" but the TUI block says Fetch), web_search→**Web Search**, memory_search→**Memory Search**, skill→**Skill**; block-less tools (plan/ask-user/task/lsp/background/memory_get/…) keep their taxonomy title, which is what OtherToolCallBlock shows (tracker.rs:1830); unknown names fall back to the raw name; `arguments` is a JSON-encoded string); `tool_result` folds its `content` (4-backtick fence) into the matching card by `tool_call_id`, and its `images[]` (`ToolResultItem.images` — e.g. read_file on an image) append `*Image*`/`*Image: url*` notices into the fold; `backend_tool_call` (SERVER-side tools — `kind.tool_type` = web_search / x_search / code_interpreter, conversation.rs:44) renders per tracker.rs:1534-1670: **Web Search** with per-action detail (search→query, open_page→`open {url}`, find→`find "{pattern}"`) + `action.sources` as a numbered `1. {url}` citations fence (tracker.rs:1641), **X Search** as `{keyword|semantic|users|thread}({query})` (tracker.rs:1573-1590), **Code Interpreter** with a code snippet (fields are OpenAI Responses API items — `rs = async_openai::types::responses`); user `content` parts are `{type:text}`/`{type:image,url}` (`ContentPart`) — image parts surface as `*Image: url*` notices (data URIs → bare `*Image*`). Test: `grok_parse_chat_history_new_format`. **LEGACY (fallback): `updates.jsonl`** — an ACP (Agent Client Protocol) `session/update` stream, still written by headless `grok -p`; used only when a session lacks chat_history.jsonl. `parse_grok_session` (the ACP path): streaming `user/agent_message_chunk` + `agent_thought_chunk` events merge consecutive same-type chunks into one message (thought → unified `> *…*` blockquote); `tool_call` opens the SAME `grok_tool_card` card as chat_history (TUI verbs — the pager renders per-block headers from the tool NAME, not the `_meta["x.ai/tool"].label`, which is the cross-harness taxonomy title); the recorded `_meta` label (JSON-pointer `~1` escape for the literal `x.ai/tool` key) is used only for names outside the table — it's what OtherToolCallBlock would show; the terminal `tool_call_update` (`completed`/`failed`) finishes the card with `⏺`/`✗` + a 4-backtick output fence (`grok_output_text` unwraps the `{type, <Variant>: {content}}` envelope); `plan` → GFM task list; intermediate tool updates / `retry_state` / `turn_completed` are transport bookkeeping (no content) and are skipped. Resume: `grok --resume <id>` (verified against `grok --help`), wired in terminal.rs + `resumeCommandFor`.
  - Tokens/daily_tokens: extracted by `grok_scan_extract` from **`updates.jsonl`** (the ACP rail) — token usage is NOT in `chat_history.jsonl`. It rides each `turn_completed` update's `usage`; every session with a real turn has one (an empty CLI-entry shell ran no inference, so it has no updates.jsonl at all — which is why the 2-of-20 split looked like "headless only"). Read via `cached_scan_extract` (mtime-cached) off the sibling path, regardless of which file `path` points at. **Usage is PER-TURN, so turns SUM** — verified in the official source: the ACP turn-end path reads `try_get_prompt_usage()` (`session/acp_session_impl/turn.rs`), the `prompt_usage` ledger documented in `xai-chat-state/src/actor/state.rs` as *"Billing for the open prompt (cleared on next prompt)"*, NOT the lifetime `session_usage` sibling; `numTurns` counts main-agent loop ROUNDS within one prompt (same unit as `--max-turns`), so it is NOT a cumulative signal. **Mapping**: grok nests the subsets per the xAI/OpenAI convention — `cachedReadTokens ⊆ inputTokens` ("Full prompt input tokens including cache reads", `extensions/notification.rs`) and `reasoningTokens ⊆ outputTokens` (its `total_tokens() = input + output` adds neither) — while Termory's `TokenStats` wants the four DISJOINT, so both subsets are subtracted out; the four then re-sum to grok's own `totalTokens` exactly (verified on real data: `69373-11264` + `60-31` + `11264` + `31` = `69433`). Do NOT take the four fields verbatim (double-counts). Per-day/hour bucketing uses `params._meta.agentTimestampMs` (epoch ms), falling back to the top-level `timestamp` (epoch SECONDS). NOTE `signals.json` also carries `contextTokensUsed`/`contextWindowTokens` (what the TUI's "System prompt / Messages / Free" bar shows) — that is CONTEXT-WINDOW OCCUPANCY, not consumption, and is deliberately not used. Tests: `grok_scan_extract_sums_per_turn_usage_and_splits_subsets`, `grok_scan_extract_empty_without_turns`.

Read source history in place. Do not modify original history files or databases.

### Memory

Verified against each tool's open-source implementation (not just docs). When docs and source disagree, source is authoritative. See Upstream References for source URLs.

- Claude Code: `~/.claude/projects/<sanitized-canonical-git-root>/memory/**/*.md` (auto-memory per project — `src/memdir/paths.ts` uses `findCanonicalGitRoot` so worktrees of the same repo share one dir), `~/.claude/rules/**/*.md` (global rules), `<cwd>/.claude/rules/**/*.md` (project rules — both recursive, all `.md`)
- Codex: `~/.codex/memories/**/*.md` — `scan_codex_memory` skips the `skills/` subdir for backward compatibility (current Codex source stores skills at `~/.codex/skills/`)
- Grok Build: `<grok-home>/memory/**/*.md` ($GROK_HOME-aware) — `scan_grok_memory`. Cross-session memory as human-readable Markdown (xai-grok-memory storage.rs:24): global files at the root, workspace-scoped files under `{project-slug}-{hash8}/` subdirs (hash not reversible; the file's relative path shows the slug). `derive_memory_project_label` recognizes `<grok-home>/{memory,skills}`.
- Gemini CLI:
  - Global: `~/.gemini/GEMINI.md` (legacy) AND `~/.gemini/MEMORY.md` (modern alias — `getAllGeminiMdFilenames()` returns both)
  - Per-project: `~/.gemini/tmp/<id>/memory/{MEMORY.md preferred, GEMINI.md legacy}` — confirmed at `packages/core/src/config/storage.ts getProjectMemoryDir()` → `getProjectMemoryTempDir() = path.join(globalTempDir, projectIdentifier, 'memory')`. Termory recursively reads .md inside, skipping the `skills/` subdir which is surfaced under Skills.
- Per-project instruction files — scanned at the cwd AND, **only when a `.git` directory exists at or above cwd**, every ancestor up to and including the git root (stopping before `$HOME`):
  - `CLAUDE.md` → tag `claude,opencode` (OpenCode officially falls back to it)
  - `CLAUDE.local.md` → tag `claude`
  - `AGENTS.md` → tag `codex,opencode`
  - `AGENTS.override.md` → tag `codex` (Codex's official override file)
  - `GEMINI.md` → tag `gemini`
  - `MEMORY.md` → tag `gemini`
  - `<cwd>/.claude/CLAUDE.md` → tag `claude` (only at cwd, not at ancestors — `.claude/CLAUDE.md` is a project-root convention)
- Global instruction files:
  - `~/.claude/CLAUDE.md` → tag `claude,opencode`
  - `~/.codex/AGENTS.md`, `~/.codex/AGENTS.override.md` → tag `codex`
  - `~/.config/opencode/AGENTS.md` → tag `opencode`

Paths intentionally NOT scanned (no current source reads them):

- `AGENTS.local.md` (any location) — not in any tool's source; Codex uses `AGENTS.override.md`
- `~/.codex/instructions.md` — legacy
- `~/.claude/CLAUDE.local.md` — not documented at user level
- `CONTEXT.md` — OpenCode deprecated, intentionally skipped
- `project_doc_fallback_filenames` from `~/.codex/config.toml` — Termory does not read user config

### Why ancestor walk gates on `.git`

All three open-source tools refuse to ascend without a project-root marker:

- **Codex** (`codex-rs/core/src/agents_md.rs`): `DEFAULT_PROJECT_ROOT_MARKERS = &[".git"]`. The doc-comment on the loader: *"If no marker is found, only the current working directory is considered."*
- **Gemini** (`packages/core/src/utils/memoryDiscovery.ts findProjectRoot`): defaults to `['.git']`. When no marker is found, returns null → caller sets `ceiling = startDir` → `findUpwardGeminiFiles` breaks immediately on the start dir.
- **OpenCode** (`packages/opencode/src/project/project.ts`): `worktree` is resolved via `git rev-parse --git-common-dir`; without git the fallback sets `worktree: sandbox` (= cwd), so `Filesystem.findUp(start=cwd, stop=cwd)` collects only cwd.

Claude Code (the only one NOT gating on `.git` — its `attachments.ts` walks to fs root) is the outlier; for simplicity we apply the stricter (more common) rule. This is a known minor mismatch documented in [`codex-ancestor-walk-rule`](memory).

The implementation lives in `scan_memory`:

1. `push_project_root_instruction_files(cwd, ...)` always runs (cwd-level files including `.claude/CLAUDE.md`).
2. `find_git_root(cwd, home)` walks up looking for `.git`. Returns `Some(dir)` or `None`.
3. If `Some(git_root)` and `git_root != cwd`, walk from `cwd.parent()` up to and including `git_root`, calling `push_ancestor_instruction_files` at each level (omits `.claude/CLAUDE.md`).
4. The final dedup-by-path keeps each file's first label; ancestor files get their own ancestor dir as the project label.

### Skills

Source-verified locations:

| Tool | Global | Project | Tag |
|---|---|---|---|
| Claude Code | `~/.claude/skills/` | `<cwd>/.claude/skills/` | `claude,opencode,grok` (OpenCode officially reads `.claude/skills/`; Grok Build reads `~/.claude/` skills for Claude compatibility — docs.x.ai/build/features/skills-plugins-marketplaces) |
| Codex | `~/.codex/skills/` (NOT `~/.codex/memories/skills/`) | `<cwd>/.codex/skills/` | `codex` |
| Gemini CLI | `~/.gemini/skills/` (`Storage.getUserSkillsDir`) | `~/.gemini/tmp/<id>/memory/skills/` (`Storage.getProjectSkillsMemoryDir`) + `<cwd>/.gemini/skills/` | `gemini` |
| OpenCode | `~/.config/opencode/skills/` | `<cwd>/.opencode/skills/` | `opencode` |
| Grok Build | `<grok-home>/skills/` ($GROK_HOME-aware) | `<cwd>/.grok/skills/` (official `./.grok/skills/`; cwd-level like the other tools) | `grok` (plugin skills dirs + `[skills] paths` extras deliberately not scanned) |
| Tool-neutral | `~/.agents/skills/` (`Storage.getUserAgentSkillsDir`) | `<cwd>/.agents/skills/` | `codex,gemini,opencode,grok` at BOTH levels (grok source: `collect_skill_config_dirs` — `.agents` is in the cwd→git-root walk's dir set AND always added at `~/.agents`; an earlier docs-only survey saw just the user level) |

Implementation notes:

- All skill scanners route through `push_doc_files_recursive(dir, base, project, tag, source="Skill", skip_dirs=&[], out)`.
- `doc_session_from_file(path, project, source)` is shared between Memory and Skill scanners; the `source` field on `AppSession` is `"Memory"` or `"Skill"` accordingly. **The `id` is the file's full path (NOT the filename or frontmatter `name`)** — those collide across projects (every `CLAUDE.md` shares the name "CLAUDE.md"), and the id keys BOTH the backend `SESSION_PATH_INDEX` `(source, id)` AND the frontend `sessionKey`; a non-unique id made every same-named memory/skill load the last-scanned file's content. `title` still uses `name` → filename for display.
- `parse_doc_file(path, source)` handles loading either kind in `get_session`.
- `derive_memory_project_label` recognizes both memory and skill on-disk paths (including `.agents/skills/`) so loading a single file by absolute path produces a sensible project label.
- **Claude `projects/<slug>/memory/` project label — read the real cwd, don't decode the slug.** Claude's project-dir slug is LOSSY: it replaces `/`, `.`, `_`, `-` (every non-alphanumeric char) all with `-`, so `/Users/me/copilot.is`, `…/copilot-is` and `…/copilot/is` collapse to the same `-Users-me-copilot-is` and can't be reversed from the slug alone (a blind `-`→`/` decode would show the label as `is`). So `claude_cwd_from_project_dir(slug_dir)` reads the authoritative `cwd` from the first records of a sibling session JSONL (only the first ~8 lines — these files run tens of MB) — independent of whether the project folder still exists on disk, since the JSONL lives under `~/.claude/projects/`, not in the project. Fallback chain when there's NO session (memory-only orphan): `resolve_dashed_path` (probe the filesystem, merging `-`-joined segments that name a real dir) → naive `decode_claude_project_slug` (`-`→`/`). Don't "simplify" this back to the naive decode — covered by `claude_cwd_from_project_dir_reads_real_cwd_from_session` + `resolve_dashed_path_recovers_hyphenated_directory` tests.

## Current Implementation Notes

- `scan_all_sessions` calls Rust scanning on a blocking worker and returns a `ScanResult { projects, records }` — `projects` (enumerated folders/entities) drive the sidebar; `records` (sessions/memories/skills, distinguished by `source`) are the contents. `applyScanResult` reconciles both against the tombstone sets and sets the `projects` + `sessions` (records) state.
- `load_session` loads one selected record by `source` + `id` (path resolved backend-side, never frontend-supplied).
- **Scan-time extraction is mtime-cached and single-pass (LOCKED).** The Claude/Codex LIST scans get tokens + model + daily_tokens + **message_count** from ONE streamed pass per session file (`claude_scan_extract` / `codex_scan_extract`, 256 KB `BufReader` — these files run 100+ MB), cached by `(path, mtime)` in `cached_scan_extract` (`SCAN_EXTRACT_CACHE`; stale entries evicted each scan by `evict_stale_scan_cache`). Do NOT reintroduce a separate per-scan full-file parse (the old uncached `estimate_*_message_count` pair pegged one core at 100% whenever any CLI was actively writing — every watcher rescan re-parsed ~800 MB; warm rescan went 2.08 s → 8.5 ms). Counting deliberately reuses `claude_message_from_value` / `codex_message_from_value` (no "cheap predicate" — `claude_display_text`'s Some/None IS the visibility rule, a parallel predicate would drift from the DETAIL count). `parse_claude_lite_session` returns the lean `ClaudeLiteSession` (head/tail metadata only), and the call sites assemble the `AppSession`, so `message_count` is a compile-time-required field rather than a fill-me-in-later 0.
- **Records page perf** (`App.tsx` detail effect + `src/components/MessageList.tsx`):
  - Detail load `useEffect` depends on `selected.source / path / id / updated_at / message_count` — narrow per-field deps instead of the whole `selected` object, so watcher-driven `applyScanResult` rebuilds that hand back a new `AppSession` reference with the same identity don't re-trigger `load_session`. A `prevSelectedKeyRef` distinguishes "new selection" (show `Loader2`) from "same selection, content advanced" (silent swap via `React.startTransition(setDetail)`).
  - `applyScanResult` no longer auto-selects `result[0]` on first launch — Records opens with an empty detail pane so app boot doesn't pay the cost of parsing the most recent session up front.
  - `setRoute` is wrapped in `React.useTransition` so clicking a rail icon (especially leaving a long Records detail) doesn't get blocked by the heavy unmount + re-render of the next route.
  - The detail pane renders messages through `<MessageList>` (`@tanstack/react-virtual`) which keeps roughly the visible window + 6-row overscan in the DOM regardless of session length. `measureElement` reports actual rendered heights so scrolling and the scrollbar stay accurate across variable-length message cards.
  - The `!selected && sessions.length === 0 && loading` "Scanning…" empty state was removed — initial boot just shows "Nothing to view yet" until data arrives, no transient spinner flash.
- `AppSession.preview` carries comma-separated tool tags (e.g. `"codex,opencode"` for AGENTS.md). The list-card `MemoryCard` renders one brand badge per tag via `memoryToolsOf()`; the detail-header badge renders a single type label (`Session` / `Memory` / `Skill`) via `typeLabelOf()`.
- For session-type entries the detail header shows the GUID (`selected.id`) on its own line below the project path, rendered as inline monospace via Tailwind (`font-mono` / `text-xs` / `text-muted-foreground`). Memory/Skill entries omit the GUID line.
- Project-level `AGENTS.md` and `AGENTS.override.md` are always tagged with both `codex` and `opencode` regardless of which tool actually has sessions in the cwd. Rationale: the AGENTS.md spec is tool-neutral — Termory reports which tools CAN read the file, not which tool happened to run there. Verified by `scan_memory_always_tags_project_agents_md_with_both_codex_and_opencode` test.
- Sidebar source filter (Codex/Claude/Gemini/OpenCode/All) applies to **all three** panes (Sessions, Memory, Skills). Memory and Skill filtering goes through `memoryToolsOf(item).includes(source as MemoryTool)`, so multi-tagged files (AGENTS.md with `codex,opencode`) appear under both Codex and OpenCode filters.
- Session list cards currently show source, date, title, project, and message count.
- `message_count` is an app-derived visible parsed message count when the official list does not provide the same count directly.
- Empty or missing official titles should stay empty unless the official tool has the same fallback.
- Brand identity lives in the `<BrandIcon>` SVG component (`src/components/BrandIcon.tsx`), one branch per `source` literal. Colors are baked into the SVG `fill` attribute (no CSS class indirection after the Tailwind v4 migration). Current colors:
  - `Codex`, `OpenCode`, and `Grok` use `#111827` (slate-900 — all render the same dark ink; cards distinguish them via the icon shape, not color). The Grok glyph is the OFFICIAL circle-comet mark from the `@lobehub/icons` brand set (24px-optimized trace; the raw grok.com favicon paths are a 512px app-icon variant that reads badly small), scaled 1.18× about center so the ring's optical weight matches the sibling marks — do NOT redraw brand marks by hand, fetch the official asset
  - `Claude` uses `#d97745` (Anthropic Clay)
  - `Gemini` uses an inline `<linearGradient>` defined in the same SVG: `#4285f4` → `#a142f4` → `#34a853` (Google blue/purple/green)
- There is no per-CLI CSS class (no `.badge.codex` etc.) — list-card "badges" are just `<BrandIcon source="…">` instances. To resize, callers pass `className="size-12"` etc., merged into the SVG via `cn()` inside `BrandIcon`. The legacy `Session` / `Memory` / `Skill` color pills are gone from the detail header (the source / type is implied by the rest of the card content); `typeLabelOf()` is still used by the Cmd-K command palette to label results.

### Unified tool-message format — LOCKED RULE

Every tool message — regardless of source platform — uses the same markdown shape. This is a hard rule: any new tool / structured-result formatter MUST follow it without exception.

**Shape:**

`````
{status} **{Verb}**({args})

⎿ {summary}              ← present when the tool has a structured summary
                           (line counts, status codes, settings, etc.)

```{lang}                ← optional fence for diff / source / structured output
{body}
```

or

````                     ← 4-backtick fence for unstructured text output
{body}                     (avoids collision with content containing ```)
````
`````

**Rules:**

1. `{status}` glyph: `⏺` success, `✗` failure (Claude `constants/figures.ts:4` + Codex `exec_cell/render.rs:236`). Cross-platform — applied to every tool card.
2. `{Verb}` text is platform-native (Claude `userFacingName`, OpenCode `routes/session/index.tsx`, Codex `exec_cell/render.rs`, Gemini `displayName`); the wrapper shape `**Verb**(args)` is identical across platforms.
3. `{args}` always passes through `wrap_inline_code` (sessions.rs:48) so embedded backticks / `*` / `()` don't break markdown.
4. **`⎿ ` prefix is REQUIRED on every summary line**, with one trailing space before the content. Tools without a structured summary skip the line entirely (Bash, generic MCP, etc.). NEVER put `⎿` inside a code fence — browser monospace fonts render U+23BF inconsistently, breaking column alignment.
5. Summary content matches the per-tool Claude TUI component verbatim (count bolding, label pluralization). Examples:
   - `⎿ Read **N** lines` — `FileReadTool/UI.tsx:138-139`
   - `⎿ Wrote **N** lines to **{file}**` — `FileWriteTool/UI.tsx:58`
   - `⎿ Found **N** {label}` — `GrepTool/UI.tsx:32-33`
   - `⎿ Added **N** lines, removed **M** lines` — `FileEditToolUpdatedMessage.tsx:42-50`
   - `⎿ Received **{size}** ({code} {codeText})` — `WebFetchTool/UI.tsx:31-58`
   - `⎿ Error: Exit code N` (Codex) / `⎿ Error: {message}` (Claude — no exit code)
6. Reasoning across all platforms: `format_reasoning_body(content)` → `> *line*` italic blockquote.
7. **No content is dropped to match official TUI behavior.** Suppressed tools (Claude `TodoWrite`, `AskUserQuestion`, `EnterPlanMode`, etc.) and synthetic / contextual wrappers (Codex `<environment_context>`, OpenCode `synthetic` parts, Claude `<tick>` / `<local-command-caveat>` / NULL_RENDERING attachments) all surface in Termory — usually as italic `*[wrapper-name]*` notices when there's no structured representation. Termory is a history browser; hiding things misleads the user.

**Failure detection per platform** (`SessionMessage.exit_code: Option<i64>` carries the parsed value through `merge_tool_outputs`):

| Platform | Signal source | Notes |
|---|---|---|
| Codex | `Process exited with code N` / `Exit code: N` in the `function_call_output.output` wrapper (`ExecCommandToolOutput.response_text()` — context.rs:409) parsed by `codex_parse_exec_output` | Limited mode default; populates `exit_code` |
| Claude | `tool_result.is_error: true` content block | No exit code field — `Error:` prefix has no `Exit code N` part |
| OpenCode | `state.status === "error"` on a tool part (and `state.error.message` for the text); `assistant.error` for whole-message failures (`SessionErrorUnknown` shape per types.gen.ts:2905) | Body gets `✗ ` marker + 4-backtick `Error: {message}` fence; kind = `tool_error` so future UI can colour the card |
| Gemini | `status` field on each `toolCalls[]` entry; per `sessionUtils.ts:654-657` anything other than `'success'` (e.g. `'error'`, `'cancelled'`) maps to `CoreToolCallStatus.Error` | No exit code; body gets an `Error:` prefix |

### Per-platform verb mapping

Every Termory branch cites the exact source file that produces the verb in each TUI. Survey under `.audit-sources/{codex,gemini-cli,opencode,claude-code}/`.

**Codex** (`codex_function_call_message` for ResponseItem::FunctionCall, `codex_custom_tool_call_message` for ResponseItem::CustomToolCall) — `.audit-sources/codex/codex-rs/tui/src/exec_cell/render.rs:381-385`:

- `exec_command` / `shell` / `shell_command` / `local_shell` (all 4 names per `rollout-trace/src/tool_dispatch.rs:263`) → `**Bash**({wrap_inline_code(cmd)})` — verb unified with Claude per design call (was `**Ran** \`cmd\`` before unification).
- `apply_patch` → `**{Verb}**({wrap_inline_code(path)})\n\n```diff\n{patch}\n```` ` — `codex_parse_patch_actions` scans `*** Add File:` / `*** Delete File:` / `*** Update File:` markers, picks `Added` / `Deleted` / `Edited` per `diff_render.rs:421-436`. Multi-file patches collapse to `**Edited**({N} files)`. Modern Codex stores apply_patch as `payload.type = "custom_tool_call"` with `input` field (raw patch text); legacy form is `function_call` with `arguments`. Both shapes route to the same patch-header builder.
- `update_plan` → `**Updated Plan**` + optional `*explanation*` + GFM task list `- [x]/[~]/[ ]` (matches PlanUpdateCell at `history_cell/plans.rs:138-194` — TUI uses ✔/□ symbols with crossed-out / bold / dim styling; Termory stays on GFM markers so checkboxes render natively in react-markdown)
- `view_image` → `**Viewed Image**({wrap_inline_code(path)})` (patches.rs:63-72 — capital `I` per TUI)
- other → `**{name}**({compact args})` fallback

**Codex EventMsg dispatch** (`codex_event_msg_to_message`) — `RolloutItem::EventMsg` records are the canonical replay source for Codex; the wrapper `codex_message_from_value` routes `event_msg` records here. Handled variants:

- `user_message` / `agent_message` / `agent_reasoning` / `agent_reasoning_raw_content`
- `web_search_end` → `**Searched**({wrap_inline_code(detail)})` where `detail` follows Codex's `web_search_action_detail` (search.rs:13-38): `query` for `Search` (or first of `queries` with ` ...` suffix when multiple), `url` for `OpenPage`, `'pattern' in url` / `'pattern'` / `url` for `FindInPage`
- `mcp_tool_call_end` → `**MCP**({server}.{tool})` (dot separator per Codex `format_mcp_invocation` mcp.rs:761-780); when `arguments` is a non-empty / non-`null` JSON value, appends `, {compact_json}` inside the parens
- `image_generation_end` → `**Generated Image**({wrap_inline_code(prompt)})` + saved path (capital `I` per TUI patches.rs:74-93)
- `view_image_tool_call` → same shape as the function_call variant
- `plan_update` → same as the function_call `update_plan` (payload IS the UpdatePlanArgs)
- `patch_apply_end` (Extended mode) → per-file `**Verb**({path})` lines; on failure appends stderr fence + `**Error**`
- `context_compacted` → `*Context compacted*` system notice
- `error` → `**Error**: {message}` system notice
- `turn_aborted` → `*Turn interrupted by user*` / `*Turn stopped — budget limit reached*`
- `thread_rolled_back` → `*Rolled back N turn(s)*`
- `entered_review_mode` / `exited_review_mode` → italic notices

**Codex `custom_tool_call` / `custom_tool_call_output`** (`codex_custom_tool_call_message` / `codex_custom_tool_call_output_message`) — modern shape for apply_patch and similar tools, differs from `function_call`:
* input arrives in an `input` field (raw text) instead of `arguments` (JSON-encoded args)
* output is wrapped in a JSON envelope `{"output":"..."}` — the message handler unwraps `output` / `text` / `result` keys, falling back to raw on parse failure

Without these handlers, modern apply_patch was silently dropped and no ```diff fence was emitted.

`exec_command_end` (Extended-mode shell) is intentionally NOT dispatched yet — it would duplicate the ResponseItem-derived card. Need call_id-based dedup before enabling.

`Limited` vs `Extended` mode (per `codex-rs/rollout/src/policy.rs:135-153`): the CLI default is Limited (`tui/src/app_server_session.rs: persist_extended_history: false`), so most rollouts only carry `ResponseItem::FunctionCall` + `FunctionCallOutput` for shell tools — NOT `EventMsg::ExecCommandEnd`. Termory's `codex_function_call_output_message` is the authoritative path for shell output in that mode; `codex_parse_exec_output` strips the wrapper to recover `aggregated_output`.

**Claude Code** (`claude_tool_use_text`) — `.audit-sources/claude-code/src/components/messages/AssistantToolUseMessage.tsx:152` wraps `<bold>{userFacingName}</bold>({renderToolUseMessage})`. Each Tool's `UI.tsx` provides both pieces. All argument values pass through `wrap_inline_code` so markdown-special chars in user payloads can't leak.

`claude_tool_use_text` returns `Option<String>` so tools that Claude TUI explicitly suppresses (`userFacingName: ''` AND `renderToolUseMessage: () => null`) can return `None` and the entire tool card is skipped — matching the TUI which renders nothing for them:

| Raw name | userFacingName source | Termory output |
|---|---|---|
| `Bash` | BashTool/UI.tsx | `**Bash**({command})` (empty cmd → just `**Bash**`) |
| `Read` / `View` | FileReadTool/UI.tsx:179 → "Read" / "Read agent output" (path matches `/tasks/{taskId}.output` per `getAgentOutputTaskId`); `getPlansDirectory` "Reading Plan" variant is intentionally skipped (depends on session config) | `**Read**({path} · lines X-Y / · pages N / · limit N)` / `**Read agent output**({taskId})` |
| `Write` | FileWriteTool/UI.tsx → "Write" | `**Write**({path})` |
| `Edit` / `MultiEdit` / `str_replace*` | FileEditTool/UI.tsx:28-87 → "Update" by default, "Create" when `old_string === ''` (or first edit's `old_string === ''` for MultiEdit) | `**Update**({path})` / `**Create**({path})` |
| `Grep` | GrepTool.ts:170 → "Search" | `**Search**(pattern: ..., path: ...)` |
| `Glob` | GlobTool/UI.tsx:13 → "Search" | `**Search**(pattern: ..., path: ...)` |
| `WebFetch` | WebFetchTool.ts:81 → "Fetch" | `**Fetch**({url})` |
| `WebSearch` | WebSearchTool.ts:160 → "Web Search" (space) | `**Web Search**({query})` |
| `NotebookEdit` | NotebookEditTool.ts → "Edit Notebook" | `**Edit Notebook**({notebook_path})` |
| `Task` / `Agent` | AgentTool/UI.tsx `userFacingName` — "Agent" for `worker`/`general-purpose`/missing subagent_type, else the subagent_type verbatim | `**{verb}**({description})` (`verb` = "Agent" or `subagent_type`) |
| `Skill` | SkillTool/UI.tsx | `**Skill**({name})` |
| `ReadMcpResource` | ReadMcpResourceTool/UI.tsx → literal **`readMcpResource`** (camelCase, NOT title-cased) | `**readMcpResource**({uri})` |
| `ListMcpResources` | literal **`listMcpResources`** | `**listMcpResources**({server})` |
| `McpAuth` | McpAuthTool.ts → literal `'{server} - authenticate (MCP)'` (the whole label IS the verb) | `**{server} - authenticate (MCP)**` |
| `mcp__{server}__{tool}` (generic MCP) | — | `**MCP**({server}/{tool})` (matches Codex MCP) |
| **SUPPRESSED in Claude TUI** — `TodoWrite` / `AskUserQuestion` / `EnterPlanMode` / `ExitPlanMode` / `ExitPlanModeV2` / `TaskCreate` / `TaskUpdate` / `TaskGet` / `TaskList` / `TaskStop` / `TaskOutput` / `ToolSearch` | userFacingName `''` AND renderToolUseMessage returns null | `claude_tool_use_text` returns `None` → no tool card emitted at all |

**Claude content blocks** beyond `text` / `tool_use`:

- `thinking` and `redacted_thinking` → reasoning message via `claude_thinking_blocks` + `format_reasoning_body`. Claude TUI renders `∴ Thinking…` (AssistantThinkingMessage.tsx); Termory emits the unified `> *content*` blockquote instead.
- `image` (`{source: {type: "base64"|"url", media_type, ...}}`) → italic `*Image ({mime})*` or `*Image: {url}*` notice via `claude_image_part_label`.
- `tool_result.content` may be `Value::String` or `Value::Array` of `text` blocks. For `Edit` / `MultiEdit` / `Write` tools, Termory prefers the structured diff over the brief tool_result text — `claude_format_structured_patch` reads the JSONL line's sibling `toolUseResult.structuredPatch` field (the same data Claude TUI's `StructuredDiff.tsx` consumes) and emits a `**Added N lines, removed M lines**` summary header on its own line, then a ```diff fence with the actual hunks. NO `@@ -X,N +Y,M @@` text in the fence — Claude's `formatDiff` (StructuredDiff/Fallback.tsx:373-440) conveys hunk boundaries via gutter line-number jumps, not the unified-diff header. Multi-hunk patches get a blank line between hunks instead.

`claude_display_text` strips / rewrites the following Claude-internal text wrappers and constants (per `UserTextMessage.tsx:40-197` dispatch chain and `constants/messages.ts`):

| Wrapper / signal | Claude TUI | Termory output |
|---|---|---|
| `(no content)` (NO_CONTENT_MESSAGE) | null (UserTextMessage.tsx:48) | drop |
| `[Request interrupted by user]` / `[Request interrupted by user for tool use]` | `<InterruptedByUser>` italic (l.83-92) | `*[Interrupted by user]*` |
| `<tick>...</tick>` | null (l.57-59) | drop |
| `<local-command-caveat>...` | null (l.61-64) | drop |
| `<bash-stdout>...` / `<bash-stderr>...` | `<UserBashOutputMessage>` → stdout + stderr (l.66-71) | unwrapped + concatenated (stdout then `\n\n` + stderr); inner `<persisted-output>` also stripped |
| `<local-command-stdout>` / `<local-command-stderr>` | `<UserLocalCommandOutputMessage>` indented w/ Markdown (l.74-79) | inner text passed through |
| `<bash-input>...` | `<UserBashInputMessage>` `! {input}` (l.110-113) | `! {input}` |
| `<command-message>...` | `<UserCommandMessage>` `❯ /cmd args` (l.115-118) | `/cmd args` |
| `<user-memory-input>...` | `<UserMemoryInputMessage>` `# {content}` chip (l.120-122) | `\# {content}` (H1-escape so markdown doesn't render as heading) |
| `<task-notification>...<summary>...</summary>...` | `<UserAgentNotificationMessage>` `⏺ {summary}` (l.139-141) | `⏺ {summary}` |
| `<tool_use_error>...` (inside tool_result.content only) | stripped to inner text | inner error text only |
| `({tool} completed with no output)` (toolResultStorage.ts:293 placeholder) | `(No output)` summary via `BashToolResultMessage.tsx:107-121` | `(No output)` |

Feature-gated wrappers not handled: `<github-webhook-activity>` (KAIROS_GITHUB_WEBHOOKS), `<teammate-message>` (swarms), `<fork-boilerplate>` (FORK_SUBAGENT), `<cross-session-message>`, `<channel source=...>`, `<mcp-resource-update>` / `<mcp-polling-update>`. All are dropped silently via the generic `strip_display_tags` fallback.

**Claude top-level record types** (per Message.tsx:103-281 dispatch):

- `user` / `assistant` — message containers (see content-block handling above)
- `attachment` — dispatched per `attachment.type` by `claude_attachment_messages` (sessions.rs). Subtypes that emit a notice line: `directory`, `file` / `already_read_file` (with `numLines` / `cells` / `unchanged` / `bytes` detail), `compact_file_reference`, `pdf_reference`, `selected_lines_in_ide`, `nested_memory`, `skill_listing` (non-initial only), `queued_command` (prompt text run through `claude_display_text` so embedded `<task-notification>` etc. dispatch correctly), `plan_file_reference`, `invoked_skills`, `mcp_resource`. NULL_RENDERING subtypes (`task_reminder`, `deferred_tools_delta`, `command_permissions`, `date_change`, `hook_success`, `async_hook_response`, `agent_setting`, `relevant_memories`, `dynamic_skill`, `agent_listing_delta`) drop silently — matches `nullRenderingAttachments.ts:14-49`.
- `system` — dispatched by `subtype` via `claude_system_message`:
  - `local_command` → strips `<command-message>`/`<command-args>` to `/cmd args` (kind=LOCAL_COMMAND)
  - `turn_duration` → `*※ Worked for {duration}*` italic dim (matches SystemTextMessage.tsx:342-401). Duration formatted via `format_duration_short` (e.g. `45269ms` → `45.3s`).
  - `away_summary` → `*※ {content}*` italic dim (l.70-84)
  - `agents_killed` → `**Error** All background agents stopped` (l.87-101)
  - `compact_boundary` → `---\n\n*{content}*\n\n---` GFM divider notice (Message.tsx:195-203 `CompactBoundaryMessage`)
  - `microcompact_boundary` / `api_error` / other → silent drop (matches verbose-only or null fallthrough)

**OpenCode** (`opencode_v2_tool_part_text`) — each tool header uses the unified `**Verb**(args)` shape but the verb text + body content stay platform-native (matching the per-tool functions in `routes/session/index.tsx` cited below). Body decorations (`\# description` BlockTool title, bash fence with `$ cmd` prefix, ```diff diff fence, `↳ Loaded` instruction-file list, `{✓/~/✕/☐}` todo icons) are preserved verbatim — only the header line was reshaped:

(Source coordinates re-verified against OpenCode 1.17.11 `packages/tui/src/routes/session/index.tsx` — each renderer is now a named function, the durable anchor; inner sub-line numbers are approximate.)

- `Bash` / `Shell` (`function Shell`, l.2036): header `**Shell**({wrap_inline_code(cmd)})`. With output → followed by `\# {description ?? "Shell"}\n\n```bash\n$ {cmd}\n{output}\n```` (original BlockTool body). Without output → header alone (original InlineTool was `$ {cmd}`). Output resolution: `metadata.output ?? state.content` then `stripAnsi` (l.2041) to drop terminal colour codes.
- `Glob` (`function Glob`, l.2132): `**Glob**(pattern: {wrap_inline_code(pattern)}, path: {wrap_inline_code(path)} — {N} match[es])` (singular/plural matched).
- `Read` (`function Read`, l.2145): `**Read**({wrap_inline_code(filePath)} [other=...])` + per-entry `↳ Loaded {path}` lines using CommonMark hard breaks (`\` line terminator). `metadata.loaded` is the `instruction.resolve` array from `read.ts` — the auto-loaded instruction files (AGENTS.md / CLAUDE.md / etc.) the Read tool fetched alongside the requested file; surfaced because it's data, not decoration.
- `Grep` (`function Grep`, l.2180): `**Grep**(pattern: {pattern}, path: {path} — {N} match[es])`.
- `WebFetch` (`function WebFetch`, l.2193): `**WebFetch**({wrap_inline_code(url)})`.
- `WebSearch` (`function WebSearch`, l.2201): `**{provider label}**({wrap_inline_code(query)} — {N} results)`. Verb is provider-derived per `webSearchProviderLabel` (`tool/websearch.ts`): `"parallel"` → `Parallel Web Search`, `"exa"` → `Exa Web Search`, otherwise → `Web Search` (default, with space — matches Claude's verb).
- `Write` (`function Write`, l.2095): `**Write**({wrap_inline_code(filePath)})` + ```{lang from ext}\n{content}\n``` body when completed.
- `Edit` (`function Edit`, l.2325): `**Edit**({wrap_inline_code(filePath)})` + ```diff\n{diff}\n``` body when diff present.
- `ApplyPatch` (`function ApplyPatch`, l.2378): per-file header → `**Deleted**({path})` / `**Created**({path})` / `**Moved**({old → new})` / `**Patched**({path})` + ```diff fence (matches FileChange tags in fileTitle()). When a file has no `patch` text, body falls back to `-N line` / `-N lines` (pluralized).
- `TodoWrite` (`function TodoWrite`, l.2454): `**Todos**\n\n{✓/~/✕/☐} {content}` per todo (verb is "Todos" — matches the original BlockTool title `\# Todos`; icons from todoIcon helper).
- `Question` (`function Question`, l.2480): `**Questions**\n\n{Q}\n{A}` per Q/A pair (verb "Questions" matches `\# Questions` title).
- `Skill` (`function Skill`, l.2516): `**Skill**({wrap_inline_code(name)})`.
- `Task` (`function Task`, l.2210): `**{Titlecase(subagent_type ?? "General")} Task**({wrap_inline_code(description)})` — verb includes the agent name prefix, matching the original `{Agent} Task — description` heading.
- generic (`function GenericTool`, l.1788): `**{name}**({input})` header + 4-backtick output fence when present.
- `reasoning` part (`function ReasoningPart`, l.1572) → `format_reasoning_body` (unified italic blockquote — replaces the old `_Thinking:_` inline prefix).

All tool cards emit a `⏺` / `✗` leading marker (Termory's own output). NOTE the 1.17 TUI no longer has a `status_marker` helper — `InlineTool` (l.1826) / `BlockTool` (l.1984) take `pending` / `complete` props and colour by `state.status`, the error path still carries `state.error.message`. Termory's mapping is unchanged: failed parts get the `✗` marker + a 4-backtick `Error: {message}` body, mirroring Codex / Claude / Gemini failure formatting.

Top-level `SessionMessage` types beyond the tool parts. **1.17 model change:** the message DATA is now constructed in `packages/tui/src/context/data.tsx` (the old single Match-arm dispatch is gone) and rendered by `UserMessage` / `AssistantMessage` in `routes/session/index.tsx`; `synthetic` and `compaction` are now PART types, not top-level message types. Termory's `opencode_v2_*` output is unchanged — these are the new source coordinates:

- `user` (`UserMessage`, l.1350) → `text` body + attachment row built by `opencode_v2_user_attachments`. Files surface as `` `{mime}` `` `` `{name ?? uri}` `` code-span pairs; agents as `` `agent` `` `` `{name}` ``. `references` (PromptReferenceAttachment) are persisted but TUI skips them, so Termory does too.
- `assistant` (`AssistantMessage`, l.1455) → text from parts; if `message.error.message` is set, append `*✕ {message}*` italic notice on its own line.
- `synthetic` → now a PART property (`part.synthetic`), filtered out of the visible set (`routes/session/index.tsx` l.387); Termory returns `None` so they don't appear in the transcript.
- `shell` → message type still exists, constructed in `context/data.tsx` l.192 → `$ {command}` + `strip_ansi(output)` on a second line.
- `compaction` → now a PART type, handled inside `UserMessage` (l.1378 / rendered l.1442) → bold header `**Auto Compaction**` (when `reason === "auto"`) or `**Compaction**`, then the `summary` body.
- `agent-switched` (constructed `context/data.tsx` l.136) → `▣ Switched agent to {Titlecase(agent)}`.
- `model-switched` (constructed `context/data.tsx` l.146) → `◇ Switched model to {provider}/{id}[/{variant}]`.

Audit reference is now OpenCode `1.17.11` (commit `77f2d22`) — up from the original `1.15.5` (`9324ef0`), which kept the TUI in `…/feature-plugins/system/session-v2.tsx`. 1.17.x **extracted the whole TUI into the `packages/tui` package**: tool rendering is now `packages/tui/src/routes/session/index.tsx` (per-tool named functions + `BlockTool`/`InlineTool` helpers) and message-type DATA is built in `packages/tui/src/context/data.tsx`. The verb-mapping citations above were re-verified against 1.17.11. **Structural changes from 1.15.5** (Termory's `opencode_v2_*` OUTPUT is unchanged — it's the unified `**Verb**(args)` format, independent of OpenCode's TUI): renderers are now functions (not Match arms); `synthetic` / `compaction` are PART types (not top-level message types); the `status_marker` helper is gone (replaced by `pending`/`complete` props on InlineTool/BlockTool). Per-tool body BEHAVIOUR (fences, icons, summaries) was spot-checked, not exhaustively re-diffed — if a specific tool's output looks off, diff that function in `routes/session/index.tsx` against `opencode_v2_tool_part_text`.

**Gemini CLI** (`gemini_tool_messages_from_value` + `gemini_thought_messages_from_value` + `gemini_part_to_string`) — `.audit-sources/gemini-cli/packages/cli/src/ui/components/messages/`:

- `toolCalls[]` entries (ToolShared.tsx:202 `ToolInfo`) → `{status_marker} **{displayName}**({description})` with status-aware body. `status === 'success'` → `⏺` marker, body fenced verbatim. Otherwise `✗` marker + `Error: ` prefix inside the fence (per sessionUtils.ts:654-657 only `'success'` is success). `resultDisplay` body shapes per `ToolResultDisplay.tsx` are dispatched in `gemini_result_display_to_text`:
  - `string` → as-is (markdown / plain text)
  - `Array<AnsiLine>` (each line is `Array<AnsiToken {text, ...}>`, detected via `isAnsiOutput`) → join token `text` fields, trim per-line trailing whitespace (xterm-headless pads to terminal width)
  - `{todos: ...}` → drop body (TUI hides it; TodoTray renders todos separately, ToolResultDisplay.tsx:84-87)
  - `{isSubagentProgress: true, ...}` → drop body (live-progress widget, no useful static representation)
  - `{fileDiff, fileName?}` → `gemini_format_file_diff` (DiffRenderer.tsx:204-214 `isNewFile`): when every non-header line is an addition, emit ```{lang}\n{added lines}\n``` (lang inferred from the filename extension); otherwise ```diff\n{full diff}\n```
  - `{summary, ...}` (StructuredToolResult / GrepResult / ListDirectoryResult / ReadManyFilesResult) → emit the `summary` string
  - other object → `serde_json::to_string_pretty` fallback (matches TUI's `JSON.stringify(obj, null, 2)`)
- `thoughts[{subject, description}]` array (ThinkingMessage.tsx:22 `normalizeThoughtLines`) → one reasoning message per entry. Subject is wrapped in `**...**` so `format_reasoning_body` keeps it as a bold blockquote header line (mirrors the TUI's bold-italic subject + italic body at l.84-93); description lines render italic. `gemini_normalize_escaped_newlines` applies the same `\\n` / `\\r\\n` → real-newline pass as `textUtils.ts:168` so persisted escaped literals split into multiple lines. Noise filtering matches the source (skip whitespace-only or `...` runs)
- System-notice records (`type: 'info' | 'error' | 'warning'`) → `format_gemini_system_notice` wraps the body in an italic span with the TUI icon prefix (`ℹ` per InfoMessage.tsx:30 / `✕` per ErrorMessage.tsx:16 / `⚠` per WarningMessage.tsx:17). Multi-line bodies use CommonMark hard breaks (`  \n`) so the italic span survives across visual lines without a paragraph break terminating it
- **`<session_context>` injected environment context** — Gemini prepends a `<session_context>…</session_context>` block (date / OS / workspace / **directory tree**) as the FIRST `user` record of every session (`environmentContext.ts getEnvironmentContext`; the official TUI hides it). Termory surfaces it (full-history rule) but handled like Codex's `<environment_context>` (`gemini_is_injected_context` + the `gemini_message_from_value` branch): unwrap the tag, emit a `role: "system"` notice `*[Gemini session_context]*\n\n{inner}`. Unwrapping also stops the raw `<…>` block from being dropped by the markdown renderer. The inner directory tree is **selectively fenced** by `gemini_fence_tree_blocks` (a contiguous run of `├│└` box-drawing lines + the root-path line before it gets a 4-backtick fence) so the tree keeps its monospace layout while the surrounding `- **…**` markdown still renders. For the session **title**, `gemini_extract_first_user_message` strips the `<session_context>…</session_context>` wrapper off the first user message's content (`extract_xml_tag_value` before `gemini_clean_message`) — so a session that titles on the injected block (it IS the first `user` record, and the session has no summary) shows the **inner text**, NOT the literal tag. `parse_gemini_session_file` then caps that first-user-message title with `snippet_line` (120 chars — the block is long; the summary path is left untouched), and the detail-pane title `<h2>` (App.tsx) is `line-clamp-2`'d. **Do NOT reintroduce the earlier approach** that SKIPPED session_context entirely and fell back to a fabricated "Empty conversation" placeholder — that broke the title and cascaded into a string of other bugs. Tests: `gemini_extract_first_user_message_strips_session_context_tag`, `gemini_session_context_unwraps_and_fences_only_the_tree`, `gemini_fence_tree_blocks_leaves_non_tree_text_untouched`.
- Parts with `executableCode: {code, language}` → ```{lang}\n{code}\n``` fence
- Parts with `codeExecutionResult: {outcome, output}` → 4-backtick output fence + italic `*Outcome: OUTCOME_FAILED*` footer when non-OK
- Parts with `inlineData: {mimeType, ...}` → `*Inline data ({mime})*` italic notice
- Parts with `fileData: {fileUri}` → `*File: {uri}*` italic notice
- Parts with `functionCall: {name}` → `*Tool call: {name}*` (inline marker; the structured card comes from `toolCalls[]`)
- Parts with `functionResponse: {name}` → `*Tool response: {name}*`

### Helpers used across all four platforms

- `wrap_inline_code(content)` (sessions.rs:48) — CommonMark §6.1: pick a backtick delimiter longer than the longest run inside the content; pad with spaces when content starts or ends with a backtick. Used everywhere an unsafe user payload (path, command, URL, query, pattern) becomes inline `\`code\`` in markdown.
- `format_reasoning_body(content)` (sessions.rs:71) — line-by-line `> *...*` italic blockquote, escapes stray `*` / `_` so italic spans can't break mid-line.
- `merge_tool_outputs(messages)` (sessions.rs runs in `parse_claude_session` and `parse_codex_session`): folds matching `tool_result` / `tool_error` into the leading `tool_use` card. On a matched failure it prefixes the leading line with `✗ ` (instead of `⏺ `) and prepends the fence body with `Error:` (plus `Exit code N` when `SessionMessage.exit_code` is set). Orphan results (no matching tool_use) keep their text but also get a `⏺` / `✗` status prefix.
- `codex_parse_exec_output(text)` returns `CodexExecOutput { raw, exit_code }` — strips Codex's `Chunk ID: ... Output:` wrapper line-by-line so the visible body is just `aggregated_output`, AND extracts the exit code for the `Error: Exit code N` line.
- `codex_parse_patch_actions(patch_text)` scans `*** Add/Delete/Update File:` markers and returns `Vec<CodexPatchAction>` for the apply_patch header builder.
- `strip_ansi(text)` — drop ANSI escapes (CSI colour / cursor codes, OSC title-set sequences, and lone `ESC + letter` escapes). Used for OpenCode Bash output (`routes/session/index.tsx` l.2041) and `type: "shell"` message captures (`context/data.tsx` l.192). No regex crate — small inline state machine, leaves non-ESC content untouched.

### Tool message metadata + UI

- `SessionMessage` carries two `#[serde(skip)]` fields used only during parsing/merging:
  - `tool_use_id: Option<String>` — links `tool_use` ↔ `tool_result` by provider id (Claude `tool_use.id` / Codex `function_call.call_id`).
  - `exit_code: Option<i64>` — Codex shell exit code parsed from `function_call_output` metadata; surfaced in the `Error: Exit code N` fence line.
- Provider-native combined formats (OpenCode parts, Gemini toolCalls, Codex EventMsg-derived cards) skip `merge_tool_outputs` — they already arrive complete with their own fence and add the `⏺` / `✗` prefix at emission time.

### Markdown rendering (frontend)

- The detail-pane body uses `react-markdown` + `remark-gfm` (tables / task lists / strikethrough). No syntax-highlight pass: code blocks render as plain monospace until a per-language renderer is added intentionally.
- No DOMPurify / rehype-sanitize: react-markdown emits React elements (not HTML strings), so raw `<tag>` in session content is auto-escaped by React's text node rendering and displays as literal text — same characters the CLI shows.
- No raw / rendered toggle and no `viewMode` state — every message renders through the same react-markdown pipeline. The "open original file" affordance in the detail header still lets the user inspect the underlying JSONL / db row outside Termory.
- **TUI-style scrollback layout**: messages render as continuous vertical flow without card borders. The role chip + per-role color bar live as inline Tailwind utilities in `MessageList.tsx` (no `.roleBar` / `.roleLabel` CSS classes after the v4 migration). Role color tokens are still in `src/styles.css` via CSS vars (`--role-user`, `--role-assistant`, `--role-tool`, `--role-event`); the bar/text inline styles read those vars so the color palette stays single-sourced.
- The only meaningful CSS class still in `src/styles.css` is `.message-body` (note hyphen, **not** the old `.messageBody`). It wraps the react-markdown output and carries:
  - `padding-left: 11px` so body text aligns under the role label (bar 3px + 8px gap = 11px) — overridden to `padding: 0` for the memory/skill single-doc view via a Tailwind variant on the wrapping element
  - `word-break: break-all` on inline `<code>` so long paths inside `**Read**(\`/very/long/path\`)` wrap with the surrounding paragraph
  - `.message-body pre { margin: 0; padding: 0 0 0 1em }` — fences sit flush with the verb header above, 1em left indent so fence content lines up under the `⏺` marker
  - `.message-body p + pre { margin-top: -0.4em }` — pulls a fence visually onto the preceding paragraph (summary + diff pair). CommonMark-required blank line stays in the source
- The `.message.tool .messageBody p + p { padding-left: 1em }` rule that used to indent second-paragraph summaries was removed when the role-chip layout moved to inline Tailwind; the visual still works because of the verb-header layout's intrinsic indent. If you regress this, the `⎿ Added N lines, removed M lines` summary above an Edit diff will sit flush-left instead of aligning under the verb.
- Unordered lists render with the `- ` text marker via `list-style-type: "- "` (matching Codex TUI's `start_item` output at `codex-rs/tui/src/markdown_render.rs:754-760`).
- Tool detail-pane loading state shows only the spinner icon (no `Loading transcript` label) so the brief delay between session select and detail load is unobtrusive.

## History and Preview Behavior

- Session lists should come from the same stored records the official tool uses for its history/resume list.
- Session list fields should use official values when available: title, project/cwd, timestamps, source id, and original path.
- Loading a session should parse the underlying transcript/messages for that exact selected record.
- Message previews in the detail pane should show the same user-visible content style as the official tool, including command/tool output formatting.
- **Show everything that was recorded.** Termory deliberately surfaces content that the official TUIs hide (Claude's suppressed tools, `<tick>` / `<local-command-caveat>` / NULL_RENDERING attachments / `isMeta` user messages; Codex's `<environment_context>` / `<user_instructions>` / `<skill_instructions>` / etc.; OpenCode's `synthetic` parts). The transcript is the source of truth — hiding things makes the history misleading. List-time filters (`isSidechain`, `kind === "subagent"`) that decide *which sessions* appear in the list are separate and still apply.
- Compatibility readers are allowed only for real older/alternate storage layouts and should not override the current official path.
- App-only UI features such as source filters, project grouping, search, stats, cross-source sorting, and the Memory/Skills pane organization must not be used as evidence for official data behavior.

## Implementation Rules

- Keep data acquisition and message preview formatting aligned with the official tool behavior, but **never hide content** — Termory is a history browser, so anything recorded must be surfaced (usually as an italic `*[wrapper-name]*` notice when there's no nicer representation). See the "Unified tool-message format — LOCKED RULE" rule 7.
- Do not add custom title/message fallbacks unless the official tool does the same.
- Format command and tool output the way the official tool **actually renders it in its TUI** — not what its docs say, and not what feels right. Always grep `.audit-sources/<repo>/` for the real render function and put a `// path/to/file.tsx:LINE` citation next to the matching Termory branch. Earlier rounds of this codebase had ~600 lines of tool-formatting guesswork that diverged from every TUI; those have been replaced and the rule exists to prevent regressing.
- Treat UI behavior separately from official data behavior. Source filters, project grouping, search, stats, cross-source sorting, and the Memory/Skills views are app UI behavior.
- Keep changes scoped. Avoid unrelated refactors.
- **Cross-platform is a hard requirement (macOS / Linux / Windows).** The release CI builds all four targets (macOS arm64 + x64, Linux, Windows) on every `v*` tag — keep it green, and don't let a single-platform API leak into a shared path. Concretely:
  - Gate every OS-specific call behind `#[cfg(...)]`: `cfg(unix)` for `chmod`/permissions (with a `cfg(not(unix))` fallback), `cfg(target_os = "macos")` for Dock / `set_dock_visibility` / tray niceties, per-OS branches for CLI-binary PATH probing and the filesystem watcher.
  - `src-tauri/src/main.rs` carries `#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]` — without it the Windows release binary uses the console subsystem and a terminal window opens alongside the app on launch. DO NOT REMOVE (debug keeps the console so logs stay visible).
  - **Every SILENT subprocess spawn must go through `providers::hide_console`** (std `Command`; for tokio use `creation_flags(providers::CREATE_NO_WINDOW)`): on Windows a GUI-subsystem parent flashes a console WINDOW for every console child, and `.cmd` shims (all npm-installed CLIs) are additionally routed through `cmd.exe` by the Rust runtime — so even `codex --version` pops a cmd window without the flag. Applied at `output_with_timeout` (version probes), terminal.rs `where_` detect, the OUTER `cmd /C start` helper (the inner `start` still opens the real terminal window), and the `codex login` capture. NEVER apply it to a terminal-launch spawn the user is meant to see (wt / powershell branches). Same mechanism as cc-switch (`misc.rs` `CREATE_NO_WINDOW` on every silent spawn); its extra explicit `cmd /D /S /C call` + `raw_arg` form for `.cmd` shims was deliberately NOT adopted — Rust ≥1.77's implicit `.cmd` routing covers our only-arg-is-`--version` case, and hand-rolled `raw_arg` quoting reopens the escaping risk the runtime hardening exists for.
  - Resolve each CLI's data path to what THAT tool actually uses per-OS. Claude/Codex/Gemini use home-relative dotdirs (`~/.codex`, …) on all platforms. OpenCode uses xdg-basedir → `~/.config` / `~/.local/share` on **every** OS including Windows, so build those via `home().join(".config/…")` and **do NOT use `dirs::config_dir()` / `dirs::data_dir()`** for them (those return `~/Library/Application Support` on macOS and `%APPDATA%` on Windows — both wrong for these tools). Cite the upstream path source.
  - Build paths with `PathBuf::join` from `home()` / `dirs::*` — never string-concatenate with `/`, and no shell/`.exe` assumptions outside a `cfg`. **Never call `dirs::home_dir()` directly — use `crate::home_dir()`** (lib.rs): in TEST builds it honors the `HOME` env override on every OS (plain `dirs::home_dir()` on Windows resolves via SHGetKnownFolderPath and ignores the environment, which made every HOME-override test silently read/write the runner's real profile); release behavior is identical to `dirs::home_dir()`.
  - Backend tests use a `HOME` override (`testutils::override_home`, honored by `crate::home_dir()`'s test-build env seam) and run on ubuntu + macos + **windows** in CI (test.yml). **Known Windows test note:** the original `migrate_claude_*` tests and the absolute-path `decode_claude_project_slug` assert are `#[cfg(unix)]`-gated — their fixtures are unix-path-shaped (Windows `fs::canonicalize` adds the `\\?\` prefix and JSON escapes the backslashes, so the substring asserts can't hold verbatim). All three migrate flows now ALSO have `#[cfg(windows)]` counterparts — `migrate_claude_session_in_rewrites_windows_cwd`, `migrate_claude_project_move_mode_rewrites_windows_cwd`, `migrate_claude_memory_in_windows_preserves_subpath_and_rejects_stray` — each using a `C:\…` cwd, deriving the expected `new_canon` the same way production does (canonicalize → strip `\\?\`), and PARSING the migrated JSONL to assert the decoded `cwd` (dodging the backslash-escaping that blocked the substring asserts). Their logic is platform-agnostic, so each was verified locally by temporarily switching its gate to `#[cfg(unix)]` before restoring `#[cfg(windows)]`; on Windows CI they run for real. Green tests ≠ full runtime verification; call out anything that needs real-device testing.
- Add or update tests when changing a parser or formatter. Skill/memory scanners have parallel tests at the bottom of `sessions.rs` — extend that block when adding scan paths. Tool-rendering tests should assert verbatim strings (e.g. `"**Search**(pattern: \"TODO\", path: \"src\")"`), not regex matches, so renames are caught.
- When adding a new scan location for an existing tool, verify against the tool's official source first (then docs as a secondary reference); do not infer from naming conventions alone.

## Verification

Run when practical:

```sh
cd src-tauri
cargo fmt
```

```sh
cargo test --manifest-path src-tauri/Cargo.toml --lib
```

```sh
npm run build
```

Parser/formatter tests should cover the relevant official storage shape, title extraction, visible messages, hidden metadata, and command/tool preview formatting. Skill/memory tests should cover the actual scan paths and the per-tool tag string (e.g. `claude,opencode` for `.claude/skills/`).

## Providers (switch CLI to a third-party API platform)

User flow: a Provider is a named snapshot of `{baseUrl, apiKey, model, ...}`. Each CLI has its own list. Activate = materialize into the CLI's live config so the next launch picks it up. Switching back to Official = clear the Termory-injected fields; the CLI's native OAuth/credentials file is **never touched**, so logins survive a round-trip.

**Local storage** — `~/.termory/` (same path on macOS / Linux / Windows), permissions `0700` dir / `0600` files on Unix. Atomic write (tmp + rename):

- `~/.termory/config.json` — UI prefs (`records_pane`, `providers_app`, `recent_searches`). No secrets.
- `~/.termory/providers.json` — provider library. On disk it's `{ "version": N, "providers": [...] }` (`PROVIDERS_SCHEMA_VERSION = 1` in `config.rs`) where `providers` is a UNIFIED array holding BOTH per-CLI providers (`kind: "official"|"custom"`) and gateways (`kind: "gateway"`). `config.rs` splits/merges by `kind`: `read_providers()` returns `kind != "gateway"`, `read_gateways()` returns `kind == "gateway"`, and each writer preserves the other kind (`entry_is_gateway`), so the strongly-typed `Vec<Provider>` parse (tray, IPC) never sees a gateway entry. `read_all_entries` runs the array through `migrate_entries(version, entries)` (a no-op at v1 — the seam where a future schema bump hooks its upgrade). No bare-array / old-`gateways[]` compatibility (clean dev baseline). Contains API keys, `0600`. **Lenient config parsing (LOCKED — valid JSON must never error):** config content that is syntactically valid JSON must NEVER make the code error, even when it holds a value THIS build doesn't recognize. The canonical case is a **downgrade**: an older binary reads a providers.json a newer version wrote, containing e.g. a `grok` provider/binding for an app this build has no feature for. That is Termory's OWN legitimate data from another version (NOT arbitrary foreign junk), so the unrecognized entry is **skipped** (simply absent from this version's UI — "unknown" ≠ "error") and everything else keeps working; only a real JSON *syntax* error may fail. Mechanism (all in providers.rs): `providers_from_json` / `gateways_from_json` parse the array entry-by-entry, dropping (with `log::warn!`) any that fail (unknown `app`/`kind`, corrupt row); `Gateway.bindings` uses `#[serde(deserialize_with = "lenient_vec")]` so an unknown-app binding drops just that binding, not the whole gateway. IPC commands with a providers-**list** arg (`provider_active_state(s)`, `activate_provider`, `deactivate_provider`) take the `ProviderList` newtype (lenient `Deserialize`) instead of a bare `Vec<Provider>` — otherwise one unknown entry fails the whole call at Tauri's arg-binding layer BEFORE the handler runs (the original `unknown variant 'grok'` bug). Single-provider args (`delete_provider`/`test_provider_api`/`fetch_provider_models`, activate's primary `provider`) stay strict — you're acting on exactly that one. The tray reads through `providers_from_json` too. Tests: `providers::tests::{providers_from_json_skips_unrecognized_entries_keeps_the_rest, gateway_with_unknown_binding_survives_keeping_known_bindings}`.

Termory does **not** store an "active provider" pointer anywhere. `provider_active_state` reverse-derives the active state on every read by parsing the CLI's live config files and matching against the in-memory provider list — this keeps Termory consistent when other tools (`cc-switch`, manual `vim`, the CLI's own OAuth flow) change the same files.

**Editing a live provider re-applies it.** Saving the editor (`saveProvider` in `ProvidersPage.tsx`) writes providers.json AND, when the edited provider is the one currently live for its CLI (matched for single-slot, or an enabled OpenCode slot), re-runs `activate_provider` so the change (model / base URL / key / options / …) reaches the live config — otherwise the edit would silently not take effect until a manual re-activate. It passes the *previous* provider copy into `providersForApp` too, so option keys the edit **removed** are still in the strip set and get cleaned (single-slot); for OpenCode the block is rebuilt wholesale so removal is automatic. If the OpenCode provider was also the startup default, it re-runs `set_opencode_default` to refresh the `model` pointer. New or inactive providers don't touch any live config.

`Provider` schema (in `providers.json`, fields default to omitted when empty):

```
{ id, app: "claude"|"codex"|"gemini"|"opencode", kind: "custom"|"official",
  name, baseUrl?, apiKey?, model?,
  npm?,            // OpenCode only — the AI SDK package, e.g. "@ai-sdk/openai-compatible"
  models?: [{ id, name }],  // OpenCode only — extra models for the picker (alongside primary `model`)
  options?: [{ key, value }] }   // "Advanced settings" — extra config entries merged into the CLI config
```

`config.ts` strips `""`/`null`/`undefined` top-level fields when writing providers.json, so an unfilled field is simply omitted. The two OpenCode-only fields are flat (no nested `opencode` block): `npm` is the official `provider.<id>.npm` package name written verbatim to opencode.json (defaults to `@ai-sdk/openai-compatible` when omitted — the editor drops it when left at the default); `models` is a list of extra models (`{ id, name }`, Rust `Vec<ProviderModel>`) surfaced in OpenCode's picker alongside the primary `model` — each written as `models[id] = { name }` (name defaults to id when blank); the editor renders it as add/delete ID+name rows. The **primary `model` is written first** in the `models` map — this relies on `serde_json`'s `preserve_order` feature (enabled in `src-tauri/Cargo.toml`), which makes ALL Termory JSON writes keep insertion order instead of alphabetizing (so merges into user configs also preserve the user's existing key order, and tool-arg renders like OpenCode `Read`'s `[offset, limit]` stay in source order). (note: `model` = primary id, `models` = the extras list). There is **no** `claude` block — Claude's per-size `/model` routing (`env.ANTHROPIC_DEFAULT_{HAIKU,SONNET,OPUS}_MODEL`) is expressed through `options` (those keys are deliberately NOT in `override_key_is_managed`, so they pass through; append `[1m]` to a value for the 1M context window). The editor seeds a fresh Claude provider's `options` with those three keys as a protected template (read-only key, no delete) and blocks save on **duplicate keys** or **managed keys** (`isManagedOptionKey` in `provider-utils.ts` mirrors `override_key_is_managed` — keep the two in sync; a managed key would otherwise be silently skipped at write time). The persisted field is `options`; the Rust struct field is `Provider.options: Vec<ProviderOption>` and the mechanism helpers keep their `override_*` / `apply_*_overrides` names since they still describe merging-and-overriding the CLI config.

### Per-CLI materialization (source-of-truth: cite official source AND cc-switch when both verified)

All four were cross-verified against the upstream CLI source (`.audit-sources/{codex,claude-code,gemini-cli,opencode}/`) AND cc-switch's implementation (`.audit-sources/cc-switch/src-tauri/`). Cite `file:line` next to each branch — don't infer from docs.

| CLI | Files written | Key fields | OAuth credential file (untouched) |
|---|---|---|---|
| **Claude Code** | `~/.claude/settings.json` (merge) | `env.ANTHROPIC_BASE_URL`, `env.ANTHROPIC_AUTH_TOKEN`, `env.ANTHROPIC_MODEL`, plus optional sub-routing `env.ANTHROPIC_DEFAULT_{HAIKU,SONNET,OPUS}_MODEL` for Claude Code's `/model` size picker (see `model.ts:69, 105-138`) | `~/.claude/.credentials.json` (or macOS Keychain — `auth.ts:1323`). Independent file, isolated by design. |
| **Codex** | `~/.codex/auth.json` (merge) + `~/.codex/config.toml` (merge) | auth.json: `auth_mode = "apikey"`, `OPENAI_API_KEY` (matches `login_with_api_key` shape `manager.rs:529-542`; we **merge** instead of nulling `tokens` so ChatGPT OAuth survives a swap). config.toml: top-level `model_provider`, `model`; `[model_providers.termory]` block with `name`, `base_url`, `wire_api = "responses"`, `requires_openai_auth = true` (gates `AuthManager`'s auth.json load — `tui/src/lib.rs:1817`). **Never set `env_key`** — it forces Codex onto a hard env-var path with no fallback (`model-provider/src/auth.rs:92-103`). | The `tokens` / `last_refresh` / `agent_identity` fields **inside** auth.json. Termory never overwrites them; deactivate only removes `auth_mode` + `OPENAI_API_KEY`. |
| **Gemini CLI** | `~/.gemini/.env` (dotenv merge, `chmod 0600`) | `GOOGLE_GEMINI_BASE_URL` (triggers GATEWAY mode `contentGenerator.ts:85-87`), `GEMINI_API_KEY`, `GEMINI_MODEL` (`config.ts:836-837`). Other env vars in the file are preserved. | `~/.gemini/oauth_creds.json` + `~/.gemini/google_accounts.json` (`storage.ts:22, 87`). Independent files, isolated. |
| **OpenCode** | `~/.config/opencode/opencode.json` (merge) — `auth.json` is **never touched** | Everything lives in opencode.json under `provider.<termory-id>` (key `termory-<provider.id>`): `name`, `npm` (the official AI SDK package, e.g. `@ai-sdk/openai-compatible` — from the `npm` field, defaulting to openai-compatible), `options.{baseURL, apiKey}`, `models: { <id>: { name } }` (primary `model` + the `models` extras — **model-list dedup**: `reject_duplicate_model_ids` (providers.rs, SHARED by grok / OpenCode / Claude Desktop activate + both editors' frontend) rejects a repeat WITHIN the `models` list, but does NOT compare against the primary `model`, so re-declaring the primary ONCE in the list to give it a custom name stays allowed — pinned by `opencode_dedup_allows_primary_redeclared_in_list` vs `opencode_activate_rejects_duplicate_models`), and the provider's `options` entries nested **inside the same `options` bag** (`provider.<id>.options.<dot-path>` — keys relative to `options`; `baseURL`/`apiKey` are managed, see `override_key_is_managed`). Enabling rebuilds the whole `provider.<termory-id>` block (`block.clear()`), so options are scoped per-provider — removed keys vanish on re-enable, deleting the slot drops them, and **sibling providers' options are never touched (no top-level strip)**. Matches cc-switch's pattern (`opencode_config.rs:89-104`). The top-level `model` default is set only by the separate `set_opencode_default`; "Set Official" (`deactivate_opencode`) only clears that pointer and leaves enabled slots + their options. All other `provider.*` blocks are preserved. | `~/.local/share/opencode/auth.json` — reserved for `providers login` / `/connect` (OAuth + api-key entries). Termory writes nothing there, so those credentials survive a round-trip untouched. |
| **Grok Build** | `~/.grok/config.toml` (merge, atomic) | The OFFICIAL custom-model mechanism (docs.x.ai/build/settings#example-configtoml + settings/reference#toml-values; verified against the real grok 0.2.93 TUI): **SINGLE-SLOT (one active provider, like Claude/Codex) but MULTI-MODEL.** Grok's `[model.*]` is a FLAT list — each `[model.<key>]` is one picker entry (NOT OpenCode's nested `provider.models`). A grok provider carries a `models` LIST (REQUIRED — reuses the OpenCode `{id,name}` rows) + an OPTIONAL default (`model`, must be one of the listed models). `activate_grok` is ONE action: sweep Termory's previous entries (single-slot ownership by api_key VALUE — `grok_entry_is_ours` / `strip_grok_entries`), write THIS provider's models as `[model."<provider-id>-<model-id>"]` entries (key = provider id + `-` + model id, unique per model; `model` field = upstream id — grok sends the ENTRY KEY when omitted, empirically captured; `base_url`, `name` = the model's name-else-id, `description` = provider name, `api_key`), AND — in the same call — set `models.default = "<pid>-<default>"` when `p.model` is set. `[model]` is written IMPLICIT (`set_implicit(true)`) so only `[model.<key>]` sub-table headers are emitted, never a bare empty `[model]`. `deactivate_grok` sweeps our entries + clears a `models.default` that pointed at one → factory shape. `delete_provider_traces` is a no-op (single-slot — the delete flow deactivates separately). `read_active_grok`: active iff `models.default` names one of our entries, matched by that entry's base_url+api_key. Frontend: grok uses the standard SINGLE-slot activate/deactivate/delete (NOT the OpenCode multi-slot `isMultiSlot` branches — grok is excluded); ProviderEditor/GatewayEditor make `models` required + `model` an OPTIONAL default chosen from the list (validation: default ∈ models). NOTE: an earlier pass wrongly mirrored OpenCode's enable-vs-default split + multi-provider coexistence — removed; grok is one-active-provider. Factory config has NO `models.default` and NO `[model]` entries (VERIFIED against a fresh install's `~/.grok/config.toml`: only `[cli]`/`[marketplace]`, the official account's model choice is NOT stored there) — deleting all Termory providers returns to that shape. Termory writes NOTHING to config.json for grok (the earlier `grok_prev_*` snapshots were deleted 2026-07). **`api_backend` (wire API) is a dedicated dropdown** — Default / `responses` / `chat_completions` / `messages` (enum per `ApiBackend` in xai-grok-sampling-types/types.rs). **"Default" (unset) OMITS the field from the `[model.*]` entries** so grok applies its OWN default — `chat_completions` via `#[default]` + `ModelInfo::fallback` — and keeps following grok if that default ever changes; only an explicit choice is written (aligned to official logic, user decision 2026-07; the editor default was previously a hardcoded `responses`). Stored on `Provider.apiBackend` (+ `GatewayBinding.apiBackend`), written into `[model.*]` entries by `activate_grok` only when explicitly chosen (unset → omitted), and shown in the editor BEFORE the model fields. **Model-list dedup**: `reject_duplicate_model_ids` (shared — see the OpenCode row) rejects a repeat within `models`. Managed override keys: `models.default` + the SIX owned fields (`GROK_ENTRY_FIELDS` = model/base_url/name/description/api_key/**api_backend**) of ANY `model.*` entry (dynamic rule — entry keys are provider ids; mirrored in isManagedOptionKey), so `model.*.api_backend` is owned by the dropdown, not a pass-through; other per-entry keys (`context_window`, `extra_headers`, … per docs.x.ai/build/settings/reference) pass through as Advanced settings. All grok paths honor `$GROK_HOME` (`providers::grok_home_dir`, docs.x.ai/build/settings — like CLAUDE_CONFIG_DIR): config, sessions scan, skills, watcher target. Do NOT touch `endpoints.xai_api_base_url` — that's grok's internal endpoint override, not the custom-model mechanism (a first implementation guessed from binary strings used it; replaced after reading the official docs). Gateway binding: `openai-compatible`, `models` list REQUIRED + optional default (`grokMissingModels` / `grokDefaultInvalid`). Tests: `grok_activate_roundtrip_preserves_config_and_auth` (activate writes entries+default+api_backend in one action → deactivate returns to factory) + `grok_single_slot_activate_replaces_previous_provider` + `grok_read_active_matches_grok_provider_not_a_same_creds_other_cli` (the missing-app-filter fix: a same-creds provider on another CLI must not be matched, else editing the grok provider never re-applies). **Quota: IMPLEMENTED (2026-07-16, source-verified + live-tested)** — Grok Build is CREDIT-based; the TUI's `/usage` data is the `x.ai/billing` extension (`xai-grok-shell/src/extensions/billing.rs`) fetching `GET https://cli-chat-proxy.grok.com/v1/billing?format=credits` (proxy default agent/config.rs:46) with the auth.json bearer + `X-XAI-Token-Auth: xai-grok-cli` (auth/config.rs:288) + `x-userid` + `x-grok-client-version`. `quota.rs` grok arm: `parse_grok_credentials` (first auth.json entry with `key`+`user_id`; JWT-exp staleness check) → `query_grok_quota` → `parse_grok_billing` (`creditUsagePercent` 0-100, proto3 omits zero → missing = 0%; `currentPeriod` WEEKLY→`seven_day` / MONTHLY→`30_day` so tray W/M + card labels apply; `currentPeriod.end` else `billingPeriodEnd` → resetsAt; on-demand cap/used → `ExtraUsage` USD). `plan` = the subscription tier display name, official precedence (mvp_agent/mod.rs `resolve_subscription_tier_for_telemetry`): `fetch_grok_plan` GETs cli-chat-proxy `/v1/settings` → `RemoteSettings.subscription_tier_display` ("SuperGrok"/"X Premium+"/"Free" — config-types/lib.rs:718, snake_case verbatim, no rename_all; headers mirror the official settings fetch remote/client.rs:17-41 — billing's 5 + optional `x-email` + `x-grok-client-identifier: grok-shell` + `x-grok-client-mode: interactive`, the last two as consts mirroring xai-grok-http/lib.rs defaults; the billing fetch itself also gained official billing.rs's `x-grok-client-mode` — each request aligns to ITS official counterpart's exact header set, user decision 2026-07-18, live-verified 200 on both; the billing GET and the `/v1/settings` plan lookup run CONCURRENTLY via `tokio::join!` — independent requests, no reason to serialize two 10s-timeout calls; both `jwt_exp_seconds` and `grok_jwt_tier_plan` share one `jwt_claims` payload-decode helper; both plan sources route through the existing `display_plan` tidy helper, same as Claude/Codex/Gemini), falling back to `grok_jwt_tier_plan` (numeric JWT `tier` claim 0-6 → display names, mirror of `jwt_tier_claim` mvp_agent/mod.rs; claim can be STALE right after an upgrade, and real tokens may LACK it entirely — a live team-account token had no `tier` claim at all, so `/settings` is the practical primary). Both best-effort: any failure → plan absent, never an error. **NO token refresh — LOCKED (2026-07-16, learned by BREAKING a real login):** auth.x.ai rotates refresh tokens WITH REUSE DETECTION — a refresh rotates the RT server-side, and reusing a stale RT revokes the whole family; grok itself persists the rotated RT to auth.json under a file lock (manager.rs:64), so a client that refreshes without persisting (Termory's never-write-credentials rule) leaves grok holding a dead RT and LOGS THE USER OUT. (A probe's "old RT still works" observation was a rotation grace window, not reusability.) So Termory uses the stored ~1-day access token while valid and reports Expired otherwise ("run grok once" — grok's own refresh rewrites auth.json and the quota returns). Tests: `parse_grok_billing_*`, `parse_grok_credentials_reads_entry_and_expiry`, `credential_path_matches_grok_auth_json`, `grok_jwt_tier_plan_maps_claims_to_display_names`, `parse_grok_settings_plan_reads_display_name`.
| **Claude Desktop** (`claude-desktop`, GUI app — `src-tauri/src/claude_desktop.rs`, NOT settings.json env) | macOS: `~/Library/Application Support/{Claude,Claude-3p}/claude_desktop_config.json` (merge) + `Claude-3p/configLibrary/<PROFILE_ID>.json` (profile) + `configLibrary/_meta.json`. Windows: MSIX-virtualized — `%LOCALAPPDATA%\Packages\Claude_<hash>\LocalCache\Roaming\{Claude,Claude-3p}\…` (see "Windows paths" below; raw `%APPDATA%`/`%LOCALAPPDATA%` are unpackaged fallbacks). | **Direct mode only** (cc-switch's proxy mode is OUT OF SCOPE — needs a local HTTP proxy Termory doesn't have). Both config files get `deploymentMode: "3p"` (merge — `read_json_or_empty` PROPAGATES a parse error so a corrupt user config aborts + `with_rollback` restores, never truncates); the profile is the gateway shape `{ inferenceProvider: "gateway", inferenceGatewayBaseUrl, inferenceGatewayApiKey, inferenceGatewayAuthScheme: "bearer", disableDeploymentModeChooser: true, coworkEgressAllowedHosts: ["*"] }` + an **optional `inferenceModels`** built from the editor's model list (`build_profile`/`inference_models`: each `{id, name}` → `{name, labelOverride?, supports1m?}`, a trailing `[1m]` on the id → `supports1m`; empty list → key omitted, Claude Desktop auto-discovers). The provider's `options` (Advanced settings) merge into the profile JSON — managed keys (`inferenceProvider`/`inferenceGateway*`/`inferenceModels`/`disableDeploymentModeChooser`/`coworkEgressAllowedHosts`) are skipped, checking BOTH the exact key AND its dot-path ROOT so `inferenceGatewayBaseUrl.x` can't clobber a managed scalar. `_meta.json` upserts `{ id: PROFILE_ID, name: "Termory" }` + `appliedId`. Restore-official flips both to `"1p"`, deletes the profile, clears our meta entry, strips any `enterpriseConfig` gateway keys. All writes are snapshot+rollback / atomic (`with_rollback`). Reverse-derive matches `inferenceGatewayBaseUrl` + `inferenceGatewayApiKey` against the provider list. | Claude Desktop's own first-party login — never touched (only `deploymentMode` + our profile/meta change), so a 3P↔Official round-trip is lossless. |

**Claude Desktop (3P gateway profile) — Termory specifics.** TERMINOLOGY: Claude Desktop is managed as a **provider** under provider management — NOT Termory's separate **AI Gateway** feature (the Gateways tab). "Gateway profile" / `inferenceProvider: "gateway"` / `inferenceGateway*` are **Claude Desktop's OWN** field names for its 3P config — unrelated to Termory's AI Gateway. (Claude Desktop can also be a binding *target* of the AI Gateway feature, like any other provider — see "Gateway binding" below.) It is **not** a CLI: `CliApp::ClaudeDesktop` (serde `"claude-desktop"`) has `is_cli() == false`, so it's excluded from terminal flows (tray recent/new-session) and from `$PATH` binary probes; it's detected by config-dir existence (`claude_desktop::is_installed`) and platform-gated to macOS/Windows (`is_supported`). **Version**: `claude_desktop::version()` reads `CFBundleShortVersionString` from the app bundle's `Info.plist` on macOS (XML scan, no plist crate), with a cross-platform fallback (and the primary source on Windows) of `updaterLastSeenVersion` in `<config-dir>/config.json`; surfaced via `detect_cli_versions` like a CLI's `--version`.

**Tab always shows** (mapped from `CLI_APPS` like the others). `installed["claude-desktop"]` only drives the InstallGuide-vs-provider-list choice + activation + gateway-binding gating, NOT tab visibility. On Linux / unsupported / no-install, `is_installed` is false → the tab shows the InstallGuide (download link), activation is blocked, gateway binding is hidden, and `read_active` returns Official without touching disk.

**Editor** (`ProviderEditor`/`GatewayEditor`, `isClaudeDesktop`): **Name + Base URL (the only required fields) + an OPTIONAL API key + an OPTIONAL Models list + Advanced settings**. The API key may be left blank (filled in later) — `apply` only errors on a missing Base URL, not a missing key (like the other providers; an empty bearer is written, no `*` on the field). NO primary Model field, no AI SDK. The Models list reuses the OpenCode `{id, name}` rows (shared `providers.modelList` title across OpenCode + Claude Desktop) with Claude-Desktop help copy (`help.cdModels` / `providers.cdModelIdPlaceholder`); 1M is a `[1m]` suffix on the id (no separate field). Model ids are HARD-validated by `isClaudeSafeModelId` (mirror of Claude Desktop's own rule: `claude-*` / `anthropic/claude-*` + role sonnet/opus/haiku/fable, after stripping `[1m]`) — a non-Claude id **blocks save** in BOTH editors (`cdModelsValid` / `cdBindingInvalidModel`), matching Claude Desktop's `invalid_config` rejection of non-Anthropic models (verified in its log). `inference_models` does NOT read the top-level `provider.model` (claude-desktop has no primary model). Advanced settings (`options`) merge into the profile JSON; managed keys are the `inferenceProvider`/`inferenceGateway*`/`inferenceModels`/`disableDeploymentModeChooser`/`coworkEgressAllowedHosts` set (`override_key_is_managed` / `isManagedOptionKey` mirror).

**Gateway binding**: claude-desktop IS bindable when the gateway speaks Anthropic — `appProtocols["claude-desktop"] = caps.anthropic ? ["anthropic"] : []` — reusing `providerFromBinding` / `provider_from_binding` (both now pass `models` for claude-desktop). The GatewayEditor install-gates **every** app: `for app of CLI_APPS: if (!installed[app]) protocols[app] = []`, with `installed` threaded ProvidersPage → GatewaysPage → GatewayEditor — so you can't bind a tool you can't run (mirrors the Providers tab).

**Windows paths**: the claude.ai exe installs an **MSIX-packaged** app whose AppData is VIRTUALIZED — the app writes `%APPDATA%\Claude`, but the REAL on-disk location (what an unpackaged app like Termory sees) is `%LOCALAPPDATA%\Packages\Claude_<publisherhash>\LocalCache\Roaming\Claude` (verified on real hardware; MSIX copy-on-write means files Termory writes there ARE what the app reads back). `current_paths()` resolves the parent via `select_config_parent(msix_package_roaming_parents() + [%APPDATA%, %LOCALAPPDATA%])` — package `LocalCache\Roaming` parents come first (scanned by the `Claude_*` prefix in `Packages\`, publisher hash never hardcoded), first parent already holding a `Claude*` (non-3p) dir wins, else the first candidate. (The original bug: only `%LOCALAPPDATA%` was checked — cc-switch's assumption — where nothing matches the `Claude*` rule, so Windows installs were undetectable.) Within the chosen parent, `pick_windows_claude_dir(parent, threep)` — exact `Claude`/`Claude-3p` fast path, else a single shallow `read_dir` scan for `Claude*`/`*-3p` variants (`windows_claude_dir_matches`). The `Claude-3p` dir is resolved INDEPENDENTLY (`select_threep_dir`): a fresh Windows install pre-creates `%LOCALAPPDATA%\Claude-3p` before any normal config dir exists (observed on real hardware), so an EXISTING `*-3p` dir under any candidate parent wins, else it defaults to the normal parent's sibling. The watcher watches every candidate parent PLUS the `Packages\` dir itself (a `Claude_*` package dir appearing = the MSIX install event); a LocalCache that appears after watcher startup self-heals via the tray menu-open / Recheck probes (`install_watch_parents`). NOTE: the Windows path/runtime is **untested on real hardware** (logic + CI compile only); macOS is end-to-end verified (the app accepts the profile, enters 3P gateway mode, routes to the gateway — confirmed in Claude Desktop's own log).

Reuses the generic `activate_provider` / `provider_active_states` / `test_provider_api` IPCs and the `"Claude"` BrandIcon. `pub(crate)` helpers reused from providers.rs: `atomic_write` / `load_json_object` / `write_json_object` / `mask_secret` (char-safe slicing) / `string_match` / `infer_json_value` / `json_set_path` / `override_key_is_managed`. Tray: provider-switch submenu when installed, EXCLUDED from terminal flows (`is_cli()`), `all()` orders it right after Claude Code. Tests: `claude_desktop::tests::*` (apply / restore / read_active / build_profile-models+options / **corrupt-config-no-truncate** / version / windows-match) + frontend `provider-utils.test.ts` (`isClaudeSafeModelId`, `isProviderList`, `providerFromBinding`) / `ProviderEditor.test.tsx` / `GatewayEditor.test.tsx` (per-app install-gating).

**Model inputs use `ModelCombobox`** (`src/components/ModelCombobox.tsx`): a cmdk-`Command`-based inline combobox (free-text typing + auto-fetched `fetch_provider_models` suggestions) used for EVERY model field — ProviderEditor primary + OpenCode extra models, GatewayEditor binding + extra models. It renders INLINE (no portal, no Radix Popover) because both editors live in a Radix Dialog, where a portaled popup gets blocked by the Dialog's `pointer-events:none` + `react-remove-scroll` lock (and Radix Popover separately freezes the Tauri WebKit overlay). cmdk filters by plain substring; options are deduped; chevron on the right, no search icon; unified placeholder. Gotcha: cmdk's `CommandInput` does NOT forward `id`, so each field is labelled via `aria-label`, not `<Label htmlFor>` (tests query by label).

**Codex "stable provider id" rationale:** Codex stores session history keyed by `model_provider`. If we used a different id per provider, switching would visually "drop" history. We pin all Termory-written Codex provider blocks to id `"termory"` (`TERMORY_PROVIDER_ID` constant) and refuse to overwrite Codex's built-in reserved ids (`openai`/`amazon-bedrock`/`ollama`/`lmstudio` — `CODEX_RESERVED_IDS`).

**Claude sub-model routing (via overrides):** Claude Code's `/model` menu in 3P mode reads `process.env.ANTHROPIC_DEFAULT_{HAIKU,SONNET,OPUS}_MODEL` via `getDefaultXxxModel()` (`model.ts:105-138`). There is no dedicated UI/field for this anymore — users route sizes by adding `env.ANTHROPIC_DEFAULT_{SONNET,OPUS,HAIKU}_MODEL` **overrides** (e.g. Sonnet → `gpt-5`, Opus → `claude-opus-4-7`). Those three keys are intentionally absent from `override_key_is_managed`, so they pass through to settings.json's `env`. `activate_claude` no longer special-cases them; cross-provider switching strips them via the override-key union, and `deactivate_claude` removes them explicitly. The editor seeds a fresh Claude provider's override list with the three keys as an empty template (`CLAUDE_OVERRIDE_TEMPLATE`), and the overrides Collapsible defaults open for Claude. Blank-key **or** blank-value rows are dropped on save, so an untouched template row never persists.

**Claude 1M context:** declared by appending the `[1m]` model-id suffix directly to an override value (e.g. `claude-sonnet-4-6[1m]`) — Claude Code recommends 1M, so it's just part of the value the user types. There is no separate 1M flag/checkbox and no `with_claude_1m` helper anymore. Reverse-derivation (`read_active_claude`) matches on base_url + api_key only, so any `[1m]` suffix never affects active-state / checkmark matching.

**Test coverage:** Each CLI has an activate/reverse roundtrip test and an unrelated-fields-preserved test. The single-slot CLIs (Claude/Codex/Gemini) also have an OAuth-credentials-isolated three-stage test (Stage 1 simulates a prior CLI login, Stage 2 activates a Custom provider via Termory, Stage 3 deactivates — the credentials file must be byte-identical at the end). OpenCode never writes `auth.json`, so its isolation tests instead assert `auth.json` stays byte-identical and sibling `provider.*` blocks/options survive (`opencode_activate_preserves_unrelated_provider_blocks`, `opencode_enabling_one_provider_keeps_siblings_options`). See `providers::tests::*` in `src-tauri/src/providers.rs`.

### Codex "follow sessions" on provider switch (Codex-ONLY)

**The problem this solves — Codex-specific.** Codex tags every thread with the `model_provider` active at creation, and `codex resume` lists ONLY threads whose `model_provider` equals the CURRENT config provider (`resume_picker.rs` `picker_provider_filter` → `MatchDefault(config.model_provider_id)`). So after Termory switches Codex to a custom platform (provider id `termory`), a project's prior official-era sessions (`openai`) vanish from `codex resume`. **No other CLI has this** — Claude/Gemini/OpenCode list resume history by project/path, not by provider, so switching the API endpoint never hides sessions. The "follow" feature exists for Codex alone; the other CLIs' switch paths do nothing extra. (Verified: the installed Claude 2.1.175 binary's resume filter checks only `isSidechain`/`teamName`/`sessionKind`/`entrypoint`/`isLoopSession` — no provider/account dimension. Codex memories are separately gated on official auth — `uses_codex_backend()`, false for api-key — so custom-API sessions can't generate new memories; that's a Codex limitation the follow feature does NOT address.)

**What "follow" does (`src-tauri/src/codex_follow.rs`).** At Codex switch time the user picks which projects' sessions should follow into the now-active provider bucket so `codex resume` finds them. For each selected project's live, non-target sessions it:
1. **Rewrites the rollout JSONL's first-line `payload.model_provider`** to the target — this is the AUTHORITATIVE source. The `threads` table is only a CACHE: Codex rebuilds each row's `model_provider` from the rollout file's `session_meta` on startup backfill / resume reconcile (`state/src/extract.rs` `apply_session_meta_from_item`; upsert with no COALESCE guard at `runtime/threads.rs:754`). **Editing the table alone is silently reverted the moment Codex runs** — this was the original bug. The rewrite streams the (100+ MB) remainder unchanged, atomic temp+rename.
2. **Updates `threads.model_provider`** for the same rows (immediate visibility before the next backfill re-derives the same value).
3. **Restores the original file mtime** after the rewrite. The resume picker shows each session's time from the rollout file's MTIME (not `threads.updated_at`), so a naive rewrite makes every session read "just now" and destroys chronological order. Durability does NOT depend on the mtime (table is updated directly; reconcile re-reads file content).

**No journal / no per-session bookkeeping (deliberate).** The official bucket is ALWAYS `openai` (official subscription AND official api-key both default to it; `login_with_api_key` never sets `model_provider`) and Termory's custom bucket is ALWAYS `termory`. So the fold target is fully determined by switch direction — switching to Official → `openai`, to a custom provider/gateway → `termory`. Reversal is the symmetric switch-back (fold to the other fixed target), exactly like Claude project migrate, which also keeps no journal. An earlier versioned change-journal (`codex-changes.json`) was built then **removed** as over-engineering once the two-fixed-target invariant was confirmed — do not reintroduce it without a new requirement. (cc-switch's legacy `custom` bucket folds one-way into `openai`/`termory` along with everything else; it's just another non-target id.)

**Safety / rollback.** Opens RW with a 1.5s `busy_timeout` and surfaces a clean "database is locked — quit Codex" error (no write) when Codex holds the lock; per-file rewrites are atomic (temp + rename, original mtime restored) and only successfully-rewritten files get their table row updated, so a partial failure self-heals (Codex's backfill re-syncs the table from the files). No DB backup is taken — the `threads` table is a self-healing cache (snapshotting it protects nothing), and rollout files aren't individually backed up either (single-field atomic change); rollback is the symmetric switch-back.

**UI flow (`ProvidersPage.tsx` + `CodexFollowDialog.tsx`).** Only an official↔custom transition changes the bucket (custom→custom stays `termory`), so the prompt fires ONLY then (`effectiveActiveId === null` ⇒ official→custom for `setAsDefault`/`activateGateway`; `!== null` ⇒ custom→official for `setOfficialAsDefault`). **The Gateways tab also triggers it** (BOTH directions): `GatewaysPage.activateBinding` / `deactivateBinding` write to the CLI directly and would otherwise skip the prompt, so for a Codex binding on a bucket-changing switch they route through `ProvidersPage.codexFollowForBinding(direction, label, activate)` (a thin wrapper over `maybePromptThenActivate`, passed down as a prop) — `"toCustom"` on activate when Codex is Official (`activeStates.codex.kind === "official"`), `"toOfficial"` on deactivate when Codex is custom/unmanaged (turning the binding off folds Codex back to `openai`). The dialog is rendered once at the ProvidersPage root regardless of which tab is open. `maybePromptThenActivate` fetches `recent_codex_projects(0)` (limit 0 = no cap, newest-first, dialog scrolls), filters to projects with a session whose provider ≠ target; if none, activates directly with no prompt. On confirm the order is **activate first → toast → then migrate → toast** (activation returns a bool; migration only runs if it landed). The dialog uses the stock shadcn `Dialog`; project rows are raw `<button>` toggles with a `Check` icon (no shadcn Checkbox exists; raw rows are the project convention) and default UNCHECKED. Two actions: **"Activate only"** (`followSkip`, always enabled — switch the provider, migrate nothing) and **"Activate & keep"** (`followApply`, **DISABLED until ≥1 project is checked** — with nothing checked it would be identical to "Activate only", so the keep action requires a selection). IPC: `recent_codex_projects(limit)` + `follow_codex_sessions(projects, target_provider_id)` (lib.rs). Tests: `codex_follow::tests::*` (backend) + `GatewaysPage.test.tsx` (both follow directions fire from the Gateways tab; custom→custom does not; + delete-cleanup keep-on-failure) + `CodexFollowDialog.test.tsx` (keep gated on a selection).

**Memories do NOT need this (no feature) — verified in source.** Codex memories live in `~/.codex/memories_1.sqlite` (`stage1_outputs`, keyed by `thread_id` with an FK to `threads`; migration `0006_memories.sql`) — there is NO `model_provider`/account column, so they are NOT provider-bucketed and switching provider never hides them. The follow feature touches only `threads.model_provider`, never memories. Injection is LOCAL and UN-gated: `ext/memories/src/extension.rs:32` enables on `config.memories.use_memories` (default true) + `Feature::MemoryTool` with NO `uses_codex_backend()` check, and `local.rs` reads sqlite + `memory_summary.md` with no network — so under a custom-API (api-key) login EXISTING memories are still injected. Only GENERATION/consolidation is auth-gated (`memories/write/src/guard.rs:17` `uses_codex_backend()` is false for api-key, so phase1/phase2 are skipped), meaning no NEW memories grow under a custom provider — a Codex backend limitation, not a "hidden by provider" problem, so no Termory feature applies.

### Gateways — a SECOND, independent kind of provider management

Additive feature; does NOT touch the per-CLI provider management above. A **gateway** is ONE `{baseUrl, apiKey}` that may speak several API *modes*; you add it once, Termory auto-detects which modes respond, and you **bind** it to the CLIs whose required mode matches. One gateway → many CLIs (one key, edit once). Design recorded in the `gateway-station-provider-design` memory.

- **Storage (`PROVIDERS_SCHEMA_VERSION = 1`):** gateways live in the **UNIFIED `providers` array** of `providers.json`, discriminated by **`kind: "gateway"`** (alongside per-CLI providers whose `kind` is `"official"`/`"custom"`) — "a gateway is a kind of provider." A gateway carries `{ kind:"gateway", id, name, baseUrl?, apiKey?, capabilities?, bindings[], favicon? }`. `config.rs` keeps ONE `providers[]`: `read_providers()` returns entries `kind != "gateway"`, `read_gateways()` returns `kind == "gateway"`, and each writer preserves the other kind (`entry_is_gateway` filter). No migration from the old v2 separate `gateways[]` array — old data is dropped (per project rule). Frontend IPC names are unchanged (`read_app_gateways`/`write_app_gateways`); `write_gateways` injects `kind:"gateway"`, and `isGatewayList` requires it.
- **API mode ↔ CLI mapping** (the binding gate): Claude → Anthropic Messages; Codex → OpenAI **Responses** (`wire_api="responses"`, needs the `/v1/responses` probe specifically); Gemini → Gemini API; OpenCode → any mode (npm SDK, one binding per supported protocol); Claude Desktop → Anthropic Messages (when `caps.anthropic`). `appProtocols(caps)` in `provider-utils.ts` computes the capability-bindable set; on top of that the **GatewayEditor install-gates every app** (`if (!installed[app]) protocols[app] = []`, `installed` threaded from ProvidersPage) so a binding is only offered for a tool you can actually run — mirrors the Providers tab.
- **Detection:** `detect_gateway_apis` (backend) probes the four modes concurrently; `GatewayCapabilities` carries per-mode `ok` + a model list (for the binding's model picker). Codex (protocol `"openai"`) reuses the `/v1/models` list since the Responses probe has none.
- **A binding is a provider minus the gateway's common fields, WITH its own id:** `{ id, app, model?, npm?, models?, options? }`. There is **no `protocol` field** — it's DERIVED via `protocolForBinding` (Claude→anthropic, Codex→openai, Gemini→gemini, OpenCode→`protocolForNpm(npm)`). The own id (vs the old `gateway:<id>:<app>`) lets one CLI hold several bindings and is the activation-marker key. `options` = advanced settings (Claude seeds the `ANTHROPIC_DEFAULT_{SONNET,OPUS,HAIKU}_MODEL` routing template); `npm` + `models` are OpenCode-only. `GatewayEditor` keeps an internal-only `BindDraft.protocol` for the model picker; `isGatewayList` rejects bindings without `id` so pre-refactor data drops (no migration).
- **Detection probes each capability at its OWN real API endpoint** (POST, empty-body route-exists `code≠404/405`) — a GET `/models` list NEVER gates a capability (only proves a list route exists → false positives). Naming mirrors OpenCode's AI SDK packages: `openaiCompatible`→`POST /v1/chat/completions` (`@ai-sdk/openai-compatible`), `openai`→`POST /v1/responses` (`@ai-sdk/openai`, **Codex**), `anthropic`→`POST /v1/messages`. **Gemini is the exception**: gated on `GET /v1beta/models?key=` returning data — that path is Gemini-SPECIFIC (a non-Gemini gateway 404s it, so data ⇒ support), unlike OpenAI's generic `/v1/models` which every compatible gateway answers. The same Gemini GET also feeds the `models` catalog (`fetch_models_list` returns `(got_data, models)`). `GatewayCapabilities` = **4 booleans + flat `models` catalog** (DATA-ONLY autocomplete, union of `GET /v1/models` + `GET /v1beta/models`; gateway routes by model id, no per-mode split). `GatewayProtocol` = `"openai-compatible" | "openai" | "anthropic" | "gemini"` (`protocolForNpm` matches `openai-compatible` before `openai`). `appProtocols`: Codex gates on `caps.openai` (Responses). `modelsForProtocol` + `ApiProbe` removed.
- **`capabilities` IS persisted** (now small — 4 booleans + one flat `models` catalog): `handleSave` writes `capabilities: caps`. Reopening a gateway shows the saved bindable sources + autocomplete immediately (rendered from the loaded `caps`); `lastTried` starts empty so opening the editor also **auto-redetects once** (when base URL + key are present) — mirrors ProviderEditor's model auto-fetch, so editing always refreshes the `models` catalog and shows a current "N models available", and `handleSave` persists the refreshed `caps`. Manual refresh / a base/key change re-probe too. A new gateway with no detection persists nothing (caps undefined → stripped).
- **Gateway base URL is stored PATH-LESS** (bare root, no API-version path): the editor strips a pasted `/v1` or `/v1beta` on save, and EACH CLI's real URL is DERIVED per protocol — `gatewayBaseForProtocol` reduces to the bare root then adds OpenAI `/v1`, Anthropic bare (Claude appends `/v1`), Gemini bare (it appends `/v1beta`). Detection mirrors this (`detect_gateway_apis` builds every probe URL from the stripped `root`; `join_v1` was removed). So a base entered as `…/v1` no longer breaks the Gemini path.
- **Activation reuses the existing path:** `providerFromBinding(gateway, binding)` (provider-utils) synthesizes the normal `Provider` shape — id = `binding.id`, base URL derived per the DERIVED protocol (`gatewayBaseForProtocol`), OpenCode `npm` + `models`, the binding's `options`, and `favicon = gateway.favicon`. That synth flows through `activate_provider` / `deactivate_provider` / reverse-derivation UNCHANGED. Editing a gateway re-activates its active bindings (mirrors `ProvidersPage.saveProvider`); deleting a gateway (or unbinding) `deactivate_*`s only the bindings that were actually active (marker + creds). A delete that can't clear a binding's live config (CLI running → DB locked) surfaces `toast.clearFailed` and **KEEPS the gateway** so it isn't silently lost while a CLI still points at its endpoint — mirrors `ProvidersPage.deleteProvider`; the retry is idempotent (already-cleaned bindings skip). When a standalone provider and a binding share identical creds (indistinguishable on disk), the per-CLI marker `active_provider_ids` (config.json) — honored only while its creds match the live snapshot — disambiguates which is "in use" via `resolveActiveProviderId`.
- **UI:** a separate **"Gateways" tab** in `ProvidersPage` (`view` state — `"providers" | "gateways"`, the tab value `"gateways"` is not a `CliApp`). `GatewaysPage` lists gateways (add/detect/bind via `GatewayEditor`, activate/deactivate per binding, delete). The per-CLI Providers list ALSO surfaces gateway bindings targeting that CLI as `ProviderCard`s with a `gatewayBadge` — **view + activate/test only, NO Edit/Delete** (those props are omitted, so `ProviderCard` hides the buttons; bindings are managed exclusively from the Gateways tab — by design). The synth provider carries `favicon = gateway.favicon` so the card shows the gateway's icon. `ProvidersPage.refreshActive` passes `[...providers, ...gatewaySynth]` so a gateway-activated CLI matches the synth id instead of reading "Unmanaged".
- **Tests:** `config::tests` (gateways roundtrip + providers↔gateways no-clobber), `providers::tests` (`parse_model_ids`), `provider-utils.test.ts` (`gatewayBaseForProtocol` incl. path-less/sub-path cases, `appProtocols`, `protocolForBinding`, `providerFromBinding`, `isGatewayList`). The HTTP probes themselves are network-bound (not unit-tested).
- **Tray** surfaces gateway bindings: each CLI submenu lists its bindings alongside standalone providers (the tray synthesizes them via `providers::gateway_providers` (read once, grouped per CLI) → `provider_from_binding`, the Rust mirror of the frontend `providerFromBinding`, and activates through the same `activate` / `read_active_state` path; only **installed** CLIs (`detect_install_snapshot`) get a submenu). **Known gap (not yet done):** `detect_gateway_apis` is best-effort (a permissive gateway can answer both OpenAI and Anthropic on `/v1/models`).

## Menu-bar tray (macOS system tray)

A native system-tray icon (the macOS menu bar) for quick provider switching, separate from the in-app Providers rail route. **Click model per OS:** on macOS (menu-bar item) left click opens the menu; on Windows/Linux the menu is on RIGHT click (`show_menu_on_left_click(false)`) and LEFT click (release) opens the app window — the platform convention. (Linux appindicator trays deliver no click events, so left-click-opens-window is effectively the Windows path.) Implemented in `src-tauri/src/tray.rs` and installed from `lib.rs` `setup()`. Requires the `tauri` features `tray-icon` + `image-png` (Cargo.toml).

**Menu shape** — "Open", then up to 5 recent session titles, then one submenu per CLI (the active choice shown inline on the first-level row; submenu lists Official + the user's providers + this CLI's gateway bindings, with the active one checkmarked):

```
Open
─────────────────────
Fix the flaky stats test          ← up to 5 most-recent session titles
Refactor the gateway editor          (newest first; click → terminal + resume)
…
─────────────────────
New Session             ▸  termory  ▸  Claude Code  ← up to 5 recent project dirs,
                           chats    ▸  Codex           each a nested submenu of every
                           …           Gemini          INSTALLED CLI; pick one → terminal
                           ─────────                   opens in the dir + CLI launches fresh
                           Choose Folder… ▸ (CLIs)  ← native dir picker → CLI fresh there
…
─────────────────────
Claude Code · Official · 🟢 12% 5h · 🟡 78% Weekly  ▸  ☑ Official   ← quota inline on the FIRST-LEVEL row,
                           ─────────
                           ☐ Anthropic
                           ☐ OpenRouter
Codex · OpenRouter      ▸  …
Gemini · …              ▸  …
OpenCode · Official     ▸  …
─────────────────────
Exit
```

- **Recent sessions + New session** — up to 5 most-recent session titles sit flat under "Open" (newest first by `updated_at`; Memory/Skill, slash-command and empty-id records excluded; label = title → snippet → "(untitled)", truncated) — a single click (`tray:session:{source}:{id}` — content-addressed, looked up by identity so a watcher rebuild while the menu is open can't resume the WRONG session; missing → silent no-op) **launches a fresh OS terminal**, `cd`s into the session's cwd (when that dir exists) and resumes via **`terminal::resume_session(source, id, project)`** — the SAME entry point the Records / Favorites right-click "Resume in terminal" uses. Below the sessions one localized **"New Session" SUBMENU** lists the first 5 distinct project cwds in the same recency order (dedup ACROSS CLIs; empty-cwd records skipped) as NESTED submenus (cwd basename), each holding one row per INSTALLED CLI, ordered by which CLI was last used in THAT project (cli_recency — most likely choice on top), then the rest (flat header+rows grew too long) — picking a row (`tray:new:{cliKey}:{full project path}` — the path rides in the id, fully decoupled from RECENT; the `tray:newpick:` dispatch check must stay FIRST since it shares the prefix) calls `terminal::new_session(cli_source(cli), project)`: the chosen terminal opens in that cwd and that CLI launches fresh (any installed CLI, not just the ones with history there). A **"Choose Folder…"** tail submenu (`tray:newpick:{cliKey}`, localized `tray.chooseFolder`) opens the native directory picker (tauri-plugin-dialog `pick_folder`, async callback) and launches the picked CLI fresh in the chosen dir — works for brand-new directories and keeps New Session present with zero history (whole submenu skipped only when no CLI is installed). Both halves are computed by `select_recent_state` into `static RECENT: Mutex<RecentState>`, refreshed by `tray::refresh_recent(app, &records)` from the **existing** scans (the `scan_all_sessions` IPC + the watcher) — the tray never scans on its own, and the update is skipped when nothing changed. The refresh splices the dynamic region IN PLACE (`update_recent_region`: `Menu::remove_at`/`insert_items` over the cached root handle + region length in `RECENT_REGION`; region = items from `build_recent_region`, shared with build_menu, starting at the fixed `REGION_START` = 2 right after "Open"+separator) — a full `set_menu` rebuild CLOSES an open menu on macOS, while in-place splicing updates the open menu live (user requirement: refreshes apply immediately AND never dismiss the menu). Any splice failure falls back to a full rebuild. ONE deliberate exception to "never dismiss": an installed-set change (CLI installed/uninstalled since the menu was built) forces a full rebuild even from the menu-open path — the splice can't refresh the per-CLI submenus, and a one-time dismissal beats a permanently stale CLI list (see "Install/uninstall auto-refresh" below). ALL menu mutations (rebuilds, the splice, quota title set_text) are QUEUED on the main thread via `run_on_main_thread` — the queue serializes concurrent refreshers (watcher thread + the scan_all_sessions IPC) that would otherwise interleave remove/insert ops or write a stale menu handle back; a mutex was rejected (worker-holds-lock-waits-main vs main-waits-lock deadlock). (An earlier per-project-submenu design — project rows each holding new+sessions — was tried and rejected: it added a hover step to the most-frequent action and produced duplicate same-named rows.) `resume_session` builds the command via `session_launch_command` (mirrors the frontend `resumeCommandFor` — all four resume by id: `claude --resume <id>` / `codex resume <id>` / `opencode --session <id>` / `gemini --resume <id>`; the session id is charset-guarded `[A-Za-z0-9._-]` before interpolation — injection defense), reads the user's **Settings → Terminal** choice (`terminal` config key; empty / `"auto"` → the OS default), and calls `terminal::open(choice, project, cmd)`. `terminal::detect()` (the `detect_terminals` IPC) lists the mainstream terminals found installed per-OS (macOS: Terminal/iTerm/Ghostty/Alacritty/Kitty/WezTerm; Linux: GNOME/Konsole/XFCE/Alacritty/Kitty/WezTerm/xterm; Windows: Windows Terminal/PowerShell/cmd) — only macOS is verified, Linux/Windows best-effort.
  - **Cold-launch single-window (macOS GUI terminals).** Terminal.app and iTerm are activated via AppleScript (`terminal_app_script` / `iterm_script`), and a naive `activate` + `do script` / `create window` opens TWO windows when the app wasn't already running (the launch's default empty window plus the command's). The builders guard against this: already running → a fresh window; cold launch → reuse the window the launch creates (Terminal: `do script … in window 1`; iTerm: a delay-guarded `count of windows` check that writes into the launch window, else makes one). Asserted by `terminal::tests::{terminal_app_script,iterm_script}_reuses_launch_window_on_cold_start` — don't revert to the bare `activate`+`do script` form. The CLI-launched terminals (Ghostty via `open -na`, Alacritty/Kitty/WezTerm and ALL Linux/Windows terminals) spawn one window per invocation, so they don't have this issue.
- **Recent-session live work status (Claude-only)** — a currently-running Claude session's row shows its live status after the title: `{title} · Working` / `{title} · Needs input` (idle / not-running rows show just the title). **Plain text, no glyph** — a native macOS menu is an all-text list, so a colored dot would mean either an emoji (cheap) or a far-left icon column (breaks the otherwise icon-less menu); the word alone stays clean. Source is Claude's OWN native runtime file `~/.claude/sessions/<pid>.json` (respects `CLAUDE_CONFIG_DIR`) — the snapshot it writes for `claude ps`, NOT the transcript and NOT a hook (so Termory reads it in place). Verified against Claude source: `utils/concurrentSessions.ts:19` `SessionStatus = 'busy' | 'idle' | 'waiting'` (+ `waitingFor` detail), derived in `screens/REPL.tsx:1605`. `sessions::claude_work_statuses()` → `claude_work_statuses_in` reads the dir → `parse_claude_status_snapshot` maps `busy`→Busy, `waiting`(unless `waitingFor == "dialog open"`, the user-opened-UI case)→Waiting, and `idle`/terminal `state`/missing → no entry (prefers the richer `state` field — working/blocked — when present, else `status`+`waitingFor`). Files are filtered by Claude's exact filename guard `^\d+\.json$` (all-ASCII-digit stem; the `2026-…_notes.md`-as-pid bug, anthropics/claude-code#34210), a **PID-liveness probe** (`process_alive`: `libc::kill(pid, 0)` on unix via `[target.'cfg(unix)'.dependencies] libc`, `OpenProcess` on Windows via `[target.'cfg(windows)'.dependencies] windows-sys`, conservative `true` on exotic targets; mirrors Claude's `isProcessRunning`, so a crashed session's residual busy file isn't shown — mtime alone can't tell a long-busy live session from a crashed one), and a 24h-mtime **PID-reuse backstop** (`CLAUDE_STATUS_MAX_AGE`). Joined onto recent rows by `id == sessionId` (Claude rows only) in `tray::attach_work_statuses`; the FS read runs on `refresh_recent`'s caller thread (off the main thread), and `RecentSession.status` feeds `work_status_label` at render time in `build_recent_region`. **Refresh is event-driven, no timer:** a live status change rewrites the `<pid>.json` under the recursively-watched `~/.claude/`, so the watcher's `refresh_recent` reflects busy↔idle↔waiting promptly. A CRASH (kill -9) leaves the file untouched — no filesystem event — so the tray click (`on_tray_icon_event`) ALSO calls `tray::refresh_work_status`, which re-runs the liveness probe against the CACHED recent list (no session scan — only re-reads `~/.claude/sessions/` + re-attaches) and re-splices, clearing a crashed session's stale status on the next menu open. `refresh_recent` + `refresh_work_status` share `installed_cli_list` + `commit_recent` (store-if-changed → in-place splice → full-rebuild fallback, queued on the main thread). Status text is localized via the tray-labels push (`tray.statusBusy` / `tray.statusWaiting`). **Caveat:** the `status` field is written only when Claude's `BG_SESSIONS` feature is on (`REPL.tsx:1623`); with it off the file has no status → no work status surfaces (graceful degradation). Other CLIs have no equivalent live-status file, so this is Claude-only — and that's a hard limit, not a missing feature (cc-status is Claude-only for the same reason). **Codex (verified, don't re-investigate):** its live session state lives in the app-server **daemon, in-memory**, reachable only over an IPC socket (`app-server/src/thread_state.rs` — `Arc<CodexThread>` + listeners), NOT on disk; `~/.codex/sessions/` holds only rollout JSONL (history), the `threads` table in `state_5.sqlite` has NO status/active/pid column, and the one daemon `pid.rs` file is a single `{pid, processStartTime}` for the app-server process (not per-session). The only disk-derivable Codex signal is rollout-file mtime = "recently active", which can't distinguish busy/waiting/idle and reads identical for an idle-but-running session vs a closed one — misleading, so NOT used. Real Codex status would require speaking the daemon's IPC protocol (a live socket client, not a file read) — out of scope. **Gemini / OpenCode** likewise write no per-session run-status file. Tests: `sessions::tests::{parse_claude_status_snapshot_maps_running_states, claude_work_statuses_in_filters_by_filename_and_liveness}`, `tray::tests::{attach_work_statuses_joins_claude_rows_by_id_only, work_status_label_maps_states}`.
- **Quota inline title** — a quota-capable CLI's FIRST-LEVEL row appends the official-account quota after "Official" (`Claude Code · Official · 🟢 12% 5h · 🟡 78% Weekly`, percent right after the glyph; all windows except Claude's per-model weeklies (`TRAY_HIDDEN_TIERS`); `tray_tier_label` (card mirror: `tierLabels`) renders `30_day` → localized "Monthly", other generated `{n}_hour`/`{n}_day` → "{n}h"/"{n}d", Gemini classes → brand "Pro"/"Flash"/"Flash Lite", unknown ids raw; from `quota::fetch_quota`). Each window carries a pressure emoji (`quota_glyph`: <75% 🟢, ≥75% 🟡, ≥90% 🔴 — thresholds are `quota::{WARN_PCT,CRIT_PCT}`, mirrored by `QUOTA_WARN_PCT`/`QUOTA_CRIT_PCT` in src/lib/quota-utils.ts for the app's ring); emoji because macOS menu text can't be colored. Suppressed while a custom provider is active — the quota belongs to the official login, so gluing it onto a custom provider's name would misread as that provider's usage. Cached per quota-capable CLI (`quota::SUPPORTED` — the backend's single support list; frontend mirror `QUOTA_SUPPORTED` in ProvidersPage.tsx) in `static QUOTA: Mutex<Vec<(CliApp, TrayQuota)>>`; updated by `tray::refresh_quota` from THREE triggers (NO periodic polling): a one-shot warm-up at startup (`trigger_quota_refresh` in `setup()`), every tray-icon click (= menu open, via `on_tray_icon_event` — rate-limited per CLI to one fetch per 2 min (60s after a failure) via `QUOTA_LAST_FETCH`, which `refresh_quota` updates on EVERY completed fetch — including the Providers page's IPC — so page and tray share one rate limit; the fetch lands after the menu is showing; the changed numbers are applied IN PLACE via `Submenu::set_text` on cached `CLI_ROWS` handles (`update_cli_row_title` / `cli_row_title`) — a full `rebuild_menu`/`set_menu` here CLOSED the open menu on macOS), every COMPLETED `fetch_subscription_quota` IPC (success or failure — a Providers-page refresh updates the tray, the shared marker, and the not_found clearing with no extra network hit), and the filesystem watcher: a change to a CLI's credential file (`.credentials.json` → Claude, `.codex/auth.json` → Codex, `.gemini/oauth_creds.json` → Gemini; the path→CLI map is `quota::credential_cli_for_path`, single-sourced next to the credential readers — watcher.rs just delegates) calls `tray::force_quota_refresh` — bypasses the normal floors (10s burst-dedup only) so login/logout reflects near-instantly (Keychain-backed creds emit no file event; the 60s not_found retry is the fallback). Every completed fetch is ALSO pushed to the frontend via the `termory:quota-changed` event (payload = `SubscriptionQuota`; const `quota::QUOTA_CHANGED_EVENT`, mirrored in src/constants.ts) so an open Providers page reflects backend-initiated fetches without its own request. A `not_found` result (logged out — definitive, unlike transient failures) CLEARS that CLI's tray quota entry instead of keeping stale numbers; likewise a SUCCESSFUL fetch with no usable windows — logged in but with no active subscription, where `/api/oauth/usage` returns windows whose `utilization` is null/absent (verified against the official CLI: `services/api/usage.ts` types `utilization` as `number | null` and `Settings/Usage.tsx:218` shows "/usage is only available for subscription plans" precisely when no window is present) — clears the entry, so a cancelled-subscription account doesn't keep showing stale percentages. Other failures keep the last good numbers; the title update is skipped when unchanged. Quota changes never trigger a full rebuild while running (set_text only; full rebuilds still happen on provider mutations / recent-list changes / label pushes / installed-set changes).
- **Active state is reverse-derived** via `providers::read_active_state` (same as the Providers page) — Termory still stores no "active provider" pointer. Checkmark + inline `· name` use the matched provider name, else "Official".
- **CLI display names** match the Providers page tabs (`CLI_APP_LABEL` in `src/constants.ts`) — keep `cli_label` in `tray.rs` in sync (it's "Gemini", not "Gemini CLI").
- **Static row labels are localized** (Open / Official / Exit). The tray is Rust (built in `setup()` before the frontend loads, so it can't read the i18n dicts), so the FRONTEND pushes the translated strings: an `App.tsx` effect (deps `[t, localeReady]`) calls the **`set_tray_labels(open, official, exit, fiveHour, weekly, monthly, newSession, chooseFolder, statusBusy, statusWaiting)`** IPC on language load/change → `tray::set_labels` stores them in a `static TRAY_LABELS: Mutex<Option<TrayLabels>>` (English default) → `rebuild_menu`. CLI / provider names stay untranslated (brand / user data). Keeps i18n single-sourced in the frontend dicts (no Rust translation table to drift). `tray.open` / `tray.official` / `tray.exit` / `tray.newSession` / `tray.chooseFolder` / `tray.statusBusy` / `tray.statusWaiting` keys (+ the quota row reuses `providers.quotaFiveHourShort` / `providers.quotaSevenDayShort` / `providers.quotaMonthlyShort` (30_day → "Monthly")). **The push is gated on `useI18n().ready`** — `<I18nProvider>` starts `ready=false`, flips it `true` (via `.finally`) once the saved `language` config has loaded, so the tray gets ONE push with the FINAL locale instead of first flashing the `navigator`-guessed one (the provider seeds from `navigator.language`, then the async config override wins; without the gate the tray briefly showed the system-guessed locale). `ready` defaults `true` in the no-provider context so isolated component tests are unaffected.
- **Click handler** reuses the same write helpers as the IPC commands (`activate` / `deactivate`), so the on-disk path is single-sourced. For **OpenCode** a click does `activate` **+** `set_opencode_default` (the slot must exist before it can be the default — `set_opencode_default` errors otherwise), so one click both enables the slot and sets it as default, matching the Providers page end state. Single-slot CLIs (Claude / Codex / Gemini) need only `activate`.
- **Menu is rebuilt** (`tray::rebuild_menu`) after every tray click and after the five provider-mutating IPC commands (`activate_provider` / `deactivate_provider` / `delete_provider` / `set_opencode_default_provider` / `write_app_providers`) so the checkmarks / inline titles stay in sync.
- After a tray switch the handler emits **`termory:providers-changed`**; `ProvidersPage` listens for it and re-derives active state, so an open Providers page reflects a tray switch even when unfocused.
- **Open / Exit** are plain `MenuItem`s (not `PredefinedMenuItem::quit`) so macOS attaches no native item icon. Exit calls `app.exit(0)`.
- **Icon**: embedded `icons/tray-icon.png` (36×36 = 18pt @2x, the macOS standard), a pure-black template image of the three-card terminal "chip" from the app icon, optically centered. On **macOS** it's marked `icon_as_template(true)` so the bar themes it for light / dark; on **Windows / Linux** the tray instead uses the COLORED app icon (`default_window_icon`) — a monochrome template reads as a black blob in those system trays, which don't do menu-bar theming. `icons/tray-template.svg` is the vector source; the menu-bar size is governed by the whole image (tray-icon crate scales it to a fixed 18pt height), so the artwork's transparent margin controls how large it reads.

Only **installed** CLIs get a submenu — callers probe `detect_install_snapshot()` (the same probe as the Providers page's `detect_clis`; the snapshot = installed map + `codex_terminal`, see the `detect_codex_installs` bullet) and `build_menu` skips any CLI not found, so the tray lists only what you can actually switch. **Install/uninstall auto-refresh:** `static INSTALLED` caches the install snapshot the VISIBLE menu reflects — stored only AFTER a successful `set_menu` (`do_rebuild_menu_with`) / tray `install()`, never inside `build_menu` (an early store would let a failed rebuild claim the new set and permanently kill the staleness check; on failure the cache keeps the old set so the next refresh retries). `rebuild_if_installed_stale` (main-thread, the single owner of the compare→rebuild step) is fed by three probe paths, each probing on ITS OWN thread and passing the snapshot along so nothing re-probes on main: the watcher's install-detection branch (the same one that emits `cli-install-changed`) calls `tray::refresh_installed`, which RETURNS the probed snapshot so the same burst's rescan reuses it via `refresh_recent_with` (one PATH probe per burst, not two); the `detect_clis` IPC (Providers-page Recheck) hands its result to `tray::refresh_installed_with`; and the shared `commit_recent` path (fed by `refresh_recent` + the tray-click `refresh_work_status`) does the same compare, so an install missed by the watcher self-heals on the next menu open / scan. A stale snapshot (including a `codex_terminal` flip — the standalone CLI installed/removed while the desktop app stays) forces a FULL rebuild (not the recent-region splice — the splice can't refresh the per-CLI submenus; this is the documented exception to "never dismiss the menu" above). **Claude Desktop is covered too:** its install marker is the config dir itself (`claude_desktop::is_installed`), which can't be watched before it exists — so the watcher watches the PARENT dir(s) (`claude_desktop::install_watch_parents()`, derived from the same `config_parent_dir()` helper `current_paths` uses: `~/Library/Application Support`; on Windows the candidate parents incl. the MSIX `Packages\` dir, non-recursive) and `event_touches_claude_desktop` (watcher.rs) filters events to direct children matching `claude_desktop::is_install_marker_name` (the `Claude*` rule, owned next to the Windows dir resolution so it can't drift) before routing them into the same install-changed branch. Those parent dirs are busy shared dirs, so their direct-child events are ALSO excluded from the session-rescan gate (`event_has_relevant_path` takes the parents as an ignore list) — otherwise every other app's activity there would trigger a full `scan_sessions`. **Claude Desktop** is included in the per-CLI provider-switch loop (its own `installed` gate via `claude_desktop::is_installed`, `cli_label`/`cli_key` arms) so it gets a switch submenu when installed, but it is **excluded from the terminal flows** (recent-session resume + "New Session"): those filter on `CliApp::is_cli()`, which is false for the GUI app. It has no quota row (`quota::SUPPORTED` excludes it).

**Known minor gaps (by design for a quick-switcher):** the tray doesn't expose OpenCode's multi-slot enable/disable or delete; failures are `log::error!`-only (no toast). The **provider** checkmarks/inline titles refresh only on Termory-initiated changes, so an external switch (cc-switch / manual edit) can leave them stale until the next Termory action. The **recent-projects** list does refresh on the filesystem watcher (and every `scan_all_sessions`), so it stays current with on-disk session changes.

### Window / Dock behavior (menu-bar app)

**Single instance:** `tauri-plugin-single-instance` is registered FIRST in the plugin chain (per its docs) — a second launch (double-clicked exe on Windows; macOS bundles are natively single-instance) surfaces the running instance's window via `tray::show_main_window` and exits.

Closing the window does **not** quit — `lib.rs` `on_window_event` intercepts `CloseRequested`, calls `tray::hide_main_window` (window.hide + macOS `set_dock_visibility(false)`) + `api.prevent_close()`, so the app keeps running in the menu bar with no Dock icon. The tray's **Open** restores the Dock icon (`set_dock_visibility(true)`) then shows / unminimizes / focuses the window. The window is declared `visible: false` in tauri.conf.json; a NORMAL launch shows it in `setup()` (`tray::show_main_window`), while a login-item launch (`--autostart`) stays tray-only (the Dock icon is suppressed PRE-RUN via `app.set_activation_policy(Accessory)` before `app.run()` — a setup()-time `set_dock_visibility(false)` was too late and flashed the icon) — declaring it hidden is what stops the window flashing before setup() runs. `hide_main_window` / `show_main_window` (tray.rs) are the shared hide/show pair used by close-to-tray, --autostart, and the tray's Open. `set_dock_visibility` is the purpose-built API — a raw `set_activation_policy(Accessory)`/`Regular` toggle did NOT reliably restore the Dock icon when re-showing the window.

The window's title-bar **text is hidden** (`"hiddenTitle": true` on the window in `tauri.conf.json`) — the traffic-light buttons and a normal draggable title bar stay; only the "Termory" label is suppressed. `title` is still `"Termory"` so Mission Control / the window menu identify it.

## Stats

One continuous page over a single shared filtered window (reference design: ccusage-style cards). Header: a `bg-muted` card holding the source-filter pills (All / Claude / Codex / Gemini / OpenCode), the **All / 30d / 7d** range pills, and refresh — **no view tabs** (the old Overview / Trends / Models tabs were collapsed into one page). No custom calendar range — `react-day-picker` and `src/components/ui/calendar.tsx` were REMOVED with the old StatsFilterBar. Two stacked blocks:

1. **Overview** (`OverviewSection.tsx`) — 8 KPI cards (Sessions / Messages / Total tokens / Active days / Current streak / Longest streak / Peak hour / Favorite model) above a GitHub-style all-history calendar heatmap (week columns Sun→Sat; follows the source filter but NOT the All/30d/7d range). Cells use `intensity()` + `tierClass()` from `heatmap-shared.ts` (the weighted-geometric-mean rule + tier ramp). (The old two-panel layout with a per-day 24-hour heatmap was removed — only the calendar remains, so `heatmap-shared.ts` now has a single consumer.)
2. **Tokens** (`TokensChart.tsx`) — ONE stacked-bar chart with a **Type / Model** toggle (top-left, next to the "Tokens" title; state `TokensGroupBy = "type" | "model"`, controlled by StatsPage). Both breakdowns sum to the SAME per-day total (the locked cross-consistency rule), so the Y-axis domain (`niceMax(max daily.total)`) never jumps on toggle:
   - **Type** — per-day tokens split by kind (Input / Output / Cached / Reasoning), stacked with Cached at the base (it dwarfs the rest by 1-3 orders of magnitude from prompt-cache hits). Same `dailyTokens` data as the KPI token total. Colors: four DISTINCT hues (input/output/cached/reasoning are NOMINAL kinds, so NOT an ordinal one-blue ramp), mode-aware CSS vars `--stat-tok-{input,output,cached,reasoning}` (globals.css), validated as a set via the dataviz skill.
   - **Model** — per-day tokens split by model (top-6 by window tokens stacked, the rest + Unknown fold into "Others") + a legend (color dot · raw model id · "{in} in · {out} out" · share%). Colors are PROVIDER-FAMILY ramps: every model of a provider is a shade of that provider's hue. **Nine vendors are recognized** (`inferProvider` in TokensChart.tsx, matched after stripping a gateway `vendor/` prefix): claude=clay, gemini=blue, openai=teal (`gpt`/`o1`/`o3`/`o4`/`chatgpt`), mistral=gold (`mistral`/`mixtral`/`codestral`/`magistral`/`ministral`/`pixtral`/`devstral`), deepseek=violet, qwen=magenta (`qwen`/`qwq`/`qvq`), grok=crimson, glm=indigo (`glm`/`chatglm`/`codegeex`), minimax=rose (`minimax`/`abab`). Each ramp is `--stat-<provider>-0..4` in globals.css (ONE set of values for both themes); step 0 = the BOLD brand anchor for the most-used model, then LIGHTER by within-provider usage rank — so a Claude model never looks "custom". The nine anchors are a validated categorical set (dataviz `--pairs all`: worst CVD ΔE **9.4** — the 8–12 floor band, legal because every segment carries a legend label; worst NORMAL-vision ΔE 15.9, so none collapse for the sighted — on BOTH surfaces). The three fixed brand anchors (claude/gemini/openai) crowd the hue space, so the six added ones are brand-ADJACENT distinct hues (gold≈Mistral, violet≈DeepSeek, magenta≈Qwen, crimson=Grok, indigo≈GLM-blue, rose≈MiniMax-coral) rather than exact logos — with nine families past the CVD-safe ceiling, distinctness is the hard requirement and brand a lean (user call). Unknown-provider ids (an exotic custom / gateway model — rare now that the nine mainstream vendors are covered) fall to a two-hue neutral pool (`EXTRA_MODEL_COLORS` = cyan + slate, the only hues left ≥ ΔE 8 from all nine anchors), then the Others grey. Color tracks each model's ALL-TIME rank (StatsPage's `globalModelRank`), so a source/range filter change never repaints the survivors.

### Accuracy rules (LOCKED — do not weaken)

All Stats values are **window-accurate** — they reflect what actually happened in the chosen date range, NOT lifetime totals.

| Metric | Source | Notes |
|---|---|---|
| Sessions | `started_at ∈ window` count | Old session reused today does NOT count |
| Messages | `Σ daily_tokens[date ∈ window].messages` | Per-message timestamps from backend |
| Tokens | `Σ daily_tokens[date ∈ window].tokens` | Same |
| Active days | window days with `messages>0 ∨ tokens>0` | From `dailyTokens` buckets |
| Streaks | consecutive active days (`overviewKpis`) | current = run ending at the window's last day, with a one-day grace (not-yet-active today counts back from yesterday); longest = max run |
| Peak hour | argmax of in-window `daily_tokens.hours` sums | null (— shown) with no hourly data |
| Favorite model | `overviewKpis().favoriteModel` — top model by in-window tokens, skipping "Unknown" | session-level attribution |

**No fallback smearing**: a session with `s.tokens` but no `s.daily_tokens` contributes ZERO to Messages/Tokens — Termory does not even-distribute lifetime totals across days (that would fabricate per-day numbers indistinguishable from real ones).

**filter uses interval overlap**: `filterSessions` keeps a session if `[started_at, updated_at] ∩ window ≠ ∅` — filtering on `updated_at ∈ window` alone would drop sessions whose window contribution predates their last update.

**`all` range**: `resolveRange(range, sessions)` — starts at the earliest `daily_tokens` date (fallback `started_at`); with no datable activity it degrades to the 30d window. `to` is always end-of-today so sessions still being written stay in the filter.

**Cross-source consistency** (backend-guaranteed, test-pinned): `Σ dailyModelTokens.series[*][d] === dailyTokens[d].total` for every date — the stacked chart's per-day totals reconcile exactly with the daily rollup (the "Unknown" bucket rides inside "Others" in the chart precisely so this holds while the legend keeps hiding Unknown).

### Model attribution is session-level (approximate)

`AppSession.model` is one best-guess id per session, so `dailyModelTokens` (stacked series + the legend's input/output split) and `overviewKpis().favoriteModel` attribute a session's whole token contribution to its single recorded model — a session that switched models mid-run lands entirely under its main model. Sessions with no recorded model bucket under `"Unknown"`, hidden in the legend/KPIs but folded into the chart's Others bucket (see cross-consistency above). True per-message model splits would need a backend `daily_tokens` model dimension — deliberately not built (approximation accepted by design decision, 2026-07).

### Calendar heatmap intensity (LOCKED rationale carried over)

Day cells use the SAME weighted-geometric-mean rule as the retired 24h heatmap, at day granularity: `ratio = m^0.6 · t^0.4` (m/t = messages/tokens normalized to the window max), with single-dimension degradation (no tokens anywhere → messages-only; no messages → tokens-only) and the same low-skewed tier thresholds (0.08/0.18/0.35/0.55/0.75 → `bg-primary/25…100`; truly inert → `bg-foreground/[0.08]`) — the calendar bumps each tier ~10% darker than the retired 24h grid's `/15…90`. The rationale (messages alone under-light "one big request" hours, tokens alone under-light "many short messages") is unchanged — see git history of `DailyActivitiesHeatmap.tsx` if it needs re-deriving. The heatmap ALWAYS spans the full history (earliest activity day → today, following the source filter but NOT the All/30d/7d range — that scopes only the KPIs + Tokens chart), like a GitHub contribution graph. Weeks render left→right oldest→newest with a GitHub-style top month axis (label on each month's first column) and a left weekday axis (Mon/Wed/Fri, fixed while the grid scrolls); leading/trailing padding cells share the empty-day grey so the grid is one full rectangle, and it scrolls horizontally auto-scrolled to the newest week.

### Frontend aggregation

**One statistics function per UI module (LOCKED alignment).** `src/lib/stats-utils.ts` exposes exactly **one aggregator per Stats module**, plus the shared window/filter setup — the 4 modules map 1:1 to 4 functions:

| Module | Function | Returns |
|---|---|---|
| 1. KPI cards | `overviewKpis(filtered, resolved)` | ALL 8 KPI values in one pass — sessions / messages / tokens + activeDays / current & longest streak + peakHour + favoriteModel (top model by in-window tokens, skipping "Unknown") |
| 2. Activity heatmap | `dailyActivity(sourceFiltered)` | full-history per-day `{date, messages, tokens}` — **no range condition**; builds its own fixed 365-day window internally (follows the source filter, NOT All/30d/7d) |
| 3. Tokens · type | `dailyTokens(filtered, resolved)` | range-scoped per-day `{input, output, cached, reasoning, total, messages}` |
| 4. Tokens · model | `dailyModelTokens(filtered, resolved)` | range-scoped stacked series (`dates` / `models` / `series`) **plus** `legend[]` (per-model in/out/total — folded in from the old `modelBreakdown`) |

Shared setup used by all: `resolveRange` + `filterSessions` (+ internal `localDateKey`). `globalModelRank` (stable model colors) reuses `dailyModelTokens(sourceFiltered, allTime).models`. Each aggregator iterates its sessions once; StatsPage memoizes per `(filtered, resolved)`.

**Rendering / format helpers are NOT statistics functions** — they live with the component that uses them, not in stats-utils: `calendarWeeks` (heatmap grid reshape) in `OverviewSection.tsx`, `niceMax` (Y-axis bound) in `TokensChart.tsx`. Model ids render **as-is** — there is no name-prettifying helper (a former `displayModelName` was removed; `claude-opus-4-8` shows verbatim in the legend / Favorite-model KPI).

`stats-utils.test.ts` covers window accuracy, streak edges, the full-history heatmap window, the stacked-vs-daily cross-consistency, and the model legend + favoriteModel; `StatsPage.test.tsx` covers the Type/Model toggle, range, source-filter, refresh wiring, and the provider-family coloring (Radix Tabs activate on `mouseDown`, not click — the tests fire that).

`stats.tokens.*` i18n keys are ALSO used by the Records detail token tooltip (App.tsx) — they are not Stats-private; don't remove them with Stats changes.

## Favorites

Per-message starring + dedicated Favorites route. Stars live next to every message in the Records detail pane; the Favorites route lists them in a Records-style 2-column shell (list / detail) and survives source-file deletion via local snapshots.

### Snapshot rule (LOCKED — do not weaken)

A `Favorite` is a **self-contained snapshot** of the parsed message. The full `SessionMessage` (role + complete markdown text + timestamp + kind) gets stored verbatim alongside source-session metadata (title, project, path, message index). When the source session is later deleted / renamed / re-parsed differently, the Favorite **stays readable and renders identically** — the detail pane uses the same `<MessageBody>` (react-markdown + remark-gfm) pipeline as the Records detail, so what you see in Favorites matches what you saw when you starred it.

Do not:
- Replace the snapshot with a `(source_session_id, message_index)` reference that re-fetches the message at render time. The "Open original" button still uses that tuple, but as a navigation hint, not the source of truth. If the index has drifted by the time the user clicks it, that's accepted — the snapshot in the favorite is authoritative.
- Strip the snapshot to "just the markdown text" — `role` / `kind` / `timestamp` are read by the role-bar color, the lowercase chip, and possible future tooltips.
- Lazy-fetch session metadata. `source_session_title` / `source_session_project` are stored at favorite time so the list card still has something to show when the original session is gone.

### Wire shape

`Favorite` (per favorite, in `~/.termory/favorites.json` as a JSON array):

```ts
{
  id: string;                     // UUID v4 (frontend-generated)
  favorited_at: string;           // ISO 8601 UTC
  message: SessionMessage;        // full snapshot
  source: SessionSource;          // narrowed: "Claude" | "Codex" | "Gemini" | "OpenCode"
  source_session_id: string;
  source_session_path: string;
  source_session_title: string;
  source_session_project: string;
  source_message_index: number;
}
```

Same `SessionMessage` struct used by `scan_all_sessions` / Records — no schema duplication.

### Identity key

The `(source, source_session_id, source_message_index)` tuple is the "is this message currently favorited?" key (`favoriteKey()` in `src/lib/favorites.ts`). Used by:
- The star button to render its fill state in O(1)
- `toggleFavoriteEntry()` to flip between add / remove
- The `[&_*]:text-primary-foreground` highlight cascade in the list cards

Index drift across re-parses is the documented trade-off — if a session's `merge_tool_outputs` produces a different message at the same index later, the Records star may light up on the wrong message. The Favorites page itself is unaffected because it renders the snapshot. We deliberately did NOT pick a content hash because (a) it complicates "click star to remove" UX (hash mismatch ≠ "wasn't favorited") and (b) index drift is rare; sessions are append-only in all four scanners.

### Frontend layout (LOCKED for visual parity with Records)

FavoritesPage is the same shell as Records' middle + right columns:
- List column: `bg-sidebar` aside, 240–300px, `text-sidebar-foreground` default text, `bg-primary text-primary-foreground [&_*]:text-primary-foreground` active state — identical class set to Records' session list buttons. Newest first by `favorited_at`. Auto-selects the first card on mount and after the previously-selected card is removed.
- Detail column: same `<header>` shape as Records detail (`text-lg font-semibold` title, `text-xs leading-none` meta row with `·` chips, size=12 lucide icons). Action icons (`ExternalLink` / `Trash2`) cluster top-right of the meta row, NOT the title row — keeps the title clean.

`MessageList` (used in Records, optionally pluggable into anywhere else) accepts an optional `favorites: FavoriteContext` bundle (session + keys + onToggle). Caller that doesn't want the affordance simply omits the bundle — the three-field grouping prevents "forgot to pass one" bugs.

### Scroll-to-message navigation (shared by Favorites + Search + Cmd-K + in-session find)

Four sites navigate "into a specific message" — Favorites "Open original", Search results, Cmd-K palette results, and the in-session find bar's next/prev jumps. All converge on the same `openItem(item, messageIndex?, query?)` → `pendingScroll` → `MessageList scrollRequest` → `onScrolled` path (the find bar skips `openItem` and sets `pendingScroll` directly via `jumpToFindMatch`):

1. **Backend** — `SearchHit` carries `first_match_index: Option<usize>` (the 0-based index of the message containing the first query hit). `serde(default, skip_serializing_if = "Option::is_none")` so old hits without it still deserialize.
2. **`openItem(item, messageIndex?, query?)`** in App.tsx — sets `selected` AND a `pendingScroll = {sessionKey, index, nonce}` state. Nonce is `Date.now()`; same target clicked twice still re-triggers. A non-empty `query` additionally stages `pendingFind` (see "In-session find" below).
3. **Records renders**; once `detail.messages` loads, `MessageList` receives `scrollRequest` (only when `pendingScroll.sessionKey === selected`'s key).
4. **`MessageList`'s effect** fires a `requestAnimationFrame` → `virtualizer.scrollToIndex(idx, {align: "start"})` → `onScrolled()` callback.
5. **`clearPendingScroll = useCallback(() => setPendingScroll(null), [])`** in App.tsx nulls the state — required so the scroll doesn't fire again when the user selects another session and comes back.

The callback identity is stable (`useCallback` with empty deps) so the MessageList effect's dep array doesn't oscillate. SearchPage / CommandPalette pass `hit.first_match_index` through to `onOpenItem`; Favorites passes `source_message_index`.

### In-session find (⌘F in the Records detail pane)

Frontend-only find over the loaded detail — no IPC. **Search shortcut layout (revised plan, user-confirmed item-by-item 2026-07-17)**: `⌘K` = the quick-jump palette, its ONLY trigger; `⌘F` = in-session find when Records shows a detail with messages (session OR memory/skill doc) and **does NOTHING anywhere else** (no palette fallback — the two keys must never both open the palette, user requirement); `⌘⇧F` = nothing. Both keydowns exclude `shiftKey`.

**Palette (CommandPalette) — user-specified shape** (iterated to spec item-by-item; don't rearrange shortcuts or palette structure again without an approved plan): `ui/command.tsx` is the VERBATIM shadcn-CLI output (no `shouldFilter` passthrough), so the palette composes the official structure itself — `Dialog > sr-only DialogHeader > DialogContent(overflow-hidden p-0 sm:max-w-md rounded-2xl, showCloseButton={false}) > Command(shouldFilter={false})`. The Command className is BASED ON the official CommandDialog's but palette-owned and TRIMMED: the blanket `[cmdk-item]` py-3 / svg h-5 clauses are deliberately removed so rows size themselves without `!important` fights (the ONE remaining `!` is the `[&_svg]:!size-[11px]` wrapper around SnippetLine, whose inner icon has no size-* class and would otherwise hit CommandItem's `svg:not([class*='size-'])` rule). **`open` is CONTROLLED by App** (`paletteOpen`) so App's ⌘F handler can no-op while the palette is open (window keydown listeners still fire under a Radix dialog); the ⌘K listener lives inside the palette using the props. Input row CLONES the Search page's input (rounded-md `bg-muted` pill, h-11, size-4 icon, borderless — via `[data-slot=command-input-wrapper]` targeting; wrapper `mx-3 mt-3 mb-2`): the stock left icon swaps to a `Loader2` while `searching` (= the whole debounce+IPC window, not the ms-level `loading` flag; "No matches" renders ONLY when settled — `settled && !loading`, same as SearchPage's `noResults`, else it flashes on every keystroke), and a shared `SearchClearButton` (`src/components/SearchClearButton.tsx`, grey-circle ✕ — also used by SearchPage, whose native `type="search"` control was replaced by it) clears the query. NO dialog close X, no group headings, no Recent-searches section (Search page keeps its own recents), and NO instant metadata fallback rows — the palette shows BACKEND results only, in lockstep with the Search page (the old fallback flashed title-only rows that then swapped into snippet rows). Rows render through the SHARED `SearchResultRow` (`src/components/search/SearchResultRow.tsx` — the same component the Search page's `SearchResultCard` wraps in its `<button>`), so the two result lists are ONE implementation: title + relative date / `📁 project … [💬 count] source icon(s)` / highlighted `SnippetLine`; message count only for Session-type rows, memory/skill rows show one icon per tool tag. Its icon sizes are class-based (`size-3` etc., NOT the lucide `size` attribute) so cmdk's `svg:not([class*='size-'])` item rule can't render the row differently between the two hosts. Rows sit in a `px-3` group with `gap-1.5`, each `rounded-md bg-card px-2 py-2` (Search-page card styling). The **"View all results" bridge** (`command.viewAllResults`) is a footer `<button>` OUTSIDE the scrolling CommandList (`rounded-md mx-3 mt-2 mb-3 px-3`, no divider line; plain button by design — not a cmdk item; Enter reaches it via the input's no-rows onKeyDown) → `onOpenSearchPage(query)` → App sets `searchSeed {query, nonce}` and routes to the Search page. **Seed is CONSUME-ONCE**: `SearchPage.seed` + `onSeedConsumed` — the page applies the seed in an effect and App nulls it, so later normal visits (the page is route-gated and remounts) can't resurrect a stale query.

- **Match semantics** — `matchingLoweredIndices(loweredTexts, query)` (`src/lib/search-utils.ts`): case-insensitive substring over each message's formatted markdown `text`, same as the backend `search_all_sessions`; takes PRE-LOWERED texts (App.tsx lowercases once per opened find). Blank query matches nothing.
- **Find bar UI** — `src/components/TranscriptFindBar.tsx`: full-width in-flow row between the detail header and the body (user decisions: NO floating overlay, 100% width), styled as the SearchPage `rounded-md bg-muted` pill (SearchPage.tsx:51) with a borderless input, `{pos+1}/{total}` counter, prev/next/close (`icon-xs` ghosts). Enter/Shift+Enter navigate, Esc closes, `focusNonce` re-focuses + selects on repeat ⌘F. Purely presentational — all state lives in App.tsx (`findOpen`/`findQuery`/`findPos`/`findFocusNonce`).
- **Term highlight** — `rehypeHighlightTerm` (`src/lib/highlight-term.ts`), a dependency-free rehype pass wrapping every case-insensitive occurrence in `<mark>`; `MessageBody` gains an optional `highlight` prop (only matched messages mount the plugin). Highlight rendering: ONE component — `SearchMark` (`src/components/SearchMark.tsx`, `bg-yellow-300 text-yellow-950! dark:bg-yellow-800 dark:text-amber-100!`) — is the single place every highlight in the app renders through: SnippetLine (Search page + palette snippets) uses it directly, and MessageBody maps react-markdown's `mark` elements onto it (`components={{ mark: SearchMark }}`; only `rehypeHighlightTerm` produces mark elements — raw HTML in session text is never parsed, so the mapping can't catch user content). The rehype plugin emits BARE `<mark>` nodes (no styling in the tree). NO custom CSS rule, NO invented hex — framework palette only (referencing `--color-*` vars in plain CSS doesn't work: Tailwind v4 tree-shakes unused theme variables). Design rules: YELLOW family per the universal text-find convention (primary-tinted highlights were tried and rejected — they collide with the selection color and go invisible on selected solid-primary rows); SOLID colors so it reads identically over cards/code/selected rows; the trailing `!` on the text utilities beats the selected row's equal-specificity `[&_*]:text-primary-foreground` cascade. No rounding/padding, no current-match special color (scroll + counter convey position); `data-find`/`data-current` attributes are still set but unstyled. Settled after many iterations — don't re-litigate piecemeal. **NO whole-message background** (user decision — an early message-level wash was rejected) and the highlight must NOT change layout (no margins/padding on the wrapper; ring/box-shadow only). Caveat: counting is source-based, highlighting is rendered-text-based, so a query matching only markdown syntax (`**bash**`) counts but shows no mark.
- **Sessions navigate per-MESSAGE** (`MessageList` is virtualized — rows mount/unmount, so occurrence DOM walking is impossible): `find?: FindHighlight { query, indices, current }` prop; next/prev wrap around and reuse `pendingScroll`.
- **Docs navigate per-OCCURRENCE** — `src/components/DocDetailView.tsx` (the memory/skill detail branch, replacing the old inline MessageBody card in App.tsx): the whole doc is in the DOM, so it counts rendered `<mark>`s, moves a `data-current` attribute across them, and `scrollIntoView({block:"center"})`s the current one.
- **Search → find linkage** — opening a record from Search/Cmd-K passes the committed query through `openItem`'s third arg; App stages `pendingFind = {sessionKey, query}`. **ONE effect** (`findPrevSelectedKeyRef` tracks real selection changes) owns the find bar's reaction: staged find matching the selection → open pre-filled + consume; selection actually changed → close (query kept, browser-style) AND drop a stale `pendingFind` so it can't fire on a much-later manual selection; the re-run caused by consuming is a no-op. (An earlier two-effect split relied silently on declaration order — don't reintroduce it.) Match scanning is single-pass per keystroke: `loweredTexts` memo lowercases the transcript ONCE per opened find (gated on `findOpen` — loading a detail pays nothing), `findMatches` = `matchingLoweredIndices` over it, and the jump-to-first-match runs in an effect off that memo (`lastJumpQueryRef` dedups) instead of a second handler-side scan.
- **Tests** — `search-utils.test.ts` (matchingLoweredIndices), `highlight-term.test.ts` (mark tree walk), `TranscriptFindBar.test.tsx`, `DocDetailView.test.tsx` (occurrence nav wrap-around), `MessageList.test.tsx` (data-find + `<mark>` scoping), `CommandPalette.test.tsx` (⌘F routing + query passthrough).

## First-class projects, migration & deletion

### Project model (LOCKED)

A **project is a folder / entity each CLI keeps**, enumerated DIRECTLY and **independently of records** — a project shows because its entity exists, not because a record groups under it. `scan_sessions` returns `ScanResult { projects: Vec<Project>, records: Vec<AppSession> }`:

- `Project { source, project }` — `project` is the cwd string records also carry (the join key). There is **no `id`** and there are **no empty-project placeholders** anymore — the old `id == ""` placeholder approach (records masquerading as projects) was removed from BOTH backend and frontend.
- **`projects`** = `scan_project_folders()` + `opencode_empty_projects()` + `add_record_projects()`:
  - `scan_project_folders()` enumerates real folders — Claude: every `~/.claude/projects/<slug>` dir (cwd via `claude_cwd_from_project_dir` reading a sibling session, else lossy `decode_claude_project_slug`; slug must contain `-`); Gemini: every `~/.gemini/tmp/<id>/` with a `.project_root` marker (excludes `bin` etc.).
  - `opencode_empty_projects()` enumerates OpenCode's `project` table for rows with **no live top-level session** (OpenCode keeps projects independently of sessions; deleting/archiving a session doesn't drop the row) → a project keyed by `worktree`. (OpenCode projects WITH sessions come from `add_record_projects` by the session's `directory`; the two don't overlap, since "empty" means session-less.)
  - `add_record_projects()` fills in CLIs with **no folder entity** — Codex (sessions in one sqlite keyed by `cwd`) and OpenCode-with-sessions — by taking each session record's `(source, cwd)`.
- **`records`** = sessions + memory + skills (the contents inside projects), scanned separately.
- Consequence: a folder with sessions, with only auto-memory, or empty all show as a project; **deleting a record never removes its project** (the project stays until its folder is deleted).

### Memory terminology (do NOT conflate — safety-critical)

- **Auto-memory** — `~/.claude/projects/<slug>/memory/**.md` (Gemini: `tmp/<id>/memory/`). It lives **INSIDE the slug/hash folder**, so it's deleted when the project folder is deleted.
- **The project's own files** — `<cwd>/CLAUDE.md`, `AGENTS.md`, `<cwd>/.claude/...`. They live in the **USER's working directory, OUTSIDE the slug folder** → the backend **NEVER deletes them** (Termory only ever writes/deletes under `~/.claude/projects` & `~/.gemini/tmp`).

### Delete (Claude + Gemini; Codex/OpenCode have no delete yet)

Right-click actions in Records. The frontend **never sends a raw filesystem path** for delete/migrate — it sends `(project, rel)` and the backend builds the path under a bounded root.

- **Single record** (session / memory): `rel` = the file's path within its project dir (`recordRel(path)`). Backend joins it under `sanitize_claude_path(project)` (Claude) / the `.project_root`-matched tmp dir (Gemini), guarded by `is_safe_rel(rel)` (every component `Normal`; rejects `..`/absolute). Claude session delete also removes the companion `<id>/` dir.
- **Project**: `delete_claude_project(project)` → `remove_dir_all(~/.claude/projects/<sanitize(project)>)` (sessions + companion dirs + **auto-memory**, all inside the folder). `delete_gemini_project` removes the `tmp/<id>/` dir(s) whose `.project_root` matches. **Neither touches the cwd.**
- **Display = "no project → no data":** delete-project drops EVERYTHING shown under that project — sessions + auto-memory + the cwd's own `CLAUDE.md`/`AGENTS.md`/skills — from the UI via `projectDataMatch(source, cwd)` (`r.project === cwd && (r.source === source || ((memory||skill) && memoryToolsOf(r).includes(source)))`). The cwd's own files **survive on disk**; they just stop showing (the next scan won't return them once the project's sessions are gone).

### Migrate (Claude + Codex + Gemini + OpenCode + Grok)

`(project, rel, new_path, delete_old=true)` — MOVE mode. `migrate_claude_project` copies the whole slug subtree (sessions + companion dirs + `memory/`, skipping the regenerable `sessions-index.json` + `.DS_Store`) to the new path's slug, **rewrites only the top-level `cwd`** of each JSONL line (history content referencing the old path is left intact on purpose), then removes the old dir. `resolve_new_claude_dir` = slug of `fs::canonicalize(new_path)` **minus the Windows `\\?\` prefix**. Frontend remaps the moved records via `projectRecordMatch` (records under the slug folder). **mtime is preserved** on every migrated file (`preserve_mtime` after each `fs::write` / `fs::copy` in `copy_tree_rewrite_cwd` + the single session/memory writers) — `fs::write`/`fs::copy` reset it, but Claude's `/resume` list and Termory's mtime fallback order by it, so a naive copy would make every moved session read "just now" (same reason the Codex rollout rewrite restores mtime).

**Codex migrate** (`migrate_codex_session(id, new_path)` / `migrate_codex_project(project, new_path)`, IPC in lib.rs; returns `CodexMigrationResult { sessions, new_project }`). Codex has **no project folder** — a "project" is just `threads.cwd`, and the rollout JSONL files live under `~/.codex/sessions/` regardless of cwd — so migrating is a **pure metadata rewrite, no file relocation** (unlike Claude). The `threads` table is only a CACHE (Codex rebuilds `cwd` from the rollout's first-line `session_meta.payload.cwd` on backfill/resume reconcile — same story as `codex_follow.rs`'s model_provider), so `migrate_codex_threads_in` **rewrites each rollout file's `payload.cwd` FIRST** (`rewrite_rollout_cwd` — authoritative, atomic temp+rename, original mtime restored since the resume picker orders by file mtime), **then `UPDATE threads.cwd`** for the rewritten rows (file-first so a crash self-heals to the new cwd via backfill). Project migrate matches `where cwd = ?` (includes archived — whole-project move, consistent with `delete_codex_project`). `new_path` is canonicalized via `codex_canonical_cwd` (strips `\\?\`, falls back to the trimmed input when the dir doesn't exist yet). **Lower value than Claude migrate** — `codex resume <id>` already works across a rename (global DB keyed by id), so this is a regrouping convenience, not a "history lost" fix. Frontend `runCodexMigration` (migrate.ts) adapts the result to the shared `MigrateResult` shape with empty `old_dir`/`new_dir` (the record's path stays put; only `project` is re-pointed), and remaps via a direct `(source==="Codex" && project===old)` match — NOT `projectRecordMatch` (Codex rollout files share one `~/.codex/sessions` dir, so a folder match would hit unrelated sessions). Codex-specific i18n: `menu.migrateConfirmCodex` / `menu.migrateSuccessCodex`.

**Gemini migrate** (`migrate_gemini_project(project, new_path)`, IPC in lib.rs; returns `GeminiMigrationResult { sessions, new_project }`; **project-level only** — Gemini sessions are project-scoped (share one tmp slug dir), so there's no per-session/memory migrate). Gemini identifies a project by a slug in `~/.gemini/projects.json` **backed by `.project_root` ownership markers** under BOTH base dirs (`~/.gemini/tmp/<slug>/` and `~/.gemini/history/<slug>/`) holding the project's absolute path. `getShortId()` **SELF-HEALS the registry from those markers** — `findExistingSlugForPath` adopts a slug whose marker names the launch cwd, then rewrites projects.json + re-ensures markers (verified: gemini-cli `projectRegistry.ts:216-303` + `storage.ts` `initialize()`). So migrating is a **pure MARKER rewrite** — NO file move (the slug dir stays put, its whole history follows) and **NO projects.json write** (avoids gemini's `proper-lockfile` race; gemini reconciles the registry on its next run, and the stale old-path entry auto-removes when the old path is next opened — same "never write the CLI's live registry" stance as Claude's `~/.claude.json`). `migrate_gemini_project_in(tmp, history, project, new_path)` finds the slug dir(s) via `gemini_project_dirs` (`.project_root == project`), refuses when the destination already owns a Gemini dir (else two slugs collide → gemini's `ensureOwnershipMarkers` throws EEXIST), then `write_project_root_marker`s each tmp + sibling history marker to the destination (NO trailing newline, matching `ensureOwnershipMarkers`). **Marker value = `canonical_dest_dir`: CANONICALIZED (symlinks resolved)** — the SHARED helper grok also uses. gemini's `normalizePath` is `path.resolve` (no symlink resolution), BUT the QUERY it compares against is `process.cwd()`, which Node has ALREADY resolved — so the marker must be the resolved path or `verifySlugOwnership` fails on a symlinked destination. **This was caught by LIVE testing** (an earlier `NOT canonicalized` version made real gemini mint a fresh slug for `/private/tmp/x` instead of adopting the migrated one when the marker said `/tmp/x`). No mtime handling needed (session `.jsonl` files are untouched, so their resume-ordering mtimes are preserved). The result's `sessions` count comes from `count_gemini_chat_files`, which gates each `chats/session-*.{jsonl,json}` on the SAME `parse_gemini_session_file` (metadata_only) `hasResumableContent` rule the scanner uses — so the "Migrated N sessions" toast matches what Termory/gemini list, not the raw file count (startup-/command-/context-only shells are excluded). Frontend `runGeminiMigration` (migrate.ts) adapts to the shared `MigrateResult` (empty dirs; only `project` re-points) and remaps by `(source==="Gemini" && project===old)` like Codex (per-project memory/skills reconcile on the next scan). i18n: `menu.migrateConfirmGemini` / `menu.migrateSuccessGemini`. Tests: `sessions::tests::{migrate_gemini_project_in_rewrites_markers_and_counts_sessions, migrate_gemini_project_in_guards_source_dest_and_collision}`. **Live-verified end-to-end** (2026-07-18) against the real gemini binary on an isolated `$HOME`: after `migrate_gemini_project`, gemini launched in the destination cwd self-healed `projects.json` to the SAME migrated slug (both a canonical dest AND a symlinked `/tmp` dest — the latter is what surfaced the canonicalization fix).

**OpenCode migrate** (`migrate_opencode_session(id, new_path)` / `migrate_opencode_project(project, new_path)`, IPC in lib.rs; returns `OpencodeMigrationResult { sessions, new_project }`). Termory groups OpenCode records by `session.directory` (add_record_projects), so migrating is a **pure SQLite `UPDATE session SET directory = ?, path = NULL`** — session form `WHERE id = ?`, project form `WHERE directory = ?` (a directory belongs to exactly one project, so scoped). **`project_id` (the git-workspace `project` row) is deliberately LEFT INTACT**: the session stays attached to its project, so it never resurfaces as an empty project at the old cwd (`opencode_empty_projects` only lists session-less projects), and Termory never hand-writes OpenCode's internal `project`/worktree registry tables (verified schema: `core/src/{session,project}/sql.ts` — `project` keyed by `worktree`, a separate `(project_id, directory)` worktree-map table exists; touching those is unjustified for this feature). `path` (the nullable cwd-relative-to-worktree column) is set NULL so a stale relative path doesn't linger — OpenCode tolerates a null path (`listByProject` has an `isNull(path)` branch). Directory is stored absolute + forward-slashed on Windows (`database/path.ts` storagePath) — `opencode_directory_value` matches that: it runs the destination through the SHARED `canonical_dest_dir` (symlink-resolved — OpenCode also stores a resolved directory since it derives it from `process.cwd`, so canonicalizing keeps a migrated row in the SAME form as OpenCode's own rows and the grok/gemini migrates), then `\`→`/` on Windows. **LOW value** (like Codex): `opencode --session <id>` resumes by id regardless of cwd, so this is a Termory-display regrouping convenience, not a "history lost" fix (opencode's OWN per-worktree resume list won't move — `project_id` unchanged — which is the accepted tradeoff). Opens RW with the same `open_opencode_rw` (busy_timeout + `map_db_locked` "quit OpenCode"). Frontend `runOpencodeMigration` (session + project) adapts to the shared `MigrateResult` (empty dirs; only `project` re-points); wired in `ListItemMenu.tsx` (session) + the sidebar project row in `App.tsx` (by `(source,cwd)` like Codex/Gemini). **The session-row `onLocalMigrate` matches by `sessionKey`, NOT path** — OpenCode shares one db-file path across all sessions, so a path match would re-point the whole list (same reason as `onLocalDelete`). i18n: `menu.migrateConfirmOpencode` / `menu.migrateSuccessOpencode`. Tests: `sessions::tests::{migrate_opencode_session_in_repoints_directory_and_nulls_path, migrate_opencode_project_in_repoints_matching_directory_only}`. **Live-verified end-to-end** (2026-07-18) against the real opencode binary on a copy of the real db: after `migrate_opencode_session`, `opencode export <id>` read the session cleanly (DB uncorrupted) with `directory` = the new path and `path` = null. (The destination is canonicalized via the shared `canonical_dest_dir` for consistency with OpenCode's own resolved directories + the grok/gemini migrates.)

**Grok migrate** (`migrate_grok_session(id, new_path)` / `migrate_grok_project(project, new_path)`, IPC in lib.rs; returns `GrokMigrationResult { sessions, new_project, old_dir, new_dir }`). Grok is FILE-based: a session lives at `<grok-home>/sessions/<encode_cwd_dirname(info.cwd)>/<id>/` and grok's per-cwd resume scans EXACTLY `sessions/<encode_cwd_dirname(launch_cwd)>/` (jsonl/mod.rs:130), so — UNLIKE the metadata CLIs — the session dir must physically MOVE to the destination's encoded-cwd dir. `move_grok_session_dir` `fs::rename`s the `<id>/` dir into `<root>/<new-enc>/` then rewrites its `summary.json` info.cwd (mtime preserved — grok's cross-workspace recent list sorts by summary.json mtime; move-then-rewrite, and grok isn't running during a migrate). Session form resolves the dir via the scan path index (like `delete_grok_session`); project form scans for the encoded-cwd dir whose sessions record `info.cwd == project` (authoritative, never decodes the dir name — like `delete_grok_project`), moves all its `<id>/` dirs, then `remove_dir_all`s the emptied old dir (matching delete's "project moved away"). **`grok_encode_cwd_dirname` replicates grok's URL-encoding byte-for-byte** (paths.rs:112 — RFC-3986 unreserved `-._~` + alnum kept, else uppercase `%XX`); the long-path blake3 fallback (encoded > 255 bytes) is **refused** rather than pulling blake3 + slugify for a pathological >250-char cwd. **Destination cwd uses the shared `canonical_dest_dir` helper — CANONICALIZED (resolves symlinks)**: grok's cwd comes from the OS `current_dir` which resolves symlinks, so a session launched in `/tmp/x` scans the `/private/tmp/x` encoded dir. This was caught by LIVE testing against the real grok binary: a non-canonicalized `/tmp/x` destination made `grok -c` report "No session found"; canonicalizing to `/private/tmp/x` fixed it (test `canonical_dest_dir_canonicalizes_symlinks`). **`canonical_dest_dir` is shared with gemini + opencode** — all three CLIs derive/store their cwd from the OS `current_dir` (symlink-resolved), so all need the resolved form (gemini is the non-obvious case: its `path.resolve`-based comparison still needs a canonicalized input because its query is the already-resolved `process.cwd()`). Because paths CHANGE (file move, like Claude), the result carries `old_dir`/`new_dir` (the old/new encoded-cwd dirs) so the frontend `remappedPath` prefix-remaps moved record paths; `runGrokMigration` maps them into the shared `MigrateResult`. Search index (`session_search.sqlite` at the sessions root) needs no update — the session id (dir name) is unchanged, so its FTS doc stays valid. Wired in `ListItemMenu.tsx` (session) + the sidebar project row in `App.tsx` (by `(source,cwd)`, remap uses `old_dir`/`new_dir`). i18n: `menu.migrateConfirmGrok` / `menu.migrateSuccessGrok`. Tests: `sessions::tests::{grok_encode_cwd_dirname_matches_url_encoding, grok_dest_cwd_canonicalizes_symlinks, migrate_grok_project_in_moves_dirs_rewrites_cwd_and_removes_old, migrate_grok_session_moves_single_dir_via_path_index}`. **Live-verified end-to-end** (2026-07-18) against the real grok 0.2.103 binary on a copy of real session data: after `migrate_grok_project`, `grok -c` (continue-most-recent-for-cwd) in the destination dir resolved the migrated session (A/B: found at the new cwd, "No session found" at an unrelated cwd).

**MOVE semantics (LOCKED):** copy would leave the same session UUID in two slug dirs → `scan_claude_projects` dedups by UUID → the old project vanishes anyway + leaves hidden duplicates. So MOVE (remove old only **after** a complete copy). Backend keeps the `delete_old` flag (copy still tested); the frontend always sends `true`.

**`claude --resume` registration note (verified — not a Termory bug):** the moved files land in the right slug dir, but `claude --resume` (the command-line flag) only lists a project's sessions once that project is registered in `~/.claude.json` — and Claude registers a project when you OPEN it (run `claude` in the dir), NOT from the migrated files. So a project you've never opened in Claude won't list the moved sessions via the `--resume` flag until you run `claude` there once (afterwards `--resume` works too); **`/resume` inside a running Claude shows them regardless** (it reads the slug dir directly). Termory therefore: (1) **never writes `~/.claude.json`** — it's Claude's live config, rewritten by any running claude, so a concurrent write would clash; (2) on migrate, calls the **read-only** `claude_project_registered(new_path)` and, when unregistered, the confirm dialog adds a plain user-facing line (`menu.migrateConfirmUnregistered`) telling the user to run claude in the folder once. (Confirmed empirically: `claude --resume` of a never-opened project showed nothing; entering `claude` then `/resume` showed the migrated sessions.)

### Local update (success only) + tombstones

After a **successful** invoke the frontend mutates its in-memory lists in place (no full re-scan): `onDeleted`/`onMigrated` are called **only in the success branch** (a failure shows a toast and removes nothing). `tombstonesRef` (record paths) + `projectTombstonesRef` (source+cwd) hide just-removed entries until a fresh scan confirms them gone, defending against a stale in-flight scan re-adding them (`reconcileTombstones`, generic over the key).

### Code map

- **Backend** (`sessions.rs`, `*_in` inner fns for env-free tests): `scan_project_folders` / `claude_project_folders_in` / `gemini_project_folders_in` / `add_record_projects`; `migrate_claude_{project,session,memory}_in`; `migrate_codex_threads_in` (+ public `migrate_codex_{session,project}`, helpers `rewrite_rollout_cwd` / `codex_canonical_cwd`); `migrate_gemini_project_in` (+ public `migrate_gemini_project`, helpers `write_project_root_marker` / `count_gemini_chat_files`); `migrate_opencode_{session,project}_in` (+ public `migrate_opencode_{session,project}`, helper `opencode_directory_value`); `migrate_grok_{session,project}` (+ `migrate_grok_project_in`, helpers `grok_encode_cwd_dirname` / `rewrite_grok_summary_cwd` / `move_grok_session_dir`); the SHARED `canonical_dest_dir` (symlink-resolving destination, used by grok + gemini + opencode); `delete_claude_{project,session,memory}_in`; `delete_gemini_{project,record}_in`; helpers `is_safe_rel`, `gemini_project_dirs`, `resolve_new_claude_dir`, `rewrite_jsonl_cwd`, `copy_tree_rewrite_cwd`. IPC wrappers in `lib.rs`.
- **Frontend:** `src/lib/migrate.ts` (`runClaudeMigration`, `runCodexMigration`, `runGeminiMigration`, `runOpencodeMigration`, `runGrokMigration`, `runRecordDelete` — confirm → invoke → toast → success-only local callback); `src/lib/records.ts` (`projectKey`, `projectDirOf`, `recordUnderFolder`, `remappedPath`, `reconcileTombstones`, `withProject`); `recordRel` in `session-utils.ts`; `App.tsx` (`projects` state, `applyScanResult(ScanResult)`, `sourceGroups` rendered from `projects`, `removeRecordsLocally` / `remapRecordsLocally` / `projectRecordMatch` / `projectDataMatch`); per-row menu in `ListItemMenu.tsx`. i18n keys under `menu.migrate*` / `menu.delete*`.

**Tests:** `sessions::tests::{claude_project_folders_in, gemini_project_folders_in, add_record_projects, is_safe_rel, migrate_claude_*, migrate_codex_*, migrate_gemini_project_in_*, migrate_opencode_*, migrate_grok_*, grok_encode_cwd_dirname_*, canonical_dest_dir_canonicalizes_symlinks, rewrite_rollout_cwd_*, codex_canonical_cwd_*, delete_claude_*, delete_gemini_*}` + `records.test.ts` + `session-utils.test.ts` (`recordRel`) + `ListItemMenu.test.tsx` (per-entry wiring incl. Codex/OpenCode/Grok migrate, cancel paths). **Live end-to-end verification against the real CLI binaries (2026-07-18):** Claude (real copilot.is→chats.is data, 39/39 files moved + `cwd` rewritten); Grok (`grok -c` resolves the migrated session at the new cwd, A/B'd); Gemini (real gemini self-heals `projects.json` to the migrated slug); OpenCode (`opencode export` reads the moved session, DB uncorrupted). The grok+gemini runs SURFACED the symlink-canonicalization bug now fixed by `canonical_dest_dir`.

**Migrate + delete are now COMPLETE for all five CLIs.** Migrate — Claude (folder subtree move), Codex (rollout `payload.cwd` + `threads.cwd` rewrite), Gemini (project-level `.project_root` marker rewrite), OpenCode (session/project `UPDATE session.directory`), Grok (session/project dir move + `summary.json` cwd rewrite); see "Migrate (Claude + Codex + Gemini + OpenCode + Grok)". Delete — session/project for all five, + memory for Claude/Gemini/Codex/Grok (OpenCode has no deletable memory). (OpenCode empty-project enumeration via the `project` table IS done — `opencode_empty_projects`.) Remaining known gap: none for migrate/delete; **per-session/memory migrate is intentionally NOT built for Gemini** (its sessions are project-scoped, so only project-level migrate is meaningful).

## UI conventions

### Internationalization (i18n)

The whole frontend UI is translated into **English / 简体中文 / 繁體中文** via a lightweight, type-safe in-house i18n (no `i18next` — the languages are a fixed bundled set, and Chinese has no plural rules, so the heavy machinery isn't warranted).

**Module: `src/i18n/`**

- `locales/en.ts` — the **source dictionary**. A flat `{ "dot.key": "text" }` object `as const`; its keys form the `MessageKey` union type.
- `locales/zh-Hans.ts` / `locales/zh-Hant.ts` — `Record<MessageKey, string>`, so **omitting any key the English dict defines is a compile error** (this is the completeness check — there's also a runtime test in `i18n.test.ts` asserting the three key sets are identical).
- `index.tsx` — `<I18nProvider>` + `useI18n()` / `useT()`, `{var}` interpolation, `resolveLocale()` (system locale → one of ours), and `LOCALES` (the picker list, each language shown in its own name).

**Wiring & behavior**

- `<I18nProvider>` wraps the app in `main.tsx` (outside `ThemeProvider`). It seeds the locale from `navigator.language` (`resolveLocale`), then overrides with the saved `language` config key on mount; `setLocale` persists back via `config.ts` and sets `<html lang>`. Switching re-renders all `useT()` consumers immediately.
- `useI18n()` falls back to a **default English context when no provider is mounted** (e.g. a unit test rendering a component in isolation) — so component tests don't need to wrap in `<I18nProvider>` and assert the English strings. `t()` itself falls back missing-translation → English → the raw key.

**Adding / changing UI text (the rule)**

1. Add the key to **`en.ts`** first (this defines the `MessageKey`), then add the SAME key to **both** `zh-Hans.ts` and `zh-Hant.ts` — the build breaks until all three have it. Keep keys grouped by area (`nav.*`, `search.*`, `stats.*`, `records.*`, `menu.*`, `providers.*`, `help.*`, `errors.*`, `toast.*`, `update.*`, `install.*`, `settings.*`, `command.*`, `favorites.*`, `footer.*`, `time.*`, `common.*`).
2. In the component: `const t = useT();` then `t("area.key")` / `t("area.key", { var })`. For a string passed to a child as a prop (e.g. `EmptyState title`, `Kpi label`, a const array of `{ labelKey }`), store the `MessageKey` and call `t()` at the render site. For module-level constants (preset/theme/shortcut tables) store a `labelKey: MessageKey`, not the literal.
- **Pure (non-React) helpers that produce UI copy take a translator param.** They can't call `useT()`, so the calling component passes its `t` in: `formatTimeAgo(ts, t)` / `formatRelativeDate(value, t)` (`src/lib/format.ts` — relative time like `time.justNow` / `time.minutesAgo` / `time.yesterday`) and `baseUrlHelp(app, npm, t)` / `apiKeyHelp(app, t)` / `overrideHelpFor(app, t)` (`src/lib/provider-utils.ts` — the editor's per-CLI help text). The format helpers keep `t` **optional** and fall back to English when omitted, so their pure-unit tests stay unchanged; the provider-utils helpers require it. The param is typed `Translate = (key: MessageKey, params?) => string` (provider-utils imports `MessageKey` from `@/i18n` — no cycle, since i18n imports `config`, not these).
- **Locale-aware date & number formatting follows the APP language, not the OS locale.** `src/lib/format.ts` keeps a module-level `currentLocale`; the `<I18nProvider>` calls `setFormatLocale(locale)` **in its render body** (not an effect — so it's set BEFORE children format in the same pass; the provider renders above all consumers). `Intl.DateTimeFormat` is rebuilt only on change (construction is expensive). Consumers: the cached date formatters (`formatDate` / `formatRelativeDate`) use it directly; ad-hoc `toLocale*` sites outside the module (FreshnessFooter tooltip, stats `formatDateLong`) read it via `getFormatLocale()`. `formatCompact` also branches on it — Chinese groups large numbers by **万 (10⁴) / 亿 (10⁸)** (`21.5B → 215亿`, `1.2M → 120万`), English keeps **K/M/B**. Without a provider (isolated unit tests) the locale is `undefined` = OS default, so existing tests are unaffected. Covered by `format.test.ts`.
- **English plurals** use two keys (`*_one` / `*_other`) selected by the caller (`t(n === 1 ? "x_one" : "x_other", { n })`); Chinese just maps both to one form.
- Brand / product names are **not** translated — they read identically in the zh dicts: CLI labels via `CLI_APP_LABEL`, `BrandIcon source`, **`AI Gateway` / `AI Gateways`** (the gateway product name — user decision), **`Base URL`**, **`AI SDK`**, **`Tokens`**, plus literals like `config.json`, `chmod 0600`, `sk-…`, and example URLs. `BrandIcon source` must stay a source literal, so translate the *display* text separately from the icon's `source` prop (see the Stats source-filter pills / sidebar "All", `MemoryCard`). (The "zh value == en value" set is the audit list for this — every entry must be a deliberate keep-English.)
- The gateway editor's add/edit dialog reuses **`providers.addProvider` / `providers.editProvider`** ("Add/Edit provider") — shared with the standalone provider editor, by user decision; the per-gateway-card edit icon keeps its own `providers.editGateway` tooltip.
- Toasts and `ask()` confirm dialogs are translated too; the only deliberate English left is `toast.error(String(err))` — a raw pass-through of a backend error with no template.
- The Rust backend's user-facing strings are mostly technical error pass-throughs surfaced via those `String(err)` toasts, so the backend is not part of the i18n system.

**Audit**: `i18n.test.ts` asserts the three key sets are identical. For dead keys (defined-but-unused) and placeholder-parity (the `{var}` set must match across all three locales or interpolation renders the literal `{var}`), there's no automated test — re-run an ad-hoc scan when in doubt (the last sweep found 0 mismatches; `providers.unmanaged` was the one dead key, since removed).

### Never hand-edit `src/components/ui/*` (LOCKED)

The files under `src/components/ui/` are stock shadcn/ui components — keep them unmodified. Do **not** hand-edit them to add styling or behavior.

Two documented baseline notes (2026-07-17): `command.tsx` is the untouched `npx shadcn@latest add command` output (an older hand-added `commandProps` passthrough was removed; CommandPalette composes `Dialog`+`Command` at the usage site to place `shouldFilter`). `dialog.tsx` carries ONE deliberate committed deviation — the overlay is `bg-white/20 dark:bg-black/30 backdrop-blur-xs` instead of stock `bg-black/50` (commit `5b02e89`, app-wide design decision; user-confirmed keep) — so a blanket CLI `--overwrite` of dialog WILL clobber it; restore the overlay line if a CLI update touches dialog.tsx.

- Need a new primitive (e.g. Collapsible, Accordion)? Add it with the shadcn CLI: `npx shadcn@latest add <name>`. It writes the stock component + pulls any radix dep. Don't hand-write the file.
- All customization (layout, spacing, animation, state) goes at the **usage site** via `className` / props on the consuming component, never inside `ui/*`. Example: the Custom config overrides collapsible in `ProviderEditor.tsx` applies its `grid`, `animate-collapsible-*`, focus-ring `-mx`/`px`, and trigger classes at the call site — `ui/collapsible.tsx` stays the untouched CLI output.
- Animation keyframes come from the already-imported `tw-animate-css` (`globals.css`), referenced via utility classes (`animate-collapsible-down/up`, `animate-in`, …) at the usage site — don't add `@keyframes` to component files.
- Verify before committing: `git diff --stat src/components/ui/` should be empty (new shadcn additions show as untracked files, which is fine).
- **Use the shadcn `Button` for standard action buttons** (`variant` = default / secondary / ghost / destructive; `size` = sm / icon-sm / …) instead of a hand-styled `<button className="…">`. Icon-only actions use `size="icon-sm"` (size-8); a destructive ghost icon (no shadcn variant for it) is `variant="ghost"` + `className="text-destructive hover:bg-destructive/10"`. A raw `<button>` is correct ONLY for things that aren't standard buttons: clickable list **rows/cards**, the activity **rail**, segmented **toggles/tabs**, filter **pills/chips**, and **inline-in-input icons** (e.g. the API-key eye toggle, `absolute`-positioned inside an `<Input>`) — shadcn Button's chrome would break those. (Converted ProviderCard / GatewaysPage / InstallGuide action buttons to `Button` in 2026-06; the rows/toggles/pills/inline-icons stay raw by design.)

### Tooltips (LOCKED)

Termory uses **shadcn `Tooltip`** (Radix-backed, mounted via `TooltipProvider` at root in `main.tsx`) — not raw HTML `title=` attributes. Native `title` was removed project-wide and replaced where the affordance is needed:

| Place | Trigger |
|---|---|
| Records detail reveal button (platform-aware label, shares `revealLabelKey` with the right-click menu) | shadcn `Tooltip` |
| `CopyMenu` trigger | shadcn `Tooltip` (suppressed while menu open via `open={menuOpen ? false : undefined}`) |
| `MessageList` star button | shadcn `Tooltip` (dynamic label: Add / Remove from favorites) |
| Per-message copy button (`MessageCopyButton` — every `MessageList` row + the Favorites detail message header; copies the message's raw markdown, 1.2s ✓ confirmation) | shadcn `Tooltip` (dynamic label: Copy / Copied) |
| Favorites detail "Open original session" + "Remove favorite" | shadcn `Tooltip` |
| Stats header compact-number chips (e.g. "1.2B tokens") | shadcn `Tooltip` showing `formatFullNumber(...)` |

Do NOT add `title="..."` to JSX. The reasons: (1) inconsistent styling vs the rest of the app, (2) no touch / long-press support, (3) browser-native tooltip can fight with focus-visible / aria-label, (4) shadcn version respects the project's color tokens.

Icon-only buttons still need an `aria-label` regardless of whether they have a Tooltip — screen-reader users don't trigger hover.

When a button opens a popover or dropdown that anchors on the SAME element (CopyMenu's case), suppress the Tooltip while the popover is open: pass `open={isOpen ? false : undefined}` to `<Tooltip>` so the two surfaces don't stack on the same anchor.

Tests that mount a component using `<Tooltip>` standalone (without the app's root `TooltipProvider`) must wrap their `render(...)` in a `TooltipProvider`. Pattern:

```tsx
function render(ui: React.ReactElement, options?: RenderOptions) {
  return rtlRender(<TooltipProvider>{ui}</TooltipProvider>, options);
}
```

## Pending feature work

The current UI shell is settled: activity rail (Providers / Records / Favorites / Search / Stats / Settings, in that order — Providers is the default landing route via `readRouteFromHash` → `"providers"` fallback), routed via URL hash, with a passive bottom freshness footer fed by the Rust filesystem watcher. **All six rail destinations are real implementations** — no route renders a placeholder anymore (`RoutePlaceholder` was removed when Favorites shipped).

Roadmap below is grouped by priority. Pick top-down within a tier.

### P0 — core capabilities for v1 ✅

All P0 items have shipped:

- ~~Search page + Cmd-K palette~~ — done. Backend `search_all_sessions` IPC + frontend `SearchPage` (grouped by source with snippet highlight) + global Cmd-K palette reusing the same store + recent-search history persisted via `config.ts`.
- ~~Empty states per route~~ — done. Distinguishes first-launch (no data) from filtered-empty (with "Clear filters" action) across Sessions / Memories / Skills panes.
- Message content rendering polish — deferred. `renderMessages` / `TimeSeparator` helpers stay staged. Revisit only with a concrete pain point.

### P1 — new pages & persistence

- ~~`tauri-plugin-store`~~ — replaced with custom `config.rs` module (`~/.termory/{config,providers,favorites}.json` with `chmod 0600`). The plugin couldn't control file location or Unix permissions; rolling our own KV gives both.
- ~~Providers page~~ — done. See the "Providers" section above. Cross-verified against `.audit-sources/cc-switch/` for the per-CLI write shapes; 4 CLIs supported with per-CLI tests. OpenCode adapter writes a full `provider.<termory-id>` block to `~/.config/opencode/opencode.json` (`npm` / `name` / `options.{baseURL,apiKey}` / `models`) — cc-switch's pattern — and never touches `auth.json`. The editor's AI SDK dropdown picks the official `npm` package (`@ai-sdk/openai-compatible` / `@ai-sdk/anthropic` / …), stored verbatim in the Provider's top-level `npm` field.
- ~~Stats page~~ — done, redesigned 2026-07 (see the "Stats" section): Overview (8 KPI cards + calendar heatmap + fun fact) / Models (stacked per-model daily bars + legend) tabs, All/30d/7d range. All values window-accurate from each session's `daily_tokens[]` — no fallback smearing.
- ~~App Settings page~~ — done. 9 sections, in order (Appearance → Tools → Startup → Language → … per John): Appearance (next-themes System/Light/Dark), **Tools** (per-app on/off toggles — see "Tool toggles" below), Startup (launch-at-login toggle via `tauri-plugin-autostart` — macOS LaunchAgent / Windows Run key / Linux XDG autostart; state read from the OS on mount, `autostart:default` capability; the login item launches with `--autostart`, which `setup()` detects to start TRAY-ONLY — window hidden + Dock icon off, same end state as closing the window), Terminal (which terminal the tray's session-resume opens — `detect_terminals` dropdown, `terminal` config key), Storage (`~/.termory/` path + "Open in Finder"), Search history (count + Clear), Keyboard shortcuts (display-only reference), About (version + manual / auto update check). The only spec item NOT in the page is **scan-path overrides** — users with non-default CLI install locations still rely on the per-CLI env vars (`CLAUDE_CONFIG_DIR` etc.). The originally-listed "watcher toggle" was dropped — the watcher just runs unconditionally (see P2 "Watcher completion").
  **Tool toggles (Settings → Tools; UI label "工具/Tools" — providers-first naming per John, i18n keys stay `settings.sources.*`).** config.json `sources` = `Partial<Record<CliApp, boolean>>`, absent key = enabled — EXCEPT default-off tools (`DEFAULT_OFF_KEYS` in config.rs ⟷ `DEFAULT_OFF_SOURCES` in provider-utils.ts, a keep-in-sync mirror pair): **gemini is OFF by default** (Gemini CLI stopped serving individual accounts 2026-06-18 — HTTP 410; enterprise Code Assist still works and the repo is still updated, Google steers individuals to Antigravity CLI) and needs an explicit `true` to show. The frontend gate is ALWAYS `isSourceEnabled()` in provider-utils — never inline truthiness. **Drag order:** the Settings → Tools rows are drag-sortable via **@dnd-kit/sortable** (`SortableSourceRow` in SettingsPage: PointerSensor with a 4px activation distance, `closestCenter` + `verticalListSortingStrategy`, grip-button-only listeners so the Switch stays clickable). Two hand-rolled iterations were REJECTED before landing here — HTML5 drag (WKWebView's native `dragDropEnabled` interception swallows the events, and drop-on-target can't insert after the last row) and raw pointermove reordering (midpoint oscillation, no cursor-following ghost). Don't re-attempt hand-rolled drag; the order persists as config `source_order` (CliApp[], same `source` family as the `sources` toggle key — the drag order is frontend-only, never read backend-side), is resolved by `orderSources()` (saved-first, unknown ids dropped, new tools appended — provider-utils + test), and drives Settings rows, Providers tabs, the Records sidebar pills, and Stats pills (all via App.tsx's `orderedCliApps`). The tray keeps `CliApp::all()` order (deliberate — not worth a snapshot field yet). Every tool is a full `CliApp` (grok included — it has provider switching), so ALL surfaces iterate `CLI_APPS`/`CLI_APP_LABEL`/`CLI_APP_SOURCE_BADGE` directly; the interim `ToolKey`/`TOOL_*` alias layer was deleted. Record/pill surfaces share the `visibleSources(order, toggles, {recordsOnly})` selector (provider-utils — excludes claude-desktop, which keeps no records). ONE backend filter point: `sessions::apply_source_toggles` post-pass at the end of `scan_sessions` (sessions/projects of a disabled source drop — source name lowercased == cli key, test-pinned convention; Memory/Skill records trim disabled tags from `preview` and drop when none remain), so Records / Search / Stats / the tray recent list all follow automatically. `refresh_session_path_index` runs BEFORE the filter so `load_session` by (source,id) still works for a disabled source (Favorites snapshots stay readable and by-id loads don't break; the FavoritesPage "Open original" button still hides via `sourceMissing` while the source is off — Records couldn't display it anyway). `write_app_config` (lib.rs) is a PER-KEY merge — the frontend `setConfig` sends only the ONE changed `{key, value}`; the backend reads the live config.json, replaces just that key, and writes it back (every other key untouched). When `key == "sources"` and its value actually changed (`obj.get("sources") != incoming`) → rescan + emit `SOURCES_CHANGED_EVENT` with the fresh ScanResult + `tray::refresh_recent` + full `rebuild_menu`. This per-key merge replaced the old whole-object write (frontend `flushConfig` → `writeConfigKey`), which round-tripped the entire startup cache and so resurrected renamed/orphan keys — a key ever written used to live forever (e.g. the retired `default_pane`, and, after an emptied file, everything came back). The disabled set RIDES IN `InstallSnapshot.disabled` (probed on the caller's thread, participates in the staleness compare) so the tray's `build_menu` / `terminal_clis` do zero config I/O on the main thread; quota triggers (`trigger_quota_refresh` / `force_quota_refresh`) skip disabled CLIs. Frontend: App.tsx owns `sourceToggles` (usePersistentState `"sources"`), filters the Records sidebar pills, guards the selected source / Providers tab back to an enabled one (plus a render-time clamp `effectiveProvidersApp` so Radix Tabs never sees a triggerless value for a frame), and threads the map to SettingsPage (Switch rows — the LAST enabled tool's switch is disabled, so all-off is unreachable) / StatsPage (pills) / ProvidersPage (tabs + `bindableInstalled` = installed ∧ enabled → GatewaysPage `installed`, + `enabledApps` → `visibleApps` → GatewayEditor rows & gateway-card binding rows, hidden not dimmed). **GatewayEditor `handleSave` carries hidden apps' existing bindings over VERBATIM** — rebuilding them from drafts would silently drop a disabled tool's binding on any save (regression test: "save preserves a hidden tool's existing binding verbatim"). Tests: `sessions::tests::apply_source_toggles_filters_sessions_projects_and_tags`, provider-utils `isSourceEnabled`, StatsPage disabled-pill, GatewayEditor visibleApps ×2.

### P2 — quality of life

- **Right-click context menus** — done. List rows (Records Sessions/Memories/Skills + Favorites) have a right-click menu via `ListItemMenu.tsx` (shadcn `ContextMenu`): "Reveal in Finder" (`revealItemInDir`, its own group; the label is platform-aware via `src/lib/platform.ts` `revealLabelKey` — Finder / File Explorer / file manager, detected from the UA incl. jsdom's darwin/win32 forms so component tests see the host platform; the detail-header button shares the SAME key since both call `revealItemInDir`) then a resume/copy group — **"Resume in terminal"** (session rows; opens the chosen terminal + resumes via the `resume_session_in_terminal` IPC, passing the row's `project`) + "Copy resume command" (all session sources, via `resumeCommandFor`), "Copy path", "Copy filename", "Copy session ID" (session/favorite rows). The Favorites menu adds "Copy message ID" (the favorite's own id) via the optional `messageId` prop, and passes two flags so the menu fits a saved SNAPSHOT rather than a live row: **`hideSessionOps`** drops the session-management actions (resume-in-terminal + migrate + delete) — those act on the SOURCE session, not the snapshot (delete would remove the real session); **`sourceMissing`** (set when the favorite's source session is no longer in the scans) additionally drops "Reveal in Finder" + "Copy resume command" — the file/session is gone, so those are dead. Copy-path/filename/ids stay. (When the source IS gone, `FavoritesPage` also hides the detail-header "Open original session" button — no "archived" label is shown.) List rows carry `select-none` so a right-click doesn't text-select the row; `MemoryCard` is `forwardRef` + spread-rest so it can be a `ContextMenu` `asChild` trigger. **No manual "Re-scan this source" / "Re-read this file" entries — deliberately not added: the watcher auto-refreshes everything Termory surfaces (see P2 "Watcher completion"), so they'd be redundant.** The **sidebar project row** has its own `ContextMenu` (in `App.tsx`, not `ListItemMenu`): "Reveal in Finder" (opens the project's `cwd` source dir via `revealItemInDir(projectName)`, `.catch` → `menu.revealError` toast since the dir may be gone after a rename/migrate) + "Open in terminal" (`menu.openInTerminal` → `new_session_in_terminal` IPC → opens a terminal in the cwd and launches the row's `group.source` CLI fresh) + "Migrate project…" (Claude / Codex) + "Delete project…".
- **Keyboard navigation** — ✅ `⌘1..6` switch rail routes (App.tsx:235), `⌘K` summons the quick-jump palette (CommandPalette.tsx; its pinned bottom row bridges to the Search page with the current query), `⌘F` opens the in-session find bar when the Records detail pane has content and does nothing elsewhere (see "In-session find"), `Esc` closes find bar / palette / dropdowns. The Cmd-K palette's own ↑/↓/Enter is `cmdk`'s built-in. ❌ Not implemented: arrow-key navigation inside the Records/Favorites lists or the source/project sidebar (a `useListNav` two-step highlight prototype was tried and removed — the focus model was unintuitive).
- ~~Watcher completion~~ — done. The filesystem watcher (`src-tauri/src/watcher.rs`) **statically** watches the CLI data dirs (`~/.codex`, `~/.claude`, `~/.gemini`, opencode, the CLI-binary dirs for install detection) and **dynamically + recursively** watches each project's cwd — the dynamic set is recomputed from every scan's `projects` list (`dynamic_paths_from_sessions` over `result.projects` → `reconfigure_dynamic`, `RecursiveMode::Recursive`). So per-project files (`<cwd>/CLAUDE.md`, `AGENTS.md`, `.claude/skills/`, …) auto-refresh on edit too, not just at launch. A debounce (`DEBOUNCE_WINDOW` = 600ms) folds the flurry of fs events (incl. `node_modules` / build noise under the recursive cwd watch) into a single re-scan that emits `termory:sources-changed`. Net: **no manual per-source / per-file refresh is needed** — everything Termory surfaces lives under a watched path. **CPU: the `termory:sources-changed` emit is gated on main-window visibility** (`app_handle.get_webview_window("main").is_visible()`) — while the window is hidden (close-to-tray, a menu-bar app's common state) the re-scan still runs (the tray's `refresh_recent` needs it) but the full `ScanResult` is NOT serialized + sent to a frontend nobody's looking at. The frontend re-scans on window **focus** (`App.tsx` `getCurrentWindow().onFocusChanged` → a **silent** `scan_all_sessions` + `applyScanResult`, NOT `refresh()` — focus fires on every activation, so don't toggle the global `loading` or the Stats refresh indicator flashes; mirrors the sources-changed handler), and the next emit once visible is the fallback, so data is fresh whenever the window is shown. (During active CLI use the dominant per-scan cost is re-parsing the one changed session JSONL — mtime-cached so only the active file re-parses — plus this emit; the visibility gate removes the emit half whenever the window's hidden.)
- ~~Frontend test baseline~~ — done. 368 Vitest tests across 30 files.
  - **Pure helpers** (`src/lib/`): `session-utils`, `format`, `stats-utils` (the 4 module aggregators `overviewKpis`/`dailyActivity`/`dailyTokens`/`dailyModelTokens` + shared `resolveRange`/`filterSessions`), `favorites`, `provider-utils` (incl. `isManagedOptionKey` — mirrors the Rust `override_key_is_managed`), `search-utils` (`splitSnippet` + `matchingLoweredIndices`), `highlight-term` (rehype `<mark>` tree walk), `set-utils` (`addSetValue`/`toggleSetValue`).
  - **Hooks / components**: `usePersistentState`, `CopyMenu`, `FreshnessFooter`, `ListItemMenu`, `FavoritesPage`, `MessageList` (star wiring; `@tanstack/react-virtual` is `vi.mock`'d to bypass jsdom layout limits), `ProviderEditor` (duplicate/managed-key blocking, save trim/drop, OpenCode `{id,name}` models, Claude routing template), `GatewayEditor` (validate / detect+bind / save-shape), `GatewaysPage` (Codex follow prompt fires on a binding activate/deactivate via the `codexFollowForBinding` prop in both directions, not on custom→custom; delete-cleanup keeps the gateway + toasts on a failed deactivate), `CodexFollowDialog` ("Activate & keep" disabled until a project is selected), `CommandPalette` (filter / select incl. `first_match_index` + query passthrough / ⌘K-only shortcut ownership / Records-card row layout / pinned view-all-results bridge), `TranscriptFindBar` (counter / nav / Esc / focusNonce), `DocDetailView` (occurrence nav + wrap-around), `ModelCombobox` (aria-label reachability, free-text `onValueChange`, suggestion select/dedup/filter), `ProviderCard` (gateway-binding card hides Edit/Delete), `StatsPage` (view tabs + All/30d/7d + source pills + refresh wiring), `i18n` (the three locale key sets are identical).
  - **Convention reminders**: components using shadcn `Tooltip`/`HoverCard` are wrapped in `<TooltipProvider>` via a local `render` helper; `useI18n()` falls back to English with no provider so tests assert the English strings; jsdom needs local `ResizeObserver` / `scrollIntoView` shims for `cmdk`-based components (kept inside the test file, NOT in shared setup).

### P3 — nice to have

- ~~New-item badges~~ — explicitly excluded; the freshness footer ("Synced 2m ago") is enough passive feedback, an unread/red-dot system isn't desired here.
- ~~Starred messages~~ — done. See the "Favorites" section above. Per-message star (not per-session) with full-snapshot storage in `~/.termory/favorites.json` and a dedicated rail destination.
- **Export session** — single session → markdown / PDF, surfaced via the detail header's existing action row.
- **Starred sessions / tags** — virtual "Starred" source in sidebar; custom labels per record (orthogonal to per-message favorites — favorites snapshot a message body, tags label the session as a whole).
- ~~Remaining migrate/delete + project gaps for the other CLIs~~ — **DONE for all five CLIs.** Migrate + delete (session/project, + memory where the CLI has deletable memory) are complete for Claude / Codex / Gemini / OpenCode / Grok — see "First-class projects, migration & deletion" (mechanisms) and "Migrate (Claude + Codex + Gemini + OpenCode + Grok)". Per-approach: Claude = folder subtree move; Codex = rollout `payload.cwd` + `threads.cwd` rewrite (table is a backfill cache); Gemini = project-level `.project_root` marker rewrite (registry self-heals; per-session/memory deliberately skipped — sessions are project-scoped); OpenCode = `UPDATE session.directory` (durable table, `project_id` left intact); Grok = session-dir move + `summary.json` cwd rewrite. **SQLite-write safety pattern:** RW open with a 1.5s `busy_timeout`; a locked DB → "database is locked — quit the running {app}" (`map_db_locked`), primed by the confirm's `menu.exitCliHint`. Env-free `*_in` fns + IPC + a confirm flow that sends id / `{ project }` / `{ rel }` (never a raw path).

---
> Source: [chats-is/termory](https://github.com/chats-is/termory) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-07-18 -->
