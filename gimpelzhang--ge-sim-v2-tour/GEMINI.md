## ge-sim-v2-tour

> Project guide for Claude Code working in **GE-Sim-V2-tour** — a closed-loop VLA

# CLAUDE.md

Project guide for Claude Code working in **GE-Sim-V2-tour** — a closed-loop VLA
(video world-model + policy) simulation benchmark built on AgiBot's GE-Sim 2.0.
Read this before touching anything.

## What this repo is

A thin orchestration layer (in `scripts/` + `run_stage1.sh`) that drives a
**closed-loop simulation**: a video world model generates frames, a VLA policy
produces actions from those frames, and a remote VLM "World Judge" scores the
result against a task instruction. The heavy ML code lives in the **`gesim`
git submodule** (AgiBot's `GE-Sim-V2`), pinned to an upstream commit. We do
*not* modify submodule code except via a tracked patch (see "gesim submodule").

```
run_stage1.sh        one-shot entry: setup -> services -> preflight -> closed loop -> verify
scripts/             orchestration (Python + bash). ALL our code is here.
  env.sh             sourced in every shell; exports paths, CUDA, conda, MiMo config
  setup_gesim_submodule.sh   init submodule + apply our local patch (idempotent)
  launch_services.py start/probe world-model (HTTP :9000) + policy (WS :8000)
  run_stage1_eval.py 3-demo closed-loop rollout with in-loop MiMo judging
  vlm_reward_client.py MiMoRewardClient — our RewardClient impl (the World Judge)
  download_checkpoints_v2.py chunked Range downloader (resumable, anti-truncation)
  test_mimo_api.py   MiMo preflight (exit 0/1/2 semantics)
  verify_outputs.py  deliverable checks (exit 1 = fail)
  secrets.env         GITIGNORED — real API keys (HF_TOKEN, MIMO_API_KEY, GITHUB_TOKEN)
gesim/               third-party submodule (GE-Sim 2.0). Tracked only as a pointer.
patches/             our local modifications to gesim (Stage 1) + R2E2R (Stage 2), applied by setup scripts
outputs_stage1/      GITIGNORED — eval artifacts (videos, logs, reports), regenerated by re-running
run_stage2.sh        (Stage 2) one-shot entry: setup -> preflight -> Phase1 -> Phase2 -> verify
outputs_stage2/      GITIGNORED — R2E2R generated episodes, regenerated by re-running. See "Stage 2" below.
```

## Environment & hard constraints (READ FIRST)

These are user-mandated constraints. Violating them breaks the box.

- **GPU/driver is fixed at 580.159.03 (NVIDIA A800-SXM4-40GB, headless server).**
  NEVER change or reinstall the driver. The `gesim` env ships `torch 2.7.0+cu126`
  (CUDA 12.6 runtime bundled in the wheel) — it does **not** touch the system CUDA
  or driver. Do not install a different CUDA that could clobber the local 11.8.
- **Disk: `/` is small (~55G free). ALL installs/downloads go under `/mnt/robot/`.**
  `env.sh` already pins `HF_HOME`, `HF_HUB_CACHE`, `PIP_CACHE_DIR` to
  `/mnt/robot/.cache/...`. Conda envs live under `/mnt/robot/conda/envs/`.
  Never `pip install` into the base env or write large files to `/`.
- **NEVER develop in the `cc` conda env** (`/home/vipuser/.conda/envs/cc`). It is
  the default-active env in this shell — explicitly `conda activate gesim` (or the
  target env) before running anything. Create new envs under `/mnt/robot/conda/`.
- **Secrets are never hardcoded.** `MIMO_API_KEY`, `HF_TOKEN`, `GITHUB_TOKEN` live
  only in `scripts/secrets.env` (gitignored) and are loaded into the environment by
  `env.sh`. Access credentials are documented in
  `/mnt/genesis-world-tour-docs/user_access_methods.txt` (outside the repo) — do
  not print or commit them. `vlm_reward_client.py` reads keys from `os.environ`
  only; it never logs them.
- **Stay in `/mnt/GE-Sim-V2-tour/`** as the dev root.

### Conda envs (created at `/mnt/robot/conda/envs/`)

| env       | purpose                                  | used by                |
|-----------|------------------------------------------|------------------------|
| `gesim`   | world-model server, eval driver, torch   | launch_services (WM), run_stage1_eval |
| `openpi`  | π0.5 policy server (Physical-Intelligence)| launch_services (policy) |
| `autosim`, `lerobot-arena*` | other sims / arena   | (not part of Stage 1)  |

`launch_services.py` launches cross-env subprocesses via
`conda run -n <env> --no-capture-output` (falls back to `source activate`) and
sets `CUDA_VISIBLE_DEVICES=0` + `PYTHONPATH=gesim/openpi_serving` for each.

## The closed-loop architecture

```
EpisodeBundle.load(demo) ──► WorldModelEnv(reward=MiMoRewardClient)
        reset(bundle, conditioning="action")
            │
            ▼  (per step, ×8 per demo)
   OpenPIPolicy.infer(obs)  ──► actions (horizon,16)   [WS :8000, π0.5]
            │   compress_actions=True  ──► 25 actions/chunk (not per-frame)
            ▼
   env.step(actions)
      ├─► WorldModelEnv posts actions to HTTP :9000  ──► 25 frames  [GE-Sim 2B, 4 denoise steps]
      ├─► last frame = next obs
      └─► MiMoRewardClient.evaluate(head_frames, task) ──► RewardResult  [remote VLM, in-loop]
            │
   save_video(rollout_multiview.mp4, fps=16)
   MiMoRewardClient.evaluate_with_reasoning(...)  ──► reasoning + final score (post-rollout)
```

**Two services (started by `launch_services.py`, detached, PIDs in `outputs_stage1/logs/*.pid`):**
- **World-model server** — `gesim` env, `python -m gesim.server --model gesim_v2 --config
  configs/gesim_v2.yaml`, HTTP `:9000`, health at `GET /healthz`.
- **Policy server** — `openpi` env, `serve_policy_pi05.sh` (reads `OPENPI_CKPT`, `PORT=8000`),
  WebSocket `:8000`. **No HTTP health endpoint** — probed via a one-shot
  `WebsocketClientPolicy.reset()` round-trip (model load ~30-60s).

**Three demos**: `demo_000`, `demo_001`, `demo_002` (assets in `gesim/assets/`).

## The World Judge: MiMoRewardClient

`scripts/vlm_reward_client.py` is our realization of GE-Sim 2.0's "World Judge".
The upstream repo exposes only the `RewardClient` Protocol
(`gesim/src/gesim/rewards/base.py`: `evaluate(head_frames, task) -> RewardResult` at line 28,
with `success`/`progress` as `(T,)` float32). We supply the backend: Xiaomi **MiMo
`mimo-v2.5`** omni-modal VLM over an OpenAI-compatible API
(`MIMO_BASE_URL=https://token-plan-cn.xiaomimimo.com/v1`). It is remote → costs **no
local GPU**; the A800 only runs world-model + policy.

- `evaluate()` is the Protocol method, called **in-loop by every `env.step`**
  (3 demos × 8 steps = 24 calls) → per-frame `success`/`progress`.
- `evaluate_with_reasoning()` is a non-Protocol extension, called once per demo
  post-rollout → `{success, completion_score, reasoning, judge_source}` for the report.
- Module-load `assert isinstance(MiMoRewardClient(), RewardClient)` enforces the Protocol.
- **Fallbacks (never raises — must not abort the loop):** no `MIMO_API_KEY` →
  heuristic frame-diff (`judge_source="Heuristic-Fallback (No API Key)"`);
  network/5xx error → heuristic with `[Fallback-NetErr]` tag; 4xx API error → 0.0.
- **`max_tokens: 2048`** is deliberate and load-bearing: MiMo emits a chain-of-thought
  `reasoning_content` before the final JSON; the old 256 limit let the CoT eat the whole
  budget so `content` came back empty. Do not lower it.

## gesim submodule (reproducibility model — read before touching gesim)

`gesim/` is a git submodule pointing to **`github.com/AgibotTech/GE-Sim-V2.git`** at an
upstream-fetchable commit. We have exactly **one** local modification — disabling
`sparge_attention` (the `spas_sage_attn` kernel is not on PyPI; the model still runs
via `F.scaled_dot_product_attention`, just slower — plan risk #2). Because we cannot
push to AgiBot's repo, the fix is stored as a **tracked patch** + a setup script:

- **`patches/gesim-disable-sparge-attention.patch`** — the diff.
- **`scripts/setup_gesim_submodule.sh`** — runs `git submodule update --init --recursive`
  then applies the patch **idempotently** (skips if `sparge_attention: false` already present).
  This is `run_stage1.sh` step `[0/5]`.

**Consequences for git status:** after setup, `gesim` shows `(modified content)` —
that is the uncommitted patch in the submodule's working tree. **This is expected and
correct.** The parent repo only tracks the pointer commit, not the patched content.
Running a bare `git submodule update` reverts the patch — re-run `setup_gesim_submodule.sh`
to restore it.

When extending gesim: prefer another patch + setup-script step over committing into the
submodule. Never change `.gitmodules` to a private fork URL without confirming the fork is
pushed and durable.

## How to run

```bash
# 1. one-shot (does setup + services + preflight + loop + verify)
bash run_stage1.sh

# 2. step by step
source scripts/env.sh                    # every shell — paths, CUDA, conda, MiMo, secrets
conda activate gesim
bash scripts/setup_gesim_submodule.sh    # init submodule + patch (idempotent)
python scripts/launch_services.py --timeout 600    # start :9000 + :8000
python scripts/test_mimo_api.py          # preflight (0=ok no-key, 1=blocked, 2=ok with-key)
python scripts/run_stage1_eval.py --steps 8         # 3-demo closed loop
python scripts/verify_outputs.py         # deliverable checks
# cleanup: kill $(cat outputs_stage1/logs/{world_model,policy}.pid)
```

Checkpoints (~12GB) are downloaded by `scripts/download_checkpoints_v2.py` into
`gesim/checkpoints/` (gitignored). It uses **chunked Range requests** (8MB) and
**appends only on HTTP 206**, rejecting 200 responses — this avoids the truncation
bug that `curl -C -` hit when the CDN ignored Range. Resumes from local file size.

## Conventions

- **Python style:** `from __future__ import annotations` first; explicit `Path` for
  filesystem; type hints on signatures; module docstrings explain *why*, not just *what*.
- **Fail-safe by design:** the closed loop must never abort on a flaky remote (MiMo).
  Catch network/5xx → heuristic fallback; only 4xx API logic → 0.0.
- **Transparency in reports:** `judge_source` always records who scored (MiMo vs heuristic
  fallback). `outputs_stage1/stage1_eval_report.md` carries a "判分透明声明" header.
- **Outputs are not version-controlled.** `outputs_stage1/`, `*.mp4`, `*.log`, `*.pid`,
  `checkpoints/`, `reward.npy` are all gitignored. They are regenerated by re-running the eval.
- **Commit hygiene:** commit messages are imperative + body explains *why*; end with
  `Co-Authored-By: Claude <noreply@anthropic.com>`. Do not commit secrets, large artifacts,
  or `.omc/` (OMC state).
- **Shell cwd gotcha:** background/parallel Bash calls here have reset the cwd to `gesim/`
  before. Use absolute paths (`/mnt/GE-Sim-V2-tour/...`) or `git -C <dir>` — do not rely on
  a cwd persisting across calls.

## Stage 1 results (last run)

3 demos × 8 steps × ~25 frames. Full analysis in
`/mnt/GE-Sim-V2-tour-docs/Complete_Stage_1.md` (retrospective + pitfalls) and
`/mnt/GE-Sim-V2-tour-docs/Stage1_Simulation_Time.md` (frame-rate profiling).
Wallclock closed-loop rate ≈ **0.88 fps** with in-loop MiMo (~18× slower than real-time);
pure sim+VLA ≈ **3.2 fps**; world-model throughput ≈ **4.3 fps**. π0.5 is action-chunking
(50 actions per `infer()`, compressed to 25) — not called per-frame.

## Stage 2: Real2Edit2Real data generation (COMPLETE)

Stage 2 runs Agibot's **Real2Edit2Real (R2E2R)** toolchain to generate edited
robotic demonstration episodes via 3D scene editing + spatial augmentation
(curobo IK). It is **fully additive** - new scripts/configs/patch/.gitignore
entries only; no Stage 1 file was touched. Full retrospective (pitfalls, root
causes, fixes) at `/mnt/GE-Sim-V2-tour-docs/Complete_Stage_2.md`.

```
run_stage2.sh        one-shot entry: setup -> preflight -> Phase1 -> Phase2 -> verify
scripts/             Stage 2 orchestration (added alongside Stage 1 scripts)
  r2e2r_env_wrapper.sh  THE env wrapper: CUDA_HOME->env 12.1, system gcc 11.4,
                        no_proxy="*", HF_HUB_OFFLINE=1, EGL, secrets. Source per shell.
  r2e2r_env.sh          Stage 2 env (inherits Stage 1 env.sh)
  setup_r2e2r.sh        idempotent: clone R2E2R + create r2e2r env + weights
  setup_r2e2r_env.sh    one-command env setup [1-6/6] (libmamba solver)
  setup_r2e2r_submodule_patch.sh  apply the ARK inpaint patch (idempotent, marker-based)
  download_r2e2r_weights.py   chunked Range downloader (reuses Stage 1 anti-truncation)
  preflight_stage2.py   environment self-check (exit 0 = ready)
  run_phase1_debug.py   Phase 1 smoke test (lift_box example data)
  run_phase2_batch.py   Phase 2 batch driver (calls R2E2R native generate_demo.sh)
  verify_and_report_stage2.py  deliverable checks + report (exit 1 = fail)
  render_stage2_videos.py  OPTIONAL final-video render (Cosmos): shadow-symlink
                          redirect to /home/vipuser + render/verify subcommands
  check_gpu_stage2.py / check_headless.py / check_r2e2r_env.py / check_agibotworld_gate.py
  configs/phase2_batch50.yaml  R2E2R-native config: n_gen_per_source=50, mode=full_random
  configs/phase2_video_render.yaml  video-render config: absolute output_root under
                          /home/vipuser (redirects writes off the full /mnt disk)
patches/
  r2e2r-update-inpaint-model.patch  ARK: seededit->seedream-5-0-pro, b64_json, SIGALRM, size>=960
  r2e2r-video-render-oom-fix.patch  infer script: resume-skip + per-clip gc/cuda cache (fix OOM-kill at clip 6)
Real2Edit2Real/       third-party R2E2R clone at /mnt/robot/Real2Edit2Real (NOT a submodule,
                      NOT under repo root - patched via tracked patch, like gesim)
outputs_stage2/       GITIGNORED - generated episodes, regenerated by re-running
```

### Conda envs (added at `/mnt/robot/conda/envs/`)

| env       | purpose                                  | used by                |
|-----------|------------------------------------------|------------------------|
| `r2e2r`   | R2E2R toolchain: py3.10 + CUDA 12.1 + curobo | run_stage2 (all phases) |

`r2e2r` pins **CUDA Toolkit 12.1.0** (conda nvidia channel, isolated from system
11.8) + **torch 2.5.1+cu121**. It is treated as **read-only after creation** -
never run a bare `conda install` into it (the classic solver is unusably slow and
conda-forge gcc deps try to upgrade CUDA 12.1->13.x). The host compiler is
**system gcc 11.4** (`/usr/bin/gcc`), NOT conda gcc. `r2e2r_env_wrapper.sh`
encodes this: `CUDA_HOME=$CONDA_PREFIX` (env's 12.1) + `CC=/usr/bin/gcc`.

### The R2E2R pipeline

```
source demo (lift_box) ──► preprocess_demo.sh
   VGGT (14GB, metric_vggt_cotrain.pth) 3D recon ──► pointcloud + camera params
   GroundingDINO + SAM2 ──► object/arm/desktop masks
   ARK doubao-seedream-5-0-pro (b64_json, no TOS CDN) ──► inpaint occluded regions
        │  12/12 calls, 0 retries (patch: SIGALRM 150s + size>=960x960 + 6 retries)
        ▼  generate_demo.sh  (configs/phase2_batch50.yaml)
   spatial edit: trans_range ±20cm + rot_range ±30°  (mode: full_random)
   curobo FK/IK batch ──► reprojected trajectories (relax_thresholds filters IK failures)
        │  n_gen_per_source=50  (R2E2R native batch - NO custom sampler/loop)
        ▼  per episode: outputs_stage2/phase2_batch_50/source_0_trans_NNNN/
   aligned_joints.h5 (NESTED schema, NOT flat (T,16)) + depthmap.npz + depthmap_canny.npz
        + {head,hand_left,hand_right}_frame.png + intrinsics/extrinsics JSON + meta_info.json
```

### Stage 2 gotchas (full detail in Complete_Stage_2.md)

- **ARK inpaint was the #1 risk.** Upstream `doubao-seededit` is deprecated; the
  fix is `doubao-seedream-5-0-pro-260628` + `response_format="b64_json"` (image
  inline, bypasses the flaky TOS CDN) + SIGALRM 150s hard timeout + size floor
  960x960 (auto upscale/downscale to preserve alignment). All in
  `patches/r2e2r-update-inpaint-model.patch`.
- **CUDA-pinned conda env is fragile.** Use libmamba + system gcc (not conda
  gcc); env is read-only after creation.
- **`no_proxy="*"` + `HF_HUB_OFFLINE=1` are load-bearing** - the local
  quickqservice proxy (127.0.0.1:10900) hangs on long ARK/aria2c calls.
- **H5 schema is nested** (`/state/joint/position: (580,14)`, `/action/{left,right}_effector/position`),
  NOT flat `(T,16)` - the original plan's errata predicted this correctly.
- **Use R2E2R's native batch** (`n_gen_per_source` + `mode: full_random`), not a
  custom 5x10 sampler loop.
- 4/50 episodes fail curobo IK under `relax_thresholds` - this is expected, not
  a bug. Threshold: ≥40 (actual: 46).
- **Video render: `pkg_resources` missing (坑 M).** `setuptools 83.x` removed
  `pkg_resources`; `imageio-ffmpeg` (via `moviepy`) imports it at load. Fix:
  `pip install --no-deps "setuptools==80.10.2"` (CUDA-safe, pure-Python).
- **Video render: writes hardcoded into the full /mnt disk (坑 N).** Redirect via
  shadow file-level symlinks to `/home/vipuser` + absolute `output_root` config.
- **Video render: OOM-kill at clip 6's save_video (坑 O).** The `save_video` step
  spikes RSS ~12GB (rearrange + clamp copy the full ~4GB video tensor) on top of the
  resident model + depth/canny - an in-process peak that `gc` after the save cannot
  prevent. Fix (patch + orchestrator): `--single_clip` mode + `render_per_clip`
  driver runs each clip in a **fresh subprocess** so the OS reclaims all memory
  between clips (resume-skip + post-save gc also added, but per-clip subprocess is
  the load-bearing fix). Driver: `render_stage2_videos.py render_per_clip`.

### How to run

```bash
# one-shot (does setup + preflight + Phase1 + Phase2 + verify)
bash run_stage2.sh
# step by step: source scripts/r2e2r_env_wrapper.sh, then preflight_stage2.py,
# run_phase1_debug.py, run_phase2_batch.py, verify_and_report_stage2.py
```

### Stage 2 results (last run)

46/50 edited episodes from 1 source demo (lift_box). Overall **PASS**
(`verify_and_report_stage2.py`). Report at `outputs_stage2/stage2_verification_report.md`
(判分透明声明 header: Stage 2 verification is form-checking only - file existence,
size, H5 schema - it does NOT assign task success scores; episode quality is
determined by the upstream R2E2R generation pipeline). Committed on
`feature-stage2` (`72c8df9`).

### Stage 2 optional: final-video render (Cosmos)

`generate_demo_video.sh --save_video` renders the final RGB rollout videos for the
46 episodes using **Cosmos-Predict2-2B-Video2World** (the same video-diffusion world
model the closed loop uses, run offline here on the edited episodes). It is **not**
part of the core Stage 2 deliverable (the episodes + H5 trajectories are) - it is an
optional visualization. Driver: `scripts/render_stage2_videos.py`
(`prepare` -> `render_per_clip` -> `verify`). Use `render_per_clip` (one subprocess
per clip), NOT `render` (in-process batch OOM-kills at clip 6 - see 坑 O).

Two load-bearing pitfalls (full detail in Complete_Stage_2.md 坑 M/N):

- **`pkg_resources` missing at import (坑 M).** `r2e2r` shipped `setuptools 83.x`,
  which **removed `pkg_resources`**; `imageio-ffmpeg` (imported by `moviepy` <->
  `agibotworld_dataset`) does `from pkg_resources import resource_filename` at module
  load -> the render crashed before reaching the GPU. Fix: `pip install --no-deps
  "setuptools==80.10.2"` in the `r2e2r` env (pure-Python, last version with
  `pkg_resources`; does NOT touch CUDA/torch). This is the ONE documented exception to
  "r2e2r env is read-only after creation" - it is a compatibility shim, not a solver run.
- **Hardcoded output path + /mnt disk pressure (坑 N).** The infer script writes the
  final mp4 + ~1737 JPEG frames/episode back into each `input_root/clip/` dir, and
  `input_root = dirname(output_root)`. The real episodes live on `/mnt` (`/dev/vdb`,
  6.6G free at render time) -> ~4-9GB of new artifacts would fill it. Fix: a **shadow
  directory tree** under `/home/vipuser/r2e2r_videos/lift_box/` - each `source_0_trans_NNNN/`
  is a real dir containing **file-level symlinks** (not a dir symlink) to the 12 real
  data files on `/mnt/robot`. New writes land on `/home/vipuser` (55G free); reads follow
  the symlinks. Driven by `configs/phase2_video_render.yaml` (absolute `output_root`).

Output: 384×1536 (3 camera views side-by-side), 30fps, ~19s/episode, H.264. Renders at
~16s/chunk × 24 chunks/episode ≈ ~6 min/episode on the A800 (~5hr for 46). Videos live
under `/home/vipuser/r2e2r_videos/` (outside the repo, NOT committed). Report at
`outputs_stage2/stage2_video_render_report.md` (判分透明声明: form-checking only -
file existence + size, NOT visual quality).

## Stage 3: R2E2R -> GE-Sim 2.0 batch closed-loop generalization eval (COMPLETE)

Stage 3 loads the 46 Stage-2 spatial-generalization episodes into GE-Sim 2.0
`WorldModelEnv` via an in-memory format bridge, runs a closed-loop rollout with
the π0.5 policy per episode (10 steps), and scores each with `MiMoRewardClient`
(the Stage-1 World Judge). It then analyzes π0.5's generalization across XY
translation magnitude/direction and rotation. **Fully additive** - 8 new files
(5 scripts + `run_stage3.sh` + `.gitignore`/`CLAUDE.md` additions, 1615 lines),
no Stage 1/2 file touched. Detailed plan at
`/mnt/GE-Sim-V2-tour-docs/Stage3_Plan_Detailed.md`; **full retrospective
(pitfalls 坑 A-J, root causes, fixes) at
`/mnt/GE-Sim-V2-tour-docs/Complete_Stage_3.md`**.

```
run_stage3.sh        one-shot entry: setup -> verify inputs -> launch services
                     -> probe bridge -> batch eval -> report -> verify
scripts/
  convert_r2e2r_to_bundle.py   Phase A: R2E2R nested H5/JSON -> gesim EpisodeBundle
                               adapter (in-memory assembly + 4 small staging .npy) + --probe mode
  verify_stage2_inputs.py      Phase A: enumerate 46 episodes (no range(50)),
                               validate 11 files + H5 keys + offset distribution
  run_stage3_batch_eval.py     Phase B: 46-ep batch closed-loop driver; resume-skip,
                               smart visualization (sample every 5 + force-save
                               failures), incremental JSON write, fail-safe
  generate_stage3_report.py    Phase C: XY distance / dx / dy / rotation / failure-
                               mode analysis -> JSON + Markdown (判分透明声明)
  verify_stage3_outputs.py     Phase C: deliverable assertions (form-checking only)
outputs_stage3/      GITIGNORED - lightweight report copies; large artifacts under
                     /home/vipuser/stage3_outputs/ (NOT committed)
```

### The format bridge (the #1 technical risk)

gesim `WorldModelEnv.reset(episode, conditioning="action")` only accepts an
`EpisodeBundle` (Stage-1 flat `.npy` layout). R2E2R episodes use a **completely
different disk schema** (`aligned_joints.h5` nested + camera JSONs + PNGs), and
there is **no** `env.reset(episode_path=...)` API. `convert_r2e2r_to_bundle.build_bundle()`
assembles an `EpisodeBundle` in memory (intrinsic from JSON, extrinsic 4×4 from
JSON, first_frame from 3 PNGs, initial_state from H5 `/state/joint/position[0,:14]`
+ grip from **`/action/{left,right}_effector/position[0,0]`** [normalized command,
NOT `/state/` encoder counts], head_waist from H5 head+waist; `actions`/`eef_poses`
are `None`). The staging-dir disk-read caveat is documented below in 坑 A.

### Stage 3 gotchas

- **Task text feeds THREE coupled consumers (坑 B, the #1 hidden trap).**
  `SOURCE_TASK_TEXT` (in `convert_r2e2r_to_bundle.py`) is the Stage-2 **lift_box**
  task (`"Pick up the box on the table with both arms and lift it."`), NOT the
  Stage-1 kettle-pouring task. This ONE constant drives (1) the π0.5 policy prompt
  (`obs.task` -> server `"prompt"`), (2) the WM `set_task`, and (3) the MiMo judge
  criterion. The first run used the wrong (kettle) text -> 0% OSR was a
  task-criterion artifact (MiMo saw box scenes but judged kettle-pouring). If OSR
  is 0%, first check the task text matches the scene, not the model strength.
  The `pi05_gesim_g01op_test` checkpoint was trained on Stage-1 demos (kettle/
  facial-cleanser/towel), NOT lift_box - so the corrected 6.5% OSR is cross-task
  generalization to an untrained manipulation, not same-task spatial generalization.
- **Staging dir (坑 A, the #1 technical risk):** even on the `conditioning="action"`
  path, `PolicyBandRenderer.__init__` reads `state_joints_0.npy` + `state_eef_poses_0.npy`
  + `intrinsic.npy` + `extrinsic_alignstate_0.npy` **from disk via `bundle.path`**.
  R2E2R dirs lack these, so `build_bundle(staging_dir=...)` writes the 4 small
  `.npy` files (synthesized from H5; ~1KB) per episode. The plan's claim that
  "recorded trajectory fields are unused" was partially wrong - `actions`/`eef_poses`
  are unused, but `state_joints`/`state_eef_poses` row-0 are read from disk.
- **46 episodes, non-contiguous** `source_0_trans_NNNN` (missing 0004/0022/0028/0047
  - curobo IK failures). **Never `range(50)`** - glob the real dirs.
- **Z=0 for all 46** (`trans_range` pins Z=0, `dz≡0`) - the report does **not**
  include Z-axis/table-height analysis (the original plan's §2.2 was wrong).
- **`obj_rot_angle` is stored unwrapped** (rad, range ~[-1094°,+1004°]); the
  report normalizes via `((a%(2π))+2π)%(2π)` then takes minimal circular distance.
- **Frame resolution 518×294** (R2E2R native) vs Stage 1's 512×384: `--probe`
  verifies the WM server accepts 518×294; if not, falls back to resizing the 3
  PNGs to 512×384 + scaling intrinsics proportionally (`STAGE3_TARGET_WH=512,384`).
- **Output redirect**: `/mnt` is 98% full - ALL Stage-3 outputs (videos +
  `raw_eval_results.json` + reports) go to `/home/vipuser/stage3_outputs/`.
- **`no_proxy="*"` is load-bearing for Stage 3**: the localhost quickqservice
  proxy (127.0.0.1:10900) 503s on direct `127.0.0.1:9000` hits and hangs long
  calls. `run_stage3.sh` exports it; gesim's `requests`/`urllib` clients then
  bypass the proxy. (Same trap as Stage 2's `no_proxy="*"` + `HF_HUB_OFFLINE=1`.)
- **`launch_services.py` policy probe hangs** when the policy server is down
  (`WebsocketClientPolicy.reset()` has no short timeout) - it can stall the
  launcher for the full 600s before starting anything. If reusing it, start the
  WM server first and confirm health, then start policy separately.
- **MiMo is the long pole**: ~`max_steps` in-loop calls + 1 reasoning call per
  episode × 46 = several hundred remote VLM calls; accepted per the user's
  "execute without compromise, even if long wait times" constraint.
- **`h5py` not in the gesim env (坑 F)** - it's only in `r2e2r`. The ONE gesim-env
  package add for Stage 3: `pip install --no-deps h5py` (CUDA-safe, numpy-only;
  torch 2.7.0+cu126 + numpy 1.26.4 untouched). `scipy`/`Pillow` already present.
- **grip source (坑 G):** use H5 `/action/{left,right}_effector/position[0,0]`
  (normalized command {0,0.07,...}), NOT `/state/*_effector/position` (raw encoder
  counts ~35-106) - the g01 policy 16-D state layout expects normalized grip.
- **`python scripts/foo.py` sys.path (坑 E):** puts `scripts/` not repo root on
  `sys.path[0]`, so `from scripts.X import` fails. Stage 3 scripts self-bootstrap
  `sys.path.insert(0, repo_root)` at top.
- **`conda run` service kill (坑 I):** pid files record the `conda run` wrapper,
  not the child `gesim.server`/`serve_pi05`. Kill the real child PID via
  `ps aux | grep -E "gesim.server|serve_pi05" | grep -v grep`; confirm with
  `nvidia-smi` (not `pgrep` - false positives).

### How to run

```bash
# one-shot (does setup + verify inputs + launch services + probe + batch eval + report + verify)
bash run_stage3.sh
# step by step: source scripts/env.sh, conda activate gesim, export no_proxy="*",
#   verify_stage2_inputs.py, (launch WM :9000 + policy :8000), convert_r2e2r_to_bundle.py --probe,
#   run_stage3_batch_eval.py --max_steps 10, generate_stage3_report.py, verify_stage3_outputs.py
# Debugging tip: launch services manually (WM first, confirm /healthz, then policy) to avoid the
# launch_services.py policy-probe hang (坑 C).
```

### Stage 3 results (last run)

46/46 episodes evaluated, 0 errors, all judged by real MiMo VLM (0 fallback).
`verify_stage3_outputs.py` Overall **PASS**. OSR **3/46 = 6.5%**, mean completion
**0.211**, 31/46 non-zero scores, 43 videos. Report at
`/home/vipuser/stage3_outputs/stage3_generalization_report.md` (判分透明声明 header:
judging reuses Stage-1 `MiMoRewardClient` in-loop; Z-axis NOT analyzed since
`dz≡0`; error episodes count in the denominator, not inflating SR). Raw results at
`/home/vipuser/stage3_outputs/raw_eval_results.json`; videos under
`/home/vipuser/stage3_outputs/visualizations/` (outside the repo, NOT committed).

The 3 successes (`source_0_trans_0019`/`0039`/`0042`, scores 0.90/1.00/1.00) all
fall in the **≥10cm XY-offset** bucket (8.8% SR there) - counterintuitively, larger
displacements did better than small ones. Dominant failure mode "gripper" (18/43):
the robot reaches the box but fails to grasp with both arms. Because the
`pi05_gesim_g01op_test` checkpoint was trained on Stage-1 demos (kettle/
facial-cleanser/towel), NOT lift_box, the 6.5% OSR is **cross-task generalization
to an untrained manipulation** under the correct (lift_box) criterion - an honest
finding, not a judging artifact. Committed on `feature-stage3` (`6d09122`).

## Stage 4: RL fine-tune π0.5 in the world model (lift_box) (COMPLETE)

Stage 4 fine-tunes the π0.5 policy via **reward-weighted flow-matching regression
(RWR)** inside the GE-Sim 2.0 world model, scored by the MiMo World Judge, to
raise the lift_box success rate that Stage 3 measured at 6.5% OSR. It is
**fully additive** - 8 new files (7 scripts + `run_stage4.sh`, 1924 lines) +
`.gitignore` entries, no Stage 1/2/3 file touched (verified:
`git diff 13fc916..aefd737 --stat` = 10 files, 2058 insertions, 0 deletions).
Detailed plan at
`/mnt/GE-Sim-V2-tour-docs/Stage4_Plan_Detailed.md`; **full retrospective
(pitfalls 坑 R-CC with symptom/root-cause/fix/verification, debugging
chronicle with the real smoke-test error sequence, step-by-step reproduction
guide for agents, failure-mode triage table) at
`/mnt/GE-Sim-V2-tour-docs/Complete_Stage_4.md`** - read its §6 pitfall
catalog and §7 reproduction guide BEFORE writing any Stage-4-adjacent code.

The original plan proposed REINFORCE+Gaussian+PEFT, but source verification
(Plan §0 errata E1-E9) showed π0.5 is a flow-matching diffusion policy (no
log_prob), PEFT is unusable (custom `nn.Module`), and gradients can't cross the
WebSocket. Corrected: load π0.5 **in-process**, freeze the backbone, fine-tune
the 4 action-expert small-head modules (`action_in_proj`/`action_out_proj`/
`time_mlp_in`/`time_mlp_out`, 2.16M params / 8.66MB), and weight the native
differentiable flow-matching loss `model.forward(obs, actions)` by trajectory
reward.

```
run_stage4.sh        one-shot: split -> WM -> 60-iter train -> test eval -> report -> verify
scripts/
  split_stage4_dataset.py        36 train + 10 test (hardcoded from Stage-3 scores, no range(50))
  launch_stage4_wm.py            WM-only launcher (direct python, NO conda-run wrapper - 坑 I;
                                 no policy probe - 坑 C)
  openpi_finetune_head.py        trainer core: load_pi05_inprocess (坑 U/V),
                                 setup_finetune (freeze backbone + 4 small-head modules - 坑 S),
                                 flow_matching_loss (RWR - 坑 T), compute_reward, ckpt save/load
  run_stage4_rl_train.py         main 60-iter RWR loop (resume-skip, incremental JSON, fail-safe)
  run_stage4_test_eval.py        10-ep greedy eval (smart vis, resume-skip, s3 baseline comparison)
  generate_stage4_report.py      JSON + MD (判分透明声明)
  verify_stage4_outputs.py       form-checking (exit 1 = fail)
outputs_stage4/ -> /home/vipuser/stage4_outputs/   (GITIGNORED, /mnt is 98% full)
  stage4_dataset_split.json, checkpoints/{iter_020,iter_040,final}/, logs/rl_training_curve.json,
  test_eval/raw_eval_results.json + visualizations/, stage4_rl_report.{json,md}
```

### Conda env (added)

| env | purpose | used by |
|-----|---------|---------|
| `openpi` | RL trainer + π0.5 in-process (jax/flax/transformers/h5py/torch 2.7.1; imports gesim via `gesim/src` on PYTHONPATH) | run_stage4 (train + test eval) |

The trainer runs in the **openpi env** (NOT gesim) because it needs openpi's
`create_trained_policy` + `PI0Pytorch` + the `pi05_gesim` transforms, while also
importing gesim's `WorldModelEnv` (added `gesim/src` to PYTHONPATH). The ONE
documented openpi-env addition: `pip install --no-deps matplotlib` + its
pure-Python runtime deps (cycler/contourpy/kiwisolver/pyparsing/fonttools) -
gesim imports matplotlib at load. **NOT peft** (unusable - 坑 S).

### The RWR algorithm (corrected from REINFORCE)

π0.5 is flow-matching: `forward(obs, actions)` returns `F.mse_loss(u_t, v_θ)` on
the velocity field (differentiable, no log_prob). `sample_actions` is
`@torch.no_grad` 10-step Euler (no density). So RWR reuses the native loss:
```
R(τ) = MiMo completion_score (with action-smoothness penalty)
w(τ) = softmax((R - b) / temp)   over the trajectory's steps
loss = Σ w_t · model.forward(obs_t, a_t_taken).mean()   # a_t in model layout, normalized
backward -> clip_grad(1.0) -> AdamW.step (small head only) -> empty_cache
b ← 0.9·b + 0.1·R   (EMA baseline)
```
`flow_matching_loss` re-runs the policy's full transform chain (`GesimInputs` +
`Normalize` + tokenizer) on the recorded obs, reorders actions WM->model layout
(`_wm_to_model_layout`), re-normalizes via the extracted `Normalize` transform,
and calls `model.forward()` inside `disable_typechecking()`. Exploration noise
`N(0,0.05)` is added to `sample_actions` output in WM layout BEFORE `env.step`
(affects data collection only, not the loss).

### Stage 4 gotchas (full detail in Complete_Stage_4.md)

- **坑 R (JAX prealloc OOM, head risk):** `import jax` preallocates ~75% GPU
  (~30G) -> OOMs the WM server (13G). `export XLA_PYTHON_CLIENT_PREALLOCATE=false`
  BEFORE any `import jax` (in run_stage4.sh + module heads). In-process π0.5 =
  7.6G with the flag.
- **坑 S (PEFT unusable + wrong module names):** `PI0Pytorch` is a custom
  `nn.Module` (not HF PreTrainedModel); `peft` not installed; `action_head`
  doesn't exist. Real modules: `action_in_proj`/`action_out_proj`/`time_mlp_in`/
  `time_mlp_out`. Fix: freeze backbone + open those 4 Linears (2.16M params).
- **坑 T (flow-matching not Gaussian):** REINFORCE log_prob invalid. Use RWR.
- **坑 U (gradients can't cross WS):** trainer loads π0.5 in-process via
  `create_trained_policy` (reuses `serve_pi05.py:39-44`); policy WS server NOT
  started during training.
- **坑 V (DEFAULT_PROMPT is kettle):** `pi05_gesim.py:34` DEFAULT_PROMPT is still
  kettle. `load_pi05_inprocess` passes lift_box explicitly; asserted in trainer.
- **坑 Z (new, ascontiguousarray on 0-d masks):** `np.ascontiguousarray(np.True_)`
  promotes 0-d to (1,), breaking `embed_prefix`'s `img_mask[:, None].expand`.
  Fix: only ascontiguousarray when `arr.ndim > 0`; mirror `Policy.infer`'s exact
  conversion.
- **坑 AA (new, from_dict typecheck):** `Observation.from_dict` is `@at.typecheck`,
  expects NHWC but permutes torch.uint8 to NCHW. Call inside
  `with at.disable_typechecking()`.
- **坑 BB (new, gesim src/ layout):** PYTHONPATH must include `gesim/src`, NOT
  `gesim/` (gesim uses src/ layout; `gesim/` is the repo root, not package root).
- **坑 X (reward sparse + high variance):** RWR uses EMA baseline + softmax
  weights + action-smoothness penalty to handle per-trajectory MiMo variance.
- **no_proxy="*"** is load-bearing (same as Stage 2/3): the localhost
  quickqservice proxy 503s on 127.0.0.1:9000 hits.
- **Output redirect:** all Stage-4 outputs -> `/home/vipuser/stage4_outputs/`
  (`/mnt` is 98% full).

### How to run

```bash
# one-shot (does split + WM + 60-iter train + test eval + report + verify + cleanup)
bash run_stage4.sh
# step by step: source scripts/env.sh + scripts/secrets.env, export no_proxy="*"
#   XLA_PYTHON_CLIENT_PREALLOCATE=false, conda activate openpi (or conda run),
#   PYTHONPATH=gesim/openpi_serving:gesim/src:repo_root,
#   split_stage4_dataset.py, launch_stage4_wm.py start, run_stage4_rl_train.py,
#   run_stage4_test_eval.py, generate_stage4_report.py, verify_stage4_outputs.py
# Debugging: launch WM first (launch_stage4_wm.py start), confirm /healthz, then
# run the trainer. Kill WM via launch_stage4_wm.py stop (kills real gesim.server).
```

### Stage 4 results (last run)

60-iteration RWR training, then 10-episode greedy test eval (judged by real
MiMo, 0 fallback). Training: flow-matching loss dropped **2.055 -> 1.080** (min
0.710), EMA baseline rose **0.100 -> 0.716** (max 0.834), 43/60 high-reward
iters, **0 errors** across all iterations. Test eval: **OSR 8/10 = 80.0%** (up
from Stage-3's 3/10 = 30.0% on the same test set - **+50pp**); mean completion
score **s4=0.805 vs s3=0.46** (delta +0.345); 5 Stage-3 failures (0021/0017/
0029/0033/0037) now succeed. `verify_stage4_outputs.py` **PASS** (exit 0). Report
at `/home/vipuser/stage4_outputs/stage4_rl_report.md` (判分透明声明: form-checking
+ MiMo judging reuse; OSR/scores are measured not promised; Z-axis not analyzed
since `dz≡0`). Committed on `feature-stage4` (`906140b` scripts; `COMMIT` final).

## Stage 5: RoboColiseum online eval of the Stage-1 π0.5 policy (COMPLETE)

Stage 5 connects the Stage-1 π0.5 policy server (`pi05_gesim_g01op_test`) to
**RoboColiseum** - an online benchmark where the platform renders Isaac Sim
scenes + auto-judges, and our local inference agent dials OUT via a reverse
WebSocket tunnel (no public IP, no local Isaac Sim, no local GPU for the agent).
Two pre-created job boards were evaluated: **instruction** (job 1140) and
**manip** (job 1141). **Fully additive** - 11 new files + `run_stage5.sh`, no
Stage 1/2/3/4 file touched. Detailed plan at
`/mnt/GE-Sim-V2-tour-docs/Stage5_Plan_Detailed.md`; **full retrospective
(debugging chronicle, 坑 S5-1..S5-12 pitfall catalog, SKILL-vs-live-API
correction table for the `~/.claude/skills/challenge-*` skills, reproduction
guide for agents, failure-mode triage table) at
`/mnt/GE-Sim-V2-tour-docs/Complete_Stage_5.md`** - read its §2 (SKILL
participation) and §4 (pitfalls) BEFORE writing any Stage-5-adjacent code.
Human operations guide (progress-watching + results-viewing) at
`/mnt/GE-Sim-V2-tour-docs/Stage5_Human_Operations_Guide.md`.

```
run_stage5.sh        one-shot: env -> policy server -> mock test -> job check
                     -> (online eval gated on STAGE5_RUN_ONLINE=1) -> report -> verify
scripts/
  stage5_env.sh        env: sources env.sh, OUT_STAGE5=/home/vipuser/stage5_outputs,
                       no_proxy="localhost,127.0.0.1,::1" (NOT "*"), XLA prealloc off
  stage5_policy_client.py   WS client for the Stage-1 policy server (:8000), lock+retry
  stage5_adapter.py    THE core: TunnelClient (ported from ACoT-VLA), obs/action
                       wire-format adaptation (JPEG->RGB, 3 camera views, state16
                       concat(arm[14],gripper[2]), WM grip-interleaved -> grip-last
                       reorder, JOINT_ABS envelope .tolist()), obs-frame recording
                       (JPEG per step per camera + meta.jsonl), fail-safe no-op fallback
  stage5_mock_test.py  6 offline assertions (plain-msgpack round-trip, reorder, ...)
  stage5_check_jobs.py parse access file, login (POST JSON), map uuid->numeric job id,
                       persist creds to ~/.simubotix-challenge.env (0600, NO password)
  stage5_poll_result.py   poll /jobs + /result; snapshot results/final_<tag>.json
  generate_stage5_report.py  MD + JSON report (判分透明声明) + best-score/leaderboard
  render_stage5_obs_videos.py  stitch obs JPEGs -> per-session MP4 (3 views side-by-side)
  verify_stage5_outputs.py    deliverable checks (exit 1 = fail)
outputs -> /home/vipuser/stage5_outputs/   (GITIGNORED, /mnt is 98% full)
  results/final_job{A,B}.json, obs_frames/<tag>/<session>/*.jpg, obs_videos/<tag>/,
  logs/{agent,job,policy_server}*.log, stage5_robocoliseum_report.{md,json}
```

### Architecture (reverse tunnel)

The agent (one per job, no GPU) connects to `ws://120.92.88.78/api/challenge/
tunnel?job=<uuid>&agent=<id>` with `Authorization: Bearer <job_token>`. Control
frames are JSON text (`warmup`/`session_open`/`session_close`/`drain`); data
frames are binary `[uint32 BE sid_len][session_id][msgpack_numpy payload]`.
Each obs is a JSON-RPC `{"method":"infer","params":{images{head,hand_left,
hand_right}, states{arm_joint_states[14],gripper_states[2],waist_joint_states[5]},
prompt, task_name}}` -> we decode JPEGs, call the shared Stage-1 policy server
over WS, reorder actions, and reply with a `JOINT_ABS` envelope (horizon 50
instruction / 30 manip, all arrays `.tolist()`).

### Stage 5 gotchas

- **T1 - gateway decodes with plain msgpack** (not msgpack_numpy): every array
  in the reply envelope must be `.tolist()`, or the platform-side decode fails.
- **T2 - warmup empty frame**: `session_id=""`, `frame=b""` must return `b""`
  without raising, before any session is open.
- **Three 16-D layouts**: policy-input grip-last `[L7,R7,Lg,Rg]`; policy-server
  output WM-interleaved `[L7,Lg,R7,Rg]` (same as Stages 1-4); envelope slice
  basis grip-last. Reorder: `acts[:,0:7]=wm[:,0:7]; acts[:,7:14]=wm[:,8:15];
  acts[:,14]=wm[:,7]; acts[:,15]=wm[:,15]`.
- **Login is POST JSON to `/api/challenge/login`** (GET -> 405; email must be
  whitespace-stripped). `/api/challenge/job/<id>/result` takes the **numeric**
  job id from `/api/challenge/jobs` `items[].id` (uuid -> 500). Quota is 99
  total (not 4/day). Jobs-list key is `items`.
- **`no_proxy` must NOT be `"*"` for Stage 5**: the tunnel gateway is dialed
  direct (`websockets proxy=None`); use precise `no_proxy="localhost,
  127.0.0.1,::1"` so localhost policy-server calls bypass the flaky
  quickqservice proxy. (Opposite of Stages 2-4 where `"*"` was load-bearing.)
- **Two agents share one policy server** (agents hold no GPU; :8000 ~10G VRAM).
  The policy client serializes with a lock + 3-retry reconnect.
- **Official SDK unpublished**: TunnelClient ported from ACoT-VLA
  `scripts/tunnel_agent.py` (branch agibot_world_challenge) - equivalent impl.
- **Security**: credentials live only in `/mnt/genesis-world-tour-docs/
  user_access_methods.txt` (never print/commit) and `~/.simubotix-challenge.env`
  (chmod 0600; password deliberately NOT persisted). `drain` -> exit 0, never
  reconnect. Kill agents by pid file; kill the policy server by real process
  name (`ps aux | grep serve_pi05`), not the conda-run wrapper pid (坑 I).
- **Cross-embodiment expectation**: G2 joints are radians with a rest pose
  mostly outside the g01op training q01-q99 range; gripper -0.735 vs training
  0..119.8 (clips). Documented, not silently corrected - scores reflect honest
  cross-domain transfer.

### How to run

```bash
# one-shot (offline parts only unless STAGE5_RUN_ONLINE=1)
bash run_stage5.sh
# step by step: source scripts/stage5_env.sh, start policy server (openpi env,
#   serve_policy_pi05.sh on :8000), python3 scripts/stage5_mock_test.py (6/6),
#   python3 scripts/stage5_check_jobs.py, then per job:
#   python3 scripts/stage5_adapter.py --job-uuid ... --access-token ... --tag jobA
#   python3 scripts/stage5_poll_result.py --tag jobA; render_stage5_obs_videos.py;
#   generate_stage5_report.py --tags jobA,jobB; verify_stage5_outputs.py
```

### Stage 5 results (last run)

Both boards completed 10/10 episodes, **0 policy fallbacks**, clean drains.
**Job A (instruction)**: total score **0** (10 instruction-following pick
tasks all 0), 16182s; best-score rank 19 (leaderboard top ~0.82, in-domain
`pi05_instruction_baseline` 0.754 - ours is zero-shot cross-domain). **Job B
(manip)**: total score **0.0725** - `g2op_manip_scoop_popcorn` scored **0.725**
(one real success), 9 other tasks 0, 11043s. 5442 + 3552 real inferences, 8994
obs frames recorded, 20 session MP4s rendered. All judging is platform-side
(Isaac Sim auto-judge); our obs videos are inference-side evidence only.
`verify_stage5_outputs.py` **PASS (13/13, exit 0)**. Report at
`/home/vipuser/stage5_outputs/stage5_robocoliseum_report.md`. Committed on
`feature-stage5`. The honest finding: the Stage-1 checkpoint (trained on
kettle/facial-cleanser/towel demos in GE-Sim 2.0) does not transfer zero-shot
to RoboColiseum's G2 embodiment except one popcorn-scoop success - consistent
with the cross-embodiment joint-space gap documented above.

### Stage 5b: re-run with the OFFICIAL pi0.5 baseline weights (COMPLETE)

Stage 5b re-ran the two boards with the **official RoboColiseum pi0.5 baseline
weights** (ModelScope `agibot_world/GenieSim3.0-Dataset`
`checkpoints/instruction_and_robust_pi05` + `checkpoints/manipulation_pi05`,
~12.4GB each, anonymous download - no login needed) served by the **official
ACoT-VLA `agibot_world_challenge` tunnel agent** (`scripts/tunnel.sh` +
`tunnel_agent.py`, cloned at `/home/vipuser/stage5_outputs/acot_vla_src`, uv
env) - NOT our adapter, NOT our checkpoint. Usage-page guidance (the page is a
JS SPA - extract text from the `UsageView-*.js` bundle) points at exactly this
repo+weights; the `challenge-baseline-model` SKILL matched it with no drift.
3 new additive scripts (`stage5b_submit_jobs.py`, `stage5b_run_agents.sh`,
`generate_stage5b_report.py`); no Stage 1-5 file touched. **Results (platform
judged, jobs 1145/1146)**: instruction **0.745** (vs Stage-5 zero-shot 0.000),
manip **0.604** (vs 0.0725) - quantifies the in-domain-vs-cross-domain gap and
proves the Stage-5 zeros were model-domain, not pipeline. Report at
`/home/vipuser/stage5_outputs/stage5b/stage5b_report.md`; **full retrospective
(new pitfalls S5-13..S5-17, reproduction guide) at
`/mnt/GE-Sim-V2-tour-docs/Complete_Stage_5b_Official_Baseline.md`**. Key
gotchas: official agent has no `proxy=None` -> unset proxy env or the gateway
dial dies on the quickqservice proxy (S5-15); 4 concurrent policy cold-starts
OOM-kill the 47GB host (~11-12GB RSS each - stagger 120s, S5-16); gateway-side
stall mid-case (keepalive ping timeout, progress frozen) requires restarting
the agent pool with FRESH agent ids - same-id reconnect hangs forever
(S5-17); `no_proxy` flip from Stage 5 no longer needed since proxies are
unset entirely.

---
> Source: [GimpelZhang/GE-Sim-V2-tour](https://github.com/GimpelZhang/GE-Sim-V2-tour) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-09-22 -->
