## ldm-skill

> 这是李大魔个人 Skill 的公开真源。每个 Skill 必须来自真实、重复发生的工作流，并能在不依赖私人上下文的情况下使用。

# ldm-skill 仓库规则

## 仓库定位

这是李大魔个人 Skill 的公开真源。每个 Skill 必须来自真实、重复发生的工作流，并能在不依赖私人上下文的情况下使用。

## 目录

- `skills/<skill-name>/SKILL.md`：Skill 入口，只放用途、决策标准、核心流程和资源路由。
- `skills/<skill-name>/references/`：只在特定场景需要的规则、格式、案例和说明。
- `skills/<skill-name>/scripts/`：重复执行且适合确定性实现的操作。
- `skills/<skill-name>/evals/`：验证触发边界和关键行为，不校验固定措辞。

## 编写规则

- 假设模型已经具备通用能力，只写会改变决策、避免真实错误或提高稳定性的内容。
- 一个 Skill 聚焦一项可复用工作。相邻能力应在 `description` 中写清边界，不用包罗万象。
- Frontmatter 至少包含 `name` 和 `description`。名称与目录一致，使用小写字母、数字和连字符。
- `description` 说明“做什么、何时使用”，并包含用户实际会说的触发表达。详细流程不要堆在 frontmatter。
- 用户当前明确指令优先于 Skill 的默认流程。Skill 不得把“推荐步骤”写成阻止用户明确请求的硬卡点。
- 绝对措辞只用于安全、真实性、不可逆操作或已验证的脆弱流程。风格判断和启发式扫描必须标为候选信号。
- 主入口保持精简。条件化规则、长案例、平台差异和历史教训放入 `references/`，并注明何时读取。
- 同一规则只维护一处。README 面向安装者，SKILL 面向 Agent，不复制整套操作手册。
- 反复重写的确定性逻辑放进脚本；脚本输出事实或候选项，不冒充需要语境判断的结论。
- 不写与平台策略、安全机制或全局个人偏好重复的规则。

## 修改流程

- 修改前读取目标 `SKILL.md` 及其直接引用的资源，确认调用关系后再删改。
- 保留用户选择、公开接口、文件格式和真实业务约束；不要把一次事故扩写成普遍规则。
- 新增或大改 Skill 时，检查真实使用场景、近邻误触发场景和明确指令覆盖默认流程的场景。
- 修改脚本后运行语法检查、`--help` 或最小样例；只改文档时不添加复述实现的测试。

## 完成标准

- Frontmatter 可解析，名称与目录一致，描述能准确触发且不会吸引无关任务。
- `SKILL.md` 中每个引用路径都存在，并说明读取条件。
- 没有相互冲突、重复维护或无条件阻塞用户的规则。
- 相关脚本和行为评测通过，最终 diff 已复查。
- 不提交凭证、缓存、`__pycache__`、`.pyc`、临时文件或备份文件。

---
> Source: [Holden323/ldm-skill](https://github.com/Holden323/ldm-skill) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-09-23 -->
