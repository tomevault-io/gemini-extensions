## pom1

> Architecture / invariants / gotchas for the **emulator side** of POM1. User walkthrough → `README.md`; open work → `TODO.md`; history → `CHANGELOG.md` / `git log`. **Full doc map → [`doc/README.md`](doc/README.md)**.

# CLAUDE.md

Architecture / invariants / gotchas for the **emulator side** of POM1. User walkthrough → `README.md`; open work → `TODO.md`; history → `CHANGELOG.md` / `git log`. **Full doc map → [`doc/README.md`](doc/README.md)**.

**Contents:** [Overview](#project-overview) · [Build](#build--run) · [Architecture](#architecture) · [Invariants](#invariants--gotchas) · [Memory map](#memory-map) · [Testing](#testing) · [Version bump](#version-string-locations)

- Apple 1 software (BASIC, SID tunes, microSD shell tools, games) → `sketchs/doc/APPLE1DEV.md` + `sketchs/doc/Programming_Apple1_ASM.md`.
- CLI flags → [`doc/CLI.md`](doc/CLI.md) (impl: `CliDispatcher.cpp`).
- DevBench / cc65 details → [`doc/DEVBENCH.md`](doc/DEVBENCH.md) + [`doc/CC65_WASM.md`](doc/CC65_WASM.md).
- GEN2 HGR card → [`doc/GEN2_RELEASE.md`](doc/GEN2_RELEASE.md).
- 6502 ASM sources for every shipped program → `dev/` (`lib/{apple1,m6502,tms9918,gen2,gen2c,games,…}/`, `cc65/`, `codetank/` = cartridge composition ONLY) and `sketchs/<card>/<name>/` (every program source, single- or multi-file). Compiled artefacts land under `software/<dir>/` — that's what POM1 loads. Official release packages bundle `dev/` (linker cfgs + runtime libs) next to the cc65 toolchain so the in-app DevBench compiles asm/C; a bare source build without cc65 omits both.

## Project Overview

Apple 1 emulator (Dear ImGui, MOS 6502 + display + keyboard + ACI cassette) plus expansion cards: Uncle Bernie's GEN2 HGR, P-LAB A1-SID (6581/8580), TMS9918, microSD (65C22+ATMEGA), **IEC daughterboard** (1541 drive on microSD's spare VIA pins), MODEM BBS (65C51+TCP), Terminal Card, A1-IO & RTC, Juke-Box, CodeTank, Rich Dreher's CFFA1, SWTPC GT-6144 + PR-40 (1976, Jobs' *Interface Age* mod), **Uncle Bernie's Extended ACI** ($C500 page). Linux / macOS / Windows / Web (Emscripten).

## Build & Run

```bash
./setup_pom1.sh             # one-time deps (Linux/macOS)
cd build && cmake .. && make
./run_emulator.sh            # runs from repo root
```

Windows: `setup_pom1.bat` + vcpkg + `cmake --build . --config Release`. `compile_commands.json` symlinked for clangd.

**Graphics backend:** `POM1_RENDERER` cache option picks `opengl` or `metal`. Defaults: **Metal on macOS-non-WASM**, OpenGL everywhere else (Linux, Windows, WASM). Override with `cmake -DPOM1_RENDERER=opengl ..` for the legacy macOS GL path; selecting `metal` outside Apple is a hard configure error. Single seam in `src/PomRenderer.h` (the `pom1::renderer()` singleton fronts both backends — no direct `gl*` calls outside `PomRenderer_GL.cpp` / `PomRenderer_Metal.mm`).

**GL tier — `POM1_GL_ES` (`src/POM1Build.h`):** orthogonal to the backend choice above. It says *"we speak GLES"*, **not** *"we are a browser"* — the distinction the old `__EMSCRIPTEN__` guards conflated. On: always under WASM (WebGL 2.0 **is** GLES 3.0), and natively via `cmake -DPOM1_GLES=ON` (**Raspberry Pi 4/5** — Mesa's V3D caps *desktop* GL at 3.1, so the default GL 3.2 core request fails outright; also most ARM SoCs / old iGPUs). POM1's GL usage (VAO/VBO, shaders, FBO, 2D textures) is the common subset of GL 3.2 core and GLES 3.0, so **one set of sources covers both** — only four things differ, all keyed on `POM1_GL_ES`: the header (`<GLES3/gl3.h>` vs `<GL/gl.h>`+`GLProcs.h`, plus `GLFW_INCLUDE_ES3` in `PomRenderer_GL.cpp`), the `#version 300 es` + precision prologue (`OpenGLShader.cpp`), **direct** entry points instead of the lazy `glfwGetProcAddress` slots (libGLESv2 exports them; same branch as macOS), and the GLFW hints (`GLFW_OPENGL_ES_API` + `GLFW_EGL_CONTEXT_API` — GLX can only hand out a GLES context where `GLX_EXT_create_context_es2_profile` exists, which V3D lacks). Native GLES links `GLESv2`+`EGL` instead of `libGL`; `-DPOM1_GLES=ON` with `POM1_RENDERER=metal` or under Emscripten is a hard configure error. CI compiles the four affected TUs on every push (`ci.yml` → "GLES 3.0 tier"), since the release job that ships it is tag-only. **Two shipped GLES packages, deliberately different**: `release.yml`'s `raspberry` job builds a **generic** aarch64 AppImage (must run Pi 3 → Pi 5), while `pi-borne.yml` (manual dispatch, ported from NeoST) builds for **one core** (`-mcpu=cortex-a72` = Pi 4/400) with two-pass PGO + LTO and emits both an AppImage and a kiosk `tar.gz` — see `packaging/raspberrypi/{build_in_bookworm_pi.sh,README.md}`. Both build inside a `debian:bookworm` container, never on the `ubuntu-24.04-arm` runner: Pi OS *is* bookworm (glibc 2.36) and the runner would stamp `GLIBC_2.39`, which starts on no Pi.

**Desktop-GL fallbacks (the GLES tier is preferred, these are the net).** Nothing hardcodes `#version 150` any more, so a *desktop-GL* build now survives a sub-3.2 stack: `main_imgui.cpp` retries context creation **3.2 core → 3.2 → 3.1 → 3.0**; `PomRenderer_GL.cpp`'s `imguiGlslVersion()` steps the ImGui preamble **down** to what `GL_SHADING_LANGUAGE_VERSION` reports (150 → 140 → 130, never up, GLES strings untouched); `OpenGLShader.cpp` picks the CRT dialect the same way but *by actually compiling* each candidate — a driver can advertise a version and refuse it in that context, and the failed attempts must stay silent AND clear `errorOut`, or the CRT panel says "shader unavailable" over a working stack. Both shader sets only need GLSL 1.30 (`in`/`out`, `texture()`, `fwidth()`); Mesa's V3D caps desktop GLSL at **1.40**, which is what used to kill the CRT effects on a Pi. Startup traces: `[CRT] GLSL 140 (driver: 1.40)`, `[GL] ImGui shaders: #version 140 instead of #version 150`. Keep any new shader body inside GLSL 1.30 or the cascade is a lie. `option(POM1_LTO)` (ON) exists so a ≤2 GB box can link at all.

**WASM:** `source emsdk_env.sh && emcmake cmake .. && emmake make && emrun POM1.html`. MEMFS preloads `roms/ pic/ fonts/ software/ sdcard/ cassettes/ cfcard/cfcard.po` (other `.po` are desktop-only, >140 MB). Rebuild after any change under those trees or `build-wasm/shell.html`. The `sketchs/` + `dev/` staging copies are **deny-lists** (`file(COPY … REGEX … EXCLUDE)`, excluding heavy media + build residue), never allow-lists: the desktop packagers ship the whole trees, so an allow-list makes any newly-introduced extension vanish from the *web build only* — silently, in the one build nobody runs locally (that's how `sketchs/logo/*.logo` went missing from `POM1.data`).

**cc65:** per-project Makefiles call `ca65`+`ld65`, then `python3 emit_*_txt.py` lands `.bin`+Woz-hex `.txt` under `software/<dir>/`. `make -C dev/codetank` is the CI gate. Linker configs in `dev/cc65/`. The gen2c runtime is split into per-family C modules (`gen2_init.c`, `gen2_pixel.c`, `gen2_rect.c`, `gen2_text.c` + `gen2_text_num.c` (glyph core vs number formatters — numbers pull rect/hex/soft-mul, strings don't), `gen2_sprites.c`, `gen2_preshift.c`, `gen2_sprengine.c`, `gen2_geom.c`, `gen2_lores.c` — see `dev/lib/gen2c/gen2c.mk`) so ld65 dead-strips per family; hot paths stay in `gen2_blit.s` (+ the masked save-under sprite kernels in `gen2_sprmask.s`), which any project linking those modules must also assemble.

## Architecture

All C++ sources under **`src/`** (vendored deps in `src/third_party/`). `imgui/`, `tests/`, `tools/` stay at repo root. File references below are bare basenames — find them under `src/`.

### Parmigiani's golden rule — "one board at a time"

Claudio PARMIGIANI (P-LAB designer): on real hardware exactly ONE P-LAB card is plugged. The 6502 bus has no arbitration and many P-LAB cards overlap windows (A1-SID `$C800-$CFFF` vs TMS9918 `$CC00/$CC01`; A1-IO RTC `$2000-$200F` vs GEN2 HGR `$2000-$3FFF`; Juke-Box claims `$4000-$BFFF`; microSD's Applesoft Lite RAM window `$6000-$7FFF` vs CodeTank `$4000-$7FFF` — plugging either evicts the other, TMS9918 host stays). POM1 **breaks this on purpose** in the "Multiplexing Fantasy" preset — fantasy, not buildable. Mutex rules elsewhere mirror real bus conflicts. When adding a card, honour the rule and document any intentional coexistence.

### Core layers

- **M6502** (CPU). `op = quint16`, `tmp = int` (carry/borrow via bit 8). BCD ADC propagates low→high carry from `(A & 0xF0)`, not the unadjusted sum. `run(maxCycles)` **returns actual cycle count** (overshoot up to 6) — wallclock pacers must deduct. 24-slot PC control-flow-edge ring + `dumpPcTrace(tag)`. `setTestMode(true)` = flat 64 KB for the Klaus harness only. **At most one interrupt per instruction boundary** — NMI takes priority over IRQ (7 cycles, 3 bytes pushed); never both. **Undocumented multi-byte opcodes advance PC by their real NMOS operand length** (`Unoff2`/`Unoff3` dispatch + matching disassembler addressing mode, mnemonic `???`) — never the no-op 1-byte fallback that desynced the stream.
- **CpuClock.h** — `POM1_CPU_CLOCK_HZ = 1 022 727` (14.31818 MHz ÷ 14). Single source of truth.
- **Memory** — 64 KB. Owns every peripheral (`unique_ptr` + enable flag). MMIO dispatched via **`PeripheralBus`**; `memRead/memWrite` only handle PIA `$D0xx` aliasing, ROM write-protect, OOR strict mode, the cassette write-sniffer, `DisplayDevice::onChar` + TerminalCard hook, then raw `mem[]`. **Redundant-ROM-load guards**: on the microSD Applesoft Lite path, skip the **Woz Monitor** reload when `mem[$FF00..$FF01] == D8 58` (Woz Monitor's `CLD`/`CLI` signature — Applesoft Lite itself at `$6000` always loads); skip SD CARD OS reload when `mem[$8000..$8001] == A9 00`.
- **PeripheralBus** — central MMIO dispatch. Peripherals register `(name, range, priority, onRead, onWrite)`. O(1) hot path via `pageMask[256]` bitmap. `std::stable_sort` by priority — TMS9918 wins over SID at `$CC00/$CC01` via priority 10. `onWrite = {}` → pass-through to RAM; explicit no-op = block (CFFA1 ROM).
- **EmulationController** — façade over CPU + Memory + emulation thread. **Mutex order**: `stateMutex > keyboard.keyMutex > publisher.snapshotMutex`. `stateMutex` is a `PriorityMutex` (MAX speed yields on `hasWaiters()`). Slice cap: 6000 cycles desktop, 50 000 WASM (single-threaded). Native = dedicated thread; WASM = `pumpEmulationMainThread()`.
- **SnapshotPublisher** — SPSC slot, page-level dirty copy (1 bit per 256 B page, contiguous runs → single memcpy). Idle Wozmon = zero copy. TMS9918's 16 KB skipped when unplugged.
- **RewindBuffer** — microM8-style timeline; operates on snapshot blobs (`Memory::saveSnapshotToBuffer`, same byte layout as the `.snap` file). KEYFRAME + DELTA frames anchored in segments; eviction drops whole leading segments past the budget (128 MB default). Touched only under `stateMutex`. Pinned by `rewind_buffer_smoke`.
- **DisplayDevice** — abstract `onChar(char)` sink for `$D012`. Injected via `Memory::setDisplayDevice()` so tests/peripherals can tee.

### UI (ImGui)

- **Docking** (`MainWindow_Dock.cpp`) — POM1 builds against Dear ImGui's **`docking` branch**, pinned **`v1.92.9-docking`** by `setup_pom1.sh` / `setup_pom1.bat` / `ci.yml` / `release.yml` (`IMGUI_TAG`) / `pi-borne.yml` (`IMGUI_TAG`) / the two `packaging/linux/build_in_*.sh` / `packaging/raspberrypi/{install.sh,build_in_bookworm_pi.sh}`. **Bump all nine together** — a master-branch checkout has no `IMGUI_HAS_DOCK` and fails to compile `MainWindow_Dock.cpp`. `renderDockSpace()` submits one host window (`##POM1DockHost`, `NoBackground` + `NoBringToFrontOnFocus`) spanning the viewport minus the menu+toolbar band and the status band, hosting a single `DockSpace` with `PassthruCentralNode`; it **must run before the first dockable `Begin()`** of the frame — `render()` calls it straight after `renderToolbar()`. The three windows that frame it (`##Toolbar`, `##StatusBar`, `##ProfileChooser`) carry `ImGuiWindowFlags_NoDocking` so the workspace can never swallow its own borders. **Multi-viewport stays OFF** (no `ViewportsEnable`): WASM cannot do it at all and the hand-rolled Metal backend (`PomRenderer_Metal.mm` + its `CAMetalLayer` + the per-slot CRT stack) would need secondary-viewport support — two divergent paths for a marginal gain. The factory arrangement lives in `kDockLayout[]` (Apple-1 raster in the central node, card framebuffers + editors + Bench in a workspace node to its right, inspectors in a right column, consoles/printers/terminals in a bottom strip); anything absent from that table floats by design (tutorials, photos, About/Welcome, settings panels, transient file dialogs, and both Memory Map Bars — they own bespoke geometry persistence via `syncMemoryBarIniToDisk`). `buildDefaultDockLayout()` re-runs whenever `DockBuilderGetNode()` finds nothing: fresh profile, legacy pre-docking ini, or a preset switch (`applyMachineConfig`'s existing `ImGui::ClearIniSettings()` drops every node, and the incoming ini either restores its own or has none). Settings → Reset Windows Layout → **"Docking only"** forces a rebuild over an existing node via `wantDockLayoutRebuild`. Nothing else was needed for persistence: `SaveIniSettingsToDisk`/`LoadIniSettingsFromDisk` already round-trip `[Docking][Data]` into each `ini/imgui_preset_NN.ini`. `ini_defaults/*.ini` **do** carry a curated `[Docking][Data]` (hand-arranged per profile, promoted from a real session — regenerate one by rearranging the workspace, then copying `ini/imgui_preset_NN.ini` + `ini/preset_NN.size` over the `ini_defaults/` pair). So a shipped profile boots on ITS curated arrangement and `buildDefaultDockLayout()` is the fallback only: no seed, a legacy pre-docking ini, or Settings → Reset Windows Layout → "Docking only".
- **main_imgui.cpp** — GLFW + per-platform graphics backend (see **PomRenderer** below): OpenGL 3.2 Core / GLSL 150 on Linux + Windows (and legacy macOS via `POM1_RENDERER=opengl`), WebGL 2 / GLSL ES 300 on WASM, Metal on macOS by default. `GLFW_OPENGL_FORWARD_COMPAT` only on the macOS-GL fallback path; macOS-Metal uses `GLFW_CLIENT_API=GLFW_NO_API`. SIGINT/SIGTERM → `glfwSetWindowShouldClose(1)` so `--save-tape` flushes. **Adaptive UI refresh** (desktop only, `idle_throttle` in `ini/ui.settings`, Display Settings toggle): the loop drops to a ~5 Hz render floor when idle — full rate for 10 s at boot, 2 s after any GLFW event, and while `MainWindow::wantsContinuousRender()` (pending screen output, a card framebuffer that actually changed < 2 s ago (`lastCardFbChangeTime`, stamped by GEN2's diffed `render()` and the TMS/GT-6144 upload dirty-gates — a static title screen idles), status message, deferred-plug frames — deliberately NOT the CRT shader, which is on by default and whose only owned motion, the persistence trail, finishes fading at the idle floor). Frame-COUNTED logic in `render()` must either finish within the boot/input grace or appear in that predicate. **Card texture uploads are dirty-gated**: TMS9918 (288×216) + GT-6144 memcmp the fresh framebuffer against the last uploaded copy and skip `updateTexture` when identical (GEN2 already gated by `render()`'s return).
- **Interface zoom** (`applyUiTheme()` in `MainWindow_Presets.cpp`, ported from POM2's `Pom2Theme`) — ONE style apply scales colours, geometry and fonts together: the function **rebuilds the style from a default-constructed `ImGuiStyle`** every call, because `ImGuiStyle::ScaleAllSizes()` is *cumulative* (re-applying a zoom on a live style compounds the padding), then `ScaleAllSizes(uiScale_ * dpi)` + `FontScaleMain = uiScale_` / `FontScaleDpi = dpi` (1.92 dynamic fonts — no atlas rebuild, no backend texture refresh mid-session). `io.FontGlobalScale` is gone. Persisted as `ui_scale` in `ini/ui.settings` (old `hidpi_scale` migrates in); the DPI half is the monitor content scale, polled every frame by `syncUiDpiScale()` and gated by the "Auto (follow monitor DPI)" checkbox. **POM1's own chrome constants are NOT covered by `ScaleAllSizes`** — `kToolbarBandHeight` / `kStatusBarBandHeight` / the Apple-1 window pads live in `MainWindow_Internal.h`, authored at 100 %, and every use site multiplies them through `detail::uiPx()`; forget one and the dockspace (which reserves the same two bands) overlaps a toolbar whose contents grew. **The same rule covers every explicit widget size** — `ImGui::Button(label, ImVec2(w,h))`, `SetNextItemWidth`, `SameLine(0, spacing)`, window size constraints: the box is authored at 100 % but the glyph inside it is the live font, which the zoom *does* scale, so a raw literal clips its own icon (that is what cut the cassette deck's header icons past ~150 %) or breaks the button's proportions (`x1`/`x2` kept a raw 24 px height next to `uiPx(28,24)` neighbours). Prefer a floor over a fixed size (`max(authored × zoom, GetFontSize() × iconScale + 2×FramePadding.y)`) so an oversized font grows the box. **Portable widgets** (`CassetteDeck_ImGui`, the paint/sprite editors, Bench) must not include `MainWindow_Internal.h` for this: read the same factor from `ImGuiStyle` — `FontScaleMain × FontScaleDpi` is exactly `uiPx`'s multiplier, published by this function. A zoom change also queues a one-shot `ScaleWindowsInViewport` (`uiPendingWindowScale_`, applied at the top of the next `render()` like ImGui's own DPI path) so FLOATING windows follow; docked ones keep their share of the dockspace.
- **PomRenderer** (`src/PomRenderer.h` + `_GL.cpp` / `_Metal.mm` / `_Internal.h`) — opaque renderer seam. Public API: `createTexture` / `updateTexture` / `destroyTexture` / `asImTextureID` / `textureWidth` / `textureHeight` / `beginFrame` / `clear` / `renderDrawData` / `present` / `readBackbufferRGBA` / `initImGuiBackend` / `shutdownImGuiBackend`. Factory `pom1::makeRenderer(GLFWwindow*)` dispatches via `#if POM1_HAS_METAL`; `pom1::renderer()` exposes the singleton wired by `main_imgui.cpp` after window creation. Every texture site in the rest of the codebase (Screen_ImGui glyph atlas, HGR/TMS9918/GT6144 framebuffers, 10 photo textures in `MainWindow_Dialogs` — Help → Photos windows, incl. the **interactive Apple-1 keyboard** (`a-1_Keyboard.png`: `kKbdKeys[]` overlays one `InvisibleButton` per keycap, normalised rects validated against the photo; clicking queues the key through `EmulationController::queueKey`, with sticky one-shot SHIFT/CTRL latches, `CTRL+letter`→`toupper&0x1F` control codes, REPT re-send, and the red keycap → warm `reset()`), HGR/TMS Paint editor canvases via `Pom1HgrPaintHost`/`Pom1TmsPaintHost`) goes through this. The Metal backend wraps a `CAMetalLayer` on GLFW's NSWindow, BGRA8 drawables (`framebufferOnly = NO` so the screenshot blit can read them), `@autoreleasepool` per frame (compiled `-fno-objc-arc`; the patched `imgui_impl_metal.mm` next to it is `-fobjc-arc` to match upstream — ARC discipline is asymmetric). **Filter::Nearest on Metal**: `imgui_impl_metal` binds ONE sampler for the whole frame, so PomRenderer's per-texture `Filter::Nearest` contract needs a backend patch. CMake rewrites the upstream copy at configure time and compiles the patched copy — pixel-art framebuffers/glyphs/canvases stay crisp; photos + fonts also sample with nearest, which is visually invisible at integer zoom. Since **v1.92.8** upstream builds two `MTLSamplerState`s and exposes them as standard draw callbacks (`ImGuiPlatformIO::DrawCallback_SetSamplerNearest`), so the patch now flips the *linear* descriptor to nearest (`MTLSamplerMinMagFilterLinear`/`MTLSamplerMipFilterLinear` → `…Nearest`) instead of rewriting the old inline MSL `constexpr sampler` (that ≤ v1.92.7 replace is kept as a fallback). Both replaces are guarded: if neither matches, configure emits a **WARNING** rather than silently shipping blurry pixel art. Going per-image would mean threading `AddCallback()` through every texture draw site — deliberately not done. Screenshot path encodes the blit + presentDrawable on the same command buffer so the just-rendered frame is readable.
- **NativeFileDialog** (`src/NativeFileDialog.{h,cpp}` + `_Mac.mm`) — opens the OS's localised file picker for Load/Save Memory, Load/Save Tape, Load/Save Snapshot. Win32 `GetOpenFileNameW`/`GetSaveFileNameW` (links `comdlg32`), Cocoa `NSOpenPanel`/`NSSavePanel`, Linux probes `$PATH` for `zenity` then `kdialog` (forked, stdout-captured) on first call and caches the choice. WASM and Linux-without-zenity-or-kdialog fall back to the existing ImGui browser. **The Raspberry Pi defaults to the ImGui browser even when zenity exists** (`defaultEnabled()`): the kiosk session is a **bare X server** (no WM at all since the kiosk rework — POM1 goes fullscreen itself via `--fullscreen`) with no GTK/KDE desktop, so a forked picker cold-starts for seconds off the SD card and can land behind the fullscreen window. Detected from BOTH the native GLES tier (`POM1_GL_ES` — `-DPOM1_GLES=ON`, what `build_native_pi.sh` now uses by default) and a runtime `/proc/device-tree/model` probe, which still covers a `--desktop-gl` build. The Settings toggle overrides it and persists as `native_dialogs` in `ini/ui.settings`. Load Memory binary still pops a small ImGui "address only" follow-up since `.bin` files have no embedded address. Cassette deck's Load/Save buttons route through the same action methods so they pick up native too. **The portable HGR/TMS Paint editors and DevBench also route through native** — their host seams (`IHgrPaintHost`/`ITmsPaintHost`/`IBenchHost`) gained a `pickFilePath(...)` method whose **default returns `false`** (so the modules stay standalone + portable for POM2 and keep their built-in ImGui browser as the WASM/no-zenity fallback); POM1's hosts override it to call `NativeFileDialog::pickFiltered` (a CSV-extension convenience over `openFile`/`saveFile` that appends an "All files" filter on loads). The paint/bench hosts get the GLFW parent via a `GLFWwindow* const*` slot pointing at `MainWindow::window` (dereferenced lazily at pick time because the hosts are constructed before `ctx.window` is set); `Pom1BenchHost` reads `mw_->window` directly as a friend. Each editor's `openFileBrowser`/`browse` tries `pickFilePath` first and only opens its ImGui popup when that returns false; the action dispatch is factored into `performFileAction` so both paths share it.
- **MainWindow_ImGui** — single class across 9 TUs sharing `MainWindow_Internal.h`: `_Layout`, `_Presets` (`kMachinePresets[]`), `_Menu`, `_Dialogs`, `_HardwareWindows`, `_FileDialogs` (auto-enable-by-directory), `_DebugWindows`, `_Keyboard`, `_TMS9918Inspector`. Keyboard bypasses `InputQueueCharacters` — keys go GLFW → Apple 1 directly. With autorepeat off (default, matches TTL keyboards) REPEAT drops printable+Enter/Backspace/Escape; F7 hold-to-step honours REPEAT.
- **Screen_ImGui** — 40×24 grid; 2 charmap modes (Apple1/HostAscii), 3 monitor tints (Green/Amber/Mono). **Charmap text renders to ONE native-resolution framebuffer** (`buildScreenFramebuffer`: 40×24 cells × 7×8 Apple-1 dots → `kFbWidth`×`kFbHeight` = 280×192, the 5×8 charmap glyph inset by `kGlyphXOffset`), uploaded as a `Filter::Nearest` texture (like the GEN2/TMS9918/GT-6144 cards) and drawn with a single `AddImage` scaled to the display rect — the rect's aspect applies the `kApple1RasterWidthScale` non-square-dot squish. White dots tinted by the `AddImage(col)` param, so mode/brightness changes need no rebuild; the FB is re-rasterised only when the effective grid (content + cursor + power-on pattern) changes (`screenFbContentValid` / `lastEffectiveGrid`). **This replaced the old per-glyph scaled-atlas blit**, whose double-resample garbled characters under the Metal nearest-sampler patch (a 32×40 cell baked for linear, point-sampled to a non-integer size). HostAscii / no-renderer keep the per-cell fallback (`drawCharmapGlyph` / `AddText`). CRT scanlines = 1-px `AddRectFilled` every 2 display pixels at integer Y. `AddLine` avoided — its AA on macOS GL 3.2 / WebGL2 splits sub-2-px thickness across two rows and halves alpha.
- **CRT effect stack** (`CrtEffectStack` + `CrtParams` + `OpenGLShader` + manager `Pom1CrtEffects`, ported from POM2) — universal GPU post-process, **ON by default** (opt-out via the master button in Settings → "CRT Effects (sliders)..."; a saved `crt_enabled=0` is respected), applied to **every** emulated framebuffer: text screen + GEN2/TMS9918/GT-6144 windows. One shared `CrtParams` (MainWindow-owned, persisted `ini/ui.settings` `crt_*` keys) drives a **per-slot** effect stack (`Pom1CrtEffects::Slot`, one ping-pong pair per framebuffer for phosphor persistence). Single call site `crtEffects.apply(slot, tex, srcW, srcH, dstW, dstH)` → returns the ImTextureID to draw (processed output, or the raw texture on OFF / failure / headless). **Two backends, one look**: `CrtEffectStack` (GL/GLES — reaches the raw GLuint via `PomRenderer::glTextureName()`/`isOpenGL()`, FBO ping-pong) and `CrtEffectStackMetal` (macOS — `metalTexture()`/`metalDevice()`/`metalCommandQueue()`/`isMetal()`, MTLRenderPipelineState + two private RGBA8 render targets). `Pom1CrtEffects::ensureInit()` picks one from the live renderer; the other array stays empty. Each file stubs out on the backend that isn't its own, so both always link. **The two shaders are hand-kept in lockstep** — GLSL 150/300-es in `CrtEffectStack.cpp`, MSL in `CrtEffectStackMetal.mm`, same effect order and constants; a knob added to one must be added to the other or macOS silently diverges. MSL gotchas already handled: `fmod` truncates toward zero where GLSL `mod` floors (barrel warp pushes uv negative at the corners → mask misphase), and Metal render targets are **top-left** origin so the vertex stage emits `uv.y = (1 - pos.y) * 0.5`. The Metal pass encodes on its **own** command buffer committed during the UI build — the ImGui frame's buffer is created in `beginFrame()` but only enqueued at `present()`, and Metal orders by enqueue, so the effect always lands before the draws that sample it. The pass renders at **on-screen resolution** (scanline/mask patterns analytically anti-aliased via `fwidth`, no downstream resample); when `active()`, the legacy ImGui scanline/phosphor overlays are skipped (`Screen_ImGui` + GEN2 window) to avoid double-dipping — monitor tint still applies via the `AddImage(col)` param.
- **Bench** (`Pom1BenchHost.cpp` here, portable `bench/CodeBench.cpp` + `BenchLang.cpp` + `IBenchHost.h` under `src/bench/`). Details → [`doc/DEVBENCH.md`](doc/DEVBENCH.md), WASM specifics → [`doc/CC65_WASM.md`](doc/CC65_WASM.md). Desktop uses bundled cc65 (`ca65/ld65/cl65`) resolved exe-relative first (`<exe>/cc65/bin`, macOS `<exe>/../Resources/cc65/bin`, AppImage `<exe>/../share/POM1/cc65/bin`) so packaged builds are self-contained. **WASM ships the full cc65-in-WASM toolchain, so it exposes every target (asm/C/hex/BASIC) — same matrix as desktop, identity `targetMap_`.** **C targets link the runtime as an `ar65` ARCHIVE** (per-module `.o` → `rt.lib`, mtime-cached under `benchScratchDir()/rtlib_<target>` on desktop, same model in-browser) so ld65 dead-strips unused families — direct objects always link whole, which is why the old single-`cl65` command force-linked the whole ~20 KB gen2c runtime (kept only as the no-`ar65` fallback). Spec field `userAsm` (a sketch's `EXTRA_ASM`) = direct objects, never archived — they must survive unreferenced. See DEVBENCH.md "Link model".
  - **Release packaging bundles cc65 (asm `ca65`+`ld65` *and* C `cl65`+`cc65`).** `tools/build_cc65_bundle.sh` stages a relocatable `cc65/bin` + `cc65/share/cc65` tree (Windows: `packaging/windows/fetch_cc65.ps1` pulls the official snapshot); each packager (`packaging/linux/build_appimage.sh`, `package_macos_release.sh`, `package_windows_release.bat`) copies it next to POM1 + the `dev/` cfgs/libs. `tools/verify_cc65_bundle.sh` asserts asm+C+runtime are present; `POM1_REQUIRE_CC65=1` makes a missing/partial bundle a hard packaging failure. CI: `.github/workflows/release.yml` (one native job per OS — cc65 can't be cross-built) on `v*` tags + manual dispatch; runtime resolution lives in `Pom1BenchHost::probe()` + `ensureCc65Home()`.
  - **macOS ships ONE Universal 2 (arm64 + x86_64) `.dmg`, and NEVER links a Homebrew dylib.** Both are the same lesson: `brew` installs exactly one arch and the linker bakes brew's *absolute* prefix into the binary, so the old `brew install glfw` job produced an x86_64-only app hard-wired to `/usr/local/opt/glfw/lib/libglfw.3.dylib` — a path that doesn't exist on Apple Silicon (brew is `/opt/homebrew` there), so every shipped DMG died at dyld and Finder blamed it on the misleading "POM1 quit unexpectedly". The CI smoke-launch structurally *cannot* catch this: the runner has brew glfw at that exact path. `packaging/macos/build_universal_deps.sh` builds both deps from source for both slices — **GLFW statically** (no dylib left to resolve at all) and **cc65 universal** (it ships inside the `.app` and the DevBench *executes* it, so a single-arch toolchain would mean Rosetta-or-nothing). `package_macos_release.sh` honours `POM1_MACOS_ARCHS` (unset ⇒ native-only, what a dev wants) + `POM1_BUILD_DIR`, bundles any surviving non-system dylib into `Contents/Frameworks` with `@rpath` install names, and hard-fails on a leftover `/usr/local`, `/opt/homebrew` or `/opt/local` reference or a missing slice — including on the five cc65 tools. The release job runs on `macos-15` (arm64) and cross-compiles the x86_64 slice; that slice is verified by `lipo`, not run, since GitHub's arm64 images ship without Rosetta.
- **HGR Paint editor** (`src/Pom1HgrPaintHost.{h,cpp}` here, portable `src/hgrpaint/` — `HgrPaintEditor`, `HgrPaintModel`, `IHgrPaintHost`, plus the image importer `Cam16` + `HgrConvert` + `HgrImageDecode` — same split as Bench, intended for POM2 reuse). The editor + model depend only on ImGui/GL/`IconsFontAwesome6` and the `hgrpaint::IHgrPaintHost` seam (poke / `renderHgrPage` / file I/O); POM1's host wraps `GraphicsCard` + `EmulationController`. The pure model owns its own Apple II HGR interleave + geometry constants (no `GraphicsCard` dependency). **Flood fill floods by *perceived* artifact colour** (renders the page through the host NTSC pipeline): a chromatic field is bit-dithered (solid violet = `$55`, odd columns off), so a raw-bit flood leaks through it — `fillRegion` clears the region then stamps the new colour's parity pattern. **Image import (PNG/JPG → HGR)** is ii-pix-style analysis-by-synthesis: `HgrConvert` carries its *own* copy of the MAME NTSC decode (byte-identical to `GraphicsCard`, pinned in `hgr_convert_smoke`), scores candidate byte patterns in CAM16-UCS (`Cam16.cpp`, chroma-weighted) and Floyd-Steinberg-dithers. Pinned by `hgr_paint_plot_smoke` + `hgr_convert_smoke` (both link the relevant `hgrpaint/` .cpp; `GraphicsCard`/`Gen2VideoScanner` for the decode cross-check).
- **HGR Sprite editor** (`src/hgrsprite/` — `HgrSpriteEditor` + the pure `HgrSpriteBlit`; **reuses the `hgrpaint::IHgrPaintHost` seam + `HgrPaintModel` verbatim**, so `Pom1HgrPaintHost` doubles as its host — MainWindow constructs it with `hgrPaintHost.get()`). Menu entry sits right after the HGR Paint editor; window `showHGRSpriteEditor`. **The Apple II has NO hardware sprites** — a "sprite" is a byte-aligned rectangle of HIRES bytes (`wBytes`×`hRows`) a program blits; the editor draws it at pixel granularity through `hgrpaint::plotPage` (faithful 7 px/byte + shared palette high bit + artifact colour — a lone "White" dot shows violet/green by parity, as on real HGR) into its **own 8 KB scratch page**, so it is **non-destructive until you explicitly Stamp** to the live page (unlike the paint editor which draws straight into VRAM). `HgrSpriteBlit::extract`/`stamp` move the W×H byte rectangle in/out of a page honouring the row interleave (`hgrpaint::hgrByteOffset`); the in-context preview composites the sprite over the live page render (`renderHgrPage`) without mutating it. Tools: Pencil/Eraser/Fill (bounded flood) + transforms (clear/invert/flip/shift) + undo/redo; files (Save/Load raw sprite bytes, Save PNG) are native-picker-only via `std::FILE`/`host->savePng`. A **Dev sprite library browser** lists the bundled GEN2 HGR SCROLL-O-SPRITES catalogue (`dev/lib/gen2/sprites/sprites_*_hgr.asm`, 16 rows × 3 bytes/sprite) by category with clickable thumbnails — parsed once by `Pom1HgrPaintHost::devSprites()` (via the optional `hgrpaint::IHgrPaintHost::devSprites()` seam, default empty) which reads the `.byte`/`; slot … -- name` (or bare `xxx_pat:` label) format; clicking drops the sprite onto the canvas. A **×2 magnify** toggle (`mag2_`, `magnify2x`) doubles each pixel into a 2×2 block when stamping/previewing (2·wBytes × 2·hRows footprint, byte palette bit copied to both doubled bytes) — the editor canvas still edits the 1× shape. Pinned by `hgr_sprite_blit_smoke` (draws through the model, extracts, stamps shifted, asserts every lit pixel reappears + off-page clip; the per-pixel colour model is already covered by `hgr_paint_plot_smoke`).
- **TMS9918 Paint editor** (`src/Pom1TmsPaintHost.{h,cpp}` here, portable `src/tmspaint/` — `TmsPaintEditor`, `TmsPaintModel`, `ITmsPaintHost`, plus the importer `TmsConvert` + `TmsImageDecode`; reuses `hgrpaint/Cam16` + `HgrFont` verbatim). Same split + seam shape as the HGR editor, but for the P-LAB Graphic Card. **Two canvas modes**, each with a FIXED canonical VRAM layout the editor programs onto the live chip (registers + name table) so what's poked actually displays: **Graphics II** (256×192, name `$1800` identity, pattern `$0000`, colour `$2000`) and **Multicolor** (64×48 blocks of 4×4 px, per-section name table `name[r*32+c]=(r/4)*32+c` so every block addresses a distinct nibble). The rendered texture is always the chip's 256×192 active area — in Multicolor a logical block spans 4×4 texture px (`lpx` factor threads through all mouse/overlay math). The **central constraint is Graphics II colour-clash**: each 8×1 cell holds only 2 colours (`fg`/`bg`); `plotPixel` reassigns whichever slot disturbs fewer of the cell's other pixels, and `TmsConvert` picks the best 2 of 15 palette colours per cell + Floyd-Steinberg-dithers (CAM16-UCS, no NTSC synthesis — the palette is 15 fixed RGB). The seam uses **VRAM addresses** (0..`$3FFF`), not CPU addresses (TMS9918 VRAM is behind `$CC00/$CC01`); the host forwards pokes/register writes through `EmulationController` to `TMS9918::editorPokeVram`/`editorSetRegister` (out-of-band edits that bypass the silicon-strict drain — never counted as dropped writes). **The canvas displays the chip's OWN live framebuffer** (`TMS9918::copyActiveFramebuffer`, the active 256×192 of the 288×216 raster — exactly what the Graphic Card window shows) rather than re-deriving the image from VRAM, so it never diverges from the real card; `renderToBuffer` is used only for the import preview (a hypothetical VRAM not yet on the chip). Entering the editor is **non-destructive** — the canonical paint registers + name table are pushed to the chip lazily on the first edit (`ensureModeApplied`, via `beginStroke`) or an explicit mode pick, so merely viewing the card doesn't disturb a running program. Pinned by `tms_paint_plot_smoke` + `tms_convert_smoke` (both cross-check the model's VRAM addressing + the converter against the real `TMS9918` per-line renderers — install canonical tables, plot/convert, render, assert the displayed colour).
- **TMS9918 Sprite editor** (`src/tmssprite/` — `TmsSpriteEditor`, `TmsSpriteModel`; **reuses the `tmspaint::ITmsPaintHost` seam verbatim**, so `Pom1TmsPaintHost` doubles as its host and no new POM1 host exists — MainWindow constructs it with `tmsPaintHost.get()`). Menu entry sits right after the TMS9918 Paint editor; window `showTMSSpriteEditor`. Edits **monochrome** sprite patterns (one palette colour per sprite, index 0 = transparent → no colour-clash logic, just set/clear bits) at **8×8** (one 8-byte pattern slot) or **16×16** (four slots in TMS quadrant order `(x≥8?2:0)+(y≥8?1:0)`, pattern # masked `&0xFC`). Sprite pattern table `$3800`, SAT `$1B00`. The editor programs a **canonical Graphics-I backdrop** (blank name/pattern/colour tables, sprite engine live) onto the chip and places the sprite being drawn as SAT entry 0 (terminator at entry 1) at a user X/Y (SAT Y = display line − 1) + colour + Early-Clock, so it renders live on the Graphic Card window. Same **non-destructive-until-first-edit** discipline as the paint editor (`ensureModeApplied`). Unlike the paint editor the canvas is a zoomed self-rasterised NxN grid (sprites are tiny) with a live 256×192 framebuffer thumbnail; tools are Pencil/Eraser/Fill + whole-sprite transforms (clear/invert/flip/rotate/shift) + undo/redo. A **sprite-bank browser** rasterises the whole sprite pattern table straight from live VRAM (`renderSpriteBank`: 128×128 sheet = 16×16 slots of 8×8, or 8×8 sprites of 16×16) so every sprite in the chip is visible — click a cell to jump `patNum_` to it. A **Dev sprite library browser** lists the bundled TMS9918 SCROLL-O-SPRITES catalogue (`dev/lib/tms9918/sprites_*.asm`, native 32-byte 16×16 patterns) by category with clickable thumbnails — parsed once by `Pom1TmsPaintHost::devSprites()` (via the optional `tmspaint::ITmsPaintHost::devSprites()` seam, default empty); clicking streams the 32 bytes into the current `patNum` slot group (switches to 16×16). File I/O (Load/Save VRAM, Save PNG) is native-picker-only — the shared 16 KB VRAM is otherwise reachable from the TMS Paint editor. Pinned by `tms_sprite_plot_smoke` (draws 8×8/16×16 sprites through the model, places via SAT, renders through `TMS9918::renderToBuffer`, asserts displayed pixels == `kPalette[colour]`/backdrop incl. all four quadrants + Early-Clock).

### Peripherals

One `.cpp/.h` pair per card under `src/`. Bus windows + priorities are listed in the [Memory map](#memory-map) below; cycle/timing details live in each peripheral header. Card-specific docs:

- **GEN2 HGR** beam-race renderer + soft-switches → `doc/GEN2_RELEASE.md`, `doc/GEN2_RELEASE_questions.md`. Pinned by `gen2_floatingbus_smoke`, `gen2_softswitch_msb_smoke`, `gen2_beam_race_smoke`, `gen2_horizontal_split_smoke`.
- **TMS9918** sprite rules → `sketchs/doc/TMS9918-SPRITE_BEST_PRACTICES.md`, `sketchs/doc/TMS9918-SPRITE_INIT.md`, `sketchs/doc/Programming_TMS9918.md`.
- **CodeTank** — P-LAB ROM **daughterboard** of the TMS9918 card (no standalone bus presence). `Memory::setCodeTankEnabled(true)` cascade-plugs TMS9918; `setTMS9918Enabled(false)` cascade-unplugs CodeTank. ROM library at `roms/codetank/` — **4 release carts** (juillet 2026 reorg) rebuilt by `tools/build_codetank_rom.py`: **CLASSICS** (Tetris drop-in recovered verbatim from the committed rom | Chess), **BASIC_LOGO** (LOGO V2.6 `-D CODETANK_BUILD` | Applesoft TMS — the stabilised source of BOTH DevBench TMS interpreter banks, `Pom1BenchHost` loads them from here), **ARCADE** (menu Galaga/Sokoban/Snake | Rogue — default probe rom), **DEMOS** (menu Life/Mandel/Plasma/Vague/Nyan | Animals C). `CODETANKDEV.rom` = pure two-slot flash cart, **generated, never committed** (.gitignore) — the DevBench flashes lower OR upper (toolbar "Upper" toggle) and boots the matching jumper. Burn gate before any EPROM: `tools/verify_codetank_roms.py`. TEST + GAME4/LightCorridor retired June 2026; GAME1-7 layout + GAME5/6 machinery retired juillet 2026.
- **SWTPC GT-6144 / PR-40** → `doc/SWTPC_GT-6144.md`, `doc/SWTPC_PR-40.md`.
- **Extended ACI** — Uncle Bernie's improved Gen-2 cassette interface (Applefritter, august 2026). **No new MMIO**: a second 256-byte PROM page at `$C500-$C5FF` beside Woz's untouched `$C100` firmware, still driving the same `$C000` flip-flop / `$C081` comparator. `C500R` relocates the stock ACI ROM into the **stack page** (`$0100-$01FF`), patches the RAM copy's `R`/`W` branches to an `RTS` and re-vectors its two `JSR $C1F1` to `$C5F1` — which both installs the Apple-II-style checksum accumulator *and* retires the copy's own `$01F1-$01FF` tail, freeing exactly the bytes the relocated code needs for a stack (SP starts at `$FF`). That is why the page cannot be relocated or trimmed: touch either patch and the firmware overwrites its own stack. Tape format: 8-byte from/to headers, equal addresses = autostart, so `RX RX` loads without a typed load range (`<from>.<to>WX` to write). Backward compatible — a stock ACI or an Apple-II reads the same tape by subtracting 8 from each `<from>` and skipping the autostart block. Daughter page of the ACI: `setExtendedACIEnabled(true)` cascade-plugs the ACI, `setACIEnabled(false)` cascade-unplugs it. **Plugged by default wherever the ACI is** — every ACI preset except the two historically faithful 1976 machines (`#4` ACI & BASIC cassette, `#5` GT-6144) and the CC65 bench that mirrors `#4` — and every manual ACI plug (Hardware menu, toolbar chip, `--enable aci`, the SFX editor's rescue plug) brings it along. Only `Memory` stays pure mechanism: `setACIEnabled(true)` does NOT auto-plug it, so a `--disable xaci` override can't be undone by the deferred ACI plug firing afterwards. Pinned by `extended_aci_smoke`.
- **IEC daughterboard** — no new MMIO; piggybacks on microSD's VIA PORTB bits 2-6 (SN7406 inverters). Wired-AND open-collector in `IECBus`; virtual 1541 in `Drive1541` backed by 174 848 B `.d64` files. Enabling IEC cascade-enables microSD.
- **SID** — wraps libresidfp (GPL-2.0+, vendored). Cycle-driven, SPSC ring (4096-cycle chunks → 16 384-sample ring, drop-newest on overflow so producer never touches `ringTail`). `chipMutex` serialises register writes / `clock` / `setChipModel`. **WASM**: 4 MB stack (`-s STACK_SIZE=4194304`) — filter tables overflow default 64 KB; filter builders run sequentially under `__EMSCRIPTEN__ && !__EMSCRIPTEN_PTHREADS__` (~50 ms cold start).
- **TerminalCard** — TCP loopback :6502 (IPv6 `::1` refused). 7-bit and 8-bit raw modes. `Ctrl-S` / `ESC S` arms a screenshot; main render thread calls `PomRenderer::readBackbufferRGBA` between `renderDrawData` and `present`, writes `screenshots/pom1_latest.png` (GL backend does `glReadPixels` + Y-flip, Metal backend blits the drawable to a shared-storage staging texture + swizzles BGRA→RGBA; both return top-down RGBA8 so the writer is renderer-agnostic). Result string uses `screenshotResultMutex` (render thread never blocks on `cardMutex`). Desktop only.
- **WiFiModem** — 65C51 + ESP8266 AT emulation, TELNET IAC filtering, non-blocking TCP. Desktop only; WASM stubs return `NO CARRIER`.

## Invariants & gotchas

### Presets (`kMachinePresets[]` in `MainWindow_Presets.cpp`)

`--preset N` indexes this array — **13 entries, indices 0-12**. The README preset table must stay in lockstep. Pinned by `preset_ram_profiles_smoke`.

- **Indices 0-2 are DevBench profiles** (CC65 / TMS9918 / GEN2 HGR Development Bench). Each MIRRORS an existing preset's machine config (cards + RAM + BASIC): CC65 ≡ "ACI & BASIC cassette" (8 KB), TMS9918 ≡ "TMS9918 (CodeTank)" (8 KB), GEN2 ≡ "GEN2 HGR Color" (48 KB). The mirror is **exact and enforced** (`preset_ram_profiles_smoke`), Extended ACI included: the CC65 bench therefore inherits `#4`'s stock Woz ACI, while the GEN2 bench inherits `#11`'s extended page. Give a bench a card its model lacks and that test fails by design — you'd be developing against a machine your users do not have. `kP1Targets[].preset` in `Pom1BenchHost.cpp` maps each (language × machine) target back to one of these.
- Listed FIRST in the Presets menu, but the **array still ends with POM1 Fantasy** so "default = last" holds. **Keep Fantasy last** — default-picker, banner, and OS window-size floor all key off the terminal index (`kMachinePresetCount - 1`).
- A1-SID / A1-AUDIO SE, A1-IO & RTC, Wi-Fi Modem, Juke-Box have **no dedicated preset** (removed June 2026) — plug from Hardware menu or `--enable {sid,rtc,wifi,jukebox}`. A1-SID card variant + I/O window (`$C800-$CFFF` vs `$CC00-$CC1F`) picked at runtime from Settings → "A1-SID version & addresses".
- **No TMS9918 preset without CodeTank** (daughterboard rule).

### `applyMachineConfig(int)`

- Unplugs every card, optional `hardReset()` (skipped first invocation), sets UI flags immediately, queues deferred plug via `pendingCardEnableFrames = kCardEnableDeferFrames` (15 frames ≈ 200 ms). Deferring past first CPU cycle fixes silent-card-on-boot.
- **ROM selection uses preset config, not live flags** — live flags still false at deferred plug time; microSD Applesoft Lite vs CFFA1 flavour picked from `cfg.microSD`/`cfg.cffa1` directly.
- `applyPendingLayout()` runs before `Begin()` with **`ImGuiCond_FirstUseEver`**. **Widgets must not call `SetNextWindowSize(..., FirstUseEver)`** — overrides preset. `SetNextWindowSizeConstraints` is OK.
- **Per-preset ini** `ini/imgui_preset_NN.ini` (geometry + `[Docking][Data]` + `[POM1Windows][Open]` presence) + `ini/preset_NN.size` (OS window, `W H [X Y maximized fullscreen]` — legacy 2-field files still parse; restore clamps to a live monitor work-area). `io.IniFilename = nullptr`; POM1 manages files via `savePresetLayout`/`loadPresetLayout`, plus a **debounced autosave** (`maybeAutosaveLayout`: `io.WantSaveIniSettings` + presence-hash, ~2 s) so crashes/tab-closes lose seconds, not the session. Global (non-preset) UI prefs live in `ini/ui.settings` (theme Dark/Light/High-contrast + HiDPI scale) and `ini/startup` (boot preference — **default with no file boots POM1 Fantasy**, the last preset; `auto=1,preset=N` boots preset N; `chooser=1` shows the profile chooser; CLI `--preset` wins over all). Boot gate in `render()`; `readStartupPreset`/`startupShowsChooser`/`writeStartupPreset`/`writeStartupChooser` in `MainWindow_Presets.cpp`. **WASM: `ini/` is an IDBFS mount** (shell.html `preRun` + `FS.syncfs` after each save + `pagehide` flush via exported `pom1_save_layout_now`) — layouts survive page reloads. F10 toggles ImGui keyboard-nav mode (keys stop reaching the Apple-1; status bar shows `UI NAV`).
- **Auto-enable by source dir** of loaded file — note `software/Apple-1_TMS_CC65/` is matched by the code but **no longer ships** (it went with the 41 files removed from `software/` on 2026-06-22, commit `72b39a7`, whose message does not mention it); the branch still serves a user-made folder of that name. `software/Graphic HGR/` → GEN2; `Graphic TMS9918/` and `software/Apple-1_TMS_CC65/` → TMS9918; `SOUND SID/` → A1-SID (evicts A1-AUDIO SE + Juke-Box); `NET/` → Wi-Fi Modem; `sdcard/` → microSD; `a1io_rtc/` → A1-IO & RTC; `Graphic gt-6144/` → GT-6144. Each branch always raises the corresponding `show*` window flag.

### MMIO

- `$D010` (KBD) — last key with bit 7 set; read clears strobe.
- `$D011` (KBDCR) — bit 7 = 1 when key ready.
- **PIA register banking** — each port hides TWO registers behind one address, selected by **bit 2 of its control register**: clear → the **data direction register**, set → the peripheral (data) register. `$D010` KBD/DDRA + `$D011` CRA; `$D012` DSP/DDRB + `$D013` CRB. **POM1 seeds the POST-reset state** (`CRA=CRB=$A7`, `DDRB=$7F`, `DDRA=$00`) rather than the silicon's all-zero power-on, and this is **load-bearing**: POM1 jumps straight into programs (`--run`, DevBench Run, `jumpTo`) without executing the Monitor's reset at `$FF00`, and a zeroed CRB leaves the DDRs banked in — the Monitor's own ECHO then writes its characters into DDRB and **hangs forever** on its `BIT $D012 / BMI` as soon as one with bit 7 set lands there (that is exactly what blanked the GEN2 golden frame while this was being built). `$D011` reads keep their historical semantics (bit 7 = key ready, nothing else) — every program tests it with BIT/BPL and modelling CRA read-back would change every read in the corpus for no caller. Pinned by **`pia_ddr_smoke`**; **`extended_aci_smoke`** part C is the end-to-end proof, since Uncle Bernie's Codebreaker probes DDRB (`CRB:=0` / read `$D012` / expect `$7F`) and prints *"I WANT TO RUN ON A REAL APPLE-1 !"* when an emulator gets it wrong.
- `$D012` (DSP) — write triggers display callback + busy-counter except **raw `$7F`** (WOZ Monitor's reset-time `LDY #$7F / STY $D012` DDR setup would paint a spurious `_`). Real hardware latches only on PB7=1; POM1 is permissive so emulator-era demos calling WOZ ECHO with bit 7 clear still render. `$DF` via ECHO still yields `_`. TerminalCard + PR-40 sniffers get every write unfiltered.
- PIA 6821 incomplete decoding aliases `$D0xx` → `$D010-$D013` by low 2 bits (all **four** registers — `$D0x3` is CRB, not RAM). `setKeyPressed` forces uppercase; `setKeyPressedRaw()` bypasses it (Terminal Card).
- **`Memory::configureResetVectors(addr)` only writes `$FFFC/$FFFD` (RES)**. NMI (`$FFFA/$FFFB`=$0F00) and IRQ (`$FFFE/$FFFF`=$0000) stay at the authentic WozMonitor.rom values so P-LAB programs installing an IRQ trampoline at `$0000` route correctly.

### Other gotchas

- **AIFF is parsed by POM1, not miniaudio.** `ma_decoder` covers WAV/FLAC/MP3 (+Vorbis) and has no AIFF backend, but AIFF is what Uncle Bernie's `ACIace` tape synthesiser emits — so `CassetteDevice::loadAiffTape` reads big-endian IFF by hand (COMM/SSND, the 80-bit extended sample rate, PCM 8/16/24/32 + AIFF-C `sowt`/`fl32`). Two traps it already handles: AIFF PCM is **signed at every width** including 8-bit (WAV's 8-bit is unsigned), and `.aiff` takes the **pulse path unconditionally** like `.aci` — it is cassette DATA, never deck music, and audio-stream mode would carry no transitions for `$C081` to read.
- **`AudioDevice::getActualSampleRate()` returns the miniaudio-negotiated rate** (44.1 kHz requested, often 48 kHz on Apple Silicon) — cycle-driven sources must use this or tempo drifts. WASM always 44.1 kHz.
- **OOR (`ramKB < 64`)**: strict mode returns `$FF` and drops writes in `[ramKB*1024, $8000)` — matches real Apple-1 with no RAM board. Status bar: `OOR:N!` or `[strict]`.
- **`loadHexDump` has THREE parsers, picked by content.** Default (legacy): joins every line into one string and recovers boundaries heuristically — comments (`//` `#` `;`, stripped inline, else `LDA`/`DEX` mnemonics parse as data), continuation lines, `R` suffix (run address), and **merged data+address** (`ED0300:` → data `ED` + addr `0300`, the artefact of that joining). A **TurboType `.TUR`** takes a **line-structured** parse, and an **Intel HEX** file takes `src/IntelHexFile.h`.
- **Why `.TUR` needs its own parser:** a turbo block puts the address on its own line and prefixes each 16-byte data line with `:` — the inverse of WOZMON's `AAAA:` suffix. Joined, every 32-digit data run is followed by the next line's `:`, so the legacy "hex token before `:` is an address" rule sliced off its last 4 digits as an address and **shredded the 15 Puzzle's $0300 block into 60+ bogus zones** ($18A9, $4159, $F460…). Keeping lines apart makes both dialects unambiguous and needs no merge hacks. `X` is a bare end-of-stream marker (it used to also swallow the `015ER` run line behind it, so POM1 jumped into the serial receiver at `$0100` instead). **Selected by the `.tur` EXTENSION *or* a lone `T` line** — the marker is optional in the wild (the same transfer is also published as plain address + `:data` blocks), and a marker-less file left to the legacy parser hits exactly the shredding above. Everything else still needs its `T`, so the ~100 bundled `.txt` dumps keep byte-identical legacy behaviour.
- **Intel HEX (`:LLAAAATT<data>CC`) is a second, unrelated `.hex` dialect** — `src/IntelHexFile.h`, pinned by **`intel_hex_smoke`**. Fed to the WOZMON parser it had no `AAAA:` address, so every digit (byte count, load address, record type, checksum) was written as **data** at whatever address was current — silent, same class as the old `.apl` fall-through. Detection is **structural, never extension-based** (`looksLikeIntelHex` validates the first record's count/type/checksum together), so an Intel HEX named `.txt` loads correctly and a WOZMON dump named `.hex` is untouched; no bundled dump even starts a line with `:`. Once the first record validates POM1 **commits**: a later bad checksum, an unknown record type, or a record past `$FFFF` (the type 02/04 base records are an x86 inheritance) **fails the load loudly** instead of falling back to the WOZMON parser, which would resume writing record headers into RAM. A type 03/05 start record is the Intel-HEX equivalent of WOZMON's trailing `AAAAR`; a missing type-01 EOF only warns.
- **A `.TUR` load is self-checking**: POM1 injects RAM directly, so the `$0100` receiver is written but never executed; the file's last run address is its **CRC-CCITT checker**, which re-verifies the injected image on the emulated 6502 and jumps to the program on match / prints `EE` on mismatch. `hex_dump_turbotype_smoke` runs a real `.TUR` (`tests/fixtures/15puzzle.tur`) and asserts the game's banner appears — no golden byte array needed.
- **Which extensions reach `loadHexDump` is `pom1::isHexDumpPath` in `src/HexDumpFile.h`** — `.txt` `.hex` `.apl` `.mon` `.tur`, one list, four call sites (`CliDispatcher::runLoad`, the Load/Save Memory native filters + the ImGui fallback browser's directory listing in `MainWindow_FileDialogs.cpp`, `Pom1BenchHost::targetForPath`). `.apl` is Uncle Bernie's canonical Apple-1 name for a WOZMON-keystroke file; before the list existed, `--load 0280:prog.apl` fell through to `loadBinary` and wrote the file's **ASCII text** into RAM with no error. Bundled programs keep their `*.apl.txt` double extension (plain-text tooling + pinned test paths). Pinned by `hex_dump_extension_smoke`. `CodeBench.cpp` repeats the extensions literally on purpose (portable module, no POM1 headers).
- **ACI TAPE OUT vs bundled cassette** — `cassettes/WOZ_talk.mp3` is preloaded only when the **POM1 Multiplexing Fantasy (2026)** preset is applied (last preset index); Integer-BASIC cassette presets load `BASIC.aci` instead. While a stream-mode tape is inserted, live `$C0xx` toggles (`$C030` chiptune in GEN2 *A-1-CrazyCycle*) stay silent because `CassetteDevice::fillAudioBuffer()` never mixes the pulse queue. DevBench Run auto-ejects on ACI presets without the Integer-BASIC program tape.
- macOS: `pom1_macos_provision_user_data_dir()` creates `~/Library/Application Support/POM1/` with symlinks for read-only dirs + seeded `sdcard/`/`cfcard/`/`ini/` then chdirs there. Refreshed each launch (handles Gatekeeper App Translocation + `/Applications` drag-installs).

## Memory Map

```
$0000-$00FF  Zero page
$0100-$01FF  Stack
$0200-$1FFF  User RAM (programs typically load at $0280 or $0300)
$0000-$7FFF  microSD card RAM expansion — 32 KB contiguous (manual §6.1, three
             allocation jumpers fitted); 36 KB with the $E000-$EFFF bank
$2000-$200F  A1-IO RTC VIA 65C22 (mutex with GEN2 below)
$2000-$3FFF  GEN2 HGR page 1 framebuffer (8 KB)
$4000-$5FFF  GEN2 HGR page 2 framebuffer ($C255 selects; mutex with CodeTank/Juke-Box windows)
$4000-$7FFF  CodeTank ROM (16 KB half of 32 KB 28c256; daughterboard of TMS9918, mutex with Juke-Box)
$4000-$BFFF  Juke-Box ROM (RAM-16/ROM-32 jumper; up to 512 KB paged via $CA00)
$5000-$7FFF  EhBASIC 2.22 flashed into RAM (NOT a ROM — cold `5000R` / warm
             `5003R`; programs live $0300-$4FFF; needs ≥32 KB; mutex with the
             microSD Applesoft window / CodeTank / Juke-Box)
$6000-$7FFF  Applesoft Lite in microSD CARD RAM (NOT a ROM — cold `6000R` / warm `6003R`)
$8000-$BFFF  Juke-Box ROM (RAM-32/ROM-16 jumper; Sx bit 4 of $CA00)
$8000-$9FFF  SD CARD OS ROM (microSD; same EEPROM serves the IEC daughterboard)
$9000-$AFDF  CFFA1 firmware ROM
$A000-$A00F  microSD VIA 65C22
$AFE0-$AFFF  CFFA1 ATA/IDE regs (A4 undecoded; ID at $AFDC/$AFDD)
$B000-$B003  MODEM BBS ACIA 65C51
$C000-$C0FF  ACI I/O ($C081 input, $C000 flip-flop)
$C100-$C1FF  Woz ACI ROM
$C500-$C5FF  Uncle Bernie's EXTENDED ACI PROM page (2nd half of the ACI's 512x4
             pair; daughter page — cascade-plugs/unplugs with the ACI)
$C250-$C257  GEN2 soft switches (READ toggles + returns HST0 in D7; writes no-op;
             mirrors across $C2/$C3/$C6/$C7xx where A4=1 — SEL = $Cxxx & !A11 & A9 & A4)
$C800-$CFFF  A1-SID (29 regs, & 0x1F)
$CA00        Juke-Box Px/Sx bank-select latch (write-only; mutex with SID)
$CC00-$CC1F  A1-AUDIO SE (excludes TMS9918)
$CC00/$CC01  TMS9918 DATA / CTRL (priority 10, wins over A1-SID)
$D00A        SWTPC GT-6144 command port (write-only; bus wins over PIA alias)
$D010-$D013  KBD/DDRA · CRA · DSP/DDRB · CRB (alias $D0F0-F3, $D030-33, …;
             CRx bit 2 banks direction vs data — see Invariants)
$E000-$EFFF  Integer BASIC ROM (mutex with Microsoft BASIC below — one socket)
$E000-$FEFF  Microsoft BASIC ROM (OSI lineage, 8 KB, FLOATING POINT; cold E000R,
             warm E003R; loaded from Settings → Memory Settings → ROM Loading)
$FF00-$FFFF  Woz Monitor ROM + vectors ($FFFA-$FFFF)
```

## Testing

`ctest` from `build/` (native-only, opt-out `-DPOM1_ENABLE_TESTS=OFF`). Inventory in `tests/CMakeLists.txt`; `ctest -N` lists exact names. CMake never invokes `dev/codetank/Makefile`; those are developer-only build steps. Tests load whatever artefact lives under `software/`.

```bash
ctest                       # full suite (~5–30 s wall time; Klaus + TMS9918 tests dominate)
ctest --output-on-failure
ctest -R klaus -V
```

Load-bearing pins worth knowing:

- **`klaus_6502_functional`** — Klaus Dormann's 6502 functional test. Gates all CPU refactors (functional only — no cycle counts).
- **`cpu_harte_smoke`** — Tom Harte "65x02 ProcessorTests" (`tests/cpu/harte_6502.bin`, 100 cases × 151 documented opcodes). **Cycle-exact** per-opcode oracle; complements Klaus by pinning page-cross/branch penalties + decimal-mode ADC/SBC + PLP/RTI P-bits.
- **`cpu_interrupt_smoke`** — IRQ/NMI line timing (7-cycle entry, vectors, pushed-P B bit, edge-cleared NMI, RTI restore).
- **`gfx_regress_gen2_testcard`** — headless golden-image graphics regression (`--dump-gen2-frame` + sha256 vs committed golden PNG; TMS9918 counterpart `--dump-tms-frame`). Harness `tools/test_gfx_regress.py` skips if Python3 / `build/POM1` absent.
- **`basic_compiler_smoke`** — the Applesoft "BASIC compiler" (`src/BasicTokeniserApplesoft.*`, `basicc` tool, [`doc/BASIC_COMPILER.md`](doc/BASIC_COMPILER.md)): compiles `3DHat.apf` to a 6502 image (tokenized program `$0801` + launcher `$0280` = `JSR SETPTRS;JMP NEWSTT`) and runs it injection-free on **both** GEN2 HGR and TMS9918, asserting the float 3-D plot fills the framebuffer. Re-pins the two interpreter entry points per card (TMS `$4596`/`$46F2`, GEN2 `$9D96`/`$9EF2`) — a ROM rebuild that shifts them fails here.
- **`basic_native_codegen` / `basic_native_run` / `basic_native_bench`** — the **native** BASIC→6502 compiler (`src/BasicCompilerApplesoft.*`, runtime `dev/lib/basicrt/`, [`doc/BASIC_COMPILER.md`](doc/BASIC_COMPILER.md)): integer **and** binary32-**float** phases (incl. `SIN`/`SQR`/`INT`). Codegen unit pins (strength reduction, native FOR/GOTO/GOSUB, graphics ABI, float ops via `fp_*`, the temp-fusing peephole) + a cc65-gated end-to-end run that builds the standalone binary (loads/runs at `$0300`, no interpreter), runs it vs the interpreter, asserts **same framebuffer + native faster** (~22× integer compute, ~4.5× pixel-bound, ~2.0× float ceiling), and **compiles+runs the real `3DHat.apf` native** (transcendentals + `HCOLOR=0` hidden-line erase). `basic_native_bench` prints the size+speed-by-program-type table. Both skip (77) without cc65. **GEN2 standalone code shares RAM with the framebuffer, so it must fit `$0300–$1FFF` (cfg: BSS moved to `$0200`); the peephole's ~640 B saving is what lets 3DHat fit.**
- **`basic_float_runtime`** — the standalone **binary32 software-float** runtime (`dev/lib/basicrt/basicrt_float.s`, the FP-phase foundation, no Applesoft ROM): cc65-gated, assembles the runtime (with `-D FP_INT/FP_SQRT/FP_SIN`) and checks `fp_fromint16/toint16/add/sub/mul/div/cmp` + `fp_int`/`fp_sqrt`/`fp_sin` against the host IEEE `float`/`sqrtf`/`sinf` over a grid + random pairs (5736 cases). The transcendentals are feature-gated so they link only when used. Skips (77) without cc65.
- **`size_report_smoke`** — `tools/size_report.py` (ld65 map → per-region headroom incl. minus-the-C-stack, per-module byte table, `--why module` reference chains, `--min-headroom [REGION:]N` CI gate) **+ the DevBench archive-link model**: rebuilds the gen2c runtime `.lib` from `dev/bench/gen2c.json` (pins the spec's buildability), links a text-only program, asserts unused families are dead-stripped and the headroom math holds. Skips (77) without cl65+ar65.
- **`ehbasic_smoke`** — Lee Davison's Enhanced 6502 BASIC 2.22 at `$5000-$7FFF` (`roms/ehbasic.rom`, flashed into RAM, cold `5000R` / warm `5003R`). **The Apple-1 port is POM1's own** — none existed; upstream only ships a Kowalski-simulator stub with a fake ACIA — so `dev/ehbasic/src/` carries the PIA I/O and the entry stub while `basic.asm` stays untouched. There is therefore no published image to diff against and **running the interpreter IS the verification**: the test boots it, checks `PRINT 1/4` = `.25` and `PRINT SQR(2)` = `1.41421`, then types a `FOR` loop and runs it. It also asserts the string `DERIVED FROM EHBASIC` is still in the image — **that is a licence condition, not a style check** (EhBASIC requires it in any distributed binary; `dev/ehbasic/README.md` is the human-readable half). Rebuild with `dev/ehbasic/build_ehbasic.sh`.
- **`msbasic_smoke`** — Microsoft BASIC 6502 at `$E000` (`roms/msbasic.rom`, floating point, cold `E000R` / warm `E003R`). Boots the ROM on the emulated 6502, answers its two cold-start prompts and asserts `PRINT 1/4` yields `.25` — what no checksum can tell you. Also pins that `loadMsBasic` restores POM1's own Woz Monitor over the image's bundled copy (the file is a full 8 KB `$E000-$FFFF` EPROM image, kept intact so its sha256 still matches the published ROM) and that `unloadBasic` clears the FULL `$E000-$FEFF` window — MS BASIC is nearly twice Integer BASIC's size, so the old `$E000-$EFFF` clear left 3 KB of the previous interpreter behind when switching flavours. **The ROM is reproducible from public sources** — `dev/msbasic/build_msbasic.sh` rebuilds it from pinned mist64/msbasic + coopzone-dc overlay commits and asserts the hash; provenance, the four missing target-dispatch patches and the licence position are in `dev/msbasic/README.md`.
- **`measured_cpu_rate_smoke`** — the status bar's MEASURED clock rate (`EmulationController::getMeasuredCpuHz`) vs the pacer's target. Wall-clock based, loose bands; it separates "measured" from "target" and from the burst-rate bug (charging elapsed time only to slices that ran the CPU reads x1 as tens of MHz). Also pins that a parked CPU publishes 0 — the window only refreshes inside `runEmulationSlice`, which the park branch skips.
- **`extended_aci_smoke`** — Uncle Bernie's Extended ACI end to end: pins the `$C500` mapping / write-protect / ACI cascade *both ways*, then plays his own `cassettes/codebrk.aiff` through the real pulse path, types `C500R` + `RX RX`, and asserts Codebreaker autostarts (banner on `$D012`). Also the **only** gate on POM1's hand-rolled AIFF reader.

New invariant tests follow `tests/peripheral_bus_smoke_test.cpp` — `<cassert>` + `add_test` suffices; GTest/Catch2 only once multi-threaded tests land.

**Manual telnet tests** in `tools/test_*_telnet.py`. `test_sdcard_subdir_navigation_telnet.py` pins SD CARD OS "commands only search `currentDirectory`" — run from repo root.

## Version string locations

**Single source of truth: the repo-root `VERSION` file.** To bump: edit `VERSION`, run `tools/set_version.sh` (syncs the static docs), rebuild. Never hardcode the version anywhere else.

- **C++** reads it via the generated `PomVersion.h` (`POM1_VERSION_STRING`) — CMake `configure_file`s `cmake/PomVersion.h.in` from `VERSION` into the build dir (on the global include path). Used by `src/main_imgui.cpp`, `src/MainWindow_Dialogs.cpp`, `src/Screen_ImGui.cpp`.
- **CMake** reads `VERSION` for `MACOSX_BUNDLE_BUNDLE_VERSION` / `MACOSX_BUNDLE_SHORT_VERSION_STRING`.
- **Packaging scripts** read `VERSION` at runtime (override via `POM1_VERSION` env, e.g. the release workflow's git tag): `package_macos_release.sh`, `package_windows_release.bat`, `packaging/linux/build_appimage.sh`.
- **Static docs** carry a literal `POM1 v<version>` that `tools/set_version.sh` rewrites and `tools/check_version_sync.sh` (ctest **`version_sync`**) guards: `README.md` (title), `build-wasm/shell.html` (3×: `<meta>`, `<title>`, `.sub` header), `packaging/windows/README.txt` (header).

---
> Source: [habib256/pom1](https://github.com/habib256/pom1) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-08-11 -->
