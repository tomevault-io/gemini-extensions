## calendar-plus

> Read this before making changes. It captures project state, intentional decisions, and known non-blocking issues so you don't re-derive context or undo prior work.

# CLAUDE.md

Read this before making changes. It captures project state, intentional decisions, and known non-blocking issues so you don't re-derive context or undo prior work.

## Project overview

- Calendar Plus is an Obsidian plugin: a fork / new plugin based on Liam Cain's Calendar plugin, with integrated periodic note functionality inspired by Periodic Notes.
- Intentionally a **separate** plugin from the original Calendar plugin — both can be installed and enabled simultaneously.
- Plugin id: `calendar-plus`
- User-facing name: `Calendar Plus`
- Current version: `2.1.13` (kept in sync across `manifest.json`, `package.json`, `package-lock.json`, `versions.json`). **2.1.13 is an internal robustness + perf patch (no new settings, no user-visible behavior change; `minAppVersion` unchanged at 1.8.7) landing two deferred Fable-review items: (1) **settings deep-merge** — `loadOptions`/`writeOptions` in `main.ts` now use a single generic `deepMerge` (replacing a hand-enumerated one-level merge on load + a shallow top-level spread on write), so defaults backfill at any depth and a `writeOptions` caller returning a nested partial can't drop sibling fields. This is the forward-compat safety net under the no-migration policy; behavior-preserving for the current shape (verified against all 25 callers), arrays replace wholesale, undefined patch values are ignored. (2) **month-grid perf** — `Calendar.svelte` keys `localeData` on the locale settings (`localeOverride`/`weekStart`) via `moment()` instead of spreading `{...today.localeData()}` every tick, and the vendored `Calendar.svelte`'s `daysOfWeek` derived is retargeted from `today` to `localeData`, so `getMonth` (42 moment clones) / `getDaysOfWeek` stop rebuilding on every content tick and only re-derive on month/locale/weekStart change. Also a README FAQ note: a mobile "failed to load" (`main.js` couldn't be opened) is an Obsidian Sync install gap — reinstall from the Community Plugins store rather than relying on Sync to carry the plugin's code file. The remaining Fable items (tags-regex hoist P3, `view.ts` `openOrCreate*` de-dup, async-ceremony-for-sync-sources, `getDateUID`/memo-eviction watch, module-singleton watch) stay deferred — see FUTURE_PLANS "Deferred from the Fable review".** **2.1.12 is a regression hotfix + one perf tweak from a follow-up Fable review of the 2.1.11 diff (no new settings; `minAppVersion` unchanged at 1.8.7): (1) **rename regression fix** — 2.1.11's `removeByOldPath` identity guard compared `current[uid]?.path !== oldPath`, but Obsidian mutates `TFile.path` to the new path *before* firing `rename`, so the stored entry's `.path` already equaled the new path → the guard never matched → the old-date entry was never removed, leaving a stale duplicate dot at the old date until reindex. Fixed by guarding on file *identity* (`current[uid] !== file`), passing the renamed `file` into `removeByOldPath` (both stores) from `onFileRenamed`. (2) **perf** — `monthsWithNotes` in `src/ui/Calendar.svelte` is now gated on `showYearOverview` (the note-store reads aren't dependencies while the popup is disabled, so it no longer re-scans all stores on every file event — the one path that bypasses tick coalescing, notably during sync bursts), and `src/io/yearNotes.ts` reads year/month off the UID's ISO substring instead of `moment.parseZone` per note. The Fable review's other findings (settings `deepMergeDefaults`, `localeData` stabilization, tags-regex hoist, `view.ts` `openOrCreate*` de-dup, async-ceremony-for-sync-sources, module-singleton watch) were **deferred, not applied** — see FUTURE_PLANS "Deferred from the Fable review". Note: the emoji-tag regex hoist (P3) was intentionally left out of this hotfix because safely moving its `\uXXXX`-escaped surrogate-pair literal carries transcription risk not worth a hotfix.** **2.1.11 is a correctness/reliability batch from a full code review (no new settings; `minAppVersion` unchanged at 1.8.7): (1) **featured-image memo vs image-file events** — `resolveFeatureImageFile`'s memo is note-keyed, so deleting/renaming the *image* didn't invalidate it; `invalidateFeatureImageCacheForImage(imagePath)` (`src/io/featureImage.ts`) drops entries whose resolved `file` matches, called from `onFileDeleted`/`onFileRenamed` (gated on `featureImage.enabled`) which then tick so cells re-resolve instead of showing a dead image. (2) **second-daily open path** — `openOrCreateSecondDailyNote` now sets `activeFile`/`activeSecondDailyFile` right after `openFile` (matching the daily/weekly 1.8.3 pattern, so the grey highlight paints without relying on `file-open` timing) and guards concurrent creates by filename (`secondDailyCreateInFlight`). (3) **thumbnail-cache lifecycle** — an `epoch` counter (bumped on teardown/remove/prune) lets an in-flight `prepare()` discard + revoke its result instead of repopulating a cleared `urlByKey` (leak); `revokeSiblingUrls` drops old-mtime blobs on re-save; `ensureDir` resets `dirReady` on `mkdir` failure; the ready-callback is now a Set (multi-view) unregistered in `onClose`. (4) **robustness** — a rejecting source no longer blanks a cell (`metadataReducer` catches per source), the confirm modal closes in a `finally`, and note-store `removeFile`/`removeByOldPath` only delete when the stored entry is that file. (5) **perf** — `monthsWithNotes` keys on a derived `displayedYear` so same-year month navigation skips the store scan. (6) **docs** — README explains folders in the date format (the filename after the last `/` must identify the period; nesting like `YYYY/MM/…` is supported only if the full date stays in the filename), and the FUTURE_PLANS vault-enumeration note was corrected (checker keys on `getFiles`/`getMarkdownFiles`, which the plugin avoids). The nested-format parser bug itself was investigated and intentionally **not** changed — the fix would risk regressing the majority for a narrow, non-destructive case; see the analysis in the git history / the note below.** **2.1.10 is a small-fixes patch (no new settings; `minAppVersion` unchanged at 1.8.7): (1) a hidden featured image now stays hidden across a note rename/move — `CalendarView.onFileRenamed` calls `migrateHiddenNotePath(oldPath, newPath)` to swap the stale path in `featureImage.hiddenNotes` via `writeOptions` (no-op unless the note was hidden; see FUTURE_PLANS "DONE: feature-image hidden notes path staleness on rename"). (2) thumbnail generation uses `activeDocument.createElement` instead of `document` for popout-window compatibility (Obsidian plugin-checker hygiene; the canvas is off-DOM so it's cosmetic). FUTURE_PLANS also gained a deferred "Global `app` usage — prefer `this.app`" checker item.** **2.1.9 is a follow-on performance patch (no new settings; `minAppVersion` unchanged at 1.8.7) building on the 2.1.8 arc: (1) **tick coalescing** — the imperative `tick()` in `src/ui/Calendar.svelte` is now `debounce`d (~16ms), so file-event fan-out (editing the active daily note fires both `onFileModified` and `onMetadataCacheChanged`; a sync burst fires one per modified file) collapses to a single `today` bump. A tick is idempotent, so this only removes redundant recompute; the settings-change and heartbeat `today` bumps stay immediate. (Note: gating `onFileModified` on `word-count-tasks` mode was considered and rejected — `customTagsSource` relies on that handler to refresh `data-tags` attributes when feature images are off, so gating it would silently break tag-attribute refresh; coalescing achieves the same win with no behavior change.) (2) **thumbnail cold-open memory** — `generateAndStore` now reads the source bytes *inside* the concurrency gate (`src/io/thumbnailCache.ts`), so a cold month of ~30 images holds at most `MAX_CONCURRENT_GENERATIONS` full-res buffers at once instead of ~30. (3) **word-count-tasks memo** — the `wordCount` and `tasks` sources now share `getContentMetrics` (`src/ui/sources/contentMetrics.ts`), one `cachedRead` + scan per note memoized by `(path, mtime)` (Promise-cached so overlapping ticks dedupe), replacing two independent reads+scans per cell per tick; zero effect in the default `exists` mode (sources still self-gate). (4) **wording** — user-facing "feature image" → "featured image" across settings, the day-cell menu, and docs (the internal `featureImage` settings key + symbol names are unchanged, so saved data is unaffected).** **2.1.8 is a performance + reliability release (no new settings) bundling: (1) **feature-image thumbnail cache** — feature images now render from small downscaled thumbnails cached as files in `.obsidian/plugins/calendar-plus/thumbnail-cache/` (`src/io/thumbnailCache.ts`) instead of decoding the full-resolution photo per cell per render; this removes the sidebar-open stutter and bounds cost on image-heavy vaults / phones. See the Feature Images product model (Rendering + Thumbnail cache). (2) **note-switch perf** — `updateActiveFile()` no longer ticks the calendar; the active-file highlight is reactive via the stores, so switching notes no longer recomputes all cells (delete/rename still tick, since dots change). (3) **in-place cell rendering** — `MetadataResolver` resolves into `$state` and renders with `{@render}` instead of `{#await}`, so a new metadata promise per tick no longer destroys/recreates each cell's DOM (which was re-compositing feature images every tick — the typing/sync lag). (4) **stale-image fix** — removing the last image from a note now clears the cell immediately (the `metadataCache.changed` handler invalidates the resolution memo before ticking; see the Feature Images "Cache invalidation" note). (5) **day-cell menu divider** consistency (Create daily note moved to the `calendar-daily` section). (6) README wording cleanups.** **2.1.7 added a distinct grey active-file highlight for second daily notes (a folder-aware `activeSecondDailyFile` store → `selectedSecondDailyId` prop → `.second-daily-active` grey `::after` overlay in `Day.svelte`, which suppresses `.active` so purple + grey never stack) and made Option/Alt + click create a second daily note when one doesn't exist (delegates to `openOrCreateSecondDailyNote`) — see the Second Daily Note product model.** **2.1.6 moved the per-note feature-image opt-out from note frontmatter to plugin data (`featureImage.hiddenNotes: string[]`), fixing the 2.1.5 toggle whose frontmatter flag added an unwanted Properties box and didn't refresh reliably — see the Feature Images product model.** **2.1.5 added the per-note "Hide feature image" day-cell menu toggle (the frontmatter mechanism it shipped with was replaced in 2.1.6).** **2.1.4 is a performance patch: the periodic-note store reindex (a folder walk + per-filename moment-parse) is now **debounced ~300ms** on settings changes, so editing a periodic note's folder or date-format field no longer rescans that folder on every keystroke (the date-format field has no autocomplete, which confirmed the lag was the rescan, not the suggester). The mount reindex stays immediate; locale config and the cheap `today` recompute stay immediate so display settings respond instantly; pending rescans cancel on teardown. See "selective + debounced reindex" in the Stores note.** **2.1.3 modernized the settings path autocomplete onto Obsidian's native `AbstractInputSuggest` (deleting the ~150-line hand-rolled `src/ui/suggest.ts` + its `.calendar-plus-suggest` styles rule) and landed three perf wins: memoized `getFeatureImageUrl` per `(path, mtime, props)`, a day-rollover-gated heartbeat, and removal of the redundant `view.ts` settings-subscribe `tick()` (the wrapper's own `$effect.pre` already refreshes on `$settings` change). See the "Autocomplete" architecture bullet and FUTURE_PLANS "Performance pass".** **2.1.2 completes the 2.1.0 modernization (no user-facing behavior change, verified by an Obsidian load-test): the calendar components were rewritten from Svelte's legacy (non-runes) syntax to idiomatic Svelte 5 runes (`$props` / `$state` / `$derived` / `$effect`), slot content became snippets, and `view.ts` now mounts the calendar via Svelte 5's native `mount()` / `unmount()` instead of the legacy `createClassComponent` bridge (the `@typescript-eslint/no-deprecated` suppression in `view.ts` was removed along with it). The view drives `displayedMonth` through an exported `setDisplayedMonth()` (replacing the old `$set`), keeping it per-view local state — see FUTURE_PLANS.md for when it might become shared state. 2.1.2 also folds in two fixes for pre-existing issues (both predate the runes migration; surfaced when load-testing on a large vault): (1) **hover feedback now shows on feature-image cells** — the base `:hover` background-color sat behind the image and was invisible, so image cells now convey hover by lightening the `::before` overlay (and keep `background-color` stable on hover to avoid re-compositing the photo, which also reduces hover lag on image-heavy vaults); (2) **selective store reindex** — the wrapper now only reindexes a periodic-note store when its own `folder`/`format`/`enabled` changed, instead of rescanning all six folders on every settings keystroke (the old behavior lagged settings typing on large vaults). Treated as a 2.1.x patch since it's part of the same modernization arc (2.2.0 is reserved for the next feature). `minAppVersion` stays at `1.8.7`.** **2.1.1 is a maintenance release that clears the Obsidian plugin-review warnings the 2.1.0 toolchain migration introduced (no behavior change): listed `@eslint/js` in `devDependencies`, converted the ESLint flat config to ESM (`eslint.config.js` → `eslint.config.mjs`) using `defineConfig()` / `globalIgnores()` (clears the `require()` and deprecated-`config()` warnings), and documented the deliberate Svelte 5 legacy `createClassComponent` bridge in `view.ts` with a suppressed `@typescript-eslint/no-deprecated` directive (native `mount()`/`unmount()` migration is planned with the runes pass — see FUTURE_PLANS.md "NEXT UP").** **2.1.0 is a toolchain modernization with no user-facing behavior change (verified by an Obsidian load-test across all features): Svelte 3 → 5 (components stay on legacy syntax, no runes rewrite; the view mounts the calendar via `createClassComponent` from `svelte/legacy`), TypeScript → 5, Rollup → 4, ESLint 7 → 9 with the unified `typescript-eslint` package and a flat `eslint.config.js`, and CI build Node → 22. The stricter toolchain prompted an internal type-safety pass: dropped the now-redundant `keymap` shim (Obsidian publicly types `Keymap.pushScope/popScope`), corrected the calendar event-handler prop types from `=> boolean` to `=> void`, and removed type assertions TS 5 makes redundant. Six never-wired month/quarter/year `onHover`/`onContextMenu` handler methods in `view.ts` were left in place (their dead props removed) for a planned wire-up — see FUTURE_PLANS.md. `minAppVersion` stays at `1.8.7`. See "Dependency + type-aware lint" and "Svelte advisory disposition + modernization" under Important implementation notes.** **2.0.2 cleared the last review warnings (hand-rolled moment types + a Svelte bump). 2.0.1 is a maintenance release (no behavior change): it switched the `obsidian` dependency from a GitHub tarball to the standard npm package, tightened type-safety in a few spots, and enabled type-aware ESLint — see "Dependency + type-aware lint" under Important implementation notes for why.** **`2.0.0` bundles three features: Second Daily Note, Feature Images, and the year-navigator (history popup).** History note: a `1.9.0` version bump was committed to `main` but **never tagged or released** (releases went 1.8.3 → 2.0.0), so no user ever received 1.9.0; the 2.0.0 release renumbered the version files from 1.9.0 → 2.0.0 and `versions.json` maps `"2.0.0": "1.8.7"` (the never-shipped `"1.9.0"` line was dropped). See the per-feature product models below. 1.7.14 replaced `localStorage.getItem("language")` with Obsidian's `getLanguage()` API, bumped the pinned `obsidian` d.ts to v1.8.7, raised `minAppVersion` to `1.8.7`, and added `"1.7.14": "1.8.7"` to `versions.json` — older Obsidian users keep getting 1.7.13 via that compatibility envelope. 1.7.15 was a metadata-only release that updated the plugin description / tagline in `manifest.json` and `package.json` to clarify Calendar Plus is an update of the original Calendar plugin; no runtime change. 1.7.16 was a visual polish + settings release: left-aligned the month/year/quarter header cluster, made the active quarter label accent-text-only (no background/pill under themes like Minimal), reworked weekend column shading to render as a continuous column (including SAT/SUN headers) under default theme, and added a "Shade weekend columns" toggle under Calendar behavior (defaults ON at the time). 1.7.17 was a checker-recovery styling patch: 1.7.16 had introduced ~24 `!important` declarations in `styles.css` to force uniform weekend rendering under themes like Minimal, which dropped the Obsidian community-plugin checker score from ~99% to 74%. 1.7.17 removed all of those overrides, kept the simple `.weekend-shading-enabled`-gated tint on `th.weekend` / `td.weekend`, and intentionally stopped chasing Minimal-specific weekend-cell uniformity — themes where weekend shading conflicts with the theme's day-cell styling are handled by the user toggling **Shade weekend columns** off, not by additional CSS. 1.7.18 added configurable weekend days (`weekendDays: number[]`, default `[0, 6]` for Sun + Sat) and flipped `shadeWeekendColumns` default to `false` so new installs are opt-in; existing users keep whatever they had saved. 1.7.18 also bundled a small UI-internals tidy (dot data-model cleanup, dead-CSS removal, selector clarification) and a settings-page polish pass (Weekend days picker conditional on the master toggle; "Change week number side" relocated under Weekly Notes; Calendar behavior reordered to Confirm-before-create → Start week on → Shade weekend columns → Ctrl + Click Behavior; user-facing strings standardized to American English). 1.8.0 added configurable dot styles: a new `Dot style` setting under Calendar behavior with options "Note exists" (default, unchanged from previous Calendar Plus behavior) and "Word count and open tasks" (opt-in, restores filled word-count dots + one hollow `- [ ]` open-task dot). The richer mode adds a conditional `Words per dot` sub-setting (default 250, dots capped at 5). New sources `src/ui/sources/wordCount.ts` and `src/ui/sources/tasks.ts` are self-gated and use `vault.cachedRead` only for visible daily/weekly cells with notes in the opt-in mode. The `has-note` CSS class is preserved on day/week cells in both modes as a theme hook. The active-quarter accent-text-only treatment from 1.7.16 is preserved. 1.8.1 is a small patch release: mobile header alignment polish for the month/year title and quarter row (mobile-scoped `Nav.svelte` tweak, desktop layout unchanged); modifier-click consistency so month/quarter/year labels use the same cross-platform `isMetaPressed(e)` helper as day/week-number cells (Ctrl-click now works on Windows/Linux; Cmd-click on macOS continues to work) and so monthly/quarterly/yearly note openings respect the `ctrlClickOpensInNewTab` ("Ctrl + Click Behavior") setting — plain-click behavior unchanged; an internal periodic-note open/create helper consolidation that introduced `getLeafForModifierClick(...)` and `tryToCreatePeriodicNoteAndOpen(...)` in `src/io/periodicNotes.ts` and reduced `src/io/{daily,weekly,monthly,quarterly,yearly}Notes.ts` to thin delegators (refactor only, no intended user-visible change); and a SECURITY.md addition for repository hygiene. 1.8.2 is a Today-button + settings-polish patch: the Today button now still jumps the calendar to the current month and, when Daily notes are enabled, also opens or creates today's daily note via the same `openOrCreateDailyNote(today, false)` path day-cell clicks use (so confirm-before-create is honored, plain-pane only — modifier state is intentionally not threaded into the Today control); a new opt-in setting `showTodayButtonOnMobile` (default `false`) controls whether the Today button renders in the mobile calendar header (desktop always shows it; the mobile-visibility gate moved from a `.is-mobile .reset-button { display: none }` CSS rule to a `{#if !isMobile || showTodayButtonOnMobile}` block in `Nav.svelte`); the Calendar behavior settings section was reordered to Confirm-before-create → Ctrl/Cmd + Click Behavior → Dot style → Show Today button on mobile → Start week on → Shade weekend columns, and the "Ctrl + Click Behavior" label was renamed to "Ctrl/Cmd + Click Behavior" to better reflect the cross-platform modifier-click behavior already in place since 1.8.1. 1.8.3 is an active-note highlighting reliability patch: the daily and weekly existing-note open paths in `src/view.ts` (`openOrCreateDailyNote` / `openOrCreateWeeklyNote`) now call `activeFile.setFile(existingFile)` synchronously after `await leaf.openFile(existingFile)`, matching the create-callback paths for those periodicities, the monthly/quarterly/yearly existing-file paths, and the original Calendar plugin's pattern — this means the highlight updates the moment the note opens instead of waiting on `workspace.on("file-open")` event timing (which on mobile didn't always fire reliably enough to drive the highlight); and `onOpen()` now performs an initial `updateActiveFile()` sync after mounting the calendar (`new Calendar(...)` through 2.1.1; `mount(Calendar, ...)` since the 2.1.2 runes migration) so an already-open daily/weekly note gets highlighted immediately when the calendar mounts (closes a real lifecycle gap where the constructor's `file-open` listener couldn't catch transitions that happened before it was registered). No new workspace listeners (no `active-leaf-change`), no CSS changes, no settings/defaults changes, no note-opening behavior change. 1.9.0 (a version bump committed to `main` but never tagged or released — see the 2.0.0 release note at the top of this section) added the Second Daily Note feature: a second, independent daily note per day accessed via right-click on a day cell (own folder/format/template, disabled by default), a custom day-cell context menu (Create Daily Note + Create Second Daily Note when neither exists; Open + Delete Second Daily Note when it exists, deletion via `app.fileManager.trashFile`; primary note's file-menu items below when it exists), and a half-opacity second dot (`.dot.filled.second-daily { opacity: 0.5 }`) distinguishing the second-daily dot from the primary. See the "Second Daily Note product model" section below for the full design. `minAppVersion` unchanged at 1.8.7 across 1.7.15 / 1.7.16 / 1.7.17 / 1.7.18 / 1.8.0 / 1.8.1 / 1.8.2 / 1.8.3 / 2.0.0; `versions.json` maps `"1.8.3": "1.8.7"` and `"2.0.0": "1.8.7"` (the never-released `"1.9.0"` line was dropped at the 2.0.0 release).
- **Active branch is `main`.** Calendar Plus 2.0.0 is the current stable baseline — released (tagged `2.0.0`, GitHub Release published) and living on `main`. Earlier sessions worked on a `merge-periodic-notes` branch that has since been promoted to `main` — that name is no longer the working branch. If a branch named `old-main` exists, treat it as archival/reference only (the pre-merge upstream state); do not commit to it or use it as a base for new work.
- **Active repo path on this machine:** `/Users/matt/Desktop/claude-code-projects/obsidian-calendar-plus/`. The folder was previously at `/Users/matt/Desktop/obsidian-plugin-merge/calendar-plus/` and before that named `calendar/`; treat any references to those old paths as stale. `original-calendar/` and `periodic-notes/` (sibling directories at the old parent level) were read-only reference material and may no longer be present.

## Important product decisions

- **No backwards compatibility.** Do not write migration code, fallbacks, or compatibility shims for: the original Calendar plugin, the Periodic Notes plugin, Obsidian core Daily Notes settings, or any old data shape. Calendar Plus assumes a clean-slate settings model.
- Calendar Plus owns daily, weekly, monthly, quarterly, and yearly periodic-note settings directly. It does **not** read from Obsidian's core Daily Notes plugin or the Periodic Notes plugin.
- **Dot style product model (1.8.0).** Day and week cells can render dots in one of two modes. Keep this model intact — don't collapse the two modes, don't change the default, and don't move the gate.
  - **`dotMode: "exists"` (default)** — one filled dot when the corresponding daily/weekly note exists. This is Calendar Plus's long-standing behavior. **Must remain the default** for both new installs and existing users (the `loadOptions` spread merge preserves the default for users missing the key).
  - **`dotMode: "word-count-tasks"` (opt-in)** — filled dots based on word count (1 dot per `wordsPerDot`, capped at 5) plus one hollow task dot when the note has `- [ ]` or `* [ ]` open checkboxes. Completed `- [x]` boxes are intentionally excluded.
  - Applies to **daily and weekly cells only**. Monthly, quarterly, and yearly header labels never get dots in either mode.
  - The `has-note` class is emitted on day/week cells in **both modes** as a stable theme hook.
  - Word-count and task sources are **self-gated** inside `src/ui/sources/wordCount.ts` and `src/ui/sources/tasks.ts` — they return empty dots and skip `vault.cachedRead` entirely when the mode is "exists". Don't move the gate into `src/view.ts` or rebuild the source array on settings change; the per-source gate is intentional so toggling modes is reactive without restarting the view.
  - **User-facing copy must not directly reference the original Calendar plugin.** Use descriptive labels ("Note exists" / "Word count and open tasks") in the dropdown, never "Original Calendar plugin" or similar. README + CHANGELOG can mention the plugin in credits/context, but the settings UI stays descriptive.
  - Note-content reads (`vault.cachedRead`) are **only** in the opt-in mode and **only** for visible daily/weekly cells with notes. Do not add new file-content reads outside this gate.
- The plugin must coexist with the original Calendar plugin without collisions (separate view type, separate event namespace, separate manifest id).
- **No `.view-content` padding override.** Calendar Plus intentionally does not target `.workspace-leaf-content[data-type="calendar-plus-view"] .view-content` (or any other `.view-content` selector) for padding, margin, width, or other outer-chrome spacing. The plugin relies on Obsidian's and the active theme's default `.view-content` spacing, matching the original Calendar plugin's baseline. This was investigated and intentionally reverted: a previous attempt to tighten or zero `.view-content` padding produced inconsistent results across themes, leaked spacing into unrelated views, and chased theme-specific renderings (notably Minimal) into a fragile rule. Theme-specific spacing differences should not be addressed by reintroducing a `.view-content` override — any spacing polish should live on internal calendar elements (e.g. `.nav`, `.title-container`, `.quarters`, `.calendar`, `.day`) only.
- **Avoid `!important` in `styles.css`.** The Obsidian community-plugin checker flags every `!important` declaration as a warning ("Avoid !important — override styles by increasing selector specificity or using CSS variables instead"), and accumulating them drops the checker score quickly. 1.7.16 added ~24 `!important`s to force uniform weekend rendering under Minimal; the checker score fell from ~99% to 74%, and 1.7.17 walked all of them back. The standing rule: when a theme conflicts with Calendar Plus styling, prefer (a) modest higher-specificity scoped selectors using the existing `.calendar-plus-wrapper #calendar-container ...` namespace, (b) CSS custom properties (`var(--color-background-day)`, `var(--color-background-weekend)`, etc.) that themes can override cleanly, or (c) a settings toggle that lets users opt out — **do not fight theme internals with `!important`, long view-scoped ancestor chains, or pseudo-element resets**. The **Shade weekend columns** toggle is the canonical example of approach (c): under themes whose `.day` styling makes weekend tinting look busy or inconsistent, the user turns the setting off; Calendar Plus does not try to override the theme.
- **Today button product model.** The Today button in the calendar header is a shortcut to today's daily note, not only a month-navigation reset. Click behavior: (a) always jump the calendar to the current month (`displayedMonth = today.clone()`), and (b) if Daily notes are enabled, route through the same `openOrCreateDailyNote(today, false)` path day-cell clicks use — so confirm-before-create is honored, no-existing-file creates today's note via the configured folder/format/template, and the file opens in the current pane. Plain pane only — modifier state is intentionally not threaded into the Today control (don't add it without a product discussion). When Daily notes are disabled, only (a) runs. Visibility: desktop always shows the Today button; on mobile it is hidden by default and only renders when the user opts in via **Show Today button on mobile** (`showTodayButtonOnMobile`, default `false`). Default OFF is intentional to avoid crowding the mobile header — don't flip the default without revisiting the mobile-layout question. The visibility gate is `{#if !isMobile || showTodayButtonOnMobile}` in `Nav.svelte`; there is intentionally no `.is-mobile .reset-button { display: none }` CSS rule.
- **Active-note highlight model (1.8.3).** Active-file state for the calendar's day/week-num cells is driven by the `activeFile` writable store, populated from two sources: (a) `workspace.on("file-open", onFileOpen)` catches external opens (file explorer, search, link click) via `getActiveFile()`; (b) the per-period existing-file open paths in `view.ts` (`openOrCreateDailyNote` / `openOrCreateWeeklyNote` / `openOrCreateMonthlyNote` / `openOrCreateQuarterlyNote` / `openOrCreateYearlyNote`) call `activeFile.setFile(existingFile)` synchronously immediately after `await leaf.openFile(existingFile)`, and the create-flow callbacks call it from inside the callback. The manual `setFile` after plugin-initiated opens means the highlight updates the moment the note opens, independent of `file-open` event timing — this matters because mobile `file-open` didn't fire reliably enough to drive the highlight in 1.8.2 and earlier, and it matches the original Calendar plugin's daily/weekly pattern (`reference/obsidian-calendar-plugin-1.5.10/src/view.ts:281, 310`). The five periodicities share the same pattern: after `await leaf.openFile(existingFile)`, call `activeFile.setFile(existingFile)`. **Do not casually replace this with a broad `workspace.on("active-leaf-change", ...)` listener** — that approach was tested during 1.8.3 development and introduced a two-click navigation regression because `active-leaf-change` fires on focus-into-calendar before Day.svelte's `on:click` runs, and `updateActiveFile()` → `calendar.tick()` re-renders Day cells mid-click, swallowing the click event. If a future change needs to catch leaf-focus transitions that `file-open` misses, route the work through the manual-`setFile` pattern at the plugin-initiated call site, not through a workspace-wide listener. The initial `updateActiveFile()` sync at the bottom of `onOpen()` (after `new Calendar(...)`) is intentional and should remain — it closes a real lifecycle gap where the constructor's `file-open` listener can't catch transitions that happened before the view mounted (common on mobile when the user opens a file, then reveals the calendar sidebar).
- **Weekend shading product model (1.7.18).** Two orthogonal settings drive weekend rendering, and they are intentionally kept independent:
  - **Week start** (`weekStart`) — controls **column order only**. Choosing Monday vs Sunday vs Locale default rotates the day-of-week header and the body grid; it does not determine which columns are tinted.
  - **Weekend days** (`weekendDays: number[]`, JS weekday numbers 0 = Sunday … 6 = Saturday) — controls **which columns are tinted** when shading is on. Default `[0, 6]` (Sun + Sat). Independent of locale, week start, or any other setting.
  - **Shade weekend columns** (`shadeWeekendColumns`) — master on/off. Defaults to **off**. Opt-in by design: under themes that paint their own opaque background on day cells, weekend tinting can look busy; the toggle is the user-side remedy and the docs make this clear.
  Do not collapse these into a single setting or infer weekend days from `weekStart`. Do not change the default of `shadeWeekendColumns` back to on. Existing users who saved a `shadeWeekendColumns: true` preference under 1.7.16 / 1.7.17 keep it via the existing `loadOptions` spread merge; only users literally missing the key get the new opt-in default.
- **Second Daily Note product model (2.0.0).** A second daily note per day, independent of the primary daily note. Key decisions:
  - Accessed via **right-click on a day cell** (context menu) or **Option/Alt + click** on a day cell. Plain left-click always opens the primary daily note; not accessible via any command.
  - **Option/Alt + click** (`isAltPressed` in `src/ui/calendar-ui/utils.ts`, threaded as an optional 3rd arg to `Day.svelte`'s `onClick` → `openOrCreateDailyNote(date, ctrlPressed, altPressed)`) is dedicated to the second daily note: the Alt branch delegates to `openOrCreateSecondDailyNote(date, ctrlPressed)`, which **opens** it if it exists or **prompts to create** it if it doesn't (same confirm-before-create flow as the right-click "Create Second Daily Note" — 2.1.7 changed this from the earlier no-op-when-missing behavior). When `secondDaily` is disabled, Alt falls through to the normal primary-note flow (Alt ignored). `ctrlPressed` is still honored, so Cmd/Ctrl + Option + click opens/creates the second note in a new tab/split.
  - Has its own `secondDaily: PeriodicNoteSettings` key in `ISettings` (folder, format, template, enabled). Defaults to disabled. The format defaults to `YYYY-MM-DD` (same as daily) — users who want the two note sets in the same folder must use a different format string to avoid filename collisions.
  - **Context menu behavior:** when second daily is enabled, right-clicking a day cell shows a custom `Menu`. The primary-daily item(s) always live in the `calendar-daily` section and the second-daily items in `calendar-actions`, so Obsidian renders a **divider between the two groups consistently** — whether the primary note exists ("Open Daily Note" + "Delete Daily Note") or not ("Create Daily Note"). (Earlier, "Create Daily Note" shared the `calendar-actions` section with the second-daily items, so the divider only appeared when the primary note existed — fixed by moving it to `calendar-daily`.) If a second note exists: "Open Second Daily Note" then "Delete Second Daily Note" (`calendar-actions` section; deletes use `app.fileManager.trashFile` so Obsidian's trash preference is respected). If the primary note exists, its standard file-menu items are still appended below the custom sections. If second daily is disabled: existing behavior unchanged (file menu for primary note if it exists, no-op otherwise).
  - **Dot:** `secondDailyNoteSource` in `src/ui/sources/secondDailyNote.ts` emits one filled dot with `className: "second-daily"` when a second daily note exists, regardless of `dotMode`. `styles.css` sets `opacity: 0.5` on `.dot.filled.second-daily` to visually distinguish it from the full-opacity primary dot without introducing a new color. The `className` is a stable CSS hook if the visual treatment is ever revisited (see `FUTURE_PLANS.md`).
  - **Store:** `secondDailyNotes` in `src/ui/stores.ts` uses `"daily"` as the periodicity for date parsing and UID generation (same granularity as daily notes). No collision with `dailyNotes` because they are separate store objects. Does not add `"secondDaily"` to the `Periodicity` type — the type remains the five calendar periodicities only.
  - **Open/create:** `openOrCreateSecondDailyNote` in `view.ts` calls `createPeriodicNote("daily", date, settings.secondDaily)` directly (passing `settings.secondDaily` for folder/format/template) and handles confirm-before-create inline, so the confirmation dialog correctly says "New Second Daily Note". Does not go through `tryToCreatePeriodicNote` (which indexes into settings by periodicity key).
  - **Active-file highlight (2.1.7):** when a second daily note is the active file, its day cell gets a **distinct grey highlight** (not the purple primary-active fill). A separate `activeSecondDailyFile` store (`src/ui/stores.ts`) holds the active second daily note's UID, set in `CalendarView.updateActiveFile()` alongside `activeFile`. Its detection is **folder-aware** (checks `secondDaily.folder` via `isFileInConfiguredFolder` + parses `secondDaily.format`), so a second daily note is never mistaken for a primary daily note even when the two share a date format — this is why it doesn't reuse `getDateUIDFromFile` (which is basename-only). The store flows wrapper → base → `Day.svelte` as `selectedSecondDailyId`; `Day` renders `.second-daily-active` (a translucent grey `::after` overlay, works over plain and feature-image cells) and **suppresses `.active`** when second-daily-active so the two never stack. The primary `activeFile` highlight still only covers daily/weekly (`getDateUIDFromFile`).
- **Feature Images product model (2.0.0).** Day and week-num cells can show the note's feature/banner image as a cell background. Key decisions:
  - **Scope:** daily cells + weekly cells. Monthly/quarterly/yearly header labels never get background images.
  - **Resolution order:** frontmatter properties (configurable, default `["banner", "image", "cover"]`) are checked first in priority order; the first one that resolves to a vault image file wins. Falls back to the first `![[…]]` embed in `cache.embeds` that resolves to an image. Vault files only — external `https://` URLs are rejected.
  - **Per-note opt-out (2.1.6).** A day's feature image can be hidden individually, stored in **plugin data** as `featureImage.hiddenNotes: string[]` (vault paths) — **not** in note frontmatter. (2.1.5 shipped a frontmatter-flag version that was abandoned: writing `calendar-hide-image` to a note that otherwise had no frontmatter made Obsidian render a Properties box at the top of the note, which users disliked, and the refresh was unreliable. Don't reintroduce a frontmatter flag for this.) The check lives in the **source** (`src/ui/sources/featureImage.ts`): `getDailyMetadata`/`getWeeklyMetadata` return `{}` when `s.featureImage.hiddenNotes.includes(file.path)`, before calling `getFeatureImageUrl` (so it's independent of the URL memoization — the hidden set isn't part of the memo key). Toggled from the **day-cell context menu**: when feature images are enabled and the daily note exists, the menu shows "Hide feature image" (if an image currently resolves via `getFeatureImageUrl`) or "Show feature image" (if the path is already in `hiddenNotes`); `CalendarView.setFeatureImageHidden(note, hidden)` adds/removes the path via `this.plugin.writeOptions(...)`. Persisting through `writeOptions` updates the `settings` store, which the calendar wrapper's `$effect.pre` reacts to (bumps `today`), so the cell refreshes **without** any file write or metadata-cache dependency — this is what made it reliable where the frontmatter version wasn't. The menu item is added by `addFeatureImageMenuItem(...)` in both context-menu paths (the custom second-daily menu and the `showFileMenu` fallback, which takes an optional `addExtraItems` callback). **Known minor gap:** a hidden note's path goes stale if the note is renamed/moved (the image reappears; the orphaned path lingers in `hiddenNotes`) — daily notes are date-named and rarely renamed, so this is deferred (see FUTURE_PLANS).
  - **Implementation:** `src/io/featureImage.ts` exports `resolveFeatureImageFile(note, app, frontmatterProperties)` (2.1.8 renamed it from `getFeatureImageUrl`) which calls `app.metadataCache.getFileCache(file)` synchronously and returns the resolved image **`TFile`** (not a URL) or `null`, memoized on `(path, mtime, propsKey)`. It also exports `invalidateFeatureImageCache(path)` and `isImagePath(path)`. `src/ui/sources/featureImage.ts` exports `createFeatureImageSource(app)` — a factory that returns a self-gated `ICalendarSource`. Its `backgroundFor(note, props)` resolves the image `TFile`, then returns `getThumbnailUrl(file)` when the thumbnail cache is available (falling back to `app.vault.getResourcePath(file)` — the full-res `app://` URL — until the thumbnail is ready or if the cache is unavailable). Gating is inside the source (returns `{}` when `featureImage.enabled` is false, or when the note's path is in `featureImage.hiddenNotes`); do not move the gate elsewhere. `backgroundImage?: string` is added to `IDayMetadata` in `types.ts`; `metadataReducer` propagates it with first-source-wins semantics (`acc.backgroundImage ?? meta.backgroundImage`).
  - **Thumbnail cache (2.1.8).** `src/io/thumbnailCache.ts` is a persistent, file-backed thumbnail cache living in the **plugin folder** (`.obsidian/plugins/calendar-plus/thumbnail-cache/`, via `DataAdapter` so it works on mobile). It exists to remove the full-resolution decode-per-cell-per-render cost (the sidebar-open stutter / typing lag on image-heavy vaults). `initThumbnailCache(app, dir)` is called from `main.ts onload` (`teardownThumbnailCache()` in `onunload`). `getThumbnailUrl(file)` is **synchronous**: returns a ready object URL or `null` and kicks off async preparation (load-from-disk-or-generate). Thumbnails are keyed `hashPath(file.path)-mtime` → `<key>.jpg`; generation uses `createImageBitmap` → `<canvas>` downscale (MAX_DIM 128) → `toBlob` JPEG (quality 0.72), with bounded concurrency (3) and an in-flight dedupe map; generation failures fall back to the full-res resource path. The source `readBinary` runs **inside** the concurrency gate (2.1.9), so a cold month of ~30 images holds at most 3 full-res buffers in memory at once rather than reading all ~30 up front. A debounced (50ms) ready-callback (`setThumbnailReadyCallback`, wired in `view.ts onOpen` to `calendar.tick()`) refreshes cells as thumbnails finish. The day↔image connection is **never persisted** — it's derived live each tick from the note filename + frontmatter/embeds; the cache only maps image-file → thumbnail and is fully regenerable, so per-device differences after folder-sync are expected and harmless. **GC:** `removeThumbnailsForSource(sourcePath)` drops a source image's thumbnails (called from `onFileDeleted`/`onFileRenamed` in `view.ts`, gated by `isImagePath`); `pruneThumbnailsExcept(inUseFiles)` is a mark-and-sweep keeping only currently-in-use thumbnails, driven by `sweepUnusedThumbnailsOnce(app)` (once per session, ~3s after `onOpen`, gated on `featureImage.enabled`) which collects in-use images via `collectInUseFeatureImages(app)` — this clears thumbnails for images that still exist but are no longer a feature image (e.g. the user swapped to a different banner).
  - **Settings:** `featureImage: FeatureImageSettings` in `ISettings` — `enabled` (default `false`, opt-in), `frontmatterProperties: string[]` (comma-separated in the UI, stored as array), `showForWeekly` (default `true`). Settings section added to "Calendar behavior" after the Shade weekend columns section.
  - **Rendering:** `Day.svelte` and `WeekNum.svelte` set `background-image: url("...")` inline and add `has-background-image` when `backgroundImage` is present (the URL is the cached thumbnail when available, else the full-res resource path — see Thumbnail cache). The per-cell `MetadataResolver` resolves the source metadata into reactive `$state` and renders it with `{@render}` (2.1.8 replaced the old `{#await}` block, which produced a *new* promise each tick and so destroyed/recreated each image cell's DOM — forcing the browser to re-decode/re-composite the photo on every render; that was the typing/sync lag). The cell uses `isolation: isolate` to create a stacking context so pseudo-element overlays resolve within the cell. The date/week number is wrapped in a `<span>` (`.day-number` / `.week-num-number`) carrying `position: relative; z-index: 1` — purely for stacking; it has **no** visual styling, so the number's color/weight/today/active behavior is identical with or without an image (a prior attempt to add `color: white` + `font-weight: 600` + `text-shadow` was removed because the user wanted the number unchanged). A `::before` readability overlay (dark gradient `rgba(0,0,0,0.45)→rgba(0,0,0,0.35)`, `z-index: 0`) sits above the image but below the number/dots. The active state suppresses the solid accent fill (`background-color: transparent`) and adds a `::after` accent overlay (`background-color: var(--interactive-accent)`, `opacity: 0.65`, `z-index: 0`) so the photo stays visible while reading as selected. **Hover (2.1.2):** the base `.day:hover` / `.week-num:hover` `background-color` change is invisible behind the image, so image cells instead **lighten the `::before` overlay** on hover (`rgba(0,0,0,0.45→0.35)` → `rgba(0,0,0,0.2→0.1)`, with a `transition: background` on `::before`) to read as hovered, and keep `background-color: transparent` on `:hover` (and `.active:hover`) so hovering doesn't repaint/re-composite the photo (that repaint was the source of hover lag on image-heavy vaults). **Do not reintroduce number styling on image cells** — only the background, the readability gradient, the hover-lightened overlay, and the active overlay should differ.
  - **Cache invalidation:** `view.ts` registers `app.metadataCache.on("changed", onMetadataCacheChanged)` to tick the calendar when a daily/weekly note's metadata changes (e.g. user edits `banner:` frontmatter). `vault.modify` fires before the cache updates, making `metadataCache.changed` the reliable post-update trigger for frontmatter-driven images. **`onMetadataCacheChanged` also calls `invalidateFeatureImageCache(file.path)` before ticking (2.1.8 fix):** the `resolveFeatureImageFile` memo is keyed on `(path, mtime, props)`, and `vault.modify` bumps `mtime` *before* the cache reparses — so a resolve triggered by `onFileModified` can store a stale result under the new mtime, which the later `metadataCache.changed` tick (same mtime) would then return from the memo. This left the image on the cell after removing the last image from a note, until reload. Dropping the memo entry on the authoritative metadata-change event forces a fresh resolve. Don't remove this invalidation while the `mtime`-keyed memo exists.
  - **No new note-index store.** The day↔image *mapping* is not indexed: it's derived on each `calendar.tick()` from the existing `dailyNotes`/`weeklyNotes` stores + `app.metadataCache`. (2.1.8 did add a file-backed *thumbnail* cache — see Thumbnail cache — but that's an image-file→thumbnail cache, not a note/date index, and holds no day↔image association.)
- **Year overview product model (2.0.0).** A compact 12-month navigation grid (inspired by Notebook Navigator's year panel) for jumping between months/years. Key decisions:
  - **Popup, not a stacked panel or mode switch.** `Nav.svelte` renders a `history` lucide icon button in `.right-nav` (before the prev arrow) when `showYearOverviewButton` is true. Clicking it opens `<YearGrid>` as a floating popup (`.year-popup`, `position: absolute` anchored top-right inside the `position: relative` `#calendar-container`, with `var(--background-secondary)` bg, border, `var(--shadow-s)`). The popup overlays the day grid; it neither pushes layout (no stacked panel) nor replaces the grid (no `viewMode`). Both of those earlier iterations were removed — do not reintroduce them. `YearGrid` draws a compact `< YYYY >` year-arrow header and a 4×3 month grid. The existing month/year/quarter header labels are untouched; the year grid is navigation-only (no note creation in v1).
  - **Button gates on the persisted setting; popup open/close is transient local state.** The persisted `showYearOverview` now means "show the history button" (gates `{#if showYearOverviewButton}` in `Nav`). Whether the popup is *open* is a non-persisted `yearOverviewOpen` boolean local to the vendored `Calendar.svelte` — popups shouldn't survive reloads. `toggleYearOverview()` flips it; the button's `on:click|stopPropagation` prevents the open click from immediately bubbling to the `<svelte:window on:click>` handler that closes the popup on any outside click; `.year-popup` also `stopPropagation`s so interacting with the year arrows doesn't close it; selecting a month closes it explicitly. The icon is `var(--text-muted)` (grey) when closed and `var(--text-normal)` (white-ish) when open via a `.active` class — intentionally **not** accent/purple. (An earlier iteration persisted *this* toggle by threading the plugin into the view; that plumbing was reverted since the popup is transient and shouldn't persist. Note `view.ts` *does* take a plugin arg again as of 2.1.6 — but only to persist the feature-image `hiddenNotes` list via `writeOptions`; the year-overview popup state remains transient and is **not** persisted through it.)
  - **`displayedMonth` is the sole state.** No separate "displayed year" and no view-mode state: the shown year is `displayedMonth.year()`, year arrows do `displayedMonth.clone().add(±1, "year")`, clicking a month does `selectMonth(date)` → `displayedMonth = date.clone().month(i).startOf("month")`, which simultaneously moves the day grid and the panel's `.active` highlight (they share `displayedMonth`).
  - **Month dot = any note in that month.** A month cell shows one filled dot (reusing `Dot.svelte`) when it contains any daily, weekly, OR monthly note. Computed by `getMonthsWithNotes(year, daily, weekly, monthly)` in `src/io/yearNotes.ts`, which buckets each store's UID timestamps (`moment.parseZone` of the post-`unit-` portion) into a `boolean[12]`. Weekly notes bucket by their start-of-week month (coarse signal, acceptable). Disabled periodicities have empty stores so contribute nothing.
  - **Store-decoupled.** The vendored `Calendar.svelte` takes `monthsWithNotes: boolean[]` and `showYearOverview: boolean` as props; the app wrapper (`src/ui/Calendar.svelte`) computes `monthsWithNotes` reactively from `$dailyNotes`/`$weeklyNotes`/`$monthlyNotes` + `displayedMonth.year()` and passes it down. The wrapper's reactive uses `(displayedMonth ?? today ?? moment()).year()` because the wrapper's `displayedMonth` defaults to a reactively-assigned `today` that is `undefined` at prop-init time — calling `.year()` on it unguarded threw during the first flush and blanked the entire calendar.
  - **Settings:** `showYearOverview: boolean` in `ISettings` (default `false`, opt-in), toggle in "Calendar behavior" after "Show Today button on mobile". Top-level boolean — the `loadOptions` `{ ...old, ...options }` spread preserves the default; no per-object merge needed.
  - **Styling reuses calendar tokens; intentionally compact.** `YearGrid` cells mirror `.day` (same `--color-background-day`, `--interactive-accent` fill when the cell is the displayed month, `--color-text-today` when it's the current real month, `--interactive-hover`). The framing/elevation comes from the `.year-popup` card (in `Calendar.svelte`), so `YearGrid` itself carries no border/margin. The year number is `0.95em` (not the large `Nav` `.title` size), month labels are `0.8em` (matching `.day`), and cell padding is tight. Year arrows reuse `Arrow.svelte`.

## Current architecture

- **Manifest** (`manifest.json`): id `calendar-plus`, name `Calendar Plus`, minAppVersion `0.9.11`. (Current version is in the Project overview above.)
- **Internal namespaces** (`src/constants.ts`):
  - `VIEW_TYPE_CALENDAR = "calendar-plus-view"`
  - `TRIGGER_ON_OPEN = "calendar-plus:open"` (event external plugins listen on to inject sources)
- **Settings** (`src/settings.ts`): `CalendarSettingsTab` extends `PluginSettingTab`. Periodic-note sections are rendered into per-section wrapper divs via `renderPeriodicNoteSection(sectionEl, ...)`, so toggling Enable re-renders only that section (no full-tab rebuild, no scroll jump).
- **Periodic note helpers** (`src/io/periodicNoteHelpers.ts`): pure path/format/parse helpers + vault scan. Includes `getDateFromFilename`, `getPeriodicNotePath`, `applyTemplateTokens`, `getAllPeriodicNotes`, `getDateUID`, `getPeriodicNote`, `ensureParentFolderExists`, `getTemplateInfo`.
- **Shared creation + open logic** (`src/io/periodicNotes.ts`): `createPeriodicNote(...)`, `tryToCreatePeriodicNote(...)` (confirm-before-create modal), `getLeafForModifierClick(ctrlPressed, settings, workspace)` (returns the destination `WorkspaceLeaf` — plain click → current pane; modifier + `ctrlClickOpensInNewTab` → new tab; modifier + !setting → vertical split), and `tryToCreatePeriodicNoteAndOpen(periodicity, date, ctrlPressed, settings, cb?)` (creates + opens via the modifier-click leaf picker + optional post-open callback). 1.8.1 consolidated the per-period open/create flow here; `view.ts` uses `getLeafForModifierClick(...)` directly for the existing-file open paths so all five periodicities pick the destination leaf the same way.
- **Per-period wrapper files** (`src/io/{daily,weekly,monthly,quarterly,yearly}Notes.ts`): thin one-line delegators to `tryToCreatePeriodicNoteAndOpen("<periodicity>", ...)` after the 1.8.1 consolidation. They remain as named entry points for call-site clarity (`tryToCreateDailyNote`, etc.) and are the only place the `"daily" | "weekly" | "monthly" | "quarterly" | "yearly"` periodicity literal is captured per-file.
- **Stores** (`src/ui/stores.ts`): module-level Svelte writables for `settings`, `dailyNotes`, `weeklyNotes`, `monthlyNotes`, `quarterlyNotes`, `yearlyNotes`, `activeFile`, and `secondDailyNotes`. Each per-period store has a `reindex()` method that rescans the configured folder; the wrapper (`src/ui/Calendar.svelte`) calls these **selectively and debounced** on settings change — only for stores whose own `folder`/`format`/`enabled` changed (2.1.2), and the rescan is debounced ~300ms so per-keystroke folder/format edits coalesce to one walk (2.1.4). The mount reindex stays immediate (calendar populated on open). Don't revert to reindexing all stores synchronously on every settings change. `secondDailyNotes` is created by a dedicated `createSecondDailyNotesStore()` factory (not `createPeriodicNotesStore`) because it reads from `settings.secondDaily` rather than a `Periodicity`-keyed settings entry; it uses `"daily"` as the periodicity for date parsing and UID generation.
- **View / lifecycle** (`src/view.ts`): `CalendarView` extends `ItemView`. Owns the Svelte `Calendar` component, vault file events, settings subscription (registered via `this.register(...)` so it cleans up on view detach), and the periodic-note open/create methods.
- **Plugin registration / commands** (`src/main.ts`): `CalendarPlugin` extends `Plugin`. Registers the view, ribbon icon, and three commands (`show-calendar-view`, `open-weekly-note`, `reveal-active-note`). Commands look up the live view fresh via `getCalendarView()` / `ensureCalendarView()` helpers — no stale `this.view` reference.
- **Calendar UI**: vendored under `src/ui/calendar-ui/` (`components/*.svelte` plus `localization.ts`, `metadata.ts`, `types.ts`, `utils.ts`). Origin: the patched `obsidian-calendar-ui` 0.3.12 source (the patch added quarter navigation, clickable month/year/quarter labels, weekend col classes, and week-num grid borders + active state). The npm dependency and the `patches/obsidian-calendar-ui+0.3.12.patch` file have been removed. `Day.svelte` and `WeekNum.svelte` use the local `getDateUID` from `src/io/periodicNoteHelpers` (with `"daily"` / `"weekly"` periodicity) instead of `obsidian-daily-notes-interface`, which is no longer a transitive dep. Component-internal CSS now lives inside each vendored component's `<style>` block; `styles.css` retains only the wrapper-state-dependent rules (depend on `.daily-enabled`/`.monthly-enabled`/etc. on the outer Calendar Plus wrapper) and the `.calendar-plus-suggest` rule for the document-body-attached settings autocomplete.
- **Autocomplete** (`src/ui/file-suggest.ts`): folder/template path autocomplete in settings. As of 2.1.3, built on Obsidian's native **`AbstractInputSuggest<T>`** (positioning, keyboard nav, popup lifecycle, and keymap scope are all handled by Obsidian) — this replaced a ~150-line hand-rolled `TextInputSuggest` (no-Popper, manual `getBoundingClientRect` positioning + scroll/resize listeners + a keymap `Scope`), ported from the Daily Checklist plugin, which lived in `src/ui/suggest.ts` (now deleted), along with its `.calendar-plus-suggest` rule in `styles.css` (also deleted — the native popup is themed by Obsidian). `FolderSuggest` / `FileSuggest` keep a 2-arg `(app, inputEl)` constructor and override `selectSuggestion` to `setValue(...) + inputEl.trigger("input") + close()`, so the existing `Setting.addText(...).onChange(...)` handlers in `settings.ts` still save on selection with **no call-site changes**. (`AbstractInputSuggest` is available in the pinned `obsidian@1.8.7` types.)

## Important implementation notes

- **Dependency + type-aware lint (added 2.0.1 — do not regress).** The `obsidian` dependency must be the **standard npm package** (`"obsidian": "1.8.7"` in `devDependencies`), **not** a GitHub tarball (`obsidianmd/obsidian-api#…`). The Obsidian plugin-review environment does a **registry-only `npm ci`** and cannot fetch a GitHub-sourced dependency — when it fails to install, the whole dep tree (obsidian, moment, svelte) resolves as `any`/error types and the review's **type-aware `@typescript-eslint`** rules emit hundreds of `no-unsafe-*` warnings across every file (this tanked the 2.0.0 review score ~15%). `versions.json` / `minAppVersion` stay at `1.8.7`, matching `obsidian@1.8.7`. To mirror the review locally, the flat **`eslint.config.mjs`** (2.1.0 migrated `.eslintrc.js` → flat config for ESLint 9 + `typescript-eslint` 8 and removed `.eslintignore`; 2.1.1 renamed it to `.mjs` / ESM and switched from the deprecated `tseslint.config()` to `defineConfig()` + `globalIgnores()` from `eslint/config`, to clear the review's `require()` and deprecated-`config()` warnings) sets `languageOptions.parserOptions.project` + extends `tseslint.configs.recommendedTypeChecked` (with `require-await` / `prefer-regexp-exec` / `restrict-template-expressions` disabled — the review doesn't flag those; **`@typescript-eslint/no-deprecated` is enabled** so local lint mirrors the review's deprecation findings), and its `globalIgnores` covers `reference/`, `main.js`, and JS/MJS/CJS config files (the lint is scoped to `.ts`, replicating the old `--ext .ts`). `@eslint/js` is pinned in `devDependencies` (the review flags imported packages not listed there). Keep `npx eslint .` at **zero** errors. The mounted Svelte component instance in `view.ts` is typed via a local `CalendarComponent` interface (`tick`/`setDisplayedMonth`) + a cast of the `mount(...)` result (type-aware tooling sees `.svelte` default imports as `any`); don't revert that to `private calendar: Calendar`. (Through 2.1.1 the component used the legacy `createClassComponent` bridge with `$set`/`$destroy` and a suppressed `no-deprecated` directive; 2.1.2's runes migration replaced all of that with native `mount()`/`unmount()`, so there's no longer a deprecation to suppress.)
- **Hand-rolled `moment` types (added 2.0.2 — do not reintroduce a `"moment"` import).** `src/types/moment.ts` no longer imports from the `"moment"` package — the review flags any such import via `no-restricted-imports` (Obsidian bundles moment and wants it imported from `"obsidian"`, but Obsidian only re-exports the *value*, not nameable types; and deriving types via `ReturnType<typeof moment>` collapses `Moment` to `any` and reintroduces ~60 `no-unsafe-*` warnings). Instead the file **hand-rolls** the slice of moment's API the plugin uses: a `Moment` instance interface (getter/setter overloads for `year`/`month`/`date`/`day`/`weekday`; `clone`/`format`/`add`/`subtract`/`startOf`/`get`/`set`/`isSame`/`week`/`calendar`/`localeData`/`isValid`/`locale`), a `MomentFactory` (call signatures + `parseZone`/`locale`/`locales`/`weekdays`/`weekdaysShort`/`monthsShort`/`updateLocale`/`localeData`), `DurationUnit`, `Locale` (= `Record<string, unknown>`), and `WeekSpec`. **Maintenance:** this mirror must be extended whenever a new moment method/overload is called anywhere in `src/` (including `.svelte` files — note `tsc` and `eslint --ext .ts` don't check `.svelte`, only CI `svelte-check` does, so a missing method may only surface in CI). Performance impact is zero (types erased at build; runtime value is still Obsidian's bundled moment). **Keep an eye on whether this workaround is still needed** and re-review periodically — if Obsidian starts re-exporting moment's types, or the review stops flagging a type-only `"moment"` import, the simpler `import type { Moment, Locale } from "moment"` seam could come back. See FUTURE_PLANS § "Hand-rolled moment types".
- **Do not edit bundled `main.js` directly.** Always edit source under `src/` and run `npm run build`. The bundled file is a build artifact.
- **Use npm**, not yarn. The lockfile of record is `package-lock.json`. This repo intentionally does not include `yarn.lock`; do not re-introduce it.
- **Baseline build state.** The 2.1.0 `svelte-check` 4 no longer hits the old `mappings.wasm` flake — `npm run build` is reliably clean. `svelte-check` now reports **0 errors + 18 accessibility (`a11y_*`) warnings** on the vendored calendar components (keyboard handlers / ARIA roles on clickable `<div>`/`<span>` elements). These a11y warnings are **expected and intentionally deferred** (see FUTURE_PLANS.md → keyboard accessibility) — don't chase them unless doing the dedicated a11y pass. TypeScript reading `node_modules/obsidian/obsidian.d.ts` / `node_modules/@codemirror/view/dist/index.d.ts` may still surface dependency-level notes unrelated to source changes; don't chase those unless your edits involve those files.
- **Run `git status` before and after changes** so you can confirm the diff matches intent.
- **Keep commits small and focused.** One logical change per commit. Each commit's diff should fit on a screen.
- **Do not run `npm audit fix`.** It can rewrite the lockfile and break the patched dependency setup. The standing audit-findings disposition lives in `FUTURE_PLANS.md` → "Review npm audit findings for dev dependencies"; `--force` is especially unsafe because it would upgrade Svelte to a breaking major version and may bump Rollup/Moment outside the pinned ranges.
- **Svelte advisory disposition + modernization (June 2026).** The Obsidian checker flags svelte dependency advisories (currently the mXSS GHSA-8266; `npm audit` also shows four Feb-2026 SSR-XSS ones patched only in svelte 5.51.5/5.53.5). **All are SSR-only and not reachable here** (no SSR, no `{@html}`), and svelte ships new SSR advisories regularly, so no version stays clean — **accepted, do not chase via migration for the checker's sake.** The **Svelte 5 + full toolchain modernization** that was once planned here is now **done**: 2.1.0 migrated the build/lint chain (Svelte 5, TS 5, Rollup 4, ESLint 9 flat config) keeping components on legacy syntax via `createClassComponent`, and 2.1.2 completed it by rewriting the components to runes and moving `view.ts` to native `mount()`/`unmount()`. Historical findings (incl. the contained-but-skipped Svelte 4 path) are in `FUTURE_PLANS.md` → "DONE (2.1.0): Svelte 5 + full toolchain modernization".
- **`reference/` is local-only, git-ignored, read-only comparison material.** All subdirectories are listed in `.gitignore`, are not part of the published plugin, and **must not be committed or modified**. They may not exist in every contributor's checkout — use them as read-only reference if present, ignore them otherwise. Current contents:
  - `obsidian-calendar-plugin-1.5.10/` — Liam Cain's original Calendar plugin (the direct ancestor of Calendar Plus). Primary reference for view lifecycle, periodic note open/create patterns, and the original dot/streak sources.
  - `obsidian-calendar-plugin-1.5.13-FBarrca/` — FBarrca's Calendar fork (one of the inspirations for Calendar Plus; adds quarterly navigation and other enhancements over 1.5.10).
  - `obsidian-calendar-plugin-2.0.0-beta.2/` — Liam Cain's unreleased 2.0.0 beta of the Calendar plugin. Useful for seeing the direction the upstream was heading.
  - `obsidian-calendar-ui-master/` — source of the `obsidian-calendar-ui` npm package (the Svelte component library Calendar Plus vendored into `src/ui/calendar-ui/`). Authoritative reference for the original `Day.svelte`, `WeekNum.svelte`, `Nav.svelte`, `Calendar.svelte`, and associated types.
  - `obsidian-periodic-notes-1.0.0/` — Liam Cain's Periodic Notes plugin. Reference for periodic note path/format/template helpers that were ported into `src/io/periodicNoteHelpers.ts`.
  - `obsidian-periodic-notes-1.0.0-beta.3/` — beta version of Periodic Notes; useful for seeing API changes between the beta and the 1.0.0 release.
  - `notebook-navigator-3.1.2/` — Notebook Navigator plugin (Johan Sanneblad). A modern Obsidian plugin with its own calendar view (`src/components/calendar/`) built in React. Key ideas worth studying for Calendar Plus: (a) **feature images** — thumbnails extracted from daily notes displayed inside calendar day cells (`useCalendarFeatureImages.ts`, `CalendarDayButton.tsx`), backed by IndexedDB for caching; (b) **year panel view** (`CalendarYearPanel.tsx`) showing all 12 months at once; (c) hover tooltips on day cells (`CalendarHoverTooltip.tsx`). Also uses esbuild, Vitest, and the declarative `getSettingDefinitions()` settings API introduced in Obsidian 1.13.0.
  - `obsidian-dev-skills-main/` — Agent Skills for Obsidian plugin development, maintained by Liam Cain (Obsidian's staff person focused on plugins). Treat as authoritative guidance. Currently contains one skill: `settings/SKILL.md`, which documents the declarative `getSettingDefinitions()` API introduced in Obsidian 1.13.0 — a potential future replacement for Calendar Plus's imperative `display()` settings tab. Directly installable into Claude Code by copying contents into `.claude/` at the repo root.
- **Stay scoped to the active repo.** Do not search outside `/Users/matt/Desktop/obsidian-plugin-merge/calendar-plus/` (or `reference/` when comparing against the original plugins). Do not use `find /`, `find ~`, `find ..`, broad recursive `rg` rooted outside the repo, or any other command that traverses parent directories — macOS TCC will surface privacy prompts and you may accidentally access user-vault, Documents, Downloads, or Desktop content unrelated to the project. If you need a file from outside the repo, ask the user for its path instead.
- **Do not commit unless explicitly asked.** Show the diff summary and wait.

## Recent important fixes (1.6.0)

In rough order:

- Removed direct dependency on `obsidian-daily-notes-interface`.
- Consolidated periodic note creation into `src/io/periodicNotes.ts`; per-period files are thin wrappers.
- Added folder/template autocomplete in settings (ported from periodic-notes).
- Fixed `this.calendar.tick is not a function` crash on settings change — root cause was a leaked `settings.subscribe` in `CalendarView` constructor; now wrapped in `this.register(...)` plus a `typeof` guard at the call site.
- Guarded command callbacks (`open-weekly-note`, `reveal-active-note`) against missing or stale `CalendarView` — now look up the live view fresh per invocation; `open-weekly-note` mounts the view if needed, `reveal-active-note` no-ops if absent.
- Initialized `window._bundledLocaleWeekSpec` from settings tab `display()` via `configureGlobalMomentLocale(...)` so the settings tab can render before the calendar view has mounted.
- Deep-merged saved per-period settings on load so a partial `data.json` can't wipe `format`/`folder`/`template` defaults (would have caused `"Invalid date"` filename collisions).
- Settings periodic-note sections re-render only their own wrapper div on toggle (no full-tab rebuild → no scroll jump).
- Added Calendar Plus ribbon icon; clicking reveals the existing leaf or creates one.
- Visual affordance refinements: cursor/hover behavior gated by enabled-class wrappers (`monthly-enabled`, etc.), TODAY/arrow hover opacity, suppressed transient `.day:active` purple flash, weekend column shading via `--color-background-weekend`, week-num font-size matched to day numbers, dot-container reservation to prevent week-num jitter, `localeData` spread to a fresh object so `<svelte:options immutable />` re-renders weekend cols on weekStart change.

## UI internals cleanup (landed in 1.7.18)

A small behavior-preserving polish pass landed as part of the 1.7.18 release, alongside the configurable-weekend-days work. Items **kept**:

- **Dot data model trim**: dropped the unused `color` field from `IDot`, made `className` optional, removed the unused `isActive` prop from `Dot.svelte` and its unreachable `.active.filled` / `.active.hollow` CSS rules, trimmed the streak-source dot payload to `{ isFilled: true }`.
- **Dead `th { ... }` block removed from `WeekNum.svelte`** (the component renders only `<td>`, so the rule fired on nothing).
- **`.active` selector in `Day.svelte`** rewritten as `.day.active` / `.day.active.today` for clarity. No behavior change — Svelte-scoped CSS already pinned the selector to elements inside Day.svelte.
- **`Nav.svelte` comment rephrase**: removed the only remaining `!important` mention anywhere in `src/`, so grep / checker output stays clean.

Item intentionally **not kept**:

- **Unused helper parameters** (the `..._args: unknown[]` rest params on `getDaysOfWeek`, `getMonth`, `getDailyMetadata`, `getWeeklyMetadata`) were briefly removed in the same pass, but the cleanup was reverted. Removing the params required `(localeData, getMonth(...))` / `(today, getDailyMetadata(...))` comma-operator expressions in `Calendar.svelte` to keep Svelte's reactive-dependency chain intact. The comma-operator pattern is technically correct but less readable and more surprising than passing `localeData` / `today` as ordinary arguments — a reader shouldn't have to recognize the comma-operator idiom to see the reactive deps. The current code keeps the rest-args; `Calendar.svelte` keeps `localeData` / `today` as ordinary call arguments so reactivity is obvious at the call site. A future pass that wants to clean these up should pick a style that doesn't hide reactive deps from readers (e.g. a named `_trigger?: unknown` param is fine, comma operators are not).

## Known baseline / non-blocking issues

These are catalogued in `FUTURE_PLANS.md`. Highlights:

- `styles.css` is intentionally limited to wrapper-state-dependent rules (gated on `.daily-enabled` / `.monthly-enabled` / `.yearly-enabled` / `.quarterly-enabled` classes set on the outer Calendar Plus wrapper) and the `.calendar-plus-suggest` rule for the autocomplete dropdown attached to `document.body`. Component-internal styling already lives inside each vendored Svelte component's `<style>` block.

None of the above blocks shipping.

## Future plans

See `FUTURE_PLANS.md` for full descriptions. Short list:

- **Optional: active-file correctness for monthly / quarterly / yearly (deferred)** — `getDateUIDFromFile` only handles daily / weekly. Nav.svelte doesn't consume `selectedId` for month / year / quarter labels, so M/Q/Y active UIDs would be dead code today. Revisit only if active-file styling for header labels is wanted.
- **Optional: Svelte settings migration (deferred)** — cleaner conditional UI, slide animations. Not planned; the current per-section re-render in `src/settings.ts` works well. Revisit only if the settings UI becomes harder to extend.
- **Optional: Resolve remaining Obsidian plugin warnings (deferred)** — after 1.7.15 (1.7.14 was the last release that touched checker state, replacing `localStorage.getItem("language")` with `getLanguage()` via a d.ts pin bump to 1.8.7) the checker reports zero source-code Errors and a small residual warning set. Two root causes remain: the intentional single `no-restricted-imports` warning on `src/types/moment.ts:2` (the type-only seam that lets the checker resolve `Moment` precisely — only fixable by hand-rolling local Moment interfaces); and Svelte component instance typing on the mounted calendar's exports (`this.calendar.tick` / `setDisplayedMonth`) in `view.ts` (framework-typing noise). Both documented in `FUTURE_PLANS.md`.
- **Accepted: Obsidian checker behavior recommendations** — the checker also flags **Vault Enumeration** as a behavior characteristic (via `Vault.recurseChildren` for note discovery and `vault.getAllLoadedFiles()` for settings autocomplete). Both are required for Calendar Plus's core features (note-existence dots, folder/template autocomplete) and are already scoped when users configure folders. Documented as an accepted architectural trade-off in `FUTURE_PLANS.md` → "Accepted Obsidian checker behavior recommendations". Do not "fix" without revisiting that trade-off.

## Release prep

When the user explicitly asks to bump the plugin version, treat the changelog as part of the same release-prep task unless the user says otherwise. Update these five files together in the same commit:

- `manifest.json` — `version`
- `package.json` — `version`
- `package-lock.json` — both `version` fields (top-level + the root `packages[""]` entry)
- `versions.json` — add `"<new-version>": "<minAppVersion>"`
- `CHANGELOG.md` — add a `## <new-version>` section at the top, summarizing user-facing changes, major refactors, dependency removals, and notable bug fixes since the previous version

Changelog content guidance:
- Lead with what the user will notice. Group related items under short headings if the section is long (see the 1.6.0 entry as a template).
- Mention dependency additions/removals and architectural refactors when they're release-significant, even if user-invisible — future-you will want the context.
- Skip implementation paths and internal symbol names unless they aid debugging.

Proposing version bumps:
- For major user-facing features, major refactors, dependency changes, or other release-significant cleanup, mention in your plan whether a changelog entry and version bump are warranted, and flag the proposed version (major / minor / patch) so the user can confirm.
- For small fixes, do not silently invent a new version number or changelog entry. Ask, or flag the bump as optional if unclear.
- Do not commit the version bump unless the user explicitly asks.

## Suggested workflow for future Claude sessions

1. **Read this file first.** Then read `FUTURE_PLANS.md` if the user's request touches deferred work.
2. **Run `git status`** to see what's already in flight before editing.
3. **Inspect the relevant files** before editing — don't assume they match a memory of an earlier session.
4. **Make one focused change at a time.** Match the scope of the user's request; don't bundle unrelated cleanup.
5. **Run `npm run build`** after each change. Confirm `created main.js` and that any warnings are pre-existing baseline noise (not in your changed files).
6. **Report the diff summary** (`git diff <files>`) and explain what changed and why.
7. **Do not commit** unless the user explicitly asks. Wait for the next instruction.

---
> Source: [mattmaiorana/calendar-plus](https://github.com/mattmaiorana/calendar-plus) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-08-12 -->
