## earshot

> Cross-platform desktop app for **real-time translation of system audio** (meetings, videos, calls). **Named "Earshot" on 2026-07-24** (previously "Real-Time Translator") — the display string; the npm slug `claude-real-time-translator` and the `appId` `com.amir.realtimetranslator` deliberately keep the old name, since changing the `appId` would orphan existing installs. **The GitHub repo WAS renamed to `Earshot` on 2026-07-27** (github.com/amirmokarram/Earshot; GitHub redirects the old clone URL, `origin` updated), and `electron-builder.json`'s publish block was updated to `Earshot` so CI releases still target the right repo. The user (Amir) listens to audio in one language and reads a live translation in another. The **language pair is fully user-configurable** (Settings → Languages) — any supported source→target pair from a curated catalog.

# Earshot — Project Context

Cross-platform desktop app for **real-time translation of system audio** (meetings, videos, calls). **Named "Earshot" on 2026-07-24** (previously "Real-Time Translator") — the display string; the npm slug `claude-real-time-translator` and the `appId` `com.amir.realtimetranslator` deliberately keep the old name, since changing the `appId` would orphan existing installs. **The GitHub repo WAS renamed to `Earshot` on 2026-07-27** (github.com/amirmokarram/Earshot; GitHub redirects the old clone URL, `origin` updated), and `electron-builder.json`'s publish block was updated to `Earshot` so CI releases still target the right repo. The user (Amir) listens to audio in one language and reads a live translation in another. The **language pair is fully user-configurable** (Settings → Languages) — any supported source→target pair from a curated catalog.

> Full detail lives in [`docs/memory/`](docs/memory/). This file is the auto-loaded summary — it travels with the repo, so project context survives a machine/OS change. The canonical copies I auto-read also live in `~/.claude/projects/D--Claude-RealTimeTranslator/memory/`; keep the two in sync (treat `~/.claude` as source of truth I edit, then copy here before committing).

## Stack
- **Angular 21** (standalone components, signals, `@if`/`@for`) — renderer
- **Electron 43** — desktop shell
- 2 switchable streaming STT providers: **DeepGram** (cloud, WebSocket) and **Whisper** (local, WhisperLive WebSocket) — renderer-side `ISttStream` strategy
- **Providers are data, not code** (Phase 14): one catalog serves BOTH translation and assist. `electron/config/providers.json` (+ an optional `<userData>/providers.json` overlay, **never containing credentials**) declares each provider over one of 8 built-in wire-protocol **adapters** in `electron/providers/adapters/`. Ships Claude, OpenAI, Ollama, OpenAI-compatible, Google, DeepL, Microsoft, LibreTranslate — see [`docs/memory/provider-catalog.md`](docs/memory/provider-catalog.md)
- Configurable source/target languages from a curated catalog — **single source of truth `src/app/core/models/languages.json`** (renderer imports it; the build copies it to `dist-electron/config/` so the main process reads it at runtime via `electron/languages.ts`); per-cell text direction/font driven by each language's `rtl` flag (Vazirmatn for RTL)
- Dark theme; Vazirmatn font for Persian/RTL text

## Architecture (key rules)
- **Translation & assist API keys + calls stay in the Electron MAIN process** — never move them to Angular services. Keys live in `settings.providerConfigs[providerId]` (userData), **one bag per provider shared by both features**; the catalog JSON is refused outright if it contains a key-shaped field anywhere.
- **A provider is a catalog entry naming an adapter.** Adding anything with an OpenAI-shaped API (Groq, OpenRouter, Together, DeepSeek, Azure OpenAI, LM Studio, vLLM) is a JSON edit over the `openai-chat` adapter — no code. Only a genuinely new wire protocol needs an adapter class. Never put provider knowledge in the renderer; if a panel needs to know something about a provider, it belongs in the descriptor.
- **STT streaming is the exception: it runs in the RENDERER** (browser `WebSocket`, like DeepGram's subprotocol-token auth). Both providers live behind a renderer-side `ISttStream` strategy (`src/app/core/services/stt/`); `TranscriptionService` owns sentence segmentation and picks the strategy from `stt.provider`. Whisper uses a local WhisperLive WS — see [`docs/memory/whisper-stt-provider.md`](docs/memory/whisper-stt-provider.md).
- Renderer ↔ main via secure IPC bridge (`contextBridge` + `electron/preload.ts`, typed `ElectronAPI`).
- Router uses **HashLocationStrategy** (required for `file://` prod load; also how the overlay targets `#/overlay`).
- Translation events (`translation:source`/`:chunk`/`:complete`) are **broadcast to all windows** so the overlay mirrors the main window for free.
- Settings persist to `userData/settings.json`. The `AppSettings` schema is defined once in `shared/app-settings.d.ts` (shared across both TS contexts); defaults live in `electron/config/default-settings.json`.
- TS isolation: `tsconfig.app.json` (renderer, browser types) vs `tsconfig.electron.json` (main, CommonJS/Node).

## Build & run (Windows / PowerShell)
- `npm run electron:dev` — dev (ng serve :4200 + Electron hot-reload)
- `npm run electron:compile` — compile Electron TS only → `dist-electron/`
- `npm run electron:dist:win` — package Windows installer
- After any **main-process** change: recompile Electron + restart.

## Critical gotchas (see docs/memory/gotchas-and-lessons.md)
- **Web Speech API does NOT work in Electron** (no Google keys → always `error: network`). Use DeepGram.
- **Desktop audio capture needs a video track** — request both in `getUserMedia`, discard the video track.
- **`:host { display:flex; height:100% }`** required on full-height components or flex children grow unbounded.
- **LibreTranslate** needs explicit `Content-Length` (else "socket hang up"); `libretranslate.com` is paid — run local Docker `--load-only en,fa`.
- **CSP must include `wss:`** for streaming providers.
- **A new `AppSettings` field touches THREE files** — `shared/app-settings.d.ts`, `electron/config/default-settings.json`, and the renderer's own `DEFAULT_SETTINGS` in `electron-bridge.service.ts` (Electron compiles fine; `ng build` fails). **`ProviderDescriptor` is likewise declared twice** — `electron/providers/adapter.interface.ts` and `src/app/core/models/app.models.ts` — with no compiler watching either, since the two tsconfigs never see each other.
- **`__dirname` is `dist-electron/` only for a top-level main file.** `electron/providers/catalog.ts` compiles one level deeper and needs a `'..'` hop to reach `dist-electron/config/`; copying `languages.ts`'s path expression there silently loaded nothing.

## Status
Phases 1–5 done: capture → STT → translate → live dual-pane display, **overlay mode**, **history export (TXT/SRT)**, **assist mode**. **Question Bank** (2026-07-16): assist-panel "Query From Q Bank" — an LLM router picks which prepared markdown Q&A file (local folder, Settings → General) answers the selected question; match → open the file, no match → generated interview-ready answer (prompt: live-read file → Settings editor → built-in default; see `docs/memory/question-bank.md`). **System tray + close-to-tray** (2026-07-16): tray menu (show/hide window, start/stop capture, toggle overlay, quit); X hides to tray while translation keeps running (`settings.tray.closeToTray`, Settings → General toggle); capture toggle reaches the renderer via `command:toggle-capture` → `CommandService`. **Global hotkeys** (2026-07-16): `electron/hotkeys.ts` registers `settings.hotkeys` (`Ctrl+Alt+C` capture, `Ctrl+Alt+O` overlay, `Ctrl+Alt+H` show/hide; `''` = disabled) system-wide, re-applied live on every settings save; Settings → Hotkeys panel records combos via keydown (`event.code`); nothing registers under `TRANSLATOR_E2E`. **Roadmap complete — no planned features remain.** Post-roadmap polish (2026-07-17): assist stop-generation (requestId-tagged `assist:chunk`/`assist:complete` events) + copy-answer; persistent audio-source selection; **always-on-top** main window (`settings.window.alwaysOnTop` behind three synced controls — header pin, tray checkbox, Settings → General — one `toggleAlwaysOnTop()` path in main broadcasts `window:always-on-top`). **Phase 6 — STT accuracy (2026-07-21):** errors compound (bad STT → faithfully translated wrong words), so improve recognition at the source. **Custom vocabulary** (`stt.keyterms` → DeepGram `keyterm=` on Nova-3 / legacy `keywords=` on Nova-2, ≤100 terms) · **selectable DeepGram model** (`stt.deepgramModel`, default `nova-3`; was hardcoded `nova-2`) · `numerals=true` · **configurable Opus bitrate** (`stt.audioBitrateKbps`, default 32; was hardcoded 16 — applied at MediaRecorder creation, so it needs a capture restart) · **confidence surfacing** (`SttCallbacks.final` carries it → `TranslationEntry.confidence`; `takePending()` now returns `PendingSentence[]`) with a dotted underline on low-confidence rows and a per-session `STT %` readout — comparable **only within one model**. **Phase closed** — Whisper `initial_prompt` (6E) deliberately dropped: Whisper is a *batch* model faking streaming (latency floor, self-revising segments), so it stays as the **offline/privacy fallback only** and gets no further investment; Settings/README now label it honestly. Don't re-propose Whisper accuracy work. **UI cleanup (2026-07-21):** the header's translation-provider combo-box was removed — Settings → Providers is the single control for switching provider; the active one is still displayed as `.provider-tag` in the translator's target-column header. **Phases 7–9 — meeting recording, review & notes (2026-07-21):** each capture session is recorded to `<recordings>/meeting-….webm` (own `MediaRecorder` in `RecordingService`, optional system+mic mix that is **recording-only** and never reaches the recognizer; Settings → Recording panel) alongside a `.json` transcript sidecar carrying **recording-relative `offsetMs`** per line (from `TranscriptionService.bufferStartedAt` → `TranslationEntry.startedAt`, i.e. when the speech *began*, not when the translation returned). A `#/review` route lists past sessions, plays them over a custom **`rec://` scheme** (the CSP refuses `file://`; `stream: true` gives the range requests that make seeking fast) with the scrubber driven by the sidecar's `durationMs` (a streamed WebM reports `Infinity`), seeks from any transcript line, holds session/per-line **notes** in that same sidecar, and can hand a whole session — or one line — to assist. Recording never gates translation. Line timing comes from **DeepGram's per-word `start` times** (a single finalized fragment often holds several sentences, so per-fragment timing gives them all the same offset), and anything identifying a row keys on its **index, not `offsetMs`** — offsets are not unique. **Still not verified:** the `mix`/`separate` mic modes, which only engage for system audio. **Faster sentence commits (2026-07-22):** a finalized tail is committed as soon as it ends in unambiguous `.!?` instead of waiting for the next sentence to start — one shared `isRealBoundary()` predicate now vetoes abbreviations/initialisms for both the tail and `drainSentences`, so `"I met Dr. Smith"` stays one row (there is deliberately **no decimal rule** — see gotchas). **Restore Defaults (2026-07-22):** every Settings panel resets its own fields (footer bound to `activeNode()`), General adds a confirmed "Restore all"; `RESET_SECTIONS` in `electron/settings-store.ts` is a **whitelist of leaf paths per panel** so API keys, endpoints, folder paths and the chosen mic are never reset, and the split must follow **what each panel renders** (filing a field by concept instead shipped a silent no-op). **Assist scrolling (2026-07-23):** the assist thread **never scrolls itself while an answer streams** — an answer you're part-way through reading must stay put. It performs exactly one scroll: on ask, the new question is moved to the top of the panel so the answer fills the space below it. The live end then drifts out of view and a sticky "Jump to latest" pill catches up on demand. Don't reintroduce stick-to-bottom auto-follow. **App icon (2026-07-22):** the old icon read "EN→فا" — a language pair the app dropped when Settings → Languages shipped — so it was replaced by an alphabet-free speech bubble + waveform. **SVG is the source, `.ico` is build output:** `public/icon.svg` (squircle tile → window/installer/favicon) and `public/tray.svg` (bare glyph → tray) are rasterized by `npm run icons` (`scripts/generate-icons.mjs`, which runs under Electron so Chromium does the rasterizing — no sharp/ImageMagick). **The header logo renders `tray.svg` via `<img>`** rather than inlining its own copy; it used to be a hand-coded globe unrelated to any icon file, which is exactly how the two marks drifted apart. `tray-active.ico` (teal bars) is shown while capturing — `TrayManager.applyIcon()` swaps it in on `audioCapture.isActive()` and reverts on stop. **Phase 10 — translation context & glossary (2026-07-24; verified 2026-07-25):** the translator was stateless (one isolated sentence per call), so pronouns/ellipsis/terminology had nothing to resolve against. New `settings.translation.{contextTurns (default 3, 0=off), glossary}`. **Context:** `TranslationService.recentTurns()` sends the last N committed rows (snapshotted before the await) as `TranslationRequest.context`; `takeContext()` re-clamps in MAIN (the one authority); **Claude & OpenAI replay them as real user/assistant turns**, Ollama + MT providers ignore them. **Glossary** (`electron/glossary.ts`, `term = translation` forces a rendering / bare `term` = never translate): split by a **required** `ProviderMeta.glossaryMode` — `'prompt'` (Claude/OpenAI/Echo, injected into the system prompt) vs `'protect'` (MT APIs **and Ollama**, `%%n%%` placeholder swap+restore around the call); `relevantGlossary()` sends only terms present in the sentence. Both applied centrally by `applyGlossary()` in ipc-handlers. Two gotchas: `translation:source` always broadcasts the ORIGINAL text; **streaming is skipped for a sentence with protected terms** (placeholders resolve only on the full output). Anti-drift: `TRANSLATION_CONTEXT_NOTE` is appended **only when turns are sent** (a chat model instinctively answers the last line instead of translating). `RESET_SECTIONS['translation-context']` resets only `contextTurns` — the glossary is authored content and is never wiped. E2E `translation-context.spec.ts` (10: prompt path via EchoProvider `__ECHO_CONTEXT__`/`__ECHO_GLOSSARY__` sentinels, MT path via a sibling `EchoProtectProvider` asserting on the committed row). **Verified 2026-07-25:** Amir live-tested with Claude (context + glossary) — translates rather than converses, terminology holds, MT protect/restore survives. **Phase 13 — window geometry (2026-07-28):** the main window reopens at its last size/position/maximized state. `settings.window.{bounds?, maximized?}` — **optional**, so absence means "never saved" (first boot = shipped size + OS placement) and the three-file rule below doesn't apply. New `electron/window-state.ts`: `usableBounds()` re-validates a saved rect against the displays connected *now* (off-screen → re-centre keeping the size), `trackWindowState()` debounces resize/move/maximize/unmaximize and flushes synchronously on `close` via the new `SettingsStore.updateSync()`. Snapshots use **`getNormalBounds()`, not `getBounds()`** (see gotchas). The window is built `show: false` so a restored `maximize()` beats the first paint, then `show()` synchronously. **Main window only** — the overlay keeps per-session placement, by choice. The whole `window` section stays out of `RESET_SECTIONS` (live state, no Settings UI). E2E `window-state.spec.ts` (3) relaunches a second app over the same userData dir via the extracted `launchApp()` in `e2e/fixtures.ts`; 74 passed/1 skipped. **Phase 14 — pluggable, config-driven providers (2026-07-29; verified same day):** "provider" used to mean two unrelated things — translation owned the definitions, while assist had none and silently borrowed its API key from `settings.providers[assistProvider]`, so `openai-compatible` could never hold a key and Ollama shared one endpoint slot with it. Now **one system for both**: a JSON catalog of descriptors over 8 code adapters (`electron/providers/`), 12 provider classes collapsed to 8, `electron/translation/` and `electron/assist/` deleted. Settings: `providerConfigs[id]` (apiKey/endpoint/region/translationPrompt) + `translation.{providerId,model}` + `assist.{providerId,model}`; `activeProvider`/`providers`/`assist.{provider,model,endpoint}` gone, migrated on load **before the defaults merge** (after it, the deep-merge resurrects them). **Key per provider, model per feature** — one key, but Sonnet for translation and Opus for assist. UI: new top-level **Providers** panel (credentials, Test, catalog warnings, open/reload providers.json) with Translation and Assist reduced to a capability-filtered picker + model; the Providers panel deliberately has **no Restore defaults** button since it renders only credentials. `ProviderMeta`/`ConfigField` and `translation:get-providers` deleted. E2E 88/1; committed `ab69801`, pushed. **Verified 2026-07-29** — Amir live-tested it, and the migration was checked field-by-field against his real `settings.json` (key, custom LibreTranslate endpoint, region, and the model each feature was actually using all survived; legacy keys removed, not shadowed). See [`docs/memory/provider-catalog.md`](docs/memory/provider-catalog.md). See `docs/memory/phase-status.md`.

## Working with Amir
Plan first, then build phase-by-phase (he approves each, then says "next"). He tests each phase and reports precise bugs — trust them, verify the real cause. Commit only when asked. Shell is **PowerShell** (the Bash tool has quoting issues here).

---
> Source: [amirmokarram/Earshot](https://github.com/amirmokarram/Earshot) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-08-12 -->
