## datocms-plugin-icons

> This project uses **pnpm** as the package manager. Use `pnpm` instead of `npm` for all commands:

# Project Notes for Claude

## Package Manager

This project uses **pnpm** as the package manager. Use `pnpm` instead of `npm` for all commands:

- `pnpm install` - Install dependencies
- `pnpm test` - Run tests
- `pnpm build` - Build the project
- `pnpm dev` - Start development server

---
> Converted and distributed by [TomeVault](https://tomevault.io/claim/commoncurriculum)
> This is a context snippet only. You'll also want the standalone SKILL.md file — [download at TomeVault](https://tomevault.io/claim/commoncurriculum)
<!-- tomevault:4.0:gemini_md:2026-04-08 -->
