## howard-toolbox

> - Do not delete user comments in code when editing; preserve all existing comments

# AGENTS.md

## Learned User Preferences

- Do not delete user comments in code when editing; preserve all existing comments
- Do not wrap code in tryCatch or other defensive error-handling unless explicitly requested
- Assume required columns/variables always exist; do not write column-existence checks or fallback logic (e.g., `if ("col" %in% names(dt))`)
- Undersampling belongs in model_run scripts, not dataprep scripts; dataprep should output the full dataset
- Explain reasoning and present a plan before changing code; do not make changes without approval; never add items not in the plan — user will ask for double-checks
- Use raw (uncapped) variable names for report continuous vars (e.g., `cur_factor` not `c_cur_factor`, `dscr_ratio` not `c_dscr`) so reports show actual data distributions
- Only add model features that are statistically significant; do not add variables just because they seem relevant
- Store SQL queries in separate files from analysis scripts; put ad-hoc analysis under `Residential/non_qm/adhoc/`
- When presenting model output, pass only one `model_list` at a time to report functions; for insample reports use `ed_fit_full` (all data) with Begg normalization applied, dropping excluded states; default `use_parallel = TRUE` in optimized reporting where supported
- Never edit production model files in place; copy to a local gitignored working directory first and write edits as separate files (e.g., smooth notebook copies the N-drive prod JSON into `smooth/local_models/` and writes `*.smooth_edit.json` outputs alongside)
- "Revise for management" means conclusions/takeaways first, less technical jargon, executive-style bullets with summary at top; internal emails should sound natural and like the user, using full forms such as "I will" rather than "I'll"
- Usage analyzer "Usage Volume" section must keep tokens as the primary signal; prefer one chart with dual axes (daily token bars left, 7-day request average line right) over removing tokens

## Learned Workspace Facts

- NQM CtoP uses Begg competing-risk (Cto0, CtoM3, CtoC); split into turnover (non-positive incentive) and refi (positive incentive) sub-models; scripts under `C/CtoP/script/1.4/Fixed/` with shared `C/` config/dataprep; `add_nqm_model_features()` `model_type` must match the model ("turnover", "refi", etc.) — there is no "Cto0" branch; CtoC report needs `annulize = FALSE` (monthly ~97-98% annualizes to flat 100%)
- Non-QM LLPA uses a GAM (not a static FICO x LTV grid); doc groups FULL (Full Doc + Tax Returns), BANKSTAT, DSCR, ALT (Asset Depletion + VOE), PL_CPA (P&L/CPA), OTHER; LLPA on lag1 rates; SATO and incentive use lag0/lowest-weekly; export via `build_sim_model_from_list()` with adaptive smooth (`bs='ad'`) auto-skip; time smooth endpoints appended per doc group for C++ decay; 2 SD residual trimming standard for SATO
- Incentive pipeline is spread-based: `inc_0_spread_llpa`, `sato_lag0_spread_llpa`, `burnout_lag0_spread_llpa`; `full_index_spread_fade15` replaced `full_index_inc_fade15`; turnover incentive capping targets `c_inc_0_spread_llpa`
- Turnover prepayment penalty: `inc_regime_f` includes `pp_active_Y`; `new_m2ppexp_f` bins months-to-PP-expiry for `pp_penalty == 1` (No PP plus ordered buckets through 4+); NQM burnout window 60 months (`burnout_inv_b2_60`); `inc_fade_period` 36; seasonality categorical (`month_f`); prefer `months_since_m30p` over `mc_age_ratio`; exclude COVID 2020-02–2020-06 from insample reports; foreign national `lmt.foreign` 0–3, not binary
- Model extract pipeline: `build_sim_model()` / `build_sim_model_from_list()` export R models to C++ text; `variable_mapping_{product}_test.csv` maps R names to C++ names; CSV OLD column needs spaces stripped (not `make.names()` — `check.names=FALSE` preserves curve column names through assembly/write/read); `verify_model_txt()` validates all entries post-extraction; extract sources `C/config.R` for model paths; production C++ scoring uses logit (sigmoid), so align R GAM/link with logit rather than cloglog where outputs feed that pipeline
- Non-QM data quirks: Fixed Balloon loans are ARM (`fix_f = "ARM"`) via NON_QM_FIX_MAP but often margin=0 — treat margin=0 as missing for ARM reset calculations; DSCR doc_map: loans with `dscr_ratio` should be "DSCR" when doc_map is NA, "NO DATA", or "OTHER"; `dscr_valid_f` and `doc_type_2` can be near-collinear in GAM (bam contrast errors)
- Data sources: loan-level `lps.lp_stat`, `lps.lp_dy`, `lps.lp_loss`; NQM rates `Libremax..MonthlyNQMRate`; HPI joins for inflation-adjusted o_bal. LMSim NQM monthly flatfiles under `N:\FlatFilesMonthly\NQMMonthly` include generated pseudo/stratification files; when deriving actual pool IDs or combine inputs, filter by the `_YYYYMMDD` `asof_date` suffix and exclude prefixes like `NQM_TOP`, `NQM_ALL`, `NQMAGE`, `NQMDOCTYPE`, `NQMFICO`, `NQMPRODTYPE`, `NQMUPDLTV`, and `NQMWAC`. Explicit deal-name filters should query `CRTStats` directly by `bbg_deal_name`, then separately warn/skip deals with no monthly flat file. LMSim `lm_sim_pub_main.py` no longer accepts `-dbsavefromdll_json*`; use `-purpose`, with forward projection purpose as `{purpose}_tracking`.
- Emailer (`emailer/`): always read `emailer/ai_instructions.md` first for formatting rules (image syntax, Outlook spacing, bold headers, code blocks, no sign-offs — Outlook inserts the signature). Helpers (`render_markdown`, `normalize_local_images`, `process_clipboard_images`, `_ensure_asset`, `maybe_resize_image_bytes`) live in `render.py`; `run.py` exposes CLI flags `--md-file`/`--preview`/`--no-clipboard`/`--no-resize`. Default font stack is Aptos/Calibri/'Segoe UI'/Arial sans-serif. Local images auto-resize in memory at 1600px long edge / 2MB threshold (never mutates source files or `assets/` copies). Bare `.png` filenames pasted by the user must be wrapped in `![](...)` or `<img>` syntax; formulas should use inline code/plain text rather than LaTeX blocks because Outlook can render LaTeX as invalid equation images. NQM vector emails refresh from `S:\QR\hzeng\vector_compare_nqm_v7.xlsx` sheet `vector_compare_0925` via `python emailer/plot_vector_compare.py`, write plots to `emailer/assets/nqm_vector_compare/`, and embed Base/SuperBear side by side at 310px per deal; each chart has exactly four curves: Blue = Prod, Orange = New NQM, Grey = JPM, Yellow = Dialed NQM. Daily RESI/CLO routines under `claude-code-routines/` must inspect Outlook/AUTO sources first and send through `send_outlook_summary.py`; git activity is fallback context only and must not be used to infer summaries.
- Smooth curve editor: `smooth/smooth_edit_v2.ipynb` edits JSON GAM model curves under `curves.simple` (1D smooths) and `curves.interactions[*].variants` (with `selector_field`/`selector_value`); each curve has `name`/`x`/`y`/`x_min`/`x_max`/`n_points`. Reusable helpers in `smooth/smooth_utils.py` (`read_model_json`, `write_model_json`, `find_curve`, `update_curve`, `extract_control_points`, `rebuild_curve`, `plot_curve`, `plot_model_curves`, `ensure_local_model_copy`); `smooth_edit_v2.ipynb` reloads `smooth_utils` before importing helpers so notebook kernels pick up edits without restart; `write_model_json` refuses N-drive (read-only) destinations and auto-suffixes outputs (`*.smooth_edit.json` → `_001.json` → `_002.json` etc.). `smooth/nqm_media_template.ipynb` is the focused NONQM media editor for `media_3y3m_spread_pp_full_dscr_doc`: source JSON is copied to `smooth/local_models/`, V2-style `CONTROL_POINTS_BY_SELECTOR` can override full curves, and `EXTENSION_BY_SELECTOR` appends pchip tails (then terminal-slope continuation) for selected docs before writing a separate edited JSON. Legacy txt-based `smooth_edit.ipynb` is preserved unchanged. Source prod JSONs live at `N:\DevSimData\LMSimData_Jay\data\<product>\ModelFiles\<version>\`; local working copies go in `smooth/local_models/` (gitignored).
- Roll-rate-model (`roll-rate-model/`): Monte Carlo loan-status simulator for MPL loans with statuses `C`/`D1M`..`D4M`/`PIF`/`LIQ`. C++ engine `sim_main.exe` built via CMake (`simengine` library, OpenMP-parallel runner in `src/runners/cf_parallel.cpp`); parallel Python implementation under `python/simengine/` with CLI in `python/run.py`. Data prep via `python/data_prep_for_sim.py`; coefficient files at `input/coef/<coef_version>/from*.txt` (tab-delimited). Config in `config/default.json`. C++ runner consolidates intermediate CSVs into `output/<deal>/<scenario>/sim_results.xlsx` via `python tools/csvs_to_xlsx.py` (and removes the per-CSV intermediates after). R GAM dump tools (`tools/dump_gam_to_coef.R`) depend on local `S:/QR/jli/GitHub/RCode/...` paths. When syncing from `S:\QR\jli\roll-rate-model`, copy source/config/docs and small runnable inputs only; exclude `build/`, `output/`, and large deal files, and preserve destination-only `platform_type_f` unless explicitly overwriting. The vendored baseline is pinned to upstream `67306e56`, defaults to `STACKED_v4`, and uses raw `input/macro/CPIAUCNS.csv` for both CPI inflators with freeze-on-missing behavior. Python horizon precedence is explicit `n_per`, then config `n_per`, then 360; workbooks include age-indexed `Metrics_Grouped` and projection-period-indexed `Metrics_Grouped_Period`. Regression coverage lives in `tests/test_roll_rate_model_sync.py`. Beginner onboarding is in `roll-rate-model/LEARNING_GUIDE.md`; its self-contained one-loan demo must keep `--skip-dump` to avoid invoking external R models and overwriting local coefficients, and a C++ run with `--dup 100` should report `100 done, 0 errors`.
- Additional tools: `auto-sim/` is a vendored auto-loan simulator copied from `S:\QR\jli\auto-sim`; its lightweight import keeps source/config/reporting plus one Prime (`CRVNA_2024_P2`), Near Prime (`CRVNA_2021_N2`), and Subprime (`EART_2023_1`) example tape while excluding `.git`, `build/`, `output/`, bulk tapes, and generated artifacts. Gomoku (`gomoku/`) is a LAN browser game served by `python -m gomoku.run --host 0.0.0.0 --port 8765`; check port 8765 before starting, `/config` returns room links, rematch/undo requests use modal accept/reject dialogs, and `Leave seat` preserves the board while pausing play until reseated. LLM CLO parser (`llm_clo_parser/`) is an offline benchmark/parser for CLO dealer color emails using frozen eval data, regex baseline first, schema validation, and private raw text/CUSIPs. Interview review uses `interview_notes/Sending/` for the assignment package, `S:\QR\Hiring\HS_JrModeler\Test Submissions` for original submissions, and the rubric/triage markdown under `interview_notes/`. Audit the canonical `C(t) -> PD(t+1 calendar month)` risk set and three-column dump for duplicate, gap, terminal, and post-PD handling; derive PIT-safe modification status from `mod_ft_pay_dt` rather than raw backfilled `mod`; distinguish simple macro/HPI lags from release/vintage-safe joins; require train-only preprocessing and an untouched final test; evidence-label metrics and check count-, UPB-, and calendar-month calibration. Hard gates override aggregate scores/AUC; calendar-time actual-vs-projected tracking and prepayment-penalty term/expiry are strong signals. Inventory submitted artifacts directly because ignore-aware searches can hide binary/ignored deliverables; review candidate code statically rather than executing it, and check economic constraints in tree models plus nonlinear transforms in logistic models instead of assuming straight-line effects.
- Usage analyzer (`usage/analyze.py`): supports multiple personas via `--name` (Howard default, exports at `c:\Users\hzeng\Downloads\usage-events-*.csv`; Yiming's exports at `c:\blp\data\usage-events-*.csv`). Pricing lives in `usage/cost_estimate.py`; `analyze.py` imports `compute_cost_estimates`, `map_model`, and `rates_for_event()` so headline spend, cost breakdown, and cache savings share the same rates. Cost model: `Included` events cost $0 (Cursor plan quota); `User API Key` events use Cursor's published per-model rates as out-of-pocket spend; normalize Cursor labels like `claude-opus-4-7-thinking-high/max` to `claude-4.7-opus`; GPT-5.5 is `$5` input / `$0.50` cache read / `$30` output per 1M tokens (higher than Claude 4.7 Opus on output only); GPT-5.6 Luna/Terra/Sol aliases and official rates are mapped, with the 272k long-context 2x input/cache rule applying to Sol only; Cursor CSV rows can aggregate multiple model calls and exceed GPT context windows, so headline `Cost`/`Cost Primary` uses conservative GPT long-context handling while `Cost High` preserves the row-threshold method (`GPT_LONG_CONTEXT_ROW_THRESHOLD`, 272k, 2x input/cache) for comparison; 20% Max Mode upcharge applies only to `Included` (Cursor-billed) events, not `User API Key`; Composer/Auto rides the Included pool so out-of-pocket cost is effectively $0

---
> Source: [howie-zeng/howard-toolbox](https://github.com/howie-zeng/howard-toolbox) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-08-12 -->
