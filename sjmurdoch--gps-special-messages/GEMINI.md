## gps-special-messages

> Project guide for Claude Code working in this repo.

# CLAUDE.md

Project guide for Claude Code working in this repo.

This is the reproducibility companion to *Inside GNSS* (May/June 2026)
"The Empty Field That Wasn't: GPS, OTAD, and Two Decades of Encrypted
Broadcasts." It rebuilds every quantitative claim and figure in the
article from the raw GFZ navbit + NAVCEN advisory inputs.

## Orientation

Read in this order when first dropped into the repo:

- [README.md](README.md) — what the project is + quickstart.
- [REPRODUCING.md](REPRODUCING.md) — fast path (download DB, verify) and full path (rebuild from raw).
- [DATA.md](DATA.md) — provenance, citations, Zenodo DOI.
- [CLAIMS.md](CLAIMS.md) — every article footnote ↔ verifier or external citation.
- [article/the-empty-field-that-wasnt.md](article/the-empty-field-that-wasnt.md) — the v2 article itself (tracked source; published via GitHub Pages).
- [CITATION.cff](CITATION.cff) — machine-readable citation.

## Layout (one-way DAG)

```
pipeline/   raw archives  → data/messages.duckdb
analysis/   DB            → derived tables in DB + analysis/reports/*.md
figures/    DB + reports  → figures/output/*.{png,pdf}
verify/     DB            → exit code (0 ⇔ all claims hold)
article/    Markdown source + figures → article/site/ (GitHub Pages page) + the-empty-field-that-wasnt.pdf (print)
src/        the GPSSpecialMessages module (parsers, decoders, DB writer)
test/       unit tests (913 without data/ops_advisories; grows with the OA set)
bin/        fetch_zenodo.sh, full_build.sh
data/       arrow_test/ committed; everything else fetched or rebuilt
```

Each bucket reads from the previous and writes to a well-known output.
Per-bucket entry points: `pipeline/run_all.sh`, `analysis/run_all.sh`,
`figures/build_all.sh`, `verify/run_all.sh`, `article/build.sh` (web page) and
`article/build-pdf.sh` (print PDF). End-to-end: `bin/full_build.sh` chains
everything (its final stage, `08_article`, renders the PDF; it skips gracefully
when pandoc/xelatex are absent); designed to run unattended (see top of script
for nohup invocation).

When a researcher asks where new code goes, follow the DAG: a new
quantitative claim → `verify/`; a new derived metric → `analysis/`; a
new chart → `figures/`; new pure helper functions → `src/` with tests.

## Adding a new claim

1. Write `verify/<claim>.jl` (≤100 lines). Follow the pattern in
   `verify/corpus_totals.jl`: read DB read-only, assert with a `check()`
   helper, exit non-zero on divergence.
2. Append it to the loop in `verify/run_all.sh`.
3. Add a row to the table in `CLAIMS.md` linking the article footnote to
   the new script.
4. Run `verify/run_all.sh data/messages.duckdb`; commit only when every
   verifier passes.

Tolerances: counts/exact totals → equality. Test statistics with random
seeds → tight numeric tolerance with the seed pinned. Plot byte-equality
is **not** verified (Cairo embeds timestamps; visual review only).

## Adding a new analysis

1. New script `analysis/<topic>.jl`. Read DB, write the report under
   `get(ENV, "REPORTS_DIR", joinpath(@__DIR__, "reports"))`, not a hard-coded
   `joinpath(@__DIR__, "reports")` — `bin/full_build.sh` sets `REPORTS_DIR` to a
   work-dir path so a full build stays hermetic and never dirties the tracked
   `analysis/reports/`. `mkpath` it before writing.
2. Prepend the report's first line with the AUTO-GENERATED header
   (`<!-- AUTO-GENERATED by analysis/<topic>.jl on <ISO-UTC, e.g. 2026-06-21T07:14:08Z> — do not edit by hand -->`;
   build the timestamp with `Dates.format(now(UTC), dateformat"yyyy-mm-ddTHH:MM:SS") * "Z"` so headers are UTC, not local)
   so hand-written and machine-written reports stay distinguishable.
3. Hook into `analysis/run_all.sh`. Respect FK ordering: scripts that
   write child rows must run after the parents.
4. For DB writes use `INSERT ... ON CONFLICT DO NOTHING`, never
   `INSERT OR REPLACE`. REPLACE is implemented as DELETE+INSERT in
   DuckDB and breaks foreign keys from sibling correlation tables.
5. New table? Add `create_<topic>_table!(db)` in `src/duckdb_writer.jl`
   and a wipe-prelude entry in `analysis/run_all.sh` Step 0.

## Adding a new figure

1. New file `figures/figN_<name>.jl` (CairoMakie) or `.typ` (Typst).
2. Output paths must be `joinpath(@__DIR__, "output", "...")`. Emit
   PNG, PDF, and SVG.
3. Add to `figures/build_all.sh`.
4. Commit the rendered PNG + PDF + SVG outputs alongside the script.
5. CairoMakie embeds creation timestamps and font-cache ordering in PNG
   `tIME` chunks and PDF `/CreationDate` — repeated runs differ by
   ~25 bytes per output. Visually identical. Do not assert byte-equality.

## Running things

| Task | Command |
|---|---|
| Tests | `julia --project test/runtests.jl` (~30 s, expect 913/913 without `data/ops_advisories/`; with it the count grows with the OA file set — all must pass) |
| Article page | `article/build.sh` (Pandoc → `article/site/index.html`; deployed by `.github/workflows/pages.yml` on push) |
| Article PDF | `article/build-pdf.sh` (Pandoc → xelatex → `article/the-empty-field-that-wasnt.pdf`; bottom-of-page footnotes, gitignored output) |
| Pipeline smoke | `julia --project pipeline/04_build_database.jl data/arrow_test/ data/test.duckdb` (~10 s, 3,712 msgs / 2 unique) |
| Fast verify | `bin/fetch_zenodo.sh messages.duckdb && verify/run_all.sh data/messages.duckdb` |
| Full unattended rebuild | `nohup bin/full_build.sh > /dev/null 2>&1 &` then `cat $WORK_DIR/_build/STATUS.md` |
| Single verifier | `julia --project verify/<name>.jl data/messages.duckdb` |
| One analysis | `julia --project analysis/<name>.jl data/messages.duckdb` |

Without `data/ops_advisories/` present the OA-parser real-data tests
skip silently (913 remain). With OA data the total depends on how many
advisory files are present (e.g. 1,057 with the source-repo symlink as
of June 2026). Either fetch via `bin/fetch_zenodo.sh nanu` or symlink
from the source repo.

## GPS terminology gotchas

- **`prn` (1–32) ≠ `sv_id` (always 55)**. The PRN is the satellite that
  transmitted the message; the SV ID is the page identifier within
  Subframe 4. Confusing the two is the most common bug. Special
  messages always have `sv_id = 55` because that's what flags Page 17.
- **Bit positions**: APIs expose 1-indexed positions (per IS-GPS-200);
  internal code uses 0-indexed. Don't mix in the same function.
- **CP437 encoding** for the 22-byte payload. `0x00` renders as the
  Unicode replacement character.
- **Frame inversion + D30\* un-complement**: per-word, parity-checked,
  not per-byte. See `src/lnav_decoder.jl` for the principled approach
  (try parity, retry with un-complement, use whichever passes).
- **D30\*-retained streams corrupt page identification, not just
  extraction.** When the TLM word's D30 is 1, the HOW's data bits are
  complemented on air; the TOW check misreads this as frame inversion
  and Page 17's SV ID 55 reads as its 6-bit complement, 8. When a
  Subframe 4 SV ID reads 8, `is_d30_complemented_page17` re-reads it
  under the D30* interpretation and confirms with parity before
  accepting. The aliasing also runs the other way — in such a stream a
  genuine SV ID 8 page reads 55 — so direct reads of 55 are likewise
  confirmed under the parity-verified interpretation
  (`confirm_direct_page17`). Frames verifiable under neither
  interpretation (bit errors) are kept only if the TLM preamble is
  plausible, and are stored with `parity_ok = FALSE`. Identification
  needs `seconds_of_day` supplied (the pipeline always does).

For the underlying GPS context see DATA.md and the article. For
deeper detail see IS-GPS-200N §20.3.3.5.1.8 (linked from DATA.md).

## Performance hot paths

- Arrow column views (`row.navbits`) are zero-copy `SubArray{UInt8}`.
  Always **filter before materialising** — `collect()` then `filter`
  costs ~1000× more allocations than `filter` then `collect()`.
- `_add_bits_to_buffer!` in `src/lnav_decoder.jl` must stay top-level
  (not a closure) for type stability. Verify with `@code_warntype`
  after edits.
- For parallel DB writes use `parallel_processor_threaded.jl` with the
  parallel-collect-then-serial-write pattern. **Never share a DuckDB
  connection across threads** — corrupts the WAL.
- After bit-extraction edits, profile a million-row run; allocation
  count should stay in the low tens of thousands, not millions.

## Pitfalls (each one bit the codebase)

- **Null bytes in DuckDB VARCHARs crash the C API.** Use
  `sanitize_ascii_message()` or substitute a visible placeholder. Be
  especially careful when porting Python (`str(None)` → `"None"` is a
  4-char string; a Julia null byte is a real null).
- **`save_nanu_records!` and FK cycles.** Re-running the analysis used
  to trip a foreign-key constraint (REPLACE = DELETE+INSERT). Pattern
  is now `INSERT ... ON CONFLICT DO NOTHING`. Don't switch back.
- **Report sorts in `analysis/behavioral_changes.jl` must be total
  orders** (fixed). Every display `sort!` now carries a tiebreaker that
  uniquely orders rows — `sort!(sub, [:detected_at, :prn, :granularity,
  :direction])`, `sort!(dedup, [:detected_at, :metric, :direction])`, and
  the count sorts tie-broken by `:metric`/`:prn` — so the report is
  reproducible across runs. A bare `sort!(df, :detected_at)` (or any
  single-key sort with possible ties) reintroduces run-to-run variation in
  the top-N slices; keep new sorts total-ordered.
- **`marginal_entropy_ppm.md` line layout is load-bearing.**
  `figures/fig2_entropy_distribution.jl` parses the sorted-results code
  block via regex. If you change the column layout, update the regex.
- **`pipeline/04_build_database.jl` row count is a load-bearing
  invariant.** With the fixed decoder the full-corpus build produces
  24,087,691 rows / 5,009 unique, now pinned by the verifiers (the
  published v1 article says 12,163,006 / 3,994 — see `CHANGELOG.md`).
  The 2026-06-11 identification hardening
  (week-rollover TOW wrap, aliased-SV-ID-8 rejection, TLM-preamble gate
  on parity-unverifiable frames) may shift a from-raw rebuild count
  marginally from those numbers. If a refactor changes the count away
  from what the current decoder produces, investigate before "fixing
  the test."
  The extractor reports ~19.8k more insertions than the table holds:
  identical-duplicate `(datetime, prn, hash)` keys from original +
  reprocessed copies of the same GFZ daily files, skipped at insert
  (quantified in `analysis/reports/duplicates_audit.md`).
- **Pandoc footnotes are numbered by reference position; reused labels
  are de-duplicated by `article/footnotes.lua`.** `article/build.sh` runs
  plain Pandoc (`--from markdown+footnotes`); the `[^N]` labels are just
  keys, and the rendered number follows order of first reference. Two
  things to know: (1) **Inserting a new footnote mid-document renumbers
  every footnote after it.** CLAIMS.md keys every claim by its rendered
  footnote number, so a genuine new footnote means renumbering the
  article *and* re-keying CLAIMS.md — avoid it. To cite an
  existing note from a new spot, **reuse its label** (e.g. `[^39]` marks
  the diversity verifier, `[^52]` the TEXT-timeline verifier) — reuse
  renumbers nothing. (2) **Plain Pandoc has no multi-reference support** —
  citing the same `[^N]` twice emits a *duplicate note* with a shifted
  number (the raw build has 57 notes for 56 labels: `[^16]` is cited
  twice). The `--lua-filter footnotes.lua` step fixes this:
  it merges notes with byte-identical content, so every reference to a
  label renders as that label's number pointing to one shared body, and
  sidenotes.js clones that body beside each reference. After editing
  footnotes, rebuild and check `article/site/index.html`: the count of
  `id="fnN"` note bodies must equal the label count (56) with no number
  above it, and the markdown first-appearance order must stay 1..56.

## What does not belong in this repo

- Working notes, plans, slide decks. The repo is user-facing
  reproducibility; chronology lives in commit history. Scratch material
  lives in `wip/` (gitignored), never in tracked directories.
  **Exception (2026-06-12): the article itself.** Its Markdown source is
  tracked at `article/the-empty-field-that-wasnt.md`, corrected in place
  and published via GitHub Pages (`article/build.sh` + the pages.yml
  workflow). Drafts-in-progress still belong in `wip/`; only the
  publication-grade text is tracked.
- Full `data/raw/`, `data/processed/`, `data/arrow/`, or
  `data/messages.duckdb`. Gitignored; fetched from Zenodo or rebuilt.
  Likewise the generated `article/site/`.
- The publisher's article PDF, Tyley 2015 briefing, IS-GPS-200/240
  reference documents. All cited via DATA.md, never vendored.
- Generic-overview analysis scripts that don't back an article claim.
  If a claim isn't in `CLAIMS.md`, either add it or drop the script.

## Conventions worth keeping

- Numeric prefixes only where order matters: `pipeline/0X_*` and
  `figures/figN_*`. No category prefix when the directory implies it.
- Output filenames are semantic, not numeric (`lnav_schematic.png`,
  not `fig1.png`) — the article cites them by name.
- `Manifest.toml` is committed (not the Julia template default) for
  exact dependency pinning.
- Reports drop the `_report` suffix in `analysis/reports/`.

---
> Source: [sjmurdoch/gps-special-messages](https://github.com/sjmurdoch/gps-special-messages) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-07-04 -->
