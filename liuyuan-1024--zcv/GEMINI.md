## zcv

> Zcv 是一个以本地编辑体验为核心的代码编辑器项目。

# AGENTS.md

Zcv 是一个以本地编辑体验为核心的代码编辑器项目。

修改代码前，应先理解相关架构和已有实现。Zed 是 Zcv 的主要参考实现；当修改涉及 Zed 已经解决的编辑器能力时，优先研究其实现和设计，再决定如何在 Zcv 中实现。

## 开发阶段原则

Zcv 当前处于快速演进阶段。

**架构正确性优先于向后兼容。**

在没有明确兼容要求时：

- 可以删除、重命名或重新组织已有实现。
- 不维护新旧两套逻辑。
- 不为了兼容错误的历史设计增加适配层。
- 相关调用方、测试和文档应在同一次修改中一起更新。
- 优先建立正确的长期架构，而不是通过局部补丁维持当前实现。

如果一个需求暴露出底层抽象错误，应优先修正抽象，而不是继续在错误抽象上增加条件分支。

## 项目范围

以下是当前阶段明确的项目范围。

### Zed

Zed 是 Zcv 编辑器架构和功能实现的重要参考。

当修改涉及编辑器已有成熟实现时，可以查阅本机：

```text
macOS：/Users/liuyuan/project/zed
Windows：D:\project\zed
```

重点理解：

- 模块职责如何划分；
- 状态由谁拥有；
- 数据如何流动；
- 生命周期如何管理；
- 编辑器核心抽象如何组合；
- 边界条件如何处理。

不要为了形式一致而机械复制 Zed。

Zcv 与 Zed 的项目范围、已有抽象或依赖不同的地方，应按照 Zcv 自身架构进行调整。只有在设计目标和上下文一致时，才直接采用对应实现。

### 远程与协作

当前不为 Zcv 引入：

- 实时协作；
- 远程开发；
- 与上述能力直接相关的基础设施。

不要为了潜在的未来需求提前设计这些能力。

### 语言智能

当前阶段暂不引入 LSP。

语法层面的能力，包括但不限于：

- 语法高亮；
- 语法节点识别；
- 基础语法感知；
- 基于语法树的编辑能力；

统一优先依赖 Tree-sitter 以及对应的 `.scm` 查询文件。

不要为了实现当前需求提前建立 LSP 抽象或兼容层。

未来正式引入 LSP 时，再重新设计相关能力抽象。

## 修改代码前

修改代码前，根据修改范围读取相关文档和实现。

至少确认：

1. 当前模块的职责。
2. 调用方和消费方。
3. 数据和状态的所有者。
4. 是否已有相同或相近能力。
5. 是否存在相关文档或项目约定。
6. Zed 是否存在值得参考的对应实现。

不要无条件读取整个 `docs/` 目录。

优先阅读：

```text
AGENTS.md
相关目录中的 AGENTS.md
相关架构文档
相关模块 README
修改代码直接引用的设计文档
```

如果文档继续引用其他规范，再沿引用关系读取。

## 修改权限

读取代码、搜索仓库、阅读文档、查看 Zed 源码以及进行其他只读分析不需要额外授权。

修改仓库文件必须来自开发者明确提出的修改任务。

一次明确的代码修改请求，视为对该任务合理范围内文件修改的授权，不需要为每一个文件重复请求许可。

出现以下情况时，应重新确认：

- 修改范围明显超出原始任务；
- 需要删除大量代码或文件；
- 涉及不可逆的数据操作；
- 存在多个会显著影响架构方向的方案，且无法根据现有设计判断；
- 需要修改任务明确排除的模块。

## 架构规范

### 单一编辑器承载所有文档形态

Zcv 从始至终只有一个 `Editor`。普通文档、组合文档以及差异、搜索等文档形态，都是编辑器展示内容的差异，不是不同的编辑器，也不是不同的编辑逻辑。所有文档都应共享同一套选择、滚动、输入、编辑、折叠、命中测试和渲染管线。

修改编辑器或文档展示能力时，AI 必须把这条规则作为架构约束，不得按文档形态复制 `Editor`、建立普通文档与组合文档两套编辑流程，或分别维护平行的交互状态。文档内容组织、源坐标映射、可编辑性和显示投影的差异，应收敛在对应的文档模型与显示层；新增行为应优先扩展唯一的 `Editor` 管线。不同构造入口只表达输入场景或内容来源，不代表不同编辑器实现。

### 架构优先于补丁

实施任何非机械修改时，首先判断问题属于：

```text
局部实现错误
还是
底层抽象错误
```

如果是底层抽象错误，应修正抽象。

禁止通过不断增加以下机制来掩盖架构问题：

- 特殊分支；
- 回退逻辑（fallback）；
- 兼容路径；
- 隐式默认行为；
- 一次性状态；
- 绕过正常生命周期的调用。

如果局部修复必须依赖越来越多特殊条件，应重新检查当前抽象是否正确。

### 跨平台架构

跨平台支持必须围绕语义能力建立统一入口，而不是让调用方分别拼接各平台流程。

- 业务层只依赖平台无关的能力接口或门面；平台差异由对应能力域的私有后端、适配模块或平台实现承载。
- 统一入口表达稳定语义，例如“终止当前进程树”“准备应用更新”“原子替换安装目录”，不暴露 `cfg`、平台路径、系统命令或平台资源布局。
- 同一能力的跨平台流程应共享状态机、错误处理和生命周期；只有系统调用、路径布局、权限、进程模型等真实差异留在平台边界内。
- 不建立包罗万象的全局 `Platform` 或万能平台 crate。只有两个或更多能力域拥有相同且稳定的语义、生命周期和错误契约时，才提取共享平台原语；否则将后端放在实际消费方附近。
- 平台后端必须明确创建者、所有者、调用者和销毁时机；不得通过业务层散落的条件编译、隐式回退或双轨流程维持平台一致性。
- 新增平台时，应优先新增或替换后端实现，不复制一套业务流程；平台无关测试覆盖共同语义，平台测试只覆盖后端契约和系统边界。

跨平台重构应先绘制“能力域 → 统一入口 → 平台后端”的依赖图，再按真实消费方决定模块归属。
更新、终端进程、Git 进程树、文件监视、路径和设置等能力可以各自拥有边界；“统一入口”不等于把所有平台行为集中到一个全局抽象中。

### 高内聚

代码应放在真正拥有其语义、状态和生命周期的模块中。

只服务单一消费方、且不形成独立语义边界的局部实现，通常可以与消费方放在一起。单一消费方本身不能决定代码归属；即使只有一个直接调用方，只要代码具有独立的领域职责、状态、生命周期或测试边界，也应放在对应的领域模块中。

例如：

```text
window_controls 只由 top_bar 使用

→ shell/components/top_bar/window_controls.rs

而不是：

→ shell/components/window_controls.rs
```

不要仅因为代码“可能以后复用”就提前提升到公共层。

### 低耦合

存在两个或更多真实消费方时，可以考虑提取共享原语；但共享方数量不是提取模块的必要条件。应先确认代码是否已经形成稳定、内聚的能力边界，再决定是否独立成模块。

例如：

```text
bar_frame / glyph / bar_region
同时被 top_bar 和 bottom_bar 使用

→ shell/components/primitives/
```

消费方应共同依赖共享原语，而不是彼此依赖。

判断顺序：

1. 确定代码表达的语义和领域职责。
2. 确定状态所有权、生命周期和主要维护者。
3. 确定它是否形成独立且可测试的能力边界。
4. 根据依赖方向决定模块归属，避免让领域模块反向依赖具体消费方。
5. 将真实消费方数量作为辅助信号，而不是模块归属的决定条件。
6. 没有稳定边界和真实需求时，不要提前抽象公共模块。

### 所有权必须明确

新增状态或能力时，应明确：

- 谁创建它；
- 谁拥有它；
- 谁可以修改它；
- 谁消费它；
- 生命周期何时开始；
- 生命周期何时结束。

避免通过全局状态、隐式注册、跨模块访问或临时桥接模糊所有权。

如果无法明确说明某个状态由谁负责维护，通常说明当前模块划分仍然存在问题。

### 单一数据源

同一项事实必须只有一个权威数据源。

任何状态、配置或业务事实，都应明确由一个位置负责存储和维护。其他模块如果需要相同信息，应读取、引用或从权威数据源派生，而不是维护另一份需要同步的副本。

禁止为同一项事实建立多个可以独立修改的数据源。

尤其避免：

- 在多个模块中分别保存同一状态；
- 为方便读取而复制一份长期存在的影子状态；
- 同一配置同时存在多个独立来源；
- 同一业务事实既保存在模型中，又在 UI 或其他模块中维护另一份可写副本；
- 修改一项状态时需要手动同步多个位置；
- 通过双写、双向同步或补偿逻辑维持多个副本的一致性；
- 在派生数据失效后尝试反向修复权威数据。

派生数据不属于新的权威数据源。

如果某项数据能够从权威数据源确定性地计算得到，应优先在需要时计算，或者作为可丢弃、可重建的缓存保存。

例如：

```text
文档内容
→ 文档模型是权威数据源

UI 中显示的文本、行号、选择状态投影
→ 从文档模型或对应状态派生
→ 不维护另一份独立可写的文档内容
```

### 显式优于隐式

模块之间的行为应显式表达。

不要把重要业务行为隐藏在：

- 隐式回退；
- 隐式默认值；
- 副作用初始化；
- 不透明的自动注册；
- 调用方无法看到的状态修复。

如果存在默认值或策略选择，应由负责该决策的模块显式完成。

跨模块调用时，应让调用方能够从接口本身理解主要行为，而不是依赖隐藏的全局规则。

## 实现原则

### 编码前思考

不要根据名称或局部代码猜测行为。

实施前明确：

- 需求是什么；
- 当前实现为什么不能满足需求；
- 哪些模块真正需要改变；
- 修改后的成功条件是什么。

存在多个合理方案时，应比较它们的架构影响，而不是默默选择其中一种。

对于可逆、局部且低风险的歧义，可以采用最符合现有代码和项目原则的方案，并在回复中说明假设。

只有当歧义会显著改变架构、数据或修改范围时，才需要停止并询问开发者。

### 最小必要实现

只实现当前需求需要的能力。

不要：

- 添加未经请求的功能；
- 为假设中的未来需求增加配置；
- 为单一调用方创建抽象层；
- 为静态类型已经保证的不可能情况增加防御逻辑；
- 建立当前没有真实消费者的接口；
- 为“以后可能会用”提前设计扩展点。

如果实现明显比问题本身复杂，应重新检查抽象是否正确。

能通过删除错误抽象解决的问题，不要通过增加更多抽象解决。

### 精准修改

只修改解决当前问题需要的代码。

不要顺手：

- 重构无关模块；
- 调整无关格式；
- 重命名无关符号；
- 修改无关注释；
- 清理历史遗留代码。

如果本次修改使已有代码变成无用代码，应一起删除由本次修改直接产生的：

- 未使用的导入；
- 未使用的变量；
- 未使用的函数；
- 已失效的分支。

发现与当前任务无关的问题时，可以在回复中指出，但不要顺手修改。

每个修改片段都应能解释为：

```text
需求本身
必要的配套修改
或
本次修改直接产生的清理
```

不要借当前任务扩大修改范围。

### 不做推测性实现

不要因为某种场景“以后可能出现”就提前增加：

- 配置项；
- 抽象接口；
- 兼容逻辑；
- 回退逻辑；
- 状态字段；
- 扩展点；
- 错误恢复机制。

只有存在当前真实需求或明确消费方时，才引入对应能力。

### 信任类型系统

对于 Rust 类型系统已经在同一进程的受控代码中保证的条件，不要重复增加运行时校验。

运行时校验应集中在真正存在不可信输入的位置，例如：

- 用户输入；
- 文件输入；
- 配置文件；
- IPC；
- 网络协议；
- 外部进程输出；
- 持久化数据；
- FFI；
- 第三方库返回的不受控数据。

不要通过大量“不可能发生”的错误处理掩盖类型设计问题。

## 验证

修改必须有明确的成功条件。

例如：

```text
修复错误
→ 能稳定复现原问题
→ 修改后复现用例通过

新增行为
→ 为目标行为添加测试
→ 测试能够证明该行为存在

重构
→ 原有行为测试保持通过
→ 新结构满足新的架构要求
```

多步骤修改开始前，可以简要列出：

```text
1. 修改内容 → 验证方式
2. 修改内容 → 验证方式
3. 修改内容 → 验证方式
```

验证应与修改范围匹配。

优先运行最小、最相关的验证，不要默认运行与修改无关的整个测试集合。

不要运行全量测试。全量测试由开发者人工执行；只有开发者明确要求时，才可以运行全量测试。

验证行为时，优先选择能够直接证明目标行为的测试，而不是仅运行一个范围很大但信号模糊的命令。

如果修改涉及多个独立层面，应分别提供相应证据。

例如：

```text
行为修改 → 对应行为测试
类型或接口修改 → 编译 / 类型检查
格式问题 → 格式检查
Lint 问题 → Lint 检查
文档修改 → 文档检查
```

如果某项验证因为环境、依赖、网络或平台限制无法执行，应明确说明：

- 尝试了什么；
- 为什么无法执行；
- 哪部分因此仍未被验证。

不要把环境失败描述成代码已经验证通过。

不要仅因为某个大型检查通过，就声称所有相关行为都已经验证。

## 测试

测试用于描述和保护行为，而不是记录实现细节。

测试专用代码不得作为生产 crate 的 API、状态或 Cargo feature 保留在 `src` 中。

如果函数、构造器或注入入口仅由测试使用，应按实际消费范围归属到：

- 同一模块的测试模块；
- 对应的 `test` 目录；
- 多个测试目标共享的测试支持模块。

不要通过 `test-support` 等 feature 在生产模块中条件编译测试专用 `pub` API。正常生产构建不应包含或暴露测试专用入口；测试需要的 fixture、构造和依赖注入应由测试代码自身持有。

只有当某项能力本身是正式产品契约、存在真实生产调用方时，才能保留在生产代码中；被测试使用并不能单独构成保留理由。

新增或修改行为时，应优先覆盖：

- 正常路径；
- 会影响行为的边界条件；
- 本次修复对应的回归场景。

修复错误时，如果可以稳定复现，应优先先建立能够暴露原问题的测试，再修改实现。

不要仅为了提高覆盖率测试没有实际行为意义的内部细节。

测试文件按照 Zcv 的项目约定统一放入 `test` 目录。

测试应尽量验证外部可观察行为，而不是绑定：

- 私有函数的内部步骤；
- 临时数据结构；
- 实现顺序；
- 与行为无关的中间状态。

当行为发生变化时，应同步修改已经过时的测试，而不是为了让旧测试继续通过而维护错误行为。

## 警告与 Lint

不要使用以下方式隐藏警告：

```rust
#[allow(...)]
#![allow(...)]
```

以及任何等价的抑制手段。

新增或修改代码不应引入新的警告。

如果警告暴露出真实问题，应修复问题本身。

不要通过以下方式让警告消失：

- 无意义地读取变量；
- 无意义的占位调用；
- 死代码；
- 假使用；
- Lint 抑制。

确实存在项目级例外时，必须先有明确的项目约定，并在最窄范围内说明该 Lint 为什么不适用于当前代码。

不要为了获得“干净输出”而隐藏有价值的编译器或 Lint 信号。

## 中文第一性

中文是 Zcv 项目中的第一协作语言。

自然语言说明应优先使用准确、直接的中文。

除代码标识符、工具名、协议名、标准名、第三方 API、生态固定术语以及需要精确检索的原文名称外，不使用英文替代能够准确表达相同含义的中文。

英文术语确有保留价值时，优先采用：

```text
中文说明 + 英文术语
```

而不是让英文承担标题、句子或段落的主要语义。

### 默认使用中文

以下内容默认使用中文：

```text
代码注释
项目文档
测试说明
错误提示
日志和诊断信息
CLI 输出
UI 文案
提交信息
PR / review / issue / changelog
AI 回复与修改说明
```

如果这些内容需要与外部协议、第三方接口或固定格式精确匹配，则遵循外部要求。

### 保留英文

以下内容通常保持英文原文：

```text
Rust 标识符
类型名
trait 名
函数名
字段名
模块名
crate / package / binary 名称
协议名
标准名
错误码
环境变量
命令行参数
第三方 API
上游术语
需要与外部生态精确匹配的固定文本
```

例如：

```text
事务基准版本（base_version）不匹配。
```

而不是为了中文化把 `base_version` 改写成无法检索或无法对应代码的名称。

像以下技术名称也应保留原文：

```text
Rust
Tree-sitter
LSP
CLI
API
Git
TODO
Lint
```

但自然语言主体仍然使用中文。

例如优先：

```text
运行 Lint 检查。
```

而不是：

```text
Run lint checks.
```

中文第一性不是强制翻译所有英文，而是让中文承担项目沟通和工程语义的主体。

## 注释与文档

文档和注释应描述当前有效的行为、约束和使用方式。

不要把以下内容长期保留在代码注释或当前状态文档中：

- 修改过程；
- 调试过程；
- review 对话；
- 历史方案比较；
- 已经失效的设计；
- 临时推理记录。

如果设计理由对未来维护确实重要，应写入专门的设计文档，而不是散落在实现注释中。

文档修改应与代码修改保持同步。

如果代码修改改变了：

- 对外行为；
- 使用方式；
- 模块职责；
- 生命周期；
- 配置；
- 错误条件；
- 重要约束；

应检查对应 README、文档注释和项目文档是否需要同步更新。

### 注释

注释只补充代码本身无法清晰表达的信息。

优先说明：

- 非直观业务规则；
- 边界条件；
- 外部协议要求；
- 生命周期约束；
- 并发约束；
- 性能或安全要求；
- 上游兼容原因；
- 容易被误改的不变量；
- 临时限制和 TODO 的背景。

不要：

- 把代码逐字翻译成人话；
- 描述显而易见的控制流；
- 重复变量名或函数名；
- 用注释弥补错误命名；
- 用注释掩盖复杂结构；
- 在注释中记录修改过程或 review 历史；
- 用注释解释可以直接通过代码结构表达的事实。

优先通过以下方式表达代码意图：

1. 清晰命名；
2. 合理的数据结构；
3. 拆分职责；
4. 明确的类型；
5. 局部结构；
6. 最后才是注释。

注释应描述稳定的业务语义，而不是绑定容易变化的实现细节。

例如，优先：

```rust
// 外部接口要求传入字节偏移，字符索引必须先转换。
```

而不是：

```rust
// 把 char_index 转换成 byte_offset 后传给 foo。
```

前者描述的是稳定约束；后者绑定具体变量和函数实现，更容易随着重构失效。

注释应靠近被解释的代码。

多行注释按语义分段，不按固定字符宽度机械换行。

例如，优先：

```rust
// 外部接口要求传入字节偏移。
// 这里必须先转换字符索引，否则多字节字符会导致定位错误。
```

而不是：

```rust
// 外部接口要求传入的是字节
// 偏移。这里必须先转换字符
// 索引，否则多字节字符会导致
// 定位错误。
```

当说明已经复杂到需要多个段落时，优先考虑将其移动到：

- 文档注释；
- 测试；
- 设计文档。

不要无限扩展普通行内注释。

## 模块组织

代码组织遵循 **高内聚、低耦合**。

新增文件、模块、组件或抽象时，应先判断它表达的语义、状态所有权和生命周期，再分析消费方。

判断流程：

1. 确定代码属于数据模型、语言分析、编辑事务、界面布局还是其他能力域。
2. 确定谁创建、拥有、修改和销毁相关状态。
3. 确定代码是否形成独立且可测试的能力边界。
4. 按语义归属和依赖方向选择模块位置；单一消费方不自动意味着必须靠近消费方。
5. 存在多个真实消费方时，再考虑提取共享原语或共享模块。
6. 没有稳定边界和真实需求时，不要预建公共抽象。
7. 不要为了目录视觉上的“整齐”破坏代码的实际依赖关系。

例如：

```text
window_controls 只被 top_bar 使用

→ shell/components/top_bar/window_controls.rs
```

而不是：

```text
shell/components/window_controls.rs
```

如果：

```text
bar_frame
glyph
bar_region
```

同时被 `top_bar` 和 `bottom_bar` 使用，则可以考虑：

```text
shell/components/primitives/
```

共享层应保存真正的共享原语。

不要把某个完整消费方的业务逻辑抽到共享目录，再让其他模块间接依赖它。

### 导入与路径限定

已经通过 `use` 导入的模块、类型、函数或常量，后续使用时默认采用导入后的短名称，避免无意义地重复书写 crate 或模块前缀。

进行项目级代码清理时，应搜索所有 `zcv_*::` 等 crate 限定路径，而不能只处理当前暴露问题的 crate。对每个位置按同一规则判断：已导入且重复使用的符号统一改用短名称；职责边界、名称歧义、文档链接、属性宏和单次使用路径可以保留限定形式。

推荐：

```rust
use zcv_theme::typography;

let size = typography::content_size();
```

不推荐：

```rust
use zcv_theme::typography;

let size = zcv_theme::typography::content_size();
```

具体规则：

1. 同一文件中重复使用某个模块或类型时，通常应先导入，再使用短路径。
2. 只使用一次的符号，可以保留完整路径，避免产生仅为一次调用服务的导入。
3. 表达跨 crate 职责边界、且名称较为通用的 API，可以保留 crate 限定路径。例如 `zcv_assets::text(...)` 和 `zcv_workspace::register(...)`。
4. 同名冲突或语义容易混淆时，使用明确的别名或保留模块限定路径，不强行缩短。
5. 属性宏、字符串协议值和文档链接不适用本规则，例如 `#[gpui::test]` 和 `"dock::FocusOrHidePanel"`。
6. 本规则不禁止所有 `::`；路径限定用于表达归属或消除歧义，短路径用于减少无意义重复。两者冲突时优先保证可读性。

## 错误处理

错误处理应服务于真实的失败场景。

不要：

- 捕获错误后静默忽略；
- 将配置错误退化成默认行为；
- 用回退逻辑掩盖状态错误；
- 为理论上不可能出现的同进程类型状态增加大量分支；
- 将开发阶段应当暴露的问题转换成无声失败。

对于能够在初始化阶段确定的配置错误，应尽早失败。

对于只能在运行时确定的问题，应在最早能够准确判断的位置失败。

错误信息应尽量说明：

- 发生了什么；
- 哪个输入或状态存在问题；
- 调用方能够如何定位问题。

不要在底层错误信息中加入与定位无关的大量叙述。

### 用户可见错误与终端输出

项目当前没有持久化日志系统，`eprintln!` 只是进程标准错误输出，不能作为用户获知错误的唯一途径。

遵循以下规则：

- 用户主动发起的操作失败，或操作结果不确定，必须通过 toast 告知用户；
- toast 中的错误必须包含明确的操作标题或错误上下文，并使用链式错误格式（例如 `自动更新失败：{error:#}`），既说明发生了什么，也保留内层原因；
- 同一错误已经通过 toast 展示时，不要再重复使用 `eprintln!` 输出；
- 没有用户可见价值的后台状态、成功信息、调试信息和高频渲染失败输出应直接删除，不要当作日志保留；
- 只有确实需要辅助诊断、且尚未有更合适的用户反馈机制时，才保留 `eprintln!`；
- 后台可自动恢复的内部错误不得因为提示方便而频繁弹出 toast，避免提示风暴。

## 默认值与配置

部署环境、用户偏好或运行场景可能变化的值，应通过明确的配置机制表达。

不要把本应配置的参数散落为：

- 魔法数字；
- 隐式字符串；
- 难以发现的局部常量；
- 只有测试能够覆盖的隐藏参数。

但不要把固定协议值、安全不变量或真正不会变化的实现常量错误地变成配置项。

是否应该配置，应根据值的语义决定，而不是简单遵循“所有常量都可配置”。

默认值必须由负责该策略的模块显式确定。

不要在执行路径深处通过类似：

```text
value ?? default
```

的方式隐藏重要策略决策。

## 兼容性

当前阶段默认不为旧实现维护兼容层。

如果新的正确设计与旧实现冲突：

1. 修改核心实现；
2. 更新调用方；
3. 更新测试；
4. 更新文档；
5. 删除失效逻辑。

不要：

- 同时维护新旧代码路径；
- 自动识别并兼容已经废弃的内部格式；
- 为旧内部 API 长期保留转发层；
- 用 fallback 保留已经明确淘汰的行为。

只有存在明确的外部兼容要求时，才增加兼容逻辑，并应明确记录兼容对象和退出条件。

## Git 规范

提交信息使用中文。

优先使用简洁、具体、可检索的动宾短语，例如：

```text
接入 zcv-text
补充协作规范
修复事务版本校验
```

避免：

```text
update
fix
wip
调整
```

提交信息应说明“做了什么”，而不是记录完整实现过程。

不要在 commit message 中展开：

- 背景分析；
- 调试过程；
- 方案比较；
- review 对话；
- 多段设计说明。

详细背景和设计理由放在 PR、changelog 或设计文档中。

## 完成标准

一次代码修改完成前，至少确认：

1. 修改解决了原始问题。
2. 修改建立在正确的架构方向上，而不是局部补丁上。
3. 没有为了兼容错误架构增加双轨逻辑。
4. 没有引入当前需求之外的抽象或能力。
5. 修改后的模块职责清晰。
6. 新增或修改的状态具有明确所有权。
7. 没有隐藏重要行为的隐式回退或默认逻辑。
8. 没有新增警告，也没有通过抑制手段隐藏警告。
9. 相关测试已经增加或更新。
10. 测试验证的是行为，而不是无意义的实现细节。
11. 执行了与修改范围对应的验证。
12. 验证失败时没有把环境问题错误描述为代码通过。
13. 相关注释与当前实现一致。
14. 相关文档与当前实现一致。
15. diff 中没有与当前任务无关的修改。
16. 没有为了潜在未来需求引入推测性实现。
17. 没有违反当前不引入实时协作、远程能力和 LSP 的项目范围。
18. 涉及编辑器成熟能力时，已经检查是否需要参考 Zed 的对应实现。
19. 中文自然语言遵循中文第一性原则。
20. 回复中明确说明实际执行过的验证以及仍未验证的部分。

---
> Source: [liuyuan-1024/Zcv](https://github.com/liuyuan-1024/Zcv) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-09-17 -->
