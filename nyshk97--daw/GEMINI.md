## daw

> - アプリ名（表示名・成果物名）は **LaLa**。内部識別子（CMakeターゲット `daw`・bundle id `local.d0ne1s.daw`・`~/Music/daw/`・`~/Library/Logs/daw/`・配信repo `daw-releases`・Sparkle鍵アカウント `daw`）は旧名のまま（意図的。TCC・Sparkle・既存データの連続性のため変更しない）

# LaLa — 個人用DAW

- アプリ名（表示名・成果物名）は **LaLa**。内部識別子（CMakeターゲット `daw`・bundle id `local.d0ne1s.daw`・`~/Music/daw/`・`~/Library/Logs/daw/`・配信repo `daw-releases`・Sparkle鍵アカウント `daw`）は旧名のまま（意図的。TCC・Sparkle・既存データの連続性のため変更しない）

## プロジェクト概要

### 目的
- Logic Proの機能過多による認知コストを削減するため、自分が本当に使う機能だけに絞った個人用DAWをJUCEでゼロから自作する
- 「全部入り」ではなく「引き算」が価値。機能追加の提案は常に「本当に自分が使うか」で判断する

### 非目的
- Apple純正音源（Logic付属音源）の代替は狙わない
- トラック数は〜50程度までを想定。数百トラック規模の運用は想定しない
- 商用配布・他人向けの汎用性は考えない

### サブ目的: 作りながら音楽の理解を深める

このアプリを作る過程で、音楽・音響・既存DAWの設計思想への理解を深めることもゴールに含む。そのための知識は専用ファイルに貯めず、**実装中の説明・提案・選択肢提示の書き方そのもの**で渡す。

**制作者の前提知識**（説明の粒度を決める基準）

- 制作歴3年。ただし「購入したビートの構成を切り直し、歌詞を書いてラップを録る」が主戦場。DAWのワークフロー用語（トラック・リージョン・構成・テイク・パンチイン・BPM・小節）は説明不要
- 作る曲のボリュームゾーンは**メロディアスな chill 系 hiphop で、トラップはたまに**（2026-08-06 /dig で確定）。機能・生成器の初期値やトレードオフの判断はこの前提に倒す
- EQ・コンプ・リバーブ・リミッターは**名前とレシピは知っているが原理を知らない**。「ボーカルにはハイパス」のような手順は知っていても、なぜそうするか・かけないとどうなるかが空欄になっている
- ゲインステージング・dBFS/ヘッドルーム・位相・サチュレーション・シンセの音作り・ドラムの音色選びはほぼ未知（ビートを買っていた＝この領域を外注できていた）
- ゼロからのビートメイクは1〜2曲のみ
- 統計・機械学習の用語は未知。AUC・p値・多重比較補正・相関係数は**都度、数値の読み方まで説明する**。数字を出したら「それが大きい/小さいとは何を意味するか」を1〜2文で添える

**説明・提案の作法**

- 機能の説明は、実装の話より先に「これは音楽的に何を解決する道具か」を1〜2文置く
- 「〜するといい」で終わらせず必ず因果で書く（レシピの丸暗記こそが脱したい状態）。ただし4文を超えたら削る
- 数値（`-0.3dBFS`・`20ms`・`Q=1.4` 等）を出したら、その値である理由を一言添える
- Logic / 一般的なDAWでの呼び名を併記する。LaLa独自語で完結させると外で得た知識と接続できなくなる
- 用語の注釈は初出時のみ。2回目以降は付けない
- 選択肢を出すときは、実装コストではなく**音楽的な結果**（聴こえ方・作業の速さ）でトレードオフを書き、「他のDAWはどちらを選んでいて、なぜか」を添える。「世の中のソフトが複雑な理由」を知る近道になる
- ビートメイク側（ドラム・シンセ・音色）の機能は経験が薄い前提で厚めに補う。逆に録音・構成側は本人の実地経験の方が確かなので、判断に迷ったら先に「Logicではどうしてた?」と聞く
- 深掘りは押し付けず「聞きたければ」のフックを1行置くに留める
- 歴史的経緯・逸話は幻覚しやすい領域。確度が低いときは断定せず、その旨を明示する
- **知識を理由に機能追加を勧めない**。知識の使い道は「意味が分かった上で捨てる」判断に倒れるのが健全（Logicとの差が「知らないから無い」から「知った上で無い」に変わる）

### 操作設計の方針
- キーボードショートカットは Logic Pro のデフォルトキーコマンドに合わせる（例: `r` = 録音、`m` = ミュート、`⌘←/→` = 横ズーム）。新しい操作を追加するときは、まずLogicの割り当てを確認してから決める
- ただし本人の好みが確定しているものはそちらを優先する（例: `,`/`.` = 1拍シーク・`Shift+,`/`.` = 1小節シーク。Logicの小節単位とは異なる）
- ビジュアル（音量バー・フェーダー・メーター等）もLogicのルックと読み方に準拠する。メーターは固定dBスケール（-60〜0dBFS・フェーダー位置と独立＝大きい信号はつまみを追い越す）、色は緑→黄→赤。常設のdB数値表示はミキサー/FXパネルのみに置き、常時見えるヘッダーには置かない（ドラッグ中ポップアップのみ）— Logicの配置と同じで、引き算の方針とも整合する
- undoの粒度は「ユーザーから見た1操作＝1件」で判定する。値を変える経路が複数あるなら（ドラッグ・ダブルクリック・数値クリック・ホイール等）**全経路で区切りを揃える**。1つ漏れると⌘Zの戻り先が変わり、「戻したはずが中間値」になる。経路を減らして揃えるのも有効な手段（使わない経路は無効化する）
- **仮配置・プレビュー系の機能は、キャンセルで巻き戻せない副作用を確定操作まで遅らせる**。SR確定・dirty化・ファイル書き出し・undo履歴への追加は「残す」の瞬間に一括で行い、プレビュー段階はメモリ内の変更（＋セッションbaselineで復元可能な値）だけに留める。プレビュー中に副作用を起こすと、キャンセル・失敗経路の数だけ復元処理が必要になり漏れが生まれる（ループ採用のSR確定で実際に3回レビュー指摘を受けた）

### ショートカットキーの追加ルール
- 定義は `Source/ui/Shortcuts.h` のテーブルが単一の真実の源。追加時は必ずここに1行足し（ID・カテゴリ・説明・表示用keyLabel・判定matcher）、`MainComponent::keyPressed` では `Shortcuts::matches()` で判定する（KeyPress直書き禁止）
- ボタンのツールチップ・右クリックメニューのショートカット表記は `Shortcuts::tooltipText()` / `Shortcuts::keyText()` から生成する（表記文字列の手書き禁止）
- ⌘?一覧（`ShortcutListOverlay`）はテーブル走査で自動生成されるため、追加時の個別対応は不要

## 現在地とゴール

**Tier 1（マルチトラック録音）と Tier 2（MIDI打ち込み・ミキサー・FX）は実装済み**。現在の主戦場はリファレンス駆動ビートメイク（[reference-beat.md](docs/design/reference-beat.md) のフェーズ進行）。機能の詳細仕様は `docs/plans/` の各planが真実の源。

実装済みの大枠（「無いと思って再実装」を防ぐための一覧。個別仕様はplanを見る）:

- **オーディオ**: マルチトラック録音（カウントイン・メトロノーム）・リージョン操作（分割・移動・フェード・ゲイン・ミュート・リピート/ループ・移調/タイムストレッチ〔非破壊・v20〕）・トラック並べ替え・サイクル再生・セクションマーカー・バウンス/リージョン書き出し・曲末フェード。**端ドラッグリサイズ（トリム）はMIDIリージョンのみ**（オーディオは意図して未実装）
- **取り込み**: オーディオファイルD&D（wav/aiff/flac/mp3/m4a→24bit WAVクリップ変換）・URL取り込み（yt-dlp）・右パネルのファイルブラウザ（`~/Downloads`起点・プレビュー再生付き）・MIDIファイル取り込み
- **MIDI**: MIDIトラック・ピアノロール・内蔵GM音源（DLSMusicDevice・厳選8音色＋ドラム）・サンプラー音源（サンプル割当）
- **ミキサー・FX**: 音量・パン・ミュート/ソロ・センドバス（Reverb A/B・Delay）・トラックEQ/Comp・バス/MasterのFX（常在・バイパスなし）
- **ボーカルのピッチ補正**（クリップ単位・非破壊・独立ウィンドウ。YIN 検出＋WORLD 再合成・自動スナップ＋ノート手直し・横移動は隣接吸収・解析はサイドカー世代保存）
- **リファレンスビート**: 参照曲の分析（Python外部パイプライン・並列実行）・アプリ内レポート・ドラム/ベースガチャ・ループトラック・制約カード・アンカーカード
- **配布**: Release版のSparkle自動アップデート（`daw-releases`）

まだ作らないもの:

- 汎用AU/VSTプラグインホスティング（Ext 枠は 2026-08-22 に廃止。理由は fx-roadmap.md「削った候補」。内蔵GM音源の固定ホストとは別物）
- UI面の意図的な見送りは [ui-principles.md](docs/design/ui-principles.md) の見送りリスト、機能の要否判断は [feature-scope.md](docs/design/feature-scope.md) を参照
- 現在のフェーズのスコープ外の実装を先回りして入れない（抽象化のしすぎに注意）

## 設計判断の参照先

複数の実装にまたがる方針は `docs/design/` にある。着手前に関係するものを読む。

- [feature-scope.md](docs/design/feature-scope.md) — 制作スコープの3層と機能の要否判断（使用頻度×複雑さの恒常的なコスト）。**機能追加を検討・提案する前に必ず読む**
- [ui-principles.md](docs/design/ui-principles.md) — 情報の強弱・画面の性格別の方針・日英混在のフォントサイズ・**意図的な見送りリスト**。UI追加時とUIレビュー対応時に読む
- [grid-resolution.md](docs/design/grid-resolution.md) — グリッド解像度の上限（タイムライン1/16・マーカーは拍・将来のピアノロールの要件）
- [transport-playhead.md](docs/design/transport-playhead.md) — 再生ヘッド（今いる場所＝編集の基準）と再生開始位置（次に鳴る場所）の分離。停止・録音・シークで両者がどう動くか。**トランスポートや「止めた場所」に関わる機能を触る前に読む**
- [mixer-fx.md](docs/design/mixer-fx.md) — Tier 2 ミキサー・FXの確定設計（画面構成・チャンネル構成・6スライス）
- [fx-roadmap.md](docs/design/fx-roadmap.md) — EQ・Comp後の残りFX・メーター群の実装順（5バッチ格子・耳セッションの方針・削った候補）。**新しいFX/メーターに着手する前に読む**
- [region-settings.md](docs/design/region-settings.md) — リージョン設定UIの割り当て方針
- [arrangement.md](docs/design/arrangement.md) — 少ない要素で作る方針と曲の尺・構成（90〜180秒・ボリュームゾーン120秒／verse-hook系の3構成／hookを先に作りverseは抜きで作る／常時鳴るのは5枠・足すならhookだけに1枠）。**ビートの編成や曲の構成を組む前に読む**
- [drum-pattern.md](docs/design/drum-pattern.md) — ドラムパターンの好みの境界（はっきり鳴るハットは毎秒4打点まで・実時間で決まる。キックは制約にしない）と、ドラム制作の方向（ガチャ＝基準からの崩しを見せる道具・レーン＝サンプル1個でレーン別トラック・音色は「組んで比べる」で決める）。**ドラムの打ち込み・ドラムガチャ・ドラム音色を触る前に読む**
- [reference-beat.md](docs/design/reference-beat.md) — リファレンス駆動ビートメイク（長期・2本柱: ガチャで作る＋レポートでわかる）。何を借りて何を借りないか・Python外部パイプライン＋LaLa内ガチャUIの分業・分析レポートとライブラリ蓄積・フェーズ表。実験記録は `docs/labs/reference-beat.md`（追記専用）。**上モノ素材の好みの境界（明るさ・コードの動きの目標帯）を含むので、ループ選び・推薦・上モノガチャを触る前に読む**
- [corpus-comparison.md](docs/design/corpus-comparison.md) — 2つの集団を比べる分析の作法（1次元化にラベルを使わない・相関した軸へのomnibus検定・交差ペアでhaloを潰す・非対称の事前監査・検出力の下限を書く）。**コーパス比較や対照分析を始める前に必ず読む**
- [sampling-rights.md](docs/design/sampling-rights.md) — 実曲サンプリングの権利リスクの見立て（検出・特定・執行の3変数）と帯分け（国内曲は全面回避・海外有名曲はYouTube限定・本命は海外×古い×マイナー）・レコード発掘の方針。**サンプリング素材の調達・曲の公開/配信に関わる機能や判断の前に読む**

新しい設計判断が出たらここに追記する（planに書かない。planは1実装分の記録なので後から探せなくなる）。

- **調査を設計する前に「その答えで何が変わるか」を確かめる。** 前提が実装都合で置いた制約なら、調査するより制約を外すほうが早いことがある（実例: 2026-08-22、「常設レーンを8つに絞る」という枠を疑わないまま、何を8つにするかの調査を2回作り直した。枠は画面の都合で置いたもので、外したら調査ごと不要になった）
- ゲート・閾値の判定が「誤検知に見える」場合でも、緩める前に `docs/labs/` の実験記録を検索して同じ症状の切り分け結論が無いか確認する（実例: tempo_stable の `min(gc)>1.0` を「裏拍が強い hiphop の癖」と仮説して緩めかけたが、labs 2026-08-04 が同一曲を「ライブ演奏の実揺れ」と切り分け済みだった）。症状の見た目から立てた仮説だけで判定を動かさない

耳確認など再取得できない人間判断は `docs/labs/reference-beat-human-answers/` にコピーを置く。
音声・分析JSON・距離表は再計算できるが、人間の回答だけはやり直しが要るため。

ブラインド試聴を依頼するときは、ファイルを並べるだけでなく**①ラベル（W/X/Y/Z 等）が「方式をランダムに伏せた記号」であること ②聴く順番（素材ごと・ケースごと） ③注目点（プツプツ・こもり・ピッチの追従）④回答テンプレ**を先に1通で渡す（pitchlab で聞き方の質問が3往復・素材の取り違えが1回起きた）。正解表はテンプレと同じディレクトリに `*-key.json` で置き、回答後に突き合わせる。

**人間に「分類」を求める問いは失敗する。「どちらに近いか」に組み替える。** 「この差は音色か処理か」のような分類は聞き分けの訓練が済んでいる人の作業で、答えられずに終わる。変数を1つだけ動かした3段（A / Aの1要素だけBに差し替えたもの / B）を作り「2段目は1と3のどちらに近いか」を聞けば、分類させずに同じ結論が出る。

**聴取タスクの重さは「聴く時間」でなく「記入する量」で決まる。** 7曲×2区間×7項目=98マスのチェックシートは、聴く時間が4分でも「大変すぎる」で却下された。表を作る前にマス目を数える。

**このプロジェクトの知見はClaude Codeのmemory機能に保存しない**（複数マシン・複数クローンで開発しており共有されないため）。恒久ルールはこのファイル、設計判断は `docs/design/`、技術的な落とし穴は `GOTCHAS.md`、動作確認手順は `VERIFY.md` に書く。

## 技術スタック

- JUCE（GUI・オーディオI/O・波形描画）
- CMake（JUCEはFetchContentまたはsubmoduleで取得）
- C++17以上（JUCEの要求に合わせる）
- macOS限定（CoreAudio）。クロスプラットフォーム対応のための抽象化はしない

## 手持ちの機材（2026-08 時点）

機材の提案・接続経路の検討はこの前提で行う。

- オーディオインターフェース: MOTU UltraLite mk5
  - **Phones 端子は CueMix で Phones Mix (L-R) に分離済み**（2026-08-18。デフォルトの Main 1-2 ミラーから変更）。アプリの出力を Phones Out 1+2 に向ければ「ヘッドフォンのみ・スピーカー無音」になる — ボーカル録音時の回り込み対策。逆に、通常の音楽再生（Main 1-2 行き）はヘッドフォンに流れない
  - **ボーカル録音のゲイン基準: 声のピークが -12〜-6dBFS**（録り音の検品は `tools/noiselab/measure.py`。回り込み・クリップ事故の実測記録は `docs/labs/noise-removal.md`）
- モニタースピーカー: GENELEC 8010AM
- モニターヘッドフォン: SONY MDR-7506
- ダイナミックマイク: SHURE SM58SE
- サンプラー: AKAI MPC（Macから見えるUSBオーディオ出力先になり、MPC側の機能で入力音を取り込める。オーディオファイルの直転送はUSB不可・microSD経由のみ）
- ネットワークストリーマー: Bluesound NODE NANO（mk5 の OPTICAL IN にデジタル接続。アナログのライン入力は消費していない）
- レコードプレイヤー: Audio-Technica AT-LP120XBT-USB（2026-08購入。フォノイコ内蔵＝LINE出力で mk5 のライン入力に直結。針は VM95E・交換針は VM95 系で入手容易）

## ビルド・実行コマンド

```sh
mise run build   # dev版ビルド（初回はconfigure＝JUCE 8.0.9取得も自動。数分かかる）
mise run start   # LaLa-dev.app を起動
mise run test    # daw_tests（C++回帰テスト）
# 素のコマンド: cmake -B build -DCMAKE_BUILD_TYPE=Debug && cmake --build build
```

- **Debug = dev版**（`LaLa-dev.app`・bundle id `local.d0ne1s.daw.dev`・DEVリボン付きアイコン）、**Release = 常用版**（`LaLa.app`・`mise run build:release`）。名前・bundle id・アイコンはCMakeLists.txtで切り替え。開発中の動作確認はdev版で行う
- アプリアイコンは `Assets/make_icon.swift` で生成（`swiftc` でビルドして実行。dev版は `--dev` フラグ。生成済みPNGは `Assets/` にコミット済み）
- アイコンPNG（`Assets/icon*.png`）を差し替えたら `cmake -B build ...` の**configureを再実行**する。`Icon.icns` はビルド時でなくconfigure時にjuceaideが生成するため、`cmake --build` だけでは古いアイコンのまま（生成済みicnsを手で消すと `No rule to make target` で落ちる）。反映確認は `sips -s format png <app>/Contents/Resources/Icon.icns --out /tmp/x.png` で展開して目視

- JUCEは`CMakeLists.txt`で **8.0.9 に pin** 済み。GOTCHAS.mdの`createWriterFor`（`AudioFormatWriterOptions`版）が実在することは`build/_deps/juce-src/modules/juce_audio_formats/format/juce_AudioFormat.h`で照合済み。**バージョンを上げるときは再照合すること**
- プロジェクトの置き場所: `~/Music/daw/<プロジェクト名>/`（project.json＋`clip-NNN.wav`）
- 録音フォーマット: モノラル24bit WAV。サンプルレートはデバイスに追従し project.json に記録
- アプリログ: `~/Library/Logs/daw/`（セッションごと1ファイル）。不具合調査・動作確認の裏取りはまずここを見る（`shared/Log.h`。オーディオスレッドからは呼ばない）
- **リリース状況の真実の源はリモート**。このリポジトリは複数マシン・複数クローンで並行開発しており、別クローンからリリースされることがある。ローカルのタグやCMakeListsのバージョンでリリース済みかを判断しない（`git fetch origin --tags` と `gh release list --repo nyshk97/daw-releases` で確認する）

## ディレクトリ構成方針

```
daw/
├── CMakeLists.txt
├── Source/
│   ├── Main.cpp          # エントリポイント・MainWindow
│   ├── audio/            # オーディオエンジン（リアルタイムスレッド側のコード）
│   ├── ui/               # UIコンポーネント（メッセージスレッド側のコード）
│   └── shared/           # スレッド間で受け渡すデータ構造（FIFO・atomic等）
├── docs/plans/           # 実装計画（/plot で生成）
├── docs/design/          # 複数planにまたがる設計判断（索引は「設計判断の参照先」節）
├── GOTCHAS.md            # JUCE・リアルタイムオーディオの落とし穴集
└── VERIFY.md             # 動作確認手順
```

- **audio/ と ui/ の境界 = スレッド境界**。audio/ のコードはオーディオスレッドで走る前提で書き、ui/ への直接参照を持たない。受け渡しは必ず shared/ の構造を経由する
- スレッド境界に関する注意点は GOTCHAS.md の**必読コア**（オーディオコールバック内の禁止事項・スレッド分離パターン。ファイル先頭の2セクション）を必ず参照すること。それ以外の個別の罠（JUCE一般・DSP・分析パイプライン）は全読みせず、`grep "^###" GOTCHAS.md` で見出しを引いて触る対象に該当するセクションだけ読む
- **判断ロジックを ui/ に書かない**。`daw_tests` はUIの `.cpp` を含まない（テストを軽く保つ設計）ため、UIに埋めた判断は**回帰テストで固定できない**。「終端をどう決めるか」「範囲をどう切るか」のような分岐は `Project` や `BounceRenderer::Request` のメソッドへ切り出し、UIからは呼ぶだけにする。テストが書けるかどうかが、そのロジックの置き場所を決める基準になる

---
> Source: [nyshk97/daw](https://github.com/nyshk97/daw) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-09-23 -->
