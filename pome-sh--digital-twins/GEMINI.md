## digital-twins

> > Operational notes for anyone — human or AI agent — working in this repo.

# AGENTS.md

> Operational notes for anyone — human or AI agent — working in this repo.

## What this repo is

The open-source Pome twins and the `pome` CLI. The twins are local, resettable
services that answer the same REST, GraphQL, and MCP calls your agent makes in
production (GitHub, Stripe, Slack, Gmail, Linear), each backed by real SQLite
state. The CLI boots a
twin, runs an agent against it, and records the trace for you to inspect.
Evaluation and scoring are hosted features — `pome run --local` records a trace
only, no local scoring. The Pome platform (evaluation, simulation,
observability) is at https://pome.sh. Apache-2.0.

## Docs

Repo layout, full build/test workflow, conventions, and the contributor guide
live at **https://docs.pome.sh**.

## Before you build

- **npm only** — one root `package-lock.json` for `packages/*`; use
  `npm ci` / `npm install`.
- **Vocabulary: the product term is "task"** — "scenario" is retired (F-778,
  F-860, F-892). Never introduce "scenario" in new code, docs, or CLI copy.
  F-892 completed the CLI rename end-to-end: the command (`pome tasks`), scaffold
  (`./tasks/`), and the internal runner/schema surface (`src/task/`, `runTask*`,
  the `Task` type, `parseTask`, `taskSchema`). `pome scenarios` survives only as
  a hidden deprecated alias. The ONLY remaining sanctioned survivors are the
  serialized `scenario` / `scenario_*` keys that have a contract behind them —
  `meta.json`'s `scenario` slug (uploaded to cloud finalize; also read back by
  `pome eval` / `pome inspect` from run dirs older CLIs wrote) and the
  finalize / result / compile-seed wire fields (server contract; flip with
  W3/FDRS-653) — and the in-memory carriers whose value flows straight into
  them. F-933 renamed the two artifact keys that had no such contract:
  `runs/latest.json` now writes `task` (was `scenario`) and
  `runs/<task>/<session>/verdict.json` writes `task_path` (was
  `scenario_path`). F-1195 retired the verdict READ path's `scenario_path`
  tolerance: `verdict.json` is at artifact version 2, every file spelling it
  the old way is version 1, and the version check refuses those before the
  spelling could matter — so the normalize step was a dual-format reader that
  could no longer fire. Such a file is still RECOGNIZED as one of ours and
  `pome fix-prompt` names the skip (`staleVersionCount`) rather than dropping
  it silently. F-1411 gives the same treatment to a verdict.json that EXISTS
  on disk but cannot be read at all — truncated, hand-edited into an
  unexpected `state`, or valid JSON that is not one of our artifacts (the
  version check never even reaches such a file) — a different fact (nothing
  wrote this file correctly, vs. an older CLI that did) — counted and
  path-named separately as `unreadableCount` / `unreadablePaths`, never
  folded into `staleVersionCount`. A run dir with NO verdict.json is neither,
  and stays silently skipped.
- **The CLI (`cli/`) IS a root workspace member** — `workspaces: ["packages/*",
  "cli"]`, one `package-lock.json`, one `npm ci`. Use `npm run -w @pome-sh/cli
  ...` from the root. The former `cli/package-lock.json` and
  `cli/pnpm-workspace.yaml` (the changesets/manypkg root marker) are gone.
- **Internal `@pome-sh/*` deps are `"*"`** — sdk, wire and the five
  twins are workspace members resolved by npm's workspace linking, never from
  a registry. Never reintroduce an exact version pin between them: the exact
  pins drifted and installed a second registry copy of `@pome-sh/shared-types`
  (two zod schema identities at one runtime). This holds for `@pome-sh/wire`
  even though it is now published (F-949): in-repo consumers must keep
  resolving it through the workspace, never through GitHub Packages.

## Releases (`@pome-sh/cli`, `@pome-sh/adapter-claude-sdk`, `@pome-sh/checks`, `@pome-sh/wire`)

`@pome-sh/cli` and `@pome-sh/adapter-claude-sdk` are the ONLY things published
to npm for END USERS. `@pome-sh/checks` is also on npm but is not an end-user
package: it carries the five twins' check declarations, seed schemas and the
check DSL to `pome-sh/pome-cloud`, which grades every `[code]` criterion out of
them (F-1308). It re-exports rather than copies, and tsup inlines the twins'
compiled output, so it declares zero `@pome-sh/*` runtime dependencies — which is
also what keeps the `"*"`-only pin rule below intact for a PUBLISHED package. `@pome-sh/wire` is published to **GitHub Packages**
(`npm.pkg.github.com`) for internal cross-repo consumers — pome-cloud — while
ALSO still being bundled into both npm tarballs by tsup; publishing it was
additive and changed nothing about how this repo consumes it (F-949).
Everything else (`@pome-sh/sdk`, the five `@pome-sh/twin-*`) is a
`private: true` workspace member bundled into the two npm tarballs by tsup
(`noExternal: [/^@pome-sh\//]`).

**Do not write a version number in a PR.** Add an `## Unreleased (patch)` or
`## Unreleased (minor)` entry to that package's `CHANGELOG.md`; the number is
allocated on `main` after the merge by
[`.github/workflows/allocate-version.yml`](.github/workflows/allocate-version.yml)
(F-1511; reasoning and the rejected alternative in [`RELEASING.md`](RELEASING.md)),
and `release.yml` publishes from that commit's push exactly as before. Every PR
carrying the shared version line meant every merge silently invalidated every
open PR that had pinned a consumed number.

The full runbook — the version-diff-on-push model, what earns a release, the
one-time `pome-ops-push` app-token setup + ruleset bypass a maintainer owns, why the four
packages version independently, why the npmjs and GitHub Packages publishes are
separate jobs with different auth (OIDC vs `GITHUB_TOKEN`), the one-time package
visibility step, and the PR-time release-note gate — lives in
[`RELEASING.md`](RELEASING.md). One repo-specific historical note that doesn't
belong there: F-1180's `RELEASE_BOT_TOKEN` / release-PR check verifier applied to
the old Changesets version PR, and that `RELEASE_BOT_TOKEN` secret is still sitting
on this repository unused. The `pome-ops-push` app token `allocate-version.yml`
mints is not that machinery coming back — there is still no bot-pushed version PR
and nothing waits on one; the bot pushes one commit straight to `main` and nothing
gates on it.

## Invariants ↔ CI checks (P8)

Docs are contracts. Any PR that changes an invariant below must update this
section in the same PR.

| Invariant | Enforced by |
| --- | --- |
| npm only | root `packageManager` is npm; CI/Docker use `npm ci` and committed `package-lock.json` |
| Renovate is the sole dependency updater — fortnightly batched non-majors, ≤1 concurrent PR, security anytime; ignores release-gated `@pome-sh/*` npm bumps | [`renovate.json`](renovate.json) (Mend Renovate GitHub App) |
| Capture is open, evaluation is the product — no local eval/scoring/judging/correlation anywhere in `cli/src/**`, `cli/scripts/**`, `packages/**`, or repo-root `scripts/**` | [`scripts/no-eval-in-oss.mjs`](scripts/no-eval-in-oss.mjs) (`npm run gate:no-eval`) in [`.github/workflows/ci.yml`](.github/workflows/ci.yml) — repo-wide, path + module-name + import denylist, empty file allowlist. The module-name rule is a **prefix** match (`correlate*`/`score*`/`judge*`/`verdict*`); an infix like `runScorer.ts` relies on the import rule instead — accepted policy, not a gap. |
| No cloud imports in OSS (`packages/`, `cli/src/`, `cli/scripts/`, `scripts/`) — including bare `pome-cloud/*` | [`scripts/lint-no-cloud-imports.sh`](scripts/lint-no-cloud-imports.sh) (+ fixture test `scripts/lint-no-cloud-imports.test.sh`) |
| Public `main` protection: PR required + conversation resolution + strict required checks matching `config/required-checks.json` + no force-push + no branch deletion (all from ruleset `main founder-bypass`, with classic branch protection underneath as the layer the ruleset's bypass actors cannot lift), zero required approving reviews so either founder can merge on green CI | [`scripts/ci/assert-repo-policy.sh`](scripts/ci/assert-repo-policy.sh) via [`.github/workflows/repo-policy.yml`](.github/workflows/repo-policy.yml) — reads `GET /repos/{owner}/{repo}/rules/branches/{branch}` with the ordinary `GITHUB_TOKEN` (F-1212: the legacy pair it replaces needed Administration:read, which `GITHUB_TOKEN` cannot hold — `.../protection` is auth-gated outright, and `.../rulesets/{id}` answers 200 for anyone on a public repo but elides `bypass_actors` without that scope — so the live check depended on a PAT, `REPO_POLICY_TOKEN`, that was never minted and the weekly cron was red since it shipped; this endpoint needs no PAT and runs on every PR too, not just weekly). Coverage is asserted, not assumed: an empty/missing rules response, or any policy (`pull_request`, `required_status_checks`, `non_fast_forward`, `deletion`) with no matching rule, is a hard failure naming that policy — never a silent pass, and a declared policy whose assertion block stops running is itself a failure. Flipping the ruleset to `evaluate`/`disabled` removes its rules from this endpoint, so losing enforcement reds rather than passing. NOT covered live, and printed as unwatched on every green run: the ruleset's founder-team bypass actor — GitHub elides the `bypass_actors` field for callers without Administration:read, so asserting it would fail open. That gap is why classic branch protection must stay: ruleset `18797095`'s bypass actors are the `Founder` team and the `pome-ops-push` app, both `bypass_mode: always`, so every rule above — including the `deletion` rule F-1517 added and asserts here — is bypassable by them. Classic protection (`allow_deletions: false`, `allow_force_pushes: false`, `enforce_admins: true`) binds them too and is the only layer that does. **Do not remove it on the strength of the ruleset rule.** Neither it nor `bypass_actors` is readable without Administration:read, so neither is asserted; what F-1517 closed is that deletion is now watched for non-bypass actors, where before it was watched for nobody |
| Twin image publish waits for both `ci.yml` and `secret-scan.yml` on the same SHA; PR image matrix builds only changed twins (full matrix on main/tags) | [`scripts/ci/wait-for-workflow.sh`](scripts/ci/wait-for-workflow.sh) in [`.github/workflows/twin-image.yml`](.github/workflows/twin-image.yml); regression: [`scripts/ci/wait-for-workflow.test.mjs`](scripts/ci/wait-for-workflow.test.mjs) |
| Required `typecheck-test` always reports on PRs; heavy npm build/test/parity is skipped for docs/chore-only diffs | [`.github/workflows/ci.yml`](.github/workflows/ci.yml) path-aware scope step |
| No cross-package file copies | [`scripts/check-copy-markers.mjs`](scripts/check-copy-markers.mjs) (empty allowlist) |
| Every internal `@pome-sh/*` dep declared by any member of the root `workspaces` field — `packages/*` and `cli` today, and whatever that field grows next, since the gate reads it rather than holding its own list — is `"*"`/`workspace:*` or an exact semver EQUAL to the sibling's workspace version, in `dependencies`, `devDependencies`, `peerDependencies` or `optionalDependencies`. A drifted exact pin is the one that silently swaps the workspace tree for a nested registry install; the other refused forms (caret/tilde/range, `file:`/`link:`/`npm:`, dist-tag) are refused for legibility rather than because each would resolve from the registry today — a range admitting the sibling's current version resolves to the workspace, and `file:` cannot reach a registry at all, but neither keeps holding across the next bump or directory move. Every member is also a SIBLING, so a `packages/*` package pinning a stale `@pome-sh/cli` reds too. Deliberately NOT covered: `examples/*` (standalone packages that install from the registry on purpose — `examples/support-triage` pins the published adapter because it is `npx degit`-fetchable as a standalone subtree; its own published-pin drift is a *different* rule, the row below), `bundleDependencies`, and root `overrides` (which has a live block and would defeat workspace linking for every consumer if an `@pome-sh/*` entry were added to it) | [`scripts/check-workspace-pins-match-workspace.mjs`](scripts/check-workspace-pins-match-workspace.mjs) in [`.github/workflows/ci.yml`](.github/workflows/ci.yml)'s always-on block; regression: [`scripts/check-workspace-pins-match-workspace.test.mjs`](scripts/check-workspace-pins-match-workspace.test.mjs) |
| `examples/*`'s own `@pome-sh/*` pins are exact versions ON PURPOSE (the row above's "must resolve to the workspace" rule is wrong for them), and each one must equal the sibling workspace version WHENEVER that version is published — `examples/support-triage` pinned a stale published `@pome-sh/adapter-claude-sdk` twice (F-1231/F-942: #308 off `0.2.5`, then off `0.3.1`, which also dragged the retired `@pome-sh/shared-types` back into the example's install graph as a runtime dependency) with nothing watching. This needs the registry, so it cannot live in the offline gate above; it runs where `examples/*` are already `npm ci`'d instead of standing up a second mechanism. A workspace version that is NOT yet published is a named, counted skip (the ordinary cloud-first ordering), never a silent pass; a registry answer that is neither "found" nor a genuine `E404` (a 401/5xx/timeout — the lookup carries a 60s `timeout` so a stalled registry becomes an error rather than a hung job) is a hard failure, matching `scripts/ci/decide-publish.sh`'s own 404-vs-error stance. Every `@pome-sh/*` dep with a workspace sibling is classified and ALL THREE classes are reported, because "cannot be compared" must not read as "exempt": an exact semver is checked against the registry; a `file:`/`link:` link resolves from source, cannot reach a registry, and is counted-but-out-of-scope; anything else (caret/tilde/range, `"*"`, dist-tag, `npm:`) resolves FROM the registry yet has no single version to compare, so it **reds as unwatchable**. Without that third class, re-pinning the hero example to `^0.3.3` silently deleted its watch while the report printed a pass — the zero-pins floor only caught it because support-triage owns the tree's sole exact pin, so one more exact pin anywhere restored the silence. The floor counts EXACT pins only (`linked` deliberately does not satisfy it, or an all-`file:` `examples/` tree would pass green having made no registry call), a lookup is retried three times before a non-404 answer reds the required check while an `E404` short-circuits on the first attempt, and the parity report runs even when an example FAILS typecheck — it used to sit behind that exit, so an unrelated tsc error hid a real drift. Deliberately NOT covered: `overrides` — npm refuses `EOVERRIDE` for an override conflicting with a direct dependency, so an `examples/*` override cannot silently retarget its own `@pome-sh/*` dep | [`scripts/check-example-pins-published.mjs`](scripts/check-example-pins-published.mjs)'s `reportExamplePinParity()`, called from [`scripts/typecheck-examples.mjs`](scripts/typecheck-examples.mjs) (`npm run typecheck:examples`) in [`.github/workflows/ci.yml`](.github/workflows/ci.yml)'s heavy block; regression (pure functions, injected registry answers): [`scripts/check-example-pins-published.test.mjs`](scripts/check-example-pins-published.test.mjs) in the always-on block |
| `.claude-plugin/plugin.json` lists every skill under `skills/`. The manifest is what makes the `skills` CLI group the set under one collapsible row whose radio selects all six at once (README → "Plugin Manifest Discovery"); without it the installer renders a flat list with nothing ticked and a user installs one skill of a set that only works whole (F-1226). A seventh skill nobody lists lands in the installer's "Other" bucket, outside that row — both files stay valid and the install still succeeds, so nothing else notices. Lives in ci.yml's always-on block on purpose: adding a skill touches only `skills/`, which the scope step classifies as docs-only | [`scripts/check-plugin-manifest-lists-every-skill.mjs`](scripts/check-plugin-manifest-lists-every-skill.mjs) (`npm run lint:skill-manifest`) in [`.github/workflows/ci.yml`](.github/workflows/ci.yml); regression: [`scripts/check-plugin-manifest-lists-every-skill.test.mjs`](scripts/check-plugin-manifest-lists-every-skill.test.mjs) |
| Criterion markers are `[code]`/`[model]` — the retired bracketed `D`/`P` forms never reappear (allowlist: the parser's legacy-detection + its rejection tests; `CHANGELOG.md`s are historical records) | [`scripts/lint-legacy-criterion-markers.mjs`](scripts/lint-legacy-criterion-markers.mjs) via `npm run lint:legacy-markers` in [`.github/workflows/ci.yml`](.github/workflows/ci.yml) |
| The criterion grammar quoted in [`skills/pome-author-task/references/task-format.md`](skills/pome-author-task/references/task-format.md) is the one `CRITERION_LINE_RE` in `cli/src/task/parseTask.ts` actually runs. That file is a byte-identical mirror of pome-cloud's `apps/mcp/docs/task-format.md`, which pome-cloud gates; nothing gated the mirror, so it published a grammar that dropped `always-scored` criteria as prose for the whole of F-1296. A cross-repo byte diff is not runnable here, so the pin is doc↔local-parser — and the parser is itself required to match the hosted one. Always-on block: a PR that edits only `skills/` is classified docs-only | [`scripts/lint-task-format-doc.mjs`](scripts/lint-task-format-doc.mjs) (`npm run lint:task-format-doc`) in [`.github/workflows/ci.yml`](.github/workflows/ci.yml); regression: [`scripts/lint-task-format-doc.test.mjs`](scripts/lint-task-format-doc.test.mjs) |
| Dead code / orphan packages = 0 | [`knip.json`](knip.json) via `npm run lint:dead-code` in [`.github/workflows/ci.yml`](.github/workflows/ci.yml) |
| Zero native modules in the production dependency closure (gyp markers: `binding.gyp` / `"gypfile"`; prebuilt installers like esbuild/fsevents pass) | [`scripts/no-native-modules.mjs`](scripts/no-native-modules.mjs) via `npm run gate:no-native` in [`.github/workflows/ci.yml`](.github/workflows/ci.yml) |
| Every third-party dep of a package INLINED into the CLI bundle is declared by the CLI (a bundled package's own `dependencies` are never installed for it) | [`scripts/check-bundled-runtime-deps.mjs`](scripts/check-bundled-runtime-deps.mjs) via `npm run gate:bundled-deps` in [`ci.yml`](.github/workflows/ci.yml) and [`release.yml`](.github/workflows/release.yml) |
| Both published tarballs install and run with no access to this workspace; all five twins boot from the CLI tarball; a consumer typechecks against the adapter's shipped declarations | [`scripts/clean-room-pack-test.mjs`](scripts/clean-room-pack-test.mjs) via `npm run test:pack` in [`ci.yml`](.github/workflows/ci.yml) and [`release.yml`](.github/workflows/release.yml) |
| Package barrels + file-size hygiene | [`scripts/lint-code-health.mjs`](scripts/lint-code-health.mjs) |
| A published version is never behind its registry's `latest` — a publish must not retag `latest` backwards (an unpublished package's `0.0.0` baseline passes; a 404 is "unpublished", a 401/network error is a hard failure, which matters for GitHub Packages where an under-scoped read answers 401 for a package that exists) | [`scripts/ci/decide-publish.sh`](scripts/ci/decide-publish.sh), called by the `plan` and `plan-wire` jobs in [`.github/workflows/release.yml`](.github/workflows/release.yml); regression: [`scripts/ci/decide-publish.test.mjs`](scripts/ci/decide-publish.test.mjs) |
| The npmjs and GitHub Packages release lanes cannot block each other — `plan`/`publish` never reads `npm.pkg.github.com` and `plan-wire`/`publish-wire` never reads npmjs. A GitHub Packages 401 (which an org owner can cause from the package settings UI, with no commit) is a hard failure by design, so a shared `plan` job would let it silently skip the `@pome-sh/cli` and `@pome-sh/adapter-claude-sdk` publishes | job wiring in [`.github/workflows/release.yml`](.github/workflows/release.yml); asserted by [`scripts/ci/decide-publish.test.mjs`](scripts/ci/decide-publish.test.mjs) |
| **No PR writes a version number.** The number is allocated on `main` after the merge, and a PR that moves a published package's `version` field is refused. `release.yml` only publishes on a version diff, so an unreleased change silently never reaches the registry — that failure is unchanged and is why the publish-relevance table (`cli/`, `packages/twin-*/`, `packages/wire/`, `packages/sdk/` for the CLI; `packages/adapter-claude-sdk/`, `packages/wire/` for the adapter; `packages/wire/` for wire itself; the named declaration FILES for `@pome-sh/checks`) moved rather than being deleted: it is now the allocator's authority over which packages get a number, and the PR gate's over which need an `## Unreleased (patch\|minor)` entry. What a PR owes is the WORDS and the patch/minor judgement; `packages/wire/` still appears in three entries because a wire change alters three published artifacts, and the allocator moves all three whether or not all three entries were written (a version-only entry naming the commits is written for those that were not — the same accommodation `cli/CHANGELOG.md` 0.21.7 records). The reason the number left PRs: every twins PR hand-wrote the shared version line, so every merge invalidated every open PR that had pinned a consumed number — silently, while still green, across workspaces and humans, worse with parallelism (measured 2026-08-13: #402/#405 stale-green overnight, five renumber+force-push cycles in one night, 0.23.35/.36 burned unpublished). A version that differs DOWNWARD used to be its own named failure (a stale branch rebased past a release hard-failed the release floor check after merge, taking its lane's publishes with it); it is now the same refusal as any other hand-written number, because there is no longer a right value for a PR to carry. Not a return to changesets or batch releases — #239 killed batching and release-PR waits and both stay dead; only *who writes the number* moved | [`scripts/ci/check-release-note-required.mjs`](scripts/ci/check-release-note-required.mjs) in [`.github/workflows/ci.yml`](.github/workflows/ci.yml) (PR-only) + [`.github/workflows/allocate-version.yml`](.github/workflows/allocate-version.yml) via [`scripts/ci/allocate-release-versions.mjs`](scripts/ci/allocate-release-versions.mjs); shared table: [`scripts/ci/publish-relevance.mjs`](scripts/ci/publish-relevance.mjs); runbook, reasoning and the rejected alternative: [`RELEASING.md`](RELEASING.md) (the full ADR lives in the private pome-cloud docs, per this repo's convention that design records do not live here) |
| A published package's `CHANGELOG.md` is INSERTIONS ONLY, and its newest released heading names the version its manifest declares. The heading↔number binding is the old CHANGELOG contract's surviving half, moved to where the number is now written: one script writes `"version": "0.23.46"` and `## 0.23.46 — 2026-08-14` in one commit, so they cannot drift instead of being re-asserted against two hand-written copies. Insertions-only is held twice — by construction in the writer (`writeRelease()` reassembles a file as `preamble + newSection + releasedRegion`, so a released entry is carried across byte-for-byte) and against the base branch in the PR gate. The released region is also never PARSED for release requests: it is a record, `packages/adapter-claude-sdk/CHANGELOG.md` has carried a bare `## Unreleased` from the Changesets era since July, and refusing to cut a release over a heading from July would be a parser policing history it may not correct — the accident that could otherwise hide (a real pending entry placed BELOW the newest release, whose release then silently never happens) is caught by insertions-only, since it changes the released region. A pending heading that is not exactly `## Unreleased (patch)`/`(minor)` is a hard failure rather than treated as absent, and there is no default level: whether consumers must act is not inferable, and guessing "patch" would ship a breaking change under a caret range that resolves it automatically | [`scripts/ci/changelog-entry.mjs`](scripts/ci/changelog-entry.mjs) (the grammar), asserted by [`scripts/ci/check-release-note-required.mjs`](scripts/ci/check-release-note-required.mjs) pre-merge and by [`scripts/ci/allocate-release-versions.mjs`](scripts/ci/allocate-release-versions.mjs) at write time; regressions: [`scripts/ci/check-release-note-required.test.mjs`](scripts/ci/check-release-note-required.test.mjs), [`scripts/ci/allocate-release-versions.test.mjs`](scripts/ci/allocate-release-versions.test.mjs) |
| The version-allocating push can only ever happen with the `pome-ops-push` installation token. `allocate-version.yml`'s checkout carries `token: ${{ steps.app-token.outputs.token || github.token }}`, and that fallback exists for ONE arm — the plan-only `pull_request` arm, which never writes and which on a fork has no secrets at all. Reaching it on a push is a silent DOUBLE failure: `github-actions` can never be a ruleset bypass actor (UI and API both refuse it, and deploy keys are org-disabled), so the push is refused; and a push made with `GITHUB_TOKEN` does not trigger workflows, so a commit that somehow landed would publish nothing. Both read as a quiet green. Four one-line edits would each make it reachable and none looks wrong in review — dropping the credential precondition, marking the precondition or the mint step `continue-on-error: true` (asserted in both the literal and the `${{ true }}` spelling, since the expression form reads as configuration), narrowing either step's `if:` so it can be false on a push, or handing the push step the ambient token — so reachability is a PROPERTY over the workflow text rather than a comment claiming it. The mint step is what holds it: it runs on every non-PR event with no `continue-on-error`, so on a push `steps.app-token.outputs.token` is either non-empty or the job has already failed, because every later step inherits an implicit `success()`. Every ambient-token mention in the file must be that one sanctioned fallback expression; a bare `token: ${{ github.token }}` — the shape someone reaches for while the mint step is commented out — reds, in both the `github.token` and `secrets.GITHUB_TOKEN` spellings. Comment stripping is `list-scheduled-workflows.mjs`'s quote-aware one, not a third copy. The same check owns the CHECKOUT REF, which is per arm and is how this workflow's first live run died: on a push it must be `main` — the moving TIP, not the event sha, because a merge that landed while the run queued belongs in the same release — and on a `pull_request` it must NOT be `main`, because that is a tree without the PR's own files, so the arm whose whole job is proving this PR's allocator runs instead fails on the first missing module (PR #421, `Cannot find module …/allocate-release-versions.test.mjs`). Asserted only while a `pull_request:` trigger exists to serve, and both spellings of the merge ref are accepted, because a guard that reds a right answer gets deleted. The pushing step must also DISTINGUISH a rule refusal (GH013) from a non-fast-forward race, because the first live run spent all three attempts reporting "a merge landed first" while `main` was answering `GH013 — 3 of 3 required status checks are expected`: classic branch protection carried a duplicate required-checks rule, no App can bypass it (`enforce_admins` applies to everyone), and retrying cannot fix a rule. That copy was deleted 2026-08-13; the ruleset's copy — the one `assert-repo-policy.sh` asserts in both directions — still enforces the same three contexts, so nothing checked stopped being checked. RESIDUAL, stated rather than implied: nothing in CI can assert the classic copy stays deleted (`GET …/branches/main/protection` is 401 without `Administration:read`, which `GITHUB_TOKEN` cannot hold since F-1212 deleted the PAT that could, and the `rules/branches/main` endpoint returns ruleset-sourced rules only), so a re-added copy surfaces as `release-alarm.yml`'s UNALLOCATED leg at the next daily cron — ~24h latency, not a red PR. Floors: a missing `allocate-version.yml` throws rather than passing on an empty subject, and zero pushing steps, zero mint steps or zero checkouts red | [`scripts/ci/assert-allocate-token-path.mjs`](scripts/ci/assert-allocate-token-path.mjs) in [`ci.yml`](.github/workflows/ci.yml)'s always-on block (a `.github/workflows/**`-only diff is classified docs-only, so a heavy-block gate would be skipped by exactly the PR that breaks this); regression: [`scripts/ci/assert-allocate-token-path.test.mjs`](scripts/ci/assert-allocate-token-path.test.mjs), which mutates the REAL workflow — fifteen break-on-purpose edits plus the positive case, because a checker that has never failed is a checker nobody knows works |
| The version-allocating commit cannot re-trigger a publish loop, and two PRs merging close together cannot double-allocate a number. The loop guard is structural: relevance is measured from the newest commit that MOVED a package's version, and the bump commit is one, so its own diff sits inside the release it created and the entry it consumed is gone. Two independent guards sit on top — a `CHANGELOG.md` is not a publish-relevant path, and `allocate-version.yml` skips a push whose head commit carries `[release-bump]` — and none of the three is load-bearing alone. Double allocation is prevented by making `main`'s TIP the unit of allocation (never the pushed range) plus a `concurrency` group that serialises the workflow and a retry that RECOMPUTES rather than re-pushing a stale write: two merges inside one window get ONE number carrying both entries, which is the truth rather than a lost release. `release.yml`'s per-ref concurrency already serialises the publish half. The allocator refuses to run in a shallow clone, because a truncated version walk silently mis-scopes which packages are owed a release | [`scripts/ci/allocate-release-versions.mjs`](scripts/ci/allocate-release-versions.mjs) + [`.github/workflows/allocate-version.yml`](.github/workflows/allocate-version.yml); asserted over real git history (apply a plan, commit it, re-plan; two merges in one window) in [`scripts/ci/allocate-release-versions.test.mjs`](scripts/ci/allocate-release-versions.test.mjs), run in [`ci.yml`](.github/workflows/ci.yml)'s always-on block and again in `allocate-version.yml` before it writes |
| F-1520 — `release.yml` dispatches `allocate-version.yml` once, right after a publish, closing the window nothing else covers: the merge commit's own `allocate-version.yml` run plans before the version is on the registry, and the bump commit's own run is skipped by its `[release-bump]` guard, so the registry-aware re-pin (`planExampleRepins`, the row above) had no run left to fire from. The event is a `repository_dispatch` (`event_type: repin-examples`, an explicit `types:` list rather than a bare trigger that would accept every event anyone POSTs at this repo as a licence to push to `main`) sent with the `pome-ops-push` app token, and that choice is the difference between working and 403ing on every release: `POST /repos/{owner}/{repo}/dispatches` is a `contents: write` endpoint and the installation holds exactly `contents: write` + `metadata: read`, while `gh workflow run`'s `POST /repos/…/actions/workflows/{id}/dispatches` needs `actions: write`, which it does not have — that version would have left `release.yml` red after every publish IN ADDITION to the drift, i.e. strictly worse than the deadlock it fixes, until an org owner re-approved the app. (The ambient `GITHUB_TOKEN` would work — dispatch events are the documented exception to `GITHUB_TOKEN` event suppression, unlike a push — and is refused anyway rather than granting the publishing workflow's ambient token write scope to save minting a token this repo already mints.) The job `needs: [publish, publish-wire]` ONLY — never `plan`/`plan-wire` — so a plan-side failure that already skipped its own publish job cannot also take this one down through a dependency it was never given, and nothing downstream consumes its outcome, so its failure can never un-publish, retry, or block either lane. Its `if:` is an OR over `.result == 'success'` on both publish jobs (never their per-package outputs), which is what makes a partly-skipped matrix (some packages had nothing to publish) fire correctly while "nothing published at all" (both skipped) does not — and it is prefixed with `!cancelled()`, which is the difference between this job existing and being decoration: with NO status-check function in an `if:`, GitHub additionally requires every job in `needs:` to have SUCCEEDED, and one of the two publish lanes is skipped on nearly every release (each fires only when its own registry's version moved), so the OR would never have been reached — the job drops for depending on a skipped lane and reads as an innocent "skipped" while the re-pin never happens. The widening it buys is deliberate: `publish` failing no longer suppresses the dispatch when `publish-wire` succeeded, and since the matrix does not fail fast, a run where the adapter published and the CLI failed HAS put a version on the registry that `examples/*` is drifted from. TERMINATION DOES NOT REST ON `[release-bump]`, and cannot: a `repository_dispatch` payload has no `head_commit`, so that guard cannot read the marker on this event — and the tip being dispatched from always carries it. The guard is scoped to `push` explicitly so the dispatch arm always runs and a later reader cannot "fix" the null by widening it into a skip, which would make the dispatch a silent no-op. What terminates the chain is that a dispatch costs a publish: the single commit the dispatched run pushes is either re-pin-only (`examples/*` manifests and their lockfiles, no version — so the `plan` of the run it triggers finds nothing to publish and nothing dispatches) or it moved a version by consuming a pending `## Unreleased` entry, and CI writes none, so iterations are bounded by what a human merged. The push arm's own marker skip still holds independently and neither mechanism is load-bearing alone. THE OTHER HALF IS THE PROPAGATION RACE, without which the dispatch fires and achieves nothing: `planExampleRepins` re-pins only to a version the registry already SERVES, and an `E404` is deliberately indistinguishable from "never published", so a read that beats npm propagation makes the dispatched run a no-op — and there is no later run to retry it, because the next PR's own required `typecheck-test` reds on the same drift. So the publish job proves visibility before this job exists: it polls `npm view <pkg>@<version> --prefer-online` (staleness forced, as is `defaultNpmView`'s own read and the re-pin's lockfile regen: both workflows restore npm's HTTP cache via `cache: npm` keyed on the root lockfile, which a release commit CHANGES — so the run just before a publish takes the miss and saves a cache whose packument predates it, and the dispatched run restores that and reads E404 for a version that exists) for eighteen attempts over roughly three minutes (sized for the CDN's packument cache, exiting on the first attempt in the ordinary case), and reds the already-successful publish rather than handing a green run to a dispatch that cannot work | [`.github/workflows/release.yml`](.github/workflows/release.yml)'s `dispatch-allocate-version` job and its `publish` visibility wait, [`.github/workflows/allocate-version.yml`](.github/workflows/allocate-version.yml)'s `repository_dispatch` trigger and push-scoped guard; regression: the `release.yml wiring` block in [`scripts/ci/decide-publish.test.mjs`](scripts/ci/decide-publish.test.mjs), which cross-checks the sent `event_type` against the accepted `types:` (a mismatch is a 204 that starts nothing), refuses `gh workflow run`, and reds on a `[release-bump]` guard that is not push-scoped |
| A release that does not deliver reaches a human within a day. `release.yml` has no failure path of its own and deliberately gets none: on 2026-08-06 four consecutive push runs failed over eleven hours and nothing fired, and an `if: failure()` step could not have seen the other silence — a release that never triggers at all (F-1180) takes an embedded check down with it. So the alarm is a separate file on its own daily cron, in THIS repo rather than pome-cloud: the shared-fate causes (Actions disabled, billing, org) are org-scoped and pome-cloud is the same org, `issues: write` is per-repo so filing from pome-cloud would need a cross-repo PAT to reach the repo where the fix happens, and `@pome-sh/wire`'s GitHub Packages version is only readable with an in-repo `packages: read`. It is primarily an OUTCOME check — does each registry serve the version `main` declares — because that question is blind to *how* a publish went missing; the run-state legs (`NEVER_RAN`, `STUCK`, `FAILED`) cover a broken release path when no version was owed. F-1511 adds the leg the outcome check structurally cannot see now that the number is written after the merge: a broken or unconfigured `allocate-version.yml` leaves `main` carrying the words and no number, with `main` and the registry in perfect agreement about the OLD version and every other leg green. A pending `## Unreleased` entry is transient by construction (the allocator consumes it on the push that created it), so one still there past the grace window is that silence, reported as `UNALLOCATED`. Its residual is stated in the function's own comment rather than implied away: the publish-RELEVANCE half of what earns a release is not re-derived here, because a watcher that re-derived it would either share the allocator's table and go blind with it or keep a second copy and drift from it — the PR gate is what stops that state being reachable, and a dead allocator is dead for every package with an entry, so the leg still fires. The watched package list is PARSED from `release.yml`'s own `decide-publish.sh` calls, never typed twice, and a `release.yml` the parser cannot follow is a hard failure — an alarm watching zero packages passes forever. Green is asserted as hard as red: silence on green, and the tracking issue closes itself on recovery | [`scripts/ci/release-alarm.mjs`](scripts/ci/release-alarm.mjs) via [`.github/workflows/release-alarm.yml`](.github/workflows/release-alarm.yml) (daily `schedule` + `workflow_dispatch`); dead-guard + regression in [`ci.yml`](.github/workflows/ci.yml)'s always-on block: `release-alarm.mjs --targets` and [`scripts/ci/release-alarm.test.mjs`](scripts/ci/release-alarm.test.mjs) |
| Every workflow with a `schedule:` trigger files or updates a tracking issue when its scheduled run fails, and closes it on recovery — a scheduled run that fails silently reads exactly like one that passed. `repo-policy.yml` and `secret-scan.yml` each reach the alarm from a `failure()`-guarded job in the SAME file, not a separate cron: unlike `release.yml` (push-triggered, and F-1180's four-consecutive-silent-failures happened because it never ran at all), these two workflows' own weekly cron demonstrably fires — `repo-policy.yml` has been red every Monday since it shipped, which is how a human noticed by hand — so the gap is "fired, failed, told nobody," which an embedded job sees for free in the same run, with the real step-level error text in its own env. Both alarm jobs are gated on `schedule`/`workflow_dispatch` specifically and NOT on `!= 'pull_request'`: `secret-scan.yml` also triggers on push to main, and a push run scans only `before..sha` while the full-history TruffleHog leg is itself schedule-only — so a push run neither reproduces nor disproves what the cron found, and a recovery job firing on push would CLOSE a real secret-in-history finding minutes after it was filed. A recovery path must be gated to the events that actually re-run the check that failed, or the alarm flaps weekly and teaches people to skim the label. The theoretical "cron itself goes silent" risk (GitHub disables scheduled workflows after 60 days with no commits) is the same risk `release-alarm.yml` already carries and accepts for itself; a future edit that deletes a workflow's `schedule:` or its alarm job is a code-review-time regression, which F-1471 (the row below) catches, not a runtime watchdog. `release-alarm.yml` is itself schedule-triggered and calls the same reusable alarm on its OWN mechanism failing before it reaches a verdict (checkout/regression-suite/dead-guard breaking) — a fact distinct from "the release is not delivering," which it already reports separately. That gate tests its `check` step's `outcome` for **not being one of the two verdicts** (`success`/`failure`), never for being empty: a step carrying an explicit `if:` still inherits an implicit `success()`, so a preceding step's failure SKIPS it and records `'skipped'`, and an `== ''` test — which only catches the rarer "job died before the step entered the context at all" — would have left the meta-alarm unfireable in precisely the case it was written for. Recovery is the exact negation of that gate and deliberately does not also require a green job: the tracked fact is "did the mechanism reach a verdict", so requiring green would strand the issue open for the whole duration of any genuine release outage, which is when the run reds on purpose. One label and one long-lived issue per alarm, reused across consecutive failures rather than refiled, body naming what failed plus the run URL — the pattern `check-release-staleness.yml` proved (#300) before its deletion. Filing is loud, not best-effort: every `gh` call the alarm needs is unguarded and the script exits non-zero if one fails, and there is deliberately no un-labelled `gh issue create` fallback — the label is the only key the reuse lookup has, so an issue filed without it is invisible to every later run and would silently turn "one long-lived issue" into "a new issue per run". The reusable workflow likewise carries no `if:` on its `outcome` input, because a gate there would skip the job and render a green check for an alarm that is dead. The set of scheduled workflows is DERIVED from `.github/workflows/*.yml`, never hand-listed. Its floor is NOT the bare non-zero count — the four workflows scheduled today satisfy that forever, which makes it blind to a parser that stops recognising a fourth — but set EQUALITY between two independent reads (`on: schedule:` and `cron:`), plus an assertion that no workflow calls a `uses: ./.github/workflows/…` file that is absent, since that is a workflow GitHub refuses to run and so an alarm that passes review and is dead in production. Both reads must agree on all three of the YAML shapes above, which is the assertion the fixtures originally left out: the `cron:` read was anchored at line start, so it missed `schedule: [{cron: …}]` and two of the three shapes the `on:` read was fixed to handle would have failed the cross-check — a correctly-alarmed workflow reddening the derivation. A flow `on:` mapping that SPANS lines is a hard failure rather than a parse attempt: neither read sees it, and two blind reads AGREE, so the cross-check has nothing to disagree about and an unalarmed cron passes everything (actionlint accepts the shape too). An unsupported shape must never be indistinguishable from an absent trigger | [`.github/workflows/schedule-alarm.yml`](.github/workflows/schedule-alarm.yml) (reusable `workflow_call`) plus [`scripts/ci/file-schedule-alarm.sh`](scripts/ci/file-schedule-alarm.sh); called from `repo-policy.yml`, `secret-scan.yml`, `smoke-examples-live.yml` and `release-alarm.yml`'s `schedule-alarm`/`schedule-alarm-recovery`/`meta-alarm`/`meta-alarm-recovery` jobs; derivation + non-zero floor: [`scripts/ci/list-scheduled-workflows.mjs`](scripts/ci/list-scheduled-workflows.mjs) in [`ci.yml`](.github/workflows/ci.yml)'s always-on block; regression, including the dry-run reuse-vs-duplicate assertion: [`scripts/ci/list-scheduled-workflows.test.mjs`](scripts/ci/list-scheduled-workflows.test.mjs) and [`scripts/ci/file-schedule-alarm.test.sh`](scripts/ci/file-schedule-alarm.test.sh) |
| A workflow that gains a `schedule:` trigger with no failure alarm reds CI, naming the file — the row above wires today's three; this asserts it stays true as a PROPERTY, not a fact about those three. Reads `list-scheduled-workflows.mjs`'s own derivation (never re-parses the tree by separate rules), then for each scheduled workflow parses PER JOB BLOCK, anchored on indentation at both ends, for one whose `uses:` resolves (by path, not string equality) to `schedule-alarm.yml` and whose `with: outcome:` is the literal `failure` — a recovery-only workflow with no failure leg still reds, and a call whose `if:` is a block scalar (`if: >-`, release-alarm.yml's real meta-alarm shape) is still read by joining its indented continuation lines, so a "not spelled `failure()`" guard still counts as long as `outcome: failure` is what is actually passed. Three ways a textually-present call is dead were closed rather than left for a runtime watchdog to never see: job-level `continue-on-error: true` (swallows the reusable call's own failure so a broken alarm still reports green), a literal `if: false`, and an alarm job whose `needs:` does not reach every other job in its own workflow — `failure()` is scoped to the alarm job's own dependency graph, not to the run, so a sibling it does not depend on can fail while the alarm is SKIPPED and files nothing, which is what copy-pasting `repo-policy.yml`'s `needs: assert` into a workflow that later grows a second job produces. That closure is TRANSITIVE (`alarm` needs `b` needs `a` counts) because requiring every work job to be named directly would red correct work. A step-level `if:` or a commented-out job cannot be mistaken for job-level state, because comments are stripped before parsing (shared with the row above) and job-level keys are read only at the job's own minimum indent. The shared stripper is quote-aware and requires start-of-line or preceding whitespace, because YAML's comment rule is: a blanket `#.*$` strip truncated `label: "…:x#1"` and `label: "…:x#2"` to the same mangled prefix, so the bijection below compared two equal wrecks and reported green on exactly the typo it exists to catch. Both spellings of `with:` are read — block form and flow form (`with: {title: …, outcome: failure}`), which GitHub and actionlint both accept and an earlier revision reported as BOTH uncovered and missing its inputs; a guard that reds on right answers is a guard someone deletes. Also closes the other half of "wired but broken, still uncaught": the `title`/`label` pair is typed twice per alarm — once on the failure leg, once on recovery — with nothing gating that they agree, so a typo'd label files under one key and the recovery job looks up another, opening a fresh issue every run instead of ever closing one. Asserted as a bijection derived from the calls themselves (any title mapped to more than one label, or label mapped to more than one title, anywhere in the tree), never a restated pairing. A bijection alone is satisfied by an identical pair copy-pasted into a SECOND workflow — consistent and wrong, because the label is the only key the lookup has, so two workflows then share one issue and B's recovery leg closes the alarm A is still failing on; a key used by more than one file reds too. And because "files when it fails" and "closes on recovery" are one property rather than two, a label with a failure leg and no recovery sibling reds — its issue would stay open past the fix and stop being read. Both neutralisers are tested through their expression form as well as their literal one (`${{ false }}`, `continue-on-error: ${{ true }}`), since the expression spelling is what someone reaches for when silencing a noisy alarm because it reads as configuration rather than deletion. Inherits the row above's zero-workflows, set-equality and absent-`uses:`-target floors by calling `list-scheduled-workflows.mjs`'s own **entry point**, not just its derivation helper — re-implementing only the zero check locally left set-equality claimed but not held, so retiring that separate `ci.yml` step would have quietly taken it along. F-1493: a textually-present, correctly-neutralised, correctly-paired alarm call can still be dead at filing time over PERMISSIONS, and the property has two halves because the caller's grant is a CEILING rather than the effective set — a reusable workflow's own `permissions:` can only NARROW what its caller handed it. Half one, the caller: for every call this check resolves the EFFECTIVE grant exactly the way GitHub does — the calling job's own `permissions:` block if it has one (which REPLACES, not merges with, the workflow-level block), else the workflow-level block, else the repository default, which this script cannot read from the filesystem and therefore treats as a hard failure naming the workflow and job, not a silent pass, since an unstated grant is the same silent-degradation shape as everything else in this row. Half two, the callee: `schedule-alarm.yml`'s own filing job (the one running `file-schedule-alarm.sh`, found rather than named) must itself resolve `issues: write` — deleting it there 403s every alarm in the tree at `gh issue create` while every caller stays perfectly compliant, so a caller-only check reports green on the same defect it exists to catch. The scope set callers are held to is DERIVED from that job, never typed here: requiring only `issues: write` would have accepted the minimal `permissions:\n  issues: write` this check's own error message invites, which withholds the `contents: read` the filing job's `actions/checkout` needs and GitHub refuses the CALL outright for. Derivation alone would be self-defeating (deleting `issues: write` from the callee would simply lower the bar for everyone and pass), so the callee carries an absolute `issues: write` floor on top of it, and a `write-all` shorthand there is refused rather than expanded — a check that cannot enumerate its own requirement must not report a pass. Handles every `permissions:` shape GitHub accepts — a map (quoted keys included, since this repo's yamllint truthy workaround produces `"on":` and the same hand writes `"issues":`, and reading a quoted JOB-level block as absent silently fell back to the permissive workflow-level one), the shorthand scalars `read-all`/`write-all`, and the explicit empty mapping `permissions: {}` (which grants nothing, distinct from "unset" but the same failure here); a flow mapping that SPANS lines is REFUSED by name, because stripping a closing brace that is not there yielded an empty map and red a workflow that really did grant the scope. The summary line prints the resolved-caller count and the derived scope set, but that count is not an independent denominator — it is derived from the same parse, so what actually holds the floor is the coverage assertion above: every scheduled workflow must appear among the calls with a live failure leg, so the count cannot fall without coverage reddening first. What actionlint (the row below) covers instead of this check: a `needs:` naming a job that does not exist, and a `uses:` missing its leading `./` | [`scripts/ci/assert-schedule-alarm-coverage.mjs`](scripts/ci/assert-schedule-alarm-coverage.mjs) in [`ci.yml`](.github/workflows/ci.yml)'s always-on block; regression, including both coverage directions, the continue-on-error/neutralising-if/typo'd-path/title-label-mismatch/commented-out/recovery-only break-on-purpose cases, and the permission-guard cases (missing/`read-all`/`{}`/absent-at-both-levels/callee-lost-`issues: write`/caller-withheld-`contents: read`/quoted-job-level-block/multi-line-flow-mapping negative, job-level-map/workflow-level-map/`write-all`/quoted-keys positive, and job-level-replaces-workflow-level): [`scripts/ci/assert-schedule-alarm-coverage.test.mjs`](scripts/ci/assert-schedule-alarm-coverage.test.mjs) |
| `.github/workflows/*.yml` is actionlint-clean — a malformed workflow (bad expression syntax, an undefined `needs.<job>` reference, a broken embedded shell script) reached `main` unchecked before this line, which is how a wired-but-broken alarm job could ship looking correct in review. actionlint also runs shellcheck over every `run:` block when shellcheck is on PATH (pre-installed on `ubuntu-latest`), catching the embedded-shell half the two rows above's YAML-shape parsing does not. Binary fetched from a pinned release and sha256-verified, same pattern as `secret-scan.yml`'s gitleaks install, rather than trusting a third-party Action | `Install actionlint` + `actionlint -color` steps in [`ci.yml`](.github/workflows/ci.yml)'s always-on block |
| Every third-party network call `.github/workflows/**` makes on the publish path goes through one hardened path, and a new one that does not reds CI. Four shapes, all read out of the workflow tree rather than from a list of the calls someone remembered to harden — that list is the same shape as the bug, staying green while a sixth call lands bare. **(a)** a `run:` block fetching a non-loopback URL must call [`scripts/ci/fetch-pinned-release.sh`](scripts/ci/fetch-pinned-release.sh) (retry + backoff + an UNCONDITIONAL sha256 check + a fail-closed message naming the CDN); a target the resolver cannot work out counts as remote, which is the fail-safe direction. **(b)** a `uses:` action that installs a binary must appear as a repeated-attempt group — ≥3 attempts, every earlier one `continue-on-error` and gated on `steps.<earlier>.outcome == 'failure'`, the LAST one with no escape hatch, and every copy carrying byte-identical `uses:` and `with:`, which is the check that keeps `twin-image.yml`'s three cosign installs on the same `cosign-release: 'v2.6.4'`. The SET of actions to judge is derived; the per-action verdict (does this action download an executable? unreadable from this repo) is a reviewed row in [`cdn-fetch-actions.json`](scripts/ci/cdn-fetch-actions.json), and an action with no row reds. **(c)** a `run:` block that WRITES to a registry (`docker push`, `docker manifest push`, `docker buildx imagetools create`) must call [`scripts/ci/push-scanned-image.sh`](scripts/ci/push-scanned-image.sh) — five attempts per tag, and a tag counts as published only once `imagetools inspect` reads its manifest BACK, because `docker push` exiting 0 is not the same fact as "this tag resolves" and the fault this closes is a registry that accepts every layer and commits no manifest. Registry READS are deliberately not in shape (c): a read that fails publishes nothing. Exhausting a tag's attempts exits immediately rather than pushing on, which is what keeps the per-commit `<twin>-<sha>` tag — emitted LAST by `docker/metadata-action`, and the one pome-cloud resolves — absent instead of half-published: `resolve-image-digest.ts` then reports `not found`, and whatever DID land is named in the error because the sign/attest step never runs after this failure, so those tags are published and unsigned. **(d)** a `run:` block that invokes `cosign` must call [`sign-image-digests.sh`](scripts/ci/sign-image-digests.sh) — the same five-attempt, 5s/10s/15s/20s ladder as (c), deliberately one budget for the whole publish path rather than three, but applied per OPERATION rather than around the per-tag body, because re-signing a digest whose VERIFY could not read it is wasted work, a second signature layer over a good one, and the loss of the one thing the failure message must say. READS are in scope here where (c) exempts them, and that inversion is the whole point: a failed read during the push publishes nothing, while this step runs only AFTER the push published every tag. Measured, not imagined — on 2026-08-18 a degraded GHCR took out three of five legs of run 32144441622 in two different steps: gmail and linear in the push (`error parsing HTTP 403 response body`, nothing published), and stripe HERE, on `verify-attestation`'s read with `DENIED: denied`, after both tags were pushed, signed and attested. So exhaustion names the operation that ran out, the ref, the registry, the attempts spent, that the tags are ALREADY PUBLISHED, the exact state that ref is in (published-and-unsigned is a different re-run decision from published-signed-attested-but-unverified) and which refs ARE fully verified; without that, the run reads as a broken twin. Two residuals stated rather than hidden: a call smuggled through a tool this scanner does not know (`crane`, `skopeo`, a Makefile) is invisible; and shape (d) matches cosign in COMMAND POSITION only — segment start behind any prefix a command can hide behind, or inside a command substitution — because unlike `docker push`, `cosign` is also an English word here, appearing in the three `echo` annotations of `twin-image.yml`'s own install ladder, so the `[\s(`$]` prefix rule shapes (a) and (c) use reds the tree on its own prose (it did, on this gate's first run); the cost is that `bash -c "cosign …"` and a call assembled in a variable are invisible | [`scripts/ci/assert-hardened-cdn-fetches.mjs`](scripts/ci/assert-hardened-cdn-fetches.mjs) + [its regression suite](scripts/ci/assert-hardened-cdn-fetches.test.mjs) in [`ci.yml`](.github/workflows/ci.yml)'s always-on block — always-on because a `.github/workflows/**`-only diff is classified docs-only by that job's scope step, i.e. skipped by exactly the PR that would add an unhardened call. Every rule is exercised against a PLANTED violation in a scratch workflow tree, and all three helpers against a fault injected on demand (a local server that 503s; a fake `docker` that reproduces GHCR's `unknown blob`; a fake `cosign` that reproduces its `DENIED: denied`, injectable per subcommand so the after-publication case is a fixture). Both fail-closed messages are asserted through the clause that lists what LANDED, never through a bare `stderr.includes(tag)` — the rolling `…:stripe` tag is a substring of the per-commit `…:stripe-bbf27bf` one, so the weaker form is satisfied by the FAILING tag being named and stayed green with the landed list deleted outright. Five vacuous-green dead guards: zero `uses:` actions, zero classified CDN actions, zero call sites for any of the three helpers |
| `@pome-sh/checks` is installable with NO `@pome-sh/*` dependency (the sdk and the five twins are `private: true` and on no registry at these versions, so a leaked runtime dep 404s on the consumer); `zod` stays a `peerDependency` and is never inlined (two zod copies = two schema identities, F-942, in a brand-new package); the twin ENGINE is never inlined (`twin-stripe`'s seed reaches one zod schema that `@pome-sh/sdk/server` also re-exports — that barrel drags hono, `hono/jwt` and `node:sqlite`, which is why the narrow `@pome-sh/sdk/failure-injection` subpath exists); and `defineCheck` plus the vacuity sentinels exist EXACTLY once across seven entries, since without `splitting: true` each entry would carry its own copy and `@pome-sh/checks/github`'s `defineCheck` would not be `@pome-sh/checks/stripe`'s. There is deliberately no drift gate: `packages/checks/src/*.ts` is `export … from` lines only, so there is no second copy to drift — the twin LIST is the part that can, and `CHECKS_TWIN_NAMES` is compared against `config/first-party-twins.json` | [`scripts/ci/check-checks-tarball.mjs`](scripts/ci/check-checks-tarball.mjs) — `npm run gate:checks-tarball` in [`ci.yml`](.github/workflows/ci.yml)'s heavy block and in `release.yml`'s publish job, with the network-free `--manifest-only` half in ci.yml's always-on block; twin-list drift: [`scripts/check-first-party-twin-registration.mjs`](scripts/check-first-party-twin-registration.mjs); surface: `packages/checks/test/surface.test.ts` |
| `@pome-sh/sandbox-domains` is installable with NO `@pome-sh/*` dependency, for the same 404 reason as `@pome-sh/checks` above, and `zod` stays a `peerDependency` for the same F-942 reason — but the rest of its tarball audit is deliberately the INVERSE of that one, and reading it as a copy gets three assertions backwards. `node:sqlite` is REQUIRED and required exactly once (checks treats those bytes as an engine leak; here a runtime bundle whose `open*Database` exports cannot open a database is a second declarations package that fails only on the grader, and two copies means the per-twin entries hand out different DB layers behind different closures — the `splitting: true` invariant, one layer down from checks' `defineCheck` count). `hono` is a declared external, not a forbidden byte: each domain arrives through its twin's package ROOT, which is where `defineTwin()` runs at module scope, and `./server` re-exports `toTwinHttpEventRow` from `@pome-sh/sdk/server`. Two assertions checks does not need, because this package's `dependencies` are load-bearing in BOTH directions: every bare specifier in the SHIPPED bytes (JS and `.d.ts`) must be a declared dependency, and every declared dependency must actually be imported. Those are one knob — `noExternal` forces only `@pome-sh/*` inlining, so tsup decides every other third-party package by whether the manifest declares it (declared ⇒ external ⇒ must stay declared, `hono`; undeclared ⇒ inlined ⇒ must NOT be declared, `graphql`, which twin-linear reaches through its package root and which surfaced here only as tsup's `// ../../node_modules/graphql/…` source comments). Both directions were live: `@hono/node-server` was declared and imported by nothing, `graphql` was declared for a package tsup had inlined. The upstream shape anchors `@octokit/openapi-types` and `stripe` are declared because they reach the shipped `.d.ts` (`GitHubDomain.pullRequestStack()`, `StripeDomain`'s `PaymentIntent[…]` field types) and their specs must EQUAL the twins' own, since the declarations were generated against those versions. F-1526's export spec — the table measured from pome-cloud's own imports — is asserted against the packed modules, not just the manifest | [`scripts/ci/check-sandbox-domains-tarball.mjs`](scripts/ci/check-sandbox-domains-tarball.mjs) — `npm run gate:sandbox-domains-tarball` in [`ci.yml`](.github/workflows/ci.yml)'s heavy block and in `release.yml`'s publish job, with the network-free `--manifest-only` half in ci.yml's always-on block; regression (the gate asserted by BREAKING the manifest nine ways): [`scripts/ci/check-sandbox-domains-tarball.test.mjs`](scripts/ci/check-sandbox-domains-tarball.test.mjs); twin-list drift: [`scripts/check-first-party-twin-registration.mjs`](scripts/check-first-party-twin-registration.mjs) compares `SANDBOX_DOMAIN_NAMES`; surface + a real SQLite open and domain construction: `packages/sandbox-domains/test/surface.test.ts` and the sandbox-domains room in [`scripts/clean-room-pack-test.mjs`](scripts/clean-room-pack-test.mjs) |
| `@pome-sh/wire` is published to GitHub Packages ONLY, never npmjs; its manifest is `private: false` (not merely absent — `npm publish -w` on a `private: true` workspace warns and EXITS 0, so the regression is a green release that published nothing); and every path it declares in `exports`/`main`/`types` physically ships in its tarball, with no dangling `.map` and no hard links. Inside this workspace every consumer resolves wire through npm's workspace symlink to the full source tree, so an export pointing at a file `files` does not ship resolves forever here and dies with `ERR_PACKAGE_PATH_NOT_EXPORTED` only in a cross-repo consumer's repo. Registry selection is doubly pinned — `publishConfig.registry` in the manifest and an explicit `--registry` at publish time — because a mistaken publish to public npm cannot be taken back after 72 hours | [`scripts/ci/check-wire-tarball.mjs`](scripts/ci/check-wire-tarball.mjs) — `npm run gate:wire-tarball` in the `publish-wire` job of [`.github/workflows/release.yml`](.github/workflows/release.yml), and the network-free `--manifest-only` half in [`ci.yml`](.github/workflows/ci.yml)'s always-on block so the two silent-failure modes are caught pre-merge |
| Every tool a bundled example registers is answered by the twin it targets, on EVERY `.seed.json` that example ships — not just the first. The load-bearing assertion is that each probe REACHED a twin: `refused` reads the wire status, and no wire call at all reduces to `status = 0`, which is `< 400`, which read as "the twin did not refuse". That is how `examples/minimal-viktor-langgraph`'s ENTIRE tool table went uninvoked by this gate from the day it shipped — the driver knew `handler`/`execute` and LangChain's `tool()` returns a `StructuredTool` invoked through `.invoke()`, so every probe produced `calls: []` and the gate printed OK. The driver now knows three shapes, but the shape list is not the guard: a probe that makes zero HTTP calls to any twin is a `silent-probe` red naming the tool and every shape the driver tried, so a FOURTH framework's tool shape reds on arrival instead of going quiet. A 4xx/5xx reds as `refused` naming the example, the seed, and the twin's status; a registered tool with no probe reds too; and every finding names its seed, because an example ships up to six. Seeds are found by DISCOVERY (`examples/*/tasks/*.seed.json`), never a hand-kept list, and each probe's `owner`/`repo`/PR/issue/file arguments are read off that seed itself (`$repo.owner`, `$pr.number`, `$pr.last_number` for the seed whose interesting subject is its LAST pull request, `$file.ref` = the file's own branch) rather than hand-written — before F-1163, 13 of the 20 shipped seeds were never probed and a new seed landed uncovered by construction. The one legitimate exemption is DERIVED from the seed, never a map keyed by seed filename: `expect_status_if: "$pr.merge_blocked"` mirrors every 409 path `mergePullRequest` has that a seed can express (a non-open PR, or a `failure`/`error` combined status by the twin's own latest-per-context rule), so a seed that blocks its own merge expects the 409 and its siblings still assert the merge succeeds. Both staleness directions are closed: the twin ceasing to refuse a blocked merge reds as `stale-expect`, and an `expect_status_if` that NO seed the example ships can satisfy — what deleting `03-failing-ci` or flipping its `ci/test` green would leave behind — reds before anything boots, naming the tool and every seed it checked — a filename map goes stale in silence when a seed is renamed or deleted, and a new failing-CI seed would need a hand edit. Two totality checks back the discovery: an example with a `tasks/` dir and zero seeds is a loud failure, and the manifest's example names must equal exactly the on-disk set of examples shipping a seed, reds naming both sides and the directory. `typecheck:examples` is green on a well-typed tool whose endpoint 404s and `smoke:examples` returns before any tool fires, which is how `comment_on_pull_request` 404'd in two examples for their entire existence. Needs no model | [`scripts/probe-example-tools.mjs`](scripts/probe-example-tools.mjs) (`npm run probe:examples`) in [`ci.yml`](.github/workflows/ci.yml)'s heavy block, driver in [`scripts/example-tool-probe-driver.mjs`](scripts/example-tool-probe-driver.mjs), probe templates in [`config/example-tool-probes.json`](config/example-tool-probes.json); regression: [`scripts/probe-example-tools.test.mjs`](scripts/probe-example-tools.test.mjs) — the fourth-shape break-on-purpose is the one that locks the class |
| A bundled example that launches for real (`smoke:examples`, POME_PREFLIGHT unset) must leave evidence it reached real work, and the gate FAILS it otherwise. Before F-1478 any exit inside `SETTLE_MS` (5s) was unconditionally OK — the only thing checked was a TDZ regex over stdout+stderr — so a launch that returned having done nothing passed exactly like a healthy one. F-1478 replaced that with a REACHED-OUTBOUND verdict decided by matching the failure TEXT against `BENIGN_FAILURE_SIGNATURES` (`ECONNREFUSED`, `fetch failed`, a 401/403-shaped string, …) — which held until F-1519 found it was not deterministic: `@anthropic-ai/claude-agent-sdk`'s `query()` picks between two error shapes for the SAME underlying model-call failure depending on an internal race between its own stream-parsing and the child process's `exit` event (`Claude Code returned an error result: … 401 invalid x-api-key` if the race is won, `Claude Code process exited with code N. stderr: …` if it is lost), and only one shape matched the signature list — so the identical commit read `7 of 8 reached + exit 1` on one CI attempt and `8 of 8` on the re-run (run 31711971267). F-1519's fix: classify on POSITIVE EVIDENCE THE EXAMPLE ITSELF EMITS, never on which error-text shape survived a dependency's internal race. `OUTBOUND_MARKER` (`POME_SMOKE_REACHED_OUTBOUND`) is a fixed literal every example prints, synchronously, at the earliest point it is about to make its first outbound (twin or model) call — before either racing error shape can even be constructed. Three examples (`pr-summary-agent`, `pr-summary-review`, `triage-agent`) get this for free from `@pome-sh/adapter-claude-sdk`'s `query()`, which wraps the exact racy `sdkQuery()` call — the shared seam they route through by a `file:` link to the workspace. The other five print the literal themselves, gated on `POME_SMOKE_MARK_OUTBOUND=1` so real users never see it: four (`gmail-retry-notify`, `merge-agent`, `minimal-viktor`, `minimal-viktor-langgraph`) import no `@pome-sh/*` package at all — no shared seam covers a raw Vercel-AI-SDK/LangGraph call — and `support-triage` DEPENDS on the adapter but pins the PUBLISHED tarball on purpose (it stays `npx degit`-fetchable standalone), and a tarball cut before the marker existed prints nothing: measured, its installed `dist/index.js` has zero occurrences of the marker while the three `file:`-linked siblings have one each. So `assertEveryExampleEmitsMarker()` counts the dependency as coverage ONLY when it resolves to the workspace (`file:`/`link:`/a relative path), and otherwise requires the literal inside a `console.error(...)` call in the example's own `src/*.ts` — a mention in a comment is not coverage. It asserts the PROPERTY rather than hand-listing which examples need their own print, so a ninth example with no route to the marker reds by name before anything launches. What a static check cannot see (is the line reachable, is it placed early enough) is fail-closed downstream: an example whose marker never prints FAILS, with the marker named in the reason. Four verdicts follow, still exactly `ok`/`reached`/`fail` — never a fourth state, and REACHED-OUTBOUND now REQUIRES the marker: **FAIL** on a TDZ signature anywhere in the output, unconditionally — whatever the exit code, whatever the timing, even mixed with a benign signature (F-900's subject). **OK** = still alive at the settle (unchanged by F-1519 — this signal was never text-matched). **REACHED-OUTBOUND** = exited non-zero AFTER the marker was observed in the output, and NOT vetoed; `BENIGN_FAILURE_SIGNATURES` still runs but only to decorate the verdict with a human-readable reason, never to decide it — a benign-looking crash with NO marker (a deliberately-injected pre-wiring throw containing `ECONNREFUSED`/`401 invalid x-api-key` verified this) still FAILS. The veto is the marker's one honest limit: it is printed at the outbound call SITE, so anything throwing between that line and the syscall prints it first. Two such classes were measured on real launches — the Agent SDK failing to resolve the `claude` binary (`native binary not found`, no request at all) and a malformed twin URL (`ERR_INVALID_URL`, thrown while parsing) — and both FAILED under the old text classifier, so `PRE_OUTBOUND_VETOES` reds them on their own error CODE (never prose), which stays deterministic because no such code can be produced by a failure after a call went out. A pre-outbound class the list misses keeps the marker verdict; no real outbound failure can be redded by it. Crashes AFTER a successful outbound call are out of scope by design, here as before: this gate's claim is "reached an outbound call", never "the work was correct". **FAIL** also on marker-then-exit-0 against the PR leg's dead wiring (impossible to succeed there, so exit 0 means the failure was swallowed) and on any exit with no marker and no TDZ — no positive evidence of real work, whatever the text looks like. The tail is printed on EVERY verdict, not only failures, so a green run carries its own evidence. `SETTLE_MS` racing the `claude` CLI's cold-start time still shifts examples between OK and REACHED (a dev machine with an authenticated `claude` login can be 4 OK + 4 REACHED where CI is 0 OK + 8 REACHED) — that split is stated in the printed summary as environment-shaped rather than engineered away, because neither bucket's pass/fail verdict depends on it: the marker fires within milliseconds of process start, long before the settle. Entry guard is a realpath compare on both sides, never bare `import.meta.main` (that landed in Node 24.2, root `engines` allows `>=24`, and `undefined` there makes the guard false and the whole gate exit 0 having smoked nothing — F-1353's shape), and a guard miss while invoked as this file THROWS rather than exiting 0. Zero examples discovered is a hard failure | [`scripts/smoke-examples.mjs`](scripts/smoke-examples.mjs) (`npm run smoke:examples`) in [`ci.yml`](.github/workflows/ci.yml)'s heavy block, marker emission in [`packages/adapter-claude-sdk/src/query.ts`](packages/adapter-claude-sdk/src/query.ts) (shared seam, pinned by `packages/adapter-claude-sdk/test/outboundMarker.test.ts` — the three seam-covered examples print nothing themselves, so that behaviour has to red in the package that owns it) and directly in the other five examples' `src/index.ts`; regression: [`scripts/smoke-examples.test.mjs`](scripts/smoke-examples.test.mjs) (always-on block — pure-function tests, no `npm ci` needed), whose break-on-purpose cases are the do-nothing exit 0, the TDZ under every combination including mixed with a benign signature, both of the SDK's racing error shapes converging on the same verdict once the marker is present, a benign-looking crash with no marker still failing, a marker-then-pre-outbound-crash (unresolvable `claude` binary, `ERR_INVALID_URL`, `ERR_MODULE_NOT_FOUND`) still failing, six real broken-example outputs that must NOT be excused as benign, the real CI outputs asserting a benign REASON was recognized and not just a marker-decided status, and the marker-coverage guard reding on an example with no route to `OUTBOUND_MARKER` — including one that depends on the adapter from the REGISTRY and one that only mentions the literal in a comment |
| F-1486 — `REACHED-OUTBOUND` (the row above) is the only verdict any environment has ever proven: measured across #386, 0 of 8 examples are ever OK in CI, while 4 of 8 are locally, so the gate's strongest signal (alive at the settle) was structurally unreachable in the only environment that gates anything. A nightly + `workflow_dispatch` leg runs the SAME gate — same `classifyLaunch()`, same discovery, same output-tail printing — with real local twins (booted for real via `pome twin start`, the same in-process boot path `quickstart-smoke.yml` already proves for github/gmail) and a real `ANTHROPIC_API_KEY` repo secret, so OK is achievable. `SMOKE_EXAMPLES_LIVE=1` is the switch: it stops overlaying `SMOKE_DEAD_WIRING`'s dead loopback ports and invalid keys (so real twin wiring and the real key flow through unmodified) and asserts a floor after the run — **at least one example alive at the settle**, zero-alive is a hard failure naming what was expected. The floor is a literal `>= 1`, deliberately NOT a fraction or count of the discovered example set: comparing the floor against `total` would be the milestone's own "two quantities that move together" shape, satisfied forever by construction. The switch is STRICT — `SMOKE_EXAMPLES_LIVE=true`/`yes`/`1 ` is a hard error, never "not live", because treating an unrecognised value as the PR leg is a one-character typo away from a green nightly that overlays dead ports, skips the credential check, skips the floor and exits 0. Dropping the overlay is only half the job: the leg must still supply what the overlay was quietly providing that is NOT a fake credential, and `SMOKE_LIVE_DEFAULTS` is that set — measured by running the leg's exact env shape, each of its two entries alone reds the nightly on its FIRST run for a reason unrelated to the property it proves. `POME_TASK` (four of the eight `requiredEnv()` it; without it they die on `Error: POME_TASK is required`, classified FAIL as "returned without evidence it did any real work"), and `VIKTOR_MODEL` pinned to an `anthropic/*` slug, because `minimal-viktor` defaults to `alibaba/qwen-3-32b`, which its `resolveModel()` can reach ONLY through the AI Gateway and which throws `needs AI_GATEWAY_API_KEY` before any outbound call — the PR leg papers over that with a FAKE `AI_GATEWAY_API_KEY`, and the credentialed leg must not, because a fake gateway key routes all eight into a gateway that rejects them and the real `ANTHROPIC_API_KEY` is then never exercised at all. Both are DEFAULTS: a caller-supplied value wins, and a blank one counts as unset so `requiredEnv`'s own trim never receives `""`. That pinning is what keeps the human provisioning list at exactly ONE secret. An absent credential fails BEFORE any example launches — `missingLiveEnv()` checks `ANTHROPIC_API_KEY` and `POME_AUTH_TOKEN`, and the workflow's own upfront step checks the secret before paying for `npm ci` or booting any twin — because a credentialed leg that silently fell back to dead loopback ports on a missing secret would report REACHED-OUTBOUND and pass, proving nothing while looking like it proves something, which is this ticket's own subject. Presence is checked TRIMMED on both sides: GitHub Actions substitutes an unset secret as the EMPTY STRING (falsy, caught either way), but a secret whose stored value is a pasted space or newline is truthy-but-blank — the F-1187/F-1184 blank-in-Infisical shape — and untrimmed it sails through to be reported as eight broken examples instead of one blank credential. Of the two names, only `ANTHROPIC_API_KEY` is human-provisioned: `POME_AUTH_TOKEN` is MINTED per run by `pome twin start` and written to `$GITHUB_ENV`, which is why the absent-credential demo names two vars while the provisioning list names one. Every twin wiring value is read out of the twin's own printed boot line through a `grab()` helper that reds naming the var and the log, never `echo "VAR=$(grep …)"` — a failing command substitution does NOT trip `set -e`, so the plain form writes `VAR=` and hands every example blank wiring, and only the two names in `LIVE_REQUIRED_ENV` are re-checked downstream. Known ceiling: `OK` is defined as "still alive at the settle", so the floor is satisfied by a process that merely breathed for 5s — a real-credential run blocked on a slow host or retrying a rate-limited call counts, and conversely a genuinely CORRECT but FAST example that exits 0 inside the settle lands on FAIL. The verdict is accepted for now (distinguishing "exited 0 having produced work output" needs a work marker no example emits in a common form across three frameworks) but the FAIL message names that possibility explicitly ON THE LIVE LEG ONLY, so the first real red is diagnosable rather than training its reader to ignore the alarm. The PR leg (`ci.yml`) never sets `SMOKE_EXAMPLES_LIVE`, so its behavior — `REACHED-OUTBOUND` × up to 8, uncredentialed, a pass — is unchanged | [`scripts/smoke-examples.mjs`](scripts/smoke-examples.mjs)'s `resolveLiveFlag`/`launchEnv`/`missingLiveEnv`/`assertAliveFloor`, invoked by [`.github/workflows/smoke-examples-live.yml`](.github/workflows/smoke-examples-live.yml) (`schedule:` + `workflow_dispatch`, with `schedule-alarm`/`schedule-alarm-recovery` jobs per F-1230/F-1471); regression: [`scripts/smoke-examples.test.mjs`](scripts/smoke-examples.test.mjs) — the break-on-purpose cases are the floor at 0 alive, the floor being a no-op off the live leg, an empty-string and a whitespace-only secret, a partial env, every unrecognised flag value, and `launchEnv` on both legs (the live leg supplies a task and an `anthropic/*` slug and leaks NO `59321` loopback port; the PR leg still overlays the dead port, the invalid key and the fake gateway key) |
| Every endpoint a twin DECLARES is called and answered. The endpoint set is read from each twin's own `tools/list`, never from the manifest, so a twin that gains a tool gains a probe with no hand edit and a declared tool no probe supplies arguments for is a red naming the twin and the endpoint; a refusal names the tool, the status, and the twin's own error text. The row above probes what seven bundled EXAMPLES expose, which reached 9 of the 137 declared tools and none of stripe's; slack's 8 and linear's 15 uncalled tools were reached by nothing over the MCP wire, because their own suites drive them through `executeTool()` on the domain — the layer `comment_on_pull_request` was NOT broken at. The two gates have different subjects and neither replaces the other: an example red points at `examples/`, a twin red at `packages/twin-*/`. Status is read from the twin's recorded event, not the HTTP response, because MCP JSON-RPC answers 200 for a tool that failed. Needs no model, no API key, and no socket — every twin boots in-process on `:memory:` against its own default seed. The delta this closed is measured in [`docs/declared-endpoint-coverage.md`](docs/declared-endpoint-coverage.md). Entry guard is a realpath compare on both sides, never bare `import.meta.main` (F-1481, same shape as the smoke-examples and contract-runner rows above); a manifest declaring zero twins, or twins declaring zero probes in total, throws rather than reporting a pass having probed nothing | [`scripts/probe-twin-endpoints.mjs`](scripts/probe-twin-endpoints.mjs) (`npm run probe:twins`) in [`.github/workflows/ci.yml`](.github/workflows/ci.yml), fixture arguments in [`config/twin-endpoint-probes.json`](config/twin-endpoint-probes.json); regression: [`scripts/probe-twin-endpoints.test.mjs`](scripts/probe-twin-endpoints.test.mjs) |
| No entry guard anywhere the repo ships one uses bare `import.meta.main` — that property is `undefined` on Node 24.0.0/24.0.1/24.0.2/24.1.0 (it landed in 24.2), root `engines` allows `>=24`, and a guard built on it exits 0 having run nothing, the shape F-1353 fixed once by hand in `contract/run.mjs` and F-1481 generalizes (superseding the single-file check that lived in `capture-mcp-tools-list.test.mjs`, which now only pins that ONE producer's guard is the sanctioned form — a positive assertion the repo-wide gate does not make). Scope is the load-bearing part: the first cut of this gate watched `scripts/**` and `contract/**` only, and SIX bundled examples were live instances at that moment (`minimal-viktor`, `merge-agent`, `gmail-retry-notify`, `pr-summary-agent`, `pr-summary-review`, `triage-agent` — all `examples/*/src/index.ts`, all `engines: >=24`), so the gate would have reported the class closed while `npm start` in any of them printed nothing and exited 0. It now walks `scripts/`, `contract/`, `cli/src/`, `cli/scripts/`, `packages/` and `examples/` — `no-eval-in-oss.mjs`'s root list plus the two that hid the instances — across `.mjs`/`.js`/`.cjs`/`.ts`/`.mts`/`.cts`/`.tsx`, and a root that contributes zero files is a hard failure PER ROOT, so relocating one reds instead of silently dropping out of coverage. The parser is `typescript`, not a JS-only one, precisely because the live instances were `.ts`: a gate that cannot parse its own subject is a skip that reads as a pass. It PARSES rather than grepping, so a comment or a string literal naming `import.meta.main` never false-positives — including this gate's own header, the producer's positive-form check, and any generator that WRITES the string into source it emits — while `import.meta?.main`, a line break, parens, `!import.meta.main`, `import.meta["main"]`, a template-literal computed key, a ternary or `&&` chain, and `const { main } = import.meta` (renamed, computed-key, or `...rest`) all red. `import.meta["ma" + "in"]` is a documented non-goal (it needs constant folding and nobody reaches it by accident). Symlinked files and directories are followed, because a dirent's `isFile()` is false for a symlink and skipping one would be the gate's own failure mode; an unparseable file reds as its own category rather than as a phantom guard, since `typescript` recovers from syntax errors instead of throwing. F-1488 extends the same walk with a second AST check over the SANCTIONED replacement itself: comparing `process.argv[1]` against `import.meta.url` has its own silent-skip shape when either side lacks `realpathSync` — Node resolves symlinks before deriving `import.meta.url`, so an unresolved (or only-partly-resolved) argv0 disagrees with it through any symlinked invocation and the guard falls false, the same vacuous-pass shape reached through a different comparison. Ten shipped instances realpath'd NEITHER side (`resolve()`-only, a bare compare with no `resolve()` at all, or a `pathToFileURL(...).href`/`fileURLToPath(...)` compare with no `realpathSync` anywhere) — `no-eval-in-oss.mjs` (the capture-is-open gate itself), `no-catch-and-continue.mjs`, `no-native-modules.mjs`, `capture-mcp-tools-list.mjs`, `check-workspace-pins-match-workspace.mjs`, `check-plugin-manifest-lists-every-skill.mjs`, `ci/release-alarm.mjs`, and `packages/wire/scripts/emit-trace-contract.mjs` — plus two bundled examples' `run-trials.ts` the mechanical scan turned up beyond the ten named in the ticket, same shape, discovered because the gate asserts the PROPERTY rather than a fixed list. `lint-task-class.mjs` and `lint-task-format-doc.mjs` compared a `basename()` instead, its own weaker verdict: satisfied by any file of that name anywhere on disk, no symlink required. `findEntryGuardRealpathGaps` classifies every relation shape (an equality/inequality compare, or `X.endsWith(Y)`) by walking each operand's own parent chain for a wrapping `realpathSync`/`basename` call — never a second directory walk, never a grep — and reports `no-realpath` / `one-sided-realpath` / `basename-comparison` distinctly; a relation with `realpathSync` on both sides and no `basename()` is the sanctioned form and is not a finding. Two properties of that classifier are load-bearing and each was a live vacuous pass in the first cut. **Single-assignment aliases are followed one hop**, because the sanctioned fix itself routes both operands through `const SELF`/`const ENTRY` — an operand-local search sees `ENTRY === SELF`, finds no argv0 and no `import.meta.url`, and classifies NOTHING in any of the twelve files it just fixed, nor in `contract/run.mjs`, `probe-twin-endpoints.mjs` or this gate's own guard; it would have reported the class closed over a repo in which it could see zero of the guards. And **the count of relations classified is itself the floor**: `guardGaps.length === 0` is satisfied equally by "every guard realpaths both sides" and by "the walk recognized no guard at all", so `scanRepo` returns `guardRelations` and the runner hard-fails BELOW A PROPORTIONAL FLOOR of 12 (`MIN_ENTRY_GUARD_RELATIONS`), the same shape as the per-root file-count floor beside it. Proportional, not `> 0`: only ONE guard in the repo (`check-packages-scripts-wired.mjs`) compares the two inline, so a regression breaking only the alias deref leaves the count at 1 and a `> 0` floor would still print a pass — the same vacuous pass one step down. 27 classified today, floor 12 (F-1488's instance count), so a classifier regression reds and deleting a script does not. Both directions of the boundary are pinned by the test, because a gate that reds correct work gets deleted rather than obeyed: `fs.realpathSync`, `realpathSync.native` and `await realpath()` from `fs/promises` are all the same act as the bare identifier and none is a finding, argv0 is recognized as `process.argv[1]` OR `process.argv.at(1)`, and the self-path side covers `import.meta.filename`/`dirname` as well as `.url` (all three derive AFTER symlink resolution, so all three have the bug). Two documented ceilings, both erring toward a false RED on correct work rather than a false green on broken work: a side that realpaths inside a HELPER (`const real = (p) => realpathSync(p)`) is not followed, and argv0 reached by destructuring (`const [, entry] = process.argv`) is not recognized at all — that one is a false GREEN, and the relation floor is what stands behind it | [`scripts/lint-no-bare-import-meta-main.mjs`](scripts/lint-no-bare-import-meta-main.mjs) (`npm run lint:import-meta-main`) in [`.github/workflows/ci.yml`](.github/workflows/ci.yml)'s HEAVY block (it needs `npm ci` for the parser; every root it scans is in the heavy-suite scope regex, so it is not path-filtered away from a diff that could trip it); regression: [`scripts/lint-no-bare-import-meta-main.test.mjs`](scripts/lint-no-bare-import-meta-main.test.mjs) — the assertions that matter are the per-root coverage floor, the `.ts` break-on-purpose, the CI-wiring check that locates the gate's OWN step rather than grepping the whole workflow, and (F-1488) the one-sided-realpath / no-realpath / basename-comparison break-on-purpose matrix over a scratch fixture |
| Every bundled Claude-Agent-SDK example's `query()` sets BOTH `options.tools` and `options.settingSources`. They close DIFFERENT doors and shutting one says nothing about the other: `tools` replaces the SDK's BUILT-IN base set (`Bash`, `Read`, `WebFetch`, …), while `settingSources` governs FILESYSTEM settings — user, project and local — INCLUDING the Claude Code plugin MCP servers configured on whoever's machine the example runs on, all three of which the SDK loads when the option is omitted ("matches CLI defaults"; `[]` is its documented isolation mode). Measured 2026-08-05 (F-1295): a hosted `claude-haiku-4-5` trial of `support-triage-dedup` launched from a developer shell with `tools: []` ALREADY SET called `mcp__plugin_slack_slack__slack_search_channels`/`…__slack_search_public`/`…__slack_list_channel_members` — it searched the developer's real Slack workspace, made zero twin calls, and would have scored as *the agent failed to triage*, a verdict about the wrong workspace entirely; with `settingSources: []` the same trial called only `mcp__github__*`/`mcp__slack__*` and scored 75. Those servers arrive as Claude Code PLUGINS, not from the repo's committed `.mcp.json` and not from `~/.claude.json`, so no repo hygiene reaches them. `allowedTools` is NOT a third latch and does not satisfy this row: it only AUTO-APPROVES, and a one-entry `allowedTools` was measured leaving 152 tools live including `Bash`, `Read`, `Write` and `WebFetch` — which is why `triage-agent`, `pr-summary-agent` and `pr-summary-review` were open-book AND inheriting host MCP servers until F-1295 gave all three `tools: []` too. The subjects are DISCOVERED from `examples/*/package.json` (either dependency section), never a hand-kept list, and the gate PARSES rather than greps because the likeliest regression is deleting the options and keeping the comment that explains them. Presence is the rule, not the literal `[]`: `examples/support-triage` names its policy (`tools: BUILT_IN_TOOLS`) and its own unit test pins the value, so the resolver follows a same-file `const`, an unconditional spread, and a single-`return` helper — `examineeOptions()` is the shape the one example that already got this right uses, and a resolver that only read inline literals would have had to red it or skip it. A CONDITIONAL spread (`...(FLAG ? { settingSources: [] } : {})`) deliberately does not count — a door shut only when an env var is set is open — and an options object the gate cannot resolve is a FINDING, never a pass, because "I could not tell" and "it is isolated" must not print the same way. Two floors against a vacuous pass: zero SDK examples discovered is a hard failure, and so is an example that declares the SDK while the walk finds no `query()` call in it; below `MIN_QUERY_CALL_SITES` (4, one per bundled example) the runner refuses to report a pass at all. **Per-example, and deliberately not the whole answer** — [DECISION] F-1295 records that the central half belongs in `@pome-sh/adapter-claude-sdk`'s `query()`, the one in-process chokepoint every bundled example already routes through and the only one covering all three launchers (`pome run`, the coach's `run_task` local-subprocess spawn, a plain `npm start`); NOT the `pome run` path, whose CLI-side clamp would have missed the measured incident entirely (it was launched from a developer shell) and whose obvious mechanism, `CLAUDE_CONFIG_DIR` at an empty dir, also holds `.credentials.json` and would break the subscription auth FDRS-667 fixed. The per-example options stay regardless: `examples/support-triage` is `npx degit`-fetchable as a standalone subtree, so the options a reader copies out must carry the isolation with them, and no adapter can fix an example that has left the repo | [`scripts/check-example-sdk-isolation.mjs`](scripts/check-example-sdk-isolation.mjs) (`npm run lint:example-isolation`) in [`.github/workflows/ci.yml`](.github/workflows/ci.yml)'s HEAVY block (it needs `npm ci` for the `typescript` parser; `examples/` and `scripts/` are both in the heavy-suite scope regex, so it is not path-filtered away from a diff that could trip it); regression: [`scripts/check-example-sdk-isolation.test.mjs`](scripts/check-example-sdk-isolation.test.mjs) — the assertions that matter are the break-on-purpose matrix (each door missing alone and together, `allowedTools` offered instead of `tools`, the option named only in a comment or a string), the resolver's green shapes (helper call, `const`, unconditional spread, named constants, aliased/namespace/raw-SDK imports), the not-a-subject set (a `query` from `pg`, a locally declared one, a type-only import), manifest-driven discovery over a scratch fixture, and a full run of the real gate against a leaky fixture repo asserting it EXITS 1 |
| Every member of the event union (`otelEventSchema` — the seven `eventSchema` variants plus `OtelSpanEvent`) has at least one wire fixture under `packages/wire/test/fixtures/v1/event/<Kind>/`, and the kind list in `trace-contract.json` is enumerated from the zod union rather than typed out. Adding a kind with no fixture, or renaming one and leaving its directory behind, fails BOTH `emit:trace-contract` and `--check` — regenerating is not an escape hatch. The old script read no schema at all: `canonicalSchemas` was a hardcoded four-string literal and the rest was a directory walk, so a new kind moved zero bytes and the byte-compare was green by construction, which is how M1 shipped `LlmTurnEvent` with no fixture anywhere | [`packages/wire/scripts/emit-trace-contract.mjs`](packages/wire/scripts/emit-trace-contract.mjs) (`npm run check:trace-contract -w @pome-sh/wire`) in [`.github/workflows/ci.yml`](.github/workflows/ci.yml); regression: [`packages/wire/scripts/emit-trace-contract.test.mjs`](packages/wire/scripts/emit-trace-contract.test.mjs). The dropped/renamed half is `typecheck` — `test/export-surface.test.ts` guards the per-kind types, `test/v1-event-corpus.test.ts` keys its coverage map on `OtelEvent["kind"]` |
| No emitter writes a bare `parent_id`. It meant four different things depending on which of five writers produced the row — a spawning `event_id` (`wrapQuery`), a raw SDK `tool_use_id` (`hooks`), a hard null (`turn-usage`, and every twin HTTP row), and a mirror of `parent_span_id` (`OtelSpanEvent`) — so reading a trace meant knowing which writer a row came from. The vocab is `parent_event_id` (always the spawning row's `event_id`) plus `causing_tool_use_id` for the meaning that was never a parent edge. The schema still ACCEPTS `parent_id` as a legacy input key, and must: every shipped 0.13.0 emitter writes it and a row that fails to parse is dropped silently, so the tolerant readers stay. That tolerance is exactly why the gate is a linter and not zod — nothing in the type system can stop a new writer emitting the old spelling and having it parse. Allowlist is five reader files, one of which is the Linear twin's own domain model (an issue's parent issue, not an event's parent row) | [`scripts/lint-parent-vocab.mjs`](scripts/lint-parent-vocab.mjs) (`npm run lint:parent-vocab`) in [`.github/workflows/ci.yml`](.github/workflows/ci.yml); regression: [`scripts/lint-parent-vocab.test.mjs`](scripts/lint-parent-vocab.test.mjs) — case 2 locks the quoted-key hole the first version of the gate had |
| Every upstream `tools/list` golden under [`fixtures/mcp-tools-list/`](fixtures/mcp-tools-list/) is exactly what the producer derives from the committed raw bytes, and both shas are computed rather than typed. These goldens are the vendor half of F-1325's comparison, so a hand edit here would move the answer without moving the question. The producer names no twin: the substrate, the endpoint and the configuration each read assumed live in [`config/mcp-capture-sources.json`](config/mcp-capture-sources.json), a capturable source with no declared `configuration` is rejected at load rather than defaulted, and a twin the producer does not capture must record why — an unstated assumption about WHICH deployment was read is how a lane starts reporting gaps that are not real (F-1179). The github adapter pins `--toolsets=default` because that is what `api.githubcopilot.com/mcp/` serves and what `examples/support-triage` declares; the union of every toolset would be 85 tools against `default`'s 44. Refreshing a golden from the live substrate is deliberate and human-reviewed, never a cron; the staleness alarm is F-1328. A twin's `<twin>.status.json` — the recorded refusal to capture — is RETIRED by the capture that supersedes it, and `--check` reds while both are committed: every consumer resolves the status file first, so a golden landing beside a stale one is dead bytes and the lane goes on publishing the old `401` with nothing red (F-1329). A deferred `live-wire-oauth` row is also asserted to be capture-ready — flipping `capture` must not throw — so the credential errand is the one env var it claims to be, and `POME_*_MCP_TOKEN` blank is reported as blank rather than as unset (F-1184). Every capturable source also declares `configuration.completeness` — `exact`, `subset-of-remote` or `credential-scoped` — and an absent or unknown class is rejected at load beside the `configuration` itself, because a consumer calling a tool the golden lacks a twin-only DIVERGENCE is spending that claim: sound against `exact`, sound against `subset-of-remote` only where the delta is enumerated with evidence (github's `knownRemoteOnlyToolsNotInCapture`), unsound against `credential-scoped`. F-1394 is the bill for leaving it unstated — linear's golden was taken under a read-only grant, and pome-cloud's promotion gate reported six write tools `mcp.linear.app` really serves as tools twin-linear had invented. `--offline` without `--check` is therefore a WRITE mode: `configuration` is prose about a capture that is copied verbatim into two derived files, and three of the five sources sit behind one-shot OAuth grants, so without it a corrected sentence means either re-minting a vendor grant or hand-editing the goldens. It cannot forge a capture — `raw.json` is its input, so every derived field still comes from the vendor's own bytes, and `captureDate` is carried from the committed `meta.json` rather than stamped, so a re-derivation cannot reset F-1328's staleness clock | [`scripts/capture-mcp-tools-list.mjs`](scripts/capture-mcp-tools-list.mjs) (`npm run gate:mcp-tools-list`, i.e. `--check --offline`: no network, no Go) in [`.github/workflows/ci.yml`](.github/workflows/ci.yml)'s always-on block; regression, including a guard-fires block per golden: [`scripts/capture-mcp-tools-list.test.mjs`](scripts/capture-mcp-tools-list.test.mjs) |
| No twin's MCP tool table is written in TypeScript. Each of the five derives its served listing from `packages/twin-*/fixtures/mcp-tools-list.raw.json`, whose provenance — substrate, endpoint, protocol version, capture date, the `configuration` an upstream substrate was read under — is validated at module load, along with `sha256(raw.json) === meta.rawFileSha256`, so an edited golden fails loudly instead of silently becoming the new truth. `deriveMcpToolTable` joins the listing to the twin's implementations 1:1 in BOTH directions: a tool the fixture does not declare and a fixture tool nothing implements are each a throw before the twin exists. This is why it matters: `twin-slack` served eight tool names Slack does not have because commit `6abec3c` copied them out of an archived reference server, and nothing read Slack's own `tools/list` for as long as the array existed. F-1330 fixed that twin — its fixture is now `live-wire-oauth`, F-1329's live capture of `mcp.slack.com/mcp` minus one tool ruled `cold`, so its substrate no longer claims to be a transcription — but the mechanism is what stops the next one. The substrates that admit nobody read the table from upstream — `twin-code-transcription`, `twin-authored-from-vendor-docs` — require a `transcription` record saying so; a hand-written table that does not admit it is the failure. `fidelity.inventory.json` is bound 1:1 to the fixture, not to the code | [`packages/sdk/src/mcp-tool-fixture.ts`](packages/sdk/src/mcp-tool-fixture.ts); per twin, `packages/twin-*/test/mcp-tool-fixture.test.ts` drives the real `tools/list` and compares it to the fixture field by field, re-derives `canonical.json` byte for byte, and re-hashes `raw.json` off disk — all under `npm test` in [`.github/workflows/ci.yml`](.github/workflows/ci.yml); loader regression: [`packages/sdk/test/mcp-tool-fixture.test.ts`](packages/sdk/test/mcp-tool-fixture.test.ts) |
| A twin whose fixture claims to BE the vendor's listing is that listing, minus only what a ruling removed. **twin-slack, twin-gmail and twin-github** — the three twins serving an upstream capture rather than their own table, so the three where the raw bytes can quietly stop matching the golden they were copied from. (twin-github joined in F-1468; its fixture was a transcription of its own `src/tools.ts` until then, which pome-cloud's MCP lane priced at 577 findings across 34 tools against twin-slack's 0 — almost all of them zod projection artifacts rather than decisions, including an `additionalProperties: false` that `z.object()` ADVERTISES while accepting the unknown key at runtime.) The sha in a `meta.json` catches a hand edit to its `raw.json`, and nothing else catches `raw.json` diverging from `fixtures/mcp-tools-list/<twin>.raw.json` — a renamed tool, a softened `required`, or a description edited to suit the twin would all re-hash clean while the fixture went on declaring a `live-wire-*` substrate. So each file is DERIVED by a producer that can only SUBTRACT: it re-hashes the golden before reading it and copies every surviving name, description, `inputSchema` and annotation through byte for byte. It cannot rename, re-describe, re-shape a schema, or add a tool the vendor does not serve — which is exactly the set of edits that produced the defect F-1330 fixed. slack's producer subtracts one ruled-cold tool, and each suppression carries its reason in the script and is ruled per tool in [`docs/slack-mcp-unexposed-tools.md`](docs/slack-mcp-unexposed-tools.md); gmail's subtracts nothing, so its raw file is the golden's own bytes and the two shas are asserted equal. **twin-github's is the one that can also ADD, and the exception is forced rather than convenient:** two tools it serves are ones GitHub registers behind client-settable `X-MCP-Features` flags, and the golden is captured with none set ON PURPOSE because that is the surface an examinee pointed at `api.githubcopilot.com/mcp/` gets — so those rows cannot come from the capture and cannot be dropped without the twin refusing a call GitHub answers. Both doors are fenced the same way: a dropped name the capture does not carry fails, a carried name the capture DOES carry fails (the flag ruling has expired), every name in either map must give a reason, and the reasons ship on the file in `meta.projection`. That block is also the only thing proving WHAT was projected — `rawFileSha256` proves the file was not hand-edited since, and only `projection.sourceRawFileSha256` proves the golden it came from, so re-pointing the producer at a stale one would re-hash clean. Ten drops are ruled per tool in [`docs/github-mcp-unmodelled-tools.md`](docs/github-mcp-unmodelled-tools.md), two of them marked `[COVERAGE GAP]` rather than scope. ⚠️ The substrate word `upstream-capture-projection` exists in THREE places across two repos and only two of them are import-checked; pome-cloud's copy fails CLOSED and `fidelity-watch.yml` reads this repo's main unpinned, so a vocabulary value added here and not there stops comparing that twin's table at the next daily cron rather than reddening anything. **The gate is what makes staleness visible, and F-1400 is the bill for not having it:** twin-gmail's capture was internally consistent and seventeen days behind Google, its own sha green the whole time, and pome-cloud's lane reported 34 findings across 11 tools that were all one file's date. Adopting the newer capture is a CAPABILITY change — gmail's added `Message.bccRecipients`, `Label.messagesTotal`/`messagesUnread`, and a `list_labels` that returns all labels and takes no page arguments — so a re-adoption that does not move the handlers advertises capabilities the twin lacks, which is F-1330's defect reached from the other side | [`packages/twin-slack/scripts/adopt-upstream-mcp-fixture.ts`](packages/twin-slack/scripts/adopt-upstream-mcp-fixture.ts) and [`packages/twin-gmail/scripts/adopt-upstream-mcp-fixture.ts`](packages/twin-gmail/scripts/adopt-upstream-mcp-fixture.ts) (`npm run gate:mcp-fixture -w @pome-sh/twin-slack` and `-w @pome-sh/twin-gmail`, i.e. `--check`) in [`.github/workflows/ci.yml`](.github/workflows/ci.yml)'s heavy block after `npm run build`, since they import `@pome-sh/sdk` to re-derive the canonical bytes; the served-vs-golden half is each twin's `test/mcp-tool-fixture.test.ts`, which compares every served tool to the golden's entry — slack asserting the dropped set is exactly the ruled one, gmail asserting nothing was dropped and that its `captureDate` and `rawFileSha256` are the golden's |
| **twin-github's error `documentation_url` names the operation the caller asked for, and the urls are the vendor's own bytes.** Real GitHub is operation-specific on 45 of 59 measured error responses and GENERIC on 14, and both halves are load-bearing: the generic 14 are every 401 (auth fails before dispatch), every unrouted path, and `GET /users/:username`, so "name the operation everywhere" would be a new divergence pointing the other way. The urls cannot be typed from the docs site — the anchors are `/rest/repos/contents#…`, `/rest/commits/commits#…`, `/rest/branches/branches#…`, none of them derivable from the path — so they are sliced out of GitHub's published OpenAPI description (`externalDocs.url`, plus the `x-github.category`/`subcategory` pair that reproduces the anchor) into a committed artifact. The 12.9 MB description is NOT committed: the producer pins it by commit AND SHA-256, so adopting newer vendor bytes is a two-constant edit somebody reads rather than a `main` that moved. What makes the 24 KB slice trustworthy without it is that `rest` and `mcp` carry operation IDS rather than urls, so every pairing the producer DECIDES is re-derivable offline: `--check` demands the REST keys be exactly the surfaces the twin MOUNTS (`GITHUB_ROUTE_INPUTS`, 66 — **not** `route-inputs.json`'s 65, which drops zero-input surfaces and is how `GET /user` went missing from the ticket's own mapping table), the MCP keys exactly the 36 tools it serves, every mapped `method` one the tool's zod schema accepts, every operation referenced by some door, and every url equal to `…/rest/<category>/<subcategory>#<anchor>` — the two committed columns checking each other. Hand it the description and it re-derives every url and byte-diffs. The url is attached at the DOOR (the route declaration's `surface`, the tool name plus its parsed `method`), never at the throw site, because `notFound()` in `requireRepo` is reachable from ~40 routes and ~30 tools | [`packages/twin-github/scripts/vendor-operation-docs.ts`](packages/twin-github/scripts/vendor-operation-docs.ts) (`npm run gate:operation-docs -w @pome-sh/twin-github`, i.e. `--check`) in [`ci.yml`](.github/workflows/ci.yml)'s heavy block after `npm run build`; the derivation's own teeth are [`packages/twin-github/scripts/operation-docs-artifact.ts`](packages/twin-github/scripts/operation-docs-artifact.ts) driven by `test/operation-documentation-url.test.ts`, which feeds `verifyOperationDocs` mutated copies of the committed artifact and demands a refusal, and pins the served urls on the wire — both the named and the deliberately generic |
| Every input a twin's REST/GraphQL handlers can see arrives through the route's own DECLARATION, and that declaration is what `packages/twin-*/route-inputs.json` publishes to pome-cloud's declared-fidelity lane. `declareRouteInputs()` makes two of the three failure modes structural — a handler is handed only the parse result, so it cannot accept an input the declaration omits, and the declaration IS the validator, so it cannot reject one the declaration names. The third, a handler that ignores its parsed input and reads `c.req.query("sort")` anyway, no type can prevent: it is one line, it typechecks, every test passes, and the published surface silently stops being complete — after which the lane compares vendor inputs against a short list and reports no drift, a pass nobody measured, which is worse than the `not-compared` it reported before. Hence a reachability gate rather than a convention: route registrars are found by the router parameter they cannot avoid taking (every twin registers some routes through a path VARIABLE, so a literal-path pattern misses gmail and slack entirely), then every in-twin module they reach is checked — which is how gmail's input names, which lived two modules away in `rest-common.ts`, are covered. A twin with no discoverable registrar is a hard failure, not a skip. Exemptions grant specific READ KINDS in specific engine-boundary modules (stripe's idempotency/x402/session/bodyReader), must still contain each read they grant, and are printed on success | [`scripts/lint-route-input-declarations.mjs`](scripts/lint-route-input-declarations.mjs) (`npm run lint:route-inputs`) in [`ci.yml`](.github/workflows/ci.yml)'s always-on block; regression: [`scripts/lint-route-input-declarations.test.mjs`](scripts/lint-route-input-declarations.test.mjs). The artifact half is [`scripts/emit-route-inputs.mjs`](scripts/emit-route-inputs.mjs) (`npm run gate:route-inputs`, i.e. `--check`) in the heavy block after `npm run build`, since it imports the twins' compiled declarations rather than reading TypeScript with a regex; the twin LIST is compared against [`config/first-party-twins.json`](config/first-party-twins.json), so a twin nobody publishes is a red rather than four green artifacts. Per-route 1:1 mounting is `packages/twin-*/test/route-input-declarations.test.ts` via `diffRegisteredRoutes` |
| What a route does with an undeclared query or top-level body key is one ruling PER TWIN, set by measuring the vendor, and no route may disagree with its own twin. F-1179 gave all five `refuse` as an unruled default; F-1372 measured and found the vendors disagree — github, slack and linear accept and discard, gmail and stripe refuse ([`docs/undeclared-route-inputs.md`](docs/undeclared-route-inputs.md) is the evidence, and a flip must add its transcript there). Linear's is the one that was never the vendor's choice: RFC 6749 §3.1 and §3.2 both say an authorization server MUST ignore unrecognized request parameters, so four of its six routes had been violating the RFC its own OAuth flow implements. Neither disposition lets a handler SEE an undeclared input — `parse()` returns declared names only — and neither moves `route-inputs.json`, which is derived from the declared schemas alone; so a declaration short of the vendor's real surface is exactly as visible to the lane either way, and the choice is about fidelity to the vendor and nothing else. The failure mode being guarded is the reverted first attempt at F-1372: source flipped, tests left behind, ~200 assertions red across four suites | `routeInputDeclarer()` in [`packages/sdk/src/route-inputs.ts`](packages/sdk/src/route-inputs.ts) is the only place a twin sets it, so 66 specs cannot hold 66 answers. Per twin, `packages/twin-*/test/route-input-declarations.test.ts` pins the ruling as a literal `RULED` and asserts every declaration carries it, then drives the behaviour it implies over real HTTP — for an `ignore` twin, two apps through one identical request sequence with the probe added to every call in one of them, asserting they agree answer for answer. Mechanism-level cases: [`packages/sdk/test/route-inputs.test.ts`](packages/sdk/test/route-inputs.test.ts) |
| **twin-github only so far** — a twin's `fidelity.inventory.json` `rest` rows and the routes it really registers are the same set. `lintFidelityInventory` diffs the inventory against the FIDELITY doc tables, so those two DOCUMENTS agreed with each other and neither was ever compared to the code that serves traffic: 62 rest rows stood against 66 mounted routes and a route could be added or removed with both staying green. The inventory is the denominator every fidelity lane counts against, so a surface missing from it does not report `not-compared` — it is invisible, the same shape as F-1347. The subject is `<TWIN>_ROUTE_INPUTS`, which `routes.ts` mounts every route FROM (F-1179) and which `route-input-declarations.test.ts` already pins equal to the registrar and to the booted app, so the comparison needs no parse of handler source. A row keeps the vendor's spelling where the router's differs and names the pattern(s) it means in `routes` (`.../branches/:branch` is hono's `.../branches/*`; `/contents/{path}` is TWO patterns, because hono cannot match an empty wildcard); a row standing for no route says why in `unregistered`, `engine` for the two MCP transport rows the sdk mounts and `unserved` for a documented surface the loud-501 catch-all answers, and that declaration self-expires the day the twin registers the surface. The umbrella row is what the ticket was really about: `GET /search/*` and `GET /repos/:owner/:repo/pulls/:number/*` stood for thirteen routes under two names the router serves neither of, and are now thirteen rows. The other four twins' inventories are not yet compared to their route sets | `lintFidelityRestRoutes` in [`packages/sdk/src/fidelity-inventory.ts`](packages/sdk/src/fidelity-inventory.ts), called from [`packages/twin-github/test/fidelity-contract.test.ts`](packages/twin-github/test/fidelity-contract.test.ts) under `npm test` in [`.github/workflows/ci.yml`](.github/workflows/ci.yml); mechanism regression, including both mismatch directions: [`packages/sdk/test/fidelity-parity.test.ts`](packages/sdk/test/fidelity-parity.test.ts) |
| `main`'s required status-check contexts have exactly one home — the live-protection assertion reads it, so it cannot drift into watching a context GitHub no longer requires | [`config/required-checks.json`](config/required-checks.json), read by [`scripts/ci/assert-repo-policy.sh`](scripts/ci/assert-repo-policy.sh) |
| Every `packages/*` and `cli/` npm script is either an npm LIFECYCLE name (`build`/`dev`/`start`/`test`/`typecheck`/`prepack`/`prepare`/`prepublishOnly`/`postinstall`, by EXACT name — six of npm's own plus `build`/`dev`/`typecheck`, which are this repo's convention, not npm's, plus `pome`, `cli/`'s own equivalent of `start` (runs the built tarball entry point, asserts nothing); what makes the set safe is that each is reached WITHOUT the `-w <package>` shape, uniformly across every workspace — root `test`/`typecheck` are `--workspaces --if-present`, root `build` is `scripts/build.mjs` over the whole graph, `prepack`/`prepare`/`prepublishOnly`/`postinstall` are npm's pack/publish/install hooks, and `dev`/`start` assert nothing), or reachable from a workflow or root aggregate as `npm run <script> ... -w <package>`, or carrying a `pome:unwired-ok(<script>): <reason>` marker in the file its command invokes. The partition is TOTAL by construction, which is the second version of this gate. The first was an allowlist of check-name PREFIXES (`validate:*`/`check:*`/`lint:*`/`gate:*`/`test:*`/`assert:*`), and a prefix list of what counts as a check is itself a hand-maintained list — the shape D5 names as the bug. It proved it on its first day: it could not see `verify:cloud-token`, a real check that had also never run, and could not see `smoke` or `review:harness` either; all three were found by hand instead. `validate:mcp` sat red on `main` for weeks before F-1354 wired it, and the audit that followed (F-1472) found the rest of the shape: twin-slack's own `validate:mcp` (plus a committed output artifact that could never reproduce byte-for-byte — it embeds the ephemeral port each run bound to — so it now prints to stdout instead of writing one, same fix as F-1354); twin-slack's `test:coverage`, whose vitest thresholds nothing had ever run with `--coverage`; twin-github's and twin-slack's `smoke`, each stale since the fixture drift that predated them (65→36 tools under F-1376, 11→18 tools plus two response-shape changes under F-1330) — a hardcoded expected count is the bug wearing a new number, and a count DERIVED from the fixture is the same bug in a passing costume — `deriveMcpToolTable` joins fixture to implementations 1:1 and throws both ways at module load, so `served.length === fixture.tools.length` cannot fail and the twin would not construct; both smoke scripts now assert only that each transport answered with a NON-EMPTY list, and the count itself is `gate:mcp-fixture` plus `test/mcp-tool-fixture.test.ts`, both wired; twin-slack's `verify:cloud-token`; and twin-github's `review:harness`, which assumed a human had already booted a dev server in another terminal and minted a token missing the `login` claim `mergePullRequest`'s permission check reads — it now self-boots on an ephemeral port and 403'd on every prior run for that reason alone. `regenerate-mcp-tool-fixture.ts`'s `--check` mode had no npm script at all, and F-1472 added one; F-1468 then deleted that file, replacing twin-github's own tool table with an upstream capture and the same `fixture:mcp`/`gate:mcp-fixture` producer pair the other twins use, which ci.yml already wires — so the gap is closed from the other direction and no `gate:mcp-tool-fixture` exists. The derivation below absorbed that substitution with no edit to this gate, which is the point of deriving rather than listing. No central allowlist. Twelve scripts are deliberately unwired: eight carry their reason inline in the file each command invokes, which the gate reads directly rather than from a list here, and four are DERIVED — a write mode whose own package's wired sibling runs the identical command plus `--check`, so no sentence has to be kept true by hand. That derivation is same-package and `--check`-exact on purpose: `twin-gmail` and `twin-slack` both declare `fixture:mcp = tsx scripts/adopt-upstream-mcp-fixture.ts`, one command string over two different files, so a flat command list let one twin's write half be certified by the other twin's file; and `startsWith(command + " ")` let a wired `--watch` dev script certify a verdict. A marker NAMES its script and its reason must be non-blank on the same line: three of those files implement a write mode AND a wired `--check` verdict (`fixture:mcp`/`gate:mcp-fixture` in three twins, `emit:`/`check:trace-contract` in wire), so an unnamed marker for the write half would pre-authorise the check half going unwired — the original defect, granted in advance. The gate and its own regression suite are excluded from the corpus they scan, because both quote `npm run <x> -w <pkg>` strings and a gate that accepts its own prose as proof a check runs is the bug it exists to catch. One residual, stated rather than implied: the corpus is workflow YAML plus root scripts with whole-line comments stripped, so it cannot see that a workflow is `paths:`-filtered, `workflow_dispatch`-only or `if: false` — a check wired ONLY there reads as wired. F-1476 widened the denominator from `packages/*` alone to also cover `cli/`, on the same terms: `cli/package.json`'s own non-lifecycle scripts join the same wired/derived/marker audit (its `emit:manifest-schema`/`check:manifest-schema` pair is the same write/`--check` derived shape three `packages/*` pairs already have), and `cli/scripts/**` gained a second, file-level denominator — every file there that is neither the invoked file of a declared `cli/package.json` script nor imported by a sibling file is a candidate entry point with no script name, so it can only be cleared by its own `pome:unwired-ok(<relative-path>): <reason>` marker (same syntax, keyed by path instead of a script name) or it fails. That closed `cli/scripts/make-unwired-fixture.mjs`'s exact shape: broken on `main` since a stale exact-text replacement started throwing, reached by nothing, invisible to a script-name-only denominator — deleted rather than fixed, since it still threw on a second, different drifted replacement even after F-1481 (#390) fixed the first one it hit, and its own purpose (a fixture for the Gen-1 `pome-setup` skill's manual wiring acceptance run) was retired at F-859. `gate:no-eval` and `gate:no-native` in `cli/package.json` were deleted rather than wired — both duplicated a gate the ROOT `gate:no-eval`/`gate:no-native` (rows above) already run repo-wide including `cli/src/**`/`cli/scripts/**` and the whole npm workspace's production closure, and `cli`'s own `gate:no-native --root .` was additionally broken (no `cli/package-lock.json` exists post-workspace-merge, so it threw `ENOENT`) — the enforcement those two rows claim is real; `cli/`'s local copies were the false thing, not the rows. `test:e2e` was deleted too: `vitest run test/e2e` is a strict subset of what the lifecycle `test` script (plain `vitest run`, no `include` filter) already runs. `overhead-gate.ts` and `cas-adapter-acceptance.ts` — real gates in [`.github/workflows/agent-trace-overhead-gate.yml`](.github/workflows/agent-trace-overhead-gate.yml) that predated this row's `cli/` coverage — gained their own `gate:agent-trace-overhead`/`gate:cas-adapter-acceptance` npm scripts so that workflow could call them via the standard `npm run <name> -w <pkg>` shape instead of a direct `tsx <path>` line invisible to this gate; `gate:recorder-overhead` already existed and only needed the workflow's direct `tsx` line converted the same way. `npm run <name> ... -w <pkg>` is the ONLY calling convention this gate accepts, and for a DECLARED script a direct file invocation is a FALSE RED rather than a blind spot — a workflow step that really runs `npx tsx scripts/overhead-gate.ts` reds by name saying nothing reaches it, which is how all three of those steps were written before F-1476 converted them. Accepted deliberately: one detection mechanism is what makes the marker path trustworthy, and the red names its own fix (declare the file as a script, call it the standard way) instead of failing silently. Two floors keep the denominator from shrinking unnoticed: `packages/` missing is a hard throw (a zero-entry scan would exit 0 having asserted nothing), and — since the `cli/` half had no equivalent and renaming `cli/` or `cli/scripts/` would have dropped eight entries green — a root `workspaces` array that names `cli` while the `cli/` half contributes zero entries is also a hard throw, derived from the manifest npm itself reads rather than from a constant. The `cli/scripts/**` sibling-import derivation strips whole-line comments before scanning, the same as the corpus: a commented-out `// import ... from "./dead.js"` must not certify `dead.js` as a live library module. Two residuals specific to the file-level half, both in the false-red direction (a live file looking dead, whose suggested remedy is a marker — so following the diagnosis records a false statement in the file): the importer scan reads only non-test files UNDER `cli/scripts/**`, so a module imported solely from `cli/src/**`, a `*.test.ts`, or a build config is not derived-covered — `write-build-info.mjs` is that case, and its marker states the gate's limit rather than a real decision to leave a check unrun; and `readdirSync` reports a symlink as neither file nor directory, so a symlinked DIRECTORY under `cli/scripts/` is skipped with its contents, unlike `lint:import-meta-main`, which follows symlinks for exactly this reason. Neither has an instance today | [`scripts/check-packages-scripts-wired.mjs`](scripts/check-packages-scripts-wired.mjs) in [`ci.yml`](.github/workflows/ci.yml)'s always-on block; regression, including the exemption path: [`scripts/check-packages-scripts-wired.test.mjs`](scripts/check-packages-scripts-wired.test.mjs) |
| `npm run test:contract` runs every `contract/*.test.mjs` file by discovery, never by a hand-kept list — `contract/run.mjs` hand-listed three of the four files that existed until F-1353, and a file left off a list reads exactly like one that passed. `cli-start.test.mjs` is the one deliberate exclusion: it needs a built `cli/` workspace, which `run.mjs` never builds (only wire/sdk/twin-*), so ci.yml runs it as its own step after the heavy job's earlier `npm run build`. An exclusion set with one entry is still a list, so what is asserted is the UNION: every `contract/*.test.mjs` file is either discovered by the runner or invoked by a named ci.yml step, and a second cli-dependent test excluded from the runner with no workflow step to run it reds. That mutual assumption — two lists each trusting the other to cover the file — is the drift F-1353 found. The runner's entry guard compares `process.argv[1]` against `import.meta.url`, never `import.meta.main`: that landed in Node 24.2, `engines` allows `>=24`, and `undefined` there makes the guard false and the whole suite exit 0 having built nothing and asserted nothing. Both sides are `realpathSync`'d, because node resolves symlinks before deriving `import.meta.url` and an unresolved argv misses through a symlinked checkout in the same silent shape; a miss while invoked AS `run.mjs` throws rather than exits 0, and so does an empty discovery (bare `node --test` searches cwd recursively) | [`contract/run.mjs`](contract/run.mjs)'s `discoverTestFiles()`; the union-totality and no-literal-argv assertions in [`contract/run.test.mjs`](contract/run.test.mjs), itself discovered by the runner under `npm run test:contract` in [`.github/workflows/ci.yml`](.github/workflows/ci.yml) |
| A run whose correctness is known by CONSTRUCTION scores the right way for the right REASONS. Two fixture agents — deterministic scripts, no model, no socket, no network — drive `examples/support-triage`'s task over the real github + slack twins seeded from that task file's own `## Seed State`, and their result is answered by the product's own path: `parseTaskFile` → `bindCriterion` → the declared check's own `evaluate`. Four things are asserted and the ORDER is the invariant. (1) The PER-CRITERION breakdown for both runs — this is what catches "right total, wrong reasons", and it is not theoretical: against a `github.no-new-issues` that binds but compares issue TITLES instead of numbers (F-1198's own named trap) the wrong run's aggregate lands on 50, still under a threshold of 100, so an aggregate-only gate stays GREEN while the breakdown reds. (2) The DENOMINATOR, for both runs — without it the wrong run's 0 is equally satisfied by a pipeline that graded nothing, which is the all-skip defect wearing the right answer's clothes. (3) No criterion `skipped` and none binding nothing — an unbound sentence is dropped by the grader and the denominator moves for a reason nobody wrote down, the silent failure this whole gate is aimed at. (4) The aggregate, last, pinned to `[100, 0]` rather than only compared to the threshold. The breakdown table is keyed by CHECK ID and exhaustive in both directions — a `[code]` criterion the task grows and the table does not name is a red, and so is a table row the task does not carry — so F-1338's positive tape assertion cannot arrive silently; the harness already captures a per-twin, tool-stamped tape, so landing it is one table row and no harness change. `[model]` criteria are COUNTED and deliberately not graded: golden tasks are restricted to the `[code]` half so the gate is fast, free and flake-proof. Substrates are supplied per DECLARATION (`seed+final` gets the seed, `tape` gets the tape, anything else gets `null`), so a check that reads a hole returns a named skip and arm 3 reds rather than a predicate crashing. Vertical slice only for now — the stripe pair lands with M3's replication; "github and stripe at minimum" is still the end state | [`cli/test/golden/`](cli/test/golden/) — the harness is `goldenRun.ts`, the fixtures and the expected breakdown are `supportTriageFixtures.ts`, the assertions are `support-triage-gate.test.ts`. Runs inside `npm test -w @pome-sh/cli` (so the ticket's `bun run --filter '*' test` includes it) AND as its own named `npm run gate:golden -w @pome-sh/cli` step in [`.github/workflows/ci.yml`](.github/workflows/ci.yml)'s heavy block, deliberately twice: this gate's subject is a wrong verdict that looks exactly like a right one, so which line went red is not a detail to bury in 60 lines of bash. The aggregation arithmetic lives under `test/`, is exported to nothing, and is the gate's assertion apparatus rather than a scorer — evaluation is still the product and its engine still lives in pome-cloud (`gate:no-eval`, row above) |

## Public Repo Guardrails

The public `main` branch requires a PR and green required checks before merge
(no approving-review gate — the founder team is two people). Direct pushes are
reserved for release automation only.

“Zero embedded cloud config” means no credentials and no non-overridable env
wiring. An overrideable public API base (`https://api.pome.sh`, via
`--api-url` / `POME_API_URL`) remains allowed.

Secret scanning runs in CI via [`.github/workflows/secret-scan.yml`](.github/workflows/secret-scan.yml)
with both gitleaks and TruffleHog. Install the local hook with
`bash scripts/hooks/install.sh`; staged changes are blocked unless the same
boundary gates pass and installed secret scanners find no verified secrets.

Twin images publish only after both the `ci` and `secret-scan` workflows
succeed for that SHA (`scripts/ci/wait-for-workflow.sh`) and Trivy scans pass.
Published GHCR
digests are cosign-signed with GitHub OIDC, and each digest receives an SPDX
SBOM attestation. Downstream cloud snapshot promotion must pin and verify those
signed digests before rebuilding runtime snapshots. That promotion is operated
from the private `pome-sh/pome-cloud` repo — maintainers: see
`docs/runbooks/twin-release-and-promotion.md` there.

`@pome-sh/cli` and `@pome-sh/adapter-claude-sdk` are published to npm for end
users and `@pome-sh/checks` to npm for the cloud grader;
`@pome-sh/wire` is published to GitHub Packages for sibling repositories only.
Everything else is internal to the CLI tarball, so the old per-package
version-bump runbook (`PACKAGE_RELEASE.md`, one changeset per `packages/*`
member) is gone; see [`RELEASING.md`](RELEASING.md) for what replaced it.

Everything else — architecture, per-package details, and the CI gotchas
(no-cloud-imports, twin Docker build) — is documented at
https://docs.pome.sh. Internal package layout (the twin registry,
`@pome-sh/wire`, private vs. published) is also mapped in
[`packages/README.md`](packages/README.md).

---
> Source: [pome-sh/digital-twins](https://github.com/pome-sh/digital-twins) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-08-19 -->
