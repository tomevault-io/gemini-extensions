## devflow-autopilot

> DevFlow is a single [Claude Code](https://code.claude.com) plugin published at the repo root; the repo is also its own marketplace. It turns a GitHub issue into a reviewed, documented, merged PR, and a weekly retrospective loop improves the automation. Full system reference: [`docs/DEVFLOW_SYSTEM_OVERVIEW.md`](docs/DEVFLOW_SYSTEM_OVERVIEW.md). Contributor guide: [`CONTRIBUTING.md`](CONTRIBUTING.md).

# DevFlow — project memory

DevFlow is a single [Claude Code](https://code.claude.com) plugin published at the repo root; the repo is also its own marketplace. It turns a GitHub issue into a reviewed, documented, merged PR, and a weekly retrospective loop improves the automation. Full system reference: [`docs/DEVFLOW_SYSTEM_OVERVIEW.md`](docs/DEVFLOW_SYSTEM_OVERVIEW.md). Contributor guide: [`CONTRIBUTING.md`](CONTRIBUTING.md).

## Commands

```bash
bash lib/preflight.sh      # verify git/gh/jq/python3.11+/PyYAML on PATH
bash lib/test/run.sh       # full suite: jq filters + shell helpers + python; gh-stubbed, no network/auth
git ls-files '*.sh' | grep -v '^lib/test/' | xargs -r shellcheck --severity=warning -e SC1091
shellcheck --severity=warning -e SC1091 --extended-analysis=false lib/test/run.sh
git ls-files '*.py' | xargs -r ruff check
```

**The `lib/test/run.sh` lint needs ShellCheck ≥ 0.10.0 and the `--extended-analysis=false` flag — both, or it does not work (issue #745).** ShellCheck's dataflow ("extended analysis") pass OOMs on a file this size; the flag disables it (the measured figures and what the flag costs live in the `ci.yml` pin step's own comment — a past-time snapshot to re-measure after a ShellCheck upgrade, not an invariant). But `--extended-analysis` (and the equivalent `# shellcheck extended-analysis=false` directive) landed in **0.10.0**: on 0.9.x — which is what `ubuntu-latest`/Ubuntu 24.04 ships — the flag errors and the directive is silently ignored (SC1107), so the lint OOMs exactly as if the flag were absent. CI therefore installs a pinned ≥ 0.10.0 binary rather than using the runner image's; at the desk, check `shellcheck --version` before concluding the lint is broken. The remaining `lib/test/**/*.sh` files are linted by the explicit list in `.github/workflows/ci.yml`, and `lib/test/lint-carveout-guard.py` (driven from `lib/test/run.sh`) turns the suite RED if a tracked `lib/test` script is neither in that CI-linted set nor under `lib/test/fixtures/`.

CI (`.github/workflows/ci.yml`) runs the same suite + lint on every PR. The **required** status check is the job name **`lib + python tests`** (not "CI", which is the workflow name and never resolves). CI checks out full history (`fetch-depth: 0`) so `origin/main` resolves and the `#434` stale-prose self-scan runs live there rather than self-skipping (issue #456).

**The suite reports three tallies — passed, failed, and *skipped* — and `0 failed` does NOT mean "everything ran" (issue #456).** A check can **self-skip** (it neither passed nor failed because the host could not run it or express its condition). With nothing skipped the summary is byte-identical to before — `N passed, M failed`; with `K` skips it reads `N passed, M failed, K skipped` followed by one line per skipped check (name, kind, reason). The exit code is unchanged (`[ "$FAIL" -eq 0 ]` — a skip never fails the suite). Each skip carries a **kind** — `blocking-gate` (a real gate that should have run here but could not — the `#434` self-scan arms) and `host-capability` (the host cannot express the condition — `#423 T6b`). Every self-skip routes through the single `skip <name> <kind> <reason>` helper in `lib/test/run.sh`; a meta-assertion turns the suite RED if a `printf '  NOTE ` emit appears outside that helper (it is the only sanctioned NOTE-emit; the scan is comment-aware and matches only the contiguous `printf` emit shape — an echo-based or variable-assembled emit is a disclosed non-goal). The renderer is `lib/test/summary.sh` (added to the CI shellcheck scope explicitly, since `lib/test/` is otherwise excluded). **A skipped check is never a clean pass:** when reading a suite run as verification (issue #405), a non-empty skip population must be surfaced, not laundered into `result: pass` — `/devflow:review-and-fix` records skips in `verification_evidence.skipped_checks` (distinct from `result: skipped`, which means the whole suite run was skipped) and a run whose `skipped_checks` is non-empty is not reported as a clean pass.

**Running the suite when the `bash <path>` wrapper above is denied — the tier matters.** The commands above are the *local* form. If the classifier denies the `bash <path>` wrapper (see the *LEADING token* gotcha and the classifier-fallback Convention below), do **not** silently fall back to "CI is the gate" — work the tiers in order:

1. **Local/interactive tier:** the suite scripts carry the executable bit + a `#!/usr/bin/env bash` shebang, so retry by invoking the path **directly as the leading token** — `lib/test/run.sh`, `lib/preflight.sh` (not `bash lib/test/run.sh`). The exec bit is necessary but not sufficient — the leading-token form must also be permitted; on this tier it typically is. **On this tier, focused verification is the iteration default and the final gate is parallelized (issue #707):** an intermediate commit or push iterates on the focused test covering the changed surface — a registered shell module (`lib/test/run-module.sh <module-id>`), or, for a `scripts/*.py`/`lib/*.py` unit, the `lib/test/test_*.py` file its `lib/test/modules/coverage-map.json` entry names in a `focused_test` field, invoked as a **direct leading token** (`lib/test/test_python_scripts.py`) and never as `python3 <path>`, the interpreter-head shape the cloud matcher denies (#789). The full suite runs mid-iteration only for a surface no focused test covers, and then only on its **first** cycle: a second mid-iteration cycle on that same uncovered surface extracts a durable module instead (#789), and any mid-iteration full run records a `## Devflow Reflection` bullet naming which closed-set fallback case applied. A mid-iteration `#434` stale-prose `blocking-gate` skip on a dirty tree is expected and clears on commit — never re-run the full suite solely to clear it. When the full suite does fail, read its terminal `Failure recap` (#789) from the stderr-merged capture rather than relaunching; before a completion or PR-ready claim, push to trigger CI **and** start the full suite plus every required lint gate locally at the same time — the push is not gated on the local run finishing, but the *claim* is — read the local run's summary before making one, and a nonzero failure tally, a nonempty skip tally, or a run that never started is not a completion whatever the push already did — and the local run stays the authoritative local signal (its failure detail is richer than CI's). The `#456` skip accounting is unchanged: a nonempty skip tally is not clean, and a focused module may not self-skip. The operative statement of this policy lives in the three prompt extensions (`.devflow/prompt-extensions/{implement,review-and-fix,receiving-code-review}.md`) — this bullet is their coupled mirror, and the two are edited together.

   **There is no known-flake set — every FAIL is a real failure to diagnose.** The one formerly-documented flake (a load-sensitive timing assertion in `SignalCleanupMatrixTests`) was fixed at the source rather than tolerated, and no assertion is dismissed as a flake. A red assertion that reproduces only under pool saturation is a defect in that assertion's slack budget: fix the assertion, do not re-run the suite hoping for green.
2. **Cloud `/devflow:implement` tier — in-env verification is the gate during the run; CI is the post-PR merge gate, not an in-run verification channel (issue #405).** This repo grants the suite/lint direct forms — `Bash(lib/test/run.sh:*)`, `Bash(lib/test/run-module.sh:*)`, `Bash(lib/preflight.sh:*)`, `Bash(shellcheck:*)` — in **both** `devflow_implement.allowed_tools` and `devflow.allowed_tools`, so a cloud implement run **runs the suite (or a focused module) in its own environment** (the direct leading-token form; the `bash <path>` wrapper stays deny-floored) and verifies every verification-command AC and the inline review pass's test evidence there. The run **never waits on, polls, re-checks, or cites CI for its own progress** — a verification command that is *not* granted goes **Blocked** naming `devflow_implement.allowed_tools` as the remedy (never a silent stall, never a verdict resting on a CI result the run never saw). CI (`lib + python tests`) remains the **required post-PR check that gates the human merge**; the run does not tick anything on it. Consumer repos follow the same rule: list your test/lint commands in `devflow_implement.allowed_tools` (and `devflow.allowed_tools` for the command path) and the run verifies in-env; leave them ungranted and the run goes Blocked naming the key. The read-only `review` tier (`devflow-runner.yml`) is a **separate** case — its wait-for-CI-then-review posture is the correct post-PR sequence and is untouched by #405.
3. **Auditable skip only for a genuine block:** only a *permission/sandbox denial* of the direct form (e.g. the runner's `mktemp` writes are blocked) — **never** a suite that *runs and fails* — routes a local run to the CI gate, and the skip must be recorded in the workpad so it is an auditable decision. A reflection that says "could not run the suite" without having tried the direct-leading-token form first (on a tier where it is available) is the bypass this rule exists to stop.

## Architecture

- `skills/` — one `SKILL.md` per command (`/devflow:implement`, `/devflow:review`, `/devflow:review-and-fix`, the `/devflow:docs` family, `/devflow:create-issue`, `/devflow:retrospective-weekly`, …), plus the two skills vendored from the `superpowers` plugin (`requesting-code-review` — the review engine's final-pass reviewer, dispatched as a `general-purpose` Task; `receiving-code-review` — the fix-loop principles) under their upstream MIT license (Jesse Vincent — see `LICENSES/superpowers-LICENSE`). Both are *runtime-dispatched* by DevFlow's engine; the `superpowers:writing-skills` authoring discipline is **not** vendored — it is a development-time tool, referenced as the external skill in the Conventions below. **Both vendored skills (`receiving-code-review`, `requesting-code-review`) are installed into third-party consumer repos — their SKILL.md bodies must be repo-agnostic: no `lib/test/run.sh`, no `lib + python tests`, no `CLAUDE.md tiered-runner convention`, no DevFlow-internal step numbers. Use generic language ("the project's test suite", "the fix step") instead.**
- `agents/` — first-party subagents: the review-engine trio `checklist-generator`, `checklist-deduper`, `checklist-verifier`, plus `code-explorer` and `code-architect` (the `/devflow:implement` discovery/planning agents, vendored from Anthropic's feature-dev plugin under Apache-2.0 — see `LICENSES/feature-dev-LICENSE`), and the five `pr-review-toolkit` review agents `code-reviewer`, `comment-analyzer`, `pr-test-analyzer`, `silent-failure-hunter`, `type-design-analyzer` (vendored under Apache-2.0 — see `LICENSES/pr-review-toolkit-LICENSE`).
- `scripts/` — Python + shell CLIs (`workpad.py`, `config-get.sh`, `match-deferrals.py`, `file-deferrals.py`, `parse-acs.py`, `resolve-*-trigger.sh`, …).
- `lib/` — retrospective-loop helpers (`*.sh`, `*.jq`), `preflight.sh`, `test/`.
- `.github/` — optional cloud tier: workflows + composite actions (incl. `vendor-plugin`).
- `.devflow/` — `config.example.json` + `config.schema.json` (+ tracked `learnings/`, `logs/`). The live `config.json` is **tracked** here too — force-added past the `/.devflow/*` ignore rule (`git add -f`) so the cloud tier reads it from the committed tree.

## Gotchas (load-bearing — easy to get wrong)

- **Skills locate bundled helpers via the portable single-statement anchor `"${CLAUDE_SKILL_DIR:-<absolute skill base directory this runner reports in context>}"/../../<dir>/…`, NEVER `${CLAUDE_PLUGIN_ROOT}`** (not substituted inside `SKILL.md` bodies). The cloud-tier **workflows** are the one exception: they use the literal vendored path `.devflow/vendor/devflow/scripts/…`. **Why this exact shape (issues #241 + #275, generalized across every skill/phase file that carries the anchor):** `${CLAUDE_SKILL_DIR}` is *empty* (not merely unset) on non-Claude-Code runners (confirmed on Copilot CLI; expected on Cursor, Codex CLI, Gemini CLI) — the runner instead reports a `Base directory for this skill:` context line the agent substitutes for the placeholder — AND Copilot CLI's inline `bash -c` marshaling **strips a variable assigned in one statement and read in a later statement of the same inline command** (`bash -c 'v=hi && echo $v'` → empty), so the old assign-then-reuse recipe (`SKILL_DIR="…"; … "$SKILL_DIR"/../…`) is banned: resolve the anchor **inline in the statement that uses it**, every call site (a reintroduced bare or cross-statement form turns `lib/test/run.sh`'s #275 pin loop RED). Windows-form runner-reported paths are normalized at **prompt time** (one standalone `wslpath -u`/`cygpath -u` probe or `lib/normalize-path.sh`'s textual rules; the skills carry **no** shell normalization mirrors anymore — create-issue's preamble is the reference prose paraphrase, kept in lockstep with the lib helper). On Claude Code the resolved value equals the bare form, so behavior is unchanged there. Relatedly (#275): `workpad.py` reads `.devflow.workpad_marker` from `.devflow/config.json` directly in Python — never by exec-ing `config-get.sh`, which Windows can't exec ([WinError 193]). The cloud-tier vendored-path carve-out is untouched. **Repo-root anchoring (#295, supersedes the #275 cwd-relative contract):** the six `.devflow/` readers — `config-get.sh`, `workpad.py` (marker read), `load-prompt-extension.sh`, `match-deferrals.py` (`_default_config_path`), `match-lint-adjudications.py` (`_default_config_path`), `render-audit-prompt.py` (consumer prompt-extension read, issue #600) — resolve the *default* `.devflow/` path anchored to the git repo **root** (`git rev-parse --show-toplevel`, falling back to `pwd`/`Path.cwd()`), mirroring `lib/config-source.sh`, so a skill run from a **subdirectory** still loads the consumer's root config/prompt-extension instead of silently missing it. A **non-empty** explicit `CONFIG_FILE`/`--config` arg is still honored verbatim (an explicit empty value still selects the root-anchored default; root anchoring applies only to the default); a no-git-root-**and**-no-`.devflow/` tree emits a stderr breadcrumb instead of a silent default. The Python readers resolve the root via a **native `git` subprocess** (Windows-safe, like the `gh` calls — not a `.sh` exec). Keep the three contract mirror sites (`config-get.sh` + `workpad.py` + `match-lint-adjudications.py` headers, the `SHARED REPO-ROOT CONFIG CONTRACT` phrase) and the `lib/test/run.sh` pins in lockstep. **Known limitation:** `--show-toplevel` returns the *nearest* git root, so a nested submodule/inner repo or a monorepo whose `.devflow/` is not at the git root is **not** covered. **`load-prompt-extension.sh` anchors on the repo root only on its FALLBACK branch (#874):** when `DEVFLOW_PROMPT_EXTENSION_ROOT` is set and non-empty it composes `<root>/<skill>.md` from that value directly and never calls `git rev-parse`. The cloud review tier sets it, because that job checks out the PR head and the loader's bytes become the merge-gating reviewer's own appended prompt — the canonical statement of that trust boundary is `docs/DEVFLOW_SYSTEM_OVERVIEW.md`'s base-ref-trust-boundary bullet, and the enumerations naming this reader in `.devflow/config.schema.json` and `scripts/emit-git-env.sh` are a coupled pair that records the fallback-only scope.
- **Config is `.devflow/config.json` (JSON), read via python3 by a single resolver** (`config-get.sh` / `config-source.sh`) — `node` is NOT required anywhere in the config-read path (issue #220; python3 is a hard preflight prerequisite). PyYAML is required only because `match-deferrals.py` (deferred-findings blocks in PR bodies) and `consolidate-changesets.py` (changeset frontmatter) lazily import it (config itself is JSON — no PyYAML needed to read it; `workpad.py` is stdlib-only and needs no PyYAML). Renaming a config key? grep `.py`, `.sh`, `.yml`, and jq lines — silent-fail consumers exist (`match-deferrals.py`, `workpad.py`).
- **`/devflow:create-issue` is a thin root plus MARKER-gated references, and its entry gate degrades — it does NOT fail closed (#614).** `skills/create-issue/SKILL.md` is an always-loaded root (routing table, entry-gate rule, Step 1, Step 3's drafting rules + no-options gate, and four **non-degradable invariants**); the step procedures, the shared revision-delta procedure, and the predicate-gated fallback arms live in `skills/create-issue/references/`. A reference is loaded with the runner's **file-read tool** (never a shell invocation) and accepted only when its literal **first line is its `start` boundary marker and its last line the matching `end` marker**, each naming that file's own path — another instance of the repo's progressive-disclosure pattern, with **deliberate divergences from the #529 review bundle**: references live in the existing `references/` dir (not a new `phases/`), the gate carries **no identity-manifest/hash layer**, and a failed load **degrades best-effort** (breadcrumb + the routing row's named degraded behavior) instead of stopping — no routing row's failure arm may terminate the run, because this skill's documented ethos is that nothing blocks issue *creation*. Do not "unify" it onto the review engine's fail-closed contract. **Coupled sites, all edited together:** the routing table's marker-contract column must byte-match each file's marker id (step number / `revision-delta` / `fallback-<name>`); the `#614` block in `lib/test/modules/create-issue-contract.sh` reconciles the routing table against the on-disk `references/*.md` set each way round — **exempting exactly two files, `issue-template.md` and `audit-prompt-template.md`**, which are renderer/template inputs rather than routed step procedures — so a non-exempt reference carrying no routing row, and a routing row naming no file, is RED. **The Step 3.6 audit LIFECYCLE is owned by `scripts/issue-audit-state.py`, not by prose** — transition legality, round numbering and the round **kind** (`discovery`/`targeted`, issue #793), the arm routing, the budgets and bounded retries, digest and sentinel comparison, the offer triggers, override records, presentation eligibility and the audit-summary field set are all its answers, which the skill obeys rather than re-deriving; the canonical description lives in [`docs/DEVFLOW_SYSTEM_OVERVIEW.md`](docs/DEVFLOW_SYSTEM_OVERVIEW.md) (the state-owner and round-kind sections), and this bullet is a non-authoritative pointer to it. **Pin routing is decided per pin, never by default:** a pin asserting content *survives somewhere in the shipped skill* targets the concatenated `$CI_BUNDLE` (built in-module and hoisted into `lib/test/run.sh` as `CREATE_ISSUE_BUNDLE`, bound through `CI_MOD_VARS` so it resolves at meta-guard time); a pin whose guarantee is **location-sensitive** — the root's non-degradable invariants, the four default-path purity pins — keeps a specific-file target. **Pointer purity is non-negotiable:** no routing row, seam pointer, or restatement may quote a literal the module pins uniquely — a collision is always resolved by **rewording the pointer**, never by weakening the pin or its home sentence. Keep prompt prose lean; trim on growth — no gate. **Runtime main-thread context is a distinct quantity from static shipped size (#767):** the behavioral instrument `scripts/create-issue-context-eval.py` (maintainer-run, never on the skill's runtime path) measures the live per-turn orchestrator context, and the authoritative-vs-redundant-addition determination + the pointer-not-re-quote reduction live in `docs/create-issue-context.md` — the single source of truth for that axis.
- **The review engine is shared, and it is a BUNDLE — a thin root plus gated phase references (#529).** `/devflow:review` Phases 0–4.3 are executed *verbatim* by `/devflow:review-and-fix` (which adds the fix loop + shadow pass and skips Phase 4.4). Edit `skills/review/SKILL.md`; never paraphrase the engine into the fix-loop skill. The root keeps only shared state (the live-progress-comment machinery, the overrides block), the cross-phase invariants, the extension load, routing, and fail-closed entry; **each phase's authoritative procedure lives in `skills/review/phases/*.md`** and is reached through the root's entry-gate (re-derive bundle identity → `Read` the reference → clear its boundary contract, every entry, including a `/devflow:review-and-fix` Step 2.6 **shadow** entry). Three references are gated *out* of the default path by their existing predicates — `phase-0-3-6-blocker-recheck.md` (standalone PR mode **and** only over a prior REJECT driven solely by carve-out blockers; on a hit it *replaces* Phases 1–3), `phase-0-6-stale-prose-lint.md` (`devflow_review.stale_prose.enabled`), `phase-4-1-7-stale-adjudication.md` (PR mode, STALE adjudication); `phase-4-4-github-post.md` is standalone-only. **The reviewed surface is every source, not the root:** `extract-command-heads.py`/`extract-command-shapes.py` take the whole bundle in one call, and `lib/test/run.sh` asserts engine-content pins against a concatenated `$REVIEW_BUNDLE` (so a sentence may move between references without breaking its pin), reserving `$REVIEW_ROOT` for pins that are genuinely about the root. Keep prompt prose lean; trim on growth — no gate.
- **Cloud allowlists & command shapes — a head or shape the runner doesn't grant is SILENTLY denied, and a run can end with NO verdict (issues #363, #401, #484).** `skills/review/SKILL.md` runs under two allowlists — the `review` profile's `TOOLS='…'` in `.github/workflows/devflow-runner.yml` (auto-review) and `devflow.yml`'s hoisted `TOOLS='…'` (the manual `/devflow:review` path) — while the implement engine runs the shared review engine inline under `devflow-implement.yml`'s baked `--allowed-tools` (#484). A command the active profile omits is refused *before it runs*: it burns budget and produces no output, not a loud failure. **Heads are not enough — the matcher also denies composite *shapes* whose every head is granted:** a leading `VAR=`/`cd`, a `/tmp` redirect, a heredoc write, an interpreter head, or the unexpanded `"${CLAUDE_SKILL_DIR:-…}"` anchor as the leading token. Use a probe-proven permitted shape (Write into `.devflow/tmp/**`, `… | tee`, a repo-relative vendored-literal helper path), and on two denials of a shape *switch* to a permitted alternative — never iterate variants. **Grants are per-HEAD across the whole pipeline** (#455): one ungranted head in a tail refuses the entire statement (`paste` is granted nowhere — use `tr`/`sed`/`grep`). `lib/test/extract-command-heads.py` + `extract-command-shapes.py` (driven by `lib/test/run.sh`) turn an ungranted head or a denied-shape fence RED at the desk; `.github/workflows/matcher-probe.yml` (repo-internal, NOT shipped by `install.sh`) is the re-runnable evidence source — re-run it after a `claude-code-action` upgrade before changing shape rules or grants. Full probe evidence (run IDs, the review R1–R4 / implement I1–I6 tables, the per-tier caveats, the non-label-capture INFERENCE-not-measurement gap, the grant flows) lives in [`docs/cloud-allowlist.md`](docs/cloud-allowlist.md).
- **The five runner/probe allowlist literals are GENERATED from one versioned manifest — never hand-edit them (#561).** `lib/capability-profiles.json` (integer `manifest_version`, token `groups`, the `review`/`implement`/`command` profiles) is the single source of truth; `python3 lib/generate-capability-profiles.py` compiles it into the five `TOOLS='…'` / `--allowed-tools` / probe-baseline regions, each banner-stamped with a sha256, and `--check` (wired into `lib/test/run.sh`) turns any manifest↔literal drift RED with a directional diff — so you never hand-edit a workflow literal. **The review profile is a security boundary:** the generated review literal *is* the read-only reviewer allowlist, so `lib/review-profile.tokens` locks its exact resolved token list — any manifest edit that widens the resolved review list fails closed until you update that lock in the same PR. Adding a grant = edit the manifest (+ the lock if it widens review), regenerate, bump `manifest_version` once. **Implement-tier bundled-helper grant flow (issue #555):** a bundled helper a `/devflow:implement` fence invokes (the §4.0.5-class case) is granted by adding its vendored-literal token to the `implement` profile and regenerating, which rewrites `devflow-implement.yml`'s baked `--allowed-tools` baseline **and** `matcher-probe.yml`'s `IMPLEMENT` baseline in lockstep — an implement-only grant leaves the review lock byte-identical. **Never hand-edit either workflow literal** to add such a grant. Grant flows and the `install.sh`-vs-vendor-fetch skew warning: [`docs/cloud-allowlist.md`](docs/cloud-allowlist.md).
- **Every cloud review prompt carries an injected engine-ground-truth block — rendered ONCE by `scripts/render-grounding-block.sh`, never hand-copied.** It states the observed CI conclusions for the reviewed HEAD (from `scripts/summarize-ci-checks.sh`) and the exact resolved `--allowed-tools` string. Both workflows call the renderer and are pinned to carry **no** second copy: it holds security-sensitive prompt-injection prose (check names are attacker-controlled, so a name is *data to quote, never an instruction to obey*), and two drifting copies of an injection defense is the coupled-mirror hazard above at its worst. The block scopes "untrusted" to the **names**; the **conclusions** beside them are API facts — a baseline engine told otherwise refused to cite CI at all and declared its verification phase undischarged.
- **Unknown is not zero — never collapse an unestablished measurement onto a real value.** `permission_denials_count` publishes a digit string, or the literal `unavailable` when the count could not be established (diagnostics disabled/skipped, execution file absent or unparseable). Collapsing both onto `0` made the no-verdict `::error::` assert *"The harness refused 0 command(s)"* about a run it never measured — steering the reader away from the actual cause. `scripts/describe-denial-count.sh` owns the three-way clause selection (unestablished / refused none / refused N) precisely so `lib/test/run.sh` can drive every arm: an inline `case` in YAML is untestable, and a reordered arm misattributes the diagnosis while the suite stays green.
- **A value that decides a SELECTION or an EMITTED result must not be derived through a non-preflight PATH tool.** `lib/preflight.sh` guarantees only `git`/`gh`/`jq`/`python3`/PyYAML — **not** `tr`/`sed`/`wc`/`cut`/`head`. A missing one does not fail: the pipeline runs, the value comes out empty, and the wrong thing is selected. Verified live in this repo: `_publish_denials`' `sed`-based count made a run with 14 denials publish `0` and emit no `::warning::`; a `wc`-derived total made a head *with* CI signals render as "No CI signals reported". Derive such values with bash builtins (`while IFS= read -r`, `case`, `${var#prefix}`). *Cosmetic* sanitization may still use `tr`/`cut` **iff** a missing tool fails CLOSED (an emptied name becomes `(unnamed check)`, never a raw injected one). This is the repo's `.devflow/prompt-extensions/review-and-fix.md` guard-class 2, and it applies to code you are writing right now, not only to code you are reviewing.
- **Partition invariant with Anthropic's Claude app.** Every DevFlow trigger `if:` negates `@claude`, so DevFlow and the stock `claude.yml` never double-fire. Tests in `lib/test/run.sh` enforce this. Never create/overwrite `claude.yml`. Triggers fire on real *comments* only — never issue/PR bodies or titles.
- **Cloud allowlist needs the helper as the command's LEADING token.** No `VAR=value` prefix, no `bash <path>` wrapper — otherwise the read-only `review` profile silently denies it (overrides/telemetry resolve to empty). Invoke `config-get.sh`/`efficiency-trace.sh`/`resolve-review-overrides.py` directly by path.
- **Working-directory contract (summary — canonical page is [`docs/working-directory-contract.md`](docs/working-directory-contract.md), which is the single source; this bullet is non-authoritative).** On the **cloud tiers** the run begins at the `actions/checkout` workspace root and the Bash tool's working directory **persists across calls**, which is why every granted helper literal is repo-relative with no matcher-accepted re-anchored form — so **no DevFlow surface emits a leading `cd`** (a leading `cd` would move every later helper's resolution base out from under it). The permitted alternative is the repo-relative vendored literal as the command's **leading token**, with any absolute path confined to **argument position**. The **local/interactive tier** carries no such guarantee and re-anchors instead — `git rev-parse --show-toplevel` for the six `.devflow/` readers, `BASH_SOURCE` self-anchoring for `scripts/*.sh` helpers. The ban is an **authoring rule** enforced as the `IR4` desk lint (issue #855), **not** a claim that a matcher refuses a `cd` — a leading `cd` was observed executing on the review tier where the grant is already absent. `Bash(cd:*)` is revoked from `devflow_implement.allowed_tools`; a leading `cd` authored into a scanned implement-profile prompt surface (`skills/implement/`, `skills/create-issue/`, `skills/init/`, the `skills/review-and-fix/` bundle) fails the suite via `find_implement_violations`' `IR4` hit.
- **Trigger-time-resolved config is post-merge-only — the grant-timing bootstrap (issue #593, hoisting #591 + #551).** `devflow-implement.yml`'s `config` job checks out the **default branch** at trigger time (a bare `actions/checkout@v6` under the `issue_comment` trigger, no `ref:`) and resolves **every output of its extract step** from that tree (no key count and no full enumeration is carried here — that set accretes and nothing couples a list to the workflow's `outputs:` block, so a transcribed list is the PR-553 stale-count rot class), so a `.devflow/config.json` change to a **trigger-time-resolved** key that a PR ships is in-PR-inert and post-merge-only. Two worked examples: the tool-grant keys, and `workflows.devflow` (enabling the workflow inside a PR cannot take effect for that PR). **Contrast:** keys skills read **at runtime** through `config-get.sh` resolve from the checked-out **working tree** (the #295 repo-root contract) and **are** live in the same implementing run — e.g. `deferred.labels` in implement Phase 4.0 — so the in-PR-inert rule applies only to the trigger-time channel. So a grant added to any checkout-resolved key — `devflow_implement.allowed_tools`, and `devflow.allowed_tools` which `devflow.yml` resolves the same way (its own bare `actions/checkout@v6` plus a `.devflow.allowed_tools // []` jq extraction) — inside a PR is inert during that PR's own implementing run and takes effect only after merge (`devflow_runner.allowed_tools` is likewise in-PR-inert, but via the separate issue-402 trusted-base-ref read). Consequence for issue drafting: an issue draft must **not** place an in-run verification obligation on a grant the same change ships. **Semantic (not literal) mirror sites — sweep and reconcile all three when editing this bullet:** the create-issue extension's Grant-timing-bootstrap evidence axis (`.devflow/prompt-extensions/create-issue.md`), `docs/cloud-setup.md`, and `docs/implement-skill.md`.
- **The reviewer deny-list floor strips tree-mutation tools by NAME — bare AND parameterized — and lives in a suite-driven helper, not inline YAML (#402).** The authoritative floor over `devflow_runner.allowed_tools` is `scripts/filter-runner-tools.sh`, and `devflow-runner.yml`'s review profile executes it **only from a TRUSTED source — never the PR-head checkout** (the PR-#404 REJECT: the review job checks out the PR head, so a checked-out-tree copy — committed vendor dir or `scripts/` — is PR-author-editable, and a floor the PR controls is no floor): rank 1 is the copy `baseprovision` materializes from the base ref (`git show FETCH_HEAD:…` into `RUNNER_TEMP`, inside the fetch-success branch only — FETCH_HEAD elsewhere can be the PR head), rank 2 is the vendored copy gated on the `vendor-plugin` action's `vendor_source` output being `fetch` (fresh official-repo clone at the pinned `devflow_version`; `committed`/`self` never qualify), else the workflow fails **closed** — appends nothing, warns naming the trusted-source rule. Tree-mutation tools (`Edit`/`Write`/`MultiEdit`/`NotebookEdit`) are matched by the tool **name** (the token before the first `(`, compared case-insensitively via bash `nocasematch` — never `tr`, which is a non-preflight PATH tool the SELECTION must not depend on), so `Write(**)` / `Edit(src/**)` / `notebookedit(x)` are stripped exactly like the bare name; the Bash tier is byte-for-byte the pre-#402 command-position-basename check. The `denylisted` jq mirror in `scripts/detect-project-tools.sh` (fast feedback only) carries the same name-before-paren, case-insensitive check — coupled, same-commit. `lib/test/run.sh` drives the helper over the full adversarial matrix, and the workflow→helper call stays covered end-to-end by the `emit_tools` behavioral block. When editing the filter, change the helper + jq mirror + docs (`docs/DEVFLOW_SYSTEM_OVERVIEW.md`) + the run.sh driver together.
- **Vendor to `.devflow/vendor/devflow/`, never `.claude/`.** `claude-code-action` `rm -rf`s sensitive paths (incl. `.claude/`) and restores them from the *base* branch before installing plugins, which would wipe a plugin vendored under `.claude/`.
- **Cloud workflows invoke bundled helpers at the vendored path, never the repo-relative `scripts/` — and `install.sh` ships every relay workflow (#502).** A consumer repo's checked-out tree has no repo-root `scripts/` (the plugin is vendored under `.devflow/vendor/devflow/` by `install.sh` and materialized at runtime by the `vendor-plugin` action), so a workflow step invoking `scripts/foo.sh` hits rc 127 in every consumer run. For the telemetry relay that surfaced as a spurious "deployment fault" `::warning::` on every consumer auto-review: the producer steps in `devflow-runner.yml` ran, but the trusted consumer `telemetry-push.yml` was never shipped (missing from `install.sh`'s copy loop), and the collect step's repo-relative `scripts/collect-staged-telemetry.sh` was absent in consumers. Two coupled obligations: (1) invoke bundled helpers at `.devflow/vendor/devflow/scripts/…` (the `vendor-plugin` step materializes it; mirror the vendored-or-repo fallback the engine-result `parse-engine-error.sh` step uses so a self-repo checkout still finds the repo-root copy); (2) `install.sh`'s workflow copy loop (`for w in …`) must list every workflow in a cross-workflow relay. Both remain covered by ordinary executable assertions in `lib/test/run.sh`.
- **In a `claude-code-action` job, `git push` authenticates with the credential `actions/checkout` persists — NOT the `github_token` passed to the action.** `checkout@v6` writes `http.<server>/.extraheader` into an external config wired in via `includeIf.gitdir:` (covering `.git/worktrees/*`), not into `.git/config`, so the action's `git config --unset-all http.<server>/.extraheader` clears nothing and its `git remote set-url origin https://x-access-token:<app-token>@…` loses to that surviving preemptive `Authorization:` header. Unseeded, every push runs as `github-actions[bot]` (no `workflows` permission): ordinary pushes succeed and ONLY `.github/workflows/` pushes fail — `refusing to allow a GitHub App to create or update workflow … without workflows permission` — which reads as a missing App permission but is not (`devflow-autopilot`, app_id `3102164`, declares `workflows: write`). Every pushing job mints its App token **above** the checkout and seeds `token: ${{ steps.app-token.outputs.token || secrets.GITHUB_TOKEN }}` (`devflow-implement.yml` `claude`, `devflow.yml` `command`, `version-consolidate.yml`); the read-only `DevFlow-Reviewer` token is NEVER a checkout credential (#300). Non-pushing mints (`gate`, `review_dedupe`) hand their token straight to `gh` and stay put. Coupled sites: the two workflow mints ↔ the `checkout-token:` pins in `lib/test/run.sh`. (#357)
- **Single-source-of-truth is the default for new work; the coupled-invariant discipline governs the copies that still exist.** **Go-forward policy (the canonical statement):** when new work introduces a fact, contract, or figure, it lives in exactly one canonical location and every other mention is a pointer, not a copy — and a test asserts behavior and the one canonical statement, not that prose exists in a secondary location. Those universals ("every other mention", "not a copy") are scoped **only to the new fact under introduction and to content migrated to single-source** — they range over no existing copy and make no present-tense claim about the current tree, which still carries byte-identical copies the copy-mandating conventions elsewhere in this file continue to govern (so this policy does not retroactively declare those copies violations). **The coupled-invariant discipline stays a named concept** (cited by anchor from `docs/implement-skill.md`, `skills/implement/phases/phase-2-implement.md`, and `docs/cutovers/`): wherever content *is* physically copied, changing one site means updating every mirror site AND reconciling the asserting tests in the SAME change — never a follow-up commit (editing one half of a two-sided contract and leaving the other stale is the dominant `convention-violation` sub-pattern: the artifacts silently disagree and ship CI-red or desynced for a later shadow pass to catch). A "coupled site" is any value/text more than one file must carry identically: a label literal, a constant, a `SKILL.md` contract pin a `run.sh` grep asserts, a config key name (grep `.py`/`.sh`/`.yml`/jq lines — silent-fail consumers exist), an attribution marker `run.sh` counts, or a self-record (the issue workpad's Plan/AC) that must track what the PR shipped after a revert. Before editing such a value, `git grep` the literal across `.sh`/`.py`/`.jq`/`.yml`/`.md`/`.gitignore` to enumerate every site, change them together, delete in the same commit any `run.sh` assertion on a marker/pin you remove, and update the workpad self-record when a revert changes what shipped. This same-commit reconciliation discipline applies to **each of the two** populations below (permanent exceptions and not-yet-migrated existing copies alike) — the policy changes the default for *new* facts, it does not abolish mirror-maintenance for copies that still exist:
  - **(1) Permanent exceptions** where a physical copy is intended to persist: the vendored skill bodies shipped into consumer repos (`receiving-code-review`, `requesting-code-review`) — including `receiving-code-review`'s restatement of the Phase 4.1.5 behavior-inertness limbs, which cannot point at a DevFlow phase number and so is a sanctioned uncoupled copy — and a short `CLAUDE.md` summary paired with a pointer to a canonical `docs/` page (the `docs/` page is the single source; the summary is non-authoritative). **Generated/compiled artifacts are out of scope entirely** — single-sourced build output, not prose copies: `capability-profiles.json`→the allowlist literals, `cloud_writer_contract.py`→`scripts/devflow-cloud-writer-contract.json` (regenerate the build output in the same commit — `python3 lib/test/cloud_writer_contract.py generate`; it SHA-256-pins every AC1-reached `skills/**`/`scripts/**`/`lib/**` asset it lists, so editing one turns the required `lib + python tests` check RED until regenerated). The Phase 4.3 `--tick-progress` Progress-label ↔ `scripts/workpad.py`'s `_PROGRESS_PHASES` / `_STATUS_TO_PROGRESS_PHASE` source-of-truth is a real coupled pair to edit together.
  - **(2) Existing required-copy sets** that stay under the coupled-invariant same-commit discipline until follow-up migration tickets collapse them — **backlog, not violations**, and this list is **non-exhaustive** (it does not claim to have found every existing copy): the 17-way path-normalization preamble (canonical copy `skills/docs/SKILL.md`), the three-contract config-header mirror (`config-get.sh`/`workpad.py`/`match-lint-adjudications.py`), and the intra-`CLAUDE.md` coupled pairs (e.g. the #366 carve-out ↔ `skills/implement/SKILL.md`).
- **A self-referential ordinal count in a `lib/test/run.sh` comment rots on your OWN edit (PR #553 REJECT).** A comment citing an exact index/count of a same-file thing — "the Nth grep call", "N FAIL arms", "three real sites", a `file:line` ref — drifts the instant an edit in the same change adds/removes an item of that kind. **Routing (post-#797):** a stale ordinal in such a comment is normally behavior-inert prose — judge it per finding under the review engine's Phase 4.1.5 limbs, which fail closed when inertness cannot be established, and note that `lib/test/run.sh` *does* carry tool-read comments (`# structural-pin-ok:`, `# raw-guard-ok:`, `# tree-walk-ok:`, the `#456` NOTE scan) that limb one excludes. Where the limbs hold, the count is capped at Suggestion — no REJECT at the default `verdict_severity_threshold`, and the fix loop still corrects it at this repo's `suggestion` fix threshold. In prose that *can* change behavior it remains a non-demotable REJECT at every threshold. Prefer count-free wording (drop the ordinal), or pin the count so `run.sh` goes RED on drift; after adding/removing a `grep`/arm/site/token, `git grep` for any comment counting that thing and reconcile in the same commit.
- **Guard executable behavior and machine-consumed contracts, never prose presence (issues #375, #666, and #810).** Wrapped adjacent literals can defeat line-based source searches, so a real rendered interface is tested at its executable surface (`--help`, stderr, parser output), not by grepping source wording. A wording-only pin protects a literal that can change without changing executable behavior or a machine-consumed contract; new wording-only, secondary-prose, documentation-presence, advisory-heading, and comment-presence pins are prohibited. Behavioral regressions require ordinary executable tests that exercise the relevant interface or contract and fail when it breaks. A new source-presence pin is allowed only for a typed executable boundary and declares `# structural-pin-ok: <category> -- <non-empty rationale>`, where `<category>` is one of `helper-contract`, `schema-config-vocabulary`, `security-credential-boundary`, `machine-sentinel-provenance`, `routing-dispatch-contract`, `lifecycle-state-transition`, `generated-artifact-identity`, or `cross-file-phase-contract`. The former mutation-taking helpers and wrappers are retired: the issue-#810 gate requires their audited-source census and checked-in inventory to remain empty and fails closed if either cannot be established. It does not execute or interpret mutations, classify mutation effects, or infer assignment dependencies.
  - **Recorded decision (issue #843, generalized to the class by #876): agent-executed prompt prose whose only reader is the runtime agent carries no automated regression coverage, by design — and the compensating control is the review pass itself, not a pin.** The governing question is **whether any tool or consumer reads the content**, never where the content lives: a pin over a prompt surface protecting a **machine-consumed contract** (a marker a tool parses, a routing-table row a module reconciles, a generated-artifact identity) is outside this policy and stays under the parent bullet's `# structural-pin-ok:` rule, while prose no tool reads is inside it wherever it sits. That predicate governs the **existing stock**, not merely new pins — #375/#666/#810 stopped the inflow, and this authorizes draining it (the re-adjudication that makes any particular pin retirable is separate, still-open work; every site in the shipped census is currently adjudicated `boundary`). Each pin's answer is a per-pin adjudication read from `.devflow/logs/pin-corpus-inventory.tsv` (a single-row lookup, never a glob and never a whole-file read) and *changed* in `lib/test/pin-corpus-adjudications.tsv`; that census is a frozen snapshot, so an absent row means unanswered, never "no". Two consequences. **(i) Retirement owes no replacement coverage:** there was never a behavior to cover, so removing such a pin is not a regression to compensate — `CONTRIBUTING.md`'s #798 rule states the disposition, the counted-homes test, and the fail-closed handling of an absent row. **(ii) The gap is narrowed, never closed** — the same honest-claims framing the marketing convention uses; never restate it as coverage. The worked case is the review engine's Phase 4.1.5 behavior-inert prose cap (`skills/review/phases/phase-4-verdict.md`), which shows both edges at once: its **applicability limbs** are read only by the reviewing agent, so nothing executable changes when a limb is deleted and no test can drive one, while its **consequence** is genuinely consumed — the `#291` boundaries assert that the cap's operative sentence survives, that review-and-fix Step 2.6 consumes it, and that it is not re-forked. Deleting a limb is caught only by the merge gate that reads the prose (the standalone `/devflow:review` pass and `/devflow:review-and-fix`'s convergence shadow pass, which re-derive the cap from the shipped text every run). Do not read a green suite as coverage of the limbs, and do not "fix" the absence with a wording pin — the absence is the decision.
- **External binaries resolve through the execution-verified `resolve-*.sh` family — never reintroduce a bare `gh`/`jq` invocation in a `.sh` helper.** `lib/resolve-bin.sh` is the single shared resolver (issue #247): `DEVFLOW_<TOOL>` override wins verbatim and is **never probed** (the test-stub contract); else the first of `<tool>`/`<tool>.exe` whose `--version` runs (network/auth-free); else the bare tool + stderr breadcrumb, always rc 0. `jq` routes through it via sourced `lib/resolve-jq.sh` (`DEVFLOW_JQ`) in every jq-calling helper — `lib/test/run.sh` pins this both ways (a de-converted helper or a reintroduced invocation-position bare `jq` goes RED); `lib/resolve-gh.sh` delegates to it; override derivation for jq/gh is pure bash (no `tr` dependency). `lib/normalize-path.sh` converts Windows-form paths (wslpath → cygpath → env-detect → unchanged+breadcrumb, all failure modes fail closed); the skills carry **no shell mirrors** of it (issue #275) — Windows-form anchors are normalized at prompt time, with create-issue's preamble as the reference prose paraphrase and the shared 17-way preamble (canonical copy `skills/docs/SKILL.md`, identity-pinned by run.sh P4) carrying the same rules — edit lib + create-issue + the canonical copy (re-propagated 17-way) + the T5/T5b/T5c pins together. Partial-copy/unreadable-sibling deployments degrade with breadcrumbs (never an empty `DEVFLOW_JQ`, never a raw set -e abort). `install.sh` alone carries an inline *adaptation* (standalone/curl-piped; a broken `DEVFLOW_JQ` warns and routes to python3 — a deliberate divergence, don't "unify" it).
- **Embedded `jq '…'` programs are inside bash single quotes — keep their `#` comments apostrophe-free ASCII.** `scripts/scaffold-config.sh` carries multi-line jq programs; an ASCII `'` (e.g. `user's`) terminates the bash string (shellcheck **SC1073/SC1011** + test failures), and a curly `'` trips **SC1112** (CI's lint job fails on it). Reword to avoid contractions. Same trap: `startswith`/string ops on a possibly-non-string field abort the *whole* filter — guard with `(.x | strings)` and type-check before indexing (`(.a | type) == "object"`).
- **Editing a best-effort parser (`scaffold-config.sh`, `config-get.sh`, the jq/`config.json` consumers)? Lead with an adversarial input-shape matrix, not the happy path.** These run on configs a human can hand-corrupt, so the bug class is "a shape detonates the filter or yields a misdirected/silent breadcrumb." Enumerate the finite matrix up front — `{top-level, devflow_review, agent_overrides, a value, model}` × `{object, array, scalar, valid-falsy (explicit false / 0 / empty string), missing, wrong-type}` — and add a `run.sh` block asserting *exit-0 + a specific (not generic) breadcrumb* for each. The **valid-falsy** row is load-bearing: a real `false` / `0` / `""` that an `// true` / `// default`-style extraction silently coerces to its truthy default is the documented-off-switch-that-never-worked bug (issue #312, the #304 workflow `// true` coercion). **Inline jq inside a workflow file is a parser this matrix applies to** — not only standalone `.sh` / `.py` / jq / `config.json` consumers; a workflow expression extracting a config value gets the same shape sweep. **The governed surface is broader than config JSON: a parser over agent- or human-mutable markdown (a workpad, a PR body, a `SKILL.md`/`phases/*.md` block a step reads back) and a reader of a new external structured format (a check-run summary, an API response shape the repo does not itself produce) are best-effort parsers too**, each governed by the malformed-shape matrix appropriate to *its* input type — for mutable markdown: missing/duplicate sections and markers, non-canonical layout, empty/truncated input; for an external format: that format's own boundary rows — while the six-shape set above stays the matrix for config-JSON consumers. One deterministic sweep beats finding these one-at-a-time across review iterations. **The shell sibling of that rule: inline shell in a workflow that selects a branch or composes a user-facing message is extracted into a `scripts/*.sh` helper so the suite can drive each branch (and its arm-order) — a grep-pin on a message literal is not coverage of the selection that chooses it, so a branch-selecting `case`/`if` chain left inline is a feature the suite cannot catch defeated.** The reference implementation is `scripts/describe-denial-count.sh` (PR #367): `devflow-review.yml`'s three-clause `finalize_check` selector (unestablished / refused none / refused N) was inline and asserted only by grep-pins on two of its three message literals, so a reordered arm or a glob typo would misattribute the diagnosis while the suite stayed green — the fix extracted it and drove every arm plus arm-order from `lib/test/run.sh`.
- **Adding a guard, predicate, or coverage-invariant? Trace every operand it reads back to its producer and prove the producer emits it on every path the new code now selects — a guard whose comparand can be absent fails open exactly where it claims to fail closed.** This is the recurring `unverified-assumption` bug class: the bot writes a check that *asserts* an invariant but reads a field the upstream writer never guarantees, so the check silently passes on the very inputs it was added to catch. Discipline when hardening: (1) for **every** comparand/field your guard reads, name the exact producer line and the shape it writes; (2) when a new selection/predicate *widens* the set of inputs (a new union arm, a new label/closes-issue path), re-walk every *downstream* derivation those inputs flow into and confirm each still resolves — the gap is usually one path's worth of inputs the older derivation never saw; (3) add a `run.sh` assertion for the absent-operand / newly-selected-but-underived shape, asserting the guard fails *closed* (or the derivation still resolves), not just the happy path. Treat "I asserted X" and "X holds on the inputs that now reach this code" as two different claims — the second needs evidence.
- **`DevFlow` is the reserved provenance label — retrospective detection is label-first, not branch-prefix-first.** DevFlow stamps the literal `DevFlow` label (a hardcoded constant — *the provenance-detection path in `lib/scan.sh` / `lib/classify-pr-kind.jq` matches this exact string and no config key controls it*; `docs.labels` is the *separate* docs-pass label, default `Documented`) on every issue/PR it creates, via the idempotent best-effort `scripts/ensure-label.sh <name>` (always exits 0). `lib/scan.sh` selects a PR via a **union predicate** — (a) the `DevFlow` label (author/branch-agnostic), (b) a watched author that **closes an issue** (`closingIssuesReferences` non-empty — the branch-naming-independent fallback), (c) `implementation_branch_prefix` *only when set non-empty* (empty ≠ match-all). Don't reintroduce a branch-prefix-only predicate; it silently no-ops on any bot whose branches don't match the prefix. **The follow-up issues `/devflow:implement` files for deferred work (Phase 4.0 deferred ACs, Phase 4.0.5 deferred review findings) ARE labeled by a config key — `deferred.labels` (default `DevFlow,Deferred`)** — resolved/normalized/applied by the *skill* (Phase 4.0/4.0.5) exactly like `docs.labels`, never by `file-deferrals.py` (config stays resolver territory for shell/skill callers — read through `config-get.sh`, not re-parsed ad hoc in Python; the one sanctioned exception is a Python helper reading its own key in-process where a `.sh` exec cannot work on Windows, e.g. `workpad.py`'s marker read, issue #275). That key controls only *deferred-issue labeling*; it does **not** touch the hardcoded `DevFlow` provenance string the scan/classify path matches.
- **Retrospective lifecycle records key an OPAQUE filing key; the `category` field, not the key, names the category (issue #891).** `.devflow/learnings/overrides.json` is `schema_version: 3`: every `patterns{}` record carries an explicit `category` string, and status derivation (`lib/compute-patterns.jq`), the per-category filing cap (`lib/filing-decisions.sh`'s `devflow_open_filed_for_category`, which sums `filed` across every record sharing a category) and the run report all read that stored `category` rather than re-deriving it from the key. The **cooldown lookup is the deliberate exception** — it stays keyed on the entry's own opaque filing key (`lib/actionable-patterns.sh`), never on the stored `category`, so cooldown is per-record rather than category-scoped. `lib/pattern-state.sh migrate` stamps it (v2→v3); `lib/meta-issue.sh` takes a required `--category`; `lib/compose-filing-key.sh` composes a collision-resistant ≤40-char `[a-z0-9-]+` key (python3/hashlib digest suffix on overflow), and both it and `compute-patterns.jq` resolve `slugify` from the shared `lib/slugify.jq` module. Canonical reference: [`docs/DEVFLOW_SYSTEM_OVERVIEW.md`](docs/DEVFLOW_SYSTEM_OVERVIEW.md) §12 (this bullet is the non-authoritative summary).
- **Label-create, label-apply, and PR-body edits go through REST `gh api` (repo-scope only), never GraphQL-resolving porcelain.** `gh label create` / `gh issue edit --add-label` / `gh pr edit --add-label`/`--body` resolve the repo via org-scoped GraphQL and fail silently under a repo-scoped token (GitHub App installation token, fine-grained `repo`-only PAT). Use the REST equivalents (`POST repos/{owner}/{repo}/labels`, `POST repos/{owner}/{repo}/issues/{n}/labels`, `PATCH repos/{owner}/{repo}/pulls/{n}`). **On a surface that can run outside Actions — a skill or `phases/*.md` body, a `scripts/`/`lib/` helper — the `gh api` **path argument** uses the `{owner}/{repo}` placeholders `gh` fills from the git remote, and does not interpolate `$GITHUB_REPOSITORY`** (issue #664): that variable is produced by the Actions runner and is *empty* on the local/interactive tier, so the path collapses to `repos//issues/…` — and `gh` writes the HTTP error body to **stdout**, so a best-effort `VAR=$(gh api … 2>/dev/null || true)` capture stores a 404 blob that satisfies the caller's own `[ -n "$VAR" ]` guard instead of leaving it empty. The counter-case is a surface that runs **only** inside Actions and has no remote to resolve from — `.github/workflows/`'s checkout-less jobs (`devflow-review.yml`'s `create_check` runs with no `actions/checkout`) and `.github/actions/` composite actions — where environment addressing is the correct form; those two prefixes are exempt. The enforcing mechanism is `lib/test/lint-gh-api-repo-path.py`, driven from `lib/test/run.sh`, which audits the tracked-and-unignored files outside that exempt set **and** outside its own documented exclusion set (`lib/test/`, `docs/`, `.devflow/logs/`, `.devflow/learnings/`, `.changeset/`, `CHANGELOG.md`, `.claude/worktrees/` — the prose surfaces that carry the rule's own statement text, the machine-appended corpora that quote it, and — since this scanner's population is a *working-tree* enumeration — the sibling worktrees whose files belong to another branch's checkout (issue #711); see the module docstring for each one's reason), and turns the suite RED at the desk. **The rule is scoped to the `gh api` path argument, not to every mention of the variable:** a repo string reached through an assignment hop or a flag value still carries it — `scripts/react-to-trigger.sh` composes `repos/$repo/…` from the `--repo` value the implement fence passes as `$GITHUB_REPOSITORY`, and fails closed (a warning, exit 0, no POST) when that value is empty. Those indirections are accepted residuals the scanner cannot see, named in its docstring; do not read the rule as a claim that no outside-Actions surface mentions the variable at all. `scripts/apply-labels.sh <number> <label…|comma-list>` is the **single hardened label-apply path** every call site routes through (mirrors `scripts/ensure-label.sh`'s best-effort, always-exit-0, stderr-breadcrumb contract; `ensure-label.sh` does REST label *creation*). Don't reintroduce porcelain in a new call site (issue #228).
- **The bash that RUNS the `.sh` helpers is chosen at the INVOCATION boundary, honored via `DEVFLOW_BASH` — deliberately NOT a `resolve-*.sh` helper (issue #248).** Unlike `gh`/`jq` (tools a *running* bash calls, chosen by a sourced resolver), the shell that *runs* the scripts is picked before any `.sh` executes, so a sourced resolver can't select it (bootstrap: it would itself need a chosen bash to run). DevFlow supports **any** POSIX bash — WSL bash / Git Bash / MSYS2 bash, none mandated — and `DEVFLOW_BASH` is the invocation-layer override the agent/runner honors to point at one. `lib/preflight.sh` is a **diagnostic, not a selector**: it emits a `devflow-bash:` breadcrumb (interpreter path + `$BASH_VERSION`), surfaces `DEVFLOW_BASH` when set, and — when `$BASH_VERSION` is empty (a non-bash POSIX shell) — prints a remedy naming the three supported bashes + the override and **exits 1 BEFORE the first bash-only `${BASH_SOURCE[0]}`** (fail-closed; it must stay above that line). Non-goal: a host with **no** POSIX bash at all (PowerShell-only) is out of scope. `lib/test/run.sh` guards the breadcrumb and remedy through the `#248` source-level recovery contract and drives the non-bash remedy under a real non-bash shell when one is present (`dash`/`busybox`); after #809 it carries no wording-only fallback pins for bash-only hosts (`lib/test/` remains excluded from CI shellcheck). Edit the preflight block, the `#248` test block, and `docs/install.md`/`README.md` together.
- **DevFlow's `gh` binary is chosen by ONE execution-verified resolver, `lib/resolve-gh.sh` (`devflow_resolve_gh`) — the sibling of `lib/resolve-python.sh`; never hardcode a bare `gh` default.** It honors a non-empty `DEVFLOW_GH` (top precedence, echoed with no probe — the probe runs only when `DEVFLOW_GH` is unset or empty (`:=` fires on both), so the test-suite's `DEVFLOW_GH` stubs are untouched), else returns the first of `gh`, `gh.exe` whose network/auth-free `gh --version` **runs** (rejecting a present-but-unrunnable Windows/WSL shim — the shadowing-`gh` bug), else bare `gh` (rc 0, so the caller's best-effort warning path still fires). Every gh-calling shell helper sources it and assigns via `: "${DEVFLOW_GH:=$(devflow_resolve_gh)}"`; `lib/preflight.sh` sources the same resolver but deliberately does **not** use that `:=` form — it DETECTS only (`_GH="$(devflow_resolve_gh)"`, re-probed, never assigned back to `DEVFLOW_GH`), consistent with `docs/DEVFLOW_SYSTEM_OVERVIEW.md`'s "preflight only detects" contract; the Python gh-callers (`workpad.py`, `file-deferrals.py`, `match-deferrals.py`, `parse-acs.py`, `export-workflow-lifecycle-census.py`, `build-experiment-records.py`, `check-completion-evidence.py`, `preflight.py`, …) read `os.environ.get("DEVFLOW_GH") or "gh"` (default `gh`) but deliberately do **not** run the probe. `DEVFLOW_GH` is the documented Windows/WSL escape hatch (`docs/install.md`). Adding a new gh-caller? Route it through the resolver — `lib/test/run.sh` pins "no bare `DEVFLOW_GH:=gh` default remains" **and** ">=13 helpers source `resolve-gh.sh`" (issue #245).
- **The workpad lives on the ISSUE, not the PR thread.** `lib/fetch-pr-context.sh` reads the `<!-- devflow:workpad -->` comment (and its `## Devflow Reflection` bullets → `reflections[]`, and final Status → `workpad_final_status`) from the **issue** comments — detect it by marker, never by author (it's authored by github-actions). Reading it from the PR conversation thread (the old bug) leaves those fields ~always empty and makes `cheap-gate.jq`'s workpad/reflection checks inert. Strip the status glyph (🚀/🎉/👎/💥/🛑) to the bare word (`🎉 Complete` → `Complete`) — 💥 `Failed` (the stall-backstop dead-run flip, #356) and 🛑 `Cancelled` (the stall-backstop cancelled-run flip, #498) are the workflow-written statuses; like any non-`Complete` value they gate non-clean in `cheap-gate.jq`; a non-empty `reflections[]` forces LLM analysis even when every other signal is clean.
- **Versioning is repo-policy, kept OUT of the engine — and DevFlow versions itself with changesets (issue #290).** `/devflow:implement` is versioning-agnostic; DevFlow manages its own version through its consumer prompt extension `.devflow/prompt-extensions/implement.md` — **edit that extension, not the skill**, to change the rule. A PR touching the engine surface (`skills/`, `agents/`, `lib/`, `scripts/`, workflows, config schema) adds a uniquely-named `.changeset/*.md` file (a `bump: patch|minor|major` frontmatter key + Keep-a-Changelog prose, PR-cited) and does **NOT** edit `.claude-plugin/plugin.json` or `CHANGELOG.md` directly — those two shared hot spots caused a version/CHANGELOG merge conflict on every concurrent PR. The Phase 3 review gate now FAILs an engine-surface change that carries **no** changeset (it replaces the old version↔`CHANGELOG` presence check); internal-only changes (tests, CI, dev-only docs) add no changeset; the default `bump` is **`patch`** (`minor`/`major` only when the issue explicitly authorizes it). On merge to `main`, the `version-consolidate` workflow runs `scripts/consolidate-changesets.py`: it bumps `plugin.json` by the **highest** pending bump, prepends one dated CHANGELOG entry from all pending prose, deletes the consumed changesets, and commits with the **load-bearing `chore: bump version`** subject (a coupled invariant with `skills/docs-release-notes/SKILL.md` Step 4b — the producer is now that workflow, not an in-PR commit; both pinned in `lib/test/run.sh`). **The workflow YAML lives at `.github/workflows/version-consolidate.yml`.** Editing any `.github/workflows/` file needs a `workflows`-scoped push — the App HAS that scope; what was broken is which credential reached `git push` (see the checkout-token gotcha above, #357). Until #357 is deployed, workflow changes still land via a human/PAT; and whoever changes one must ensure `main` branch protection permits the job's `contents: write` bump commit. A malformed changeset fails the Action loudly (naming the file); zero pending is a clean no-op. The consolidator makes no `git` calls (the workflow stages/commits) so it is unit-tested directly in `lib/test/run.sh`.
- **Review-outcome analysis sources verdicts from the IMMUTABLE bot PR reviews, never the progress comments.** The run-keyed `devflow:review-progress` comment is edited in place (superseded REJECTs vanish); the bot-posted PR review is the durable verdict record — but its body is a stub ("full report in PR comment"), so findings/severities live in the progress comment, joined via `review.commit_id` = the comment's "Reviewed HEAD:" line (the engine's own fast-path join). Sequencing analyses over comments silently lose review rounds.
- **`agent_overrides` model/effort values reach the standalone `/devflow:review` — the merge-gating judge — not just the fix loop.** The engine is shared, so "cheapen a reviewer agent" changes what reviews every PR before merge. During any measurement/experiment window, treat the judge's roster economics as frozen: dispatch-scoping keys are judge-neutral (standalone review is single-pass), model repricing is not (see issue #425's deferred-repricing record).

## Conventions

- **Portability:** no GNU-only flags. Use `python3` for date math (not `date -d`) and ERE / `sed -E` (not `grep -P`). Helpers must work on macOS/BSD without GNU coreutils.
- **No secrets, owner-specific IDs, or product names in committed files.**
- New `.py`/`.sh` files carry the SPDX header (`SPDX-FileCopyrightText: 2026 Daniel Radman` / `SPDX-License-Identifier: MIT`).
- Docs reference bare source paths (`scripts/workpad.py`), never `path:line` — line numbers rot.
- **Prefer generated evidence over exact checked-in numbers — EXCEPT where an exact literal is itself the enforcement (issue #656).** A figure a reader treats as *current* rots silently the moment the measured thing changes and the copy does not, so render it live and assert it positionally, never hand-transcribe it. Steelmanned exception, so the rule never demands a vacuous or impossible guard: a literal stays checked-in when it *is* the enforcement — a **past-time snapshot** (unre-derivable history), an **enforcement constant** (a ceiling/threshold the check compares against — prefer *pinning* it), or a **future target** (a pin would trap the change that moves it). Register each exempt literal with a one-line rationale; a frozen figure keeps its provenance and is never machine-rendered, which would overwrite the snapshot and falsify the record.
- **A suite assertion or lint helper that enumerates the repository tree sources its population from an index-reading `git ls-files` — with no `--others` — never a repository-root-anchored recursive walk (issue #711).** A root-anchored walk descends into every sibling git worktree under `.claude/worktrees/` (this repo's own working mode — `EnterWorktree` puts every worktree it makes there), so the check counts those checkouts' copies and goes red locally with a number that varies between runs on the same commit, while CI — a fresh `actions/checkout` with no worktrees — stays green. A **working-tree** enumeration (`--others`, even with `--exclude-standard`) is **not automatically** worktree-immune either: when its pathspec can reach sibling worktrees, its protection comes from an untracked `.git/info/exclude` line that no clone inherits, so a helper that needs one carries its own `.claude/worktrees/` exclusion. A closed, non-recursive leaf pathspec that cannot reach that subtree is the narrow exception: issue #810's pin gate enumerates untracked `lib/test/test_*.py` leaves so a newly authored Python wording pin enters the same diff gate before its file is staged. The enforcing mechanism is `lib/test/lint-tree-enumeration.py`, which turns the suite RED for an **undeclared** recursive walk in a tracked `lib/test/` `.py`/`.sh` file — a Python `rglob`/`os.walk`/`iglob`/`recursive=True`/`**`-or-non-literal-pattern `glob`, or a shell `find`/`grep -r` **rooted at the repository root** — carrying no `# tree-walk-ok: <reason>` marker, the third member of the declaration-marker family alongside `# raw-guard-ok:` and `# structural-pin-ok:`. It is deliberately not exhaustive over every possible walk: the guard's own path is excluded, and its module docstring closes the candidate-token set and the accepted residuals by enumeration, each with its own reason — read it there rather than restating a scope here that would drift as residuals accrete. It does not bar a walk; it makes one a reviewable, greppable declaration.
- **Updating `CLAUDE.md`? Use `/claude-md-management:revise-claude-md` (targeted additions) or `/claude-md-management:claude-md-improver` (full audit) — never hand-edit without running at least one.**
- **Autonomous `/devflow:implement` runs make a *required* `CLAUDE.md` edit directly — carve-out to the rule above (issue #366).** Any `CLAUDE.md` edit an autonomous DevFlow run is *required* to make — whether by a Phase-3 review finding **or by the issue's own acceptance criteria** — is made **directly by the orchestrator**, citing this carve-out and recording it in the workpad, **never** by invoking `revise-claude-md`/`claude-md-improver` mid-run (a nested interactive skill runs as a tail call and stalls the run — the defect issue #366 fixes). Interactive/human sessions still use `revise-claude-md`/`claude-md-improver` per the bullet above. Other interactive skills a run is required to invoke are instead dispatched into an **Agent-tool subagent** (issue #362) — see `skills/implement/SKILL.md`. This bullet is the `CLAUDE.md` half of a coupled pair — its mirror is the `CLAUDE.md` edit carve-out in `skills/implement/SKILL.md`; edit both together.
- **Retiring duplicate-prose pins:** an existence-only pin whose census row reads `counted_occurrences >= 2` is removed only together with at least one copy; one reading `< 2` **and** adjudicated into a prose bucket may be removed on its own (issue #876) — every other case, and an absent row, retains. See `CONTRIBUTING.md` under **Retiring existence-only pins** for the canonical ordered arms; note that no site is adjudicated into a prose bucket today, so the pin-only arm currently selects nothing.
- **Editing any skill file (`SKILL.md`)? ALWAYS invoke the `writing-skills` skill first** (`superpowers:writing-skills`) and follow its RED/GREEN discipline — never hand-edit a `SKILL.md` body directly. This applies to every `skills/*/SKILL.md` (the shared review engine, the docs family, retrospective, etc.); the skill enforces the verify-before-deploy loop that keeps a skill's triggering + behavior intact. **Autonomous `/devflow:implement` runs satisfy this mandate differently:** they never invoke `writing-skills` through the Skill tool mid-phase (a tail call that would stall the run), so the implement prompt extension (`.devflow/prompt-extensions/implement.md`'s "Prompt-surface edit routing" rule) routes each prompt-surface edit through a context-isolated Agent-tool subagent that invokes `superpowers:writing-skills`, recording a `Writing-skills evidence:` marker in the workpad (with the degraded inline mode noted when the skill is unavailable). Interactive/human sessions keep invoking the skill directly **and record a `Writing-skills evidence:` line in the PR description** (the review gate's discharge surface for human PRs). This bullet and that extension routing rule are a coupled pair — edit both together.
- **Helper cutover deletes the superseded prose — same change.** When an executable helper becomes the sole tested owner of a workflow decision on every path that previously consumed the prose, that same change removes the superseded mandatory prose, the duplicated branch/enum mirrors, and the obsolete prose pins — re-anchoring each removed pin's guarded regression to a helper-behavior test. Policy, human decision points, invocation contracts (including fail-closed handling of the helper's absence), and essential stop conditions stay in the skill; rare-path explanation moves to progressively loaded references — operative decision logic never does. Removing decision-owning mandatory prose whose decision has no sole tested owner is unauthorized. Related advisory guidance: `.devflow/prompt-extensions/implement.md` "Keeping prompt prose lean" (advisory — keep prompt prose lean; no length gate).
- **Marketing/positioning copy** (`README.md`, `docs/DEVFLOW_SYSTEM_OVERVIEW.md`) leads with the wedge — *out-of-the-box agentic coding works on pet projects but can't complete a real ticket in a large production codebase; DevFlow makes it ship*. Honor the honest-claims guardrails (review-ready, **not** auto-merged; shadow review **narrows, never closes** the gap). §21 is the messaging source of truth.
- Don't push to `main` from an agent; the classifier blocks `git push origin main`. Inside `/devflow:implement` & `/devflow:review-and-fix` there is standing permission to auto-commit+push to the *feature* branch.
- **Local-tier classifier blocks shell-script invocation — this is expected; use the documented fallbacks, don't fight it.** On the local/interactive tier the permission classifier routinely denies `bash <path>`, `shellcheck`, `ruff`, and helper scripts invoked by path (the recurring friction behind retrospective pattern `convention-violation`, PRs #72/#85/#87/#98). It is a **structural harness boundary**: an agent is barred from writing `.claude/` to widen its own allowlist (`docs/efficiency-trace.md` "Note on this repo's `.claude/settings.json`"), so the run cannot self-grant the missing permission — the fix is operator-side, not in-run. When blocked, fall back **without re-deriving it each run**: run helpers via `python3 <path>` (the classifier permits Python by path) and `jq`; rely on the required CI job `lib + python tests` to run `bash lib/test/run.sh` + lint on push (verify it is green before merge); for the workpad use `gh api` PATCH if `workpad.py` is denied. To actually widen the allowlist, a **human** adds the rules at user scope (`~/.claude/settings.json`) or runs the consent-gated `scripts/provision-auto-mode.sh` / `scripts/provision-local-settings.sh` — never commit a `permissions.allow` block to this repo's `.claude/settings.json` from an agent (the classifier denies it, and the cloud tier discards `.claude/` anyway).

---
> Source: [The01Geek/devflow-autopilot](https://github.com/The01Geek/devflow-autopilot) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-07-29 -->
