## streamtospeaker

> 1. **Research, don't speculate.** When the user reports a problem you don't fully understand (especially Windows / driver / OS-level), reach for `WebSearch` and `WebFetch` *before* proposing fixes. The user has explicitly called out cases where speculation wasted iteration cycles — examples: the "Internal AUX Jack" label, the "Allow apps and Windows to use this device" toggle (which turned out to be the documented `PKEY_AudioDevice_EnableEndpointByDefault` behaviour, not anything I'd guessed). If the user says "are you sure?", that's a signal to actually look it up.

# CLAUDE.md — instructions for future Claude Code sessions

## Operating principles

1. **Research, don't speculate.** When the user reports a problem you don't fully understand (especially Windows / driver / OS-level), reach for `WebSearch` and `WebFetch` *before* proposing fixes. The user has explicitly called out cases where speculation wasted iteration cycles — examples: the "Internal AUX Jack" label, the "Allow apps and Windows to use this device" toggle (which turned out to be the documented `PKEY_AudioDevice_EnableEndpointByDefault` behaviour, not anything I'd guessed). If the user says "are you sure?", that's a signal to actually look it up.

2. **Verify with cargo check before pushing.** This is a Linux container; build the service with `cd service && cargo check && cargo check --target x86_64-pc-windows-gnu` after any service edit. The Windows cross-check catches GUI / tray / IOCTL issues that the Linux check skips via `cfg(windows)`. Driver edits can't be syntax-checked here (need WDK) — read carefully and trust the user / CI.

3. **PR-per-merge workflow.** This session pushes to a single fixed branch (`claude/stream-speaker-audio-delivery-Utgpx`), and the user merges each PR as a unit. After every push, check `mcp__github__list_pull_requests` for an open PR matching the branch; if none (previous one merged), open a new one via `mcp__github__create_pull_request` with a focused title describing the new commits. Don't let the branch accumulate too many merged-then-orphaned commits without a fresh PR.

4. **Don't claim "fixed" without verifying.** When a fix lands and the user reports it still doesn't work, the first move is `WebSearch` for what *actually* triggers the symptom — not another guess.

## Project: Stream To Speaker

Windows virtual audio device that streams system audio to network speakers — UPnP/OpenHome (Sonos primarily) **and** AirPlay (RAOP / AirPlay 1, plus AirPlay 2 / HomeKit for HomePod). Two parts:

- **`driver/`** — C++ kernel-mode WaveRT/PortCls driver. Single render endpoint, fixed L16 44.1 kHz stereo. Inverted-call IOCTL pattern delivers audio frames to user mode. Includes a separate non-PnP control device for the IOCTLs.
- **`service/`** — Rust user-mode bridge. Default mode is GUI (egui window + tray icon); `--headless` is the CLI mode; `--web` enables the HTTP/JSON API. Talks to the driver via IOCTLs. Discovers speakers via SSDP (UPnP) and mDNS (`_raop._tcp` / `_airplay._tcp`); controls UPnP via SOAP and AirPlay via RTSP; streams PCM either by serving HTTP `audio/wav` (UPnP pull) or pushing RTP/UDP (AirPlay, ALAC). AirPlay code lives in `service/src/airplay/`.

Shared ABI lives in `include/stream_to_speaker_ioctl.h`. Any change to the on-the-wire layout has to be mirrored in `service/src/ioctl_source.rs`.

## Driver versioning

INF DriverVer format: `MAJOR.MINOR.BUILD.REVISION`. Two pieces, two sources:

### Auto: revision (`REVISION` = git commit count)

Bumped on every CI run and every `build-installer.ps1` run, no manual step. Drives:

1. **INF `DriverVer`** (e.g. `1.0.0.42`) — what Windows uses for PnP version comparison and what Device Manager → Properties → Driver shows. From `StreamToSpeaker.vcxproj`: `<TimeStamp>$(DriverVersionPrefix).$(DriverBuildNumber)</TimeStamp>`, with `DriverBuildNumber` injected via `/p:DriverBuildNumber=N`.
2. **`STREAM_TO_SPEAKER_DRIVER_BUILD` in `driver/driver.h`** — runtime identifier, returned via `IOCTL_STREAM_TO_SPEAKER_GET_VERSION`, logged by the service (`StreamToSpeaker driver opened (proto=1 build=N ...)`).

Both get the same N every build, so `1.0.0.42` in Device Manager == `build=42` in the service log == same `.sys` binary. Override with `-DriverBuild N` on the local script for a reproducible value.

**CI driver cache (build.yml):** the kernel-mode driver is cached and only rebuilt when its *source* changes (cache keyed on `hashFiles('driver/**','include/**')`); a hit skips the WDK, the signing-cert generation, and the MSBuild. So `N` is no longer bumped on *every* CI run — it's the commit count at the **last commit that changed the driver**, stamped into the `.sys`/`.inf` when they were built. Still strictly monotonic across driver changes (PnP upgrades unaffected), and `build=N` still uniquely identifies the `.sys` bits — it just stops advancing on service-only commits. The cache stores the `.cer` *alongside* the `.sys`/`.cat`, so the cert the installer ships always matches the cached binaries (a freshly-generated cert per run would not). `build-installer.ps1` (local) is unchanged — still bumps every run. Bump the `v1` tag in the cache key to force a rebuild after changing build flags without touching driver source.

### Manual: prefix (`MAJOR.MINOR.BUILD`)

`DriverVersionPrefix` in `driver/StreamToSpeaker.vcxproj` — defaults to `1.0.0`. **Bump it by hand when the change is significant.** Semver-ish discipline:

- **MAJOR (`1.0.0 → 2.0.0`)** — breaking changes that aren't safe to roll back without uninstalling first. IOCTL ABI changes (modifying `stream_to_speaker_ioctl.h`), changing the device hardware-ID, changing PortCls→WaveRT to a different audio class, etc.
- **MINOR (`1.0.0 → 1.1.0`)** — new shipped features. Adding AirPlay support, adding alternative codecs, adding a new control device interface, adding a new PKEY in the INF.
- **BUILD (`1.0.0 → 1.0.1`)** — meaningful bug fixes worth flagging in the version string. The kind of thing a user grepping `1.0.X` would care about. (Note: this is the *third* INF field — not the auto-bumped revision.)

**Also bump `service/Cargo.toml`'s `version =` to match the prefix** when bumping MAJOR or MINOR — the service version shows up in `--version`, in `User-Agent` headers we send to speakers, and is what GitHub Releases use as the tag. (Patch-level disagreements between service and driver are fine; what matters is that the *prefix* tells the same story.)

Don't bump the prefix for routine fixes — the auto-bumped revision is the right granularity for "the bits changed, no semantic difference." Save prefix bumps for "the user should know something changed."

## Install / upgrade flow

End users install via `StreamToSpeakerSetup-<version>.exe` (produced by CI). The installer:

1. Imports our test-signing cert to TrustedPublisher + Root
2. **Cleans up any prior install** — `Pre-Install.ps1` removes the live device (`devcon remove`), unstages the old driver (`pnputil /delete-driver`), wipes cached `HKLM\…\MMDevices\Audio\Render\<id>` entries that would otherwise keep the old INF properties live
3. `pnputil /add-driver` stages the new driver
4. `devcon install` creates the root-enumerated device — `pnputil /install` alone does not for root-enumerated devices
5. `Rename-Endpoint.ps1` overwrites the cached friendly name + flips `DeviceState = ACTIVE` and restarts `AudioEndpointBuilder` so changes take effect without sign-out

User-machine prerequisites that aren't automatable:
- Test-signing mode (`bcdedit /set testsigning on` + reboot) — we ship a test-signed driver, not WHQL
- Secure Boot off, HVCI / Memory Integrity off — required for test-signed drivers to load
- Windows 10 1809+ (the INF targets `NTamd64.10.0...17763`)
- Click "Allow" once in Sound Settings for the per-device privacy gate (Windows 11 22H2+ only) — addressed by `PKEY_AudioDevice_EnableEndpointByDefault` in the INF, but pre-existing endpoints created without that property still need the manual click

## Key knowledge — Windows-audio specifics learned the hard way

| Symptom | Real cause | Fix |
|---|---|---|
| Device label is "Internal AUX Jack" | Windows derives the prefix from `KSNODETYPE_LINE_CONNECTOR` association | Use `KSNODETYPE_SPEAKER` in INF + `PKEY_Device_FriendlyName` to override cached name |
| New install shows old endpoint name / state | `HKLM\…\MMDevices\Audio\Render\<id>` is cached per endpoint ID and survives reinstalls | `Reset-Install.ps1` wipes the cached entries; the installer now runs the cleanup automatically |
| Device installed but no Sound Settings endpoint | `pnputil /add-driver /install` doesn't create root-enumerated devices | Use `devcon install <inf> Root\<HardwareId>` after pnputil |
| "Allow apps and Windows to use this device" toggle | Endpoint builder creates certain KSNODETYPE / form-factor combinations as disabled+hidden by default | `PKEY_AudioDevice_EnableEndpointByDefault = 0x101` (FLAG_ENABLE \| FLOW_MASK_RENDER) in INF |
| OEM APO (Dolby Atmos etc.) attaches and adds latency | FormFactor=1 (Speakers) is the default match for OEM APOs | `PKEY_AudioEndpoint_Disable_SysFx = 1` in INF |
| BSOD 0x9F sub-code 3 | A device our driver owns failed to complete an `IRP_MJ_POWER` in time | Don't just override `MJ_DEVICE_CONTROL` / `CREATE` / `CLOSE`; also own `MJ_POWER` and route by device — our control device needs to complete with `STATUS_SUCCESS` + `PoStartNextPowerIrp`, audio FDO forwards to PortCls |
| Driver loaded but `build=N` shows old N in service log | `pnputil /add-driver` only stages; running kernel keeps old `.sys` | Device Manager → device → Disable/Enable, or reboot |

## Debugging recipes

- **BSOD**: minidumps at `C:\Windows\Minidump\*.dmp`. Parse the header in Python:
  ```python
  with open(dump, 'rb') as f: h = f.read(0x400)
  bugcheck = struct.unpack_from('<I', h, 0x38)[0]
  params = [struct.unpack_from('<Q', h, 0x40+i*8)[0] for i in range(4)]
  ```
- **What's in the driver store**: `pnputil /enum-drivers | Select-String "StreamToSpeaker" -Context 1,8`
- **Is the device alive**: `devcon status Root\StreamToSpeaker`
- **Why did PnP refuse**: `Get-Content C:\Windows\INF\setupapi.dev.log -Tail 200 | Select-String StreamToSpeaker -Context 2,5`
- **Audio engine state**: `mmsys.cpl` shows all endpoints incl. disabled / hidden ones
- **Driver DPC running?**: kernel `DBG_INFO` in `wavestream.cpp:DoCopyToRing` logs every ~1 s; capture with DebugView (run as admin, enable Kernel capture)

## CI workflow

`.github/workflows/build.yml` runs on **`windows-2022` (pinned)** for every push, PR, and tag. Do NOT move it back to `windows-latest`: that label migrated to Windows Server 2025 + VS 2026 (MSBuild 18) in June 2026, which can't load WDK 26100's MSBuild-17 task DLLs (`MSB4062: ValidateNTTargetVersion … Microsoft.DriverKit.Build.Tasks.18.0.dll not found`). Revisit only with an EWDK or a VS2026-matched WDK. Related war story: stale driver build outputs from a dev machine were once *tracked in git* (`driver/x64/**`, committed before `.gitignore` covered it) — after an msbuild failure, CI's artifact scan happily picked up the stale `.sys` (signed by the dev's personal `WDKTestCert`, mismatching the CI-generated `.cer`). They're untracked now and the build step fails fast on msbuild's exit code; if a `.sys` ever shows a `WDKTestCert <username>` signer in CI logs, something is leaking dev-machine bits again. Steps:

1. Install Inno Setup (chocolatey)
2. Restore / install WDK (cached — see workflow for keys)
3. Generate self-signed code-signing cert, import to local trust stores
4. **Bump driver build to git commit count**
5. Build driver with `SignMode=TestSign`
6. Build service (`cargo build --release`)
7. Stage artifacts into `installer/staging/` (sys, inf, cat, devcon.exe, cert)
8. Run `ISCC.exe` to produce `installer/out/StreamToSpeakerSetup-<version>.exe`
9. Upload `.exe` + raw binaries as artifacts
10. On `v*` tag push: the release chain (below) attaches the signed `.exe` to the GitHub Release

WDK install (~5 min cold) is cached; warm cache restores in ~30 s. Rust deps are cached via `Swatinem/rust-cache@v2` (NOT a plain `actions/cache` on `target/` — that goes permanently stale on every 6-week toolchain bump because `actions/cache` never re-saves on a key hit; rust-cache keys on rustc version + lockfile and prunes our own crate from the saved target dir). Checkout is `fetch-depth: 0` — the driver build number is `git rev-list --count HEAD`, which silently evaluates to **1** on the default depth-1 shallow clone (this bit us: CI drivers were stamped `<prefix>.1` until 2026-08).

### Signing & release pipeline (2026-08)

Full runbook: `docs/driver-signing.md`. Design: all EV signatures go through the central approval-gated signing repo (`Mihonarium/signing`, pull-by-public-URL + human approval binding a SHA-256, dispatched/polled by the composite action `.github/actions/request-signing`); the kernel driver additionally goes through **Microsoft attestation signing** (Partner Center), because Windows 10+ only loads Microsoft-signed kernel code — an EV signature on the `.sys` alone is worthless at load time. Attestation-signed drivers load on Win10 1809+/Win11 desktop incl. Secure Boot, but NOT Windows Server, and can't be published to Windows Update (fine — we ship via GitHub releases).

- **`driver-submission.yml`** — on `driver/**`/`include/**` pushes to main: builds the driver **unsigned** (`SignMode=Off`), packs inf+sys+pdb(+cat) into a Partner Center submission CAB (files under a `StreamToSpeaker\` subdir, never CAB root), creates prerelease `driver-v<ver>` with a `manifest.json` recording `source_hash = hashFiles('driver/**','include/**')`, EV-signs the CAB (1 approval). Human: submit CAB at Partner Center, attach the returned zip to the same release.
- **`driver-attested.yml`** — manual: verifies the returned zip (`signtool verify /kp` cat+embedded, signer must be *Microsoft Windows Hardware Compatibility Publisher*; INF byte-identical to submission; DriverVer matches), publishes canonical `StreamToSpeaker-Driver-<ver>-Signed.zip`, flips manifest `attested: true`. Unattested prereleases superseded by a newer driver build get auto-deleted; attested ones are kept forever.
- **`build.yml` main pushes** — if an attested driver's `source_hash` matches HEAD's driver source (service-only commits keep matching), also builds a `-attested` installer artifact: attested driver package, NO `.cer` staged (the .iss cert-import steps are gated on the `.cer` existing, so the same script serves both variants).
- **`build.yml` tag pushes** — job chain `build → sign-service → package → sign-release`: EV-sign the service exe (approval 1), build the release installer around the signed exe + attested driver (falls back to test-signed + `-testsigned` asset suffix when no attested driver matches), EV-sign the setup exe (approval 2). Release ends up with signed setup exe, signed service exe, the Microsoft-signed driver zip, and `.sha256` sidecars.
- One-time env note: the `request-signing` environment's deployment policy must allow **branch `main`** (sign-cab) in addition to tags `v*`.

## Future work — explicitly deferred

The user mentioned these but asked NOT to start them now. Pick up when they say:

- **AirPlay — GROUND TRUTH (v3, 2026-07-03, after a 13-agent adversarially-verified capture/code/reference audit; captures: `itunes_full.pcapng` = iTunes-for-Windows 12.13.10 → Sonos SYMFONISK "Picture frame" fw `AirTunes/366.0` TXT `et=0,4 cn=0,1 sf=0x4`, WORKING+AUDIBLE; `ours_full.pcapng` = our old build, stalled):** ⚠️ **CORRECTION-OF-THE-CORRECTION — the v2 claim "encryption is the SETUP gate / plaintext is not enough" was WRONG, a confound.** What's actually established:
    - **iTunes does use et=4 MFi encryption** (`a=mfiaeskey` wrapped key + `a=aesiv` in ANNOUNCE, AES-128-CBC audio) — that part of v2 holds, and `mfiaeskey` is provably NOT the raw audio key (decrypting captured audio with it = garbage in every cipher mode). **But a wrong-or-missing mfiaeskey CANNOT hard-fail the handshake**: a 16-byte wrap of a 16-byte key has no integrity tag, so the receiver can't validate it before audio flows. Wrong wrap predicts connected-but-SILENT, never cannot-connect. (Verified: our MFi ANNOUNCE body is byte-shape identical to iTunes's 252-byte body.)
    - **The old "plaintext ANNOUNCE 200 → SETUP stall" evidence was confounded THREE ways**: that session had (a) a 64-bit session id (iTunes/working senders use u32), (b) 264 unanswered NTP timing requests — **the Sonos probes our SETUP-advertised timing port DURING SETUP, before sending its SETUP 200** (iTunes answers each within 200 µs; its 200 lands 17 ms after the third reply; libraop's comment: "AppleTV expects now the timing port ot be opened BEFORE the setup message") — our responder only spawned after RECORD, in every build up to 2026-07-03; and (c) no mfiaeskey. Any of the three could have stalled SETUP.
    - **Half-open session hold (capture-PROVEN, the "can't connect" mechanism):** after one stalled SETUP with no TEARDOWN, this Sonos returns **zero application bytes on ANY new TCP connection for tens of seconds** (our capture: the AP2 fallback's GET /info + pair-setup both got ACKed but never answered for 10+ s; the hold survives the client's FIN). One bad attempt poisons every immediate retry — a failed experimental handshake reads as "speaker permanently broken" to the user.
    - **Plaintext-after-throwaway-auth-setup IS the field-proven recipe** for et=0,4 receivers: OwnTone (`et=4 → supports_auth_setup`, streams plaintext unless `ek=1`) and libraop/AirConnect (`if (strchr(p->et,'4')) rtspcl_auth_setup(...)` then plaintext/RSA ANNOUNCE) both work with Sonos in the wild. No open-source sender implements et=4 encryption.
    - **iTunes sends REAL COMPRESSED ALAC** (payloads 311–999 B variable, mean ~693), NOT the uncompressed escape (constant 1416 B) — the v1 note and the old `alac.rs` docstring claiming otherwise were wrong. OwnTone default = compressed (ffmpeg); libraop default = real encoder; node_airtunes2 = native ALAC. The only field sender using the escape (PipeWire raop-sink) reproduces the connected-but-silent-on-Sonos symptom. Uncompressed acceptance by fw 366 is UNPROVEN.
    - **Sync/anchor discipline (capture + libraop):** first `0x90` sync strictly BEFORE the first audio packet (iTunes: 254 µs before); first audio packet has marker bit `0xE0` + seq/rtptime EXACTLY equal to RECORD's RTP-Info; audio paced strictly wall-clock (mean 7.982 ms, no startup burst); sync field2 = next-to-be-sent rtptime, field1 = field2 − 88200 on the first sync (88553 steady-state — 88200 is what we use, fine); **rtptime must stay glued to wall-clock** — a sender whose rtptime freezes while 1 Hz syncs keep re-stamping it against advancing NTP burns the anchor and everything is discarded as late (the AP2-proven failure, same rule here). libraop never sends a sync unless actually streaming.
    - **iTunes sends `OPTIONS *` keepalives every 2.0 s** post-RECORD (no Session header, no Apple-Challenge); libraop every 25 s; OwnTone SET_PARAMETER progress every 25 s. Missing keepalive ≠ initial silence (audio starts 2 s before the first keepalive) but = eventual session reap while our UI still says "streaming".
    - Sonos timing requests always carry seq `0x0007`; echoing the request's seq bytes (libraop copies the whole header) is byte-identical there and correct for receivers that increment.
    - **Known deferred gaps after the fix package:** the AP2 *realtime* sender (`ap2_session.rs run_ap2_sender`) still has NO silence-fill (rtptime freezes when the hub idles — same anchor-burn risk; it's the vestigial-on-Sonos path so it wasn't touched); the upstream starvation root (app.rs `stream_active` initialized `true` while the driver pends IRPs until first KSSTATE_RUN, so `audio_loop`'s paced-silence branch is unreachable until the user first plays audio) is un-fixed at its own layer — the RAOP sender is immune via its local fill, the UPnP HTTP path still EOFs on connect-before-play; the GUI latency-adjust/silence-pacing controls don't apply to RAOP (receiver latency is protocol-fixed by the 88200 in sync packets; deliberate upstream under-delivery just gets silence-filled).
    - **IMPLEMENTED 2026-07-03 (the fix package, `session.rs`/`rtp.rs`/`timing.rs`/`rtsp.rs`):** timing responder spawns BEFORE the RTSP handshake; plaintext-first by default (the 4109755 proven-connect recipe + iTunes-verified sender-IP u32 URI + c=receiver); **et=4 MFi attempt is opt-in** (`airplay_mfi_encryption` config + GUI Advanced checkbox, falls back to plaintext on failure — wrap variants to try if pursued: ECB `AES-ECB(KEK, audio_key)`, CBC, different KEK); **real compressed ALAC** via the `alac-encoder` crate (Apple's encoder ported, Apache-2.0/MIT; round-trip unit-tested against the independent `alac` decoder crate under our exact fmtp; `airplay_uncompressed_alac` config = escape hatch); **silence-fill in the RTP sender** (deadline-driven: a stalled upstream synthesizes zero packets so rtptime never freezes — NB the driver pends audio IRPs until the first KSSTATE_RUN, so a session opened before any app plays WILL stall without this; also drops stale pre-handshake channel backlog, **re-glues after >1 s stalls** — skips missed slots instead of blasting a catch-up flood after suspend/resume — and **caps ring backlog at ~32 ms** so per-underrun latency creep is bounded); the sync spawner sends its initial `0x90` anchor **synchronously before returning** so callers get anchor-before-audio by construction (both RAOP and AP2-realtime spawn sync before the audio sender); `OPTIONS *` keepalive every 2 s (no Session header, `try_lock` so it never starves volume changes, backs off to 10 s while the receiver is unresponsive, never exits on transient failure); RTSP client **poisons the connection** on a mid-response failure and **discards stale-CSeq responses** so late replies can't shift request/response pairing; a `SpawnGuard` stops+joins already-spawned session threads if bring-up errors partway; RECORD no longer sends `X-Apple-ProtocolVersion`; UA = the modern iTunes 12.13.10 string; timing replies echo the request seq; service version bumped 0.4.0 → 0.5.0 (new codec = MINOR per the versioning convention).
- **AirPlay — implemented; remaining work + caveats.** The AirPlay sender lives in `service/src/airplay/` and is wired into discovery + the picker (each device is classified `RaopLegacy` vs `AirPlay2` via the `_airplay._tcp` `features`/`ft` bits; HomePods — `model AudioAccessory*` — always route to AirPlay 2). Two paths:
    - **AirPlay 1 / RAOP** — unencrypted (`et=0`, the default; field-proven recipe for Sonos-class et=0,4 receivers), Apple-RSA (`et=1`), or MFi (`et=4`, wrapped key — **opt-in experiment via `airplay_mfi_encryption`**, see GROUND TRUTH above): AirPort Express, shairport-sync, Sonos/Apple-TV in legacy mode. `rtsp.rs` / `rtp.rs` / `alac.rs` / `crypto.rs` / `timing.rs` / `session.rs`. Audio = real compressed ALAC (`alac-encoder`).
    - **AirPlay 2 / HomeKit transient pairing** — HomePod + AP2-only receivers: `tlv8.rs` + `srp.rs` + `pairing.rs` + `ap2_crypto.rs` + `ap2_rtsp.rs` + `ap2_session.rs`. SRP-6a (3072-bit / SHA-512, PIN `3939`, flag `0x10`) → HKDF-SHA512 → ChaCha20-Poly1305 encrypted RTSP + per-packet audio. **No FairPlay/DRM blob needed** — transient pairing alone is sufficient (confirmed against OwnTone). Audio is the same uncompressed-ALAC frame as RAOP, ChaCha-sealed (`shk` = pairing secret[0..32]).

  **Receiver-class coverage (2026-07-03, after a cited requirement-matrix audit vs OwnTone/libraop/shairport/openairplay — the Sonos et=0,4 path is field-VERIFIED working; the rest is by-reference, unvalidated on hardware):**
    - ✅ **Sonos-class AP2 licensee (et=0,4)** — plaintext RAOP after throwaway auth-setup, compressed ALAC. WORKING.
    - ✅ **Classic AirPort Express (gen1 no-`am`/ek=1; gen2 `am=AirPort4,x`)** — `AirPlayRenderer::prefers_rsa_encryption()` now selects the (already-implemented) RSA `rsaaeskey` path for these; gated on `ek=1` OR `am=AirPort*` (excluding the AP2-era `AirPort10,x`), so Sonos/third-party plaintext devices are untouched. **Sending RSA keys to a plaintext-only device breaks it (forked-daapd#108) — that's why the gate is narrow.**
    - ✅ **Apple TV 2/3 (et=0,3,5)** — plaintext et=0, and our `Cipher::None` ANNOUNCE omits rsaaeskey/aesiv AND puts no Apple-Challenge on ANNOUNCE (the "important for ATV3 6.0" rule is specifically about the *ANNOUNCE* challenge+keys, which we satisfy). ⚠️ We DO still send an Apple-Challenge on the `OPTIONS` opener for non-mfi devices (iTunes-style; OwnTone/libraop send it only when encrypting) — believed harmless (the ATV3 rule is ANNOUNCE-scoped, and we don't verify the response) but not hardware-confirmed.
    - ✅ **APEX fw 7.8 / third-party et=0,4** — same recipe as Sonos.
    - ✅ **big-DSP AVRs (Denon/Yamaha)** — now parse the RECORD-response `Audio-Latency` header and anchor at `max(reported, 88200)` (`effective_latency_samples`, unit-tested); absent header (Sonos, most) → exactly the old 88200. (NB libraop's `+11025` is a sender-side *scheduling* value that never reaches the sync packet, so it's NOT added to the on-wire anchor.)
    - ✅ **receivers that 501 `OPTIONS *`** — keepalive tolerates 501 (libraop parity); missing-`cn` receivers are treated as ALAC-capable (ALAC is mandatory in RAOP).
    - ⚠️ **HomePod / AP2-only licensees** — transient-pairing + PTP/buffered path is implemented but UNVERIFIED (no hardware). The AP2 *realtime* sender now has the same silence-fill/re-glue as RAOP (was freeze-prone).
    - ❌ **Password-protected receivers (`pw=true`)** — RTSP Digest auth is NOT implemented (needs a password-entry UI + RFC-2617 MD5, realm-driven username: `raop`→"iTunes"/UPPER-hex for APEX1, else "AirPlay"/lower). Clear error now tells the user to remove the password. THE main remaining gap.
    - ⚠️ **Apple TV with Access Control / "Require Device Verification"** — IMPLEMENTED (AP2 `470`→PIN fallback, 2026-07-03; UNVERIFIED vs real hardware): transient pair-setup answered 470 → one-time PIN ceremony (`app.rs begin_pin_pairing`). Ceremony sequencing (OwnTone parity): connection 1 = `GET /info` + `/pair-pin-start`, then CLOSE (the PIN stays on the receiver's screen); GUI modal collects the PIN (2-min window); connection 2 — FRESH, at PIN-entry time (a minutes-idle plaintext socket may be reaped) = persistent pair-setup M1–M6 (`hap_pairing.rs`); creds persisted per device (`airplay_pairings`), later connects `pair-verify` (session keys = same `SessionKeys::from_shared` derivation as transient). The pairing identity (controller id + Ed25519 seed) is per-install and persistent (`airplay_controller_*` in config) so re-pairs REPLACE the accessory's record instead of leaking its finite pairing slots (error 4 = MaxPeers is surfaced actionably). **Wipe policy (OwnTone parity): stored creds are cleared ONLY on a pair-verify REJECTION (`PairVerifyError::Rejected` — a response was received), NEVER on transport failures** — Wi-Fi blips/reboots can't force re-pairing. **Ceremonies are interactive-only** (GUI speaker click via `select_speaker_opts(interactive=true)`); watchdog/web/tray/launch paths get an actionable "open the window and click to pair" error instead — an unattended retry must never pop a PIN on the TV. Known deviation from OwnTone: it sends pin-start on the SAME connection that got the 470; we open a dedicated one after the attempt loop (we also try RAOP first — deliberate, working devices must not be interrupted). NOT implemented: the RAOP-side FRUIT (plist) pairing for ATVs that gate legacy RAOP behind PIN (those route via AP2).
    - ❌ **IPv6-only receivers, `tp=TCP`-only (AirTunes v1)** — discovery keeps only IPv4 and SDP is `IN IP4`; TCP-interleaved transport isn't implemented. Both rare (dual-stack / extinct); deferred.

  **Verified vs. not:** the crypto core is unit-tested (SRP against the RFC 5054 vector; TLV8/HKDF/channel-cipher/audio-seal round-trips); the PTP packet codec + offset math and the resend buffer are unit-tested too. The *live* pairing + stream — and **especially the PTP handshake** — have **not** been validated against a real HomePod; that needs the user's hardware. Status of the formerly-deferred items:
    - **Timing-mode routing (field-tested): bit 41 (`SupportsPTP`) → PTP, else NTP.** Two field experiments on a current-firmware SYMFONISK: (1) PTP without Signaling — full bring-up accepted (SETUP/SETPEERS/RECORD 200, audio + 0xD7 sync flowing) but the receiver never sent one PTP packet back → no lock → silent; (2) NTP — `SETUP(stream)` *stalls* (current Sonos fw treats NTP as vestigially as RAOP; NTP-mode stream SETUP had returned 200 on this device only in older sessions). Conclusion: PTP is required for bit-41 receivers, and the missing engagement trigger was libairptp's **Signaling** (now implemented — see below). The PTP master logs every first inbound message type and warns after 5 s of receiver silence, so the next log always shows *how* a receiver engages. ⚠️ Do NOT send unsolicited datagrams between the two SETUPs (a "firewall priming" packet to the receiver's timing port was the prime suspect for stalling `SETUP(stream)`; removed). A session that fails mid-bring-up MUST still TEARDOWN — `Ap2Rtsp` now does it on Drop once paired — otherwise the receiver holds the half-open session and retries time out at pairing for tens of seconds.
    - **PTP timing — IMPLEMENTED (`ap2_ptp.rs`), sender-as-grandmaster, full libairptp parity.** Receivers we route to PTP negotiate `timingProtocol: PTP`. **The sender is the PTP grandmaster; the receiver follows our clock** (NOT the other way round — an earlier follower design left the receiver with no timeline: it decrypted audio but never scheduled it → "playing" but silent). Implementation mirrors OwnTone's `libairptp` exactly: unicast Announce (1 s, port 320, **with PATH_TRACE TLV** carrying the clock id) + two-step Sync (125 ms, port 319) + Follow_Up (320) + Delay_Resp (logInterval −3) on the receiver's Delay_Req + **Apple-proprietary Signaling every 1 s** (org-extension TLVs, Apple OUI `00:0d:93`, subtypes 1/22B and 5/32B, payloads starting `00 00 03 01`, targetPortIdentity zeros, logInterval −128) — iOS senders emit these and receivers appear to gate engagement on them; flags `UNICAST|TIMESCALE(|TWO_STEP)`; Announce grandmaster fields priority1/2=128, clockClass=0x06, accuracy=0x21, variance=0x436A, timeSource=0x20; sourcePortIdentity port 0x8005. The clock served is **monotonic** (Instant since session start), and the `0xD4` sync packet is stamped from the *same clock + NTP 1900-epoch delta* (OwnTone's consistency contract). SETUP carries `timingPeerInfo` AND `timingPeerList` with `ID` (UUID string), `ClockID` (int64), `DeviceType:0`, `SupportsClockPortMatchingOverride:false`, `Addresses`; `SETPEERS` lists receiver first, then sender. Non-PTP receivers keep the NTP path. Other AP2 session-bring-up requirements learned from hardware: the **event channel TCP connection** (to the first SETUP's `eventPort`) must be open or RECORD times out, and each realtime audio packet's on-wire layout is `[RTP header][ciphertext][16-byte tag][8-byte nonce]` (missing nonce suffix = every packet fails auth = silence). Refs: OwnTone `airplay.c` + `owntone/libairptp` (`ptp_msg_handle.c`, `ptp_definitions.h`).
    - **Retransmit/resend — IMPLEMENTED (both paths).** `ResendBuffer` (~4 s ring) + `spawn_resend_responder` in `timing.rs`: listens on the control socket for `0x80 0xD5` resend requests and re-sends matching packets wrapped in `0x80 0xD6`. Wired into RAOP (`rtp.rs`/`session.rs`) and AP2 (`ap2_session.rs`); the control socket is shared with the sync sender via `try_clone`.
    - **auth-setup — IMPLEMENTED (RAOP, `rtsp.rs`).** An ANNOUNCE `403` triggers the MFi `/auth-setup` X25519 handshake (`0x01` unencrypted selector) and one retry. **403-gated**, so it cannot regress receivers that already work (OwnTone disables auth-setup by default; the 403 trigger is our safety valve). Adds the `x25519-dalek` dep.
    - **Persistent pairing — IMPLEMENTED (2026-07-03).** Full pair-setup (M1–M6, Ed25519) + pair-verify (X25519) in `hap_pairing.rs` (crypto verified against pair_ap's salts/infos/nonces + simulated-accessory round-trip tests), wired via `ap2_rtsp.rs` + the `begin_pin_pairing` ceremony. See the Access-Control bullet above for the design + wipe policy. Unverified against real Apple TV hardware.
    - **Anchor timeline (field-tested, the key AP2 discovery): the Sonos accepts `SETRATEANCHORTIME` ONLY on ITS OWN clock.** Anchors naming *our* grandmaster clock get `400 Bad Request` (empty body); the same request naming the receiver's announced clock id (its MAC as EUI-64, clockClass 248) returns 200. The receiver never sends Delay_Req (it won't follow us) but showers us with its own Announce/Sync/Follow_Up (~16/s) — i.e. **receiver-as-master**: the sender must *follow the receiver's clock* and express anchor times on its timeline. `PtpTimeline` (in `ap2_ptp.rs`) does this: it pairs the receiver's two-step Sync/Follow_Up into a smoothed `receiver_time − our_time` offset, and `receiver_now_ns()` yields (id, now) for the anchor. Also learned: the anchor must be sent **when the first audio packet is imminent** — an anchor mapping rtpTime→time that passes before audio exists means everything arrives "late" and is discarded (session up, silent). The buffered sender therefore anchors at first audio and **silence-fills** when the source idles so rtptime stays glued to wall time. Our own PTP master keeps running (the receiver's engagement is gated on our Signaling — see below — and it may follow us via Sync-only without Delay_Req).
    - **Buffered audio — IMPLEMENTED (type 103 over TCP, AAC-LC preferred), UNVERIFIED vs hardware.** This is the stream kind iOS actually uses (feature bit 40). Receivers with bit 40 + PTP get: `SETUP` type 103 with **AAC-LC first** (`ct=4`, `spf=1024`, `audioFormat=0x400000` — iOS's buffered codec, the only payload field-proven on Sonos; encoded via the **Windows-provided Media Foundation encoder MFT** (`CLSID_AACMFTEncoder`, `airplay/aac_mf.rs`, `windows` crate dep) — no third-party codec/licensing; the MFT primes 1-2 frames before first output and the encoder must live on the thread that uses it (per-thread COM); the session probes availability before negotiating), **ALAC fallback** (`ct=2`, `spf=352`, `0x40000` — buffered-ALAC completed the entire bring-up incl. anchor on the Sonos but played SILENT, the same vestigial-accept pattern as RAOP/NTP), then realtime. One **TCP** connection to the returned dataPort carrying `[u16 BE length incl. itself][RTP header][sealed payload|tag|nonce]` frames (same ChaCha seal as realtime; **2 s write timeout + stop() shuts the socket down via a kept clone** — otherwise a non-consuming receiver wedges write_all forever and stop() hangs on the join, freezing speaker switches with TEARDOWN never sent), **`SETRATEANCHORTIME`** sent by the sender **at first audio** (see anchor bullet), silence-fill on idle, no 0xD4/0xD7 sync and no resend under buffered, `FLUSHBUFFERED` before TEARDOWN. **`POST /feedback` keepalive every 2 s** (both stream kinds). `GET /info` is sent pre-pairing and logs the receiver's `audioFormats` table at info level — the authoritative per-device codec matrix. Refs: openairplay airplay2-receiver, shairport-sync `rtsp.c`, openairplay-spec.

  **References used (authoritative):** OwnTone `src/outputs/{raop,airplay}.c` + `src/pair_ap/`, the openairplay + emanuelcozzi.net specs, pyatv, RFC 5054 / RFC 3526. **Do not anchor on `imadal1n/airsink`** — zero stars, has compatibility issues; it was only a hypothesis source and every load-bearing constant was re-verified against OwnTone/pair_ap.

- **OwnTone gap audit (2026-07-03, 4-agent inventory: raop.c 50 features / airplay.c 40 / ours 48 / outputs.c infra 14).** Ranked by value for this bridge; ✅ = since implemented.
    1. ✅ **Mid-session failure watchdog + reconnect — DONE (`app.rs spawn_reconnect_watchdog`, `auto_reconnect_on_drop` config, default on).** Each session carries a `dead` flag set by its threads on a demonstrable drop (audio send error / buffered TCP write fail / repeated keepalive-or-feedback failures); the watchdog tears the zombie down and reconnects once, ~5 s later (OwnTone's spacing; respects the Sonos hold). One shot per drop → never hammers a gone speaker. GUI toggle next to launch-reconnect.
    2. **Keep-warm delayed stop** — pause/stop = FLUSH (session stays CONNECTED) + a 10 s timer before real TEARDOWN, cancelled if playback resumes; avoids re-handshake churn AND the Sonos hold hazard on quick switch-back. Ours: full teardown+reconnect always (rtsp.rs FLUSH exists but is unused). (Modest fit — we don't really "pause"; system audio idle = silence-fill.)
    3. ✅ **Password Digest auth — DONE (`crypto.rs digest_auth_*`, `rtsp.rs`, per-device `airplay_passwords` config, GUI password modal).** Byte-for-byte port of `raop_add_auth`: HA1=MD5(user:realm:pass), HA2=MD5(method:uri), response=MD5(HA1:nonce:HA2); learns realm/nonce from the first 401 then adds `Authorization: Digest` (recomputed per request's method+uri) to every request; APEX1 quirk (no `am`) = "iTunes"+UPPERCASE hex, else empty user + lowercase; 2nd 401 → clear "wrong password" error. `pw=true` devices are now selectable (were rejected). UNVERIFIED vs real password hardware; digest is unit-tested.
    4. ✅ **ATV4 PIN pairing + persistence — the AP2 half is DONE (2026-07-03):** transient pair-setup answered **470** → HOMEKIT_NORMAL PIN flow (`X-Apple-HKP: 3`, `/pair-pin-start`, persistent pair-setup M1–M6, per-device creds + per-install controller identity, `pair-verify` on later connects, creds cleared ONLY on verify rejection). See the Access-Control bullet for details. The RAOP half (OPTIONS 403 / TXT `sf` bit 9 → `/pair-setup-pin` FRUIT plist SRP) is NOT implemented — PIN-gated ATVs route via AP2.
    5. **Speaker→sender remote control** — RAOP: a DACP server keyed by our Active-Remote header receives volume/play/pause from the receiver (we send the headers, nothing listens). AP2: the events channel carries `sendMediaRemoteCommand` play/paus/nitm/pitm (we drain the channel without parsing; NB OwnTone answers each with an encrypted `RTSP/1.0 200 OK`, and speaker→sender *volume* comes via DACP not events).
    6. ✅ **SET_PARAMETER metadata (title/artist/album) — DONE, RAOP only (`now_playing.rs` WinRT SMTC read + `airplay/dmap.rs` mlit encoder + `rtsp.rs set_metadata` + `app.rs spawn_now_playing_forwarder`, `forward_now_playing` config, off by default).** Reads the Windows System Media Transport Controls, sends `application/x-dmap-tagged` on track change via a detached handle (never holds the GUI's session lock during the send); attempted-once-per-track so a receiver that mishandles metadata can't churn the drop-watchdog. Artwork + `progress:` NOT sent; AP2 metadata NOT done. UNVERIFIED vs hardware.
    7. **Multi-room sync (the big architectural one)** — one "master session" per (quality, encrypt): ONE encoder + ONE rtp_session; every device session attaches and receives byte-identical packets ⇒ sync is inherent; late joiners get a 0x90 init-sync aligning them to the in-progress timeline (marker-bit packet on join); per-device `offset_ms` trim applied ONLY to that device's sync packets. Transfers directly if we ever do multi-speaker.
    8. **IPv6-first with per-device `v6_disabled` blacklist** + automatic from-scratch retry on v4 when a v6 attempt fails softly. Ours is v4-only end-to-end.
    9. **OPTIONS probe-only mode** — reachability/password check (same 401/403 triage) without streaming, used by the selection UI; probe is also the vehicle for PIN pair-setup.
    10. Minor: per-device config (nickname/max_volume/exclude/offset_ms/fixed ports), master+relative volume model with select-time cap, receiver-dB→pct conversion for volume feedback, RTSP closecb-based connection-close detection (ours notices via the next keepalive within ~2 s).
  **Things we have that OwnTone doesn't** (for perspective): buffered AP2 (type 103) + AAC-LC + SETRATEANCHORTIME (OwnTone is realtime-ALAC-only, no FLUSHBUFFERED), receiver-clock PtpTimeline anchoring, silence-fill/re-glue/backlog-cap discipline (OwnTone leans on its player being real-time), Audio-Latency parsing (OwnTone logs and ignores it), 2 s iTunes-cadence keepalive, CSeq pairing + connection poisoning (OwnTone deliberately disables CSeq checks for AirFoil-class bugs — worth remembering if a receiver misbehaves against our stricter client), et=4 MFi experiment. Server-only OwnTone machinery that does NOT map to us: library metadata content, pause/seek-position semantics (our pause = silence-fill), queue/history handling.

- **Lower-latency formats / alternative encodings**. Currently L16 PCM 44.1 kHz stereo. Options to add: 24-bit / 96 kHz for hi-fi; FLAC (compressed, lossless, lower bandwidth — most UPnP speakers support `audio/flac`); Opus (lossy, ultra-low-latency, used in WebRTC; less broadly supported by consumer speakers); AAC (broad compatibility, less optimal for low latency). Architecture-wise: the driver only knows L16 44.1; alternative encodings would be in the service's HTTP output path (encode at packet boundary, ship in the speaker's preferred MIME). Negotiation could be via DIDL `protocolInfo` listing multiple `<res>` lines.

- **License + donations for EV code signing — largely OVERTAKEN BY EVENTS (2026-08)**: the Certum EV cert exists (central approval-gated signing repo `Mihonarium/signing`), and the CI pipeline for EV-signing installers/exes + Microsoft **attestation**-signing the driver is implemented (see "Signing & release pipeline" above + `docs/driver-signing.md`) — once the first attestation round-trip completes, releases drop the `testsigning` requirement. Still open from the original list: (a) pick a repo license (permissive MIT/Apache-2.0; check crate deps for copyleft); (b) "support"/donate link in README + GUI (covers cert renewal); (c) WHQL/HLK if Windows Server support or "Windows Certified" ever matters (attestation covers desktop incl. Secure Boot); (d) Partner Center automation via the Microsoft Hardware API (Azure AD app) to remove the manual submit/download steps.

## What I should NOT do

- Don't `process::exit` from anywhere except `main`. Use `app.request_shutdown()` and let the loop unwind.
- Don't add Power IRP completions without `PoStartNextPowerIrp` even though it's deprecated — older WHQL testers complain.
- Don't commit `service/target/` (covered by `.gitignore` now; broke the repo once).
- Don't push `windows_subsystem = "console"` for the GUI build — the brief console flash is a real UX bug. `"windows"` + `AttachConsole(ATTACH_PARENT_PROCESS)` for `--headless` is the right pattern.
- Don't ignore the user when they push back. They know what they observe; if my diagnosis doesn't match their experience, my diagnosis is probably wrong.

## Conventions for future work

- **Subagents: default to Opus.** When launching Agent-tool subagents, pass `model: "opus"` unless there's a specific reason not to (user preference, 2026-07-03).

- **egui + Windows touchpad scrolling (don't re-learn this).** egui 0.29 exponentially smooths every *Line-unit* wheel event (~90%/100 ms) and only lets sub-8pt *Point-unit* deltas through unsmoothed (its "mac trackpad" bypass in `input_state/mod.rs`). Windows precision touchpads pan via fractional Line-delta `WM_MOUSEWHEEL` events whose decay curve is already smooth, so egui's smoother stacks on the driver's inertia → lag while panning + a rubber-band acceleration/jump at finger-lift. Fix lives in `gui.rs raw_input_hook`: touchpad-shaped events (fractional line deltas, or any within 300 ms of one — flings pass through exact integers) are rewritten to pre-scaled Point events split into sub-8pt chunks so every chunk takes the unsmoothed path; isolated integer notches (real mouse wheels) keep egui's smoothing. The `update()` repaint pump (repaint while `smooth_scroll_delta != 0`) is still needed for the mouse-wheel case in reactive mode. If egui is ever upgraded, re-check `is_smooth` in `input_state` before touching this.

- **App / tray icons must have an active and a non-active variant.** When we eventually replace the procedurally-drawn tray icon with proper artwork (or add an app icon), ship at minimum two states: one for "streaming active" (full color / accent fill / animated sound waves) and one for "idle / no speaker / disabled" (desaturated or outlined). The tray icon is the only persistent UI signal when the window is hidden; a single static icon hides whether the app is actually doing its job. Same applies if we ever ship a taskbar icon. Sizes: 16×16, 24×24, 32×32, 48×48, 256×256 (Windows tray scales down a single 32×32 fine, but the 16×16 master should be hand-tuned — auto-downscale loses detail).

---
> Source: [Mihonarium/StreamToSpeaker](https://github.com/Mihonarium/StreamToSpeaker) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-08-10 -->
