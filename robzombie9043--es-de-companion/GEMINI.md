## es-de-companion

> Guidance for Claude Code when working in this repository. This project is a ground-up rebuild of **ES-DE Companion**, a dual-screen companion app for the ES-DE emulation frontend, using modern Android best practices.

# CLAUDE.md

Guidance for Claude Code when working in this repository. This project is a ground-up rebuild of **ES-DE Companion**, a dual-screen companion app for the ES-DE emulation frontend, using modern Android best practices.

This document is deliberately weighted toward **structure, layering, and code quality** rather than a fixed feature spec. The core state pipeline is foundational and must be built to a higher standard of rigor than everything else in the app.

## The One Thing That Has To Be Rock Solid

Everything this app does depends on correctly understanding what ES-DE is currently doing. The pipeline is:

```
es_log.txt (raw text, growing/truncating file)
    → tail new lines
    → filter for "Scripting::fireEvent():" lines
    → parse into a typed Event
    → reduce events into a single current AppState
    → AppState drives everything downstream (media lookup, UI, widgets, whatever comes later)
```

This pipeline is the foundation everything else is built on. Treat it accordingly:

- It lives entirely in `domain`, as plain Kotlin with no Android framework dependencies where at all possible — the parsing and state-reduction logic should be testable with plain unit tests against fixture log lines, with no emulator, no instrumentation, no mocked Android classes.
- It should have the best test coverage of anything in the codebase. Every event type, malformed/unexpected line, empty-args case, and the truncate-on-restart case need explicit tests.
- Resist the urge to let UI or media-resolution concerns leak into this layer. Its only job is: raw log text in, correct `AppState` out. Nothing else should influence it, and it should not know anything about widgets, screens, or file paths for media.
- If this layer has a bug, the entire app is wrong regardless of how polished anything built on top of it is. Prioritize correctness and clarity here over cleverness or premature abstraction.
- **Cold start is not "replay the last event."** A freshly-launched app has no state to reduce onto, and context-dependent events (e.g. `screensaver-end`, which needs to know what was active *before* the screensaver started) produce the wrong result if the reducer only sees the single most recent line. `EsdeLogFileRepository` instead finds the last self-contained ("anchor") event and replays forward from there, growing its read window backward until it finds one or hits the start of the file. See `EsdeLogFileRepositoryTest` for the anchor-replay cases. That replay is also skipped if `es_log.txt`'s `lastModified()` predates the current boot — a device reboot can auto-start this app (it carries a `HOME` intent filter for secondary-display placement) before ES-DE has (re)written the log this session, so an anchor found in a not-yet-touched-since-boot file is guaranteed to be a leftover from before the reboot, not current truth.
- **The derived `AppState` stream is shared, not rebuilt per subscriber.** `ObserveAppStateUseCase` computes its reducer fold exactly once, at construction, as an application-scoped `StateFlow` — not fresh on every `invoke()` call. See Known Gotchas for why a naive per-call `scan()` silently produces stale/`Idle` state for late-joining subscribers.

## Architecture & Structure

**Clean Architecture, three layers, strictly one-directional dependencies:**

```
ui        → depends on → domain
data      → depends on → domain
domain    → depends on → nothing (no Android imports, no other layers)
```

- `domain`: models, the event parser, the state reducer, use-case classes, repository *interfaces*. Pure Kotlin. This is the layer that should be easiest to read, test, and reason about — it's the specification of what the app does, independent of how.
- `data`: repository *implementations* — file I/O, log tailing, media file resolution, persistence. Talks to Android/filesystem APIs. Implements the interfaces `domain` defines, never the reverse.
- `ui`: Compose screens + ViewModels. ViewModels depend on `domain` use cases/repository interfaces only — never directly on `data` implementations, never directly on `java.io.File`, `PackageManager`, or `FileObserver`.

Keep this a **single Gradle module** with package-based layering (`domain/`, `data/`, `ui/` packages) rather than splitting into multiple Gradle modules. Multi-module setups add build-config overhead that isn't justified at this project's size; package boundaries plus code review discipline are enough to keep the dependency rule honest. Revisit this only if the codebase grows enough that build times or team size actually demand it.

**Within each layer, favor small, single-purpose classes over large ones.** A class that parses log lines should not also decide file paths. A ViewModel for one screen should not accumulate responsibilities for another. If a class is hard to name in one sentence, it's probably doing too much.

## Code Quality & Maintainability Standards

- **Immutability by default.** Data classes with `val`, not `var`. State changes happen by producing a new value, not mutating an existing one. This matters most in the state-reduction pipeline, where a mutable shared state object is the classic source of subtle bugs.
- **No `!!`.** Handle nullability explicitly at the point where it arises, not by asserting it away.
- **Explicit over implicit.** Sealed classes for anything with a closed set of variants (event types, UI state, results). Exhaustive `when` blocks with no `else` branch where the compiler can enforce completeness — this is what catches "forgot to handle the new event type" at compile time instead of runtime.
- **Errors are values where they're expected, exceptions where they're not.** Parsing a malformed log line is an expected, routine outcome — represent it as a nullable/`Result` return, not a thrown exception. A genuinely unexpected failure (e.g. can't open a file that should exist) can throw, but should be caught at a clear boundary rather than left to propagate into UI code.
- **Small, well-named functions over comments explaining what a large function does.** If a comment is needed to explain *what* a block does, that block is usually better extracted into a named function. Comments should explain *why*, not *what*.
- **Coroutines + Flow for all async work** — no callbacks, no `AsyncTask`, no `LiveData`. `StateFlow` for observable state, `SharedFlow`/`Channel` for one-off events.
- **Dependency injection, consistently applied.** Either Hilt or a hand-rolled composition root — pick one deliberately and use it everywhere; don't mix DI approaches or fall back to ad-hoc singletons partway through the codebase.
- **Testability is a design constraint, not an afterthought.** If a class is hard to unit test, that's usually a sign it's reaching across layers it shouldn't, or mixing concerns. Prefer constructor injection of dependencies (including things like a `Clock`/time source, or a `FileSystem` abstraction) so tests can substitute fakes.
- **Consistent formatting, enforced by tooling, not convention.** `ktlint`/`detekt` run as part of the build (`org.jlleitschuh.gradle.ktlint` and `io.gitlab.arturbosch.detekt`, applied in `app/build.gradle.kts`), not as a manual habit. Both are baselined (`config/ktlint/baseline.xml`, `config/detekt/baseline.xml`) against the pre-tooling codebase, so `ktlintCheck`/`detekt` only fail on *newly introduced* violations — don't regenerate a baseline to silence a violation in code you're actively touching; fix it instead. Regenerating (`ktlintGenerateBaseline`/`detektBaseline`) is only appropriate when deliberately doing a repo-wide reformat pass.

## What This Rebuild Explicitly Removes

The legacy app generated and wrote its own shell scripts into ES-DE's `scripts/` folder and had ES-DE call them on each event, producing separate companion-owned log files. **That mechanism is gone.** ES-DE already writes every event to its own `es_log.txt` via `Scripting::fireEvent()` calls — the rebuild only ever *reads* that file. No script writing, no script folder configuration, no "enable custom event scripts" setup step. Confirmed: ES-DE writes a fresh `es_log.txt` on every restart (truncated/recreated, not appended-forever), so the tailer must detect a size decrease and reset its read position rather than assuming monotonic growth.

## Feature Status

- **Log pipeline, state reduction, media resolution, onboarding, Settings, and the App Drawer are built** and described in README.md — treat their existing structure (package layout, repository interfaces, use cases) as the pattern to extend, not a sketch to redesign.
- **Widget overlay system is built** and has fully replaced the old fixed backdrop/logo
  display (`MainScreenImages`/`MainScreenImageState`, both deleted). Two canvases
  (System, Playing), grid-based placement, full CRUD edit mode (move/resize/add/remove/
  configure/reorder), DataStore persistence per canvas, and an edit-mode preview sourced
  from the last browsed system/game (`LastKnownContextRepository`) rather than the live
  `AppState`. See `ui/widgets/` (live rendering) and `ui/widgets/edit/` (the editor) —
  treat this as the pattern to extend for further widget types/config options, not a
  sketch to redesign.
- **Second-display presentation**: the app is a plain single-Activity app placed on the secondary display via the OS/Mjolnir, launching specific apps onto it via `ActivityOptions.setLaunchDisplayId` (see `SecondaryDisplayResolver`). No in-app `Presentation`/`DisplayManager` complexity beyond that.
- **In-app update checker/downloader/installer is built.** Checks GitHub Releases (`RobZombie9043/es-de-companion`) for a newer `versionName` than the running build: a silent, non-blocking check on every app start that auto-shows a dialog if an update is found, plus a manual "Check for Updates" entry as the top item of Settings → Other Settings. Confirming downloads the release APK, handles the Android 8+ `REQUEST_INSTALL_PACKAGES` grant (`InstallUnknownAppsPermission`, same shape as `AllFilesAccessPermission`), and launches the system installer via `FileProvider`. Separately, on the first startup after the app relaunches with a newer `BuildConfig.VERSION_CODE` than last recorded, a one-time "what's new" dialog fetches and shows that specific release's own notes (`GET .../releases/tags/v{VERSION_NAME}`, not "latest"). Version comparisons go through `domain/update/VersionComparator` — it compares the numeric `X.Y.Z` tuple first, then treats a no-suffix build as newer than a suffixed one at equal version, falling back to a plain string compare only when both are suffixed; this is a documented best-effort heuristic, not strict semver, given this project's irregular git tag history (`v0.6.0-pre-release`, `v0.7.0-alpha`, etc.). See `domain/update/`, `domain/repository/UpdateRepository.kt`, `data/update/`, `ui/update/`.
- **FAB Control is built.** Replaces the old fixed Settings/Music corner FABs and the "Show Settings Button" toggle with one Settings > UI Settings > "Floating Action Buttons" panel assigning a `FabType` (`Music`, `Settings`, `GameManual`, `AppDrawer`, `CustomApp`, `None`) to each of the four `FabPosition`s (`TopStart`/`TopEnd`/`BottomStart`/`BottomEnd`), persisted as `FabAssignments`/`FabSlot` (`domain/model/`). `FabAssignments.with()` is the one cross-corner business rule — `Music` can only occupy one top corner, and reassigning it there swaps the displaced `FabSlot` (including a `CustomApp`'s package selection) into the vacated corner rather than discarding it; unit-tested in `FabAssignmentsTest`. `Settings`/`AppDrawer` are rendered by `MainScreen` itself (their click handlers are local closures `MainActivity` can't reach); `Music`/`GameManual`/`CustomApp` are rendered from `MainActivity` via `MainScreen`'s four overlay slots (`fabSlotContent`, generalized from the old single `topStartOverlay` slot). `GameManualViewModel`'s manual resolution now follows `AppState.currentGameReference()` (Playing/Browsing/Screensaver-with-game) unconditionally, rather than being gated by the "Game Playing Behavior" setting — the FAB needs to know "does this game have a manual" independent of whether that automatic-display setting is even on. `CustomApp` shares the App Dock/App Drawer's `ObserveOtherScreenLaunchAppsUseCase`/`SetOtherScreenLaunchAppsUseCase` preference set and indicator dot, not a separate preference. See `domain/model/{FabType,FabPosition,FabAssignments,FabSlot}.kt`, `domain/usecase/{Observe,Set}FabAssignment*UseCase.kt`, `ui/settings/SettingsContent.kt` (`FabControlSetting`), `ui/main/MainScreen.kt`, `ui/MainActivity.kt`.
- **Clock/SystemStatus/ClockAndSystemStatus FABs are built.** Three more `FabType`s (`Clock`, `SystemStatus`, `ClockAndSystemStatus`), offered only in the top corners (`TOP_FAB_OPTIONS`, same option-list-omission mechanism that already keeps `Music` top-only — no new `FabAssignments.with()` cross-corner rule needed, since these have no exclusivity constraint with each other). Unlike every other `FabType`, these render at a fixed height but variable width (`ui/CornerButtonMetrics.kt`'s new `WideCornerFab`, alongside the original fixed-square `CornerFab`) since their content (a time string, a row of status icons) isn't a fixed size. `Clock` ticks once a second using the system's own 12h/24h time-format preference (`android.text.format.DateFormat.getTimeFormat`), not a hardcoded format. `SystemStatus` shows Battery (always, tier + charging bolt via `domain/model/SystemStatus.kt`'s `BatteryTier`/`batteryTierFor`), Wifi, and Bluetooth — Wifi/Bluetooth icons are shown only while actually connected and omitted entirely otherwise (no "off"/"disabled" icon variant for either). Cellular is deliberately excluded (target hardware, AYN Thor handhelds, has no modem). Backed by `domain/repository/SystemStatusRepository.kt` / `data/systemstatus/AndroidSystemStatusRepository.kt` (battery via a sticky `ACTION_BATTERY_CHANGED` receiver, Wifi via a `ConnectivityManager` network callback, Bluetooth via ACL connect/disconnect broadcasts), shared across subscribers (`SharedSystemStatusRepository`, same `shareIn` reasoning as `SharedEsdeLogRepository`) since both top corners could reference it at once. Bluetooth connection state requires `BLUETOOTH_CONNECT` (API 31+) — **this is the first runtime-dangerous-permission request in the codebase** (every other permission sends the user to a system Settings screen instead): `ui/SystemStatusFabContent.kt`'s `BluetoothPermissionPrompt` shows a one-shot rationale dialog the first time a SystemStatus-family FAB is shown with the permission ungranted (persisted via `OnboardingRepository.observeBluetoothPermissionRequested`/`setBluetoothPermissionRequested`, so it never auto-prompts twice), and rechecks on `ON_RESUME` (same idiom as `OnboardingScreen`'s `AllFilesAccessPermission` check) to pick up a grant made later via system Settings — `data/systemstatus/BluetoothPermissionRecheckSignal.kt` is what lets that recheck reach the repository, same shape as `StorageMountEvents`. The Music FAB's expandable song-title popup (`MainActivity`'s `MusicFabContent`) used to reserve a hardcoded `CORNER_BUTTON_SIZE` of width for whatever occupied the opposite top corner — correct only because every other FAB is that exact fixed width. Since a wide FAB isn't, `reservedWidth` now reads a live-measured width instead (`measuredFabWidthsPx: Map<FabPosition, Int>`, reported by each wide FAB's `Modifier.onSizeChanged`), falling back to the old fixed-size assumption when a corner hasn't reported one. See `domain/model/SystemStatus.kt`, `domain/repository/SystemStatusRepository.kt`, `data/systemstatus/`, `domain/usecase/{Observe,Set}BluetoothPermissionRequestedUseCase.kt`, `domain/usecase/ObserveSystemStatusUseCase.kt`, `ui/SystemStatusFabContent.kt`, `ui/CornerButtonMetrics.kt`.
- **Backup & Restore is built.** Settings > Setup > "Backup & Restore" exports every user-configurable setting (Setup folder paths, UI/Music/Other Settings toggles, `FabAssignments`, App Drawer/Dock settings and folders, and both widget canvases) to a single JSON file via a "Save As" picker (`ActivityResultContracts.CreateDocument`), and restores it back via a file picker (`ActivityResultContracts.OpenDocument`) behind a confirmation dialog — a full overwrite of everything the snapshot covers, not a partial merge. `domain/model/AppConfigBackup.kt` is the plain snapshot model (with a `version` field for the schema); `domain/repository/ConfigBackupRepository.kt` does pure `AppConfigBackup ⇄ String` serialization (implemented by `data/backup/JsonConfigBackupRepository.kt`, reusing `WidgetLayoutDto`/`WidgetLayoutMapping` for the canvas fields); `domain/usecase/{Export,Restore}ConfigBackupUseCase.kt` orchestrate the five settings repositories directly (not their narrower per-field use cases, since backup needs every field at once). `data/storage/ConfigBackupFileIo.kt` is a `SafPathResolver`-style sibling for this feature specifically: it reads/writes via `ContentResolver` open streams rather than resolving a plain filesystem path, since a "Save As" destination may be a genuine SAF/cloud document rather than local storage — called directly from `ui/settings/SettingsContent.kt`'s `BackupRestoreSetting`, never from `SettingsViewModel`. Restoring an older-schema backup is a documented additive-only convention (new `ConfigBackupDto` fields get Kotlin defaults so old backups still decode); restoring a *newer*-schema backup than the running build understands is rejected outright via an explicit `version` check, not left to silent lenient decoding. See `SettingsViewModel.restoreBackup()`/`reloadSettingsState()` for why a successful restore reloads the ViewModel's own UI state from the repositories rather than trusting it's still in sync — a bulk restore changes settings the ViewModel didn't itself mutate.
- **RetroAchievements integration is in progress (see the `cheevos` branch), landing in sequenced PRs — treat as a work in progress, not a finished feature.** Domain scaffolding, credentials scaffolding (Settings > RetroAchievements, `EncryptedRetroAchievementsCredentialsRepository`), and the real `RetroAchievements/api-kotlin` client are all built. `RetroAchievementsRepositoryImpl.validateCredentials` is real end-to-end (Connect calls the live `GetUserSummary` endpoint and only persists on success); `getCandidateGames`, `getUserGameProgress`, and `getAchievementSummary` are all fully wired to real identification/achievement UI. All three are cached: `GameListCache` (per-console, 24h TTL, disk + memory), `UserProgressCache` (per-username, 1h TTL, disk + memory), and `AchievementSummaryCache` (per-`(username, gameId)`, 15-minute TTL, disk + memory). `getAchievementSummary` additionally takes a `forceRefresh` flag, surfaced as a "Refresh" entry in the achievement screen's kebab menu (`RetroAchievementsScreen`'s `OptionsMenu`), so a user can bypass the TTL rather than wait it out. Unlike `GameListCache`/`UserProgressCache`'s DataStore Preferences-backed stores, `AchievementSummaryCache`'s disk layer (`FileAchievementSummaryCacheStore`) is flat per-game JSON files under `Context.cacheDir`, pruned to the 2000 most-recently-written games — see its kdoc for why (this cache's key space, one entry per game ever viewed, has no natural bound the way the other two do, and DataStore rewrites its entire backing file on every edit regardless of key count). A cached-but-stale summary is shown immediately while a background refresh replaces it, rather than blocking on a spinner (`RetroAchievementsRepository.peekAchievementSummary`/`PeekGameAchievementSummaryUseCase`, consumed by both RetroAchievements ViewModels; `RetroAchievementsFetchState.Loaded.isRefreshing` renders a small corner indicator during the background refresh without retriggering `RetroAchievementsFetchBody`'s crossfade, since it's a flag on the existing `Loaded` variant, not a new sealed state). `RetroAchievementsCredentialsRepository` is deliberately excluded from `ExportConfigBackupUseCase`/`RestoreConfigBackupUseCase` — see AppContainer's wiring comment. R8/Gson keep rules (`proguard-rules.pro`) are a best-effort starting point, not yet verified against a real minified build. Remaining work (game identification, achievement UI, manual match-override correction) is tracked in the planning doc, not restated here. `GameTitleMatcher` also has a `SpaceInsensitiveTitle` tier (deterministic, no fuzzy scoring, same as the others - catches e.g. "Moto Racer" vs. "Motoracer"), ranked between `NormalizedTitle` and `PartialTitle`. Ahead of ES-DE's own upcoming ROM-hash support in `gamelist.xml`, `GamelistFileReader`/`GameListParser`/`GameRomHashRepository` (mirroring the description-widget lookup pattern) extract a ROM hash per game (placeholder `<hash>` tag - see `GameListParser`'s `ROM_HASH_TAG` TODO) and `ResolveRetroAchievementsGameUseCase` tries it first via `GameHashMatcher`, ahead of every title tier, before falling back to today's title matching - currently a no-op for all real users since ES-DE doesn't write real hash data yet, but the pipeline (including `RetroClientRetroAchievementsApi` requesting `shouldRetrieveGameHashes = 1` and `RetroAchievementsCandidateGame.hashes`) is in place and unit-tested. A `WidgetType.AchievementSummary` widget (Playing canvas only, debug-flagged via `retroAchievementsEnabled()` in the Add Widget catalog - `EditWidgetsOverlay.widgetCatalogFor` - same picker-only gating every other RetroAchievements entry point uses) shows a compact unlocked/total/points/percent stat line for the current game. `WidgetsViewModel.resolveContent` peeks the cache instantly (`PeekGameAchievementSummaryUseCase`) and, when nothing useful is cached, fires a background fetch via `viewModelScope.launch` that bumps a new `achievementRefreshTrigger` input to `canvasState`'s `combine(...)` on completion - re-running `resolveContent` to pick up the fresh value, rather than adding a widget-specific side channel into `WidgetCanvas`. One fetch attempt per gameId per ViewModel lifetime (`lastAchievementFetchGameId`), not a periodic poll. `EditWidgetsViewModel`'s edit-mode preview mirrors this with a peek-only read (`LastKnownContextRepository`'s last-browsed game) and never calls the real fetch use case, unlike Video's "always placeholder" precedent - achievement data is a small cached value, not a heavyweight resource, so a cache-only preview costs nothing at edit time.
- **FileObserver self-healing and removable-storage (SD card) mount detection are built.** `data/storage/DirectoryWatcher.kt`'s `SelfHealingDirectoryWatcher` wraps the plain `FileObserverDirectoryWatcher` (moved out of `EsdeLogFileRepository.kt`, now shared): it refuses to arm a watch against a parent directory that doesn't exist yet (an unmounted SD card holding the ES-DE folder), and exposes `recheck(forceRearm)` so a caller can force a teardown-and-rearm once there's reason to believe the directory's existence may have changed. Two things call `recheck`: (1) `EsdeLogFileRepository`/`FileEsdeInstallationRepository`'s existing fallback poll, whenever it catches a real change the watcher should have caught but didn't — direct proof the watch has gone stale; (2) a new manifest-registered `data/storage/StorageMountReceiver`, listening for `MEDIA_MOUNTED`/`MEDIA_UNMOUNTED`/`MEDIA_EJECT`/`MEDIA_REMOVED`/`MEDIA_BAD_REMOVAL`, forwarding into `data/storage/StorageMountEvents` (a small hot `SharedFlow` bus) — the one narrow, deliberate exception to this project's "constructor injection throughout" rule, since a manifest-declared `BroadcastReceiver` is instantiated by the OS via a required no-arg constructor with no injection path available; it does nothing but call `AppContainer.storageMountEvents.notifyChanged()`. Both self-heal signals (the storage-events flow and its diagnostic callbacks) are bundled into one `data/storage/SelfHealConfig.kt` parameter on each repository's constructor, rather than several separate ones, to stay within this project's `LongParameterList` limit (see the ktlint/detekt gotcha below). See `AppContainer.kt`'s `storageMountEvents`/`logRepositorySelfHeal` wiring.
- **Thor Settings (Ayn Thor-only: Lid Wake Guard + Auto FPS Mode) is built**, ported from the sibling Asgard app (`C:\Users\rober\AndroidStudioProjects\Asgard`). A new `SettingsCategory.Thor` entry, filtered out of `SettingsMenuHome`'s rendered list unless `data/thor/DeviceCheck.isAynThorDevice()` is true (the `when` branch that routes to it in `LongPressSettingsMenu` still exists unconditionally, same forcing-function reasoning as every other category). **Lid Wake Guard** re-locks the screen if a stray button press wakes it while a Hall-effect sensor reads the clamshell lid as still closed; **Auto FPS Mode** boosts the top screen to 120Hz for a user-chosen set of trigger apps (including ones launched from Companion's own App Drawer) via a full port of Asgard's accessibility-based foreground-app detection, not a shortcut off `AppState` — it needs to see App-Drawer launches `AppState` knows nothing about, and some ES-DE library entries that report as "playing a game" aren't actually games that should get boosted. Both settings persist via `data/thor/FileThorSettingsRepository` (its own DataStore, not `OnboardingRepository`'s) and are covered by Backup & Restore (`domain/repository/BackupRepositories.kt` bundles all six settings repositories `Export`/`RestoreConfigBackupUseCase` touch into one parameter, keeping those constructors within this project's `LongParameterList` limit — same reasoning as `SelfHealConfig`).
  - **The first `AccessibilityService` in this codebase**: `data/thor/accessibility/CompanionAccessibilityService.kt`, deliberately narrower than Asgard's current `AsgardAccessibilityService` (which now also handles BACK-key/volume-key events for two Asgard-only features, Task Killer/Volume Sync, that don't exist here) — only `typeWindowStateChanged`, `canRetrieveWindowContent="false"`, no `canRequestFilterKeyEvents`. Only ever bound if the user explicitly enables a Thor Settings toggle; see `res/xml/thor_accessibility_service_config.xml`.
  - **Filters accessibility events to `Display.DEFAULT_DISPLAY` only, same as Asgard** — see the Known Gotchas entry below for why the initial port's "don't filter by display" reasoning was wrong and confirmed broken on-device.
  - **Opt-in-gated activation, the deliberate architectural divergence from Asgard's always-on `Application.onCreate()` install**: `data/thor/LidWakeGuardCoordinator.kt`/`AutoFpsCoordinator.kt` are constructed unconditionally in `AppContainer` (cheap, same as `SelfHealingDirectoryWatcher` instances), but each coordinator's own `BroadcastReceiver`/accessibility-listener registration stays gated behind its own DataStore-persisted enabled flag, torn down the moment it's turned off. Whether a screen-on/foreground-change actually *does* anything additionally depends on the user having separately granted the accessibility service (`data/thor/ThorAccessibilityPermission.kt`, same `isEnabledInSettings`/`requestIntent` shape as `AllFilesAccessPermission`) — that grant check doesn't need to gate registration itself, since `CompanionAccessibilityService.requestLock()`/the foreground-package feed simply do nothing when the service isn't bound.
  - **`data/thor/PrivilegedShell.kt`/`RefreshRateController.kt`** are a near-verbatim port of Asgard's reflection-based `ServiceManager.getService("PServerBinder")` binder-transact mechanism for writing `min_refresh_rate`/`peak_refresh_rate` directly, bypassing the public `Settings.System` API's `targetSdkVersion`-gated client-side block — see `PrivilegedShell`'s kdoc for the full provenance. Inherently brittle (Thor-firmware-specific hidden-API reflection, could break on any AYN OTA) and must degrade to "feature unavailable" (`RefreshRateController.canWrite()` false → Auto FPS UI shows "not available on this firmware"), never crash — confirmed via `@Suppress("TooGenericExceptionCaught")` on `PrivilegedShell.execute()`, deliberate, not an oversight. Also ports Asgard's "widen before narrow" write-ordering (peak before min when raising, min before peak when lowering) to avoid an inverted min>peak range mid-write, a plausible cause of a display going blank and needing a hard power cycle observed during Asgard's own testing.
  - **Hall-sensor calibration is fully automatic**, no manual picker screen ported (Asgard's `SensorPickerActivity` fallback is intentionally not carried over): the sensor is a plain on/off switch, so `ui/settings/SettingsContent.kt`'s `LidWakeGuardSetting` auto-picks the best-scoring Hall sensor (`data/thor/sensor/HallSensorPicker.kt`) and treats whichever value is live *right now* as "open" (the screen has to be on, and the user has to be looking at the panel, for that composable to be running at all — the lid can't be closed) the first time the panel is shown while uncalibrated.
  - **No system notification for Lid Wake Guard's status** (unlike Asgard, which shows one) — no `POST_NOTIFICATIONS` permission, no notification-channel infrastructure exists in this app; guard status is in-app only, in the Thor Settings panel itself.
  - Pure decision logic (`domain/thor/{LidWakeGuardDecision,RefreshRateDecision}.kt`) is unit-tested (`RefreshRateDecisionTest`/`LidWakeGuardDecisionTest`) to the same standard as everything else in `domain/`; the accessibility service, sensor reads, and the privileged-binder call itself are only verifiable on physical Thor hardware, not via instrumented tests.
- **Video is a widget, not a global Settings toggle.** The old Settings > Video Playback menu (enabled/delay/audio, a full-screen `VideoOverlayScreen` drawn on top of everything) is retired entirely, replaced by `WidgetType.Video` — placeable/resizable on the Playing canvas exactly like `GameMedia`, with its own per-instance Configure Widget dialog (audio, start delay, `ScaleMode` Contain/Cover, and a Contain-only pillarbox choice — black bars vs. transparent — via `PillarboxMode`; Cover always fills the widget with no empty space to pillarbox, so the picker is hidden under Cover, see `WidgetType.supportsPillarbox`). Still only ever resolves while `AppState.BrowsingGame` (never during actual gameplay), same restriction as the retired global toggle — enforced by `WidgetsViewModel` gating what it passes into `WidgetContentResolver.resolve`'s `videoLookup` parameter (`null` when not eligible), not by the resolver itself. `ui/widgets/WidgetVideoContent.kt` is the widget-sized rendering (same double-buffered muted-then-promoted `ExoPlayer` prepare mechanics the old overlay used, just sized to the widget's placed bounds instead of full-screen); `WidgetOverlay`'s `videoPlaybackEligible` parameter reproduces the old overlay's `mainScreenActive && isActivityVisible` gating by substituting `WidgetContent.Empty` for any `WidgetContent.Video` entries while ineligible, since `WidgetsViewModel` itself has no notion of "another overlay is drawn on top" or "the Activity isn't visible right now." Edit-mode preview deliberately never renders real video (always falls back to the placeholder box, like any other unresolvable widget) — live playback during drag-and-drop editing was judged out of scope. `VideoPlaybackStateRepository`/`MusicPlaybackCoordinator` ducking is unchanged, just fed from the widget's playback callback instead of the old overlay's. See `domain/model/{WidgetType,WidgetContent,PillarboxMode}.kt`, `domain/model/WidgetContentResolver.kt`, `ui/widgets/{WidgetVideoContent,WidgetCanvas,WidgetOverlay,WidgetsViewModel}.kt`, `ui/widgets/edit/WidgetVideoConfigControls.kt`.
- **Game Guides is built and, as of 1.1.0, no longer gated behind a debug-only feature flag** — `gameGuidesEnabled()`/`GameGuidesFeatureFlag.kt` were removed entirely; the feature now ships in release builds like everything else. Per-game guide library (Settings > Game Guides, or the `FabType.GameGuides` FAB) reads four formats — `GameGuideFormat.PlainText`/`Html`/`Pdf`/`Image` — added either by importing a file (`GameGuideImportPicker`, `.txt`/single-page `.html`/`.pdf`/an image extension) or by browsing/downloading from GameFAQs in-app (`data/gameguides/GameFaqsBrowserBridge`'s `WebView`-driven page detection/pagination, plus `NativeImageDownloader` for the guide's own images — see its kdoc and the project's networking exceptions in What NOT to Do). PlainText and Html guides both get chapter/TOC navigation (`GuideTocOutlineExtractor` for Html's real `.ftoc`-derived structure, `PlainTextGuideTocParser` synthesizing an equivalent outline for PlainText) plus in-guide search with match highlighting (`GameGuidePlainTextViewer`/`GameGuideHtmlViewer`); Pdf reuses Game Manual's page-by-page `PdfPageViewer` with pinch-zoom and page turning; Image is a single pinch-zoom view (`GameGuideImageViewerScreen`), sharing its pinch/pan gesture code with `PdfPageViewer` via `ui/PinchZoomGesture.kt`. `ScreenBehavior.GameGuide` (Settings > UI Settings > Game Playing Screen Behavior > "Guide") auto-opens the current game's most-recently-viewed guide on game start, resumed at its last read position, with an opt-in fallback to the game's manual when no guide is downloaded yet (`GameGuidesSettingsContent`'s manual-fallback toggle). A separate "Update on Screensaver" toggle keeps an already-open guide library in sync with whichever game the screensaver is showing. `GameGuideSettingsRepository` (the toggles + reading text-size preference) is covered by Backup & Restore; the downloaded guide library itself (`GameGuideLibraryRepository`) deliberately isn't — it's re-downloadable/re-importable content, not a setting, same reasoning as RetroAchievements' credentials exclusion. See `domain/gameguides/`, `domain/model/{GameGuideFormat,DownloadedGameGuide,GuideTocEntry}.kt`, `data/gameguides/`, `ui/gameguides/`, `ui/settings/{GameGuidesSettingsContent,AddGuideMethodScreen,AddGuideGamesScreen,DownloadedGuidesScreens}.kt`. As of 1.1.1, Html guide images load `loading="lazy"` with real dimensions pre-resolved (`data/gameguides/GuideImageDimensionAnnotator.kt`) so page opens/turns/TOC jumps stay fast and accurate on image-heavy guides without waiting on decode — see the three related Known Gotchas entries below for the full reasoning and the mistakes that led there.

## Known Gotchas (Confirmed Fixes — Don't Re-Litigate)

These cost real debugging time to track down. If you hit a symptom that matches one of these, the fix below is already applied; look for where it lives before proposing a new one.

- **`navigation-compose` predictive-back crash (b/384186542 / b/375343407).** Adding `SETTINGS` (or any similarly deep screen) as a real `NavHost` destination triggers an `IndexOutOfBoundsException` in `PredictiveBackHandler` on the versions this project uses. Fix: `SETTINGS` is *not* in `MainActivity`'s `NavHost` graph at all — it's shown/hidden via a plain `showSettings` boolean, and `SettingsScreen`'s own internal drill-down (category list → subpage → Manage Apps) is likewise plain `rememberSaveable` state with a manual `onBack` chain, not a nested `NavHost`. `EditWidgetsOverlay` follows the same pattern (`showEditWidgets` boolean, not a `NavHost` destination). See the doc comment on `SettingsCategory`. Do not reintroduce nav-compose destinations for these screens.
- **`CrossfadeAsyncImage` needs exactly one call site per role.** Multiple `when`-branch `AsyncImage`s for what is conceptually "one thing" cause Compose to dispose/remount on state-shape change, discarding `remember`ed crossfade animation state and producing a flash to the empty background. `WidgetCanvas`'s `WidgetContentView` keys each widget by its stable `id` (`key(widget.id) { ... }`) so identity survives reordering/recomposition per-widget, the same principle generalized from the original fixed-backdrop case. Note that transparent overlay-style content (the system logo, and `MediaType.Marquees`) deliberately uses a plain `AsyncImage` instead of `CrossfadeAsyncImage` — it's a transparent PNG/SVG with nothing opaque behind it, and crossfading it briefly double-exposes the outgoing and incoming images (see `WidgetContent.Image.crossfade`). Also note that `CrossfadeAsyncImage`'s pre-decode-then-swap protection (see its own kdoc) is bypassed on a *fresh composable mount* — it initializes `currentModel = model` directly as its first value, so there's nothing for its `LaunchedEffect` to distinguish from and the very first frame renders whatever Coil resolves live, blank until decode finishes. Any code path that remounts it (not just a same-canvas `when`-branch change) must pre-decode before mounting rather than trusting the composable's own protection — `WidgetOverlay`'s `rememberDisplayedCanvas` does this for the System/Playing canvas swap (a genuinely different widget id per canvas triggers a remount of the backdrop's `CrossfadeAsyncImage` instance), holding the previous canvas on screen until the incoming backdrop is already decoded into Coil's memory cache.
- **Independent ViewModels resolving the same logical transition asynchronously can produce a real, momentary "neither" state even when their underlying source states are mutually exclusive.** `RetroAchievementsViewModel` (tracks the current *game*) and `RetroAchievementsSystemGamesViewModel` (tracks the current *system*) are logically complementary — ES-DE is never simultaneously browsing a system and playing/browsing a game — but each resolves its own state independently and asynchronously (a candidate-list/ROM-hash lookup, or a system's cached game list fetch). On a live system↔game navigation, the side losing context drops to "no content" *synchronously*, while the side gaining context is still mid-resolve, so a value derived from both (`MainActivity`'s `retroAchievementsFabMode`) is genuinely `None` for one or more frames — confirmed on-device as a brief flash of the raw widget canvas through the RetroAchievements overlay (initially mistaken for the `CrossfadeAsyncImage` backdrop issue above; that fix alone didn't resolve it). Reacting to that momentary `None` at face value both hid both RetroAchievements screens (their `AnimatedVisibility`s require a mode match) and permanently auto-dismissed the overlay via a `LaunchedEffect` keyed on the raw value. Fixed by `rememberHeldRetroAchievementsFabMode` — holds the previous non-`None` mode for a short grace period (`RETRO_ACHIEVEMENTS_FAB_MODE_NONE_GRACE_PERIOD_MILLIS`) before actually reporting `None`, so a genuine transition lands on the new mode before the hold expires, and only a truly-nothing-to-show case (signed out, Idle) falls through to `None`. If a similar "combine two independently-resolving async states into one UI decision" pattern shows up elsewhere, the same hold-briefly-before-reacting shape applies, not a deeper restructure of either source.
- **Directory-as-file media paths.** ES-DE can report a ROM *directory* (e.g. a `.m3u` multi-disc folder) as the launchable unit, and does not strip the trailing extension in that case the way it does for an ordinary file — the extension there is the directory's literal name, not a stripped file extension. This can't be determined from the path string alone; `GameMediaPathResolver` takes an explicit `romIsDirectory: Boolean` computed via real filesystem `File.isDirectory` checks in the data layer, keeping the resolver itself pure and unit-testable against fixture paths.
- **Hardware/system back must never exit the app.** It's meant to run continuously on a secondary display. Handling is a `BackHandler` in `MainScreen` (closes the App Drawer if open, otherwise consumes the event and does nothing), a separate `BackHandler` in `SettingsScreen` driving its own back-stack state, and a step-back `BackHandler` in `EditWidgetsOverlay` (dialog → deselect widget → exit edit mode). There is no debounce/gating layer over any of these — a suspected vendor-ROM synthetic-back-event issue turned out, on investigation, to be an unrelated third-party key-mapping app intercepting the physical button, not anything in this app's own back handling; confirmed twice now across two separate build phases. If a back-button freeze or double-fire resurfaces, check for an interfering key-mapping/accessibility app on the device *first*, then verify it isn't one of the already-fixed causes above, before adding new debounce machinery.
- **Cold `Flow`s collected independently by multiple ViewModels duplicate their upstream work.** `EsdeLogRepository.observeEvents()` is a cold `Flow` — every ViewModel that collected it independently was triggering its own `FileObserver`, its own fallback poll timer, and its own parsing of the same log lines, all running concurrently for the app's entire kiosk uptime. Fixed by `SharedEsdeLogRepository`, wrapping the tailer in `shareIn()` so there's exactly one collection regardless of subscriber count. The same lesson applies one layer up: `ObserveAppStateUseCase`'s reducer fold must also be shared (`stateIn`, not rebuilt per call) — the raw event stream beneath it has no replay, so a late-joining subscriber (e.g. a ViewModel restarting after its own `WhileSubscribed(5_000)` expires while Settings/Edit Widgets was showing) rejoined an already-warm stream with no history and sat at `AppState.Idle` until the next live event, even though the real current state was something else entirely.
- **`FileObserver` can't watch a directory that doesn't exist yet, and never self-heals once armed.** `android.os.FileObserver.startWatching()` does not throw when the underlying `inotify_add_watch` fails (e.g. because the parent directory it's watching - possibly on removable storage that isn't mounted yet - doesn't exist) - it silently no-ops, and since inotify has no way to "start watching a path once it exists later," that watch is permanently dead for the life of that `FileObserver` instance. This app can auto-start via `MainActivity`'s `HOME` intent filter before a reboot has finished mounting an SD card, so if the ES-DE folder lives on one, `EsdeLogFileRepository`'s original single-arm-at-construction `FileObserver` would stay dead for the rest of the process's life once that race was lost - `SharedEsdeLogRepository`'s `shareIn(applicationScope, WhileSubscribed(5_000))` means that flow effectively never restarts on its own while the app keeps running. Fixed by `SelfHealingDirectoryWatcher` (see Feature Status above) - never arms against a nonexistent directory in the first place, and rearms on either proof-of-staleness (a fallback poll catching a change the watch missed) or a storage mount/unmount broadcast. If a similar "watches a path, silently stops working, nothing ever notices" symptom shows up elsewhere, check whether the watched path can transiently not exist before reaching for a bigger fix.
- **`pointerInput` closures go stale unless the key includes everything they read - but the key must NOT include values that change mid-gesture.** A drag detector keyed only on a widget's `id` kept using that widget's *original* size/position for its bounds clamp even after a resize changed it (`pointerInput` only restarts its coroutine, and re-captures its closure, when its key changes) — a widget resized smaller than the screen became stuck unable to move, since the clamp still used its old full-screen size. Keying on the whole mutable object instead fixes staleness but breaks continuous dragging: the object changes on every cell crossing *during the same gesture*, and each key change cancels/restarts the coroutine, ending the gesture and requiring a new finger-down. Fix: keep the key narrow and stable (id + whatever's genuinely static, e.g. cell size), and use `rememberUpdatedState` to read the latest value only where it should be fresh (e.g. `onDragStart`), not as the key itself. See `EditWidgetsOverlay`'s `PlaceholderWidgetBox`/`ResizeHandle`.
- **Delta-based drag gestures can't reach a container's true edge on their own.** Growing a size/position in proportion to finger movement works until the finger nears the physical screen edge — there's no more room left to move onto, so the last cell(s) of travel can never accumulate. Fix: track the gesture's actual on-screen position separately from the per-cell accumulator, and snap straight to the boundary once that position is within a small threshold of the true edge, rather than requiring exact remaining distance. See `ResizeHandle`'s `EDGE_SNAP_THRESHOLD`.
- **System gesture-navigation edge zones intercept touch before Compose sees it.** Dragging a widget toward the screen edge in Edit Widgets lost tracking right at the boundary — Android's edge-swipe gesture zones (back-gesture areas) claim touch input in those regions regardless of what the app wants. Fix: `View.systemGestureExclusionRects`, set for the duration `EditWidgetsOverlay` is visible and cleared on exit via `DisposableEffect`, so it doesn't affect back-gesture behavior anywhere else in the app.
- **Persistent full-width overlay chrome can make content behind it permanently unreachable, not just hidden.** Edit Widgets originally floated a translucent header bar over the whole top of the screen; a widget resized/moved to sit entirely behind it became untappable — visible through the translucency, but touch couldn't reach it, with no way to select it again short of clearing saved data. Fixed architecturally rather than by clamping widget placement around the header: replaced it with a single small corner options button (⋮) that opens a menu, leaving the *entire* grid — including what used to be behind the header — genuinely free for widget placement.
- **Draw-order overlays (e.g. a grid-line guide) need an explicit `zIndex`, not just composition order.** Once individual widgets carry their own `zIndex` values (for layering), a purely-visual layer declared last in code can still end up drawn *underneath* a widget with a higher `zIndex` — composition order only wins when nothing has an explicit `zIndex` set. `EditWidgetsOverlay`'s grid-line `Canvas` uses `Modifier.zIndex(Float.MAX_VALUE)` so it always wins regardless of any widget's value.
- **`Modifier.align()` on a composable nested inside `AnimatedVisibility`'s content lambda is silently ignored.** `Box` only reads `BoxScope.align()` parent-data from its *direct* children — and inside `BoxWithConstraints { ... AnimatedVisibility(visible = ...) { SomeComposable(modifier = Modifier.align(...)) } }`, `AnimatedVisibility` (not `SomeComposable`) is that direct child. Putting `align()` on `SomeComposable`'s own modifier compiles fine (the extension resolves against the outer `BoxWithConstraintsScope` receiver, which is still in scope) but has no layout effect — the child silently falls back to the Box's default `Alignment.TopStart`. `MainActivity`'s `MusicFabContent` had exactly this bug for months without being noticed, because its *only* position was top-start, which happens to equal the default — the bug only became visible once the Music FAB became assignable to other corners (see FAB Control above) and the panel stayed pinned to the screen's left edge regardless of which corner the FAB was actually in. Fix: put `align()` (and any other Box-scoped placement modifiers) on `AnimatedVisibility`'s own `modifier` parameter, not on a composable inside its `content` lambda.
- **`ktlintCheck`/`ktlintFormat` and `detekt`'s bundled formatting rules disagree on some multi-line indentation and line-length forms.** `ktlint` (via `org.jlleitschuh.gradle.ktlint`) defaults to a 140-char line limit and prefers collapsing short single-expression function bodies onto the signature line; `detekt`'s default `MaxLineLength`/`Indentation` rules (via the bundled `detekt-formatting` plugin) use a stricter 120-char limit and sometimes expect *less* indentation for a `name =` value continuation than `ktlintFormat` itself applies — so `ktlintFormat` can "fix" a line into a form `detekt` then rejects, and vice versa, for lines in the 120–140 char band or nested `arg =\n    value` continuations. There's no single config change that reconciles this (encountered adding the update checker's `ui/update`/`data/update` files). Work around it per-line: prefer a block function body (`{ return ... }`) over a single-expression one when the collapsed form would land in the 120–140 range; hoist a multi-line `Modifier`/collection value out to its own local `val` before passing it as a named argument, rather than writing `arg =` immediately followed by a wrapped value, to sidestep the indentation mismatch entirely. Also don't blindly regenerate `config/detekt/baseline.xml`/`config/ktlint/baseline.xml` for these — baseline entries are keyed by exact (whitespace-normalized) signature/argument text, so adding a parameter to an already-baselined function changes its key and makes the *pre-existing* violation look "new"; when that happens, hand-edit just that function's baseline entry to match its new signature (confirmed via `git stash` that the old signature was genuinely already baselined) rather than running the full baseline-regenerate task.
- **A `rememberCoroutineScope()` tied to a composable that dismisses/removes itself doesn't outlive the dismissal.** Backup & Restore's restore-confirmation `AlertDialog` originally called `onDismiss()` (removing itself from composition, via the `pendingRestoreUri = null` state write that gated its `if` block) and *then* `coroutineScope.launch { ... the actual restore work ... }`, both using a `coroutineScope` obtained from `rememberCoroutineScope()` inside the dialog composable itself. `launch` doesn't run its body synchronously — it schedules the coroutine, and the pending recomposition that tears the dialog out of composition (cancelling that exact scope) reliably wins the race, so the restore silently never ran: no crash, no error, nothing. Confirmed on-device (export worked, restore didn't) before being traced to this. Fix: do the actual work on a `coroutineScope` that belongs to a composable which stays in composition (the dialog's *caller*, not the dialog), and make the dialog itself a dumb confirm/cancel prompt with no coroutine logic of its own — see `SettingsContent.kt`'s `BackupRestoreSetting`/`RestoreBackupConfirmationDialog`. The general lesson: never launch on a scope owned by the same composable an `onClick` handler is about to conditionally remove from composition.
- **A configured wait/delay is not the same window as a technical loading gap - bridging one can silently swallow the other.** 1.0.3's video-transition-flash fix (`VideoOverlayScreen`, since retired in favor of the Video widget's `WidgetVideoContent` - see Feature Status - but this same `transitionToVideo`/`dropDisplayedVideoForDelay` logic carried over unchanged) kept the previously-displayed game's video playing across the *entire* `transitionToVideo` call, including the deliberate `delay(delaySeconds)` wait that the video widget's own Start Delay config exists specifically to enforce (so quickly scrolling the game list doesn't play a video per game). Net effect: with a nonzero delay configured, scrolling through several games in a row kept restarting that wait (each `videoPath` change cancels the prior `LaunchedEffect(videoPath)` coroutine) without ever completing it, so the *previous* game's video just kept looping for as long as scrolling continued, and the widget canvas underneath never got a chance to show - reported as "widgets no longer shown while scrolling" in 1.0.4. Fix: only bridge across the genuinely technical buffering gap right before playback; when `delaySeconds > 0`, drop the previously-displayed video immediately (`dropDisplayedVideoForDelay`) back to the widget canvas *before* waiting out the delay, not after. A zero delay has no such wait to occupy, so it still bridges straight into the buffering gap as before. The general lesson: when bridging/pre-decoding to hide a loading gap, scope the bridge tightly to the load itself - don't let it silently absorb an unrelated, intentional wait sitting in the same code path.
- **Not filtering `CompanionAccessibilityService`'s window-state events by display id was a bug, not the deliberate divergence the initial Thor Settings port claimed.** The reasoning at the time was that Companion runs on the secondary/bottom display and App-Drawer-launched apps "land there too," so Auto FPS Mode needed foreground-package changes from *any* display, not just `Display.DEFAULT_DISPLAY` (unlike Asgard, which does filter to it). That premise was wrong: `SecondaryDisplayResolver` launches an app onto whichever display Companion itself is *not* running on, which — since Companion runs on the secondary display — is always the top screen (`DEFAULT_DISPLAY`), the same one ES-DE/games run on. So App-Drawer-launched apps were never the reason to skip the filter. Confirmed on-device: without it, Companion's own bottom-screen UI (dialogs, Settings, App Drawer opening/closing) generated a stream of foreground-package "changes" indistinguishable from a genuine top-screen app switch, causing spurious refresh-rate flips and system/launcher packages slipping past the ignore-list's intent — exactly the failure mode Asgard's filter exists to prevent. Fix: `CompanionAccessibilityService.onAccessibilityEvent` now filters to `Display.DEFAULT_DISPLAY` only, same as Asgard; this loses nothing, since every event Auto FPS Mode actually needs (top-screen games, top-screen App-Drawer-launched apps) already arrives on that display. If a future feature genuinely needs bottom-screen (Companion's own) window-state events, that's a new, separate listener feed — don't remove this filter to get it.
- **`WebViewClient.onPageFinished` is not the DOM `load` event — it's the WebView's network-idle signal, and it waits on background activity you deliberately scoped to run *after* the page should already be visible.** Game Guides' Html viewer (`GameGuideHtmlViewer.kt`) marks every `<img>` `loading="lazy"` (`GuideHtmlDocumentBuilder.kt`) so first paint doesn't wait on off-screen images, then promotes them all back to eager once `window`'s `load` event fires so they keep loading in the background. A `loading="lazy"` image is specifically excluded from delaying the spec `load` event — but gating this app's own reveal on `onPageFinished` undid that entirely, because `onPageFinished` doesn't fire until *every* resource the WebView is aware of has settled, including the images that background promotion had just started fetching. Confirmed on-device via temporary timing logs: page turns on an image-heavy guide were still taking 1.5–2.5s despite the lazy marking. Fix: `loadAndAwaitStarted` (renamed from `loadAndAwaitFinished`) gates on `onPageStarted` instead — fires as soon as navigation begins, well before any resource has loaded — with `awaitScrollableLayout`'s existing bounded poll absorbing the small remaining "navigation started but text hasn't flowed in yet" gap. If a future WebView-based feature needs to know "is the page visually ready," don't reach for `onPageFinished` as a proxy for that — it answers "is all network activity done," a different and often much later question.
- **Waiting for images to finish *decoding* before trusting a WebView's `scrollHeight`/`scrollIntoView` is a workaround for not knowing their dimensions, not the actual fix.** An `<img>` with no declared `width`/`height` collapses to near-zero height until it decodes, so restoring a saved scroll fraction or jumping to a TOC anchor against a still-decoding page landed in the wrong place — first fixed with runtime waits (poll until every relevant image reports `.complete`), which worked but capped how fast a jump could ever be, since it was tied to real decode time. The actual fix: `data/gameguides/GuideImageDimensionAnnotator.kt` reads each image's real dimensions via a header-only `BitmapFactory.Options.inJustDecodeBounds` file read (no pixel decode, cheap enough to do on every page load) and bakes `width`/`height` attributes into the `<img>` tags before the document is built — the existing `img { max-width: 100%; height: auto; }` CSS means this doesn't break responsive scaling (browsers use the attributes only to compute the intrinsic aspect ratio ahead of decode), so the browser reserves the correct layout space immediately regardless of when the image actually finishes loading. `GameGuideHtmlViewer.kt`'s `awaitStableScrollHeight`/`awaitStableAnchorPosition` then wait for layout to stop *reflowing* (two consecutive polls agreeing), not for images to finish *decoding* — the same fallback behavior for an unresolved image (missing file, decode failure), but no longer an artificial ceiling on the common case where dimensions were already resolved. General lesson: if a wait exists only because a size/position isn't known yet, look for a way to make it known up front instead of tuning how long to wait for it.
- **`buildGuideContentState`'s `isResumedPage` check (`currentPageIndex == initialPageIndex`) conflated "the page the guide was opened onto" with "any later visit to that same page index."** `initialPageIndex` never changes for the life of a guide-viewing session, so once the reader navigated away and later paged back to that same index, the check matched again and silently re-applied the *original* saved scroll fraction instead of landing at the top like every other page visit. Fixed by `GuideViewerUiState.resumeConsumed` — set `true` at every real page-navigation call site (next/previous, an in-content link jump, a TOC jump) — so `isResumedPage` requires *both* the index match *and* that no navigation has happened yet. The general shape: a "was this the original X" check based purely on comparing current state to a fixed initial value breaks the moment state can return to that initial value through a different path than the original — needs an explicit "has this already been consumed" flag, not just an equality check.

## Tech Stack

| Concern | Library |
|---|---|
| UI | Jetpack Compose (Material 3) |
| Navigation | Navigation-Compose, pinned at 2.8.4 — used only for the `ONBOARDING`/`MAIN` top-level switch; deep/nested screens deliberately avoid it (see Known Gotchas) |
| Async | Kotlin Coroutines + Flow |
| Image loading | Coil 3, with the SVG decoder |
| Persistence | Jetpack DataStore (Preferences) — except RetroAchievements credentials, the app's one secret-shaped piece of state, which use `androidx.security-crypto`'s `EncryptedSharedPreferences` (AES256_GCM master key) instead; see `EncryptedRetroAchievementsCredentialsRepository` |
| Serialization | kotlinx.serialization (widget canvas layout persistence; GitHub Releases API responses for the update checker) |
| Networking | `HttpURLConnection` for the update checker and Game Guides' image downloads; `RetroAchievements/api-kotlin` (Retrofit/OkHttp/Gson, via JitPack) for RetroAchievements — three separate, deliberately narrow exceptions, see What NOT to Do |
| Build | Gradle Kotlin DSL + version catalog (`libs.versions.toml`) |
| Testing | JUnit4, Turbine (Flow testing) |

Minimum SDK: API 29 (Android 10).

Media3/`ExoPlayer` backs the Video widget (`ui/widgets/WidgetVideoContent.kt`) and background music (`data/music/ExoMusicPlayerController.kt`) — the only two places this app plays audio/video.

## What NOT to Do

- Don't let the event-parsing/state-reduction layer depend on anything outside plain Kotlin — no Android imports, no leaking UI or media concerns into it.
- Don't reintroduce script generation/writing to ES-DE's `scripts/` folder.
- Don't bypass the repository layer to touch the filesystem or `PackageManager` from UI code.
- Networking is no longer restricted to a fixed enumerated list — the app now has several legitimate online features (the update checker, RetroAchievements, GameFAQs guide browsing/downloading) and adding another networked feature doesn't need a standing exception carved out in advance. Prefer `HttpURLConnection` + `kotlinx.serialization` for a plain REST-ish call (matching the update checker's own style) over pulling in a new HTTP stack, unless the thing being talked to already has an official Kotlin/Java client worth using as-is (as RetroAchievements does). When adding or changing a networked feature, call out anything with real user-facing implications — new data leaving the device, new always-on background traffic, new credentials/PII involved, cost/rate-limit exposure — as a plain warning rather than treating it as a blocker.
  - The update checker (`domain/repository/UpdateRepository.kt` / `data/update/GitHubUpdateRepository.kt`) talks to the GitHub Releases API using plain `HttpURLConnection` + `kotlinx.serialization`.
  - RetroAchievements (`domain/repository/RetroAchievementsRepository.kt` / `data/retroachievements/`) talks to the RetroAchievements Web API via the official `RetroAchievements/api-kotlin` client (JitPack — `settings.gradle.kts`'s `dependencyResolutionManagement.repositories`, required by `repositoriesMode = FAIL_ON_PROJECT_REPOS`), which transitively pulls in Retrofit + OkHttp + Gson. `data/retroachievements/RetroAchievementsApi.kt` is a narrow seam over api-kotlin's real `RetroInterface` — the actual Retrofit/OkHttp/Gson types never leak past `RetroClientRetroAchievementsApi`, so `RetroAchievementsRepositoryImpl` stays unit-testable against a hand-rolled fake. **One confirmed, deliberate exception**: `RetroClientRetroAchievementsApi.fetchAchievementTypesById` bypasses api-kotlin entirely for one specific field, via a raw `HttpURLConnection` call (same pattern as the update checker) to `GetGameExtended` plus `org.json` parsing. This works around a confirmed upstream api-kotlin 2.0.0 bug: achievement `type`/classification (Missable/Progression/WinCondition) comes back `null` through api-kotlin's own client on both endpoints that could supply it — `GetGameInfoAndUserProgress`'s response has no `type` key at all, and `GetGameExtended`'s own class annotates the field `@SerializedName("Type")` while the live API's real key is lowercase `type`, confirmed via raw JSON inspection — and the library exposes no hook to override Gson's field mapping from outside. Revisit and remove this workaround once a fixed api-kotlin release exists (see the TODO at the top of `RetroClientRetroAchievementsApi.kt` — file an upstream issue if one doesn't already exist).
  - Game Guides' browsing/downloading (`data/gameguides/`) uses a `WebView` for browsing GameFAQs and driving page detection/pagination, plus `data/gameguides/NativeImageDownloader.kt` fetching a downloaded guide's images as base64 data URIs via plain concurrent `HttpURLConnection` calls (same tool as the update checker, just fetching image bytes by URL) rather than the WebView's own `fetch()` — the WebView's per-origin connection limit and single rendering thread, shared with page navigation, measured taking 5-10 minutes to download a real 19-chapter guide's images, versus seconds in a comparable third-party app. See `GameFaqsBrowserBridge`'s and `NativeImageDownloader`'s kdocs for the full reasoning.
- Don't split into multiple Gradle modules preemptively — package-based layering is sufficient at this size.
- Don't add `NavHost` destinations for `SETTINGS`, `EDIT WIDGETS`, or other deep screens — see Known Gotchas.
- Don't add back-press debounce/gating logic to work around a suspected double-fire without first checking for an interfering key-mapping/accessibility app on the device, then confirming it isn't caused by one of the already-fixed issues above.
- Don't let multiple ViewModels each independently collect a cold `Flow` that does real work (`FileObserver`, polling, file reads, a reducer fold) — share it (`shareIn`/`stateIn`) at the `AppContainer` level instead. See Known Gotchas.

## Useful Commands

```bash
./gradlew ktlintCheck detekt         # lint
./gradlew testDebugUnitTest          # unit tests
./gradlew connectedDebugAndroidTest  # instrumented/UI tests
./gradlew assembleDebug              # build APK
```

---
> Source: [RobZombie9043/es-de-companion](https://github.com/RobZombie9043/es-de-companion) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-09-09 -->
