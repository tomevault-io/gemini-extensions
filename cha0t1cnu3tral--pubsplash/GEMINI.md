## pubsplash

> This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Pubsplash is an accessibility-first Windows streaming client for Audio Pub (an Icecast-based livestreaming platform, server source: github.com/the-byte-bender/audiopub-sv), built with wxDragon (wxWidgets bindings). Its users are primarily screen-reader users; every control must be Tab-reachable and properly announced. The full product spec is in `project2.md` (`project.md` is an earlier draft of the same spec).

## Commands

- Build: `cargo build` (first build compiles wxWidgets via CMake/Ninja and takes several minutes; later builds are fast)
- Test: `cargo test`
- Single test: `cargo test <name_substring>`
- Ignored tests (hit the real audiopub.site or play audio): `cargo test <name> -- --include-ignored`. Notable: `real_login_bad_credentials` (live server), `sapi_synthesizes_pcm`
- Run: `./target/debug/pubsplash.exe` (release builds hide the console via `windows_subsystem`)

Per `agents.md`: every change set must update `changelog.md` (bulleted entries under `## Unreleased` → `### Additions`/`### Fixes`/`### Changes`) and keep `README.md` accurate (especially the shortcut table).

## Architecture

Three long-lived domains connected by channels; the UI never blocks on audio or network:

1. **UI (main thread)** — `src/ui/`. wxdragon main loop. A 100 ms `Timer` ("the pump", in `ui/mod.rs::pump_events`) drains `NetEvent`/`EngineEvent` receivers into UI state; once a second it refreshes durations and chat relative times. `App` (an `Rc` holding `RefCell`s) is the shared UI-side state: persisted `Config`, transient `Runtime`, handles to the engine/net/speaker threads, and `Widgets` (populated after `ui::build`).
2. **Audio engine (thread)** — `src/audio/`. `engine_loop` mixes 10 ms blocks: per-source WASAPI capture threads push f32 into rtrb rings → per-source `ChannelStrip` (volume + 50 ms mute fades) → master strip → optional LAME MP3 encode → tokio mpsc to the Icecast sender. Sources that aren't OS-captured (TTS, sound events) are `FeedKind::External`: the engine parks an rtrb producer in `ExternalFeeds` keyed by source name, and other subsystems push samples via `ExternalFeeds::push` (retry on `Full`, abort on `Gone`).

`src/audio/health.rs` is the accounting that makes a source's behaviour visible in the log, and **the two ends of the capture path are silent by design and must stay instrumented**: `capture::push_f32` discards samples when the ring is full and `mixer::pull_block` zero-pads when it is empty, neither of which can fail loudly — capture must never block and the mixer must never stall. So both return a count instead, the capture thread keeps a `CaptureStats` of atomics the engine reads, and `engine_loop` writes one `info` line per non-idle source every `HEALTH_INTERVAL` (30 s) plus one more from `stop_sources`. The line's fields are chosen to separate the three things that produce the same symptom: **ring occupancy climbing** across a session is the engine's wall clock outrunning the device's (padding inserts samples without consuming any, so a late packet adds latency that never drains — a one-way ratchet that ends with a full ring dropping samples mid-waveform); **discontinuities/gap frames** are Windows admitting it lost the audio before we saw it, read from the `BufferInfo` that `read_from_device_to_deque` returns; and the **measured ppm** is the device clock, taken from `BufferInfo.index` against a monotonic clock rather than from the format we asked for. Idle sources are skipped because a silent loopback source otherwise writes a line a minute in every user's log forever. `DeviceTimeline` handles the two ways `index` lies: the first packet has nothing to be differenced against, and a driver that does not report a position reports zero, which naively differenced makes the whole stream one enormous gap. Note that `BufferInfo.flags.silent` means the buffer contents are *undefined*, not zero, and the crate copies them out before we see the flag — `silence_appended` overwrites them, and removing it broadcasts uninitialized memory as noise.
3. **Network (tokio on a background thread)** — `src/net/`. `net_loop` owns the `AudioPubClient` (reqwest + cookie store) and, while streaming, two tasks: the Icecast source connection (hand-rolled `PUT` over `TcpStream` in `icecast.rs`) and the SSE consumer (`sse.rs` parser over `/live/{id}/events`).

TTS (`src/tts/`) is nine engines behind one router. `speaker::Speaker` holds two `tts::queue::Queue`s — bounded and **drop-oldest**, so a chat flood loses the stale messages, not the fresh ones — and dispatches on the engine id:

- **SAPI** (`tts/sapi.rs`) keeps its own COM STA thread owning `ISpVoice`, which synthesizes each message to memory at 48 kHz stereo and feeds `ExternalFeeds`. This thread's ownership rules are load-bearing; don't move work onto it. It used to *also* speak each message through an `ISpVoice` of its own, out of the default device and around the mixer — that is gone, and `tts::sapi::synth_preview` (its own apartment, samples handed back for `audio::cue`) is what the voice-preview button uses now that there is no local voice to borrow.
- **Every other engine** (`tts/engines/*.rs`) implements `engine::SpeechEngine` — one method, text in, interleaved stereo f32 at `SAMPLE_RATE` out — and runs on the `tts-net` worker, which owns a current-thread tokio runtime (`tts/net.rs::block_on`).

`ExternalFeeds` is every engine's only output, and the samples are always fed: a `SpeakRequest` has no "local instead" flag. **The broadcaster always hears speech, and `output_to_stream` gates only the stream.** The always-hearing half is `SourceSpec::local`, set for TTS sources in `App::source_specs` — the engine taps a `local` strip into `monitor_block` whether or not the user monitors it, as one tap with the user's own (`source.monitor || implicit` in `mix_one_block`), and stands the implicit tap down when `master_monitor` is already carrying the strip, so nothing is ever heard twice. The gating half is `ui/mod.rs::tts_reaches_the_stream`, which drops both `to_master` **and the source's sends** — a bus mixes into master unconditionally, so leaving the sends put the speech back on the stream by another route.

Adding an engine is a file under `tts/engines/`, a row in `engines::ALL`, and an arm in `engines::build`. Engine ids are stable `&'static str` constants decoupled from display names, and `resolve_id` falls back to SAPI for anything unknown — nothing else in the app hard-codes an engine name. Credentials live in one global `SpeechConfig` (DPAPI-encrypted via `secret::Secret`), never per-source. **`secret::Secret` is the type for every credential, in flight as well as at rest** — the login and Icecast source passwords in `SiteConfig` are `Secret`s too, as are the ones that never touch disk (`net::ServiceProfile`, `IcecastTarget::password`, `StreamIdentity::stream_key`). That is not only about encryption: `Secret`'s hand-written `Debug` prints `Secret(set)`, so the structs carrying credentials can keep their derived `Debug` without a `{:?}` — trivially reachable via `PUBSPLASH_LOG_TRACE=1` — spilling a password into the rotating log file users are asked to share. Don't reintroduce a bare `String` credential field. Migration is free in both directions of the change: `Secret` deserializes a bare string as legacy plaintext, so older settings files load and are re-encrypted on the next save. Every API is asked for PCM or WAV at a rate we choose; only Google Translate and Star need `tts/decode.rs`, which is now a thin error-mapping wrapper over `audio::convert::decode_audio`. Failures become `TtsError`, are rate-limited to one a minute per message by `speaker::Problems`, and are logged from the pump — a modal here would fire once per chat message, which turns a wrong API key from an annoyance into an unusable app. **The chat list carries what viewers said and nothing else**: Pubsplash reports on itself in the log (`ui/mod.rs::report_speech_problems`, `api::report_balance_failures`, `mastodon_post::drain_results`, and both connection-state arms of the pump), and in the Home tab's stream state for anything the user must see. The test to apply to a new notice is whether it can fire on its own: one that arrives unbidden — and especially one that can arrive mid-broadcast — belongs in the log and the stream state, while one that answers a deliberate action the user just took is free to be a modal, since they are sitting there waiting for the answer.

`tts/usage.rs` is the API tab's data: a process-global store (`OnceLock<RwLock<..>>` + a `generation` counter the pump polls, copied from `catalog.rs`) because the two writers — the `tts-net` worker and the SAPI apartment thread — are not the UI thread. Both call `usage::record` once per utterance, on the success *and* failure paths. The model and voice recorded come from `SpeechEngine::usage_model`/`usage_voice`, two defaulted trait methods, so each engine's own resolution (defaults, per-source `provider_settings`, the legacy global fallback) stays in the engine rather than being duplicated. **Only ElevenLabs publishes a credit balance reachable with the credentials the app stores** (`elevenlabs::subscription`, `GET /v1/user/subscription`); everything else answers `unavailable`, which `usage::reports_balance` is the single source of truth for. Balances are fetched only when the user presses the button — never on a timer, since that would spend their quota unasked.

`src/update/` is automatic updates, and it is **a fourth domain, deliberately not part of `net`** — `net_loop` awaits each command arm inline, so a multi-megabyte download parked in it would stall `StopStream` and `Shutdown`, the two commands that must never wait. It owns a worker thread and a channel of its own in the shape of `tts::catalog::start_refresh`, and its `EventSender` rings `wake_up_idle` for the same reason `net::EventSender` does. There is no server: the release workflow publishes `latest.json` plus every updater-facing artifact under a **stable, version-free name as well as its versioned one** (two copies of the same file), so `releases/latest/download/<name>` resolves forever — `api.github.com` is deliberately unused, being rate-limited per IP and unable to carry a checksum. The startup check is one shot, never a timer, and is **silent unless it has a question**; `ui/update.rs` additionally stands down entirely while a stream or recording is running. `update::http::tests::parses_what_the_workflow_actually_writes` pins the exact JSON that workflow step emits — if you rewrite the step, that test is the contract. Layout detection (`install_kind.rs`) is `portable.txt` (written into the ZIP) then `uninstall.exe` (written by NSIS into `$INSTDIR`, verified by listing a built installer); anything else is `Unknown` and never updates itself in place. **Applying an update is `src/bin/updater.rs`'s job, never this process's**: Windows will not replace a running exe, so both paths stage everything, copy `pubsplash-update.exe` out to `update\runner\` in the data directory — never a file being overwritten — spawn it `DETACHED_PROCESS`, and close the frame through the normal path. The helper waits on the pid, then either runs the installer or replaces the portable folder file by file, **moving each old file aside into `.pubsplash-old\` rather than deleting it** so any failure rolls the whole thing back; files it does not ship are left alone, because people keep their own things beside a portable copy.

`src/data_dir.rs` decides where everything Pubsplash writes goes, and it is the **only** module that names a real path — `config::config_dir()` delegates to it, and every other path in the app (logs, crashes, caches, sound packs, update staging, the default recordings folder) hangs off that. Two layouts, resolved once per process from `current_exe` and cached: a copy carrying `portable.txt` keeps everything in `user_data\` beside the executable, and anything else keeps it in `%LOCALAPPDATA%\pubsplash`. `portable.txt` is the *same* marker `update::install_kind` reads, deliberately — the layout that updates itself by overwriting its own folder is the layout that stores data inside it, and `data_dir::is_portable_dir` is the one place that decides. That the portable data folder is a **subfolder** is what makes the two safe together: `updater::apply_portable` replaces the top-level files the release ships and touches nothing else, so `user_data\` — including the staged download and the running helper — survives an update (`updater::tests::leaves_subfolders_alone`). A portable copy from before this existed is migrated once by `data_dir::migrate_from_legacy`, called from `main` after logging is up so the one time it happens is in the log; it is keyed on the *settings file* rather than the folder, since `logging::init` creates the folder first, and it copies rather than moves so an installed copy on the same machine keeps working. `data_dir.rs` is `#[path]`-included into the standalone soundpack binaries (which have no `crate::config`) for the same reason `audio/convert.rs` is, so it depends on nothing but `std`, `dirs` and `log`.

`Config` (in `config.rs`) is both the persisted settings file and the scene/source model; `state.rs` adds list-manipulation methods (`switch_to`, `delete_scene`, `move_up`...) that return `ListEdit::Changed/Unchanged` so the UI knows whether to refresh/save. After any scene/source edit, call the chain in `ui/scenes.rs::after_scene_edit` (save → refresh scenes list → `on_sources_changed` → `refresh_app_processes` + `sync_engine_sources` + `rebuild_mixer` → refresh sources list, which must come last so it reads the re-resolved app cache).

Everything a user sees or hears a source called comes from `src/source_name.rs`, never from `SourceConfig.name` — that field is an identity key (it routes `ExternalFeeds` and TTS `SpeakRequest`s) and is always just the kind's display name plus a counter, since there is no rename UI. `list_labels` is the verbose Scenes-list form, `strip_labels` the concise mixer form; both dedupe within a scene. They read a `NameContext` (capture devices + resolved Application processes) built from `App::name_context`. Application sources resolve to a running process via `audio::device::resolve_apps`, cached in `Runtime.apps` and refreshed every 2 s by the pump; when a pid changes the pump re-syncs the engine, and when a name changes it calls `home::relabel_source_strips`, which re-labels strips **in place** (a `rebuild_mixer` would move focus).

## Audio formats, and the files two other binaries share

**Output is MP3 and only MP3** (LAME, `audio/encoder.rs`); a recording is the *same encoded bytes* the Icecast sender gets, never a re-encode. **Input goes through one door**, `audio::convert::decode_audio(bytes, extension_hint)`: RIFF to `hound`, Ogg Opus to the hand-rolled `decode_ogg_opus`, everything else (MP3, FLAC, Ogg Vorbis) to symphonia. The hint only steers symphonia's probe — dispatch is on magic bytes, because a speech API's `Content-Type` is often wrong and a pack file may have been renamed. `tts/decode.rs` is now nothing but the `TtsError` mapping around that call.

**Opus is libopus, not symphonia** — no released symphonia decodes Opus, 0.6 included — so `opus` + `ogg` are dependencies, and the vendored libopus needs the two `[env]` entries in `.cargo/config.toml` (CMake 4 refuses its pre-3.5 minimum; the VS generator finds no compiler under a Build Tools install) plus `[profile.dev.package.audiopus_sys]` (a debug-CRT libopus will not link against Rust's release CRT). Removing any of those three breaks `cargo build` outright. In `encode_ogg_opus`, **the input is padded by `pre_skip` frames beyond the real audio**: the decoder discards that many at the front, so without the padding the encoder never emits the tail and every sound comes back ~6.5 ms short — and only when its length is an exact multiple of the 20 ms packet, since otherwise the final short packet's zero fill covers for it. The final page's granule position is the true sample count and is what undoes the padding.

**Sound packs may hold WAV or Opus, in any mixture, and `.pspack` records no format at all** (`AssetIndex` is a kind and a byte length) — every consumer sniffs, so `FORMAT_VERSION` stayed at 1 and old packs load untouched. `SOUND_EXTENSIONS` is the list of what a pack may contain, deliberately narrower than what `decode_audio` reads. `Storage::Opus` re-encodes a WAV source on the way into a project; an already-Opus source is copied, never re-encoded twice.

`audio/convert.rs` and `audio/render.rs` are **`#[path]`-included into the standalone `soundpack` and `pubsplash-soundpack` binaries** (via `soundpack.rs`, and directly, respectively), so neither may name `crate::` or `super::` — that is why `render.rs` repeats the rate/channel constants and opens the device itself rather than calling `audio::device`. External crates are fine. `cue.rs` is the crate-facing half of playback and keeps everything that needs `soundpack::active`.

## Hard-won protocol facts (verified against the real server; do not "simplify" these away)

- The server kills any live source that isn't **MP3 or AAC** (it ffprobes the mount). MP3 via LAME is the default; the spec's mention of Opus is unimplementable.
- SvelteKit form actions (`/login`, `/live/new`) return **200 + JSON envelope** (`{"type":"redirect"|"failure",...}`) to non-browser clients, not a real 303. Parse the envelope (`audiopub.rs::action_result`), never trust the HTTP status alone.
- Chat SSE user objects contain **both `name` and `displayName`** — they must be separate serde fields (an alias makes serde reject the payload as a duplicate field, silently dropping every message).
- `__data.json` responses use SvelteKit's devalue flat-array encoding, and multiple nodes can carry a `user` object; only the stream-instructions page node has `streamKey` — skip non-matching nodes, don't bail.
- Connecting to the SSE endpoint counts as a listener; subtract 1 for display.
- **An open Icecast source connection is not an audible stream, and the gap between them is minutes wide in the bad case.** A row is created `pending`; the only route to `active` is Icecast's `mount_add` webhook into `StreamingService.sourceConnected()`, which runs *serially*: `startArchiving` (a listener connection), then `ffprobe -probesize 33000` against the live mount — 33 KB of **real-time** audio, ~2 s at our 128 kbps default, with **no timeout on the `execFileAsync`** — then a *third* `fetch` purely to compare the content-type header. Nothing serves listeners until all three pass. If the probe fails, `sourceConnected` calls `disconnectSource()`, i.e. Icecast `killsource`, which arrives here as an ordinary dropped socket and sends `AUDIO_BACKOFF` round again — and each reconnect restarts the whole validation, which is how a start becomes 30–45 s of silence. The listener page compounds it: `live/[id]/+page.svelte` does not gate `handlePlay()` on `stream.state` and passes no retry options, so `icecast-metadata-player` retries silently for its default `retryTimeout` of 30 s. The residual ~4 s offset is stock Icecast `burst-on-connect` at `burst-size` 65536, which is exactly 4 s at 128 kbps. **The server tells us all of this over SSE** — `event: state` on connect and on every transition — and `ui::ServerStream` is the axis that carries it, orthogonal to `StreamState` and `AudioLink` for the same reason those two are orthogonal. Do not filter `active` out of `read_chat_feed` again to keep the log quiet; the pump de-duplicates, and that filter is what made the UI claim a healthy broadcast from the moment the socket opened.
- **Neither end of the outgoing audio channel may deliver a backlog.** `start_streaming` sends `StartEncoding` before `StartStream`, so the engine fills the 200-slot channel throughout `create_stream` and the handshake; `spawn_icecast_sender` therefore drains before its *first* send as well as before every reconnect. Sending it would land seconds-old audio in Icecast's burst buffer, which is precisely what the first listener is handed as their starting point, so the whole broadcast would begin behind live and stay there (Icecast paces listeners by the source's delivery rate, so the debt is never repaid). Note the channel holds ~4.8 s, not the ~2 s some older comments claimed: it counts *chunks*, and `Mp3Encoder::encode` emits nothing until LAME completes a 1152-sample (24 ms) frame, so it is not 200 × the 10 ms mixer block.
- **A dropped source is not a finished stream, and this is what makes fast recovery possible.** Icecast's `mount_remove` hook makes the server set the row to `disconnected` and stamp `disconnectedAt`; `StreamingService`'s poller (`pollIntervalMs = 5 * 60 * 1000`, never overridden in `hooks.server.ts`) ends a `disconnected` stream only once that stamp is older than the cutoff — a **5-to-10 minute grace window**. `sourceConnected()` explicitly accepts a prior state of `pending` **or** `disconnected`, so reconnecting inside it resumes the *same* stream id, chat history, listener counts and archive file (archiving reopens the temp file in append mode). The stream key is a long-lived per-user UUID that never rotates, so a reconnect needs no re-login. `net::mod`'s `AUDIO_RECONNECT_BUDGET` is 4 minutes for exactly this reason: it must expire *before* the earliest moment the server could finish the row, so the sender never reconnects into a stream that no longer exists. Don't raise it past the 5-minute floor.
- Stock Icecast holds a mount for `<source-timeout>` (default 10 s) after a source dies, so a fast reconnect races **our own** dying socket and gets **`403 Mountpoint in use`**. That is the expected first answer, not a failure: `IcecastError::retryable` classifies it retryable, and `AUDIO_BACKOFF` starts at 2 s so the ladder clears a 10 s hold by the third attempt. Every **401** is terminal (bad key, banned, untrusted, or `No active stream found` — the row already expired), and the reason arrives in the `icecast-auth-message` header, which is the only place the server says *why*. The real `icecast.xml` is operator infra and is not in the audiopub-sv repo.
- **`IcecastConnection::send` must stay bounded by `WRITE_TIMEOUT`.** A bare `write_all` does not report a dropped link: it fills the kernel send buffer and pends through Windows' TCP retransmit budget — tens of seconds to ~2 minutes of a UI claiming to be live while listeners hear silence. That timeout is the whole of the app's fast outage detection. A failed `send` also *poisons* the connection, because a cancelled `write_all` may have emitted a partial MP3 frame; reusing it would splice frames and hand every listener's decoder garbage.
- The Icecast sender (`spawn_icecast_sender`) mirrors `spawn_chat_feed` and **must not drop its `audio` receiver except on the terminal path** — dropping it closes the channel, which the engine reads as `TrySendError::Closed` and answers by stopping encoding for good, so a successful reconnect would have nothing to send. It also **discards the queued backlog before each reconnect**: this is a live stream, and delivering it would push every listener permanently further behind live (Icecast paces listeners by the source's delivery rate, so the debt is never repaid). Recoverable trouble is a log line via `ui/mod.rs::audio_link_line` plus the Home tab reading "Streaming (reconnecting)"; only the give-up is a `StreamError`, and a modal is worth it there because the broadcast has actually ended. Reconnecting keeps the same stream id, so it does **not** cure the server's poisoned-SSE-listener bug — `Restored`'s wording must stay silent about chat.
- An Application source's pid must be the **root of the app's process tree**, which is what `device::choose_pid` resolves a configured name to (walking `parent()` while the name still matches). `capture::run` opens it with `PROCESS_LOOPBACK_MODE_INCLUDE_TARGET_PROCESS_TREE`, which covers the target *and its descendants* — and Chromium, Electron and Spotify all play audio from a `--type=utility` **child**, not the process owning the window. Rooting the tree anywhere but the top captures **silence with no error at all**: the loopback client opens happily and never delivers a frame. `resolve_apps` used to take the first match in `sysinfo`'s `HashMap` order, i.e. a coin flip among a browser's dozen processes, re-tossed on every 2 s poll. The chosen pid is also **pinned** (`LAST_CHOICE`) until that process exits, because `absorb_apps` reports a moved pid as `capture_changed` and `SetSources` respawns *every* capture thread.
- Desktop Audio capture defaults to **process-exclusion loopback** (`new_application_loopback_client(std::process::id(), false)`) so Pubsplash's own output (TTS, cues) can never loop into the stream. That activation takes **no endpoint id** — it is inherently all-endpoints — so pinning a Desktop Audio source to one device (`SourceKindConfig::DesktopAudio { device_id: Some(_) }`) is the *other* mechanism: a render endpoint's `IAudioClient` initialized with `Direction::Capture`, which captures everything on it, Pubsplash included. **The two can never be the same endpoint**, and `capture::would_capture_pubsplash` is the check, asked against `device::effective_output_device_id` at every open and answering "collision" when the effective device is unknown. It is asked at open time and not only in the dialog because the output device can change afterwards, and a `None` output setting follows the *system* default, which moves when a headset is plugged in. A collision falls back to the all-endpoints form: capturing more than was asked for is a smaller wrong answer than broadcasting our own audio. Endpoint loopback is also the one capture form that is **polled, not event-driven** (`capture::Pump`): `AUDCLNT_STREAMFLAGS_LOOPBACK` and `EVENTCALLBACK` do not work together, since an idle endpoint has nothing to raise the event. Being polled is what forces the other two rules on that path, and both are load-bearing: `run`'s read is a **drain loop** rather than one `read_from_device_to_deque` per turn (that call is a single `GetBuffer`/`ReleaseBuffer`, i.e. exactly one device period), and the stream asks for `POLL_BUFFER_HNS` of buffer instead of the default period. A polled reader is woken by `thread::sleep`, which is bounded below by the system timer resolution and can only ever be *late*; one packet per turn makes falling behind the thing each turn does, so the endpoint's buffer fills, WASAPI drops what will not fit, and the source crackles steadily. Draining turns a late turn into latency that is paid back on the next turn, and the buffer is the headroom that lets it be. Don't reduce either back.
- **Pubsplash plays out of one device, chosen in Preferences > Audio, and `audio/render.rs` owns that setting** — not `audio/device.rs`, because the two output paths are the mixer's monitoring tap (`audio/monitor.rs`) and local cues (`render.rs` itself), and only the former may name the crate (`render.rs` is `#[path]`-included into the standalone soundpack binaries). So `render::output_render_device` is what both open, and `device::effective_output_device_id` reaches *into* it. Like `capture_device`, it refuses a configured endpoint that is not `Active` rather than falling back to the default — a silent fallback could land the output on the very endpoint a Desktop Audio source was just cleared against. Cues pick a change up on their own (they open the device per playback); the monitor thread is told by `EngineCommand::ReopenMonitor`, which just drops `monitor_out` and lets `engine_loop`'s per-block `wanted` check respawn it.

## wxDragon notes

- Widget handles are `Copy`-like and cheap to clone into event closures; state lives in `Rc<App>` captured by clone.
- Screen readers do not announce adjacent `StaticText` labels or even some controls' own labels. Use `ui/mod.rs::set_accessible_name` (a name-only MSAA `AccessibleImpl`) on any new slider, checkbox, button, or text input — and re-set it when the meaning changes (see the send-level slider re-label in `sends.rs`). The crate's `set_accessibility_label` is macOS-only; don't use it. **Never use `set_accessible_name` on a `ListBox`** — lists go through `ui/native_acc.rs::install` instead, and that file documents why at length. The short version: wx's `WM_GETOBJECT` handler calls `GetOrCreateAccessible()`, so *every* MSW control is wrapped in a `wxWindowAccessible` whether or not we install one; it answers a list's name with `wxWindow::GetName()` (the literal `"listBox"`) and its child count with the number of child *windows* (zero), and forwards everything it declines to a **second, unrelated** object from `CreateStdAccessibleObject` whose child ids index list *items*. `native_acc` answers `WM_GETOBJECT` from a subclass proc ahead of wx and returns `DefWindowProcW`, so the list is a plain Win32 control again and takes its name from the `StaticText` in front of it — which every list must therefore keep. The subclass deliberately does nothing on `WM_SETFOCUS`: the native list already raises event-driven focus notifications for its rows, and a synthetic `EVENT_OBJECT_FOCUS` makes current NVDA announce the row twice. Don't add accessibility events or work to a list's `wxEVT_SET_FOCUS` path expecting it to be free. **Radio boxes go through `native_acc::install_radio_box`** for the same reason: a `wxRadioBox` is a group box whose items are real `wxRadioButton` *windows*, so wx wraps every one of them and answers their names with `wxWindow::GetName()` — the literal `"radioButton"` for each item and `"radioBox"` for the group — leaving the items effectively unnamed. `set_accessible_name` can't reach them (it would replace the group's accessible only, and wxdragon hands out no handles for the children), so `install_radio_box` subclasses the group's HWND and every child `BUTTON` and lets OLEACC build the native objects, which take their names from the controls' own text. `set_accessible_name` stays correct for controls with no MSAA children (sliders, checkboxes, buttons, text fields).
- **Date and time pickers keep `set_accessible_name` *and* need `ui/picker_acc.rs`**, which is a different problem from the two above and was worth measuring before guessing. A `wxDatePickerCtrl`/`wxTimePickerCtrl` on MSW is a native `SysDateTimePick32`, and left/right arrow moves a caret between its fields — month/day/year, hour/minute/second/AM-PM — announcing **nothing at all**, so a screen-reader user cannot tell what Up and Down will change. It looks like the `native_acc` disease and is not: measured against the real control, a picker's fields are **not** MSAA children (its only children are the optional none-checkbox and the drop-down button) and `accFocus` answers `CHILDID_SELF` whatever the caret is on, so there is nothing for a `WM_GETOBJECT` passthrough to reveal and no API that reports the selected field. `picker_acc` therefore tracks the caret itself from a window subclass and speaks the field through `help::announce`. Two things could make that lie and neither is assumed: **which fields exist** comes from the control's own text (a probe value whose components are all distinct is written, the text read back, and each number matched to the only field it could be) rather than from the locale pattern, because wx may have the control rendering a 24-hour clock in a 12-hour locale — three fields where the pattern says four, which makes every field announce as its neighbour; and **where the caret is** is asked of the control by `observe_caret`, which nudges the value and sees which field moved. Left/right **wrap** rather than clamping, so there is no end to pin against and observation is the only absolute reference — it runs on focus and after a mouse click (which no message reports), but never per arrow key. All of it is pinned by the `#[ignore]`d `picker_contract` tests in that file, which walk the real control field by field and are the regression test if a future Windows changes any of it.
- NVDA announces a `ListBox` with zero items as "Unknown". Every list that can empty therefore keeps a placeholder row (`"No chats"`, `"No sources"`...) — see `ui/list.rs`, and use `list::fill` to populate. That row is not data: widget indices reach a config collection only through `list::selection(list, len)`, which rejects it (several call sites would otherwise `Vec::remove` past the end). `bus_list` is the exception, with its own pinned `MASTER_ROWS` mapper. The placeholder is announced because the row exists — it must never be selected, and `list::fill` never touches the selection at all. A screen reader speaks a row as it becomes selected, so a list that reselects while refreshing talks over what the user is doing, and these lists refresh constantly (the Sources list on every arrow key in the Scenes list, and again every two seconds by the app poll). Selection moves belong only to deliberate edits. The Home tab's stream overview is a list for the same reason: it was a read-only `TextCtrl` rewritten every second to keep its duration current, and `set_value` resets the caret, so arrow keys never reached the end. `home::refresh_overview` is a hand-rolled `list::sync` because of one extra rule — `set_string` on MSW deletes and reinserts the row, and the rule it adds is this: **`set_string` on the *selected* row is announced whether or not the list has focus; on any other row it is silent.** So the once-a-second refresh never writes the selected row, whichever row it is, and nothing in the list can speak on its own. Do not "improve" this into a focus test — gating on focus instead of selection means the tick rewrites the selected row the whole time the user stands on some other control, and NVDA reads the duration out every second. Currency comes from the two events that precede a read instead: `on_selection_changed` (the row just vacated updates at once, so stepping off Duration and back reads the current time) and `on_set_focus`, the one caller that passes `Selected::Write`. That callback nesting is why nothing in `refresh` may hold a `run` borrow across a `set_string`/`set_selection`. Its rows are addressed by `OverviewRow` kind, not index, since which rows exist depends on stream/record state; `ShownStreamUi.overview` caches what is really in the widget, and skipping the Duration write deliberately leaves that cache stale so the row is retried next tick. **`ui/api.rs::refresh` is the same machinery a second time** and for the same reason — its rows change once per utterance, so a chat flood through `list::sync` would rewrite the selected row continuously — with rows addressed by `ApiRow { engine, field }` (engines join the list and reorder as they are used, so an index is not an identity) and cached in `ShownStreamUi.api`. So main-window lists refresh through `list::sync`, which relabels in place or does nothing at all when the rows already match and only reports `Synced::Rebuilt` when the selection is genuinely gone — refreshes fan out (`after_scene_edit` refreshes the Scenes list on every *source* edit, `add_plugin` refreshes the bus list), and rebuilding an unchanged list is what makes a screen reader announce it.
- **There are no `&` mnemonics anywhere in the interface** — not on buttons, checkboxes, menu items, menu bar entries, notebook tabs, or any dialog — and none may be added (see `agents.md`). Controls are reached by Tab/arrow navigation; deliberate shortcuts live in the user keybind system or as a menu accelerator after the `\t` in a menu item's label. wx still parses labels for mnemonics, so a **literal** ampersand must be doubled (`"Logging && debugging"`) or wx eats it and underlines the next letter; that escape is the only `&` a label may carry. Watch the notebook tab row's width while you are there: one tab too many and wx grows a pair of scroll arrows that hide the tabs off the end, which is what took the Preferences dialog from 560 to 700 wide.
- **ENTER and ESCAPE reach a dialog's buttons by different routes, and every hand-built dialog owes both.** Build the confirm button with `ui/mod.rs::ok_button` and a dismiss-only dialog's single button with `dismiss_button`; neither half of what they do is optional. ESCAPE works because the button carries a real `ID_CANCEL` (`ok_button`'s companion Cancel still needs `.with_id(ID_CANCEL)` at the call site). ENTER works only through the top-level window's *default item*, which is the native `DM_SETDEFID` and needs a real control id — a wxdragon builder leaves the id at `ID_ANY`, wx hands out a negative auto-id, and `set_default()` then silently does nothing, with ENTER producing no event at all. So `with_id` alone and `set_default()` alone are each useless. The confirm id is the private `ID_CONFIRM`, **not `ID_OK`**: `wxDialogBase` binds `EVT_BUTTON(wxID_OK)` to a handler that calls `EndModal`, a click propagates from button to dialog unless a handler consumes it, and wxdragon's trampoline `Skip(true)`s before every closure — so under `ID_OK` a dialog would close itself behind the confirm handlers that deliberately `return` without closing. Controls that want ENTER for themselves already keep it and need no special case: a `MultiLine` `TextCtrl` (wx reports `wxDLG_WANT_CHARS` for it), a `ProcessEnter` control that `skip(false)`s, and the keybind capture box (`keybinds::capture_key` swallows the key as a chord). Confirm handlers that validate stay bound to the button, so ENTER routes through the same check a click does. Stock wx dialogs (`TextEntryDialog`, `SingleChoiceDialog`, `MessageDialog`, `FileDialog`, `DirDialog`) handle both keys themselves — leave them alone.
- **User keybinds** (`keybind.rs` = data + catalogue, `ui/keybinds.rs` = runtime, `ui/keybinds_ui.rs` = the Preferences tab and its dialog) do not use an accelerator table — there is none in the app — but ride the one `WH_KEYBOARD_LL` hook in `ui/help.rs`, which is also the only way capture can see ESC/arrows/CTRL+letter (`IsDialogMessage` eats them in pre-processing). The arm order in `help::keyboard_hook` is load-bearing: capture, then F1, then F6, then dispatch, so the two built-ins can never be shadowed — which is why `keybinds_ui` refuses F1 and F6 as bindings. Chords are Windows VK codes, never wx key codes. The hook touches only atomics and a snapshot refreshed by `keybinds::reload`; every action runs from `keybinds::pump` on the UI thread. Non-global bindings require `foreground_is_main_frame`, which is what keeps them from firing under our own modal dialogs while the pump timer keeps ticking.
- Key events: match through `ui/mod.rs::key_of` (returns `(key_code, ctrl_down)`); call `event.skip(true)` on unhandled keys or the control goes dead.
- A **CTRL+letter** shortcut needs one thing more than `skip(false)`, or Windows plays the default error sound alongside it. CTRL+letter is a control character (CTRL+M is 0x0D), so `TranslateMessage` posts a `WM_CHAR` for it regardless of what the key-down handler did; that char re-enters `wxWindowMSW::MSWProcessMessage`, whose navigation block only inspects `WM_KEYDOWN`, and falls through to `::IsDialogMessage` on the parent panel, which treats it as an unmatched mnemonic and beeps — all during pre-processing, before dispatch, where no wx handler can intervene. The control has to claim `DLGC_WANTCHARS` from `WM_GETDLGCODE`; sliders do, in `ui/slider_uia.rs`'s subclass proc, which is what makes CTRL+M on a mixer strip quiet. Anything else needs its own. Do not reach for `wxWANTS_CHARS`/`DLGC_WANTALLKEYS`: wx reads those as implying `DLGC_WANTTAB` (`window.cpp`'s `MSWProcessMessage`), which hands TAB to the control and traps focus.
- **Every `Slider` needs `ui/slider_uia.rs`**, without exception: `slider_uia::install` for the value and an `on_key_down` driving `slider_uia::key_step` for the movement keys. `set_accessible_name` alone is not enough — it only answers MSAA, while NVDA reads UIA and gets the native trackbar's percentage-of-range, and the native trackbar's arrow and page keys run backwards (Up and Page Up move *down*). Keep the returned `SliderAnnouncer` alive for the slider's lifetime and `uninstall()` it before the window is destroyed; the provider registry is keyed by HWND. `key_step` maps Home to the maximum and End to the minimum — the inverse of both the native mapping and the usual convention, but every slider in the app agrees, which is what matters. `home.rs` and `fx_params.rs` predate `key_step` and inline their own mappings (one interleaves CTRL+M, the other steps in floats); new sliders use the helper.
- Dialog outcome constants are `ID_OK`/`ID_YES`/`ID_CANCEL`; modal dialogs need explicit `.destroy()` after `show_modal()`.
- **Do not use `wxProgressDialog`.** `ui/scan_dialog.rs` is a hand-built modeless replacement and its header records why at length; the short version is that on MSW a `wxProgressDialog` is a native task dialog on its own thread, and **with `wxPD_CAN_SKIP` it never appears at all** — the thread never reports a window, so the constructor's `while (wxEventLoop::GetActive()->Dispatch())` spins forever and never returns (verified in isolation: `can_abort` and `smooth` build, `can_skip` hangs). Even when it does appear, `Update`/`Pulse` call `YieldFor(everything except user input)`, so *every* call on one re-enters the pump timers; its range is fixed at construction; and replacing it deadlocks, because wxdragon's `Drop` is `wxWindow::Destroy()` — for a top-level window only an append to `wxPendingDelete`, processed at *idle*, which that spin loop never reaches. Two live task dialogs later, the scan hung at 0% forever, ten `RefCell` panics a second, and 20 MB of rotated log. The replacement is modeless and inert: the pump writes to it, its buttons set the worker's atomics themselves, and no nested event loop exists anywhere in it.
- **Anything that shows a window can re-enter the handler that is showing it**, which is a `RefCell` hazard and a duplicate-work hazard both. `pump_scan_events` carries an `App::scan_pumping` guard beside `App::pumping`, and holds no `app.scan` borrow across a call into wx. `preferences::begin_scan` records the scan in `app.scan` *before* the dialog exists, because its `is_some()` guard is otherwise useless: the second click on the scan button arrives while the first is still inside the constructor, and every press started another scan thread.
- The mixer is rebuilt (not patched) on any source change — `rebuild_mixer` destroys and recreates an inner panel so creation order = Tab order.
- The frame `on_close` handler vetoes via `WindowEventData::General(e).veto()` when the user declines the streaming-exit confirmation.

## Buses, sends, and VST FX (implemented)

- **Routing** lives in `config.rs`: global `BusesConfig` (buses + `master_chain`), per-source `sends: Vec<SendConfig>` (bus by *name*) and `to_master`. Bus list edits are on `Config` (in `state.rs`) because rename/delete rewrites sends. `Config::fix_up_routing` (called in `load_from`) drops dangling sends.
- **Engine graph** (`audio/mod.rs::mix_one_block`): source strip → (if `to_master`) master, plus post-fader per-send `ChannelStrip` into each bus buffer; each bus runs its `FxChain` → bus strip → master; then the master `FxChain` → master strip. Buses/sources are addressed **by index**; sends carry a `bus_index` resolved from names in `ui/mod.rs::sync_engine_sources`. Denormals are flushed (FTZ/DAZ) at the top of `engine_loop`.
- **A `SetRouting` carrying `sources` respawns every capture thread in the app** (its handler calls `stop_sources` and re-spawns from the specs), so it must never be on a live-edit path — on a slider drag it would restart the microphone mid-stream. `SetSourceRouting` (one source's `to_master` + sends, rebuilt by `rebuild_sends`, which carries each surviving send's `ChannelStrip` and so its fade state across) and `SetSendLevel` (one send's level, in place) are the targeted alternatives the Sends dialog uses. Both address the *active scene's* source indices, so `App::sync_source_routing`/`set_send_level` no-op when the edited scene is not the active one. The `tts_reaches_the_stream` gate lives in `ui/mod.rs::source_routing`, which is the one place both it and `source_specs` compute routing from.
- **Plugin hosting is in-process**, both formats. `vst/host2.rs` hosts VST2 (full `AEffect`, editor, chunks) — distinct from the scan-only `vst/vst2.rs` that compiles into `pubsplash-scan.exe`; `vst/host3.rs` hosts VST3 on the `vst3-host` crate. `vst/instance.rs` is the seam: `PluginInstance` is the format enum, and `PluginInstance::process` is the **only** place that knows which format it is holding — it returns a `Processed`, never a bool, so a chain cannot leave its output unwritten and promote the previous plugin's block. `PluginInstance` is deliberately not `Clone` and holds each plugin **by value**, not behind an inner Arc: every owner reaches a plugin through one `Arc<PluginInstance>`, so the retirement path below is the only way to the last reference.
- **Threading contract** (headers at the top of `host2.rs` and `host3.rs`, enforced by structure): `load`/`Drop`/dispatcher/editor on the **UI thread**; `process_replacing` / `process_stereo` on the **engine thread**. Parameter accessors are UI-thread-only — VST2 tolerates either thread, but VST3's `IEditController` is main-thread-only, so the intersection is what callers must obey. `Vst2Plugin` is `unsafe Send+Sync` on that basis; `Vst3Plugin` gets both from the compiler (a `Mutex<Vst3Inner>` around a `Send` plugin) with no `unsafe impl` at all.
- **Nothing blocks the engine, in either format.** `Vst3Plugin::process_stereo` and `Vst2Plugin::process_replacing` both take their mutex with `try_lock` and report `Processed::Passthrough` if they can't have it. UI operations that hold it for longer than a block (editor open/close, chunks/`save_state`, the parameter-list refresh, and `Drop`) raise the shared `vst/suspend.rs` `SuspendFlag` and sleep `SETTLE` (60 ms) *before* locking; the engine sees the flag, keeps processing, and reports `Processed::Retiring`. Short dispatches (parameter names, `effEditIdle`) take the lock without the settle — they run on a timer. VST2's `get_parameter`/`set_parameter` stay outside the lock entirely, per the format's contract and because `snapshot` reads them with the plugin already held. `FxChain` carries a per-unit wet/dry gain ramped over `FADE_SECONDS`, so a plugin leaves and rejoins the signal path as a 50 ms crossfade rather than a hard switch — which also covers bypass. Poisoning is always recovered (`unwrap_or_else(|e| e.into_inner())`): a `None` from `snapshot()` means "keep what config already has", never "write zeros".
- **Layouts.** VST3 negotiates mono or stereo in *and* out (`host3.rs::negotiate_layout`); mono output is mirrored to both channels and mono input is fed `(L+R)/2` in both formats. A plugin with **no audio input bus** — an instrument or other generator — is allowed, and `PluginInstance::mix_mode` reports `MixMode::Add` for it so `FxChain` sums its output into the bus instead of replacing it. There is no MIDI plumbing: instruments are driven from their own interface. Wider-than-stereo output is still refused, with a message naming the channel count.
- **Ownership**: `App.fx` (`FxRuntime`) holds one `Arc<PluginInstance>` per live instance, kept index-aligned with `config.buses`. The engine holds clones inside `FxChain`s. **Plugin teardown must happen on the UI thread, with nothing borrowed**, and there are two halves to that:
  - the engine hands every chain it replaces back down a retirement channel (`park_chains`, used by both the `Shutdown` arm and the idle-park exit) and sends `EngineEvent::BusesApplied`; the pump then calls `AudioEngine::reclaim_retired_chains`. Never let the engine drop the last reference — every exit from `engine_loop` parks its chains first.
  - the UI's own reference goes to `App.orphaned_plugins` via `fx::orphan`, drained by the pump through `fx::release_orphans`. Dropping one inline would run teardown *inside* an `app.fx` borrow, and plugin teardown commonly pumps the Windows message queue and re-enters the UI.
  All chain lifecycle goes through `ui/fx.rs`. Chain edits send only the chain they touched (`sync_engine_chain`), and the engine carries surviving units' fade state across a replacement by `Arc::ptr_eq` (`FxChain::adopt_fades_from`) so an unrelated plugin mid-fade is not snapped to its endpoint.
- **Plugin DLLs are never unloaded, and neither are their factories.** `vst/module_pin.rs` takes one permanent extra `LoadLibraryExW` reference per distinct plugin binary before the first instance is created, and (`pin_factory`, after the first successful load) one permanent `IPluginFactory` reference. The second matters because `vst3-host` treats the factory as a local in `PluginImpl::load` and releases it once it has the component: a **shell** module — one DLL publishing many plugins, e.g. `WaveShell1-VST3` with 232 audio classes — keeps its per-process registry on the factory, and releasing it left `IComponent::terminate()` walking a torn-down table and dereferencing `-1`. `Vst2Plugin::drop`'s `FreeLibrary` and `vst3-host`'s (which has no per-module refcount and also calls `ExitDll`) then balance their own loads against that floor and can never unmap the image — which is what a plugin with a worker thread, timer, or COM stub still running does not survive. Instance identity is `vst::instance::next_effect_id`, a counter, **not** an address: an address is reused the moment the plugin is freed, and editors and `audioMasterSizeWindow` requests are matched by it.
- **A hard crash is not a panic.** A faulting plugin kills the process with `STATUS_ACCESS_VIOLATION` — no unwind, no panic hook, no backtrace. `src/crash.rs` installs a top-level exception filter that logs the faulting address's **module and offset** (which names the plugin) and writes a minidump to `crashes\` in the data directory; `Vst2Plugin::drop`/`Vst3Plugin::drop` log breadcrumbs around each teardown step, and `logging.rs` pins the `vst3_host` target to debug so the crate's own module-cleanup lines survive. When investigating a crash, the last log line before the fault is the evidence.
- **Known `vst3-host` 0.7 gaps** (documented in `host3.rs`, not worked around): `restartComponent` is a crate stub, so the parameter list is re-read at point of use rather than on notification; `IEditController::getState` and `IPlugView::onSize` have no wrappers, so editor-side state does not persist and a self-resizing editor may be clipped.
- **Chain library** (`fx.rs`): named chains in `fx_chains.json`, `.pubfx` export/import, and `resolve_chain` (drives the missing-plugin dialog). `PluginRef::resolve` matches by unique_id/class_id, then path, then name.
- **Accessible params** (`ui/fx_params.rs`): OSARA-style dialog behind the testable `ParamSource` trait (`step_param`, `filter_params`, `formatted_value`). Announces via `set_accessible_name_value` (re-set on every change) plus a read-only value TextCtrl fallback.
- **Native editor** (`ui/fx_editor.rs`): plugin editor embedded in a wx `Frame` panel via `get_handle()` HWND; a `WH_KEYBOARD_LL` hook (installed only while editors are open) turns **F6** into "refocus the toolbar" so keyboard focus is never trapped. `editor_idle` and `audioMasterSizeWindow` are serviced from the 100 ms pump. Structural chain edits call `fx_editor::close_all` first (editor slot indices would go stale).

## Deferred by spec (don't build unless asked)

MIDI input for hosted instruments, sound-event source configuration UI, sound pack creation tool, Piper and eSpeak-NG TTS engines, AAC encoding.

---
> Source: [cha0t1cnu3tral/pubsplash](https://github.com/cha0t1cnu3tral/pubsplash) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-09-23 -->
