## kaoyan-math-ai

> 面向 Codex / AI 编码助手 / 笔记整理助手的本库工作规范。

﻿# AGENTS.md

面向 Codex / AI 编码助手 / 笔记整理助手的本库工作规范。

本库是 Obsidian 考研笔记库，包含数学、英语、PDF/PPT 原始资料、OCR 工具、模板和 Obsidian 配置。所有助手开始工作前必须先阅读本文件，并优先遵守更具体目录中的 `AGENTS.md`。

## 1. 基本原则

1. 开始工作前，先阅读当前目录及上级目录中的 `AGENTS.md`。
2. 仓库内规则优先于全局规则；更具体的规则优先于通用规则。
3. 不要覆盖用户已有修改、手写补充、批注、错题记录。
4. 不要删除、重命名、移动重要文件，除非用户明确要求。
5. 不确定、缺上下文、OCR 不清时，标记 `待确认`，不要编造。
6. 当前目录如果不是 Git 仓库，应明确说明无法创建 Git 备份提交，并改用只新增、不覆盖的安全策略。

## 2. 大改前必须确认

以下情况属于大改，执行前必须先询问用户：

1. 修改多个既有文件或大面积重排内容。
2. 重构目录结构。
3. 批量移动、重命名、删除文件。
4. 修改配置文件、插件目录、索引目录、隐藏目录。
5. 覆盖已有笔记、章节、用户补充或错题记录。
6. 运行可能影响大量文件的格式化、转换、清理脚本。

确认时至少说明：

1. 本次计划修改的范围。
2. 是否只允许修改指定文件。
3. 哪些区域需要保留不动。
4. 是否需要先新建分支。
5. 是否需要先做 Git 备份提交。

大改前默认询问：

> 这次会涉及较大范围修改。要不要我先做一个 Git 备份提交？

## 3. Git 安全规则

大改前先检查状态：

```powershell
git status
```

如果当前目录不是 Git 仓库，说明这一点，不要伪造备份提交。

如果有 Git 仓库且用户确认备份，推荐：

```powershell
git add -A
git commit -m "backup: before major changes"
```

完成整理、修复或重构后，可建议创建结果提交：

```powershell
git add -A
git commit -m "notes: update study notes"
```

未经明确确认，不要执行：

```powershell
git reset --hard
git clean -fd
git checkout -- .
git restore .
```

如需回滚，优先使用更安全的方式：

```powershell
git revert <commit>
git restore --source=<commit> -- <file>
```

## 4. Obsidian 笔记库规则

1. 不要默认修改 `.obsidian/`。
2. 不要默认修改 `.smart-env/` 或 AI 索引数据。
3. 不要删除、移动、重命名原始 PDF / PPT / 图片资料。
4. 不要覆盖 `我的补充`、`错题回流`、用户批注。
5. 新资料和旧笔记冲突时，单独列出冲突点。
6. 不确定、OCR 不清、资料缺页时，标记 `待确认`。
7. 图片放到对应章节的 `fig` 或草稿资产目录。
8. Markdown 保持 Obsidian 兼容，内部链接使用 wikilink，例如双中括号包裹的笔记名。
9. 使用 frontmatter 标记笔记类型、来源、状态和标签。

## 5. 数学 PDF 入库工作流

数学资料入库时，不使用 408 模板。当前正式标准是 `工具/模版文件夹/标准数学笔记模板.md`。后续数学章节笔记必须从 `工具/模版文件夹/标准数学笔记模板.md` 出发，先形成章节级“理解母稿 / 章节复习母稿”，再决定是否拆成基础知识、方法、题目、错题等小卡。

核心原则：

1. 先做一次资料识别与边界确认：识别 PDF 属于高数、线代、概率论的哪一章，记录来源路径、页数、页码范围、是否扫描型 PDF；原始 PDF 不移动、不删除。后续步骤复用这次结果，不要反复扫描同一 PDF 元数据。
2. 对整本教材、讲义合集、张宇基础 30 讲这类多章节扫描型 PDF，必须先按目录和页码边界拆章，再对拆出的单章 PDF 逐章 OCR；禁止先对整本 PDF 做正式 OCR。
3. OCR 输出保留在草稿目录，便于复核。若已有本次任务可用且来源正确的逐章 OCR 草稿，优先复用并抽查关键页，不要重复 OCR；整本 OCR 结果只能作为临时参考，不能作为正式笔记入口。
4. 第一产物必须是对齐 `工具/模版文件夹/标准数学笔记模板.md` 的章节级母稿，不是批量小卡。母稿要体现对本章知识的理解、归纳和复习路径，不能停留在模板骨架。
5. 正式知识内容必须逐条打磨：每个概念、公式、方法、易错点都要经过理解和总结，不能把 OCR 原文整段搬进正式笔记。成熟章节母稿必须完成“理解密度 / 知识密度打磨”：在原模板结构内补足章节主线、概念本质、适用条件、判定方式、易混边界、后续用途、做题路径、命题信号、易错反例和最小记忆清单。
6. 每讲默认至少从 OCR `_final/*-assets/` 中精选 1 张高价值图片嵌入母稿；优先使用体系图、流程图、区域图、函数图、公式结构图、关键例题图。若本讲 OCR 资产确实没有高价值图片，必须在 `待确认` 或最终反馈中说明原因和后续人工筛选任务。
7. 小卡只能从母稿中拆出：先在母稿中确认知识体系、命题信号、例题价值和错题回流，再拆 `基础知识`、`方法`、`题目` 或 `错题`。
8. 只为有复习价值、代表性或易错价值的题目生成 `题目` / `错题` 卡片；不得为了“完整”机械拆分低价值小卡，也不得编造题目。
9. 最后合并做一次质量门检查：frontmatter、Obsidian 链接、图片引用、公式渲染、Dataview 查询、命题信号表、例题索引、错题回流、`待确认` 任务、用户补充/错题回流保护。母稿质量门还必须确认：至少 1 个 `图像精读` 或明确无图原因；概念理解、做题路径、命题信号、易错点、反例边界、最小记忆清单非空且可复习；没有整段搬运 OCR 原文；例题仍只进入索引，未经 PDF 复核不写完整题解。不要在每个小步骤重复做同一批检查。

### 5.1 新增数学资料硬性入口

当用户新增数学 PDF、要求“按工作流入库”“转为笔记”“处理新资料”或类似任务时，必须先执行本入口，不得跳步：

1. 开始前先复述本次执行边界，至少说明是否只做拆章、是否 OCR、是否生成笔记、是否新增脚本、哪些目录不动。
2. 第一阶段只能做资料识别与拆章边界确认：读取 PDF 总页数、目录页、章节标题、印刷页码、PDF 页码偏移和扫描型状态。
3. 第二阶段只能做拆章。拆章 PDF 统一输出到 `电子资源/split/<资料名或学科名>/`；禁止输出到学科笔记目录，例如 `张宇-线性代数/split/`。
4. 拆章完成后必须停止并反馈拆章数量、页数覆盖、首页/末页边界验证方式和待确认项；除非用户已经在同一轮明确要求继续 OCR，否则不得自动进入 OCR。
5. OCR 只能以 `电子资源/split/<资料名或学科名>/<单章>.pdf` 为输入逐章执行；禁止把 `电子资源/source/` 下的整本 PDF 作为正式 OCR 输入。
6. 生成章节母稿只能在拆章边界和逐章 OCR 已确认后执行；可以先建章节级结构和复核任务作为暂存骨架，但不能把骨架视为完成。最终母稿必须继续做知识密度打磨；不得用脚本批量生成未经逐条理解的正式知识正文。
7. 如果用户说“只 split”“只拆章”“先别 OCR”，必须严格停在拆章阶段。
8. 如果用户没有明确允许 OCR 或生成笔记，默认只做到拆章和边界清单。
9. 如果既有规则、旧流程、旧目录和本入口冲突，以本入口为准。

`工具/模版文件夹/标准数学笔记模板.md` 标准结构：

1. `来源说明`：列出 PDF、OCR 草稿、对照草稿和本文件定位。
2. `复核边界`：说明哪些题干、选项、图像、公式必须回 PDF 核对。
3. `考点权重总览`：按 `基础`、`高频`、`易混`、`后续依赖` 标注复习动作。
4. `知识体系`：只放本章主干层级、关键对象和后续可链接入口。
5. `概念区`：定义、公式、定理、条件、变量解释和适用范围。
6. `做题动作区`：看到题目后如何判断、如何下手、如何避免误用。
7. `命题信号 -> 方法入口`：至少包含命题信号、优先想到的方法、关联笔记、易错提醒。
8. `典型例题索引表`：只列编号、考点、方法入口、是否建议拆卡、复核状态；题干、选项、答案和图像必须回 PDF 复核后才能写入正式题卡。
9. `分主题易错点`：按知识模块拆分，例如 `函数易错`、`极限易错`、`连续易错`、`拆卡 / OCR 易错`，不要只堆成一段。
10. `反例 / 边界情况`：记录结论不能套用的条件、反例和易混边界。
11. `Dataview 查询块`：汇总本章基础知识、方法卡、题目 / 错题、AI 草稿待复核。
12. `后续章节链接`：连接到导数、积分、泰勒、洛必达、零点定理、中值定理等后续入口。
13. `错题回流入口`：错题不只堆在错题目录，要回流到对应基础知识、方法卡、易错点或反例边界。
14. `最小记忆清单`：沉淀必须背熟的定义、判定、公式、方法边界和高频易错结论。
15. `待确认`：必须写成可执行复核任务，例如“回 PDF 核对例 1.15 选项和答案”“检查某图是否需要截图嵌入”，不要只写泛泛的“待确认”。

模板标准：

1. `工具/模版文件夹/标准数学笔记模板.md` 是数学章节笔记第一标准；如果其他模板或旧流程和它冲突，以 `工具/模版文件夹/标准数学笔记模板.md` 为准。
2. `工具/模版文件夹` 只作为 Obsidian 手动起稿骨架，用于快速生成 frontmatter、空表格、Dataview 查询块和基础栏目；它不是最终质量标准。
3. 以后更新 `工具/模版文件夹` 中的数学模板时，应向 `工具/模版文件夹/标准数学笔记模板.md` 的结构靠齐，而不是另起一套格式。
4. 不要把模板填满当作完成笔记。完成标准是内容经过理解、归纳、复核，并且能支持复习和做题。

建议笔记类型：

- `章节理解母稿` / `章节复习母稿`：优先级最高的章节产物，结构对齐 `工具/模版文件夹/标准数学笔记模板.md`，用于沉淀本章完整理解、复习入口和拆卡依据。
- `基础知识`：从母稿中拆出的定义、定理、公式、性质、例题、易错点。
- `方法`：从母稿中拆出的通用解题套路、识别信号、适用条件、反例。
- `思维导图`：章节入口页，通常命名为 `x-0 xxx思维导图.md`，可承接母稿中的知识体系和复习入口。
- `题目` / `错题`：题干、解法、关键一步、复盘、举一反三；未回 PDF 复核的题，不写成正式题解。
- `同类题目汇总`：用 Dataview 按标签、类型、评分动态汇总；同类题数量不足或价值不高时不强行新建。
- `补充知识`：主线之外但有价值的证明、图解或扩展。

## 6. 脚本与生成器使用规范

本库笔记工作的核心是逐条理解、整理、归纳和总结。脚本与生成器只能辅助外围工作，不能代替知识加工。

禁止用脚本或生成器批量完成以下内容：

1. 批量生成正式知识正文、概念解释、公式说明或定理总结。
2. 批量生成方法总结、命题信号、易错点、反例边界或最小记忆清单。
3. 批量生成题解、答案、例题索引结论或错题复盘。
4. 批量把 OCR 文本切块后直接写成正式笔记。
5. 批量制造看似完整但未经逐条理解和复核的 Markdown 文件。

允许脚本或生成器做以下辅助工作：

1. OCR、PDF 页数读取、目录读取、页码范围识别、文件清单统计。
2. 生成空白骨架、frontmatter 占位、空表格、Dataview 查询块。
3. 检查 frontmatter 字段、Obsidian 链接、图片引用、Dataview 字段、Markdown 标题层级。
4. 汇总 `待确认`、列出可能断链、统计未复核题目或 AI 草稿。
5. 对批量候选内容做清单，不直接变成正式知识结论。

执行边界：

1. 生成器只能建空壳和检查，不能代替理解、归纳、总结。
2. 任何影响多个文件的脚本、格式化、转换、清理或生成任务，都属于大改，执行前必须说明范围并询问是否先做 Git 备份提交。
3. 如果用户要求“批量生成笔记”，默认理解为“批量准备素材和骨架，逐章逐条人工式打磨内容”，不是一口气自动产出正式笔记。
4. 如果脚本输出和人工理解冲突，以人工复核和原 PDF 为准；不确定处标记 `待确认`。
5. 新增一次性脚本必须先说明必要性、保留期限和替代方案；没有用户明确同意，不要为了单次资料入库新增多个临时脚本。
6. 数学 PDF 入库默认只允许复用既有稳定工具。若必须新增工具，优先做一个职责单一、可复用、可回滚的工具；禁止把拆章、OCR、迁移、笔记生成、验证拆成一批临时脚本堆进 `工具/tools/`。
7. 对“只做 split / 只拆章”的任务，禁止新增 OCR、笔记生成、OCR 迁移类脚本。

## 7. OCR 与 PDF 处理规则

1. 首选已有工具 `工具/tools/paddleocr_client.py` 处理扫描型 PDF。
2. OCR token 通过 `PADDLEOCR_TOKEN` 环境变量读取，不要写入笔记或脚本。
3. 对整本多章节扫描型数学资料，正式 OCR 必须在拆章之后逐章执行；禁止先整本 OCR 再按文本切讲。
4. OCR 中间结果放在任务指定的学科草稿目录，例如 `<学科>/paddleocr/_source/<讲名>/`；不要放入 `电子资源/source/`，也不要放入错误学科目录。
5. OCR 结果只需沉淀一次；后续生成笔记时复用来源正确的逐章草稿并抽查关键页，除非发现缺页、错页、乱码或任务范围变化，不要重复 OCR。
6. OCR 不清、公式无法确认、图片缺失、页码不连续时，必须标记 `待确认`。
7. 不要把 OCR 原文机械转成笔记；应整理成可复习结构。
8. 重要公式应尽量写成 LaTeX；无法确认的符号不要猜。
9. 对有图像依赖的题目或结论，应保留截图引用或标记需要人工复核。
10. 如果误跑了整本 OCR，必须立即说明并停止把它作为正式入口；只能在用户确认后归档或删除，不得继续基于该错误产物生成正式笔记。

### 7.1 PaddleOCR 草稿瘦身规则

1. 张宇基础 30 讲的 PaddleOCR 汇总产物统一放在 `张宇基础30讲/paddleocr/_final/`。
2. 每一讲只保留一个最终 OCR Markdown，例如 `_final/01-第1讲-函数极限与连续.md`；文内用 `<!-- page: N -->` 保留页边界。
3. 不要把 `doc_0.md`、`doc_1.md` 这类分页文件作为后续双链和笔记整理入口；它们只作为迁移前中间产物。
4. 已迁移并验证 `_final` 后，可按用户要求清理旧分页 Markdown、版面检测图和无引用图片；清理前必须确认不动原始 PDF/PPT、正式笔记、用户补充和错题回流。
5. 迁移或重生成后必须检查 `_final` 文档数量、图片引用、公式边界和 manifest；不要只看脚本退出码。

### 7.2 OCR 图片过滤与引用规则

1. 默认过滤 `header`、`footer`、`layout_det_res_*`、页眉页脚、水印、二维码、公众号/微信/有机研/45550060 等推广图。
2. 默认过滤过小、重复、低信息量图片；保留复杂函数图、流程图、思维导图、`chart_box` 和对理解有价值的表格/图像。
3. 保留图片放入 `_final/<讲名>-assets/`，并尽量沿用 OCR 原文上下文位置插入，不事后猜测插入点。
4. `_final` 中图片嵌入必须使用 Obsidian 可解析的库内路径。示例路径形如 `张宇基础30讲/paddleocr/_final/01-第1讲-函数极限与连续-assets/<真实图片名>.jpg`，实际嵌入时再写成 Obsidian 图片语法。
5. 不要在 `_final` 中写只相对当前 Markdown 的短路径，例如 `01-第1讲-函数极限与连续-assets/<真实图片名>.jpg`；Obsidian 可能会按库根解析并导致图片失效。
6. 图片迁移后必须验证 Markdown 中每个嵌入引用都能从仓库根定位到真实文件。

正式章节母稿使用 OCR 图片时，还必须遵守：

1. 每讲至少精选 1 张高价值 OCR 图片进入母稿；优先选择体系图、流程图、区域图、函数图、公式结构图、关键例题图。若确实无高价值图片，必须在 `待确认` 或最终反馈中说明“无图原因”和下一步复核方式。
2. 图片必须服务理解和做题，不得为了装饰堆图；广告、水印、二维码、低信息截图、无法判断价值的碎片图不进入正式笔记。
3. 每张正式嵌入图片旁必须配原创 `图像精读` 或等价说明，回答三个问题：为什么看这张图、它连接哪些考点、做题时怎么用。
4. 正式笔记图片引用必须使用从库根开始的 Obsidian 路径，例如 `张宇-线性代数/paddleocr/_final/02-第2讲-矩阵-assets/<真实图片名>.jpg`，不得依赖当前文件夹相对路径。
5. 完成后必须检查图片引用能从库根定位到真实文件；只有 `missing_refs=0` 且坏图片检查通过，才能声明正式笔记图片引用完成。

### 7.3 Git LFS 图片校验规则

1. 本库的 `.jpg`、`.png`、`.pdf`、`.pptx` 等大文件可能由 Git LFS 管理；复制图片时不能只复制 LFS 指针文本。
2. 如果图片文件内容以 `version https://git-lfs.github.com/spec/v1` 开头，说明它不是有效图片，不能交给 Obsidian 使用。
3. 从旧 OCR 图片目录迁移到 `_final/*-assets/` 后，必须验证图片真实可读，例如检查文件头为 JPEG/PNG/WebP，或用 Pillow `Image.open(...).verify()` 批量检查。
4. 若发现 `_final` 资产是 LFS 指针，应先确认本地 `.git/lfs/objects` 或 `git lfs pull` 能取得真实对象，再按 oid 回填真实二进制图片。
5. 完成后至少确认：图片引用数、缺失引用数、坏图片数；只有 `missing_refs=0` 且 `bad_ref_images=0` 才能说图片迁移完成。

### 7.4 OCR 公式后处理规则

1. OCR 后处理应保守修复明显畸形 LaTeX，例如误写成 `\frac{整条等式}` 的单参数分式、display math 内多余 `$`、被截断的 `cases/aligned` 环境。
2. 正常 `\frac{a}{b}`、合法上下标、合法多行公式不得误改。
3. 发现无法确认的数学符号或公式含义时，标记 `待确认`，不要凭上下文补出数学结论。
4. 修改公式清洗逻辑后必须补回归测试，并抽查 `_final` 中的典型坏例是否已修复。

### 7.5 Markdown 转 PDF 规则

1. 当用户要求把本库 Markdown 笔记导出为 PDF，尤其是批量导出多讲 `*-标准笔记.md` 时，默认使用 Obsidian 插件 `.obsidian/plugins/native-single-pdf-export/`，不要另起 Python、pandoc、浏览器 HTML 或 Chrome 打印流程，除非用户明确要求不用 Obsidian 原生效果。
2. 插件 ID 是 `native-single-pdf-export`，由 `.obsidian/community-plugins.json` 启用。若刚新增或更新插件后 URI 没反应，优先重启 Obsidian 让插件加载，再继续导出。
3. 单文件导出 URI 格式：

```text
obsidian://native-single-pdf-export?file=<vault-relative-md>&output=<absolute-pdf>&open=false
```

4. `file` 必须是库内相对 Markdown 路径，建议使用 `/` 分隔，例如 `张宇基础30讲/01-第1讲-函数极限与连续/01-第1讲-函数极限与连续-标准笔记.md`。
5. `output` 必须是 `.pdf` 结尾的 Windows 绝对路径，例如 `C:\Users\11832\Desktop\数学资料\01-第1讲-函数极限与连续-标准笔记.pdf`。插件默认创建父目录并覆盖同名 PDF。
6. 批量导出时由 Codex 枚举目标 Markdown，逐个构造 URI 调用插件；插件本身只负责单文件导出，不内置批量 UI。
7. 导出后必须检查目标 PDF 数量、文件大小是否大于 0；条件允许时用 `pypdf` 读取页数，确认 PDF 可解析。

## 8. PDF 按章节拆分规则

当用户要求把整本教材 PDF 按目录拆成每章一个 PDF 时，必须把“拆分边界验证”作为核心任务，而不是只按目录页码机械切分。

数学资料拆章输出硬规则：

1. 数学资料拆章 PDF 统一放入 `电子资源/split/<资料名或学科名>/`。
2. 禁止把拆章 PDF 放入正式学科笔记目录，例如 `张宇-线性代数/split/` 或 `张宇-概率论与数理统计/split/`。
3. `电子资源/source/` 只存原始 PDF/PPT/资料，不存 OCR 草稿、不存拆章 PDF、不存章节笔记。
4. 原始 PDF 不移动、不删除、不覆盖；拆章只写新文件。
5. 若发现 split 输出到了错误目录，必须先停止后续 OCR/笔记生成，修正 split 位置并向用户说明。

推荐流程：

1. 先读取源 PDF 总页数、是否有内置书签、是否为扫描型 PDF；扫描型 PDF 不要依赖文本抽取判断章节。
2. 从目录页读取每章的印刷页码，但不要直接假设它等于 PDF 页码。
3. 必须用渲染页或抽取页面图片的方式确认“印刷页码 1”对应的真实 PDF 页码，计算偏移量，例如 `PDF页码 = 印刷页码 + 偏移量`。
4. 用该偏移量生成初始章节起止页：本章起始页到下一章起始页前一页，最后一章到书末或下一部分前一页。
5. 拆分前必须为所有章节边界生成或查看成对预览：左边是“上一章预计最后一页”，右边是“下一章预计首页”。
6. 边界验证必须同时满足两点：
   - 截取的最后一页仍属于本章，不是下一章标题页、目录页或附录首页。
   - 截取的最后一页确实是本章最后一页；源 PDF 的下一页才是下一章或下一部分首页。
7. 如果发现某一章末尾越过到下一章，优先怀疑“目录印刷页码到 PDF 页码的偏移量”算错，不要只单独修那一章。
8. 输出时优先写入新的明确目录，例如 `书名-按章修正版/`；如果旧输出 PDF 可能被阅读器占用或边界错误，不要强行覆盖。
9. 完成后重新打开每个输出 PDF，核对文件数量、每章页数、首页边界、末页边界、总页数覆盖范围，并在反馈中说明验证方式。
10. 原始 PDF 不得修改、移动或删除；如果当前目录不是 Git 仓库，应说明无法做 Git 备份提交。
11. 拆章完成后默认停止，等待用户确认是否继续逐章 OCR 或生成笔记；不得把“拆章完成”自动解释成“继续 OCR 和生成笔记”。

推荐验证清单：

```text
1. 文件数量是否等于章节数加附录数。
2. 每个输出 PDF 的首页是否为本章标题页或本章第一页。
3. 每个输出 PDF 的末页是否仍属于本章。
4. 源 PDF 中该末页的下一页是否为下一章标题页或下一部分首页。
5. 所有输出 PDF 页数之和是否等于计划覆盖的源 PDF 页数。
6. 源 PDF 的大小和修改时间是否未变化。
```

## 9. 中文路径与编码规则

1. Windows / PowerShell 下读取中文路径优先使用 `-LiteralPath`。
2. 脚本内处理中文文件名时，优先通过目录枚举找到目标文件，避免手写长中文路径导致编码替换。
3. 新建 Markdown 文件使用 UTF-8。
4. 不要批量改写旧笔记编码；如果终端显示乱码，先判断是显示层问题还是文件内容问题。
5. 文件名保持中文可读，避免使用会破坏 Obsidian 链接的特殊字符。

## 10. Markdown 与 Obsidian 格式

新建笔记建议包含：

```yaml
---
type: 基础知识
title: "笔记名"
tags:
  - 高数
  - 函数极限与连续
source: 张宇基础30讲
status: ai-draft
---
```

正文规范：

1. 标题层级清晰，不要把整章堆成一个长段落。
2. 内部链接使用 wikilink，外部链接使用 Markdown 链接；规范示例里不要把占位符写成真实双链。
3. 重要提醒使用 callout，例如 `> [!warning] 易错点`。
4. 数学公式使用 `$...$` 或 `$$...$$`。
5. 图片使用 Obsidian 嵌入语法；规范示例里不要把占位图片写成真实双链。
6. 跨目录图片引用优先使用从仓库根开始的库内路径，尤其是 `_final` OCR 资产；不要依赖 Obsidian 对当前文件夹相对路径的解析。
7. 对 AI 草稿明确标记 `status: ai-draft`，待用户审核后再合并到正式笔记。
8. 数学章节级笔记的正文结构优先对齐 `工具/模版文件夹/标准数学笔记模板.md`；普通模板栏目不足以替代章节理解母稿。

## 11. 并行工作规则

多个 Codex 会话、agent、分支或并行任务同时工作时：

1. 每个任务尽量负责不同文件或不同目录。
2. 不要让多个任务同时修改同一个章节笔记、同一段内容或同一批二进制文件。
3. 不要让多个任务同时修改 `.obsidian/`、配置文件、索引文件、锁文件。
4. 每个任务开始前先执行 `git status`；如果不是 Git 仓库，说明原因。
5. 每个任务完成后尽快提交；如果无法提交，应说明无法提交的原因。
6. 合并前检查差异、冲突和改动范围。

当用户说“新开功能”“开一个新功能”“多个进程改不同功能”或类似表达时，默认理解为使用 Git worktree 做隔离开发：

1. 先为该功能创建新的 worktree，并绑定对应的临时功能分支。
2. 在新的 worktree 中完成代码修改，不在主工作区直接切换到该功能分支。
3. 修改完成后先反馈改动范围、测试结果和待确认事项，等待用户审阅；未经用户确认，不要直接合并到 `main`。
4. 用户确认没问题后，再按顺序合并到 `main`，处理冲突并再次检查。
5. 合并完成且确认无误后，删除该 worktree，并删除对应的已合并临时分支。
6. 如果用户只说“开新功能”但没有命名功能，先根据任务内容生成清晰的 worktree / 分支名；不清楚时再询问。

推荐功能开发指令：

```text
开一个新 worktree 做 xxx，做完先给我审阅，不要直接合并。
我确认后再合并到 main，并删除该 worktree 和对应临时分支。
```

推荐并行方式：

```text
A 只整理高数第01章；
B 只整理操作系统第02章；
双方不修改对方文件，开始前都先检查状态。
```

## 12. GitHub 上传规则

用户要求上传到 GitHub 时，必须先确认上传范围：

1. 上传整个项目，还是只上传某个子目录。
2. 是否包含 `.obsidian/`、`.smart-env/`、PDF、PPT、大文件、缓存、私密配置。
3. 如果只上传某个 skill / 子项目，必须创建干净的独立仓库，不要把整个工作区一起推送。

上传前检查：

```powershell
git status --short --branch
git remote -v
git log --oneline -5
```

如果使用 GitHub CLI，优先检查：

```powershell
gh --version
gh auth status
```

如果 `gh` 不可用或安装失败，不要卡死；请用户在 GitHub 网页创建空仓库，并提供仓库 URL。

## 13. 默认反馈格式

开始前简短说明准备做什么。

完成后说明：

1. 改了哪些文件。
2. 有没有 `待确认` 内容。
3. 做了哪些测试或检查。
4. 是否建议提交。
5. 如果跳过了低价值题卡、方法卡或同类题汇总，说明跳过项和理由。
6. 如果上传到 GitHub，说明仓库地址、分支、commit hash、commit message 和实际上传范围。

---
> Source: [patrick-andstar/kaoyan-math-ai](https://github.com/patrick-andstar/kaoyan-math-ai) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-06-05 -->
