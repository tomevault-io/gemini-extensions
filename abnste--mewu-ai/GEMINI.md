## mewu-ai

> - 当前维护分支为 `master`；不要自动创建或推送以 AI 工具名命名的开发分支。

# 喵呜AI 开发约束
- 当前维护分支为 `master`；不要自动创建或推送以 AI 工具名命名的开发分支。
- 项目自有源代码正式采用 MPL-2.0。新 C#/XAML 源码保持 SPDX 标识，根 LICENSE 必须保持 Mozilla 官方协议原文；SOURCE.md、双语 README 与关于页须一致说明允许合规商用、对外分发时提供受覆盖源码并保留声明，不得恢复“商用必须另行授权”或添加强制通知/界面署名条件。第三方条款保持独立，后续发布包必须携带 LICENSE 和 SOURCE.md。依赖禁用 GPL/AGPL 的开发约束不改变标准 MPL 的条款。
- 解决方案 `.slnx` 必须显式声明 x64 平台，并为主程序、测试和 ProviderSmoke 映射 `Platform Project="x64"`；只改 csproj 不能阻止解决方案构建下传 AnyCPU。根目录 `dotnet build -c Release --no-restore -warnaserror` 必须可用。
- Windows-only，保持 C#、WPF 与现代 .NET，不迁移到其他 UI 技术。
- Capture 与 Presentation 分层；离线基础能力不得依赖 AI。
- `app.manifest` 必须保持 `PerMonitorV2` DPI 感知；核心区域统一使用虚拟桌面物理像素，正确处理多屏、负坐标和混合 DPI。
- WPF DIP 与截图物理像素之间的换算统一放在 `ScreenCoordinateService`，不要在窗口业务代码内重复计算比例。
- AI Provider 与 UI 解耦；用户明确发送前不得上传屏幕内容。
- 不提交密钥、`.env`、用户配置、日志、临时截图或录屏。
- 禁止 GPL/AGPL；优先微软官方、MIT、BSD、Apache-2.0。
- 重大修改必须构建和测试，仓库始终保持可运行、可回滚。
- 区域 MP4 使用 MIT 的 ScreenRecorderLib + Windows Media Foundation；不要改为捆绑 GPL FFmpeg。
- .NET 10 WPF `GifBitmapEncoder` 会把帧延迟写成 0；GIF 导出后必须按 GIF 块结构校正 Graphic Control Extension，并用解码测试验证播放时长。
- 产品默认使用 `NativeMethods.ExcludeFromCapture` 防捕获。用户明确开启持久化 `TeachingMode` 后，下次截图覆盖层、它创建的贴图/贴视频及原位颜色/保存对话框通过独立 `ApplyPresentationCaptureVisibility` 允许教学共享；设置和凭据窗口不得受该设置影响。模式在覆盖层创建时固定，避免录制中途切换策略；演示模式阻止自带区域录屏和长截图。Debug 的 `MEWU_QA_CAPTURE_WINDOWS=1` 仅供自动化验收，Release 必须继续编译掉该环境开关，不能让它改变任何窗口的保护。
- 全屏截图根画布必须保持 `Background="Transparent"` 才能接收空白区域命中；拖选期间鼠标必须由 `Root` 捕获和释放，不能由窗口捕获后让挂在根画布上的移动/松开事件断路。
- InkCanvas 的画笔可使用 `FitToCurve`，矩形和箭头等几何标注必须在生成 Stroke 时关闭曲线拟合，否则直角和箭头轮廓会被平滑变形。
- 默认 AI Provider 固定为 MiniMax M3：国内 OpenAI-compatible 地址 `https://api.minimaxi.com/v1`，模型 ID `MiniMax-M3`。M3 原生接受 `image_url` 与 `video_url`，图片单张最大 10 MB，URL/Base64 视频单文件最大 50 MB、整个内联请求体最大 64 MB，视频抽帧率范围 0.2–5 FPS；50 MB 原视频经 Base64 展开会超过请求体上限，必须提前提示压缩至约 47 MB 或改用 Files API 的 `mm_file://` 引用。不得自动回退或重新默认到火山方舟。
- 所有 OpenAI-compatible 内联多模态请求最多 16 个附件、序列化请求体最多 64 MiB；读取文件和 Base64 编码前必须用原始字节数与 JSON/Base64 上界提前拒绝。Provider 能力需分别公开图片和视频单体上限，不能再用一个最大值误导截图编码策略。
- 发送给 Provider 的历史上下文最多 20 条消息、24,000 个 UTF-16 字符，只保留至多一条系统消息和最近的完整“用户—助手”问答对；不得恢复无限历史、孤立回复或未完成的单边消息。
- UI 编码生成的截图附件 `byte[]` 属于敏感屏幕内容，必须明确所有权；批量编码中途失败或取消时清零已生成的全部缓冲，Provider 请求结束后也要清零附件字节，不能只清序列化请求体。
- 屏幕 AI 的产品形态不是把选区截图搬进普通窗口：应保持全屏覆盖层，冻结原桌面作为背景，把选区、发光边框、归一化批注连线和回答/输入卡放回原屏幕坐标；用户点击 AI 即代表明确发送并应自动开始分析。
- 屏幕助手唤醒阶段只显示底部对话条；拖选、移动、缩放或鼠标进入选区时对话条向下收纳，工具栏在选区上下自适应并避开对话条。AI 回答区域只能在首个回复内容到达后展开；从选区层进入回答层时必须复用最初的干净桌面帧，不能再次截图把旧覆盖控件冻结进背景。
- 屏幕助手的输入、分析状态和回答必须始终是同一个覆盖层里的同一条对话控件原地伸展，严禁关闭覆盖层后再创建独立 AI 对话窗口。覆盖层支持连续多区域框选、编号、删除和移动缩放，并把所有区域作为多张视觉输入发送；AI 批注用从 0 开始的 `regionIndex` 映射回对应区域。
- 选区工具条必须与底部对话条保持统一的浅色圆角纯图标风格；第一个按钮固定为引用当前区域，不得恢复成直接调用 AI。
- 区域引用以输入条内可点击、可移除的 `@区域N` 标签呈现；引用必须绑定区域对象而非普通字符串或可变编号，删除和重排区域时同步更新标签，发送和 AI 批注映射均以实际引用对象为准。
- UI 布局和避让算法必须保证有限次结束；坐标触及边界后不得继续使用值不再变化的无界 `while` 循环，空间不足时应选择有界的最优回退位置。
- 标注、录屏控制和录制完成预览必须留在唯一全屏覆盖层的原选区位置，严禁恢复 `DrawingWindow`、`RecordingControlWindow` 或 `RecordingPreviewWindow` 等独立弹窗。录制结果作为绑定视频对象的 `@视频N` 引用，可保存、复制、原位贴视频，并允许继续框选其他截图区域。
- OCR 文字选择必须在每个区域内使用单一文档控件以支持跨行连续选择；Esc 的取消/退出处理优先级必须高于文字控件自身按键处理，不能被只读编辑控件吞掉。
- 火山方舟 Agent Plan 的翻译请求必须显式关闭深度思考，并使用流式 Chat Completions、独立超时和 JSON 完整性提前终止条件；其兼容流可能已返回完整正文但迟迟不结束，不能依赖服务端结束标记让原位翻译永久停在“接收中”。
- 区域录屏开始后截图覆盖层必须继续显示暗化遮罩和当前区域边框，并依赖防捕获让遮罩不进入成片；录屏控制窗结束后再关闭覆盖层。
- 贴图默认按选区的原物理像素位置和大小显示，阴影放在额外透明边距内；双击取消置顶，右键菜单需使用全局浅色菜单样式。
- 截图覆盖层中的 OCR 和翻译不得关闭覆盖层或新开结果窗口。OCR 必须保持原截图视觉不变，仅建立透明文字命中层，让用户按字符拖选、Ctrl+C 或右键复制；不得再绘制可见文本框。翻译使用 OCR 像素坐标缩放到对应选区，在冻结桌面的原位置逐行显示译文。两者都必须保留选区、工具条和底部对话条。
- OCR 默认使用随软件离线交付的 PP-OCRv6 Small 多语言模型，通过 RapidOcrNet/ONNX Runtime 识别并返回字符位置；初始化或推理失败时才降级到 `Windows.Media.Ocr`，界面状态必须明确显示实际引擎。
- AI 流式回调必须绑定创建它的请求实例；请求完成、取消或被新请求替换后不得再接收迟到增量。最终思考内容以 Provider 返回值为准，成功、取消和失败都必须停止动画并自动折叠；正文为空或只有思考内容时按失败处理，不得显示“完成”、写入历史或渲染批注。
- 结构化 AI 回答优先严格解析 JSON；只有当整段回复明确是根对象或 JSON 代码块且含根 `answer` 字段时，才允许从损坏或截断的结构化响应中容错提取正文。普通正文里的 JSON 片段必须原样保留，`annotations: null`、数组内 `null` 或损坏的批注均按无批注处理。
- 当前发布形态是无 MSIX 身份的桌面 WPF EXE，语音输入使用微软 `System.Speech` 桌面 SAPI；不得改回要求 package identity 的 `Windows.Media.SpeechRecognition`，除非发布流程同时提供并验证包身份。缺少麦克风、语言识别器或音频设备时必须显示可操作的中文提示。
- 发布视频理解前必须在 Release 覆盖层走通“区域录屏 → 自动生成 `@视频N` 引用 → 原位发送 → 语义回答”闭环；核对成片时长与发送前后哈希不变，并抽查首、中、末帧确认画面无遮罩、选区框或录制控件。
- 覆盖层中文本框或 OCR 文档控件拥有焦点时，必须隔离 C/S/P/D/T/O/R 等全局快捷键；Esc 仍由窗口预览阶段优先处理。
- 隐式全屏区域不得因用户只输入文字而自动创建或发送；只有用户显式选择截图、录屏或上传附件时才发送视觉内容。无视觉附件的请求保持纯文字对话，Hermes 继续使用同一 Profile 的持续上下文；一旦产生显式选区，引用、发送和批注映射都必须彻底排除隐式全屏区域。
- 解析 SSE 时必须先消费当前事件的正文与思考增量，再处理同一事件里的 `finish_reason`；结束标志不得吞掉最后一段正文。
- SSE 只有收到 `[DONE]`、非空 `finish_reason`，或调用方的完整性谓词确认结构化正文已经完整时才算成功；连接自然 EOF 但未完成必须按截断失败，严禁把半段正文写入历史或让 Smoke 误判通过。
- MiniMax 兼容流的 `content` 与 `reasoning_details[].text` 可能返回累计全文，UI 只能转发相对上一块的新增后缀；普通 OpenAI-compatible Provider 的增量正文仍须按片段追加，不能全局套用同一种语义。
- MiniMax M3 的长多模态流偶尔会在句段边界重新开始累计块；非前缀块必须作为续段并按“已有后缀—新块前缀”的最长精确重叠去重，不能把它当整段替换，否则最终答案会丢失开头主语。
- PP-OCR 返回 0 行是合法空结果，不得因此降级 Windows OCR；只有初始化或推理异常才允许降级，并要向界面报告实际引擎。
- GIF 只在用户保存时从最终 MP4 按需抽帧生成；严禁恢复录制期间 `CopyFromScreen`、PNG 帧目录或其他重复捕获链，导出不得修改源 MP4。
- GIF 导出必须同时限制帧数、单帧尺寸和全部解码帧的总像素预算；长视频应保留覆盖完整时长的采样帧，通过动态缩小分辨率控制内存峰值。
- 冻结虚拟桌面时应从 `Bitmap.LockBits` 像素缓冲直接创建已冻结的 WPF 位图，严禁恢复整桌面 PNG 编码再解码；OCR 输入也应优先直接复制 BGRA 像素，避免无意义的图片压缩往返。
- 覆盖层防捕获启用失败，或 Debug 视觉验收开关主动绕过防捕获时，必须阻止区域录屏，不能冒险把遮罩和控件录入成片。
- 停止录屏必须有有限超时；录制器不返回完成/失败回调时，覆盖层要自动退出“处理中”并异步释放会话。截图区域成功转换为视频后必须清除并隐藏原 OCR、翻译、AI 批注和手绘层，失败时才恢复原图层。
- AI 请求取消、替换、窗口关闭或流结束后必须拒绝所有迟到增量和最终结果，写入内存/磁盘历史前也要再次校验请求身份与取消状态。
- 对话历史在取得并发写锁后必须再次检查取消状态；单条 JSONL 一旦开始落盘则必须完整写完，不能因中途取消留下半行损坏记录。
- 图片与视频混合发送时 `regionIndex` 统一按完整附件顺序映射。视频只接受带 `startTime`、`endTime` 和归一化 `keyframes` 的时间轴批注：单点事件跳转后暂停，动作区间原位播放并在相邻关键帧之间线性插值跟踪框；缺少时间轴的视频静态框必须拒绝，时间越界、乱序/重复关键帧或越界矩形只丢弃对应批注，不能影响正文和合法兄弟项。
- API Key 以及 Authorization、token、cookie、signature、subscription key、secret、password 等敏感自定义 Header 必须写入 DPAPI 凭据文件，不得进入 `settings.json`；保存失败或解密失败时不得静默丢弃既有凭据映射。
- 旧设置中的明文敏感 Header 迁移必须使用新的 DPAPI 凭据并在设置原子替换成功后才清理旧凭据；迁移或保存失败必须删除本轮新凭据并保留旧映射/旧值，共享给主 API Key 的凭据不得误删。
- Provider ID 必须非空且唯一，默认 Provider 必须指向现存项；设置文件使用无 BOM UTF-8 和同目录临时文件原子替换，失败后必须清理临时文件并保留原配置。
- Provider 工厂必须对缺失或悬空的默认 Provider、无法解密的敏感 Header、以及主 API Key 与认证 Header 并存全部 fail closed，并向界面返回可操作的重新配置提示；不得静默回退到列表首项或少发认证字段。
- `settings.json` 中每个 Provider 的 `Id`、`Type`、`BaseUrl` 与 `Model` 都是反序列化必填字段；字段缺失必须使整份旧配置安全失效且保留原文件，显式 `null`、空值、不支持类型或非法地址也必须在读取凭据和发起网络请求前 fail closed。
- 仅在 `settings.json` 不存在的全新安装中初始化默认 MiniMax；已有设置即使 JSON 可解析，也不得静默重写空白/重复 Provider ID 或悬空默认项。应保留结构错误供 Provider 工厂和设置页明确提示修复；JSON 解析失败必须安全记录且不得覆盖原文件。
- 单实例仲裁必须早于设置加载、凭据迁移和临时文件清理；非主实例只负责唤醒主实例，退出时不得删除主实例仍在使用或预览的临时媒体。命名互斥体若只用作实例存活哨兵，不得获取线程关联所有权后再从其他线程释放。
- 主实例退出时必须在仍持有单实例哨兵期间完成临时媒体清理，最后才释放哨兵；ProviderSmoke 也必须在读取/迁移真实设置前取得同一哨兵，主程序运行时直接失败，严禁并发改写设置或凭据。
- 主实例必须维护无敏感内容的原子会话诊断标记，记录最近的高层操作阶段并在干净退出时完成标记；原生访问冲突等绕过 .NET 未处理异常事件的崩溃，要在下一次主实例启动时根据遗留标记写入脱敏日志。非主实例不得创建或更新该标记，诊断记录不得包含 prompt、回答、附件路径、凭据或屏幕内容。
- ProviderSmoke 的录屏秒数、布尔值和视频语义词必须严格校验；视频语义词去除空白后至少一项，动态帧变化默认必检。首中末帧保留解码后原始像素 SHA-256 作为证据；动态判定使用 32 × 18 亮度签名，任意两帧中亮度差至少 12 的网格达到 3% 才算有效变化。非法配置不得静默退化为“未请求视频”后返回成功。
- 环境 Provider 导入选择默认项时必须验证其认证凭据确实可读，不能只看 Provider ID 唯一；全新安装的无凭据 MiniMax 占位项不得阻止仅 xAI 或仅 Volcengine 的合法导入。
- Provider 连通性验收必须校验挑战标记而非只看非空正文：文本流同时返回 `MEWU_OK`，视觉探针正确返回测试图的颜色标记，才能判定对应能力通过。
- MiniMax 视觉连通性探针使用至少 256 × 256 的纯色图；过小的 96px 近纯色图可能在服务端视觉预处理后被识别为空白，造成与附件协议无关的假失败。
- 自包含 Windows 发布包必须携带 Microsoft .NET Library License、.NET/WPF 第三方声明及所有 NuGet/模型许可证；发布审计同时排除 PDB、Linux 原生资产、设置、凭据、日志、截图、录屏和 QA 临时文件。
- 临时媒体必须通过 `TempMediaRegistry` 的规范化路径租约管理；录制器、覆盖层视频对象、贴视频窗口和异步导出各自持有独立租约，清理只能删除未租用文件。主实例退出时应在仍持有单实例哨兵期间有界等待租约释放，超时后保留仍租用文件。
- 视频文件放入剪贴板前必须先原子复制到 `%LOCALAPPDATA%\MewuAI\Clipboard` 的持久暂存目录；设置系统剪贴板失败时回滚本次副本，程序退出不得清空该目录，只能按保留天数清理过期副本。
- 本机 Hermes 多 Agent/人格使用 TUI Gateway 的 `profiles.list` 与 profile-scoped RPC；设置页只能选择现有 Profile，不得编辑其 `SOUL.md` 或改变 Hermes 全局默认项。会话、模型、思考程度、附件与 TTS 必须统一绑定所选 Profile，不同 Profile 的持续会话不得混用。
- 区域录屏开始前必须在原选区中央显示有限的 `3 → 2 → 1` 倒计时；倒计时期间顶层覆盖窗必须跨进程鼠标穿透，让用户继续操作底层窗口。倒计时结束且动画完全隐藏、窗口交互切回正常后，才能建立录屏孔洞并启动录制；取消、失败或关闭必须恢复窗口扩展样式，恢复失败则安全关闭覆盖层。
- 所有视频首轮回答都必须在同一请求所有权和附件租约内再做至多一次独立完整性核验，逐项补齐原问题涉及的不同人物、设备、画面和事件，并重新核对稳定可见帧的时间；核验结果有效批注数不得少于初稿，否则保留初稿。有效单点/区间批注既要在原视频坐标中呈现，也要在回答末尾生成本地可点击跳转链接。链接只能驱动同一覆盖层内的 seek、暂停、区间播放和跟踪框，不得打开外部 URL 或新窗口。
- 视频批注返回后必须按所有视频批注关键帧的最早时间自动 seek、暂停并显示第一处标记，不能自动播放完整区间掩盖首标记。每个独立固定事件生成一条跳转链接，每个动作区间生成一条播放并跟踪链接；同一区间的起止关键帧不得重复生成额外链接，链接总数必须有界。
- 单个视频附件允许把模型误写的非零 `regionIndex` 安全重映射到唯一视频；多附件仍必须严格按完整附件顺序。初稿有效批注必须立即落地、自动跳转并生成链接，完整性核验只能在结果不少于初稿时替换，取消或失败不得清除已显示的初稿。模型时间仅轻微超出片尾时可连同关键帧一起钳制到真实时长，明显越界仍拒绝，并记录脱敏映射统计。
- 对话条因鼠标进入截图区域而收纳后，必须忽略收纳前的旧占位矩形，直到鼠标真正离开截图区域，防止在与截图重叠时循环闪现。
- 原位标注支持统一颜色的画笔、高亮笔、矩形、椭圆、箭头、文字与实心递增序号；颜色支持 Windows 原生完整 RGB 选择器。文字使用本机系统字体、字号和荧光底色；椭圆按 Shift 约束为选区内正圆。非 Stroke 标注必须纳入截图导出、截图快照和标注撤销/重做，不得只停留在临时 UI。
- 应用图标更新必须同时替换窗口资源 PNG 与程序集 ICO；ICO 至少包含 16、20、24、32、40、48、64、128、256px。生成图中的棋盘格不能视为透明，写入前必须检查 PNG Alpha，并从构建后的 EXE 提取图标确认任务栏和托盘没有继续使用旧资源。
- 图片或视频存在 AI 批注、手写 Stroke、形状、文字或序号时，保存必须提供“带标注 / 原件 / 取消”三态选择；原件导出不得混入任何标注。带标注图片应一次性扁平化 AI 与手工层；带标注 MP4 使用 Windows Media Editing 覆盖层保留源音频并按时间轴烘焙动态 AI 框，GIF 在按需抽帧后逐帧合成。两类视频导出都必须有界、原子写入且保持源 MP4 哈希不变。
- 覆盖层中的保存确认与 RGB 选择不得调用系统 `MessageBox` 或 WinForms `ColorDialog`，应使用统一的喵呜AI浅色圆角模态窗、应用防捕获并作为覆盖层 owned window。字体选择器显示 `FontFamily.FamilyNames` 的本地化名称并保存稳定的 `Source`；标注主颜色始终控制文字前景色，荧光开关只添加独立的固定黄色底色。
- 视频回答的本地定位入口使用独立圆角气泡，不显示“视频定位”标题、下划线链接、重复动作词或完整批注描述；标签采用“6秒：圈出的文字”“1分6秒：黑色手机”等紧凑格式。每次回答刷新必须重建且只保留当前动作组，每个气泡捕获独立的选区、批注和关键帧，严禁复用旧文档动作。
- 正式版本由 `.github/workflows/release.yml` 在 `v*` 标签上构建：标签版本必须与 csproj `Version` 一致，先运行 Release 全量测试和自包含 publish 审计，再生成 Inno Setup 当前用户安装 EXE、便携 ZIP 与无 BOM `SHA256SUMS.txt`，最后创建 GitHub Release。README 的固定下载链接、双语版本号、安装脚本和 release notes 必须同步更新；发行配图只能使用 Debug 显式 QA 防捕获开关取得的无敏感界面，严禁提交真实提示词、回答、设置、凭据、日志或屏幕附件。
- 新版本号不得复用 GitHub 已存在的标签；发布还原必须使用提交的 `packages.lock.json` 锁定依赖，CI 同时编译主程序、单元测试和 ProviderSmoke，并将编译警告视为错误。第三方传递依赖的准确 LICENSE/NOTICE 必须随包发布；GitHub Actions 固定到完整提交 SHA，支持的卫星资源仅保留 `en` 与 `zh-Hans`。
- 界面语言设置持久化为 `system`、`zh-CN` 或 `en-US`，无效旧值安全回退 `system`；单实例仲裁和设置加载完成后、创建首个 WPF 窗口前应用语言。运行中切换不得留下中英混合界面，当前实现统一在重启应用后生效；Debug 的 `MEWU_QA_UI_CULTURE` 仍只用于视觉验收并优先于用户设置。
- 关于页“检查更新”仅查询 `abnste/mewu_ai` 的正式 GitHub Release，精确匹配版本化 `MewuAI-Setup-*-win-x64.exe` 并优先使用该资产的 GitHub `digest`（严格 `sha256:` + 64 位 ASCII 十六进制）。只有 digest 缺失/null 时才兼容旧 `SHA256SUMS.txt`；存在但畸形的 digest 不得回退。REST API 限流时只可回退官方 latest release 重定向及旧校验文件，两者都无法取得可信哈希时必须拒绝安装并提示稍后重试，不能接收任意 URL。下载必须有界、原子落盘且 SHA-256 校验通过后才允许启动安装器；用户确认“安装并重启”后静默当前用户安装，安装器必须自动重新启动应用。
- 并发与租约测试必须用事件明确同步后台任务启动，不能把 `Task.Delay` 或 1 秒以内的线程池调度速度当作正确性条件；GitHub runner 高负载时应仍验证锁、释放通知和有界超时的真实契约，不能靠反复重跑掩盖偶发失败。
- Windows Hermes 发现必须兼容官方 `%LOCALAPPDATA%\hermes\hermes-agent\bin\hermes.exe`、`venv\Scripts\hermes.exe` 及旧版根 `bin\hermes.exe` 布局；PATH 只允许从这些确定目录反推 `HERMES_HOME`，不得全盘搜索或执行不可信命令探测。
- AI 入口显隐必须按实际可用后端决定：Hermes 对话仅在已启用且安装可发现时显示，远程 Provider 对话和原位翻译仅在默认 Provider 与凭据可用时显示；没有可用对话后端时隐藏覆盖层对话条和引用入口，离线截图/OCR/标注/录屏仍保持可用。产品不再提供独立文字问答界面或入口。
- 原位翻译属于可导出的视觉内容；复制与贴图默认包含译文，保存时与 AI/手工标注共用“带标注 / 原件 / 取消”选择，原件不得混入译文。
- System.Speech 不得在系统语言无匹配识别器时静默选用任意其他语言；应明确提示安装对应 Windows 语音包，并在最终识别为空时利用同一轮假设/拒绝候选恢复可用文字，区分“没有麦克风声音”和“有声音但语言不匹配”。
- 智能框选只使用有界的 Windows 窗口边界枚举，不得全桌面递归扫描 UIA 树；覆盖层自身及本进程窗口必须排除。单击采用预选窗口，超过系统拖动阈值后立即切换为手工框选；缩放句柄只吸附当前拖动方向的邻近边缘。
- 矩形马赛克必须作为不可变的手工标注元素进入截图撤销/重做、快照、复制、贴图和带标注导出；像素化只读取干净截图区域，不能递归采样已渲染马赛克造成累积失真。
- 长截图使用覆盖层防捕获下的有限滚轮采集与像素重叠拼接，必须限制采集段数和总输出像素；页面到底、重叠置信度不足、取消或交互样式恢复失败时应及时停止，严禁无界滚动。结果以可旋转、可保存的贴图呈现。
- 贴图右键旋转必须基于原始位图按 90° 整数倍重建，复制和保存使用当前旋转结果并保持纵横比；双击仍固定为取消置顶，不能被拖动手势抢占。
- 托盘 `ContextMenuStrip` 的 `OnRenderMenuItemBackground` 使用菜单项局部绘图坐标，不能重复使用带父级偏移的 `Item.Bounds`；悬浮胶囊必须在菜单项内部留出有界的上下左右安全边距，确保首尾圆角不被裁切。
- 标准火山方舟模型使用 `https://ark.cn-beijing.volces.com/api/v3` 并通过受限的 `GET models` 动态获取当前账户真实可用 ID；不得把未出现在账户目录中的新模型伪装成已可用。DeepSeek V4 Pro/Flash 与 GLM 5.2 在方舟按文本模型处理；Doubao Seed 2.x、精确的 `glm-5-3-flash` 以及名称含 `vision` 的 DeepSeek V4 变体才声明多模态能力。纯文本模型可以显示文字对话条，但选择视觉附件时必须明确提示模型不支持图片/视频。
- 图片与视频的 AI 可执行标注统一使用版本化 `mewu.visual-annotations/1`：目标由完整附件 `regionIndex` 与不可变 `referenceHandle` 双重绑定，空间采用附件内 0–1 坐标，视频采用秒级时间轴关键帧；支持 callout、画笔、高亮、矩形、椭圆、箭头、文字、序号和真实像素化马赛克。解析必须逐条严格校验并有界，未知协议版本不得执行；旧扁平矩形协议仅保留读取兼容。
- Hermes 自动朗读必须播放所选 Profile 的真实 TTS Provider 音频，严禁用系统 SAPI 等机械音冒充成功；Hermes 返回 Ogg/Opus 时应在本机有界解码为 PCM/WAV 后播放，保持克隆声线不变，并清零中间音频缓冲。

---
> Source: [abnste/mewu_ai](https://github.com/abnste/mewu_ai) — distributed by [TomeVault](https://tomevault.io).
<!-- tomevault:4.0:gemini_md:2026-09-06 -->
